# PR #1252: Suspend a running Job without requeueing

**Summary**: Suspend a running Job without requeueing

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1252

**Last updated**: 2024-02-12T20:56:15Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vicentefb](https://github.com/vicentefb)
- **Branch**: `SuspendableJobWithoutRequeuing` → `main`
- **Created**: 2023-10-24T21:24:23Z
- **Updated**: 2024-02-12T20:56:15Z
- **Merged**: 2023-12-01T02:26:05Z
- **Closed**: 2023-12-01T02:26:05Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@andrewsykim](https://github.com/andrewsykim)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 10   **+353 / -2**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Some systems need the ability to "terminate" arbitrary jobs (because it's running for too long or other policies) without deleting the Job object, so it can be debugged.

#### Which issue(s) this PR fixes:

Fixes #1091 

#### Special notes for your reviewer:


#### Does this PR introduce a user-facing change?
```release-note
Support for a mechanism to suspend a running Job without requeueing
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2023-10-24T21:24:26Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: vicentefb / name: Vicente Ferrara  (8f3ef1ba0483ede3a1ac5c7bc952eb0f4ebf844e)</li></ul>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-24T21:24:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8f3ef1ba0483ede3a1ac5c7bc952eb0f4ebf844e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65693f9f91ab040008cab7c8 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-24T21:24:31Z

Welcome @vicentefb! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-24T21:24:32Z

Hi @vicentefb. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-25T11:57:52Z

/ok-to-test

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-25T13:49:03Z

@vicentefb @andrewsykim I think we should write a KEP before moving this forward since this is a slightly big change.

cc: @alculquicondor

### Comment by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-25T14:38:07Z

@tenzen-y we had some back and forth in https://github.com/kubernetes-sigs/kueue/issues/1091 about the API design. I wouldn't consider it a big change since it's only introducing a single (optional) field, but I'm fairly new to the code base and I might be missing something. 

Is there specific concern or issue you want covered in more details that would warrant a KEP? Happy to write one but I didn't feel this change was that big. Also see discussion in https://github.com/kubernetes-sigs/kueue/issues/1091 for more context

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-25T14:45:40Z

> @tenzen-y we had some back and forth in #1091 about the API design. I wouldn't consider it a big change since it's only introducing a single (optional) field, but I'm fairly new to the code base and I might be missing something.
> 
> Is there specific concern or issue you want covered in more details that would warrant a KEP? Happy to write one but I didn't feel this change was that big. Also see discussion in #1091 for more context

Let me check the discussion on that issue.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-27T20:43:48Z

> Manually modifying workloads looks slightly have risks.
>
> Actually, we used to provide all features so that users can be able to propagate via Job to workloads.

For now, this is just meant to be used by administrators, which will have access to the Workload object.

Other than that, it's important that we start working on a CLI #487, which would allow administrators to make these kind of changes safely.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-31T16:20:39Z

> > Manually modifying workloads looks slightly have risks.
> > Actually, we used to provide all features so that users can be able to propagate via Job to workloads.
> 
> For now, this is just meant to be used by administrators, which will have access to the Workload object.
> 
> Other than that, it's important that we start working on a CLI #487, which would allow administrators to make these kind of changes safely.

Ah, I see. That makes sense.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-15T19:59:55Z

The release note should be user friendly. What does the field do?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T19:36:45Z

Please use rebase instead of merge

### Comment by [@vicentefb](https://github.com/vicentefb) — 2023-11-29T01:07:55Z

Once I get a final lgtm I will squash the commits

### Comment by [@vicentefb](https://github.com/vicentefb) — 2023-11-30T22:27:44Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-30T23:36:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1252#pullrequestreview-1758389090" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1252#pullrequestreview-1758678051" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1252#" title="Author self-approved">vicentefb</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@vicentefb](https://github.com/vicentefb) — 2023-11-30T23:50:18Z

/retest

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-01T01:09:14Z

Flaky test happened: https://github.com/kubernetes-sigs/kueue/issues/1389

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-01T02:20:27Z

LGTM label has been added.  <details>Git tree hash: 8dbf7e4995e772630f663b245e3c1011a8a1195c</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-01T02:23:37Z

/hold cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-01T08:17:51Z

/release-note-edit

```release-note
Support for a mechanism to suspend a running Job without requeueing
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T20:56:13Z

/kind api-change

## Reviews

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-10-24T22:10:51Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-24T22:09:27Z
```diff
@@ -59,6 +59,9 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// Queueing that will determine if a job needs to be requeued or not after being suspended
```

  Queueing -> QueueingPolicy

- **apis/kueue/v1beta1/workload_types.go:207** by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-24T22:09:31Z
```diff
@@ -200,6 +203,9 @@ const (
 
 	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue
 	WorkloadEvicted = "Evicted"
+
+	// WorkloadHasQueueingDisabled means that
```

  complete doc string

- **pkg/controller/jobframework/reconciler.go:738** by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-24T22:10:09Z
```diff
@@ -725,6 +735,26 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 	}
 }
 
+func generateRequeueingPolicyCondition(job GenericJob, wl *kueue.Workload, queueing bool) metav1.Condition {
```

  Not needed anymore?

- **pkg/workload/workload.go:44** by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-24T22:10:48Z
```diff
@@ -40,6 +40,10 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadQuotaReserved, kueue.WorkloadEvicted, kueue.WorkloadAdmitted}
 )
 
+const (
+	QueuingPolicyIsNever = "Never"
```

  This constant should be defined in apis/kueue/v1beta1/workload_types.go instead

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-10-24T22:12:40Z

- **pkg/workload/workload.go:44** by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-24T22:12:40Z
```diff
@@ -40,6 +40,10 @@ var (
 	admissionManagedConditions = []string{kueue.WorkloadQuotaReserved, kueue.WorkloadEvicted, kueue.WorkloadAdmitted}
 )
 
+const (
+	QueuingPolicyIsNever = "Never"
```

  also consider a new type for it, see example in https://github.com/kubernetes/api/blob/master/batch/v1/types.go#L115-L138

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-10-24T22:16:16Z

- **pkg/workload/workload.go:383** by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-24T22:14:32Z
```diff
@@ -376,6 +380,7 @@ func SyncAdmittedCondition(w *kueue.Workload) bool {
 	hasReservation := HasQuotaReservation(w)
 	hasAllChecksReady := HasAllChecksReady(w)
 	isAdmitted := IsAdmitted(w)
+	queuingPolicy := IsQueueingPolicyNever(w)
```

  isQueuingPolicyNever

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-10-24T22:16:33Z

- **apis/kueue/v1beta1/workload_types.go:207** by [@vicentefb](https://github.com/vicentefb) — 2023-10-24T22:16:33Z
```diff
@@ -200,6 +203,9 @@ const (
 
 	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue
 	WorkloadEvicted = "Evicted"
+
+	// WorkloadHasQueueingDisabled means that
```

  Not needed anymore

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-10-24T22:20:17Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-24T22:20:12Z
```diff
@@ -59,6 +59,9 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// Queueing that will determine if a job needs to be requeued or not after being suspended
```

  We should enumerate the supported policies in the API docs, see https://github.com/kubernetes/api/blob/master/batch/v1/types.go#L214-L228 as one example

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-26T20:45:48Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-26T20:45:47Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  ```suggestion
  	// QueueingPolicy that will determine if a job can be queued.
  ```
  
  Then you can expand about how it can be used to kick out a job that is running.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-26T20:51:33Z

I think we are also missing a change in `pkg/controller/jobframework/reconciler.go` so that when we find a job that has:
- Workload.spec.queueingPolicy: Never
- Job.suspend: true
- Workload.status.admission not nil and Workload.status.Condition[Admitted]=true)

Then, set Workload.status.Condition[Evicted]=true

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-27T19:54:47Z

I'm wondering if we should be able to modify `queueingPolicy` via Job like `suspend`
since I'm wondering if we shouldn't provide features that must manually modify workloads.

Manually modifying workloads looks slightly have risks.

Actually, we used to provide all features so that users can be able to propagate via Job to workloads.

@alculquicondor @kerthcet @mimowo WDYT?

- **apis/kueue/v1beta1/workload_types.go:69** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-27T19:53:30Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
+	// Possible values are:
+	//
+	// - Never: indicates that a suspended job should not be queued
+	//  it will not suspend the job automatically
+	// - Always: indicates that a suspended job should be queued
+	//   if there's quota available the job should resume automatically
```

  ```suggestion
  	// - Never: indicates that a suspended job should not be queued
  	//   it will not suspend the job automatically.
  	// - Always: indicates that a suspended job should be queued
  	//   if there's quota available the job should resume automatically.
  	// +kubebuilder:validation:Enum={Never, Always}
  	// +kubebuilder:default="Always"
  ```

- **apis/kueue/v1beta1/workload_types.go:320** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-27T19:54:30Z
```diff
@@ -243,6 +252,15 @@ type WorkloadList struct {
 	Items           []Workload `json:"items"`
 }
 
+// QueueingPolicyAction specifies if a suspended job should be queued.
+type QueueingPolicyAction string
+
+const (
+	// QueueingPolicyActionNeverQueue means that we don't queue a workload
+	// after it was suspended
```

  ```suggestion
  	// after it was suspended.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-27T20:04:56Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-27T20:04:55Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  ```suggestion
  	// ReQueueingPolicy that will determine if a job needs to be requeued or not after being suspended
  ```
  
  Also, `QueuingPolicy` sounds like the queuing method can be changed regardless of whether the job is suspended or not.
  
  So, we should mention as an API name that this policy will apply to the job after jobs are admitted at least once.
  
  WDYT? @alculquicondor @kerthcet

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-10-27T20:14:11Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-27T20:14:11Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  > So, we should mention as an API name that this policy will apply to the job after jobs are admitted at least once.
  
  I think in most cases the use-case will be suspending already admitted jobs, but you can have a job that is not yet admitted (due to no available resources) and then have queueing disabled

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-10-27T20:15:40Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@andrewsykim](https://github.com/andrewsykim) — 2023-10-27T20:15:40Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  > Also, QueuingPolicy sounds like the queuing method can be changed regardless of whether the job is suspended or not.
  
  Yes, this is true as of the current implementation. If you disable for suspended job, it will be suspended forever. If you disable for a running job, it will NOT suspend the job but disable re-queueing if manually suspended

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-31T16:36:35Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-31T16:36:35Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  > but you can have a job that is not yet admitted (due to no available resources) and then have queueing disabled
  
  Currently, we can disable queuing by removing the queue name label from the job. 
  So I think that it will cause confusion for users to support the same feature in multiple ways.

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-11-02T15:31:04Z

- **apis/kueue/v1beta1/workload_types.go:318** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:17:21Z
```diff
@@ -296,6 +314,15 @@ type WorkloadList struct {
 	Items           []Workload `json:"items"`
 }
 
+// QueueingPolicyAction specifies if a suspended job should be queued.
+type QueueingPolicyAction string
```

  Just `QueueingPolicy` or `QueueingPolicyType` is fine since this is not an action

- **apis/kueue/v1beta1/workload_types.go:323** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:17:34Z
```diff
@@ -296,6 +314,15 @@ type WorkloadList struct {
 	Items           []Workload `json:"items"`
 }
 
+// QueueingPolicyAction specifies if a suspended job should be queued.
+type QueueingPolicyAction string
+
+const (
+	// QueueingPolicyActionNeverQueue means that we don't queue a workload
+	// after it was suspended
+	QueueingPolicyActionNeverQueue QueueingPolicyAction = "Never"
```

  QueueingPolicyTypeNever

- **test/integration/controller/jobs/job/job_controller_test.go:1129** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:19:07Z
```diff
@@ -1086,6 +1086,67 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("creating workload")
+		wl := &kueue.Workload{}
+		wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(devJob.Name), Namespace: devJob.Namespace}
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching QueueingPolicy from Always to Never")
+		wl.Spec.QueueingPolicy = "Never"
+		gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching suspend to true")
+		gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).Should(gomega.Succeed())
+		createdDevJob.Spec.Suspend = ptr.To(true)
+		gomega.Expect(k8sClient.Update(ctx, createdDevJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking it stays suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+		// Workload should stay pending and be evicted
+		util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
```

  Test is failing here, try wrapping it in a `gomega.Eventually`? It might be flaky since the controller didn't get a chance to update the status yet

- **apis/kueue/v1beta1/workload_types.go:287** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:20:28Z
```diff
@@ -267,6 +281,10 @@ const (
 	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
 	// beacuse at least one admission check transitioned to False.
 	WorkloadEvictedByAdmissionCheck = "AdmissionCheck"
+
+	// WorkloadEvictedByQueingPolicy indicates that the workload was evicted
+	// because QueingPolicy was set to Never.
+	WorkloadEvictedByQueingPolicy = "QueingPolicyDisabled"
```

  WorkloadEvictedByQueueingPolicy

- **apis/kueue/v1beta1/workload_types.go:285** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:20:31Z
```diff
@@ -267,6 +281,10 @@ const (
 	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
 	// beacuse at least one admission check transitioned to False.
 	WorkloadEvictedByAdmissionCheck = "AdmissionCheck"
+
+	// WorkloadEvictedByQueingPolicy indicates that the workload was evicted
```

  WorkloadEvictedByQueueingPolicy

- **apis/kueue/v1beta1/workload_types.go:287** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:20:43Z
```diff
@@ -267,6 +281,10 @@ const (
 	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
 	// beacuse at least one admission check transitioned to False.
 	WorkloadEvictedByAdmissionCheck = "AdmissionCheck"
+
+	// WorkloadEvictedByQueingPolicy indicates that the workload was evicted
+	// because QueingPolicy was set to Never.
+	WorkloadEvictedByQueingPolicy = "QueingPolicyDisabled"
```

  QueueingPolicyNever?

- **apis/kueue/v1beta1/workload_types.go:63** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:27:15Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  ```
  // QueueingPolicy determines if a workload can be admitted into a queue.
  // Changing the QueueingPolicy to Never does not evict or suspend workloads that are already running.
  // Possible values are:
  //
  //   - Never: indicates that a workload should never be admitted  
  //   - Always: indicates that a workload can be evaluated for admission into it's respective queue.
  //
  // Defaults to Always
  // +kubebuilder:validation:Enum={Never, Always}
  // +kubebuilder:default="Always"
  ```

- **apis/kueue/v1beta1/workload_types.go:323** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:29:39Z
```diff
@@ -296,6 +314,15 @@ type WorkloadList struct {
 	Items           []Workload `json:"items"`
 }
 
+// QueueingPolicyAction specifies if a suspended job should be queued.
+type QueueingPolicyAction string
+
+const (
+	// QueueingPolicyActionNeverQueue means that we don't queue a workload
+	// after it was suspended
+	QueueingPolicyActionNeverQueue QueueingPolicyAction = "Never"
```

  Add a QueueingPolicyTypeAlways for consistency? You might get lint errors if it's not used though

- **pkg/controller/jobframework/reconciler.go:316** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:30:06Z
```diff
@@ -312,7 +312,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if job.IsSuspended() {
 		// start the job if the workload has been admitted, and the job is still suspended
 		if workload.IsAdmitted(wl) {
+			if workload.IsQueueingPolicyNever(wl) {
+				workload.UnsetQuotaReservationWithCondition(wl, "Pending", "QueueingDisabled")
```

  The last parameter is a message, so it can be something like `QueueingPolicy is Never`

- **pkg/workload/admissionchecks.go:40** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:30:27Z
```diff
@@ -33,9 +33,11 @@ func SyncAdmittedCondition(w *kueue.Workload) bool {
 	hasReservation := HasQuotaReservation(w)
 	hasAllChecksReady := HasAllChecksReady(w)
 	isAdmitted := IsAdmitted(w)
+	queuingPolicy := IsQueueingPolicyNever(w)
 
 	if isAdmitted == (hasReservation && hasAllChecksReady) {
 		return false
+
```

  remove extra line?

- **pkg/workload/admissionchecks.go:52** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:30:46Z
```diff
@@ -44,6 +46,10 @@ func SyncAdmittedCondition(w *kueue.Workload) bool {
 		Message: "The workload is admitted",
 	}
 	switch {
+	case !hasReservation && queuingPolicy:
+		newCondition.Status = metav1.ConditionFalse
+		newCondition.Reason = "QueuingDisabled"
+		newCondition.Message = "The workload will not be queued"
```

  "QueuingPolicy is Never"

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-11-02T15:37:44Z

- **test/integration/controller/jobs/job/job_controller_test.go:1129** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T15:37:44Z
```diff
@@ -1086,6 +1086,67 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("creating workload")
+		wl := &kueue.Workload{}
+		wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(devJob.Name), Namespace: devJob.Namespace}
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching QueueingPolicy from Always to Never")
+		wl.Spec.QueueingPolicy = "Never"
+		gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching suspend to true")
+		gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).Should(gomega.Succeed())
+		createdDevJob.Spec.Suspend = ptr.To(true)
+		gomega.Expect(k8sClient.Update(ctx, createdDevJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking it stays suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+		// Workload should stay pending and be evicted
+		util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
```

  Ah, `ExpectWorkloadsToBePending` calls Eventually so maybe a bug somewhere?

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-11-02T16:27:26Z

- **pkg/controller/jobframework/reconciler.go:317** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T16:20:43Z
```diff
@@ -312,7 +312,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if job.IsSuspended() {
 		// start the job if the workload has been admitted, and the job is still suspended
 		if workload.IsAdmitted(wl) {
+			if workload.IsQueueingPolicyNever(wl) {
+				workload.UnsetQuotaReservationWithCondition(wl, "Pending", "QueueingDisabled")
+				workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByQueingPolicy, "QueingPolicy is set to Never")
```

  QueueingPolicy

- **pkg/controller/jobframework/reconciler.go:321** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T16:21:10Z
```diff
@@ -312,7 +312,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if job.IsSuspended() {
 		// start the job if the workload has been admitted, and the job is still suspended
 		if workload.IsAdmitted(wl) {
+			if workload.IsQueueingPolicyNever(wl) {
+				workload.UnsetQuotaReservationWithCondition(wl, "Pending", "QueueingDisabled")
+				workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByQueingPolicy, "QueingPolicy is set to Never")
+				_ = workload.SyncAdmittedCondition(wl)
+				err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+				if err != nil {
+					return ctrl.Result{}, fmt.Errorf("clearing admission: %w", err)
```

  fmt.Errorf("applying admission status: %w", err)

- **test/integration/controller/jobs/job/job_controller_test.go:1107** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T16:22:47Z
```diff
@@ -1086,6 +1086,67 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("creating workload")
```

  ```
  ginkgo.By("checking workload")
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:1150** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T16:24:15Z
```diff
@@ -1086,6 +1086,67 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("creating workload")
+		wl := &kueue.Workload{}
+		wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(devJob.Name), Namespace: devJob.Namespace}
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching QueueingPolicy from Always to Never")
+		wl.Spec.QueueingPolicy = "Never"
+		gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching suspend to true")
+		gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).Should(gomega.Succeed())
+		createdDevJob.Spec.Suspend = ptr.To(true)
+		gomega.Expect(k8sClient.Update(ctx, createdDevJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking it stays suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+		// Workload should stay pending and be evicted
+		util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl); err != nil {
+				return false
+			}
+			return workload.HasQuotaReservation(wl)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+		util.FinishEvictionForWorkloads(ctx, k8sClient, wl)
+
+		ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+			wl.Spec.QueueingPolicy = "Always"
+			gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
```

  shouldn't this be `return !createdDevJob.Spec.Suspend`?

- **test/integration/controller/jobs/job/job_controller_test.go:1144** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T16:24:58Z
```diff
@@ -1086,6 +1086,67 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("creating workload")
+		wl := &kueue.Workload{}
+		wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(devJob.Name), Namespace: devJob.Namespace}
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching QueueingPolicy from Always to Never")
+		wl.Spec.QueueingPolicy = "Never"
+		gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching suspend to true")
+		gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).Should(gomega.Succeed())
+		createdDevJob.Spec.Suspend = ptr.To(true)
+		gomega.Expect(k8sClient.Update(ctx, createdDevJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking it stays suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+		// Workload should stay pending and be evicted
+		util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl); err != nil {
+				return false
+			}
+			return workload.HasQuotaReservation(wl)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+		util.FinishEvictionForWorkloads(ctx, k8sClient, wl)
+
+		ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+			wl.Spec.QueueingPolicy = "Always"
```

  I would separate updating QueueingPolicy to Always inside a separate gomega check

- **test/e2e/e2e_test.go:332** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T16:25:47Z
```diff
@@ -259,6 +259,118 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueuingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueuingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = "Never"
```

  nit: use the constants for `Never`

- **test/e2e/e2e_test.go:366** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T16:26:20Z
```diff
@@ -259,6 +259,118 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueuingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueuingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = "Never"
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Workload should get unadmitted
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
+				if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wll)
+
+			ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).
+					Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				wll.Spec.QueueingPolicy = "Always"
```

  Use constants for Always

- **test/e2e/e2e_test.go:373** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T16:26:43Z
```diff
@@ -259,6 +259,118 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueuingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueuingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = "Never"
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Workload should get unadmitted
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
+				if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wll)
+
+			ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).
+					Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				wll.Spec.QueueingPolicy = "Always"
+				gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
```

  `return !createdJob.Spec.Suspend`?

- **test/e2e/e2e_test.go:366** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-02T16:27:01Z
```diff
@@ -259,6 +259,118 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueuingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueuingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = "Never"
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Workload should get unadmitted
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
+				if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wll)
+
+			ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).
+					Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				wll.Spec.QueueingPolicy = "Always"
+				gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
```

  same comment as integration tests, put this update in a separate block

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-02T16:45:45Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-02T16:45:45Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  I found the `QueueingPolicy=Never` might be useful on the workflow use case: https://github.com/kubernetes-sigs/kueue/issues/1091#issuecomment-1789324597.
  
  However, if we introduce job readiness gates (https://github.com/kubernetes/kubernetes/issues/121681), we have similar features on both the kueue side and the job-controller side.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-02T16:56:24Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-02T16:56:23Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  NVM
  
  Maybe `QueueingPolicy=Never` and Job readiness gates can co-exist.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-02T17:07:12Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-02T17:07:12Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  I think so, we still need a representation in the Workload API.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-02T17:12:33Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-02T17:12:33Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  Yes, I imagined the following steps:
  
  1. The external controller adds a gate to the job.
  2. jobframework reconciler creates workload with `QueueingPolicy=Never`.
  3. The external controller deletes a gate to the job.
  4. jobframework reconciler updates workload with `QueueingPolicy=ALways`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-02T17:13:29Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-02T17:13:29Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  just throwing another option:
  
  `queueingPolicy: Hold | Admit`, with potential extension to `AdmitOnce` if we ever want to implement it.
  
  Which one do you think is more understandable at first glance?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-02T17:13:58Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-02T17:13:58Z
```diff
@@ -59,6 +59,15 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job needs to be requeued or not after being suspended
```

  So, I agree with the `QueueingPolicy={Always, Never}` API instead of my suggestion, `ReQueuingPolicy={Never}`.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-02T19:32:40Z

- **test/e2e/e2e_test.go:373** by [@vicentefb](https://github.com/vicentefb) — 2023-11-02T19:32:40Z
```diff
@@ -259,6 +259,118 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueuingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueuingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = "Never"
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Workload should get unadmitted
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
+				if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wll)
+
+			ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).
+					Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				wll.Spec.QueueingPolicy = "Always"
+				gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
```

  In the goMega below (line 373) it's checking that the Suspend value is false

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-02T22:37:20Z

- **test/integration/controller/jobs/job/job_controller_test.go:1150** by [@vicentefb](https://github.com/vicentefb) — 2023-11-02T22:37:19Z
```diff
@@ -1086,6 +1086,67 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("creating workload")
+		wl := &kueue.Workload{}
+		wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(devJob.Name), Namespace: devJob.Namespace}
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching QueueingPolicy from Always to Never")
+		wl.Spec.QueueingPolicy = "Never"
+		gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching suspend to true")
+		gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).Should(gomega.Succeed())
+		createdDevJob.Spec.Suspend = ptr.To(true)
+		gomega.Expect(k8sClient.Update(ctx, createdDevJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking it stays suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+		// Workload should stay pending and be evicted
+		util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl); err != nil {
+				return false
+			}
+			return workload.HasQuotaReservation(wl)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+		util.FinishEvictionForWorkloads(ctx, k8sClient, wl)
+
+		ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+			wl.Spec.QueueingPolicy = "Always"
+			gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
```

  In the goMega below (line 1151) it's checking that the Suspend value is false

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-11-03T14:32:40Z

- **test/e2e/e2e_test.go:373** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-03T14:32:24Z
```diff
@@ -259,6 +259,118 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueuingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueuingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = "Never"
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Workload should get unadmitted
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
+				if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wll)
+
+			ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).
+					Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				wll.Spec.QueueingPolicy = "Always"
+				gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
```

  Oh I see it now, thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-03T14:44:47Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-03T14:44:47Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  @tenzen-y wdyt?

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-11-03T16:51:40Z

- **apis/kueue/v1beta1/workload_types.go:73** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-03T16:51:24Z
```diff
@@ -59,6 +59,18 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy determines if a workload can be admitted into a queue.
+	// Changing the QueueingPolicy to Never does not evict or suspend workloads that are already running.
+	// Possible values are:
+	//
+	//   - Never: indicates that a workload should never be admitted
+	//   - Always: indicates that a workload can be evaluated for admission into it's respective queue.
+	//
+	// Defaults to Always
+	// +kubebuilder:validation:Enum={Never, Always}
+	// +kubebuilder:default="Always"
+	QueueingPolicy string `json:"queueingPolicy,omitempty"`
```

  ```
  QueueingPolicy QueueingPolicyType `json:"queueingPolicy,omitempty"`
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-03T17:35:18Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-03T17:35:17Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  `Admit` sounds good. However, I think `Hold` seems to have "keep" semantics. So if selecting `Admit`, `Suspend` might be better since `Suspend` has "temporary" semantics.
  
  Hold: https://www.oxfordlearnersdictionaries.com/definition/english/hold_1
  Suspend: https://www.oxfordlearnersdictionaries.com/definition/english/suspend?q=suspend
  
  Also, I think we can define {Never, Always, Once}, as an alternative.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-03T18:05:42Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-03T18:05:42Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  The way I see it, we are holding the workload from being queued. It's not temporary while the configuration is set.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-03T18:18:45Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-03T18:18:44Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  I see. Let me summarize "Suspend" and "Hold" semantics.
  
  Suspend: The job is enqueued and temporarily stopped until dequeued.
  Hold: The job isn't enqueued and keeps stopped until enqueued.
  
  If the above my understanding is correct, queuingPolicy={Hold, Admit, AdmitOnce} looks good.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-03T22:36:49Z

- **test/integration/controller/jobs/job/job_controller_test.go:1129** by [@vicentefb](https://github.com/vicentefb) — 2023-11-03T22:36:49Z
```diff
@@ -1086,6 +1086,67 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("creating workload")
+		wl := &kueue.Workload{}
+		wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(devJob.Name), Namespace: devJob.Namespace}
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching QueueingPolicy from Always to Never")
+		wl.Spec.QueueingPolicy = "Never"
+		gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching suspend to true")
+		gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).Should(gomega.Succeed())
+		createdDevJob.Spec.Suspend = ptr.To(true)
+		gomega.Expect(k8sClient.Update(ctx, createdDevJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking it stays suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+		// Workload should stay pending and be evicted
+		util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
```

  For context, synced offline, the PR was missing the config/components/crd/bases/kueue.x-k8s.io_workloads.yaml changes

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-11-14T13:13:00Z

- **pkg/controller/jobframework/reconciler.go:324** by [@trasc](https://github.com/trasc) — 2023-11-14T13:12:38Z
```diff
@@ -312,7 +312,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if job.IsSuspended() {
 		// start the job if the workload has been admitted, and the job is still suspended
 		if workload.IsAdmitted(wl) {
+			if workload.IsQueueingPolicyNever(wl) {
+				workload.UnsetQuotaReservationWithCondition(wl, "Pending", "QueueingPolicy is Never")
+				_ = workload.SyncAdmittedCondition(wl)
+				workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByQueueingPolicy, "QueueingPolicy is set to Never")
+				err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+				if err != nil {
+					return ctrl.Result{}, fmt.Errorf("applying admission status: %w", err)
+				}
+				return ctrl.Result{}, err
+			}
```

  ```suggestion
  			if workload.IsQueueingPolicyNever(wl) {
  				workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByQueueingPolicy, "QueueingPolicy is set to Never")
  				err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
  				if err != nil {
  					return ctrl.Result{}, fmt.Errorf("applying admission status: %w", err)
  				}
  				return ctrl.Result{}, err
  			}
  ```
  
  Setting the eviction should be sufficient, the reservation will be dropped when the job is observed as inactive, and no linger uses quota.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-15T18:19:13Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-15T18:19:13Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  ping @alculquicondor :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-16T18:49:36Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T18:49:36Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  I still like `Hold` and `Admit` better.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-16T19:21:24Z

- **apis/kueue/v1beta1/workload_types.go:285** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T18:50:42Z
```diff
@@ -267,6 +279,10 @@ const (
 	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
 	// beacuse at least one admission check transitioned to False.
 	WorkloadEvictedByAdmissionCheck = "AdmissionCheck"
+
+	// WorkloadEvictedByQueueingPolicy indicates that the workload was evicted
+	// because QueueingPolicy was set to Never.
+	WorkloadEvictedByQueueingPolicy = "QueueingPolicyNever"
```

  how so there is this constant? Given that we don't actually evict when the policy is set to never.

- **pkg/controller/jobframework/reconciler.go:324** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T18:54:08Z
```diff
@@ -312,7 +312,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if job.IsSuspended() {
 		// start the job if the workload has been admitted, and the job is still suspended
 		if workload.IsAdmitted(wl) {
+			if workload.IsQueueingPolicyNever(wl) {
+				workload.UnsetQuotaReservationWithCondition(wl, "Pending", "QueueingPolicy is Never")
+				_ = workload.SyncAdmittedCondition(wl)
+				workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByQueueingPolicy, "QueueingPolicy is set to Never")
+				err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+				if err != nil {
+					return ctrl.Result{}, fmt.Errorf("applying admission status: %w", err)
+				}
+				return ctrl.Result{}, err
+			}
```

  We should NOT set an Evicted condition. This Workload is not being evicted, it's the user who suspended it.
  
  What we should do is unset `.status.admission` and set the `Admitted` condition to false.
  
  The logic should be similar to step `// 6. handle eviction` (starting in `UnsetQuotaReservationWithCondition`). It probably can be made a function that we can reuse here.

- **pkg/scheduler/scheduler.go:295** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:03:50Z
```diff
@@ -291,6 +291,11 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		cq := snap.ClusterQueues[w.ClusterQueue]
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
+		// the workload cannot be nominated for admission if queuingPolicy is set to Never
+		if workload.IsQueueingPolicyNever(w.Obj) {
```

  I don't think this is the right place to skip the element.
  
  If this a queue with StrictFIFO, the workload will go back to the head of the queue, and it will block any other workload behind it.
  
  Instead, the workload needs to go outside of the queue, probably by not being in the queue entirely.
  I think you could handle this in the event handler in `pkg/controller/core` (methods `WorkloadReconciler.Update` and `WorkloadReconciler.Create`).

- **test/e2e/e2e_test.go:303** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:07:37Z
```diff
@@ -259,6 +259,123 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueueingPolicy is changed to Never and suspend to true", func() {
```

  ```suggestion
  		ginkgo.It("Should not readmit a job after QueueingPolicy is changed to Never and suspend to true", func() {
  ```

- **test/e2e/e2e_test.go:328** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:09:08Z
```diff
@@ -259,6 +259,123 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueueingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  This can be replaced by `util.ExpectWorkloadsToBeAdmitted`.

- **test/e2e/e2e_test.go:346** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:12:34Z
```diff
@@ -259,6 +259,123 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueueingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueueingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = kueue.QueueingPolicyTypeNever
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
```

  Not a very useful check.
  
  Better to add a check below where you use `Consistently` to also check that the job is suspended as well.
  
  See this example on how we check multiple things within one `Eventually` https://github.com/kubernetes-sigs/kueue/blob/337d7c9dfe16fb358bc032f20564a421c8aa3792/test/e2e/e2e_test.go#L239

- **test/e2e/e2e_test.go:360** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:16:44Z
```diff
@@ -259,6 +259,123 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueueingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueueingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = kueue.QueueingPolicyTypeNever
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Workload should get unadmitted
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
+				if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wll)
```

  Since this is an E2E test, the job controller should actually do this. If you need to force it, perhaps there's a bug somewhere.

- **test/e2e/e2e_test.go:363** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:17:51Z
```diff
@@ -259,6 +259,123 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueueingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueueingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = kueue.QueueingPolicyTypeNever
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Workload should get unadmitted
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
+				if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wll)
+
+			ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+			gomega.Eventually(func() kueue.QueueingPolicyType {
```

  ```suggestion
  			gomega.Eventually(func() error {
  ```

- **test/e2e/e2e_test.go:366** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:18:10Z
```diff
@@ -259,6 +259,123 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueueingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueueingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = kueue.QueueingPolicyTypeNever
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Workload should get unadmitted
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
+				if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wll)
+
+			ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
+			gomega.Eventually(func() kueue.QueueingPolicyType {
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				wll.Spec.QueueingPolicy = kueue.QueueingPolicyTypeAlways
+				gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
```

  ```suggestion
  				return k8sClient.Update(ctx, wll)).Should(gomega.Succeed()
  ```
  
  and check that it succeeds

- **test/integration/controller/jobs/job/job_controller_test.go:1137** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:19:36Z
```diff
@@ -1086,6 +1086,71 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("checking workload")
+		wl := &kueue.Workload{}
+		wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(devJob.Name), Namespace: devJob.Namespace}
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching QueueingPolicy from Always to Never")
+		wl.Spec.QueueingPolicy = kueue.QueueingPolicyTypeNever
+		gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching suspend to true")
+		gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).Should(gomega.Succeed())
+		createdDevJob.Spec.Suspend = ptr.To(true)
+		gomega.Expect(k8sClient.Update(ctx, createdDevJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking it stays suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+		// Workload should stay pending and be evicted
+		util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl); err != nil {
+				return false
+			}
+			return workload.HasQuotaReservation(wl)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+		util.FinishEvictionForWorkloads(ctx, k8sClient, wl)
```

  I think this also shouldn't be necessary in integration tests for Jobs.

- **test/integration/controller/jobs/job/job_controller_test.go:1089** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:20:23Z
```diff
@@ -1086,6 +1086,71 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
```

  This test is pretty much the same as the E2E test. Since integration is cheaper, let's just keep the integration test. There isn't that much value added from the E2E test in this case.

- **pkg/controller/jobframework/reconciler.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:20:58Z
  Please add unit tests for the changes you added in `pkg/controller/jobs/job_test.go`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-16T19:23:24Z

- **test/integration/controller/jobs/job/job_controller_test.go:1139** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T19:23:23Z
```diff
@@ -1086,6 +1086,71 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("checking workload")
+		wl := &kueue.Workload{}
+		wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(devJob.Name), Namespace: devJob.Namespace}
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching QueueingPolicy from Always to Never")
+		wl.Spec.QueueingPolicy = kueue.QueueingPolicyTypeNever
+		gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching suspend to true")
+		gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).Should(gomega.Succeed())
+		createdDevJob.Spec.Suspend = ptr.To(true)
+		gomega.Expect(k8sClient.Update(ctx, createdDevJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking it stays suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+		// Workload should stay pending and be evicted
+		util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl); err != nil {
+				return false
+			}
+			return workload.HasQuotaReservation(wl)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+		util.FinishEvictionForWorkloads(ctx, k8sClient, wl)
+
+		ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
```

  Before this step, let's add a new job job and make sure that job gets scheduled.
  
  Let's set the queueing strategy of the ClusterQueue to StrictFIFO

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-16T20:55:32Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-16T20:55:32Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  +1 on `Hold` and `Admit`.
  `Never` sounds like eternally queueing regardless of whether doing any operations.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-16T21:50:12Z

- **pkg/controller/jobframework/reconciler.go:324** by [@vicentefb](https://github.com/vicentefb) — 2023-11-16T21:50:12Z
```diff
@@ -312,7 +312,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if job.IsSuspended() {
 		// start the job if the workload has been admitted, and the job is still suspended
 		if workload.IsAdmitted(wl) {
+			if workload.IsQueueingPolicyNever(wl) {
+				workload.UnsetQuotaReservationWithCondition(wl, "Pending", "QueueingPolicy is Never")
+				_ = workload.SyncAdmittedCondition(wl)
+				workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByQueueingPolicy, "QueueingPolicy is set to Never")
+				err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+				if err != nil {
+					return ctrl.Result{}, fmt.Errorf("applying admission status: %w", err)
+				}
+				return ctrl.Result{}, err
+			}
```

  Ohhh okok i see, I think I misunderstood the Eviction condition update from this comment https://github.com/kubernetes-sigs/kueue/pull/1252#pullrequestreview-1700624556, will remove the `SetEvictedCondition` method call and refactor `UnsetQuotaReservationWithCondition`

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-17T00:38:20Z

- **test/e2e/e2e_test.go:360** by [@vicentefb](https://github.com/vicentefb) — 2023-11-17T00:38:20Z
```diff
@@ -259,6 +259,123 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should evict a running job from the queue after QueueingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			//createdJob1 := &batchv1.Job{}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			lookupKey := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			createdJob := &batchv1.Job{}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				return sampleJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlKey, wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+			ginkgo.By("Change the QueueingPolicy, suspend job and check the job remains suspended and the workload evicted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.QueueingPolicy = kueue.QueueingPolicyTypeNever
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			// Suspending the job
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+			createdJob.Spec.Suspend = ptr.To(true)
+			gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Workload should get unadmitted
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
+				if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll); err != nil {
+					return false
+				}
+				return workload.HasQuotaReservation(wll)
+			}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+			util.FinishEvictionForWorkloads(ctx, k8sClient, wll)
```

  Okok sounds good, will remove it also we are not expecting the workloads to be evicted when suspended as you mentioned in a different comment.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-17T01:57:57Z

- **test/integration/controller/jobs/job/job_controller_test.go:1089** by [@vicentefb](https://github.com/vicentefb) — 2023-11-17T01:57:57Z
```diff
@@ -1086,6 +1086,71 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
```

  okok will remove this test

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-17T18:41:01Z

- **apis/kueue/v1beta1/workload_types.go:321** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-17T16:02:22Z
```diff
@@ -296,6 +312,19 @@ type WorkloadList struct {
 	Items           []Workload `json:"items"`
 }
 
+// QueueingPolicyType specifies if a suspended job should be queued.
+type QueueingPolicyType string
+
+const (
+	// QueueingPolicyTypeNever means that we don't queue a running workload
+	// after it was suspended
+	QueueingPolicyTypeNever QueueingPolicyType = "Never"
```

  We would like to have a "stop" command in Kueue CLI (https://github.com/kubernetes-sigs/kueue/pull/1325) for workload (related to this PR) and clusterqueue(https://github.com/kubernetes-sigs/kueue/pull/1288), so from user point of view it would be really nice to have the similar naming.
  
  Maybe having values Active/Stopped is an option?

- **charts/kueue/templates/crd/kueue.x-k8s.io_workloads.yaml:8277** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-17T18:37:47Z
```diff
@@ -8270,6 +8270,18 @@ spec:
                   is associated with. queueName cannot be changed while .status.admission
                   is not null.
                 type: string
+              queueingPolicy:
+                default: Always
+                description: "QueueingPolicy determines if a workload can be admitted
+                  into a queue. Changing the QueueingPolicy to Never does not evict
+                  or suspend workloads that are already running. Possible values are:
```

  >Changing the QueueingPolicy to Never does not evict or suspend workloads that are already running
  
  Hm, I probably missing something here. 
  In https://github.com/kubernetes-sigs/kueue/issues/1091 I think we want to suspend a running job.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-17T19:00:40Z

- **charts/kueue/templates/crd/kueue.x-k8s.io_workloads.yaml:8277** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-17T19:00:40Z
```diff
@@ -8270,6 +8270,18 @@ spec:
                   is associated with. queueName cannot be changed while .status.admission
                   is not null.
                 type: string
+              queueingPolicy:
+                default: Always
+                description: "QueueingPolicy determines if a workload can be admitted
+                  into a queue. Changing the QueueingPolicy to Never does not evict
+                  or suspend workloads that are already running. Possible values are:
```

  The user journey is that the user would set the queueing policy to Never and then suspend the job.
  
  But maybe you are right... there is no much reason why Kueue couldn't take the policy as a signal to suspend the Job as well.
  
  But then the name `queueingPolicy` doesn't fit. It would have to be `admissionPolicy`?
  
  @andrewsykim initially said the following:
  
  > One approach I've been thinking about is to add a requeuePolicy field to wworkload spec, with initial values of Always (default) or Never. This seems slightly cleaner than a field to directly suspend the job since we're not introducing new mechanisms to suspend workloads. Thoughts?
  
  The job reconciler is capable of evicting the job if the workload has a signal for that.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-17T19:30:36Z

- **charts/kueue/templates/crd/kueue.x-k8s.io_workloads.yaml:8277** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-17T19:30:36Z
```diff
@@ -8270,6 +8270,18 @@ spec:
                   is associated with. queueName cannot be changed while .status.admission
                   is not null.
                 type: string
+              queueingPolicy:
+                default: Always
+                description: "QueueingPolicy determines if a workload can be admitted
+                  into a queue. Changing the QueueingPolicy to Never does not evict
+                  or suspend workloads that are already running. Possible values are:
```

  I think from user point of view the workload and the job are the same thing, no? And I thought in Kueue we want to have keep workload and the job in sync, so any action on workload == action on job and vice verse, am I wrong here?

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-17T22:58:31Z

- **test/integration/controller/jobs/job/job_controller_test.go:1139** by [@vicentefb](https://github.com/vicentefb) — 2023-11-17T22:58:31Z
```diff
@@ -1086,6 +1086,71 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectReservingActiveWorkloadsMetric(prodClusterQ, 1)
 	})
 
+	ginkgo.It("Modifying QueueingPolicy", func() {
+		ginkgo.By("creating localQueue")
+		devLocalQ = testing.MakeLocalQueue("dev-queue", ns.Name).ClusterQueue(devClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, devLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking a job starts")
+		devJob := testingjob.MakeJob("dev-job", ns.Name).Queue(devLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, devJob)).Should(gomega.Succeed())
+		createdDevJob := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdDevJob.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotUntaintedFlavor.Name))
+		util.ExpectPendingWorkloadsMetric(devClusterQ, 0, 0)
+		util.ExpectReservingActiveWorkloadsMetric(devClusterQ, 1)
+
+		ginkgo.By("checking workload")
+		wl := &kueue.Workload{}
+		wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(devJob.Name), Namespace: devJob.Namespace}
+		gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching QueueingPolicy from Always to Never")
+		wl.Spec.QueueingPolicy = kueue.QueueingPolicyTypeNever
+		gomega.Expect(k8sClient.Update(ctx, wl)).Should(gomega.Succeed())
+
+		ginkgo.By("switching suspend to true")
+		gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).Should(gomega.Succeed())
+		createdDevJob.Spec.Suspend = ptr.To(true)
+		gomega.Expect(k8sClient.Update(ctx, createdDevJob)).Should(gomega.Succeed())
+
+		ginkgo.By("checking it stays suspended")
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: devJob.Name, Namespace: devJob.Namespace}, createdDevJob)).
+				Should(gomega.Succeed())
+			return createdDevJob.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+		// Workload should stay pending and be evicted
+		util.ExpectWorkloadsToBePending(ctx, k8sClient, wl)
+		gomega.Consistently(func() bool {
+			if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl); err != nil {
+				return false
+			}
+			return workload.HasQuotaReservation(wl)
+		}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
+
+		util.FinishEvictionForWorkloads(ctx, k8sClient, wl)
+
+		ginkgo.By("checking the jobs becomes unsuspended after we update the QueueingPolicy back to Always")
```

  I'll include this into the e2e test

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-17T23:02:56Z

- **apis/kueue/v1beta1/workload_types.go:321** by [@vicentefb](https://github.com/vicentefb) — 2023-11-17T23:02:56Z
```diff
@@ -296,6 +312,19 @@ type WorkloadList struct {
 	Items           []Workload `json:"items"`
 }
 
+// QueueingPolicyType specifies if a suspended job should be queued.
+type QueueingPolicyType string
+
+const (
+	// QueueingPolicyTypeNever means that we don't queue a running workload
+	// after it was suspended
+	QueueingPolicyTypeNever QueueingPolicyType = "Never"
```

  There is an ongoing discussion about the naming here (https://github.com/kubernetes-sigs/kueue/pull/1252#discussion_r1396325245) about using Hold/Admit as possible values. I'll quote this comment and mention you in that discussion so that everyone is aware.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-17T23:03:51Z

- **apis/kueue/v1beta1/workload_types.go:63** by [@vicentefb](https://github.com/vicentefb) — 2023-11-17T23:03:51Z
```diff
@@ -59,6 +59,20 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy that will determine if a job can be queued.
```

  @yaroslava-serdiuk commented the following: 
  
  > We would like to have a "stop" command in Kueue CLI (https://github.com/kubernetes-sigs/kueue/pull/1325) for workload (related to this PR) and clusterqueue(https://github.com/kubernetes-sigs/kueue/pull/1288), so from user point of view it would be really nice to have the similar naming. Maybe having values Active/Stopped is an option?

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-17T23:53:23Z

- **apis/kueue/v1beta1/workload_types.go:285** by [@vicentefb](https://github.com/vicentefb) — 2023-11-17T23:53:23Z
```diff
@@ -267,6 +279,10 @@ const (
 	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
 	// beacuse at least one admission check transitioned to False.
 	WorkloadEvictedByAdmissionCheck = "AdmissionCheck"
+
+	// WorkloadEvictedByQueueingPolicy indicates that the workload was evicted
+	// because QueueingPolicy was set to Never.
+	WorkloadEvictedByQueueingPolicy = "QueueingPolicyNever"
```

  okok will remove it!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-24T17:01:53Z

- **apis/kueue/v1beta1/workload_types.go:73** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T17:01:52Z
```diff
@@ -59,6 +59,18 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// QueueingPolicy determines if a workload can be admitted into a queue.
+	// Changing the QueueingPolicy to Never does not evict or suspend workloads that are already running.
+	// Possible values are:
+	//
+	//   - Never: indicates that a workload should never be admitted
+	//   - Always: indicates that a workload can be evaluated for admission into it's respective queue.
+	//
+	// Defaults to Always
+	// +kubebuilder:validation:Enum={Never, Always}
+	// +kubebuilder:default="Always"
+	QueueingPolicy QueueingPolicyType `json:"queueingPolicy,omitempty"`
```

  As discussed offline, we decided to simplify this by adding a boolean for `active`.
  
  If the boolean is set to false, then a running Job gets evicted.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-27T20:56:20Z

- **apis/kueue/v1beta1/workload_types.go:73** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T20:50:36Z
```diff
@@ -59,6 +59,18 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// Active determines if a workload can be admitted into a queue.
+	// Changing the Active to false will suspend workloads that are already running.
+	// Possible values are:
+	//
+	//   - false: indicates that a workload should never be admitted
+	//   - true: indicates that a workload can be evaluated for admission into it's respective queue.
+	//
+	// Defaults to true
+	// +kubebuilder:validation:Enum={false, true}
+	// +kubebuilder:default=true
+	Active bool `json:"active,omitempty"`
```

  ```suggestion
  	Active *bool `json:"active,omitempty"`
  ```

- **pkg/controller/core/workload_controller.go:379** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T20:51:34Z
```diff
@@ -376,6 +376,7 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	status := workloadStatus(wl)
 	log := r.log.WithValues("workload", klog.KObj(wl), "queue", wl.Spec.QueueName, "status", status)
 	ctx := ctrl.LoggerInto(context.Background(), log)
+	queueing := workload.IsWorkloadActive(wl)
```

  `wl.Spec.Active` isn't too much to write, so I wouldn't have this function.

- **pkg/controller/core/workload_controller.go:397** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T20:51:45Z
```diff
@@ -392,13 +393,18 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 		log = log.WithValues("prevClusterQueue", oldWl.Status.Admission.ClusterQueue)
 	}
 	log.V(2).Info("Workload update event")
+	log.V(2).Info(fmt.Sprintf("[VICENTE] WorkloadCONTROLLER is ACTIVE:  %v", workload.IsWorkloadActive(wl)))
+	log.V(2).Info(fmt.Sprintf("[VICENTE] OLD Workload is ACTIVE:  %v", workload.IsWorkloadActive(oldWl)))
```

  Please remove

- **pkg/controller/core/workload_controller.go:406** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T20:56:04Z
```diff
@@ -392,13 +393,18 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 		log = log.WithValues("prevClusterQueue", oldWl.Status.Admission.ClusterQueue)
 	}
 	log.V(2).Info("Workload update event")
+	log.V(2).Info(fmt.Sprintf("[VICENTE] WorkloadCONTROLLER is ACTIVE:  %v", workload.IsWorkloadActive(wl)))
+	log.V(2).Info(fmt.Sprintf("[VICENTE] OLD Workload is ACTIVE:  %v", workload.IsWorkloadActive(oldWl)))
 
 	wlCopy := wl.DeepCopy()
 	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
 	workload.AdjustResources(ctrl.LoggerInto(ctx, log), r.client, wlCopy)
 
 	switch {
-	case status == finished:
+	case status == finished || !queueing:
+		if !queueing {
+			log.Info("Workload will not be admitted because the workload is not active, the job was suspended", "workload", klog.KObj(wl))
```

  ```suggestion
  			log.V(2).Info("Workload will not be queued because the workload is not active", "workload", klog.KObj(wl))
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-27T21:15:05Z

- **pkg/controller/jobframework/reconciler.go:327** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T20:57:06Z
```diff
@@ -317,22 +324,29 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if workload.HasQuotaReservation(wl) {
 			if !job.IsActive() {
 				log.V(6).Info("The job is no longer active, clear the workloads admission")
-				workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
-				_ = workload.SyncAdmittedCondition(wl)
-				err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+				err := UnsetQuotaReservation(wl, "Pending", evCond.Message, ctx, r, true)
```

  this change looks unnecessary

- **pkg/controller/jobframework/reconciler.go:381** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:00:22Z
```diff
@@ -362,10 +376,11 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
 		return ctrl.Result{}, nil
 	}
-
+	log.V(2).Info(fmt.Sprintf("[VICENTE] Workload is ACTIVE:  %v", workload.IsWorkloadActive(wl)))
 	// 8. handle job is unsuspended.
-	if !workload.IsAdmitted(wl) {
+	if !workload.IsAdmitted(wl) || !workload.IsWorkloadActive(wl) {
```

  I think this should be done in step 6: handle eviction.

- **pkg/controller/jobframework/reconciler.go:881** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:01:11Z
```diff
@@ -861,3 +877,10 @@ func clearMinCountsIfFeatureDisabled(in []kueue.PodSet) []kueue.PodSet {
 	}
 	return in
 }
+
+func UnsetQuotaReservation(wl *kueue.Workload, reason, message string, ctx context.Context, r *JobReconciler, strict bool) error {
```

  ```suggestion
  func unsetQuotaReservation(wl *kueue.Workload, reason, message string, ctx context.Context, r *JobReconciler, strict bool) error {
  ```

- **pkg/controller/jobs/job/job_controller_test.go:535** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:05:54Z
```diff
@@ -450,6 +450,66 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is not admitted due to Active field being false and job is suspended": {
+			job: *baseJobWrapper.Clone().
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
```

  This is not ideal. We generally don't want to directly set the admitted condition to False. Instead, we add an Eviction condition and set suspend to false.
  
  A future reconcile would observe that the Job actually paused, and only then we would swap the Admitted condition to false.
  
  So overall you can split this into 2 test cases, where the second test case is the second reconcile described above.

- **pkg/workload/admissionchecks.go:51** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:09:57Z
```diff
@@ -44,6 +45,10 @@ func SyncAdmittedCondition(w *kueue.Workload) bool {
 		Message: "The workload is admitted",
 	}
 	switch {
+	case !hasReservation && !isActive:
+		newCondition.Status = metav1.ConditionFalse
+		newCondition.Reason = "QueueingDisabled"
+		newCondition.Message = "Active is set to false"
```

  I don't think this will be necessary.
  
  syncAdmission is just about QuotaReserved and AdmissionChecks. We don't handle evictions here.

- **test/e2e/e2e_test.go:516** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:10:50Z
```diff
@@ -464,6 +472,127 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should not readmit a job after QueueingPolicy is changed to Never and suspend to true", func() {
```

  update according to the new API

- **test/e2e/e2e_test.go:540** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:12:43Z
```diff
@@ -464,6 +472,127 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should not readmit a job after QueueingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdJob := &batchv1.Job{}
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				g.Expect(sampleJob.Spec.Suspend).To(gomega.Equal(ptr.To(false)))
+				g.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wll)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Change the Active field to suspend the job and check the job remains suspended and the workload unadmitted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.Active = false
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			ginkgo.By("checking a second job starts after first job is suspended")
+			sampleJob2 = (&testingjob.JobWrapper{Job: *sampleJob2}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"1s"}).Obj()
```

  `1s` could make things flaky.
  
  I would prefer we test the behavior in integration tests, so we can have good control of when a Job finishes, without having to add a sleep period.

- **test/e2e/e2e_test.go:574** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:14:49Z
```diff
@@ -464,6 +472,127 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should not readmit a job after QueueingPolicy is changed to Never and suspend to true", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdJob := &batchv1.Job{}
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				g.Expect(sampleJob.Spec.Suspend).To(gomega.Equal(ptr.To(false)))
+				g.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wll)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Change the Active field to suspend the job and check the job remains suspended and the workload unadmitted")
+			// Changing QueuingPolicy to Never
+			wll.Spec.Active = false
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			ginkgo.By("checking a second job starts after first job is suspended")
+			sampleJob2 = (&testingjob.JobWrapper{Job: *sampleJob2}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"1s"}).Obj()
+			wll2 := &kueue.Workload{}
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob2)).Should(gomega.Succeed())
+			wlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob2.Name), Namespace: sampleJob2.Namespace}
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobKey2, sampleJob2)).Should(gomega.Succeed())
+				g.Expect(sampleJob2.Spec.Suspend).To(gomega.Equal(ptr.To(false)))
+				g.Expect(k8sClient.Get(ctx, wlKey2, wll2)).Should(gomega.Succeed())
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wll2)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job stays suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			// Job should stay pending
+			gomega.Consistently(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.ConsistentDuration, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			ginkgo.By("checking the first workload gets unadmitted")
+			// Workload should get unadmitted
+			gomega.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+
+			// Workload should stay pending
+			gomega.Consistently(func() bool {
```

  You have 2 "Consistently" operations. This adds too much runtime to the test. You can combine both checks into a single "Consistently".

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-28T20:48:41Z

- **pkg/controller/jobframework/reconciler.go:365** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T20:29:12Z
```diff
@@ -363,6 +362,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, nil
 	}
 
+	if !*wl.Spec.Active {
```

  I'm not sure what will happen when we receive a Workload object that was created before the `Active` field was added.
  
  Could this pointer be nil? I guess we can just call `!ptr.Deref(wl.Spec.Active, true)` to be on the safe side.

- **pkg/controller/jobs/job/job_controller_test.go:453** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T20:35:40Z
```diff
@@ -450,6 +450,127 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted, spec.active is set to false and workload's conditions is set to Evicted": {
```

  ```suggestion
  		"when workload is admitted and spec.active is set to false the workload's conditions is set to Evicted": {
  ```

- **pkg/controller/jobs/job/job_controller_test.go:502** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T20:37:32Z
```diff
@@ -450,6 +450,127 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted, spec.active is set to false and workload's conditions is set to Evicted": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is not admitted due to spec.active field being false, job gets suspended and quota is unset": {
```

  ```suggestion
  		"when workload is evicted due to spec.active field being false, job gets suspended and quota is unset": {
  ```

- **pkg/util/testing/wrappers.go:67** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T20:40:28Z
```diff
@@ -64,6 +64,7 @@ func MakeWorkload(name, ns string) *WorkloadWrapper {
 	return &WorkloadWrapper{kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{Name: name, Namespace: ns},
 		Spec: kueue.WorkloadSpec{
+			Active: ptr.To(true),
```

  better remove this. It could hide some bugs.

- **pkg/controller/jobs/job/job_controller_test.go:651** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T20:42:20Z
```diff
@@ -450,6 +450,127 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted, spec.active is set to false and workload's conditions is set to Evicted": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is not admitted due to spec.active field being false, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "spec.active is set to false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
 		"when workload is admitted and PodSetUpdates conflict between admission checks on labels, the workload is finished with failure": {
```

  Let's add one more unit test:
  If the job is initially suspended, and the Workload has active=false and it's not admitted, it should not get an evicted condition, but the job should remain suspended.

- **test/e2e/e2e_test.go:487** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T20:43:04Z
```diff
@@ -464,6 +472,124 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
```

  I don't think this flavor is relevant for the test. Remove it to keep the test simple.

- **test/e2e/e2e_test.go:572** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T20:46:57Z
```diff
@@ -464,6 +472,124 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should not readmit a job to the queue after Active is changed to false", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdJob := &batchv1.Job{}
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				g.Expect(sampleJob.Spec.Suspend).To(gomega.Equal(ptr.To(false)))
+				g.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wll)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Change the Active field to suspend the job and check the job remains suspended and the workload unadmitted")
+			// Changing Active to false
+			wll.Spec.Active = ptr.To(false)
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			ginkgo.By("checking a second job starts after first job is suspended")
+			sampleJob2 = (&testingjob.JobWrapper{Job: *sampleJob2}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			wll2 := &kueue.Workload{}
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob2)).Should(gomega.Succeed())
+			wlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob2.Name), Namespace: sampleJob2.Namespace}
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobKey2, sampleJob2)).Should(gomega.Succeed())
+				g.Expect(sampleJob2.Spec.Suspend).To(gomega.Equal(ptr.To(false)))
+				g.Expect(k8sClient.Get(ctx, wlKey2, wll2)).Should(gomega.Succeed())
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wll2)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job is suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			ginkgo.By("checking the first job and workload stay suspended and unadmitted")
+			gomega.Consistently(func(g gomega.Gomega) {
+				// Job should stay pending
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				g.Expect(createdJob.Spec.Suspend).To(gomega.Equal(ptr.To(true)))
+				// Workload should get unadmitted
+				g.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+				// Workload should stay pending
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll)).Should(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(wll)).Should(gomega.BeFalse())
```

  No need to check this separately from `util.ExpectWorkloadsToBePending`

- **test/e2e/e2e_test.go:576** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T20:48:28Z
```diff
@@ -464,6 +472,124 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
+			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+					*testing.MakeFlavorQuotas("spot").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotRF, true)
+		})
+
+		ginkgo.It("Should not readmit a job to the queue after Active is changed to false", func() {
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			lookupKey1 := types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}
+			wll := &kueue.Workload{}
+
+			ginkgo.By("checking the job starts")
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdJob := &batchv1.Job{}
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob.Name), Namespace: sampleJob.Namespace}
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, lookupKey1, sampleJob)).Should(gomega.Succeed())
+				g.Expect(sampleJob.Spec.Suspend).To(gomega.Equal(ptr.To(false)))
+				g.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wll)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Change the Active field to suspend the job and check the job remains suspended and the workload unadmitted")
+			// Changing Active to false
+			wll.Spec.Active = ptr.To(false)
+			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
+
+			ginkgo.By("checking a second job starts after first job is suspended")
+			sampleJob2 = (&testingjob.JobWrapper{Job: *sampleJob2}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"50s"}).Obj()
+			wll2 := &kueue.Workload{}
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob2)).Should(gomega.Succeed())
+			wlKey2 := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(sampleJob2.Name), Namespace: sampleJob2.Namespace}
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobKey2, sampleJob2)).Should(gomega.Succeed())
+				g.Expect(sampleJob2.Spec.Suspend).To(gomega.Equal(ptr.To(false)))
+				g.Expect(k8sClient.Get(ctx, wlKey2, wll2)).Should(gomega.Succeed())
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wll2)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			// Checking job stays suspended
+			ginkgo.By("checking job is suspended")
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+
+			ginkgo.By("checking the first job and workload stay suspended and unadmitted")
+			gomega.Consistently(func(g gomega.Gomega) {
+				// Job should stay pending
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: sampleJob.Name, Namespace: sampleJob.Namespace}, createdJob)).
+					Should(gomega.Succeed())
+				g.Expect(createdJob.Spec.Suspend).To(gomega.Equal(ptr.To(true)))
+				// Workload should get unadmitted
+				g.Expect(k8sClient.Get(ctx, wlKey, wll)).Should(gomega.Succeed())
+				util.ExpectWorkloadsToBePending(ctx, k8sClient, wll)
+				// Workload should stay pending
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wll), wll)).Should(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(wll)).Should(gomega.BeFalse())
+				// Should have Evicted condition
+				isEvicting := apimeta.IsStatusConditionTrue(wll.Status.Conditions, kueue.WorkloadEvicted)
+				gomega.Expect(isEvicting).Should(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  ```suggestion
  			}, util.ConsistendDuration, util.Interval).Should(gomega.Succeed())
  ```
  
  this constant is just 3s, which should be enough.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-29T00:46:24Z

- **test/e2e/e2e_test.go:487** by [@vicentefb](https://github.com/vicentefb) — 2023-11-29T00:46:23Z
```diff
@@ -464,6 +472,124 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("QueueingPolicy", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			spotRF       *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			spotRF = testing.MakeResourceFlavor("spot").
+				Label("instance-type", "spot").Obj()
```

  Okok I will use "default-flavor" to keep it simple instead

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-29T07:55:00Z

- **apis/kueue/v1beta1/workload_types.go:71** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T06:44:37Z
```diff
@@ -59,6 +59,18 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// Active determines if a workload can be admitted into a queue.
+	// Changing the Active to false will suspend workloads that are already running.
+	// Possible values are:
+	//
+	//   - false: indicates that a workload should never be admitted
+	//   - true: indicates that a workload can be evaluated for admission into it's respective queue.
+	//
+	// Defaults to true
+	// +kubebuilder:validation:Enum={false, true}
```

  ```suggestion
  ```
  
  This field can be received null, it is useful for existing users.

- **apis/kueue/v1beta1/workload_types.go:285** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T06:49:49Z
```diff
@@ -267,6 +279,10 @@ const (
 	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
 	// beacuse at least one admission check transitioned to False.
 	WorkloadEvictedByAdmissionCheck = "AdmissionCheck"
+
+	// WorkloadEvictedByAcitveField indicates that the workload was evicted
+	// because spec.active is set to false.
+	WorkloadEvictedByAcitveField = "WorkloadIsNotActive"
```

  ```suggestion
  	WorkloadEvictedByDeacitvation = "InactiveWorkload"
  ```
  
  The `Field` suffix sounds weird to me.
  @alculquicondor How about my suggested API name?

- **pkg/controller/core/workload_controller.go:379** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:12:46Z
```diff
@@ -376,6 +376,7 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	status := workloadStatus(wl)
 	log := r.log.WithValues("workload", klog.KObj(wl), "queue", wl.Spec.QueueName, "status", status)
 	ctx := ctrl.LoggerInto(context.Background(), log)
+	queueing := *wl.Spec.Active
```

  ```suggestion
  	queueing := ptr.Deref(*wl.Spec.Active, true)
  ```

- **pkg/controller/jobframework/reconciler.go:320** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:19:36Z
```diff
@@ -317,9 +317,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if workload.HasQuotaReservation(wl) {
 			if !job.IsActive() {
 				log.V(6).Info("The job is no longer active, clear the workloads admission")
-				workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
-				_ = workload.SyncAdmittedCondition(wl)
-				err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+				err := unsetQuotaReservation(wl, "Pending", evCond.Message, ctx, r, true)
```

  I don't think this `unsetQuotaReservation` is needed.

- **pkg/controller/jobframework/reconciler.go:367** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:21:38Z
```diff
@@ -363,6 +362,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, nil
 	}
 
+	if !ptr.Deref(wl.Spec.Active, true) {
```

  ```suggestion
  	// 8. handle workload is deactivated.
  	if !ptr.Deref(wl.Spec.Active, true) {
  ```

- **pkg/controller/jobframework/reconciler.go:374** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:21:47Z
```diff
@@ -363,6 +362,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, nil
 	}
 
+	if !ptr.Deref(wl.Spec.Active, true) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAcitveField, "spec.active is set to false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err != nil {
+			return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
+		}
+		return ctrl.Result{}, nil
+	}
+
 	// 8. handle job is unsuspended.
```

  ```suggestion
  	// 9. handle job is unsuspended.
  ```

- **pkg/controller/jobframework/reconciler.go:366** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:26:17Z
```diff
@@ -363,6 +362,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, nil
 	}
 
+	if !ptr.Deref(wl.Spec.Active, true) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAcitveField, "spec.active is set to false")
```

  ```suggestion
  		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAcitveField, "the workload is deactivated")
  ```

- **pkg/controller/jobs/job/job_controller_test.go:453** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:29:30Z
```diff
@@ -450,6 +450,203 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted and spec.active is set to false the workload's conditions is set to Evicted": {
```

  ```suggestion
  		"when workload is admitted and spec.active is set to false, the workload's conditions is set to Evicted": {
  ```

- **pkg/controller/jobs/job/job_controller_test.go:507** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:50:24Z
```diff
@@ -450,6 +450,203 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted and spec.active is set to false the workload's conditions is set to Evicted": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is evicted due to spec.active field being false, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
```

  ```suggestion
  ```
  
  The `baseJob` already has `suspend=true`.

- **pkg/controller/jobs/job/job_controller_test.go:578** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:51:19Z
```diff
@@ -450,6 +450,203 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted and spec.active is set to false the workload's conditions is set to Evicted": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is evicted due to spec.active field being false, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "spec.active is set to false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when job is initially suspended, the Workload has active=false and it's not admitted, " +
+			"it should not get an evicted condition, but the job should remain suspended": {
+			job: *baseJobWrapper.Clone().
+				Suspend(true).
```

  ```suggestion
  ```
  
  ditto

- **pkg/controller/jobs/job/job_controller_test.go:581** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:51:28Z
```diff
@@ -450,6 +450,203 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted and spec.active is set to false the workload's conditions is set to Evicted": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is evicted due to spec.active field being false, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "spec.active is set to false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when job is initially suspended, the Workload has active=false and it's not admitted, " +
+			"it should not get an evicted condition, but the job should remain suspended": {
+			job: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
```

  ```suggestion
  ```
  
  ditto

- **test/e2e/e2e_test.go:315** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:53:47Z
```diff
@@ -304,7 +312,7 @@ var _ = ginkgo.Describe("Kueue", func() {
 			spotRF = testing.MakeResourceFlavor("spot").
 				Label("instance-type", "spot").Obj()
 			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
-			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").QueueingStrategy("StrictFIFO").
```

  ```suggestion
  			clusterQueue = testing.MakeClusterQueue("cluster-queue").
  ```
  
  Please revert this change.

- **test/e2e/e2e_test.go:475** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T07:54:42Z
```diff
@@ -464,6 +472,115 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("Suspend a running Job without requeueing through Workload's spec.active field", func() {
```

  Please move this case to integration test: https://github.com/kubernetes-sigs/kueue/blob/71b5cee8da608c1fa3bdd462641d53fa67f6bde5/test/integration/controller/jobs/job/job_controller_test.go

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-11-29T15:30:10Z

- **apis/kueue/v1beta1/workload_types.go:64** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-28T01:27:33Z
```diff
@@ -59,6 +59,18 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// Active determines if a workload can be admitted into a queue.
+	// Changing the Active to false will suspend workloads that are already running.
```

  `Changing active from true to false will evict any running workloads.`

- **apis/kueue/v1beta1/workload_types.go:67** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-28T01:27:58Z
```diff
@@ -59,6 +59,18 @@ type WorkloadSpec struct {
 	// +kubebuilder:default=""
 	// +kubebuilder:validation:Enum=kueue.x-k8s.io/workloadpriorityclass;scheduling.k8s.io/priorityclass;""
 	PriorityClassSource string `json:"priorityClassSource,omitempty"`
+
+	// Active determines if a workload can be admitted into a queue.
+	// Changing the Active to false will suspend workloads that are already running.
+	// Possible values are:
+	//
+	//   - false: indicates that a workload should never be admitted
```

  `- false: indicates that a workload should never be admitted and evicts running workloads`

- **pkg/controller/jobframework/reconciler.go:369** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-29T15:30:06Z
```diff
@@ -363,6 +362,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, nil
 	}
 
+	if !ptr.Deref(wl.Spec.Active, true) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAcitveField, "spec.active is set to false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
```

  Do we still need to apply admission status here or will it be covered in the next sync loop?

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-29T17:51:30Z

- **pkg/controller/jobframework/reconciler.go:320** by [@vicentefb](https://github.com/vicentefb) — 2023-11-29T17:51:30Z
```diff
@@ -317,9 +317,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if workload.HasQuotaReservation(wl) {
 			if !job.IsActive() {
 				log.V(6).Info("The job is no longer active, clear the workloads admission")
-				workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
-				_ = workload.SyncAdmittedCondition(wl)
-				err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+				err := unsetQuotaReservation(wl, "Pending", evCond.Message, ctx, r, true)
```

  Ohh it's a refactor, I included `unsetQuotaReservation` in this PR https://github.com/kubernetes-sigs/kueue/pull/1252/files/76eb96f94bbfe43e0491ae7f98024fcaf5cfd24e#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR872

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T18:25:45Z

- **pkg/controller/jobframework/reconciler.go:320** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T18:25:44Z
```diff
@@ -317,9 +317,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if workload.HasQuotaReservation(wl) {
 			if !job.IsActive() {
 				log.V(6).Info("The job is no longer active, clear the workloads admission")
-				workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
-				_ = workload.SyncAdmittedCondition(wl)
-				err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+				err := unsetQuotaReservation(wl, "Pending", evCond.Message, ctx, r, true)
```

  let's just revert if it's not necessary for implementing this feature.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T18:26:42Z

- **pkg/controller/jobframework/reconciler.go:369** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T18:26:41Z
```diff
@@ -363,6 +362,15 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, nil
 	}
 
+	if !ptr.Deref(wl.Spec.Active, true) {
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAcitveField, "spec.active is set to false")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
```

  We do. This will update the condition on the apiserver

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T18:28:27Z

- **pkg/controller/jobs/job/job_controller_test.go:507** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T18:28:27Z
```diff
@@ -450,6 +450,203 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted and spec.active is set to false the workload's conditions is set to Evicted": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is evicted due to spec.active field being false, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
```

  This doesn't hurt, and it makes the test easier to follow.
  
  OTOH, should we set `.Suspend(false)` in the input job?

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-29T18:53:16Z

- **test/e2e/e2e_test.go:475** by [@vicentefb](https://github.com/vicentefb) — 2023-11-29T18:53:16Z
```diff
@@ -464,6 +472,115 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("Suspend a running Job without requeueing through Workload's spec.active field", func() {
```

  Ohh so basically the whole e2e test should be moved `integration/controller/jobs/job/job_controller_test.go` ?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T18:58:24Z

- **test/e2e/e2e_test.go:475** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T18:58:24Z
```diff
@@ -464,6 +472,115 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("Suspend a running Job without requeueing through Workload's spec.active field", func() {
```

  My preference is also to make this an integration test, which gives us greater control.
  
  integration tests are cheaper to run too.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-29T19:04:25Z

- **test/e2e/e2e_test.go:475** by [@vicentefb](https://github.com/vicentefb) — 2023-11-29T19:04:25Z
```diff
@@ -464,6 +472,115 @@ var _ = ginkgo.Describe("Kueue", func() {
 		})
 	})
 
+	ginkgo.When("Suspend a running Job without requeueing through Workload's spec.active field", func() {
```

  okok sounds good, i'll move it there!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-29T20:16:59Z

- **pkg/controller/jobs/job/job_controller_test.go:507** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T20:16:59Z
```diff
@@ -450,6 +450,203 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted and spec.active is set to false the workload's conditions is set to Evicted": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is evicted due to spec.active field being false, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
```

  > OTOH, should we set .Suspend(false) in the input job?
  
  SGTM

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-30T00:24:15Z

- **pkg/controller/jobs/job/job_controller_test.go:578** by [@vicentefb](https://github.com/vicentefb) — 2023-11-30T00:24:15Z
```diff
@@ -450,6 +450,203 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted and spec.active is set to false the workload's conditions is set to Evicted": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is evicted due to spec.active field being false, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "spec.active is set to false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when job is initially suspended, the Workload has active=false and it's not admitted, " +
+			"it should not get an evicted condition, but the job should remain suspended": {
+			job: *baseJobWrapper.Clone().
+				Suspend(true).
```

  Ack, but will leave it so it's easy to follow :)

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-30T00:24:21Z

- **pkg/controller/jobs/job/job_controller_test.go:581** by [@vicentefb](https://github.com/vicentefb) — 2023-11-30T00:24:21Z
```diff
@@ -450,6 +450,203 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"when workload is admitted and spec.active is set to false the workload's conditions is set to Evicted": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is evicted due to spec.active field being false, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					Active(false).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "spec.active is set to false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "WorkloadIsNotActive",
+						Message: "spec.active is set to false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when job is initially suspended, the Workload has active=false and it's not admitted, " +
+			"it should not get an evicted condition, but the job should remain suspended": {
+			job: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
```

  Ack, but will leave it so it's easy to follow :)

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-30T00:24:33Z

- **test/e2e/e2e_test.go:315** by [@vicentefb](https://github.com/vicentefb) — 2023-11-30T00:24:33Z
```diff
@@ -304,7 +312,7 @@ var _ = ginkgo.Describe("Kueue", func() {
 			spotRF = testing.MakeResourceFlavor("spot").
 				Label("instance-type", "spot").Obj()
 			gomega.Expect(k8sClient.Create(ctx, spotRF)).Should(gomega.Succeed())
-			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").QueueingStrategy("StrictFIFO").
```

  Done

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-11-30T02:11:16Z

- **pkg/controller/core/workload_controller.go:380** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-30T02:09:57Z
```diff
@@ -376,6 +377,7 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	status := workloadStatus(wl)
 	log := r.log.WithValues("workload", klog.KObj(wl), "queue", wl.Spec.QueueName, "status", status)
 	ctx := ctrl.LoggerInto(context.Background(), log)
+	queueing := ptr.Deref(wl.Spec.Active, true)
```

  just name the variable `active`?

- **pkg/controller/jobframework/reconciler.go:336** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-30T02:10:23Z
```diff
@@ -333,6 +333,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		// start the job if the workload has been admitted, and the job is still suspended
 		if workload.IsAdmitted(wl) {
 			log.V(2).Info("Job admitted, unsuspending")
+
```

  remove diff

- **pkg/controller/jobframework/reconciler.go:604** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-30T02:10:54Z
```diff
@@ -599,7 +610,6 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 // Returns whether any operation was done to stop the job or an error.
 func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload, eventMsg string) error {
 	info := GetPodSetsInfoFromWorkload(wl)
-
```

  remove diff

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-11-30T16:53:18Z

Please squash / rebase commits, otherwise LGTM :)

- **test/integration/controller/jobs/job/job_controller_test.go:65** by [@andrewsykim](https://github.com/andrewsykim) — 2023-11-30T16:52:13Z
```diff
@@ -62,7 +62,6 @@ var (
 // +kubebuilder:docs-gen:collapse=Imports
 
 var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-
```

  remove diff

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-30T20:55:16Z

/approve
I'll leave the LGTM to @andrewsykim 
/hold
for review comments

Please squash

- **apis/kueue/v1beta1/workload_types.go:285** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-30T20:27:26Z
```diff
@@ -267,6 +279,10 @@ const (
 	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
 	// beacuse at least one admission check transitioned to False.
 	WorkloadEvictedByAdmissionCheck = "AdmissionCheck"
+
+	// WorkloadEvictedByAcitveField indicates that the workload was evicted
+	// because spec.active is set to false.
+	WorkloadEvictedByAcitveField = "WorkloadIsNotActive"
```

  `WorkloadEvictedByDeactivation` sgtm

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-30T23:36:21Z

Otherwise LGTM

/approve

- **pkg/util/testing/wrappers.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-30T23:35:38Z
```diff
@@ -102,7 +102,11 @@ func (w *WorkloadWrapper) Queue(q string) *WorkloadWrapper {
 	return w
 }
 
-// ReserveQuota sets workload admission and adds a "QuotaReserved" status condition
```

  Can you move this comment to Line 109 instead of removing it?

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2023-11-30T23:41:03Z

- **pkg/util/testing/wrappers.go:105** by [@vicentefb](https://github.com/vicentefb) — 2023-11-30T23:41:03Z
```diff
@@ -102,7 +102,11 @@ func (w *WorkloadWrapper) Queue(q string) *WorkloadWrapper {
 	return w
 }
 
-// ReserveQuota sets workload admission and adds a "QuotaReserved" status condition
```

  ohhh my bad, done

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-12-01T02:00:16Z

- **apis/kueue/v1beta1/workload_types.go:282** by [@andrewsykim](https://github.com/andrewsykim) — 2023-12-01T01:28:19Z
```diff
@@ -267,6 +278,10 @@ const (
 	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
 	// beacuse at least one admission check transitioned to False.
 	WorkloadEvictedByAdmissionCheck = "AdmissionCheck"
+
+	// WorkloadEvictedByAcitveField indicates that the workload was evicted
```

  WorkloadEvictedByAcitveField -> WorkloadEvictedByDeactivation

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-12-01T02:20:21Z

Please update release notes! 

Thanks @vicentefb 

/lgtm
