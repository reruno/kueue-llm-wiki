# PR #1271: Add an event recorder for Admission Check Controller

**Summary**: Add an event recorder for Admission Check Controller

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1271

**Last updated**: 2024-05-23T14:43:08Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@stuton](https://github.com/stuton)
- **Branch**: `recorder-admission-checks` → `main`
- **Created**: 2023-10-26T15:18:00Z
- **Updated**: 2024-05-23T14:43:08Z
- **Merged**: 2023-11-14T16:08:15Z
- **Closed**: 2023-11-14T16:08:15Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `tide/merge-method-squash`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 4   **+29 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add an event recorder for Admission Check Controller

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1246

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add events for transitions of the provisioning AdmissionCheck
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-26T15:18:10Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 53a8d24993a15fe6137ebc0daec3b52af417dca8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65534f2c67feb1000881ea11 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-26T18:37:18Z

/assign @mimowo

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-06T22:08:21Z

/assign @trasc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-14T16:03:31Z

LGTM label has been added.  <details>Git tree hash: 28fd6aad35a979621c0b92f3b01cf88561f8ff32</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-14T16:03:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1271#pullrequestreview-1730118969" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1271#" title="Author self-approved">stuton</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-01T21:28:53Z

/release-note-edit
```release-notes
Add events for transitions of the provisioning AdmissionCheck
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-01T21:30:14Z

/cherry-pick

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-01T21:30:28Z

/cherry-pick release-0.5

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2023-12-01T21:31:07Z

@alculquicondor: new pull request created: #1394

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1271#issuecomment-1836804360):

>/cherry-pick release-0.5


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-27T08:00:11Z

- **pkg/controller/admissionchecks/provisioning/controller.go:456** by [@trasc](https://github.com/trasc) — 2023-10-27T07:51:00Z
```diff
@@ -449,6 +452,7 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
 	}
 	if updated {
+		c.record.Eventf(wl, corev1.EventTypeNormal, "UpdatedWorkload", "Updated admission checks state for workload %s", workload.Key(wl))
 		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
```

  ```suggestion
  		err := c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
  		if err != nil {
  			return err
  		}
  		c.record.Eventf(wl, corev1.EventTypeNormal, "UpdatedWorkload", "Updated admission checks state for workload %s", workload.Key(wl))
  ```
  
  "UpdatedWorkload" is a bit too generic, we should record what happened also. 
  + A workload can have multiple admission checks managed by this controller, we should  record the  check as well.

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go:64** by [@trasc](https://github.com/trasc) — 2023-10-27T07:57:28Z
```diff
@@ -61,6 +64,7 @@ func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 
 	if currentCondition.Status != newCondition.Status {
 		apimeta.SetStatusCondition(&ac.Status.Conditions, newCondition)
+		a.record.Eventf(ac, corev1.EventTypeNormal, "UpdatedAdmissionChecks", "Admission checks status is changed to %s", newCondition.Status)
 		return reconcile.Result{}, a.client.Status().Update(ctx, ac)
```

  Record the event only if the status update is successful.

- **pkg/controller/core/workload_controller.go:220** by [@trasc](https://github.com/trasc) — 2023-10-27T07:59:59Z
```diff
@@ -210,8 +214,11 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
 		wl.Status.AdmissionChecks = newChecks
-		err := r.client.Status().Update(ctx, wl)
-		return true, client.IgnoreNotFound(err)
+		if err := r.client.Status().Update(ctx, wl); !apierrors.IsNotFound(err) {
+			return true, err
+		}
+		r.record.Eventf(wl, corev1.EventTypeNormal, "UpdatedWorkload", "Updated admission checks of the workload %v for clusterQueue %s", workload.Key(wl), cqName)
```

  Also the event name is a bit generic

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-27T08:30:50Z

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go:64** by [@mimowo](https://github.com/mimowo) — 2023-10-27T08:23:14Z
```diff
@@ -61,6 +64,7 @@ func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 
 	if currentCondition.Status != newCondition.Status {
 		apimeta.SetStatusCondition(&ac.Status.Conditions, newCondition)
+		a.record.Eventf(ac, corev1.EventTypeNormal, "UpdatedAdmissionChecks", "Admission checks status is changed to %s", newCondition.Status)
 		return reconcile.Result{}, a.client.Status().Update(ctx, ac)
```

  +1

- **pkg/controller/core/workload_controller.go:220** by [@mimowo](https://github.com/mimowo) — 2023-10-27T08:30:37Z
```diff
@@ -210,8 +214,11 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
 		wl.Status.AdmissionChecks = newChecks
-		err := r.client.Status().Update(ctx, wl)
-		return true, client.IgnoreNotFound(err)
+		if err := r.client.Status().Update(ctx, wl); !apierrors.IsNotFound(err) {
+			return true, err
+		}
+		r.record.Eventf(wl, corev1.EventTypeNormal, "UpdatedWorkload", "Updated admission checks of the workload %v for clusterQueue %s", workload.Key(wl), cqName)
```

  +1 `WorkloadAdmissionChecksUpdated` ?

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-27T12:02:55Z

- **pkg/controller/core/workload_controller.go:220** by [@stuton](https://github.com/stuton) — 2023-10-27T12:02:55Z
```diff
@@ -210,8 +214,11 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
 		wl.Status.AdmissionChecks = newChecks
-		err := r.client.Status().Update(ctx, wl)
-		return true, client.IgnoreNotFound(err)
+		if err := r.client.Status().Update(ctx, wl); !apierrors.IsNotFound(err) {
+			return true, err
+		}
+		r.record.Eventf(wl, corev1.EventTypeNormal, "UpdatedWorkload", "Updated admission checks of the workload %v for clusterQueue %s", workload.Key(wl), cqName)
```

  changed https://github.com/kubernetes-sigs/kueue/pull/1271/commits/99243245c6f0e4573eefe1e6b60840aec6dae92e

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-03T17:31:19Z

- **cmd/kueue/main.go:229** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-03T17:15:21Z
```diff
@@ -226,7 +226,7 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 	// setup provision admission check controller
 	if features.Enabled(features.ProvisioningACC) && provisioning.ServerSupportsProvisioningRequest(mgr) {
 		// A info message is added in setupIndexes if autoscaling is not supported by the cluster
-		if err := provisioning.NewController(mgr.GetClient()).SetupWithManager(mgr); err != nil {
+		if err := provisioning.NewController(mgr.GetClient(), mgr.GetEventRecorderFor("admission-checks-controller")).SetupWithManager(mgr); err != nil {
```

  The name should be specific to ProvReq.
  
  Let's say `kueue-provisioning-request-controller`.

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-03T17:21:09Z
```diff
@@ -61,7 +64,11 @@ func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 
 	if currentCondition.Status != newCondition.Status {
 		apimeta.SetStatusCondition(&ac.Status.Conditions, newCondition)
-		return reconcile.Result{}, a.client.Status().Update(ctx, ac)
+		if err := a.client.Status().Update(ctx, ac); err != nil {
+			return reconcile.Result{}, err
+		}
+		a.record.Eventf(ac, corev1.EventTypeNormal, "UpdatedAdmissionChecks", "Admission checks status is changed to %s", newCondition.Status)
```

  I'm not sure about the usefulness of this event. Typically, an administrator doesn't need to pay attention to changes in this object.
  If they happen to need more detail, inspecting the AdmissionCheck status should be enough.

- **pkg/controller/admissionchecks/provisioning/controller.go:438** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-03T17:24:18Z
```diff
@@ -431,9 +436,19 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 			}
 		}
 		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
```

  we should only report events if the status of this check changed.

- **pkg/controller/admissionchecks/provisioning/controller.go:450** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-03T17:25:36Z
```diff
@@ -431,9 +436,19 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 			}
 		}
 		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+		message := fmt.Sprintf("Admission check %s set state %s", checkState.Name, checkState.State)
+		if checkState.Message != "" {
+			message += fmt.Sprintf(" with message %s", checkState.Message)
+		}
+		recorderMessages = append(recorderMessages, message)
 	}
 	if updated {
-		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+		if err := c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership); err != nil {
+			return err
+		}
+		for i := range recorderMessages {
+			c.record.Eventf(wl, corev1.EventTypeNormal, "WorkloadAdmissionChecksUpdated", api.TruncateEventMessage(recorderMessages[i]))
```

  ```suggestion
  			c.record.Eventf(wl, corev1.EventTypeNormal, "AdmissionCheckUpdated", api.TruncateEventMessage(recorderMessages[i]))
  ```
  
  No need to repeat Workload, as this is already referencing a workload object.

- **pkg/controller/core/workload_controller.go:225** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-03T17:30:15Z
```diff
@@ -216,8 +219,11 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
 		wl.Status.AdmissionChecks = newChecks
-		err := r.client.Status().Update(ctx, wl)
-		return true, client.IgnoreNotFound(err)
+		if err := r.client.Status().Update(ctx, wl); !apierrors.IsNotFound(err) {
+			return true, err
+		}
+		r.record.Eventf(wl, corev1.EventTypeNormal, "WorkloadAdmissionChecksUpdated", "Updated admission checks of the workload %v for clusterQueue %s", workload.Key(wl), cqName)
```

  ```suggestion
  		r.record.Eventf(wl, corev1.EventTypeNormal, "AdmissionChecksChanged", "Updated the list of admission checks for the workload, based on the clusterQueue %s", cqName)
  ```

- **pkg/controller/core/workload_controller.go:224** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-03T17:31:10Z
```diff
@@ -216,8 +219,11 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
 		wl.Status.AdmissionChecks = newChecks
-		err := r.client.Status().Update(ctx, wl)
-		return true, client.IgnoreNotFound(err)
+		if err := r.client.Status().Update(ctx, wl); !apierrors.IsNotFound(err) {
+			return true, err
+		}
```

  ```suggestion
  		if err := r.client.Status().Update(ctx, wl); err != nil {
  			return true, client.IgnoreNotFound(err)
  		}
  ```

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-11-06T13:44:38Z

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go:70** by [@stuton](https://github.com/stuton) — 2023-11-06T13:44:38Z
```diff
@@ -61,7 +64,11 @@ func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 
 	if currentCondition.Status != newCondition.Status {
 		apimeta.SetStatusCondition(&ac.Status.Conditions, newCondition)
-		return reconcile.Result{}, a.client.Status().Update(ctx, ac)
+		if err := a.client.Status().Update(ctx, ac); err != nil {
+			return reconcile.Result{}, err
+		}
+		a.record.Eventf(ac, corev1.EventTypeNormal, "UpdatedAdmissionChecks", "Admission checks status is changed to %s", newCondition.Status)
```

  removed https://github.com/kubernetes-sigs/kueue/pull/1271/commits/3b15bd9a0ad460731328c4e0eaeb91897f09cfb0

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-11-06T13:44:56Z

- **cmd/kueue/main.go:229** by [@stuton](https://github.com/stuton) — 2023-11-06T13:44:56Z
```diff
@@ -226,7 +226,7 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 	// setup provision admission check controller
 	if features.Enabled(features.ProvisioningACC) && provisioning.ServerSupportsProvisioningRequest(mgr) {
 		// A info message is added in setupIndexes if autoscaling is not supported by the cluster
-		if err := provisioning.NewController(mgr.GetClient()).SetupWithManager(mgr); err != nil {
+		if err := provisioning.NewController(mgr.GetClient(), mgr.GetEventRecorderFor("admission-checks-controller")).SetupWithManager(mgr); err != nil {
```

  added https://github.com/kubernetes-sigs/kueue/pull/1271/commits/3b15bd9a0ad460731328c4e0eaeb91897f09cfb0

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-13T18:56:38Z

- **test/integration/controller/admissionchecks/provisioning/suite_test.go:58** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-13T18:44:49Z
```diff
@@ -55,7 +55,7 @@ func managerSetup(mgr manager.Manager, ctx context.Context) {
 	err = provisioning.SetupIndexer(ctx, mgr.GetFieldIndexer())
 	gomega.Expect(err).NotTo(gomega.HaveOccurred())
 
-	reconciler := provisioning.NewController(mgr.GetClient())
+	reconciler := provisioning.NewController(mgr.GetClient(), mgr.GetEventRecorderFor("admission-checks-controller"))
```

  ```suggestion
  	reconciler := provisioning.NewController(mgr.GetClient(), mgr.GetEventRecorderFor("kueue-provisioning-request-controller"))
  ```

- **pkg/controller/core/workload_controller.go:222** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-13T18:56:34Z
```diff
@@ -216,8 +219,11 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
 		wl.Status.AdmissionChecks = newChecks
-		err := r.client.Status().Update(ctx, wl)
-		return true, client.IgnoreNotFound(err)
+		if err := r.client.Status().Update(ctx, wl); err != nil {
```

  The more I look at this, the less value I see.
  
  Users shouldn't be interested about this either.
  They just want to know when the admission checks are satisfied. Let's remove this event too.

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-11-14T10:39:10Z

- **pkg/controller/core/workload_controller.go:222** by [@stuton](https://github.com/stuton) — 2023-11-14T10:39:10Z
```diff
@@ -216,8 +219,11 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		log := ctrl.LoggerFrom(ctx)
 		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
 		wl.Status.AdmissionChecks = newChecks
-		err := r.client.Status().Update(ctx, wl)
-		return true, client.IgnoreNotFound(err)
+		if err := r.client.Status().Update(ctx, wl); err != nil {
```

  removed https://github.com/kubernetes-sigs/kueue/pull/1271/commits/e27ddfae37502bac202e7688a52f8624a71dfd72

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-14T16:03:24Z

/lgtm
/approve
/label tide/merge-method-squash
