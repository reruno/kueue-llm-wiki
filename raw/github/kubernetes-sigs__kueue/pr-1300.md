# PR #1300: KEP-168-2: Pending workloads visibility

**Summary**: KEP-168-2: Pending workloads visibility

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1300

**Last updated**: 2024-01-29T11:27:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `kep-168-2` → `main`
- **Created**: 2023-10-30T16:31:45Z
- **Updated**: 2024-01-29T11:27:38Z
- **Merged**: 2023-11-16T10:42:20Z
- **Closed**: 2023-11-16T10:42:20Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: _none_
- **Changed files**: 2   **+366 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
Introduces a new API to expose information about the position of pending workloads in both ClusterQueue and LocalQueue.

#### Which issue(s) this PR fixes:
Part of #168 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-30T16:31:52Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bb3df088ec9eaccef05dca71e241826d54af45c4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6555ed88d35acb0008c37fd6 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-30T16:31:55Z

Hi @PBundyra. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-10-30T16:32:05Z

/assign @mimowo 
/assign @mwielgus

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-31T06:34:53Z

/ok-to-test

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-10-31T09:39:16Z

/assign @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-31T16:54:03Z

Maybe, I can review this KEP in the next week.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-13T22:42:07Z

I came back here today.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-15T17:19:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1300#pullrequestreview-1732534173" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1300#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-16T10:33:42Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-16T10:33:48Z

LGTM label has been added.  <details>Git tree hash: 2d1ff78485b8093d033a843de53e2267bc3a5ade</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-31T08:32:33Z

- **keps/168-2-pending-workloads-visibility/README.md:52** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:03:39Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
```

  ```suggestion
  This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
  ```

- **keps/168-2-pending-workloads-visibility/README.md:66** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:11:19Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
```

  ```suggestion
  Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
  ```
  Just a suggestion, to make it easier to read.

- **keps/168-2-pending-workloads-visibility/README.md:121** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:13:49Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+The only risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
```

  ```suggestion
  One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
  ```
  To avoid the need to change this section when another risk is reported :).

- **keps/168-2-pending-workloads-visibility/README.md:121** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:18:05Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+The only risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
```

  Maybe we can say that this risk is mitigated by the API server P&F mechanism which will queue the incoming requests if there are too many: https://kubernetes.io/docs/concepts/cluster-administration/flow-control/. Additionally, based on the user feedback, we may consider another caching mechanism inside kueue.

- **keps/168-2-pending-workloads-visibility/README.md:125** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:19:57Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+The only risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager using its cache without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). It is an alternative to extending the Kueue's API with CRDs, meaning there will be no additional etcd objects or even the need to use existing ones. It reduces the number of necessary requests to provide consistency across all LocalQueues as discussed before. 
```

  This sentence "It is an alternative to extending the Kueue's API with CRDs, meaning there will be no additional etcd objects or even the need to use existing ones. It reduces the number of necessary requests to provide consistency across all LocalQueues as discussed before. " sounds more like motivation. I don't think it is needed in this section.

- **keps/168-2-pending-workloads-visibility/README.md:145** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:21:01Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+The only risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager using its cache without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). It is an alternative to extending the Kueue's API with CRDs, meaning there will be no additional etcd objects or even the need to use existing ones. It reduces the number of necessary requests to provide consistency across all LocalQueues as discussed before. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to. 
+
+### URIs:
```

  ```suggestion
  ### API endpoints:
  ```

- **keps/168-2-pending-workloads-visibility/README.md:189** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:23:24Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+The only risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager using its cache without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). It is an alternative to extending the Kueue's API with CRDs, meaning there will be no additional etcd objects or even the need to use existing ones. It reduces the number of necessary requests to provide consistency across all LocalQueues as discussed before. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to. 
+
+### URIs:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
+```
+
+### API Objects:
+
+```
+// User-facing representation of a PendingWorkload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueNamestring
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+type PendingWorkloadsSummaryList struct {
+	TypeMeta TypeMeta
+	ListMeta ListMeta
+
+	Items []PendingWorkloadSummaryInClusterQueue
+}
+```
+
+### KueueManager changes
+
+We propose changing KueueManager's cache, so it contains more information about pending workloads. This allows us to expose that information. We'll need cache:
```

  Maybe this is not needed at this point. For example instead of using the cache structure the queue manager could implement `ComputeQueueHead(queueName, maxCount) []*workload.Info`, bypassing the cache. Still, it might be desirable to reuse the cache, but maybe we can defer this for later, based on user / review feedback.

- **keps/168-2-pending-workloads-visibility/README.md:254** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:25:50Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+The only risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager using its cache without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). It is an alternative to extending the Kueue's API with CRDs, meaning there will be no additional etcd objects or even the need to use existing ones. It reduces the number of necessary requests to provide consistency across all LocalQueues as discussed before. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to. 
+
+### URIs:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
+```
+
+### API Objects:
+
+```
+// User-facing representation of a PendingWorkload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueNamestring
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+type PendingWorkloadsSummaryList struct {
+	TypeMeta TypeMeta
+	ListMeta ListMeta
+
+	Items []PendingWorkloadSummaryInClusterQueue
+}
+```
+
+### KueueManager changes
+
+We propose changing KueueManager's cache, so it contains more information about pending workloads. This allows us to expose that information. We'll need cache:
+- workload's priority,
+- workload's LocalQueue name,
+- workload's creation timestamp,
+- relevant metadata.
+
+Workload's position in a ClusterQueue and a LocalQueue will be computed on request.
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Overview
+
+Our main focus is integration tests, as most of the added code is responsible for integrating with the Kueue and RBAC roles.
+
+#### Unit Tests
+
+We plan on adding unit tests that cover getting a list of pending workloads at the KueueManager level.
+
+- `pkg/visibility`: `30 Oct 2023` - `0%`
+
+#### Integration tests
+
+The integration tests should check if:
+- Pending workloads are returned according to their actual status without delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues
```

  Maybe this can be checked as part of the previous point.

- **keps/168-2-pending-workloads-visibility/README.md:256** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:27:03Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+The only risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager using its cache without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). It is an alternative to extending the Kueue's API with CRDs, meaning there will be no additional etcd objects or even the need to use existing ones. It reduces the number of necessary requests to provide consistency across all LocalQueues as discussed before. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to. 
+
+### URIs:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
+```
+
+### API Objects:
+
+```
+// User-facing representation of a PendingWorkload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueNamestring
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+type PendingWorkloadsSummaryList struct {
+	TypeMeta TypeMeta
+	ListMeta ListMeta
+
+	Items []PendingWorkloadSummaryInClusterQueue
+}
+```
+
+### KueueManager changes
+
+We propose changing KueueManager's cache, so it contains more information about pending workloads. This allows us to expose that information. We'll need cache:
+- workload's priority,
+- workload's LocalQueue name,
+- workload's creation timestamp,
+- relevant metadata.
+
+Workload's position in a ClusterQueue and a LocalQueue will be computed on request.
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Overview
+
+Our main focus is integration tests, as most of the added code is responsible for integrating with the Kueue and RBAC roles.
+
+#### Unit Tests
+
+We plan on adding unit tests that cover getting a list of pending workloads at the KueueManager level.
+
+- `pkg/visibility`: `30 Oct 2023` - `0%`
+
+#### Integration tests
+
+The integration tests should check if:
+- Pending workloads are returned according to their actual status without delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues
+- User with only LocalQueue visibility can view only appropriate LocalQueues
+
```

  - User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue

- **keps/168-2-pending-workloads-visibility/README.md:234** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:27:48Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+The only risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager using its cache without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). It is an alternative to extending the Kueue's API with CRDs, meaning there will be no additional etcd objects or even the need to use existing ones. It reduces the number of necessary requests to provide consistency across all LocalQueues as discussed before. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to. 
+
+### URIs:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
+```
+
+### API Objects:
+
+```
+// User-facing representation of a PendingWorkload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueNamestring
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+type PendingWorkloadsSummaryList struct {
+	TypeMeta TypeMeta
+	ListMeta ListMeta
+
+	Items []PendingWorkloadSummaryInClusterQueue
+}
+```
+
+### KueueManager changes
+
+We propose changing KueueManager's cache, so it contains more information about pending workloads. This allows us to expose that information. We'll need cache:
+- workload's priority,
+- workload's LocalQueue name,
+- workload's creation timestamp,
+- relevant metadata.
+
+Workload's position in a ClusterQueue and a LocalQueue will be computed on request.
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Overview
+
+Our main focus is integration tests, as most of the added code is responsible for integrating with the Kueue and RBAC roles.
+
+#### Unit Tests
+
+We plan on adding unit tests that cover getting a list of pending workloads at the KueueManager level.
+
+- `pkg/visibility`: `30 Oct 2023` - `0%`
+
+#### Integration tests
+
+The integration tests should check if:
+- Pending workloads are returned according to their actual status without delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues
+- User with only LocalQueue visibility can view only appropriate LocalQueues
+
+#### E2E tests
+
+We plan on adding sanity e2e tests.
+
+### Graduation Criteria
+
+First iteration (0.6):
+- Release the new API in alpha. This allows us to adjust the API according to users' and reviewers' feedback.
+
+Second iteration (0.7):
+- Release the API in beta and guarantee backwards compatibility,
+- Improve the throttling mechanism if needed.
+
+#### Throttling
```

  I think this is enough to mention just in the risks section.

- **keps/168-2-pending-workloads-visibility/README.md:232** by [@mimowo](https://github.com/mimowo) — 2023-10-31T08:31:26Z
```diff
@@ -0,0 +1,292 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  <!-- - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional) -->
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [URIs](#uris)
+  - [API Objects](#api-objects)
+  - [KueueManager changes](#kueuemanager-changes)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+  - [Alternative within the proposal](#alternatives-within-the-proposal)
+<!-- /toc -->
+
+## Summary
+
+Introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with ```K``` LocalQueues, in some cases (e.g., inserting a high-priority workload), LocalQueuesManager has to send ```K``` requests to all LocalQueues to update their status. Not only does it block all other requests, but it also takes ```K/QPS``` seconds to make data consistent across all LocalQueues. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+The only risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan to introduce throttling so that even with numerous requests, the Kueue Manager remains functional. 
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager using its cache without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). It is an alternative to extending the Kueue's API with CRDs, meaning there will be no additional etcd objects or even the need to use existing ones. It reduces the number of necessary requests to provide consistency across all LocalQueues as discussed before. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to. 
+
+### URIs:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
+```
+
+### API Objects:
+
+```
+// User-facing representation of a PendingWorkload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueNamestring
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+type PendingWorkloadsSummaryList struct {
+	TypeMeta TypeMeta
+	ListMeta ListMeta
+
+	Items []PendingWorkloadSummaryInClusterQueue
+}
+```
+
+### KueueManager changes
+
+We propose changing KueueManager's cache, so it contains more information about pending workloads. This allows us to expose that information. We'll need cache:
+- workload's priority,
+- workload's LocalQueue name,
+- workload's creation timestamp,
+- relevant metadata.
+
+Workload's position in a ClusterQueue and a LocalQueue will be computed on request.
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Overview
+
+Our main focus is integration tests, as most of the added code is responsible for integrating with the Kueue and RBAC roles.
+
+#### Unit Tests
+
+We plan on adding unit tests that cover getting a list of pending workloads at the KueueManager level.
+
+- `pkg/visibility`: `30 Oct 2023` - `0%`
+
+#### Integration tests
+
+The integration tests should check if:
+- Pending workloads are returned according to their actual status without delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues
+- User with only LocalQueue visibility can view only appropriate LocalQueues
+
+#### E2E tests
+
+We plan on adding sanity e2e tests.
+
+### Graduation Criteria
+
+First iteration (0.6):
+- Release the new API in alpha. This allows us to adjust the API according to users' and reviewers' feedback.
+
+Second iteration (0.7):
+- Release the API in beta and guarantee backwards compatibility,
+- Improve the throttling mechanism if needed.
```

  ```suggestion
  - Reconsider to introduce a throttling mechanism based on user and review feedback,
  ```
  Suggestion for clarity. Maybe also reference the risk section here.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2023-10-31T09:55:31Z

- **keps/168-2-pending-workloads-visibility/README.md:90** by [@denkensk](https://github.com/denkensk) — 2023-10-31T09:54:06Z
```diff
@@ -0,0 +1,297 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about single Workload](#fetch-information-about-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+    - [Pagination](#pagination)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
```

  Will the implementation consider the ability to support paged queries? I think this is still pretty important, especially with a lot of workloads.

- **keps/168-2-pending-workloads-visibility/README.md:191** by [@denkensk](https://github.com/denkensk) — 2023-10-31T09:55:16Z
```diff
@@ -0,0 +1,297 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about single Workload](#fetch-information-about-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+    - [Pagination](#pagination)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to. 
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
+```
+
+### API Objects:
+
+```
+// User-facing representation of a PendingWorkload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueNamestring
```

  ```suggestion
  	LocalQueueName string
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-10-31T10:27:28Z

- **keps/168-2-pending-workloads-visibility/README.md:90** by [@PBundyra](https://github.com/PBundyra) — 2023-10-31T10:27:27Z
```diff
@@ -0,0 +1,297 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about single Workload](#fetch-information-about-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+    - [Pagination](#pagination)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
```

  Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also expose the endpoint to fetch information about a single workload, so there is a way to query its position without listing all the remaining pending workloads.
  
  I've updated the non-goals and API Details sections to cover this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-31T15:12:49Z

- **keps/168-2-pending-workloads-visibility/README.md:104** by [@mimowo](https://github.com/mimowo) — 2023-10-31T15:07:39Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
```

  nit: The last point does not seem important enough to mention in this section. 
  
  Also, I would suggest to formulate to capture the main technical aspects of the proposal, without going too much detail like in the "Design Details" section (or something along the lines):
  
  "Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads."
  
  Then, it may also be worth adding something like:
  "The returned information about pending workloads includes all the necessary information relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."

- **keps/168-2-pending-workloads-visibility/kep.yaml:6** by [@mimowo](https://github.com/mimowo) — 2023-10-31T15:12:17Z
```diff
@@ -0,0 +1,38 @@
+title: On-demand Kueue visibility
+kep-number: 168-2
+authors:
+  - "@pbundyra"
+status: implementable
+creation-date: 2023-10-
```

  ```suggestion
  creation-date: 2023-10-30
  ```
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-31T16:31:00Z

LGTM overall. Left some nits, but it is IMO good enough to:
/assign @alculquicondor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-31T16:51:48Z

cc @yaroslava-serdiuk

- **keps/168-2-pending-workloads-visibility/README.md:80** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T16:25:24Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
```

  ```suggestion
  Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
  ```

- **keps/168-2-pending-workloads-visibility/README.md:84** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T16:26:14Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
```

  ```suggestion
  Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
  Users can observe outdated information, which might not be convenient.
  ```

- **keps/168-2-pending-workloads-visibility/README.md:90** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T16:29:05Z
```diff
@@ -0,0 +1,297 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about single Workload](#fetch-information-about-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+    - [Pagination](#pagination)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
```

  Let's clarify what we mean by pagination.
  Let's say I want to list 1000 elements now, and I can only see 100 at a time. Do you keep the 1000 elements somewhere in memory, with some key id, and allow users to query that list in 100 increments? In other words, you get a consistent view if you use the same id?
  
  I don't think we need that. Simply listing elements from position X to Y, at this time, is enough.
  
  Can we do something simple like having 2 query parameters for the API:
  - first position
  - number of elements (and this is some sane value by default: probably around 1000?)
  
  Doing it in the client side (let's say a dashboard) could be wasteful.

- **keps/168-2-pending-workloads-visibility/README.md:157** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T16:39:47Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
```

  that this mean that there will be 2 endpoints for clusterqueue, 2 for localqueue, etc?

- **keps/168-2-pending-workloads-visibility/README.md:172** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T16:44:28Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
```

  ```suggestion
  We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates the following endpoints:
  ```

- **keps/168-2-pending-workloads-visibility/README.md:178** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T16:47:47Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
```

  would something like this be possible?
  
  ```suggestion
  GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads
  ```

- **keps/168-2-pending-workloads-visibility/README.md:188** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T16:49:47Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about a single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
```

  What if the workload is admitted or finished? would you see some information?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-31T16:58:17Z

- **keps/168-2-pending-workloads-visibility/README.md:178** by [@mimowo](https://github.com/mimowo) — 2023-10-31T16:58:16Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
```

  We tried like this, but there seems to be a limitation of the framework, that the path can only have one fixed fragment. The fragment is the key in this map in the prototype: https://github.com/kubernetes-sigs/kueue/blob/5bbb00f8c05ed02a84c11419e6af676222aaf787/pkg/visibility/api/install.go#L45-L47. 
  
  Then, depending on the value returned here: https://github.com/kubernetes-sigs/kueue/blob/5bbb00f8c05ed02a84c11419e6af676222aaf787/pkg/visibility/api/pending_workload_CQ.go#L100 we may yet have the namespace fragment on the path. 
  
  However, I don't think we can have the path you suggest here within the framework.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-31T17:03:13Z

- **keps/168-2-pending-workloads-visibility/README.md:188** by [@mimowo](https://github.com/mimowo) — 2023-10-31T17:03:13Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about a single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
```

  Not at the moment. However, for admitted or finished workloads users don't need extra information that is dynamically changing, such as position in the queue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-31T17:06:58Z

- **keps/168-2-pending-workloads-visibility/README.md:172** by [@mimowo](https://github.com/mimowo) — 2023-10-31T17:06:58Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
```

  Given the technical limitation of the framework, I think we can have either of this paths:
  1. `GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME`
  2. `GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME`` (pending workloads implied)
  3. `GET /apis/visibility.kueue.x-k8s.io/VERSION/pending_workloads_in_cluster_queue/CQ_NAME``
  
  Having `pending-workloads` as a group (option 1.) may seem too specific, but all of them work for me.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-31T17:10:31Z

- **keps/168-2-pending-workloads-visibility/README.md:157** by [@mimowo](https://github.com/mimowo) — 2023-10-31T17:10:31Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
```

  IIUC there is only one endpoint for ClusterQueue, and one for LocalQueue. These translate in an implied way into endpoints for admins and users, but this may depend on RBAC. We may rephrase this sentence to clarify.
  For example: "There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues". Or something along the lines.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-31T17:13:29Z

- **keps/168-2-pending-workloads-visibility/README.md:90** by [@mimowo](https://github.com/mimowo) — 2023-10-31T17:13:29Z
```diff
@@ -0,0 +1,297 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about single Workload](#fetch-information-about-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+    - [Pagination](#pagination)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
```

  We don't know at the moment how to have extra query parameters, so went for the simple proposal without pagination at all. In the prototype we implement it here: https://github.com/kubernetes-sigs/kueue/blob/5bbb00f8c05ed02a84c11419e6af676222aaf787/pkg/visibility/api/pending_workload_CQ.go#L80-L91. However, at this layer we don't already have access to `http.Request` to see the query parameters. It might be possible, but it is not obvious how to get them. AFAIK metrics server also does not do pagination, so we don't have a good example of passing the query parameters.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-31T17:45:54Z

- **keps/168-2-pending-workloads-visibility/README.md:188** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T17:45:53Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about a single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
```

  ok, let's clarify in the KEP.
  Maybe this is an argument for keeping the name of the API group as `pending-workloads.kueue.x-k8s.io`.
  
  Let's go with that and we can iterate in the future as we see the need for new "visibility APIs".

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-31T18:54:33Z

- **keps/168-2-pending-workloads-visibility/README.md:178** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T18:54:32Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
```

  It seems that `/` is treated specially.
  We do this for pods https://github.com/kubernetes/kubernetes/blob/029452198566a41bc39d04a1ec5bad3f37621a1c/pkg/registry/core/rest/storage_core.go#L231

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2023-11-01T03:26:03Z

- **keps/168-2-pending-workloads-visibility/README.md:90** by [@denkensk](https://github.com/denkensk) — 2023-11-01T03:26:03Z
```diff
@@ -0,0 +1,297 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about single Workload](#fetch-information-about-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+    - [Pagination](#pagination)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
```

  > Can we do something simple like having 2 query parameters for the API:
  
  > first position
  > number of elements (and this is some sane value by default: probably around 1000?)
  
  > Doing it in the client side (let's say a dashboard) could be wasteful.
  
  I also think it's better to support on the server side or not support. On the client side is not a good choice.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-01T13:15:55Z

- **keps/168-2-pending-workloads-visibility/README.md:90** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-01T13:15:55Z
```diff
@@ -0,0 +1,297 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about single Workload](#fetch-information-about-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+    - [Pagination](#pagination)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
```

  Cross linking: The Pod log subresource implements query parameters https://github.com/kubernetes/kubernetes/blob/029452198566a41bc39d04a1ec5bad3f37621a1c/pkg/registry/core/pod/rest/log.go#L78
  
  It's a different interface.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-01T13:31:18Z

- **keps/168-2-pending-workloads-visibility/README.md:114** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-01T11:52:19Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
```

  How does the position allow to estimate the arrival time for workload? 
  Wouldn't the accumulated resources of previous pending workloads be more useful value to know?

- **keps/168-2-pending-workloads-visibility/README.md:166** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-01T13:24:39Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
```

  ```suggestion
  Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-06T09:24:22Z

- **keps/168-2-pending-workloads-visibility/README.md:114** by [@mimowo](https://github.com/mimowo) — 2023-11-06T09:24:22Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
```

  This is the starting point. If you can estimate that, on average, the ClusterQueue admits 10jobs per hour, and your workload's position is 5, you can estimate it gets admitted after around 30min. This is a rough estimate, but it is similar to what we have when waiting for a hotline service. 
  
  I think this KEP exposes the information about pending workloads, being a replacement for https://github.com/kubernetes-sigs/kueue/pull/991, but supports a larger scale. Later, we can enrich the returned pending workloads with additional information.
  
  Later. we could have a more sophisticated approach to compute the workload admission time. Another potential approach is to estimate execution time for each job of a given type, based on historical data. Then, a user can estimate the admission time by summing the runtime estimations for all jobs in front of the given job. I suggest there could a new KEP for this which we can prioritize independently.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-09T11:43:31Z

- **keps/168-2-pending-workloads-visibility/README.md:178** by [@PBundyra](https://github.com/PBundyra) — 2023-11-09T11:43:31Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
```

  Thanks for pointing  that out @alculquicondor. Indeed, it's possible to introduce that kind of subresource so I'll change the KEP

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-09T12:10:47Z

- **keps/168-2-pending-workloads-visibility/README.md:188** by [@PBundyra](https://github.com/PBundyra) — 2023-11-09T12:10:47Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQuees the workloads are assigned to.
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we don't want to commit to it, since it would require significant effort. However, it can be implemented on the client side. We also expose [the endpoint to fetch information about a single workload](#fetch-information-about-a-single-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```pending-workloads.kueue.x-k8s.io``` that aggregates the following endpoints:
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME
+```
+
+#### Fetch information about a single Workload
+```
+GET /apis/pending-workloads.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/workloads/WL_NAME
```

  I've clarified that this endpoint refers to a single pending workload

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-09T12:12:29Z

- **keps/168-2-pending-workloads-visibility/README.md:157** by [@PBundyra](https://github.com/PBundyra) — 2023-11-09T12:12:29Z
```diff
@@ -0,0 +1,300 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single Workload](#fetch-information-about-a-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing in order all pending workloads in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination.
+
+## Proposal
+
+- Add new API,
+- Add a new Extension API Server,
+- Modify existing KueueManager's cache to support fetching additional information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints for administrators and regular users. Each endpoint exposes information about a pending workload, such as:
```

  Clarified

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-09T12:21:29Z

- **keps/168-2-pending-workloads-visibility/README.md:90** by [@PBundyra](https://github.com/PBundyra) — 2023-11-09T12:21:29Z
```diff
@@ -0,0 +1,297 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about single Workload](#fetch-information-about-single-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+    - [Pagination](#pagination)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads but for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced. Users can experience delays, which might not be convenient.
+
+
+### Goals
+
+- Support listing in order all pending workloads in a ClusterQueue, no matter the size of the queue, and without delay,
```

  Indeed, implementing a different interface allows us to pass query parameters. Hence, following @alculquicondor proposal, we will enable users to fetch pending workloads from position X to Y.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-09T15:16:50Z

- **keps/168-2-pending-workloads-visibility/README.md:103** by [@mimowo](https://github.com/mimowo) — 2023-11-09T15:10:08Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
```

  ```suggestion
  Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints:
  1. to list the pending workloads in ClusterQueue
  2. list the pending workloads in LocalQueue, and
  3. get a specific pending workload.
  ```

- **keps/168-2-pending-workloads-visibility/README.md:104** by [@mimowo](https://github.com/mimowo) — 2023-11-09T15:11:17Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
```

  ```suggestion
  In order to expose the API endpoints we introduce a new Extension API server.
  ```

- **keps/168-2-pending-workloads-visibility/README.md:251** by [@mimowo](https://github.com/mimowo) — 2023-11-09T15:14:47Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workload/WL_NAME
+```
+
+### API Objects:
+
+```
+// PendingWorkloadSummary is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueName string
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+// PendingWorkloadSummaryList contains a list of pending workloads in the context
+// of the query (within LocalQueue or ClusterQueue).
+type PendingWorkloadsSummaryList struct {
+	TypeMeta TypeMeta
+	ListMeta ListMeta
+
+	Items []PendingWorkloadSummaryInClusterQueue
+}
+```
+
+### Future extensions
+
+The introduced API uses mechanism of subresources. It means, that in the future it can be easily extended by adding additional endpoints related e.g. to admitted workloads. Potentially the endpoint could look like this:
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/admitted_workloads?offset=0&limit=1000
+```
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Overview
+
+Our main focus is integration tests, as most of the added code is responsible for integrating with the Kueue and RBAC roles.
+
+#### Unit Tests
+
+We plan on adding unit tests that cover getting a list of pending workloads at the KueueManager level.
+
+- `pkg/visibility`: `30 Oct 2023` - `0%`
+
+#### Integration tests
+
+Integration tests should check if our server work correctly according to the assumptions we mentioned:
+- Pending workloads are returned according to their actual status without delay.
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue
+
+#### E2E tests
+
+We plan on adding sanity e2e tests.
```

  Add that we also want to use e2e tests for RBAC. You may list some of the critical scenarios:
  - clusters queues can only be accessed by admin users
  - local queues can be accessed by only users with the visibility to the corresponding namespaces

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-09T16:29:49Z

- **keps/168-2-pending-workloads-visibility/README.md:171** by [@mimowo](https://github.com/mimowo) — 2023-11-09T16:29:31Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
```

  Update it, since we actually now figured out how to support pagination server side :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-13T21:16:50Z

- **keps/168-2-pending-workloads-visibility/README.md:190** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-13T19:54:17Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workload/WL_NAME
```

  ```suggestion
  GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workloads/WL_NAME
  ```
  
  Although it would be nice if in the future it is just `workload`, so we support both. But that's for another KEP.

- **keps/168-2-pending-workloads-visibility/README.md:230** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-13T20:19:36Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workload/WL_NAME
+```
+
+### API Objects:
+
+```
+// PendingWorkloadSummary is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueName string
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+// PendingWorkloadSummaryList contains a list of pending workloads in the context
+// of the query (within LocalQueue or ClusterQueue).
+type PendingWorkloadsSummaryList struct {
+	TypeMeta TypeMeta
+	ListMeta ListMeta
+
+	Items []PendingWorkloadSummaryInClusterQueue
+}
+```
+
+### Future extensions
+
+The introduced API uses mechanism of subresources. It means, that in the future it can be easily extended by adding additional endpoints related e.g. to admitted workloads. Potentially the endpoint could look like this:
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/admitted_workloads?offset=0&limit=1000
```

  For now, if the user doesn't set any limits, we should still cap it. 1000 sounds about right.

- **keps/168-2-pending-workloads-visibility/README.md:197** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-13T21:15:55Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workload/WL_NAME
+```
+
+### API Objects:
+
+```
+// PendingWorkloadSummary is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
```

  ```suggestion
  type PendingWorkload struct {
  ```

- **keps/168-2-pending-workloads-visibility/README.md:211** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-13T21:16:19Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workload/WL_NAME
+```
+
+### API Objects:
+
+```
+// PendingWorkloadSummary is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueName string
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
```

  Can we actually get this information? Otherwise, I wouldn't prioritize it.

- **keps/168-2-pending-workloads-visibility/README.md:209** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-13T21:16:33Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workload/WL_NAME
+```
+
+### API Objects:
+
+```
+// PendingWorkloadSummary is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueName string
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+// PendingWorkloadSummaryList contains a list of pending workloads in the context
+// of the query (within LocalQueue or ClusterQueue).
+type PendingWorkloadsSummaryList struct {
```

  ```suggestion
  type PendingWorkloadsSummary struct {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-14T08:39:32Z

- **keps/168-2-pending-workloads-visibility/README.md:211** by [@mimowo](https://github.com/mimowo) — 2023-11-14T08:39:32Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workload/WL_NAME
+```
+
+### API Objects:
+
+```
+// PendingWorkloadSummary is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueName string
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
```

  We don't have it at hand, but the computation is rather cheap - when we iterate over the snapshoted data for cluster queue, we increment position counters per local queue. Still, I agree it does not seem like a priority. I think we can do this later if needed for dashboards.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-14T08:58:18Z

- **keps/168-2-pending-workloads-visibility/README.md:66** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T06:52:21Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
```

  ```suggestion
  As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility), there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
  ```

- **keps/168-2-pending-workloads-visibility/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T07:01:34Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
```

  > Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS.
  
  This might be able to avoid by creating a client for clusterQueue visibility separate from other processes.

- **keps/168-2-pending-workloads-visibility/README.md:97** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T07:06:25Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
```

  Sorry, what does mean "ETA"?  Estimated Time Of Arrival?

- **keps/168-2-pending-workloads-visibility/README.md:135** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T07:30:36Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
```

  I'm not familiar with the api-server aggregation. Can we implement pagination as similar to the generic gRPC server?

- **keps/168-2-pending-workloads-visibility/README.md:135** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T07:42:53Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
```

  > The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/).
  
  I think we should provide FlowScheme and PriorityLevelConfiguration for this feature to allow admins to tune API priorities easily. Then, we should say in the document that batch admins can modify PriorityLevelConfiguration based on Cluster size.

- **keps/168-2-pending-workloads-visibility/README.md:173** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T07:54:30Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
```

  Can we set the limitation on this query param to decrease DDoS attack affections?

- **keps/168-2-pending-workloads-visibility/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T08:00:36Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
```

  How can we access these endpoints? IIUC, we can not access these via the kubectl command.
  Do you have any plans to provide client libraries/commands?

- **keps/168-2-pending-workloads-visibility/README.md:276** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T08:28:16Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workload/WL_NAME
+```
+
+### API Objects:
+
+```
+// PendingWorkloadSummary is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueName string
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+// PendingWorkloadSummaryList contains a list of pending workloads in the context
+// of the query (within LocalQueue or ClusterQueue).
+type PendingWorkloadsSummaryList struct {
+	TypeMeta TypeMeta
+	ListMeta ListMeta
+
+	Items []PendingWorkloadSummaryInClusterQueue
+}
+```
+
+### Future extensions
+
+The introduced API uses mechanism of subresources. It means, that in the future it can be easily extended by adding additional endpoints related e.g. to admitted workloads. Potentially the endpoint could look like this:
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/admitted_workloads?offset=0&limit=1000
+```
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Overview
+
+Our main focus is integration tests, as most of the added code is responsible for integrating with the Kueue and RBAC roles.
+
+#### Unit Tests
+
+We plan on adding unit tests that cover getting a list of pending workloads at the KueueManager level.
+
+- `pkg/visibility`: `30 Oct 2023` - `0%`
+
+#### Integration tests
+
+Integration tests should check if our server work correctly according to the assumptions we mentioned:
+- Pending workloads are returned according to their actual status without delay.
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue
+
+#### E2E tests
+
+We plan on adding sanity e2e tests.
+
+### Graduation Criteria
+
+First iteration (0.6):
+- Release the new API in alpha. This allows us to adjust the API according to users' and reviewers' feedback.
+
+Second iteration (0.7):
+- Release the API in beta and guarantee backwards compatibility,
+- Reconsider to introduce a throttling mechanism based on user and review feedback,
+
```

  Can we add the criteria for the stable (GA)?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-14T09:39:44Z

- **keps/168-2-pending-workloads-visibility/README.md:177** by [@PBundyra](https://github.com/PBundyra) — 2023-11-14T09:39:44Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
```

  Good question. Actually, it is possible to access these via the kubectl command using `--raw` flag and passing the URI. I'll clarify that.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-14T09:42:30Z

- **keps/168-2-pending-workloads-visibility/README.md:97** by [@PBundyra](https://github.com/PBundyra) — 2023-11-14T09:42:30Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
```

  Yes, I've added clarification to that

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-14T11:05:05Z

- **keps/168-2-pending-workloads-visibility/README.md:76** by [@PBundyra](https://github.com/PBundyra) — 2023-11-14T11:05:05Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
```

  Can you please elaborate on that? In the old approach of keeping the list of pending workloads inside of LocalQueue's status we still need to send a number of requests to update positions in all of the LocalQueues pointing to the same ClusterQueue, for which the workload was admitted.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-14T13:51:26Z

- **keps/168-2-pending-workloads-visibility/README.md:173** by [@PBundyra](https://github.com/PBundyra) — 2023-11-14T13:51:26Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
```

  We don't have hard, experimental data, but we believe the most computationally heavy operation is obtaining a read lock on Kueue and copying the heap, which are independent of the limit parameter. Hence, we believe limiting this query parameter won't have a significant impact on DDoS attacks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-14T13:56:12Z

- **keps/168-2-pending-workloads-visibility/README.md:135** by [@PBundyra](https://github.com/PBundyra) — 2023-11-14T13:56:12Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
```

  This proposal supports pagination using query parameters. I've added a sentence clarifying it.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-14T13:56:37Z

- **keps/168-2-pending-workloads-visibility/README.md:276** by [@PBundyra](https://github.com/PBundyra) — 2023-11-14T13:56:37Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workload/WL_NAME
+```
+
+### API Objects:
+
+```
+// PendingWorkloadSummary is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkloadSummary struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueName string
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+// PendingWorkloadSummaryList contains a list of pending workloads in the context
+// of the query (within LocalQueue or ClusterQueue).
+type PendingWorkloadsSummaryList struct {
+	TypeMeta TypeMeta
+	ListMeta ListMeta
+
+	Items []PendingWorkloadSummaryInClusterQueue
+}
+```
+
+### Future extensions
+
+The introduced API uses mechanism of subresources. It means, that in the future it can be easily extended by adding additional endpoints related e.g. to admitted workloads. Potentially the endpoint could look like this:
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/admitted_workloads?offset=0&limit=1000
+```
+
+### Test Plan
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Overview
+
+Our main focus is integration tests, as most of the added code is responsible for integrating with the Kueue and RBAC roles.
+
+#### Unit Tests
+
+We plan on adding unit tests that cover getting a list of pending workloads at the KueueManager level.
+
+- `pkg/visibility`: `30 Oct 2023` - `0%`
+
+#### Integration tests
+
+Integration tests should check if our server work correctly according to the assumptions we mentioned:
+- Pending workloads are returned according to their actual status without delay.
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue
+
+#### E2E tests
+
+We plan on adding sanity e2e tests.
+
+### Graduation Criteria
+
+First iteration (0.6):
+- Release the new API in alpha. This allows us to adjust the API according to users' and reviewers' feedback.
+
+Second iteration (0.7):
+- Release the API in beta and guarantee backwards compatibility,
+- Reconsider to introduce a throttling mechanism based on user and review feedback,
+
```

  Good point, I've added it

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-14T14:25:55Z

- **keps/168-2-pending-workloads-visibility/README.md:177** by [@PBundyra](https://github.com/PBundyra) — 2023-11-14T14:25:55Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
```

  There is also an ongoing effort to create [Kueue CLI](https://github.com/kubernetes-sigs/kueue/pull/1325). We aim to provide similar behaviour to metrics server with `kubectl top` command

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-14T15:07:53Z

- **keps/168-2-pending-workloads-visibility/README.md:135** by [@PBundyra](https://github.com/PBundyra) — 2023-11-14T15:07:53Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
```

  It's definitely worth considering, but I would focus on that in the next release. I'll add it to the graduation criteria section.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-14T15:34:15Z

- **keps/168-2-pending-workloads-visibility/README.md:217** by [@PBundyra](https://github.com/PBundyra) — 2023-11-14T15:34:14Z
```diff
@@ -209,7 +209,7 @@ type PendingWorkload struct {
 
 // PendingWorkloadSummaryList contains a list of pending workloads in the context
 // of the query (within LocalQueue or ClusterQueue).
-type PendingWorkloadsSummaryList struct {
+type PendingWorkloadsSummary struct {
```

  @alculquicondor 
  I think it's a better idea to change it into `PendingWorkloadList` so it's similar to what we already have in Kueue:
  https://github.com/kubernetes-sigs/kueue/blob/cf25c71bef51fa6d0880432352dd92da6f3a8ea6/apis/kueue/v1beta1/workload_types.go#L293

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-14T17:30:55Z

- **keps/168-2-pending-workloads-visibility/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T17:30:55Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
```

  > In the old approach of keeping the list of pending workloads inside of LocalQueue's status we still need to send a number of requests to update positions in all of the LocalQueues pointing to the same ClusterQueue, for which the workload was admitted.
  
  Yes, that's right.
  IIRC, we had discussions about the concerns in the following thread:
  
  https://github.com/kubernetes-sigs/kueue/pull/991#discussion_r1284352679
  
  At that time, we thought we could avoid the concerns using a separate client.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-14T17:36:07Z

- **keps/168-2-pending-workloads-visibility/README.md:135** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T17:36:07Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
```

  Ah, that's right. We can provide the pagination mechanism using `offset` and `limit`.
  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-14T17:37:29Z

- **keps/168-2-pending-workloads-visibility/README.md:135** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T17:37:29Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-14T17:40:25Z

- **keps/168-2-pending-workloads-visibility/README.md:173** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T17:40:24Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
```

  That sounds reasonable. If we find unexpected issues, we can set limitations on these query parameters, later.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-14T17:47:17Z

- **keps/168-2-pending-workloads-visibility/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-14T17:47:17Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
```

  > Good question. Actually, it is possible to access these via the kubectl command using --raw flag and passing the URI. I'll clarify that.
  
  Oh, it's great to see. Thanks for clarifying.
  
  > There is also an ongoing effort to create https://github.com/kubernetes-sigs/kueue/pull/1325. We aim to provide similar behaviour to metrics server with kubectl top command
  
  I'm wondering if we should provide CRUD golang libraries (probably, it's ok with only READ) so that external systems could call these endpoints.
  
  Also, we can implement the Kueue CLI using the golang library.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-15T08:31:48Z

- **keps/168-2-pending-workloads-visibility/README.md:177** by [@PBundyra](https://github.com/PBundyra) — 2023-11-15T08:31:47Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
```

  We'll provide a client generated with `k8s.io/code-generator` similarly to the core Kueue API. I'll mention that in the KEP

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-15T09:00:03Z

- **keps/168-2-pending-workloads-visibility/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-15T09:00:03Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+- Support pagination with a consistent view of a specific state using a key id.
+
+## Proposal
+- Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints: (1) to list the pending workloads in ClusterQueue, (2) list the pending workloads in LocalQueue, and (3) get a specific pending workload."
+- Add a new Extension API server to expose on-demand endpoints for fetching information about pending workloads.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Since the K8s Aggregation Layer does not support pagination out of the box, we do not want to commit to it since it would require significant effort. However, it can be implemented on the client side. We also provided above-mentioned query parameters that can help in cases where one does not want to fetch the whole Cluster/LocalQueue. Additionally, we expose [the endpoint to fetch information about a single pending workload](#fetch-information-about-a-single-pending-workload), so there is a way to query its position without listing all the remaining pending workloads.
+
+### API endpoints:
```

  Oh, it sounds good. 
  Thanks for the clarifications. Because I'm unfamiliar with API aggregation, your response would always be helpful to me.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-15T09:30:28Z

- **keps/168-2-pending-workloads-visibility/README.md:217** by [@mimowo](https://github.com/mimowo) — 2023-11-15T09:30:27Z
```diff
@@ -209,7 +209,7 @@ type PendingWorkload struct {
 
 // PendingWorkloadSummaryList contains a list of pending workloads in the context
 // of the query (within LocalQueue or ClusterQueue).
-type PendingWorkloadsSummaryList struct {
+type PendingWorkloadsSummary struct {
```

  I'm fine with `PendingWorkloadsSummary` and `PendingWorkloadList`, slightly leaning towards `PendingWorkloadList` as the `List` sufix seems like a convention.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-15T13:50:17Z

- **keps/168-2-pending-workloads-visibility/README.md:217** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-15T13:50:17Z
```diff
@@ -209,7 +209,7 @@ type PendingWorkload struct {
 
 // PendingWorkloadSummaryList contains a list of pending workloads in the context
 // of the query (within LocalQueue or ClusterQueue).
-type PendingWorkloadsSummaryList struct {
+type PendingWorkloadsSummary struct {
```

  It is a convention for list endpoints. This is not a list endpoint.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-15T15:24:46Z

- **keps/168-2-pending-workloads-visibility/README.md:217** by [@mimowo](https://github.com/mimowo) — 2023-11-15T15:24:46Z
```diff
@@ -209,7 +209,7 @@ type PendingWorkload struct {
 
 // PendingWorkloadSummaryList contains a list of pending workloads in the context
 // of the query (within LocalQueue or ClusterQueue).
-type PendingWorkloadsSummaryList struct {
+type PendingWorkloadsSummary struct {
```

  Makes sense, let's go with `PendingWorkloadsSummary` for the Get endpoint.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-15T15:42:51Z

- **keps/168-2-pending-workloads-visibility/README.md:184** by [@mimowo](https://github.com/mimowo) — 2023-11-15T15:42:47Z
```diff
@@ -0,0 +1,328 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Future extensions](#future-extensions)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility), there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA (Estimated Time of Arrival) for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+
+## Proposal
+Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints:
+1. to list the pending workloads in ClusterQueue
+2. list the pending workloads in LocalQueue, and
+3. get a specific pending workload.
+In order to expose the API endpoints we introduce a new Extension API server.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern. This is also mitigated by the [query parameters](#api-details) introduced below.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Thanks to these parameters our server also support pagination.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pending_workloads?offset=0&limit=1000
```

  ```suggestion
  GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pendingworkloads?offset=0&limit=1000
  ```
  To synchronize with the decision made on the PR.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-15T17:19:31Z

/approve
I'll leave lgtm to @mimowo

- **keps/168-2-pending-workloads-visibility/README.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-15T17:16:42Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
```

  Yes, a separate client to do LocalQueue updates could be an approach, but I think the API extensions is overall more flexible.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-15T17:20:20Z

- **keps/168-2-pending-workloads-visibility/kep.yaml:33** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-15T17:20:20Z
```diff
@@ -0,0 +1,38 @@
+title: On-demand Kueue visibility
+kep-number: 168-2
+authors:
+  - "@pbundyra"
+status: implementable
+creation-date: 2023-10-30
+reviewers:
+  - "@mwielgus"
+  - "@mimowo"
+approvers:
+  - "@alculquicondor"
+
+replaces:
+  - "/keps/168-pending-workloads-visibility"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.6"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.6"
+  beta: "v0.7"
+  stable: 
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueVisibilityOnDemand
```

  ```suggestion
    - name: VisibilityOnDemand
  ```
  
  No need to mention Kueue

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-15T17:20:33Z

Also please squash

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-15T17:26:13Z

- **keps/168-2-pending-workloads-visibility/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-15T17:26:12Z
```diff
@@ -0,0 +1,313 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility),  there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
```

  > Yes, a separate client to do LocalQueue updates could be an approach, but I think the API extensions is overall more flexible.
  
  Yes, that's right. I also think that we should introduce this API extension. I wanted to say that mentioning `separate client` would be worth it here.
  
  Maybe, we can say like "Although we can use a client separate from a default one, this way can not completely resolve scalability issues".

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-15T17:33:24Z

Other than https://github.com/kubernetes-sigs/kueue/pull/1300#discussion_r1394526875, LGTM.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-16T09:34:23Z

- **keps/168-2-pending-workloads-visibility/README.md:189** by [@mimowo](https://github.com/mimowo) — 2023-11-16T09:33:55Z
```diff
@@ -0,0 +1,328 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Future extensions](#future-extensions)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility), there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA (Estimated Time of Arrival) for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+
+## Proposal
+Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints:
+1. to list the pending workloads in ClusterQueue
+2. list the pending workloads in LocalQueue, and
+3. get a specific pending workload.
+In order to expose the API endpoints we introduce a new Extension API server.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern. This is also mitigated by the [query parameters](#api-details) introduced below.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Thanks to these parameters our server also support pagination.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pendingworkloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
```

  ```suggestion
  GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pendingworkloads?offset=0&limit=1000
  ```

- **keps/168-2-pending-workloads-visibility/README.md:194** by [@mimowo](https://github.com/mimowo) — 2023-11-16T09:34:03Z
```diff
@@ -0,0 +1,328 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List all pending workloads in ClusterQueue](#list-all-pending-workloads-in-clusterqueue)
+    - [List all pending workloads in LocalQueue](#list-all-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Future extensions](#future-extensions)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list all pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility), there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. 
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA (Estimated Time of Arrival) for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+
+## Proposal
+Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints:
+1. to list the pending workloads in ClusterQueue
+2. list the pending workloads in LocalQueue, and
+3. get a specific pending workload.
+In order to expose the API endpoints we introduce a new Extension API server.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern. This is also mitigated by the [query parameters](#api-details) introduced below.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Thanks to these parameters our server also support pagination.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List all pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pendingworkloads?offset=0&limit=1000
+```
+
+#### List all pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pending_workloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pending_workloads/WL_NAME
```

  ```suggestion
  GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pendingworkloads/WL_NAME
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-16T09:55:09Z

- **keps/168-2-pending-workloads-visibility/README.md:217** by [@PBundyra](https://github.com/PBundyra) — 2023-11-16T09:55:09Z
```diff
@@ -209,7 +209,7 @@ type PendingWorkload struct {
 
 // PendingWorkloadSummaryList contains a list of pending workloads in the context
 // of the query (within LocalQueue or ClusterQueue).
-type PendingWorkloadsSummaryList struct {
+type PendingWorkloadsSummary struct {
```

  Thanks for the clarification.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-16T10:20:11Z

LGTM, Just some nits

- **keps/168-2-pending-workloads-visibility/README.md:217** by [@mimowo](https://github.com/mimowo) — 2023-11-16T10:17:02Z
```diff
@@ -0,0 +1,328 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List pending workloads in ClusterQueue](#list-pending-workloads-in-clusterqueue)
+    - [List pending workloads in LocalQueue](#list-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Future extensions](#future-extensions)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility), there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. Although we can use a client separate from a default one, it would not not completely resolve all scalability issues
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA (Estimated Time of Arrival) for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+
+## Proposal
+Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints:
+1. to list the pending workloads in ClusterQueue
+2. list the pending workloads in LocalQueue, and
+3. get a specific pending workload.
+In order to expose the API endpoints we introduce a new Extension API server.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern. This is also mitigated by the [query parameters](#api-details) introduced below.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Thanks to these parameters our server also support pagination.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pendingworkloads?offset=0&limit=1000
+```
+
+#### List pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pendingworkloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pendingworkloads/WL_NAME
+```
+
+Those endpoints can be accessed using `kubectl get --raw <ENDPOINT_PATH>` command.
+
+Another way to access API is to use a client generated with `k8s.io/code-generator`, similarly to the core Kueue API.
+
+### API Objects:
+
+```
+// PendingWorkload is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkload struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueName string
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+// PendingWorkloadSummary contains a list of pending workloads in the context
+// of the query (within LocalQueue or ClusterQueue).
+type PendingWorkloadSummary struct {
```

  ```suggestion
  type PendingWorkloadsSummary struct {
  ```

- **keps/168-2-pending-workloads-visibility/README.md:217** by [@mimowo](https://github.com/mimowo) — 2023-11-16T10:17:07Z
```diff
@@ -0,0 +1,328 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List pending workloads in ClusterQueue](#list-pending-workloads-in-clusterqueue)
+    - [List pending workloads in LocalQueue](#list-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Future extensions](#future-extensions)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility), there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. Although we can use a client separate from a default one, it would not not completely resolve all scalability issues
+
+#### Expanding API in the future
+
+Moreover, there are some functional issues with the current approach. It does not expose any information about pending workloads except for ```name```, ```namespace```, and position in a queue (by listing workloads in order). Adding new fields would result in a decrease in the potential maximum number of fetched pending workloads, caused by the etcd object's size.
+
+#### Delay
+
+Additionally, in the previous proposal, Kueue updated the most prioritized workloads every 5 seconds. It is configurable, but since computing the most prioritized workloads can be expensive, it cannot be significantly reduced.
+Users can observe outdated information, which might not be convenient.
+
+
+### Goals
+
+- Support listing pending workloads on positions from X to Y in a ClusterQueue, no matter the size of the queue, and without delay,
+- Support listing pending workloads on positions from X to Y in a LocalQueue, no matter the size of the queue, and without delay,
+- Support fetching information about a specific pending workload, no matter its position in the queue, and without delay,
+- Provide consitent data across all the LocalQueues without hitting `QPS`.
+
+### Non-Goals
+
+- Provide ETA (Estimated Time of Arrival) for a workload,
+- Provide information on whether a workload is admissible,
+- Provide information about the requested resource for a workload,
+
+## Proposal
+Add new API exposing information about pending workloads relevant for their position in the queue, along with the position itself. There are three such endpoints:
+1. to list the pending workloads in ClusterQueue
+2. list the pending workloads in LocalQueue, and
+3. get a specific pending workload.
+In order to expose the API endpoints we introduce a new Extension API server.
+
+### User Stories
+
+#### Story 1 
+
+As a user of Kueue with LocalQueue visibility only, I would like to know the position in the ClusterQueue of a workload that I've just submitted, no matter how big the queue is. Knowing the position and assuming stable velocity in the ClusterQueue, would allow me to estimate the arrival time of my workload.
+
+Provided by the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue) and the [Workload endpoint](#fetch-information-about-a-single-pending-workload).
+
+#### Story 2 
+
+As an administrator of Kueue with ClusterQueue visibility, I would like to be able to check directly and compare the positions of pending workloads in the queue, no matter the size of it. It is important that data across all LocalQueue is consistent, and no two workloads have the same position in ClusterQueue. This will help me answer users' questions about their workloads.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue).
+
+#### Story 3 
+
+As a developer who uses Kueue, I would like to be able to monitor the state of my ClusterQueue/LocalQueue using dashboards. I need a mechanism that allows me to easily build it.
+
+Provided by the [ClusterQueue endpoint](#list-all-pending-workloads-in-clusterqueue) and the [LocalQueue endpoint](#list-all-pending-workloads-in-localqueue).
+
+
+### Risks and Mitigations
+
+#### DDoS
+
+One risk we foresee is that the server may be exposed to DDoS attacks. A potential attacker may flood the server with requests, which will result in constantly locking the Kueue Manager. To mitigate this risk, we plan on relying on throttling, so that even with numerous requests, the Kueue Manager remains functional. The first approach we propose is to use [API server P&F mechanism](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/). Additionally, based on the user feedback, we may consider another caching mechanism inside Kueue.
+
+#### Payload size
+
+Another risk we took into account is that the payload size would be too large in the case of 100k pending workloads. However, in the worst case scenario (which we foresee as a rather unrealistic one, since it would mean all string fields would be filled with 256 chars) its size is about 1,4kB. Even with 100k pending workloads, it takes 140 MB, which is still a reasonable number compared to `metrics-server's` payloads. Hence, we believe it should not be a concern. This is also mitigated by the [query parameters](#api-details) introduced below.
+
+## Design Details
+
+The proposal introduces a new server running on the Kueue's pod. It computes the current state of KueueManager without any additional request overhead. The server uses the [K8s API Aggregation Layer](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/) mechanism. The same mechanism is used by the [metrics-server](https://github.com/kubernetes-sigs/metrics-server). There will be no additional etcd objects or need to use existing ones. No additional requests to sync information across LocalQueues will be required. 
+
+Similarly to the ```metrics-server``` the server will be implemented with the [apiserver library](https://github.com/kubernetes/apiserver), which provides authentication and authorization.
+
+All computation will be done on-demand without additional reconcile loops.
+
+The server provides that:
+- Pending workloads are returned according to their actual status without significant delay. This includes adding and removing/admitting new workloads with various priorities,
+- Adding workloads to one LocalQueues results in position changes for workloads submitted to other LocalQueues,
+- Data is consistent across all LocalQueues,
+- User with only LocalQueue visibility cannot access the list of pending workloads for ClusterQueue.
+
+### API Details
+
+We introduce a new API that will extend the existing one.
+
+There will be separate endpoints exposing the information about pending workloads for LocalQueues, and ClusterQueues. Each endpoint exposes information about a pending workload, such as:
+- workload's position in a ClusterQueue,
+- workload's position in a LocalQueue,
+- workload's priority,
+- creation timestamp.
+
+The API does not allow for the modification of any objects.
+
+Regular users will have access only to the LocalQueues they are assigned to. However, they will be able to fetch information about the global position of a workload in a ClusterQueue, without any details about workloads in different LocalQueues. 
+
+Administrators will have access to all the data at the ClusterQueue level. They will be able to view all the workloads, no matter the LocalQueues the workloads are assigned to.
+
+The API also allows user to fetch information about part of the Cluster/LocalQueue from position X to Y. There are two query parameters to do so:
+- `offset` indicates position of the first fetched workload - default: `0`
+- `limit` indicates max number of workloads to be fetched - default: `1000`
+
+Thanks to these parameters our server also support pagination.
+
+### API endpoints:
+
+We introduce a new API group ```visibility.kueue.x-k8s.io``` that aggregates following endpoints: 
+
+#### List pending workloads in ClusterQueue
+
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/clusterqueues/CQ_NAME/pendingworkloads?offset=0&limit=1000
+```
+
+#### List pending workloads in LocalQueue
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/LQ_NAMESPACE/localqueues/LQ_NAME/pendingworkloads?offset=0&limit=1000
+```
+
+#### Fetch information about a single pending Workload
+```
+GET /apis/visibility.kueue.x-k8s.io/VERSION/namespaces/WL_NAMESPACE/pendingworkloads/WL_NAME
+```
+
+Those endpoints can be accessed using `kubectl get --raw <ENDPOINT_PATH>` command.
+
+Another way to access API is to use a client generated with `k8s.io/code-generator`, similarly to the core Kueue API.
+
+### API Objects:
+
+```
+// PendingWorkload is a user-facing representation of a pending workload in both LocalQueues and ClusterQueue that summarizes neccessary information from the admission order perspective
+type PendingWorkload struct {
+	TypeMeta TypeMeta
+	ObjectMeta ObjectMeta
+
+	LocalQueueName string
+	PositionInClusterQueue int32
+	PositionInLocalQueue int32
+	Priority int32
+}
+
+// PendingWorkloadSummary contains a list of pending workloads in the context
+// of the query (within LocalQueue or ClusterQueue).
+type PendingWorkloadSummary struct {
```

  nit

- **keps/168-2-pending-workloads-visibility/README.md:78** by [@mimowo](https://github.com/mimowo) — 2023-11-16T10:17:35Z
```diff
@@ -0,0 +1,328 @@
+# KEP-168-2: Pending-workloads-visibility
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Cons of the current solution](#cons-of-the-current-solution)
+    - [Size of the queue](#size-of-the-queue)
+    - [Consistency across all LocalQueues](#consistency-across-all-localqueues)
+    - [Expanding API in the future](#expanding-api-in-the-future)
+    - [Delay](#delay)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [DDoS](#ddos)
+    - [Payload size](#payload-size)
+- [Design Details](#design-details)
+  - [API Details](#api-details)
+  - [API endpoints:](#api-endpoints)
+    - [List pending workloads in ClusterQueue](#list-pending-workloads-in-clusterqueue)
+    - [List pending workloads in LocalQueue](#list-pending-workloads-in-localqueue)
+    - [Fetch information about a single pending Workload](#fetch-information-about-a-single-pending-workload)
+  - [API Objects:](#api-objects)
+  - [Future extensions](#future-extensions)
+  - [Test Plan](#test-plan)
+    - [Overview](#overview)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Alternative approaches](#alternative-approaches)
+    - [Approach described in <a href="https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility">KEP#168</a>](#approach-described-in-kep168)
+    - [Extend API using CRDs](#extend-api-using-crds)
+  - [Alternatives within the proposal](#alternatives-within-the-proposal)
+    - [apiserver-builder library](#apiserver-builder-library)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes to introduce a new API that allows users to on-demand fetch information about pending workloads in both ClusterQueue and LocalQueue. Users will be able to look up the position of a specific workload in both types of queues and list pending workloads in a specific queue.
+
+## Motivation
+
+As presented in [KEP#168](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-pending-workloads-visibility), there is currently a proposal for a mechanism that supports fetching the order of pending workloads, but it comes with a lot of cons. This proposal addresses all of those problems.
+
+### Cons of the current solution
+
+#### Size of the queue
+
+There are a few scalability concerns. The first one is that the number of fetched pending workloads is limited by the etcd object's size limit. By default, a user is able to fetch only 10 workloads stored at the head of a queue. This number can be increased up to 4000, but comes with a performance loss.
+
+#### Consistency across all LocalQueues
+
+Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. Although we can use a client separate from a default one, it would not not completely resolve all scalability issues
```

  ```suggestion
  Another scalability drawback is that in a Kueue setup with a lot of LocalQueues it is very likely to hit the Kueue QPS. Assuming Kueue setup with multiple LocalQueues pointing to the same ClusterQueue, Kueue needs to send updates to all LocalQueues to update their status, in order to keep the workload positional information up-to-date. This consumes `QPS` which can lead to blocking other requests. Although we can use a client separate from a default one, it would not not completely resolve all scalability issues.
  ```
  nit
