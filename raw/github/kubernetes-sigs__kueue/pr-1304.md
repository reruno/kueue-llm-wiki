# PR #1304: Adding toleration to the job doesn't trigger workload change

**Summary**: Adding toleration to the job doesn't trigger workload change

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1304

**Last updated**: 2024-03-12T08:55:57Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@stuton](https://github.com/stuton)
- **Branch**: `update-workload-when-add-toleration` → `main`
- **Created**: 2023-11-02T14:13:59Z
- **Updated**: 2024-03-12T08:55:57Z
- **Merged**: 2024-01-16T05:49:29Z
- **Closed**: 2024-01-16T05:49:29Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 7   **+377 / -76**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1264

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Changing tolerations in an inadmissible job triggers an admission retry with the updated tolerations.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-02T14:14:02Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-02T14:14:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ed5c81f04845465b80249e7f90ac452816d94602 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65a613e39f906b0008178c82 |

### Comment by [@trasc](https://github.com/trasc) — 2023-12-15T14:46:24Z

> And another one where if you change the toleration for a Job that is admitted, the Workload gets evicted.

Is this a very common use-case, I think is easier to make the tolerations immutable while the job is not suspended.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-15T15:18:45Z

> Is this a very common use-case, I think is easier to make the tolerations immutable while the job is not suspended.

Tolerations are already immutable for k8s Jobs when not suspended https://kubernetes.io/docs/concepts/workloads/controllers/job/#mutable-scheduling-directives

But that's not true for all CRDs. We might be able to add the restriction as webhooks for jobs we have integrations for, but we can't control other implementations. So worth having the safe guard in the reconciler.

### Comment by [@trasc](https://github.com/trasc) — 2023-12-19T14:27:07Z

> Let's add a unit test for the job controller where we can see that the Workload object is updated when you change the toleration.
> 
> And another one where if you change the toleration for a Job that is admitted, the Workload gets evicted.

Done, however the overall experience for the case when the tolerations are changed while admitted is at least strange, the  job is suspended and the workload is removed but the tolerations from the workload are restored in the job ...

### Comment by [@trasc](https://github.com/trasc) — 2024-01-11T20:51:00Z

/assign

### Comment by [@trasc](https://github.com/trasc) — 2024-01-16T05:29:20Z

nits covered
/unhold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-16T05:32:17Z

LGTM label has been added.  <details>Git tree hash: 480a555650ad54d8cfc4e0105aa398e13eb0b32c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-16T05:32:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1304#pullrequestreview-1822176874" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1304#" title="Author self-approved">stuton</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1304#pullrequestreview-1822549153" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor,tenzen-y]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T20:18:23Z

/remove-kind cleanup
/kind feature

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T20:20:46Z

/release-note-edit
```release-note
Changing tolerations in an inadmissible job triggers an admission retry.
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T20:21:14Z

/release-note-edit
```release-note
Changing tolerations in an inadmissible job triggers an admission retry with the updated tolerations.
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-13T18:51:32Z

- **pkg/util/equality/podset.go:30** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-13T18:51:32Z
```diff
@@ -26,30 +26,33 @@ import (
 
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
-func comparePodTemplate(a, b *corev1.PodSpec) bool {
+func comparePodTemplate(a, b *corev1.PodSpec, checkCount, changePodSpecFields bool) bool {
+	if changePodSpecFields && !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
```

  why do we need this boolean?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-24T16:59:35Z

Let's add a unit test for the job controller where we can see that the Workload object is updated when you change the toleration.

And another one where if you change the toleration for a Job that is admitted, the Workload gets evicted.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-15T14:14:56Z

- **pkg/util/equality/podset.go:30** by [@trasc](https://github.com/trasc) — 2023-12-15T14:14:56Z
```diff
@@ -26,30 +26,33 @@ import (
 
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
-func comparePodTemplate(a, b *corev1.PodSpec) bool {
+func comparePodTemplate(a, b *corev1.PodSpec, checkCount, changePodSpecFields bool) bool {
+	if changePodSpecFields && !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
```

  the tolerations could change when the job is unsuspended

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-27T19:06:37Z

- **pkg/controller/jobs/job/job_controller_test.go:2088** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T18:57:47Z
```diff
@@ -1804,6 +1805,101 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is not admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey1",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey2",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+		},
+		"the workload is admitted and tolerations change": {
```

  Is there already a case where the workload is admitted, but the job is still suspended?

- **pkg/controller/jobs/job/job_controller_test.go:1827** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T18:58:42Z
```diff
@@ -1804,6 +1805,101 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is not admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey1",
```

  ```suggestion
  								Key:      "tolerationkey1",
  ```

- **pkg/controller/jobs/job/job_controller_test.go:1870** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T19:04:10Z
```diff
@@ -1804,6 +1805,101 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is not admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey1",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey2",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+		},
+		"the workload is admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey1",
```

  So we are undoing the user changes. Not ideal, but somewhat fair. Can you make sure that there is an event for the suspension, indicating that the queued workload doesn't match the job? This way the user knows that Kueue took some action and they can fix whatever they wanted to do.

- **pkg/controller/jobs/kubeflow/jobs/mxjob/mxjob_controller_test.go:369** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T19:05:07Z
```diff
@@ -365,9 +366,29 @@ func TestReconciler(t *testing.T) {
 						*utiltesting.MakePodSet("worker", 10).Request(v1.ResourceCPU, "5").Obj(),
 					).
 					ReserveQuota(utiltesting.MakeAdmission("cq").
-						AssignmentPodCount(1).
-						AssignmentPodCount(5).
-						AssignmentPodCount(10).
+						PodSets(
```

  Thanks

- **pkg/util/equality/podset.go:28** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T19:06:14Z
```diff
@@ -27,14 +27,17 @@ import (
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
```

  Do we need more code to support changing node selectors, or can we just add the comparison now?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-09T09:36:18Z

- **pkg/controller/jobs/job/job_controller_test.go:1870** by [@trasc](https://github.com/trasc) — 2024-01-09T09:36:18Z
```diff
@@ -1804,6 +1805,101 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is not admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey1",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey2",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+		},
+		"the workload is admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey1",
```

  My biggest concern around this is that if the scheduler is fast and readmits the workload the user will never be able to change the toleration. 
   Maybe we can / should add a configuration key to skip restoring from an unmatching workload.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-09T09:37:12Z

- **pkg/util/equality/podset.go:28** by [@trasc](https://github.com/trasc) — 2024-01-09T09:37:12Z
```diff
@@ -27,14 +27,17 @@ import (
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
```

  It should work with adding the check only.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-12T08:11:48Z

- **pkg/controller/jobs/job/job_controller_test.go:1827** by [@trasc](https://github.com/trasc) — 2024-01-11T15:36:12Z
```diff
@@ -1804,6 +1805,101 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is not admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey1",
```

  done

- **pkg/controller/jobs/job/job_controller_test.go:2088** by [@trasc](https://github.com/trasc) — 2024-01-11T15:42:53Z
```diff
@@ -1804,6 +1805,101 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is not admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey1",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey2",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+		},
+		"the workload is admitted and tolerations change": {
```

  "suspended job with matching admitted workload is unsuspended" .. why?

- **pkg/util/equality/podset.go:28** by [@trasc](https://github.com/trasc) — 2024-01-12T08:03:16Z
```diff
@@ -27,14 +27,17 @@ import (
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
```

  However realigning the tests will take some effort.

- **pkg/controller/jobs/job/job_controller_test.go:1870** by [@trasc](https://github.com/trasc) — 2024-01-12T08:04:18Z
```diff
@@ -1804,6 +1805,101 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is not admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey1",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey2",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+		},
+		"the workload is admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey1",
```

  The source of the restore info is mentioned in the stop event message now.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-12T16:52:29Z

- **pkg/controller/jobframework/reconciler.go:516** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T16:21:48Z
```diff
@@ -511,7 +511,13 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		}
 
 		if _, finished := job.Finished(); !finished {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
+			var msg string
+			if w == nil {
+				msg = "Missing Workload, No restore PodSet info provided"
```

  Let's try to be more user friendly:
  ```suggestion
  				msg = "Missing Workload; unable to restore pod templates"
  ```

- **pkg/controller/jobframework/reconciler.go:518** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T16:23:14Z
```diff
@@ -511,7 +511,13 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		}
 
 		if _, finished := job.Finished(); !finished {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
+			var msg string
+			if w == nil {
+				msg = "Missing Workload, No restore PodSet info provided"
+			} else {
+				msg = "No matching Workload, Restore PodSet info provided from an out of sync Workload"
```

  ```suggestion
  				msg = "No matching Workload; restoring pod templates according to existent Workload"
  ```

- **pkg/controller/jobframework/reconciler.go:617** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T16:27:12Z
```diff
@@ -607,8 +613,39 @@ func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *ku
 	return true, nil
 }
 
+// get the expected podsets during the job execution, returns nil if the workload has no reservation or
+// the admission dose not fit.
```

  ```suggestion
  // the admission does not fit.
  ```
  
  did you mean match instead of fit?

- **pkg/controller/jobframework/reconciler.go:642** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T16:35:56Z
```diff
@@ -607,8 +613,39 @@ func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *ku
 	return true, nil
 }
 
+// get the expected podsets during the job execution, returns nil if the workload has no reservation or
+// the admission dose not fit.
+func expectedRunningPodSets(ctx context.Context, c client.Client, wl *kueue.Workload) []kueue.PodSet {
+	if !workload.HasQuotaReservation(wl) {
+		return nil
+	}
+	info, err := getPodSetsInfoFromStatus(ctx, c, wl)
+	if err != nil {
+		return nil
+	}
+	infoMap := slices.ToRefMap(info, func(psi *podset.PodSetInfo) string { return psi.Name })
+	runningPodSets := wl.Spec.DeepCopy().PodSets
+	canBePartiallyAdmitted := workload.CanBePartiallyAdmitted(wl)
+	for i := range runningPodSets {
+		ps := &runningPodSets[i]
+		psi, found := infoMap[ps.Name]
+		if !found {
+			return nil
+		}
+		err := podset.Merge(&ps.Template.ObjectMeta, &ps.Template.Spec, *psi)
+		if err != nil {
+			return nil
+		}
+		if canBePartiallyAdmitted && ps.MinCount != nil {
+			// update the expected running count
+			ps.Count = psi.Count
+		}
```

  can this just be:
  ```
  psi.Count = psi.Count
  ```
  ?
  
  If the value is set, admission already determined if partial admission was possible.

- **pkg/controller/jobframework/reconciler.go:633** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T16:46:00Z
```diff
@@ -618,33 +655,14 @@ func equivalentToWorkload(job GenericJob, wl *kueue.Workload) bool {
 
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
-	if !workload.CanBePartiallyAdmitted(wl) || !workload.HasQuotaReservation(wl) {
-		// the two sets should fully match.
-		return equality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
-	}
-
-	// Check everything but the pod counts.
-	if !equality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, false) {
-		return false
-	}
-
-	// If the workload is admitted but the job is suspended, ignore counts.
-	// This might allow some violating jobs to pass equivalency checks, but their
-	// workloads would be invalidated in the next sync after unsuspending.
```

  a version of this comment should be left before `return job.IsSuspended() && equality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets)`
  
  I guess we are saying that, if the job is suspended, it can match either the running or the base specs.

- **pkg/controller/jobs/job/job_controller_test.go:2088** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T16:51:56Z
```diff
@@ -1804,6 +1805,101 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is not admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey1",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey2",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+		},
+		"the workload is admitted and tolerations change": {
```

  We need to cover what I describe above: "a suspended job should match either the running or base specs".
  
  If it doesn't match either, I guess it's pretty much treated as this case "the workload is admitted and tolerations change", correct?

- **pkg/util/equality/podset.go:28** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T16:52:15Z
```diff
@@ -27,14 +27,17 @@ import (
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
```

  ok, it can be a follow up.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-15T08:35:05Z

- **pkg/controller/jobframework/reconciler.go:516** by [@trasc](https://github.com/trasc) — 2024-01-15T07:39:50Z
```diff
@@ -511,7 +511,13 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		}
 
 		if _, finished := job.Finished(); !finished {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
+			var msg string
+			if w == nil {
+				msg = "Missing Workload, No restore PodSet info provided"
```

  done

- **pkg/controller/jobframework/reconciler.go:518** by [@trasc](https://github.com/trasc) — 2024-01-15T07:40:36Z
```diff
@@ -511,7 +511,13 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		}
 
 		if _, finished := job.Finished(); !finished {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
+			var msg string
+			if w == nil {
+				msg = "Missing Workload, No restore PodSet info provided"
+			} else {
+				msg = "No matching Workload, Restore PodSet info provided from an out of sync Workload"
```

  done

- **pkg/controller/jobframework/reconciler.go:617** by [@trasc](https://github.com/trasc) — 2024-01-15T07:42:17Z
```diff
@@ -607,8 +613,39 @@ func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *ku
 	return true, nil
 }
 
+// get the expected podsets during the job execution, returns nil if the workload has no reservation or
+// the admission dose not fit.
```

  could be. done

- **pkg/controller/jobframework/reconciler.go:642** by [@trasc](https://github.com/trasc) — 2024-01-15T07:50:12Z
```diff
@@ -607,8 +613,39 @@ func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *ku
 	return true, nil
 }
 
+// get the expected podsets during the job execution, returns nil if the workload has no reservation or
+// the admission dose not fit.
+func expectedRunningPodSets(ctx context.Context, c client.Client, wl *kueue.Workload) []kueue.PodSet {
+	if !workload.HasQuotaReservation(wl) {
+		return nil
+	}
+	info, err := getPodSetsInfoFromStatus(ctx, c, wl)
+	if err != nil {
+		return nil
+	}
+	infoMap := slices.ToRefMap(info, func(psi *podset.PodSetInfo) string { return psi.Name })
+	runningPodSets := wl.Spec.DeepCopy().PodSets
+	canBePartiallyAdmitted := workload.CanBePartiallyAdmitted(wl)
+	for i := range runningPodSets {
+		ps := &runningPodSets[i]
+		psi, found := infoMap[ps.Name]
+		if !found {
+			return nil
+		}
+		err := podset.Merge(&ps.Template.ObjectMeta, &ps.Template.Spec, *psi)
+		if err != nil {
+			return nil
+		}
+		if canBePartiallyAdmitted && ps.MinCount != nil {
+			// update the expected running count
+			ps.Count = psi.Count
+		}
```

  no, if we are not using partial admission we need to be strict about the count

- **pkg/controller/jobframework/reconciler.go:633** by [@trasc](https://github.com/trasc) — 2024-01-15T07:54:37Z
```diff
@@ -618,33 +655,14 @@ func equivalentToWorkload(job GenericJob, wl *kueue.Workload) bool {
 
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
-	if !workload.CanBePartiallyAdmitted(wl) || !workload.HasQuotaReservation(wl) {
-		// the two sets should fully match.
-		return equality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, true)
-	}
-
-	// Check everything but the pod counts.
-	if !equality.ComparePodSetSlices(jobPodSets, wl.Spec.PodSets, false) {
-		return false
-	}
-
-	// If the workload is admitted but the job is suspended, ignore counts.
-	// This might allow some violating jobs to pass equivalency checks, but their
-	// workloads would be invalidated in the next sync after unsuspending.
```

  done

- **pkg/controller/jobs/job/job_controller_test.go:2088** by [@trasc](https://github.com/trasc) — 2024-01-15T08:08:30Z
```diff
@@ -1804,6 +1805,101 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is not admitted and tolerations change": {
+			job: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
+				Key:      "tolarationkey2",
+				Operator: corev1.TolerationOpExists,
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey1",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					Queue("foo").
+					PodSets(
+						*utiltesting.MakePodSet("main", 10).
+							Toleration(corev1.Toleration{
+								Key:      "tolarationkey2",
+								Operator: corev1.TolerationOpExists,
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "",
+					}).
+					Priority(0).
+					Obj(),
+			},
+		},
+		"the workload is admitted and tolerations change": {
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-15T18:02:50Z

overall lgtm

- **pkg/util/equality/podset_test.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T18:02:08Z
```diff
@@ -77,11 +71,25 @@ func TestComparePodSetSlices(t *testing.T) {
 			}).Obj()},
 			wantEqual: false,
 		},
+		"different requests in toleration": {
+			a: []kueue.PodSet{*utiltestting.MakePodSet("ps", 10).SetMinimumCount(5).Toleration(corev1.Toleration{
+				Key:      "instance",
+				Operator: corev1.TolerationOpEqual,
+				Value:    "spot",
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj()},
+			b: []kueue.PodSet{*utiltestting.MakePodSet("ps", 10).SetMinimumCount(5).Toleration(corev1.Toleration{
+				Key:      "instance",
+				Operator: corev1.TolerationOpEqual,
+				Value:    "demand",
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj()},
+			wantEqual: false,
+		},
 		"different count when checked": {
```

  ```suggestion
  		"different count": {
  ```

- **pkg/controller/jobframework/reconciler.go:616** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T18:02:32Z
```diff
@@ -607,8 +613,39 @@ func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *ku
 	return true, nil
 }
 
+// get the expected podsets during the job execution, returns nil if the workload has no reservation or
```

  ```suggestion
  // expectedRunningPodSets gets the expected podsets during the job execution, returns nil if the workload has no reservation or
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-15T19:24:54Z

/approve
/hold
for nits

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-16T05:29:00Z

- **pkg/util/equality/podset_test.go:89** by [@trasc](https://github.com/trasc) — 2024-01-16T05:24:34Z
```diff
@@ -77,11 +71,25 @@ func TestComparePodSetSlices(t *testing.T) {
 			}).Obj()},
 			wantEqual: false,
 		},
+		"different requests in toleration": {
+			a: []kueue.PodSet{*utiltestting.MakePodSet("ps", 10).SetMinimumCount(5).Toleration(corev1.Toleration{
+				Key:      "instance",
+				Operator: corev1.TolerationOpEqual,
+				Value:    "spot",
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj()},
+			b: []kueue.PodSet{*utiltestting.MakePodSet("ps", 10).SetMinimumCount(5).Toleration(corev1.Toleration{
+				Key:      "instance",
+				Operator: corev1.TolerationOpEqual,
+				Value:    "demand",
+				Effect:   corev1.TaintEffectNoSchedule,
+			}).Obj()},
+			wantEqual: false,
+		},
 		"different count when checked": {
```

  done

- **pkg/controller/jobframework/reconciler.go:616** by [@trasc](https://github.com/trasc) — 2024-01-16T05:26:39Z
```diff
@@ -607,8 +613,39 @@ func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *ku
 	return true, nil
 }
 
+// get the expected podsets during the job execution, returns nil if the workload has no reservation or
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-16T05:32:11Z

Thanks!
/lgtm
/approve
