# PR #1325: [KEP] Kueue kubectl plugin describe and list commands

**Summary**: [KEP] Kueue kubectl plugin describe and list commands

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1325

**Last updated**: 2024-04-29T17:13:42Z

---

## Metadata

- **State**: closed
- **Author**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Branch**: `kueue-cli` → `main`
- **Created**: 2023-11-13T20:40:44Z
- **Updated**: 2024-04-29T17:13:42Z
- **Merged**: —
- **Closed**: 2024-04-29T17:13:40Z
- **Labels**: `size/L`, `release-note-none`, `needs-rebase`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 4   **+333 / -0**

## Description

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:
Related to #487 

#### Special notes for your reviewer:
Renaming existing KEP as PRD and adding a new KEP that limits down the scope of CLI work and provides some level of implementation details.

#### Does this PR introduce a user-facing change?
```release-note
None
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-13T20:40:50Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1325#" title="Author self-approved">yaroslava-serdiuk</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [alculquicondor](https://github.com/alculquicondor) for approval. For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["alculquicondor"]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-13T20:40:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f60e9b80616a0ec628c28d50b830cef3812a4322 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/657b05cc22e7f200087b7283 |

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-13T20:46:16Z

/assign @mimowo

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-21T09:48:52Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-20T03:48:11Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-28T11:47:43Z

@yaroslava-serdiuk: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-main-1-27 | f60e9b80616a0ec628c28d50b830cef3812a4322 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1325/pull-kueue-test-e2e-main-1-27/1735293790916186112) | true | `/test pull-kueue-test-e2e-main-1-27`
pull-kueue-test-unit-main | f60e9b80616a0ec628c28d50b830cef3812a4322 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1325/pull-kueue-test-unit-main/1735293790689693696) | true | `/test pull-kueue-test-unit-main`
pull-kueue-test-multikueue-e2e-main-1-29 | f60e9b80616a0ec628c28d50b830cef3812a4322 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1325/pull-kueue-test-multikueue-e2e-main-1-29/1773315235185168384) | true | `/test pull-kueue-test-multikueue-e2e-main-1-29`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=1325). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Ayaroslava-serdiuk). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:13:37Z

/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-29T17:13:41Z

@alculquicondor: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1325#issuecomment-2083247627):

>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-11-14T12:56:37Z

- **keps/487-kubectl-plugin-listing/README.md:72** by [@trasc](https://github.com/trasc) — 2023-11-14T12:56:25Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
```

  ```suggestion
  ```bash
  $kubectl kueue <command> <type> <name> <flags> <options>
  ```
  
  maybe it helps to have a similar order as `kubectl`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-14T13:41:05Z

- **keps/487-kubectl-plugin-listing/README.md:89** by [@mimowo](https://github.com/mimowo) — 2023-11-14T13:23:43Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
```

  How this is going to give us more information than `kubectll describe workloads <name>`? Since the is usability KEP, and most of the use cases can be supported with vanilla k8s API, I would suggest to give example commands "before" and "after" the KEP, for comparison.

- **keps/487-kubectl-plugin-listing/README.md:102** by [@mimowo](https://github.com/mimowo) — 2023-11-14T13:24:19Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
+# Describe particular workload.
+$kubeclt kueue workload describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workload describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
```

  Please clarify the steps needed for the command "before" the KEP.

- **keps/487-kubectl-plugin-listing/README.md:107** by [@mimowo](https://github.com/mimowo) — 2023-11-14T13:34:58Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
+# Describe particular workload.
+$kubeclt kueue workload describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workload describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+The API of Workload and ClusterQueue doesn't have a State field. Here we are making assumptions regarding the state of the object based on the values of other fields.
```

  IIUC, the risk is that the CLI implementation for a commands becomes obsolete due to changes in the Kueue API (for example addition of the dedicated CQ state field). 
  Overall, I'm not sure there is a need to mention this risk here as it applies to virtually any feature which uses some of the Kueue API. Also, the mitigation of having a good test coverage seems generic, and applies to any feature. Also, I'm not sure I understand this part "In case of any change the state field should be disabled."

- **keps/487-kubectl-plugin-listing/README.md:112** by [@mimowo](https://github.com/mimowo) — 2023-11-14T13:35:34Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
+# Describe particular workload.
+$kubeclt kueue workload describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workload describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+The API of Workload and ClusterQueue doesn't have a State field. Here we are making assumptions regarding the state of the object based on the values of other fields.
+We need to make sure to have good test coverage in order to quickly observe any API changes that may affect the state of the object. In case of any change the state field should be disabled.
+
+## Design Details
+
+As a kubectl plugin, the name should have `kubectl-` prefix. The most obvious plugin naming choice would be `kubectl-kueue`.
```

  Add a link to k8s documentation for the mechanism.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-16T13:54:05Z

- **keps/487-kubectl-plugin-listing/README.md:89** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-16T13:54:05Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
```

  Sorry, that's a leftover, should be `list` instead of `get`. 
  The output and the difference with kubectl get command is described in "details" section. 
  Just a note,the goal is to provide a tool that is easy to use for users, who may not be familiar with kubectl. So in general we are fine to duplicate kubectl functionality if this is support our needs.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-17T18:57:37Z

- **keps/487-kubectl-plugin-listing/README.md:72** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-17T18:57:37Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
```

  I suggested current order, since it looks more usable. I think pretty common workflow would be `kubectl kueue workloads list`, and then `kubectl kueue workloads describe <name>`. So changing one last word is handy. 
  However I don't have a strong opinion here. I'm fine to have the same order as `kubectl`

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-17T20:09:31Z

- **keps/487-kubectl-plugin-listing/README.md:102** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-17T20:09:31Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
+# Describe particular workload.
+$kubeclt kueue workload describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workload describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
```

  What do you mean? 
  We need an API first, this currently in progress https://github.com/kubernetes-sigs/kueue/pull/1288

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-17T20:09:41Z

- **keps/487-kubectl-plugin-listing/README.md:107** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-17T20:09:41Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
+# Describe particular workload.
+$kubeclt kueue workload describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workload describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+The API of Workload and ClusterQueue doesn't have a State field. Here we are making assumptions regarding the state of the object based on the values of other fields.
```

  Yes you're correct. Yes, it's probably obvious, however I think the risk here is higher than for other features. 
  For example in case when we add `stop ClusterQueue` functionality, the State heuristic here should be updated.
  
  > Also, I'm not sure I understand this part "In case of any change the state field should be disabled."
  
  This was added as possible mitigation approach, but I agree it's confusing, so I removed it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-20T17:19:13Z

- **keps/487-kubectl-plugin-listing/README.md:89** by [@mimowo](https://github.com/mimowo) — 2023-11-20T17:19:12Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-20T17:20:34Z

- **keps/487-kubectl-plugin-listing/README.md:102** by [@mimowo](https://github.com/mimowo) — 2023-11-20T17:20:33Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
+# Describe particular workload.
+$kubeclt kueue workload describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workload describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
```

  Ah, I thought this `stop` refers to some currently available sequence of commands. In that case, maybe just add a link to the PR or Issue in the KEP, so that it is clear to the next readers (even if the work is on-going).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-20T17:36:51Z

- **keps/487-kubectl-plugin-listing/README.md:107** by [@mimowo](https://github.com/mimowo) — 2023-11-20T17:36:51Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
+# Describe particular workload.
+$kubeclt kueue workload describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workload describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+The API of Workload and ClusterQueue doesn't have a State field. Here we are making assumptions regarding the state of the object based on the values of other fields.
```

  I don't have a strong opinion here, but it feels like not specific enough. For example, the "good test coverage" feels applicable to any feature. It does not feel actionable. 
  
  Maybe this part about the "state" field is not really risk & mitigation, but more about "Design details"? 
  
  There we could explain that we compute the `State` field based on other fields. Also, this seems to be the case for the standard `kubectl` command, where for a pod it returns `State` which can be `Terminating` when a pod has `.deletionTimestamp`.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-21T09:38:09Z

- **keps/487-kubectl-plugin-listing/README.md:107** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-21T09:38:09Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
+# Describe particular workload.
+$kubeclt kueue workload describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workload describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+The API of Workload and ClusterQueue doesn't have a State field. Here we are making assumptions regarding the state of the object based on the values of other fields.
```

  Sgtm, done

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-21T09:39:34Z

- **keps/487-kubectl-plugin-listing/README.md:102** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-21T09:39:33Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads get
+# Describe particular workload.
+$kubeclt kueue workload describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workload describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
```

  Added links to the issues

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-11-30T11:05:34Z

- **keps/487-kubectl-plugin-listing/README.md:72** by [@mwielgus](https://github.com/mwielgus) — 2023-11-30T11:05:34Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
```

  I would agree with @trasc. We should keep the order and the style of kubectl. These two commands, kubectl and kueuectl, will be mixed over and over and having swapped order of arguments will be annoying.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-11-30T11:06:03Z

- **keps/487-kubectl-plugin-listing/README.md:75** by [@mwielgus](https://github.com/mwielgus) — 2023-11-30T11:06:03Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
```

  Can we have get instead of list? So that it follows kubectl?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-11-30T11:07:53Z

- **keps/487-kubectl-plugin-listing/README.md:188** by [@mwielgus](https://github.com/mwielgus) — 2023-11-30T11:07:53Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads list
+# Describe particular workload.
+$kubeclt kueue workloads describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workloads describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+
+## Design Details
+
+As a kubectl plugin, the name should have `kubectl-` prefix as was mention in [documentation](https://kubernetes.io/docs/tasks/extend-kubectl/kubectl-plugins/). The most obvious plugin naming choice would be `kubectl-kueue`.
+The code will live in cmd/ directory.
+
+The CLI command will have the following format:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+### Version
+
+```bash
+$kubectl kueue version
+kueue pod was not found in kueue-system namespace
+
+$kubectl kueue version
+kueue:v0.5.0
+```
+
+`kubectl kueue version` command will verify that kueue is running by checking the `kueue-controller-manager` pod in kueue-system namespace and return the version of kueue based on the image name in pod spec.
+
+### Describe
+
+```bash
+$kubectl kueue workloads describe job-team-a --namespace=team-a --status
+State: Pending
+Status
+  Conditions:
+    Last Transition Time:  2023-11-09T09:26:58Z
+    Message:               couldn't assign flavors to pod set main: resource ephemeral-storage unavailable in ClusterQueue
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+
+$kubectl kueue workload describe job-team-a --namespace team-a --spec
+Spec:
+  Pod Sets:
+    Count:  3
+    Name:   main
+    Template:
+      Metadata:
+        Labels:
+          batch.kubernetes.io/controller-uid:  37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          batch.kubernetes.io/job-name:        sample-job-team-a-vdjg9
+          Controller - UID:                    37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          Job - Name:                          sample-job-team-a-vdjg9
+      Spec:
+        Containers:
+          Args:
+            10s
+          Image:              gcr.io/k8s-staging-perf-tests/sleep:latest
+          Image Pull Policy:  Always
+          Name:               dummy-job
+          Resources:
+            Limits:
+              Cpu:                  500m
+              Ephemeral - Storage:  512Mi
+              Memory:               512Mi
+              nvidia.com/gpu:       1
+            Requests:
+              Cpu:                     500m
+              Ephemeral - Storage:     512Mi
+              Memory:                  512Mi
+              nvidia.com/gpu:          1
+          Termination Message Path:    /dev/termination-log
+          Termination Message Policy:  File
+        Dns Policy:                    ClusterFirst
+        Node Selector:
+          cloud.google.com/gke-accelerator:  nvidia-tesla-t4
+        Restart Policy:                      Never
+        Scheduler Name:                      default-scheduler
+        Security Context:
+        Termination Grace Period Seconds:  30
+  Priority:                                0
+  Priority Class Source:                   
+  Queue Name:                              lq-team-a
+
+```
+
+In order to make kueue CLI simple, we are going to have only one abstraction for jobs specific information — workload. Currently the `describe` command will mostly duplicate the `kubectl describe` command with few modifications:
+`--status` option will display Workload.Status field and state of Workload.
```

  Why do we need these --status --spec options?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-11-30T11:11:44Z

- **keps/487-kubectl-plugin-listing/README.md:203** by [@mwielgus](https://github.com/mwielgus) — 2023-11-30T11:11:44Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads list
+# Describe particular workload.
+$kubeclt kueue workloads describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workloads describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+
+## Design Details
+
+As a kubectl plugin, the name should have `kubectl-` prefix as was mention in [documentation](https://kubernetes.io/docs/tasks/extend-kubectl/kubectl-plugins/). The most obvious plugin naming choice would be `kubectl-kueue`.
+The code will live in cmd/ directory.
+
+The CLI command will have the following format:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+### Version
+
+```bash
+$kubectl kueue version
+kueue pod was not found in kueue-system namespace
+
+$kubectl kueue version
+kueue:v0.5.0
+```
+
+`kubectl kueue version` command will verify that kueue is running by checking the `kueue-controller-manager` pod in kueue-system namespace and return the version of kueue based on the image name in pod spec.
+
+### Describe
+
+```bash
+$kubectl kueue workloads describe job-team-a --namespace=team-a --status
+State: Pending
+Status
+  Conditions:
+    Last Transition Time:  2023-11-09T09:26:58Z
+    Message:               couldn't assign flavors to pod set main: resource ephemeral-storage unavailable in ClusterQueue
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+
+$kubectl kueue workload describe job-team-a --namespace team-a --spec
+Spec:
+  Pod Sets:
+    Count:  3
+    Name:   main
+    Template:
+      Metadata:
+        Labels:
+          batch.kubernetes.io/controller-uid:  37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          batch.kubernetes.io/job-name:        sample-job-team-a-vdjg9
+          Controller - UID:                    37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          Job - Name:                          sample-job-team-a-vdjg9
+      Spec:
+        Containers:
+          Args:
+            10s
+          Image:              gcr.io/k8s-staging-perf-tests/sleep:latest
+          Image Pull Policy:  Always
+          Name:               dummy-job
+          Resources:
+            Limits:
+              Cpu:                  500m
+              Ephemeral - Storage:  512Mi
+              Memory:               512Mi
+              nvidia.com/gpu:       1
+            Requests:
+              Cpu:                     500m
+              Ephemeral - Storage:     512Mi
+              Memory:                  512Mi
+              nvidia.com/gpu:          1
+          Termination Message Path:    /dev/termination-log
+          Termination Message Policy:  File
+        Dns Policy:                    ClusterFirst
+        Node Selector:
+          cloud.google.com/gke-accelerator:  nvidia-tesla-t4
+        Restart Policy:                      Never
+        Scheduler Name:                      default-scheduler
+        Security Context:
+        Termination Grace Period Seconds:  30
+  Priority:                                0
+  Priority Class Source:                   
+  Queue Name:                              lq-team-a
+
+```
+
+In order to make kueue CLI simple, we are going to have only one abstraction for jobs specific information — workload. Currently the `describe` command will mostly duplicate the `kubectl describe` command with few modifications:
+`--status` option will display Workload.Status field and state of Workload.
+There is no `State` field in Workload, but it could be identified from other fields. The `Workload.Status` has an `Admitted` field, in case of pending workload it is empty, so from the user point of view it's not clear what the state of the Workload is. I suggest adding the state information that displays `Admitted` if the `Status.Admitted` is not empty and `Pending` if the `Status.Admitted` is empty.
+The `--spec` option will display `Workload.Spec` information.
+
+For ClusterQueue the Status field might look confusing. For example for misconfigured ClusterQueur `Status.Conditions[-1].Type` have value `Active` and `Status.Conditions[-1].Status` have value `False`. I suggest including a state field that will display `Error` if `Status.Conditions.Status` is False and `Active` if `Status.Conditions.Status` is `True` and `Status.Conditions.Type` is `Active`. Later, when we'll support other types, the logic will probably stay the same, but it should be double checked.
+
+Since the logic for the `State` field is complex, we need to make sure it's well tested and be able to identify when the logic should be updated. The proposal is to have a dedicated test for `State` next to the API definition.
+
+### List
+
+```bash
+$kubectl kueue worlkoads list --clusterqueue=cq
+| Namespace | Name     |  Queue  | ClusterQueue | Pods |   State   | Age
+|-----------|----------|---------|--------------|------|-----------|-----
```

  Can we skip table drawing and have it in the same way as kubectl does? Drawing tables make it a bit inconvenient to use kueuectl output as a input to other command.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-11-30T11:15:07Z

- **keps/487-kubectl-plugin-listing/README.md:222** by [@mwielgus](https://github.com/mwielgus) — 2023-11-30T11:15:07Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads list
+# Describe particular workload.
+$kubeclt kueue workloads describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workloads describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+
+## Design Details
+
+As a kubectl plugin, the name should have `kubectl-` prefix as was mention in [documentation](https://kubernetes.io/docs/tasks/extend-kubectl/kubectl-plugins/). The most obvious plugin naming choice would be `kubectl-kueue`.
+The code will live in cmd/ directory.
+
+The CLI command will have the following format:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+### Version
+
+```bash
+$kubectl kueue version
+kueue pod was not found in kueue-system namespace
+
+$kubectl kueue version
+kueue:v0.5.0
+```
+
+`kubectl kueue version` command will verify that kueue is running by checking the `kueue-controller-manager` pod in kueue-system namespace and return the version of kueue based on the image name in pod spec.
+
+### Describe
+
+```bash
+$kubectl kueue workloads describe job-team-a --namespace=team-a --status
+State: Pending
+Status
+  Conditions:
+    Last Transition Time:  2023-11-09T09:26:58Z
+    Message:               couldn't assign flavors to pod set main: resource ephemeral-storage unavailable in ClusterQueue
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+
+$kubectl kueue workload describe job-team-a --namespace team-a --spec
+Spec:
+  Pod Sets:
+    Count:  3
+    Name:   main
+    Template:
+      Metadata:
+        Labels:
+          batch.kubernetes.io/controller-uid:  37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          batch.kubernetes.io/job-name:        sample-job-team-a-vdjg9
+          Controller - UID:                    37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          Job - Name:                          sample-job-team-a-vdjg9
+      Spec:
+        Containers:
+          Args:
+            10s
+          Image:              gcr.io/k8s-staging-perf-tests/sleep:latest
+          Image Pull Policy:  Always
+          Name:               dummy-job
+          Resources:
+            Limits:
+              Cpu:                  500m
+              Ephemeral - Storage:  512Mi
+              Memory:               512Mi
+              nvidia.com/gpu:       1
+            Requests:
+              Cpu:                     500m
+              Ephemeral - Storage:     512Mi
+              Memory:                  512Mi
+              nvidia.com/gpu:          1
+          Termination Message Path:    /dev/termination-log
+          Termination Message Policy:  File
+        Dns Policy:                    ClusterFirst
+        Node Selector:
+          cloud.google.com/gke-accelerator:  nvidia-tesla-t4
+        Restart Policy:                      Never
+        Scheduler Name:                      default-scheduler
+        Security Context:
+        Termination Grace Period Seconds:  30
+  Priority:                                0
+  Priority Class Source:                   
+  Queue Name:                              lq-team-a
+
+```
+
+In order to make kueue CLI simple, we are going to have only one abstraction for jobs specific information — workload. Currently the `describe` command will mostly duplicate the `kubectl describe` command with few modifications:
+`--status` option will display Workload.Status field and state of Workload.
+There is no `State` field in Workload, but it could be identified from other fields. The `Workload.Status` has an `Admitted` field, in case of pending workload it is empty, so from the user point of view it's not clear what the state of the Workload is. I suggest adding the state information that displays `Admitted` if the `Status.Admitted` is not empty and `Pending` if the `Status.Admitted` is empty.
+The `--spec` option will display `Workload.Spec` information.
+
+For ClusterQueue the Status field might look confusing. For example for misconfigured ClusterQueur `Status.Conditions[-1].Type` have value `Active` and `Status.Conditions[-1].Status` have value `False`. I suggest including a state field that will display `Error` if `Status.Conditions.Status` is False and `Active` if `Status.Conditions.Status` is `True` and `Status.Conditions.Type` is `Active`. Later, when we'll support other types, the logic will probably stay the same, but it should be double checked.
+
+Since the logic for the `State` field is complex, we need to make sure it's well tested and be able to identify when the logic should be updated. The proposal is to have a dedicated test for `State` next to the API definition.
+
+### List
+
+```bash
+$kubectl kueue worlkoads list --clusterqueue=cq
+| Namespace | Name     |  Queue  | ClusterQueue | Pods |   State   | Age
+|-----------|----------|---------|--------------|------|-----------|-----
+| default   | wl-1     | queue-1 |   cq         |   2  | Admitted  | 2h
+| default   | wl-2     | queue-2 |   cq         |   5  | Pending   | 2h
+
+
+$kubectl kueue clusterqueue list
+| Name             | Cohort   |  State  | PENDING WORKLOADS
+|------------------|----------|---------|-------------------
+| cluster-queue-1  | cohort   | Active  | 2  
+| cluster-queue-2  |          | Error   | 0    
+
+```
+
+The list command will use kubectl get command with CustumnColumntPrinter (to include ClusterQueue column) and with attached column `State`, that was described earlier. For Workloads we'll use sorting for pending workloads that are on the same ClusterQueue based on their position on the queue. Since ClusterQueue is not namespaced object, I suggest using all namespaces by default.
+The command will support --state, --cohort, --clusterqueue, --namespace flags when applicable.
+
+### Stop
+
+```bash
+$kubectl kueue worlkoad stop workload-name --clusterqueue cluster-queue-name
```

  Stop is ambiguous.  Does it kill the job with the failed status, or just removes it from the queue for immediate readmission? How to "unstop" a workload?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-14T13:33:48Z

- **keps/487-kubectl-plugin-listing/README.md:188** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-14T13:33:48Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads list
+# Describe particular workload.
+$kubeclt kueue workloads describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workloads describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+
+## Design Details
+
+As a kubectl plugin, the name should have `kubectl-` prefix as was mention in [documentation](https://kubernetes.io/docs/tasks/extend-kubectl/kubectl-plugins/). The most obvious plugin naming choice would be `kubectl-kueue`.
+The code will live in cmd/ directory.
+
+The CLI command will have the following format:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+### Version
+
+```bash
+$kubectl kueue version
+kueue pod was not found in kueue-system namespace
+
+$kubectl kueue version
+kueue:v0.5.0
+```
+
+`kubectl kueue version` command will verify that kueue is running by checking the `kueue-controller-manager` pod in kueue-system namespace and return the version of kueue based on the image name in pod spec.
+
+### Describe
+
+```bash
+$kubectl kueue workloads describe job-team-a --namespace=team-a --status
+State: Pending
+Status
+  Conditions:
+    Last Transition Time:  2023-11-09T09:26:58Z
+    Message:               couldn't assign flavors to pod set main: resource ephemeral-storage unavailable in ClusterQueue
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+
+$kubectl kueue workload describe job-team-a --namespace team-a --spec
+Spec:
+  Pod Sets:
+    Count:  3
+    Name:   main
+    Template:
+      Metadata:
+        Labels:
+          batch.kubernetes.io/controller-uid:  37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          batch.kubernetes.io/job-name:        sample-job-team-a-vdjg9
+          Controller - UID:                    37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          Job - Name:                          sample-job-team-a-vdjg9
+      Spec:
+        Containers:
+          Args:
+            10s
+          Image:              gcr.io/k8s-staging-perf-tests/sleep:latest
+          Image Pull Policy:  Always
+          Name:               dummy-job
+          Resources:
+            Limits:
+              Cpu:                  500m
+              Ephemeral - Storage:  512Mi
+              Memory:               512Mi
+              nvidia.com/gpu:       1
+            Requests:
+              Cpu:                     500m
+              Ephemeral - Storage:     512Mi
+              Memory:                  512Mi
+              nvidia.com/gpu:          1
+          Termination Message Path:    /dev/termination-log
+          Termination Message Policy:  File
+        Dns Policy:                    ClusterFirst
+        Node Selector:
+          cloud.google.com/gke-accelerator:  nvidia-tesla-t4
+        Restart Policy:                      Never
+        Scheduler Name:                      default-scheduler
+        Security Context:
+        Termination Grace Period Seconds:  30
+  Priority:                                0
+  Priority Class Source:                   
+  Queue Name:                              lq-team-a
+
+```
+
+In order to make kueue CLI simple, we are going to have only one abstraction for jobs specific information — workload. Currently the `describe` command will mostly duplicate the `kubectl describe` command with few modifications:
+`--status` option will display Workload.Status field and state of Workload.
```

  User wants to know why the workload is pending or was there any errors during admission. I suggest to add --status option that will limit describe command output and will provide information for the users needs.
  For --spec option I'm not so confident if this is needed. My thoughts were is that user would like to verify if the spec is correct when they've seen some error in admission check.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-14T13:43:31Z

- **keps/487-kubectl-plugin-listing/README.md:222** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-14T13:43:31Z
```diff
@@ -0,0 +1,316 @@
+# KEP-487: Kubectl plugin for listing objects
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Version](#version)
+  - [Describe](#describe)
+  - [List](#list)
+  - [Stop](#stop)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Provide an easy way to view and manage kueue abstractions via command line tool.
+
+## Motivation
+
+Create tools that provide useful information in an intuitive way for cluster users and could be later extended as managing tool for cluster administrators.
+As a cluster user I don’t need to dive into the difference between job and Workload concept in kueue and should be able quickly discover the job/Workload and ClusterQueue status or see the error message what is misconfigured/should be fixed.
+
+### Goals
+
+Impement basic kueue CLI commands that was described in PRD:
+
+- Display the Workload status.
+- Describe Workload.
+- List Workloads in specific queue/namespace.
+- List admitted/pending workloads
+- List all ClusterQueues.
+- Describe ClusterQueue.
+- Stop Workload.
+- Stop ClusterQueue.
+
+### Non-Goals
+
+## Proposal
+
+We propose creating a Kubectl plugin that exposes kueue specific information. The main command-line interactions will take the following shape:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+Set of supported commands: `list`, `describe`, `stop`.
+Set of supported types: `workload`, `clusterqueue`, `localqueue`.
+Sets of supported flags are different for each command/type.
+
+The plugin could be downloaded directly from the repo or installed by the Krew plugging manager.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a user submitting workloads, I want to easily see the status of an entire Workload, or check on why my workload is not being admitted. I can do this with the new proposed plugin.
+
+```bash
+# List workloads in the cluster.
+$kubectl kueue workloads list
+# Describe particular workload.
+$kubeclt kueue workloads describe <name>
+# Display state and conditions to see what could be wrong with workload.
+$kubeclt kueue workloads describe <name> --status
+
+```
+
+#### Story 2
+
+As a cluster admin I want to have an easy way to manage cluster queues:
+
+```bash
+$kubectl kueue clusterqueue stop <queue-name>
+```
+
+### Risks and Mitigations
+
+
+## Design Details
+
+As a kubectl plugin, the name should have `kubectl-` prefix as was mention in [documentation](https://kubernetes.io/docs/tasks/extend-kubectl/kubectl-plugins/). The most obvious plugin naming choice would be `kubectl-kueue`.
+The code will live in cmd/ directory.
+
+The CLI command will have the following format:
+
+```bash
+$kubectl kueue <type> <command> <name> <flags> <options>
+```
+
+### Version
+
+```bash
+$kubectl kueue version
+kueue pod was not found in kueue-system namespace
+
+$kubectl kueue version
+kueue:v0.5.0
+```
+
+`kubectl kueue version` command will verify that kueue is running by checking the `kueue-controller-manager` pod in kueue-system namespace and return the version of kueue based on the image name in pod spec.
+
+### Describe
+
+```bash
+$kubectl kueue workloads describe job-team-a --namespace=team-a --status
+State: Pending
+Status
+  Conditions:
+    Last Transition Time:  2023-11-09T09:26:58Z
+    Message:               couldn't assign flavors to pod set main: resource ephemeral-storage unavailable in ClusterQueue
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+
+$kubectl kueue workload describe job-team-a --namespace team-a --spec
+Spec:
+  Pod Sets:
+    Count:  3
+    Name:   main
+    Template:
+      Metadata:
+        Labels:
+          batch.kubernetes.io/controller-uid:  37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          batch.kubernetes.io/job-name:        sample-job-team-a-vdjg9
+          Controller - UID:                    37a03eb0-5c59-41f2-be3c-e9e5ca75bc7c
+          Job - Name:                          sample-job-team-a-vdjg9
+      Spec:
+        Containers:
+          Args:
+            10s
+          Image:              gcr.io/k8s-staging-perf-tests/sleep:latest
+          Image Pull Policy:  Always
+          Name:               dummy-job
+          Resources:
+            Limits:
+              Cpu:                  500m
+              Ephemeral - Storage:  512Mi
+              Memory:               512Mi
+              nvidia.com/gpu:       1
+            Requests:
+              Cpu:                     500m
+              Ephemeral - Storage:     512Mi
+              Memory:                  512Mi
+              nvidia.com/gpu:          1
+          Termination Message Path:    /dev/termination-log
+          Termination Message Policy:  File
+        Dns Policy:                    ClusterFirst
+        Node Selector:
+          cloud.google.com/gke-accelerator:  nvidia-tesla-t4
+        Restart Policy:                      Never
+        Scheduler Name:                      default-scheduler
+        Security Context:
+        Termination Grace Period Seconds:  30
+  Priority:                                0
+  Priority Class Source:                   
+  Queue Name:                              lq-team-a
+
+```
+
+In order to make kueue CLI simple, we are going to have only one abstraction for jobs specific information — workload. Currently the `describe` command will mostly duplicate the `kubectl describe` command with few modifications:
+`--status` option will display Workload.Status field and state of Workload.
+There is no `State` field in Workload, but it could be identified from other fields. The `Workload.Status` has an `Admitted` field, in case of pending workload it is empty, so from the user point of view it's not clear what the state of the Workload is. I suggest adding the state information that displays `Admitted` if the `Status.Admitted` is not empty and `Pending` if the `Status.Admitted` is empty.
+The `--spec` option will display `Workload.Spec` information.
+
+For ClusterQueue the Status field might look confusing. For example for misconfigured ClusterQueur `Status.Conditions[-1].Type` have value `Active` and `Status.Conditions[-1].Status` have value `False`. I suggest including a state field that will display `Error` if `Status.Conditions.Status` is False and `Active` if `Status.Conditions.Status` is `True` and `Status.Conditions.Type` is `Active`. Later, when we'll support other types, the logic will probably stay the same, but it should be double checked.
+
+Since the logic for the `State` field is complex, we need to make sure it's well tested and be able to identify when the logic should be updated. The proposal is to have a dedicated test for `State` next to the API definition.
+
+### List
+
+```bash
+$kubectl kueue worlkoads list --clusterqueue=cq
+| Namespace | Name     |  Queue  | ClusterQueue | Pods |   State   | Age
+|-----------|----------|---------|--------------|------|-----------|-----
+| default   | wl-1     | queue-1 |   cq         |   2  | Admitted  | 2h
+| default   | wl-2     | queue-2 |   cq         |   5  | Pending   | 2h
+
+
+$kubectl kueue clusterqueue list
+| Name             | Cohort   |  State  | PENDING WORKLOADS
+|------------------|----------|---------|-------------------
+| cluster-queue-1  | cohort   | Active  | 2  
+| cluster-queue-2  |          | Error   | 0    
+
+```
+
+The list command will use kubectl get command with CustumnColumntPrinter (to include ClusterQueue column) and with attached column `State`, that was described earlier. For Workloads we'll use sorting for pending workloads that are on the same ClusterQueue based on their position on the queue. Since ClusterQueue is not namespaced object, I suggest using all namespaces by default.
+The command will support --state, --cohort, --clusterqueue, --namespace flags when applicable.
+
+### Stop
+
+```bash
+$kubectl kueue worlkoad stop workload-name --clusterqueue cluster-queue-name
```

  I see, removed it since it's out of scope for this kep
