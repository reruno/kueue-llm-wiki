# PR #133: Implement ResourceFlavor API

**Summary**: Implement ResourceFlavor API

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/133

**Last updated**: 2022-03-22T13:18:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-resflv` → `main`
- **Created**: 2022-03-19T16:56:18Z
- **Updated**: 2022-03-22T13:18:06Z
- **Merged**: 2022-03-21T22:22:47Z
- **Closed**: 2022-03-21T22:22:47Z
- **Labels**: `kind/feature`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 22   **+568 / -322**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Implements the ResourceFlavor API. ClusterQueues now don't embed the labels/taints of a flavor, they are looked-up from a matching ResourceFlavor resource.

The scheduler will skip flavors with no matching ResourceFlavor. In a followup work we need to introduce a new "OutOfCommision/Frozen" state for ClusterQueue when this happens. ClusterQueues in this state should be taken out of the cohort and no jobs can schedule via them until the referenced flavors are defined. This will be tracked in https://github.com/kubernetes-sigs/kueue/issues/134.

#### Which issue(s) this PR fixes:
Fixes #59

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-19T16:56:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/133#" title="Author self-approved">ahg-g</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-19T16:57:52Z

/hold

Just so it doesn't get merged by mistake

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T15:56:51Z

@alculquicondor

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T17:57:06Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T18:35:27Z

Also update the sample yamls, please https://github.com/kubernetes-sigs/kueue/tree/main/config/samples

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T19:38:07Z

> Also update the sample yamls, please https://github.com/kubernetes-sigs/kueue/tree/main/config/samples

done.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T19:40:55Z

can you rebase to see if that fixes verify?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T21:11:19Z

/lgtm

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T22:17:51Z

/hold cancel

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-21T18:34:32Z

- **api/v1alpha1/clusterqueue_types.go:230** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T16:05:43Z
```diff
@@ -226,22 +226,13 @@ type Resource struct {
 }
 
 type Flavor struct {
-	// name is the type name, e.g., nvidia-tesla-k80.
+	// name is the ResourceFlavor, which should match the name
+	// of a ResourceFlavor object.
```

  Can we have a reserved name that doesn't require a matching ResourceFlavor?

- **api/v1alpha1/clusterqueue_types.go:231** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T17:43:27Z
```diff
@@ -226,24 +226,17 @@ type Resource struct {
 }
 
 type Flavor struct {
-	// name is the type name, e.g., nvidia-tesla-k80.
+	// resourceFlavor is a reference to the resourceFlavor that defines this flavor.
 	// +kubebuilder:default=default
-	Name string `json:"name"`
+	ResourceFlavor ResourceFlavorReference `json:"name"`
```

  ```suggestion
  	ResourceFlavor ResourceFlavorReference `json:"resourceFlavor"`
  ```

- **pkg/cache/cache.go:131** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T17:50:14Z
```diff
@@ -128,25 +128,44 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue) error {
 
 		existingUsedFlavors := c.UsedResources[r.Name]
 		usedFlavors := make(map[string]int64, len(r.Flavors))
-		resKeys := sets.NewString()
 		for _, t := range r.Flavors {
```

  `t` was for type, I suppose. Let's change it to `f`.

- **pkg/cache/cache_test.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T18:04:19Z
```diff
@@ -38,72 +38,126 @@ import (
 )
 
 func TestCacheClusterQueueOperations(t *testing.T) {
+	initialClusterQueues := []kueue.ClusterQueue{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "a"},
+			Spec: kueue.ClusterQueueSpec{
+				RequestableResources: []kueue.Resource{
+					{
+						Name: corev1.ResourceCPU,
+						Flavors: []kueue.Flavor{
+							{
+								ResourceFlavor: "default",
+								Quota: kueue.Quota{
+									Guaranteed: resource.MustParse("10"),
+									Ceiling:    resource.MustParse("20"),
+								},
+							},
+						},
+					}},
+				Cohort: "one",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "b"},
+			Spec: kueue.ClusterQueueSpec{
+				RequestableResources: []kueue.Resource{
+					{
+						Name:    corev1.ResourceCPU,
+						Flavors: []kueue.Flavor{{ResourceFlavor: "default"}},
+					}},
+				Cohort: "one",
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "c"},
+			Spec:       kueue.ClusterQueueSpec{Cohort: "two"},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "d"},
+		},
+	}
+	setup := func(cache *Cache) {
+		cache.AddOrUpdateResourceFlavor(&kueue.ResourceFlavor{
+			ObjectMeta: metav1.ObjectMeta{Name: "default"},
+			Labels:     map[string]string{"cpuType": "default"},
+		})
+		for _, c := range initialClusterQueues {
+			if err := cache.AddClusterQueue(context.Background(), &c); err != nil {
+				t.Fatalf("Failed adding ClusterQueue: %v", err)
+			}
+		}
+	}
 	scheme := runtime.NewScheme()
 	if err := kueue.AddToScheme(scheme); err != nil {
 		t.Fatalf("Failed adding kueue scheme: %v", err)
 	}
-	cache := New(fake.NewClientBuilder().WithScheme(scheme).Build())
 	steps := []struct {
```

  s/steps/cases

- **pkg/controller/core/resourceflavor_controller.go:79** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T18:10:33Z
```diff
@@ -0,0 +1,91 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	"github.com/go-logr/logr"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/cache"
+)
+
+// ResourceFlavorReconciler reconciles a ResourceFlavor object
+type ResourceFlavorReconciler struct {
+	log   logr.Logger
+	cache *cache.Cache
+}
+
+func NewResourceFlavorReconciler(cache *cache.Cache) *ResourceFlavorReconciler {
+	return &ResourceFlavorReconciler{
+		log:   ctrl.Log.WithName("resourceflavor-reconciler"),
+		cache: cache,
+	}
+}
+
+//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavor,verbs=get;list;watch
+
+func (r *ResourceFlavorReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	// Nothing to do here.
+	return ctrl.Result{}, nil
+}
+
+func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
+	flv, match := e.Object.(*kueue.ResourceFlavor)
+	if !match {
+		return true
+	}
+	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+	return true
+}
+
+func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
+	flv, match := e.Object.(*kueue.ResourceFlavor)
+	if !match {
+		return true
+	}
+	r.cache.DeleteResourceFlavor(flv)
+	return true
+}
+
+func (r *ResourceFlavorReconciler) Update(e event.UpdateEvent) bool {
+	flv, match := e.ObjectNew.(*kueue.ResourceFlavor)
+	if !match {
+		return true
+	}
+	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
+	log.V(2).Info("ResourceFlavor update event")
```

  why only log for Update?

- **pkg/controller/core/resourceflavor_controller.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T18:10:57Z
```diff
@@ -0,0 +1,91 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	"github.com/go-logr/logr"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/cache"
+)
+
+// ResourceFlavorReconciler reconciles a ResourceFlavor object
+type ResourceFlavorReconciler struct {
+	log   logr.Logger
+	cache *cache.Cache
+}
+
+func NewResourceFlavorReconciler(cache *cache.Cache) *ResourceFlavorReconciler {
+	return &ResourceFlavorReconciler{
+		log:   ctrl.Log.WithName("resourceflavor-reconciler"),
+		cache: cache,
+	}
+}
+
+//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavor,verbs=get;list;watch
+
+func (r *ResourceFlavorReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	// Nothing to do here.
+	return ctrl.Result{}, nil
+}
+
+func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
+	flv, match := e.Object.(*kueue.ResourceFlavor)
+	if !match {
+		return true
+	}
+	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+	return true
```

  just `false`, as we are never supposed to reconcile flavors.`true` would cause the names to be queued, which just wastes memory.

- **pkg/cache/cache.go:211** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T18:16:32Z
```diff
@@ -184,14 +203,32 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
+func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) {
+	c.Lock()
+	c.resourceFlavors[rf.Name] = rf
+	for _, cq := range c.clusterQueues {
+		// We call update on all ClusterQueues irrespective of which CQ actually use this flavor
+		// because it is not expensive to do so, and is not worth tracking which ClusterQueues use
```

  Debatable, because we create maps. I would be slightly worried about restarts, as now the startup is `O(n*m)`, for number of CQs and RFs.
  
  But for the scale we expect for n and m, it might be fine for now or even long term.

- **pkg/cache/cache.go:473** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T18:20:58Z
```diff
@@ -433,19 +470,12 @@ func resourcesByName(in []kueue.Resource) map[corev1.ResourceName][]FlavorLimits
 		flavors := make([]FlavorLimits, len(r.Flavors))
 		for i := range flavors {
 			f := &r.Flavors[i]
-			fInfo := FlavorLimits{
-				Name:       f.Name,
+			fLimits := FlavorLimits{
```

  let's also change the name of the function to `resourceLimitsByName`

- **pkg/controller/workload/job/job_controller.go:263** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T18:23:36Z
```diff
@@ -258,32 +255,27 @@ func (r *JobReconciler) startJob(ctx context.Context, w *kueue.QueuedWorkload, j
 	return nil
 }
 
-func getNodeSelectors(cq *kueue.ClusterQueue, w *kueue.QueuedWorkload) map[string]string {
+func (r *JobReconciler) getNodeSelectors(ctx context.Context, w *kueue.QueuedWorkload) (map[string]string, error) {
 	if len(w.Spec.Admission.PodSetFlavors[0].Flavors) == 0 {
-		return nil
-	}
-
-	// Create a map of resources-to-flavors.
-	// May be cache this?
-	flavors := make(map[corev1.ResourceName]map[string]*kueue.Flavor, len(cq.Spec.RequestableResources))
-	for _, r := range cq.Spec.RequestableResources {
-		flavors[r.Name] = make(map[string]*kueue.Flavor, len(r.Flavors))
-		for j := range r.Flavors {
-			flavors[r.Name][r.Flavors[j].Name] = &r.Flavors[j]
-		}
+		return nil, nil
 	}
 
+	processedFlvs := sets.NewString()
```

  is this a leftover?

- **pkg/controller/workload/job/job_controller.go:271** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T18:24:53Z
```diff
@@ -258,32 +255,27 @@ func (r *JobReconciler) startJob(ctx context.Context, w *kueue.QueuedWorkload, j
 	return nil
 }
 
-func getNodeSelectors(cq *kueue.ClusterQueue, w *kueue.QueuedWorkload) map[string]string {
+func (r *JobReconciler) getNodeSelectors(ctx context.Context, w *kueue.QueuedWorkload) (map[string]string, error) {
 	if len(w.Spec.Admission.PodSetFlavors[0].Flavors) == 0 {
-		return nil
-	}
-
-	// Create a map of resources-to-flavors.
-	// May be cache this?
-	flavors := make(map[corev1.ResourceName]map[string]*kueue.Flavor, len(cq.Spec.RequestableResources))
-	for _, r := range cq.Spec.RequestableResources {
-		flavors[r.Name] = make(map[string]*kueue.Flavor, len(r.Flavors))
-		for j := range r.Flavors {
-			flavors[r.Name][r.Flavors[j].Name] = &r.Flavors[j]
-		}
+		return nil, nil
 	}
 
+	processedFlvs := sets.NewString()
 	nodeSelector := map[string]string{}
-	for res, flvr := range w.Spec.Admission.PodSetFlavors[0].Flavors {
-		if cqRes, existRes := flavors[res]; existRes {
-			if cqFlvr, existFlvr := cqRes[flvr]; existFlvr {
-				for k, v := range cqFlvr.Labels {
-					nodeSelector[k] = v
-				}
-			}
+	for _, flvName := range w.Spec.Admission.PodSetFlavors[0].Flavors {
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		// Lookup the ResourceFlavors to fetch the node affinity labels to apply on the job.
+		flv := kueue.ResourceFlavor{}
+		if err := r.client.Get(ctx, types.NamespacedName{Name: flvName}, &flv); err != nil {
```

  Don't forget to update the RBAC for this controller

- **pkg/scheduler/scheduler.go:274** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T18:28:38Z
```diff
@@ -263,11 +263,17 @@ func findFlavorForResource(
 	name corev1.ResourceName,
 	val int64,
 	wUsed map[string]int64,
+	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
 	spec *corev1.PodSpec) (string, int64) {
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
-	for _, flavor := range cq.RequestableResources[name] {
+	for _, flvLimit := range cq.RequestableResources[name] {
+		flavor, exist := resourceFlavors[flvLimit.Name]
+		if !exist {
+			log.Error(nil, "Flavor %v not found", flvLimit.Name)
```

  why log Error?
  
  Are you assuming that #134 would block the workloads at a different point and, thus, reaching this line will be an error?
  
  In the meantime, I think we should return `"", 0` here.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-21T19:37:31Z

- **pkg/cache/cache.go:211** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T18:48:39Z
```diff
@@ -184,14 +203,32 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
+func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) {
+	c.Lock()
+	c.resourceFlavors[rf.Name] = rf
+	for _, cq := range c.clusterQueues {
+		// We call update on all ClusterQueues irrespective of which CQ actually use this flavor
+		// because it is not expensive to do so, and is not worth tracking which ClusterQueues use
```

  I think it is fine, even if this number grows to O(1k), it is ok on restart.

- **pkg/controller/core/resourceflavor_controller.go:79** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T18:55:54Z
```diff
@@ -0,0 +1,91 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	"github.com/go-logr/logr"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/cache"
+)
+
+// ResourceFlavorReconciler reconciles a ResourceFlavor object
+type ResourceFlavorReconciler struct {
+	log   logr.Logger
+	cache *cache.Cache
+}
+
+func NewResourceFlavorReconciler(cache *cache.Cache) *ResourceFlavorReconciler {
+	return &ResourceFlavorReconciler{
+		log:   ctrl.Log.WithName("resourceflavor-reconciler"),
+		cache: cache,
+	}
+}
+
+//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavor,verbs=get;list;watch
+
+func (r *ResourceFlavorReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	// Nothing to do here.
+	return ctrl.Result{}, nil
+}
+
+func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
+	flv, match := e.Object.(*kueue.ResourceFlavor)
+	if !match {
+		return true
+	}
+	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+	return true
+}
+
+func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
+	flv, match := e.Object.(*kueue.ResourceFlavor)
+	if !match {
+		return true
+	}
+	r.cache.DeleteResourceFlavor(flv)
+	return true
+}
+
+func (r *ResourceFlavorReconciler) Update(e event.UpdateEvent) bool {
+	flv, match := e.ObjectNew.(*kueue.ResourceFlavor)
+	if !match {
+		return true
+	}
+	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
+	log.V(2).Info("ResourceFlavor update event")
```

  added for the rest

- **pkg/controller/workload/job/job_controller.go:263** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T18:58:19Z
```diff
@@ -258,32 +255,27 @@ func (r *JobReconciler) startJob(ctx context.Context, w *kueue.QueuedWorkload, j
 	return nil
 }
 
-func getNodeSelectors(cq *kueue.ClusterQueue, w *kueue.QueuedWorkload) map[string]string {
+func (r *JobReconciler) getNodeSelectors(ctx context.Context, w *kueue.QueuedWorkload) (map[string]string, error) {
 	if len(w.Spec.Admission.PodSetFlavors[0].Flavors) == 0 {
-		return nil
-	}
-
-	// Create a map of resources-to-flavors.
-	// May be cache this?
-	flavors := make(map[corev1.ResourceName]map[string]*kueue.Flavor, len(cq.Spec.RequestableResources))
-	for _, r := range cq.Spec.RequestableResources {
-		flavors[r.Name] = make(map[string]*kueue.Flavor, len(r.Flavors))
-		for j := range r.Flavors {
-			flavors[r.Name][r.Flavors[j].Name] = &r.Flavors[j]
-		}
+		return nil, nil
 	}
 
+	processedFlvs := sets.NewString()
```

  no, I have to insert it at the end. This is just a little optimization since a flavor could be used by more than one resource.

- **pkg/controller/workload/job/job_controller.go:271** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T18:59:13Z
```diff
@@ -258,32 +255,27 @@ func (r *JobReconciler) startJob(ctx context.Context, w *kueue.QueuedWorkload, j
 	return nil
 }
 
-func getNodeSelectors(cq *kueue.ClusterQueue, w *kueue.QueuedWorkload) map[string]string {
+func (r *JobReconciler) getNodeSelectors(ctx context.Context, w *kueue.QueuedWorkload) (map[string]string, error) {
 	if len(w.Spec.Admission.PodSetFlavors[0].Flavors) == 0 {
-		return nil
-	}
-
-	// Create a map of resources-to-flavors.
-	// May be cache this?
-	flavors := make(map[corev1.ResourceName]map[string]*kueue.Flavor, len(cq.Spec.RequestableResources))
-	for _, r := range cq.Spec.RequestableResources {
-		flavors[r.Name] = make(map[string]*kueue.Flavor, len(r.Flavors))
-		for j := range r.Flavors {
-			flavors[r.Name][r.Flavors[j].Name] = &r.Flavors[j]
-		}
+		return nil, nil
 	}
 
+	processedFlvs := sets.NewString()
 	nodeSelector := map[string]string{}
-	for res, flvr := range w.Spec.Admission.PodSetFlavors[0].Flavors {
-		if cqRes, existRes := flavors[res]; existRes {
-			if cqFlvr, existFlvr := cqRes[flvr]; existFlvr {
-				for k, v := range cqFlvr.Labels {
-					nodeSelector[k] = v
-				}
-			}
+	for _, flvName := range w.Spec.Admission.PodSetFlavors[0].Flavors {
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		// Lookup the ResourceFlavors to fetch the node affinity labels to apply on the job.
+		flv := kueue.ResourceFlavor{}
+		if err := r.client.Get(ctx, types.NamespacedName{Name: flvName}, &flv); err != nil {
```

  done.

- **pkg/scheduler/scheduler.go:274** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T19:01:31Z
```diff
@@ -263,11 +263,17 @@ func findFlavorForResource(
 	name corev1.ResourceName,
 	val int64,
 	wUsed map[string]int64,
+	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
 	spec *corev1.PodSpec) (string, int64) {
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
-	for _, flavor := range cq.RequestableResources[name] {
+	for _, flvLimit := range cq.RequestableResources[name] {
+		flavor, exist := resourceFlavors[flvLimit.Name]
+		if !exist {
+			log.Error(nil, "Flavor %v not found", flvLimit.Name)
```

  > Are you assuming that #134 would block the workloads at a different point and, thus, reaching this line will be an error?
  
  Yes, but even if we don't have that, shouldn't we have an error in the logs on missing flavors? 
  
  > In the meantime, I think we should return "", 0 here.
  
  This will be strange if the CQ was part of a cohort, workloads from other CQ will be able to borrow the resources of this CQ and start, which this CQ's workloads will not be able to.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-21T19:40:03Z

- **api/v1alpha1/clusterqueue_types.go:230** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T19:40:03Z
```diff
@@ -226,22 +226,13 @@ type Resource struct {
 }
 
 type Flavor struct {
-	// name is the type name, e.g., nvidia-tesla-k80.
+	// name is the ResourceFlavor, which should match the name
+	// of a ResourceFlavor object.
```

  may be, but lets leave this to a followup PR if we actually decide to have one.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-21T19:43:20Z

- **go.mod:27** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T19:43:20Z
```diff
@@ -24,30 +24,50 @@ require (
 	github.com/Azure/go-autorest/autorest/date v0.3.0 // indirect
 	github.com/Azure/go-autorest/logger v0.2.1 // indirect
 	github.com/Azure/go-autorest/tracing v0.6.0 // indirect
+	github.com/PuerkitoBio/purell v1.1.1 // indirect
```

  @alculquicondor @ArangoGutierrez any idea where those changes came from? I had to run `o get sigs.k8s.io/kustomize/kustomize/v4` to deploy, so probably from that; is it expected?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-21T19:44:45Z

/lgtm

- **go.mod:27** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T19:42:50Z
```diff
@@ -24,30 +24,50 @@ require (
 	github.com/Azure/go-autorest/autorest/date v0.3.0 // indirect
 	github.com/Azure/go-autorest/logger v0.2.1 // indirect
 	github.com/Azure/go-autorest/tracing v0.6.0 // indirect
+	github.com/PuerkitoBio/purell v1.1.1 // indirect
```

  Can you update your go to 1.18?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-21T19:49:04Z

- **go.mod:27** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T19:49:04Z
```diff
@@ -24,30 +24,50 @@ require (
 	github.com/Azure/go-autorest/autorest/date v0.3.0 // indirect
 	github.com/Azure/go-autorest/logger v0.2.1 // indirect
 	github.com/Azure/go-autorest/tracing v0.6.0 // indirect
+	github.com/PuerkitoBio/purell v1.1.1 // indirect
```

  you shouldn't use `go get`, but `go install` instead. The new makefile does that.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-21T21:03:17Z

- **go.mod:27** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T21:03:17Z
```diff
@@ -24,30 +24,50 @@ require (
 	github.com/Azure/go-autorest/autorest/date v0.3.0 // indirect
 	github.com/Azure/go-autorest/logger v0.2.1 // indirect
 	github.com/Azure/go-autorest/tracing v0.6.0 // indirect
+	github.com/PuerkitoBio/purell v1.1.1 // indirect
```

  It is not working for me, make deploy still fails with 
  
  ```
  no required module provides package sigs.k8s.io/kustomize/kustomize/v4; to add it:
          go get sigs.k8s.io/kustomize/kustomize/v4
  make: *** [Makefile:183: kustomize] Error 1
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-21T21:03:36Z

- **go.mod:27** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T21:03:36Z
```diff
@@ -24,30 +24,50 @@ require (
 	github.com/Azure/go-autorest/autorest/date v0.3.0 // indirect
 	github.com/Azure/go-autorest/logger v0.2.1 // indirect
 	github.com/Azure/go-autorest/tracing v0.6.0 // indirect
+	github.com/PuerkitoBio/purell v1.1.1 // indirect
```

  updated but I also reverted this file

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-21T21:12:10Z

- **go.mod:27** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-21T21:12:10Z
```diff
@@ -24,30 +24,50 @@ require (
 	github.com/Azure/go-autorest/autorest/date v0.3.0 // indirect
 	github.com/Azure/go-autorest/logger v0.2.1 // indirect
 	github.com/Azure/go-autorest/tracing v0.6.0 // indirect
+	github.com/PuerkitoBio/purell v1.1.1 // indirect
```

  did you figure it out or did you just revert the file?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-21T21:23:34Z

- **go.mod:27** by [@ahg-g](https://github.com/ahg-g) — 2022-03-21T21:23:34Z
```diff
@@ -24,30 +24,50 @@ require (
 	github.com/Azure/go-autorest/autorest/date v0.3.0 // indirect
 	github.com/Azure/go-autorest/logger v0.2.1 // indirect
 	github.com/Azure/go-autorest/tracing v0.6.0 // indirect
+	github.com/PuerkitoBio/purell v1.1.1 // indirect
```

  Just reverted the file

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-22T13:18:06Z

- **pkg/scheduler/scheduler.go:274** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-22T13:18:06Z
```diff
@@ -263,11 +263,17 @@ func findFlavorForResource(
 	name corev1.ResourceName,
 	val int64,
 	wUsed map[string]int64,
+	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
 	spec *corev1.PodSpec) (string, int64) {
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
-	for _, flavor := range cq.RequestableResources[name] {
+	for _, flvLimit := range cq.RequestableResources[name] {
+		flavor, exist := resourceFlavors[flvLimit.Name]
+		if !exist {
+			log.Error(nil, "Flavor %v not found", flvLimit.Name)
```

  Oops, I forgot about this thread.
  
  > Yes, but even if we don't have that, shouldn't we have an error in the logs on missing flavors?
  
  Maybe an Info, because this is not a system error.
  
  > This will be strange if the CQ was part of a cohort, workloads from other CQ will be able to borrow the resources of this CQ and start, which this CQ's workloads will not be able to.
  
  Makes sense.
  
  My worry is that, during a controller restart, the flavors might not all be available. Then workloads could be scheduled in flavors that are second in the list, just because the resource was not seen yet.
