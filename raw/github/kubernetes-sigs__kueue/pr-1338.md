# PR #1338: KEP update: Allow replacement pods in groups of pods

**Summary**: KEP update: Allow replacement pods in groups of pods

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1338

**Last updated**: 2023-11-29T21:09:35Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `pod-group-failures` → `main`
- **Created**: 2023-11-16T20:46:44Z
- **Updated**: 2023-11-29T21:09:35Z
- **Merged**: 2023-11-29T21:09:33Z
- **Closed**: 2023-11-29T21:09:33Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Changed files**: 1   **+63 / -43**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #976

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-16T20:46:51Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1338#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T20:46:51Z

cc @achernevskii

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-16T20:46:52Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 054a8438f8171dc84549c8a25a3eae586c0b6f0f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/656510676749ff0008bc3cb8 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-17T17:27:44Z

/hold

I'm rethinking whether all Pods owning the Workload is the best idea.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-22T15:28:31Z

@tenzen-y @ahaysx any additional feedback?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-22T15:28:45Z

oh, and @nstogner

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-23T19:02:58Z

/assign

### Comment by [@ahaysx](https://github.com/ahaysx) — 2023-11-23T22:47:41Z

lgtm, thanks Aldo

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:55:59Z

/hold cancel

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T15:46:06Z

@tenzen-y anything to add before merging?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-29T21:05:11Z

LGTM label has been added.  <details>Git tree hash: d235a6b17f4db2dfeab923b528fcb2a1a63778d8</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-16T21:24:41Z

- **keps/976-plain-pods/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-16T21:24:15Z
```diff
@@ -66,14 +68,11 @@ use of this API to implement queuing semantics for Pods.
 - Support queueing of individual Pods.
 - Support queueing of groups of Pods of fixed size, identified by a common label.
 - Opt-in or opt-out Pods from specific namespaces from queueing.
+- Support for [dynamic reclaiming of quota](https://kueue.sigs.k8s.io/docs/concepts/workload/#dynamic-reclaim)
+  for succeeded Pods.
 
 ### Non-Goals
 
-- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
-
-  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
-  parent controller for the Pods.
-
```

  As I remember, we decided not to support these features to recommend users using Job or CustomJob.
  
  https://github.com/kubernetes-sigs/kueue/pull/1031#discussion_r1291649359
  
  Why did you decide to support these features on the naked pods?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-16T22:09:15Z

- **keps/976-plain-pods/README.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-16T22:09:15Z
```diff
@@ -66,14 +68,11 @@ use of this API to implement queuing semantics for Pods.
 - Support queueing of individual Pods.
 - Support queueing of groups of Pods of fixed size, identified by a common label.
 - Opt-in or opt-out Pods from specific namespaces from queueing.
+- Support for [dynamic reclaiming of quota](https://kueue.sigs.k8s.io/docs/concepts/workload/#dynamic-reclaim)
+  for succeeded Pods.
 
 ### Non-Goals
 
-- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
-
-  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
-  parent controller for the Pods.
-
```

  It doesn't add much complexity. We can just count the number of Succeeded Pods. We still will keep all of the finished Pods with finalizers.
  On the other hand, it adds a lot of value to users to keep their clusters with high utilization.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-17T04:32:35Z

- **keps/976-plain-pods/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-17T04:32:35Z
```diff
@@ -66,14 +68,11 @@ use of this API to implement queuing semantics for Pods.
 - Support queueing of individual Pods.
 - Support queueing of groups of Pods of fixed size, identified by a common label.
 - Opt-in or opt-out Pods from specific namespaces from queueing.
+- Support for [dynamic reclaiming of quota](https://kueue.sigs.k8s.io/docs/concepts/workload/#dynamic-reclaim)
+  for succeeded Pods.
 
 ### Non-Goals
 
-- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
-
-  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
-  parent controller for the Pods.
-
```

  I don't have strong objections to supporting dynamic reclaiming quota on the plain pods.
  I wanted to clarify the reason why expanding the plain pods integration.
  
  Thanks.

### Review by [@ahaysx](https://github.com/ahaysx) (commented) — 2023-11-18T03:18:24Z

- **keps/976-plain-pods/README.md:577** by [@ahaysx](https://github.com/ahaysx) — 2023-11-18T03:18:23Z
```diff
@@ -538,39 +537,62 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is
+composed of multiple objects.
+The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the
+reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs
+an in-memory Workload. If there is an existing Workload in the cache and it
+has smaller Pod counters than the in-memory Workload, then it is considered
+unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
 1. If the Pod is not terminated,
   create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
+2. If the Pod is terminated and (the Workload doesn't exist or the workload is finished),
+   then remove the finalizer.
+3. Build the in-memory Workload. If its podset counters are greater than the
+  stored Workload, then evict the Workload.
+4. For gated pods:
+  - remove the gate, set nodeSelector
 5. If the number of terminated pods with a finalizer is greater than or equal to the admission
   count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
   finalizers from the Pods.
 
 Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
 of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+terminated.
+
+
+### Dynamically reclaiming Quota
+
+Succeeded Pods will note be considered replaceable. In other words, the quota
+from Succeeded Pods will be released by filling [reclaimablePods](https://kueue.sigs.k8s.io/docs/concepts/workload/#dynamic-reclaim)
+in the Workload status.
+
+### Retrying Failed Pods
+
+The Pod group will generally only be considered finished if all the Pods finish
```

  What about pod groups that do not replace failed pods? We have this use case where a user does not allow retries (and I imagine is a common batch case). So a pod group would be finished if all pods have exited, regardless of success or failure.
  
  I think one way to do this is for every pod in the group to have the `kueue.x-k8s.io/last-in-group: true` annotation, if "group finished" does not mean the Workload is cleaned up or other running pods in the group are affected.
  
  This seems weird given the name, but would this be the recommendation? Is it worth some kind of alternative annotation configuring this, like `pod-group-mode: Batch` or `pod-group-retry: false`?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-20T15:21:57Z

- **keps/976-plain-pods/README.md:577** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-20T15:21:57Z
```diff
@@ -538,39 +537,62 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is
+composed of multiple objects.
+The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the
+reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs
+an in-memory Workload. If there is an existing Workload in the cache and it
+has smaller Pod counters than the in-memory Workload, then it is considered
+unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
 1. If the Pod is not terminated,
   create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
+2. If the Pod is terminated and (the Workload doesn't exist or the workload is finished),
+   then remove the finalizer.
+3. Build the in-memory Workload. If its podset counters are greater than the
+  stored Workload, then evict the Workload.
+4. For gated pods:
+  - remove the gate, set nodeSelector
 5. If the number of terminated pods with a finalizer is greater than or equal to the admission
   count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
   finalizers from the Pods.
 
 Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
 of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+terminated.
+
+
+### Dynamically reclaiming Quota
+
+Succeeded Pods will note be considered replaceable. In other words, the quota
+from Succeeded Pods will be released by filling [reclaimablePods](https://kueue.sigs.k8s.io/docs/concepts/workload/#dynamic-reclaim)
+in the Workload status.
+
+### Retrying Failed Pods
+
+The Pod group will generally only be considered finished if all the Pods finish
```

  Just having one pod with `last-in-group` has the semantics you want: The pod group will be considered Finished when there are no more Running Pods and there is at least one pod with the annotation.
  
  But I agree that the name is maybe not the best for this use case. I'm thinking of alternatives.
  
  Definitely `pod-group-mode: Batch` is not accurate, as batch doesn't imply that retries are not possible.
  
  `pod-group-retry` doesn't fit the semantics I originally wanted that well.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-20T17:39:05Z

- **keps/976-plain-pods/README.md:577** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-20T17:39:05Z
```diff
@@ -538,39 +537,62 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is
+composed of multiple objects.
+The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the
+reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs
+an in-memory Workload. If there is an existing Workload in the cache and it
+has smaller Pod counters than the in-memory Workload, then it is considered
+unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
 1. If the Pod is not terminated,
   create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
+2. If the Pod is terminated and (the Workload doesn't exist or the workload is finished),
+   then remove the finalizer.
+3. Build the in-memory Workload. If its podset counters are greater than the
+  stored Workload, then evict the Workload.
+4. For gated pods:
+  - remove the gate, set nodeSelector
 5. If the number of terminated pods with a finalizer is greater than or equal to the admission
   count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
   finalizers from the Pods.
 
 Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
 of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+terminated.
+
+
+### Dynamically reclaiming Quota
+
+Succeeded Pods will note be considered replaceable. In other words, the quota
+from Succeeded Pods will be released by filling [reclaimablePods](https://kueue.sigs.k8s.io/docs/concepts/workload/#dynamic-reclaim)
+in the Workload status.
+
+### Retrying Failed Pods
+
+The Pod group will generally only be considered finished if all the Pods finish
```

  I decided to go with `retriable-in-group: false`, similar to your proposal.
  But I also added another mechanism for termination: just delete the Workload.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-11-20T18:00:12Z

- **keps/976-plain-pods/README.md:378** by [@achernevskii](https://github.com/achernevskii) — 2023-11-20T17:42:21Z
```diff
@@ -372,12 +371,11 @@ matchExpressions:
 Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
 the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
 
-Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
-delete the Workload object, even before the Pod terminates (if it has a grace period).
-This means that the controller needs to manually delete the Workload object once it has the
-Finished condition (after we have determined that all pods have finished).
+The Workload will be owned by all Pods. Once all the Pods that own the workload are deleted (and
+their finalizers are removed), the Workload will be automatically cleaned up.
 
-A possible extension here is to add a TTL, which we can consider based on user feedback.
+If individual Pods in the group fail and a replacement Pod comes in, the replacement Pod will be
+added as an owner of the Workload as well.
```

  What should be done to a failed pod when it's replaced? Should it be deleted/finalized?
  
  Do we only support replacement for failed pods?

- **keps/976-plain-pods/README.md:560** by [@achernevskii](https://github.com/achernevskii) — 2023-11-20T17:49:45Z
```diff
@@ -538,39 +536,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
 1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished or if the Pods are
+Failed. This is a simple way of managing finalizers, but it might lead to too many Pods lingering
```

  ```suggestion
  Note that we are only removing Pod finalizers once the Workload is finished or if the Pods are
  Failed and replaced. This is a simple way of managing finalizers, but it might lead to too many Pods lingering
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-20T18:04:14Z

- **keps/976-plain-pods/README.md:378** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-20T18:04:14Z
```diff
@@ -372,12 +371,11 @@ matchExpressions:
 Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
 the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
 
-Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
-delete the Workload object, even before the Pod terminates (if it has a grace period).
-This means that the controller needs to manually delete the Workload object once it has the
-Finished condition (after we have determined that all pods have finished).
+The Workload will be owned by all Pods. Once all the Pods that own the workload are deleted (and
+their finalizers are removed), the Workload will be automatically cleaned up.
 
-A possible extension here is to add a TTL, which we can consider based on user feedback.
+If individual Pods in the group fail and a replacement Pod comes in, the replacement Pod will be
+added as an owner of the Workload as well.
```

  Nothing is done to a Failed pod until the Workload is declared finished.
  
  Yes, we only support replacement for Failed Pods. Although this is implicit, as we just don't count failed pods towards the podsets. We do for Succeeded Pods.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-20T18:06:47Z

- **keps/976-plain-pods/README.md:560** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-20T18:06:47Z
```diff
@@ -538,39 +536,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
 1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished or if the Pods are
+Failed. This is a simple way of managing finalizers, but it might lead to too many Pods lingering
```

  Oops, I actually wanted to say that we remove finalizers only when the workload is finished.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-24T04:33:23Z

I'm wondering if it is possible to configurable conditions to say that the job failed. 
In a group of pods, users may want to mark the job as a failure if the driver pod fails to start.

However, I'm not sure if it would be worth it. Maybe we should suggest users migrate to batch/job or custom job.

- **keps/976-plain-pods/README.md:378** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-24T03:52:23Z
```diff
@@ -372,12 +371,11 @@ matchExpressions:
 Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
 the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
 
-Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
-delete the Workload object, even before the Pod terminates (if it has a grace period).
-This means that the controller needs to manually delete the Workload object once it has the
-Finished condition (after we have determined that all pods have finished).
+The Workload will be owned by all Pods. Once all the Pods that own the workload are deleted (and
+their finalizers are removed), the Workload will be automatically cleaned up.
 
-A possible extension here is to add a TTL, which we can consider based on user feedback.
+If individual Pods in the group fail and a replacement Pod comes in, the replacement Pod will be
+added as an owner of the Workload as well.
```

  What happens if the dynamically reclaiming of quota (DRQ) is enabled?
  I can imagine the following steps for the case:
  
  1. A pod fails
  2. release resources for the failed pod
  3. replacement pod come in
  
  I guess that replacement pod will be unsuspended before being admitted by queues, right?
  It means that the replacement pod will violate the order of queue.
  
  I think that we need to move forward https://github.com/kubernetes-sigs/kueue/issues/77 before DRQ is supported in the plain pod integration. WDYT?

- **keps/976-plain-pods/README.md:438** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-24T04:02:10Z
```diff
@@ -433,9 +431,12 @@ scheduling. The list of fields to keep are:
   - `preemptionPolicy`
   - `topologySpreadConstraints`
   - `overhead`
-  - `volumes`
   - `resourceClaims`
 
+Note that fields like `env` and `command` can sometimes change among all the pods of a group and
+they don't influence scheduling, so they are safe to skip. `volumes` can influence scheduling, but
+they can be parameterized, like in StatefulSets, so we will ignore them for now.
```

  Currently, we compare the entire containers to verify if the existing workload matches the desired workload:
  
  https://github.com/kubernetes-sigs/kueue/blob/1ecd79f748af2617111d0bfa1ca848f9e172f4d7/pkg/util/equality/podset.go#L29-L34
  
  So, I'm wondering if we should update envs and commands. WDYT?

- **keps/976-plain-pods/README.md:548** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-24T04:13:55Z
```diff
@@ -538,39 +539,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
```

  What happens if a cached existing Workload has larger than pod counters than an in-memory Workload?
  Will The reconciler evict the Workload the same as smaller case?

- **keps/976-plain-pods/README.md:564** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-24T04:18:09Z
```diff
@@ -538,39 +539,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
-1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+1. If the Pod is not terminated and doesn't have a deletionTimestamp,
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
+managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated.
```

  Why did you remove this future work?
  
  > > In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).

- **keps/976-plain-pods/README.md:586** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-24T04:23:42Z
```diff
@@ -538,39 +539,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
-1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+1. If the Pod is not terminated and doesn't have a deletionTimestamp,
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
+managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated.
+
+### Retrying Failed Pods
+
+The Pod group will generally only be considered finished if all the Pods finish with a Succeeded
+phase.
+This allows the user to send replacement Pods when a Pod in the group fails or if the group is
+preempted. The replacement Pods can have any name, but they must point to the same pod group.
+
+To declare that a group is failed, a user can execute one of the following actions:
+1. Issue a Delete for the Workload object. The controller would terminate all running Pods and
+   clean up Pod finalizers.
+2. Add an annotation to any Pod in the group `kueue.x-k8s.io/retriable-in-group: false`.
+  The annotation can be added to an existing Pod or added on creation.
+
+  Kueue will consider a group finished if there are no running or pending Pods, and at
+  least one terminated Pod (Failed or Succeeded) has the `retriable-in-group: false` annotation.
+
+### Dynamically reclaiming Quota
+
+Succeeded Pods will not be considered replaceable. In other words, the quota
+from Succeeded Pods will be released by filling [reclaimablePods](https://kueue.sigs.k8s.io/docs/concepts/workload/#dynamic-reclaim)
+in the Workload status.
```

  It might be useful to support the Dynamic reclaiming Quota for the Failed pods as well when `retriable-in-group` is false.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-27T21:49:14Z

- **keps/976-plain-pods/README.md:586** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:49:14Z
```diff
@@ -538,39 +539,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
-1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+1. If the Pod is not terminated and doesn't have a deletionTimestamp,
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
+managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated.
+
+### Retrying Failed Pods
+
+The Pod group will generally only be considered finished if all the Pods finish with a Succeeded
+phase.
+This allows the user to send replacement Pods when a Pod in the group fails or if the group is
+preempted. The replacement Pods can have any name, but they must point to the same pod group.
+
+To declare that a group is failed, a user can execute one of the following actions:
+1. Issue a Delete for the Workload object. The controller would terminate all running Pods and
+   clean up Pod finalizers.
+2. Add an annotation to any Pod in the group `kueue.x-k8s.io/retriable-in-group: false`.
+  The annotation can be added to an existing Pod or added on creation.
+
+  Kueue will consider a group finished if there are no running or pending Pods, and at
+  least one terminated Pod (Failed or Succeeded) has the `retriable-in-group: false` annotation.
+
+### Dynamically reclaiming Quota
+
+Succeeded Pods will not be considered replaceable. In other words, the quota
+from Succeeded Pods will be released by filling [reclaimablePods](https://kueue.sigs.k8s.io/docs/concepts/workload/#dynamic-reclaim)
+in the Workload status.
```

  ah, that is a good point.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-27T21:49:48Z

- **keps/976-plain-pods/README.md:564** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:49:48Z
```diff
@@ -538,39 +539,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
-1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+1. If the Pod is not terminated and doesn't have a deletionTimestamp,
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
+managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated.
```

  Because that is a massive amount of work and Job solves it pretty well already.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-27T21:55:54Z

- **keps/976-plain-pods/README.md:586** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-27T21:55:53Z
```diff
@@ -538,39 +539,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
-1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+1. If the Pod is not terminated and doesn't have a deletionTimestamp,
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
+managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated.
+
+### Retrying Failed Pods
+
+The Pod group will generally only be considered finished if all the Pods finish with a Succeeded
+phase.
+This allows the user to send replacement Pods when a Pod in the group fails or if the group is
+preempted. The replacement Pods can have any name, but they must point to the same pod group.
+
+To declare that a group is failed, a user can execute one of the following actions:
+1. Issue a Delete for the Workload object. The controller would terminate all running Pods and
+   clean up Pod finalizers.
+2. Add an annotation to any Pod in the group `kueue.x-k8s.io/retriable-in-group: false`.
+  The annotation can be added to an existing Pod or added on creation.
+
+  Kueue will consider a group finished if there are no running or pending Pods, and at
+  least one terminated Pod (Failed or Succeeded) has the `retriable-in-group: false` annotation.
+
+### Dynamically reclaiming Quota
+
+Succeeded Pods will not be considered replaceable. In other words, the quota
+from Succeeded Pods will be released by filling [reclaimablePods](https://kueue.sigs.k8s.io/docs/concepts/workload/#dynamic-reclaim)
+in the Workload status.
```

  Added

### Review by [@ahaysx](https://github.com/ahaysx) (commented) — 2023-11-28T02:43:34Z

- **keps/976-plain-pods/README.md:566** by [@ahaysx](https://github.com/ahaysx) — 2023-11-28T02:43:34Z
```diff
@@ -538,39 +539,54 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
-1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+1. If the Pod is not terminated and doesn't have a deletionTimestamp,
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
+managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated.
+
+### Retrying Failed Pods
```

  Would you consider supporting individual pod retries? 
  
  In this mode, the external controller manages pod retries by uniquely identifying a pod with an "index" label, where index is `[0,count)`. This would be useful for controllers which are grouping roughly homogenous jobs together but each have different retry specifications. 
  
  When the controller no longer wants to retry an index, it adds the `retriable-in-group: false` annotation to the last pod submitted with the index. You then consider a group finished when an index has succeeded or has a pod with that annotation.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-28T13:27:25Z

- **keps/976-plain-pods/README.md:566** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T13:27:25Z
```diff
@@ -538,39 +539,54 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
-1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+1. If the Pod is not terminated and doesn't have a deletionTimestamp,
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
+managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated.
+
+### Retrying Failed Pods
```

  I would prefer not.
  We have Indexed Jobs for that, which has all these powerful semantics. It would be extremely complex to implement this again, and even worse without a backing CRD.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-28T13:28:13Z

- **keps/976-plain-pods/README.md:566** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T13:28:13Z
```diff
@@ -538,39 +539,54 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
-1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+1. If the Pod is not terminated and doesn't have a deletionTimestamp,
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
+managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated.
+
+### Retrying Failed Pods
```

  Example of a powerful feature: https://kubernetes.io/docs/concepts/workloads/controllers/job/#backoff-limit-per-index

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-29T08:12:14Z

- **keps/976-plain-pods/README.md:564** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T08:12:14Z
```diff
@@ -538,39 +539,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
 
 In the Pod-group reconciler:
-1. If the Pod is not terminated,
-  create a Workload for the pod group if one does not exist.
-2. If the Pod is terminated,
-   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
-3. ungated_pods_in_client: the number of non-terminated pods in the client that are admitted.
-   We only look at non-terminated pods to allow for terminated pods to be replaced.
-4. ungated_pods = ungated_pods_in_client + expected_ungated_pods. Note that this might temporarily
-   lead to double counting.
-2. For gated pods:
-  - If ungated_pods < admission.count, remove the gate, set nodeSelector, an increase
-    expected_ungated_pods
-  - Else,
-    - If ungated_pods_in_informer < admission.count, we can't admit this Pod now to prevent
-      overbooking, but requeue this Pod for retry.
-    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
-5. If the number of terminated pods with a finalizer is greater than or equal to the admission
-  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
-  finalizers from the Pods.
-
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
-of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
+1. If the Pod is not terminated and doesn't have a deletionTimestamp,
+   create a Workload for the pod group if one does not exist.
+2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
+   timestamp or is finished.
+3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
+   then evict the Workload.
+4. For gated pods:
+   - remove the gate, set nodeSelector
+5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
+   and remove the finalizers from the Pods.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
+managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated.
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-29T20:21:08Z

- **keps/976-plain-pods/README.md:378** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T20:21:08Z
```diff
@@ -372,12 +371,11 @@ matchExpressions:
 Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
 the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
 
-Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
-delete the Workload object, even before the Pod terminates (if it has a grace period).
-This means that the controller needs to manually delete the Workload object once it has the
-Finished condition (after we have determined that all pods have finished).
+The Workload will be owned by all Pods. Once all the Pods that own the workload are deleted (and
+their finalizers are removed), the Workload will be automatically cleaned up.
 
-A possible extension here is to add a TTL, which we can consider based on user feedback.
+If individual Pods in the group fail and a replacement Pod comes in, the replacement Pod will be
+added as an owner of the Workload as well.
```

  This will be adapted by `kueue.x-k8s.io/retriable-in-group`. If anyone interested in this concern, you can refer to "Dynamically reclaiming Quota" section.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T20:45:55Z

- **keps/976-plain-pods/README.md:378** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T20:45:55Z
```diff
@@ -372,12 +371,11 @@ matchExpressions:
 Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
 the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
 
-Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
-delete the Workload object, even before the Pod terminates (if it has a grace period).
-This means that the controller needs to manually delete the Workload object once it has the
-Finished condition (after we have determined that all pods have finished).
+The Workload will be owned by all Pods. Once all the Pods that own the workload are deleted (and
+their finalizers are removed), the Workload will be automatically cleaned up.
 
-A possible extension here is to add a TTL, which we can consider based on user feedback.
+If individual Pods in the group fail and a replacement Pod comes in, the replacement Pod will be
+added as an owner of the Workload as well.
```

  Oops, forgot to answer here:
  By default, DRQ only applies to Succeeded Pods, not Failed ones.
  
  But yes, also for Failed when there is a Pod with `retriable-in-group: false`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T20:48:01Z

- **keps/976-plain-pods/README.md:438** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T20:48:01Z
```diff
@@ -433,9 +431,12 @@ scheduling. The list of fields to keep are:
   - `preemptionPolicy`
   - `topologySpreadConstraints`
   - `overhead`
-  - `volumes`
   - `resourceClaims`
 
+Note that fields like `env` and `command` can sometimes change among all the pods of a group and
+they don't influence scheduling, so they are safe to skip. `volumes` can influence scheduling, but
+they can be parameterized, like in StatefulSets, so we will ignore them for now.
```

  We could, but I'm not sure about the usefulness.
  
  In Pod groups, it is useful because each Pod might have a slightly different spec. But in Jobs, there is only one template.
  
  But that is a separate discussion nevertheless.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T20:49:03Z

- **keps/976-plain-pods/README.md:548** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T20:49:03Z
```diff
@@ -538,39 +539,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
```

  We only evict if the counters in the Workload are smaller, not larger.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-29T21:00:20Z

- **keps/976-plain-pods/README.md:438** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T21:00:20Z
```diff
@@ -433,9 +431,12 @@ scheduling. The list of fields to keep are:
   - `preemptionPolicy`
   - `topologySpreadConstraints`
   - `overhead`
-  - `volumes`
   - `resourceClaims`
 
+Note that fields like `env` and `command` can sometimes change among all the pods of a group and
+they don't influence scheduling, so they are safe to skip. `volumes` can influence scheduling, but
+they can be parameterized, like in StatefulSets, so we will ignore them for now.
```

  It sounds reasonable. I may be able to have discussions about this.
  Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-29T21:04:38Z

- **keps/976-plain-pods/README.md:548** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-29T21:04:37Z
```diff
@@ -538,39 +539,51 @@ spec:
 Pods need to have finalizers so that we can reliably track how many of them run to completion and be
 able to determine when the Workload is Finished.
 
-A Pod-group reconciler would keep track of the groups of Pods and their respective Workload object,
-based on the `jobframework.Reconciler`.
-After a Workload is admitted, as the Pod-group reconciler ungates pods, it would keep an in-memory
-cache of expected ungated pods: the number of ungated pods that are not reflected in the informers
-yet, per Pod-group. This number decreases as the event handler observes Pods transition from being
-gated to ungated.
+The Pod reconciler will run in a "composable" mode: a mode where a Workload is composed of multiple
+objects. The `jobframework.Reconciler` will be reworked to accomodate this.
 
-In the Pod event handler, we decrement the counter when we see a transition from having
-the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+After a Workload is admitted, each Pod that owns the workload enters the reconciliation loop.
+The reconciliation loop collects all the Pods that are not Failed and constructs an in-memory
+Workload. If there is an existing Workload in the cache and it has smaller Pod counters than the
+in-memory Workload, then it is considered unmatching and the Workload is evicted.
```

  It makes sense. Thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-29T21:05:06Z

Thanks!
/lgtm
