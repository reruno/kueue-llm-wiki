# PR #1354: Introduce util admissioncheck package and make ConfigHelper generic

**Summary**: Introduce util admissioncheck package and make ConfigHelper generic

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1354

**Last updated**: 2023-12-14T11:17:44Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `utils-acc` → `main`
- **Created**: 2023-11-21T07:28:12Z
- **Updated**: 2023-12-14T11:17:44Z
- **Merged**: 2023-12-11T15:42:03Z
- **Closed**: 2023-12-11T15:42:03Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 10   **+439 / -134**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Crete an utility package helpful for admission check controller development. Containing the generic form of  some ProvisioningAdmissionCheck controller functionality that is useful in other Admission check controllers.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to  #693 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-21T07:28:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 43256007ecbe5c21e685d938f1b8309accdd7253 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/656f12e0f70837000810d148 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-04T16:21:09Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1354#pullrequestreview-1762717109" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1354#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-05T07:51:29Z

Proposal: feel free to adjust, but I think the generalization is worth to emphasize

/retitle Introduce util admissioncheck package and make ConfigHelper generic

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-11T15:15:30Z

/lgtm
/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-11T15:15:38Z

LGTM label has been added.  <details>Git tree hash: bebdfe88ad759696aeb89a36aadacb872edb7e8b</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-29T11:29:29Z

First pass, without tests. My main ask would be to clarify the scope of the PR. The title and description suggest this PR is to create the AdmissionCheck util package, but I see also changes generalizing the ProvisioningRequestConfig helper. I'm not sure why this is needed, please make it clear in the title / description, or send a dedicated PR for the generalization.

- **pkg/controller/admissionchecks/provisioning/controller.go:609** by [@mimowo](https://github.com/mimowo) — 2023-11-29T11:05:09Z
```diff
@@ -606,7 +610,7 @@ func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q
 }
 
 type prcHandler struct {
-	helper            *storeHelper
```

  Why we move the reference to the storeHelper from prcHandler to the Controller?

- **pkg/controller/admissionchecks/provisioning/controller.go:689** by [@mimowo](https://github.com/mimowo) — 2023-11-29T11:05:45Z
```diff
@@ -676,14 +681,21 @@ func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string,
 }
 
 func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	helper, err := newProvStoreHelper(c.client)
+	if err != nil {
+		return err
+	}
+
+	c.helper = helper
```

  Can we avoid mutating the controller here? Related to the other comment about moving the reference of the helper.

- **pkg/controller/admissionchecks/provisioning/indexer.go:65** by [@mimowo](https://github.com/mimowo) — 2023-11-29T11:06:36Z
```diff
@@ -52,23 +54,20 @@ func indexWorkloadsChecks(obj client.Object) []string {
 	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
 }
 
-func indexAdmissionCheckConnfig(obj client.Object) []string {
-	ac, isAc := obj.(*kueue.AdmissionCheck)
-	if !isAc || ac.Spec.ControllerName != ControllerName || !parametersRefValid(ac.Spec.Parameters) {
-		return nil
-	}
-
-	return []string{ac.Spec.Parameters.Name}
-}
-
-func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer, schema *runtime.Scheme) error {
 	if err := indexer.IndexField(ctx, &autoscaling.ProvisioningRequest{}, RequestsOwnedByWorkloadKey, indexRequestsOwner); err != nil {
 		return fmt.Errorf("setting index on provisionRequest owner: %w", err)
 	}
 	if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadsWithAdmissionCheckKey, indexWorkloadsChecks); err != nil {
 		return fmt.Errorf("setting index on workloads checks: %w", err)
 	}
-	if err := indexer.IndexField(ctx, &kueue.AdmissionCheck{}, AdmissionCheckUsingConfigKey, indexAdmissionCheckConnfig); err != nil {
+
+	idxFnc, err := admissioncheck.GetIndexerByConfigFnc(ControllerName, &kueue.ProvisioningRequestConfig{}, schema)
```

  We pass the empty `ProvisioningRequestConfig` object just to retrieve its GVK. Can we pass the GVK directly?

- **cmd/kueue/main.go:202** by [@mimowo](https://github.com/mimowo) — 2023-11-29T11:08:34Z
```diff
@@ -194,7 +194,7 @@ func setupIndexes(ctx context.Context, mgr ctrl.Manager, cfg *configapi.Configur
 	if features.Enabled(features.ProvisioningACC) {
 		if !provisioning.ServerSupportsProvisioningRequest(mgr) {
 			setupLog.Error(nil, "Provisioning Requests are not supported, skipped admission check controller setup")
-		} else if err := provisioning.SetupIndexer(ctx, mgr.GetFieldIndexer()); err != nil {
+		} else if err := provisioning.SetupIndexer(ctx, mgr.GetFieldIndexer(), mgr.GetScheme()); err != nil {
```

  This change might not be necessary if we pass the GVK instead of the empty `ProvisioningRequestConfig` object?

- **pkg/util/admissioncheck/admissioncheck.go:38** by [@mimowo](https://github.com/mimowo) — 2023-11-29T11:09:48Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
```

  Why are we generalizing the ConfigHelper? Don't do it if not needed, if needed it should be reflected in the title and description of the PR.

- **pkg/util/admissioncheck/admissioncheck.go:74** by [@mimowo](https://github.com/mimowo) — 2023-11-29T11:22:23Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any](c client.Client) (*ConfigHelper[PCT, CT], error) {
+	helper := ConfigHelper[PCT, CT]{
+		client: c,
+	}
+
+	gvk, err := apiutil.GVKForObject(helper.newConfigPtr(), c.Scheme())
+	if err != nil {
+		return nil, err
+	}
+
+	helper.gk = gvk.GroupKind()
+	return &helper, nil
+}
+
+func (sh *ConfigHelper[PCT, CT]) newConfigPtr() PCT {
+	return PCT(new(CT))
+}
+
+// IsValidConfigReference - checks if the provided reference is addressing an object specific to this helper.
+func (sh *ConfigHelper[PCT, CT]) IsValidConfigReference(ref *kueue.AdmissionCheckParametersReference) (bool, error) {
+	return refValidForGK(ref, sh.gk)
+}
+
+// ConfigByName - get the config identified by its name
+func (sh *ConfigHelper[PCT, CT]) ConfigByName(ctx context.Context, name string) (PCT, error) {
+	cnf := sh.newConfigPtr()
```

  What `cnf` stands for? Maybe we could have the name more descriptive?

- **pkg/util/admissioncheck/admissioncheck.go:46** by [@mimowo](https://github.com/mimowo) — 2023-11-29T11:27:20Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PCT interface {
```

  What does `PCT` stand for? ProvisioningConfigType? I'm wondering if the `Provisioning` is obvious here, since the implementation in the file generally does not use it. Maybe the type should be called `ProvisioningConfigHelper` in that case?

- **pkg/util/admissioncheck/admissioncheck.go:46** by [@mimowo](https://github.com/mimowo) — 2023-11-29T11:28:30Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PCT interface {
```

  Can we avoid duplication of the declaration of the type here and above as `[PCT interface { client.Object, *CT }`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-04T11:45:08Z

- **pkg/util/admissioncheck/admissioncheck.go:38** by [@mimowo](https://github.com/mimowo) — 2023-12-04T11:45:08Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
```

  Oh I see, the helper is generalized in this WIP PR (https://github.com/kubernetes-sigs/kueue/pull/1313/files) to support the new `DelegateConfig` type. However, it is not clear for me we need this type. Is it KEPed / discussed somewhere we could reference? 
  
  Actually, IIUC, the currently review-pending KEP (https://github.com/kubernetes-sigs/kueue/pull/1380/files) assumes the configurations are synchronized (copied) by the user, so maybe there is no reason to introduce the delegating config? From non-goals: 
  ```
  * Synchronize configuration across the clusters. It is expected that the 
  user will create the appropriate objects, roles and permissions
  in the clusters (manually, using gitops or some 3rd-party tooling).
  ```
  
  If not, I suggest to scope this PR just to the package change, which is good on its own, because AdmissionChecks are not just for provisioning.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-04T13:59:30Z

- **cmd/kueue/main.go:202** by [@trasc](https://github.com/trasc) — 2023-12-04T06:51:20Z
```diff
@@ -194,7 +194,7 @@ func setupIndexes(ctx context.Context, mgr ctrl.Manager, cfg *configapi.Configur
 	if features.Enabled(features.ProvisioningACC) {
 		if !provisioning.ServerSupportsProvisioningRequest(mgr) {
 			setupLog.Error(nil, "Provisioning Requests are not supported, skipped admission check controller setup")
-		} else if err := provisioning.SetupIndexer(ctx, mgr.GetFieldIndexer()); err != nil {
+		} else if err := provisioning.SetupIndexer(ctx, mgr.GetFieldIndexer(), mgr.GetScheme()); err != nil {
```

  It could work like that as well, but we'll most likely need to hard-code the group kind in  `SetupIndexer`.

- **pkg/controller/admissionchecks/provisioning/controller.go:609** by [@trasc](https://github.com/trasc) — 2023-12-04T06:53:23Z
```diff
@@ -606,7 +610,7 @@ func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q
 }
 
 type prcHandler struct {
-	helper            *storeHelper
```

  prcHandler no longer needs the helper.

- **pkg/controller/admissionchecks/provisioning/controller.go:689** by [@trasc](https://github.com/trasc) — 2023-12-04T07:08:29Z
```diff
@@ -676,14 +681,21 @@ func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string,
 }
 
 func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	helper, err := newProvStoreHelper(c.client)
+	if err != nil {
+		return err
+	}
+
+	c.helper = helper
```

  done

- **pkg/controller/admissionchecks/provisioning/indexer.go:65** by [@trasc](https://github.com/trasc) — 2023-12-04T07:11:45Z
```diff
@@ -52,23 +54,20 @@ func indexWorkloadsChecks(obj client.Object) []string {
 	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
 }
 
-func indexAdmissionCheckConnfig(obj client.Object) []string {
-	ac, isAc := obj.(*kueue.AdmissionCheck)
-	if !isAc || ac.Spec.ControllerName != ControllerName || !parametersRefValid(ac.Spec.Parameters) {
-		return nil
-	}
-
-	return []string{ac.Spec.Parameters.Name}
-}
-
-func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer, schema *runtime.Scheme) error {
 	if err := indexer.IndexField(ctx, &autoscaling.ProvisioningRequest{}, RequestsOwnedByWorkloadKey, indexRequestsOwner); err != nil {
 		return fmt.Errorf("setting index on provisionRequest owner: %w", err)
 	}
 	if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadsWithAdmissionCheckKey, indexWorkloadsChecks); err != nil {
 		return fmt.Errorf("setting index on workloads checks: %w", err)
 	}
-	if err := indexer.IndexField(ctx, &kueue.AdmissionCheck{}, AdmissionCheckUsingConfigKey, indexAdmissionCheckConnfig); err != nil {
+
+	idxFnc, err := admissioncheck.GetIndexerByConfigFnc(ControllerName, &kueue.ProvisioningRequestConfig{}, schema)
```

  We'll need to hardcode or retrive the GVK for all admission check controllers using `admissioncheck.GetIndexerByConfigFnc`.

- **pkg/util/admissioncheck/admissioncheck.go:46** by [@trasc](https://github.com/trasc) — 2023-12-04T07:17:32Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PCT interface {
```

  PCT = Pointer to Config Type , CT = Config Type,  (renamed to PtrConfT and ConfT )

- **pkg/util/admissioncheck/admissioncheck.go:46** by [@trasc](https://github.com/trasc) — 2023-12-04T07:20:13Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PCT interface {
```

  I was not able to find a way.

- **pkg/util/admissioncheck/admissioncheck.go:74** by [@trasc](https://github.com/trasc) — 2023-12-04T07:21:38Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any](c client.Client) (*ConfigHelper[PCT, CT], error) {
+	helper := ConfigHelper[PCT, CT]{
+		client: c,
+	}
+
+	gvk, err := apiutil.GVKForObject(helper.newConfigPtr(), c.Scheme())
+	if err != nil {
+		return nil, err
+	}
+
+	helper.gk = gvk.GroupKind()
+	return &helper, nil
+}
+
+func (sh *ConfigHelper[PCT, CT]) newConfigPtr() PCT {
+	return PCT(new(CT))
+}
+
+// IsValidConfigReference - checks if the provided reference is addressing an object specific to this helper.
+func (sh *ConfigHelper[PCT, CT]) IsValidConfigReference(ref *kueue.AdmissionCheckParametersReference) (bool, error) {
+	return refValidForGK(ref, sh.gk)
+}
+
+// ConfigByName - get the config identified by its name
+func (sh *ConfigHelper[PCT, CT]) ConfigByName(ctx context.Context, name string) (PCT, error) {
+	cnf := sh.newConfigPtr()
```

  done

- **pkg/util/admissioncheck/admissioncheck.go:38** by [@trasc](https://github.com/trasc) — 2023-12-04T07:56:31Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
```

  We'll use the generic form for multikueue as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-04T15:21:31Z

- **pkg/util/admissioncheck/admissioncheck.go:46** by [@mimowo](https://github.com/mimowo) — 2023-12-04T15:21:31Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PCT interface {
```

  renaming is fine, a comment would do as well

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-04T15:25:56Z

- **cmd/kueue/main.go:202** by [@mimowo](https://github.com/mimowo) — 2023-12-04T15:25:55Z
```diff
@@ -194,7 +194,7 @@ func setupIndexes(ctx context.Context, mgr ctrl.Manager, cfg *configapi.Configur
 	if features.Enabled(features.ProvisioningACC) {
 		if !provisioning.ServerSupportsProvisioningRequest(mgr) {
 			setupLog.Error(nil, "Provisioning Requests are not supported, skipped admission check controller setup")
-		} else if err := provisioning.SetupIndexer(ctx, mgr.GetFieldIndexer()); err != nil {
+		} else if err := provisioning.SetupIndexer(ctx, mgr.GetFieldIndexer(), mgr.GetScheme()); err != nil {
```

  but `SetupIndexer` anyway has the type `ProvisioningRequestConfig` hardcoded. Going via creating the object based on type, then getting back type from the object seems unnecessary. 
  
  We do something similar for the various job integrations, for example here we memorize the gvk for MPIJob and use in `SetupIndexer` function: https://github.com/kubernetes-sigs/kueue/blob/a2fffa4686c47fada07d8a02566408566c5e163b/pkg/controller/jobs/mpijob/mpijob_controller.go#L201C60-L201C63/

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-04T15:29:52Z

- **pkg/controller/admissionchecks/provisioning/indexer.go:65** by [@mimowo](https://github.com/mimowo) — 2023-12-04T15:29:51Z
```diff
@@ -52,23 +54,20 @@ func indexWorkloadsChecks(obj client.Object) []string {
 	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
 }
 
-func indexAdmissionCheckConnfig(obj client.Object) []string {
-	ac, isAc := obj.(*kueue.AdmissionCheck)
-	if !isAc || ac.Spec.ControllerName != ControllerName || !parametersRefValid(ac.Spec.Parameters) {
-		return nil
-	}
-
-	return []string{ac.Spec.Parameters.Name}
-}
-
-func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer, schema *runtime.Scheme) error {
 	if err := indexer.IndexField(ctx, &autoscaling.ProvisioningRequest{}, RequestsOwnedByWorkloadKey, indexRequestsOwner); err != nil {
 		return fmt.Errorf("setting index on provisionRequest owner: %w", err)
 	}
 	if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadsWithAdmissionCheckKey, indexWorkloadsChecks); err != nil {
 		return fmt.Errorf("setting index on workloads checks: %w", err)
 	}
-	if err := indexer.IndexField(ctx, &kueue.AdmissionCheck{}, AdmissionCheckUsingConfigKey, indexAdmissionCheckConnfig); err != nil {
+
+	idxFnc, err := admissioncheck.GetIndexerByConfigFnc(ControllerName, &kueue.ProvisioningRequestConfig{}, schema)
```

  We may add in a future a more generic function if needed in the future, but the need seems questionable for now, since we can just pass the gvk, without going over the object.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-04T16:21:00Z

/approve
I will leave lgtm to @mimowo

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler_test.go:51** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-04T15:23:48Z
```diff
@@ -48,7 +48,7 @@ func TestReconcileAdmissionCheck(t *testing.T) {
 				Type:    kueue.AdmissionCheckActive,
 				Status:  metav1.ConditionFalse,
 				Reason:  "BadParametersRef",
-				Message: "Unexpected parameters reference",
+				Message: "nil parameters reference",
```

  ```suggestion
  				Message: "no parameters reference",
  ```
  
  Or `Parameters reference is required`. `nil` is golang specific, and the API is independent of the language. Another option is `null`, because the API is a json/yaml API.

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler_test.go:122** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-04T15:25:26Z
```diff
@@ -118,13 +118,11 @@ func TestReconcileAdmissionCheck(t *testing.T) {
 			builder = builder.WithLists(&kueue.ProvisioningRequestConfigList{Items: tc.configs})
 
 			k8sclient := builder.Build()
-			helper := storeHelper{
-				client: k8sclient,
-			}
 
+			helper, _ := newProvStoreHelper(k8sclient)
```

  ```suggestion
  			helper := newProvStoreHelper(k8sclient)
  ```
  
  Or `helper: newProvStoreHelper(k8sClient) below.

- **pkg/controller/admissionchecks/provisioning/controller.go:126** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-04T15:27:15Z
```diff
@@ -114,7 +123,7 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	}
 
 	// get the lists of relevant checks
-	relevantChecks, err := c.helper.FilterChecksForProvReq(ctx, wl.Status.AdmissionChecks)
+	relevantChecks, err := admissioncheck.ChecksWithController(ctx, c.client, wl.Status.AdmissionChecks, ControllerName)
```

  ```suggestion
  	relevantChecks, err := admissioncheck.FilterForController(ctx, c.client, wl.Status.AdmissionChecks, ControllerName)
  ```

- **pkg/controller/admissionchecks/provisioning/indexer.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-04T15:31:27Z
```diff
@@ -52,23 +54,20 @@ func indexWorkloadsChecks(obj client.Object) []string {
 	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
 }
 
-func indexAdmissionCheckConnfig(obj client.Object) []string {
-	ac, isAc := obj.(*kueue.AdmissionCheck)
-	if !isAc || ac.Spec.ControllerName != ControllerName || !parametersRefValid(ac.Spec.Parameters) {
-		return nil
-	}
-
-	return []string{ac.Spec.Parameters.Name}
-}
-
-func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer, schema *runtime.Scheme) error {
 	if err := indexer.IndexField(ctx, &autoscaling.ProvisioningRequest{}, RequestsOwnedByWorkloadKey, indexRequestsOwner); err != nil {
 		return fmt.Errorf("setting index on provisionRequest owner: %w", err)
 	}
 	if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadsWithAdmissionCheckKey, indexWorkloadsChecks); err != nil {
 		return fmt.Errorf("setting index on workloads checks: %w", err)
 	}
-	if err := indexer.IndexField(ctx, &kueue.AdmissionCheck{}, AdmissionCheckUsingConfigKey, indexAdmissionCheckConnfig); err != nil {
+
+	idxFnc, err := admissioncheck.GetIndexerByConfigFnc(ControllerName, &kueue.ProvisioningRequestConfig{}, schema)
```

  ```suggestion
  	idxFnc, err := admissioncheck.IndexerByConfigFunction(ControllerName, &kueue.ProvisioningRequestConfig{}, schema)
  ```

- **pkg/controller/admissionchecks/provisioning/indexer.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-04T15:32:45Z
```diff
@@ -52,23 +54,20 @@ func indexWorkloadsChecks(obj client.Object) []string {
 	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
 }
 
-func indexAdmissionCheckConnfig(obj client.Object) []string {
-	ac, isAc := obj.(*kueue.AdmissionCheck)
-	if !isAc || ac.Spec.ControllerName != ControllerName || !parametersRefValid(ac.Spec.Parameters) {
-		return nil
-	}
-
-	return []string{ac.Spec.Parameters.Name}
-}
-
-func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer, schema *runtime.Scheme) error {
 	if err := indexer.IndexField(ctx, &autoscaling.ProvisioningRequest{}, RequestsOwnedByWorkloadKey, indexRequestsOwner); err != nil {
 		return fmt.Errorf("setting index on provisionRequest owner: %w", err)
 	}
 	if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadsWithAdmissionCheckKey, indexWorkloadsChecks); err != nil {
 		return fmt.Errorf("setting index on workloads checks: %w", err)
 	}
-	if err := indexer.IndexField(ctx, &kueue.AdmissionCheck{}, AdmissionCheckUsingConfigKey, indexAdmissionCheckConnfig); err != nil {
+
+	idxFnc, err := admissioncheck.GetIndexerByConfigFnc(ControllerName, &kueue.ProvisioningRequestConfig{}, schema)
```

  or maybe the call to `indexer.IndexField` should be part of the helper package?
  
  Then it can be called `admissioncheck.IndexByConfig`

- **pkg/util/admissioncheck/admissioncheck.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-04T16:13:32Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any](c client.Client) (*ConfigHelper[PtrConfT, ConfT], error) {
+	helper := ConfigHelper[PtrConfT, ConfT]{
+		client: c,
+	}
+
+	gvk, err := apiutil.GVKForObject(helper.newConfigPtr(), c.Scheme())
+	if err != nil {
+		return nil, err
+	}
+
+	helper.gk = gvk.GroupKind()
+	return &helper, nil
+}
+
+func (sh *ConfigHelper[PtrConfT, ConfT]) newConfigPtr() PtrConfT {
```

  ```suggestion
  func (ch *ConfigHelper[PtrConfT, ConfT]) newConfigPtr() PtrConfT {
  ```

- **pkg/util/admissioncheck/admissioncheck.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-04T16:19:02Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any] struct {
```

  ```suggestion
  type ConfigHelper[PtrT interface { client.Object; *T}, T any] struct {
  ```
  
  Maybe this? But I'll leave it up to you.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-04T17:02:29Z

- **pkg/util/admissioncheck/admissioncheck.go:41** by [@trasc](https://github.com/trasc) — 2023-12-04T17:02:29Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any] struct {
```

  it is not up to me, `gofmt` has its own mind :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-04T18:39:40Z

- **pkg/util/admissioncheck/admissioncheck.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-04T18:39:40Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any] struct {
```

  oh I see. Still, `T` and `PtrT` should be enough

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-05T06:45:24Z

- **pkg/util/admissioncheck/admissioncheck.go:41** by [@trasc](https://github.com/trasc) — 2023-12-05T06:45:24Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any] struct {
```

  @mimowo are you OK with `T` and `PtrT`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-05T07:49:22Z

- **pkg/util/admissioncheck/admissioncheck.go:41** by [@mimowo](https://github.com/mimowo) — 2023-12-05T07:49:22Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any] struct {
```

  Yes, my confusion was about the `P` which I thought initially stands for Provisioning

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-05T07:53:48Z

- **pkg/util/admissioncheck/admissioncheck.go:46** by [@mimowo](https://github.com/mimowo) — 2023-12-05T07:53:48Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PCT interface {
```

  ok

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-05T07:59:35Z

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler_test.go:51** by [@trasc](https://github.com/trasc) — 2023-12-05T06:50:10Z
```diff
@@ -48,7 +48,7 @@ func TestReconcileAdmissionCheck(t *testing.T) {
 				Type:    kueue.AdmissionCheckActive,
 				Status:  metav1.ConditionFalse,
 				Reason:  "BadParametersRef",
-				Message: "Unexpected parameters reference",
+				Message: "nil parameters reference",
```

  let's go with `missing`

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler_test.go:122** by [@trasc](https://github.com/trasc) — 2023-12-05T07:04:45Z
```diff
@@ -118,13 +118,11 @@ func TestReconcileAdmissionCheck(t *testing.T) {
 			builder = builder.WithLists(&kueue.ProvisioningRequestConfigList{Items: tc.configs})
 
 			k8sclient := builder.Build()
-			helper := storeHelper{
-				client: k8sclient,
-			}
 
+			helper, _ := newProvStoreHelper(k8sclient)
```

  I still want to check the potential error

- **pkg/controller/admissionchecks/provisioning/controller.go:126** by [@trasc](https://github.com/trasc) — 2023-12-05T07:06:11Z
```diff
@@ -114,7 +123,7 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	}
 
 	// get the lists of relevant checks
-	relevantChecks, err := c.helper.FilterChecksForProvReq(ctx, wl.Status.AdmissionChecks)
+	relevantChecks, err := admissioncheck.ChecksWithController(ctx, c.client, wl.Status.AdmissionChecks, ControllerName)
```

  done

- **pkg/controller/admissionchecks/provisioning/indexer.go:65** by [@trasc](https://github.com/trasc) — 2023-12-05T07:10:05Z
```diff
@@ -52,23 +54,20 @@ func indexWorkloadsChecks(obj client.Object) []string {
 	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
 }
 
-func indexAdmissionCheckConnfig(obj client.Object) []string {
-	ac, isAc := obj.(*kueue.AdmissionCheck)
-	if !isAc || ac.Spec.ControllerName != ControllerName || !parametersRefValid(ac.Spec.Parameters) {
-		return nil
-	}
-
-	return []string{ac.Spec.Parameters.Name}
-}
-
-func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer, schema *runtime.Scheme) error {
 	if err := indexer.IndexField(ctx, &autoscaling.ProvisioningRequest{}, RequestsOwnedByWorkloadKey, indexRequestsOwner); err != nil {
 		return fmt.Errorf("setting index on provisionRequest owner: %w", err)
 	}
 	if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadsWithAdmissionCheckKey, indexWorkloadsChecks); err != nil {
 		return fmt.Errorf("setting index on workloads checks: %w", err)
 	}
-	if err := indexer.IndexField(ctx, &kueue.AdmissionCheck{}, AdmissionCheckUsingConfigKey, indexAdmissionCheckConnfig); err != nil {
+
+	idxFnc, err := admissioncheck.GetIndexerByConfigFnc(ControllerName, &kueue.ProvisioningRequestConfig{}, schema)
```

  done

- **pkg/controller/admissionchecks/provisioning/indexer.go:65** by [@trasc](https://github.com/trasc) — 2023-12-05T07:17:36Z
```diff
@@ -52,23 +54,20 @@ func indexWorkloadsChecks(obj client.Object) []string {
 	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
 }
 
-func indexAdmissionCheckConnfig(obj client.Object) []string {
-	ac, isAc := obj.(*kueue.AdmissionCheck)
-	if !isAc || ac.Spec.ControllerName != ControllerName || !parametersRefValid(ac.Spec.Parameters) {
-		return nil
-	}
-
-	return []string{ac.Spec.Parameters.Name}
-}
-
-func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer, schema *runtime.Scheme) error {
 	if err := indexer.IndexField(ctx, &autoscaling.ProvisioningRequest{}, RequestsOwnedByWorkloadKey, indexRequestsOwner); err != nil {
 		return fmt.Errorf("setting index on provisionRequest owner: %w", err)
 	}
 	if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadsWithAdmissionCheckKey, indexWorkloadsChecks); err != nil {
 		return fmt.Errorf("setting index on workloads checks: %w", err)
 	}
-	if err := indexer.IndexField(ctx, &kueue.AdmissionCheck{}, AdmissionCheckUsingConfigKey, indexAdmissionCheckConnfig); err != nil {
+
+	idxFnc, err := admissioncheck.GetIndexerByConfigFnc(ControllerName, &kueue.ProvisioningRequestConfig{}, schema)
```

  the more generic approach wold have helped, however done

- **pkg/util/admissioncheck/admissioncheck.go:63** by [@trasc](https://github.com/trasc) — 2023-12-05T07:19:19Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any](c client.Client) (*ConfigHelper[PtrConfT, ConfT], error) {
+	helper := ConfigHelper[PtrConfT, ConfT]{
+		client: c,
+	}
+
+	gvk, err := apiutil.GVKForObject(helper.newConfigPtr(), c.Scheme())
+	if err != nil {
+		return nil, err
+	}
+
+	helper.gk = gvk.GroupKind()
+	return &helper, nil
+}
+
+func (sh *ConfigHelper[PtrConfT, ConfT]) newConfigPtr() PtrConfT {
```

  done

- **cmd/kueue/main.go:202** by [@trasc](https://github.com/trasc) — 2023-12-05T07:22:25Z
```diff
@@ -194,7 +194,7 @@ func setupIndexes(ctx context.Context, mgr ctrl.Manager, cfg *configapi.Configur
 	if features.Enabled(features.ProvisioningACC) {
 		if !provisioning.ServerSupportsProvisioningRequest(mgr) {
 			setupLog.Error(nil, "Provisioning Requests are not supported, skipped admission check controller setup")
-		} else if err := provisioning.SetupIndexer(ctx, mgr.GetFieldIndexer()); err != nil {
+		} else if err := provisioning.SetupIndexer(ctx, mgr.GetFieldIndexer(), mgr.GetScheme()); err != nil {
```

  done

- **pkg/util/admissioncheck/admissioncheck.go:46** by [@trasc](https://github.com/trasc) — 2023-12-05T07:38:00Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
+	client.Object
+	*CT
+}, CT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PCT interface {
```

  finally done

- **pkg/util/admissioncheck/admissioncheck.go:41** by [@trasc](https://github.com/trasc) — 2023-12-05T07:39:46Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any] struct {
```

  With the interface part simplified , I think we can also keep the `Conf` part.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-05T08:05:00Z

- **pkg/util/admissioncheck/admissioncheck.go:41** by [@trasc](https://github.com/trasc) — 2023-12-05T08:05:00Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PtrConfT interface {
+	client.Object
+	*ConfT
+}, ConfT any] struct {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-05T08:07:10Z

- **pkg/util/admissioncheck/admissioncheck.go:38** by [@trasc](https://github.com/trasc) — 2023-12-05T08:07:10Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
```

  #1313 and  #1380 will eventually get in sync

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-05T08:19:09Z

- **pkg/util/admissioncheck/admissioncheck.go:52** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:12:14Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("missing parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type objAsPtr[T any] interface {
+	client.Object
+	*T
+}
+
+type ConfigHelper[PtrConfT objAsPtr[ConfT], ConfT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PtrConfT objAsPtr[ConfT], ConfT any](c client.Client) (*ConfigHelper[PtrConfT, ConfT], error) {
+	helper := ConfigHelper[PtrConfT, ConfT]{
+		client: c,
+	}
+
+	gvk, err := apiutil.GVKForObject(helper.newConfigPtr(), c.Scheme())
```

  Can we eliminate retrieval of the GK from object? It would eliminate the need for returning error, and going via interim object.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-05T08:23:59Z

- **pkg/util/admissioncheck/admissioncheck_test.go:188** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:19:42Z
```diff
@@ -0,0 +1,241 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestConfigHelper(t *testing.T) {
+	testConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "className",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+			ManagedResources: []corev1.ResourceName{"cpu"},
+		},
+	}
+
+	cases := map[string]struct {
+		admissioncheck       *kueue.AdmissionCheck
+		config               *kueue.ProvisioningRequestConfig
+		targetAdmissionCheck string
+		wantConfig           *kueue.ProvisioningRequestConfig
+		wantError            error
+	}{
+		"admission check bad name": {
+			targetAdmissionCheck: "123",
+			wantError:            cmpopts.AnyError,
+		},
+		"no parameter reference": {
+			admissioncheck:       utiltesting.MakeAdmissionCheck("ac").Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrNilParametersRef,
+		},
+		"bad parameter reference, no name": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"bad parameter reference, bad group": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters("not-"+kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"bad parameter reference, bad kind": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "NptProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"config not found": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            cmpopts.AnyError,
+		},
+		"config found": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			config:               testConfig.DeepCopy(),
+			targetAdmissionCheck: "ac",
+			wantConfig:           testConfig.DeepCopy(),
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			builder := fake.NewClientBuilder().WithScheme(scheme)
+			if tc.admissioncheck != nil {
+				builder = builder.WithObjects(tc.admissioncheck)
+			}
+			if tc.config != nil {
+				builder = builder.WithObjects(tc.config)
+			}
+			client := builder.Build()
+			ctx := context.Background()
+
+			helper, err := NewConfigHelper[*kueue.ProvisioningRequestConfig](client)
+
+			if err != nil {
+				t.Fatalf("cannot built the helper: %s", err)
+			}
+
+			gotConfig, gotError := helper.ConfigForAdmissionCheck(ctx, tc.targetAdmissionCheck)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("unexpected config (-want/+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantConfig, gotConfig, cmpopts.IgnoreFields(kueue.ProvisioningRequestConfig{}, "TypeMeta", "ObjectMeta")); diff != "" {
+				t.Errorf("unexpected config (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestIndexerFunc(t *testing.T) {
+	cases := map[string]struct {
+		admissioncheck *kueue.AdmissionCheck
+		wantResult     []string
+	}{
+		"nil ac": {},
+		"wrong controller": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("other-controller").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config-name").
+				Obj(),
+		},
+		"wrong ref": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("test-controller").
+				Parameters(kueue.GroupVersion.Group, "NotProvisioningRequestConfig", "config-name").
+				Obj(),
+		},
+		"good": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("test-controller").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config-name").
+				Obj(),
+			wantResult: []string{"config-name"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+
+			indexFnc := IndexerByConfigFunction("test-controller", kueue.GroupVersion.WithKind("ProvisioningRequestConfig"))
+
+			gotResult := indexFnc(tc.admissioncheck)
+
+			if diff := cmp.Diff(tc.wantResult, gotResult); diff != "" {
+				t.Errorf("unexpected result (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestFilterCheckStates(t *testing.T) {
+
```

  nit: remove empty line

- **pkg/util/admissioncheck/admissioncheck_test.go:232** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:21:02Z
```diff
@@ -0,0 +1,241 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestConfigHelper(t *testing.T) {
+	testConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "className",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+			ManagedResources: []corev1.ResourceName{"cpu"},
+		},
+	}
+
+	cases := map[string]struct {
+		admissioncheck       *kueue.AdmissionCheck
+		config               *kueue.ProvisioningRequestConfig
+		targetAdmissionCheck string
+		wantConfig           *kueue.ProvisioningRequestConfig
+		wantError            error
+	}{
+		"admission check bad name": {
+			targetAdmissionCheck: "123",
+			wantError:            cmpopts.AnyError,
+		},
+		"no parameter reference": {
+			admissioncheck:       utiltesting.MakeAdmissionCheck("ac").Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrNilParametersRef,
+		},
+		"bad parameter reference, no name": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"bad parameter reference, bad group": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters("not-"+kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"bad parameter reference, bad kind": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "NptProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"config not found": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            cmpopts.AnyError,
+		},
+		"config found": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			config:               testConfig.DeepCopy(),
+			targetAdmissionCheck: "ac",
+			wantConfig:           testConfig.DeepCopy(),
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			builder := fake.NewClientBuilder().WithScheme(scheme)
+			if tc.admissioncheck != nil {
+				builder = builder.WithObjects(tc.admissioncheck)
+			}
+			if tc.config != nil {
+				builder = builder.WithObjects(tc.config)
+			}
+			client := builder.Build()
+			ctx := context.Background()
+
+			helper, err := NewConfigHelper[*kueue.ProvisioningRequestConfig](client)
+
+			if err != nil {
+				t.Fatalf("cannot built the helper: %s", err)
+			}
+
+			gotConfig, gotError := helper.ConfigForAdmissionCheck(ctx, tc.targetAdmissionCheck)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("unexpected config (-want/+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantConfig, gotConfig, cmpopts.IgnoreFields(kueue.ProvisioningRequestConfig{}, "TypeMeta", "ObjectMeta")); diff != "" {
+				t.Errorf("unexpected config (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestIndexerFunc(t *testing.T) {
+	cases := map[string]struct {
+		admissioncheck *kueue.AdmissionCheck
+		wantResult     []string
+	}{
+		"nil ac": {},
+		"wrong controller": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("other-controller").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config-name").
+				Obj(),
+		},
+		"wrong ref": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("test-controller").
+				Parameters(kueue.GroupVersion.Group, "NotProvisioningRequestConfig", "config-name").
+				Obj(),
+		},
+		"good": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("test-controller").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config-name").
+				Obj(),
+			wantResult: []string{"config-name"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+
+			indexFnc := IndexerByConfigFunction("test-controller", kueue.GroupVersion.WithKind("ProvisioningRequestConfig"))
+
+			gotResult := indexFnc(tc.admissioncheck)
+
+			if diff := cmp.Diff(tc.wantResult, gotResult); diff != "" {
+				t.Errorf("unexpected result (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestFilterCheckStates(t *testing.T) {
+
+	cases := map[string]struct {
+		admissionchecks []kueue.AdmissionCheck
+		states          []kueue.AdmissionCheckState
+		wantResult      []string
+	}{
+		"empty": {},
+		"no match": {
+
+			states: []kueue.AdmissionCheckState{
+				{Name: "check1"},
+				{Name: "check2"},
+				{Name: "check3"},
+			},
+		},
+		"two matches": {
+			admissionchecks: []kueue.AdmissionCheck{
+				*utiltesting.MakeAdmissionCheck("check1").ControllerName("test-controller").Obj(),
+				*utiltesting.MakeAdmissionCheck("check2").ControllerName("other-controller").Obj(),
+				*utiltesting.MakeAdmissionCheck("check3").ControllerName("test-controller").Obj(),
+			},
+			states: []kueue.AdmissionCheckState{
+				{Name: "check1"},
+				{Name: "check2"},
+				{Name: "check3"},
+			},
+			wantResult: []string{"check1", "check3"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			builder := fake.NewClientBuilder().WithScheme(scheme)
+			if len(tc.admissionchecks) > 0 {
+				builder = builder.WithLists(&kueue.AdmissionCheckList{Items: tc.admissionchecks})
+			}
+			client := builder.Build()
+			ctx := context.Background()
```

  I think we should make sure the context is closed in defer

- **pkg/util/admissioncheck/admissioncheck.go:80** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:23:09Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("missing parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type objAsPtr[T any] interface {
+	client.Object
+	*T
+}
+
+type ConfigHelper[PtrT objAsPtr[T], T any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PtrT objAsPtr[T], T any](c client.Client) (*ConfigHelper[PtrT, T], error) {
+	helper := ConfigHelper[PtrT, T]{
+		client: c,
+	}
+
+	gvk, err := apiutil.GVKForObject(helper.newConfigPtr(), c.Scheme())
+	if err != nil {
+		return nil, err
+	}
+
+	helper.gk = gvk.GroupKind()
+	return &helper, nil
+}
+
+func (ch *ConfigHelper[PtrT, T]) newConfigPtr() PtrT {
+	return PtrT(new(T))
+}
+
+// IsValidConfigReference - checks if the provided reference is addressing an object specific to this helper.
+func (ch *ConfigHelper[PtrT, T]) IsValidConfigReference(ref *kueue.AdmissionCheckParametersReference) (bool, error) {
+	return refValidForGK(ref, ch.gk)
+}
+
+// ConfigByName - get the config identified by its name
+func (ch *ConfigHelper[PtrT, T]) ConfigByName(ctx context.Context, name string) (PtrT, error) {
+	configPtr := ch.newConfigPtr()
+	if err := ch.client.Get(ctx, types.NamespacedName{Name: name}, configPtr); err != nil {
+		return nil, err
+	}
+	return configPtr, nil
+}
+
+// ConfigFromRef - get the config identified by ref if valid.
+func (ch *ConfigHelper[PtrT, T]) ConfigFromRef(ctx context.Context, ref *kueue.AdmissionCheckParametersReference) (PtrT, error) {
```

  No need to export this function

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-05T08:24:57Z

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler_test.go:51** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:24:57Z
```diff
@@ -48,7 +48,7 @@ func TestReconcileAdmissionCheck(t *testing.T) {
 				Type:    kueue.AdmissionCheckActive,
 				Status:  metav1.ConditionFalse,
 				Reason:  "BadParametersRef",
-				Message: "Unexpected parameters reference",
+				Message: "nil parameters reference",
```

  I'm ok with either "no" or "missing"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-05T10:59:24Z

- **pkg/util/admissioncheck/admissioncheck.go:38** by [@mimowo](https://github.com/mimowo) — 2023-12-05T10:59:24Z
```diff
@@ -0,0 +1,151 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("nil parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type ConfigHelper[PCT interface {
```

  I see, I'm ok proceeding with the generic Config, because it is no API change (in this PR), even though I'm not 100% sure it will be needed.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-05T11:44:32Z

- **pkg/util/admissioncheck/admissioncheck.go:52** by [@trasc](https://github.com/trasc) — 2023-12-05T11:44:32Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("missing parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type objAsPtr[T any] interface {
+	client.Object
+	*T
+}
+
+type ConfigHelper[PtrConfT objAsPtr[ConfT], ConfT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PtrConfT objAsPtr[ConfT], ConfT any](c client.Client) (*ConfigHelper[PtrConfT, ConfT], error) {
+	helper := ConfigHelper[PtrConfT, ConfT]{
+		client: c,
+	}
+
+	gvk, err := apiutil.GVKForObject(helper.newConfigPtr(), c.Scheme())
```

  I wold like to keep it as is, since it guarantees to use the right GVK for the config type (T). From the error point of view .I think it's better to get it at the begging (if T is not a valid k8s object  registered in the client's schema ), then to create a helper that will forever fail  when trying  to read the config from the API.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-05T11:55:07Z

- **pkg/util/admissioncheck/admissioncheck.go:80** by [@trasc](https://github.com/trasc) — 2023-12-05T11:50:06Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("missing parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type objAsPtr[T any] interface {
+	client.Object
+	*T
+}
+
+type ConfigHelper[PtrT objAsPtr[T], T any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PtrT objAsPtr[T], T any](c client.Client) (*ConfigHelper[PtrT, T], error) {
+	helper := ConfigHelper[PtrT, T]{
+		client: c,
+	}
+
+	gvk, err := apiutil.GVKForObject(helper.newConfigPtr(), c.Scheme())
+	if err != nil {
+		return nil, err
+	}
+
+	helper.gk = gvk.GroupKind()
+	return &helper, nil
+}
+
+func (ch *ConfigHelper[PtrT, T]) newConfigPtr() PtrT {
+	return PtrT(new(T))
+}
+
+// IsValidConfigReference - checks if the provided reference is addressing an object specific to this helper.
+func (ch *ConfigHelper[PtrT, T]) IsValidConfigReference(ref *kueue.AdmissionCheckParametersReference) (bool, error) {
+	return refValidForGK(ref, ch.gk)
+}
+
+// ConfigByName - get the config identified by its name
+func (ch *ConfigHelper[PtrT, T]) ConfigByName(ctx context.Context, name string) (PtrT, error) {
+	configPtr := ch.newConfigPtr()
+	if err := ch.client.Get(ctx, types.NamespacedName{Name: name}, configPtr); err != nil {
+		return nil, err
+	}
+	return configPtr, nil
+}
+
+// ConfigFromRef - get the config identified by ref if valid.
+func (ch *ConfigHelper[PtrT, T]) ConfigFromRef(ctx context.Context, ref *kueue.AdmissionCheckParametersReference) (PtrT, error) {
```

  let's make `IsValidConfigReference` private instead.

- **pkg/util/admissioncheck/admissioncheck_test.go:188** by [@trasc](https://github.com/trasc) — 2023-12-05T11:50:39Z
```diff
@@ -0,0 +1,241 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestConfigHelper(t *testing.T) {
+	testConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "className",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+			ManagedResources: []corev1.ResourceName{"cpu"},
+		},
+	}
+
+	cases := map[string]struct {
+		admissioncheck       *kueue.AdmissionCheck
+		config               *kueue.ProvisioningRequestConfig
+		targetAdmissionCheck string
+		wantConfig           *kueue.ProvisioningRequestConfig
+		wantError            error
+	}{
+		"admission check bad name": {
+			targetAdmissionCheck: "123",
+			wantError:            cmpopts.AnyError,
+		},
+		"no parameter reference": {
+			admissioncheck:       utiltesting.MakeAdmissionCheck("ac").Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrNilParametersRef,
+		},
+		"bad parameter reference, no name": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"bad parameter reference, bad group": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters("not-"+kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"bad parameter reference, bad kind": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "NptProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"config not found": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            cmpopts.AnyError,
+		},
+		"config found": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			config:               testConfig.DeepCopy(),
+			targetAdmissionCheck: "ac",
+			wantConfig:           testConfig.DeepCopy(),
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			builder := fake.NewClientBuilder().WithScheme(scheme)
+			if tc.admissioncheck != nil {
+				builder = builder.WithObjects(tc.admissioncheck)
+			}
+			if tc.config != nil {
+				builder = builder.WithObjects(tc.config)
+			}
+			client := builder.Build()
+			ctx := context.Background()
+
+			helper, err := NewConfigHelper[*kueue.ProvisioningRequestConfig](client)
+
+			if err != nil {
+				t.Fatalf("cannot built the helper: %s", err)
+			}
+
+			gotConfig, gotError := helper.ConfigForAdmissionCheck(ctx, tc.targetAdmissionCheck)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("unexpected config (-want/+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantConfig, gotConfig, cmpopts.IgnoreFields(kueue.ProvisioningRequestConfig{}, "TypeMeta", "ObjectMeta")); diff != "" {
+				t.Errorf("unexpected config (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestIndexerFunc(t *testing.T) {
+	cases := map[string]struct {
+		admissioncheck *kueue.AdmissionCheck
+		wantResult     []string
+	}{
+		"nil ac": {},
+		"wrong controller": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("other-controller").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config-name").
+				Obj(),
+		},
+		"wrong ref": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("test-controller").
+				Parameters(kueue.GroupVersion.Group, "NotProvisioningRequestConfig", "config-name").
+				Obj(),
+		},
+		"good": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("test-controller").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config-name").
+				Obj(),
+			wantResult: []string{"config-name"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+
+			indexFnc := IndexerByConfigFunction("test-controller", kueue.GroupVersion.WithKind("ProvisioningRequestConfig"))
+
+			gotResult := indexFnc(tc.admissioncheck)
+
+			if diff := cmp.Diff(tc.wantResult, gotResult); diff != "" {
+				t.Errorf("unexpected result (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestFilterCheckStates(t *testing.T) {
+
```

  done

- **pkg/util/admissioncheck/admissioncheck_test.go:232** by [@trasc](https://github.com/trasc) — 2023-12-05T11:51:49Z
```diff
@@ -0,0 +1,241 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestConfigHelper(t *testing.T) {
+	testConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "className",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+			ManagedResources: []corev1.ResourceName{"cpu"},
+		},
+	}
+
+	cases := map[string]struct {
+		admissioncheck       *kueue.AdmissionCheck
+		config               *kueue.ProvisioningRequestConfig
+		targetAdmissionCheck string
+		wantConfig           *kueue.ProvisioningRequestConfig
+		wantError            error
+	}{
+		"admission check bad name": {
+			targetAdmissionCheck: "123",
+			wantError:            cmpopts.AnyError,
+		},
+		"no parameter reference": {
+			admissioncheck:       utiltesting.MakeAdmissionCheck("ac").Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrNilParametersRef,
+		},
+		"bad parameter reference, no name": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"bad parameter reference, bad group": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters("not-"+kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"bad parameter reference, bad kind": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "NptProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            ErrBadParametersRef,
+		},
+		"config not found": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			targetAdmissionCheck: "ac",
+			wantError:            cmpopts.AnyError,
+		},
+		"config found": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config").
+				Obj(),
+			config:               testConfig.DeepCopy(),
+			targetAdmissionCheck: "ac",
+			wantConfig:           testConfig.DeepCopy(),
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			builder := fake.NewClientBuilder().WithScheme(scheme)
+			if tc.admissioncheck != nil {
+				builder = builder.WithObjects(tc.admissioncheck)
+			}
+			if tc.config != nil {
+				builder = builder.WithObjects(tc.config)
+			}
+			client := builder.Build()
+			ctx := context.Background()
+
+			helper, err := NewConfigHelper[*kueue.ProvisioningRequestConfig](client)
+
+			if err != nil {
+				t.Fatalf("cannot built the helper: %s", err)
+			}
+
+			gotConfig, gotError := helper.ConfigForAdmissionCheck(ctx, tc.targetAdmissionCheck)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("unexpected config (-want/+got):\n%s", diff)
+			}
+			if diff := cmp.Diff(tc.wantConfig, gotConfig, cmpopts.IgnoreFields(kueue.ProvisioningRequestConfig{}, "TypeMeta", "ObjectMeta")); diff != "" {
+				t.Errorf("unexpected config (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestIndexerFunc(t *testing.T) {
+	cases := map[string]struct {
+		admissioncheck *kueue.AdmissionCheck
+		wantResult     []string
+	}{
+		"nil ac": {},
+		"wrong controller": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("other-controller").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config-name").
+				Obj(),
+		},
+		"wrong ref": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("test-controller").
+				Parameters(kueue.GroupVersion.Group, "NotProvisioningRequestConfig", "config-name").
+				Obj(),
+		},
+		"good": {
+			admissioncheck: utiltesting.MakeAdmissionCheck("ac").
+				ControllerName("test-controller").
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "config-name").
+				Obj(),
+			wantResult: []string{"config-name"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+
+			indexFnc := IndexerByConfigFunction("test-controller", kueue.GroupVersion.WithKind("ProvisioningRequestConfig"))
+
+			gotResult := indexFnc(tc.admissioncheck)
+
+			if diff := cmp.Diff(tc.wantResult, gotResult); diff != "" {
+				t.Errorf("unexpected result (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestFilterCheckStates(t *testing.T) {
+
+	cases := map[string]struct {
+		admissionchecks []kueue.AdmissionCheck
+		states          []kueue.AdmissionCheckState
+		wantResult      []string
+	}{
+		"empty": {},
+		"no match": {
+
+			states: []kueue.AdmissionCheckState{
+				{Name: "check1"},
+				{Name: "check2"},
+				{Name: "check3"},
+			},
+		},
+		"two matches": {
+			admissionchecks: []kueue.AdmissionCheck{
+				*utiltesting.MakeAdmissionCheck("check1").ControllerName("test-controller").Obj(),
+				*utiltesting.MakeAdmissionCheck("check2").ControllerName("other-controller").Obj(),
+				*utiltesting.MakeAdmissionCheck("check3").ControllerName("test-controller").Obj(),
+			},
+			states: []kueue.AdmissionCheckState{
+				{Name: "check1"},
+				{Name: "check2"},
+				{Name: "check3"},
+			},
+			wantResult: []string{"check1", "check3"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			scheme := runtime.NewScheme()
+			if err := clientgoscheme.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			if err := kueue.AddToScheme(scheme); err != nil {
+				panic(err)
+			}
+			builder := fake.NewClientBuilder().WithScheme(scheme)
+			if len(tc.admissionchecks) > 0 {
+				builder = builder.WithLists(&kueue.AdmissionCheckList{Items: tc.admissionchecks})
+			}
+			client := builder.Build()
+			ctx := context.Background()
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-11T15:15:18Z

- **pkg/util/admissioncheck/admissioncheck.go:52** by [@mimowo](https://github.com/mimowo) — 2023-12-11T15:15:18Z
```diff
@@ -0,0 +1,143 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissioncheck
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+var (
+	ErrNilParametersRef = errors.New("missing parameters reference")
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type objAsPtr[T any] interface {
+	client.Object
+	*T
+}
+
+type ConfigHelper[PtrConfT objAsPtr[ConfT], ConfT any] struct {
+	gk     schema.GroupKind
+	client client.Client
+}
+
+func NewConfigHelper[PtrConfT objAsPtr[ConfT], ConfT any](c client.Client) (*ConfigHelper[PtrConfT, ConfT], error) {
+	helper := ConfigHelper[PtrConfT, ConfT]{
+		client: c,
+	}
+
+	gvk, err := apiutil.GVKForObject(helper.newConfigPtr(), c.Scheme())
```

  I think this comment is non-blocking to me. For visibility @alculquicondor.
