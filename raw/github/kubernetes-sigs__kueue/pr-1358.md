# PR #1358: Prebuilt workload suport

**Summary**: Prebuilt workload suport

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1358

**Last updated**: 2023-12-19T07:05:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `prebuilt-workload` → `main`
- **Created**: 2023-11-22T11:05:08Z
- **Updated**: 2023-12-19T07:05:51Z
- **Merged**: 2023-12-18T16:28:12Z
- **Closed**: 2023-12-18T16:28:12Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 13   **+575 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add the possibility for a job to take the ownership of an existing workload instead of creating it's own.

The target workload is specified in the `kueue.x-k8s.io/prebuilt-workload-name`  label of the job.

If the workload is not present , the job will stay suspended.
If the workload exists but is not equivalent with the job, it will be marked as `Finished` with failure and the job will remain suspended.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693

#### Special notes for your reviewer:

For `batch/job`, the pod-template gets a set of lables derived from  the job name and object UID, these labels should not be modified by ` RunWithPodSetsInfo`  or `RestorePodSetsInfo`.  This is covered in the current PR. 

Other job types might have different special behavior to take into account, that is why we  limit the usage of the feature to `batch/job` for now. We should extend the list of  supported job types as needed during the job type integration with multikueue.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add prebuilt workload support for batch/job.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-22T11:05:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6bb596b2c8d298acaa7ec889004281b437f667c3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/658016a68a39720008aa55c1 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-22T15:51:03Z

/assign @mwielgus

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-15T09:26:50Z

/lgtm
/assign @tenzen-y @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-15T09:26:58Z

LGTM label has been added.  <details>Git tree hash: ef0bf3e49d10268e0a2af58edaec274ad8f63eb5</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-15T09:44:56Z

/hold
To address the comment about the e2e tests

### Comment by [@trasc](https://github.com/trasc) — 2023-12-15T12:21:47Z

/unhold
@mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-15T13:09:55Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-15T13:10:02Z

LGTM label has been added.  <details>Git tree hash: 4424a785f8e5370c93a75082120e0b1b879438c6</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T13:23:55Z

ACK

### Comment by [@trasc](https://github.com/trasc) — 2023-12-18T10:38:41Z

> What if I want to delete a Workload before a Job had a chance to use it?
> Would the Workload be finalized? Or is that something that will be solved with #1454 ?

This should be manage by the mechanism that creates the wl. 

(overall having the wl finalizer set only for the duration ta ability to restore the podset info wold have made this easier)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-18T15:44:44Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1358#pullrequestreview-1787196776" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1358#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-18T15:44:50Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-18T15:44:57Z

LGTM label has been added.  <details>Git tree hash: 151eb975a07171c0cdd6c9ae3750051015550022</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-05T08:44:12Z

The new label is part of API, and as such its name should be discussed in a KEP, can we have it mentioned in the MultiKueue KEP https://github.com/kubernetes-sigs/kueue/pull/1380/? cc @mwielgus

- **pkg/controller/jobframework/reconciler.go:243** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:32:33Z
```diff
@@ -233,7 +235,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// 1. make sure there is only a single existing instance of the workload.
 	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := r.ensureOneWorkload(ctx, job, object)
+	var wl *kueue.Workload
+
+	if usePrebuiltWorkload {
+		wl, err = r.getPrebuiltWorkload(ctx, job, object, prebuiltWorkloadName)
+		if wl == nil {
+			if !job.IsSuspended() {
```

  Do we need this check for `!job.IsSuspended()`? I think it might happen that a job is suspended, but the nodeSelectors are not restored: https://github.com/kubernetes-sigs/kueue/blob/d91a7d5497739bf75969abb035b304d5495498b2/pkg/controller/jobs/job/job_controller.go#L160-L185. The other job types introduce the optimization of checking suspended inside the stop function.

- **pkg/controller/jobframework/reconciler.go:241** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:36:33Z
```diff
@@ -233,7 +235,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// 1. make sure there is only a single existing instance of the workload.
 	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := r.ensureOneWorkload(ctx, job, object)
+	var wl *kueue.Workload
+
+	if usePrebuiltWorkload {
+		wl, err = r.getPrebuiltWorkload(ctx, job, object, prebuiltWorkloadName)
```

  I'm wondering if this could be also part of `ensureOneWorkload`. It seems conceptually fitting. Also, it would let other worlkloads be deleted, if for whatever reason, we have 2 workloads with the annotation.

- **pkg/controller/jobframework/reconciler.go:303** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:39:22Z
```diff
@@ -283,9 +300,11 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// 3. handle workload is nil.
 	if wl == nil {
-		err := r.handleJobWithNoWorkload(ctx, job, object)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
+		if !usePrebuiltWorkload {
```

  Why do we need this `if`? IIUC,  If  `usePrebuiltWorkload=true`  and `wl=nil` then, it is a job without workload, so the handling should apply.

- **pkg/controller/jobframework/reconciler.go:580** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:40:16Z
```diff
@@ -530,6 +549,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 
 	return match, toDelete, nil
 }
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
+	if err != nil {
+		return nil, client.IgnoreNotFound(err)
+	}
+
+	if !metav1.IsControlledBy(wl, object) {
+		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
+			// don't return an error here, since a retry cannot give a different result
+			return nil, nil
+		}
+
+		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
+			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
+		}
+
+		if err := r.client.Update(ctx, wl); err != nil {
+			return nil, err
+		}
+	}
+
+	if !equivalentToWorkload(job, wl) {
```

  Maybe if this was part of `ensureOneWorkload`, we would not need to repeat this logic.

- **pkg/controller/jobframework/validation.go:45** by [@mimowo](https://github.com/mimowo) — 2023-12-05T08:41:46Z
```diff
@@ -34,7 +36,33 @@ var (
 func ValidateCreateForQueueName(job GenericJob) field.ErrorList {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, ValidateLabelAsCRDName(job, constants.QueueLabel)...)
+	allErrs = append(allErrs, ValidateLabelAsCRDName(job, constants.PrebuiltWorkloadLabel)...)
+	allErrs = append(allErrs, validateOnlyOneLablel(job, constants.QueueLabel, constants.PrebuiltWorkloadLabel)...)
 	allErrs = append(allErrs, ValidateAnnotationAsCRDName(job, constants.QueueAnnotation)...)
+
+	// this rule should be relaxed when its confirmed that running wit a prebuilt wl is fully supported by each integration
+	if _, hasPrebuilt := job.Object().GetLabels()[constants.PrebuiltWorkloadLabel]; hasPrebuilt {
+		supportedJobGVKs := sets.New("batch/v1, Kind=Job")
```

  Extract the set of supported jobs to the var in this file.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-07T08:35:14Z

- **pkg/controller/jobframework/reconciler.go:303** by [@trasc](https://github.com/trasc) — 2023-12-07T07:50:30Z
```diff
@@ -283,9 +300,11 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// 3. handle workload is nil.
 	if wl == nil {
-		err := r.handleJobWithNoWorkload(ctx, job, object)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
+		if !usePrebuiltWorkload {
```

  In case of prebuilt workloads we don't want to create new ones.

- **pkg/controller/jobframework/reconciler.go:243** by [@trasc](https://github.com/trasc) — 2023-12-07T08:27:00Z
```diff
@@ -233,7 +235,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// 1. make sure there is only a single existing instance of the workload.
 	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := r.ensureOneWorkload(ctx, job, object)
+	var wl *kueue.Workload
+
+	if usePrebuiltWorkload {
+		wl, err = r.getPrebuiltWorkload(ctx, job, object, prebuiltWorkloadName)
+		if wl == nil {
+			if !job.IsSuspended() {
```

  We need something like it, in a no prebultWorkload scenario, the stop will happen in the fist reconcile after the workload creation. In case of prebultWorkload we don't want to create new workloads if the target one is not present or out of sync.

- **pkg/controller/jobframework/reconciler.go:241** by [@trasc](https://github.com/trasc) — 2023-12-07T08:27:13Z
```diff
@@ -233,7 +235,22 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 
 	// 1. make sure there is only a single existing instance of the workload.
 	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := r.ensureOneWorkload(ctx, job, object)
+	var wl *kueue.Workload
+
+	if usePrebuiltWorkload {
+		wl, err = r.getPrebuiltWorkload(ctx, job, object, prebuiltWorkloadName)
```

  The link is done from  job to workload via a immutable label, we cannot have more than one workload.

- **pkg/controller/jobframework/reconciler.go:580** by [@trasc](https://github.com/trasc) — 2023-12-07T08:29:43Z
```diff
@@ -530,6 +549,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 
 	return match, toDelete, nil
 }
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
+	if err != nil {
+		return nil, client.IgnoreNotFound(err)
+	}
+
+	if !metav1.IsControlledBy(wl, object) {
+		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
+			// don't return an error here, since a retry cannot give a different result
+			return nil, nil
+		}
+
+		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
+			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
+		}
+
+		if err := r.client.Update(ctx, wl); err != nil {
+			return nil, err
+		}
+	}
+
+	if !equivalentToWorkload(job, wl) {
```

  This is now called from inside `ensureOneWorkload`, but it's more clear to keep it here, we don't want to delete the workload in case it is out of sync, we just want to mark it as finished.

- **pkg/controller/jobframework/validation.go:45** by [@trasc](https://github.com/trasc) — 2023-12-07T08:31:31Z
```diff
@@ -34,7 +36,33 @@ var (
 func ValidateCreateForQueueName(job GenericJob) field.ErrorList {
 	var allErrs field.ErrorList
 	allErrs = append(allErrs, ValidateLabelAsCRDName(job, constants.QueueLabel)...)
+	allErrs = append(allErrs, ValidateLabelAsCRDName(job, constants.PrebuiltWorkloadLabel)...)
+	allErrs = append(allErrs, validateOnlyOneLablel(job, constants.QueueLabel, constants.PrebuiltWorkloadLabel)...)
 	allErrs = append(allErrs, ValidateAnnotationAsCRDName(job, constants.QueueAnnotation)...)
+
+	// this rule should be relaxed when its confirmed that running wit a prebuilt wl is fully supported by each integration
+	if _, hasPrebuilt := job.Object().GetLabels()[constants.PrebuiltWorkloadLabel]; hasPrebuilt {
+		supportedJobGVKs := sets.New("batch/v1, Kind=Job")
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-07T09:09:03Z

- **pkg/controller/jobframework/reconciler.go:827** by [@mimowo](https://github.com/mimowo) — 2023-12-07T09:08:26Z
```diff
@@ -780,12 +823,24 @@ func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.W
 func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job GenericJob, object client.Object) error {
 	log := ctrl.LoggerFrom(ctx)
 
+	// Stop the job if not already suspended
+	if !job.IsSuspended() {
```

  Why do we need this? Is there any test that would fail without it? 
  
  Also, I'm concerned with the check `!job.IsSuspended() {` before `stopJob`. StopJob is has multiple steps in case of k8s Job. Setting it suspend is the first, then we reset the StartTime and restore nodeSelectors. It is possible, that one of the following steps fails on the first attempt, then this if would prevent completing the following stop steps.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-07T09:36:35Z

- **pkg/controller/jobframework/reconciler.go:827** by [@trasc](https://github.com/trasc) — 2023-12-07T09:36:35Z
```diff
@@ -780,12 +823,24 @@ func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.W
 func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job GenericJob, object client.Object) error {
 	log := ctrl.LoggerFrom(ctx)
 
+	// Stop the job if not already suspended
+	if !job.IsSuspended() {
```

  In a no prebultWorkload scenario, the stop will happen in the fist reconcile after the workload creation. In case of prebultWorkload we don't want to create new workloads if the target one is not present or out of sync. 
  
  I'm dropping the `job.IsSuspended()` since it is already checked in `stopJob` when necessary.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-07T14:16:33Z

- **pkg/controller/jobframework/reconciler.go:196** by [@mimowo](https://github.com/mimowo) — 2023-12-07T13:34:44Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  Why to disable the validation? This seems relevant regardless of the new label.

- **pkg/controller/jobframework/reconciler.go:580** by [@mimowo](https://github.com/mimowo) — 2023-12-07T14:15:48Z
```diff
@@ -530,6 +549,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 
 	return match, toDelete, nil
 }
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
+	if err != nil {
+		return nil, client.IgnoreNotFound(err)
+	}
+
+	if !metav1.IsControlledBy(wl, object) {
+		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
+			// don't return an error here, since a retry cannot give a different result
+			return nil, nil
+		}
+
+		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
+			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
+		}
+
+		if err := r.client.Update(ctx, wl); err != nil {
+			return nil, err
+		}
+	}
+
+	if !equivalentToWorkload(job, wl) {
```

  WDYT about an alternative approach to modify the `FindMatchingWorkloads` function, to constrain that the workload is matching only if its name matches the name. If the workload is out-of-sync we would update it inside the `ensureOneWorkload` function, rather than mark finished. https://github.com/kubernetes-sigs/kueue/blob/006d825b6ded5097a46af27be277e392eae0eac1/pkg/controller/jobframework/reconciler.go#L463. 
  
  If we want to go with the marking as finished I would like to better understand the pros & cons of the marking as finished vs. updating.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-07T15:06:18Z

- **pkg/controller/jobframework/reconciler.go:547** by [@mimowo](https://github.com/mimowo) — 2023-12-07T15:01:35Z
```diff
@@ -537,6 +544,42 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
```

  ```suggestion
  func (r *JobReconciler) adoptPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
  ```
  Alternative names could be `adoptWorkload`, or `attachToWorkload`. The main point is that `getPrebuiltWorkload` is misleading, because this function is mutating the workload object.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-08T11:30:46Z

- **pkg/controller/jobframework/reconciler.go:547** by [@trasc](https://github.com/trasc) — 2023-12-08T11:30:45Z
```diff
@@ -537,6 +544,42 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
```

  The attach part is done only once , after that and most of the time it's just a get.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-08T11:50:45Z

- **pkg/controller/jobframework/reconciler.go:547** by [@mimowo](https://github.com/mimowo) — 2023-12-08T11:50:45Z
```diff
@@ -537,6 +544,42 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
```

  So maybe `ensureWorkloadIsAttached`, `ensurePrebuiltWorkloadIsAttached`, `ensureWorkloadIsAdopted`. Something along the lines, but I don't think we should call mutating functions using `get`. I was confused by this on the first reading.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-08T11:54:03Z

- **pkg/controller/jobframework/reconciler.go:580** by [@trasc](https://github.com/trasc) — 2023-12-08T11:54:02Z
```diff
@@ -530,6 +549,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 
 	return match, toDelete, nil
 }
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
+	if err != nil {
+		return nil, client.IgnoreNotFound(err)
+	}
+
+	if !metav1.IsControlledBy(wl, object) {
+		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
+			// don't return an error here, since a retry cannot give a different result
+			return nil, nil
+		}
+
+		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
+			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
+		}
+
+		if err := r.client.Update(ctx, wl); err != nil {
+			return nil, err
+		}
+	}
+
+	if !equivalentToWorkload(job, wl) {
```

  The most relevant usecase at this point MultiKueue, where we create the Workload in a remote cluster and after it's admitted the job is also created and use that as prebuilt workload. The sync of the workloads in multikueue should only be done from manager to worker cluster.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-08T14:26:36Z

- **pkg/controller/jobframework/reconciler.go:580** by [@mimowo](https://github.com/mimowo) — 2023-12-08T14:26:36Z
```diff
@@ -530,6 +549,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 
 	return match, toDelete, nil
 }
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
+	if err != nil {
+		return nil, client.IgnoreNotFound(err)
+	}
+
+	if !metav1.IsControlledBy(wl, object) {
+		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
+			// don't return an error here, since a retry cannot give a different result
+			return nil, nil
+		}
+
+		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
+			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
+		}
+
+		if err := r.client.Update(ctx, wl); err != nil {
+			return nil, err
+		}
+	}
+
+	if !equivalentToWorkload(job, wl) {
```

  Let's see, currently in Kueue we support changing the `.spec.parallelism` in Job, which results in updating the workload. I would suspect when using MultiKueue we can have the same user experience, since we want to support all features: https://github.com/kubernetes-sigs/kueue/blob/6e59f26e4750daeb3067ec6d9f236ee1571bbce1/keps/693-multikueue/README.md?plain=1#L37-L41. 
  
  So, in the scenario of parallelism changed by a user, we would get different behavior if marking finished.
  
  EDIT: I'm ok if we need to make some limitations, that not all features are supported in MultiKueue, that's fine, but we should first make a note of the behavior change / limitation from the user perspective. Second, in this particular case, I think we could have the same user experience by reusing the code, unless I'm missing something, but let's try to make this clear.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T09:28:12Z

- **pkg/controller/jobframework/reconciler.go:580** by [@mimowo](https://github.com/mimowo) — 2023-12-12T09:28:08Z
```diff
@@ -537,6 +544,42 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
+	if err != nil {
+		return nil, client.IgnoreNotFound(err)
+	}
+
+	if !metav1.IsControlledBy(wl, object) {
+		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
+			// don't return an error here, since a retry cannot give a different result
+			return nil, nil
+		}
+
+		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
+			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
+		}
+
+		if err := r.client.Update(ctx, wl); err != nil {
+			return nil, err
+		}
+	}
+
+	if !equivalentToWorkload(job, wl) {
```

  Currently `getPrebuiltWorkload` may do mutating actions: "adopt" and "mark as finished". I would suggest splitting this function into two with the dedicated functions, proposing: `ensurePrebuildWorkloadIsAdopted` and `markPrebuildWorkloadFinishedIfOutOfSync` (or something along the lines).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T12:11:11Z

- **pkg/controller/jobframework/reconciler.go:580** by [@trasc](https://github.com/trasc) — 2023-12-12T12:08:22Z
```diff
@@ -537,6 +544,42 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
+	wl := &kueue.Workload{}
+	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
+	if err != nil {
+		return nil, client.IgnoreNotFound(err)
+	}
+
+	if !metav1.IsControlledBy(wl, object) {
+		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
+			// don't return an error here, since a retry cannot give a different result
+			return nil, nil
+		}
+
+		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
+			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
+		}
+
+		if err := r.client.Update(ctx, wl); err != nil {
+			return nil, err
+		}
+	}
+
+	if !equivalentToWorkload(job, wl) {
```

  done

- **pkg/controller/jobframework/reconciler.go:196** by [@trasc](https://github.com/trasc) — 2023-12-12T12:10:21Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  we should be able to continue the reconcile for prebuiltworkloads also.

- **pkg/controller/jobframework/reconciler.go:547** by [@trasc](https://github.com/trasc) — 2023-12-12T12:10:39Z
```diff
@@ -537,6 +544,42 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
```

  reworked

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T12:17:35Z

- **pkg/controller/jobframework/reconciler.go:562** by [@mimowo](https://github.com/mimowo) — 2023-12-12T12:12:22Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
 	if !metav1.IsControlledBy(wl, object) {
 		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 			// don't return an error here, since a retry cannot give a different result
```

  I don't think we should be silencing the error. There might be different ones, and they may be important to investigate. We should log them at least. I think, ideally, we would mark the workload finished, as we do here for other non-retriable errors: https://github.com/kubernetes-sigs/kueue/blob/4cff47bd12e0915680bb9a0f3d0e10d9b284912a/pkg/controller/jobframework/reconciler.go#L354-L361

- **pkg/controller/jobframework/reconciler.go:579** by [@mimowo](https://github.com/mimowo) — 2023-12-12T12:13:40Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
 	if !metav1.IsControlledBy(wl, object) {
 		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 			// don't return an error here, since a retry cannot give a different result
-			return nil, nil
+			return false, nil
 		}
 
 		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
 			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
 		}
 
 		if err := r.client.Update(ctx, wl); err != nil {
-			return nil, err
+			return false, err
 		}
 	}
+	return true, nil
+}
 
+func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *kueue.Workload, job GenericJob) (bool, error) {
```

  Return just `error` since it is equivalent to `false`.

- **pkg/controller/jobframework/reconciler.go:558** by [@mimowo](https://github.com/mimowo) — 2023-12-12T12:15:25Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
```

  Can we return just error? If there are two types of errors permanent and non-permanent, can we use the appropiate type of the error to distinguish? Similarly, as we do here: https://github.com/kubernetes-sigs/kueue/blob/4cff47bd12e0915680bb9a0f3d0e10d9b284912a/pkg/controller/jobframework/reconciler.go#L354-L361

- **pkg/controller/jobframework/reconciler.go:447** by [@mimowo](https://github.com/mimowo) — 2023-12-12T12:17:23Z
```diff
@@ -442,8 +442,20 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	log := ctrl.LoggerFrom(ctx)
 
 	if usePrebuiltWorkload, prebuiltWorkloadName := prebuiltWorkload(job); usePrebuiltWorkload {
-		wl, getErr := r.getPrebuiltWorkload(ctx, job, object, prebuiltWorkloadName)
-		return wl, getErr
+		wl := &kueue.Workload{}
+		err := r.client.Get(ctx, types.NamespacedName{Name: prebuiltWorkloadName, Namespace: object.GetNamespace()}, wl)
+		if err != nil {
+			return nil, client.IgnoreNotFound(err)
```

  Why do we wrap the error with NotFound? The reason could be different, for example a timeout from API server.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T12:24:55Z

- **pkg/controller/jobframework/reconciler.go:447** by [@trasc](https://github.com/trasc) — 2023-12-12T12:24:55Z
```diff
@@ -442,8 +442,20 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	log := ctrl.LoggerFrom(ctx)
 
 	if usePrebuiltWorkload, prebuiltWorkloadName := prebuiltWorkload(job); usePrebuiltWorkload {
-		wl, getErr := r.getPrebuiltWorkload(ctx, job, object, prebuiltWorkloadName)
-		return wl, getErr
+		wl := &kueue.Workload{}
+		err := r.client.Get(ctx, types.NamespacedName{Name: prebuiltWorkloadName, Namespace: object.GetNamespace()}, wl)
+		if err != nil {
+			return nil, client.IgnoreNotFound(err)
```

  We don't want to retry in case of not found.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T12:36:47Z

- **pkg/controller/jobframework/reconciler.go:447** by [@mimowo](https://github.com/mimowo) — 2023-12-12T12:36:47Z
```diff
@@ -442,8 +442,20 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	log := ctrl.LoggerFrom(ctx)
 
 	if usePrebuiltWorkload, prebuiltWorkloadName := prebuiltWorkload(job); usePrebuiltWorkload {
-		wl, getErr := r.getPrebuiltWorkload(ctx, job, object, prebuiltWorkloadName)
-		return wl, getErr
+		wl := &kueue.Workload{}
+		err := r.client.Get(ctx, types.NamespacedName{Name: prebuiltWorkloadName, Namespace: object.GetNamespace()}, wl)
+		if err != nil {
+			return nil, client.IgnoreNotFound(err)
```

  In that case we should check if this is NotFound error with `apierrors.IsNotFound`, instead of wrapping any error unconditionally. Example: https://github.com/kubernetes/kubernetes/blob/0c645922edcc06adff43c70c02fb56751364bbb5/pkg/controller/job/job_controller.go#L630

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T12:43:41Z

- **pkg/controller/jobframework/reconciler.go:447** by [@trasc](https://github.com/trasc) — 2023-12-12T12:43:41Z
```diff
@@ -442,8 +442,20 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	log := ctrl.LoggerFrom(ctx)
 
 	if usePrebuiltWorkload, prebuiltWorkloadName := prebuiltWorkload(job); usePrebuiltWorkload {
-		wl, getErr := r.getPrebuiltWorkload(ctx, job, object, prebuiltWorkloadName)
-		return wl, getErr
+		wl := &kueue.Workload{}
+		err := r.client.Get(ctx, types.NamespacedName{Name: prebuiltWorkloadName, Namespace: object.GetNamespace()}, wl)
+		if err != nil {
+			return nil, client.IgnoreNotFound(err)
```

  `IgnoreNotFound ` is doing exactly that, calling IsNotFound

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T12:47:18Z

- **pkg/controller/jobframework/reconciler.go:447** by [@mimowo](https://github.com/mimowo) — 2023-12-12T12:47:18Z
```diff
@@ -442,8 +442,20 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 	log := ctrl.LoggerFrom(ctx)
 
 	if usePrebuiltWorkload, prebuiltWorkloadName := prebuiltWorkload(job); usePrebuiltWorkload {
-		wl, getErr := r.getPrebuiltWorkload(ctx, job, object, prebuiltWorkloadName)
-		return wl, getErr
+		wl := &kueue.Workload{}
+		err := r.client.Get(ctx, types.NamespacedName{Name: prebuiltWorkloadName, Namespace: object.GetNamespace()}, wl)
+		if err != nil {
+			return nil, client.IgnoreNotFound(err)
```

  Oh I see, thanks for clarifying

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T13:34:11Z

- **pkg/controller/jobframework/reconciler.go:558** by [@trasc](https://github.com/trasc) — 2023-12-12T13:28:08Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
```

  In case the workload is owned by other job, we do not return an error since a retry us useless, but  setting the ownership fails.

- **pkg/controller/jobframework/reconciler.go:562** by [@trasc](https://github.com/trasc) — 2023-12-12T13:28:39Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
 	if !metav1.IsControlledBy(wl, object) {
 		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 			// don't return an error here, since a retry cannot give a different result
```

  logged

- **pkg/controller/jobframework/reconciler.go:579** by [@trasc](https://github.com/trasc) — 2023-12-12T13:31:50Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
 	if !metav1.IsControlledBy(wl, object) {
 		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 			// don't return an error here, since a retry cannot give a different result
-			return nil, nil
+			return false, nil
 		}
 
 		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
 			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
 		}
 
 		if err := r.client.Update(ctx, wl); err != nil {
-			return nil, err
+			return false, err
 		}
 	}
+	return true, nil
+}
 
+func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *kueue.Workload, job GenericJob) (bool, error) {
```

  We need to have a way of knowing that they are out of sync even if updating the workload status succeeds.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T13:38:19Z

- **pkg/controller/jobframework/reconciler.go:558** by [@mimowo](https://github.com/mimowo) — 2023-12-12T13:38:19Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
```

  If the retry is useless, as in the case referenced in the previous message, should we not mark the workload as finished, to return API-level message to the user, rather than just logs? 
  
  I think it could be a follow up, so that we unblock the merge, just trying to establish what is the desirable behavior.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T13:41:46Z

- **pkg/controller/jobframework/reconciler.go:558** by [@mimowo](https://github.com/mimowo) — 2023-12-12T13:41:46Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
```

  Oh, I see, you don't want to modify the workload, if it is owned by another job. Fair, I would suggest just a comment update to clarify.
  
  EDIT: but technically the error is thrown rather due to other reasons that another job owning, for example here: https://github.com/kubernetes-sigs/controller-runtime/blob/76b5567314a88dbc30c43449885353c1c3151bf7/pkg/controller/controllerutil/controllerutil.go#L73. So still, it seems justified I think to just mark it finished and have a proper error message in the condition.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T14:07:06Z

- **pkg/controller/jobframework/reconciler.go:558** by [@mimowo](https://github.com/mimowo) — 2023-12-12T14:07:06Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
```

  Ok. I'm not certain how to handle the error. I'm fine with the log for now. Maybe other reviewers will have a better suggestion.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T14:13:32Z

- **pkg/controller/jobframework/reconciler.go:838** by [@mimowo](https://github.com/mimowo) — 2023-12-12T14:12:43Z
```diff
@@ -780,12 +834,22 @@ func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.W
 func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job GenericJob, object client.Object) error {
 	log := ctrl.LoggerFrom(ctx)
 
+	// Stop the job if not already suspended
+	if stopErr := r.stopJob(ctx, job, nil, StopReasonNoMatchingWorkload, "missing workload"); stopErr != nil {
```

  I'm concerned with this change as it calls stopJob unconditionally, so affects also the `!usePrebuiltWorkload` scenario. Can we check if `!usePrebuiltWorkload` prior to calling it?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T14:26:53Z

- **pkg/controller/jobframework/reconciler.go:838** by [@trasc](https://github.com/trasc) — 2023-12-12T14:26:53Z
```diff
@@ -780,12 +834,22 @@ func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.W
 func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job GenericJob, object client.Object) error {
 	log := ctrl.LoggerFrom(ctx)
 
+	// Stop the job if not already suspended
+	if stopErr := r.stopJob(ctx, job, nil, StopReasonNoMatchingWorkload, "missing workload"); stopErr != nil {
```

  Before the need for prebuiltWorkload, the creation of the new workloads wold have trigger a new reconcile just after the current one in which the job wold have been stopped. We are doing the same thing , just one reconcile call earlier.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T14:27:52Z

- **pkg/controller/jobframework/reconciler.go:196** by [@mimowo](https://github.com/mimowo) — 2023-12-12T14:18:02Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  I still don't understand why we need this check. From an offline sync we discussed that this is because `QueueName` is not copied when creating the mirror job on the worker cluster. Is this the case, can you elaborate why we don't copy the QueueName?

- **pkg/controller/jobs/job/job_controller.go:251** by [@mimowo](https://github.com/mimowo) — 2023-12-12T14:25:49Z
```diff
@@ -230,6 +246,7 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []podset.PodSetInfo) error {
 			j.Spec.Completions = j.Spec.Parallelism
 		}
 	}
+	addPreservedLabelsToInfo(j.Spec.Template.Labels, &info)
```

  Can you elaborate why this is needed? Some particular questions:
  1. Why just this subset of whitelist labels is copied?
  2. Are there any specific labels we don't want to copy
  3. Why don't we use of blacklist?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T14:33:44Z

- **pkg/controller/jobframework/reconciler.go:196** by [@trasc](https://github.com/trasc) — 2023-12-12T14:33:44Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  In a multikueue scenario the manager cluster can run `manageJobsWithoutQueueName`, but the worker cluster not having it, since we don't need the queue name  (it's already present in the prebuilt workload) , we need to continue the reconcile and not exit via `if isStandaloneJob { ....`

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T14:42:48Z

- **pkg/controller/jobs/job/job_controller.go:251** by [@trasc](https://github.com/trasc) — 2023-12-12T14:42:48Z
```diff
@@ -230,6 +246,7 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []podset.PodSetInfo) error {
 			j.Spec.Completions = j.Spec.Parallelism
 		}
 	}
+	addPreservedLabelsToInfo(j.Spec.Template.Labels, &info)
```

  Comment extended.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T15:07:24Z

- **pkg/controller/jobframework/reconciler.go:838** by [@mimowo](https://github.com/mimowo) — 2023-12-12T15:07:23Z
```diff
@@ -780,12 +834,22 @@ func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.W
 func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job GenericJob, object client.Object) error {
 	log := ctrl.LoggerFrom(ctx)
 
+	// Stop the job if not already suspended
+	if stopErr := r.stopJob(ctx, job, nil, StopReasonNoMatchingWorkload, "missing workload"); stopErr != nil {
```

  Hm, before, if `job.IsActive()` returned `false`, then we created the workload lower in this function, without calling the `stopJob` at all. 
  
  It is not very easy to assess potential nuances of the change on the old code paths. I think surrounding this call with `usePrebuiltWorkload` would make it safer, so that we don't modify the regular code paths.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T15:22:15Z

- **pkg/controller/jobframework/reconciler.go:196** by [@mimowo](https://github.com/mimowo) — 2023-12-12T15:22:15Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  I don't think we need to care about deployments with skewed configuration when the manager has `manageJobsWithoutQueueName` while workers don't. 
  
  I'm thinking that working with the skewed configuration may result in unnecessary code complications. 
  
  I would suggest to copy the `QueueName` label from the main job object to the worker Job. cc @mwielgus in case you have an opinion here.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T15:32:59Z

- **pkg/controller/jobframework/reconciler.go:196** by [@trasc](https://github.com/trasc) — 2023-12-12T15:32:59Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  > I don't think we need to care about deployments with skewed configuration when the manager has manageJobsWithoutQueueName while workers don't.
  
  Why? 
  
  How is this  extra `&& ` so problematic. Is there a case in which it can generate any issue?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T15:52:55Z

- **pkg/controller/jobframework/reconciler.go:196** by [@mimowo](https://github.com/mimowo) — 2023-12-12T15:52:55Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  > How is this extra `&& ` so problematic. Is there a case in which it can generate any issue?
  
  I think the need to care and support skewed configuration adds a lot of conceptual complexity, not only measured by the amount of code.
  
  Second, I think the `&&` is a consequence of code somewhere else to do not copy the "kueue.x-k8s.io/queue-name" label from the Job created by the user to the job running on the worker cluster. However, I'm not sure where that is, could you provide a pointer to the code which is responsible for copying the job, and skipping the label? It will allow me to better assess the complexity, and understand the solution e2e.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T16:29:46Z

- **pkg/controller/jobframework/reconciler.go:579** by [@mimowo](https://github.com/mimowo) — 2023-12-12T16:29:46Z
```diff
@@ -544,28 +556,25 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
-func (r *JobReconciler) getPrebuiltWorkload(ctx context.Context, job GenericJob, object client.Object, name string) (*kueue.Workload, error) {
-	wl := &kueue.Workload{}
-	err := r.client.Get(ctx, types.NamespacedName{Name: name, Namespace: object.GetNamespace()}, wl)
-	if err != nil {
-		return nil, client.IgnoreNotFound(err)
-	}
-
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
 	if !metav1.IsControlledBy(wl, object) {
 		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 			// don't return an error here, since a retry cannot give a different result
-			return nil, nil
+			return false, nil
 		}
 
 		if errs := validation.IsValidLabelValue(string(object.GetUID())); len(errs) == 0 {
 			wl.Labels = maps.MergeKeepFirst(map[string]string{controllerconsts.JobUIDLabel: string(object.GetUID())}, wl.Labels)
 		}
 
 		if err := r.client.Update(ctx, wl); err != nil {
-			return nil, err
+			return false, err
 		}
 	}
+	return true, nil
+}
 
+func (r *JobReconciler) ensurePrebuiltWorkloadInSync(ctx context.Context, wl *kueue.Workload, job GenericJob) (bool, error) {
```

  Fine, though currently the first param can be inferred from `err` by `err==nil`, so I was thinking we could have two params once proven to be needed. However, it is a minor thing, so it may stay that way, IMHO.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T08:07:33Z

- **pkg/controller/jobframework/reconciler.go:196** by [@trasc](https://github.com/trasc) — 2023-12-13T08:07:33Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  As discussed offline, while using prebuilt workloads `kueue.x-k8s.io/queue-name` is not used in any way, why should we require it to be specified?
  
  > I think the need to care and support skewed configuration adds a lot of conceptual complexity, not only measured by the amount of code.
  
  It adds an `&&`. 
  
  > Second, I think the && is a consequence of code somewhere else to do not copy the "kueue.x-k8s.io/queue-name" label from the Job created by the user to the job running on the worker cluster. However, I'm not sure where that is, could you provide a pointer to the code which is responsible for copying the job, and skipping the label? It will allow me to better assess the complexity, and understand the solution e2e.
  
  There is no such code in this PR, in #1313 the queue name is cleared because it based in an earlier version of this PR, keeping it if present will have no functional impact on that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T11:06:08Z

- **pkg/controller/jobframework/reconciler.go:196** by [@mimowo](https://github.com/mimowo) — 2023-12-13T11:06:08Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  Not just `&&`, in order to support the skewed configuration you also remove the `kueue.x-k8s.io/queue-name` label from in here (found it): https://github.com/kubernetes-sigs/kueue/blob/2b0ae7914ba2346644f209abbe969b7478585ad9/pkg/controller/admissionchecks/multikueue/batchjobadaptor.go#L54. 
  
  I think these two changes are highly non-trivial to connect. It is non-trivial for me to understand if the worker Job without the label will perform the same, there might be some other places in code of Kueue which do look it up. 
  Thus, I prefer keeping it simple, at least in the first iteration, until we have a signal to support it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T12:34:41Z

- **pkg/controller/jobs/job/job_controller.go:229** by [@mimowo](https://github.com/mimowo) — 2023-12-13T12:34:36Z
```diff
@@ -216,6 +216,24 @@ func (j *Job) PodSets() []kueue.PodSet {
 	}
 }
 
+// For prebuilt workloads we should preserve the following template labels since they
+// are generated by the batch/job controller upon job object creation, and only used
+// by it to identify the jobs pods.
+var (
+	labelPrefix         = "batch.kubernetes.io/"
+	jobNameSuffix       = "job-name"
+	controllerUidSuffix = "controller-uid"
+	preservedLabelKeys  = []string{jobNameSuffix, controllerUidSuffix, labelPrefix + jobNameSuffix, labelPrefix + controllerUidSuffix}
+)
+
+func addPreservedLabelsToInfo(labels map[string]string, info *podset.PodSetInfo) {
```

  I see *what* we do - copy a hardcoded subset of labels that to the workload on `RunWithPodSetsInfo`, and copied back to Job on  `RunWithPodSetsInfo`.  
  
  However, I still don't understand *why*. Also, I don't see this code being protected by the presence of prebuilt-workload, so it makes me uncertain if the currently existing functionality is not affected.
  
  Some clarifying questions to help me understand:
  - is this code needed for the management cluster Job, or the worker Job (or both?)
  - just a suspicion: does the code aim that the UID of the Job on the worker is the same as the UID of the job in management cluster?
  - can the scope be narrowed down to only jobs with prebuilt-workload?
  - what would happen if we don't do it?
  - why we do this for this specific set of labels, and not others?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T13:18:40Z

- **pkg/controller/jobframework/reconciler.go:196** by [@trasc](https://github.com/trasc) — 2023-12-13T13:18:40Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  >Not just &&, in order to support the skewed configuration you also remove the kueue.x-k8s.io/queue-name label from in here (found it): 
  
  >>There is no such code in this PR, in https://github.com/kubernetes-sigs/kueue/pull/1313 the queue name is cleared because it based in an earlier version of this PR, keeping it if present will have no functional impact on that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T13:36:40Z

- **pkg/controller/jobframework/reconciler.go:196** by [@mimowo](https://github.com/mimowo) — 2023-12-13T13:36:40Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  So, IIUC, you say that you are going to remove that line `delete(remoteJob.Labels, constants.QueueLabel)` from the 1313 PR. Ok, and in this case you can support `manageJobsWithoutQueueName: true` on manager and `manageJobsWithoutQueueName: false` on worker. by adding this check `&&` in case the job doesn't have `QueueName` label. 
  
  Ok I think I understand, still it was a journey to wrap my head around it. I don't like supporting the skewed configuration in general. Deciding if a change is "easy to understand" in that case is subjective. IMHO, we are better of if not investing time in developing, reviewing, testing and maintaining support for such configurations, unless really good reasons, but I don't see such now. 
  
  So, I would vote for removing this check. Also, it is possible that we forget about some corner case anyway, and it would fail later, rather than fast for the user, making it even harder to diagnose. I would rather see some sort of validation for the cluster configurations to verify they are consistent.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T13:44:31Z

- **pkg/controller/jobs/job/job_controller.go:229** by [@trasc](https://github.com/trasc) — 2023-12-13T13:44:18Z
```diff
@@ -216,6 +216,24 @@ func (j *Job) PodSets() []kueue.PodSet {
 	}
 }
 
+// For prebuilt workloads we should preserve the following template labels since they
+// are generated by the batch/job controller upon job object creation, and only used
+// by it to identify the jobs pods.
+var (
+	labelPrefix         = "batch.kubernetes.io/"
+	jobNameSuffix       = "job-name"
+	controllerUidSuffix = "controller-uid"
+	preservedLabelKeys  = []string{jobNameSuffix, controllerUidSuffix, labelPrefix + jobNameSuffix, labelPrefix + controllerUidSuffix}
+)
+
+func addPreservedLabelsToInfo(labels map[string]string, info *podset.PodSetInfo) {
```

  I think we discussed offline at least part of this.
  
  >  is this code needed for the management cluster Job, or the worker Job (or both?)
  
  Not relevant , trying to change them will fail, they are generated and "guarded" by the main batch/job controller/webhook.
  
  > just a suspicion: does the code aim that the UID of the Job on the worker is the same as the UID of the job in management cluster?
  
  No. The UIDs will most likely be different, which will lead to the inability to start the job on the worker.
  
  > can the scope be narrowed down to only jobs with prebuilt-workload?
  
  Maybe but it will not make a difference,  if the workload is not prebuilt, the values are already the same.
  
  >  what would happen if we don't do it?
  
  Trying to set them to something else than the values enforced by the main batch/job controller / webhook  will result in a web-hook error at first.
  
  > why we do this for this specific set of labels, and not others?
  
  These are the labels generated by  main batch/job controller / webhook and used to track the child pods in execution.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T14:46:00Z

- **pkg/controller/jobframework/reconciler.go:196** by [@trasc](https://github.com/trasc) — 2023-12-13T14:46:00Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  I vote to keep it, as debugging a case in which you the prebuilt workload admitted , the job created and not starting will be hard to diagnose.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T15:51:39Z

- **pkg/controller/jobframework/reconciler.go:196** by [@mimowo](https://github.com/mimowo) — 2023-12-13T15:51:38Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  TBH, I think this goes the other way. 
  
  If we support it, then and a job does not start (for whatever reason) in a skewed setup after 10days, then.seeing that the setup is skewed will be red-harrying that takes time to understand. 
  
  Maybe it is clear for you now this is supported, but I'm not sure, and the admins will not know, and this will be their first question to OnCalls if they see the configuration is skewed. Unless, we document that it is supported, and say, and have tests for it. I'm just reluctant to commit to support it. 
  
  Finally, I really don't want to multiply the discussions by every feature we have. For example, what if `waitForPodsReady` is enabled in management cluster, but not worker, what if the set of `integrations.frameworks` differ, etc. 
  
  Ideally I would like to have a multi-cluster configuration validator, it could access the configurations in config-maps, and diagnose if there are skewed. It would make Kueue fail fast (to be decided at which point exactly), and inform that skewed configurations are not supported.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T16:08:45Z

- **pkg/controller/jobframework/reconciler.go:196** by [@trasc](https://github.com/trasc) — 2023-12-13T16:08:45Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  I don't understand.  
  
  > If we support it, then and a job does not start (for whatever reason) in a skewed setup after 10days, then.seeing that the setup is skewed will be red-harrying that takes time to understand.
  
  We should not try to cover this case because, in 10 days (while the workload locks the worker resources),  after debugging why the job is not started we might find some other configuration differences between the manager and worker cluster besides `manageJobsWithoutQueueName`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T16:14:42Z

- **pkg/controller/jobframework/reconciler.go:196** by [@mimowo](https://github.com/mimowo) — 2023-12-13T16:14:42Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  I was just saying that the users will face a problem after 10days for whatever reason. It does not necessarily mean the workload is 10days old. The admin starts investigating and compares the configurations. They identify a difference in `manageJobsWithoutQueueName`, they suspect the difference in `manageJobsWithoutQueueName` has something to do with the issue. It might or might not be related, but it catches attention, and raises questions "could this be the reason?". The questions are escalated to the OnCall team.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T16:18:55Z

- **pkg/controller/jobs/job/job_controller.go:229** by [@mimowo](https://github.com/mimowo) — 2023-12-13T16:18:54Z
```diff
@@ -216,6 +216,24 @@ func (j *Job) PodSets() []kueue.PodSet {
 	}
 }
 
+// For prebuilt workloads we should preserve the following template labels since they
+// are generated by the batch/job controller upon job object creation, and only used
+// by it to identify the jobs pods.
+var (
+	labelPrefix         = "batch.kubernetes.io/"
+	jobNameSuffix       = "job-name"
+	controllerUidSuffix = "controller-uid"
+	preservedLabelKeys  = []string{jobNameSuffix, controllerUidSuffix, labelPrefix + jobNameSuffix, labelPrefix + controllerUidSuffix}
+)
+
+func addPreservedLabelsToInfo(labels map[string]string, info *podset.PodSetInfo) {
```

  Sorry, I still don't understand, maybe another offline session will help :).
  
  In the meanwhile, you say:
  > Not relevant , trying to change them will fail, they are generated and "guarded" by the main batch/job controller/webhook.
  
  what would be trying to change the values? Can you point to the code, I'm really confused about this part.
  
  > No. The UIDs will most likely be different, which will lead to the inability to start the job on the worker.
  
  This is also confusing to me. Could you please provide maybe snippets for the mainJob and the mirrorJob, containing the example values of the labels, to illustrate what would be the problem / conflict without this change, and how the values of the labels will relate after this change.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T16:46:13Z

- **pkg/controller/jobframework/reconciler.go:196** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T16:46:13Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  Even if the label won't be used (because the queue name is in the Workload), it's important that the jobs in the worker and management cluster look as identical as possible, so we should copy the label.
  
  Additionally, while `&& !usePrebuiltWorkload` looks simple, in theory it duplicates the number of test cases that we need to fully cover this piece of code. If we can avoid that, it's a win.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T20:24:16Z

- **pkg/controller/jobframework/reconciler.go:196** by [@trasc](https://github.com/trasc) — 2023-12-13T20:24:15Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  By not adding it we leave open the possibility of hitting. 
  
  https://github.com/kubernetes-sigs/kueue/pull/1358#discussion_r1424081189

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T20:44:23Z

- **pkg/controller/jobframework/reconciler.go:196** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T20:44:23Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  > In a multikueue scenario the manager cluster can run manageJobsWithoutQueueName
  
  In that case, the only thing that Kueue does is prevent the job from running, and the job is never admitted without a queue, which means that this job shouldn't make it to the worker cluster.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-14T09:23:23Z

- **pkg/controller/jobs/job/job_controller.go:229** by [@mimowo](https://github.com/mimowo) — 2023-12-14T09:23:23Z
```diff
@@ -216,6 +216,24 @@ func (j *Job) PodSets() []kueue.PodSet {
 	}
 }
 
+// For prebuilt workloads we should preserve the following template labels since they
+// are generated by the batch/job controller upon job object creation, and only used
+// by it to identify the jobs pods.
+var (
+	labelPrefix         = "batch.kubernetes.io/"
+	jobNameSuffix       = "job-name"
+	controllerUidSuffix = "controller-uid"
+	preservedLabelKeys  = []string{jobNameSuffix, controllerUidSuffix, labelPrefix + jobNameSuffix, labelPrefix + controllerUidSuffix}
+)
+
+func addPreservedLabelsToInfo(labels map[string]string, info *podset.PodSetInfo) {
```

  I see the code is changed in https://github.com/kubernetes-sigs/kueue/pull/1358/commits/697d83efe6d97ca12d9beb49864a6c080b5a7de8. 
  The new version is much easier to comprehend. Thank you!

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-14T09:29:55Z

- **pkg/controller/jobframework/reconciler.go:196** by [@trasc](https://github.com/trasc) — 2023-12-14T09:29:55Z
```diff
@@ -187,11 +188,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	isStandaloneJob := ParentWorkloadName(job) == ""
+	usePrebuiltWorkload, _ := prebuiltWorkload(job)
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
-	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
+	if !r.manageJobsWithoutQueueName && QueueName(job) == "" && !usePrebuiltWorkload {
```

  Still i feel like we are ignoring a potential future issue, ... removed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-14T11:33:31Z

LGTM overall, couple of nits. I'm yet to review tests.

- **pkg/controller/jobframework/reconciler.go:558** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:15:00Z
```diff
@@ -537,6 +555,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
```

  nit: maybe to consider `adoptPrebuildWorkload`, by analogy to core k8s: https://github.com/kubernetes/kubernetes/blob/68ef2423e81ed5efdb3f9881bbf5c91a81fb7448/pkg/controller/controller_ref_manager.go#L222

- **pkg/controller/jobframework/validation.go:91** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:18:45Z
```diff
@@ -73,6 +85,12 @@ func ValidateUpdateForQueueName(oldJob, newJob GenericJob) field.ErrorList {
 	if !newJob.IsSuspended() {
 		allErrs = append(allErrs, apivalidation.ValidateImmutableField(QueueName(oldJob), QueueName(newJob), queueNameLabelPath)...)
 	}
+
+	oldHasPrebuiltWl, oldWlName := prebuiltWorkload(oldJob)
+	newHasPrebuiltWl, newWlName := prebuiltWorkload(newJob)
+	if oldHasPrebuiltWl || newHasPrebuiltWl {
```

  This check does not seem necessary, here we do an analogous check: https://github.com/kubernetes-sigs/kueue/blob/615dc8e42d160174962f379dd8767e95298a1457/pkg/controller/jobframework/validation.go#L80

- **pkg/controller/jobs/job/job_controller.go:214** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:21:41Z
```diff
@@ -205,11 +205,27 @@ func (j *Job) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	}}, nil
 }
 
+// The following labels are managed internally by batch/job controller, we should not
+// propagate them to the workload.
+var (
+	// the legacy names are no longer defined in the api, only in k/2/apis/batch
+	legacyJobNameLabel       = "job-name"
+	legacyControllerUidLabel = "controller-uid"
+	mangedLabels             = []string{legacyJobNameLabel, legacyControllerUidLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  ```suggestion
  	managedLabels             = []string{legacyJobNameLabel, legacyControllerUidLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
  ```

- **pkg/controller/jobs/job/job_controller.go:269** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:31:37Z
```diff
@@ -247,7 +263,17 @@ func (j *Job) RestorePodSetsInfo(podSetsInfo []podset.PodSetInfo) bool {
 			j.Spec.Completions = j.Spec.Parallelism
 		}
 	}
-	changed = podset.RestorePodSpec(&j.Spec.Template.ObjectMeta, &j.Spec.Template.Spec, podSetsInfo[0]) || changed
+	info := podSetsInfo[0]
+	for _, mangedLabel := range mangedLabels {
+		if v, found := j.Spec.Template.Labels[mangedLabel]; found {
+			if info.Labels == nil {
```

  I'm wondering what we could do to avoid checks like this scattered in codebase. I would suggest either:
  1. make sure `info.Labels` is not nil  when creating in `FromPodSet`, as we do in the `FromAssignment` already, so seems to make sense being consistent: https://github.com/kubernetes-sigs/kueue/blob/615dc8e42d160174962f379dd8767e95298a1457/pkg/podset/podset.go#L56
  2. use some utility functions to merge the maps for the managedLabels. I think creating an extra map to use `MergeKeepFirst` is fine, but if we want to avoid that, maybe we could have a new utility function `MergeInto`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-14T12:21:11Z

- **pkg/controller/jobframework/reconciler.go:558** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:21:11Z
```diff
@@ -537,6 +555,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
```

  Feel free to ignore it if you like the current name, I'm fine either way, just found the reference, so thought it might be good to share. Some folks might be familiar with the concept of adoption.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-14T12:51:37Z

Pass for tests. Overall lgtm.

- **pkg/controller/jobs/job/job_controller_test.go:419** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:39:43Z
```diff
@@ -402,6 +416,8 @@ func TestReconciler(t *testing.T) {
 		wantJob           batchv1.Job
 		wantWorkloads     []kueue.Workload
 		wantErr           error
+		checkWlOwner      bool
```

  I would suggest always checking

- **pkg/controller/jobs/job/job_controller_test.go:420** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:40:16Z
```diff
@@ -402,6 +416,8 @@ func TestReconciler(t *testing.T) {
 		wantJob           batchv1.Job
 		wantWorkloads     []kueue.Workload
 		wantErr           error
+		checkWlOwner      bool
+		skipOwnerUpdates  bool
```

  I think we don't need it, just skip the adoption in test when `PrebuiltWorkloadLabel` label is set.

- **test/e2e/singlecluster/e2e_test.go:690** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:43:37Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
```

  Add ginkgo Steps to make the test easier to follow, below are just examples, step names as in integration tests are fine either.

- **test/e2e/singlecluster/e2e_test.go:698** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:44:26Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
```

  ginkgo.By("Create a prebuilt workload")

- **test/e2e/singlecluster/e2e_test.go:692** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:44:51Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
```

  Move it down, when creating the job

- **test/e2e/singlecluster/e2e_test.go:706** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:45:10Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
+				Queue(localQueue.Name).
+				PodSets(
+					*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
```

  ginkgo.By("Creating a job that adopts the prebuilt workload")

- **test/e2e/singlecluster/e2e_test.go:713** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:45:51Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
+				Queue(localQueue.Name).
+				PodSets(
+					*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			gomega.Eventually(func(g gomega.Gomega) {
```

  ginkgo.By("Verify the prebuilt workload is adopted by the job")

- **test/e2e/singlecluster/e2e_test.go:724** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:46:28Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
+				Queue(localQueue.Name).
+				PodSets(
+					*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).To(gomega.Succeed())
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(wl.Spec.PodSets[0].Template.Spec.Containers).To(gomega.BeComparableTo(createdJob.Spec.Template.Spec.Containers), "Check the way the job and workload is created")
+				g.Expect(createdWorkload.OwnerReferences).To(gomega.ContainElement(
+					gomega.BeComparableTo(metav1.OwnerReference{
+						Name: sampleJob.Name,
+						UID:  sampleJob.UID,
+					}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
```

  ginkgo.By("Verify the job is running")

- **test/e2e/singlecluster/e2e_test.go:728** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:46:43Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
+				Queue(localQueue.Name).
+				PodSets(
+					*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).To(gomega.Succeed())
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(wl.Spec.PodSets[0].Template.Spec.Containers).To(gomega.BeComparableTo(createdJob.Spec.Template.Spec.Containers), "Check the way the job and workload is created")
+				g.Expect(createdWorkload.OwnerReferences).To(gomega.ContainElement(
+					gomega.BeComparableTo(metav1.OwnerReference{
+						Name: sampleJob.Name,
+						UID:  sampleJob.UID,
+					}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+				"instance-type": "on-demand",
+			})
+
+			gomega.Eventually(func(g gomega.Gomega) {
```

  ginkgo.By("Await for job completion")

- **test/e2e/singlecluster/e2e_test.go:694** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:49:12Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
```

  Could you follow the pattern we used with the `blockingJob`: https://github.com/kubernetes-sigs/kueue/blob/615dc8e42d160174962f379dd8767e95298a1457/test/e2e/singlecluster/e2e_test.go#L120C5-L120C16. Once the check are done, we just finish the workload by deleting the pods: https://github.com/kubernetes-sigs/kueue/blob/615dc8e42d160174962f379dd8767e95298a1457/test/e2e/singlecluster/e2e_test.go#L175-L177.
  
  This way we don't need to waste 5s on each test. I already opened an issue for the existing tests: https://github.com/kubernetes-sigs/kueue/issues/1449

- **test/integration/controller/jobs/job/job_controller_test.go:448** by [@mimowo](https://github.com/mimowo) — 2023-12-14T12:50:11Z
```diff
@@ -382,6 +382,125 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 		})
 	})
 
+	ginkgo.When("A prebuilt workload is used", func() {
+		ginkgo.It("Should get suspended if the workload is not found", func() {
+			job := testingjob.MakeJob("job", ns.Name).
+				Queue("main").
+				Label(constants.PrebuiltWorkloadLabel, "missing-workload").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJob := batchv1.Job{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(job), &createdJob)).To(gomega.Succeed())
+				g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should take the ownership of the workload and continue the usual execution", func() {
+			container := corev1.Container{
+				Name:  "c",
+				Image: "pause",
+			}
+			testingjob.SetContainerDefaults(&container)
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(*testing.MakePodSet("main", 1).
+					Containers(*container.DeepCopy()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWl := kueue.Workload{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWl)).To(gomega.Succeed())
+				g.Expect(createdWl.OwnerReferences).To(gomega.BeEmpty())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			job := testingjob.MakeJob("job", ns.Name).
+				Queue("main").
+				Label(constants.PrebuiltWorkloadLabel, "wl").
+				Containers(*container.DeepCopy()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+			ginkgo.By("Checking the job gets suspended", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdJob := batchv1.Job{}
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(job), &createdJob)).To(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the job gets the ownership of the workload", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdWl := kueue.Workload{}
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWl)).To(gomega.Succeed())
+
+					g.Expect(createdWl.OwnerReferences).To(gomega.ContainElement(
+						gomega.BeComparableTo(metav1.OwnerReference{
+							Name: job.Name,
+							UID:  job.UID,
+						}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+
+					// The workload is not marked as finished.
+					g.Expect(apimeta.IsStatusConditionTrue(createdWl.Status.Conditions, kueue.WorkloadFinished)).To(gomega.BeFalse())
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Admitting the workload , the job should unsuspend", func() {
```

  ```suggestion
  			ginkgo.By("Admitting the workload, the job should unsuspend", func() {
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-14T14:52:15Z

- **pkg/controller/jobframework/reconciler.go:838** by [@trasc](https://github.com/trasc) — 2023-12-14T14:52:15Z
```diff
@@ -780,12 +834,22 @@ func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.W
 func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job GenericJob, object client.Object) error {
 	log := ctrl.LoggerFrom(ctx)
 
+	// Stop the job if not already suspended
+	if stopErr := r.stopJob(ctx, job, nil, StopReasonNoMatchingWorkload, "missing workload"); stopErr != nil {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-14T19:17:05Z

- **pkg/controller/jobframework/reconciler.go:558** by [@trasc](https://github.com/trasc) — 2023-12-14T15:03:46Z
```diff
@@ -537,6 +555,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
```

  The semantics are a bit different in the sense that we deliberately call it on workloads very likely already owned by us.

- **pkg/controller/jobframework/validation.go:91** by [@trasc](https://github.com/trasc) — 2023-12-14T15:07:46Z
```diff
@@ -73,6 +85,12 @@ func ValidateUpdateForQueueName(oldJob, newJob GenericJob) field.ErrorList {
 	if !newJob.IsSuspended() {
 		allErrs = append(allErrs, apivalidation.ValidateImmutableField(QueueName(oldJob), QueueName(newJob), queueNameLabelPath)...)
 	}
+
+	oldHasPrebuiltWl, oldWlName := prebuiltWorkload(oldJob)
+	newHasPrebuiltWl, newWlName := prebuiltWorkload(newJob)
+	if oldHasPrebuiltWl || newHasPrebuiltWl {
```

  done

- **pkg/controller/jobs/job/job_controller.go:214** by [@trasc](https://github.com/trasc) — 2023-12-14T15:12:24Z
```diff
@@ -205,11 +205,27 @@ func (j *Job) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	}}, nil
 }
 
+// The following labels are managed internally by batch/job controller, we should not
+// propagate them to the workload.
+var (
+	// the legacy names are no longer defined in the api, only in k/2/apis/batch
+	legacyJobNameLabel       = "job-name"
+	legacyControllerUidLabel = "controller-uid"
+	mangedLabels             = []string{legacyJobNameLabel, legacyControllerUidLabel, batchv1.JobNameLabel, batchv1.ControllerUidLabel}
```

  done

- **pkg/controller/jobs/job/job_controller.go:269** by [@trasc](https://github.com/trasc) — 2023-12-14T15:16:23Z
```diff
@@ -247,7 +263,17 @@ func (j *Job) RestorePodSetsInfo(podSetsInfo []podset.PodSetInfo) bool {
 			j.Spec.Completions = j.Spec.Parallelism
 		}
 	}
-	changed = podset.RestorePodSpec(&j.Spec.Template.ObjectMeta, &j.Spec.Template.Spec, podSetsInfo[0]) || changed
+	info := podSetsInfo[0]
+	for _, mangedLabel := range mangedLabels {
+		if v, found := j.Spec.Template.Labels[mangedLabel]; found {
+			if info.Labels == nil {
```

  we can add a new  util function like
  
  ```go
  func AddOrUpdate[M ~map[K]V](m M, k K, v V) M {
  	if m == nil {
  		return M{k: v}
  	}
  	m[k]=v
  	return m
  }
  ```
  
  But the overall benefit is limited in my opinion.

- **pkg/controller/jobs/job/job_controller_test.go:419** by [@trasc](https://github.com/trasc) — 2023-12-14T15:38:15Z
```diff
@@ -402,6 +416,8 @@ func TestReconciler(t *testing.T) {
 		wantJob           batchv1.Job
 		wantWorkloads     []kueue.Workload
 		wantErr           error
+		checkWlOwner      bool
```

  The non prebuiltWorkload tests, have the owner set in the body of the test L1835-L1837, we'll end up testing the test.
  
  We could remove that part in the future and hardcode the references in the tc-s , but the refernce should never change in a non prebuiltWorkload setup.

- **pkg/controller/jobs/job/job_controller_test.go:420** by [@trasc](https://github.com/trasc) — 2023-12-14T15:45:54Z
```diff
@@ -402,6 +416,8 @@ func TestReconciler(t *testing.T) {
 		wantJob           batchv1.Job
 		wantWorkloads     []kueue.Workload
 		wantErr           error
+		checkWlOwner      bool
+		skipOwnerUpdates  bool
```

  It could be a bit hard to follow, done

- **test/e2e/singlecluster/e2e_test.go:690** by [@trasc](https://github.com/trasc) — 2023-12-14T17:02:04Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
```

  done

- **test/e2e/singlecluster/e2e_test.go:692** by [@trasc](https://github.com/trasc) — 2023-12-14T17:02:10Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
```

  we need to modify it before creating the workload which uses its template.

- **test/e2e/singlecluster/e2e_test.go:694** by [@trasc](https://github.com/trasc) — 2023-12-14T18:20:31Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
```

  it's a bit hacky, and the job will end with failure, however I guess we can live with it.

- **test/e2e/singlecluster/e2e_test.go:713** by [@trasc](https://github.com/trasc) — 2023-12-14T18:24:42Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
+				Queue(localQueue.Name).
+				PodSets(
+					*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			gomega.Eventually(func(g gomega.Gomega) {
```

  done

- **test/e2e/singlecluster/e2e_test.go:706** by [@trasc](https://github.com/trasc) — 2023-12-14T18:24:44Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
+				Queue(localQueue.Name).
+				PodSets(
+					*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
```

  done (one step to create both)

- **test/e2e/singlecluster/e2e_test.go:698** by [@trasc](https://github.com/trasc) — 2023-12-14T18:24:47Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
```

  done (one step to create both)

- **test/integration/controller/jobs/job/job_controller_test.go:448** by [@trasc](https://github.com/trasc) — 2023-12-14T19:12:22Z
```diff
@@ -382,6 +382,125 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 		})
 	})
 
+	ginkgo.When("A prebuilt workload is used", func() {
+		ginkgo.It("Should get suspended if the workload is not found", func() {
+			job := testingjob.MakeJob("job", ns.Name).
+				Queue("main").
+				Label(constants.PrebuiltWorkloadLabel, "missing-workload").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJob := batchv1.Job{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(job), &createdJob)).To(gomega.Succeed())
+				g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeTrue())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should take the ownership of the workload and continue the usual execution", func() {
+			container := corev1.Container{
+				Name:  "c",
+				Image: "pause",
+			}
+			testingjob.SetContainerDefaults(&container)
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(*testing.MakePodSet("main", 1).
+					Containers(*container.DeepCopy()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWl := kueue.Workload{}
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWl)).To(gomega.Succeed())
+				g.Expect(createdWl.OwnerReferences).To(gomega.BeEmpty())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			job := testingjob.MakeJob("job", ns.Name).
+				Queue("main").
+				Label(constants.PrebuiltWorkloadLabel, "wl").
+				Containers(*container.DeepCopy()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, job)).To(gomega.Succeed())
+			ginkgo.By("Checking the job gets suspended", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdJob := batchv1.Job{}
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(job), &createdJob)).To(gomega.Succeed())
+					g.Expect(ptr.Deref(createdJob.Spec.Suspend, false)).To(gomega.BeTrue())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the job gets the ownership of the workload", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdWl := kueue.Workload{}
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &createdWl)).To(gomega.Succeed())
+
+					g.Expect(createdWl.OwnerReferences).To(gomega.ContainElement(
+						gomega.BeComparableTo(metav1.OwnerReference{
+							Name: job.Name,
+							UID:  job.UID,
+						}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+
+					// The workload is not marked as finished.
+					g.Expect(apimeta.IsStatusConditionTrue(createdWl.Status.Conditions, kueue.WorkloadFinished)).To(gomega.BeFalse())
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Admitting the workload , the job should unsuspend", func() {
```

  done

- **test/e2e/singlecluster/e2e_test.go:724** by [@trasc](https://github.com/trasc) — 2023-12-14T19:12:34Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
+				Queue(localQueue.Name).
+				PodSets(
+					*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).To(gomega.Succeed())
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(wl.Spec.PodSets[0].Template.Spec.Containers).To(gomega.BeComparableTo(createdJob.Spec.Template.Spec.Containers), "Check the way the job and workload is created")
+				g.Expect(createdWorkload.OwnerReferences).To(gomega.ContainElement(
+					gomega.BeComparableTo(metav1.OwnerReference{
+						Name: sampleJob.Name,
+						UID:  sampleJob.UID,
+					}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
```

  done

- **test/e2e/singlecluster/e2e_test.go:728** by [@trasc](https://github.com/trasc) — 2023-12-14T19:12:40Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+				Obj()
+			testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+			wl := testing.MakeWorkload("prebuilt-wl", ns.Name).
+				Queue(localQueue.Name).
+				PodSets(
+					*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).To(gomega.Succeed())
+				g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(wl.Spec.PodSets[0].Template.Spec.Containers).To(gomega.BeComparableTo(createdJob.Spec.Template.Spec.Containers), "Check the way the job and workload is created")
+				g.Expect(createdWorkload.OwnerReferences).To(gomega.ContainElement(
+					gomega.BeComparableTo(metav1.OwnerReference{
+						Name: sampleJob.Name,
+						UID:  sampleJob.UID,
+					}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+				"instance-type": "on-demand",
+			})
+
+			gomega.Eventually(func(g gomega.Gomega) {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-15T06:25:41Z

- **pkg/controller/jobs/job/job_controller.go:269** by [@trasc](https://github.com/trasc) — 2023-12-15T06:25:41Z
```diff
@@ -247,7 +263,17 @@ func (j *Job) RestorePodSetsInfo(podSetsInfo []podset.PodSetInfo) bool {
 			j.Spec.Completions = j.Spec.Parallelism
 		}
 	}
-	changed = podset.RestorePodSpec(&j.Spec.Template.ObjectMeta, &j.Spec.Template.Spec, podSetsInfo[0]) || changed
+	info := podSetsInfo[0]
+	for _, mangedLabel := range mangedLabels {
+		if v, found := j.Spec.Template.Labels[mangedLabel]; found {
+			if info.Labels == nil {
```

  Also we could add a method on PodSetInfo lile AddOrUpdateLabel(k,v)...

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T08:20:43Z

- **pkg/controller/jobframework/reconciler.go:558** by [@mimowo](https://github.com/mimowo) — 2023-12-15T08:20:43Z
```diff
@@ -537,6 +555,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T08:21:19Z

- **pkg/controller/jobs/job/job_controller.go:269** by [@mimowo](https://github.com/mimowo) — 2023-12-15T08:21:19Z
```diff
@@ -247,7 +263,17 @@ func (j *Job) RestorePodSetsInfo(podSetsInfo []podset.PodSetInfo) bool {
 			j.Spec.Completions = j.Spec.Parallelism
 		}
 	}
-	changed = podset.RestorePodSpec(&j.Spec.Template.ObjectMeta, &j.Spec.Template.Spec, podSetsInfo[0]) || changed
+	info := podSetsInfo[0]
+	for _, mangedLabel := range mangedLabels {
+		if v, found := j.Spec.Template.Labels[mangedLabel]; found {
+			if info.Labels == nil {
```

  going with the `AddOrUpdate` or `AddOrUpdateLabel` sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T08:28:24Z

- **pkg/controller/jobs/job/job_controller_test.go:419** by [@mimowo](https://github.com/mimowo) — 2023-12-15T08:28:23Z
```diff
@@ -402,6 +416,8 @@ func TestReconciler(t *testing.T) {
 		wantJob           batchv1.Job
 		wantWorkloads     []kueue.Workload
 		wantErr           error
+		checkWlOwner      bool
```

  I see, but:
  1. `checkWlOwner` is set true in tandem with `skipOwnerUpdates`, we could just have one param `setWorkloadOwnershipInTest` (or alike)
  2. we can infer the the value of `setWorkloadOwnershipInTest` based on `PrebuiltWorkloadLabel` set on job.
  
  I think eliminating 2 params (or at least 1) from the test options is a win. First, we maintain the same coverage in non-prebuilt workload scenario. Second, we test that the owner is set in the prebuilt-workload scenario.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T08:29:10Z

- **test/e2e/singlecluster/e2e_test.go:692** by [@mimowo](https://github.com/mimowo) — 2023-12-15T08:29:10Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
```

  got it, sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T08:32:07Z

- **test/e2e/singlecluster/e2e_test.go:694** by [@mimowo](https://github.com/mimowo) — 2023-12-15T08:32:06Z
```diff
@@ -686,6 +687,57 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+				Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+				Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
```

  Yeah, but we don't care if it is a failure or not. Anyway, I'm ok keeping it out of scope for the PR.
  
  We can discuss it on the dedicated issue. I'm just concerned that going forward with 5s overhead per test is not right.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T08:35:52Z

- **pkg/controller/jobs/job/job_controller_test.go:419** by [@mimowo](https://github.com/mimowo) — 2023-12-15T08:35:52Z
```diff
@@ -402,6 +416,8 @@ func TestReconciler(t *testing.T) {
 		wantJob           batchv1.Job
 		wantWorkloads     []kueue.Workload
 		wantErr           error
+		checkWlOwner      bool
```

  Hm, sorry, I didn't see in this test "when the prebuilt workload is missing, no new one is created and the job is suspended" it is not in tandem.
  
  Still, something feels over-complicated for the test setup.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T08:42:43Z

- **pkg/controller/jobs/job/job_controller_test.go:419** by [@mimowo](https://github.com/mimowo) — 2023-12-15T08:42:43Z
```diff
@@ -402,6 +416,8 @@ func TestReconciler(t *testing.T) {
 		wantJob           batchv1.Job
 		wantWorkloads     []kueue.Workload
 		wantErr           error
+		checkWlOwner      bool
```

  Maybe it is a sign this should be a dedicated unit test. I also don't see anything wrong in asserting on the owners even if they are set in the test code `L1835-L1837`. Ok, it might be unnecessary, but allows to eliminate the `checkWlOwner` param, so feels like a win. Also, we would verify also that, for example, the extra reference is not added.
  
  Anyway, the current version is acceptable to me.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-15T09:23:21Z

- **pkg/controller/jobs/job/job_controller.go:269** by [@trasc](https://github.com/trasc) — 2023-12-15T09:17:55Z
```diff
@@ -247,7 +263,17 @@ func (j *Job) RestorePodSetsInfo(podSetsInfo []podset.PodSetInfo) bool {
 			j.Spec.Completions = j.Spec.Parallelism
 		}
 	}
-	changed = podset.RestorePodSpec(&j.Spec.Template.ObjectMeta, &j.Spec.Template.Spec, podSetsInfo[0]) || changed
+	info := podSetsInfo[0]
+	for _, mangedLabel := range mangedLabels {
+		if v, found := j.Spec.Template.Labels[mangedLabel]; found {
+			if info.Labels == nil {
```

  AddOrUpdateLabel done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T09:43:08Z

- **test/e2e/singlecluster/e2e_test.go:747** by [@mimowo](https://github.com/mimowo) — 2023-12-15T09:42:54Z
```diff
@@ -686,6 +687,69 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			var wl *kueue.Workload
+			ginkgo.By("Create the pebuilt workload and the job adopting it", func() {
+				sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+					Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s", "-termination-grace-period", "0s"}).
+					BackoffLimit(0).
+					Obj()
+				testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+				wl = testing.MakeWorkload("prebuilt-wl", ns.Name).
+					Queue(localQueue.Name).
+					PodSets(
+						*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+					).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+			})
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			ginkgo.By("Verify the prebuilt workload is adopted by the job", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).To(gomega.Succeed())
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(wl.Spec.PodSets[0].Template.Spec.Containers).To(gomega.BeComparableTo(createdJob.Spec.Template.Spec.Containers), "Check the way the job and workload is created")
+					g.Expect(createdWorkload.OwnerReferences).To(gomega.ContainElement(
+						gomega.BeComparableTo(metav1.OwnerReference{
+							Name: sampleJob.Name,
+							UID:  sampleJob.UID,
+						}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the job is running", func() {
+				expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+					"instance-type": "on-demand",
+				})
+			})
+
+			ginkgo.By("Delete all pods", func() {
+				gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Await for jobs completion", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
+
+					g.Expect(createdWorkload.Status.Conditions).To(gomega.ContainElement(
+						gomega.BeComparableTo(metav1.Condition{
+							Type:    kueue.WorkloadFinished,
+							Status:  metav1.ConditionTrue,
+							Reason:  "JobFinished",
+							Message: "Job failed",
```

  Actually, I think this job may ether fail or succeed, so I'm concern it may flake. I think not asserting on the message would be enough.
  
  The reason is that when SIGTERM is sent to the `"gcr.io/k8s-staging-perf-tests/sleep:v0.0.3` container it may exit with 0 if the trap is already registered: https://github.com/kubernetes/perf-tests/blob/83cc34c18d8b0a5583d3eec257a0c1bcc3cbd200/util-images/sleep/main.go#L55.  I think if the trap is not registered it will exit with 137 after SIGKILL. To ensure this is fast we also set `TerimnationGracePeriod(1).` here: https://github.com/kubernetes-sigs/kueue/blob/a68c95860549a85061ae53db73d32de4df3c3446/test/e2e/singlecluster/e2e_test.go#L124C6-L124C32. 
  
  I know this is hacky, we may revisit how to do it better.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-15T12:20:38Z

- **test/e2e/singlecluster/e2e_test.go:747** by [@trasc](https://github.com/trasc) — 2023-12-15T12:20:38Z
```diff
@@ -686,6 +687,69 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			var wl *kueue.Workload
+			ginkgo.By("Create the pebuilt workload and the job adopting it", func() {
+				sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+					Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s", "-termination-grace-period", "0s"}).
+					BackoffLimit(0).
+					Obj()
+				testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+				wl = testing.MakeWorkload("prebuilt-wl", ns.Name).
+					Queue(localQueue.Name).
+					PodSets(
+						*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+					).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+			})
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			ginkgo.By("Verify the prebuilt workload is adopted by the job", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).To(gomega.Succeed())
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(wl.Spec.PodSets[0].Template.Spec.Containers).To(gomega.BeComparableTo(createdJob.Spec.Template.Spec.Containers), "Check the way the job and workload is created")
+					g.Expect(createdWorkload.OwnerReferences).To(gomega.ContainElement(
+						gomega.BeComparableTo(metav1.OwnerReference{
+							Name: sampleJob.Name,
+							UID:  sampleJob.UID,
+						}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the job is running", func() {
+				expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+					"instance-type": "on-demand",
+				})
+			})
+
+			ginkgo.By("Delete all pods", func() {
+				gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Await for jobs completion", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
+
+					g.Expect(createdWorkload.Status.Conditions).To(gomega.ContainElement(
+						gomega.BeComparableTo(metav1.Condition{
+							Type:    kueue.WorkloadFinished,
+							Status:  metav1.ConditionTrue,
+							Reason:  "JobFinished",
+							Message: "Job failed",
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T12:30:40Z

- **test/e2e/singlecluster/e2e_test.go:696** by [@mimowo](https://github.com/mimowo) — 2023-12-15T12:30:40Z
```diff
@@ -686,6 +687,68 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			var wl *kueue.Workload
+			ginkgo.By("Create the pebuilt workload and the job adopting it", func() {
+				sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+					Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s", "-termination-grace-period", "0s"}).
+					BackoffLimit(0).
```

  Please also add terminationGracePeriod 1, for the case when the pods start running, but the SIGTERM trap is not registered yet. Otherwise the termination will take 30s.
  
  EDIT: there are three scenarios:
  1. pods are created and pending (termination is immediate, ends with 137)
  2. pods are running, and the SIGTERM trap is not registered, termination is long until SIGKILL and ends with 137 (unlikely, but possible)
  3. pods are running, and the SIGTERM trap is registered, termination is immediate and ends with 0

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-15T13:08:24Z

- **test/e2e/singlecluster/e2e_test.go:696** by [@trasc](https://github.com/trasc) — 2023-12-15T13:08:24Z
```diff
@@ -686,6 +687,68 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			var wl *kueue.Workload
+			ginkgo.By("Create the pebuilt workload and the job adopting it", func() {
+				sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+					Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s", "-termination-grace-period", "0s"}).
+					BackoffLimit(0).
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-15T20:44:08Z

What if I want to delete a Workload before a Job had a chance to use it?
Would the Workload be finalized? Or is that something that will be solved with https://github.com/kubernetes-sigs/kueue/pull/1454 ?

- **pkg/controller/jobframework/interface.go:133** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-15T20:23:26Z
```diff
@@ -129,3 +129,8 @@ func workloadPriorityClassName(job GenericJob) string {
 	}
 	return ""
 }
+
+func prebuiltWorkload(job GenericJob) (bool, string) {
```

  a bit of a nitpick, but why not keep the order `(string, bool)`?

- **pkg/controller/jobframework/reconciler.go:561** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-15T20:25:46Z
```diff
@@ -537,6 +555,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
+	if !metav1.IsControlledBy(wl, object) {
+		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
+			// don't return an error here, since a retry cannot give a different result,
```

  Under which circumstances can this fail?
  We should at least report an event for the Job, so that the user can take action.

- **pkg/controller/jobframework/reconciler.go:450** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-15T20:29:16Z
```diff
@@ -439,6 +440,23 @@ func (r *JobReconciler) getParentWorkload(ctx context.Context, job GenericJob, o
 func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, object client.Object) (*kueue.Workload, error) {
 	log := ctrl.LoggerFrom(ctx)
 
+	if usePrebuiltWorkload, prebuiltWorkloadName := prebuiltWorkload(job); usePrebuiltWorkload {
+		wl := &kueue.Workload{}
+		err := r.client.Get(ctx, types.NamespacedName{Name: prebuiltWorkloadName, Namespace: object.GetNamespace()}, wl)
+		if err != nil {
+			return nil, client.IgnoreNotFound(err)
+		}
+
+		if owns, err := r.ensurePrebuiltWorkloadOwnership(ctx, wl, object); !owns || err != nil {
```

  For consistency with the rest of the code, use an UnretriableError, instead of the extra boolean https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/errors.go

- **test/e2e/singlecluster/e2e_test.go:699** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-15T20:41:46Z
```diff
@@ -686,6 +687,69 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			var wl *kueue.Workload
+			ginkgo.By("Create the pebuilt workload and the job adopting it", func() {
+				sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+					Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s", "-termination-grace-period", "0s"}).
+					BackoffLimit(0).
+					TerimnationGracePeriod(1).
+					Obj()
+				testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
```

  why is this necessary?

- **test/e2e/singlecluster/e2e_test.go:742** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-15T20:42:42Z
```diff
@@ -686,6 +687,69 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			var wl *kueue.Workload
+			ginkgo.By("Create the pebuilt workload and the job adopting it", func() {
+				sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+					Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s", "-termination-grace-period", "0s"}).
+					BackoffLimit(0).
+					TerimnationGracePeriod(1).
+					Obj()
+				testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+				wl = testing.MakeWorkload("prebuilt-wl", ns.Name).
+					Queue(localQueue.Name).
+					PodSets(
+						*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+					).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+			})
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			ginkgo.By("Verify the prebuilt workload is adopted by the job", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).To(gomega.Succeed())
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(wl.Spec.PodSets[0].Template.Spec.Containers).To(gomega.BeComparableTo(createdJob.Spec.Template.Spec.Containers), "Check the way the job and workload is created")
+					g.Expect(createdWorkload.OwnerReferences).To(gomega.ContainElement(
+						gomega.BeComparableTo(metav1.OwnerReference{
+							Name: sampleJob.Name,
+							UID:  sampleJob.UID,
+						}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the job is running", func() {
+				expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+					"instance-type": "on-demand",
+				})
+			})
+
+			ginkgo.By("Delete all pods", func() {
+				gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Await for jobs completion", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
```

  Let's also make sure it's finalized.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-18T10:19:36Z

- **pkg/controller/jobframework/interface.go:133** by [@trasc](https://github.com/trasc) — 2023-12-18T07:11:24Z
```diff
@@ -129,3 +129,8 @@ func workloadPriorityClassName(job GenericJob) string {
 	}
 	return ""
 }
+
+func prebuiltWorkload(job GenericJob) (bool, string) {
```

  I think it used to do more, order changed

- **pkg/controller/jobframework/reconciler.go:450** by [@trasc](https://github.com/trasc) — 2023-12-18T07:27:12Z
```diff
@@ -439,6 +440,23 @@ func (r *JobReconciler) getParentWorkload(ctx context.Context, job GenericJob, o
 func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, object client.Object) (*kueue.Workload, error) {
 	log := ctrl.LoggerFrom(ctx)
 
+	if usePrebuiltWorkload, prebuiltWorkloadName := prebuiltWorkload(job); usePrebuiltWorkload {
+		wl := &kueue.Workload{}
+		err := r.client.Get(ctx, types.NamespacedName{Name: prebuiltWorkloadName, Namespace: object.GetNamespace()}, wl)
+		if err != nil {
+			return nil, client.IgnoreNotFound(err)
+		}
+
+		if owns, err := r.ensurePrebuiltWorkloadOwnership(ctx, wl, object); !owns || err != nil {
```

  the usage is a bit different as in case the workkoad is owned by some other job we want to suspend the current job.

- **pkg/controller/jobframework/reconciler.go:561** by [@trasc](https://github.com/trasc) — 2023-12-18T07:28:18Z
```diff
@@ -537,6 +555,41 @@ func FindMatchingWorkloads(ctx context.Context, c client.Client, job GenericJob)
 	return match, toDelete, nil
 }
 
+func (r *JobReconciler) ensurePrebuiltWorkloadOwnership(ctx context.Context, wl *kueue.Workload, object client.Object) (bool, error) {
+	if !metav1.IsControlledBy(wl, object) {
+		if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
+			// don't return an error here, since a retry cannot give a different result,
```

  bad types of objects (unlikely), the target has another owner

- **test/e2e/singlecluster/e2e_test.go:699** by [@trasc](https://github.com/trasc) — 2023-12-18T07:38:28Z
```diff
@@ -686,6 +687,69 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			var wl *kueue.Workload
+			ginkgo.By("Create the pebuilt workload and the job adopting it", func() {
+				sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+					Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s", "-termination-grace-period", "0s"}).
+					BackoffLimit(0).
+					TerimnationGracePeriod(1).
+					Obj()
+				testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
```

  to make the workload equivalent to the job, the job first container is used in crating the prebuilt workload and we need it to the as the one in the job after going through the batch/job mutating webhhok

- **test/e2e/singlecluster/e2e_test.go:742** by [@trasc](https://github.com/trasc) — 2023-12-18T07:56:46Z
```diff
@@ -686,6 +687,69 @@ var _ = ginkgo.Describe("Kueue", func() {
 			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
 		})
 
+		ginkgo.It("Should run with prebuilt workload", func() {
+			var wl *kueue.Workload
+			ginkgo.By("Create the pebuilt workload and the job adopting it", func() {
+				sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).
+					Label(constants.PrebuiltWorkloadLabel, "prebuilt-wl").
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s", "-termination-grace-period", "0s"}).
+					BackoffLimit(0).
+					TerimnationGracePeriod(1).
+					Obj()
+				testingjob.SetContainerDefaults(&sampleJob.Spec.Template.Spec.Containers[0])
+
+				wl = testing.MakeWorkload("prebuilt-wl", ns.Name).
+					Queue(localQueue.Name).
+					PodSets(
+						*testing.MakePodSet("main", 1).Containers(sampleJob.Spec.Template.Spec.Containers[0]).Obj(),
+					).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, wl)).Should(gomega.Succeed())
+				gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+			})
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := client.ObjectKeyFromObject(wl)
+			createdJob := &batchv1.Job{}
+			jobLookupKey := client.ObjectKeyFromObject(sampleJob)
+
+			ginkgo.By("Verify the prebuilt workload is adopted by the job", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, jobLookupKey, createdJob)).To(gomega.Succeed())
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(wl.Spec.PodSets[0].Template.Spec.Containers).To(gomega.BeComparableTo(createdJob.Spec.Template.Spec.Containers), "Check the way the job and workload is created")
+					g.Expect(createdWorkload.OwnerReferences).To(gomega.ContainElement(
+						gomega.BeComparableTo(metav1.OwnerReference{
+							Name: sampleJob.Name,
+							UID:  sampleJob.UID,
+						}, cmpopts.IgnoreFields(metav1.OwnerReference{}, "APIVersion", "Kind", "Controller", "BlockOwnerDeletion"))))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the job is running", func() {
+				expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+					"instance-type": "on-demand",
+				})
+			})
+
+			ginkgo.By("Delete all pods", func() {
+				gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Await for jobs completion", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-18T15:44:35Z

/approve
