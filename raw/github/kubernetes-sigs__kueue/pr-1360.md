# PR #1360: Multi-cluster tests setups

**Summary**: Multi-cluster tests setups

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1360

**Last updated**: 2023-12-14T11:17:10Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `mk-e2e-infra` → `main`
- **Created**: 2023-11-23T08:02:58Z
- **Updated**: 2023-12-14T11:17:10Z
- **Merged**: 2023-12-13T16:07:08Z
- **Closed**: 2023-12-13T16:07:08Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: _none_
- **Changed files**: 14   **+713 / -122**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Create the e2e and integration tests setup for multi cluster suites.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relate to #693

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-23T08:03:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d7bc7dae24b0955cab476e626272fca6ef249521 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/657850a04938b200085547b8 |

### Comment by [@kundan2707](https://github.com/kundan2707) — 2023-11-23T12:40:04Z

/retitle Multi-cluster tests setups

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-06T17:13:22Z

/remove-kind feature
/kind cleanup

/lgtm
/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-06T17:13:29Z

LGTM label has been added.  <details>Git tree hash: 84e312aad267e84cc4ac3d08b2684b18e0f229ab</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-13T15:39:13Z

LGTM label has been added.  <details>Git tree hash: df9df0b3e015a26de720a0e67e67c5f788acbb3f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-13T15:39:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1360#discussion_r1425535664" title="LGTM">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1360#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-29T14:22:51Z

- **hack/kubectl.yaml:12** by [@mimowo](https://github.com/mimowo) — 2023-11-29T14:05:37Z
```diff
@@ -0,0 +1,24 @@
+apiVersion: v1
+kind: Pod
+metadata:
+  name: kubectl
+spec:
+  volumes:
+    - name: mk
+      secret:
+        secretName: multikueue
+  containers:
+    - name: kubectl
+      image: bitnami/kubectl:latest
```

  I would prefer using the image from the k8s official repo: `registry.k8s.io/kubectl:v1.28.4`. However, maybe we could have some basic ginkgo test, which just lists nodes (or any other resource) on the worker cluster? We need to have the ginkgo tests anyway, so moving the setup to this PR seems to make sense.

- **hack/multikueue-e2e-test.sh:29** by [@mimowo](https://github.com/mimowo) — 2023-11-29T14:11:38Z
```diff
@@ -0,0 +1,144 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
+export WORKER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker
```

  I would suggest adding 2 worker clusters in this setup. No reason to go beyond 2 workers, but I think we need 2 to test some important scenarios, for example, 
  - to make sure a job does not start at 2 workers at the same time
  - the prebuilt workloads are removed from the other workers.

- **test/integration/multikueue/multikueue_test.go:2** by [@mimowo](https://github.com/mimowo) — 2023-11-29T14:20:05Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2023 The Kubernetes Authors.
```

  I feel this is independent of e2e tests, could you send a dedicated PR?

- **test/integration/multikueue/multikueue_test.go:40** by [@mimowo](https://github.com/mimowo) — 2023-11-29T14:21:11Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		leaderNs *corev1.Namespace
+		workerNs *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		leaderNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		workerNs = leaderNs.DeepCopy()
```

  I would suggest these tests to also setup the communication between the leader and the worker, creating the appropriate secret, for symmetry with e2e tests. Still, I would suggest this is a dedicated PR.

- **test/integration/multikueue/multikueue_test.go:32** by [@mimowo](https://github.com/mimowo) — 2023-11-29T14:21:43Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		leaderNs *corev1.Namespace
+		workerNs *corev1.Namespace
```

  Similarly as for e2e tests, I would suggest having 2 workers to cover some scenarios, such as removing workloads from the other workers.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-06T10:39:14Z

- **test/integration/multikueue/multikueue_test.go:2** by [@trasc](https://github.com/trasc) — 2023-12-04T13:59:03Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2023 The Kubernetes Authors.
```

  indeed, but this pr is just meant to make #1313 smaller.

- **hack/kubectl.yaml:12** by [@trasc](https://github.com/trasc) — 2023-12-04T14:09:11Z
```diff
@@ -0,0 +1,24 @@
+apiVersion: v1
+kind: Pod
+metadata:
+  name: kubectl
+spec:
+  volumes:
+    - name: mk
+      secret:
+        secretName: multikueue
+  containers:
+    - name: kubectl
+      image: bitnami/kubectl:latest
```

  done, however the usage will be removed when we add some real tests (#1313)

- **hack/multikueue-e2e-test.sh:29** by [@trasc](https://github.com/trasc) — 2023-12-06T07:26:51Z
```diff
@@ -0,0 +1,144 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
+export WORKER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker
```

  Done, however the extra resource usage could be a problem,  on my local setup I have encounter     https://kind.sigs.k8s.io/docs/user/known-issues/#pod-errors-due-to-too-many-open-files

- **test/integration/multikueue/multikueue_test.go:40** by [@trasc](https://github.com/trasc) — 2023-12-06T10:30:49Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		leaderNs *corev1.Namespace
+		workerNs *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		leaderNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		workerNs = leaderNs.DeepCopy()
```

  done, but I think there is no need to split the PR

- **test/integration/multikueue/multikueue_test.go:32** by [@trasc](https://github.com/trasc) — 2023-12-06T10:31:00Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		leaderNs *corev1.Namespace
+		workerNs *corev1.Namespace
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-06T11:38:52Z

- **Makefile:186** by [@mimowo](https://github.com/mimowo) — 2023-12-06T11:35:27Z
```diff
@@ -176,6 +178,12 @@ run-test-e2e-%: FORCE
 	@echo Running e2e for k8s ${K8S_VERSION}
 	E2E_KIND_VERSION="kindest/node:v$(K8S_VERSION)" KIND_CLUSTER_NAME=$(KIND_CLUSTER_NAME) CREATE_KIND_CLUSTER=$(CREATE_KIND_CLUSTER) ARTIFACTS="$(ARTIFACTS)/$@" IMAGE_TAG=$(IMAGE_TAG) ./hack/e2e-test.sh
 
+run-test-multikueue-e2e-%: K8S_VERSION = $(@:run-test-multikueue-e2e-%=%)
+run-test-multikueue-e2e-%: FORCE
+	@echo Running multikueue e2e for k8s ${K8S_VERSION}
+	E2E_KIND_VERSION="kindest/node:v$(K8S_VERSION)" KIND_CLUSTER_NAME=$(KIND_CLUSTER_NAME) CREATE_KIND_CLUSTER=$(CREATE_KIND_CLUSTER) ARTIFACTS="$(ARTIFACTS)/$@" IMAGE_TAG=$(IMAGE_TAG) ./hack/multikueue-e2e-test.sh
+
+
```

  nit, remove one empty line

- **hack/mk-leader-cluster.yaml:2** by [@mimowo](https://github.com/mimowo) — 2023-12-06T11:37:09Z
```diff
@@ -0,0 +1,4 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
```

  Can we call it manager rather than leader? To align the vocab with KEP, see the naming discussion in the KEP PR: https://github.com/kubernetes-sigs/kueue/pull/1380#discussion_r1409336565.

- **hack/multikueue-e2e-test.sh:28** by [@mimowo](https://github.com/mimowo) — 2023-12-06T11:38:06Z
```diff
@@ -0,0 +1,239 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
```

  Similar naming comment, I would suggest MANGER, instead of LEADER to align with KEP

- **hack/multikueue-e2e-test.sh:232** by [@mimowo](https://github.com/mimowo) — 2023-12-06T11:38:33Z
```diff
@@ -0,0 +1,239 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
+export WORKER1_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker1
+export WORKER2_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker2
+
+# $1 - cluster name
+function cluster_cleanup {
+	kubectl config use-context kind-$1
+        kubectl logs -n kube-system kube-scheduler-$1-control-plane > $ARTIFACTS/$1-kube-scheduler.log || true
+        kubectl logs -n kube-system kube-controller-manager-$1-control-plane > $ARTIFACTS/$1-kube-controller-manager.log || true
+        kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/$1-kueue-controller-manager.log || true
+        kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
+        kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
+        $KIND delete cluster --name $1
+}
+
+function cleanup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_cleanup $LEADER_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER1_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER2_KIND_CLUSTER_NAME
+    fi
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
+}
+
+
+# $1 cluster name
+# $2 cluster kind config
+function cluster_create {
+        $KIND create cluster --name $1 --image $E2E_KIND_VERSION --config $2 --wait 1m -v 5  > $ARTIFACTS/$1-create.log 2>&1 \
+		||  { echo "unable to start the $1 cluster "; cat $ARTIFACTS/$1-create.log ; }
+	kubectl config use-context kind-$1
+        kubectl get nodes > $ARTIFACTS/$1-nodes.log || true
+        kubectl describe pods -n kube-system > $ARTIFACTS/$1-system-pods.log || true
+}
+
+function startup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_create $LEADER_KIND_CLUSTER_NAME $SOURCE_DIR/mk-leader-cluster.yaml
+
+	# NOTE: for local setup, make sure that your firewalk allows tcp from leader to the GW ip
+	# eg. ufw `sudo ufw allow from 172.18.0.0/16 proto tcp to 172.18.0.1`
+	#
+	# eg. iptables    `sudo iptables --append INPUT --protocol tcp --src 172.18.0.0/16 --dst 172.18.0.1 --jump ACCEPT
+	#                  sudo iptables --append OUTPUT --protocol tcp --src 172.18.0.1 --dst 172.18.0./0/16 --jump ACCEPT`
+
+	# have the worker forward the api to the docker gateway address instead of lo
+	export GW=$(docker inspect ${LEADER_KIND_CLUSTER_NAME}-control-plane -f '{{.NetworkSettings.Networks.kind.Gateway}}')
+	$YQ e '.networking.apiServerAddress=env(GW)'  $SOURCE_DIR/mk-worker-cluster.yaml > $ARTIFACTS/worker-cluster.yaml
+
+	cluster_create $WORKER1_KIND_CLUSTER_NAME $ARTIFACTS/worker-cluster.yaml
+	cluster_create $WORKER2_KIND_CLUSTER_NAME $ARTIFACTS/worker-cluster.yaml
+
+	# push the worker kubeconfig in a leader's secret
+	$KIND get kubeconfig --name $WORKER1_KIND_CLUSTER_NAME  > ${ARTIFACTS}/worker1.kubeconfig
+	$KIND get kubeconfig --name $WORKER2_KIND_CLUSTER_NAME  > ${ARTIFACTS}/worker2.kubeconfig
+	kubectl config use-context kind-${LEADER_KIND_CLUSTER_NAME}
+	kubectl create secret generic multikueue --from-file=${ARTIFACTS}/worker1.kubeconfig --from-file=${ARTIFACTS}/worker2.kubeconfig
+    fi
+}
+
+# $1 cluster
+function cluster_kind_load {
+        $KIND load docker-image $E2E_TEST_IMAGE --name $1
+        $KIND load docker-image $IMAGE_TAG --name $1
+}
+
+function kind_load {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        docker pull $E2E_TEST_IMAGE
+        cluster_kind_load $LEADER_KIND_CLUSTER_NAME
+        cluster_kind_load $WORKER1_KIND_CLUSTER_NAME
+        cluster_kind_load $WORKER2_KIND_CLUSTER_NAME 
+    fi
+}
+
+# $1 cluster
+function cluster_kueue_deploy {
+    kubectl config use-context kind-${1}
+    kubectl apply --server-side -k test/e2e/config
+}
+
+function kueue_deploy {
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=$IMAGE_TAG)
+
+    cluster_kueue_deploy $LEADER_KIND_CLUSTER_NAME
+    cluster_kueue_deploy $WORKER1_KIND_CLUSTER_NAME
+    cluster_kueue_deploy $WORKER2_KIND_CLUSTER_NAME
+
+    #wait for the manager to start
+    # failing a this point might indicate a manifestation of
+    # https://kind.sigs.k8s.io/docs/user/known-issues/#pod-errors-due-to-too-many-open-files
+    kubectl config use-context kind-$LEADER_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+    kubectl config use-context kind-$WORKER1_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+    kubectl config use-context kind-$WORKER2_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+}
+
+trap cleanup EXIT
+startup
+kind_load
+kueue_deploy 
+
+#TODO: run some actual tests
+#$GINKGO --junit-report=junit.xml --output-dir=$ARTIFACTS -v ./test/multikueue-e2e/...
+
+
+#TODO: remove the following lines
+
+# even if waiting for deployment, the webhook server might not be up, wait an extra 10 sec
+sleep 10
+
+kubectl config use-context kind-$LEADER_KIND_CLUSTER_NAME
+kubectl run kubectl-to-1 --image registry.k8s.io/kubectl:v1.28.4 --overrides=\
+'
+{
+	"spec": {
+		"volumes": [
+			{
+				"name": "mk",
+				"secret": {
+					"secretName": "multikueue"
+				}
+			}
+		],
+		"containers": [
+			{
+				"name": "kubectl",
+				"image": "registry.k8s.io/kubectl:v1.28.4",
+				"env": [
+					{
+						"name": "KUBECONFIG",
+						"value": "/kubeconfig/worker1.kubeconfig"
+					}
+				],
+				"volumeMounts": [
+					{
+						"name": "mk",
+						"mountPath": "/kubeconfig"
+					}
+				], 
+				"args":[
+					"run", 
+					"testpod",
+					"--image='${E2E_TEST_IMAGE}'"
+				]
+			}
+		], 
+		"restartPolicy":"Never"
+	}
+}'
+kubectl run kubectl-to-2 --image registry.k8s.io/kubectl:v1.28.4 --overrides=\
+'
+{
+	"spec": {
+		"volumes": [
+			{
+				"name": "mk",
+				"secret": {
+					"secretName": "multikueue"
+				}
+			}
+		],
+		"containers": [
+			{
+				"name": "kubectl",
+				"image": "registry.k8s.io/kubectl:v1.28.4",
+				"env": [
+					{
+						"name": "KUBECONFIG",
+						"value": "/kubeconfig/worker2.kubeconfig"
+					}
+				],
+				"volumeMounts": [
+					{
+						"name": "mk",
+						"mountPath": "/kubeconfig"
+					}
+				], 
+				"args":[
+					"run", 
+					"testpod",
+					"--image='${E2E_TEST_IMAGE}'"
+				]
+			}
+		], 
+		"restartPolicy":"Never"
+	}
+}'
+kubectl wait pods kubectl-to-1 --for condition=Ready=False --timeout=180s
+kubectl wait pods kubectl-to-2 --for condition=Ready=False --timeout=180s
+
+sleep 10
```

  Please add a comment why the sleep is needed

- **hack/multikueue-e2e-test.sh:229** by [@mimowo](https://github.com/mimowo) — 2023-12-06T11:38:50Z
```diff
@@ -0,0 +1,239 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
+export WORKER1_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker1
+export WORKER2_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker2
+
+# $1 - cluster name
+function cluster_cleanup {
+	kubectl config use-context kind-$1
+        kubectl logs -n kube-system kube-scheduler-$1-control-plane > $ARTIFACTS/$1-kube-scheduler.log || true
+        kubectl logs -n kube-system kube-controller-manager-$1-control-plane > $ARTIFACTS/$1-kube-controller-manager.log || true
+        kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/$1-kueue-controller-manager.log || true
+        kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
+        kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
+        $KIND delete cluster --name $1
+}
+
+function cleanup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_cleanup $LEADER_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER1_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER2_KIND_CLUSTER_NAME
+    fi
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
+}
+
+
+# $1 cluster name
+# $2 cluster kind config
+function cluster_create {
+        $KIND create cluster --name $1 --image $E2E_KIND_VERSION --config $2 --wait 1m -v 5  > $ARTIFACTS/$1-create.log 2>&1 \
+		||  { echo "unable to start the $1 cluster "; cat $ARTIFACTS/$1-create.log ; }
+	kubectl config use-context kind-$1
+        kubectl get nodes > $ARTIFACTS/$1-nodes.log || true
+        kubectl describe pods -n kube-system > $ARTIFACTS/$1-system-pods.log || true
+}
+
+function startup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_create $LEADER_KIND_CLUSTER_NAME $SOURCE_DIR/mk-leader-cluster.yaml
+
+	# NOTE: for local setup, make sure that your firewalk allows tcp from leader to the GW ip
+	# eg. ufw `sudo ufw allow from 172.18.0.0/16 proto tcp to 172.18.0.1`
+	#
+	# eg. iptables    `sudo iptables --append INPUT --protocol tcp --src 172.18.0.0/16 --dst 172.18.0.1 --jump ACCEPT
+	#                  sudo iptables --append OUTPUT --protocol tcp --src 172.18.0.1 --dst 172.18.0./0/16 --jump ACCEPT`
+
+	# have the worker forward the api to the docker gateway address instead of lo
+	export GW=$(docker inspect ${LEADER_KIND_CLUSTER_NAME}-control-plane -f '{{.NetworkSettings.Networks.kind.Gateway}}')
+	$YQ e '.networking.apiServerAddress=env(GW)'  $SOURCE_DIR/mk-worker-cluster.yaml > $ARTIFACTS/worker-cluster.yaml
+
+	cluster_create $WORKER1_KIND_CLUSTER_NAME $ARTIFACTS/worker-cluster.yaml
+	cluster_create $WORKER2_KIND_CLUSTER_NAME $ARTIFACTS/worker-cluster.yaml
+
+	# push the worker kubeconfig in a leader's secret
+	$KIND get kubeconfig --name $WORKER1_KIND_CLUSTER_NAME  > ${ARTIFACTS}/worker1.kubeconfig
+	$KIND get kubeconfig --name $WORKER2_KIND_CLUSTER_NAME  > ${ARTIFACTS}/worker2.kubeconfig
+	kubectl config use-context kind-${LEADER_KIND_CLUSTER_NAME}
+	kubectl create secret generic multikueue --from-file=${ARTIFACTS}/worker1.kubeconfig --from-file=${ARTIFACTS}/worker2.kubeconfig
+    fi
+}
+
+# $1 cluster
+function cluster_kind_load {
+        $KIND load docker-image $E2E_TEST_IMAGE --name $1
+        $KIND load docker-image $IMAGE_TAG --name $1
+}
+
+function kind_load {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        docker pull $E2E_TEST_IMAGE
+        cluster_kind_load $LEADER_KIND_CLUSTER_NAME
+        cluster_kind_load $WORKER1_KIND_CLUSTER_NAME
+        cluster_kind_load $WORKER2_KIND_CLUSTER_NAME 
+    fi
+}
+
+# $1 cluster
+function cluster_kueue_deploy {
+    kubectl config use-context kind-${1}
+    kubectl apply --server-side -k test/e2e/config
+}
+
+function kueue_deploy {
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=$IMAGE_TAG)
+
+    cluster_kueue_deploy $LEADER_KIND_CLUSTER_NAME
+    cluster_kueue_deploy $WORKER1_KIND_CLUSTER_NAME
+    cluster_kueue_deploy $WORKER2_KIND_CLUSTER_NAME
+
+    #wait for the manager to start
+    # failing a this point might indicate a manifestation of
+    # https://kind.sigs.k8s.io/docs/user/known-issues/#pod-errors-due-to-too-many-open-files
+    kubectl config use-context kind-$LEADER_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+    kubectl config use-context kind-$WORKER1_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+    kubectl config use-context kind-$WORKER2_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+}
+
+trap cleanup EXIT
+startup
+kind_load
+kueue_deploy 
+
+#TODO: run some actual tests
+#$GINKGO --junit-report=junit.xml --output-dir=$ARTIFACTS -v ./test/multikueue-e2e/...
+
+
+#TODO: remove the following lines
+
+# even if waiting for deployment, the webhook server might not be up, wait an extra 10 sec
+sleep 10
+
+kubectl config use-context kind-$LEADER_KIND_CLUSTER_NAME
+kubectl run kubectl-to-1 --image registry.k8s.io/kubectl:v1.28.4 --overrides=\
+'
+{
+	"spec": {
+		"volumes": [
+			{
+				"name": "mk",
+				"secret": {
+					"secretName": "multikueue"
+				}
+			}
+		],
+		"containers": [
+			{
+				"name": "kubectl",
+				"image": "registry.k8s.io/kubectl:v1.28.4",
+				"env": [
+					{
+						"name": "KUBECONFIG",
+						"value": "/kubeconfig/worker1.kubeconfig"
+					}
+				],
+				"volumeMounts": [
+					{
+						"name": "mk",
+						"mountPath": "/kubeconfig"
+					}
+				], 
+				"args":[
+					"run", 
+					"testpod",
+					"--image='${E2E_TEST_IMAGE}'"
+				]
+			}
+		], 
+		"restartPolicy":"Never"
+	}
+}'
+kubectl run kubectl-to-2 --image registry.k8s.io/kubectl:v1.28.4 --overrides=\
+'
+{
+	"spec": {
+		"volumes": [
+			{
+				"name": "mk",
+				"secret": {
+					"secretName": "multikueue"
+				}
+			}
+		],
+		"containers": [
+			{
+				"name": "kubectl",
+				"image": "registry.k8s.io/kubectl:v1.28.4",
+				"env": [
+					{
+						"name": "KUBECONFIG",
+						"value": "/kubeconfig/worker2.kubeconfig"
+					}
+				],
+				"volumeMounts": [
+					{
+						"name": "mk",
+						"mountPath": "/kubeconfig"
+					}
+				], 
+				"args":[
+					"run", 
+					"testpod",
+					"--image='${E2E_TEST_IMAGE}'"
+				]
+			}
+		], 
+		"restartPolicy":"Never"
+	}
+}'
+kubectl wait pods kubectl-to-1 --for condition=Ready=False --timeout=180s
```

  Please add a comment why this is needed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-06T12:08:58Z

- **test/integration/multikueue/multikueue_test.go:40** by [@mimowo](https://github.com/mimowo) — 2023-12-06T12:08:57Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		leaderNs *corev1.Namespace
+		workerNs *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		leaderNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		workerNs = leaderNs.DeepCopy()
```

  thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-06T12:09:21Z

- **test/integration/multikueue/multikueue_test.go:2** by [@mimowo](https://github.com/mimowo) — 2023-12-06T12:09:21Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2023 The Kubernetes Authors.
```

  I proposed to because the suites are independent, so one suite could merge earlier, once ready, but if you prefer to keep them together, I'm ok with that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-06T12:15:06Z

- **hack/kubectl.yaml:12** by [@mimowo](https://github.com/mimowo) — 2023-12-06T12:15:06Z
```diff
@@ -0,0 +1,24 @@
+apiVersion: v1
+kind: Pod
+metadata:
+  name: kubectl
+spec:
+  volumes:
+    - name: mk
+      secret:
+        secretName: multikueue
+  containers:
+    - name: kubectl
+      image: bitnami/kubectl:latest
```

  Yeah, that's why here ``However, maybe we could have some basic ginkgo test, which just lists nodes (or any other resource) on the worker cluster? We need to have the ginkgo tests anyway, so moving the setup to this PR seems to make sense.` I was thinking adding some basic ginkgo test even in this PR seems to make sense, so that the main PR is reduced as much as possible. 
  
  The main PR is very big, so even if in this PR you prefer to use kubectl, we could have interim PR which uses ginkgo.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-06T12:39:45Z

- **hack/multikueue-e2e-test.sh:29** by [@mimowo](https://github.com/mimowo) — 2023-12-06T12:39:44Z
```diff
@@ -0,0 +1,144 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
+export WORKER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker
```

  I think it is a fair concern, I just tested locally creating 3 clusters and it worked, but I see this might be an issue. If you had to increase the limits to run the tests, I think we could add a comment referring to the potential need to increase the limits. Not clear how often does this happen to me at this moment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-06T13:33:58Z

- **hack/multikueue-e2e-test.sh:29** by [@mimowo](https://github.com/mimowo) — 2023-12-06T13:33:58Z
```diff
@@ -0,0 +1,144 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
+export WORKER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker
```

  I can confirm that the limit is hit relatively fast, I got it to fail when creating the 5th cluster, and the docker container was returning the error message as you point out. After increasing the limits I could create the 5th cluster. Still, it seems we should be fine with 3.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-06T16:22:05Z

- **Makefile:186** by [@trasc](https://github.com/trasc) — 2023-12-06T16:17:26Z
```diff
@@ -176,6 +178,12 @@ run-test-e2e-%: FORCE
 	@echo Running e2e for k8s ${K8S_VERSION}
 	E2E_KIND_VERSION="kindest/node:v$(K8S_VERSION)" KIND_CLUSTER_NAME=$(KIND_CLUSTER_NAME) CREATE_KIND_CLUSTER=$(CREATE_KIND_CLUSTER) ARTIFACTS="$(ARTIFACTS)/$@" IMAGE_TAG=$(IMAGE_TAG) ./hack/e2e-test.sh
 
+run-test-multikueue-e2e-%: K8S_VERSION = $(@:run-test-multikueue-e2e-%=%)
+run-test-multikueue-e2e-%: FORCE
+	@echo Running multikueue e2e for k8s ${K8S_VERSION}
+	E2E_KIND_VERSION="kindest/node:v$(K8S_VERSION)" KIND_CLUSTER_NAME=$(KIND_CLUSTER_NAME) CREATE_KIND_CLUSTER=$(CREATE_KIND_CLUSTER) ARTIFACTS="$(ARTIFACTS)/$@" IMAGE_TAG=$(IMAGE_TAG) ./hack/multikueue-e2e-test.sh
+
+
```

  done

- **test/integration/multikueue/multikueue_test.go:2** by [@trasc](https://github.com/trasc) — 2023-12-06T16:18:33Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2023 The Kubernetes Authors.
```

  it's simpler in one PR

- **hack/kubectl.yaml:12** by [@trasc](https://github.com/trasc) — 2023-12-06T16:19:21Z
```diff
@@ -0,0 +1,24 @@
+apiVersion: v1
+kind: Pod
+metadata:
+  name: kubectl
+spec:
+  volumes:
+    - name: mk
+      secret:
+        secretName: multikueue
+  containers:
+    - name: kubectl
+      image: bitnami/kubectl:latest
```

  ginkgo based tests added

- **hack/multikueue-e2e-test.sh:29** by [@trasc](https://github.com/trasc) — 2023-12-06T16:20:45Z
```diff
@@ -0,0 +1,144 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
+export WORKER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker
```

  There is a comment in the most likely place to fail (when waiting for kueue to start).

- **hack/mk-leader-cluster.yaml:2** by [@trasc](https://github.com/trasc) — 2023-12-06T16:21:01Z
```diff
@@ -0,0 +1,4 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
```

  done

- **hack/multikueue-e2e-test.sh:28** by [@trasc](https://github.com/trasc) — 2023-12-06T16:21:12Z
```diff
@@ -0,0 +1,239 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
```

  done

- **hack/multikueue-e2e-test.sh:232** by [@trasc](https://github.com/trasc) — 2023-12-06T16:21:46Z
```diff
@@ -0,0 +1,239 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
+export WORKER1_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker1
+export WORKER2_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker2
+
+# $1 - cluster name
+function cluster_cleanup {
+	kubectl config use-context kind-$1
+        kubectl logs -n kube-system kube-scheduler-$1-control-plane > $ARTIFACTS/$1-kube-scheduler.log || true
+        kubectl logs -n kube-system kube-controller-manager-$1-control-plane > $ARTIFACTS/$1-kube-controller-manager.log || true
+        kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/$1-kueue-controller-manager.log || true
+        kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
+        kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
+        $KIND delete cluster --name $1
+}
+
+function cleanup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_cleanup $LEADER_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER1_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER2_KIND_CLUSTER_NAME
+    fi
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
+}
+
+
+# $1 cluster name
+# $2 cluster kind config
+function cluster_create {
+        $KIND create cluster --name $1 --image $E2E_KIND_VERSION --config $2 --wait 1m -v 5  > $ARTIFACTS/$1-create.log 2>&1 \
+		||  { echo "unable to start the $1 cluster "; cat $ARTIFACTS/$1-create.log ; }
+	kubectl config use-context kind-$1
+        kubectl get nodes > $ARTIFACTS/$1-nodes.log || true
+        kubectl describe pods -n kube-system > $ARTIFACTS/$1-system-pods.log || true
+}
+
+function startup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_create $LEADER_KIND_CLUSTER_NAME $SOURCE_DIR/mk-leader-cluster.yaml
+
+	# NOTE: for local setup, make sure that your firewalk allows tcp from leader to the GW ip
+	# eg. ufw `sudo ufw allow from 172.18.0.0/16 proto tcp to 172.18.0.1`
+	#
+	# eg. iptables    `sudo iptables --append INPUT --protocol tcp --src 172.18.0.0/16 --dst 172.18.0.1 --jump ACCEPT
+	#                  sudo iptables --append OUTPUT --protocol tcp --src 172.18.0.1 --dst 172.18.0./0/16 --jump ACCEPT`
+
+	# have the worker forward the api to the docker gateway address instead of lo
+	export GW=$(docker inspect ${LEADER_KIND_CLUSTER_NAME}-control-plane -f '{{.NetworkSettings.Networks.kind.Gateway}}')
+	$YQ e '.networking.apiServerAddress=env(GW)'  $SOURCE_DIR/mk-worker-cluster.yaml > $ARTIFACTS/worker-cluster.yaml
+
+	cluster_create $WORKER1_KIND_CLUSTER_NAME $ARTIFACTS/worker-cluster.yaml
+	cluster_create $WORKER2_KIND_CLUSTER_NAME $ARTIFACTS/worker-cluster.yaml
+
+	# push the worker kubeconfig in a leader's secret
+	$KIND get kubeconfig --name $WORKER1_KIND_CLUSTER_NAME  > ${ARTIFACTS}/worker1.kubeconfig
+	$KIND get kubeconfig --name $WORKER2_KIND_CLUSTER_NAME  > ${ARTIFACTS}/worker2.kubeconfig
+	kubectl config use-context kind-${LEADER_KIND_CLUSTER_NAME}
+	kubectl create secret generic multikueue --from-file=${ARTIFACTS}/worker1.kubeconfig --from-file=${ARTIFACTS}/worker2.kubeconfig
+    fi
+}
+
+# $1 cluster
+function cluster_kind_load {
+        $KIND load docker-image $E2E_TEST_IMAGE --name $1
+        $KIND load docker-image $IMAGE_TAG --name $1
+}
+
+function kind_load {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        docker pull $E2E_TEST_IMAGE
+        cluster_kind_load $LEADER_KIND_CLUSTER_NAME
+        cluster_kind_load $WORKER1_KIND_CLUSTER_NAME
+        cluster_kind_load $WORKER2_KIND_CLUSTER_NAME 
+    fi
+}
+
+# $1 cluster
+function cluster_kueue_deploy {
+    kubectl config use-context kind-${1}
+    kubectl apply --server-side -k test/e2e/config
+}
+
+function kueue_deploy {
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=$IMAGE_TAG)
+
+    cluster_kueue_deploy $LEADER_KIND_CLUSTER_NAME
+    cluster_kueue_deploy $WORKER1_KIND_CLUSTER_NAME
+    cluster_kueue_deploy $WORKER2_KIND_CLUSTER_NAME
+
+    #wait for the manager to start
+    # failing a this point might indicate a manifestation of
+    # https://kind.sigs.k8s.io/docs/user/known-issues/#pod-errors-due-to-too-many-open-files
+    kubectl config use-context kind-$LEADER_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+    kubectl config use-context kind-$WORKER1_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+    kubectl config use-context kind-$WORKER2_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+}
+
+trap cleanup EXIT
+startup
+kind_load
+kueue_deploy 
+
+#TODO: run some actual tests
+#$GINKGO --junit-report=junit.xml --output-dir=$ARTIFACTS -v ./test/multikueue-e2e/...
+
+
+#TODO: remove the following lines
+
+# even if waiting for deployment, the webhook server might not be up, wait an extra 10 sec
+sleep 10
+
+kubectl config use-context kind-$LEADER_KIND_CLUSTER_NAME
+kubectl run kubectl-to-1 --image registry.k8s.io/kubectl:v1.28.4 --overrides=\
+'
+{
+	"spec": {
+		"volumes": [
+			{
+				"name": "mk",
+				"secret": {
+					"secretName": "multikueue"
+				}
+			}
+		],
+		"containers": [
+			{
+				"name": "kubectl",
+				"image": "registry.k8s.io/kubectl:v1.28.4",
+				"env": [
+					{
+						"name": "KUBECONFIG",
+						"value": "/kubeconfig/worker1.kubeconfig"
+					}
+				],
+				"volumeMounts": [
+					{
+						"name": "mk",
+						"mountPath": "/kubeconfig"
+					}
+				], 
+				"args":[
+					"run", 
+					"testpod",
+					"--image='${E2E_TEST_IMAGE}'"
+				]
+			}
+		], 
+		"restartPolicy":"Never"
+	}
+}'
+kubectl run kubectl-to-2 --image registry.k8s.io/kubectl:v1.28.4 --overrides=\
+'
+{
+	"spec": {
+		"volumes": [
+			{
+				"name": "mk",
+				"secret": {
+					"secretName": "multikueue"
+				}
+			}
+		],
+		"containers": [
+			{
+				"name": "kubectl",
+				"image": "registry.k8s.io/kubectl:v1.28.4",
+				"env": [
+					{
+						"name": "KUBECONFIG",
+						"value": "/kubeconfig/worker2.kubeconfig"
+					}
+				],
+				"volumeMounts": [
+					{
+						"name": "mk",
+						"mountPath": "/kubeconfig"
+					}
+				], 
+				"args":[
+					"run", 
+					"testpod",
+					"--image='${E2E_TEST_IMAGE}'"
+				]
+			}
+		], 
+		"restartPolicy":"Never"
+	}
+}'
+kubectl wait pods kubectl-to-1 --for condition=Ready=False --timeout=180s
+kubectl wait pods kubectl-to-2 --for condition=Ready=False --timeout=180s
+
+sleep 10
```

  removed in favor of ginkgo based tests

- **hack/multikueue-e2e-test.sh:229** by [@trasc](https://github.com/trasc) — 2023-12-06T16:21:53Z
```diff
@@ -0,0 +1,239 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export LEADER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-leader
+export WORKER1_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker1
+export WORKER2_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker2
+
+# $1 - cluster name
+function cluster_cleanup {
+	kubectl config use-context kind-$1
+        kubectl logs -n kube-system kube-scheduler-$1-control-plane > $ARTIFACTS/$1-kube-scheduler.log || true
+        kubectl logs -n kube-system kube-controller-manager-$1-control-plane > $ARTIFACTS/$1-kube-controller-manager.log || true
+        kubectl logs -n kueue-system deployment/kueue-controller-manager > $ARTIFACTS/$1-kueue-controller-manager.log || true
+        kubectl describe pods -n kueue-system > $ARTIFACTS/$1-kueue-system-pods.log || true
+        kubectl describe pods > $ARTIFACTS/$1-default-pods.log || true
+        $KIND delete cluster --name $1
+}
+
+function cleanup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_cleanup $LEADER_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER1_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER2_KIND_CLUSTER_NAME
+    fi
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
+}
+
+
+# $1 cluster name
+# $2 cluster kind config
+function cluster_create {
+        $KIND create cluster --name $1 --image $E2E_KIND_VERSION --config $2 --wait 1m -v 5  > $ARTIFACTS/$1-create.log 2>&1 \
+		||  { echo "unable to start the $1 cluster "; cat $ARTIFACTS/$1-create.log ; }
+	kubectl config use-context kind-$1
+        kubectl get nodes > $ARTIFACTS/$1-nodes.log || true
+        kubectl describe pods -n kube-system > $ARTIFACTS/$1-system-pods.log || true
+}
+
+function startup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_create $LEADER_KIND_CLUSTER_NAME $SOURCE_DIR/mk-leader-cluster.yaml
+
+	# NOTE: for local setup, make sure that your firewalk allows tcp from leader to the GW ip
+	# eg. ufw `sudo ufw allow from 172.18.0.0/16 proto tcp to 172.18.0.1`
+	#
+	# eg. iptables    `sudo iptables --append INPUT --protocol tcp --src 172.18.0.0/16 --dst 172.18.0.1 --jump ACCEPT
+	#                  sudo iptables --append OUTPUT --protocol tcp --src 172.18.0.1 --dst 172.18.0./0/16 --jump ACCEPT`
+
+	# have the worker forward the api to the docker gateway address instead of lo
+	export GW=$(docker inspect ${LEADER_KIND_CLUSTER_NAME}-control-plane -f '{{.NetworkSettings.Networks.kind.Gateway}}')
+	$YQ e '.networking.apiServerAddress=env(GW)'  $SOURCE_DIR/mk-worker-cluster.yaml > $ARTIFACTS/worker-cluster.yaml
+
+	cluster_create $WORKER1_KIND_CLUSTER_NAME $ARTIFACTS/worker-cluster.yaml
+	cluster_create $WORKER2_KIND_CLUSTER_NAME $ARTIFACTS/worker-cluster.yaml
+
+	# push the worker kubeconfig in a leader's secret
+	$KIND get kubeconfig --name $WORKER1_KIND_CLUSTER_NAME  > ${ARTIFACTS}/worker1.kubeconfig
+	$KIND get kubeconfig --name $WORKER2_KIND_CLUSTER_NAME  > ${ARTIFACTS}/worker2.kubeconfig
+	kubectl config use-context kind-${LEADER_KIND_CLUSTER_NAME}
+	kubectl create secret generic multikueue --from-file=${ARTIFACTS}/worker1.kubeconfig --from-file=${ARTIFACTS}/worker2.kubeconfig
+    fi
+}
+
+# $1 cluster
+function cluster_kind_load {
+        $KIND load docker-image $E2E_TEST_IMAGE --name $1
+        $KIND load docker-image $IMAGE_TAG --name $1
+}
+
+function kind_load {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        docker pull $E2E_TEST_IMAGE
+        cluster_kind_load $LEADER_KIND_CLUSTER_NAME
+        cluster_kind_load $WORKER1_KIND_CLUSTER_NAME
+        cluster_kind_load $WORKER2_KIND_CLUSTER_NAME 
+    fi
+}
+
+# $1 cluster
+function cluster_kueue_deploy {
+    kubectl config use-context kind-${1}
+    kubectl apply --server-side -k test/e2e/config
+}
+
+function kueue_deploy {
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=$IMAGE_TAG)
+
+    cluster_kueue_deploy $LEADER_KIND_CLUSTER_NAME
+    cluster_kueue_deploy $WORKER1_KIND_CLUSTER_NAME
+    cluster_kueue_deploy $WORKER2_KIND_CLUSTER_NAME
+
+    #wait for the manager to start
+    # failing a this point might indicate a manifestation of
+    # https://kind.sigs.k8s.io/docs/user/known-issues/#pod-errors-due-to-too-many-open-files
+    kubectl config use-context kind-$LEADER_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+    kubectl config use-context kind-$WORKER1_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+    kubectl config use-context kind-$WORKER2_KIND_CLUSTER_NAME
+    kubectl wait -n kueue-system deployment kueue-controller-manager --for condition=Available --timeout=120s
+}
+
+trap cleanup EXIT
+startup
+kind_load
+kueue_deploy 
+
+#TODO: run some actual tests
+#$GINKGO --junit-report=junit.xml --output-dir=$ARTIFACTS -v ./test/multikueue-e2e/...
+
+
+#TODO: remove the following lines
+
+# even if waiting for deployment, the webhook server might not be up, wait an extra 10 sec
+sleep 10
+
+kubectl config use-context kind-$LEADER_KIND_CLUSTER_NAME
+kubectl run kubectl-to-1 --image registry.k8s.io/kubectl:v1.28.4 --overrides=\
+'
+{
+	"spec": {
+		"volumes": [
+			{
+				"name": "mk",
+				"secret": {
+					"secretName": "multikueue"
+				}
+			}
+		],
+		"containers": [
+			{
+				"name": "kubectl",
+				"image": "registry.k8s.io/kubectl:v1.28.4",
+				"env": [
+					{
+						"name": "KUBECONFIG",
+						"value": "/kubeconfig/worker1.kubeconfig"
+					}
+				],
+				"volumeMounts": [
+					{
+						"name": "mk",
+						"mountPath": "/kubeconfig"
+					}
+				], 
+				"args":[
+					"run", 
+					"testpod",
+					"--image='${E2E_TEST_IMAGE}'"
+				]
+			}
+		], 
+		"restartPolicy":"Never"
+	}
+}'
+kubectl run kubectl-to-2 --image registry.k8s.io/kubectl:v1.28.4 --overrides=\
+'
+{
+	"spec": {
+		"volumes": [
+			{
+				"name": "mk",
+				"secret": {
+					"secretName": "multikueue"
+				}
+			}
+		],
+		"containers": [
+			{
+				"name": "kubectl",
+				"image": "registry.k8s.io/kubectl:v1.28.4",
+				"env": [
+					{
+						"name": "KUBECONFIG",
+						"value": "/kubeconfig/worker2.kubeconfig"
+					}
+				],
+				"volumeMounts": [
+					{
+						"name": "mk",
+						"mountPath": "/kubeconfig"
+					}
+				], 
+				"args":[
+					"run", 
+					"testpod",
+					"--image='${E2E_TEST_IMAGE}'"
+				]
+			}
+		], 
+		"restartPolicy":"Never"
+	}
+}'
+kubectl wait pods kubectl-to-1 --for condition=Ready=False --timeout=180s
```

  removed in favor of ginkgo based tests

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-08T17:28:59Z

- **hack/mk-manager-cluster.yaml:4** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-08T17:12:04Z
```diff
@@ -0,0 +1,4 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
+networking:
+  apiServerAddress: "172.17.0.1"
```

  where is this coming from? Why is it the same IP as the worker?

- **hack/mk-manager-cluster.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-08T17:13:11Z
  maybe let's create a folder "multikueue" and name this `manager-cluster.kind.yaml`

- **test/integration/multikueue/multikueue_test.go:38** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-08T17:20:19Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
```

  what are these namespaces for?

- **test/integration/multikueue/multikueue_test.go:91** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-08T17:20:58Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+
+		managerMultikueueSecret *corev1.Secret
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(worker1Cluster.client.Create(worker1Cluster.ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(worker2Cluster.client.Create(worker1Cluster.ctx, worker2Ns)).To(gomega.Succeed())
+
+		w1Kubeconfig, err := worker1Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		w2Kubeconfig, err := worker2Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		managerMultikueueSecret = &corev1.Secret{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "multikueue",
+				Namespace: managerNs.Name,
+			},
+			Data: map[string][]byte{
+				"worker1.kubeconfig": w1Kubeconfig,
+				"worker2.kubeconfig": w2Kubeconfig,
+			},
+		}
+
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerMultikueueSecret)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(managerCluster.ctx, managerCluster.client, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(worker1Cluster.ctx, worker1Cluster.client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(worker2Cluster.ctx, worker2Cluster.client, worker2Ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *cluster, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect(c.client.Create(c.ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &managerCluster, &managerNs),
+			ginkgo.Entry("worker1", &worker1Cluster, &worker1Ns),
+			ginkgo.Entry("worker2", &worker2Cluster, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *cluster, key string, ns **corev1.Namespace) {
```

  What is this testing?

- **hack/multikueue-e2e-test.sh:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-08T17:22:29Z
  There is a lot of repeated code. Could you put the functions in a file that can be shared with the single e2e tests?

- **test/mke2e/suite_test.go:66** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-08T17:24:46Z
```diff
@@ -0,0 +1,78 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"context"
+	"fmt"
+	"os"
+	"testing"
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var (
+	k8sManagerClient client.Client
+	k8sWorker1Client client.Client
+	k8sWorker2Client client.Client
+	ctx              context.Context
+)
+
+const (
+	Timeout  = time.Minute
+	Interval = time.Millisecond * 250
+)
+
+func TestAPIs(t *testing.T) {
+	suiteName := "End To End MultiKueue Suite"
+	if ver, found := os.LookupEnv("E2E_KIND_VERSION"); found {
+		suiteName = fmt.Sprintf("%s: %s", suiteName, ver)
+	}
+	gomega.RegisterFailHandler(ginkgo.Fail)
+	ginkgo.RunSpecs(t,
+		suiteName,
+	)
+}
+
+var _ = ginkgo.BeforeSuite(func() {
+	managerClusterName := os.Getenv("MANAGER_KIND_CLUSTER_NAME")
+	gomega.Expect(managerClusterName).NotTo(gomega.BeEmpty(), "MANAGER_KIND_CLUSTER_NAME should not be empty")
+
+	worker1ClusterName := os.Getenv("WORKER1_KIND_CLUSTER_NAME")
+	gomega.Expect(worker1ClusterName).NotTo(gomega.BeEmpty(), "WORKER1_KIND_CLUSTER_NAME should not be empty")
+
+	worker2ClusterName := os.Getenv("WORKER2_KIND_CLUSTER_NAME")
+	gomega.Expect(worker1ClusterName).NotTo(gomega.BeEmpty(), "WORKER2_KIND_CLUSTER_NAME should not be empty")
+
+	k8sManagerClient, _ = util.CreateClientUsingCluster("kind-" + managerClusterName)
```

  Don't silence errors (I assume that's what `_` is catching).

- **test/mke2e/e2e_test.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-08T17:28:57Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *client.Client, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect((*c).Create(ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &k8sManagerClient, &managerNs),
+			ginkgo.Entry("worker1", &k8sWorker1Client, &worker1Ns),
+			ginkgo.Entry("worker2", &k8sWorker2Client, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  Same question, what is this testing?
  
  It looks like it's creating a client based on the secret. But this is not going through the manager cluster in any way. So we are just testing that we can create clients?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-11T08:22:49Z

- **hack/mk-manager-cluster.yaml:4** by [@trasc](https://github.com/trasc) — 2023-12-11T07:37:38Z
```diff
@@ -0,0 +1,4 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
+networking:
+  apiServerAddress: "172.17.0.1"
```

  It's the IP used  by the host in the bridge that connects the kind clusters, it is updated at runtime :
  https://github.com/kubernetes-sigs/kueue/blob/0a0c49ce4adc50c5742a9322b6b8ad494d74a119/hack/multikueue-e2e-test.sh#L83-L85
  
  But it's not needed for the manager.

- **test/mke2e/suite_test.go:66** by [@trasc](https://github.com/trasc) — 2023-12-11T07:43:44Z
```diff
@@ -0,0 +1,78 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"context"
+	"fmt"
+	"os"
+	"testing"
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var (
+	k8sManagerClient client.Client
+	k8sWorker1Client client.Client
+	k8sWorker2Client client.Client
+	ctx              context.Context
+)
+
+const (
+	Timeout  = time.Minute
+	Interval = time.Millisecond * 250
+)
+
+func TestAPIs(t *testing.T) {
+	suiteName := "End To End MultiKueue Suite"
+	if ver, found := os.LookupEnv("E2E_KIND_VERSION"); found {
+		suiteName = fmt.Sprintf("%s: %s", suiteName, ver)
+	}
+	gomega.RegisterFailHandler(ginkgo.Fail)
+	ginkgo.RunSpecs(t,
+		suiteName,
+	)
+}
+
+var _ = ginkgo.BeforeSuite(func() {
+	managerClusterName := os.Getenv("MANAGER_KIND_CLUSTER_NAME")
+	gomega.Expect(managerClusterName).NotTo(gomega.BeEmpty(), "MANAGER_KIND_CLUSTER_NAME should not be empty")
+
+	worker1ClusterName := os.Getenv("WORKER1_KIND_CLUSTER_NAME")
+	gomega.Expect(worker1ClusterName).NotTo(gomega.BeEmpty(), "WORKER1_KIND_CLUSTER_NAME should not be empty")
+
+	worker2ClusterName := os.Getenv("WORKER2_KIND_CLUSTER_NAME")
+	gomega.Expect(worker1ClusterName).NotTo(gomega.BeEmpty(), "WORKER2_KIND_CLUSTER_NAME should not be empty")
+
+	k8sManagerClient, _ = util.CreateClientUsingCluster("kind-" + managerClusterName)
```

  that was not an error, it used to be a visibility client, however removed after the last rebase.

- **test/mke2e/e2e_test.go:70** by [@trasc](https://github.com/trasc) — 2023-12-11T07:44:55Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *client.Client, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect((*c).Create(ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &k8sManagerClient, &managerNs),
+			ginkgo.Entry("worker1", &k8sWorker1Client, &worker1Ns),
+			ginkgo.Entry("worker2", &k8sWorker2Client, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  These are just setup sanity checks, most likely we can remove them later on.
  Same as  the integration one.

- **test/integration/multikueue/multikueue_test.go:91** by [@trasc](https://github.com/trasc) — 2023-12-11T07:46:10Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+
+		managerMultikueueSecret *corev1.Secret
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(worker1Cluster.client.Create(worker1Cluster.ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(worker2Cluster.client.Create(worker1Cluster.ctx, worker2Ns)).To(gomega.Succeed())
+
+		w1Kubeconfig, err := worker1Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		w2Kubeconfig, err := worker2Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		managerMultikueueSecret = &corev1.Secret{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "multikueue",
+				Namespace: managerNs.Name,
+			},
+			Data: map[string][]byte{
+				"worker1.kubeconfig": w1Kubeconfig,
+				"worker2.kubeconfig": w2Kubeconfig,
+			},
+		}
+
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerMultikueueSecret)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(managerCluster.ctx, managerCluster.client, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(worker1Cluster.ctx, worker1Cluster.client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(worker2Cluster.ctx, worker2Cluster.client, worker2Ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *cluster, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect(c.client.Create(c.ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &managerCluster, &managerNs),
+			ginkgo.Entry("worker1", &worker1Cluster, &worker1Ns),
+			ginkgo.Entry("worker2", &worker2Cluster, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *cluster, key string, ns **corev1.Namespace) {
```

  These are just setup sanity checks, most likely we can remove them later on.

- **test/integration/multikueue/multikueue_test.go:38** by [@trasc](https://github.com/trasc) — 2023-12-11T07:46:34Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
```

  These are just cluster sanity checks, most likely we can remove them later on.
  Some test objects are created in them.

- **hack/mk-manager-cluster.yaml:1** by [@trasc](https://github.com/trasc) — 2023-12-11T08:17:12Z
  done

- **hack/multikueue-e2e-test.sh:1** by [@trasc](https://github.com/trasc) — 2023-12-11T08:17:27Z
  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-11T15:23:43Z

- **test/mke2e/e2e_test.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-11T15:23:43Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *client.Client, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect((*c).Create(ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &k8sManagerClient, &managerNs),
+			ginkgo.Entry("worker1", &k8sWorker1Client, &worker1Ns),
+			ginkgo.Entry("worker2", &k8sWorker2Client, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  Let's just remove it.
  
  If we merge and then we forget about them, we might later be wondering whether we needed this test or not.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-11T15:50:22Z

- **test/mke2e/e2e_test.go:70** by [@trasc](https://github.com/trasc) — 2023-12-11T15:50:21Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *client.Client, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect((*c).Create(ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &k8sManagerClient, &managerNs),
+			ginkgo.Entry("worker1", &k8sWorker1Client, &worker1Ns),
+			ginkgo.Entry("worker2", &k8sWorker2Client, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  I'll remove them in #1313, even without them we'l, need some other place holders.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-11T16:36:01Z

- **test/mke2e/e2e_test.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-11T16:36:00Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *client.Client, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect((*c).Create(ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &k8sManagerClient, &managerNs),
+			ginkgo.Entry("worker1", &k8sWorker1Client, &worker1Ns),
+			ginkgo.Entry("worker2", &k8sWorker2Client, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  Isn't "Create workload in cluster" enough as a placeholder?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-11T17:17:23Z

- **test/mke2e/e2e_test.go:70** by [@trasc](https://github.com/trasc) — 2023-12-11T17:17:23Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *client.Client, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect((*c).Create(ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &k8sManagerClient, &managerNs),
+			ginkgo.Entry("worker1", &k8sWorker1Client, &worker1Ns),
+			ginkgo.Entry("worker2", &k8sWorker2Client, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  the second table has a closer behavior to what we'll need in the future as is also uses the created secret.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-11T17:31:17Z

- **hack/mk-manager-cluster.yaml:4** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-11T17:31:17Z
```diff
@@ -0,0 +1,4 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
+networking:
+  apiServerAddress: "172.17.0.1"
```

  If its updated at runtime, maybe we can put `"FILLED_AT_RUNTIME"` instead of a specific IP?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-11T17:33:22Z

- **test/mke2e/e2e_test.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-11T17:33:21Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *client.Client, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect((*c).Create(ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &k8sManagerClient, &managerNs),
+			ginkgo.Entry("worker1", &k8sWorker1Client, &worker1Ns),
+			ginkgo.Entry("worker2", &k8sWorker2Client, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  I still don't see the point, but we can proceed, given that you have a WIP for the next PR.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-11T18:38:00Z

- **hack/multikueue-e2e-test.sh:58** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T17:49:19Z
```diff
@@ -0,0 +1,102 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export MANAGER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-manager
+export WORKER1_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker1
+export WORKER2_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker2
+
+source ${SOURCE_DIR}/e2e-common.sh
+
+function cleanup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_cleanup $MANAGER_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER1_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER2_KIND_CLUSTER_NAME
+    fi
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
+}
+
+
+function startup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_create $MANAGER_KIND_CLUSTER_NAME $SOURCE_DIR/multikueue/manager-cluster.kind.yaml
+
+	# NOTE: for local setup, make sure that your firewalk allows tcp from manager to the GW ip
```

  ```suggestion
  	# NOTE: for local setup, make sure that your firewall allows tcp from manager to the GW ip
  ```

- **hack/multikueue/worker-cluster.kind.yaml:4** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T18:01:25Z
```diff
@@ -0,0 +1,4 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
+networking:
+  apiServerAddress: "172.17.0.1"
```

  Is this IP range used by docker as a default? Or, we use this IP range differentely from the default range?

- **test/integration/multikueue/multikueue_test.go:72** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T18:08:36Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+
+		managerMultikueueSecret *corev1.Secret
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(worker1Cluster.client.Create(worker1Cluster.ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(worker2Cluster.client.Create(worker1Cluster.ctx, worker2Ns)).To(gomega.Succeed())
+
+		w1Kubeconfig, err := worker1Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		w2Kubeconfig, err := worker2Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		managerMultikueueSecret = &corev1.Secret{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "multikueue",
```

  Is this Secret static name? Or we just have temporary name until the multikueue is implemented?

- **test/integration/multikueue/multikueue_test.go:77** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T18:10:32Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+
+		managerMultikueueSecret *corev1.Secret
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(worker1Cluster.client.Create(worker1Cluster.ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(worker2Cluster.client.Create(worker1Cluster.ctx, worker2Ns)).To(gomega.Succeed())
+
+		w1Kubeconfig, err := worker1Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		w2Kubeconfig, err := worker2Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		managerMultikueueSecret = &corev1.Secret{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "multikueue",
+				Namespace: managerNs.Name,
+			},
+			Data: map[string][]byte{
+				"worker1.kubeconfig": w1Kubeconfig,
+				"worker2.kubeconfig": w2Kubeconfig,
```

  I'm wondering if we should use the JSON/YAML form for extensibility here.
  cc: @alculquicondor

- **test/integration/multikueue/multikueue_test.go:81** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T18:25:17Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+
+		managerMultikueueSecret *corev1.Secret
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(worker1Cluster.client.Create(worker1Cluster.ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(worker2Cluster.client.Create(worker1Cluster.ctx, worker2Ns)).To(gomega.Succeed())
+
+		w1Kubeconfig, err := worker1Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		w2Kubeconfig, err := worker2Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		managerMultikueueSecret = &corev1.Secret{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "multikueue",
+				Namespace: managerNs.Name,
+			},
+			Data: map[string][]byte{
+				"worker1.kubeconfig": w1Kubeconfig,
+				"worker2.kubeconfig": w2Kubeconfig,
+			},
+		}
+
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerMultikueueSecret)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(managerCluster.ctx, managerCluster.client, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(worker1Cluster.ctx, worker1Cluster.client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(worker2Cluster.ctx, worker2Cluster.client, worker2Ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *cluster, ns **corev1.Namespace) {
```

  ```golang
  		ginkgo.DescribeTable("Create workload in cluster", func(c *cluster, ns **corev1.Namespace) {
  ```
  
  Why do we need to pass `ns` as typed `**corev1.Namespace`?
  IIUC, the following codes wouldn't modify the namespace.

- **test/mke2e/e2e_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T18:35:02Z
  Let's rename `mke2e` to `multikueuee2e`, the same as other places.
  Or, we may be able to re-organize e2e directory the similar to the integration tests like `e2e/multikueue`.

- **test/mke2e/e2e_test.go:2** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T18:35:10Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  ```suggestion
  Copyright 2023 The Kubernetes Authors.
  ```

- **test/mke2e/suite_test.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T18:36:58Z
```diff
@@ -0,0 +1,78 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"context"
+	"fmt"
+	"os"
+	"testing"
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var (
+	k8sManagerClient client.Client
+	k8sWorker1Client client.Client
+	k8sWorker2Client client.Client
+	ctx              context.Context
+)
+
+const (
+	Timeout  = time.Minute
+	Interval = time.Millisecond * 250
+)
```

  Can we commonize these variables in 
  
  https://github.com/kubernetes-sigs/kueue/blob/74377c7eb3e37db4703538abbfb9432718be4097/test/util/constants.go
  ?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T07:31:46Z

- **hack/multikueue-e2e-test.sh:58** by [@trasc](https://github.com/trasc) — 2023-12-12T06:34:19Z
```diff
@@ -0,0 +1,102 @@
+#!/usr/bin/env bash
+
+# Copyright 2023 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
+ROOT_DIR=$SOURCE_DIR/..
+export KUSTOMIZE=$ROOT_DIR/bin/kustomize
+export GINKGO=$ROOT_DIR/bin/ginkgo
+export KIND=$ROOT_DIR/bin/kind
+export YQ=$ROOT_DIR/bin/yq
+export E2E_TEST_IMAGE=gcr.io/k8s-staging-perf-tests/sleep:v0.0.3
+export MANAGER_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-manager
+export WORKER1_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker1
+export WORKER2_KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME}-worker2
+
+source ${SOURCE_DIR}/e2e-common.sh
+
+function cleanup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_cleanup $MANAGER_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER1_KIND_CLUSTER_NAME
+	cluster_cleanup $WORKER2_KIND_CLUSTER_NAME
+    fi
+    (cd config/components/manager && $KUSTOMIZE edit set image controller=gcr.io/k8s-staging-kueue/kueue:main)
+}
+
+
+function startup {
+    if [ $CREATE_KIND_CLUSTER == 'true' ]
+    then
+        if [ ! -d "$ARTIFACTS" ]; then
+            mkdir -p "$ARTIFACTS"
+        fi
+
+	cluster_create $MANAGER_KIND_CLUSTER_NAME $SOURCE_DIR/multikueue/manager-cluster.kind.yaml
+
+	# NOTE: for local setup, make sure that your firewalk allows tcp from manager to the GW ip
```

  done

- **hack/mk-manager-cluster.yaml:4** by [@trasc](https://github.com/trasc) — 2023-12-12T06:35:49Z
```diff
@@ -0,0 +1,4 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
+networking:
+  apiServerAddress: "172.17.0.1"
```

  done

- **test/mke2e/e2e_test.go:70** by [@trasc](https://github.com/trasc) — 2023-12-12T06:48:34Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *client.Client, ns **corev1.Namespace) {
+			wl := utiltesting.MakeWorkload("wl", (*ns).Name).Obj()
+			gomega.Expect((*c).Create(ctx, wl)).To(gomega.Succeed())
+		},
+			ginkgo.Entry("manager", &k8sManagerClient, &managerNs),
+			ginkgo.Entry("worker1", &k8sWorker1Client, &worker1Ns),
+			ginkgo.Entry("worker2", &k8sWorker2Client, &worker2Ns),
+		)
+
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  Removing them in this pr will partially undo what was done to cover https://github.com/kubernetes-sigs/kueue/pull/1360#discussion_r1409332488

- **hack/multikueue/worker-cluster.kind.yaml:4** by [@trasc](https://github.com/trasc) — 2023-12-12T06:49:36Z
```diff
@@ -0,0 +1,4 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
+networking:
+  apiServerAddress: "172.17.0.1"
```

  This is the default but it is updated at runtime, changed to a placeholder.

- **test/integration/multikueue/multikueue_test.go:72** by [@trasc](https://github.com/trasc) — 2023-12-12T06:51:28Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+
+		managerMultikueueSecret *corev1.Secret
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(worker1Cluster.client.Create(worker1Cluster.ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(worker2Cluster.client.Create(worker1Cluster.ctx, worker2Ns)).To(gomega.Succeed())
+
+		w1Kubeconfig, err := worker1Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		w2Kubeconfig, err := worker2Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		managerMultikueueSecret = &corev1.Secret{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "multikueue",
```

  Static just from a tests perspective, multikueue will use a config that will reference the secret to use.

- **test/integration/multikueue/multikueue_test.go:77** by [@trasc](https://github.com/trasc) — 2023-12-12T06:52:19Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+
+		managerMultikueueSecret *corev1.Secret
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(worker1Cluster.client.Create(worker1Cluster.ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(worker2Cluster.client.Create(worker1Cluster.ctx, worker2Ns)).To(gomega.Succeed())
+
+		w1Kubeconfig, err := worker1Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		w2Kubeconfig, err := worker2Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		managerMultikueueSecret = &corev1.Secret{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "multikueue",
+				Namespace: managerNs.Name,
+			},
+			Data: map[string][]byte{
+				"worker1.kubeconfig": w1Kubeconfig,
+				"worker2.kubeconfig": w2Kubeconfig,
```

  " JSON/YAML"... can you elaborate?

- **test/integration/multikueue/multikueue_test.go:81** by [@trasc](https://github.com/trasc) — 2023-12-12T06:53:53Z
```diff
@@ -0,0 +1,114 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Multikueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+
+		managerMultikueueSecret *corev1.Secret
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		worker1Ns = managerNs.DeepCopy()
+		worker2Ns = managerNs.DeepCopy()
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerNs)).To(gomega.Succeed())
+		gomega.Expect(worker1Cluster.client.Create(worker1Cluster.ctx, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(worker2Cluster.client.Create(worker1Cluster.ctx, worker2Ns)).To(gomega.Succeed())
+
+		w1Kubeconfig, err := worker1Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		w2Kubeconfig, err := worker2Cluster.kubeConfigBytes()
+		gomega.Expect(err).NotTo(gomega.HaveOccurred())
+
+		managerMultikueueSecret = &corev1.Secret{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      "multikueue",
+				Namespace: managerNs.Name,
+			},
+			Data: map[string][]byte{
+				"worker1.kubeconfig": w1Kubeconfig,
+				"worker2.kubeconfig": w2Kubeconfig,
+			},
+		}
+
+		gomega.Expect(managerCluster.client.Create(managerCluster.ctx, managerMultikueueSecret)).To(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(managerCluster.ctx, managerCluster.client, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(worker1Cluster.ctx, worker1Cluster.client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(worker2Cluster.ctx, worker2Cluster.client, worker2Ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Create workload in cluster", func(c *cluster, ns **corev1.Namespace) {
```

  `Entry` are evaluated before the test suite starts running, therefore before the `Before..` funcs are called.

- **test/mke2e/e2e_test.go:1** by [@trasc](https://github.com/trasc) — 2023-12-12T07:13:39Z
  done

- **test/mke2e/e2e_test.go:2** by [@trasc](https://github.com/trasc) — 2023-12-12T07:17:06Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  done

- **test/mke2e/suite_test.go:43** by [@trasc](https://github.com/trasc) — 2023-12-12T07:17:15Z
```diff
@@ -0,0 +1,78 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"context"
+	"fmt"
+	"os"
+	"testing"
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var (
+	k8sManagerClient client.Client
+	k8sWorker1Client client.Client
+	k8sWorker2Client client.Client
+	ctx              context.Context
+)
+
+const (
+	Timeout  = time.Minute
+	Interval = time.Millisecond * 250
+)
```

  removed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T11:25:54Z

- **test/e2e/multikueue/e2e_test.go:44** by [@mimowo](https://github.com/mimowo) — 2023-12-12T11:25:50Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
```

  This can result in different namespaces assigned to manager and workers. Can we instead use for workers the same namespace as for manager? So that the integration tests are closer to the real use?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T11:46:23Z

- **test/e2e/multikueue/e2e_test.go:44** by [@trasc](https://github.com/trasc) — 2023-12-12T11:46:23Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
```

  This is changed in 1313, the magers namespace is created with `GenarateName` , and the worker namespaces are created with `Name` set to the name got by the manager ns.
  
  The current form is sufficient for the placeholder tests we have now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T12:05:46Z

- **test/e2e/multikueue/e2e_test.go:44** by [@mimowo](https://github.com/mimowo) — 2023-12-12T12:05:46Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
```

  I see, the approach with `GenerateName`, then `Name` is what I was thinking about too. 
  
  Since 1313 PR is very large it makes sense to prepare the changes already in this PR. IIUC the purpose of this PR is making the 1313 as small as possible, to make review easier.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T13:35:35Z

- **test/e2e/multikueue/e2e_test.go:44** by [@trasc](https://github.com/trasc) — 2023-12-12T13:35:35Z
```diff
@@ -0,0 +1,94 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T14:02:47Z

- **test/e2e/multikueue/e2e_test.go:71** by [@mimowo](https://github.com/mimowo) — 2023-12-12T14:02:23Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  Can we have `*corev1.Namespace` instead of `**corev1.Namespace`? And analogously in integration tests.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T14:21:54Z

- **test/e2e/multikueue/e2e_test.go:71** by [@trasc](https://github.com/trasc) — 2023-12-12T14:21:54Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  no, https://github.com/kubernetes-sigs/kueue/pull/1360#discussion_r1423509135

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T14:31:04Z

- **test/e2e/multikueue/e2e_test.go:71** by [@mimowo](https://github.com/mimowo) — 2023-12-12T14:31:04Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  I think it makes it hard to read and reason about the test. Maybe we could wrap the body of the test in a regular function, and just call twice for both workers?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T14:35:26Z

- **test/e2e/multikueue/e2e_test.go:71** by [@mimowo](https://github.com/mimowo) — 2023-12-12T14:35:26Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  In particular, I think we we use two `Entry`, then we effectively have two tests, so we run the `BeforeEach` twice. It seems, the intention is to run the `BeforeEach` once, then check kubeconfig propagation for both workers. Unless I'm missing something, I see no point running the `BeforeEach` twice.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-12T14:49:10Z

- **test/e2e/multikueue/e2e_test.go:71** by [@trasc](https://github.com/trasc) — 2023-12-12T14:49:10Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  I can't see how is this hard to read.
  
  We will remove these placeholder test when we add some real ones in 1313.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T14:59:12Z

- **test/e2e/multikueue/e2e_test.go:71** by [@mimowo](https://github.com/mimowo) — 2023-12-12T14:59:12Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  This ** just looks scary, but maybe it's just me :). 
  
  But also that the `BeforeEach` sets up both workers, but in each test we only test one worker, this is counter-intuitive, and wasteful. I would prefer to have a function called twice from within a single `It`, once per worker. This would also eliminate the need for `**` :) 
  
  Anyway, it's not blocking to me, if @alculquicondor and @tenzen-y are happy.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-12T17:23:24Z

- **hack/multikueue/manager-cluster.kind.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T17:17:32Z
  do we even need this file?

- **test/e2e/multikueue/e2e_test.go:71** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T17:23:21Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  Do we even care about different Namespaces for the following PR?
  
  We will probably need the same namespace across all clusters. So I would just remove the namespace altogether here and use `default`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T06:09:55Z

- **test/e2e/multikueue/e2e_test.go:71** by [@trasc](https://github.com/trasc) — 2023-12-13T06:09:55Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  > Do we even care about different Namespaces for the following PR?
  
  Yes, I'd prefer to have every test run in it's own set of namespaces.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T06:13:33Z

- **hack/multikueue/manager-cluster.kind.yaml:1** by [@trasc](https://github.com/trasc) — 2023-12-13T06:13:33Z
  yes, at least for symmetry when calling `cluster_create`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T09:31:44Z

- **test/e2e/multikueue/e2e_test.go:71** by [@mimowo](https://github.com/mimowo) — 2023-12-13T09:31:44Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  I guess a dedicated namespace isn't  required, I'm fine either way. I can see we create generated namespaces for regular e2e tests. I guess one benefit could be to reuse your custom cluster for e2e test so that the tests don't conflict with other stuff.
  
  As for the use of `Entry` I still don't see a need for them, and repeating the setup to test per-worker. I would prefer to have the setup done once and check for both workers. E2e tests are generally costly so it seems to make sense to care about running time, espectially if it does not impact (or even improves) readability.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T13:29:31Z

- **test/e2e/multikueue/e2e_test.go:71** by [@trasc](https://github.com/trasc) — 2023-12-13T13:29:30Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  This is not the only ginkgo table test in the project.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T13:47:13Z

- **test/e2e/multikueue/e2e_test.go:71** by [@mimowo](https://github.com/mimowo) — 2023-12-13T13:47:13Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  Yeah, I'm not saying "never use table tests", it just feels unnecessary in this context, because it results in repeating the e2e setup of all workers, while testing per worker.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T13:55:12Z

- **test/e2e/multikueue/e2e_test.go:71** by [@trasc](https://github.com/trasc) — 2023-12-13T13:55:12Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  https://github.com/kubernetes-sigs/kueue/pull/1360#discussion_r1422042617

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T14:57:33Z

- **test/e2e/multikueue/e2e_test.go:71** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T14:57:33Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  > Yes, I'd prefer to have every test run in it's own set of namespaces.
  
  But this is running in a different namespace for each cluster. I don't see the point of this test. Even if a placeholder, it should be in a form that will be similar to the incoming test.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T14:58:44Z

- **test/e2e/multikueue/e2e_test.go:71** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T14:58:44Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  It looks like a test table isn't the right construct here. I suggest removing the case and just merging the suite_test.go

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T15:08:28Z

- **test/e2e/multikueue/e2e_test.go:71** by [@trasc](https://github.com/trasc) — 2023-12-13T15:08:28Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  The namespaces and the ability to use the kubeconfig secret are used in all the new tests that will be added.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T15:39:06Z

- **test/e2e/multikueue/e2e_test.go:71** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T15:39:05Z
```diff
@@ -0,0 +1,95 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package mke2e
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/clientcmd"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+// +kubebuilder:docs-gen:collapse=Imports
+
+var _ = ginkgo.Describe("MultiKueue", func() {
+	var (
+		managerNs *corev1.Namespace
+		worker1Ns *corev1.Namespace
+		worker2Ns *corev1.Namespace
+	)
+	ginkgo.BeforeEach(func() {
+		managerNs = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "multikueue-",
+			},
+		}
+		gomega.Expect(k8sManagerClient.Create(ctx, managerNs)).To(gomega.Succeed())
+
+		worker1Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker1Client.Create(ctx, worker1Ns)).To(gomega.Succeed())
+
+		worker2Ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: managerNs.Name,
+			},
+		}
+		gomega.Expect(k8sWorker2Client.Create(ctx, worker2Ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sManagerClient, managerNs)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker1Client, worker1Ns)).To(gomega.Succeed())
+		gomega.Expect(util.DeleteNamespace(ctx, k8sWorker2Client, worker2Ns)).To(gomega.Succeed())
+	})
+	ginkgo.When("Using multiple clusters", func() {
+		ginkgo.DescribeTable("Cluster kubeconfig propagation", func(c *client.Client, key string, ns **corev1.Namespace) {
```

  Let's proceed, given that the follow up PR is already in the pipeline.
  
  /approve
  /lgtm
