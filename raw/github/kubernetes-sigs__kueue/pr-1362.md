# PR #1362: Extend visibility ClusterQueue PendingWorkloads endpoint

**Summary**: Extend visibility ClusterQueue PendingWorkloads endpoint

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1362

**Last updated**: 2024-02-13T19:38:35Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `extend-cq` → `main`
- **Created**: 2023-11-23T12:11:31Z
- **Updated**: 2024-02-13T19:38:35Z
- **Merged**: 2023-11-27T12:05:00Z
- **Closed**: 2023-11-27T12:05:00Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `kind/api-change`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 8   **+573 / -68**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
/kind api-change

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This PR continues the development of [KEP#168-2](https://github.com/kubernetes-sigs/kueue/pull/1300)

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Visibility API: extend the information returned for the pending workloads in a ClusterQueue, including the workload position in the queue.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-23T12:11:38Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bd42e42d26eef05715372b68f865231a94bff591 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6564800844996b0008213dec |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-23T12:11:41Z

Hi @PBundyra. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-11-23T12:11:51Z

/assign @mimowo 
/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-23T12:35:34Z

@PBundyra: Cannot trigger testing until a trusted user reviews the PR and leaves an `/ok-to-test` message.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1362#issuecomment-1824358233):

>/test


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-23T12:37:20Z

@PBundyra: Cannot trigger testing until a trusted user reviews the PR and leaves an `/ok-to-test` message.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1362#issuecomment-1824360430):

>/test all


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-23T12:48:52Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-23T17:18:05Z

Oh, I see why the e2e tests are failing, because the workload name is actually derived from the job name according to: https://github.com/kubernetes-sigs/kueue/blob/1ecd79f748af2617111d0bfa1ca848f9e172f4d7/pkg/controller/jobframework/workload_names.go#L44C8-L44C8. Can we call this function from within e2e tests? If not, maybe we need to fetch the workloads (per job), with GET, and assert on the names.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-11-23T17:25:05Z

> Oh, I see why the e2e tests are failing, because the workload name is actually derived from the job name according to: https://github.com/kubernetes-sigs/kueue/blob/1ecd79f748af2617111d0bfa1ca848f9e172f4d7/pkg/controller/jobframework/workload_names.go#L44C8-L44C8. Can we call this function from within e2e tests? If not, maybe we need to fetch the workloads (per job), with GET, and assert on the names.

I didn't see this comment earlier, and I've suppressed checking the Name. Do you think it's necessary to check it, as we already do it in unit tests?

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-23T17:55:07Z

> I didn't see this comment earlier, and I've suppressed checking the Name. Do you think it's necessary to check it, as we already do it in unit tests?

I think it is ok to skip the check, given that it is done in unit tests + the other properties of the workload identify it in the e2e tests.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-11-24T08:54:52Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-24T08:55:08Z

@PBundyra: The `/retest` command does not accept any targets.
The following commands are available to trigger required jobs:
* `/test pull-kueue-build-image-main`
* `/test pull-kueue-test-e2e-main-1-25`
* `/test pull-kueue-test-e2e-main-1-26`
* `/test pull-kueue-test-e2e-main-1-27`
* `/test pull-kueue-test-e2e-main-1-28`
* `/test pull-kueue-test-integration-main`
* `/test pull-kueue-test-unit-main`
* `/test pull-kueue-verify-main`

Use `/test all` to run all jobs.


<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1362#issuecomment-1825335342):

>/retest pull-kueue-test-e2e-main-1-28


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-11-24T08:55:53Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-24T11:01:29Z

LGTM label has been added.  <details>Git tree hash: 1efbe4dde3018cb1e59f99283c3db10e77fc25bb</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-24T13:35:35Z

LGTM. please squash

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-24T13:45:01Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-24T13:45:07Z

LGTM label has been added.  <details>Git tree hash: fbbca4a5043c9117f1845c88b3dff479ae423150</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-24T15:19:13Z

Still LGTM

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T16:54:32Z

/lgtm canel
/approve
Leaving LGTM to @mimowo once comments are addressed.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-24T16:54:38Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1362#issuecomment-1825917344" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1362#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T16:54:42Z

/lgtm cancel

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-27T11:32:36Z

LGTM, please squash

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-27T12:01:20Z

> Leaving LGTM to @mimowo once comments are addressed.

I consider the comments addressed. 
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-27T12:01:26Z

LGTM label has been added.  <details>Git tree hash: 5b35eb1b0e54d7773ec8675a6d7b5eee39cb552a</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T19:38:33Z

/release-note-edit
```release-note
Visibility API: extend the information returned for the pending workloads in a ClusterQueue, including the workload position in the queue.
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-23T13:17:13Z

- **pkg/visibility/api/rest/pending_workload_CQ_test.go:295** by [@mimowo](https://github.com/mimowo) — 2023-11-23T13:11:25Z
```diff
@@ -0,0 +1,304 @@
+// Copyright 2023 The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package rest
+
+import (
+	"context"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	v1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	visibility "sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/queue"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestPendingWorkloads(t *testing.T) {
+	const (
+		nsName   = "foo"
+		cqNameA  = "cqA"
+		cqNameB  = "cqB"
+		lqNameA  = "lqA"
+		lqNameB  = "lqB"
+		lowPrio  = 50
+		highPrio = 100
+	)
+
+	var (
+		defaultQueryParams = &visibility.PendingWorkloadOptions{
+			Offset: 0,
+			Limit:  constants.DefaultPendingWorkloadsLimit,
+		}
+	)
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	if err := visibility.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+
+	now := time.Now()
+	cases := map[string]struct {
+		clusterQueues        []*kueue.ClusterQueue
+		queues               []*kueue.LocalQueue
+		workloads            []*kueue.Workload
+		queryParams          *visibility.PendingWorkloadOptions
+		wantPendingWorkloads []visibility.PendingWorkload
+	}{
+		"single ClusterQueue and single LocalQueue setup with two workloads and default query parameters": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("a", nsName).Queue(lqNameA).Priority(highPrio).Obj(),
+				utiltesting.MakeWorkload("b", nsName).Queue(lqNameA).Priority(lowPrio).Obj(),
+			},
+			queryParams: defaultQueryParams,
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "a",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 0,
+					PositionInLocalQueue:   0,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "b",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               lowPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   1,
+				},
+			},
+		},
+		"single ClusterQueue and two LocalQueue setup with four workloads and default query parameters": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+				utiltesting.MakeLocalQueue(lqNameB, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("lqA-high-prio", nsName).Queue(lqNameA).Priority(highPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("lqA-low-prio", nsName).Queue(lqNameA).Priority(lowPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("lqB-high-prio", nsName).Queue(lqNameB).Priority(highPrio).Creation(now.Add(time.Second)).Obj(),
+				utiltesting.MakeWorkload("lqB-low-prio", nsName).Queue(lqNameB).Priority(lowPrio).Creation(now.Add(time.Second)).Obj(),
+			},
+			queryParams: defaultQueryParams,
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "lqA-high-prio",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 0,
+					PositionInLocalQueue:   0,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "lqB-high-prio",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameB,
+					Priority:               highPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   0,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "lqA-low-prio",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               lowPrio,
+					PositionInClusterQueue: 2,
+					PositionInLocalQueue:   1,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "lqB-low-prio",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameB,
+					Priority:               lowPrio,
+					PositionInClusterQueue: 3,
+					PositionInLocalQueue:   1,
+				},
+			},
+		},
+		"query parameters limit set": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("a", nsName).Queue(lqNameA).Priority(highPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("b", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second)).Obj(),
+				utiltesting.MakeWorkload("c", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second * 2)).Obj(),
+			},
+			queryParams: &visibility.PendingWorkloadOptions{
+				Limit: 2,
+			},
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "a",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 0,
+					PositionInLocalQueue:   0,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "b",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   1,
+				},
+			},
+		},
+		"query parameters offset set": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("a", nsName).Queue(lqNameA).Priority(highPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("b", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second)).Obj(),
+				utiltesting.MakeWorkload("c", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second * 2)).Obj(),
+			},
+			queryParams: &visibility.PendingWorkloadOptions{
+				Offset: 1,
+				Limit:  constants.DefaultPendingWorkloadsLimit,
+			},
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "b",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   1,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "c",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 2,
+					PositionInLocalQueue:   2,
+				},
+			},
+		},
+		"query parameters offset and limit set": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("a", nsName).Queue(lqNameA).Priority(highPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("b", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second)).Obj(),
+				utiltesting.MakeWorkload("c", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second * 2)).Obj(),
+			},
+			queryParams: &visibility.PendingWorkloadOptions{
+				Offset: 1,
+				Limit:  1,
+			},
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "b",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   1,
+				},
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			manager := queue.NewManager(utiltesting.NewFakeClient(), nil)
+			ctx := context.Background()
+			pendingWorkloadsInCqRest := NewPendingWorkloadsInCqREST(manager)
+			for _, cq := range tc.clusterQueues {
+				if err := manager.AddClusterQueue(ctx, cq); err != nil {
+					t.Fatalf("Adding cluster queue %s: %v", cq.Name, err)
+				}
+			}
+			for _, q := range tc.queues {
+				if err := manager.AddLocalQueue(ctx, q); err != nil {
+					t.Fatalf("Adding queue %q: %v", q.Name, err)
+				}
+			}
+			for _, w := range tc.workloads {
+				manager.AddOrUpdateWorkload(w)
+			}
+
+			for _, cq := range tc.clusterQueues {
+				info, err := pendingWorkloadsInCqRest.Get(ctx, cq.Name, tc.queryParams)
+				if err != nil {
+					t.Fatal(err)
+				}
+				pendingWorkloadsInfo := info.(*visibility.PendingWorkloadsSummary)
+
+				for index, want := range tc.wantPendingWorkloads {
```

  I would prefer to compare the entire lists, less code and possibly more in case the len(gotPendingWorkloads) > len(wantPendingWorkloads)

- **pkg/visibility/api/rest/pending_workload_CQ_test.go:35** by [@mimowo](https://github.com/mimowo) — 2023-11-23T13:12:22Z
```diff
@@ -0,0 +1,304 @@
+// Copyright 2023 The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package rest
+
+import (
+	"context"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	v1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	visibility "sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/queue"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestPendingWorkloads(t *testing.T) {
```

  This unit test is very useful, thanks!

- **test/e2e/e2e_test.go:173** by [@mimowo](https://github.com/mimowo) — 2023-11-23T13:14:58Z
```diff
@@ -157,6 +161,83 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Equal(0))
 			})
 		})
+
+		ginkgo.It("Should allow fetching information about position of pending workloads", func() {
+			var (
+				info *visibility.PendingWorkloadsSummary
+			)
+
+			ginkgo.By("Schedule a job that when admitted workloads blocks the queue", func() {
+				sampleJob1 = testingjob.MakeJob("test-job", ns.Name).
+					Queue(localQueueA.Name).
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
```

  Can we use the trick with terminating the job, as in the other test, instead of waiting 5s?

- **test/e2e/e2e_test.go:223** by [@mimowo](https://github.com/mimowo) — 2023-11-23T13:16:03Z
```diff
@@ -157,6 +161,83 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Equal(0))
 			})
 		})
+
+		ginkgo.It("Should allow fetching information about position of pending workloads", func() {
+			var (
+				info *visibility.PendingWorkloadsSummary
+			)
+
+			ginkgo.By("Schedule a job that when admitted workloads blocks the queue", func() {
+				sampleJob1 = testingjob.MakeJob("test-job", ns.Name).
+					Queue(localQueueA.Name).
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).
+					Request(corev1.ResourceCPU, "1").
+					PriorityClass(highPriorityClass.Name).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, sampleJob1)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Schedule three different jobs with two different priorities and two different LocalQueues", func() {
+				jobCases := []struct {
+					JobName          string
+					JobPrioClassName string
+					LocalQueueName   string
+				}{
+					{
+						JobName:          "lq-a-high-prio",
+						JobPrioClassName: highPriorityClass.Name,
+						LocalQueueName:   localQueueA.Name,
+					},
+					{
+						JobName:          "lq-b-high-prio",
+						JobPrioClassName: highPriorityClass.Name,
+						LocalQueueName:   localQueueB.Name,
+					},
+					{
+						JobName:          "lq-b-low-prio",
+						JobPrioClassName: lowPriorityClass.Name,
+						LocalQueueName:   localQueueB.Name,
+					},
+				}
+				for _, jobCase := range jobCases {
+					job := testingjob.MakeJob(jobCase.JobName, ns.Name).
+						Queue(jobCase.LocalQueueName).
+						Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"1s"}).
+						Request(corev1.ResourceCPU, "1").
+						PriorityClass(jobCase.JobPrioClassName).
+						Obj()
+					gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Verify there are three pending workloads", func() {
+				gomega.Eventually(func() int {
+					var err error
+					info, err = visibilityClient.ClusterQueues().GetPendingWorkloadsSummary(ctx, clusterQueue.Name, metav1.GetOptions{})
+					gomega.Expect(err).NotTo(gomega.HaveOccurred())
+					return len(info.Items)
+				}, util.Timeout, util.Interval).Should(gomega.Equal(3))
+			})
+
+			ginkgo.By("Verify their position in ClusterQueue", func() {
+				gomega.Expect(info.Items[0].PositionInClusterQueue).To(gomega.Equal(0))
```

  This is hard to read, can we instead use the `ComparableTo` function (or such) to compare the entire list of pending workloads to the expected list?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-23T15:35:08Z

- **apis/visibility/v1alpha1/types.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-23T15:35:08Z
```diff
@@ -84,10 +84,10 @@ type PendingWorkloadOptions struct {
 	metav1.TypeMeta `json:",inline"`
 
 	// Offset indicates position of the first pending workload that should be fetched starting from 0. 0 by default
-	Offset int32 `json:"offset"`
+	Offset int `json:"offset"`
```

  use int32 or int64, as necessary.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-23T15:39:18Z

- **apis/visibility/v1alpha1/types.go:87** by [@PBundyra](https://github.com/PBundyra) — 2023-11-23T15:39:18Z
```diff
@@ -84,10 +84,10 @@ type PendingWorkloadOptions struct {
 	metav1.TypeMeta `json:",inline"`
 
 	// Offset indicates position of the first pending workload that should be fetched starting from 0. 0 by default
-	Offset int32 `json:"offset"`
+	Offset int `json:"offset"`
```

  int32 or int64 are not supported by the conversion framework and using them would require manual conversion

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-23T15:51:03Z

- **apis/visibility/v1alpha1/types.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-23T15:51:03Z
```diff
@@ -84,10 +84,10 @@ type PendingWorkloadOptions struct {
 	metav1.TypeMeta `json:",inline"`
 
 	// Offset indicates position of the first pending workload that should be fetched starting from 0. 0 by default
-	Offset int32 `json:"offset"`
+	Offset int `json:"offset"`
```

  How so? we use these types in k/k extensively.
  
  What is the conversion for?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-23T15:51:16Z

Just some nits

- **pkg/visibility/api/rest/local_queue.go:1** by [@mimowo](https://github.com/mimowo) — 2023-11-23T15:48:53Z
```diff
@@ -0,0 +1,53 @@
+// Copyright 2023 The Kubernetes Authors.
```

  This seems like unrelated to the PR?

- **pkg/visibility/api/rest/pending_workload_CQ_test.go:299** by [@mimowo](https://github.com/mimowo) — 2023-11-23T15:49:15Z
```diff
@@ -292,12 +292,16 @@ func TestPendingWorkloads(t *testing.T) {
 				}
 				pendingWorkloadsInfo := info.(*visibility.PendingWorkloadsSummary)
 
-				for index, want := range tc.wantPendingWorkloads {
-					got := pendingWorkloadsInfo.Items[index]
-					if diff := cmp.Diff(want, got, cmpopts.IgnoreTypes(metav1.TypeMeta{})); diff != "" {
-						t.Errorf("Pending workload at index %v differ: (-want,+got):\n%s", index, diff)
-					}
+				if diff := cmp.Diff(tc.wantPendingWorkloads, pendingWorkloadsInfo.Items, cmpopts.IgnoreTypes(metav1.TypeMeta{})); diff != "" {
+					t.Errorf("Pending workloads differ: (-want,+got):\n%s", diff)
 				}
+
+				// for index, want := range tc.wantPendingWorkloads {
```

  Clear this

- **test/e2e/e2e_test.go:252** by [@mimowo](https://github.com/mimowo) — 2023-11-23T15:50:31Z
```diff
@@ -219,22 +222,36 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Equal(3))
 			})
 
-			ginkgo.By("Verify their position in ClusterQueue", func() {
-				gomega.Expect(info.Items[0].PositionInClusterQueue).To(gomega.BeNumerically("==", 0))
-				gomega.Expect(info.Items[1].PositionInClusterQueue).To(gomega.BeNumerically("==", 1))
-				gomega.Expect(info.Items[2].PositionInClusterQueue).To(gomega.BeNumerically("==", 2))
-			})
-
-			ginkgo.By("Verify two first workloads have high priority and 0 LocalQueue position", func() {
-				gomega.Expect(info.Items[0].Priority).To(gomega.Equal(highPriorityClass.Value))
-				gomega.Expect(info.Items[1].Priority).To(gomega.Equal(highPriorityClass.Value))
-				gomega.Expect(info.Items[0].PositionInLocalQueue).To(gomega.BeNumerically("==", 0))
-				gomega.Expect(info.Items[1].PositionInLocalQueue).To(gomega.BeNumerically("==", 0))
+			ginkgo.By("Verify their positions and priorities", func() {
+				expectedWls := []visibility.PendingWorkload{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Namespace: ns.Name,
+						},
+						Priority:               highPriorityClass.Value,
+						PositionInLocalQueue:   0,
+						PositionInClusterQueue: 0,
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Namespace: ns.Name,
+						},
+						Priority:               highPriorityClass.Value,
+						PositionInLocalQueue:   0,
+						PositionInClusterQueue: 1,
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Namespace: ns.Name,
+						},
+						Priority:               lowPriorityClass.Value,
+						PositionInLocalQueue:   1,
+						PositionInClusterQueue: 2,
+					},
+				}
+				gomega.Expect(info.Items).To(gomega.BeComparableTo(expectedWls, cmpopts.IgnoreFields(visibility.PendingWorkload{}, "Name", "LocalQueueName")))
```

  Why excluding the LocalQueueName here, in favor of the additional step? I think it would read easier to have all the properties here.

- **test/e2e/e2e_test.go:226** by [@mimowo](https://github.com/mimowo) — 2023-11-23T15:50:50Z
```diff
@@ -219,22 +222,36 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Equal(3))
 			})
 
-			ginkgo.By("Verify their position in ClusterQueue", func() {
-				gomega.Expect(info.Items[0].PositionInClusterQueue).To(gomega.BeNumerically("==", 0))
-				gomega.Expect(info.Items[1].PositionInClusterQueue).To(gomega.BeNumerically("==", 1))
-				gomega.Expect(info.Items[2].PositionInClusterQueue).To(gomega.BeNumerically("==", 2))
-			})
-
-			ginkgo.By("Verify two first workloads have high priority and 0 LocalQueue position", func() {
-				gomega.Expect(info.Items[0].Priority).To(gomega.Equal(highPriorityClass.Value))
-				gomega.Expect(info.Items[1].Priority).To(gomega.Equal(highPriorityClass.Value))
-				gomega.Expect(info.Items[0].PositionInLocalQueue).To(gomega.BeNumerically("==", 0))
-				gomega.Expect(info.Items[1].PositionInLocalQueue).To(gomega.BeNumerically("==", 0))
+			ginkgo.By("Verify their positions and priorities", func() {
+				expectedWls := []visibility.PendingWorkload{
```

  ```suggestion
  				wantPendingWorkloads := []visibility.PendingWorkload{
  ```
  nit

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-23T15:55:32Z

- **test/e2e/e2e_test.go:252** by [@PBundyra](https://github.com/PBundyra) — 2023-11-23T15:55:32Z
```diff
@@ -219,22 +222,36 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Equal(3))
 			})
 
-			ginkgo.By("Verify their position in ClusterQueue", func() {
-				gomega.Expect(info.Items[0].PositionInClusterQueue).To(gomega.BeNumerically("==", 0))
-				gomega.Expect(info.Items[1].PositionInClusterQueue).To(gomega.BeNumerically("==", 1))
-				gomega.Expect(info.Items[2].PositionInClusterQueue).To(gomega.BeNumerically("==", 2))
-			})
-
-			ginkgo.By("Verify two first workloads have high priority and 0 LocalQueue position", func() {
-				gomega.Expect(info.Items[0].Priority).To(gomega.Equal(highPriorityClass.Value))
-				gomega.Expect(info.Items[1].Priority).To(gomega.Equal(highPriorityClass.Value))
-				gomega.Expect(info.Items[0].PositionInLocalQueue).To(gomega.BeNumerically("==", 0))
-				gomega.Expect(info.Items[1].PositionInLocalQueue).To(gomega.BeNumerically("==", 0))
+			ginkgo.By("Verify their positions and priorities", func() {
+				expectedWls := []visibility.PendingWorkload{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Namespace: ns.Name,
+						},
+						Priority:               highPriorityClass.Value,
+						PositionInLocalQueue:   0,
+						PositionInClusterQueue: 0,
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Namespace: ns.Name,
+						},
+						Priority:               highPriorityClass.Value,
+						PositionInLocalQueue:   0,
+						PositionInClusterQueue: 1,
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Namespace: ns.Name,
+						},
+						Priority:               lowPriorityClass.Value,
+						PositionInLocalQueue:   1,
+						PositionInClusterQueue: 2,
+					},
+				}
+				gomega.Expect(info.Items).To(gomega.BeComparableTo(expectedWls, cmpopts.IgnoreFields(visibility.PendingWorkload{}, "Name", "LocalQueueName")))
```

  The two first workloads have different names and localQueues, since we don't know their order I wanted to avoid checking this. An alternative would be to wait until one the first workloads gets created or add an additional priority class.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-23T17:12:48Z

Generally LGTM, remaining question about the conversion. I think potential changing the types could be handled outside of the this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-23T17:20:28Z

- **apis/visibility/v1alpha1/types.go:87** by [@mimowo](https://github.com/mimowo) — 2023-11-23T17:20:28Z
```diff
@@ -84,10 +84,10 @@ type PendingWorkloadOptions struct {
 	metav1.TypeMeta `json:",inline"`
 
 	// Offset indicates position of the first pending workload that should be fetched starting from 0. 0 by default
-	Offset int32 `json:"offset"`
+	Offset int `json:"offset"`
```

  > What is the conversion for?
  
  `func autoConvert_url_Values_To_v1alpha1_PendingWorkloadOptions(in *url.Values, out *PendingWorkloadOptions, s conversion.Scope) error {` in `zz_generated.conversion.go`.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-23T17:21:28Z

- **apis/visibility/v1alpha1/types.go:87** by [@PBundyra](https://github.com/PBundyra) — 2023-11-23T17:21:28Z
```diff
@@ -84,10 +84,10 @@ type PendingWorkloadOptions struct {
 	metav1.TypeMeta `json:",inline"`
 
 	// Offset indicates position of the first pending workload that should be fetched starting from 0. 0 by default
-	Offset int32 `json:"offset"`
+	Offset int `json:"offset"`
```

  I've double-checked, and to my surprise, int64 is supported but int32 is not. In general, the conversion is used to convert `url.Values` (query parameters) into `PendingWorkloadOptions`. 
  
  I'll use int64 then.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-23T17:21:50Z

- **test/e2e/e2e_test.go:252** by [@PBundyra](https://github.com/PBundyra) — 2023-11-23T17:21:50Z
```diff
@@ -219,22 +222,36 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Equal(3))
 			})
 
-			ginkgo.By("Verify their position in ClusterQueue", func() {
-				gomega.Expect(info.Items[0].PositionInClusterQueue).To(gomega.BeNumerically("==", 0))
-				gomega.Expect(info.Items[1].PositionInClusterQueue).To(gomega.BeNumerically("==", 1))
-				gomega.Expect(info.Items[2].PositionInClusterQueue).To(gomega.BeNumerically("==", 2))
-			})
-
-			ginkgo.By("Verify two first workloads have high priority and 0 LocalQueue position", func() {
-				gomega.Expect(info.Items[0].Priority).To(gomega.Equal(highPriorityClass.Value))
-				gomega.Expect(info.Items[1].Priority).To(gomega.Equal(highPriorityClass.Value))
-				gomega.Expect(info.Items[0].PositionInLocalQueue).To(gomega.BeNumerically("==", 0))
-				gomega.Expect(info.Items[1].PositionInLocalQueue).To(gomega.BeNumerically("==", 0))
+			ginkgo.By("Verify their positions and priorities", func() {
+				expectedWls := []visibility.PendingWorkload{
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Namespace: ns.Name,
+						},
+						Priority:               highPriorityClass.Value,
+						PositionInLocalQueue:   0,
+						PositionInClusterQueue: 0,
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Namespace: ns.Name,
+						},
+						Priority:               highPriorityClass.Value,
+						PositionInLocalQueue:   0,
+						PositionInClusterQueue: 1,
+					},
+					{
+						ObjectMeta: metav1.ObjectMeta{
+							Namespace: ns.Name,
+						},
+						Priority:               lowPriorityClass.Value,
+						PositionInLocalQueue:   1,
+						PositionInClusterQueue: 2,
+					},
+				}
+				gomega.Expect(info.Items).To(gomega.BeComparableTo(expectedWls, cmpopts.IgnoreFields(visibility.PendingWorkload{}, "Name", "LocalQueueName")))
```

  I've added an additional priority class

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-23T17:56:48Z

- **apis/visibility/v1alpha1/conversion_test.go:49** by [@mimowo](https://github.com/mimowo) — 2023-11-23T17:56:48Z
```diff
@@ -46,8 +46,8 @@ func TestPendingWorkloadsOptions(t *testing.T) {
 				"offset": {"2"},
 			},
 			wantPendingWorkloadOptions: PendingWorkloadOptions{
-				Limit:  1,
```

  Is this change required?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-23T17:57:05Z

- **pkg/constants/constants.go:41** by [@mimowo](https://github.com/mimowo) — 2023-11-23T17:57:05Z
```diff
@@ -38,5 +38,5 @@ const (
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
 
-	DefaultPendingWorkloadsLimit = 1000
```

  Is this required ?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-24T08:42:09Z

- **apis/visibility/v1alpha1/conversion_test.go:49** by [@PBundyra](https://github.com/PBundyra) — 2023-11-24T08:42:09Z
```diff
@@ -46,8 +46,8 @@ func TestPendingWorkloadsOptions(t *testing.T) {
 				"offset": {"2"},
 			},
 			wantPendingWorkloadOptions: PendingWorkloadOptions{
-				Limit:  1,
```

  Good point, I've reverted it

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-24T08:42:21Z

- **pkg/constants/constants.go:41** by [@PBundyra](https://github.com/PBundyra) — 2023-11-24T08:42:21Z
```diff
@@ -38,5 +38,5 @@ const (
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
 
-	DefaultPendingWorkloadsLimit = 1000
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-24T09:06:37Z

Looking good, couple more nits

- **test/e2e/e2e_test.go:221** by [@mimowo](https://github.com/mimowo) — 2023-11-24T08:52:42Z
```diff
@@ -157,6 +167,100 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Equal(0))
 			})
 		})
+
+		ginkgo.It("Should allow fetching information about position of pending workloads", func() {
+			var (
+				info *visibility.PendingWorkloadsSummary
+			)
+
+			ginkgo.By("Schedule a job that when admitted workloads blocks the queue", func() {
+				sampleJob1 = testingjob.MakeJob("test-job", ns.Name).
+					Queue(localQueueA.Name).
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"60s", "-termination-grace-period", "0s"}).
+					Request(corev1.ResourceCPU, "1").
+					TerimnationGracePeriod(1).
+					BackoffLimit(0).
+					PriorityClass(highPriorityClass.Name).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, sampleJob1)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Schedule three different jobs with two different priorities and two different LocalQueues", func() {
+				jobCases := []struct {
+					JobName          string
+					JobPrioClassName string
+					LocalQueueName   string
+				}{
+					{
+						JobName:          "lq-a-high-prio",
+						JobPrioClassName: highPriorityClass.Name,
+						LocalQueueName:   localQueueA.Name,
+					},
+					{
+						JobName:          "lq-b-mid-prio",
+						JobPrioClassName: midPriorityClass.Name,
+						LocalQueueName:   localQueueB.Name,
+					},
+					{
+						JobName:          "lq-b-low-prio",
+						JobPrioClassName: lowPriorityClass.Name,
+						LocalQueueName:   localQueueB.Name,
+					},
+				}
+				for _, jobCase := range jobCases {
+					job := testingjob.MakeJob(jobCase.JobName, ns.Name).
+						Queue(jobCase.LocalQueueName).
+						Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"1s"}).
+						Request(corev1.ResourceCPU, "1").
+						PriorityClass(jobCase.JobPrioClassName).
+						Obj()
+					gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+				}
+			})
+
+			ginkgo.By("Verify there are three pending workloads", func() {
```

  I don't see this check necessary, given the one below. In case of a failure, say 2 workloads instead of 3, executing the next check might actually be beneficial, instead of ending here.

- **test/e2e/e2e_test.go:173** by [@mimowo](https://github.com/mimowo) — 2023-11-24T08:53:05Z
```diff
@@ -157,6 +167,100 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Equal(0))
 			})
 		})
+
+		ginkgo.It("Should allow fetching information about position of pending workloads", func() {
+			var (
+				info *visibility.PendingWorkloadsSummary
```

  No need for this variable, inline the declaration.

- **pkg/visibility/api/rest/pending_workload_CQ_test.go:272** by [@mimowo](https://github.com/mimowo) — 2023-11-24T09:04:43Z
```diff
@@ -0,0 +1,301 @@
+// Copyright 2023 The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package rest
+
+import (
+	"context"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	v1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	visibility "sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/queue"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestPendingWorkloads(t *testing.T) {
+	const (
+		nsName   = "foo"
+		cqNameA  = "cqA"
+		cqNameB  = "cqB"
+		lqNameA  = "lqA"
+		lqNameB  = "lqB"
+		lowPrio  = 50
+		highPrio = 100
+	)
+
+	var (
+		defaultQueryParams = &visibility.PendingWorkloadOptions{
+			Offset: 0,
+			Limit:  constants.DefaultPendingWorkloadsLimit,
+		}
+	)
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	if err := visibility.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+
+	now := time.Now()
+	cases := map[string]struct {
+		clusterQueues        []*kueue.ClusterQueue
+		queues               []*kueue.LocalQueue
+		workloads            []*kueue.Workload
+		queryParams          *visibility.PendingWorkloadOptions
+		wantPendingWorkloads []visibility.PendingWorkload
+	}{
+		"single ClusterQueue and single LocalQueue setup with two workloads and default query parameters": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("a", nsName).Queue(lqNameA).Priority(highPrio).Obj(),
+				utiltesting.MakeWorkload("b", nsName).Queue(lqNameA).Priority(lowPrio).Obj(),
+			},
+			queryParams: defaultQueryParams,
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "a",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 0,
+					PositionInLocalQueue:   0,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "b",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               lowPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   1,
+				},
+			},
+		},
+		"single ClusterQueue and two LocalQueue setup with four workloads and default query parameters": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+				utiltesting.MakeLocalQueue(lqNameB, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("lqA-high-prio", nsName).Queue(lqNameA).Priority(highPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("lqA-low-prio", nsName).Queue(lqNameA).Priority(lowPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("lqB-high-prio", nsName).Queue(lqNameB).Priority(highPrio).Creation(now.Add(time.Second)).Obj(),
+				utiltesting.MakeWorkload("lqB-low-prio", nsName).Queue(lqNameB).Priority(lowPrio).Creation(now.Add(time.Second)).Obj(),
+			},
+			queryParams: defaultQueryParams,
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "lqA-high-prio",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 0,
+					PositionInLocalQueue:   0,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "lqB-high-prio",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameB,
+					Priority:               highPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   0,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "lqA-low-prio",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               lowPrio,
+					PositionInClusterQueue: 2,
+					PositionInLocalQueue:   1,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "lqB-low-prio",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameB,
+					Priority:               lowPrio,
+					PositionInClusterQueue: 3,
+					PositionInLocalQueue:   1,
+				},
+			},
+		},
+		"query parameters limit set": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("a", nsName).Queue(lqNameA).Priority(highPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("b", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second)).Obj(),
+				utiltesting.MakeWorkload("c", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second * 2)).Obj(),
+			},
+			queryParams: &visibility.PendingWorkloadOptions{
+				Limit: 2,
+			},
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "a",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 0,
+					PositionInLocalQueue:   0,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "b",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   1,
+				},
+			},
+		},
+		"query parameters offset set": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("a", nsName).Queue(lqNameA).Priority(highPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("b", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second)).Obj(),
+				utiltesting.MakeWorkload("c", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second * 2)).Obj(),
+			},
+			queryParams: &visibility.PendingWorkloadOptions{
+				Offset: 1,
+				Limit:  constants.DefaultPendingWorkloadsLimit,
+			},
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "b",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   1,
+				},
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "c",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 2,
+					PositionInLocalQueue:   2,
+				},
+			},
+		},
+		"query parameters offset and limit set": {
+			clusterQueues: []*kueue.ClusterQueue{
+				utiltesting.MakeClusterQueue(cqNameA).Obj(),
+			},
+			queues: []*kueue.LocalQueue{
+				utiltesting.MakeLocalQueue(lqNameA, nsName).ClusterQueue(cqNameA).Obj(),
+			},
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("a", nsName).Queue(lqNameA).Priority(highPrio).Creation(now).Obj(),
+				utiltesting.MakeWorkload("b", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second)).Obj(),
+				utiltesting.MakeWorkload("c", nsName).Queue(lqNameA).Priority(highPrio).Creation(now.Add(time.Second * 2)).Obj(),
+			},
+			queryParams: &visibility.PendingWorkloadOptions{
+				Offset: 1,
+				Limit:  1,
+			},
+			wantPendingWorkloads: []visibility.PendingWorkload{
+				{
+					ObjectMeta: v1.ObjectMeta{
+						Name:      "b",
+						Namespace: nsName,
+					},
+					LocalQueueName:         lqNameA,
+					Priority:               highPrio,
+					PositionInClusterQueue: 1,
+					PositionInLocalQueue:   1,
+				},
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			manager := queue.NewManager(utiltesting.NewFakeClient(), nil)
+			ctx := context.Background()
```

  I think we should wrap the ctx with cancel and timeout, and defer cancel(), for example as here (and cleanup the manager): 
  https://github.com/kubernetes-sigs/kueue/blob/1ecd79f748af2617111d0bfa1ca848f9e172f4d7/pkg/scheduler/scheduler_test.go#L1108-L1110

- **test/e2e/e2e_test.go:213** by [@mimowo](https://github.com/mimowo) — 2023-11-24T09:06:04Z
```diff
@@ -157,6 +167,100 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Equal(0))
 			})
 		})
+
+		ginkgo.It("Should allow fetching information about position of pending workloads", func() {
+			var (
+				info *visibility.PendingWorkloadsSummary
+			)
+
+			ginkgo.By("Schedule a job that when admitted workloads blocks the queue", func() {
+				sampleJob1 = testingjob.MakeJob("test-job", ns.Name).
+					Queue(localQueueA.Name).
+					Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"60s", "-termination-grace-period", "0s"}).
+					Request(corev1.ResourceCPU, "1").
+					TerimnationGracePeriod(1).
+					BackoffLimit(0).
+					PriorityClass(highPriorityClass.Name).
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, sampleJob1)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Schedule three different jobs with two different priorities and two different LocalQueues", func() {
+				jobCases := []struct {
+					JobName          string
+					JobPrioClassName string
+					LocalQueueName   string
+				}{
+					{
+						JobName:          "lq-a-high-prio",
+						JobPrioClassName: highPriorityClass.Name,
+						LocalQueueName:   localQueueA.Name,
+					},
+					{
+						JobName:          "lq-b-mid-prio",
+						JobPrioClassName: midPriorityClass.Name,
+						LocalQueueName:   localQueueB.Name,
+					},
+					{
+						JobName:          "lq-b-low-prio",
+						JobPrioClassName: lowPriorityClass.Name,
+						LocalQueueName:   localQueueB.Name,
+					},
+				}
+				for _, jobCase := range jobCases {
+					job := testingjob.MakeJob(jobCase.JobName, ns.Name).
+						Queue(jobCase.LocalQueueName).
+						Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"1s"}).
```

  Why "1s" for the runtime? Can we set 0s, or 1ms instead?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-24T10:39:37Z

- **pkg/visibility/api/rest/pending_workload_CQ_test.go:273** by [@mimowo](https://github.com/mimowo) — 2023-11-24T10:39:37Z
```diff
@@ -270,6 +270,10 @@ func TestPendingWorkloads(t *testing.T) {
 		t.Run(name, func(t *testing.T) {
 			manager := queue.NewManager(utiltesting.NewFakeClient(), nil)
 			ctx := context.Background()
+			ctx, cancel := context.WithTimeout(ctx, 100*time.Millisecond)
```

  This timeout seems very short, I'm worried it might fail on loaded environment, It should not hurt to increase the timeout, to say 5s.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-24T10:55:36Z

- **pkg/visibility/api/rest/pending_workload_CQ.go:101** by [@mimowo](https://github.com/mimowo) — 2023-11-24T10:55:36Z
```diff
@@ -83,7 +97,8 @@ func (m *pendingWorkloadsInCqREST) Get(ctx context.Context, name string, opts ru
 func (m *pendingWorkloadsInCqREST) NewGetOptions() (runtime.Object, bool, string) {
 	// If no query parameters were passed the generated defaults function are not executed so it's necessary to set default values here as well
 	return &v1alpha1.PendingWorkloadOptions{
-		Limit: constants.DefaultPendingWorkloadsLimit,
+		Limit:  constants.DefaultPendingWorkloadsLimit,
+		Offset: 0,
```

  Revert if not necessary

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-24T11:01:23Z

/lgtm
/assign @tenzen-y @alculquicondor

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-11-24T14:32:09Z

My first pass. I have yet to see the implementation.

- **apis/visibility/v1alpha1/types.go:34** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-24T14:07:52Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Priority               int32  `json:"priority"`
+	LocalQueueName         string `json:"localqueuename"`
+	PositionInClusterQueue int32  `json:"positioninclusterqueue"`
+	PositionInLocalQueue   int32  `json:"positioninlocalqueue"`
```

  ```suggestion
  	Priority               int32  `json:"priority"`
  	LocalQueueName         string `json:"localQueueName"`
  	PositionInClusterQueue int32  `json:"positionInClusterQueue"`
  	PositionInLocalQueue   int32  `json:"positionInLocalQueue"`
  ```
  
  Can we use camel case as well as other APIs?

- **pkg/queue/manager.go:574** by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-24T14:10:35Z
```diff
@@ -571,30 +571,34 @@ func (m *Manager) getClusterQueue(cqName string) ClusterQueue {
 	return m.clusterQueues[cqName]
 }
 
-// PendingWorkloadsInfo computes a head of the new snapshot. Length of the head is set by the maxCount parameter
-func (m *Manager) PendingWorkloadsInfo(cqName string) []kueue.ClusterQueuePendingWorkload {
-	pendingWlsInfo := make([]kueue.ClusterQueuePendingWorkload, 0)
+func (m *Manager) GetPendingWorkloadsInfo(cqName string) []*workload.Info {
```

  ```suggestion
  func (m *Manager) PendingWorkloadsInfo(cqName string) []*workload.Info {
  ```
  
  I would suggest referring to the contribution guide for SIG Scheduling.
  
  - https://github.com/kubernetes/community/blob/master/sig-scheduling/CONTRIBUTING.md
  - https://go.dev/doc/effective_go#Getters.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-24T16:54:04Z

- **apis/visibility/v1alpha1/types.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T16:46:41Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
```

  wondering here... do these correspond to the Job/MPIJob/etc or the Workload object?
  Ideally, it should be the Job objects, as they are the "user facing" APIs. And one of the returned values could be the Workload Name. But that could be a follow up.

- **pkg/queue/manager.go:587** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T16:49:22Z
```diff
@@ -571,30 +571,34 @@ func (m *Manager) getClusterQueue(cqName string) ClusterQueue {
 	return m.clusterQueues[cqName]
 }
 
-// PendingWorkloadsInfo computes a head of the new snapshot. Length of the head is set by the maxCount parameter
-func (m *Manager) PendingWorkloadsInfo(cqName string) []kueue.ClusterQueuePendingWorkload {
-	pendingWlsInfo := make([]kueue.ClusterQueuePendingWorkload, 0)
+func (m *Manager) PendingWorkloadsInfo(cqName string) []*workload.Info {
 	cq := m.getClusterQueue(cqName)
 	if cq == nil {
-		return pendingWlsInfo
+		return nil
+	}
+	return cq.Snapshot()
+}
+
+// UpdateSnapshot computes the new snapshot and replaces if it differs from the
+// previous version. It returns true if the snapshot was actually updated.
+func (m *Manager) UpdateSnapshot(cqName string, maxCount int32) bool {
+	cq := m.getClusterQueue(cqName)
+	if cq == nil {
+		return false
```

  kind of unrelated to this PR, but if the CQ disappears, is it deleted form the snapshot somewhere else?

- **pkg/visibility/api/rest/pending_workload_CQ.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T16:52:45Z
```diff
@@ -66,15 +66,29 @@ func (m *pendingWorkloadsInCqREST) Get(ctx context.Context, name string, opts ru
 	offset := pendingWorkloadOpts.Offset
 
 	wls := make([]v1alpha1.PendingWorkload, 0, limit)
-	allPendingWorkloads := m.queueMgr.PendingWorkloadsInfo(name)
-	for index := offset; index < offset+limit && index < int64(len(allPendingWorkloads)); index++ {
-		wlInfo := allPendingWorkloads[index]
-		wls = append(wls, v1alpha1.PendingWorkload{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:      wlInfo.Name,
-				Namespace: wlInfo.Namespace,
-			},
-		})
+	pendingWorkloadsInfo := m.queueMgr.PendingWorkloadsInfo(name)
+	localQueuePositions := make(map[string]int32, 0)
```

  can we initialize this to the number of local queues?

- **apis/visibility/v1alpha1/types.go:31** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T16:53:05Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Priority               int32  `json:"priority"`
```

  Add comments to each field.

- **apis/visibility/v1alpha1/types.go:33** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T16:53:12Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Priority               int32  `json:"priority"`
+	LocalQueueName         string `json:"localQueueName"`
+	PositionInClusterQueue int32  `json:"positionInClusterQueue"`
```

  Starts from 0 or 1? :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-24T17:09:57Z

- **pkg/queue/manager.go:587** by [@mimowo](https://github.com/mimowo) — 2023-11-24T17:09:56Z
```diff
@@ -571,30 +571,34 @@ func (m *Manager) getClusterQueue(cqName string) ClusterQueue {
 	return m.clusterQueues[cqName]
 }
 
-// PendingWorkloadsInfo computes a head of the new snapshot. Length of the head is set by the maxCount parameter
-func (m *Manager) PendingWorkloadsInfo(cqName string) []kueue.ClusterQueuePendingWorkload {
-	pendingWlsInfo := make([]kueue.ClusterQueuePendingWorkload, 0)
+func (m *Manager) PendingWorkloadsInfo(cqName string) []*workload.Info {
 	cq := m.getClusterQueue(cqName)
 	if cq == nil {
-		return pendingWlsInfo
+		return nil
+	}
+	return cq.Snapshot()
+}
+
+// UpdateSnapshot computes the new snapshot and replaces if it differs from the
+// previous version. It returns true if the snapshot was actually updated.
+func (m *Manager) UpdateSnapshot(cqName string, maxCount int32) bool {
+	cq := m.getClusterQueue(cqName)
+	if cq == nil {
+		return false
```

  Yes, we do it here: https://github.com/kubernetes-sigs/kueue/blob/44ad0d50cacd1830c8ce2e50b1b584636d1e972a/pkg/controller/core/clusterqueue_controller.go#L275. Altough, the new code does not actually use the in-memory stored snapshot, just uses the same function to generate one.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-24T17:11:49Z

- **apis/visibility/v1alpha1/types.go:29** by [@mimowo](https://github.com/mimowo) — 2023-11-24T17:11:49Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
```

  They correspond to the workload object, similarly as for the visibility in status we decided. Otherwise we also need to return the "kind". Keeping the objects small was the motivation for the status visibility, here I think we can afford more fields easily. So we could have a follow up with JobName, and jobKind fields.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-24T17:19:52Z

- **apis/visibility/v1alpha1/types.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T17:19:52Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
```

  I think the other way makes more sense, since you are already adding TypeMeta here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-24T17:28:50Z

- **apis/visibility/v1alpha1/types.go:29** by [@mimowo](https://github.com/mimowo) — 2023-11-24T17:28:50Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
```

  I see, I think we could try that, but indeed I suggest a follow up, maybe we should update the KEP for it.
  
  I thought the `TypeMeta` here is to indicate details about the `PendingWorkload` type itself, isn't it?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-24T18:03:08Z

- **apis/visibility/v1alpha1/types.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-24T18:03:08Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
```

  Right now we don't have an endpoint for an individual Workload or Job, so we are not actually using the TypeMeta right now.
  But unclear if we will need a different struct for that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-24T20:22:28Z

- **apis/visibility/v1alpha1/types.go:29** by [@mimowo](https://github.com/mimowo) — 2023-11-24T20:22:28Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
```

  We intend to use the same struct `PendingWorkload` for all three endpoints: clusterqueue, localqueue, and individual workload. 
  
  Just to be clear on the suggestion. `metav1.TypeMeta` has `Kind` and `APIVersion` fields. So, say, for a batch job we would return instance of `PendingWorkload` with `.kind=Job`, `.apiversion=batch/v1`, then `.name={job.name}`. 
  
  I like the suggestion, so that we use the concept of Jobs, which are supplied by users. However, as you mention let's keep it as a follow up. We may also have a small KEP update to clarify this.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-27T10:09:31Z

- **pkg/visibility/api/rest/pending_workload_CQ.go:70** by [@PBundyra](https://github.com/PBundyra) — 2023-11-27T10:09:31Z
```diff
@@ -66,15 +66,29 @@ func (m *pendingWorkloadsInCqREST) Get(ctx context.Context, name string, opts ru
 	offset := pendingWorkloadOpts.Offset
 
 	wls := make([]v1alpha1.PendingWorkload, 0, limit)
-	allPendingWorkloads := m.queueMgr.PendingWorkloadsInfo(name)
-	for index := offset; index < offset+limit && index < int64(len(allPendingWorkloads)); index++ {
-		wlInfo := allPendingWorkloads[index]
-		wls = append(wls, v1alpha1.PendingWorkload{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:      wlInfo.Name,
-				Namespace: wlInfo.Namespace,
-			},
-		})
+	pendingWorkloadsInfo := m.queueMgr.PendingWorkloadsInfo(name)
+	localQueuePositions := make(map[string]int32, 0)
```

  In order to do that I would need to extend the `Manager` struct in `pkg/queue/manager.go`, as right now there is no easy way to get the number. WDYT?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-27T10:24:02Z

- **apis/visibility/v1alpha1/types.go:29** by [@PBundyra](https://github.com/PBundyra) — 2023-11-27T10:24:02Z
```diff
@@ -27,6 +27,11 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
```

  To maintain this discussion I'm mentioning [issue#168](https://github.com/kubernetes-sigs/kueue/issues/168) so we do not lose visibility of it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-27T10:42:12Z

- **pkg/visibility/api/rest/pending_workload_CQ.go:70** by [@mimowo](https://github.com/mimowo) — 2023-11-27T10:42:12Z
```diff
@@ -66,15 +66,29 @@ func (m *pendingWorkloadsInCqREST) Get(ctx context.Context, name string, opts ru
 	offset := pendingWorkloadOpts.Offset
 
 	wls := make([]v1alpha1.PendingWorkload, 0, limit)
-	allPendingWorkloads := m.queueMgr.PendingWorkloadsInfo(name)
-	for index := offset; index < offset+limit && index < int64(len(allPendingWorkloads)); index++ {
-		wlInfo := allPendingWorkloads[index]
-		wls = append(wls, v1alpha1.PendingWorkload{
-			ObjectMeta: metav1.ObjectMeta{
-				Name:      wlInfo.Name,
-				Namespace: wlInfo.Namespace,
-			},
-		})
+	pendingWorkloadsInfo := m.queueMgr.PendingWorkloadsInfo(name)
+	localQueuePositions := make(map[string]int32, 0)
```

  Note that if LocalQueues are empty we would be over-allocating here. Also, in order to make sure the number of LocalQueues is accurate, we would need to record their number during the read lock. Given  the trade-off is not clear, and would suggest leaving as is.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-27T10:46:16Z

- **apis/visibility/v1alpha1/types.go:37** by [@mimowo](https://github.com/mimowo) — 2023-11-27T10:46:16Z
```diff
@@ -27,6 +27,18 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	// Priority indicates the workload's priority
+	Priority int32 `json:"priority"`
+
+	// LocalQueueName indicates the name of the LocalQueue the workload is submitted to
+	LocalQueueName string `json:"localQueueName"`
+
+	// PositionInClusterQueue indicates the workload's position in the ClusterQueue starting from 0
```

  ```suggestion
  	// PositionInClusterQueue indicates the workload's position in the ClusterQueue, starting from 1
  ```
  I'm leaning to "Position" starting from 1 `1`:) , because this is a user-facing API (and "1" seems more natural to the user). I would be leaning towards 0 if this was "Index".

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-27T11:04:04Z

- **apis/visibility/v1alpha1/types.go:37** by [@PBundyra](https://github.com/PBundyra) — 2023-11-27T11:04:04Z
```diff
@@ -27,6 +27,18 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	// Priority indicates the workload's priority
+	Priority int32 `json:"priority"`
+
+	// LocalQueueName indicates the name of the LocalQueue the workload is submitted to
+	LocalQueueName string `json:"localQueueName"`
+
+	// PositionInClusterQueue indicates the workload's position in the ClusterQueue starting from 0
```

  Given that we start `Offset` parameter from 0 , I'm thinking whether it is more natural to start counting positions from 0  as well

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-27T11:15:42Z

- **apis/visibility/v1alpha1/types.go:37** by [@mimowo](https://github.com/mimowo) — 2023-11-27T11:15:41Z
```diff
@@ -27,6 +27,18 @@ import (
 type PendingWorkload struct {
 	metav1.TypeMeta   `json:",inline"`
 	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	// Priority indicates the workload's priority
+	Priority int32 `json:"priority"`
+
+	// LocalQueueName indicates the name of the LocalQueue the workload is submitted to
+	LocalQueueName string `json:"localQueueName"`
+
+	// PositionInClusterQueue indicates the workload's position in the ClusterQueue starting from 0
```

  Indeed we say in the KEP that offset starts from 0: `offset indicates position of the first fetched workload - default: 0`. In that case, I'm ok to leave it as is in this PR, but we could consider renaming in a follow up while the API is still alpha, based on user feedback.
