# PR #1373: Improve pod/wl cleanup for pod groups

**Summary**: Improve pod/wl cleanup for pod groups

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1373

**Last updated**: 2023-11-29T20:49:56Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@achernevskii](https://github.com/achernevskii)
- **Branch**: `fix/pod_groups_cleanup` → `main`
- **Created**: 2023-11-28T17:12:49Z
- **Updated**: 2023-11-29T20:49:56Z
- **Merged**: 2023-11-29T20:43:40Z
- **Closed**: 2023-11-29T20:43:41Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 8   **+123 / -47**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug

<!--
Add one of the following kinds:

/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

* Add deletingWorkload for the Stop method of the JobWithCustomStop interface.

* Change Stop method for the pod controller to delete even suspended pods if workload is being deleted.

* Change pods in group finalization for the case of workload deletion. Now all pods in the group will be finalized right after the pod group has been stopped.

* Add new unit tests for the pod controller and webhook.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Related issue: #976

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-28T17:12:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 19445a6833f0ca59635cff084ae313102873fd70 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65679b535a36a20008483553 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T20:38:08Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-29T20:38:14Z

LGTM label has been added.  <details>Git tree hash: 25a77a79d3fdd1cc1869640431c4fe0b5e7437a3</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-29T20:38:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1373#" title="Author self-approved">achernevskii</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1373#issuecomment-1832664969" title="Approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-28T18:30:02Z

- **pkg/controller/jobframework/interface.go:68** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T18:15:43Z
```diff
@@ -65,8 +65,9 @@ type JobWithReclaimablePods interface {
 type JobWithCustomStop interface {
 	// Stop implements a custom stop procedure.
 	// The function should be idempotent: not do any API calls if the job is already stopped.
+	// deletingWorkload argument should be true, if respective workload is being deleted.
```

  ```suggestion
  	// deletingWorkload argument is true, if the respective workload is being deleted.
  ```

- **pkg/controller/jobframework/reconciler.go:604** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T18:16:18Z
```diff
@@ -601,10 +601,13 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 	info := GetPodSetsInfoFromWorkload(wl)
 
 	if jws, implements := job.(JobWithCustomStop); implements {
-		stoppedNow, err := jws.Stop(ctx, r.client, info, eventMsg)
+		deletingWorkload := wl != nil && !wl.ObjectMeta.DeletionTimestamp.IsZero()
```

  ```suggestion
  		deletingWorkload := wl != nil || !wl.ObjectMeta.DeletionTimestamp.IsZero()
  ```

- **pkg/controller/jobs/pod/pod_controller.go:262** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T18:19:14Z
```diff
@@ -258,7 +259,7 @@ func (p *Pod) Stop(ctx context.Context, c client.Client, _ []podset.PodSetInfo,
 	}
 
 	for i := range podsInGroup {
-		if podSuspended(&podsInGroup[i]) || !podsInGroup[i].DeletionTimestamp.IsZero() {
+		if !deletingWorkload && (podSuspended(&podsInGroup[i]) || !podsInGroup[i].DeletionTimestamp.IsZero()) {
```

  ```suggestion
  		if !podsInGroup[i].DeletionTimestamp.IsZero() || (!deletingWorkload && podSuspended(&podsInGroup[i]))  {
  ```
  
  If the Pod already has a deletion timestamp, no need to call delete on it again.

- **pkg/controller/jobs/pod/pod_controller.go:473** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T18:25:23Z
```diff
@@ -485,6 +469,11 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if !p.isGroup {
 		podSets = p.PodSets()
 	} else {
+		// If the whole group has finished, don't create a workload
+		if _, finished := p.Finished(); finished {
```

  I don't think this behavior is special for pod groups.
  If any job is finished, we shouldn't be creating a Workload.
  
  I think this should have been covered here https://github.com/kubernetes-sigs/kueue/blob/881288427cdbf94b24c5b56654287d02a3272ae1/pkg/controller/jobframework/reconciler.go#L453
  
  Can you debug why it's not?

- **pkg/controller/jobs/pod/pod_controller_test.go:1280** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T18:28:49Z
```diff
@@ -1275,7 +1277,7 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
-		"deleted pods in group should not be finalized if matching workload is not found": {
+		"pods in group should not be finalized if matching workload is not found": {
```

  This test case shouldn't be changing.
  
  In this case, we are successfully updating a Workload to match the group. The Pods should not be affected.

- **pkg/controller/jobs/pod/pod_controller_test.go:1415** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T18:29:59Z
```diff
@@ -1406,10 +1419,56 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"if a pod group is finished and the wl is deleted, new workload shouldn't be created": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+			},
+			workloads:       []kueue.Workload{},
+			wantWorkloads:   []kueue.Workload{},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
 	}
 
 	for name, tc := range testCases {
 		t.Run(name, func(t *testing.T) {
+			if tc.wantErrs != nil && len(tc.wantErrs) != len(tc.pods) {
```

  use `cmp.Diff` with `cmpopts.EquateErrors`

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-11-28T18:33:24Z

- **pkg/controller/jobs/pod/pod_controller.go:262** by [@achernevskii](https://github.com/achernevskii) — 2023-11-28T18:33:23Z
```diff
@@ -258,7 +259,7 @@ func (p *Pod) Stop(ctx context.Context, c client.Client, _ []podset.PodSetInfo,
 	}
 
 	for i := range podsInGroup {
-		if podSuspended(&podsInGroup[i]) || !podsInGroup[i].DeletionTimestamp.IsZero() {
+		if !deletingWorkload && (podSuspended(&podsInGroup[i]) || !podsInGroup[i].DeletionTimestamp.IsZero()) {
```

  Don't we need the new pod condition even if it's been deleted before?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-28T18:36:12Z

- **pkg/controller/jobs/pod/pod_controller.go:262** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T18:36:12Z
```diff
@@ -258,7 +259,7 @@ func (p *Pod) Stop(ctx context.Context, c client.Client, _ []podset.PodSetInfo,
 	}
 
 	for i := range podsInGroup {
-		if podSuspended(&podsInGroup[i]) || !podsInGroup[i].DeletionTimestamp.IsZero() {
+		if !deletingWorkload && (podSuspended(&podsInGroup[i]) || !podsInGroup[i].DeletionTimestamp.IsZero()) {
```

  Not necessary. The Pod condition should reflect why it was initially deleted. If it wasn't Kueue that deleted the Pod initially, there is no need to override.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-11-28T19:04:40Z

- **pkg/controller/jobs/pod/pod_controller_test.go:1415** by [@achernevskii](https://github.com/achernevskii) — 2023-11-28T19:04:40Z
```diff
@@ -1406,10 +1419,56 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"if a pod group is finished and the wl is deleted, new workload shouldn't be created": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+			},
+			workloads:       []kueue.Workload{},
+			wantWorkloads:   []kueue.Workload{},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
 	}
 
 	for name, tc := range testCases {
 		t.Run(name, func(t *testing.T) {
+			if tc.wantErrs != nil && len(tc.wantErrs) != len(tc.pods) {
```

  I'm not comparing the errors here. Just checking if `wantErrs`slice is the same size as `pods`.
  
  The actual error comparison happens here:
  https://github.com/epam/kubernetes-kueue/blob/fix/pod_groups_cleanup/pkg/controller/jobs/pod/pod_controller_test.go#L1519-L1528

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-11-28T19:06:33Z

- **pkg/controller/jobs/pod/pod_controller_test.go:1280** by [@achernevskii](https://github.com/achernevskii) — 2023-11-28T19:06:33Z
```diff
@@ -1275,7 +1277,7 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
-		"deleted pods in group should not be finalized if matching workload is not found": {
+		"pods in group should not be finalized if matching workload is not found": {
```

  Should that happen even if the pods has a deletion timestamp?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-28T19:16:38Z

- **pkg/controller/jobs/pod/pod_controller_test.go:1280** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T19:16:38Z
```diff
@@ -1275,7 +1277,7 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
-		"deleted pods in group should not be finalized if matching workload is not found": {
+		"pods in group should not be finalized if matching workload is not found": {
```

  If not deleted, the Pods should definitely not be terminated.
  
  If deleted, we shouldn't add the condition, because it wasn't Kueue that deleted it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-28T19:17:45Z

- **pkg/controller/jobs/pod/pod_controller_test.go:1415** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T19:17:45Z
```diff
@@ -1406,10 +1419,56 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"if a pod group is finished and the wl is deleted, new workload shouldn't be created": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					StatusPhase(corev1.PodSucceeded).
+					Obj(),
+			},
+			workloads:       []kueue.Workload{},
+			wantWorkloads:   []kueue.Workload{},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
 	}
 
 	for name, tc := range testCases {
 		t.Run(name, func(t *testing.T) {
+			if tc.wantErrs != nil && len(tc.wantErrs) != len(tc.pods) {
```

  ah I see.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-28T19:19:45Z

- **pkg/controller/jobs/pod/pod_webhook_test.go:343** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T19:19:45Z
```diff
@@ -282,6 +282,67 @@ func TestDefault(t *testing.T) {
 	}
 }
 
+func TestGetRoleHash(t *testing.T) {
+	testCases := map[string]struct {
+		pods []*Pod
+		// If true, hash for all the pods in test should be equal
+		wantEqualHash bool
+		wantErr       error
+	}{
+		"kueue.x-k8s.io/* labels shouldn't affect the role": {
+			pods: []*Pod{
+				{pod: *testingpod.MakePod("pod1", "test-ns").
+					Label("kueue.x-k8s.io/managed", "true").
+					Obj()},
+				{pod: *testingpod.MakePod("pod2", "test-ns").
+					Obj()},
+			},
+			wantEqualHash: true,
+		},
+		"volume name shouldn't affect the role": {
+			pods: []*Pod{
+				{pod: *testingpod.MakePod("pod1", "test-ns").
+					Volume(corev1.Volume{
+						Name: "volume1",
+					}).
+					Obj()},
+				{pod: *testingpod.MakePod("pod1", "test-ns").
+					Volume(corev1.Volume{
+						Name: "volume2",
+					}).
+					Obj()},
+			},
+			wantEqualHash: true,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+
+			var previousHash string
+			for i := range tc.pods {
+				hash, err := getRoleHash(tc.pods[i])
+
+				if diff := cmp.Diff(tc.wantErr, err); diff != "" {
+					t.Errorf("Unexpected error (-want,+got):\n%s", diff)
+				}
+
+				if previousHash != "" {
+					if tc.wantEqualHash {
+						if previousHash != hash {
+							t.Errorf("Hash of pod shapes shouldn't be different %s!=%s", previousHash, hash)
+						}
+					} else {
+						if previousHash == hash {
+							t.Errorf("Hash of pod shapes shouldn't be equal %s==%s", previousHash, hash)
+						}
+					}
+				}
+			}
```

  `previousHash = hash` somewhere here

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-11-28T19:26:04Z

- **pkg/controller/jobs/pod/pod_controller.go:473** by [@achernevskii](https://github.com/achernevskii) — 2023-11-28T19:26:04Z
```diff
@@ -485,6 +469,11 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if !p.isGroup {
 		podSets = p.PodSets()
 	} else {
+		// If the whole group has finished, don't create a workload
+		if _, finished := p.Finished(); finished {
```

  If a pod succeeds, it's considered suspended, so the reconciler won't go into that branch:
  https://github.com/kubernetes-sigs/kueue/blob/881288427cdbf94b24c5b56654287d02a3272ae1/pkg/controller/jobframework/reconciler.go#L443
  
  Let me see what we can do about that.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-28T19:37:57Z

- **pkg/controller/jobs/pod/pod_controller.go:473** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-28T19:37:57Z
```diff
@@ -485,6 +469,11 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if !p.isGroup {
 		podSets = p.PodSets()
 	} else {
+		// If the whole group has finished, don't create a workload
+		if _, finished := p.Finished(); finished {
```

  Uhm... I guess most jobs are not "suspended" when they are finished.
  
  We might need to modify the logic in the generic reconciler a little to always finalize jobs and return when the Workload is finished.
  
  Also, no need to return an error in this case, even if the error is not retriable.
  Error means that something went wrong. Nothing went wrong here, we just don't want to do anything, and that can be done with a simple `return ctrl.Result{}, nil`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T19:50:20Z

- **pkg/controller/jobs/pod/pod_controller.go:55** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T19:12:34Z
```diff
@@ -52,6 +52,7 @@ const (
 	gateNotFound                   = -1
 	ConditionTypeTerminationTarget = "TerminationTarget"
 	errMsgIncorrectGroupRoleCount  = "pod group can't include more than 8 roles"
+	errMsgPodGroupFinished         = "pod group has already finished, skipping workload creation"
```

  this is a left over, isn't it?

- **pkg/controller/jobs/pod/pod_controller.go:262** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T19:18:32Z
```diff
@@ -257,8 +258,14 @@ func (p *Pod) Stop(ctx context.Context, c client.Client, _ []podset.PodSetInfo,
 		podsInGroup = []corev1.Pod{p.pod}
 	}
 
+	// Pods shouldn't be stopped if matching workload is not found,
+	// since stop will delete the pods.
+	if _, finished := p.Finished(); stopReason == jobframework.StopReasonNoMatchingWorkload && !finished {
+		return false, nil
+	}
+
 	for i := range podsInGroup {
-		if podSuspended(&podsInGroup[i]) || !podsInGroup[i].DeletionTimestamp.IsZero() {
+		if !podsInGroup[i].DeletionTimestamp.IsZero() || (stopReason != jobframework.StopReasonWorkloadDeleted && podSuspended(&podsInGroup[i])) {
```

  Add a comment for this:
  
  If the workload is being deleted, delete even finished Pods.

- **pkg/controller/jobs/pod/pod_controller.go:265** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T19:50:10Z
```diff
@@ -257,8 +258,14 @@ func (p *Pod) Stop(ctx context.Context, c client.Client, _ []podset.PodSetInfo,
 		podsInGroup = []corev1.Pod{p.pod}
 	}
 
+	// Pods shouldn't be stopped if matching workload is not found,
+	// since stop will delete the pods.
+	if _, finished := p.Finished(); stopReason == jobframework.StopReasonNoMatchingWorkload && !finished {
+		return false, nil
+	}
```

  Let's remove this for now and focus on the case where the Workload was explicitly deleted.
