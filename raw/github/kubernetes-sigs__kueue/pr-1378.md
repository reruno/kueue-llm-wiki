# PR #1378: Fill OwnerReferences field in PendingWorkload's metadata, so a user can easily identify the Job

**Summary**: Fill OwnerReferences field in PendingWorkload's metadata, so a user can easily identify the Job

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1378

**Last updated**: 2024-02-13T19:50:09Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `lq` → `main`
- **Created**: 2023-11-29T10:52:30Z
- **Updated**: 2024-02-13T19:50:09Z
- **Merged**: 2023-12-01T10:20:41Z
- **Closed**: 2023-12-01T10:20:41Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/api-change`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 7   **+93 / -46**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
/kind api-change

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This PR continues the development of [KEP#168-2](https://github.com/kubernetes-sigs/kueue/pull/1300)

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-29T10:52:37Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 26e93e44512ff5cb97556ea6d1f68cad687ecf58 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6569ae5292df4d0008fa827b |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-29T10:52:40Z

Hi @PBundyra. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-11-29T10:52:49Z

/assign @mimowo 
/assign @alculquicondor 
/assign @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2023-11-29T12:13:29Z

/ok-to-test

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-11-30T11:31:04Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-30T16:48:48Z

LGTM label has been added.  <details>Git tree hash: 86fd6dc2819e4a6ffc4b8f2fa0507c1ce84703c9</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-01T09:53:50Z

/hold cancel
I think the comments are addressed. Please squash the commits.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-12-01T09:55:43Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-01T09:59:36Z

LGTM label has been added.  <details>Git tree hash: 2284a1d9122812dea95f7ef485eaaeed0bb22df5</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-01T09:59:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1378#pullrequestreview-1758300932" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1378#" title="Author self-approved">PBundyra</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1378#pullrequestreview-1759438574" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T19:50:04Z

/release-note-edit
```release-note
NONE
```

I don't think we need a dedicate note for this PR

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-29T14:43:29Z

- **keps/168-2-pending-workloads-visibility/README.md:225** by [@mimowo](https://github.com/mimowo) — 2023-11-29T14:39:06Z
```diff
@@ -222,6 +222,8 @@ type PendingWorkloadsSummary struct {
 }
 ```
 
+An user can easily identify the Job that is an owner of the pending workload. To enable it, the API uses `metav1.OwnerReferences` field to indicate the owner. 
```

  ```suggestion
  An user can easily identify the Job that is an owner of the pending workload. To enable it, the API uses `metav1.OwnerReferences` field to indicate the owner, typically the job created by the user. 
  ```

- **test/e2e/e2e_test.go:251** by [@mimowo](https://github.com/mimowo) — 2023-11-29T14:40:30Z
```diff
@@ -242,8 +245,12 @@ var _ = ginkgo.Describe("Kueue visibility server", func() {
 					info, err := visibilityClient.ClusterQueues().GetPendingWorkloadsSummary(ctx, clusterQueue.Name, metav1.GetOptions{})
 					gomega.Expect(err).NotTo(gomega.HaveOccurred())
 					return info.Items
-					// We do not check Name as it's generated for workloads
-				}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(wantPendingWorkloads, cmpopts.IgnoreFields(metav1.ObjectMeta{}, "Name")))
+					// We do not check Name and OwnerReference's UID as they are generated
+				}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(
+					wantPendingWorkloads,
+					cmpopts.IgnoreFields(metav1.ObjectMeta{}, "Name"),
```

  Can we create the const / var for the `cmpopts` as pendingWorkloadsCmpOpts, then move the comment to the definition of the const, rather than duplicate.

- **test/e2e/e2e_test.go:906** by [@mimowo](https://github.com/mimowo) — 2023-11-29T14:42:44Z
```diff
@@ -867,3 +896,15 @@ func expectJobUnsuspendedWithNodeSelectors(key types.NamespacedName, ns map[stri
 		return []any{*job.Spec.Suspend, job.Spec.Template.Spec.NodeSelector}
 	}, util.Timeout, util.Interval).Should(gomega.Equal([]any{false, ns}))
 }
+
+func defaultOwnerReferenceForJob(name string) []metav1.OwnerReference {
+	return []metav1.OwnerReference{
+		{
+			APIVersion:         "batch/v1",
+			Kind:               "Job",
+			Name:               name,
+			Controller:         ptr.To(true),
```

  I'm not sure there is a point in returning the Controller or BlockOwnerDeletion fields? Maybe we could nil the fields, but I don't have a strong view. WDYT @alculquicondor ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-30T08:00:22Z

- **test/e2e/rbac.yaml:1** by [@mimowo](https://github.com/mimowo) — 2023-11-30T07:58:10Z
```diff
@@ -0,0 +1,47 @@
+# my-ksa.yaml
```

  Is this used?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-30T09:38:59Z

- **test/e2e/e2e_test.go:906** by [@mimowo](https://github.com/mimowo) — 2023-11-30T09:38:59Z
```diff
@@ -867,3 +896,15 @@ func expectJobUnsuspendedWithNodeSelectors(key types.NamespacedName, ns map[stri
 		return []any{*job.Spec.Suspend, job.Spec.Template.Spec.NodeSelector}
 	}, util.Timeout, util.Interval).Should(gomega.Equal([]any{false, ns}))
 }
+
+func defaultOwnerReferenceForJob(name string) []metav1.OwnerReference {
+	return []metav1.OwnerReference{
+		{
+			APIVersion:         "batch/v1",
+			Kind:               "Job",
+			Name:               name,
+			Controller:         ptr.To(true),
```

  I think in general we should try to keep the API surface minimal, and the fields don't seem to give any value for the responses in this endpoint. OTOH, just copying the owner references makes the code simpler.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-30T10:01:42Z

- **test/e2e/e2e_test.go:906** by [@mimowo](https://github.com/mimowo) — 2023-11-30T10:01:42Z
```diff
@@ -867,3 +896,15 @@ func expectJobUnsuspendedWithNodeSelectors(key types.NamespacedName, ns map[stri
 		return []any{*job.Spec.Suspend, job.Spec.Template.Spec.NodeSelector}
 	}, util.Timeout, util.Interval).Should(gomega.Equal([]any{false, ns}))
 }
+
+func defaultOwnerReferenceForJob(name string) []metav1.OwnerReference {
+	return []metav1.OwnerReference{
+		{
+			APIVersion:         "batch/v1",
+			Kind:               "Job",
+			Name:               name,
+			Controller:         ptr.To(true),
```

  Second thought on this.
  
  I think we should try to make the API clean, rather than internal code. So I would have one of the two suggestions:
  1. clean up the `Controller` and `blockOwnerDeletion` fields, in the copy of the ownerReferences returned, because the values don't really make sense for the instances of `PendingWorkload`. In particular, `blockOwnerDeletion` does not seem to make sense
  2. introduce a dedicated type for the information we want to return. Maybe `OwnerInformation` / `JobInformation`, then only include the `APIVersion`, `Kind`, `Name` and `UID`.
  
  I'm don't have a strong preference, but slightly leaning towards (2.). ObjectReference has some concrete meaning in k8s, which we don't care about in this context, we just want to return the job-identifying information.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-30T15:05:47Z

- **test/e2e/e2e_test.go:906** by [@PBundyra](https://github.com/PBundyra) — 2023-11-30T15:05:47Z
```diff
@@ -867,3 +896,15 @@ func expectJobUnsuspendedWithNodeSelectors(key types.NamespacedName, ns map[stri
 		return []any{*job.Spec.Suspend, job.Spec.Template.Spec.NodeSelector}
 	}, util.Timeout, util.Interval).Should(gomega.Equal([]any{false, ns}))
 }
+
+func defaultOwnerReferenceForJob(name string) []metav1.OwnerReference {
+	return []metav1.OwnerReference{
+		{
+			APIVersion:         "batch/v1",
+			Kind:               "Job",
+			Name:               name,
+			Controller:         ptr.To(true),
```

  After discussion with @alculquicondor @mimowo we decided to go with the first option. We think it's a better option to reuse existing APIs and keep them minimalistic.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-30T16:04:47Z

- **test/e2e/e2e_test.go:53** by [@mimowo](https://github.com/mimowo) — 2023-11-30T16:01:31Z
```diff
@@ -43,6 +44,16 @@ import (
 
 var _ = ginkgo.Describe("Kueue visibility server", func() {
 	const defaultFlavor = "default-flavor"
+
+	// We do not check Name and OwnerReference's UID as they are generated
+	// We do not check Controller and BlockOwnerDeletion as we intentionally leave it empty
+	var pendingWorkloadsCmpOpts = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.ObjectMeta{}, "Name"),
+		cmpopts.IgnoreFields(metav1.OwnerReference{}, "UID"),
+		cmpopts.IgnoreFields(metav1.OwnerReference{}, "Controller"),
```

  Is it needed in the ignored fields since we set it to nil ? I guess it makes some value to test it is nil

- **pkg/visibility/api/rest/pending_workloads_cq.go:80** by [@mimowo](https://github.com/mimowo) — 2023-11-30T16:04:36Z
```diff
@@ -77,11 +77,21 @@ func (m *pendingWorkloadsInCqREST) Get(ctx context.Context, name string, opts ru
 		localQueuePositions[queueName]++
 
 		if index >= int(offset) {
+			ownerReferences := make([]metav1.OwnerReference, 0, len(wlInfo.Obj.OwnerReferences))
```

  I would suggest to have a utility function in the package to covert a workload into a pendingworkload. Maybe `buildPendingWorkload(wl, positionInClusterQueue, positionInLocalQueue)`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-30T16:48:43Z

/lgtm
Thanks!
/assign @alculquicondor @tenzen-y

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-30T17:28:07Z

- **pkg/visibility/api/rest/utils.go:24** by [@mimowo](https://github.com/mimowo) — 2023-11-30T17:28:04Z
```diff
@@ -0,0 +1,45 @@
+// Copyright 2023 The Kubernetes Authors.
+//
+// Licensed under the Apache License, Version 2.0 (the "License");
+// you may not use this file except in compliance with the License.
+// You may obtain a copy of the License at
+//
+//     http://www.apache.org/licenses/LICENSE-2.0
+//
+// Unless required by applicable law or agreed to in writing, software
+// distributed under the License is distributed on an "AS IS" BASIS,
+// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+// See the License for the specific language governing permissions and
+// limitations under the License.
+
+package rest
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func newPendingWorkload(wlInfo *workload.Info, positionInLq int32, positionInCq int, queueName string) *v1alpha1.PendingWorkload {
```

  We don't need to pass queueName, since it is `wlInfo.Obj.Spec.QueueName`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-30T19:47:38Z

/approve
/hold
for review comments

- **keps/168-2-pending-workloads-visibility/README.md:225** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-30T19:32:42Z
```diff
@@ -222,6 +222,8 @@ type PendingWorkloadsSummary struct {
 }
 ```
 
+An user can easily identify the Job that is an owner of the pending workload. To enable it, the API uses `metav1.OwnerReferences` field to indicate the owner, typically the job created by the user. 
```

  ```suggestion
  A user can easily identify the Job that is an owner of the pending workload. To enable it, the API uses `metav1.OwnerReferences` field to indicate the owner, typically the job created by the user. 
  ```
  
  Yes, English is weird :)

- **pkg/visibility/api/rest/pending_workloads_cq_test.go:159** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-30T19:34:17Z
```diff
@@ -149,8 +154,9 @@ func TestPendingWorkloadsInCQ(t *testing.T) {
 				},
 				{
 					ObjectMeta: v1.ObjectMeta{
-						Name:      "lqB-low-prio",
-						Namespace: nsName,
+						Name:            "lqB-low-prio",
+						Namespace:       nsName,
+						OwnerReferences: []metav1.OwnerReference{},
```

  you can use `cmpopts.EquateEmpty` to avoid this.
  
  Or... just don't instantiate a list that is empty.

- **pkg/visibility/api/rest/pending_workloads_lq_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-30T19:34:49Z
  same comment as the other test file.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-30T19:49:32Z

- **test/e2e/e2e_test.go:50** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-30T19:49:32Z
```diff
@@ -43,6 +44,14 @@ import (
 
 var _ = ginkgo.Describe("Kueue visibility server", func() {
 	const defaultFlavor = "default-flavor"
+
+	// We do not check Name and OwnerReference's UID as they are generated
+	// We do not check Controller and BlockOwnerDeletion as we intentionally leave it empty
+	var pendingWorkloadsCmpOpts = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.ObjectMeta{}, "Name"),
```

  what do you mean that the job name is generated?
  Also, shouldn't we check the Namespace?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-01T09:15:00Z

- **test/e2e/e2e_test.go:49** by [@mimowo](https://github.com/mimowo) — 2023-12-01T09:13:04Z
```diff
@@ -43,6 +44,14 @@ import (
 
 var _ = ginkgo.Describe("Kueue visibility server", func() {
 	const defaultFlavor = "default-flavor"
+
+	// We do not check Name and OwnerReference's UID as they are generated
+	// We do not check Controller and BlockOwnerDeletion as we intentionally leave it empty
```

  This comment `// We do not check Controller and BlockOwnerDeletion as we intentionally leave it empty` is not necessary and misleading. We do check that the fields are nil.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-12-01T09:15:10Z

- **test/e2e/e2e_test.go:50** by [@PBundyra](https://github.com/PBundyra) — 2023-12-01T09:15:10Z
```diff
@@ -43,6 +44,14 @@ import (
 
 var _ = ginkgo.Describe("Kueue visibility server", func() {
 	const defaultFlavor = "default-flavor"
+
+	// We do not check Name and OwnerReference's UID as they are generated
+	// We do not check Controller and BlockOwnerDeletion as we intentionally leave it empty
+	var pendingWorkloadsCmpOpts = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.ObjectMeta{}, "Name"),
```

  I meant that the Workload's name is generated based on Job's name - [we discussed it with Michal here](https://github.com/kubernetes-sigs/kueue/pull/1362#issuecomment-1824749795).
  
  > Also, shouldn't we check the Namespace?
  
   I believe we do check the Namespace in e2e tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-01T09:31:36Z

- **test/e2e/e2e_test.go:50** by [@mimowo](https://github.com/mimowo) — 2023-12-01T09:31:36Z
```diff
@@ -43,6 +44,14 @@ import (
 
 var _ = ginkgo.Describe("Kueue visibility server", func() {
 	const defaultFlavor = "default-flavor"
+
+	// We do not check Name and OwnerReference's UID as they are generated
+	// We do not check Controller and BlockOwnerDeletion as we intentionally leave it empty
+	var pendingWorkloadsCmpOpts = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.ObjectMeta{}, "Name"),
```

  Maybe to clarify we can say `// We do not check workload's Name and its OwnerReference's UID as they are generated at the server-side.`.
  
  > I believe we do check the Namespace in e2e tests.
  
  Yes, we check the namespace of the workload. We do not check the  ownerReference's namespace, because it does not have the `Namespace` field. The owner and the owned object are assumed in the same namespace.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-12-01T09:36:12Z

- **test/e2e/e2e_test.go:50** by [@PBundyra](https://github.com/PBundyra) — 2023-12-01T09:36:12Z
```diff
@@ -43,6 +44,14 @@ import (
 
 var _ = ginkgo.Describe("Kueue visibility server", func() {
 	const defaultFlavor = "default-flavor"
+
+	// We do not check Name and OwnerReference's UID as they are generated
+	// We do not check Controller and BlockOwnerDeletion as we intentionally leave it empty
+	var pendingWorkloadsCmpOpts = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.ObjectMeta{}, "Name"),
```

  Oh I see, I've improved the comment

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-01T09:59:30Z

/lgtm
/approve
