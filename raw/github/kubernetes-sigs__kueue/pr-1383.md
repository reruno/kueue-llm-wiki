# PR #1383: Update wl/job handling for the case, when the job is finished

**Summary**: Update wl/job handling for the case, when the job is finished

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1383

**Last updated**: 2024-01-11T16:50:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@achernevskii](https://github.com/achernevskii)
- **Branch**: `fix/finished_job_handling` → `main`
- **Created**: 2023-11-30T02:02:49Z
- **Updated**: 2024-01-11T16:50:14Z
- **Merged**: 2023-12-01T21:47:32Z
- **Closed**: 2023-12-01T21:47:32Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Changed files**: 5   **+140 / -12**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug

<!--
Add one of the following kinds:

/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

* Workload is not being recreated, when the job is finished.

* Job will be stopped if it's not suspended and matching workload is not found. This action doesn't depend on the job.Finished condition anymore.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->


#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Avoid recreating a Workload for a finished Job and finalize a job when the workload is declared finished.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-11-30T02:02:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9eb11c5a37c95a5fedadf93302de52380e4d411f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/656a47ba7af24100089af66b |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-11-30T05:43:12Z

@achernevskii Why do we need this change? I couldn't see what is bug.

### Comment by [@achernevskii](https://github.com/achernevskii) — 2023-11-30T16:48:36Z

> @achernevskii Why do we need this change? I couldn't see what is bug.

It's not a bug. The behavior has changed since `v0.4.x` in those two PRs:
https://github.com/kubernetes-sigs/kueue/pull/1072/files#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR431
https://github.com/kubernetes-sigs/kueue/pull/1033/files#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR186
And now workload is being recreated after the job is `Finished`.

After this discussion I've decided to go back to the original reconciler logic:
https://github.com/kubernetes-sigs/kueue/pull/1373#discussion_r1408298072

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-01T06:45:51Z

> > @achernevskii Why do we need this change? I couldn't see what is bug.
> 
> It's not a bug. The behavior has changed since `v0.4.x` in those two PRs: https://github.com/kubernetes-sigs/kueue/pull/1072/files#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR431 https://github.com/kubernetes-sigs/kueue/pull/1033/files#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR186 And now workload is being recreated after the job is `Finished`.
> 
> After this discussion I've decided to go back to the original reconciler logic: [#1373 (comment)](https://github.com/kubernetes-sigs/kueue/pull/1373#discussion_r1408298072)

That makes sense. Thanks for the clarification.

/remove-kind-bug
/kind cleanup

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-01T06:46:13Z

/remove-kind bug

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-01T21:44:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1383#" title="Author self-approved">achernevskii</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1383#pullrequestreview-1760607810" title="Approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-01T21:44:15Z

LGTM label has been added.  <details>Git tree hash: e01b5e0a14ad32dc03cc44e76754e9a23505bec4</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-11T16:48:47Z

/kind bug

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-11T16:48:55Z

/remove-kind cleanup

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-11T16:50:09Z

/release-note-edit
```release-note
Avoid recreating a Workload for a finished Job and finalize a job when the workload is declared finished.
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-30T21:46:45Z

- **pkg/controller/jobframework/reconciler.go:454** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-30T21:45:14Z
```diff
@@ -449,15 +451,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			// than one workload...
 			w = toDelete[0]
 		}
-
-		if _, finished := job.Finished(); finished {
-			if err := r.finalizeJob(ctx, job); err != nil {
-				return nil, fmt.Errorf("finalizing job with no matching workload: %w", err)
-			}
-		} else {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
-				return nil, fmt.Errorf("stopping job with no matching workload: %w", err)
-			}
+		if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
```

  I think we should only call stop if it's not already finished. Otherwise it's just a waste of API calls.
  
  Is there a case where we need this for pod groups? It doesn't look like it.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-11-30T22:14:36Z

- **pkg/controller/jobframework/reconciler.go:454** by [@achernevskii](https://github.com/achernevskii) — 2023-11-30T22:14:32Z
```diff
@@ -449,15 +451,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			// than one workload...
 			w = toDelete[0]
 		}
-
-		if _, finished := job.Finished(); finished {
-			if err := r.finalizeJob(ctx, job); err != nil {
-				return nil, fmt.Errorf("finalizing job with no matching workload: %w", err)
-			}
-		} else {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
-				return nil, fmt.Errorf("stopping job with no matching workload: %w", err)
-			}
+		if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
```

  Hypothetically, a job could be finished but unsuspended. I don't think it's possible for any of the existing integrations, but it could be possible from the `GeneticJob` interface perspective. 
  
  If we're trying to minimize the number of API calls in this case, shouldn't we call `IsSuspended` here instead of `Finished`?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-30T22:38:28Z

- **pkg/controller/jobframework/reconciler.go:454** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-30T22:38:28Z
```diff
@@ -449,15 +451,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			// than one workload...
 			w = toDelete[0]
 		}
-
-		if _, finished := job.Finished(); finished {
-			if err := r.finalizeJob(ctx, job); err != nil {
-				return nil, fmt.Errorf("finalizing job with no matching workload: %w", err)
-			}
-		} else {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
-				return nil, fmt.Errorf("stopping job with no matching workload: %w", err)
-			}
+		if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
```

  IsSuspended is just about the suspend field. It would still return false for the job if it's finished but the suspend field is false. That's why we were still checking for IsFinished as well.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-11-30T23:51:49Z

- **pkg/controller/jobframework/reconciler.go:454** by [@achernevskii](https://github.com/achernevskii) — 2023-11-30T23:50:23Z
```diff
@@ -449,15 +451,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			// than one workload...
 			w = toDelete[0]
 		}
-
-		if _, finished := job.Finished(); finished {
-			if err := r.finalizeJob(ctx, job); err != nil {
-				return nil, fmt.Errorf("finalizing job with no matching workload: %w", err)
-			}
-		} else {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
-				return nil, fmt.Errorf("stopping job with no matching workload: %w", err)
-			}
+		if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
```

  So `Finished==true` will always mean that no pods related to the job are active? 
  
  For the current reconciler, workload will be recreated. So if a job is finished, but not suspended, and new workload is not admitted, the job will be stopped:
  https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/reconciler.go#L367
  
  With this implementation we have to be sure that the job is stopped when we find out that workload is not found.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-01T07:04:15Z

- **pkg/controller/jobframework/reconciler.go:454** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-01T07:04:15Z
```diff
@@ -449,15 +451,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			// than one workload...
 			w = toDelete[0]
 		}
-
-		if _, finished := job.Finished(); finished {
-			if err := r.finalizeJob(ctx, job); err != nil {
-				return nil, fmt.Errorf("finalizing job with no matching workload: %w", err)
-			}
-		} else {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
-				return nil, fmt.Errorf("stopping job with no matching workload: %w", err)
-			}
+		if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
```

  > So Finished==true will always mean that no pods related to the job are active?
  
  IIUC, `Finished==true` means that the job has succeeded or failed regardless of being suspended.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-01T16:08:09Z

- **pkg/controller/jobframework/reconciler.go:454** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-01T16:08:09Z
```diff
@@ -449,15 +451,8 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 			// than one workload...
 			w = toDelete[0]
 		}
-
-		if _, finished := job.Finished(); finished {
-			if err := r.finalizeJob(ctx, job); err != nil {
-				return nil, fmt.Errorf("finalizing job with no matching workload: %w", err)
-			}
-		} else {
-			if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
-				return nil, fmt.Errorf("stopping job with no matching workload: %w", err)
-			}
+		if err := r.stopJob(ctx, job, w, StopReasonNoMatchingWorkload, "No matching Workload"); err != nil {
```

  > So `Finished==true` will always mean that no pods related to the job are active?
  
  It should, unless the job implemented IsFinished wrong.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-01T16:21:19Z

- **pkg/controller/jobframework/reconciler.go:267** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-01T16:10:44Z
```diff
@@ -258,15 +258,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	// 2. handle job is finished.
-	if condition, finished := job.Finished(); finished && wl != nil {
+	if condition, finished := job.Finished(); finished {
 		// Execute job finalization logic
 		if err := r.finalizeJob(ctx, job); err != nil {
 			return ctrl.Result{}, err
 		}
 
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
+		if wl != nil && !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+			err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
+			if err != nil {
+				log.Error(err, "Updating workload status")
+			}
```

  🤔 this might have escaped previous reviews.
  
  If the Workload update fails, we should return the error, so it can be reconciled again (except for NotFound errors).
  
  Another thing that escaped:
  
  Finalizing the job should happen after we update the Workload. Otherwise, the job and the Workload (which is a dependent object) could potentially be deleted before it was transitioned to Finished. This could be fine, but it's better to give complete information before removing the Workload, in case a user is watching for the changes.

- **pkg/controller/jobs/job/job_controller_test.go:1463** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-01T16:18:55Z
```diff
@@ -1460,6 +1460,16 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload shouldn't be recreated for the completed job, the job should be suspended": {
```

  ```suggestion
  		"the workload shouldn't be recreated for the completed job": {
  ```

- **pkg/controller/jobs/kubeflow/jobs/mxjob/mxjob_controller_test.go:410** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-01T16:19:16Z
```diff
@@ -407,6 +407,18 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"workload shouldn't be recreated for the completed mx job, the mx job should be suspended": {
```

  ```suggestion
  		"workload shouldn't be recreated for the completed mx job": {
  ```

- **pkg/controller/jobs/pod/pod_controller_test.go:1411** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-01T16:21:14Z
```diff
@@ -1408,6 +1408,47 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"if a pod group is finished and the wl is deleted, new workload shouldn't be created": {
```

  can we add another one (if it isn't there)?
  
  If a pod group is not finished (for example, it has a non-terminated gated Pod), a workload should be recreated.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-12-01T19:40:49Z

- **pkg/controller/jobs/pod/pod_controller_test.go:1411** by [@achernevskii](https://github.com/achernevskii) — 2023-12-01T19:40:48Z
```diff
@@ -1408,6 +1408,47 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"if a pod group is finished and the wl is deleted, new workload shouldn't be created": {
```

  We're indirectly checking for this scenario here:
  https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobs/pod/pod_controller_test.go#L1280
  
  But I've added a test with gated pod anyway.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-12-01T19:45:41Z

- **pkg/controller/jobframework/reconciler.go:267** by [@achernevskii](https://github.com/achernevskii) — 2023-12-01T19:45:41Z
```diff
@@ -258,15 +258,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	// 2. handle job is finished.
-	if condition, finished := job.Finished(); finished && wl != nil {
+	if condition, finished := job.Finished(); finished {
 		// Execute job finalization logic
 		if err := r.finalizeJob(ctx, job); err != nil {
 			return ctrl.Result{}, err
 		}
 
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
+		if wl != nil && !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+			err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
+			if err != nil {
+				log.Error(err, "Updating workload status")
+			}
```

  Regarding the job finalization/wl status update order: https://github.com/kubernetes-sigs/kueue/pull/1177

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-01T20:34:02Z

- **pkg/controller/jobframework/reconciler.go:265** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-01T20:29:35Z
```diff
@@ -258,17 +258,20 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	}
 
 	// 2. handle job is finished.
-	if condition, finished := job.Finished(); finished && wl != nil {
+	if condition, finished := job.Finished(); finished {
+		if wl != nil && !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
+			err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
+			if err != nil && !apierrors.IsNotFound(err) {
+				log.Error(err, "Updating workload status")
```

  you can remove this line, as returned errors are logged as well.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-01T21:44:09Z

/lgtm
/approve
