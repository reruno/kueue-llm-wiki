# PR #1397: Support Preemption while borrowing

**Summary**: Support Preemption while borrowing

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1397

**Last updated**: 2024-02-12T20:56:29Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `preemption-with-borrowing` → `main`
- **Created**: 2023-12-04T09:49:04Z
- **Updated**: 2024-02-12T20:56:29Z
- **Merged**: 2024-01-05T18:13:58Z
- **Closed**: 2024-01-05T18:13:58Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 16   **+940 / -24**

## Description

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #1337

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Support for preemption while borrowing
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-12-04T09:49:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5b2ec29865b2584e70e8f9e8cecd181b470cd6a8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/659837f7fcd6870008b8b110 |

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-15T15:18:18Z

/test pull-kueue-test-integration-main

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-18T16:14:53Z

/assign @yaroslava-serdiuk

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-05T18:09:02Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-05T18:09:09Z

LGTM label has been added.  <details>Git tree hash: 8c988c2cf685a17850d5e3d9efa0cc27f8a24529</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-05T18:09:10Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1397#issuecomment-1879073135" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1397#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T20:56:27Z

/kind api-change

## Reviews

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-19T12:36:09Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:431** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-19T10:45:47Z
```diff
@@ -428,7 +428,7 @@ func (a *Assignment) findFlavorForResourceGroup(
 			if mode < representativeMode {
 				representativeMode = mode
 			}
-			needsBorrowing = needsBorrowing || (mode == Fit && borrow > 0)
+			needsBorrowing = needsBorrowing || ((mode == Fit || mode == Preempt) && borrow > 0)
```

  ```suggestion
  			needsBorrowing = needsBorrowing || borrow > 0
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:601** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-19T12:35:55Z
```diff
@@ -589,6 +598,9 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		}
 	}
 	status.append(msg)
+	if borrow := val - rQuota.Nominal; borrow > 0 && mode == Preempt {
```

  What does mean this condition?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-19T14:25:22Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:431** by [@mimowo](https://github.com/mimowo) — 2023-12-19T14:25:22Z
```diff
@@ -428,7 +428,7 @@ func (a *Assignment) findFlavorForResourceGroup(
 			if mode < representativeMode {
 				representativeMode = mode
 			}
-			needsBorrowing = needsBorrowing || (mode == Fit && borrow > 0)
+			needsBorrowing = needsBorrowing || ((mode == Fit || mode == Preempt) && borrow > 0)
```

  Thanks for this suggestion. It makes sense to simplify

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-19T17:08:06Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:601** by [@mimowo](https://github.com/mimowo) — 2023-12-19T17:08:06Z
```diff
@@ -589,6 +598,9 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		}
 	}
 	status.append(msg)
+	if borrow := val - rQuota.Nominal; borrow > 0 && mode == Preempt {
```

  Good question. I added it to return `borrow>0` when returning the `Preempt` mode, so that the `FlavorFungability` rules can react to it. 
  
  In particular, when borrowing & preemption is required, and `flavorFungability.whenCanBorrow=TryNextFlavor` and `flavorFungability.whenCanPreempt=Preempt`, then I think we should do `TryNextFlavor`, because preempting on its own would not be enough. If I returned `0` (and not done other modifications in this file), then we would do "Preempt" in this case. WDYT @alculquicondor ?
  
  Your comment made me also think:
  1. can the check be simplified? we probably don't need to check `mode == Preempt`
  2. I'm missing an analogous check here: https://github.com/kubernetes-sigs/kueue/blob/5fced9e6f0b1323153d2df65816e70a087c8f6f7/pkg/scheduler/flavorassigner/flavorassigner.go#L563
  I'm going to experiment with (1.) and (2.).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-20T10:01:32Z

- **test/integration/scheduler/preemption_test.go:576** by [@mimowo](https://github.com/mimowo) — 2023-12-20T10:01:32Z
```diff
@@ -355,4 +357,325 @@ var _ = ginkgo.Describe("Preemption", func() {
 		})
 	})
 
+	ginkgo.Context("When most quota is in a shared ClusterQueue in a cohort", func() {
+		var (
+			aStandardCQ, aBestEffortCQ, bStandardCQ, sharedCQ *kueue.ClusterQueue
+			aStandardLQ, aBestEffortLQ, bStandardLQ           *kueue.LocalQueue
+			oneFlavor, fallbackFlavor                         *kueue.ResourceFlavor
+		)
+
+		ginkgo.BeforeEach(func() {
+			oneFlavor = testing.MakeResourceFlavor("one").Obj()
+			gomega.Expect(k8sClient.Create(ctx, oneFlavor)).To(gomega.Succeed())
+
+			fallbackFlavor = testing.MakeResourceFlavor("fallback").Obj()
+			gomega.Expect(k8sClient.Create(ctx, fallbackFlavor)).To(gomega.Succeed())
+
+			aStandardCQ = testing.MakeClusterQueue("a-standard-cq").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("one").Resource(corev1.ResourceCPU, "1", "5").Obj(),
+					*testing.MakeFlavorQuotas("fallback").Resource(corev1.ResourceCPU, "10", "0").Obj(),
+				).
+				FlavorFungibility(kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				}).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+					BorrowWithinCohort: &kueue.BorrowWithinCohort{
+						Policy:               kueue.BorrowWithinCohortPolicyLowerPriority,
+						MaxPriorityThreshold: ptr.To(midPriority),
+					},
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardCQ)).To(gomega.Succeed())
+			aStandardLQ = testing.MakeLocalQueue("a-standard-lq", ns.Name).ClusterQueue(aStandardCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardLQ)).To(gomega.Succeed())
+
+			aBestEffortCQ = testing.MakeClusterQueue("a-best-effort-cq").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("one").Resource(corev1.ResourceCPU, "1", "5").Obj(),
+					*testing.MakeFlavorQuotas("fallback").Resource(corev1.ResourceCPU, "10", "0").Obj(),
+				).
+				FlavorFungibility(kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				}).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+					BorrowWithinCohort: &kueue.BorrowWithinCohort{
+						Policy: kueue.BorrowWithinCohortPolicyLowerPriority,
+					},
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortCQ)).To(gomega.Succeed())
+			aBestEffortLQ = testing.MakeLocalQueue("a-best-effort-lq", ns.Name).ClusterQueue(aBestEffortCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortLQ)).To(gomega.Succeed())
+
+			bStandardCQ = testing.MakeClusterQueue("b-standard-cq").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("one").Resource(corev1.ResourceCPU, "1", "5").Obj(),
+					*testing.MakeFlavorQuotas("fallback").Resource(corev1.ResourceCPU, "10", "0").Obj(),
+				).
+				FlavorFungibility(kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				}).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+					BorrowWithinCohort: &kueue.BorrowWithinCohort{
+						Policy:               kueue.BorrowWithinCohortPolicyLowerPriority,
+						MaxPriorityThreshold: ptr.To(midPriority),
+					},
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, bStandardCQ)).To(gomega.Succeed())
+			bStandardLQ = testing.MakeLocalQueue("b-standard-lq", ns.Name).ClusterQueue(bStandardCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, bStandardLQ)).To(gomega.Succeed())
+
+			sharedCQ = testing.MakeClusterQueue("shared-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("one").Resource(corev1.ResourceCPU, "5").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, sharedCQ)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, aStandardCQ, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, aBestEffortCQ, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, bStandardCQ, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, sharedCQ, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, oneFlavor, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, fallbackFlavor, true)
+		})
+
+		ginkgo.It("Should allow a higher-priority workload to preempt a lower priority workload", func() {
+			ginkgo.By("Create a low priority workload which requires borrowing")
+			aBestEffortLowWl := testing.MakeWorkload("a-best-effort-low", ns.Name).
+				Queue(aBestEffortLQ.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortLowWl)).To(gomega.Succeed())
+
+			ginkgo.By("Await for the a-best-effort-low workload to be admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aBestEffortLowWl,
+				testing.MakeAdmission(aBestEffortCQ.Name).Assignment(corev1.ResourceCPU, "one", "5").Obj(),
+			)
+
+			ginkgo.By("Create the a-standard-high workload")
+			aStandardHighWl := testing.MakeWorkload("a-standard-high", ns.Name).
+				Queue(aStandardLQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Finish eviction fo the a-best-effort-low workload")
+			util.FinishEvictionForWorkloads(ctx, k8sClient, aBestEffortLowWl)
+
+			ginkgo.By("Verify the a-standard-high workload is admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aStandardHighWl,
+				testing.MakeAdmission(aStandardCQ.Name).Assignment(corev1.ResourceCPU, "one", "5").Obj(),
+			)
+
+			ginkgo.By("Verify the a-best-effort-low workload is re-admitted, but using flavor 2")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aBestEffortLowWl,
+				testing.MakeAdmission(aBestEffortCQ.Name).Assignment(corev1.ResourceCPU, "fallback", "5").Obj(),
+			)
+		})
+
+		ginkgo.It("Should not allow a lower-priority workload to preempt a higher priority workload", func() {
+			ginkgo.By("Create a high priority workload which requires borrowing")
+			aStandardHighWl := testing.MakeWorkload("a-standard-high", ns.Name).
+				Queue(aStandardLQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Await for the a-standard-high workload to be admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aStandardHighWl,
+				testing.MakeAdmission(aStandardCQ.Name).Assignment(corev1.ResourceCPU, "one", "5").Obj(),
+			)
+
+			aBestEffortLowWl := testing.MakeWorkload("a-best-effort-low", ns.Name).
+				Queue(aBestEffortLQ.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortLowWl)).To(gomega.Succeed())
+
+			ginkgo.By("Verify the a-best-effort-low is admitted using the second flavor, as it couldn't preempt in the first")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aBestEffortLowWl,
+				testing.MakeAdmission(aBestEffortCQ.Name).Assignment(corev1.ResourceCPU, "fallback", "5").Obj(),
+			)
+		})
+
+		ginkgo.It("Should not allow a higher-priority workload to preempt a lower-priority workload, if above the threshold", func() {
+			ginkgo.By("Create a high priority workload which requires borrowing")
+			aStandardHighWl := testing.MakeWorkload("a-standard-high", ns.Name).
+				Queue(aStandardLQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Await for the a-standard-high workload to be admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aStandardHighWl,
+				testing.MakeAdmission(aStandardCQ.Name).Assignment(corev1.ResourceCPU, "one", "5").Obj(),
+			)
+
+			ginkgo.By("Create the b-standard-very-high workload")
+			bStandardVeryHighWl := testing.MakeWorkload("b-standard-very-high", ns.Name).
+				Queue(bStandardLQ.Name).
+				Priority(veryHighPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, bStandardVeryHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Verify the b-standard-very-high is admitted using the second flavor, as it couldn't preempt in the first")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, bStandardVeryHighWl,
+				testing.MakeAdmission(bStandardCQ.Name).Assignment(corev1.ResourceCPU, "fallback", "5").Obj(),
+			)
+		})
+
+		ginkgo.It("Should not allow a higher-priority workload to preempt a lower-priority workload, if the low priority one is not borrowing", func() {
+			ginkgo.By("Create a low priority workload which does not borrowing")
+			aBestEffortLowWl := testing.MakeWorkload("a-best-effort-low", ns.Name).
+				Queue(aBestEffortLQ.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortLowWl)).To(gomega.Succeed())
+
+			ginkgo.By("Await for the a-best-effort-low workload to be admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aBestEffortLowWl,
+				testing.MakeAdmission(aBestEffortCQ.Name).Assignment(corev1.ResourceCPU, "one", "1").Obj(),
+			)
+
+			ginkgo.By("Create the a-standard-high workload")
+			aStandardHighWl := testing.MakeWorkload("a-standard-high", ns.Name).
+				Queue(aStandardLQ.Name).
+				Priority(veryHighPriority).
+				Request(corev1.ResourceCPU, "8").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Verify the a-best-effort-low is admitted using the second flavor, as it couldn't preempt in the first")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aStandardHighWl,
+				testing.MakeAdmission(aStandardCQ.Name).Assignment(corev1.ResourceCPU, "fallback", "8").Obj(),
+			)
+		})
+	})
+
+	ginkgo.It("Should prioritize scheduling of non-borrowing workloads in the same scheduling cycle", func() {
```

  I have observed with debugger this test isn't testing what I wanted. The two workloads aren't scheduled in the same scheduling cycle. I will investigate what is going on, but it seems to make sense to remove it for now. We can cover this with unit tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-20T10:36:41Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:601** by [@mimowo](https://github.com/mimowo) — 2023-12-20T10:36:41Z
```diff
@@ -589,6 +598,9 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		}
 	}
 	status.append(msg)
+	if borrow := val - rQuota.Nominal; borrow > 0 && mode == Preempt {
```

  There is another reason we need to return `borrow>0`. This is used for entry ordering in scheduler: https://github.com/kubernetes-sigs/kueue/blob/25630d072e3e58f40414ded524bd9cfdee08c933/pkg/scheduler/scheduler.go#L505
  
  I have modified the implementation. PTAL. Hopefully it is simpler, but I'm open to other suggestions.
  
  Also, the only relevant bit of information is if `borrows()` is true / false. So, I opened another PR to further simplify the flavorassigner in this aspect: https://github.com/kubernetes-sigs/kueue/pull/1498.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-20T10:37:21Z

- **test/integration/scheduler/preemption_test.go:576** by [@mimowo](https://github.com/mimowo) — 2023-12-20T10:37:20Z
```diff
@@ -355,4 +357,325 @@ var _ = ginkgo.Describe("Preemption", func() {
 		})
 	})
 
+	ginkgo.Context("When most quota is in a shared ClusterQueue in a cohort", func() {
+		var (
+			aStandardCQ, aBestEffortCQ, bStandardCQ, sharedCQ *kueue.ClusterQueue
+			aStandardLQ, aBestEffortLQ, bStandardLQ           *kueue.LocalQueue
+			oneFlavor, fallbackFlavor                         *kueue.ResourceFlavor
+		)
+
+		ginkgo.BeforeEach(func() {
+			oneFlavor = testing.MakeResourceFlavor("one").Obj()
+			gomega.Expect(k8sClient.Create(ctx, oneFlavor)).To(gomega.Succeed())
+
+			fallbackFlavor = testing.MakeResourceFlavor("fallback").Obj()
+			gomega.Expect(k8sClient.Create(ctx, fallbackFlavor)).To(gomega.Succeed())
+
+			aStandardCQ = testing.MakeClusterQueue("a-standard-cq").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("one").Resource(corev1.ResourceCPU, "1", "5").Obj(),
+					*testing.MakeFlavorQuotas("fallback").Resource(corev1.ResourceCPU, "10", "0").Obj(),
+				).
+				FlavorFungibility(kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				}).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+					BorrowWithinCohort: &kueue.BorrowWithinCohort{
+						Policy:               kueue.BorrowWithinCohortPolicyLowerPriority,
+						MaxPriorityThreshold: ptr.To(midPriority),
+					},
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardCQ)).To(gomega.Succeed())
+			aStandardLQ = testing.MakeLocalQueue("a-standard-lq", ns.Name).ClusterQueue(aStandardCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardLQ)).To(gomega.Succeed())
+
+			aBestEffortCQ = testing.MakeClusterQueue("a-best-effort-cq").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("one").Resource(corev1.ResourceCPU, "1", "5").Obj(),
+					*testing.MakeFlavorQuotas("fallback").Resource(corev1.ResourceCPU, "10", "0").Obj(),
+				).
+				FlavorFungibility(kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				}).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+					BorrowWithinCohort: &kueue.BorrowWithinCohort{
+						Policy: kueue.BorrowWithinCohortPolicyLowerPriority,
+					},
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortCQ)).To(gomega.Succeed())
+			aBestEffortLQ = testing.MakeLocalQueue("a-best-effort-lq", ns.Name).ClusterQueue(aBestEffortCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortLQ)).To(gomega.Succeed())
+
+			bStandardCQ = testing.MakeClusterQueue("b-standard-cq").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("one").Resource(corev1.ResourceCPU, "1", "5").Obj(),
+					*testing.MakeFlavorQuotas("fallback").Resource(corev1.ResourceCPU, "10", "0").Obj(),
+				).
+				FlavorFungibility(kueue.FlavorFungibility{
+					WhenCanBorrow:  kueue.Borrow,
+					WhenCanPreempt: kueue.Preempt,
+				}).
+				Preemption(kueue.ClusterQueuePreemption{
+					ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
+					BorrowWithinCohort: &kueue.BorrowWithinCohort{
+						Policy:               kueue.BorrowWithinCohortPolicyLowerPriority,
+						MaxPriorityThreshold: ptr.To(midPriority),
+					},
+				}).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, bStandardCQ)).To(gomega.Succeed())
+			bStandardLQ = testing.MakeLocalQueue("b-standard-lq", ns.Name).ClusterQueue(bStandardCQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, bStandardLQ)).To(gomega.Succeed())
+
+			sharedCQ = testing.MakeClusterQueue("shared-cq").
+				Cohort("all").
+				ResourceGroup(*testing.MakeFlavorQuotas("one").Resource(corev1.ResourceCPU, "5").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, sharedCQ)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, aStandardCQ, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, aBestEffortCQ, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, bStandardCQ, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, sharedCQ, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, oneFlavor, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, fallbackFlavor, true)
+		})
+
+		ginkgo.It("Should allow a higher-priority workload to preempt a lower priority workload", func() {
+			ginkgo.By("Create a low priority workload which requires borrowing")
+			aBestEffortLowWl := testing.MakeWorkload("a-best-effort-low", ns.Name).
+				Queue(aBestEffortLQ.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortLowWl)).To(gomega.Succeed())
+
+			ginkgo.By("Await for the a-best-effort-low workload to be admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aBestEffortLowWl,
+				testing.MakeAdmission(aBestEffortCQ.Name).Assignment(corev1.ResourceCPU, "one", "5").Obj(),
+			)
+
+			ginkgo.By("Create the a-standard-high workload")
+			aStandardHighWl := testing.MakeWorkload("a-standard-high", ns.Name).
+				Queue(aStandardLQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Finish eviction fo the a-best-effort-low workload")
+			util.FinishEvictionForWorkloads(ctx, k8sClient, aBestEffortLowWl)
+
+			ginkgo.By("Verify the a-standard-high workload is admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aStandardHighWl,
+				testing.MakeAdmission(aStandardCQ.Name).Assignment(corev1.ResourceCPU, "one", "5").Obj(),
+			)
+
+			ginkgo.By("Verify the a-best-effort-low workload is re-admitted, but using flavor 2")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aBestEffortLowWl,
+				testing.MakeAdmission(aBestEffortCQ.Name).Assignment(corev1.ResourceCPU, "fallback", "5").Obj(),
+			)
+		})
+
+		ginkgo.It("Should not allow a lower-priority workload to preempt a higher priority workload", func() {
+			ginkgo.By("Create a high priority workload which requires borrowing")
+			aStandardHighWl := testing.MakeWorkload("a-standard-high", ns.Name).
+				Queue(aStandardLQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Await for the a-standard-high workload to be admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aStandardHighWl,
+				testing.MakeAdmission(aStandardCQ.Name).Assignment(corev1.ResourceCPU, "one", "5").Obj(),
+			)
+
+			aBestEffortLowWl := testing.MakeWorkload("a-best-effort-low", ns.Name).
+				Queue(aBestEffortLQ.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortLowWl)).To(gomega.Succeed())
+
+			ginkgo.By("Verify the a-best-effort-low is admitted using the second flavor, as it couldn't preempt in the first")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aBestEffortLowWl,
+				testing.MakeAdmission(aBestEffortCQ.Name).Assignment(corev1.ResourceCPU, "fallback", "5").Obj(),
+			)
+		})
+
+		ginkgo.It("Should not allow a higher-priority workload to preempt a lower-priority workload, if above the threshold", func() {
+			ginkgo.By("Create a high priority workload which requires borrowing")
+			aStandardHighWl := testing.MakeWorkload("a-standard-high", ns.Name).
+				Queue(aStandardLQ.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Await for the a-standard-high workload to be admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aStandardHighWl,
+				testing.MakeAdmission(aStandardCQ.Name).Assignment(corev1.ResourceCPU, "one", "5").Obj(),
+			)
+
+			ginkgo.By("Create the b-standard-very-high workload")
+			bStandardVeryHighWl := testing.MakeWorkload("b-standard-very-high", ns.Name).
+				Queue(bStandardLQ.Name).
+				Priority(veryHighPriority).
+				Request(corev1.ResourceCPU, "5").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, bStandardVeryHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Verify the b-standard-very-high is admitted using the second flavor, as it couldn't preempt in the first")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, bStandardVeryHighWl,
+				testing.MakeAdmission(bStandardCQ.Name).Assignment(corev1.ResourceCPU, "fallback", "5").Obj(),
+			)
+		})
+
+		ginkgo.It("Should not allow a higher-priority workload to preempt a lower-priority workload, if the low priority one is not borrowing", func() {
+			ginkgo.By("Create a low priority workload which does not borrowing")
+			aBestEffortLowWl := testing.MakeWorkload("a-best-effort-low", ns.Name).
+				Queue(aBestEffortLQ.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aBestEffortLowWl)).To(gomega.Succeed())
+
+			ginkgo.By("Await for the a-best-effort-low workload to be admitted")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aBestEffortLowWl,
+				testing.MakeAdmission(aBestEffortCQ.Name).Assignment(corev1.ResourceCPU, "one", "1").Obj(),
+			)
+
+			ginkgo.By("Create the a-standard-high workload")
+			aStandardHighWl := testing.MakeWorkload("a-standard-high", ns.Name).
+				Queue(aStandardLQ.Name).
+				Priority(veryHighPriority).
+				Request(corev1.ResourceCPU, "8").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, aStandardHighWl)).To(gomega.Succeed())
+
+			ginkgo.By("Verify the a-best-effort-low is admitted using the second flavor, as it couldn't preempt in the first")
+			util.ExpectWorkloadToBeAdmittedAs(ctx, k8sClient, aStandardHighWl,
+				testing.MakeAdmission(aStandardCQ.Name).Assignment(corev1.ResourceCPU, "fallback", "8").Obj(),
+			)
+		})
+	})
+
+	ginkgo.It("Should prioritize scheduling of non-borrowing workloads in the same scheduling cycle", func() {
```

  Same cycle integration test is removed

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-21T13:41:05Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:569** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-21T13:41:05Z
```diff
@@ -558,9 +561,17 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		// ClusterQueue are preempted.
 		mode = Preempt
 	}
+	if cq.Preemption.BorrowWithinCohort != nil && cq.Preemption.BorrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever {
+		// when preemption with borrowing is enabled, we can succeeded admitting the
+		// workload if preemption is used.
+		if rQuota.BorrowingLimit != nil && val <= rQuota.Nominal+*rQuota.BorrowingLimit {
+			borrow = max(val-rQuota.Nominal, 0)
+			mode = Preempt
+		}
+	}
 	if rQuota.BorrowingLimit != nil && used+val > rQuota.Nominal+*rQuota.BorrowingLimit {
 		status.append(fmt.Sprintf("borrowing limit for %s in flavor %s exceeded", rName, fName))
-		return mode, 0, &status
+		return mode, borrow, &status
```

  if the val<rQuota.Nominal we will have 0 here, which means "no borrowing", is it what we want to return? if so, why?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-22T09:41:42Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:569** by [@mimowo](https://github.com/mimowo) — 2023-12-22T09:41:42Z
```diff
@@ -558,9 +561,17 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		// ClusterQueue are preempted.
 		mode = Preempt
 	}
+	if cq.Preemption.BorrowWithinCohort != nil && cq.Preemption.BorrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever {
+		// when preemption with borrowing is enabled, we can succeeded admitting the
+		// workload if preemption is used.
+		if rQuota.BorrowingLimit != nil && val <= rQuota.Nominal+*rQuota.BorrowingLimit {
+			borrow = max(val-rQuota.Nominal, 0)
+			mode = Preempt
+		}
+	}
 	if rQuota.BorrowingLimit != nil && used+val > rQuota.Nominal+*rQuota.BorrowingLimit {
 		status.append(fmt.Sprintf("borrowing limit for %s in flavor %s exceeded", rName, fName))
-		return mode, 0, &status
+		return mode, borrow, &status
```

  Yes, we want `false` (0) in that case, because this parameter means "borrowing required for the corresponding mode". I have also adjusted the comment.
  
  So, if we return `Preempt` and `val<rQuota.Nominal` for a workload then it is possible to admit the workload without borrowing (just by preemption). 
  
  Still, the workload might be later admitted using a combination of preemption and borrowing.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-27T21:24:56Z

- **apis/kueue/v1beta1/clusterqueue_types.go:413** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T19:47:54Z
```diff
@@ -382,6 +386,36 @@ type ClusterQueuePreemption struct {
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
 }
 
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not allow for preemption, in other
+	//    ClusterQueues within the cohort, for a borrowing workload.
+	// - `LowerPriority`: allow preemption, in other ClusterQueues
+	//    within the cohort, for a borrowing workload, but only if
+	//    the preempted workloads are of lower priority.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads which
+	// might be preempted by a borrowing workload, to only workloads with
+	// priority below or equal the specified level.
```

  ```suggestion
  	// priority less than or equal to the specified threshold priority.
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:557** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T20:00:46Z
```diff
@@ -547,9 +551,17 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		// ClusterQueue are preempted.
 		mode = Preempt
 	}
+	if cq.Preemption.BorrowWithinCohort != nil && cq.Preemption.BorrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever {
+		// when preemption with borrowing is enabled, we can succeeded admitting the
+		// workload if preemption is used.
+		if rQuota.BorrowingLimit != nil && val <= rQuota.Nominal+*rQuota.BorrowingLimit {
```

  I think we also need to check that the workload can fit in the cohort total resources.

- **pkg/scheduler/preemption/preemption.go:80** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T20:04:27Z
```diff
@@ -74,7 +75,7 @@ func candidatesOnlyFromQueue(candidates []*workload.Info, clusterQueue string) [
 }
 
 // GetTargets returns the list of workloads that should be evicted in order to make room for wl.
-func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) []*workload.Info {
+func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, borrowWithinCohort *kueue.BorrowWithinCohort) []*workload.Info {
 	resPerFlv := resourcesRequiringPreemption(assignment)
 	cq := snapshot.ClusterQueues[wl.ClusterQueue]
```

  `borrowWithinCohort` should be in here, so no need to pass it as a parameter.

- **pkg/scheduler/preemption/preemption.go:81** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T20:09:51Z
```diff
@@ -74,7 +75,7 @@ func candidatesOnlyFromQueue(candidates []*workload.Info, clusterQueue string) [
 }
 
 // GetTargets returns the list of workloads that should be evicted in order to make room for wl.
-func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) []*workload.Info {
+func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, borrowWithinCohort *kueue.BorrowWithinCohort) []*workload.Info {
 	resPerFlv := resourcesRequiringPreemption(assignment)
 	cq := snapshot.ClusterQueues[wl.ClusterQueue]
 
```

  shouldn't we be filtering the candidates below (`findCandidates`) based on the threshold?

- **pkg/scheduler/preemption/preemption.go:181** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T20:11:18Z
```diff
@@ -167,6 +176,10 @@ func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment,
 		if cq != candCQ && !cqIsBorrowing(candCQ, resPerFlv) {
 			continue
 		}
+		if cq != candCQ && allowBorrowingBelowPriority != nil && priority.Priority(candWl.Obj) >= *allowBorrowingBelowPriority {
+			// we have exceeded the priority threshold, so preemption while borrowing is no longer possible
```

  This is a bit risky. We might introduce different candidate orderings in the future.

- **pkg/scheduler/preemption/preemption_test.go:813** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T21:12:32Z
```diff
@@ -754,6 +809,89 @@ func TestPreemption(t *testing.T) {
 			},
 			wantPreempted: sets.New("/wl2"),
 		},
+		"preempt while borrowing in cohort": {
+			admitted: []kueue.Workload{
```

  add another workload in standard_b under its nominal quota, to make sure it's not preempted, even if it has lower priority.

- **test/integration/scheduler/preemption_test.go:360** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T21:24:20Z
```diff
@@ -355,4 +357,220 @@ var _ = ginkgo.Describe("Preemption", func() {
 		})
 	})
 
+	ginkgo.Context("When most quota is in a shared ClusterQueue in a cohort", func() {
```

  Maybe we can mix a few cases into a single more complex scenario:
  for example:
  There are running workloads in standard A and B and best effort, playing with different priorities.
  A new workload comes in in standard A, only best effort workloads get preempted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T11:00:32Z

- **apis/kueue/v1beta1/clusterqueue_types.go:413** by [@mimowo](https://github.com/mimowo) — 2024-01-03T11:00:32Z
```diff
@@ -382,6 +386,36 @@ type ClusterQueuePreemption struct {
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
 }
 
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not allow for preemption, in other
+	//    ClusterQueues within the cohort, for a borrowing workload.
+	// - `LowerPriority`: allow preemption, in other ClusterQueues
+	//    within the cohort, for a borrowing workload, but only if
+	//    the preempted workloads are of lower priority.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads which
+	// might be preempted by a borrowing workload, to only workloads with
+	// priority below or equal the specified level.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T13:23:19Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:557** by [@mimowo](https://github.com/mimowo) — 2024-01-03T13:23:19Z
```diff
@@ -547,9 +551,17 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		// ClusterQueue are preempted.
 		mode = Preempt
 	}
+	if cq.Preemption.BorrowWithinCohort != nil && cq.Preemption.BorrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever {
+		// when preemption with borrowing is enabled, we can succeeded admitting the
+		// workload if preemption is used.
+		if rQuota.BorrowingLimit != nil && val <= rQuota.Nominal+*rQuota.BorrowingLimit {
```

  Indeed, additional check might be needed if the quota available for cohort < borrowingLimit. Added the check along with unit test. I guess we could validate that `cohortAvailableQuota >= borrowingLimit` earlier, but it is tricky to do in a `clusterqueue_webhook`, because it requires lookup to other clusterqueues.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T13:26:05Z

- **pkg/scheduler/preemption/preemption.go:80** by [@mimowo](https://github.com/mimowo) — 2024-01-03T13:26:05Z
```diff
@@ -74,7 +75,7 @@ func candidatesOnlyFromQueue(candidates []*workload.Info, clusterQueue string) [
 }
 
 // GetTargets returns the list of workloads that should be evicted in order to make room for wl.
-func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) []*workload.Info {
+func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, borrowWithinCohort *kueue.BorrowWithinCohort) []*workload.Info {
 	resPerFlv := resourcesRequiringPreemption(assignment)
 	cq := snapshot.ClusterQueues[wl.ClusterQueue]
```

  Correct, simplified

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T13:34:27Z

- **pkg/scheduler/preemption/preemption.go:81** by [@mimowo](https://github.com/mimowo) — 2024-01-03T13:34:27Z
```diff
@@ -74,7 +75,7 @@ func candidatesOnlyFromQueue(candidates []*workload.Info, clusterQueue string) [
 }
 
 // GetTargets returns the list of workloads that should be evicted in order to make room for wl.
-func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot) []*workload.Info {
+func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, borrowWithinCohort *kueue.BorrowWithinCohort) []*workload.Info {
 	resPerFlv := resourcesRequiringPreemption(assignment)
 	cq := snapshot.ClusterQueues[wl.ClusterQueue]
 
```

  I don't think so, suppose you have preemption config:
  ```golang
  	  ReclaimWithinCohort: kueue.PreemptionPolicyLowerPriority,
  	  BorrowWithinCohort: &kueue.BorrowWithinCohort{
  		  Policy: kueue.BorrowWithinCohortPolicyLowerPriority,
  		  MaxPriorityThreshold: MaxPriorityThreshold: ptr.To(100),
  	  }
  ```
  The new workload (w1) is priority 300, and we have an already admitted workload (w2) with priority 200. We need to return w2 from `findCandidates`, because its preemption may not require us to borrow, and its priority is below the priority of the new workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T13:41:01Z

- **pkg/scheduler/preemption/preemption_test.go:813** by [@mimowo](https://github.com/mimowo) — 2024-01-03T13:41:01Z
```diff
@@ -754,6 +809,89 @@ func TestPreemption(t *testing.T) {
 			},
 			wantPreempted: sets.New("/wl2"),
 		},
+		"preempt while borrowing in cohort": {
+			admitted: []kueue.Workload{
```

  I've added the workload under b_best_effort, in standard I put workloads of higher priority, so it could be inconclusive why it wasn't preempted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T14:55:45Z

- **pkg/scheduler/preemption/preemption.go:181** by [@mimowo](https://github.com/mimowo) — 2024-01-03T14:55:45Z
```diff
@@ -167,6 +176,10 @@ func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment,
 		if cq != candCQ && !cqIsBorrowing(candCQ, resPerFlv) {
 			continue
 		}
+		if cq != candCQ && allowBorrowingBelowPriority != nil && priority.Priority(candWl.Obj) >= *allowBorrowingBelowPriority {
+			// we have exceeded the priority threshold, so preemption while borrowing is no longer possible
```

  Indeed, here I rely on sorting of candidates. The last target (highest priority) is never removed in the second pass (because the look is `for i := len(targets) - 2; i >= 0; i-- {`, so once `allowBorrowing=false`, then it stays.
  
  If the order is random (or just not according to priorities), then the second pass may remove the highest priority workload, and `allowBorrowing` can be true again.
  
  If we want to support other orderings, then I can see two options (relying on `allowBorrowingFinal`, and without mutating `allowBorrowing`:
  1. have a utility function `allowBorrowingForTargets` which would iterate over the targets, and check if none of them is violating `allowBorrowingBelowPriority`, then `allowBorrowingFinal := allowBorrowing && allowBorrowingForTargets(targets, ...)`
  2. keep track of the number of targets that violate `allowBorrowingBelowPriority` (`targetsAboveAllowBorrowingThreshold`., increment the counter in the first pass, and decrement the counter when removing targets in the second pass. Then, `allowBorrowingFinal := allowBorrowing && targetsAboveAllowBorrowingThreshold == 0`
  
  Alternatively, we could keep as is, but just explain better. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T15:54:58Z

- **test/integration/scheduler/preemption_test.go:360** by [@mimowo](https://github.com/mimowo) — 2024-01-03T15:54:58Z
```diff
@@ -355,4 +357,220 @@ var _ = ginkgo.Describe("Preemption", func() {
 		})
 	})
 
+	ginkgo.Context("When most quota is in a shared ClusterQueue in a cohort", func() {
```

  Yeah, I was hesitant if multiple small scenarios or one bigger is better. I have now updated to have one bigger. PTAL.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-03T18:07:53Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:557** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-03T18:07:53Z
```diff
@@ -547,9 +551,17 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		// ClusterQueue are preempted.
 		mode = Preempt
 	}
+	if cq.Preemption.BorrowWithinCohort != nil && cq.Preemption.BorrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever {
+		// when preemption with borrowing is enabled, we can succeeded admitting the
+		// workload if preemption is used.
+		if rQuota.BorrowingLimit != nil && val <= rQuota.Nominal+*rQuota.BorrowingLimit {
```

  No, that's definitively not a candidate for webhook validation.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-03T18:11:53Z

- **test/integration/scheduler/preemption_test.go:360** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-03T18:11:53Z
```diff
@@ -355,4 +357,220 @@ var _ = ginkgo.Describe("Preemption", func() {
 		})
 	})
 
+	ginkgo.Context("When most quota is in a shared ClusterQueue in a cohort", func() {
```

  We can leave small scenarios for unit tests.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-04T13:46:59Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:569** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-04T13:46:59Z
```diff
@@ -558,9 +561,17 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		// ClusterQueue are preempted.
 		mode = Preempt
 	}
+	if cq.Preemption.BorrowWithinCohort != nil && cq.Preemption.BorrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever {
+		// when preemption with borrowing is enabled, we can succeeded admitting the
+		// workload if preemption is used.
+		if rQuota.BorrowingLimit != nil && val <= rQuota.Nominal+*rQuota.BorrowingLimit {
+			borrow = max(val-rQuota.Nominal, 0)
+			mode = Preempt
+		}
+	}
 	if rQuota.BorrowingLimit != nil && used+val > rQuota.Nominal+*rQuota.BorrowingLimit {
 		status.append(fmt.Sprintf("borrowing limit for %s in flavor %s exceeded", rName, fName))
-		return mode, 0, &status
+		return mode, borrow, &status
```

  ack

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-04T17:01:21Z

- **test/integration/scheduler/preemption_test.go:360** by [@mimowo](https://github.com/mimowo) — 2024-01-04T17:01:21Z
```diff
@@ -355,4 +357,220 @@ var _ = ginkgo.Describe("Preemption", func() {
 		})
 	})
 
+	ginkgo.Context("When most quota is in a shared ClusterQueue in a cohort", func() {
```

  Sure, we have unit test scenarios corresponding to the removed integration test scenarios in `pkg/scheduler/preemption/preemption_test.go`. I have reviewed the unit test descriptions in the recently pushed commit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-04T17:34:08Z

- **pkg/scheduler/preemption/preemption.go:181** by [@mimowo](https://github.com/mimowo) — 2024-01-04T17:34:08Z
```diff
@@ -167,6 +176,10 @@ func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment,
 		if cq != candCQ && !cqIsBorrowing(candCQ, resPerFlv) {
 			continue
 		}
+		if cq != candCQ && allowBorrowingBelowPriority != nil && priority.Priority(candWl.Obj) >= *allowBorrowingBelowPriority {
+			// we have exceeded the priority threshold, so preemption while borrowing is no longer possible
```

  Added a comment for now. If you think the comment is too complex, I'm fine with either (1.) or (2.).

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-04T18:25:02Z

- **apis/kueue/v1beta1/clusterqueue_types.go:413** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T17:04:25Z
```diff
@@ -382,6 +386,36 @@ type ClusterQueuePreemption struct {
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
 }
 
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not allow for preemption, in other
+	//    ClusterQueues within the cohort, for a borrowing workload.
+	// - `LowerPriority`: allow preemption, in other ClusterQueues
+	//    within the cohort, for a borrowing workload, but only if
+	//    the preempted workloads are of lower priority.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads which
+	// might be preempted by a borrowing workload, to only workloads with
+	// priority less than or equal to the specified threshold priority.
```

  Explain what null means.

- **pkg/scheduler/flavorassigner/flavorassigner.go:562** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T17:09:03Z
```diff
@@ -547,16 +551,27 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		// ClusterQueue are preempted.
 		mode = Preempt
 	}
+	cohortAvailable := rQuota.Nominal
+	if cq.Cohort != nil {
+		cohortAvailable = cq.Cohort.RequestableResources[fName][rName]
+	}
+
+	if cq.Preemption.BorrowWithinCohort != nil && cq.Preemption.BorrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever {
+		// when preemption with borrowing is enabled, we can succeeded admitting the
+		// workload if preemption is used.
+		if rQuota.BorrowingLimit != nil && val <= rQuota.Nominal+*rQuota.BorrowingLimit && val <= cohortAvailable {
```

  `borrowingLimit == nil` means that you can borrow any amount from the cohort, so it looks like there is a bug here. Also please add a unit test.

- **pkg/scheduler/preemption/preemption.go:103** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T17:22:44Z
```diff
@@ -96,16 +97,25 @@ func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assig
 	if len(sameQueueCandidates) == len(candidates) {
 		// There is no possible preemption of workloads from other queues,
 		// so we'll try borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true)
+		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true, nil)
 	} else {
 		// There is a risk of preemption of workloads from the other queue in the
 		// cohort, proceeding without borrowing.
```

  this comment doesn't sound accurate anymore.

- **pkg/scheduler/preemption/preemption.go:100** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T18:19:03Z
```diff
@@ -96,16 +97,25 @@ func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assig
 	if len(sameQueueCandidates) == len(candidates) {
 		// There is no possible preemption of workloads from other queues,
 		// so we'll try borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true)
+		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true, nil)
```

  let's just return the targets here to reduce indentation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-05T13:26:11Z

- **apis/kueue/v1beta1/clusterqueue_types.go:413** by [@mimowo](https://github.com/mimowo) — 2024-01-05T13:26:11Z
```diff
@@ -382,6 +386,36 @@ type ClusterQueuePreemption struct {
 	WithinClusterQueue PreemptionPolicy `json:"withinClusterQueue,omitempty"`
 }
 
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not allow for preemption, in other
+	//    ClusterQueues within the cohort, for a borrowing workload.
+	// - `LowerPriority`: allow preemption, in other ClusterQueues
+	//    within the cohort, for a borrowing workload, but only if
+	//    the preempted workloads are of lower priority.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads which
+	// might be preempted by a borrowing workload, to only workloads with
+	// priority less than or equal to the specified threshold priority.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-05T13:35:37Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:562** by [@mimowo](https://github.com/mimowo) — 2024-01-05T13:35:36Z
```diff
@@ -547,16 +551,27 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		// ClusterQueue are preempted.
 		mode = Preempt
 	}
+	cohortAvailable := rQuota.Nominal
+	if cq.Cohort != nil {
+		cohortAvailable = cq.Cohort.RequestableResources[fName][rName]
+	}
+
+	if cq.Preemption.BorrowWithinCohort != nil && cq.Preemption.BorrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever {
+		// when preemption with borrowing is enabled, we can succeeded admitting the
+		// workload if preemption is used.
+		if rQuota.BorrowingLimit != nil && val <= rQuota.Nominal+*rQuota.BorrowingLimit && val <= cohortAvailable {
```

  Good spot, fixed and added a unit test in flavorassigner_test which fails on the previous version

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-05T13:39:52Z

- **pkg/scheduler/preemption/preemption.go:100** by [@mimowo](https://github.com/mimowo) — 2024-01-05T13:39:52Z
```diff
@@ -96,16 +97,25 @@ func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assig
 	if len(sameQueueCandidates) == len(candidates) {
 		// There is no possible preemption of workloads from other queues,
 		// so we'll try borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true)
+		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true, nil)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-05T13:55:08Z

- **pkg/scheduler/preemption/preemption.go:103** by [@mimowo](https://github.com/mimowo) — 2024-01-05T13:55:08Z
```diff
@@ -96,16 +97,25 @@ func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assig
 	if len(sameQueueCandidates) == len(candidates) {
 		// There is no possible preemption of workloads from other queues,
 		// so we'll try borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true)
+		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true, nil)
 	} else {
 		// There is a risk of preemption of workloads from the other queue in the
 		// cohort, proceeding without borrowing.
```

  Good spot, updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-05T16:56:04Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:562** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-05T16:36:28Z
```diff
@@ -547,16 +551,27 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 		// ClusterQueue are preempted.
 		mode = Preempt
 	}
+	cohortAvailable := rQuota.Nominal
+	if cq.Cohort != nil {
+		cohortAvailable = cq.Cohort.RequestableResources[fName][rName]
+	}
+
+	if cq.Preemption.BorrowWithinCohort != nil && cq.Preemption.BorrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever {
+		// when preemption with borrowing is enabled, we can succeeded admitting the
+		// workload if preemption is used.
+		if (rQuota.BorrowingLimit == nil || rQuota.BorrowingLimit != nil && val <= rQuota.Nominal+*rQuota.BorrowingLimit) && val <= cohortAvailable {
```

  ```suggestion
  		if (rQuota.BorrowingLimit == nil || val <= rQuota.Nominal+*rQuota.BorrowingLimit) && val <= cohortAvailable {
  ```

- **pkg/scheduler/preemption/preemption.go:102** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-05T16:42:58Z
```diff
@@ -96,19 +96,30 @@ func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assig
 	if len(sameQueueCandidates) == len(candidates) {
 		// There is no possible preemption of workloads from other queues,
 		// so we'll try borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true)
-	} else {
-		// There is a risk of preemption of workloads from the other queue in the
-		// cohort, proceeding without borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, false)
-		if len(targets) == 0 {
-			// Another attempt. This time only candidates from the same queue, but
-			// with borrowing. The previous attempt didn't try borrowing and had broader
-			// scope of preemption.
-			targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, sameQueueCandidates, true)
-		}
+		return minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true, nil)
 	}
 
+	// There is a risk of preemption of workloads from the other queue in the
```

  risk sounds like a bad thing :)
  ```suggestion
  	// There is potential of preemption of workloads from the other queue in the
  ```

- **pkg/scheduler/preemption/preemption.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-05T16:46:22Z
```diff
@@ -96,19 +96,30 @@ func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assig
 	if len(sameQueueCandidates) == len(candidates) {
 		// There is no possible preemption of workloads from other queues,
 		// so we'll try borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true)
-	} else {
-		// There is a risk of preemption of workloads from the other queue in the
-		// cohort, proceeding without borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, false)
-		if len(targets) == 0 {
-			// Another attempt. This time only candidates from the same queue, but
-			// with borrowing. The previous attempt didn't try borrowing and had broader
-			// scope of preemption.
-			targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, sameQueueCandidates, true)
-		}
+		return minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true, nil)
 	}
 
+	// There is a risk of preemption of workloads from the other queue in the
+	// cohort. We proceed with borrowing only if the dedicated policy
+	// (borrowWithinCohort) is enabled. This ensures the preempted workloads
+	// have lower priority, and so they will not preempt the preemptor when
+	// requeued.
+	var allowBorrowingBelowPriority *int32
+	borrowWithinCohort := cq.Preemption.BorrowWithinCohort
+	allowBorrowing := borrowWithinCohort != nil && borrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever
+	if allowBorrowing {
+		allowBorrowingBelowPriority = ptr.To(priority.Priority(wl.Obj))
+		if borrowWithinCohort.MaxPriorityThreshold != nil && *borrowWithinCohort.MaxPriorityThreshold < *allowBorrowingBelowPriority {
+			allowBorrowingBelowPriority = ptr.To(*borrowWithinCohort.MaxPriorityThreshold + 1)
+		}
```

  I think it makes more sense to do `return minimalPreemptions` here. It makes it more obvious that there is only one call to `minimalPreemptions`.

- **pkg/scheduler/preemption/preemption.go:194** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-05T16:55:52Z
```diff
@@ -168,6 +179,21 @@ func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment,
 		if cq != candCQ && !cqIsBorrowing(candCQ, resPerFlv) {
 			continue
 		}
+		if cq != candCQ && allowBorrowingBelowPriority != nil && priority.Priority(candWl.Obj) >= *allowBorrowingBelowPriority {
+			// We set allowBorrowing=false if there is a candidate with priority
+			// exceeding allowBorrowingBelowPriority added to targets.
+			//
+			// We need to be careful mutating allowBorrowing. We rely on the
+			// fact that once there is a candidate exceeding the priority added
+			// to targets, then at least one such candidate is present in the
+			// final set of targets (after the second phase of the function).
+			//
+			// This is true, because the candidates are ordered according
+			// to priorities (from lowest to highest, using candidatesOrdering),
+			// and the last added target is not removed in the second phase of
+			// the function.
+			allowBorrowing = false
```

  I'm no longer sure if this works.
  
  What if, in a previous iteration, we were already borrowing? Then `workloadFits` would return false and break this loop. So far so good.
  
  However, the next loop (check if any of the workloads can be added back) would be prevented from borrowing, even though it was ok to borrow. As a result, we won't be able to add back any workload, possibly leading to preempting more workloads than necessary.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-05T17:03:36Z

- **pkg/scheduler/preemption/preemption.go:194** by [@mimowo](https://github.com/mimowo) — 2024-01-05T17:03:36Z
```diff
@@ -168,6 +179,21 @@ func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment,
 		if cq != candCQ && !cqIsBorrowing(candCQ, resPerFlv) {
 			continue
 		}
+		if cq != candCQ && allowBorrowingBelowPriority != nil && priority.Priority(candWl.Obj) >= *allowBorrowingBelowPriority {
+			// We set allowBorrowing=false if there is a candidate with priority
+			// exceeding allowBorrowingBelowPriority added to targets.
+			//
+			// We need to be careful mutating allowBorrowing. We rely on the
+			// fact that once there is a candidate exceeding the priority added
+			// to targets, then at least one such candidate is present in the
+			// final set of targets (after the second phase of the function).
+			//
+			// This is true, because the candidates are ordered according
+			// to priorities (from lowest to highest, using candidatesOrdering),
+			// and the last added target is not removed in the second phase of
+			// the function.
+			allowBorrowing = false
```

  > What if, in a previous iteration, we were already borrowing? Then workloadFits would return false and break this loop. So far so good.
  
  The `workloadFits` breaks when returning `true` (not false), snippet from below:
  
  ```golang
  		if workloadFits(wlReq, cq, allowBorrowing) {
  			fits = true
  			break
  		}
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-05T17:10:42Z

- **pkg/scheduler/preemption/preemption.go:112** by [@mimowo](https://github.com/mimowo) — 2024-01-05T17:10:42Z
```diff
@@ -96,19 +96,30 @@ func (p *Preemptor) GetTargets(wl workload.Info, assignment flavorassigner.Assig
 	if len(sameQueueCandidates) == len(candidates) {
 		// There is no possible preemption of workloads from other queues,
 		// so we'll try borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true)
-	} else {
-		// There is a risk of preemption of workloads from the other queue in the
-		// cohort, proceeding without borrowing.
-		targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, false)
-		if len(targets) == 0 {
-			// Another attempt. This time only candidates from the same queue, but
-			// with borrowing. The previous attempt didn't try borrowing and had broader
-			// scope of preemption.
-			targets = minimalPreemptions(&wl, assignment, snapshot, resPerFlv, sameQueueCandidates, true)
-		}
+		return minimalPreemptions(&wl, assignment, snapshot, resPerFlv, candidates, true, nil)
 	}
 
+	// There is a risk of preemption of workloads from the other queue in the
+	// cohort. We proceed with borrowing only if the dedicated policy
+	// (borrowWithinCohort) is enabled. This ensures the preempted workloads
+	// have lower priority, and so they will not preempt the preemptor when
+	// requeued.
+	var allowBorrowingBelowPriority *int32
+	borrowWithinCohort := cq.Preemption.BorrowWithinCohort
+	allowBorrowing := borrowWithinCohort != nil && borrowWithinCohort.Policy != kueue.BorrowWithinCohortPolicyNever
+	if allowBorrowing {
+		allowBorrowingBelowPriority = ptr.To(priority.Priority(wl.Obj))
+		if borrowWithinCohort.MaxPriorityThreshold != nil && *borrowWithinCohort.MaxPriorityThreshold < *allowBorrowingBelowPriority {
+			allowBorrowingBelowPriority = ptr.To(*borrowWithinCohort.MaxPriorityThreshold + 1)
+		}
```

  Good point, thank you for the comment, I think it reads much better now.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-05T18:07:13Z

- **pkg/scheduler/preemption/preemption.go:194** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-05T18:07:13Z
```diff
@@ -168,6 +179,21 @@ func minimalPreemptions(wl *workload.Info, assignment flavorassigner.Assignment,
 		if cq != candCQ && !cqIsBorrowing(candCQ, resPerFlv) {
 			continue
 		}
+		if cq != candCQ && allowBorrowingBelowPriority != nil && priority.Priority(candWl.Obj) >= *allowBorrowingBelowPriority {
+			// We set allowBorrowing=false if there is a candidate with priority
+			// exceeding allowBorrowingBelowPriority added to targets.
+			//
+			// We need to be careful mutating allowBorrowing. We rely on the
+			// fact that once there is a candidate exceeding the priority added
+			// to targets, then at least one such candidate is present in the
+			// final set of targets (after the second phase of the function).
+			//
+			// This is true, because the candidates are ordered according
+			// to priorities (from lowest to highest, using candidatesOrdering),
+			// and the last added target is not removed in the second phase of
+			// the function.
+			allowBorrowing = false
```

  ohhhh, right.
  
  So the mechanics roughly like this:
  1. allowBorrowing=true, workload doesn't fit,
  2. we pass the threshold, workload still doesn't fit, we can continue deleting workloads, but now we cannot allow borrowing.
  3. Eventually, the workload fits. We are already deleting workloads past the threshold, so if we put workloads back, we cannot allow borrowing anyway.
  
  Ack.
