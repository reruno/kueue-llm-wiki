# PR #1399: Fix pending workload from StrictFIFO CQ doesn't block borrowing from other CQ

**Summary**: Fix pending workload from StrictFIFO CQ doesn't block borrowing from other CQ

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1399

**Last updated**: 2024-01-11T15:57:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Branch**: `scheduler-test` → `main`
- **Created**: 2023-12-04T13:53:10Z
- **Updated**: 2024-01-11T15:57:48Z
- **Merged**: 2024-01-10T20:27:14Z
- **Closed**: 2024-01-10T20:27:14Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 5   **+316 / -12**

## Description

#### Which issue(s) this PR fixes:
related to #1283

#### Does this PR introduce a user-facing change?
```release-note
Pending workload from StrictFIFO ClusterQueue doesn't block borrowing from other ClusterQueues
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-12-04T13:53:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 380ad7da0c6bba7e44eb2a81390cd31f33795939 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/659ef7abd75eff00097db422 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T13:30:20Z

/cc

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-03T21:53:09Z

/assign @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T18:54:07Z

This definitely requires a release note.

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-10T10:03:19Z

/remove-kind feature
/kind bug

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T20:03:05Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-10T20:03:11Z

LGTM label has been added.  <details>Git tree hash: 8f26d378f3d6b62823e674f008cb2380ff3706b4</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-10T20:03:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1399#issuecomment-1885633268" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1399#" title="Author self-approved">yaroslava-serdiuk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-05T15:46:00Z

- **test/integration/scheduler/scheduler_test.go:1239** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T15:35:24Z
```diff
@@ -1229,32 +1232,39 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				},
 			}
 			gomega.Expect(k8sClient.Create(ctx, matchingNS)).To(gomega.Succeed())
+
+			strictFIFOLocalQueue = testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+
+			cqTeamA = testing.MakeClusterQueue("team-a").ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).Cohort(chName).Obj()
```

  for readability, split this into multiple lines, similar to how we do for `strictFIFOClusterQ`.

- **test/integration/scheduler/scheduler_test.go:1242** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T15:35:30Z
```diff
@@ -1229,32 +1232,39 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				},
 			}
 			gomega.Expect(k8sClient.Create(ctx, matchingNS)).To(gomega.Succeed())
+
+			strictFIFOLocalQueue = testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+
+			cqTeamA = testing.MakeClusterQueue("team-a").ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).Cohort(chName).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources = testing.MakeClusterQueue("shared-resources").ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).Cohort(chName).Obj()
```

  same here

- **test/integration/scheduler/scheduler_test.go:1305** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T15:36:07Z
```diff
@@ -1291,6 +1301,34 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.FIt("Pending workload with StrictFIFO doesn't block other CQ from borrowing", func() {
```

  ```suggestion
  		ginkgo.FIt("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
  ```

- **test/integration/scheduler/scheduler_test.go:1365** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T15:39:11Z
```diff
@@ -1291,6 +1301,34 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.FIt("Pending workload with StrictFIFO doesn't block other CQ from borrowing", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating workloads")
+			wl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "5").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+
+			wl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
```

  Move `util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2)` here.
  
  This is to be 100% that the workloads are admitted before we create another workload, which could potentially move ahead of the others if the timestamps end up being the same, because timestamps only have granularity by seconds.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-04T19:27:56Z

We should also have unit test coverage for the new logic

- **pkg/scheduler/flavorassigner/flavorassigner.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T18:58:39Z
```diff
@@ -231,7 +232,7 @@ func lastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
 		(cq.Cohort != nil && cq.Cohort.AllocatableResourceGeneration > wl.LastAssignment.CohortGeneration)
 }
 
-// AssignFlavors assigns flavors for each of the resources requested in each pod set.
+// AssignFlavors assigns flavor for each of the resources requested in each pod set.
```

  ```suggestion
  // AssignFlavors assigns a flavor to each of the resources requested in each pod set.
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:550** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T18:59:57Z
```diff
@@ -542,7 +543,7 @@ func fitsResourceQuota(fName kueue.ResourceFlavorReference, rName corev1.Resourc
 	used := cq.Usage[fName][rName]
 	mode := NoFit
 	if val <= rQuota.Nominal {
-		// The request can be satisfied by the min quota, assuming quota is
+		// The request can be satisfied by the nominal quota, assuming quota is
```

  we changed the API field name 2y ago 😅 
  Good catch!

- **pkg/scheduler/scheduler.go:352** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T19:04:20Z
```diff
@@ -324,23 +324,51 @@ type partialAssignment struct {
 	preemptionTargets []*workload.Info
 }
 
+// limitUsage makes sure that cluster queue usage does not exceed nominal+borrowing quota
+func limitUsage(e flavorassigner.Assignment, cq *cache.ClusterQueue) flavorassigner.Assignment {
+	for flavor, resourceUsage := range e.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
+			}
+		}
+		for resource, usage := range resourceUsage {
+			if cqQuota[resource].BorrowingLimit != nil {
+				usage = min(usage, cqQuota[resource].Nominal+*cqQuota[resource].BorrowingLimit-cq.Usage[flavor][resource])
+			}
+			resourceUsage[resource] = usage
+		}
+		e.Usage[flavor] = resourceUsage
+	}
+	return e
+}
+
 func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cache.Snapshot) (flavorassigner.Assignment, []*workload.Info) {
 	cq := snap.ClusterQueues[wl.ClusterQueue]
 	fullAssignment := flavorassigner.AssignFlavors(log, wl, snap.ResourceFlavors, cq, nil)
-	var fullAssignmentTargets []*workload.Info
+	var preemtionTargets []*workload.Info
```

  The intention of the old name was to differentiate from the preemptionTargets when there is partial admission. Maybe we can name it `faPreemptionTargets`

- **pkg/scheduler/scheduler.go:366** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T19:18:10Z
```diff
@@ -324,23 +324,51 @@ type partialAssignment struct {
 	preemptionTargets []*workload.Info
 }
 
+// limitUsage makes sure that cluster queue usage does not exceed nominal+borrowing quota
+func limitUsage(e flavorassigner.Assignment, cq *cache.ClusterQueue) flavorassigner.Assignment {
+	for flavor, resourceUsage := range e.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
+			}
+		}
+		for resource, usage := range resourceUsage {
+			if cqQuota[resource].BorrowingLimit != nil {
+				usage = min(usage, cqQuota[resource].Nominal+*cqQuota[resource].BorrowingLimit-cq.Usage[flavor][resource])
+			}
+			resourceUsage[resource] = usage
+		}
+		e.Usage[flavor] = resourceUsage
+	}
+	return e
+}
+
 func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cache.Snapshot) (flavorassigner.Assignment, []*workload.Info) {
 	cq := snap.ClusterQueues[wl.ClusterQueue]
 	fullAssignment := flavorassigner.AssignFlavors(log, wl, snap.ResourceFlavors, cq, nil)
-	var fullAssignmentTargets []*workload.Info
+	var preemtionTargets []*workload.Info
 
 	arm := fullAssignment.RepresentativeMode()
 	if arm == flavorassigner.Fit {
 		return fullAssignment, nil
 	}
 
 	if arm == flavorassigner.Preempt {
-		fullAssignmentTargets = s.preemptor.GetTargets(*wl, fullAssignment, snap)
+		preemtionTargets = s.preemptor.GetTargets(*wl, fullAssignment, snap)
+	}
+
+	// if no preemtion and borrowing is going to happen, we should limit
+	// assignment usage, so it's won't book extra resources.
+	if len(preemtionTargets) == 0 && !fullAssignment.Borrowing {
+		fullAssignment = limitUsage(fullAssignment, cq)
```

  This looks like an odd place to call this function. We are saying that the workload is requesting less resources than it actually is requesting, corrupting logs and possibly affecting other calculations down the line. This logic instead should be called somewhere in `totalUsageForCommonFlavorResources`. And I think the conclusion was that we should limit it up to the `nominal`, not `nominal+borrowingLimit`.
  
  Also this solution wouldn't be enough when partial admission is possible.

- **test/integration/scheduler/scheduler_test.go:1212** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T19:20:34Z
```diff
@@ -1206,11 +1206,14 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 	ginkgo.When("Queueing with StrictFIFO", func() {
 		var (
-			strictFIFOClusterQ *kueue.ClusterQueue
-			matchingNS         *corev1.Namespace
+			strictFIFOClusterQ, cqTeamA, cqSharedResources *kueue.ClusterQueue
+			strictFIFOLocalQueue                           *kueue.LocalQueue
+			matchingNS                                     *corev1.Namespace
+			chName                                         string
```

  It doesn't look like we need to have this as a global variable.

- **test/integration/scheduler/scheduler_test.go:1279** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T19:23:13Z
```diff
@@ -1234,32 +1237,46 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				},
 			}
 			gomega.Expect(k8sClient.Create(ctx, matchingNS)).To(gomega.Succeed())
+
+			strictFIFOLocalQueue = testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+
+			cqTeamA = testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources = testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
 		})
 
 		ginkgo.AfterEach(func() {
 			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, matchingNS)).To(gomega.Succeed())
 			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, strictFIFOClusterQ, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqSharedResources, true)
 			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandFlavor, true)
 		})
 
 		ginkgo.It("Should schedule workloads by their priority strictly", func() {
-			strictFIFOQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
-
 			ginkgo.By("Creating workloads")
-			wl1 := testing.MakeWorkload("wl1", matchingNS.Name).Queue(strictFIFOQueue.
+			wl1 := testing.MakeWorkload("wl1", matchingNS.Name).Queue(strictFIFOLocalQueue.
 				Name).Request(corev1.ResourceCPU, "2").Priority(100).Obj()
 			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
-			wl2 := testing.MakeWorkload("wl2", matchingNS.Name).Queue(strictFIFOQueue.
+			wl2 := testing.MakeWorkload("wl2", matchingNS.Name).Queue(strictFIFOLocalQueue.
 				Name).Request(corev1.ResourceCPU, "5").Priority(10).Obj()
 			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
 			// wl3 can't be scheduled before wl2 even though there is enough quota.
-			wl3 := testing.MakeWorkload("wl3", matchingNS.Name).Queue(strictFIFOQueue.
+			wl3 := testing.MakeWorkload("wl3", matchingNS.Name).Queue(strictFIFOLocalQueue.
 				Name).Request(corev1.ResourceCPU, "1").Priority(1).Obj()
 			gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
 
-			gomega.Expect(k8sClient.Create(ctx, strictFIFOQueue)).Should(gomega.Succeed())
```

  The reason for creating this LocalQueue at this point is that we know that all the workloads exist and will be competing with each other, as opposed to arriving one by one.
  
  Maybe we can change this to be more similar to #1544

- **pkg/scheduler/scheduler_test.go:842** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T19:27:07Z
```diff
@@ -839,7 +839,7 @@ func TestSchedule(t *testing.T) {
 				"eng-alpha/new-alpha": *utiltesting.MakeAdmission("eng-alpha", "one").Assignment(corev1.ResourceCPU, "on-demand", "1").AssignmentPodCount(1).Obj(),
 			},
 			wantScheduled: []string{"eng-beta/new", "eng-alpha/new-alpha"},
-			wantLeft: map[string][]string{
+			wantInadmissibleLeft: map[string][]string{
```

  is this change justified?

- **test/integration/scheduler/scheduler_test.go:1320** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T19:27:32Z
```diff
@@ -1296,6 +1313,34 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
```

  Let's also add a test case to cover this: https://github.com/kubernetes-sigs/kueue/issues/1283#issuecomment-1841071485

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-05T18:56:09Z

- **pkg/scheduler/scheduler.go:337** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-05T18:26:32Z
```diff
@@ -324,23 +325,46 @@ type partialAssignment struct {
 	preemptionTargets []*workload.Info
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment could reserve.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.CanBePartiallyAdmitted() || e.assignment.Borrowing {
+		return e.assignment.Usage
+	}
+	reservedUsage := e.assignment.Usage
+	for flavor, resourceUsage := range e.assignment.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
```

  Once the scheduler hits here, can we break to Line 343?
  It seems that the scheduler doesn't need to continue loops.

- **pkg/scheduler/scheduler.go:342** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-05T18:49:05Z
```diff
@@ -324,23 +325,46 @@ type partialAssignment struct {
 	preemptionTargets []*workload.Info
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment could reserve.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.CanBePartiallyAdmitted() || e.assignment.Borrowing {
+		return e.assignment.Usage
+	}
+	reservedUsage := e.assignment.Usage
+	for flavor, resourceUsage := range e.assignment.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
+			}
+		}
+		for resource, usage := range resourceUsage {
+			// since assignment doesn't borrow, it's usage can't exceed nominal quota.
+			reservedUsage[flavor][resource] = min(usage, cqQuota[resource].Nominal-cq.Usage[flavor][resource])
```

  IIUC, this calculation always returns `usage` since the assignment doesn't allow partial admission, right?

- **test/integration/scheduler/scheduler_test.go:1353** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-05T18:56:06Z
```diff
@@ -1314,6 +1316,62 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqSharedResources, true)
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			util.StopClusterQueues(cqTeamA)
```

  We don't prefer such a utilization. So can we update a clusterQueue like the following?
  
  ```
  cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
  ```

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-05T19:26:59Z

- **test/integration/scheduler/scheduler_test.go:1320** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-05T19:26:59Z
```diff
@@ -1296,6 +1313,34 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
```

  Extended test case to cover both scenarios

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-05T19:28:08Z

- **pkg/scheduler/scheduler.go:366** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-05T19:28:08Z
```diff
@@ -324,23 +324,51 @@ type partialAssignment struct {
 	preemptionTargets []*workload.Info
 }
 
+// limitUsage makes sure that cluster queue usage does not exceed nominal+borrowing quota
+func limitUsage(e flavorassigner.Assignment, cq *cache.ClusterQueue) flavorassigner.Assignment {
+	for flavor, resourceUsage := range e.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
+			}
+		}
+		for resource, usage := range resourceUsage {
+			if cqQuota[resource].BorrowingLimit != nil {
+				usage = min(usage, cqQuota[resource].Nominal+*cqQuota[resource].BorrowingLimit-cq.Usage[flavor][resource])
+			}
+			resourceUsage[resource] = usage
+		}
+		e.Usage[flavor] = resourceUsage
+	}
+	return e
+}
+
 func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cache.Snapshot) (flavorassigner.Assignment, []*workload.Info) {
 	cq := snap.ClusterQueues[wl.ClusterQueue]
 	fullAssignment := flavorassigner.AssignFlavors(log, wl, snap.ResourceFlavors, cq, nil)
-	var fullAssignmentTargets []*workload.Info
+	var preemtionTargets []*workload.Info
 
 	arm := fullAssignment.RepresentativeMode()
 	if arm == flavorassigner.Fit {
 		return fullAssignment, nil
 	}
 
 	if arm == flavorassigner.Preempt {
-		fullAssignmentTargets = s.preemptor.GetTargets(*wl, fullAssignment, snap)
+		preemtionTargets = s.preemptor.GetTargets(*wl, fullAssignment, snap)
+	}
+
+	// if no preemtion and borrowing is going to happen, we should limit
+	// assignment usage, so it's won't book extra resources.
+	if len(preemtionTargets) == 0 && !fullAssignment.Borrowing {
+		fullAssignment = limitUsage(fullAssignment, cq)
```

  Updated

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-05T19:28:19Z

- **test/integration/scheduler/scheduler_test.go:1279** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-05T19:28:19Z
```diff
@@ -1234,32 +1237,46 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				},
 			}
 			gomega.Expect(k8sClient.Create(ctx, matchingNS)).To(gomega.Succeed())
+
+			strictFIFOLocalQueue = testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+
+			cqTeamA = testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources = testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
 		})
 
 		ginkgo.AfterEach(func() {
 			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, matchingNS)).To(gomega.Succeed())
 			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
 			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, strictFIFOClusterQ, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqSharedResources, true)
 			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandFlavor, true)
 		})
 
 		ginkgo.It("Should schedule workloads by their priority strictly", func() {
-			strictFIFOQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
-
 			ginkgo.By("Creating workloads")
-			wl1 := testing.MakeWorkload("wl1", matchingNS.Name).Queue(strictFIFOQueue.
+			wl1 := testing.MakeWorkload("wl1", matchingNS.Name).Queue(strictFIFOLocalQueue.
 				Name).Request(corev1.ResourceCPU, "2").Priority(100).Obj()
 			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
-			wl2 := testing.MakeWorkload("wl2", matchingNS.Name).Queue(strictFIFOQueue.
+			wl2 := testing.MakeWorkload("wl2", matchingNS.Name).Queue(strictFIFOLocalQueue.
 				Name).Request(corev1.ResourceCPU, "5").Priority(10).Obj()
 			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
 			// wl3 can't be scheduled before wl2 even though there is enough quota.
-			wl3 := testing.MakeWorkload("wl3", matchingNS.Name).Queue(strictFIFOQueue.
+			wl3 := testing.MakeWorkload("wl3", matchingNS.Name).Queue(strictFIFOLocalQueue.
 				Name).Request(corev1.ResourceCPU, "1").Priority(1).Obj()
 			gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
 
-			gomega.Expect(k8sClient.Create(ctx, strictFIFOQueue)).Should(gomega.Succeed())
```

  Updated

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-05T19:35:32Z

- **pkg/scheduler/scheduler.go:342** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-05T19:35:32Z
```diff
@@ -324,23 +325,46 @@ type partialAssignment struct {
 	preemptionTargets []*workload.Info
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment could reserve.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.CanBePartiallyAdmitted() || e.assignment.Borrowing {
+		return e.assignment.Usage
+	}
+	reservedUsage := e.assignment.Usage
+	for flavor, resourceUsage := range e.assignment.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
+			}
+		}
+		for resource, usage := range resourceUsage {
+			// since assignment doesn't borrow, it's usage can't exceed nominal quota.
+			reservedUsage[flavor][resource] = min(usage, cqQuota[resource].Nominal-cq.Usage[flavor][resource])
```

  No, in case https://github.com/kubernetes-sigs/kueue/issues/1283#issuecomment-1834370396 it returns 0 for workload 3 instead of 1 (nominal quota - cq usage = 1-1). 
  Here `usage` is accumulated resources requests.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-05T20:38:30Z

- **pkg/scheduler/scheduler.go:342** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-05T20:38:30Z
```diff
@@ -324,23 +325,46 @@ type partialAssignment struct {
 	preemptionTargets []*workload.Info
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment could reserve.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.CanBePartiallyAdmitted() || e.assignment.Borrowing {
+		return e.assignment.Usage
+	}
+	reservedUsage := e.assignment.Usage
+	for flavor, resourceUsage := range e.assignment.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
+			}
+		}
+		for resource, usage := range resourceUsage {
+			// since assignment doesn't borrow, it's usage can't exceed nominal quota.
+			reservedUsage[flavor][resource] = min(usage, cqQuota[resource].Nominal-cq.Usage[flavor][resource])
```

  Oops, you're right. The entry's representativeMode isn't only `Fit` even if the cq doesn't belong to any cohort.
  Thanks :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-08T19:54:31Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:23:18Z
```diff
@@ -231,7 +232,7 @@ func lastAssignmentOutdated(wl *workload.Info, cq *cache.ClusterQueue) bool {
 		(cq.Cohort != nil && cq.Cohort.AllocatableResourceGeneration > wl.LastAssignment.CohortGeneration)
 }
 
-// AssignFlavors assigns flavors for each of the resources requested in each pod set.
+// AssignFlavors assigns flavor for each of the resources requested in each pod set.
```

  still pending

- **pkg/scheduler/scheduler.go:208** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:24:14Z
```diff
@@ -204,7 +204,8 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			}
 			// Even if the workload will not be admitted after this point, due to preemption pending or other failures,
 			// we should still account for its usage.
-			cycleCohortsUsage.add(cq.Cohort.Name, e.assignment.Usage)
+			reserve := resourcesToReserve(e, cq)
+			cycleCohortsUsage.add(cq.Cohort.Name, reserve)
```

  ```suggestion
  			cycleCohortsUsage.add(cq.Cohort.Name, resourcesToReserve(e, cq))
  ```

- **pkg/scheduler/scheduler.go:325** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:25:14Z
```diff
@@ -319,6 +320,31 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.Borrowing || e.assignment.RepresentativeMode() != flavorassigner.Preempt {
```

  ```suggestion
  	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
  ```
  
  If it's borrowing, but it fits, it gets Fits mode.

- **pkg/scheduler/scheduler.go:328** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:26:49Z
```diff
@@ -319,6 +320,31 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.Borrowing || e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := e.assignment.Usage
```

  You need to clone the map, so that it doesn't get modified.

- **pkg/scheduler/scheduler.go:335** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:35:55Z
```diff
@@ -319,6 +320,31 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.Borrowing || e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := e.assignment.Usage
+	for flavor, resourceUsage := range e.assignment.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+					goto calaculateReservation
```

  This doesn't look accurate. An assignment could have resources contained in different resource groups, and assigned different flavors.

- **pkg/scheduler/scheduler_test.go:1949** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:37:41Z
```diff
@@ -1894,3 +1904,81 @@ func TestRequeueAndUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestResourcesToReserve(t *testing.T) {
+	cases := []struct {
+		name               string
+		assignmentUsageMem int64
+		cqMemQuota         string
+		cqMemUsage         int64
+		assigmentMode      flavorassigner.FlavorAssignmentMode
+		borrowing          bool
+		wantReservedMem    int64
+	}{
+		{
+			name:               "Reserved memory less than assignment usage, preemption",
+			assignmentUsageMem: 50,
+			cqMemQuota:         "100",
+			cqMemUsage:         60,
+			assigmentMode:      flavorassigner.Preempt,
+			wantReservedMem:    40,
+		},
+		{
+			name:               "Reserved memory equal assignment usage, borrowing",
+			assignmentUsageMem: 50,
+			cqMemQuota:         "100",
+			cqMemUsage:         60,
+			borrowing:          true,
+			wantReservedMem:    50,
+		},
+		{
+			name:               "Reserved memory equal assignment usage, preemption",
+			assignmentUsageMem: 30,
+			cqMemQuota:         "100",
+			cqMemUsage:         60,
+			assigmentMode:      flavorassigner.Preempt,
+			wantReservedMem:    30,
+		},
+	}
+	for _, tc := range cases {
+		flavorName := "flavor"
+		t.Run(tc.name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			cq := utiltesting.MakeClusterQueue("cq").
+				Cohort("eng").
+				ResourceGroup(
```

  Add a second resource group and 2 flavors per group.

- **pkg/scheduler/scheduler_test.go:1909** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:38:59Z
```diff
@@ -1894,3 +1904,81 @@ func TestRequeueAndUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestResourcesToReserve(t *testing.T) {
```

  I prefer we don't test at this level.
  
  Better incorporate one or 2 test cases in `TestSchedule`.

- **test/integration/scheduler/scheduler_test.go:1353** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:40:59Z
```diff
@@ -1314,6 +1316,62 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqSharedResources, true)
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
```

  Unless you use `k8sClient.Update`, this shouldn't have any effect.

- **test/integration/scheduler/scheduler_test.go:1388** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:43:03Z
```diff
@@ -1314,6 +1316,62 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqSharedResources, true)
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
+
+			pendingWl := testing.MakeWorkload("pending-wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(9).Obj()
+			gomega.Expect(k8sClient.Create(ctx, pendingWl)).Should(gomega.Succeed())
+
+			borrowingWl := testing.MakeWorkload("player2-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(11).Obj()
+			gomega.Expect(k8sClient.Create(ctx, borrowingWl)).Should(gomega.Succeed())
+
+			blockedWl := testing.MakeWorkload("player3-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "1").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, blockedWl)).Should(gomega.Succeed())
+
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.None)
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, pendingWl, blockedWl)
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, borrowingWl)
+
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, matchingNS)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
```

  This is already done in `AfterEach`.

- **test/integration/scheduler/scheduler_test.go:1379** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:54:29Z
```diff
@@ -1314,6 +1316,62 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqSharedResources, true)
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
+
+			pendingWl := testing.MakeWorkload("pending-wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(9).Obj()
+			gomega.Expect(k8sClient.Create(ctx, pendingWl)).Should(gomega.Succeed())
+
+			borrowingWl := testing.MakeWorkload("player2-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(11).Obj()
+			gomega.Expect(k8sClient.Create(ctx, borrowingWl)).Should(gomega.Succeed())
+
+			blockedWl := testing.MakeWorkload("player3-a", matchingNS.Name).Queue(lqTeamA.
```

  Please add comments explaining why they should or should not be blocked.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-09T12:42:23Z

- **pkg/scheduler/scheduler.go:325** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-09T09:58:54Z
```diff
@@ -319,6 +320,31 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.Borrowing || e.assignment.RepresentativeMode() != flavorassigner.Preempt {
```

  Oh, you're right. I'm curious why borrowing is going after preemption in entries ordering, if it's considered as a Fit mode?

- **test/integration/scheduler/scheduler_test.go:1388** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-09T12:27:08Z
```diff
@@ -1314,6 +1316,62 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqSharedResources, true)
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
+
+			pendingWl := testing.MakeWorkload("pending-wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(9).Obj()
+			gomega.Expect(k8sClient.Create(ctx, pendingWl)).Should(gomega.Succeed())
+
+			borrowingWl := testing.MakeWorkload("player2-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(11).Obj()
+			gomega.Expect(k8sClient.Create(ctx, borrowingWl)).Should(gomega.Succeed())
+
+			blockedWl := testing.MakeWorkload("player3-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "1").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, blockedWl)).Should(gomega.Succeed())
+
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.None)
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, pendingWl, blockedWl)
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, borrowingWl)
+
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, matchingNS)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
```

  This is needed to cleanup CQs

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-09T19:27:40Z

- **pkg/scheduler/scheduler.go:325** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T19:27:40Z
```diff
@@ -319,6 +320,31 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.Borrowing || e.assignment.RepresentativeMode() != flavorassigner.Preempt {
```

  Because preemption implies reclaiming your nominal quota. Whereas borrowing implies going beyond it. We want to favor the workloads that don't go over nominal.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-09T20:23:19Z

- **pkg/scheduler/scheduler.go:322** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T19:30:34Z
```diff
@@ -319,6 +319,32 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
```

  ```suggestion
  // resourcesToReserve calculates how much of the available resources in the cq/cohort should be blocked for borrowing by the assignment.
  ```

- **pkg/scheduler/scheduler.go:330** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T20:11:40Z
```diff
@@ -319,6 +319,32 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
```

  I think you can use `cq.RGByResource` to obtain the quotas for a resource, instead of having to loop over the resourceGroups.
  
  https://github.com/kubernetes-sigs/kueue/blob/a7ad78db2ca32d2f10f1d8fff57c98f0d9b4a824/pkg/cache/clusterqueue.go#L47
  
  
  There is this function which you can use to populate that map in unit tests https://github.com/kubernetes-sigs/kueue/blob/a7ad78db2ca32d2f10f1d8fff57c98f0d9b4a824/pkg/cache/clusterqueue.go#L257
  
  but, in production, it is maintained by the cache and snapshot logic.

- **pkg/scheduler/scheduler.go:342** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T20:14:28Z
```diff
@@ -319,6 +319,32 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
+			}
+		}
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			// since assignment doesn't borrow, it's usage can't exceed nominal quota.
+			reservedUsage[flavor][resource] = min(usage, cqQuota[resource].Nominal-cq.Usage[flavor][resource])
```

  If the CQ is currently borrowing, Nominal-Usage is negative.
  
  Add a unit test to cover this case.

- **test/integration/scheduler/scheduler_test.go:1351** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T20:15:57Z
```diff
@@ -1314,6 +1316,76 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			// cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
```

  remove this line.

- **test/integration/scheduler/scheduler_test.go:1359** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T20:18:00Z
```diff
@@ -1314,6 +1316,76 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			// cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
+			gomega.Eventually(func() error {
+				var cq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cqTeamA), &cq)).To(gomega.Succeed())
+				cq.Spec.StopPolicy = ptr.To(kueue.Hold)
+				return k8sClient.Update(ctx, &cq)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			//pendingWl exceed nominal+borrowing quota and could not preempt due to low priority.
```

  ```suggestion
  			// pendingWl exceed nominal+borrowing quota and cannot preempt due to low priority.
  ```

- **test/integration/scheduler/scheduler_test.go:1364** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T20:19:19Z
```diff
@@ -1314,6 +1316,76 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			// cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
+			gomega.Eventually(func() error {
+				var cq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cqTeamA), &cq)).To(gomega.Succeed())
+				cq.Spec.StopPolicy = ptr.To(kueue.Hold)
+				return k8sClient.Update(ctx, &cq)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			//pendingWl exceed nominal+borrowing quota and could not preempt due to low priority.
+			pendingWl := testing.MakeWorkload("pending-wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(9).Obj()
+			gomega.Expect(k8sClient.Create(ctx, pendingWl)).Should(gomega.Succeed())
+
+			//borrowingWL borrows all shared resources and should be scheduled.
```

  ```suggestion
  			// borrowingWL can borrow shared resources, so it should be scheduled even if workloads
  			// in other cluster queues are waiting to reclaim nominal resources.
  ```

- **test/integration/scheduler/scheduler_test.go:1370** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T20:20:45Z
```diff
@@ -1314,6 +1316,76 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			// cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
+			gomega.Eventually(func() error {
+				var cq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cqTeamA), &cq)).To(gomega.Succeed())
+				cq.Spec.StopPolicy = ptr.To(kueue.Hold)
+				return k8sClient.Update(ctx, &cq)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			//pendingWl exceed nominal+borrowing quota and could not preempt due to low priority.
+			pendingWl := testing.MakeWorkload("pending-wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(9).Obj()
+			gomega.Expect(k8sClient.Create(ctx, pendingWl)).Should(gomega.Succeed())
+
+			//borrowingWL borrows all shared resources and should be scheduled.
+			borrowingWl := testing.MakeWorkload("player2-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(11).Obj()
+			gomega.Expect(k8sClient.Create(ctx, borrowingWl)).Should(gomega.Succeed())
+
+			//blockedWL should try to borrow resources from strictFIFO CQ, but should be blocked
+			//from borrowing because there is a pending workload in strictFIFO CQ.
```

  ```suggestion
  			// blockedWL wants to borrow resources from strictFIFO CQ, but should be blocked
  			// from borrowing because there is a pending workload in strictFIFO CQ.
  ```

- **test/integration/scheduler/scheduler_test.go:1388** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T20:23:16Z
```diff
@@ -1314,6 +1316,62 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqSharedResources, true)
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
+
+			pendingWl := testing.MakeWorkload("pending-wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(9).Obj()
+			gomega.Expect(k8sClient.Create(ctx, pendingWl)).Should(gomega.Succeed())
+
+			borrowingWl := testing.MakeWorkload("player2-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(11).Obj()
+			gomega.Expect(k8sClient.Create(ctx, borrowingWl)).Should(gomega.Succeed())
+
+			blockedWl := testing.MakeWorkload("player3-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "1").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, blockedWl)).Should(gomega.Succeed())
+
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.None)
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, pendingWl, blockedWl)
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, borrowingWl)
+
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, matchingNS)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
```

  ah ok, because you create the additional CQs.
  
  However, these lines won't run if the test fails. You can put these in a `defer` function right after the CQs are created.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-09T20:30:39Z

- **pkg/scheduler/scheduler.go:337** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-09T20:26:45Z
```diff
@@ -319,6 +319,32 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
```

  @alculquicondor In this implementation, the `cqQuota` will have only the last flavor's resources, right?
  So, should we add all resources to the `cqQuota`?
  
  ```go
  if cqFlavor.Name == flavor {
      for name, quota := range cqFlavor.Resources {
          cqQuota[name] = quota
      }
  }
  ```
  
  (This is a pseudo code.)

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-10T11:59:44Z

- **pkg/scheduler/scheduler.go:337** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-10T11:59:44Z
```diff
@@ -319,6 +319,32 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		cqQuota := make(map[corev1.ResourceName]*cache.ResourceQuota)
+		for _, rg := range cq.ResourceGroups {
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = cqFlavor.Resources
+				}
```

  My bad, you're right. Should be fixed now.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-10T12:00:50Z

- **pkg/scheduler/scheduler.go:325** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-10T12:00:50Z
```diff
@@ -319,6 +320,31 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.Borrowing || e.assignment.RepresentativeMode() != flavorassigner.Preempt {
```

  Ack

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-10T12:01:19Z

- **test/integration/scheduler/scheduler_test.go:1388** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-10T12:01:19Z
```diff
@@ -1314,6 +1316,62 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
+
+			strictFIFOLocalQueue := testing.MakeLocalQueue("strict-fifo-q", matchingNS.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+			lqTeamA := testing.MakeLocalQueue("team-a-lq", matchingNS.Name).ClusterQueue(cqTeamA.Name).Obj()
+
+			gomega.Expect(k8sClient.Create(ctx, strictFIFOLocalQueue)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lqTeamA)).Should(gomega.Succeed())
+
+			cqSharedResources := testing.MakeClusterQueue("shared-resources").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqSharedResources)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqSharedResources, true)
+
+			ginkgo.By("Creating workloads")
+			admittedWl1 := testing.MakeWorkload("wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl1)).Should(gomega.Succeed())
+
+			admittedWl2 := testing.MakeWorkload("player1-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(1).Obj()
+			gomega.Expect(k8sClient.Create(ctx, admittedWl2)).Should(gomega.Succeed())
+
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, admittedWl1, admittedWl2)
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.Hold)
+
+			pendingWl := testing.MakeWorkload("pending-wl", matchingNS.Name).Queue(strictFIFOLocalQueue.
+				Name).Request(corev1.ResourceCPU, "3").Priority(9).Obj()
+			gomega.Expect(k8sClient.Create(ctx, pendingWl)).Should(gomega.Succeed())
+
+			borrowingWl := testing.MakeWorkload("player2-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "5").Priority(11).Obj()
+			gomega.Expect(k8sClient.Create(ctx, borrowingWl)).Should(gomega.Succeed())
+
+			blockedWl := testing.MakeWorkload("player3-a", matchingNS.Name).Queue(lqTeamA.
+				Name).Request(corev1.ResourceCPU, "1").Priority(10).Obj()
+			gomega.Expect(k8sClient.Create(ctx, blockedWl)).Should(gomega.Succeed())
+
+			cqTeamA.Spec.StopPolicy = ptr.To(kueue.None)
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, pendingWl, blockedWl)
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, borrowingWl)
+
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, matchingNS)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
```

  yep, that what I had initially.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-10T12:01:55Z

- **pkg/scheduler/scheduler.go:322** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-10T12:01:55Z
```diff
@@ -319,6 +319,32 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
```

  I don't think it's accurate. This is not about borrowing.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-10T15:43:09Z

- **pkg/scheduler/scheduler.go:323** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T15:37:18Z
```diff
@@ -319,6 +319,35 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+// For Peempt mode it makes sure to not reserve resources more than nominal quota.
```

  ```suggestion
  // For Preempt mode it makes sure to not reserve resources more than nominal quota.
  ```

- **pkg/scheduler/scheduler.go:325** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T15:38:05Z
```diff
@@ -319,6 +319,35 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+// For Peempt mode it makes sure to not reserve resources more than nominal quota.
+// TODO: we can make reservation more accurate, by accounting how many resources
+// the assignment is going to preempt.
```

  I don't think this will ever be necessary. We need to block the quota regardless of whether it will come from preemption or it fits within the unused quota.

- **pkg/scheduler/scheduler.go:332** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T15:39:34Z
```diff
@@ -319,6 +319,35 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+// For Peempt mode it makes sure to not reserve resources more than nominal quota.
+// TODO: we can make reservation more accurate, by accounting how many resources
+// the assignment is going to preempt.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
```

  This can be done once in the outer loop, without the conditional, as the flavor is unique in `e.assignment.Usage`.

- **pkg/scheduler/scheduler.go:341** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T15:40:34Z
```diff
@@ -319,6 +319,35 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+// For Peempt mode it makes sure to not reserve resources more than nominal quota.
+// TODO: we can make reservation more accurate, by accounting how many resources
+// the assignment is going to preempt.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					continue
```

  break

- **pkg/scheduler/scheduler.go:344** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T15:42:07Z
```diff
@@ -319,6 +319,35 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+// For Peempt mode it makes sure to not reserve resources more than nominal quota.
+// TODO: we can make reservation more accurate, by accounting how many resources
+// the assignment is going to preempt.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					continue
+				}
+			}
+			// since assignment doesn't borrow, it's usage can't exceed nominal quota.
```

  but this is the usage of the CQ, which can be borrowing, so it can be negative.

- **test/integration/scheduler/scheduler_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T15:43:06Z
  There are still unresolved comments here

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-10T15:44:10Z

- **pkg/scheduler/scheduler.go:322** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T15:44:10Z
```diff
@@ -319,6 +319,32 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of available resources in cq assignment should be reserved.
```

  True, not necessarily.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-10T16:31:38Z

Updated reservedResources if CQ is borrowing

- **pkg/scheduler/scheduler.go:332** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-10T16:28:27Z
```diff
@@ -319,6 +319,35 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+// For Peempt mode it makes sure to not reserve resources more than nominal quota.
+// TODO: we can make reservation more accurate, by accounting how many resources
+// the assignment is going to preempt.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
```

  Is the flavor unique? The structure allows to have multiple flavors.

- **pkg/scheduler/scheduler_test.go:1909** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-10T16:30:25Z
```diff
@@ -1894,3 +1904,81 @@ func TestRequeueAndUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestResourcesToReserve(t *testing.T) {
```

  TestSchedule test quite complex logic. I've added integration tests to test the full functionality

- **test/integration/scheduler/scheduler_test.go:1** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-10T16:31:00Z
  Should be resolved now

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-10T16:52:56Z

- **pkg/scheduler/scheduler.go:332** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T16:52:56Z
```diff
@@ -319,6 +319,35 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+// For Peempt mode it makes sure to not reserve resources more than nominal quota.
+// TODO: we can make reservation more accurate, by accounting how many resources
+// the assignment is going to preempt.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
```

  It is unique in the `e.assignment.Usage` map.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-10T16:54:12Z

- **pkg/scheduler/scheduler.go:346** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T16:54:12Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  We don't want to block up to the borrowing limit. We only want to block up to nominal.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-10T17:06:46Z

- **pkg/scheduler/scheduler_test.go:1920** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T17:04:02Z
```diff
@@ -1894,3 +1905,147 @@ func TestRequeueAndUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestResourcesToReserve(t *testing.T) {
+	resourceFlavors := []*kueue.ResourceFlavor{
+		{ObjectMeta: metav1.ObjectMeta{Name: "on-demand"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "spot"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "model-a"}},
+		{ObjectMeta: metav1.ObjectMeta{Name: "model-b"}},
+	}
+	cq := utiltesting.MakeClusterQueue("cq").
+		Cohort("eng").
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas("on-demand").
+				Resource(corev1.ResourceMemory, "100", "10").Obj(),
```

  we also need to test nil borrowingLimit

- **pkg/scheduler/scheduler.go:346** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T17:06:40Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  @mimowo what do you think should we do if borrowing with preemption? I guess we should block all of the usage in that case?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-10T19:02:23Z

- **pkg/scheduler/scheduler.go:346** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-10T19:02:23Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  Updated, cq usage shouldn't go over cqQuota.Nominal+*cqQuota.BorrowingLimit if borrowing limit is set

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-10T19:17:06Z

- **pkg/scheduler/scheduler.go:330** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T19:13:32Z
```diff
@@ -319,6 +319,38 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
```

  ```suggestion
  		reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
  		for resource, usage := range resourceUsage {
  ```

- **test/integration/scheduler/scheduler_test.go:1327** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T19:16:45Z
```diff
@@ -1314,6 +1316,77 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
```

  ```suggestion
  			defer func() {
  				gomega.Expect(util.DeleteNamespace(ctx, k8sClient, matchingNS)).To(gomega.Succeed())
  				util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
  			}
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-10T19:44:44Z

- **test/integration/scheduler/scheduler_test.go:1327** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T19:44:44Z
```diff
@@ -1314,6 +1316,77 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 			util.ExpectPendingWorkloadsMetric(strictFIFOClusterQ, 0, 1)
 		})
+
+		ginkgo.It("Pending workload with StrictFIFO doesn't block other CQ from borrowing from a third CQ", func() {
+			ginkgo.By("Creating ClusterQueues and LocalQueues")
+			cqTeamA := testing.MakeClusterQueue("team-a").
+				ResourceGroup(*testing.MakeFlavorQuotas(onDemandFlavor.Name).Resource(corev1.ResourceCPU, "5").Obj()).
+				Cohort(chName).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqTeamA)).Should(gomega.Succeed())
+			defer util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cqTeamA, true)
```

  missed this one?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T08:09:48Z

- **pkg/scheduler/scheduler.go:346** by [@mimowo](https://github.com/mimowo) — 2024-01-11T08:09:48Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  > @mimowo what do you think should we do if borrowing with preemption? I guess we should block all of the usage in that case?
  
  TBH, I'm not sure, I'm wondering if we can simplify 
  ```
  if cqQuota.BorrowingLimit == nil {
    reservedUsage[flavor][resource] = usage
  } else {
    reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
  }
  ```
  to just 
  ```
  reservedUsage[flavor][resource] = usage
  ```
  @yaroslava-serdiuk do you know a scenario when this is needed? 
  
  This looks odd to me at the moment, because in the scenario of single shared CQ as [here](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing#story-1) when `cqQuota.Nominal=0`, then if `borrowingLimit=null` we reserve `usage`, but if `borrowingLimit=nominal for the shared (essentially the same a null)`, and `cq.Usage[flavor][resource]=borrowigLimit`, then we reserve `0`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T08:13:48Z

- **pkg/scheduler/scheduler.go:346** by [@mimowo](https://github.com/mimowo) — 2024-01-11T08:13:48Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  > we shouldn't go over cqQuota.Nominal+*cqQuota.BorrowingLimit if borrowing limit is set 
  
  Correct, but this is verified already in https://github.com/kubernetes-sigs/kueue/blob/4bc332a75fe221e6dc92db269ae3832da884527a/pkg/scheduler/flavorassigner/flavorassigner.go#L563-L566. I think it is ok to go beyond `cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource]`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T08:49:39Z

- **pkg/scheduler/scheduler.go:346** by [@mimowo](https://github.com/mimowo) — 2024-01-11T08:49:38Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  I tested that the existing integration tests pass if we do `reservedUsage[flavor][resource] = usage` when `a.Borrowing=true`. Still, I'm not 100% sure what the logic should be in case assignment needs borrowing, but this PR changed logic for `a.Borrowing=true` without an integration test for this scenario.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-01-11T10:46:35Z

- **pkg/scheduler/scheduler.go:346** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-11T10:46:35Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  Had discussion with @mimowo offline. I explained the condition:
  ```if cqQuota.BorrowingLimit == nil {
    reservedUsage[flavor][resource] = usage
  } else {
    reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
  }
  ```
  However this is unlikely will be used, since the borrowing limit is often nil.
  I also constructed example when the condition unblock another workload from borrowing: 
  https://github.com/kubernetes-sigs/kueue/assets/43413443/3ee2a4f6-d8ee-4bb4-96d8-2e9662e8869c

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T12:03:10Z

- **pkg/scheduler/scheduler.go:346** by [@mimowo](https://github.com/mimowo) — 2024-01-11T12:03:10Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  Thank you for the sync and explanations indeed. 
  
  One thing that it let me understand is that the StrictFIFO isn't really the main point here (as the title suggests). If the queue has only one workload it does not matter what is its type. Maybe we could say in the release notes:
  "Fix a bug that a preempting workload in one CQ could block another pending workload in another CQ even if the other workload could be scheduled by borrowing".
  
  Regarding the extra condition I think it adds complexity (and the code is already complex). The scenario isn't tested currently by integration tests. Also, the importance of the scenario from the user perspective seems questionable, because it is only relevant if `borrowingLimit<nominalQuota of shared CQ`. I could open a PR to simplify. WDYT @alculquicondor ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-11T12:40:54Z

- **pkg/scheduler/scheduler.go:346** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-11T12:40:54Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  >  Also, the importance of the scenario from the user perspective seems questionable, because it is only relevant if borrowingLimit<nominalQuota of shared CQ. 
  
  @mimowo, Could you clarify this situation? Which does this mean, `a: borrowingLimit<(nominalQuota of shared CQ)` or `b: (borrowingLimit<nominalQuota) of shared CQ`?
  
  If you mean `a`, I think `a` will often happen. I guess admins want to set `borrowingLimit<(nominalQuota of shared CQ)` in each ClusterQueue since admins want to prevent situations where one of the CQ monopolizes all shared capacity.
  
  If my understanding is incorrect , please let me know.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T14:53:01Z

- **pkg/scheduler/scheduler.go:346** by [@mimowo](https://github.com/mimowo) — 2024-01-11T14:53:01Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  > @mimowo, Could you clarify this situation? Which does this mean, `a: borrowingLimit<(nominalQuota of shared CQ)` or `b: (borrowingLimit<nominalQuota) of shared CQ`?
  
  I meant (a) `borrowingLimit<(nominalQuota of shared CQ)`. 
  
  > If you mean a, I think a will often happen. I guess admins want to set borrowingLimit<(nominalQuota of shared CQ) in each ClusterQueue since admins want to prevent situations where one of the CQ monopolizes all shared capacity.
  
  Makes sense, I was thinking about the scenario as in [here](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing#story-1), but both are relevant. 
  
  So the remaining question: do we care about the scenario, that a workload that is borrowing (B) could get blocked by another workload (A) that is preempting and borrowing in the cohort. The trade-off does not seem obviousm because letting the other workload run may cause the waiting workload wait longer.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-11T15:57:48Z

- **pkg/scheduler/scheduler.go:346** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-11T15:57:47Z
```diff
@@ -319,6 +319,40 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+// resourcesToReserve calculates how much of the available resources in cq/cohort assignment should be reserved.
+func resourcesToReserve(e *entry, cq *cache.ClusterQueue) cache.FlavorResourceQuantities {
+	if e.assignment.RepresentativeMode() != flavorassigner.Preempt {
+		return e.assignment.Usage
+	}
+	reservedUsage := make(cache.FlavorResourceQuantities)
+	for flavor, resourceUsage := range e.assignment.Usage {
+		for resource, usage := range resourceUsage {
+			if _, ok := reservedUsage[flavor]; !ok {
+				reservedUsage[flavor] = make(map[corev1.ResourceName]int64)
+			}
+			rg := cq.RGByResource[resource]
+			cqQuota := cache.ResourceQuota{}
+			for _, cqFlavor := range rg.Flavors {
+				if cqFlavor.Name == flavor {
+					cqQuota = *cqFlavor.Resources[resource]
+					break
+				}
+			}
+			if cq.Usage[flavor][resource] <= cqQuota.Nominal {
+				// since cq and assignment doesn't borrow, cq usage can't exceed nominal quota.
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal-cq.Usage[flavor][resource])
+			} else if cqQuota.BorrowingLimit != nil {
+				// if cq is borrowing, cq usage can't exceed nominal + borrowing limit
+				reservedUsage[flavor][resource] = min(usage, cqQuota.Nominal+*cqQuota.BorrowingLimit-cq.Usage[flavor][resource])
```

  Feel free to open a PR
