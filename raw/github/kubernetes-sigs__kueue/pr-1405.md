# PR #1405: Support preemption while borrowing [KEP]

**Summary**: Support preemption while borrowing [KEP]

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1405

**Last updated**: 2024-02-10T11:48:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `preemption-with-borrowing-kep` → `main`
- **Created**: 2023-12-05T13:03:40Z
- **Updated**: 2024-02-10T11:48:11Z
- **Merged**: 2023-12-18T17:07:16Z
- **Closed**: 2023-12-18T17:07:17Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk), [@trasc](https://github.com/trasc)
- **Changed files**: 2   **+510 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #1337 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note

```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-12-05T13:03:48Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 656f3ca7c98963034b553c716f975a9f2c4a65fa |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/658070f7804508000850fe49 |

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-08T15:17:38Z

/assign @yaroslava-serdiuk

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-12T13:34:34Z

@yaroslava-serdiuk thank you for reviewing. I think I have addressed the comments, PTAL.

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-12T16:42:57Z

/retest

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-13T14:35:49Z

I think the folder name should be updated and release note should be added 
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-13T14:35:55Z

LGTM label has been added.  <details>Git tree hash: bf12c79803f38cd148975e27f50a9b3b3c26cdab</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:59:47Z

/test  pull-kueue-test-unit-main

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T10:27:02Z

/release-note-none

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-15T10:32:52Z

/remove-kind feature
/kind documentation

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-18T15:59:46Z

/approve
Leaving lgtm to @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-18T16:43:05Z

LGTM label has been added.  <details>Git tree hash: f6cf2d3dfdca522405b3275564c3bce2d0858721</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-18T16:43:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1405#issuecomment-1860895822" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1405#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1405#pullrequestreview-1787305674" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-10T13:25:22Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:74** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-10T13:25:22Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
```

  Can you add why we need to extend API, i.e. why modifying scheduler won't work?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-11T11:41:16Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:156** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-11T11:32:49Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
```

  Hm, it looks like this case could happen inside one ClusterQueue with regular preemption, isn't it?

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:74** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-11T11:35:07Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
```

  I see, we would like to have preemption while borrowing never as well.

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:216** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-11T11:38:36Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort=LowerPriority.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"borrowFromCohortConfig,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy ReclaimWithinCohortWhileBorrowingPolicy `json:"whileBorrowingPolicy,omitempty"`
+
+	// priorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// the specified level.
+	//
+	// +optional
+	PriorityThreshold *int32 `json:"threshold,omitempty"`
```

  Can you add a user case for this field, please?

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:209** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-11T11:41:00Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort=LowerPriority.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"borrowFromCohortConfig,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy ReclaimWithinCohortWhileBorrowingPolicy `json:"whileBorrowingPolicy,omitempty"`
```

  It seems that currently WhileBorrowingPolicy is the same as enable/disable PreemtionWhileBorrowing. Having a PriorityThreshold fielsd we may not use WhileBorrowingPolicy at all: if PriorityThreshold is 0, then PreemtionWhileBorrowing is disabled.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-11T12:24:33Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:74** by [@mimowo](https://github.com/mimowo) — 2023-12-11T12:24:33Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
```

  Yes, the main motivation is the ability to Disable it (setting Never). Users may want to disable. When it is enabled, it might be that there are workloads for which we want to attempt preemption, but because there are no lower priority workloads, the workload would keep blocking the queue. This is similar reason, for which we allow users to disable preemption when within ClusterQueue, or within cohort (but without borrowing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-11T12:34:06Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:74** by [@mimowo](https://github.com/mimowo) — 2023-12-11T12:34:06Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
```

  I have updated the doc with the risk, explaining the need for the API, and disabling.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-11T12:39:59Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:156** by [@mimowo](https://github.com/mimowo) — 2023-12-11T12:39:59Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
```

  Yeah, good point. For example, when `.preemption.withinClusterQueue: Any` - we offer `Any`. I think it might an argument for `Policy` enum, rather than just `Enable/Disable`, because maybe in the future we want to offer `Any` in this case also.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-11T12:43:17Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:156** by [@mimowo](https://github.com/mimowo) — 2023-12-11T12:43:16Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
```

  Added a note in alternatives, that Policy might be needed in the future for the reason.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-11T12:44:54Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:216** by [@mimowo](https://github.com/mimowo) — 2023-12-11T12:44:54Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort=LowerPriority.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"borrowFromCohortConfig,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy ReclaimWithinCohortWhileBorrowingPolicy `json:"whileBorrowingPolicy,omitempty"`
+
+	// priorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// the specified level.
+	//
+	// +optional
+	PriorityThreshold *int32 `json:"threshold,omitempty"`
```

  Sure, I will add a story. I think the reason is simply to give an admin more control. It was inspired by the note from @alculquicondor : https://github.com/kubernetes-sigs/kueue/issues/1337#issuecomment-1814766613.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-11T13:15:41Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:156** by [@mimowo](https://github.com/mimowo) — 2023-12-11T13:15:41Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
```

  Actually no, this `.preemption.withinClusterQueue: Any` isn't supported currently, probably for this exact reason. We want to design the API so that users don't run into it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-11T13:17:10Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:209** by [@mimowo](https://github.com/mimowo) — 2023-12-11T13:17:10Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort=LowerPriority.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"borrowFromCohortConfig,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy ReclaimWithinCohortWhileBorrowingPolicy `json:"whileBorrowingPolicy,omitempty"`
```

  Yes, but I seems having explicit `Never` better captures the intention, that setting the threshold to `0`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-11T16:34:19Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:216** by [@mimowo](https://github.com/mimowo) — 2023-12-11T16:34:18Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort=LowerPriority.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"borrowFromCohortConfig,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy ReclaimWithinCohortWhileBorrowingPolicy `json:"whileBorrowingPolicy,omitempty"`
+
+	// priorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// the specified level.
+	//
+	// +optional
+	PriorityThreshold *int32 `json:"threshold,omitempty"`
```

  I synced on this with @alculquicondor offline, and I add the Story 2. PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T12:52:15Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:216** by [@mimowo](https://github.com/mimowo) — 2023-12-12T12:52:15Z
```diff
@@ -0,0 +1,373 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Minimal API](#minimal-api)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I setup the following configuration:
+there are multiple cluster queues in a cohort, corresponding to different teams.
+All of the cluster queues have `nominalQuota: 0`, and `borrowingLimit>0`. In
+this cohort, there is also a single shared cluster queue with `nominalQuota>0`,
+and `borrowingLimit=0`. In this setup every workload requires borrowing. In this
+setup I would like to enable preemption for high priority workloads.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort=LowerPriority.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"borrowFromCohortConfig,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy ReclaimWithinCohortWhileBorrowingPolicy `json:"whileBorrowingPolicy,omitempty"`
+
+	// priorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// the specified level.
+	//
+	// +optional
+	PriorityThreshold *int32 `json:"threshold,omitempty"`
```

  I combined the two stories into one, because Story-2 was a more detailed version of Story 1. I also provide more details.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-12T17:04:20Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:75** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T15:45:49Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
```

  here you mention within CQ, but not in the goals. Are we supporting it or not?

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:90** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T15:50:18Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
```

  I thought maybe "reclaim" might not be accurate, as the workload will be using more than the nominalQuota for the CQ. However, it still fits the definition that we have for the API:
  
  ```
  reclaimWithinCohort determines whether a pending Workload can preempt Workloads from other ClusterQueues in the cohort that are using more than their nominal quota
  ```

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:138** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T15:52:27Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
```

  Nit:
  
  Maybe put these in a table, so it's easier to visualize.

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:134** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T15:53:54Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
```

  worth mentioning the `Shared` CQ here.

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:177** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T15:57:32Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
```

  ```suggestion
  `.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
  ```
  
  Maybe this is a justification to have something like:
  ```yaml
  preemption:
    borrowWithinCohort:
      policy: Never | LowerPriority
      priorityThreshold: 100
  ```

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:183** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T15:59:28Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+#### Workload nominated for preemption cannot find a candidate
+
+It is possible, that a workload nominated to be admitted awaits for preemption
+candidates continuously, because all the workloads are of higher or equal priority.
+In that scenario, the workload will continuously block the ClusterQueue, even
```

  Are you referring to StrictFIFO specifically? That is the expectation, so it's not a risk.

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:244** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T16:01:39Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+#### Workload nominated for preemption cannot find a candidate
+
+It is possible, that a workload nominated to be admitted awaits for preemption
+candidates continuously, because all the workloads are of higher or equal priority.
+In that scenario, the workload will continuously block the ClusterQueue, even
+though some smaller workloads could be admitted in the meanwhile.
+
+The risk is similar to enabling currently existing modes of preemption.
+We mitigate this risk by making the feature opt-in per ClusterQueue to
+allow more flexibility to users.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort is not `Never`.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"reclaimWithCohort,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy PreemptionPolicy `json:"whileBorrowingPolicy,omitempty"`
+
+	// priorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// the specified level.
+	//
+	// +optional
+	PriorityThreshold *int32 `json:"priorityThreshold,omitempty"`
```

  maxPriorityThreshold?

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:317** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T16:03:19Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+#### Workload nominated for preemption cannot find a candidate
+
+It is possible, that a workload nominated to be admitted awaits for preemption
+candidates continuously, because all the workloads are of higher or equal priority.
+In that scenario, the workload will continuously block the ClusterQueue, even
+though some smaller workloads could be admitted in the meanwhile.
+
+The risk is similar to enabling currently existing modes of preemption.
+We mitigate this risk by making the feature opt-in per ClusterQueue to
+allow more flexibility to users.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort is not `Never`.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"reclaimWithCohort,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy PreemptionPolicy `json:"whileBorrowingPolicy,omitempty"`
+
+	// priorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// the specified level.
+	//
+	// +optional
+	PriorityThreshold *int32 `json:"priorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityPolicy.Preempt` when the workload does not fit, but
+  there is enough quota within the cohort
+- modify the [`GetTargets`](https://github.com/kubernetes-sigs/kueue/blob/53d4b657655aff5df3748e65b4226cf11e50eeba/pkg/scheduler/preemption/preemption.go#L77)
+and the [`minimalPreemptions`](https://github.com/kubernetes-sigs/kueue/blob/53d4b657655aff5df3748e65b4226cf11e50eeba/pkg/scheduler/preemption/preemption.go#L159) functions
+to only, if borrowing, include candidates which satisfy the new API's constrains.
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+- `pkg/scheduler/flavorassigner`: `7 Dec 2023` - `79.2%` (to indicate the workload which needs to borrow, that it can also preempt)
+- `pkg/scheduler/preemption`: `7 Dec 2023` - `94%` (to update the `GetTargets` function, identifying the set of workloads to preempt)
+
+#### Integration tests
+
+We are going to cover the following scenarios:
+- in the setup described in the [Story 1](#story-1), verify:
+  - a workload which require borrowing can preempt a lower priority workload
+  - a workload which require borrowing cannot preempt a higher priority workload
+  - a workload which require borrowing cannot preempt a lower priority (but above the threshold) workload.
```

  also cannot preempt a workload that isn't borrowing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-12T17:24:19Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:183** by [@mimowo](https://github.com/mimowo) — 2023-12-12T17:24:19Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+#### Workload nominated for preemption cannot find a candidate
+
+It is possible, that a workload nominated to be admitted awaits for preemption
+candidates continuously, because all the workloads are of higher or equal priority.
+In that scenario, the workload will continuously block the ClusterQueue, even
```

  No I was actually thinking here about BestEffort. I have not made an experiment yet, but I think in this scenario the nominated workload can block the top of the queue if the flavorassigner says Preempt, but it cannot actually find a candidate. Am I missing something?
  
  I was thinking about a need to justify why we need the opt-in API at all and not enable it by default, so was thinking why a user may not be happy if enabled.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-12T18:37:20Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:183** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T18:37:20Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+#### Workload nominated for preemption cannot find a candidate
+
+It is possible, that a workload nominated to be admitted awaits for preemption
+candidates continuously, because all the workloads are of higher or equal priority.
+In that scenario, the workload will continuously block the ClusterQueue, even
```

  If it can't find candidates, then the workload is put in "inadmissible"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T08:57:16Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:75** by [@mimowo](https://github.com/mimowo) — 2023-12-13T08:57:15Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
```

  Yeah I mentioned within CQ just to say this is one of the supported options. However, I think this is unnecessary to say. Simplified and rephrased.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T08:57:51Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:90** by [@mimowo](https://github.com/mimowo) — 2023-12-13T08:57:50Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
```

  Correct

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T08:58:03Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:138** by [@mimowo](https://github.com/mimowo) — 2023-12-13T08:58:02Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
```

  Makes sense, table added

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T08:58:14Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:134** by [@mimowo](https://github.com/mimowo) — 2023-12-13T08:58:14Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
```

  Done, and rephrased a little

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T09:00:21Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:177** by [@mimowo](https://github.com/mimowo) — 2023-12-13T09:00:21Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
```

  I agree, and was hesitant about a new option vs config for reclaimWithinCohort. I think it is enough to explain in API comments how it relates to reclaimWithinCohort.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T09:00:42Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:183** by [@mimowo](https://github.com/mimowo) — 2023-12-13T09:00:42Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+#### Workload nominated for preemption cannot find a candidate
+
+It is possible, that a workload nominated to be admitted awaits for preemption
+candidates continuously, because all the workloads are of higher or equal priority.
+In that scenario, the workload will continuously block the ClusterQueue, even
```

  Indeed, removed the risk.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T09:01:16Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:244** by [@mimowo](https://github.com/mimowo) — 2023-12-13T09:01:15Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+#### Workload nominated for preemption cannot find a candidate
+
+It is possible, that a workload nominated to be admitted awaits for preemption
+candidates continuously, because all the workloads are of higher or equal priority.
+In that scenario, the workload will continuously block the ClusterQueue, even
+though some smaller workloads could be admitted in the meanwhile.
+
+The risk is similar to enabling currently existing modes of preemption.
+We mitigate this risk by making the feature opt-in per ClusterQueue to
+allow more flexibility to users.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort is not `Never`.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"reclaimWithCohort,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy PreemptionPolicy `json:"whileBorrowingPolicy,omitempty"`
+
+	// priorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// the specified level.
+	//
+	// +optional
+	PriorityThreshold *int32 `json:"priorityThreshold,omitempty"`
```

  Yeah, makes sense to be explicit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T09:02:16Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:317** by [@mimowo](https://github.com/mimowo) — 2023-12-13T09:02:16Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`.
+
+All these four ClusterQueues we have `nominalQuota=0`, and `borrowingLimit>0`.
+The actual quota is exposed by the `Shared` ClusterQueue. The `Shared`
+ClusterQueue supports both teams, it has `nominalQuota>0`, and `borrowingLimit=0`.
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.reclaimWithinCohortConfig.whileBorrowingPolicy: LowerPriority`.
+
+#### Workload nominated for preemption cannot find a candidate
+
+It is possible, that a workload nominated to be admitted awaits for preemption
+candidates continuously, because all the workloads are of higher or equal priority.
+In that scenario, the workload will continuously block the ClusterQueue, even
+though some smaller workloads could be admitted in the meanwhile.
+
+The risk is similar to enabling currently existing modes of preemption.
+We mitigate this risk by making the feature opt-in per ClusterQueue to
+allow more flexibility to users.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// reclaimWithinCohortConfig extends the configuration for reclaimWithinCohort
+	// to allow preemption within cohort while borrowing. This configuration
+	// can only be enabled when reclaimWithinCohort is not `Never`.
+	ReclaimWithinCohortConfig *ReclaimWithinCohortConfig `json:"reclaimWithCohort,omitempty"`
+  ...
+}
+
+type ReclaimWithinCohortWhileBorrowingPolicy string
+
+const (
+	ReclaimWithinCohortWhileBorrowingPolicyNever         ReclaimWithinCohortWhileBorrowingPolicy = "Never"
+	ReclaimWithinCohortWhileBorrowingPolicyLowerPriority ReclaimWithinCohortWhileBorrowingPolicy = "LowerPriority"
+)
+
+// ReclaimWithinCohortConfig contains additional configuration for reclaimWithinCohort
+// which allows to preempt workloads within cohort while borrowing.
+type ReclaimWithinCohortConfig struct {
+	// whileBorrowingPolicy determines additional policy for preemption while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	WhileBorrowingPolicy PreemptionPolicy `json:"whileBorrowingPolicy,omitempty"`
+
+	// priorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// the specified level.
+	//
+	// +optional
+	PriorityThreshold *int32 `json:"priorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityPolicy.Preempt` when the workload does not fit, but
+  there is enough quota within the cohort
+- modify the [`GetTargets`](https://github.com/kubernetes-sigs/kueue/blob/53d4b657655aff5df3748e65b4226cf11e50eeba/pkg/scheduler/preemption/preemption.go#L77)
+and the [`minimalPreemptions`](https://github.com/kubernetes-sigs/kueue/blob/53d4b657655aff5df3748e65b4226cf11e50eeba/pkg/scheduler/preemption/preemption.go#L159) functions
+to only, if borrowing, include candidates which satisfy the new API's constrains.
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+- `pkg/scheduler/flavorassigner`: `7 Dec 2023` - `79.2%` (to indicate the workload which needs to borrow, that it can also preempt)
+- `pkg/scheduler/preemption`: `7 Dec 2023` - `94%` (to update the `GetTargets` function, identifying the set of workloads to preempt)
+
+#### Integration tests
+
+We are going to cover the following scenarios:
+- in the setup described in the [Story 1](#story-1), verify:
+  - a workload which require borrowing can preempt a lower priority workload
+  - a workload which require borrowing cannot preempt a higher priority workload
+  - a workload which require borrowing cannot preempt a lower priority (but above the threshold) workload.
```

  added

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-13T12:52:44Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:141** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-13T12:52:44Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
```

  For the setup you described we don't need to have a Standard and Best-Effort CQs, one CQ with appropriate threshold is enough, correct?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-13T12:57:00Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:90** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-13T12:57:00Z
```diff
@@ -0,0 +1,460 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+    - [Workload nominated for preemption cannot find a candidate](#workload-nominated-for-preemption-cannot-find-a-candidate)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Dedicated policy enum - ReclaimWithinCohortWhileBorrowingPolicy](#dedicated-policy-enum---reclaimwithincohortwhileborrowingpolicy)
+  - [Minimal API](#minimal-api)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, preemption can only happen within ClusterQueue, or within cohort, but
+only if the workload that wants to be submitted does not require borrowing.
+This requirement is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
```

  +1

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-13T13:09:32Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:211** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-13T13:09:31Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *ReclaimWithinCohortConfig `json:"reclaimWithCohort,omitempty"`
```

  ```suggestion
  	BorrowWithinCohort * BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T13:09:35Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:141** by [@mimowo](https://github.com/mimowo) — 2023-12-13T13:09:35Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
```

  It would not be enough to justify `MaxPriorityThreshold`. If we had just two CQs, one with high priorities, one with low priorities, then `LowerPriority` would be enough.  Since we would only have one `Standard` CQ the preemption within would be controlled by `preemption.withinClusterQueue`, rather than `borrowWithinCohort`.
  
  So, it is important in this setup, that we have two CQs with high priority workloads (`Standard`). Now, if we only had `LowerPriority`, then it would still allow that a workload in `A_Standard` preempts a workload in `B_Standard`. By introducing `MaxPriorityThreashold` between `Standard` and `Best-Effort` we make sure no workloads are preempted in `Standard` CQs.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-13T13:57:11Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:141** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-13T13:15:37Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
```

  I see, we don't allow preemption within the CQ here

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:209** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-13T13:22:20Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort * BorrowWithinCohort `json:"reclaimWithCohort,omitempty"`
```

  ```suggestion
  	BorrowWithinCohort * BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
  ```

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:248** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-13T13:57:02Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort * BorrowWithinCohort `json:"reclaimWithCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityPolicy.Preempt` when the workload does not fit, but
```

  I don't think we should return Preempt mode, because Preempt mode here is used for preemption within cluster queue and so workload with such mode is goes before any other workload that requires borrowing in scheduler: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/scheduler.go#L499

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-13T14:05:57Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:248** by [@mimowo](https://github.com/mimowo) — 2023-12-13T14:05:57Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort * BorrowWithinCohort `json:"reclaimWithCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityPolicy.Preempt` when the workload does not fit, but
```

  Hm, possibly, I wasn't sure about that indeed, maybe we need `PreemptWithBorrowing`. 
  
  However, IIUC, `Preempt` can be returned here independently if the actual preemption is within ClusterQueue, or reclaimWithinCohort is used.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-13T14:19:57Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:248** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-13T14:19:57Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort * BorrowWithinCohort `json:"reclaimWithCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityPolicy.Preempt` when the workload does not fit, but
```

  Right, sorry. The case is that workloads that require (just) borrowing will also have preempt mode and borrowing > 0. The question is do we want to sort workload that require borrowing without preemption before workloads that have borrowing + preemption, or just priority sorting would be sufficient here?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-12-13T14:32:53Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:248** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-12-13T14:32:53Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort * BorrowWithinCohort `json:"reclaimWithCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityPolicy.Preempt` when the workload does not fit, but
```

  Anyway, this is an implementation detail and probably out of scope for this kep

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T15:51:42Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T15:48:21Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
```

  ```suggestion
  # KEP-1337: Preempt within cohort while borrowing
  ```

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:100** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T15:49:20Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
```

  Also preempt withinClusterQueue while borrowing is non-goal

- **keps/1337-reclaim-within-cohort-while-borrowing/kep.yaml:11** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T15:51:28Z
```diff
@@ -0,0 +1,24 @@
+title: Reclaim within cohort while borrowing
+kep-number: 1337
+authors:
+  - "@mimowo"
+status: implementable
+creation-date: 2023-12-05
+reviewers:
+  - "@yaroslava-serdiuk"
+  - "@mwielgus"
+approvers:
+  - "@tenzen-y"
```

  @tenzen-y do you want to review this?

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T15:51:40Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
```

  and update the file name as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-14T09:13:13Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:248** by [@mimowo](https://github.com/mimowo) — 2023-12-14T09:13:13Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort * BorrowWithinCohort `json:"reclaimWithCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityPolicy.Preempt` when the workload does not fit, but
```

  > The question is do we want to sort workload that require borrowing without preemption before workloads that have borrowing + preemption, or just priority sorting would be sufficient here?
  
  IIUC currently the workload ordering code doesn't take preempt into account.  It orders based on
  no need to borrow, then (optional) priority, finally `GetQueueOrderTimestamp`.
  
  A different ordering might be useful, but it is hard to predict which one is better without a use case, so I would suggest to wait for users feedback and keep it out of scope for the KEP. Will add to non-goals.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-14T09:18:19Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:1** by [@mimowo](https://github.com/mimowo) — 2023-12-14T09:18:19Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-14T09:18:29Z

- **keps/1337-reclaim-within-cohort-while-borrowing/README.md:100** by [@mimowo](https://github.com/mimowo) — 2023-12-14T09:18:29Z
```diff
@@ -0,0 +1,482 @@
+# KEP-1337: Reclaim within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-14T15:54:40Z

- **keps/1337-preempt-within-cohort-while-borrowing/kep.yaml:11** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-14T15:54:40Z
```diff
@@ -0,0 +1,24 @@
+title: Preempt within cohort while borrowing
+kep-number: 1337
+authors:
+  - "@mimowo"
+status: implementable
+creation-date: 2023-12-05
+reviewers:
+  - "@yaroslava-serdiuk"
+  - "@mwielgus"
+approvers:
+  - "@tenzen-y"
```

  @tenzen-y we'll assume lazy consensus by EoW.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-15T08:30:17Z

- **keps/1337-preempt-within-cohort-while-borrowing/kep.yaml:11** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T08:30:17Z
```diff
@@ -0,0 +1,24 @@
+title: Preempt within cohort while borrowing
+kep-number: 1337
+authors:
+  - "@mimowo"
+status: implementable
+creation-date: 2023-12-05
+reviewers:
+  - "@yaroslava-serdiuk"
+  - "@mwielgus"
+approvers:
+  - "@tenzen-y"
```

  ACK

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-15T08:30:28Z

- **keps/1337-preempt-within-cohort-while-borrowing/kep.yaml:11** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T08:30:28Z
```diff
@@ -0,0 +1,24 @@
+title: Preempt within cohort while borrowing
+kep-number: 1337
+authors:
+  - "@mimowo"
+status: implementable
+creation-date: 2023-12-05
+reviewers:
+  - "@yaroslava-serdiuk"
+  - "@mwielgus"
+approvers:
+  - "@tenzen-y"
```

  /assign

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T10:26:42Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:242** by [@mimowo](https://github.com/mimowo) — 2023-12-15T10:26:42Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
```

  ```suggestion
  	BelowPriority *int32 `json:"belowPriority,omitempty"`
  ```
  WDYT @alculquicondor @tenzen-y @yaroslava-serdiuk ?
  Shorter, also, it makes it "sharp" inequality, so I can in simply check: `priority(candidate) < min(belowPriority, priority(preemptor))`, because `LowerPriority` is also sharp.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-15T13:14:37Z

Basically lgtm.

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T12:32:37Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  > This configuration can only be enabled when reclaimWithinCohort is not `Never`.
  
  For better usability, can we exclusive use between `reclaimWithinCohort`  and `borrowWithinCohort`?
  It means if we set any value to `reclaimWithinCohort`, we can not set any value to `borrowWithinCohort`.
  So, can we make `reclaimWithinCohort` a pointer?
  
  ```
  ReclaimWithinCohort *PreemptionPolicy `json:"reclaimWithinCohort,omitempty"`
  ```
  
  I think that it would be the cause of confusion if both were configurable.
  @alculquicondor @mimowo WDYT?

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:242** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T12:48:22Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
```

  I agree that simply checking would be better in the implementation. However, this means restriction for `Borrowing`, not `Preemption`. So, I believe that the name having semantics of the upper limitation, not lower limitation would be better.

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:178** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T12:51:07Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
```

  ```suggestion
  then the following flopping happens: workload A preempts workload B, then
  ```

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:236** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T13:01:25Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
```

  ```suggestion
  	// to borrow within the cohort, to only workloads with priority below
  ```

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:228** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T13:02:40Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
```

  ```suggestion
  	// - `LowerPriority`: allow preempting workloads to borrow within the cohort,
  ```

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:248** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T13:14:26Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityPolicy.Preempt` when the workload does not fit, but
```

  > we return `FlavorFungibilityPolicy.Preempt`
  
  Does this mean `FlavorAssignmentMode=Preempt`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T13:40:01Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@mimowo](https://github.com/mimowo) — 2023-12-15T13:40:00Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  I don't think we can make `ReclaimWithinCohort` a pointer, this is an API breaking change.
  
  Second, if a user sets `borrowWithinCohort: LowerPriority`, then we would need to assume `reclaimWithinCohort: LowerPriority`. It makes sense, but I think we could also support setting `reclaimWithinCohort: Any`, while `borrowWithinCohort: LowerPriority`. This means allow for preemption in cohort for any priority workloads, if the preemptor is not borrowing, but only preempt lower-priority workloads if the preemptor is borrowing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T13:42:03Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:242** by [@mimowo](https://github.com/mimowo) — 2023-12-15T13:42:03Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
```

  Yeah, I think `MaxPriorityThreshold` makes sense, and we should not make API decisions just based on the implementation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T13:43:29Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:242** by [@mimowo](https://github.com/mimowo) — 2023-12-15T13:43:29Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
```

  But just to be clear this feature is about combination of `preemption` and `borrowing`. It enables preempting workloads even for a borrowing workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T14:36:39Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:178** by [@mimowo](https://github.com/mimowo) — 2023-12-15T14:36:38Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T14:38:47Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:236** by [@mimowo](https://github.com/mimowo) — 2023-12-15T14:38:47Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
```

  Rephrased, PTAL

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T14:49:48Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:228** by [@mimowo](https://github.com/mimowo) — 2023-12-15T14:49:48Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
```

  Rephrased. PTAL

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T14:55:31Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:248** by [@mimowo](https://github.com/mimowo) — 2023-12-15T14:55:30Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityPolicy.Preempt` when the workload does not fit, but
```

  Yes, this is what I meant, let me clarify

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-15T15:02:47Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:242** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T15:02:47Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
```

  > But just to be clear this feature is about combination of preemption and borrowing. It enables preempting workloads even for a borrowing workload.
  
  That's right. I wanted to say that mixed semantics would be possible to cause confusion. It might be caused confusion to use some phrases having the aspect of preemption in `borrowWithinCohort` since we declare `borrow`.
  
  Actually, I was confused phrase, `BorrowWithinCohort` in `ClusterQueuePreemption`. However, I couldn't find a better API name instead of `BorrowWithinCohort`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-15T15:14:04Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:242** by [@mimowo](https://github.com/mimowo) — 2023-12-15T15:14:04Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
```

  I see, I initially wanted to do `ReclaimWithinCohortConfig` like here in Alternatives: https://github.com/kubernetes-sigs/kueue/blob/53c6c54d77ca25b92720720e5417b42bc942abc8/keps/1337-preempt-within-cohort-while-borrowing/README.md#reclaimwithincohortconfig. But it is not ideal anyway, and lenghty.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-15T19:38:25Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T19:38:25Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  > I don't think we can make ReclaimWithinCohort a pointer, this is an API breaking change.
  
  IIUC, if we make `reclaimWithinCohort` a pointer, the CRD wouldn't be changed. So, we keep having backward compatibility. Maybe I discussed a similar situation with @alculquicondor. @alculquicondor If I don't understand correctly, please let me know.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-15T20:10:09Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-15T20:10:09Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  Transitioning to a pointer is somewhat backwards compatible (the yaml API doesn't change). But if there's someone that is using the golang types, they will be broken. So we should prefer not to change the types.
  I think in the past we chose to change one type because it was impossible to achieve the needed semantics otherwise. In this case, the semantics could be achieved using `Never`.
  
  However, I agree with @mimowo in that setting both policies makes sense.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-15T20:14:18Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:242** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-15T20:14:18Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not reclaim quota within ClusterQueue while borrowing.
+	// - `LowerPriority`: allow preempting workloads to reclaim quota within the cohort,
+	//    but only by preempting lower-priority workloads.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads for preemption
+	// to reclaim quota within the cohort, to only workloads with priority below
+	// or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
```

  Note that this new `borrowWithinCohort` field is inside the `ClusterQueuePreemption` field, so it is directly related to preemption.
  
  `maxPriorityThreshold` has `<=` semantics. I don't think the name has any problems and the implementation is not significantly harder than the alternative.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-18T07:58:31Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@mimowo](https://github.com/mimowo) — 2023-12-18T07:58:30Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  I don't think we want to use: `reclaimWithinCohort: Never` combined with `borrowWithinCohort.Policy: LowerPriority`. This would mean you can preempt in other ClusterQueues, but only if you are borrowing. I don't think this is particularly useful, I opt to reject this combination by validation.
  
  I think the only options to make sense are:
  1.  `reclaimWithinCohort: LowerPriority`, and `borrowWithinCohort.Policy: LowerPriority`; meaning you can reclaim quota from workloads in the cohort of LowerPriority, same if you are borrowing.
  2.  `reclaimWithinCohort: Any`, and `borrowWithinCohort.Policy: LowerPriority`; meaning you can reclaim quota from workloads in the cohort of Any priority, however, only from lower priority workloads, if you are borrowing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-18T10:45:31Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@mimowo](https://github.com/mimowo) — 2023-12-18T10:45:31Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  Looking at the implementation it is easier to support only 1, then we can just reject the higher priority workloads during findCandidates. Maybe that is enough in the first iteration of the feature? WDYT @alculquicondor @tenzen-y ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-18T11:14:10Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@mimowo](https://github.com/mimowo) — 2023-12-18T11:14:10Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  I take it back, the implementation would not get easier due to `MaxPriorityThreshold`. So, even in case `reclaimWithinCohort: LowerPriority` `borrowWithinCohort.Policy: LowerPriority`, we still need to accept any lower priority candidates, and filter them only later based on the threshold, and the need to borrow.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-18T16:16:07Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:250** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T19:40:38Z
```diff
@@ -0,0 +1,486 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
+	BorrowWithinCohort *BorrowWithinCohort `json:"borrowWithinCohort,omitempty"`
+  ...
+}
+
+type BorrowWithinCohortPolicy string
+
+const (
+	BorrowWithinCohortPolicyNever         BorrowWithinCohortPolicy = "Never"
+	BorrowWithinCohortPolicyLowerPriority BorrowWithinCohortPolicy = "LowerPriority"
+)
+
+// BorrowWithinCohort contains configuration which allows to preempt workloads
+// within cohort while borrowing.
+type BorrowWithinCohort struct {
+	// policy determines the policy for preemption to reclaim quota within cohort while borrowing.
+	// Possible values are:
+	// - `Never` (default): do not allow for preemption, in other
+	//    ClusterQueues within the cohort, for a borrowing workload.
+	// - `LowerPriority`: allow preemption, in other ClusterQueues
+	//    within the cohort, for a borrowing workload, but only if
+	//    the preempted workloads are of lower priority.
+	//
+	// +kubebuilder:default=Never
+	// +kubebuilder:validation:Enum=Never;LowerPriority
+	Policy BorrowWithinCohortPolicy `json:"policy,omitempty"`
+
+	// maxPriorityThreshold allows to restrict the set of workloads which
+	// might be preempted by a borrowing workload, to only workloads with
+	// priority below or equal the specified level.
+	//
+	// +optional
+	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
+}
+```
+
+### Implementation overview
+
+There are main modifications:
+- in the [`fitsResourceQuota`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/flavorassigner.go#L551)
+  we return `FlavorFungibilityMode=Preempt` when the workload does not fit, but
```

  ```suggestion
    we return `FlavorAssignmentMode=Preempt` when the workload does not fit, but
  ```
  I assumed the `FlavorAssignmentMode`.
  
  https://github.com/kubernetes-sigs/kueue/blob/9464e42a54d67f785e2e19b8c8bbd4471cd283ad/pkg/scheduler/flavorassigner/flavorassigner.go#L192-L194

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-18T16:05:34Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  > Maybe that is enough in the first iteration of the feature? 
  
  I agree with you.
  
  Thanks for clarifying this @alculquicondor @mimowo 
  This makes sense.

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-18T16:15:41Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  @mimowo Could you mention that we don't work on an opt `2` in the first iteration like this?
  
  https://github.com/kubernetes-sigs/kueue/tree/main/keps/1284-cluster-queue-stop#notesconstraintscaveats

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-18T16:18:48Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@mimowo](https://github.com/mimowo) — 2023-12-18T16:18:47Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  I will want to support opt 2 from day 1. This is what my current implementation does. I was just hesitant for a moment that without opt 2 it is easier, but as mentioned here the solution isn't really any different: https://github.com/kubernetes-sigs/kueue/pull/1405#discussion_r1429947701

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-18T16:21:07Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@mimowo](https://github.com/mimowo) — 2023-12-18T16:21:07Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  I suggest that if we decide for whatever reason during review of the implementation that it is complicated to support, then maybe we can drop the support and update KEP later.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-18T16:22:46Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-18T16:22:46Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-18T16:42:45Z

- **keps/1337-preempt-within-cohort-while-borrowing/README.md:210** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-18T16:42:44Z
```diff
@@ -0,0 +1,484 @@
+# KEP-1337: Preempt within cohort while borrowing
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Flopping between preempting pair of workloads](#flopping-between-preempting-pair-of-workloads)
+- [Design Details](#design-details)
+  - [API](#api)
+  - [Implementation overview](#implementation-overview)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Reuse PreemptionPolicy enum](#reuse-preemptionpolicy-enum)
+  - [Minimal API](#minimal-api)
+  - [ReclaimWithinCohortConfig](#reclaimwithincohortconfig)
+  - [Move ReclaimWithinCohort under ReclaimWithinCohortConfig](#move-reclaimwithincohort-under-reclaimwithincohortconfig)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces ClusterQueue-level API to support workload preemption
+to reclaim quota within cohort for a workload that requires borrowing.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, there is no support for preemption to reclaim quota within cohort
+for workloads which require borrowing. This is too constraining for some setups.
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+
+- support preemption to reclaim quota within cohort for workloads that require borrowing.
+- support preemption within cluster queue for workloads that require borrowing.
+- modify the workload ordering by scheduler to take the need to preempt into account.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+- allow preemption of workloads, from other ClusterQueues, which are not borrowing.
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+Introduce a new ClusterLevel API to support preemption while borrowing. The
+API proposes additional configuration to the `.preemption.reclaimWithinCohort`
+option.
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+As an administrator of Kueue in my organization I manage the following setup
+with two teams, `A` and `B`. There is a single cohort, and both teams have a
+pair of ClusterQueues in the cohort, called `Standard` and `Best-Effort`. There
+is also one ClusterQueue, called `Shared`, which is shared between the teams.
+
+All the `Standard` and `Best-Effort` ClusterQueues have `nominalQuota=0`, and
+`borrowingLimit>0`. The actual quota is exposed by the `Shared` ClusterQueue.
+The `Shared` ClusterQueue has `nominalQuota>0`, and `borrowingLimit=0`. This
+setup is depicted by the table:
+
+|                 | A Standard    | A Best-Effort | B Standard    | B Best-Effort | Shared        |
+|-----------------|---------------|---------------|---------------|---------------|---------------|
+|nominalQuota     | 0             | 0             | 0             | 0             | 100           |
+|borrowingLimit   | 100           | 100           | 100           | 100           | 0             |
+
+The teams send their high-priority (say `>200`) jobs to their `Standard`
+ClusterQueue, and the low-priority jobs (say `<100`) to their `Best-Effort`
+ClusterQueue. Jobs sent to the `Standard` ClusterQueues should not be preempted.
+Jobs in the `Best-Effort` ClusterQueues can be preempted with respect to
+priorities.
+
+Note that in this setup every workload is borrowing, so I need to make preemption
+while borrowing enabled. Additionally, I need a mechanism to say that workloads
+from the `Standard` ClusterQueues are not preempted. A mechanism to only preempt
+jobs with priorities below a specified threshold will suffice.
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+#### Flopping between preempting pair of workloads
+
+In a setup as described in (Story 1)[#story-1] consider there are two workloads,
+one in ClusterQueue A, and one in ClusterQueue B. There is not enough resources
+in the cohort to run both workloads at the same time.
+
+If the preemption is allowed for workloads of any priorities
+(as when `.preemption.reclaimWithinCohort` equals `Any`)
+then the following flopping happens: workload A preempts workload B, then p
+preempted workload B returns to the queue and preempts workload A to make progress.
+This repeats, and none of the workloads can make any progress.
+
+In order to mitigate this risk we only enable the preemption to reclaim
+quota within cohort, for workloads requiring borrowing when such preempted
+workloads are of lower priority. This behavior is enabled by setting
+`.preemption.borrowWithinCohort.policy: LowerPriority`.
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+### API
+
+```golang
+// ClusterQueuePreemption contains policies to preempt Workloads from this
+// ClusterQueue or the ClusterQueue's cohort.
+type ClusterQueuePreemption struct {
+  ...
+	// borrowWithinCohort provides configuration to allow preemption within
+	// cohort while borrowing. This configuration can only be enabled when
+	// reclaimWithinCohort is not `Never`.
```

  I see. Let's try to support an opt `2`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-18T16:43:00Z

Thanks @mimowo 
/lgtm
/approve
