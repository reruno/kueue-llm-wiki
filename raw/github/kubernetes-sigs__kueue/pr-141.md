# PR #141: Add a concept page for each API resource

**Summary**: Add a concept page for each API resource

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/141

**Last updated**: 2022-03-23T17:40:17Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `doc` → `main`
- **Created**: 2022-03-22T19:44:53Z
- **Updated**: 2022-03-23T17:40:17Z
- **Merged**: 2022-03-23T17:40:17Z
- **Closed**: 2022-03-23T17:40:17Z
- **Labels**: `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 5   **+120 / -9**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Add a concept page for each API resource and a glossary for commonly used terms.

#### Which issue(s) this PR fixes:

Part of #64

#### Special notes for your reviewer:

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-22T19:45:21Z

/hold
to prevent accidental premature merge

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-23T12:12:32Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/141#" title="Author self-approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/141#pullrequestreview-918663336" title="LGTM">ArangoGutierrez</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T13:49:48Z

/assign @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T16:38:24Z

/lgtm

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T16:49:14Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T17:10:03Z

/label tide/merge-method-squash

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T17:39:10Z

/hold cancel

## Reviews

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (approved) — 2022-03-23T12:12:19Z

/lgtm

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-23T15:38:42Z

- **README.md:20** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T14:50:27Z
```diff
@@ -1,14 +1,23 @@
 # Kueue
 
-Kueue is a set of APIs and controller for job queueing. It is a job-level manager that decides when 
-a job should start (as in pods can be created) and when it should stop (as in active pods should be 
-deleted). The main design principle for Kueue is to avoid duplicating existing functionality: autoscaling, 
-pod-to-node scheduling, job lifecycle management and advanced admission control are the responsibility of 
-core k8s components or commonly accepted frameworks, namely cluster-autoscaler, kube-scheduler and kube-controller-manager 
-and gatekeeper, respectively.
-
-[bit.ly/kueue-apis](https://bit.ly/kueue-apis) (please join the [mailing list](https://groups.google.com/a/kubernetes.io/g/wg-batch) to get access) discusses the
-API proposal and a high-level description of how it operates; while [bit.ly/kueue-controller-design](https://bit.ly/kueue-controller-design) presents the detailed design of the controller.
+Kueue is a set of APIs and controller for [job](docs/concepts/queued_workload.md)
+[queueing](docs/concepts/README.md#queueing). It is a job-level manager that
+decides when a job should be [admitted](docs/concepts/README.md#admission) to start
+(as in pods can be created) and when it should stop (as in active pods should be 
+deleted).
+
+The main design principle for Kueue is to avoid duplicating mature functionality
+in [Kubernetes components](https://kubernetes.io/docs/concepts/overview/components/)
+and well-established third-party controllers. Autoscaling, pod-to-node scheduling and
+job lifecycle management are the responsibility of cluster-autoscaler,
+kube-scheduler and kube-controller-manager, respectively. Advanced
+admission control can be delegated to controllers such as [gatekeeper](https://github.com/open-policy-agent/gatekeeper).
+
+<!-- TODO(#64) Remove links to google docs once the contents have been migrated to this repo -->
+[bit.ly/kueue-apis](https://bit.ly/kueue-apis) (please join the [mailing list](https://groups.google.com/a/kubernetes.io/g/wg-batch)
+to get access) discusses the API proposal and a high-level description of how it
+operates; while [bit.ly/kueue-controller-design](https://bit.ly/kueue-controller-design)
+presents the detailed design of the controller.
```

  can we list them as two bullet points

- **docs/concepts/README.md:38** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T14:54:42Z
```diff
@@ -0,0 +1,45 @@
+# Concepts
+
+## APIs
+
+### [Cluster Queue](cluster_queue.md)
+
+A cluster-scoped resource that governs a pool of resources, defining usage
+limits and fair sharing rules.
+
+### [Queue](queue.md)
+
+A namespaced resource that groups closely related workloads belonging to a
+single tenant.
+
+### [Queued Workload](queued_workload.md)
+
+An application that will run to completion. It is the unit of _admission_ in
+Kueue. Sometimes referred to as _job_.
+
+### [Resource Flavor](cluster_queue.md#resource-flavors)
+
+A kind or type of resource in a cluster. It could distinguish among different
+characteristics of resources such as availability, pricing, architecture,
+models, etc.
+
+## Glossary
+
+### Admission
+
+The process of admitting a workload to start (pods to be created). A workload
+is admitted by a ClusterQueue according to the available resources and gets
+resource flavors assigned for each requested resource. Sometimes referred to
+as _workload scheduling_ or _job scheduling_ (not to be confused with
+[pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)).
+
+### [Cohort](cluster_queue.md#cohort)
+
+A group of cluster queues that can borrow unused resources from each other.
```

  ```suggestion
  A group of ClusterQueues that can borrow unused quota from each other.
  ```

- **docs/concepts/README.md:42** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T14:55:25Z
```diff
@@ -0,0 +1,45 @@
+# Concepts
+
+## APIs
+
+### [Cluster Queue](cluster_queue.md)
+
+A cluster-scoped resource that governs a pool of resources, defining usage
+limits and fair sharing rules.
+
+### [Queue](queue.md)
+
+A namespaced resource that groups closely related workloads belonging to a
+single tenant.
+
+### [Queued Workload](queued_workload.md)
+
+An application that will run to completion. It is the unit of _admission_ in
+Kueue. Sometimes referred to as _job_.
+
+### [Resource Flavor](cluster_queue.md#resource-flavors)
+
+A kind or type of resource in a cluster. It could distinguish among different
+characteristics of resources such as availability, pricing, architecture,
+models, etc.
+
+## Glossary
+
+### Admission
+
+The process of admitting a workload to start (pods to be created). A workload
+is admitted by a ClusterQueue according to the available resources and gets
+resource flavors assigned for each requested resource. Sometimes referred to
+as _workload scheduling_ or _job scheduling_ (not to be confused with
+[pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)).
+
+### [Cohort](cluster_queue.md#cohort)
+
+A group of cluster queues that can borrow unused resources from each other.
+
+### Queueing
+
+The time between a workload is created until it is admitted by Cluster Queue.
```

  ```suggestion
  The time between a workload is created until it is admitted by a ClusterQueue.
  ```

- **docs/concepts/README.md:44** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T14:56:02Z
```diff
@@ -0,0 +1,45 @@
+# Concepts
+
+## APIs
+
+### [Cluster Queue](cluster_queue.md)
+
+A cluster-scoped resource that governs a pool of resources, defining usage
+limits and fair sharing rules.
+
+### [Queue](queue.md)
+
+A namespaced resource that groups closely related workloads belonging to a
+single tenant.
+
+### [Queued Workload](queued_workload.md)
+
+An application that will run to completion. It is the unit of _admission_ in
+Kueue. Sometimes referred to as _job_.
+
+### [Resource Flavor](cluster_queue.md#resource-flavors)
+
+A kind or type of resource in a cluster. It could distinguish among different
+characteristics of resources such as availability, pricing, architecture,
+models, etc.
+
+## Glossary
+
+### Admission
+
+The process of admitting a workload to start (pods to be created). A workload
+is admitted by a ClusterQueue according to the available resources and gets
+resource flavors assigned for each requested resource. Sometimes referred to
+as _workload scheduling_ or _job scheduling_ (not to be confused with
+[pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)).
+
+### [Cohort](cluster_queue.md#cohort)
+
+A group of cluster queues that can borrow unused resources from each other.
+
+### Queueing
+
+The time between a workload is created until it is admitted by Cluster Queue.
+Typically, the workload will compete with other workloads for available
+resources based on the fair sharing rules for the Cluster Queue.
```

  ```suggestion
  quota based on the fair sharing rules of the ClusterQueue.
  ```

- **docs/concepts/cluster_queue.md:6** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T14:59:34Z
```diff
@@ -0,0 +1,24 @@
+# Cluster Queue
+
+A `ClusterQueue` is a cluster-scoped resource that governs a pool of resources
+such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
+- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- Fair sharing rules across the tenants of the cluster.
```

  I think it will be useful to define "fair-sharing" in glossary

- **docs/concepts/cluster_queue.md:8** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T15:24:29Z
```diff
@@ -0,0 +1,24 @@
+# Cluster Queue
+
+A `ClusterQueue` is a cluster-scoped resource that governs a pool of resources
+such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
+- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- Fair sharing rules across the tenants of the cluster.
+
+Only cluster administrators should create `ClusterQueue` objects.
```

  lets also add something about personas (Batch Admin and Batch User), perhaps in glossary too

- **docs/concepts/cluster_queue.md:17** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T15:36:00Z
```diff
@@ -0,0 +1,24 @@
+# Cluster Queue
+
+A `ClusterQueue` is a cluster-scoped resource that governs a pool of resources
+such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
+- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- Fair sharing rules across the tenants of the cluster.
+
+Only cluster administrators should create `ClusterQueue` objects.
+
+## Resource Flavors
+
+Resources in a cluster are typically not homogeneus. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A `ResourceFlavor` is a resource that represents these variations and allows
```

  ```suggestion
  A `ResourceFlavor` is an object API that represents these variations and allows
  ```
  
  just so it is not confused with compute resource

- **docs/concepts/cluster_queue.md:23** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T15:36:49Z
```diff
@@ -0,0 +1,24 @@
+# Cluster Queue
+
+A `ClusterQueue` is a cluster-scoped resource that governs a pool of resources
+such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
+- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- Fair sharing rules across the tenants of the cluster.
+
+Only cluster administrators should create `ClusterQueue` objects.
+
+## Resource Flavors
+
+Resources in a cluster are typically not homogeneus. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A `ResourceFlavor` is a resource that represents these variations and allows
+administrators to associate them with node labels and taints.
+
+## Cohort
+
+`ClusterQueues` can be grouped in _cohorts_. `ClusterQueues` that belong to the
+same cohort can borrow unused resources from each other, if they have matching
```

  ```suggestion
  same cohort can borrow unused quota from each other, if they have matching
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-23T16:20:56Z

- **README.md:20** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T16:09:18Z
```diff
@@ -1,14 +1,23 @@
 # Kueue
 
-Kueue is a set of APIs and controller for job queueing. It is a job-level manager that decides when 
-a job should start (as in pods can be created) and when it should stop (as in active pods should be 
-deleted). The main design principle for Kueue is to avoid duplicating existing functionality: autoscaling, 
-pod-to-node scheduling, job lifecycle management and advanced admission control are the responsibility of 
-core k8s components or commonly accepted frameworks, namely cluster-autoscaler, kube-scheduler and kube-controller-manager 
-and gatekeeper, respectively.
-
-[bit.ly/kueue-apis](https://bit.ly/kueue-apis) (please join the [mailing list](https://groups.google.com/a/kubernetes.io/g/wg-batch) to get access) discusses the
-API proposal and a high-level description of how it operates; while [bit.ly/kueue-controller-design](https://bit.ly/kueue-controller-design) presents the detailed design of the controller.
+Kueue is a set of APIs and controller for [job](docs/concepts/queued_workload.md)
+[queueing](docs/concepts/README.md#queueing). It is a job-level manager that
+decides when a job should be [admitted](docs/concepts/README.md#admission) to start
+(as in pods can be created) and when it should stop (as in active pods should be 
+deleted).
+
+The main design principle for Kueue is to avoid duplicating mature functionality
+in [Kubernetes components](https://kubernetes.io/docs/concepts/overview/components/)
+and well-established third-party controllers. Autoscaling, pod-to-node scheduling and
+job lifecycle management are the responsibility of cluster-autoscaler,
+kube-scheduler and kube-controller-manager, respectively. Advanced
+admission control can be delegated to controllers such as [gatekeeper](https://github.com/open-policy-agent/gatekeeper).
+
+<!-- TODO(#64) Remove links to google docs once the contents have been migrated to this repo -->
+[bit.ly/kueue-apis](https://bit.ly/kueue-apis) (please join the [mailing list](https://groups.google.com/a/kubernetes.io/g/wg-batch)
+to get access) discusses the API proposal and a high-level description of how it
+operates; while [bit.ly/kueue-controller-design](https://bit.ly/kueue-controller-design)
+presents the detailed design of the controller.
```

  Done

- **docs/concepts/README.md:38** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T16:10:34Z
```diff
@@ -0,0 +1,45 @@
+# Concepts
+
+## APIs
+
+### [Cluster Queue](cluster_queue.md)
+
+A cluster-scoped resource that governs a pool of resources, defining usage
+limits and fair sharing rules.
+
+### [Queue](queue.md)
+
+A namespaced resource that groups closely related workloads belonging to a
+single tenant.
+
+### [Queued Workload](queued_workload.md)
+
+An application that will run to completion. It is the unit of _admission_ in
+Kueue. Sometimes referred to as _job_.
+
+### [Resource Flavor](cluster_queue.md#resource-flavors)
+
+A kind or type of resource in a cluster. It could distinguish among different
+characteristics of resources such as availability, pricing, architecture,
+models, etc.
+
+## Glossary
+
+### Admission
+
+The process of admitting a workload to start (pods to be created). A workload
+is admitted by a ClusterQueue according to the available resources and gets
+resource flavors assigned for each requested resource. Sometimes referred to
+as _workload scheduling_ or _job scheduling_ (not to be confused with
+[pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)).
+
+### [Cohort](cluster_queue.md#cohort)
+
+A group of cluster queues that can borrow unused resources from each other.
```

  Done

- **docs/concepts/README.md:44** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T16:15:03Z
```diff
@@ -0,0 +1,45 @@
+# Concepts
+
+## APIs
+
+### [Cluster Queue](cluster_queue.md)
+
+A cluster-scoped resource that governs a pool of resources, defining usage
+limits and fair sharing rules.
+
+### [Queue](queue.md)
+
+A namespaced resource that groups closely related workloads belonging to a
+single tenant.
+
+### [Queued Workload](queued_workload.md)
+
+An application that will run to completion. It is the unit of _admission_ in
+Kueue. Sometimes referred to as _job_.
+
+### [Resource Flavor](cluster_queue.md#resource-flavors)
+
+A kind or type of resource in a cluster. It could distinguish among different
+characteristics of resources such as availability, pricing, architecture,
+models, etc.
+
+## Glossary
+
+### Admission
+
+The process of admitting a workload to start (pods to be created). A workload
+is admitted by a ClusterQueue according to the available resources and gets
+resource flavors assigned for each requested resource. Sometimes referred to
+as _workload scheduling_ or _job scheduling_ (not to be confused with
+[pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)).
+
+### [Cohort](cluster_queue.md#cohort)
+
+A group of cluster queues that can borrow unused resources from each other.
+
+### Queueing
+
+The time between a workload is created until it is admitted by Cluster Queue.
+Typically, the workload will compete with other workloads for available
+resources based on the fair sharing rules for the Cluster Queue.
```

  Done

- **docs/concepts/README.md:42** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T16:15:06Z
```diff
@@ -0,0 +1,45 @@
+# Concepts
+
+## APIs
+
+### [Cluster Queue](cluster_queue.md)
+
+A cluster-scoped resource that governs a pool of resources, defining usage
+limits and fair sharing rules.
+
+### [Queue](queue.md)
+
+A namespaced resource that groups closely related workloads belonging to a
+single tenant.
+
+### [Queued Workload](queued_workload.md)
+
+An application that will run to completion. It is the unit of _admission_ in
+Kueue. Sometimes referred to as _job_.
+
+### [Resource Flavor](cluster_queue.md#resource-flavors)
+
+A kind or type of resource in a cluster. It could distinguish among different
+characteristics of resources such as availability, pricing, architecture,
+models, etc.
+
+## Glossary
+
+### Admission
+
+The process of admitting a workload to start (pods to be created). A workload
+is admitted by a ClusterQueue according to the available resources and gets
+resource flavors assigned for each requested resource. Sometimes referred to
+as _workload scheduling_ or _job scheduling_ (not to be confused with
+[pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)).
+
+### [Cohort](cluster_queue.md#cohort)
+
+A group of cluster queues that can borrow unused resources from each other.
+
+### Queueing
+
+The time between a workload is created until it is admitted by Cluster Queue.
```

  Done

- **docs/concepts/cluster_queue.md:6** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T16:15:33Z
```diff
@@ -0,0 +1,24 @@
+# Cluster Queue
+
+A `ClusterQueue` is a cluster-scoped resource that governs a pool of resources
+such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
+- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- Fair sharing rules across the tenants of the cluster.
```

  I'll leave it for a follow up. I need more details in the ClusterQueue page first.

- **docs/concepts/cluster_queue.md:8** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T16:16:55Z
```diff
@@ -0,0 +1,24 @@
+# Cluster Queue
+
+A `ClusterQueue` is a cluster-scoped resource that governs a pool of resources
+such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
+- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- Fair sharing rules across the tenants of the cluster.
+
+Only cluster administrators should create `ClusterQueue` objects.
```

  I was thinking about that too. I'll do it in a follow up.

- **docs/concepts/cluster_queue.md:17** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T16:18:43Z
```diff
@@ -0,0 +1,24 @@
+# Cluster Queue
+
+A `ClusterQueue` is a cluster-scoped resource that governs a pool of resources
+such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
+- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- Fair sharing rules across the tenants of the cluster.
+
+Only cluster administrators should create `ClusterQueue` objects.
+
+## Resource Flavors
+
+Resources in a cluster are typically not homogeneus. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A `ResourceFlavor` is a resource that represents these variations and allows
```

  Done, updated everywhere.

- **docs/concepts/cluster_queue.md:23** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-23T16:19:44Z
```diff
@@ -0,0 +1,24 @@
+# Cluster Queue
+
+A `ClusterQueue` is a cluster-scoped resource that governs a pool of resources
+such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
+- The resource _flavors_ that it manages, with usage limits and order of consumption.
+- Fair sharing rules across the tenants of the cluster.
+
+Only cluster administrators should create `ClusterQueue` objects.
+
+## Resource Flavors
+
+Resources in a cluster are typically not homogeneus. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A `ResourceFlavor` is a resource that represents these variations and allows
+administrators to associate them with node labels and taints.
+
+## Cohort
+
+`ClusterQueues` can be grouped in _cohorts_. `ClusterQueues` that belong to the
+same cohort can borrow unused resources from each other, if they have matching
```

  Done
