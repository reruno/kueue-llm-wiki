# PR #142: Add task for single cluster queue setup

**Summary**: Add task for single cluster queue setup

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/142

**Last updated**: 2022-03-25T15:54:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `single-cluster-queue` → `main`
- **Created**: 2022-03-23T20:36:08Z
- **Updated**: 2022-03-25T15:54:14Z
- **Merged**: 2022-03-24T20:34:43Z
- **Closed**: 2022-03-24T20:34:43Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 6   **+153 / -5**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Explains the setup for a single cluster queue.

#### Which issue(s) this PR fixes:

Part of #64

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-23T20:36:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/142#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T20:13:50Z

I removed it. Run jobs will be my next doc. Although it will probably be very small :)

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-24T20:19:10Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T20:30:58Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-25T15:54:14Z

:'( I forgot to squash

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-23T22:40:37Z

- **docs/README.md:5** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T22:23:49Z
```diff
@@ -0,0 +1,14 @@
+# Kueue documentation
+
+## Understand Kueue
+
+Learn about kubernetes and its fundamental concepts.
```

  ```suggestion
  Learn about Kueue and its fundamental concepts.
  ```

- **docs/tasks/administer_cluster_resources.md:13** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T22:25:33Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
```

  ```suggestion
  ## Single ClusterQueue setup
  ```

- **docs/tasks/administer_cluster_resources.md:15** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T22:25:46Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
```

  ```suggestion
  ### 1. Create a [ClusterQueue](/docs/concepts/cluster_queue.md)
  ```

- **docs/tasks/administer_cluster_resources.md:18** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T22:29:32Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
+
+Create a single ClusterQueue to represent the resource limits for your
+entire cluster.
```

  I think we need to talk "quota" rather than limits of an entire cluster so we don't give the illusion that Kueue will have full control over the cluster and pods created outside queued workloads.

- **docs/tasks/administer_cluster_resources.md:60** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T22:38:25Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
+
+Create a single ClusterQueue to represent the resource limits for your
+entire cluster.
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {} # match all.
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 36Gi
+```
+
+This ClusterQueue governs the usage of [resource types](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+`cpu` and `memory`. Each resource type has a single [resource flavor](/docs/concepts/cluster_queue.md#resource-flavors),
+named `default` with a guaranteed quota.
+As there is a single ClusterQueue in the cluster, it is not possible for the
+ClusterQueue to borrow resources from anywhere. Thus, the `ceiling` is equal
```

  btw, in the docs we mention that [no ceiling means unlimited](https://github.com/kubernetes-sigs/kueue/blob/27b05145f94ac528ff92790efc70d62d8c9feafc/api/v1alpha1/clusterqueue_types.go#L250), which is currently not true, the ceiling must be explicitly set to guaranteed. I opened https://github.com/kubernetes-sigs/kueue/issues/144

- **docs/tasks/administer_cluster_resources.md:83** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T22:39:38Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
+
+Create a single ClusterQueue to represent the resource limits for your
+entire cluster.
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {} # match all.
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 36Gi
+```
+
+This ClusterQueue governs the usage of [resource types](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+`cpu` and `memory`. Each resource type has a single [resource flavor](/docs/concepts/cluster_queue.md#resource-flavors),
+named `default` with a guaranteed quota.
+As there is a single ClusterQueue in the cluster, it is not possible for the
+ClusterQueue to borrow resources from anywhere. Thus, the `ceiling` is equal
+to the `guaranteed` quota.
+
+The empty `namespaceSelector` allows any namespace to use these resources.
+
+### 2. Create a [Resource Flavor](/docs/concepts/cluster_queue.md#resource-flavors)
+
+The `ClusterQueue` is not ready to be used yet, as the `default` flavor is not
+defined.
+
+Typically, a resource flavor has node labels and/or taints to scope which nodes
+can provide it. However, since we are using a single flavor to represent all the
+resources available in the cluster, you can create an empty ResourceFlavor.
+
+```shell
+kubectl apply -f default-flavor.yaml
+```
+
+```yaml
+# default-flavor.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: default
+```
+
+The `.metadata.name` matches the `.spec.requestableResources[*].flavors[0].resourceFlavor`
+field in the ClusterQueue.
+
+### 3. Create [Queues](/docs/concepts/queue.md)
+
+Users cannot directly send [workloads](/docs/concepts/queued_workload.md) to
+Queues. Instead, users need to send their workloads to a Queue in their namespace.
```

  ```suggestion
  ClusterQueues. Instead, users need to send their workloads to a Queue in their namespace.
  ```

- **docs/tasks/administer_cluster_resources.md:85** by [@ahg-g](https://github.com/ahg-g) — 2022-03-23T22:40:13Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
+
+Create a single ClusterQueue to represent the resource limits for your
+entire cluster.
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {} # match all.
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 36Gi
+```
+
+This ClusterQueue governs the usage of [resource types](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+`cpu` and `memory`. Each resource type has a single [resource flavor](/docs/concepts/cluster_queue.md#resource-flavors),
+named `default` with a guaranteed quota.
+As there is a single ClusterQueue in the cluster, it is not possible for the
+ClusterQueue to borrow resources from anywhere. Thus, the `ceiling` is equal
+to the `guaranteed` quota.
+
+The empty `namespaceSelector` allows any namespace to use these resources.
+
+### 2. Create a [Resource Flavor](/docs/concepts/cluster_queue.md#resource-flavors)
+
+The `ClusterQueue` is not ready to be used yet, as the `default` flavor is not
+defined.
+
+Typically, a resource flavor has node labels and/or taints to scope which nodes
+can provide it. However, since we are using a single flavor to represent all the
+resources available in the cluster, you can create an empty ResourceFlavor.
+
+```shell
+kubectl apply -f default-flavor.yaml
+```
+
+```yaml
+# default-flavor.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: default
+```
+
+The `.metadata.name` matches the `.spec.requestableResources[*].flavors[0].resourceFlavor`
+field in the ClusterQueue.
+
+### 3. Create [Queues](/docs/concepts/queue.md)
+
+Users cannot directly send [workloads](/docs/concepts/queued_workload.md) to
+Queues. Instead, users need to send their workloads to a Queue in their namespace.
+Thus, for the queuing system to be complete, you need to create a Queue in
+each namespace that needs to have access to the corresponding ClusterQueue.
```

  ```suggestion
  each namespace that needs access to the ClusterQueue.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-24T18:25:54Z

- **docs/README.md:5** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T18:13:35Z
```diff
@@ -0,0 +1,14 @@
+# Kueue documentation
+
+## Understand Kueue
+
+Learn about kubernetes and its fundamental concepts.
```

  clearly copy paste :see_no_evil:

- **docs/tasks/administer_cluster_resources.md:13** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T18:14:50Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
```

  Done

- **docs/tasks/administer_cluster_resources.md:15** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T18:14:55Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
```

  Done

- **docs/tasks/administer_cluster_resources.md:18** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T18:16:11Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
+
+Create a single ClusterQueue to represent the resource limits for your
+entire cluster.
```

  good point. updated

- **docs/tasks/administer_cluster_resources.md:60** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T18:17:20Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
+
+Create a single ClusterQueue to represent the resource limits for your
+entire cluster.
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {} # match all.
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 36Gi
+```
+
+This ClusterQueue governs the usage of [resource types](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+`cpu` and `memory`. Each resource type has a single [resource flavor](/docs/concepts/cluster_queue.md#resource-flavors),
+named `default` with a guaranteed quota.
+As there is a single ClusterQueue in the cluster, it is not possible for the
+ClusterQueue to borrow resources from anywhere. Thus, the `ceiling` is equal
```

  Sg. I will remove the ceiling line once when I fix #144.

- **docs/tasks/administer_cluster_resources.md:83** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T18:25:43Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
+
+Create a single ClusterQueue to represent the resource limits for your
+entire cluster.
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {} # match all.
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 36Gi
+```
+
+This ClusterQueue governs the usage of [resource types](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+`cpu` and `memory`. Each resource type has a single [resource flavor](/docs/concepts/cluster_queue.md#resource-flavors),
+named `default` with a guaranteed quota.
+As there is a single ClusterQueue in the cluster, it is not possible for the
+ClusterQueue to borrow resources from anywhere. Thus, the `ceiling` is equal
+to the `guaranteed` quota.
+
+The empty `namespaceSelector` allows any namespace to use these resources.
+
+### 2. Create a [Resource Flavor](/docs/concepts/cluster_queue.md#resource-flavors)
+
+The `ClusterQueue` is not ready to be used yet, as the `default` flavor is not
+defined.
+
+Typically, a resource flavor has node labels and/or taints to scope which nodes
+can provide it. However, since we are using a single flavor to represent all the
+resources available in the cluster, you can create an empty ResourceFlavor.
+
+```shell
+kubectl apply -f default-flavor.yaml
+```
+
+```yaml
+# default-flavor.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: default
+```
+
+The `.metadata.name` matches the `.spec.requestableResources[*].flavors[0].resourceFlavor`
+field in the ClusterQueue.
+
+### 3. Create [Queues](/docs/concepts/queue.md)
+
+Users cannot directly send [workloads](/docs/concepts/queued_workload.md) to
+Queues. Instead, users need to send their workloads to a Queue in their namespace.
```

  Done

- **docs/tasks/administer_cluster_resources.md:85** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T18:25:48Z
```diff
@@ -0,0 +1,100 @@
+# Administer Cluster resources
+
+This page shows you how to represent your cluster resource limits and to
+establish fair sharing rules among the tenants.
+
+The intended audience for this page are cluster administrators.
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+## Single Cluster Queue setup
+
+### 1. Create a [Cluster Queue](/docs/concepts/cluster_queue.md)
+
+Create a single ClusterQueue to represent the resource limits for your
+entire cluster.
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {} # match all.
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 36Gi
+```
+
+This ClusterQueue governs the usage of [resource types](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-types)
+`cpu` and `memory`. Each resource type has a single [resource flavor](/docs/concepts/cluster_queue.md#resource-flavors),
+named `default` with a guaranteed quota.
+As there is a single ClusterQueue in the cluster, it is not possible for the
+ClusterQueue to borrow resources from anywhere. Thus, the `ceiling` is equal
+to the `guaranteed` quota.
+
+The empty `namespaceSelector` allows any namespace to use these resources.
+
+### 2. Create a [Resource Flavor](/docs/concepts/cluster_queue.md#resource-flavors)
+
+The `ClusterQueue` is not ready to be used yet, as the `default` flavor is not
+defined.
+
+Typically, a resource flavor has node labels and/or taints to scope which nodes
+can provide it. However, since we are using a single flavor to represent all the
+resources available in the cluster, you can create an empty ResourceFlavor.
+
+```shell
+kubectl apply -f default-flavor.yaml
+```
+
+```yaml
+# default-flavor.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: default
+```
+
+The `.metadata.name` matches the `.spec.requestableResources[*].flavors[0].resourceFlavor`
+field in the ClusterQueue.
+
+### 3. Create [Queues](/docs/concepts/queue.md)
+
+Users cannot directly send [workloads](/docs/concepts/queued_workload.md) to
+Queues. Instead, users need to send their workloads to a Queue in their namespace.
+Thus, for the queuing system to be complete, you need to create a Queue in
+each namespace that needs to have access to the corresponding ClusterQueue.
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-24T20:09:20Z

what's with `run_jobs.md`? are you planning to add something to this file in this PR?
