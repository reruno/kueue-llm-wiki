# PR #1433: Excess pods in group deletion

**Summary**: Excess pods in group deletion

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1433

**Last updated**: 2023-12-12T21:36:08Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@achernevskii](https://github.com/achernevskii)
- **Branch**: `feature/excess_pods_in_group` → `main`
- **Created**: 2023-12-11T02:02:55Z
- **Updated**: 2023-12-12T21:36:08Z
- **Merged**: 2023-12-12T21:00:13Z
- **Closed**: 2023-12-12T21:00:13Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 4   **+533 / -69**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation


Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

* Move group metadata validation from `constructGroupPodSets`
  method into a separate `validatePodGroupMetadata` method.

* Add `cleanupExcessPods` method which will delete and
  finalize pods created last if the number of non-failed
  pods is greater than group-total-count annotation.

* Add integration/unit tests for excess pods deletion.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Related issue: #976

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-12-11T02:03:01Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 35fad1d10f0df89f2db3885f85a1cb6f389cf4e9 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6578b7995bd6a900088b3163 |

### Comment by [@achernevskii](https://github.com/achernevskii) — 2023-12-11T02:04:13Z

cc @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T20:55:16Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-12T20:55:22Z

LGTM label has been added.  <details>Git tree hash: bfe730e13e0fff44ea7bd7c6b0d6d1d9fa46406b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-12T20:55:22Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1433#" title="Author self-approved">achernevskii</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1433#issuecomment-1852797740" title="Approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-11T19:00:05Z

- **pkg/controller/jobs/pod/pod_controller.go:578** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-11T18:45:05Z
```diff
@@ -522,63 +501,138 @@ func (p *Pod) constructGroupPodSets(podsInGroup corev1.PodList) ([]kueue.PodSet,
 	return resultPodSets, nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
-	log := ctrl.LoggerFrom(ctx)
-	object := p.Object()
+// validatePodGroupMetadata validates metadata of all members of the pod group
+func (p *Pod) validatePodGroupMetadata(r record.EventRecorder) error {
+	groupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return err
+	}
+	originalQueue := jobframework.QueueName(p)
 
-	var (
-		podSets []kueue.PodSet
-	)
+	if p.countActivePods() < groupTotalCount {
+		errMsg := fmt.Sprintf("'%s' group total count is less than the actual number of pods in the cluster", p.groupName())
+		r.Eventf(p.Object(), corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
+		return jobframework.UnretryableError(errMsg)
+	}
 
-	if !p.isGroup {
-		podSets = p.PodSets()
-	} else {
-		groupTotalCount, err := p.groupTotalCount()
-		if err != nil {
-			return nil, err
+	for _, podInGroup := range p.list.Items {
+		// Skip failed pods
+		if podInGroup.Status.Phase == corev1.PodFailed {
+			continue
 		}
 
-		podCount := 0
-		for i := range p.list.Items {
-			if p.list.Items[i].Status.Phase != corev1.PodFailed {
-				podCount++
-			}
+		if podInGroupQueue := jobframework.QueueNameForObject(&podInGroup); podInGroupQueue != originalQueue {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different queue names: %s!=%s",
+				p.pod.GetName(), podInGroup.GetName(),
+				originalQueue, podInGroupQueue))
 		}
 
-		if podCount != groupTotalCount {
-			errMsg := fmt.Sprintf("'%s' group total count is different from the actual number of pods in the cluster", p.groupName())
-			r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
-			return nil, jobframework.UnretryableError(errMsg)
+		tc, err := strconv.Atoi(podInGroup.GetAnnotations()[GroupTotalCountAnnotation])
+		if err != nil {
+			return fmt.Errorf("failed to extract '%s' annotation from the pod '%s': %w",
+				GroupTotalCountAnnotation,
+				podInGroup.GetName(),
+				err)
+		}
+		if tc != groupTotalCount {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different '%s' values: %d!=%d",
+				p.pod.GetName(), podInGroup.GetName(),
+				GroupTotalCountAnnotation,
+				groupTotalCount, tc))
 		}
+	}
 
-		podSets, err = p.constructGroupPodSets(p.list)
-		if err != nil {
-			if jobframework.IsUnretryableError(err) {
-				r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", err.Error())
+	return nil
+}
+
+// countActivePods returns a count of non-failed pods in the group
+func (p *Pod) countActivePods() int {
+	if p.activePodsCount != nil {
+		return *p.activePodsCount
+	}
+
+	// Filter all failed pods
+	activePodsCount := 0
+	for _, pod := range p.list.Items {
+		if pod.Status.Phase != corev1.PodFailed {
+			activePodsCount++
+		}
+	}
+
+	p.activePodsCount = &activePodsCount
+	return activePodsCount
+}
+
+// cleanupExcessPods will delete and finalize pods created last if the number of
+// activePods is greater than the group-total-count annotation value.
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	groupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return err
+	}
+
+	extraPods := p.countActivePods() - groupTotalCount
+
+	for i := extraPods; i > 0; i-- {
```

  Better to obtain a list of non-terminated Pods and sort.

- **pkg/controller/jobs/pod/pod_controller_test.go:1809** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-11T18:55:27Z
```diff
@@ -1796,6 +1797,264 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"excess pods before wl creation, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
```

  a strategy we use in other places is to have a variable `now := time.Now()` at the beginning of the test, and then have calls like: `CreationTimestamp(now.Add(time.Second))`, which would read as "1 second ago".

- **pkg/controller/jobs/pod/pod_controller_test.go:1992** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-11T18:58:02Z
```diff
@@ -1796,6 +1797,264 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"excess pods before wl creation, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 15, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			workloads: []kueue.Workload{},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							SchedulingGates(corev1.PodSchedulingGate{Name: "kueue.x-k8s.io/admission"}).
+							Obj(),
+					).
+					Queue("user-queue").
+					Priority(0).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
+		"excess pods before admission, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(time.Date(2022, 9, 15, 15, 0, 0, 0, time.UTC)).
+					GroupTotalCount("2").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
+		"excess pods after admission, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(time.Date(2022, 9, 15, 15, 0, 0, 0, time.UTC)).
+					GroupTotalCount("2").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
+		"deletion of excess pod is blocked by another controller": {
```

  I think we are not doing anything special in this case. Let's not add this test case.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-12-11T19:21:35Z

- **pkg/controller/jobs/pod/pod_controller_test.go:1992** by [@achernevskii](https://github.com/achernevskii) — 2023-12-11T19:21:35Z
```diff
@@ -1796,6 +1797,264 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"excess pods before wl creation, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 15, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			workloads: []kueue.Workload{},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							SchedulingGates(corev1.PodSchedulingGate{Name: "kueue.x-k8s.io/admission"}).
+							Obj(),
+					).
+					Queue("user-queue").
+					Priority(0).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
+		"excess pods before admission, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(time.Date(2022, 9, 15, 15, 0, 0, 0, time.UTC)).
+					GroupTotalCount("2").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
+		"excess pods after admission, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(time.Date(2022, 9, 15, 15, 0, 0, 0, time.UTC)).
+					GroupTotalCount("2").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
+		"deletion of excess pod is blocked by another controller": {
```

  This test case validates that reconciler won't use the excess pod for workload creation, if the pod deletion is blocked by some external finalizer. 
  
  For the first commit in this PR, it was failing, since [`Pod.Load()`](https://github.com/kubernetes-sigs/kueue/blob/74eaa48336a83970bf9065d0933f2decbea1ae87/pkg/controller/jobs/pod/pod_controller.go#L595) loaded all the pods in the group including the deleted excess pod.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-11T19:58:17Z

- **pkg/controller/jobs/pod/pod_controller_test.go:1992** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-11T19:58:17Z
```diff
@@ -1796,6 +1797,264 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"excess pods before wl creation, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 15, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			workloads: []kueue.Workload{},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							SchedulingGates(corev1.PodSchedulingGate{Name: "kueue.x-k8s.io/admission"}).
+							Obj(),
+					).
+					Queue("user-queue").
+					Priority(0).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
+		"excess pods before admission, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(time.Date(2022, 9, 15, 15, 0, 0, 0, time.UTC)).
+					GroupTotalCount("2").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
+		"excess pods after admission, youngest pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(time.Date(2022, 9, 15, 15, 0, 0, 0, time.UTC)).
+					GroupTotalCount("2").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("2").
+					CreationTimestamp(time.Date(2022, 9, 15, 11, 0, 0, 0, time.UTC)).
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 2).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+		},
+		"deletion of excess pod is blocked by another controller": {
```

  ah, gotcha.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-12-11T20:35:38Z

- **pkg/controller/jobs/pod/pod_controller.go:578** by [@achernevskii](https://github.com/achernevskii) — 2023-12-11T20:35:38Z
```diff
@@ -522,63 +501,138 @@ func (p *Pod) constructGroupPodSets(podsInGroup corev1.PodList) ([]kueue.PodSet,
 	return resultPodSets, nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
-	log := ctrl.LoggerFrom(ctx)
-	object := p.Object()
+// validatePodGroupMetadata validates metadata of all members of the pod group
+func (p *Pod) validatePodGroupMetadata(r record.EventRecorder) error {
+	groupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return err
+	}
+	originalQueue := jobframework.QueueName(p)
 
-	var (
-		podSets []kueue.PodSet
-	)
+	if p.countActivePods() < groupTotalCount {
+		errMsg := fmt.Sprintf("'%s' group total count is less than the actual number of pods in the cluster", p.groupName())
+		r.Eventf(p.Object(), corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
+		return jobframework.UnretryableError(errMsg)
+	}
 
-	if !p.isGroup {
-		podSets = p.PodSets()
-	} else {
-		groupTotalCount, err := p.groupTotalCount()
-		if err != nil {
-			return nil, err
+	for _, podInGroup := range p.list.Items {
+		// Skip failed pods
+		if podInGroup.Status.Phase == corev1.PodFailed {
+			continue
 		}
 
-		podCount := 0
-		for i := range p.list.Items {
-			if p.list.Items[i].Status.Phase != corev1.PodFailed {
-				podCount++
-			}
+		if podInGroupQueue := jobframework.QueueNameForObject(&podInGroup); podInGroupQueue != originalQueue {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different queue names: %s!=%s",
+				p.pod.GetName(), podInGroup.GetName(),
+				originalQueue, podInGroupQueue))
 		}
 
-		if podCount != groupTotalCount {
-			errMsg := fmt.Sprintf("'%s' group total count is different from the actual number of pods in the cluster", p.groupName())
-			r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
-			return nil, jobframework.UnretryableError(errMsg)
+		tc, err := strconv.Atoi(podInGroup.GetAnnotations()[GroupTotalCountAnnotation])
+		if err != nil {
+			return fmt.Errorf("failed to extract '%s' annotation from the pod '%s': %w",
+				GroupTotalCountAnnotation,
+				podInGroup.GetName(),
+				err)
+		}
+		if tc != groupTotalCount {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different '%s' values: %d!=%d",
+				p.pod.GetName(), podInGroup.GetName(),
+				GroupTotalCountAnnotation,
+				groupTotalCount, tc))
 		}
+	}
 
-		podSets, err = p.constructGroupPodSets(p.list)
-		if err != nil {
-			if jobframework.IsUnretryableError(err) {
-				r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", err.Error())
+	return nil
+}
+
+// countActivePods returns a count of non-failed pods in the group
+func (p *Pod) countActivePods() int {
+	if p.activePodsCount != nil {
+		return *p.activePodsCount
+	}
+
+	// Filter all failed pods
+	activePodsCount := 0
+	for _, pod := range p.list.Items {
+		if pod.Status.Phase != corev1.PodFailed {
+			activePodsCount++
+		}
+	}
+
+	p.activePodsCount = &activePodsCount
+	return activePodsCount
+}
+
+// cleanupExcessPods will delete and finalize pods created last if the number of
+// activePods is greater than the group-total-count annotation value.
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	groupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return err
+	}
+
+	extraPods := p.countActivePods() - groupTotalCount
+
+	for i := extraPods; i > 0; i-- {
```

  Sure, I can do that. 
  
  Just keep in mind, that I will have to search in the `p.list` for the deleted excess pods here:
  https://github.com/kubernetes-sigs/kueue/blob/a2e0cf4ab5ad082951d72d257c68b5ade8fe73a3/pkg/controller/jobs/pod/pod_controller.go#L609
  
  Since those pods need to be removed from the group list if they are finalized and deleted, but still in the cluster.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-12T16:48:01Z

- **pkg/controller/jobs/pod/pod_controller.go:573** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T16:29:39Z
```diff
@@ -522,63 +501,129 @@ func (p *Pod) constructGroupPodSets(podsInGroup corev1.PodList) ([]kueue.PodSet,
 	return resultPodSets, nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
-	log := ctrl.LoggerFrom(ctx)
-	object := p.Object()
+// validatePodGroupMetadata validates metadata of all members of the pod group
+func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []corev1.Pod) error {
+	groupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return err
+	}
+	originalQueue := jobframework.QueueName(p)
 
-	var (
-		podSets []kueue.PodSet
-	)
+	if len(activePods) < groupTotalCount {
+		errMsg := fmt.Sprintf("'%s' group total count is less than the actual number of pods in the cluster", p.groupName())
+		r.Eventf(p.Object(), corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
+		return jobframework.UnretryableError(errMsg)
+	}
 
-	if !p.isGroup {
-		podSets = p.PodSets()
-	} else {
-		groupTotalCount, err := p.groupTotalCount()
-		if err != nil {
-			return nil, err
+	for _, podInGroup := range p.list.Items {
+		// Skip failed pods
+		if podInGroup.Status.Phase == corev1.PodFailed {
+			continue
 		}
 
-		podCount := 0
-		for i := range p.list.Items {
-			if p.list.Items[i].Status.Phase != corev1.PodFailed {
-				podCount++
-			}
+		if podInGroupQueue := jobframework.QueueNameForObject(&podInGroup); podInGroupQueue != originalQueue {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different queue names: %s!=%s",
+				p.pod.GetName(), podInGroup.GetName(),
+				originalQueue, podInGroupQueue))
 		}
 
-		if podCount != groupTotalCount {
-			errMsg := fmt.Sprintf("'%s' group total count is different from the actual number of pods in the cluster", p.groupName())
-			r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
-			return nil, jobframework.UnretryableError(errMsg)
+		tc, err := strconv.Atoi(podInGroup.GetAnnotations()[GroupTotalCountAnnotation])
+		if err != nil {
+			return fmt.Errorf("failed to extract '%s' annotation from the pod '%s': %w",
+				GroupTotalCountAnnotation,
+				podInGroup.GetName(),
+				err)
 		}
+		if tc != groupTotalCount {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different '%s' values: %d!=%d",
+				p.pod.GetName(), podInGroup.GetName(),
+				GroupTotalCountAnnotation,
+				groupTotalCount, tc))
+		}
+	}
 
-		podSets, err = p.constructGroupPodSets(p.list)
-		if err != nil {
-			if jobframework.IsUnretryableError(err) {
-				r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", err.Error())
+	return nil
+}
+
+// activePods returns a slice of non-failed pods in the group
+func (p *Pod) activePods() []corev1.Pod {
+	// Filter all failed pods
+	activePods := make([]corev1.Pod, 0, len(p.list.Items))
+	for _, pod := range p.list.Items {
+		if pod.Status.Phase != corev1.PodFailed {
+			activePods = append(activePods, pod)
+		}
+	}
+
+	return activePods
+}
+
+// cleanupExcessPods will delete and finalize pods created last if the number of
+// activePods is greater than the totalCount value.
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount int, activePods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	extraPodsCount := len(activePods) - totalCount
+
+	if extraPodsCount <= 0 {
+		return nil
+	}
+
+	// Sort active pods by creation timestamp
+	sort.SliceStable(activePods, func(i, j int) bool {
```

  do we need it to be stable? Regular sort uses less memory.

- **pkg/controller/jobs/pod/pod_controller.go:583** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T16:31:36Z
```diff
@@ -522,63 +501,129 @@ func (p *Pod) constructGroupPodSets(podsInGroup corev1.PodList) ([]kueue.PodSet,
 	return resultPodSets, nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
-	log := ctrl.LoggerFrom(ctx)
-	object := p.Object()
+// validatePodGroupMetadata validates metadata of all members of the pod group
+func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []corev1.Pod) error {
+	groupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return err
+	}
+	originalQueue := jobframework.QueueName(p)
 
-	var (
-		podSets []kueue.PodSet
-	)
+	if len(activePods) < groupTotalCount {
+		errMsg := fmt.Sprintf("'%s' group total count is less than the actual number of pods in the cluster", p.groupName())
+		r.Eventf(p.Object(), corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
+		return jobframework.UnretryableError(errMsg)
+	}
 
-	if !p.isGroup {
-		podSets = p.PodSets()
-	} else {
-		groupTotalCount, err := p.groupTotalCount()
-		if err != nil {
-			return nil, err
+	for _, podInGroup := range p.list.Items {
+		// Skip failed pods
+		if podInGroup.Status.Phase == corev1.PodFailed {
+			continue
 		}
 
-		podCount := 0
-		for i := range p.list.Items {
-			if p.list.Items[i].Status.Phase != corev1.PodFailed {
-				podCount++
-			}
+		if podInGroupQueue := jobframework.QueueNameForObject(&podInGroup); podInGroupQueue != originalQueue {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different queue names: %s!=%s",
+				p.pod.GetName(), podInGroup.GetName(),
+				originalQueue, podInGroupQueue))
 		}
 
-		if podCount != groupTotalCount {
-			errMsg := fmt.Sprintf("'%s' group total count is different from the actual number of pods in the cluster", p.groupName())
-			r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
-			return nil, jobframework.UnretryableError(errMsg)
+		tc, err := strconv.Atoi(podInGroup.GetAnnotations()[GroupTotalCountAnnotation])
+		if err != nil {
+			return fmt.Errorf("failed to extract '%s' annotation from the pod '%s': %w",
+				GroupTotalCountAnnotation,
+				podInGroup.GetName(),
+				err)
 		}
+		if tc != groupTotalCount {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different '%s' values: %d!=%d",
+				p.pod.GetName(), podInGroup.GetName(),
+				GroupTotalCountAnnotation,
+				groupTotalCount, tc))
+		}
+	}
 
-		podSets, err = p.constructGroupPodSets(p.list)
-		if err != nil {
-			if jobframework.IsUnretryableError(err) {
-				r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", err.Error())
+	return nil
+}
+
+// activePods returns a slice of non-failed pods in the group
+func (p *Pod) activePods() []corev1.Pod {
+	// Filter all failed pods
+	activePods := make([]corev1.Pod, 0, len(p.list.Items))
+	for _, pod := range p.list.Items {
+		if pod.Status.Phase != corev1.PodFailed {
+			activePods = append(activePods, pod)
+		}
+	}
+
+	return activePods
+}
+
+// cleanupExcessPods will delete and finalize pods created last if the number of
+// activePods is greater than the totalCount value.
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount int, activePods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	extraPodsCount := len(activePods) - totalCount
+
+	if extraPodsCount <= 0 {
+		return nil
+	}
+
+	// Sort active pods by creation timestamp
+	sort.SliceStable(activePods, func(i, j int) bool {
+		return activePods[i].ObjectMeta.CreationTimestamp.Before(&activePods[j].ObjectMeta.CreationTimestamp)
+	})
+
+	// Extract all the latest created extra pods
+	extraPods := activePods[len(activePods)-extraPodsCount:]
+
+	// Finalize and delete the active pods created last
+	for _, extraPod := range extraPods {
+		if controllerutil.RemoveFinalizer(&extraPod, PodFinalizer) {
+			log.V(3).Info("Finalizing excess pod in group", "ExcessPod.Name", extraPod.Name)
```

  ```suggestion
  			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(extraPod))
  ```

- **pkg/controller/jobs/pod/pod_controller.go:589** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T16:31:49Z
```diff
@@ -522,63 +501,129 @@ func (p *Pod) constructGroupPodSets(podsInGroup corev1.PodList) ([]kueue.PodSet,
 	return resultPodSets, nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
-	log := ctrl.LoggerFrom(ctx)
-	object := p.Object()
+// validatePodGroupMetadata validates metadata of all members of the pod group
+func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []corev1.Pod) error {
+	groupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return err
+	}
+	originalQueue := jobframework.QueueName(p)
 
-	var (
-		podSets []kueue.PodSet
-	)
+	if len(activePods) < groupTotalCount {
+		errMsg := fmt.Sprintf("'%s' group total count is less than the actual number of pods in the cluster", p.groupName())
+		r.Eventf(p.Object(), corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
+		return jobframework.UnretryableError(errMsg)
+	}
 
-	if !p.isGroup {
-		podSets = p.PodSets()
-	} else {
-		groupTotalCount, err := p.groupTotalCount()
-		if err != nil {
-			return nil, err
+	for _, podInGroup := range p.list.Items {
+		// Skip failed pods
+		if podInGroup.Status.Phase == corev1.PodFailed {
+			continue
 		}
 
-		podCount := 0
-		for i := range p.list.Items {
-			if p.list.Items[i].Status.Phase != corev1.PodFailed {
-				podCount++
-			}
+		if podInGroupQueue := jobframework.QueueNameForObject(&podInGroup); podInGroupQueue != originalQueue {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different queue names: %s!=%s",
+				p.pod.GetName(), podInGroup.GetName(),
+				originalQueue, podInGroupQueue))
 		}
 
-		if podCount != groupTotalCount {
-			errMsg := fmt.Sprintf("'%s' group total count is different from the actual number of pods in the cluster", p.groupName())
-			r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
-			return nil, jobframework.UnretryableError(errMsg)
+		tc, err := strconv.Atoi(podInGroup.GetAnnotations()[GroupTotalCountAnnotation])
+		if err != nil {
+			return fmt.Errorf("failed to extract '%s' annotation from the pod '%s': %w",
+				GroupTotalCountAnnotation,
+				podInGroup.GetName(),
+				err)
 		}
+		if tc != groupTotalCount {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different '%s' values: %d!=%d",
+				p.pod.GetName(), podInGroup.GetName(),
+				GroupTotalCountAnnotation,
+				groupTotalCount, tc))
+		}
+	}
 
-		podSets, err = p.constructGroupPodSets(p.list)
-		if err != nil {
-			if jobframework.IsUnretryableError(err) {
-				r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", err.Error())
+	return nil
+}
+
+// activePods returns a slice of non-failed pods in the group
+func (p *Pod) activePods() []corev1.Pod {
+	// Filter all failed pods
+	activePods := make([]corev1.Pod, 0, len(p.list.Items))
+	for _, pod := range p.list.Items {
+		if pod.Status.Phase != corev1.PodFailed {
+			activePods = append(activePods, pod)
+		}
+	}
+
+	return activePods
+}
+
+// cleanupExcessPods will delete and finalize pods created last if the number of
+// activePods is greater than the totalCount value.
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount int, activePods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	extraPodsCount := len(activePods) - totalCount
+
+	if extraPodsCount <= 0 {
+		return nil
+	}
+
+	// Sort active pods by creation timestamp
+	sort.SliceStable(activePods, func(i, j int) bool {
+		return activePods[i].ObjectMeta.CreationTimestamp.Before(&activePods[j].ObjectMeta.CreationTimestamp)
+	})
+
+	// Extract all the latest created extra pods
+	extraPods := activePods[len(activePods)-extraPodsCount:]
+
+	// Finalize and delete the active pods created last
+	for _, extraPod := range extraPods {
+		if controllerutil.RemoveFinalizer(&extraPod, PodFinalizer) {
+			log.V(3).Info("Finalizing excess pod in group", "ExcessPod.Name", extraPod.Name)
+			if err := c.Update(ctx, &extraPod); err != nil {
+				return err
+			}
+		}
+		if extraPod.ObjectMeta.DeletionTimestamp.IsZero() {
+			log.V(3).Info("Deleting excess pod in group", "ExcessPod.Name", extraPod.Name)
```

  same here

- **pkg/controller/jobs/pod/pod_controller.go:599** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-12T16:36:10Z
```diff
@@ -522,63 +501,129 @@ func (p *Pod) constructGroupPodSets(podsInGroup corev1.PodList) ([]kueue.PodSet,
 	return resultPodSets, nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
-	log := ctrl.LoggerFrom(ctx)
-	object := p.Object()
+// validatePodGroupMetadata validates metadata of all members of the pod group
+func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []corev1.Pod) error {
+	groupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return err
+	}
+	originalQueue := jobframework.QueueName(p)
 
-	var (
-		podSets []kueue.PodSet
-	)
+	if len(activePods) < groupTotalCount {
+		errMsg := fmt.Sprintf("'%s' group total count is less than the actual number of pods in the cluster", p.groupName())
+		r.Eventf(p.Object(), corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
+		return jobframework.UnretryableError(errMsg)
+	}
 
-	if !p.isGroup {
-		podSets = p.PodSets()
-	} else {
-		groupTotalCount, err := p.groupTotalCount()
-		if err != nil {
-			return nil, err
+	for _, podInGroup := range p.list.Items {
+		// Skip failed pods
+		if podInGroup.Status.Phase == corev1.PodFailed {
+			continue
 		}
 
-		podCount := 0
-		for i := range p.list.Items {
-			if p.list.Items[i].Status.Phase != corev1.PodFailed {
-				podCount++
-			}
+		if podInGroupQueue := jobframework.QueueNameForObject(&podInGroup); podInGroupQueue != originalQueue {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different queue names: %s!=%s",
+				p.pod.GetName(), podInGroup.GetName(),
+				originalQueue, podInGroupQueue))
 		}
 
-		if podCount != groupTotalCount {
-			errMsg := fmt.Sprintf("'%s' group total count is different from the actual number of pods in the cluster", p.groupName())
-			r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
-			return nil, jobframework.UnretryableError(errMsg)
+		tc, err := strconv.Atoi(podInGroup.GetAnnotations()[GroupTotalCountAnnotation])
+		if err != nil {
+			return fmt.Errorf("failed to extract '%s' annotation from the pod '%s': %w",
+				GroupTotalCountAnnotation,
+				podInGroup.GetName(),
+				err)
 		}
+		if tc != groupTotalCount {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different '%s' values: %d!=%d",
+				p.pod.GetName(), podInGroup.GetName(),
+				GroupTotalCountAnnotation,
+				groupTotalCount, tc))
+		}
+	}
 
-		podSets, err = p.constructGroupPodSets(p.list)
-		if err != nil {
-			if jobframework.IsUnretryableError(err) {
-				r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", err.Error())
+	return nil
+}
+
+// activePods returns a slice of non-failed pods in the group
+func (p *Pod) activePods() []corev1.Pod {
+	// Filter all failed pods
+	activePods := make([]corev1.Pod, 0, len(p.list.Items))
+	for _, pod := range p.list.Items {
+		if pod.Status.Phase != corev1.PodFailed {
+			activePods = append(activePods, pod)
+		}
+	}
+
+	return activePods
+}
+
+// cleanupExcessPods will delete and finalize pods created last if the number of
+// activePods is greater than the totalCount value.
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount int, activePods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	extraPodsCount := len(activePods) - totalCount
+
+	if extraPodsCount <= 0 {
+		return nil
+	}
+
+	// Sort active pods by creation timestamp
+	sort.SliceStable(activePods, func(i, j int) bool {
+		return activePods[i].ObjectMeta.CreationTimestamp.Before(&activePods[j].ObjectMeta.CreationTimestamp)
+	})
+
+	// Extract all the latest created extra pods
+	extraPods := activePods[len(activePods)-extraPodsCount:]
+
+	// Finalize and delete the active pods created last
+	for _, extraPod := range extraPods {
+		if controllerutil.RemoveFinalizer(&extraPod, PodFinalizer) {
+			log.V(3).Info("Finalizing excess pod in group", "ExcessPod.Name", extraPod.Name)
+			if err := c.Update(ctx, &extraPod); err != nil {
+				return err
+			}
+		}
+		if extraPod.ObjectMeta.DeletionTimestamp.IsZero() {
+			log.V(3).Info("Deleting excess pod in group", "ExcessPod.Name", extraPod.Name)
+			if err := c.Delete(ctx, &extraPod); err != nil {
+				return err
 			}
-			return nil, err
 		}
 
-		if len(podSets) > 8 {
-			return nil, jobframework.UnretryableError(errMsgIncorrectGroupRoleCount)
+		// Remove excess pod from the group list
+		for i := range p.list.Items {
+			if p.list.Items[i].Name == extraPod.Name && p.list.Items[i].Namespace == extraPod.Namespace {
+				p.list.Items = append(p.list.Items[:i], p.list.Items[i+1:]...)
+			}
```

  This is an expensive way to remove items from a list, as it will do allocations for every deleted item.
  
  Better generate a new list, like so:
  
  ```golang
  newList = make([]Pods, 0, len(p.list.Items)-len(extra))
  for _, pod := range p.list.Items {
    if (pod not found in extra) {
      newList = append(newList, p)
    }
  }
  p.list.Items = newList
  ```

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2023-12-12T19:44:03Z

- **pkg/controller/jobs/pod/pod_controller.go:573** by [@achernevskii](https://github.com/achernevskii) — 2023-12-12T19:44:03Z
```diff
@@ -522,63 +501,129 @@ func (p *Pod) constructGroupPodSets(podsInGroup corev1.PodList) ([]kueue.PodSet,
 	return resultPodSets, nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
-	log := ctrl.LoggerFrom(ctx)
-	object := p.Object()
+// validatePodGroupMetadata validates metadata of all members of the pod group
+func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []corev1.Pod) error {
+	groupTotalCount, err := p.groupTotalCount()
+	if err != nil {
+		return err
+	}
+	originalQueue := jobframework.QueueName(p)
 
-	var (
-		podSets []kueue.PodSet
-	)
+	if len(activePods) < groupTotalCount {
+		errMsg := fmt.Sprintf("'%s' group total count is less than the actual number of pods in the cluster", p.groupName())
+		r.Eventf(p.Object(), corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
+		return jobframework.UnretryableError(errMsg)
+	}
 
-	if !p.isGroup {
-		podSets = p.PodSets()
-	} else {
-		groupTotalCount, err := p.groupTotalCount()
-		if err != nil {
-			return nil, err
+	for _, podInGroup := range p.list.Items {
+		// Skip failed pods
+		if podInGroup.Status.Phase == corev1.PodFailed {
+			continue
 		}
 
-		podCount := 0
-		for i := range p.list.Items {
-			if p.list.Items[i].Status.Phase != corev1.PodFailed {
-				podCount++
-			}
+		if podInGroupQueue := jobframework.QueueNameForObject(&podInGroup); podInGroupQueue != originalQueue {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different queue names: %s!=%s",
+				p.pod.GetName(), podInGroup.GetName(),
+				originalQueue, podInGroupQueue))
 		}
 
-		if podCount != groupTotalCount {
-			errMsg := fmt.Sprintf("'%s' group total count is different from the actual number of pods in the cluster", p.groupName())
-			r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", errMsg)
-			return nil, jobframework.UnretryableError(errMsg)
+		tc, err := strconv.Atoi(podInGroup.GetAnnotations()[GroupTotalCountAnnotation])
+		if err != nil {
+			return fmt.Errorf("failed to extract '%s' annotation from the pod '%s': %w",
+				GroupTotalCountAnnotation,
+				podInGroup.GetName(),
+				err)
 		}
+		if tc != groupTotalCount {
+			return jobframework.UnretryableError(fmt.Sprintf("pods '%s' and '%s' has different '%s' values: %d!=%d",
+				p.pod.GetName(), podInGroup.GetName(),
+				GroupTotalCountAnnotation,
+				groupTotalCount, tc))
+		}
+	}
 
-		podSets, err = p.constructGroupPodSets(p.list)
-		if err != nil {
-			if jobframework.IsUnretryableError(err) {
-				r.Eventf(object, corev1.EventTypeWarning, "ErrWorkloadCompose", err.Error())
+	return nil
+}
+
+// activePods returns a slice of non-failed pods in the group
+func (p *Pod) activePods() []corev1.Pod {
+	// Filter all failed pods
+	activePods := make([]corev1.Pod, 0, len(p.list.Items))
+	for _, pod := range p.list.Items {
+		if pod.Status.Phase != corev1.PodFailed {
+			activePods = append(activePods, pod)
+		}
+	}
+
+	return activePods
+}
+
+// cleanupExcessPods will delete and finalize pods created last if the number of
+// activePods is greater than the totalCount value.
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount int, activePods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	extraPodsCount := len(activePods) - totalCount
+
+	if extraPodsCount <= 0 {
+		return nil
+	}
+
+	// Sort active pods by creation timestamp
+	sort.SliceStable(activePods, func(i, j int) bool {
```

  You're right. If there's two pods with equal creation timestamp we don't care which one to delete. 
  
  Updated to unstable sort in 35fad1d1
