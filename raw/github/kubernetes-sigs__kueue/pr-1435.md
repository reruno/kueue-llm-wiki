# PR #1435: Upgrade RayJob API to v1

**Summary**: Upgrade RayJob API to v1

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1435

**Last updated**: 2024-03-05T10:01:55Z

---

## Metadata

- **State**: closed
- **Author**: [@astefanutti](https://github.com/astefanutti)
- **Branch**: `pr-01` → `main`
- **Created**: 2023-12-11T15:59:32Z
- **Updated**: 2024-03-05T10:01:55Z
- **Merged**: —
- **Closed**: 2024-03-05T10:01:52Z
- **Labels**: `release-note`, `size/XL`, `needs-rebase`, `ok-to-test`, `do-not-merge/work-in-progress`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 20   **+679 / -149**

## Description

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
-->

#### What this PR does / why we need it:

KubeRay has promoted the RayJob API from v1alpha1 to v1 with KubeRay v1.0.0. While the v1alpha1 versions is still served, it'll eventually be removed.

#### Which issue(s) this PR fixes:

Fixes #1335.

#### Special notes for your reviewer:

This PR adds a mechanism that makes it possible to configure a framework integration dynamically, depending on the version of the framework API that is installed on the cluster for example.

That mechanism is used so both RayJob v1alpha1 and v1 can be supported. Specifically, v1alpha1-aware controller and webhooks are setup when the v1alpha1 API is installed. Otherwise, v1-aware ones are setup when the v1 API is installed, which works also for the v1alpha1 as it's fully round-trippable to v1. In the latter configuration, the v1alpha1 webhook is NOOP'ed. 

#### Does this PR introduce a user-facing change?

```release-note
Upgrade RayJob API to v1
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-12-11T15:59:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ccd647037ebe24381301a24c01a409c1de33cb90 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/658447f2d35cb200086fadc3 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1435--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1435--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE0MzUtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.NvzzmLUoK0Xp0C4hRnxqt0nwIYKQkTPKhaXO7QrrLKY)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-11T15:59:39Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1435#" title="Author self-approved">astefanutti</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-11T15:59:42Z

Welcome @astefanutti! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-11T15:59:43Z

Hi @astefanutti. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@astefanutti](https://github.com/astefanutti) — 2023-12-11T16:02:13Z

/cc @andrewsykim @tenzen-y

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T18:57:43Z

/ok-to-test

### Comment by [@andrewsykim](https://github.com/andrewsykim) — 2023-12-28T15:08:24Z

FYI @astefanutti that v1alpha1 will be deleted in the next release https://github.com/ray-project/kuberay/pull/1771 

So the only version that has both v1 and v1alpha1 will be v1.0.0

### Comment by [@kevin85421](https://github.com/kevin85421) — 2023-12-28T19:13:27Z

I just realized that Kueue contains KubeRay-specific code. Is there a timeline for this PR? I am aiming to release KubeRay 1.1.0 at the end of January 2024 or early February 2024 (need to sync with Ray 2.10.0 release timeline).

### Comment by [@kevin85421](https://github.com/kevin85421) — 2023-12-29T18:17:14Z

Update: We have already reverted the v1alpha1 deprecation. We will deprecate v1alpha1 in KubeRay 1.2.0 instead. Thanks!

### Comment by [@astefanutti](https://github.com/astefanutti) — 2024-01-05T13:43:36Z

> How can we test this for the scenarios where v1alpha1 exists/doesn't exit.

@alculquicondor 
- v1alpha1 only scenario can be tested with Kuberay v0.6.0
- v1alpha1 + v1 scenario can be tested with Kuberay v1.0.0
- v1 only scenario can be tested with Kuberay v1.0.0 and by editing the RayJob CRD to remove the v1alpha1 served version.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-05T16:10:14Z

Since v1alpha1 will be supported in kuberay v1.1.0, I would prefer we keep using v1alpha1 in kueue v0.6 and fully migrate to v1 in v0.7. WDYT?

Still, there are some learnings in this PR that can be extracted into a separate PR.

### Comment by [@astefanutti](https://github.com/astefanutti) — 2024-01-05T16:23:33Z

> Since v1alpha1 will be supported in kuberay v1.1.0, I would prefer we keep using v1alpha1 in kueue v0.6 and fully migrate to v1 in v0.7. WDYT?

I agree, that'd also be my preference to keep things as simple as possible, if having the interlock between Kueue v0.7 and Kuberay v1.2.0 is acceptable on your end.

> Still, there are some learnings in this PR that can be extracted into a separate PR.

I can prune the extra commit that adds multi-versions support from that PR, and put it on hold until we do the Kuberay v1.2.0 upgrade. We can have a separate discussion on what else you think can be extracted into a separate PR.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-11T19:03:25Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@astefanutti](https://github.com/astefanutti) — 2024-03-05T10:01:49Z

I've created a new PR #1802 that's based on the feedback that's been discussed in this one.

/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-05T10:01:53Z

@astefanutti: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1435#issuecomment-1978385337):

>I've created a new PR #1802 that's based on the feedback that's been discussed in this one.
>
>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-12-11T16:23:31Z

- **config/components/webhook/manifests.yaml:192** by [@andrewsykim](https://github.com/andrewsykim) — 2023-12-11T16:23:27Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  Wondering if we have to support both v1 and v1alpha1 for some period of time.
  
  Specifically thinking about clusters that are still using KubeRay with only v1alpha1 but upgrade Kueue to the latest version

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-11T16:27:20Z

- **config/components/webhook/manifests.yaml:192** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T16:27:20Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  > Wondering if we have to support both v1 and v1alpha1 for some period of time.
  
  I agree. Was v1alpha1 API completely removed on the kuberay side?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2023-12-11T16:32:01Z

- **config/components/webhook/manifests.yaml:192** by [@astefanutti](https://github.com/astefanutti) — 2023-12-11T16:32:01Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  With the match policy set to `Equivalent`, the webhook is also intercepting requests for v1alpha1.
  
  So, the main concern for supporting KubeRay installations (pre v1), with newer versions of Kueue is for the controller being installed (and the indexers). At the moment controller registration for integrations is very static.
  
  One option may be to introduce a mechanism that enables to register controllers dynamically, based on the discovered APIs. Alternatively, a (special) `ray.io/rayjob/v1alpha1` could be registered, but users would have to update their configuration manually.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2023-12-11T16:35:50Z

- **config/components/webhook/manifests.yaml:192** by [@astefanutti](https://github.com/astefanutti) — 2023-12-11T16:35:50Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  > > Wondering if we have to support both v1 and v1alpha1 for some period of time.
  > 
  > I agree. Was v1alpha1 API completely removed on the kuberay side?
  
  Currently in KubeRay, v1alpha1 is still served, with v1 defined as storage.

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-12-11T17:03:28Z

- **config/components/webhook/manifests.yaml:192** by [@andrewsykim](https://github.com/andrewsykim) — 2023-12-11T17:03:27Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  > With the match policy set to Equivalent, the webhook is also intercepting requests for v1alpha1.
  
  Right, so if there's a v1alpha1 request, there will be a conversion call from v1alpha1 -> v1 before dispatching the request to Kueue. The problem here is that Kuberay opted out of supporting conversion webhooks, so will this conversion work? (I don't actually know, can we test it?)

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-12-11T17:04:29Z

- **config/components/webhook/manifests.yaml:192** by [@andrewsykim](https://github.com/andrewsykim) — 2023-12-11T17:04:29Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  And follow-up to last comment, if there's no conversion webhook, do we also need to change the matchPolicy to `Exact`?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2023-12-11T17:27:06Z

- **config/components/webhook/manifests.yaml:192** by [@astefanutti](https://github.com/astefanutti) — 2023-12-11T17:27:06Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  > > With the match policy set to Equivalent, the webhook is also intercepting requests for v1alpha1.
  > 
  > Right, so if there's a v1alpha1 request, there will be a conversion call from v1alpha1 -> v1 before dispatching the request to Kueue. The problem here is that Kuberay opted out of supporting conversion webhooks, so will this conversion work? (I don't actually know, can we test it?)
  
  Yes, it works (I've tested it). RayJob v1alpha1 and v1 are actually identical, and the API server simply round-trips between the two, depending on the request. Generally, as long as the subset of the API that Kueue depends on is round tripped by the API server correctly, no conversion is required from the Kueue standpoint.
  
  FWIW, it actually works fine as it is at the moment, i.e., with Kueue using RayJob v1alpha1, even for dealing with RayJob v1. But it'd be preferable to anticipate a solution when v1alpha1 will be removed.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2023-12-11T17:29:05Z

- **config/components/webhook/manifests.yaml:192** by [@astefanutti](https://github.com/astefanutti) — 2023-12-11T17:29:05Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  > And follow-up to last comment, if there's no conversion webhook, do we also need to change the matchPolicy to `Exact`?
  
  As the subset of the API that Kueue depends on between v1alpha1 and v1 is the same, the same webhook can be used for both, so there is no need to change the match policy to `Exact`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-11T17:39:08Z

- **config/components/webhook/manifests.yaml:192** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-11T17:39:08Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  > So, the main concern for supporting KubeRay installations (pre v1), with newer versions of Kueue is for the controller being installed (and the indexers). At the moment controller registration for integrations is very static.
  
  I think that we don't have such concerns since the kueue-controller-manager will dynamically add to integration controllers to the manager like:
  
  https://github.com/kubernetes-sigs/kueue/blob/74377c7eb3e37db4703538abbfb9432718be4097/cmd/kueue/main.go#L261-L271
  
  So I think that we can just implement another controller and webhooks for the kuberay v1.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2023-12-11T17:57:10Z

- **config/components/webhook/manifests.yaml:192** by [@astefanutti](https://github.com/astefanutti) — 2023-12-11T17:57:10Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  My understanding is in the case where both v1alpha1 and v1 are installed (like in KubeRay v1.0.0), both controllers will be started and will reconcile both versions at the same time, with the API server doing the conversion / round-tripping.  Having two controllers reconciling the same resources does not sound right, is it?
  
  Also with that approach, a new, let say, `ray.io/rayjob/v1` integration would have to be introduced. Is it an acceptable pattern?
  
  I was suggesting that it may be better to keep the single controller per integration approach, but the integration layer be able to decide what controller / callbacks should be provided dynamically.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T19:03:18Z

- **config/components/webhook/manifests.yaml:192** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T19:03:17Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  I would prefer we have a single controller, but with the current approach, it sounds like Kueue would stop working for versions of kuberay older than 1.0, as the CRD wouldn't be installed.
  
  Maybe we should wait for the next release of Kueue (v0.7) before doing this change?
  
  > One option may be to introduce a mechanism that enables to register controllers dynamically, based on the discovered APIs.
  
  That would be great, but maybe too much machinery?

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-12-14T01:41:34Z

- **pkg/controller/jobs/pod/pod_controller.go:817** by [@andrewsykim](https://github.com/andrewsykim) — 2023-12-14T01:41:30Z
```diff
@@ -701,7 +700,7 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 
 func IsPodOwnerManagedByKueue(p *Pod) bool {
 	if owner := metav1.GetControllerOf(&p.pod); owner != nil {
-		return jobframework.IsOwnerManagedByKueue(owner) || (owner.Kind == "RayCluster" && strings.HasPrefix(owner.APIVersion, "ray.io/v1alpha1"))
```

  I don't have context on why the `owner.Kind == "RayCluster"` check is here, but why are we removing it?
  
  My guess is that this is here because owner reference of pods are always RayCluster, regardless if it was a RayCluster created from a RayJob. FYI @vicentefb we'll need to look at this for RayCluster support

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2023-12-14T09:25:36Z

- **config/components/webhook/manifests.yaml:192** by [@astefanutti](https://github.com/astefanutti) — 2023-12-14T09:25:35Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  > I would prefer we have a single controller, but with the current approach, it sounds like Kueue would stop working for versions of kuberay older than 1.0, as the CRD wouldn't be installed.
  
  I agree, having a single controller sounds like a more manageable option.
  
  > Maybe we should wait for the next release of Kueue (v0.7) before doing this change?
  
  That would be a good _fallback_ option.
  
  > > One option may be to introduce a mechanism that enables to register controllers dynamically, based on the discovered APIs.
  >
  > That would be great, but maybe too much machinery?
  
  I would not think it'd be that much machinery really. Currently each integration is registered and configured using a package-level init function.
  
  One option could be to keep the registration in these init functions, but instead of returning a static `jobframework.IntegrationCallbacks` value, rather registering a provider / factory for it to be called later during the setup in Kueue main, and have the manager client passed to these provider / factory functions, so they can call the discovery API for example, and switch from one controller to the other depending on the presence of the API versions. Any other pattern that would delay the configuration and make it _dynamic_ would work as well.
  
  I can do a quick PoC to see how that would like in practice if you're open to the idea.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2023-12-14T09:28:43Z

- **pkg/controller/jobs/pod/pod_controller.go:817** by [@astefanutti](https://github.com/astefanutti) — 2023-12-14T09:28:43Z
```diff
@@ -701,7 +700,7 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 
 func IsPodOwnerManagedByKueue(p *Pod) bool {
 	if owner := metav1.GetControllerOf(&p.pod); owner != nil {
-		return jobframework.IsOwnerManagedByKueue(owner) || (owner.Kind == "RayCluster" && strings.HasPrefix(owner.APIVersion, "ray.io/v1alpha1"))
```

  Actually, it's not removed. The check is moved to the `IsManagingObjectsOwner` callback of the RayJob integration in 8f289ae757ee0cd7c5b593415069e4a9666d127b, so the existing behaviour should not change there.

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2023-12-14T15:12:02Z

- **pkg/controller/jobs/pod/pod_controller.go:817** by [@andrewsykim](https://github.com/andrewsykim) — 2023-12-14T15:12:02Z
```diff
@@ -701,7 +700,7 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 
 func IsPodOwnerManagedByKueue(p *Pod) bool {
 	if owner := metav1.GetControllerOf(&p.pod); owner != nil {
-		return jobframework.IsOwnerManagedByKueue(owner) || (owner.Kind == "RayCluster" && strings.HasPrefix(owner.APIVersion, "ray.io/v1alpha1"))
```

  Thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-14T16:39:42Z

- **config/components/webhook/manifests.yaml:192** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-14T16:39:42Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  I guess we would still have duplicated code, as we would be using different API types?
  
  But I'm open to see a PoC

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-15T15:16:03Z

- **config/components/webhook/manifests.yaml:192** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-15T15:16:03Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  @astefanutti Thank you for the effort. I'm also open to seeing a PoC.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2023-12-21T14:38:07Z

- **config/components/webhook/manifests.yaml:192** by [@astefanutti](https://github.com/astefanutti) — 2023-12-21T14:38:07Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  @alculquicondor @tenzen-y I've pushed ccd647037ebe24381301a24c01a409c1de33cb90 that adds support for configuring framework integration using the Discovery API.
  
  I've applied that mechanism for Ray, and tested it successfully with Kuberay v1alpha1-only API, v1alpha1+v1 API, and v1-only API cases.
  
  It might be possible to reduce the level of duplication, but I think that may actually introduce more complexity to the solution, and makes the removal for unsupported versions in the future less trivial.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-27T22:07:50Z

The amount of duplication seems ok.

How can we test this for the scenarios where v1alpha1 exists/doesn't exit.

- **config/components/webhook/manifests.yaml:192** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T21:34:15Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  Don't we need some changes in the webhook configuration?

- **pkg/controller/jobframework/integrationmanager.go:165** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T21:39:12Z
```diff
@@ -126,11 +146,30 @@ func (m *integrationManager) getCallbacksForOwner(ownerRef *metav1.OwnerReferenc
 	return nil
 }
 
+func SetupProviders(discoveryClient discovery.DiscoveryInterface) error {
+	for name, provider := range manager.providers {
+		if integration, err := provider(discoveryClient); err != nil {
+			return err
+		} else {
+			if err := manager.register(name, integration); err != nil {
+				return err
+			}
+		}
+	}
+	return nil
+}
+
 // RegisterIntegration registers a new framework, returns an error when
 // attempting to register multiple frameworks with the same name of if a
 // mandatory callback is missing.
-func RegisterIntegration(name string, cb IntegrationCallbacks) error {
-	return manager.register(name, cb)
+func RegisterIntegration[T Integration](name string, integration T) error {
```

  Wouldn't this work?
  
  ```suggestion
  func RegisterIntegration(name string, integration Integration) error {
  ```

- **pkg/controller/jobs/pod/pod_controller.go:817** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-27T21:58:04Z
```diff
@@ -701,7 +700,7 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 
 func IsPodOwnerManagedByKueue(p *Pod) bool {
 	if owner := metav1.GetControllerOf(&p.pod); owner != nil {
-		return jobframework.IsOwnerManagedByKueue(owner) || (owner.Kind == "RayCluster" && strings.HasPrefix(owner.APIVersion, "ray.io/v1alpha1"))
```

  Context: This logic is used to avoid queueing Pods that were already queued by a RayJob. However, this is not accurate, as the user might have created a RayCluster directly instead, which is not currently supported.
  
  Other than that, `IsOwnerManagedByKueue` is used to find the parent object of a Job that is also managed by Kueue (like JobSet). IIUC, RayCluster was changed to use Job? Is that part of the v1 update?
  In that case, a Job could be owned by a RayCluster. However, we don't kueue RayClusters, we queue RayJobs, so this logic might be broken https://github.com/kubernetes-sigs/kueue/blob/a0757fa87aeb5765c455e26d6abe53f9b847a586/pkg/controller/jobframework/reconciler.go#L414-L421

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-05T13:09:24Z

- **pkg/controller/jobframework/integrationmanager.go:165** by [@astefanutti](https://github.com/astefanutti) — 2024-01-05T13:09:24Z
```diff
@@ -126,11 +146,30 @@ func (m *integrationManager) getCallbacksForOwner(ownerRef *metav1.OwnerReferenc
 	return nil
 }
 
+func SetupProviders(discoveryClient discovery.DiscoveryInterface) error {
+	for name, provider := range manager.providers {
+		if integration, err := provider(discoveryClient); err != nil {
+			return err
+		} else {
+			if err := manager.register(name, integration); err != nil {
+				return err
+			}
+		}
+	}
+	return nil
+}
+
 // RegisterIntegration registers a new framework, returns an error when
 // attempting to register multiple frameworks with the same name of if a
 // mandatory callback is missing.
-func RegisterIntegration(name string, cb IntegrationCallbacks) error {
-	return manager.register(name, cb)
+func RegisterIntegration[T Integration](name string, integration T) error {
```

  The `Integration` interface contains some type constraints, and that can only be used with type parameters.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-05T13:21:04Z

- **config/components/webhook/manifests.yaml:192** by [@astefanutti](https://github.com/astefanutti) — 2024-01-05T13:21:04Z
```diff
@@ -181,14 +181,14 @@ webhooks:
     service:
       name: webhook-service
       namespace: system
-      path: /mutate-ray-io-v1alpha1-rayjob
+      path: /mutate-ray-io-v1-rayjob
   failurePolicy: Fail
   name: mrayjob.kb.io
   rules:
   - apiGroups:
     - ray.io
     apiVersions:
-    - v1alpha1
+    - v1
```

  AFAIK, that doesn't require any other changes to those already made in the `config/components/webhook/manifests.yaml` file.
  
  Do you have any specific changes you'd think should be added?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-05T13:35:30Z

- **pkg/controller/jobs/pod/pod_controller.go:817** by [@astefanutti](https://github.com/astefanutti) — 2024-01-05T13:35:30Z
```diff
@@ -701,7 +700,7 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 
 func IsPodOwnerManagedByKueue(p *Pod) bool {
 	if owner := metav1.GetControllerOf(&p.pod); owner != nil {
-		return jobframework.IsOwnerManagedByKueue(owner) || (owner.Kind == "RayCluster" && strings.HasPrefix(owner.APIVersion, "ray.io/v1alpha1"))
```

  > Other than that, IsOwnerManagedByKueue is used to find the parent object of a Job that is also managed by Kueue (like JobSet).
  
  That PR 1) moves the existing Ray related ad-hoc logic in the Pod controller to the RayJob controller by leveraging the `IsOwnerManagedByKueue` callback and 2) adds the `v1` group.
  
  > IIUC, RayCluster was changed to use Job?
  
  RayJob (not RayCluster) now uses Job to submit the ray job to the ray cluster.
  
  > Is that part of the v1 update?
  
  No, I haven't made that change, as it's an existing bug that's probably better to be fixed as part of #1434.
  
  > In that case, a Job could be owned by a RayCluster. However, we don't kueue RayClusters, we queue RayJobs, so this logic might be broken
  
  Right, I think so too, and propose we investigate it as part of #1434.
