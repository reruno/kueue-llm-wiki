# PR #1436: Record events when QuotaReservation and Admition take place.

**Summary**: Record events when QuotaReservation and Admition take place.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1436

**Last updated**: 2023-12-14T17:10:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `admitted-event` → `main`
- **Created**: 2023-12-11T16:11:18Z
- **Updated**: 2023-12-14T17:10:13Z
- **Merged**: 2023-12-14T16:46:47Z
- **Closed**: 2023-12-14T16:46:47Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 7   **+171 / -14**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Record  different events when QuotaReservation and Admition take place.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1392 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Added event for QuotaReserved and fixed event for Admitted to trigger when admission checks complete
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-12-11T16:11:25Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 30fa93381a806b6e58ff83a7ed8a82b98241dbfc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/657b15e69ffee30009485801 |

### Comment by [@trasc](https://github.com/trasc) — 2023-12-11T16:12:31Z

/hold For #1428 to merge, it uses that same test event recorder.

### Comment by [@trasc](https://github.com/trasc) — 2023-12-11T17:59:40Z

/retest

### Comment by [@trasc](https://github.com/trasc) — 2023-12-13T14:47:51Z

/unhold

### Comment by [@trasc](https://github.com/trasc) — 2023-12-13T14:52:17Z

/cc @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-13T21:20:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1436#pullrequestreview-1780509235" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1436#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-14T15:03:20Z

/label tide/merge-method-squash
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-14T15:03:27Z

LGTM label has been added.  <details>Git tree hash: 14df760793c2eae53a56cd575d8dbf937ad066d1</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-14T16:35:07Z

/kind bug
/remove-kind feature
/cherry-pick release-0.5
Because the existing events say "admitted", when it actually refers to quota reservation.

/release-note-edit
```release-note
Added event for QuotaReserved and fixed event for Admitted to trigger when admission checks complete
```

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2023-12-14T16:35:10Z

@alculquicondor: once the present PR merges, I will cherry-pick it on top of release-0.5 in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1436#issuecomment-1856172515):

>/kind bug
>/remove-kind feature
>/cherry-pick release-0.5
>Because the existing events say "admitted", when it actually refers to quota reservation.
>
>/release-note-edit
>```release-note
>Added event for QuotaReserved and fixed event for Admitted to trigger when admission checks complete
>```


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-14T16:35:27Z

/retest
(due to flaky test)

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2023-12-14T16:47:27Z

@alculquicondor: #1436 failed to apply on top of branch "release-0.5":
```
Applying: Use different events for QuotaReservation and Admission
Using index info to reconstruct a base tree...
M	pkg/constants/constants.go
M	pkg/controller/core/workload_controller.go
M	pkg/scheduler/scheduler.go
M	pkg/scheduler/scheduler_test.go
M	pkg/util/testing/client.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/util/testing/client.go
CONFLICT (content): Merge conflict in pkg/util/testing/client.go
Auto-merging pkg/scheduler/scheduler_test.go
Auto-merging pkg/scheduler/scheduler.go
Auto-merging pkg/controller/core/workload_controller.go
Auto-merging pkg/constants/constants.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0001 Use different events for QuotaReservation and Admission
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1436#issuecomment-1856172515):

>/kind bug
>/remove-kind feature
>/cherry-pick release-0.5
>Because the existing events say "admitted", when it actually refers to quota reservation.
>
>/release-note-edit
>```release-note
>Added event for QuotaReserved and fixed event for Admitted to trigger when admission checks complete
>```


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-14T16:57:59Z

/reopen
@trasc please prepare a cherry-pick for release-0.5

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-14T16:58:04Z

@alculquicondor: Failed to re-open PR: state cannot be changed. The pull request cannot be reopened.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1436#issuecomment-1856209256):

>/reopen
>@trasc please prepare a cherry-pick for release-0.5


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T16:27:38Z

- **pkg/controller/core/workload_controller.go:163** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T16:27:38Z
```diff
@@ -153,7 +156,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	}
 
 	if workload.SyncAdmittedCondition(&wl) {
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true); err != nil {
+			return ctrl.Result{}, err
+		}
+		if workload.IsAdmitted(&wl) {
+			waitTime := time.Since(wl.CreationTimestamp.Time)
```

  I just realized that this is wrong sometimes. The wait time is either the CreationTimestamp or the LastTransitionTime for the WorkloadEvicted, if it exists.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T16:29:49Z

- **pkg/controller/core/workload_controller_test.go:307** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T16:29:49Z
```diff
@@ -290,3 +297,53 @@ func TestSyncCheckStates(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcile(t *testing.T) {
+	cases := map[string]struct {
+		workload   *kueue.Workload
+		wantError  error
+		wantEvents []utiltesting.EventRecord
+	}{
+		"admit": {
```

  Add another case where the workload is already admitted: there shouldn't be any event in that case.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T16:30:07Z

- **pkg/scheduler/scheduler.go:453** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T16:30:07Z
```diff
@@ -451,7 +451,10 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 		err := s.applyAdmission(ctx, newWorkload)
 		if err == nil {
 			waitTime := time.Since(e.Obj.CreationTimestamp.Time)
```

  same here

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T16:31:05Z

- **pkg/scheduler/scheduler_test.go:1110** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T16:31:05Z
```diff
@@ -1107,10 +1164,10 @@ func TestSchedule(t *testing.T) {
 
 			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
 			go qManager.CleanUpOnContext(ctx)
-			defer cancel()
```

  why change this?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-13T20:17:37Z

- **pkg/scheduler/scheduler.go:453** by [@trasc](https://github.com/trasc) — 2023-12-13T19:32:11Z
```diff
@@ -451,7 +451,10 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 		err := s.applyAdmission(ctx, newWorkload)
 		if err == nil {
 			waitTime := time.Since(e.Obj.CreationTimestamp.Time)
```

  The problem is that the evicted cond is updated when it gets the new reservation, let's add tome since reservation for"Admitted" (0s if it's done here), and from creation or eviction for  "QuotaReserved".

- **pkg/scheduler/scheduler_test.go:1110** by [@trasc](https://github.com/trasc) — 2023-12-13T19:35:50Z
```diff
@@ -1107,10 +1164,10 @@ func TestSchedule(t *testing.T) {
 
 			ctx, cancel := context.WithTimeout(ctx, queueingTimeout)
 			go qManager.CleanUpOnContext(ctx)
-			defer cancel()
```

  a field attempt to solve a race condition in utiltesting.EventRecorder :), reverted

- **pkg/controller/core/workload_controller_test.go:307** by [@trasc](https://github.com/trasc) — 2023-12-13T20:12:27Z
```diff
@@ -290,3 +297,53 @@ func TestSyncCheckStates(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcile(t *testing.T) {
+	cases := map[string]struct {
+		workload   *kueue.Workload
+		wantError  error
+		wantEvents []utiltesting.EventRecord
+	}{
+		"admit": {
```

  done

- **pkg/controller/core/workload_controller.go:163** by [@trasc](https://github.com/trasc) — 2023-12-13T20:16:38Z
```diff
@@ -153,7 +156,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	}
 
 	if workload.SyncAdmittedCondition(&wl) {
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true); err != nil {
+			return ctrl.Result{}, err
+		}
+		if workload.IsAdmitted(&wl) {
+			waitTime := time.Since(wl.CreationTimestamp.Time)
```

  The problem is that the evicted cond is updated when it gets the new reservation, let's add tome since reservation for"Admitted"

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T21:20:31Z

- **pkg/scheduler/scheduler_test.go:1106** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-13T21:20:31Z
```diff
@@ -1044,7 +1103,7 @@ func TestSchedule(t *testing.T) {
 				defer features.SetFeatureGateDuringTest(t, features.PartialAdmission, false)()
 			}
 			ctx, _ := utiltesting.ContextWithLog(t)
-			scheme := runtime.NewScheme()
+			//scheme := runtime.NewScheme()
```

  remove this line

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-13T21:20:40Z

/approve

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-14T09:19:04Z

- **pkg/scheduler/scheduler_test.go:1106** by [@trasc](https://github.com/trasc) — 2023-12-14T09:19:00Z
```diff
@@ -1044,7 +1103,7 @@ func TestSchedule(t *testing.T) {
 				defer features.SetFeatureGateDuringTest(t, features.PartialAdmission, false)()
 			}
 			ctx, _ := utiltesting.ContextWithLog(t)
-			scheme := runtime.NewScheme()
+			//scheme := runtime.NewScheme()
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-14T11:52:52Z

LGTM overall, just a couple of nits

- **pkg/controller/core/workload_controller.go:163** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:40:58Z
```diff
@@ -153,7 +156,15 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	}
 
 	if workload.SyncAdmittedCondition(&wl) {
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true); err != nil {
+			return ctrl.Result{}, err
+		}
+		if workload.IsAdmitted(&wl) {
+			if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved); c != nil {
```

  is this check necessary? Can a workload have the Admitted condition, but not QuotaReserved? Looking here the workload will not get Admitted condition if the quota is not reserved: https://github.com/kubernetes-sigs/kueue/blob/615dc8e42d160174962f379dd8767e95298a1457/pkg/workload/admissionchecks.go#L51C7-L51C23.

- **pkg/controller/core/workload_controller_test.go:323** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:45:31Z
```diff
@@ -290,3 +297,63 @@ func TestSyncCheckStates(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcile(t *testing.T) {
+	cases := map[string]struct {
+		workload   *kueue.Workload
+		wantError  error
+		wantEvents []utiltesting.EventRecord
+	}{
+		"admit": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				AdmissionCheck(kueue.AdmissionCheckState{
+					Name:  "check",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Namespace: "ns", Name: "wl"},
+					EventType: "Normal",
+					Reason:    "Admitted",
+				},
+			},
+		},
+		"admited": {
```

  ```suggestion
  		"already admitted": {
  ```
  nit

- **pkg/scheduler/scheduler.go:459** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:47:51Z
```diff
@@ -450,8 +451,15 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 	s.admissionRoutineWrapper.Run(func() {
 		err := s.applyAdmission(ctx, newWorkload)
 		if err == nil {
-			waitTime := time.Since(e.Obj.CreationTimestamp.Time)
-			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time was %.0fs", admission.ClusterQueue, waitTime.Seconds())
+			waitStarted := e.Obj.CreationTimestamp.Time
+			if c := apimeta.FindStatusCondition(e.Obj.Status.Conditions, kueue.WorkloadEvicted); c != nil {
+				waitStarted = c.LastTransitionTime.Time
+			}
+			waitTime := time.Since(waitStarted)
+			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "QuotaReserved", "Quota reserved in ClusterQueue %v, wait time was %.0fs", admission.ClusterQueue, waitTime.Seconds())
```

  ```suggestion
  			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "QuotaReserved", "Quota reserved in ClusterQueue %v, wait time since queued was %.0fs", admission.ClusterQueue, waitTime.Seconds())
  ```
  nit

- **pkg/controller/core/workload_controller.go:164** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:48:59Z
```diff
@@ -153,7 +156,15 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	}
 
 	if workload.SyncAdmittedCondition(&wl) {
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true); err != nil {
+			return ctrl.Result{}, err
+		}
+		if workload.IsAdmitted(&wl) {
+			if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved); c != nil {
+				r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time after reservation was %.0fs", wl.Status.Admission.ClusterQueue, time.Since(c.LastTransitionTime.Time).Seconds())
```

  ```suggestion
  				r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time since reservation was %.0fs", wl.Status.Admission.ClusterQueue, time.Since(c.LastTransitionTime.Time).Seconds())
  ```
  nit

- **pkg/scheduler/scheduler.go:461** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:50:02Z
```diff
@@ -450,8 +451,15 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 	s.admissionRoutineWrapper.Run(func() {
 		err := s.applyAdmission(ctx, newWorkload)
 		if err == nil {
-			waitTime := time.Since(e.Obj.CreationTimestamp.Time)
-			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time was %.0fs", admission.ClusterQueue, waitTime.Seconds())
+			waitStarted := e.Obj.CreationTimestamp.Time
+			if c := apimeta.FindStatusCondition(e.Obj.Status.Conditions, kueue.WorkloadEvicted); c != nil {
+				waitStarted = c.LastTransitionTime.Time
+			}
+			waitTime := time.Since(waitStarted)
+			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "QuotaReserved", "Quota reserved in ClusterQueue %v, wait time was %.0fs", admission.ClusterQueue, waitTime.Seconds())
+			if workload.IsAdmitted(newWorkload) {
+				s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time after reservation was 0s ", admission.ClusterQueue)
```

  ```suggestion
  				s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time since reservation was 0s", admission.ClusterQueue)
  ```
  nit

- **pkg/scheduler/scheduler_test.go:1213** by [@mimowo](https://github.com/mimowo) — 2023-12-14T11:52:30Z
```diff
@@ -1153,6 +1209,12 @@ func TestSchedule(t *testing.T) {
 			if diff := cmp.Diff(tc.wantInadmissibleLeft, qDumpInadmissible); diff != "" {
 				t.Errorf("Unexpected elements left in inadmissible workloads (-want,+got):\n%s", diff)
 			}
+
+			if len(tc.wantEvents) > 0 {
```

  Can we drop this check, and always compare? It may detect us some wrong events where we didn't expect them (so we left `wantEvents empty).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-14T14:50:02Z

- **pkg/controller/core/workload_controller.go:163** by [@trasc](https://github.com/trasc) — 2023-12-14T14:13:50Z
```diff
@@ -153,7 +156,15 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	}
 
 	if workload.SyncAdmittedCondition(&wl) {
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true); err != nil {
+			return ctrl.Result{}, err
+		}
+		if workload.IsAdmitted(&wl) {
+			if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved); c != nil {
```

  fair point, done

- **pkg/controller/core/workload_controller.go:164** by [@trasc](https://github.com/trasc) — 2023-12-14T14:13:59Z
```diff
@@ -153,7 +156,15 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	}
 
 	if workload.SyncAdmittedCondition(&wl) {
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true); err != nil {
+			return ctrl.Result{}, err
+		}
+		if workload.IsAdmitted(&wl) {
+			if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved); c != nil {
+				r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time after reservation was %.0fs", wl.Status.Admission.ClusterQueue, time.Since(c.LastTransitionTime.Time).Seconds())
```

  done

- **pkg/controller/core/workload_controller_test.go:323** by [@trasc](https://github.com/trasc) — 2023-12-14T14:15:05Z
```diff
@@ -290,3 +297,63 @@ func TestSyncCheckStates(t *testing.T) {
 		})
 	}
 }
+
+func TestReconcile(t *testing.T) {
+	cases := map[string]struct {
+		workload   *kueue.Workload
+		wantError  error
+		wantEvents []utiltesting.EventRecord
+	}{
+		"admit": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				AdmissionCheck(kueue.AdmissionCheckState{
+					Name:  "check",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Namespace: "ns", Name: "wl"},
+					EventType: "Normal",
+					Reason:    "Admitted",
+				},
+			},
+		},
+		"admited": {
```

  done

- **pkg/scheduler/scheduler.go:459** by [@trasc](https://github.com/trasc) — 2023-12-14T14:16:07Z
```diff
@@ -450,8 +451,15 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 	s.admissionRoutineWrapper.Run(func() {
 		err := s.applyAdmission(ctx, newWorkload)
 		if err == nil {
-			waitTime := time.Since(e.Obj.CreationTimestamp.Time)
-			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time was %.0fs", admission.ClusterQueue, waitTime.Seconds())
+			waitStarted := e.Obj.CreationTimestamp.Time
+			if c := apimeta.FindStatusCondition(e.Obj.Status.Conditions, kueue.WorkloadEvicted); c != nil {
+				waitStarted = c.LastTransitionTime.Time
+			}
+			waitTime := time.Since(waitStarted)
+			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "QuotaReserved", "Quota reserved in ClusterQueue %v, wait time was %.0fs", admission.ClusterQueue, waitTime.Seconds())
```

  done

- **pkg/scheduler/scheduler.go:461** by [@trasc](https://github.com/trasc) — 2023-12-14T14:16:13Z
```diff
@@ -450,8 +451,15 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 	s.admissionRoutineWrapper.Run(func() {
 		err := s.applyAdmission(ctx, newWorkload)
 		if err == nil {
-			waitTime := time.Since(e.Obj.CreationTimestamp.Time)
-			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time was %.0fs", admission.ClusterQueue, waitTime.Seconds())
+			waitStarted := e.Obj.CreationTimestamp.Time
+			if c := apimeta.FindStatusCondition(e.Obj.Status.Conditions, kueue.WorkloadEvicted); c != nil {
+				waitStarted = c.LastTransitionTime.Time
+			}
+			waitTime := time.Since(waitStarted)
+			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "QuotaReserved", "Quota reserved in ClusterQueue %v, wait time was %.0fs", admission.ClusterQueue, waitTime.Seconds())
+			if workload.IsAdmitted(newWorkload) {
+				s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time after reservation was 0s ", admission.ClusterQueue)
```

  done

- **pkg/scheduler/scheduler_test.go:1213** by [@trasc](https://github.com/trasc) — 2023-12-14T14:49:51Z
```diff
@@ -1153,6 +1209,12 @@ func TestSchedule(t *testing.T) {
 			if diff := cmp.Diff(tc.wantInadmissibleLeft, qDumpInadmissible); diff != "" {
 				t.Errorf("Unexpected elements left in inadmissible workloads (-want,+got):\n%s", diff)
 			}
+
+			if len(tc.wantEvents) > 0 {
```

  There are 19 tests and 48 events to take into account , some of them might not be in a strict order ,  and not directly related to Admission/QuotaReservation.
  
  Let's open a follow-up issue for this if necessary.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-14T14:54:23Z

- **pkg/scheduler/scheduler_test.go:1213** by [@mimowo](https://github.com/mimowo) — 2023-12-14T14:54:23Z
```diff
@@ -1153,6 +1209,12 @@ func TestSchedule(t *testing.T) {
 			if diff := cmp.Diff(tc.wantInadmissibleLeft, qDumpInadmissible); diff != "" {
 				t.Errorf("Unexpected elements left in inadmissible workloads (-want,+got):\n%s", diff)
 			}
+
+			if len(tc.wantEvents) > 0 {
```

  sgtm
