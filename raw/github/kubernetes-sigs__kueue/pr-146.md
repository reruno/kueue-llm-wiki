# PR #146: Abstract cluster queue interface

**Summary**: Abstract cluster queue interface

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/146

**Last updated**: 2022-03-26T00:29:58Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@denkensk](https://github.com/denkensk)
- **Branch**: `add-clusterqueue-interface` → `main`
- **Created**: 2022-03-24T13:15:03Z
- **Updated**: 2022-03-26T00:29:58Z
- **Merged**: 2022-03-26T00:29:58Z
- **Closed**: 2022-03-26T00:29:58Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 7   **+244 / -112**

## Description

Signed-off-by: Alex Wang <wangqingcan1990@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Make a `ClusterQueue` interface  so that each queueing strategy implements and gets invoked agnostically by queue manager. 


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-24T13:17:07Z

This is the first version of the implementation that ensures the right direction. Very simple.
Need your feedback. @ahg-g @alculquicondor

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-26T00:07:47Z

Squashed.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-26T00:28:42Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-26T00:28:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/146#issuecomment-1079533102" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/146#" title="Author self-approved">denkensk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-24T19:07:39Z

- **pkg/queue/strictfifo.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-24T19:07:35Z
```diff
@@ -0,0 +1,131 @@
+package queue
```

  will we have a completely different queue for other in-tree strategies?
  
  If not, this file could be `queue_impl.go`

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-24T20:29:43Z

- **pkg/queue/strictfifo.go:1** by [@ahg-g](https://github.com/ahg-g) — 2022-03-24T20:29:43Z
```diff
@@ -0,0 +1,131 @@
+package queue
```

  can we set this up such that we don't replicate all the functions for each strategy? C++ inheritance for example allows a base implementation that a derived class can choose to override.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-25T07:31:24Z

- **pkg/queue/strictfifo.go:1** by [@denkensk](https://github.com/denkensk) — 2022-03-25T07:31:24Z
```diff
@@ -0,0 +1,131 @@
+package queue
```

  Thanks. 
  This is indeed suitable for using inheritance and overwriting like c++. I made the `cluser_queue_impl.go` as the base implementation for ClusterQueue interface. Other strategies can inherit and overwrite it. I also add an example :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-25T13:34:28Z

looking good

- **pkg/queue/cluster_queue_interface.go:31** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-25T13:33:22Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	Update(*kueue.ClusterQueue)
```

  Can you document the expectations for each function?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-25T13:52:45Z

- **pkg/queue/cluster_queue_interface.go:44** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T13:50:01Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	Update(*kueue.ClusterQueue)
+
+	AddFromQueue(*Queue) bool
+	DeleteFromQueue(*Queue)
+
+	PushIfNotPresent(*workload.Info) bool
+	PushOrUpdate(*kueue.QueuedWorkload)
+	Delete(*kueue.QueuedWorkload)
+	Pop() *workload.Info
+
+	RequeueWorkload(*workload.Info) (bool, error)
+	AddInadmissibleIfNotPresent(*workload.Info) error
+	QueueAllInadmissibleWorkloads()
+
+	PendingWorkloadsSize() int32
```

  ```suggestion
  	Pending() int32
  ```

- **pkg/queue/cluster_queue_interface.go:49** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T13:52:18Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	Update(*kueue.ClusterQueue)
+
+	AddFromQueue(*Queue) bool
+	DeleteFromQueue(*Queue)
+
+	PushIfNotPresent(*workload.Info) bool
+	PushOrUpdate(*kueue.QueuedWorkload)
+	Delete(*kueue.QueuedWorkload)
+	Pop() *workload.Info
+
+	RequeueWorkload(*workload.Info) (bool, error)
+	AddInadmissibleIfNotPresent(*workload.Info) error
+	QueueAllInadmissibleWorkloads()
+
+	PendingWorkloadsSize() int32
+	Dump() (sets.String, bool)
+}
+
+var registry = map[kueue.QueueingStrategy]func(cq *kueue.ClusterQueue) (ClusterQueue, error){
+	StrictFIFO: newClusterQueueImpl,
```

  I would create a dedicated StrictFIFO type, so replace the "Example" one below with StrictFIFO. The base implementation should not be usable on its own.

- **pkg/queue/example.go:27** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T13:52:40Z
```diff
@@ -0,0 +1,48 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+)
+
+// ExampleClusterQueue is an example of implementing ClusterQueue by
+// inheriting and overwriting ClusterQueueImpl.
+type ExampleClusterQueue struct {
```

  make this the StrictFIFO one.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-25T16:20:24Z

- **pkg/queue/cluster_queue_interface.go:49** by [@denkensk](https://github.com/denkensk) — 2022-03-25T16:20:24Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	Update(*kueue.ClusterQueue)
+
+	AddFromQueue(*Queue) bool
+	DeleteFromQueue(*Queue)
+
+	PushIfNotPresent(*workload.Info) bool
+	PushOrUpdate(*kueue.QueuedWorkload)
+	Delete(*kueue.QueuedWorkload)
+	Pop() *workload.Info
+
+	RequeueWorkload(*workload.Info) (bool, error)
+	AddInadmissibleIfNotPresent(*workload.Info) error
+	QueueAllInadmissibleWorkloads()
+
+	PendingWorkloadsSize() int32
+	Dump() (sets.String, bool)
+}
+
+var registry = map[kueue.QueueingStrategy]func(cq *kueue.ClusterQueue) (ClusterQueue, error){
+	StrictFIFO: newClusterQueueImpl,
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-25T16:20:27Z

- **pkg/queue/example.go:27** by [@denkensk](https://github.com/denkensk) — 2022-03-25T16:20:27Z
```diff
@@ -0,0 +1,48 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+)
+
+// ExampleClusterQueue is an example of implementing ClusterQueue by
+// inheriting and overwriting ClusterQueueImpl.
+type ExampleClusterQueue struct {
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-25T16:20:47Z

- **pkg/queue/cluster_queue_interface.go:31** by [@denkensk](https://github.com/denkensk) — 2022-03-25T16:20:47Z
```diff
@@ -0,0 +1,60 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	Update(*kueue.ClusterQueue)
```

  added the document :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-25T17:36:39Z

- **pkg/queue/cluster_queue_interface.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-25T17:32:09Z
```diff
@@ -0,0 +1,80 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If one of the workloads is already present,
```

  If at least one workload is added, returns true

- **pkg/queue/cluster_queue_impl.go:99** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-25T17:33:41Z
```diff
@@ -0,0 +1,118 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"container/heap"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueueImpl is the base implementation of ClusterQueue interface.
+// It can be inherited and overwritten by other class.
+type ClusterQueueImpl struct {
+	// QueueingStrategy indicates the queueing strategy of the workloads
+	// across the queues in this ClusterQueue.
+	QueueingStrategy kueue.QueueingStrategy
+
+	heap heapImpl
+}
+
+var _ ClusterQueue = &ClusterQueueImpl{}
+
+func (c *ClusterQueueImpl) Update(apiCQ *kueue.ClusterQueue) {
+	c.QueueingStrategy = apiCQ.Spec.QueueingStrategy
+}
+
+func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
+	added := false
+	for _, w := range q.items {
+		if c.PushIfNotPresent(w) {
+			added = true
+		}
+	}
+	return added
+}
+
+func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		c.Delete(w.Obj)
+	}
+}
+
+func (c *ClusterQueueImpl) PushIfNotPresent(info *workload.Info) bool {
+	item := c.heap.items[workload.Key(info.Obj)]
+	if item != nil {
+		return false
+	}
+	heap.Push(&c.heap, *info)
+	return true
+}
+
+func (c *ClusterQueueImpl) PushOrUpdate(w *kueue.QueuedWorkload) {
+	item := c.heap.items[workload.Key(w)]
+	info := *workload.NewInfo(w)
+	if item == nil {
+		heap.Push(&c.heap, info)
+		return
+	}
+	item.obj = info
+	heap.Fix(&c.heap, item.index)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.QueuedWorkload) {
+	item := c.heap.items[workload.Key(w)]
+	if item != nil {
+		heap.Remove(&c.heap, item.index)
+	}
+}
+
+func (c *ClusterQueueImpl) Pop() *workload.Info {
+	if c.heap.Len() == 0 {
+		return nil
+	}
+	w := heap.Pop(&c.heap).(workload.Info)
+	return &w
+}
+
+func (c *ClusterQueueImpl) Pending() int32 {
+	return int32(len(c.heap.heap))
+}
+
+func (c *ClusterQueueImpl) RequeueWorkload(wInfo *workload.Info) (bool, error) {
```

  Why do we have this function instead of just `PushIfNotPresent`?
  
  I suspect it has to do with the new policies, but I better review that in the other PR, so I can see the big picture.

- **pkg/queue/cluster_queue_impl.go:103** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-25T17:35:27Z
```diff
@@ -0,0 +1,118 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"container/heap"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueueImpl is the base implementation of ClusterQueue interface.
+// It can be inherited and overwritten by other class.
+type ClusterQueueImpl struct {
+	// QueueingStrategy indicates the queueing strategy of the workloads
+	// across the queues in this ClusterQueue.
+	QueueingStrategy kueue.QueueingStrategy
+
+	heap heapImpl
+}
+
+var _ ClusterQueue = &ClusterQueueImpl{}
+
+func (c *ClusterQueueImpl) Update(apiCQ *kueue.ClusterQueue) {
+	c.QueueingStrategy = apiCQ.Spec.QueueingStrategy
+}
+
+func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
+	added := false
+	for _, w := range q.items {
+		if c.PushIfNotPresent(w) {
+			added = true
+		}
+	}
+	return added
+}
+
+func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		c.Delete(w.Obj)
+	}
+}
+
+func (c *ClusterQueueImpl) PushIfNotPresent(info *workload.Info) bool {
+	item := c.heap.items[workload.Key(info.Obj)]
+	if item != nil {
+		return false
+	}
+	heap.Push(&c.heap, *info)
+	return true
+}
+
+func (c *ClusterQueueImpl) PushOrUpdate(w *kueue.QueuedWorkload) {
+	item := c.heap.items[workload.Key(w)]
+	info := *workload.NewInfo(w)
+	if item == nil {
+		heap.Push(&c.heap, info)
+		return
+	}
+	item.obj = info
+	heap.Fix(&c.heap, item.index)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.QueuedWorkload) {
+	item := c.heap.items[workload.Key(w)]
+	if item != nil {
+		heap.Remove(&c.heap, item.index)
+	}
+}
+
+func (c *ClusterQueueImpl) Pop() *workload.Info {
+	if c.heap.Len() == 0 {
+		return nil
+	}
+	w := heap.Pop(&c.heap).(workload.Info)
+	return &w
+}
+
+func (c *ClusterQueueImpl) Pending() int32 {
+	return int32(len(c.heap.heap))
+}
+
+func (c *ClusterQueueImpl) RequeueWorkload(wInfo *workload.Info) (bool, error) {
+	return c.PushIfNotPresent(wInfo), nil
+}
+
+func (c *ClusterQueueImpl) AddInadmissibleIfNotPresent(*workload.Info) error {
```

  Also don't add this yet.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-25T18:28:22Z

- **pkg/queue/cluster_queue_interface.go:61** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T18:23:40Z
```diff
@@ -0,0 +1,80 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If one of the workloads is already present,
+	// return false. Otherwise return true.
+	AddFromQueue(*Queue) bool
+	// DeleteFromQueue removes all workloads belonging to this queue from
+	// the ClusterQueue.
+	DeleteFromQueue(*Queue)
+
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, return false. Otherwise return true.
+	PushIfNotPresent(*workload.Info) bool
+	// PushOrUpdate pushes the workload to ClusterQueue.
+	// If the workload is already present, update with the new one.
+	PushOrUpdate(*kueue.QueuedWorkload)
+	// Delete removes the workload from ClusterQueue.
+	Delete(*kueue.QueuedWorkload)
+	// Pop removes the head of the queue and returns it. It returns nil if the
+	// queue is empty.
+	Pop() *workload.Info
+
+	// RequeueWorkload pushes the workload back to ClusterQueue after scheduling failure.
+	RequeueWorkload(*workload.Info) (bool, error)
+	// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
+	// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
+	AddInadmissibleIfNotPresent(*workload.Info) error
+	// QueueAllInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+	QueueAllInadmissibleWorkloads()
+
+	// Pending returns the size of pending workloads.
```

  ```suggestion
  	// Pending returns the number of pending workloads.
  ```

- **pkg/queue/cluster_queue_interface.go:58** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T18:26:20Z
```diff
@@ -0,0 +1,80 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If one of the workloads is already present,
+	// return false. Otherwise return true.
+	AddFromQueue(*Queue) bool
+	// DeleteFromQueue removes all workloads belonging to this queue from
+	// the ClusterQueue.
+	DeleteFromQueue(*Queue)
+
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, return false. Otherwise return true.
+	PushIfNotPresent(*workload.Info) bool
+	// PushOrUpdate pushes the workload to ClusterQueue.
+	// If the workload is already present, update with the new one.
+	PushOrUpdate(*kueue.QueuedWorkload)
+	// Delete removes the workload from ClusterQueue.
+	Delete(*kueue.QueuedWorkload)
+	// Pop removes the head of the queue and returns it. It returns nil if the
+	// queue is empty.
+	Pop() *workload.Info
+
+	// RequeueWorkload pushes the workload back to ClusterQueue after scheduling failure.
+	RequeueWorkload(*workload.Info) (bool, error)
+	// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
+	// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
+	AddInadmissibleIfNotPresent(*workload.Info) error
+	// QueueAllInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+	QueueAllInadmissibleWorkloads()
+
+	// Pending returns the size of pending workloads.
+	Pending() int32
+	// Dump produces a dump of the current workloads in the heap of
+	// this ClusterQueue. It returns false if the queue is empty.
+	// Otherwise return true.
+	Dump() (sets.String, bool)
```

  For the future, I think we can use a similar interface to support the following UX experience: an admin or even user may want to see where the job is in the queue. So we may want to rename it to something that indicates more than "testing/debugging", but I am not suggesting we rename it now.

- **pkg/queue/cluster_queue_strict_fifo.go:17** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T18:28:20Z
```diff
@@ -0,0 +1,57 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
```

  are there tests for this specific implementation? or the idea is to have a single unit test file for all strategies?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-25T23:32:26Z

- **pkg/queue/cluster_queue_interface.go:58** by [@denkensk](https://github.com/denkensk) — 2022-03-25T23:32:25Z
```diff
@@ -0,0 +1,80 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If one of the workloads is already present,
+	// return false. Otherwise return true.
+	AddFromQueue(*Queue) bool
+	// DeleteFromQueue removes all workloads belonging to this queue from
+	// the ClusterQueue.
+	DeleteFromQueue(*Queue)
+
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, return false. Otherwise return true.
+	PushIfNotPresent(*workload.Info) bool
+	// PushOrUpdate pushes the workload to ClusterQueue.
+	// If the workload is already present, update with the new one.
+	PushOrUpdate(*kueue.QueuedWorkload)
+	// Delete removes the workload from ClusterQueue.
+	Delete(*kueue.QueuedWorkload)
+	// Pop removes the head of the queue and returns it. It returns nil if the
+	// queue is empty.
+	Pop() *workload.Info
+
+	// RequeueWorkload pushes the workload back to ClusterQueue after scheduling failure.
+	RequeueWorkload(*workload.Info) (bool, error)
+	// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
+	// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
+	AddInadmissibleIfNotPresent(*workload.Info) error
+	// QueueAllInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+	QueueAllInadmissibleWorkloads()
+
+	// Pending returns the size of pending workloads.
+	Pending() int32
+	// Dump produces a dump of the current workloads in the heap of
+	// this ClusterQueue. It returns false if the queue is empty.
+	// Otherwise return true.
+	Dump() (sets.String, bool)
```

  > where the job is in the queue
  
  is it the exact position in the queue, e.g. the 5th one in the queue？If so, we can return a `slice` instead of `sets` in the future?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-25T23:37:28Z

- **pkg/queue/cluster_queue_strict_fifo.go:17** by [@denkensk](https://github.com/denkensk) — 2022-03-25T23:37:28Z
```diff
@@ -0,0 +1,57 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
```

  oh. I still want to put a separate test file for every strategies.  Rename the test file.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-25T23:38:08Z

- **pkg/queue/cluster_queue_impl.go:103** by [@denkensk](https://github.com/denkensk) — 2022-03-25T23:38:08Z
```diff
@@ -0,0 +1,118 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"container/heap"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueueImpl is the base implementation of ClusterQueue interface.
+// It can be inherited and overwritten by other class.
+type ClusterQueueImpl struct {
+	// QueueingStrategy indicates the queueing strategy of the workloads
+	// across the queues in this ClusterQueue.
+	QueueingStrategy kueue.QueueingStrategy
+
+	heap heapImpl
+}
+
+var _ ClusterQueue = &ClusterQueueImpl{}
+
+func (c *ClusterQueueImpl) Update(apiCQ *kueue.ClusterQueue) {
+	c.QueueingStrategy = apiCQ.Spec.QueueingStrategy
+}
+
+func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
+	added := false
+	for _, w := range q.items {
+		if c.PushIfNotPresent(w) {
+			added = true
+		}
+	}
+	return added
+}
+
+func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		c.Delete(w.Obj)
+	}
+}
+
+func (c *ClusterQueueImpl) PushIfNotPresent(info *workload.Info) bool {
+	item := c.heap.items[workload.Key(info.Obj)]
+	if item != nil {
+		return false
+	}
+	heap.Push(&c.heap, *info)
+	return true
+}
+
+func (c *ClusterQueueImpl) PushOrUpdate(w *kueue.QueuedWorkload) {
+	item := c.heap.items[workload.Key(w)]
+	info := *workload.NewInfo(w)
+	if item == nil {
+		heap.Push(&c.heap, info)
+		return
+	}
+	item.obj = info
+	heap.Fix(&c.heap, item.index)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.QueuedWorkload) {
+	item := c.heap.items[workload.Key(w)]
+	if item != nil {
+		heap.Remove(&c.heap, item.index)
+	}
+}
+
+func (c *ClusterQueueImpl) Pop() *workload.Info {
+	if c.heap.Len() == 0 {
+		return nil
+	}
+	w := heap.Pop(&c.heap).(workload.Info)
+	return &w
+}
+
+func (c *ClusterQueueImpl) Pending() int32 {
+	return int32(len(c.heap.heap))
+}
+
+func (c *ClusterQueueImpl) RequeueWorkload(wInfo *workload.Info) (bool, error) {
+	return c.PushIfNotPresent(wInfo), nil
+}
+
+func (c *ClusterQueueImpl) AddInadmissibleIfNotPresent(*workload.Info) error {
```

  Yes. Delete it.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-25T23:38:39Z

- **pkg/queue/cluster_queue_impl.go:99** by [@denkensk](https://github.com/denkensk) — 2022-03-25T23:38:39Z
```diff
@@ -0,0 +1,118 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"container/heap"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueueImpl is the base implementation of ClusterQueue interface.
+// It can be inherited and overwritten by other class.
+type ClusterQueueImpl struct {
+	// QueueingStrategy indicates the queueing strategy of the workloads
+	// across the queues in this ClusterQueue.
+	QueueingStrategy kueue.QueueingStrategy
+
+	heap heapImpl
+}
+
+var _ ClusterQueue = &ClusterQueueImpl{}
+
+func (c *ClusterQueueImpl) Update(apiCQ *kueue.ClusterQueue) {
+	c.QueueingStrategy = apiCQ.Spec.QueueingStrategy
+}
+
+func (c *ClusterQueueImpl) AddFromQueue(q *Queue) bool {
+	added := false
+	for _, w := range q.items {
+		if c.PushIfNotPresent(w) {
+			added = true
+		}
+	}
+	return added
+}
+
+func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
+	for _, w := range q.items {
+		c.Delete(w.Obj)
+	}
+}
+
+func (c *ClusterQueueImpl) PushIfNotPresent(info *workload.Info) bool {
+	item := c.heap.items[workload.Key(info.Obj)]
+	if item != nil {
+		return false
+	}
+	heap.Push(&c.heap, *info)
+	return true
+}
+
+func (c *ClusterQueueImpl) PushOrUpdate(w *kueue.QueuedWorkload) {
+	item := c.heap.items[workload.Key(w)]
+	info := *workload.NewInfo(w)
+	if item == nil {
+		heap.Push(&c.heap, info)
+		return
+	}
+	item.obj = info
+	heap.Fix(&c.heap, item.index)
+}
+
+func (c *ClusterQueueImpl) Delete(w *kueue.QueuedWorkload) {
+	item := c.heap.items[workload.Key(w)]
+	if item != nil {
+		heap.Remove(&c.heap, item.index)
+	}
+}
+
+func (c *ClusterQueueImpl) Pop() *workload.Info {
+	if c.heap.Len() == 0 {
+		return nil
+	}
+	w := heap.Pop(&c.heap).(workload.Info)
+	return &w
+}
+
+func (c *ClusterQueueImpl) Pending() int32 {
+	return int32(len(c.heap.heap))
+}
+
+func (c *ClusterQueueImpl) RequeueWorkload(wInfo *workload.Info) (bool, error) {
```

  sg. I delete this func. Will add it in the other PR.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-25T23:38:55Z

- **pkg/queue/cluster_queue_interface.go:61** by [@denkensk](https://github.com/denkensk) — 2022-03-25T23:38:55Z
```diff
@@ -0,0 +1,80 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If one of the workloads is already present,
+	// return false. Otherwise return true.
+	AddFromQueue(*Queue) bool
+	// DeleteFromQueue removes all workloads belonging to this queue from
+	// the ClusterQueue.
+	DeleteFromQueue(*Queue)
+
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, return false. Otherwise return true.
+	PushIfNotPresent(*workload.Info) bool
+	// PushOrUpdate pushes the workload to ClusterQueue.
+	// If the workload is already present, update with the new one.
+	PushOrUpdate(*kueue.QueuedWorkload)
+	// Delete removes the workload from ClusterQueue.
+	Delete(*kueue.QueuedWorkload)
+	// Pop removes the head of the queue and returns it. It returns nil if the
+	// queue is empty.
+	Pop() *workload.Info
+
+	// RequeueWorkload pushes the workload back to ClusterQueue after scheduling failure.
+	RequeueWorkload(*workload.Info) (bool, error)
+	// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
+	// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
+	AddInadmissibleIfNotPresent(*workload.Info) error
+	// QueueAllInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+	QueueAllInadmissibleWorkloads()
+
+	// Pending returns the size of pending workloads.
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-25T23:52:22Z

- **pkg/queue/cluster_queue_interface.go:58** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T23:52:22Z
```diff
@@ -0,0 +1,80 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If one of the workloads is already present,
+	// return false. Otherwise return true.
+	AddFromQueue(*Queue) bool
+	// DeleteFromQueue removes all workloads belonging to this queue from
+	// the ClusterQueue.
+	DeleteFromQueue(*Queue)
+
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, return false. Otherwise return true.
+	PushIfNotPresent(*workload.Info) bool
+	// PushOrUpdate pushes the workload to ClusterQueue.
+	// If the workload is already present, update with the new one.
+	PushOrUpdate(*kueue.QueuedWorkload)
+	// Delete removes the workload from ClusterQueue.
+	Delete(*kueue.QueuedWorkload)
+	// Pop removes the head of the queue and returns it. It returns nil if the
+	// queue is empty.
+	Pop() *workload.Info
+
+	// RequeueWorkload pushes the workload back to ClusterQueue after scheduling failure.
+	RequeueWorkload(*workload.Info) (bool, error)
+	// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
+	// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
+	AddInadmissibleIfNotPresent(*workload.Info) error
+	// QueueAllInadmissibleWorkloads moves all workloads from inadmissibleWorkloads to heap.
+	QueueAllInadmissibleWorkloads()
+
+	// Pending returns the size of pending workloads.
+	Pending() int32
+	// Dump produces a dump of the current workloads in the heap of
+	// this ClusterQueue. It returns false if the queue is empty.
+	// Otherwise return true.
+	Dump() (sets.String, bool)
```

  something like that, yeah we will need to change to an ordered slice.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-25T23:56:11Z

you can squash

- **pkg/queue/cluster_queue_interface.go:35** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T23:53:08Z
```diff
@@ -0,0 +1,72 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If at least one workload is added, returns true.
+	// Otherwise return true.
```

  ```suggestion
  	// Otherwise returns false.
  ```

- **pkg/queue/cluster_queue_interface.go:42** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T23:53:37Z
```diff
@@ -0,0 +1,72 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If at least one workload is added, returns true.
+	// Otherwise return true.
+	AddFromQueue(*Queue) bool
+	// DeleteFromQueue removes all workloads belonging to this queue from
+	// the ClusterQueue.
+	DeleteFromQueue(*Queue)
+
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, return false. Otherwise return true.
```

  ```suggestion
  	// If the workload is already present, returns false. Otherwise returns true.
  ```

- **pkg/queue/cluster_queue_interface.go:57** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T23:55:35Z
```diff
@@ -0,0 +1,72 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If at least one workload is added, returns true.
+	// Otherwise return true.
+	AddFromQueue(*Queue) bool
+	// DeleteFromQueue removes all workloads belonging to this queue from
+	// the ClusterQueue.
+	DeleteFromQueue(*Queue)
+
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, return false. Otherwise return true.
+	PushIfNotPresent(*workload.Info) bool
+	// PushOrUpdate pushes the workload to ClusterQueue.
+	// If the workload is already present, update with the new one.
+	PushOrUpdate(*kueue.QueuedWorkload)
+	// Delete removes the workload from ClusterQueue.
+	Delete(*kueue.QueuedWorkload)
+	// Pop removes the head of the queue and returns it. It returns nil if the
+	// queue is empty.
+	Pop() *workload.Info
+
+	// Pending returns the number of pending workloads.
+	Pending() int32
+	// Dump produces a dump of the current workloads in the heap of
+	// this ClusterQueue. It returns false if the queue is empty.
+	// Otherwise return true.
```

  ```suggestion
  	// Otherwise returns true.
  ```

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-26T00:06:33Z

- **pkg/queue/cluster_queue_interface.go:57** by [@denkensk](https://github.com/denkensk) — 2022-03-26T00:06:32Z
```diff
@@ -0,0 +1,72 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If at least one workload is added, returns true.
+	// Otherwise return true.
+	AddFromQueue(*Queue) bool
+	// DeleteFromQueue removes all workloads belonging to this queue from
+	// the ClusterQueue.
+	DeleteFromQueue(*Queue)
+
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, return false. Otherwise return true.
+	PushIfNotPresent(*workload.Info) bool
+	// PushOrUpdate pushes the workload to ClusterQueue.
+	// If the workload is already present, update with the new one.
+	PushOrUpdate(*kueue.QueuedWorkload)
+	// Delete removes the workload from ClusterQueue.
+	Delete(*kueue.QueuedWorkload)
+	// Pop removes the head of the queue and returns it. It returns nil if the
+	// queue is empty.
+	Pop() *workload.Info
+
+	// Pending returns the number of pending workloads.
+	Pending() int32
+	// Dump produces a dump of the current workloads in the heap of
+	// this ClusterQueue. It returns false if the queue is empty.
+	// Otherwise return true.
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-26T00:06:36Z

- **pkg/queue/cluster_queue_interface.go:42** by [@denkensk](https://github.com/denkensk) — 2022-03-26T00:06:35Z
```diff
@@ -0,0 +1,72 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If at least one workload is added, returns true.
+	// Otherwise return true.
+	AddFromQueue(*Queue) bool
+	// DeleteFromQueue removes all workloads belonging to this queue from
+	// the ClusterQueue.
+	DeleteFromQueue(*Queue)
+
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, return false. Otherwise return true.
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-26T00:06:39Z

- **pkg/queue/cluster_queue_interface.go:35** by [@denkensk](https://github.com/denkensk) — 2022-03-26T00:06:39Z
```diff
@@ -0,0 +1,72 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"fmt"
+
+	"k8s.io/apimachinery/pkg/util/sets"
+	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// ClusterQueue is an interface for a cluster queue to store workloads waiting
+// to be scheduled.
+type ClusterQueue interface {
+	// Update updates the properties of this ClusterQueue.
+	Update(*kueue.ClusterQueue)
+
+	// AddFromQueue pushes all workloads belonging to this queue to
+	// the ClusterQueue. If at least one workload is added, returns true.
+	// Otherwise return true.
```

  Done
