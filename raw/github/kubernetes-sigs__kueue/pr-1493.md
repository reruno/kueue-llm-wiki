# PR #1493: Fix provisioning request test

**Summary**: Fix provisioning request test

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1493

**Last updated**: 2023-12-29T04:01:12Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@B1F030](https://github.com/B1F030)
- **Branch**: `test-ProvisioningRequest` → `main`
- **Created**: 2023-12-19T03:48:20Z
- **Updated**: 2023-12-29T04:01:12Z
- **Merged**: 2023-12-21T11:17:06Z
- **Closed**: 2023-12-21T11:17:06Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 1   **+40 / -16**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Fix integration test for [test/integration/controller/admissionchecks/provisioning/provisioning_test.go](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/admissionchecks/provisioning/provisioning_test.go)
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1484

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-12-19T03:48:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c14ac884e22f3a8d5f569596516d0285bc1ae5a7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/658418e3e6039600081bb7c9 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-19T03:48:30Z

Hi @B1F030. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-19T03:50:21Z

cc @kerthcet

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-12-19T03:51:36Z

/ok-to-test

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-19T05:25:24Z

cc @tenzen-y 
It should work now.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-19T10:01:08Z

@B1F030 Please squash all commits into one.

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-19T10:31:45Z

> @B1F030 Please squash all commits into one.

@tenzen-y  Done.

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T02:28:47Z

/test pull-kueue-test-integration-main

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T02:42:29Z

/retest

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T02:47:14Z

/retest

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T03:07:06Z

@tenzen-y @mimowo  Done. Thanks for review!

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T03:29:26Z

When you face a flaky test, please open a tracking issue.

Opened: #1501

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T05:37:39Z

> If you face a flaky test, please open a tracking issue.

OK, I thought that was because of github CI. Tests sometimes just fail, after retest, they turn out to be correct.
I will record it as an issue next time I meet this kind of situation.

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T06:43:07Z

Flaky: Provisioning when A workload is using a provision admission check
same as #1484

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T06:47:28Z

/retest

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T06:51:26Z

/retest

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T06:55:58Z

/retest

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T07:02:14Z

There seems to be another flakiness.

/lgtm cancel

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T07:11:28Z

```
  Not enough workloads are admitted
  Expected
      <int>: 0
  to equal
      <int>: 1
```

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T07:20:19Z

Maybe we should still use `ExpectWorkloadsToHaveQuotaReservation` instead of `ExpectWorkloadsToBeAdmitted`?

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T07:38:09Z

Cause we just set admission check and then set quota reservation to the workload, so `"Check the workload is admitted"` maybe confusing, it actually means `"Check the workload has quota reservation"`.

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-21T08:26:54Z

I think the intention of the test is to simulate that the workload is `admitted`, not just `quota reserved`. This is important for the `workload.IsAdmitted(wl)` check as pointed out here: https://github.com/kubernetes-sigs/kueue/pull/1493#discussion_r1433590685. 

I see this is failing on `Check the workload is admitted`. 

Now, I think the test is failing because there is no scheduler to transition the workload from `QuotaReservation` to `Admission`. So, we need to do this from test code, I think this is the function you are looking for: https://github.com/kubernetes-sigs/kueue/blob/395137afeaad94179db68043e5c74ab193f6beb1/test/util/util.go#L478-L489 - call it before `Check the workload is admitted`.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T08:31:36Z

> I think the intention of the test is to simulate that the workload is admitted, not just quota reserved. This is important for the workload.IsAdmitted(wl) check as pointed out here: https://github.com/kubernetes-sigs/kueue/pull/1493#discussion_r1433590685.
> 
> I see this is failing on Check the workload is admitted.

@mimowo Yes, that's right. Thank you for the clarifications.
@B1F030 The following job-controller test would be helpful:

https://github.com/kubernetes-sigs/kueue/blob/395137afeaad94179db68043e5c74ab193f6beb1/test/integration/controller/jobs/job/job_controller_test.go#L241-L253

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T08:32:55Z

/approve canel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T08:33:01Z

/approve cancel

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T08:34:49Z

In that case, `"Checking the workload remains admitted"` need to be replaced by `ExpectWorkloadsToBeAdmitted`, right?

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-21T08:48:04Z

> In that case, `"Checking the workload remains admitted"` need to be replaced by `ExpectWorkloadsToBeAdmitted`, right?

Correct.

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-21T10:40:24Z

/lgtm 
Please squash the commits :)
/assign @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-21T10:40:30Z

LGTM label has been added.  <details>Git tree hash: 82dcf8a7131fb6d77b02f187d367d8cc0cab641b</details>

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T10:47:54Z

Just to confirm in case:
```
ginkgo.By("Checking the admission check is ready", func() {
        gomega.Eventually(func(g gomega.Gomega) {
	        g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
	        state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
	        g.Expect(state).NotTo(gomega.BeNil())
	        g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
	        g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
		        {
			        Name: "ps1",
			        Annotations: map[string]string{
				        provisioning.ConsumesAnnotationKey: provReqKey.Name,
			        },
		        },
		        {
			        Name: "ps2",
			        Annotations: map[string]string{
				        provisioning.ConsumesAnnotationKey: provReqKey.Name,
			        },
		        },
	        }))
        }, util.Timeout, util.Interval).Should(gomega.Succeed())
})

...

ginkgo.By("Checking the admission check remains ready", func() {
        gomega.Eventually(func(g gomega.Gomega) {
	        g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
	        state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
	        g.Expect(state).NotTo(gomega.BeNil())
	        g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
        }, util.Timeout, util.Interval).Should(gomega.Succeed())
})
```
Is it ok?

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-21T10:49:32Z

> Is it ok?

lgtm

### Comment by [@B1F030](https://github.com/B1F030) — 2023-12-21T10:50:00Z

> lgtm

OK, thanks!

### Comment by [@mimowo](https://github.com/mimowo) — 2023-12-21T10:53:51Z

/lgtm

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T11:11:53Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-12-21T11:12:01Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1493#" title="Author self-approved">B1F030</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1493#issuecomment-1866071660" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-19T11:24:41Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:600** by [@mimowo](https://github.com/mimowo) — 2023-12-19T11:24:38Z
```diff
@@ -592,15 +597,32 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
```

  Should we add additional step "Check the workload is admitted"? If the workload isn't admitted, we will invoke 
  `syncOwnedProvisionRequest` which may recreate the PR, and thus evict the workload. See https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/admissionchecks/provisioning/controller.go#L138-L151

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-19T11:38:34Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:589** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-19T11:30:58Z
```diff
@@ -568,20 +569,24 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdRequest := &autoscaling.ProvisioningRequest{}
 			provReqKey := types.NamespacedName{
 				Namespace: wlKey.Namespace,
 				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
 			}
-
 			ginkgo.By("Checking that the provision request is created", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
 				gomega.Eventually(func() error {
 					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
 				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
```

  Can you use `gomega.Eventually(func(g gomega.Gomega) {`?

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:625** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-19T11:37:20Z
```diff
@@ -592,15 +597,32 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				createdRequest.Name, createdRequest.Namespace = provReqKey.Name, provReqKey.Namespace
```

  Instead of generating an object, should we get a PR object?

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:639** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-19T11:38:04Z
```diff
@@ -614,10 +636,11 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				gomega.Eventually(func() error {
 					request := &autoscaling.ProvisioningRequest{}
 					return k8sClient.Get(ctx, provReqKey, request)
-				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+				}, util.Timeout, util.Interval).Should(gomega.BeNil())
```

  Why do we need to use `BeNil()` instead of `BeNotFoundError`?

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:600** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-19T11:38:32Z
```diff
@@ -592,15 +597,32 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
```

  SGTM

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-19T15:51:03Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:625** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-19T15:51:03Z
```diff
@@ -592,15 +597,32 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				createdRequest.Name, createdRequest.Namespace = provReqKey.Name, provReqKey.Namespace
```

  This is better, as it avoids an API call. However, prefer using this style:
  ```
  object := &Type{
    field: value
  }
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-19T17:08:40Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:625** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-19T17:08:39Z
```diff
@@ -592,15 +597,32 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				createdRequest.Name, createdRequest.Namespace = provReqKey.Name, provReqKey.Namespace
```

  I'm ok with @alculquicondor recommendation way.

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T02:05:51Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:625** by [@B1F030](https://github.com/B1F030) — 2023-12-21T02:05:51Z
```diff
@@ -592,15 +597,32 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				createdRequest.Name, createdRequest.Namespace = provReqKey.Name, provReqKey.Namespace
```

  Done.

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T02:25:58Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:600** by [@B1F030](https://github.com/B1F030) — 2023-12-21T02:25:58Z
```diff
@@ -592,15 +597,32 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-21T05:08:03Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:590** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T05:03:58Z
```diff
@@ -568,39 +569,74 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdRequest := &autoscaling.ProvisioningRequest{}
 			provReqKey := types.NamespacedName{
 				Namespace: wlKey.Namespace,
 				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
 			}
-
 			ginkgo.By("Checking that the provision request is created", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
 				gomega.Eventually(func() error {
 					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Setting the provision request as Provisioned", func() {
-				gomega.Eventually(func() error {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) error {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).To(gomega.Succeed())
 					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
 						Type:   autoscaling.Provisioned,
 						Status: metav1.ConditionTrue,
 						Reason: autoscaling.Provisioned,
 					})
```

  ```suggestion
  				gomega.Eventually(func(g gomega.Gomega) {
  					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).To(gomega.Succeed())
  					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
  						Type:   autoscaling.Provisioned,
  						Status: metav1.ConditionTrue,
  						Reason: autoscaling.Provisioned,
  					})
  					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).To(gomega.Succeed())
  				}, util.Timeout, util.Interval).Should(gomega.Succeed())					
  ```

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:623** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T05:07:28Z
```diff
@@ -568,39 +569,74 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdRequest := &autoscaling.ProvisioningRequest{}
 			provReqKey := types.NamespacedName{
 				Namespace: wlKey.Namespace,
 				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
 			}
-
 			ginkgo.By("Checking that the provision request is created", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
 				gomega.Eventually(func() error {
 					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Setting the provision request as Provisioned", func() {
-				gomega.Eventually(func() error {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) error {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).To(gomega.Succeed())
 					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
 						Type:   autoscaling.Provisioned,
 						Status: metav1.ConditionTrue,
 						Reason: autoscaling.Provisioned,
 					})
-					return k8sClient.Status().Update(ctx, createdRequest)
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).To(gomega.Succeed())
+					return nil
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, string(updatedWl.Status.Admission.ClusterQueue), updatedWl)
+					return nil
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  ```suggestion
  				gomega.Eventually(func(g gomega.Gomega) {
  					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
  					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, string(updatedWl.Status.Admission.ClusterQueue), updatedWl)
  				}, util.Timeout, util.Interval).Should(gomega.Succeed())
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-21T06:24:02Z

Otherwise lgtm.

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:624** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T06:22:32Z
```diff
@@ -568,39 +569,69 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdRequest := &autoscaling.ProvisioningRequest{}
 			provReqKey := types.NamespacedName{
 				Namespace: wlKey.Namespace,
 				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
 			}
-
 			ginkgo.By("Checking that the provision request is created", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
 				gomega.Eventually(func() error {
 					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Setting the provision request as Provisioned", func() {
-				gomega.Eventually(func() error {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).To(gomega.Succeed())
 					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
 						Type:   autoscaling.Provisioned,
 						Status: metav1.ConditionTrue,
 						Reason: autoscaling.Provisioned,
 					})
-					return k8sClient.Status().Update(ctx, createdRequest)
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).To(gomega.Succeed())
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, string(updatedWl.Status.Admission.ClusterQueue), updatedWl)
```

  Should we use `ExpectWorkloadsToBeAdmitted` instead of `ExpectWorkloadsToHaveQuotaReservation`?
  
  https://github.com/kubernetes-sigs/kueue/blob/395137afeaad94179db68043e5c74ab193f6beb1/test/util/util.go#L268
  
  Since the provisioning controller verifies `Admission` status:
  
  https://github.com/kubernetes-sigs/kueue/blob/395137afeaad94179db68043e5c74ab193f6beb1/pkg/controller/admissionchecks/provisioning/controller.go#L138-L142
  
  https://github.com/kubernetes-sigs/kueue/blob/395137afeaad94179db68043e5c74ab193f6beb1/pkg/workload/workload.go#L433-L436

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T06:31:04Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:624** by [@B1F030](https://github.com/B1F030) — 2023-12-21T06:31:04Z
```diff
@@ -568,39 +569,69 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdRequest := &autoscaling.ProvisioningRequest{}
 			provReqKey := types.NamespacedName{
 				Namespace: wlKey.Namespace,
 				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
 			}
-
 			ginkgo.By("Checking that the provision request is created", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
 				gomega.Eventually(func() error {
 					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Setting the provision request as Provisioned", func() {
-				gomega.Eventually(func() error {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).To(gomega.Succeed())
 					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
 						Type:   autoscaling.Provisioned,
 						Status: metav1.ConditionTrue,
 						Reason: autoscaling.Provisioned,
 					})
-					return k8sClient.Status().Update(ctx, createdRequest)
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).To(gomega.Succeed())
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, string(updatedWl.Status.Admission.ClusterQueue), updatedWl)
```

  Sounds resonable to me.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-21T06:38:11Z

Thanks
/lgtm
/approve

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T08:29:56Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:624** by [@mimowo](https://github.com/mimowo) — 2023-12-21T08:29:56Z
```diff
@@ -568,39 +569,69 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdRequest := &autoscaling.ProvisioningRequest{}
 			provReqKey := types.NamespacedName{
 				Namespace: wlKey.Namespace,
 				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
 			}
-
 			ginkgo.By("Checking that the provision request is created", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
 				gomega.Eventually(func() error {
 					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Setting the provision request as Provisioned", func() {
-				gomega.Eventually(func() error {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).To(gomega.Succeed())
 					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
 						Type:   autoscaling.Provisioned,
 						Status: metav1.ConditionTrue,
 						Reason: autoscaling.Provisioned,
 					})
-					return k8sClient.Status().Update(ctx, createdRequest)
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).To(gomega.Succeed())
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Checking the admission check is ready", func() {
+				updatedWl := &kueue.Workload{}
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, string(updatedWl.Status.Admission.ClusterQueue), updatedWl)
```

  yes, I think we should use `ExpectWorkloadsToBeAdmitted` to meet the `IsAdmitted` check. 
  
  As discussed https://github.com/kubernetes-sigs/kueue/pull/1493#issuecomment-1865853179, I suspect this is problematic because there is no scheduler in the integration test setup. Please consider calling `SyncAdmittedConditionForWorkloads`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T08:50:31Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@mimowo](https://github.com/mimowo) — 2023-12-21T08:50:27Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  Can we revert this change to minimize the diff - does not seem related?

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T08:55:05Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@B1F030](https://github.com/B1F030) — 2023-12-21T08:55:05Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  Still we need to delete the provisioning request, if not set ObjectMeta, there will be an Error like:
  ```
    [FAILED] Expected success, but got an error:
        <*errors.errorString | 0xc000978620>: 
        resource name may not be empty
        {
            s: "resource name may not be empty",
        }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T08:57:14Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@mimowo](https://github.com/mimowo) — 2023-12-21T08:57:13Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  Yes, we delete the provisioning request, I was just thinking about the place where we declare the variable.
  EDIT: previously the variable was declared outside of the `By`s, I think it could stay that way, unless missing something.

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T09:00:48Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@B1F030](https://github.com/B1F030) — 2023-12-21T09:00:47Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  This Error happens both on `k8sClient.Delete(ctx, createdRequest)` and `util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T09:01:44Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:650** by [@mimowo](https://github.com/mimowo) — 2023-12-21T09:01:44Z
```diff
@@ -618,13 +647,10 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking the workload remains admitted", func() {
-				gomega.Eventually(func() error {
-					err := k8sClient.Get(ctx, wlKey, updatedWl)
-					if err != nil {
-						return err
-					}
-					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, string(updatedWl.Status.Admission.ClusterQueue), updatedWl)
-					return nil
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
```

  No need to call `SyncAdmittedConditionForWorkloads` here. This function is used to transition from `QuotaReservation` to `Admitted` by scheduler (which is not present in the setup). 
  However, here we don't expect this to be needed. One thing which could happen is that the workload_controller evicts the workload, but scheduler should not be involved.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-21T09:03:18Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T09:03:18Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  I'm ok with either way.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T09:03:34Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@mimowo](https://github.com/mimowo) — 2023-12-21T09:03:34Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  Don't you get the `createdRequest` populated at `k8sClient.Get(ctx, provReqKey, createdRequest)`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T09:05:43Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@mimowo](https://github.com/mimowo) — 2023-12-21T09:05:43Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  I mean, this used to work in the test, right? You were deleting the provisioning request before this PR. I'm just thinking the way it was setup does not need to change,
  
  EDIT: I just want to make the fix minimal, so that there is no red-herring of what the actual issue was.

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T09:07:58Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@B1F030](https://github.com/B1F030) — 2023-12-21T09:07:58Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  Got it. Thanks for your explanation.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-21T09:10:04Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:650** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T09:05:20Z
```diff
@@ -618,13 +647,10 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking the workload remains admitted", func() {
-				gomega.Eventually(func() error {
-					err := k8sClient.Get(ctx, wlKey, updatedWl)
-					if err != nil {
-						return err
-					}
-					util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, string(updatedWl.Status.Admission.ClusterQueue), updatedWl)
-					return nil
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
```

  Correct.

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T09:09:55Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  @mimowo ~~We have discussed about `k8sClient.Get(ctx, provReqKey, createdRequest)` in https://github.com/kubernetes-sigs/kueue/pull/1493#discussion_r1431289995, and then we decided not to call api-server.~~
  
  NVM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-21T09:11:19Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@tenzen-y](https://github.com/tenzen-y) — 2023-12-21T09:11:19Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  Oops. I misunderstood what you wanted to say.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T09:13:43Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:576** by [@mimowo](https://github.com/mimowo) — 2023-12-21T09:13:43Z
```diff
@@ -568,26 +568,31 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdRequest := &autoscaling.ProvisioningRequest{}
 			provReqKey := types.NamespacedName{
 				Namespace: wlKey.Namespace,
 				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
 			}
-
+			createdRequest := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
```

  Do we need to provide the `ObjectMeta` here? I think it will be populated on the `Get`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T09:16:49Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:627** by [@mimowo](https://github.com/mimowo) — 2023-12-21T09:16:49Z
```diff
@@ -597,10 +598,38 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
+				util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, updatedWl)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, updatedWl)
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Deleting the provision request", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{
```

  I think the old method had the same number of requests actually, we still call the `Get` at the same places.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T09:20:03Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:576** by [@mimowo](https://github.com/mimowo) — 2023-12-21T09:20:03Z
```diff
@@ -573,21 +573,21 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				Namespace: wlKey.Namespace,
 				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
 			}
-
```

  nit: revert

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T09:21:42Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:576** by [@B1F030](https://github.com/B1F030) — 2023-12-21T09:21:42Z
```diff
@@ -568,26 +568,31 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			createdRequest := &autoscaling.ProvisioningRequest{}
 			provReqKey := types.NamespacedName{
 				Namespace: wlKey.Namespace,
 				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
 			}
-
+			createdRequest := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
```

  Sorry I'm still greenhand on this. Really appreciate for your patience.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T09:24:25Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:618** by [@mimowo](https://github.com/mimowo) — 2023-12-21T09:24:25Z
```diff
@@ -597,6 +597,28 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
```

  I think this is the actual fix, to make sure that the workload is admitted, so that we don't go beyond this check: https://github.com/kubernetes-sigs/kueue/blob/395137afeaad94179db68043e5c74ab193f6beb1/pkg/controller/admissionchecks/provisioning/controller.go#L138. 
  
  Previously the test would be flaky, because going beyond the condition still required some extra work to evict the workload. Whereas the check in "Checking the workload remains admitted" would be fast. 
  
  Could you please verify this by using `Consistently` instead of `Eventually` inside "Checking the workload remains admitted" on the main branch? I expect you would get the failure consistently. If you confirm the case, then please use `Consistently` on the fix and confirm that it is stable now. If we can confirm this, I'm ok with either `Eventually` or `Consistently` to be actually merged. The drawback of `Consistently` is that by default it takes 3s, but maybe we can use 1s.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T09:26:48Z

LGTM, I would yet like to have confirm the understanding of why this was flaky with the experiment as in https://github.com/kubernetes-sigs/kueue/pull/1493#discussion_r1433796407

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T09:42:34Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:618** by [@B1F030](https://github.com/B1F030) — 2023-12-21T09:42:34Z
```diff
@@ -597,6 +597,28 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
```

  `Eventually` passed the test successfully. I will test `Consistently` next.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T09:47:34Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:618** by [@mimowo](https://github.com/mimowo) — 2023-12-21T09:47:34Z
```diff
@@ -597,6 +597,28 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
```

  You can also run the tests locally. I would usually do 
  1. mark the test I want to run in isolation with `ginkgo.FIt` instead of `ginkgo.It` (F for focus)
  2. run integration tests selectively for the package: `INTEGRATION_TARGET=./test/integration/controller/admissionchecks/provisioning make test-integration`

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T10:05:28Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:618** by [@B1F030](https://github.com/B1F030) — 2023-12-21T10:05:28Z
```diff
@@ -597,6 +597,28 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
```

  `Consistently` passed the test successfully too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T10:08:15Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:618** by [@mimowo](https://github.com/mimowo) — 2023-12-21T10:08:14Z
```diff
@@ -597,6 +597,28 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
 					g.Expect(state).NotTo(gomega.BeNil())
 					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Check the workload is admitted", func() {
```

  Ok. let's use `Eventually`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T10:10:48Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:638** by [@mimowo](https://github.com/mimowo) — 2023-12-21T10:10:48Z
```diff
@@ -607,24 +630,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking provision request is deleted", func() {
-				provReqKey := types.NamespacedName{
-					Namespace: wlKey.Namespace,
-					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
-				}
 				gomega.Eventually(func() error {
-					request := &autoscaling.ProvisioningRequest{}
-					return k8sClient.Get(ctx, provReqKey, request)
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
 			})
 
 			ginkgo.By("Checking the workload remains admitted", func() {
```

  I just realized this suite does not run the workload controller. In that case we would really not expect eviction. So, I think what we should test here is:
  1. that the admission check remains in the Ready state
  2. the Provisioning request is not recreated (remains deleted)
  
  Please add `ginkgo.By` step(s) checking these conditions.

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T10:21:16Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:638** by [@B1F030](https://github.com/B1F030) — 2023-12-21T10:21:16Z
```diff
@@ -607,24 +630,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking provision request is deleted", func() {
-				provReqKey := types.NamespacedName{
-					Namespace: wlKey.Namespace,
-					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
-				}
 				gomega.Eventually(func() error {
-					request := &autoscaling.ProvisioningRequest{}
-					return k8sClient.Get(ctx, provReqKey, request)
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
 			})
 
 			ginkgo.By("Checking the workload remains admitted", func() {
```

  Should I remove `"Checking the workload remains admitted"`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T10:27:26Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:638** by [@mimowo](https://github.com/mimowo) — 2023-12-21T10:27:26Z
```diff
@@ -607,24 +630,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking provision request is deleted", func() {
-				provReqKey := types.NamespacedName{
-					Namespace: wlKey.Namespace,
-					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
-				}
 				gomega.Eventually(func() error {
-					request := &autoscaling.ProvisioningRequest{}
-					return k8sClient.Get(ctx, provReqKey, request)
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
 			})
 
 			ginkgo.By("Checking the workload remains admitted", func() {
```

  Good question. I think it is ok to drop it. Maybe just add a comment next to the new one: "Checking the workload admission check remains Ready" saying "We use this as a proxy check to verify that the workload remains admitted, because the test suite does not run the workload controller".

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2023-12-21T10:29:17Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:638** by [@B1F030](https://github.com/B1F030) — 2023-12-21T10:29:17Z
```diff
@@ -607,24 +630,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking provision request is deleted", func() {
-				provReqKey := types.NamespacedName{
-					Namespace: wlKey.Namespace,
-					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
-				}
 				gomega.Eventually(func() error {
-					request := &autoscaling.ProvisioningRequest{}
-					return k8sClient.Get(ctx, provReqKey, request)
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
 			})
 
 			ginkgo.By("Checking the workload remains admitted", func() {
```

  OK, Thank you!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-12-21T10:29:25Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:638** by [@mimowo](https://github.com/mimowo) — 2023-12-21T10:29:25Z
```diff
@@ -607,24 +630,15 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 
 			ginkgo.By("Checking provision request is deleted", func() {
-				provReqKey := types.NamespacedName{
-					Namespace: wlKey.Namespace,
-					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name, 1),
-				}
 				gomega.Eventually(func() error {
-					request := &autoscaling.ProvisioningRequest{}
-					return k8sClient.Get(ctx, provReqKey, request)
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
 				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
 			})
 
 			ginkgo.By("Checking the workload remains admitted", func() {
```

  Alternatively we could consider enabling the workload controller in a new dedicated suite, but this feels overkill for this PR.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-12-21T11:11:28Z

Thanks! /approve
