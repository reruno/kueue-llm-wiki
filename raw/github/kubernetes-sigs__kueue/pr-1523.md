# PR #1523: Fix: remove workload finalizer if workload is finished

**Summary**: Fix: remove workload finalizer if workload is finished

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1523

**Last updated**: 2024-03-12T08:55:59Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@achernevskii](https://github.com/achernevskii)
- **Branch**: `fix/workload_finalizer_removal` → `main`
- **Created**: 2023-12-27T17:57:13Z
- **Updated**: 2024-03-12T08:55:59Z
- **Merged**: 2024-01-11T21:04:07Z
- **Closed**: 2024-01-11T21:04:07Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 6   **+102 / -18**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug


<!--
Add one of the following kinds:
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Workload will be finalized if it has a Finished condition and no OwnerReferences.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1450

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Remove finalizer from Workloads that are orphaned (have no owners).
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-12-27T17:57:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a5181ea9db13f44136da27f4987a01849cd56617 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65a051d464cb780008a71433 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T10:08:59Z

@achernevskii Also, can you fix the CI error?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-04T14:50:18Z

/cc

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T14:06:24Z

/assign @trasc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-11T15:30:40Z

LGTM label has been added.  <details>Git tree hash: 1f5b261905a2aafebc4e6108ac5fc65f9e5d10dc</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-11T15:30:49Z

/hold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-11T15:31:09Z

Please squash, so that cherry-picking is less cumbersome.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-11T15:55:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1523#" title="Author self-approved">achernevskii</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1523#pullrequestreview-1815872655" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1523#pullrequestreview-1815926979" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-11T15:58:02Z

/cherry-pick release-0.5

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-01-11T15:58:04Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of release-0.5 in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1523#issuecomment-1887471817):

>/cherry-pick release-0.5
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-11T15:59:25Z

It is, but the cherry-pick bot still goes by the original commits. Not sure if the individual commits could run into conflicts where the squashed commit wouldn't

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-11T16:02:52Z

> It is, but the cherry-pick bot still goes by the original commits. Not sure if the individual commits could run into conflicts where the squashed commit wouldn't

I see. I'm fine with manually squash.

### Comment by [@trasc](https://github.com/trasc) — 2024-01-11T20:42:22Z

squashed, but somehow it detected code changes due to rebase. Needs LGTM.

/unhold

### Comment by [@trasc](https://github.com/trasc) — 2024-01-11T20:58:00Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-11T20:58:07Z

LGTM label has been added.  <details>Git tree hash: 352be45fd720ee5036953f279568a014bda84ee7</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-01-11T21:04:45Z

@tenzen-y: #1523 failed to apply on top of branch "release-0.5":
```
Applying: fix: remove finalizer if workload finished
Using index info to reconstruct a base tree...
M	pkg/controller/core/workload_controller.go
M	pkg/controller/jobframework/reconciler.go
M	pkg/controller/jobs/pod/pod_controller_test.go
M	pkg/util/testing/wrappers.go
M	pkg/workload/workload.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/workload/workload.go
Auto-merging pkg/util/testing/wrappers.go
CONFLICT (content): Merge conflict in pkg/util/testing/wrappers.go
Auto-merging pkg/controller/jobs/pod/pod_controller_test.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/pod/pod_controller_test.go
Auto-merging pkg/controller/jobframework/reconciler.go
CONFLICT (content): Merge conflict in pkg/controller/jobframework/reconciler.go
Auto-merging pkg/controller/core/workload_controller.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0001 fix: remove finalizer if workload finished
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1523#issuecomment-1887471817):

>/cherry-pick release-0.5
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-11T21:53:38Z

@tenzen-y do you think it's that important to cherry-pick this?

It's a bit of an edge case.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-12T16:41:30Z

> @tenzen-y do you think it's that important to cherry-pick this?
> 
> It's a bit of an edge case.

@alculquicondor I think that cherry-picking might be better since we have a chance to release a patch version (v0.5.2).
If we don't have any plan to release a patch version, I'm ok with either way.

Are you concerned about manyconflicts?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T16:56:09Z

@trasc can you try to get a manual cherry-pick?

### Comment by [@trasc](https://github.com/trasc) — 2024-01-15T13:01:31Z

> @trasc can you try to get a manual cherry-pick?

done #1583

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T16:11:04Z

/release-note-edit

```release-note
Fix a bug in the pod integration that the pod's finalizer won't be removed even though the pod is finished
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-18T19:16:07Z

/release-note-edit
```release-note
Remove finalizer from Workloads that are orphaned (have no owners).
```

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-04T10:08:39Z

- **pkg/controller/core/workload_controller.go:137** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T09:45:17Z
```diff
@@ -133,8 +134,8 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) && len(wl.ObjectMeta.OwnerReferences) == 0 {
```

  Why do we need to check if the workload has ownerReference?
  Will Argo remove ownerReference from the workload?

- **pkg/controller/core/workload_controller_test.go:377** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T09:55:03Z
```diff
@@ -320,20 +344,42 @@ func TestReconcile(t *testing.T) {
 				},
 			},
 		},
-		"already admited": {
-			workload: utiltesting.MakeWorkload("wl", "ns").
+		"already admitted": {
+			workload: *utiltesting.MakeWorkload("wl", "ns").
 				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
 				Admitted(true).
 				AdmissionCheck(kueue.AdmissionCheckState{
 					Name:  "check",
 					State: kueue.CheckStateReady,
 				}).
 				Obj(),
+			wantWorkload: *utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				AdmissionCheck(kueue.AdmissionCheckState{
+					Name:  "check",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+		},
+		"remove finalizer for finished workload": {
+			workload: *utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				Obj(),
+			wantWorkload: *utiltesting.MakeWorkload("unit-test", "ns").
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				Obj(),
 		},
```

  Should we add a case for the workload with ownerReference?

- **pkg/controller/jobs/pod/pod_controller_test.go:1004** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T10:07:25Z
```diff
@@ -990,8 +990,21 @@ func TestReconciler(t *testing.T) {
 					Admitted(true).
 					Obj(),
 			},
-			wantWorkloads:   []kueue.Workload{},
-			workloadCmpOpts: defaultWorkloadCmpOpts,
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet("b990493b", 2).Request(corev1.ResourceCPU, "1").Obj()).
+					Queue("test-queue").
+					ReserveQuota(
+						utiltesting.MakeAdmission("cq").
+							Assignment(corev1.ResourceCPU, "unit-test-flavor", "2").
+							AssignmentPodCount(2).
+							Obj(),
+					).
+					Admitted(true).
+					Delete().
```

  Should we verify in https://github.com/kubernetes-sigs/kueue/blob/6242c0ff8ab90887424805321d2275ff14a6765d/pkg/controller/jobs/pod/pod_controller_test.go#L2153 if the workload has `deletionTimestamp` instead of adding such a deletionTimestamp?

- **pkg/util/workload/workload.go:12** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T10:08:29Z
```diff
@@ -0,0 +1,17 @@
+package workload
+
+import (
+	"context"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller/controllerutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+func RemoveWorkloadFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) error {
```

  Should we move this function to the `workload` (pkg/workload) package?

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2024-01-05T15:18:53Z

- **pkg/controller/core/workload_controller.go:137** by [@achernevskii](https://github.com/achernevskii) — 2024-01-05T15:18:53Z
```diff
@@ -133,8 +134,8 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) && len(wl.ObjectMeta.OwnerReferences) == 0 {
```

  If workload has an owner reference, there's a possibility that the owner job should be stopped:
  https://github.com/kubernetes-sigs/kueue/blob/bb69906b810c13c4c49d1386d4c2341f713738e3/pkg/controller/jobframework/reconciler.go#L257
  We will introduce a race condition between the workload and job controller if we don't check for the owner references here.
  
  And the workload is finalized here for the uncommon scenario, when the workload has no owner references, so the reconcile for respective job could not be queued.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-05T15:20:36Z

- **pkg/controller/core/workload_controller.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-05T15:20:36Z
```diff
@@ -133,8 +134,8 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) && len(wl.ObjectMeta.OwnerReferences) == 0 {
```

  I think it should look more like this:
  
  ```
  if len(owners) == 0 && deletionTimestamp != nil {
    finalize()
  }
  if finished {
    return
  }
  ```
  
  Note that workloads might not have owners in two cases:
  - When using prebuilt workload #1358
  - When using orphan cascade deletion https://kubernetes.io/docs/tasks/administer-cluster/use-cascading-deletion/#set-orphan-deletion-policy

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-05T17:34:30Z

- **pkg/controller/core/workload_controller.go:137** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-05T17:34:30Z
```diff
@@ -133,8 +134,8 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) && len(wl.ObjectMeta.OwnerReferences) == 0 {
```

  @achernevskii Thank you for the clarifications!
  As @alculquicondor mentioned, there are some positive situations when the workload doesn't have 
  ownerReference. So, I think that we should apply the Aldo's suggestion.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2024-01-09T02:38:07Z

- **pkg/util/workload/workload.go:12** by [@achernevskii](https://github.com/achernevskii) — 2024-01-09T02:38:07Z
```diff
@@ -0,0 +1,17 @@
+package workload
+
+import (
+	"context"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller/controllerutil"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+func RemoveWorkloadFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) error {
```

  Done in f0690dcf

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2024-01-09T02:38:29Z

- **pkg/controller/core/workload_controller.go:137** by [@achernevskii](https://github.com/achernevskii) — 2024-01-09T02:38:29Z
```diff
@@ -133,8 +134,8 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		return ctrl.Result{}, nil
+	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) && len(wl.ObjectMeta.OwnerReferences) == 0 {
```

  Changed the behavior in f0690dcf

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-09T09:52:24Z

- **pkg/controller/core/workload_controller_test.go:377** by [@trasc](https://github.com/trasc) — 2024-01-09T09:52:20Z
```diff
@@ -320,20 +344,42 @@ func TestReconcile(t *testing.T) {
 				},
 			},
 		},
-		"already admited": {
-			workload: utiltesting.MakeWorkload("wl", "ns").
+		"already admitted": {
+			workload: *utiltesting.MakeWorkload("wl", "ns").
 				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
 				Admitted(true).
 				AdmissionCheck(kueue.AdmissionCheckState{
 					Name:  "check",
 					State: kueue.CheckStateReady,
 				}).
 				Obj(),
+			wantWorkload: *utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				AdmissionCheck(kueue.AdmissionCheckState{
+					Name:  "check",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+		},
+		"remove finalizer for finished workload": {
+			workload: *utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				Obj(),
+			wantWorkload: *utiltesting.MakeWorkload("unit-test", "ns").
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				Obj(),
 		},
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-10T17:07:45Z

Overall lgtm

- **pkg/controller/core/workload_controller_test.go:392** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-10T17:05:28Z
```diff
@@ -329,6 +355,42 @@ func TestReconcile(t *testing.T) {
 					State: kueue.CheckStateReady,
 				}).
 				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				AdmissionCheck(kueue.AdmissionCheckState{
+					Name:  "check",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+		},
+		"remove finalizer for finished workload": {
+			workload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				Delete().
+				Obj(),
+			wantWorkload: nil,
+		},
+		"don't remove finalizer for owned finished workload": {
+			workload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				OwnerReference(batchv1.SchemeGroupVersion.String(), "Job", "job", "test-uid", true, true).
+				Delete().
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				OwnerReference(batchv1.SchemeGroupVersion.String(), "Job", "job", "test-uid", true, true).
+				Delete().
```

  Comparing the `deletionTimestamp` would cause a flaky test?
  So, can we verify if the workload has the `deletionTImestamp` in Line 427 instead of comparison?

- **pkg/util/testing/wrappers.go:230** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-10T17:06:15Z
```diff
@@ -226,6 +226,13 @@ func (w *WorkloadWrapper) OwnerReference(apiVersion, kind, name, uid string, con
 	return w
 }
 
+// Delete sets a deletion timestamp for the pod object
+func (w *WorkloadWrapper) Delete() *WorkloadWrapper {
```

  ```suggestion
  // DeletionTimestamp sets a deletion timestamp for the pod object
  func (w *WorkloadWrapper) DeletionTimestamp() *WorkloadWrapper {
  ```

- **pkg/util/testing/wrappers.go:232** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-10T17:07:16Z
```diff
@@ -226,6 +226,13 @@ func (w *WorkloadWrapper) OwnerReference(apiVersion, kind, name, uid string, con
 	return w
 }
 
+// Delete sets a deletion timestamp for the pod object
+func (w *WorkloadWrapper) Delete() *WorkloadWrapper {
+	t := metav1.NewTime(time.Now()).Rfc3339Copy()
+	w.Workload.DeletionTimestamp = &t
```

  ```suggestion
  	w.Workload.DeletionTimestamp := ptr.To(metav1.NewTime(time.Now()).Rfc3339Copy())
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-10T19:00:59Z

- **pkg/util/testing/wrappers.go:232** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T18:52:32Z
```diff
@@ -226,6 +226,13 @@ func (w *WorkloadWrapper) OwnerReference(apiVersion, kind, name, uid string, con
 	return w
 }
 
+// Delete sets a deletion timestamp for the pod object
+func (w *WorkloadWrapper) Delete() *WorkloadWrapper {
+	t := metav1.NewTime(time.Now()).Rfc3339Copy()
+	w.Workload.DeletionTimestamp = &t
```

  What is this `Rfc3339Copy` doing? truncating to second?

- **pkg/controller/jobframework/reconciler.go:272** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T18:55:17Z
```diff
@@ -256,13 +255,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		log.V(2).Info("The workload is marked for deletion")
 		err := r.stopJob(ctx, job, wl, StopReasonWorkloadDeleted, "Workload is deleted")
 		if err != nil {
-			log.Error(err, "Suspending job with deleted workload")
-		}
-
-		if err == nil && wl != nil {
-			err = r.removeFinalizer(ctx, wl)
```

  why change this? how will this workload be finalized?

- **pkg/controller/core/workload_controller_test.go:392** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T18:56:13Z
```diff
@@ -329,6 +355,42 @@ func TestReconcile(t *testing.T) {
 					State: kueue.CheckStateReady,
 				}).
 				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				AdmissionCheck(kueue.AdmissionCheckState{
+					Name:  "check",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+		},
+		"remove finalizer for finished workload": {
+			workload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				Delete().
+				Obj(),
+			wantWorkload: nil,
+		},
+		"don't remove finalizer for owned finished workload": {
+			workload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				OwnerReference(batchv1.SchemeGroupVersion.String(), "Job", "job", "test-uid", true, true).
+				Delete().
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				OwnerReference(batchv1.SchemeGroupVersion.String(), "Job", "job", "test-uid", true, true).
+				Delete().
```

  Or you can use EquateApproxTime? or a AcyclicTransformer that only considers whether the values are nil.

### Review by [@achernevskii](https://github.com/achernevskii) (commented) — 2024-01-10T22:29:02Z

- **pkg/util/testing/wrappers.go:232** by [@achernevskii](https://github.com/achernevskii) — 2024-01-10T22:29:02Z
```diff
@@ -226,6 +226,13 @@ func (w *WorkloadWrapper) OwnerReference(apiVersion, kind, name, uid string, con
 	return w
 }
 
+// Delete sets a deletion timestamp for the pod object
+func (w *WorkloadWrapper) Delete() *WorkloadWrapper {
+	t := metav1.NewTime(time.Now()).Rfc3339Copy()
+	w.Workload.DeletionTimestamp = &t
```

  > What is this `Rfc3339Copy` doing? truncating to second?
  
  Correct, had to add it because of https://github.com/kubernetes/kubernetes/issues/81026

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-10T22:59:25Z

- **pkg/controller/core/workload_controller_test.go:392** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-10T22:59:25Z
```diff
@@ -329,6 +355,42 @@ func TestReconcile(t *testing.T) {
 					State: kueue.CheckStateReady,
 				}).
 				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				AdmissionCheck(kueue.AdmissionCheckState{
+					Name:  "check",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+		},
+		"remove finalizer for finished workload": {
+			workload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				Delete().
+				Obj(),
+			wantWorkload: nil,
+		},
+		"don't remove finalizer for owned finished workload": {
+			workload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				OwnerReference(batchv1.SchemeGroupVersion.String(), "Job", "job", "test-uid", true, true).
+				Delete().
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				OwnerReference(batchv1.SchemeGroupVersion.String(), "Job", "job", "test-uid", true, true).
+				Delete().
```

  We can probably use `EquateApproxTime`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-11T14:37:55Z

- **pkg/util/testing/wrappers.go:230** by [@trasc](https://github.com/trasc) — 2024-01-11T14:10:51Z
```diff
@@ -226,6 +226,13 @@ func (w *WorkloadWrapper) OwnerReference(apiVersion, kind, name, uid string, con
 	return w
 }
 
+// Delete sets a deletion timestamp for the pod object
+func (w *WorkloadWrapper) Delete() *WorkloadWrapper {
```

  done

- **pkg/controller/core/workload_controller_test.go:392** by [@trasc](https://github.com/trasc) — 2024-01-11T14:11:32Z
```diff
@@ -329,6 +355,42 @@ func TestReconcile(t *testing.T) {
 					State: kueue.CheckStateReady,
 				}).
 				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				AdmissionCheck(kueue.AdmissionCheckState{
+					Name:  "check",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+		},
+		"remove finalizer for finished workload": {
+			workload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				Delete().
+				Obj(),
+			wantWorkload: nil,
+		},
+		"don't remove finalizer for owned finished workload": {
+			workload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				OwnerReference(batchv1.SchemeGroupVersion.String(), "Job", "job", "test-uid", true, true).
+				Delete().
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("unit-test", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				Condition(metav1.Condition{
+					Type:   "Finished",
+					Status: "True",
+				}).
+				OwnerReference(batchv1.SchemeGroupVersion.String(), "Job", "job", "test-uid", true, true).
+				Delete().
```

  let's just provide the desired time to the wrapper

- **pkg/controller/jobframework/reconciler.go:272** by [@trasc](https://github.com/trasc) — 2024-01-11T14:20:24Z
```diff
@@ -256,13 +255,12 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		log.V(2).Info("The workload is marked for deletion")
 		err := r.stopJob(ctx, job, wl, StopReasonWorkloadDeleted, "Workload is deleted")
 		if err != nil {
-			log.Error(err, "Suspending job with deleted workload")
-		}
-
-		if err == nil && wl != nil {
-			err = r.removeFinalizer(ctx, wl)
```

  reverted

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-11T15:30:34Z

/lgtm
/approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-11T15:55:33Z

/lgtm
/approve

> Please squash, so that cherry-picking is less cumbersome.


@alculquicondor Isn't the default merge method squash?

https://github.com/kubernetes/test-infra/blob/c96b01283ef705e2a3c1f0b836b8ffeef0223c13/config/prow/config.yaml#L783
