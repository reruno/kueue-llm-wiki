# PR #1531: KEP-79: Hierarchical Cohorts

**Summary**: KEP-79: Hierarchical Cohorts

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1531

**Last updated**: 2024-02-09T16:00:55Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mwielgus](https://github.com/mwielgus)
- **Branch**: `kep-hc` → `main`
- **Created**: 2023-12-29T18:10:03Z
- **Updated**: 2024-02-09T16:00:55Z
- **Merged**: 2024-02-09T16:00:52Z
- **Closed**: 2024-02-09T16:00:52Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 2   **+282 / -0**

## Description

#### What type of PR is this?
/kind feature
/kind api-change

#### What this PR does / why we need it:
Introduces a hierarchy among Cohorts via a new API object.

#### Which issue(s) this PR fixes:
Part of #79

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-12-29T18:10:10Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cc462ad88a8639590a26b41bd5cc36e77e7dc7da |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65c647017c856c000808f180 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-02T14:58:42Z

/cc

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-02T14:58:58Z

cc @KunWuLuan

### Comment by [@zmlcc](https://github.com/zmlcc) — 2024-01-08T06:29:51Z

What's the relationship between this kep and #1093 ?  Hierarchical Cohorts or Hierarchical ClusterQueue is better ？

### Comment by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T13:42:03Z

@zmlcc This KEP is heavily influenced by #1093, and tries to address the problems of Hierarchical ClusterQueues like:
* 2 flavors of hierarchy - ClusterQueue parent-child relation and Cohort membership.
* a bit ambiguous semantics.

I think that with Hierarchical Cohorts we don't have these problems and it should be our way forward. But I'm more than open to discussion and hearing arguments agains it.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-09T15:44:39Z

Merging this as provisional.

We can have follow ups as we start preparing for the 0.7 release.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-09T15:44:44Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-09T15:44:50Z

LGTM label has been added.  <details>Git tree hash: 8b3b24a9acf78045bb2ba583c20cd87bc4f16f32</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-09T15:44:52Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1531#issuecomment-1936157843" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1531#" title="Author self-approved">mwielgus</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T09:51:42Z

- **keps/79-hierarchical-cohorts/README.md:31** by [@mimowo](https://github.com/mimowo) — 2024-01-03T09:09:49Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
```

  ```suggestion
  enough to handle complex use cases of large organizations with tree-like
  ```

- **keps/79-hierarchical-cohorts/README.md:51** by [@mimowo](https://github.com/mimowo) — 2024-01-03T09:11:37Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
```

  ```suggestion
  Introduce a new object called Cohort with the similar quota provisioning mechanism
  ```

- **keps/79-hierarchical-cohorts/README.md:64** by [@mimowo](https://github.com/mimowo) — 2024-01-03T09:13:16Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have a owning ClusterQueue.
```

  ```suggestion
    have an owning ClusterQueue.
  ```

- **keps/79-hierarchical-cohorts/README.md:96** by [@mimowo](https://github.com/mimowo) — 2024-01-03T09:15:39Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have a owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require mor computation/resources.
```

  ```suggestion
  * Scheduling and preemption may require more computation/resources.
  ```

- **keps/79-hierarchical-cohorts/kep.yaml:28** by [@mimowo](https://github.com/mimowo) — 2024-01-03T09:36:58Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.7"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+disable-supported: true
```

  Add the feature gate name used to disable the feature if needed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-03T09:56:33Z

- **keps/79-hierarchical-cohorts/README.md:31** by [@mwielgus](https://github.com/mwielgus) — 2024-01-03T09:56:33Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-03T09:56:56Z

- **keps/79-hierarchical-cohorts/README.md:51** by [@mwielgus](https://github.com/mwielgus) — 2024-01-03T09:56:56Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-03T09:57:23Z

- **keps/79-hierarchical-cohorts/README.md:64** by [@mwielgus](https://github.com/mwielgus) — 2024-01-03T09:57:23Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have a owning ClusterQueue.
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-03T09:58:54Z

- **keps/79-hierarchical-cohorts/kep.yaml:28** by [@mwielgus](https://github.com/mwielgus) — 2024-01-03T09:58:54Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.7"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+disable-supported: true
```

  Flipped to false. Api changes should probably not be feature gated.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-03T09:59:56Z

- **keps/79-hierarchical-cohorts/README.md:96** by [@mwielgus](https://github.com/mwielgus) — 2024-01-03T09:59:56Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have a owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require mor computation/resources.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T10:14:32Z

- **keps/79-hierarchical-cohorts/README.md:117** by [@mimowo](https://github.com/mimowo) — 2024-01-03T10:14:28Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
```

  How does the Cohort API specify the parent cohort? I suppose we need another field in spec.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-03T10:19:51Z

- **keps/79-hierarchical-cohorts/README.md:117** by [@mwielgus](https://github.com/mwielgus) — 2024-01-03T10:19:51Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
```

  Good catch, somehow got lost during copy-paste. Added.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T10:28:11Z

- **keps/79-hierarchical-cohorts/README.md:117** by [@mimowo](https://github.com/mimowo) — 2024-01-03T10:28:11Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"resourceGroups,omitempty"`
```

  ```suggestion
      Parent *string `json:"parent,omitempty"`
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T10:40:29Z

- **keps/79-hierarchical-cohorts/README.md:102** by [@mimowo](https://github.com/mimowo) — 2024-01-03T10:37:47Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
```

  If we have `ClusterQueue.preemption.reclaimWithinCohort=Any`, does it mean preemption is allowed within the first level of cohort, or the whole hierarchy?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-03T10:49:56Z

- **keps/79-hierarchical-cohorts/README.md:117** by [@mwielgus](https://github.com/mwielgus) — 2024-01-03T10:49:55Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"resourceGroups,omitempty"`
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T12:43:51Z

- **keps/79-hierarchical-cohorts/README.md:192** by [@mimowo](https://github.com/mimowo) — 2024-01-03T12:43:50Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+While admitting the workload Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define 2 functions: T(x,r) and D(x,r), that take either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+* `T(x, r)` - returns the amount of resource r that is provided at the level of x from ClusterQueues
+and Cohorts that are either x or children of x (possibly indirect). In other words, how much of resource r can come from 
+the subtree. The value may be negative, what means that the subtree is borrowing from the outside of the subtree (the rest 
+of the hierarchy)
+
+* `D(x,r)` - returns the amount of resource r that is provided at the level of x from outside of the subtree. The value may
+be negative, what means that the rest of the hierarchy borrows from the subtree. 
+
+
+Obviously, with the correct admission process, for any x and r, $$T(x,r) + D(x,r)  \geq 0$$
+Otherwise there would be an unbalanced debt at level x. If there is no debt at any level then the admission is correct.
+
+New workload can be admitted to a ClusterQueue if the above conditions stays true at all elements of the hierarchy.
+Checking it only for the ClusterQueue may not be enough as changes related to borrowing may hit limits elsewhere.
+
+`T(x,r)` and `D(x,r)` can be relatively easily calculated while traversing the Cohort tree.
+
+* `T(x,r)` when x is a ClusterQueue:
+$$T(x,r) = quota(x,r) - usage(x,r)$$
```

  I'm not sure if these formulas are "definitions" or "corollaries" of some other assumptions. Please make it clear is the relationship between the formulas and the statement: "T(x,r) + D(x,r)  >= 0".

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-03T14:59:30Z

- **keps/79-hierarchical-cohorts/README.md:102** by [@mwielgus](https://github.com/mwielgus) — 2024-01-03T14:59:30Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
```

  Whole hierarchy.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-03T15:00:23Z

- **keps/79-hierarchical-cohorts/README.md:192** by [@mwielgus](https://github.com/mwielgus) — 2024-01-03T15:00:22Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+While admitting the workload Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define 2 functions: T(x,r) and D(x,r), that take either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+* `T(x, r)` - returns the amount of resource r that is provided at the level of x from ClusterQueues
+and Cohorts that are either x or children of x (possibly indirect). In other words, how much of resource r can come from 
+the subtree. The value may be negative, what means that the subtree is borrowing from the outside of the subtree (the rest 
+of the hierarchy)
+
+* `D(x,r)` - returns the amount of resource r that is provided at the level of x from outside of the subtree. The value may
+be negative, what means that the rest of the hierarchy borrows from the subtree. 
+
+
+Obviously, with the correct admission process, for any x and r, $$T(x,r) + D(x,r)  \geq 0$$
+Otherwise there would be an unbalanced debt at level x. If there is no debt at any level then the admission is correct.
+
+New workload can be admitted to a ClusterQueue if the above conditions stays true at all elements of the hierarchy.
+Checking it only for the ClusterQueue may not be enough as changes related to borrowing may hit limits elsewhere.
+
+`T(x,r)` and `D(x,r)` can be relatively easily calculated while traversing the Cohort tree.
+
+* `T(x,r)` when x is a ClusterQueue:
+$$T(x,r) = quota(x,r) - usage(x,r)$$
```

  After some consideration i came to conclusion that we can get rid of D(x,r) in favor of just borrowingLimit(x,r). I pivoted the description there a bit as well. PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-03T15:02:54Z

- **keps/79-hierarchical-cohorts/README.md:102** by [@mimowo](https://github.com/mimowo) — 2024-01-03T15:02:54Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
```

  I would suggest adding such a note on preemption in KEP.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-03T21:24:21Z

- **keps/79-hierarchical-cohorts/README.md:31** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-02T20:49:15Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
```

  ```suggestion
  enough to handle complex use cases of large organizations with tree-like
  ```

- **keps/79-hierarchical-cohorts/README.md:44** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-02T20:50:07Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
```

  ```suggestion
  * Introduce an alternate API to ClusterQueue.
  ```
  
  You are adding a new API: Cohort :)

- **keps/79-hierarchical-cohorts/README.md:51** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-02T20:51:28Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
```

  ```suggestion
  Introduce a new object called Cohort with a similar quota provisioning mechanism
  ```

- **keps/79-hierarchical-cohorts/README.md:63** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-02T20:53:05Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
```

  ```suggestion
  * Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
  ```

- **keps/79-hierarchical-cohorts/README.md:90** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-03T21:04:43Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
```

  these 2 are Notes/caveats, not Risks.

- **keps/79-hierarchical-cohorts/README.md:100** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-03T21:05:56Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
```

  Any mitigation? Or just a heads up in the documentation?

- **keps/79-hierarchical-cohorts/README.md:134** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-03T21:09:04Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
```

  FYI @kerthcet

- **keps/79-hierarchical-cohorts/README.md:170** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-03T21:15:25Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+
+Currently, with 2-level hierarchy for each Cohort and ClusterQueue, Kueue
+makes sure that the following balances are kept:
+
+* ClusterQueues don't use more resources than they have and could possibly borrow.
+* Within a Cohort, the total amount of requested capacity doesn't exceed the total quota
+from all ClusterQueues, constrained by LendingLimit.
+
+Admission of a new workload can happen if both balances are kept after 
+adding the new workload. Kueue doesn't track who is borrowing/lending from who. It
+is enough that balances are kept and with good balances, there exists such borrower-lending 
+mapping that fulfills all needs. 
+
+With Hierarchical Cohorts, Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define a function `T(x,r)` that takes either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+`T(x, r)` returns the amount of resource r that is provided at the level of x from ClusterQueues
```

  ```suggestion
  `T(x, r)` returns the amount of resource r that is available at the level of x from ClusterQueues
  ```

- **keps/79-hierarchical-cohorts/README.md:191** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-03T21:21:15Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+
+Currently, with 2-level hierarchy for each Cohort and ClusterQueue, Kueue
+makes sure that the following balances are kept:
+
+* ClusterQueues don't use more resources than they have and could possibly borrow.
+* Within a Cohort, the total amount of requested capacity doesn't exceed the total quota
+from all ClusterQueues, constrained by LendingLimit.
+
+Admission of a new workload can happen if both balances are kept after 
+adding the new workload. Kueue doesn't track who is borrowing/lending from who. It
+is enough that balances are kept and with good balances, there exists such borrower-lending 
+mapping that fulfills all needs. 
+
+With Hierarchical Cohorts, Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define a function `T(x,r)` that takes either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+`T(x, r)` returns the amount of resource r that is provided at the level of x from ClusterQueues
+and Cohorts that are either x or children of x (possibly indirect). In other words, how much of resource r can come from 
+the subtree. The value may be negative, what means that the subtree is borrowing from the outside of the subtree (the rest 
+of the hierarchy)
+
+`T(x,r)` can be relatively easily calculated while traversing the Cohort tree.
+
+* `T(x,r)` when x is a ClusterQueue:
+$$T(x,r) = quota(x,r) - usage(x,r)$$
+
+* `T(x,r)` when x is a Cohort:
+$$T(x,r) = quota(x,r) + \sum_{c \in children(x)} min(lendingLimit(c,r), T(c,r))$$
+
+Obviously, with the correct admission process, for any x and r, `T(x,r)  >=  -borrowingLimit(x,r)`
+Otherwise there would be too big debt at level x - some subtree is requesting more than allowed.
+
+Slightly less obvious, but also true is: **If there is no too big debt at any level then the admission is correct**.
+
+Negative `T(x,r)` presents the total amount of resources that a subtree needs. Positive `T(x,r)` presents the total 
```

  ```suggestion
  Negative `T(x,r)` presents the total amount of resources that a subtree is borrowing. Positive `T(x,r)` presents the total 
  ```

- **keps/79-hierarchical-cohorts/README.md:73** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-03T21:23:46Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
```

  worth putting some yaml snippets to gather feedback.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-03T21:24:52Z

- **keps/79-hierarchical-cohorts/README.md:226** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-03T21:24:52Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+
+Currently, with 2-level hierarchy for each Cohort and ClusterQueue, Kueue
+makes sure that the following balances are kept:
+
+* ClusterQueues don't use more resources than they have and could possibly borrow.
+* Within a Cohort, the total amount of requested capacity doesn't exceed the total quota
+from all ClusterQueues, constrained by LendingLimit.
+
+Admission of a new workload can happen if both balances are kept after 
+adding the new workload. Kueue doesn't track who is borrowing/lending from who. It
+is enough that balances are kept and with good balances, there exists such borrower-lending 
+mapping that fulfills all needs. 
+
+With Hierarchical Cohorts, Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define a function `T(x,r)` that takes either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+`T(x, r)` returns the amount of resource r that is provided at the level of x from ClusterQueues
+and Cohorts that are either x or children of x (possibly indirect). In other words, how much of resource r can come from 
+the subtree. The value may be negative, what means that the subtree is borrowing from the outside of the subtree (the rest 
+of the hierarchy)
+
+`T(x,r)` can be relatively easily calculated while traversing the Cohort tree.
+
+* `T(x,r)` when x is a ClusterQueue:
+$$T(x,r) = quota(x,r) - usage(x,r)$$
+
+* `T(x,r)` when x is a Cohort:
+$$T(x,r) = quota(x,r) + \sum_{c \in children(x)} min(lendingLimit(c,r), T(c,r))$$
+
+Obviously, with the correct admission process, for any x and r, `T(x,r)  >=  -borrowingLimit(x,r)`
+Otherwise there would be too big debt at level x - some subtree is requesting more than allowed.
+
+Slightly less obvious, but also true is: **If there is no too big debt at any level then the admission is correct**.
+
+Negative `T(x,r)` presents the total amount of resources that a subtree needs. Positive `T(x,r)` presents the total 
+amount of resources that a subtree can deliver (with respect of the `lendingLimit`). At the very top of hierarchy `T(x,r) >=0` 
+(`borrowingLimit` is there 0 since there is no-one to borrow from). `T(x,r)>=0` can occur also within the hierarchy.
+
+`T(x,r)>=0` means that at the level of x, the negative balance of all subtrees can be evened-out by
+other subtrees that have some extra capacity, with respect to their lendingLimit. Extra capacity can be 
+"passed" to the needing subtrees. Then, after this passing, the previously negative subtree becomes positive, and 
+we can re-apply the logic there. All negative sub-sub-trees can be balanced out by positive subtrees and the capacity
+that coming from "above". And so on and so on, up to reaching individual ClusterQueues.
+
+So a new workload can be admitted to a ClusterQueue if and only if, after admission, `T(x,r)  >=  -borrowingLimit(x,r)` 
+stays true at all elements of the hierarchy.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+As the hierarchical cohorts reside entirely inside Kueue, most of the 
+tests will be done as unit and integration tests, checking things like:
+
+* Existing functionality at 2-levels.
+* Long-distance borrowing on multi-level hierarchy.
+* Lending/borrowing limits placed on many levels.
+* Preemptions across hierarchy.
+
+### Graduation Criteria
+
+This is an a core API element and will graduate 
+
+
+## Implementation History
+
+* 2023.12.29 - Initial KEP 
```

  ```suggestion
  * 2023.12.29 - KEP: API and semantics
  ```

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T11:15:59Z

- **keps/79-hierarchical-cohorts/README.md:102** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T11:15:59Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
```

  Added to proposal section.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T11:16:33Z

- **keps/79-hierarchical-cohorts/README.md:31** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T11:16:33Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T11:16:47Z

- **keps/79-hierarchical-cohorts/README.md:51** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T11:16:47Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T11:17:18Z

- **keps/79-hierarchical-cohorts/README.md:63** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T11:17:18Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the simila quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T11:17:48Z

- **keps/79-hierarchical-cohorts/README.md:100** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T11:17:48Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
```

  Just heads up.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T11:23:30Z

- **keps/79-hierarchical-cohorts/README.md:44** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T11:23:29Z
```diff
@@ -0,0 +1,215 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use ceases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T11:24:34Z

- **keps/79-hierarchical-cohorts/README.md:90** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T11:24:34Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
```

  Removed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T11:25:47Z

- **keps/79-hierarchical-cohorts/README.md:191** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T11:25:47Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+
+Currently, with 2-level hierarchy for each Cohort and ClusterQueue, Kueue
+makes sure that the following balances are kept:
+
+* ClusterQueues don't use more resources than they have and could possibly borrow.
+* Within a Cohort, the total amount of requested capacity doesn't exceed the total quota
+from all ClusterQueues, constrained by LendingLimit.
+
+Admission of a new workload can happen if both balances are kept after 
+adding the new workload. Kueue doesn't track who is borrowing/lending from who. It
+is enough that balances are kept and with good balances, there exists such borrower-lending 
+mapping that fulfills all needs. 
+
+With Hierarchical Cohorts, Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define a function `T(x,r)` that takes either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+`T(x, r)` returns the amount of resource r that is provided at the level of x from ClusterQueues
+and Cohorts that are either x or children of x (possibly indirect). In other words, how much of resource r can come from 
+the subtree. The value may be negative, what means that the subtree is borrowing from the outside of the subtree (the rest 
+of the hierarchy)
+
+`T(x,r)` can be relatively easily calculated while traversing the Cohort tree.
+
+* `T(x,r)` when x is a ClusterQueue:
+$$T(x,r) = quota(x,r) - usage(x,r)$$
+
+* `T(x,r)` when x is a Cohort:
+$$T(x,r) = quota(x,r) + \sum_{c \in children(x)} min(lendingLimit(c,r), T(c,r))$$
+
+Obviously, with the correct admission process, for any x and r, `T(x,r)  >=  -borrowingLimit(x,r)`
+Otherwise there would be too big debt at level x - some subtree is requesting more than allowed.
+
+Slightly less obvious, but also true is: **If there is no too big debt at any level then the admission is correct**.
+
+Negative `T(x,r)` presents the total amount of resources that a subtree needs. Positive `T(x,r)` presents the total 
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-04T12:15:10Z

- **keps/79-hierarchical-cohorts/README.md:47** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T10:22:14Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
```

  This feature will be tracked by another issue? Or we will work on this in the second iteration?

- **keps/79-hierarchical-cohorts/README.md:52** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T10:23:11Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
```

  ```suggestion
  as ClusterQueue. Cohorts additionally may specify its parent, another Cohort,
  ```

- **keps/79-hierarchical-cohorts/README.md:57** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T11:38:48Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
```

  Should we create a Cohort resource specified at the ClusterQueue by cohort-controller, not to imply such understanding?
  
  It would be useful for cluster admin.

- **keps/79-hierarchical-cohorts/README.md:59** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T11:39:16Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
```

  ```suggestion
  The difference between ClusterQueue and Cohort will be that:
  ```

- **keps/79-hierarchical-cohorts/README.md:141** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T11:49:06Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
```

  I couldn't find the typed `ResourceGroup`. Did you forget to push any commits?

- **keps/79-hierarchical-cohorts/README.md:160** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T11:50:53Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
```

  What types does this condition have?

- **keps/79-hierarchical-cohorts/README.md:100** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T11:53:59Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
```

  Should we restrict the tree level to avoid increasing the computing duration too much?

- **keps/79-hierarchical-cohorts/README.md:132** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T12:14:10Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
```

  ```suggestion
      // a parent (top of the hierarchy) the BorrowingLimit has to be 0.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-04T12:16:44Z

- **keps/79-hierarchical-cohorts/README.md:141** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-04T12:16:44Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
```

  Ah, this means existing `ResourceGroup`, right?
  
  https://github.com/kubernetes-sigs/kueue/blob/1579d75e5a35bdb4ab24c64b2da4e4effa98bbc8/apis/kueue/v1beta1/clusterqueue_types.go#L137

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T16:21:47Z

- **keps/79-hierarchical-cohorts/README.md:170** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T16:21:47Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+
+Currently, with 2-level hierarchy for each Cohort and ClusterQueue, Kueue
+makes sure that the following balances are kept:
+
+* ClusterQueues don't use more resources than they have and could possibly borrow.
+* Within a Cohort, the total amount of requested capacity doesn't exceed the total quota
+from all ClusterQueues, constrained by LendingLimit.
+
+Admission of a new workload can happen if both balances are kept after 
+adding the new workload. Kueue doesn't track who is borrowing/lending from who. It
+is enough that balances are kept and with good balances, there exists such borrower-lending 
+mapping that fulfills all needs. 
+
+With Hierarchical Cohorts, Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define a function `T(x,r)` that takes either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+`T(x, r)` returns the amount of resource r that is provided at the level of x from ClusterQueues
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-04T17:57:56Z

- **keps/79-hierarchical-cohorts/README.md:192** by [@mimowo](https://github.com/mimowo) — 2024-01-04T17:57:56Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+While admitting the workload Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define 2 functions: T(x,r) and D(x,r), that take either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+* `T(x, r)` - returns the amount of resource r that is provided at the level of x from ClusterQueues
+and Cohorts that are either x or children of x (possibly indirect). In other words, how much of resource r can come from 
+the subtree. The value may be negative, what means that the subtree is borrowing from the outside of the subtree (the rest 
+of the hierarchy)
+
+* `D(x,r)` - returns the amount of resource r that is provided at the level of x from outside of the subtree. The value may
+be negative, what means that the rest of the hierarchy borrows from the subtree. 
+
+
+Obviously, with the correct admission process, for any x and r, $$T(x,r) + D(x,r)  \geq 0$$
+Otherwise there would be an unbalanced debt at level x. If there is no debt at any level then the admission is correct.
+
+New workload can be admitted to a ClusterQueue if the above conditions stays true at all elements of the hierarchy.
+Checking it only for the ClusterQueue may not be enough as changes related to borrowing may hit limits elsewhere.
+
+`T(x,r)` and `D(x,r)` can be relatively easily calculated while traversing the Cohort tree.
+
+* `T(x,r)` when x is a ClusterQueue:
+$$T(x,r) = quota(x,r) - usage(x,r)$$
```

  Thanks, it reads much better now. I see that the formulas are definitions, and in this section we argue that verifying the condition `T(x,r) + borrowingLimit(x,r) >= 0` at all levels is enough to check the correctness of the admission.
  
  One little puzzling thing is that the definition of `D(x,r)` was different than `borrowingLimit(x,r)`, so it seems the condition actually changed during the re-write. I assume the current version is correct. I will open another dedicated comment if I have some doubts, but I consider the "structural" comment addressed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-04T18:31:27Z

- **keps/79-hierarchical-cohorts/README.md:192** by [@mwielgus](https://github.com/mwielgus) — 2024-01-04T18:31:26Z
```diff
@@ -0,0 +1,220 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+While admitting the workload Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define 2 functions: T(x,r) and D(x,r), that take either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+* `T(x, r)` - returns the amount of resource r that is provided at the level of x from ClusterQueues
+and Cohorts that are either x or children of x (possibly indirect). In other words, how much of resource r can come from 
+the subtree. The value may be negative, what means that the subtree is borrowing from the outside of the subtree (the rest 
+of the hierarchy)
+
+* `D(x,r)` - returns the amount of resource r that is provided at the level of x from outside of the subtree. The value may
+be negative, what means that the rest of the hierarchy borrows from the subtree. 
+
+
+Obviously, with the correct admission process, for any x and r, $$T(x,r) + D(x,r)  \geq 0$$
+Otherwise there would be an unbalanced debt at level x. If there is no debt at any level then the admission is correct.
+
+New workload can be admitted to a ClusterQueue if the above conditions stays true at all elements of the hierarchy.
+Checking it only for the ClusterQueue may not be enough as changes related to borrowing may hit limits elsewhere.
+
+`T(x,r)` and `D(x,r)` can be relatively easily calculated while traversing the Cohort tree.
+
+* `T(x,r)` when x is a ClusterQueue:
+$$T(x,r) = quota(x,r) - usage(x,r)$$
```

  `D(x,r)` was in fact borrowing limit trimmed to what the rest of the tree can provide. However, we check the provisioning at the level above, so there is no need for extra trimming. It doesn't provide any more information.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T10:52:50Z

- **keps/79-hierarchical-cohorts/README.md:73** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T10:52:50Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
```

  Added descriptions how the proposal handles user stories. Yamls would be unnecessarily long.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T10:53:00Z

- **keps/79-hierarchical-cohorts/README.md:226** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T10:53:00Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may not create a Cohort object for Cohorts mentioned in ClusterQueues - this is fine,
+we will assume that the Cohort doesn't have any quota or other constraints/settings. 
+
+* Users may provide non-existent parent information for a Cohort - this is fine, 
+we will simply assume that the parent has no quota or other constraints/settings.
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    // Additional stats may be added in the future, like the number 
+    // of admitted workloads, their usage etc, based on the user feedback.
+}
+```
+
+Currently, with 2-level hierarchy for each Cohort and ClusterQueue, Kueue
+makes sure that the following balances are kept:
+
+* ClusterQueues don't use more resources than they have and could possibly borrow.
+* Within a Cohort, the total amount of requested capacity doesn't exceed the total quota
+from all ClusterQueues, constrained by LendingLimit.
+
+Admission of a new workload can happen if both balances are kept after 
+adding the new workload. Kueue doesn't track who is borrowing/lending from who. It
+is enough that balances are kept and with good balances, there exists such borrower-lending 
+mapping that fulfills all needs. 
+
+With Hierarchical Cohorts, Kueue will be checking the whole Cohort subtree whether the
+correct balances are kept. To be more precise what it means
+let's define a function `T(x,r)` that takes either ClusterQueue x 
+or Cohort x and resource r (from a specific resource flavor). 
+
+`T(x, r)` returns the amount of resource r that is provided at the level of x from ClusterQueues
+and Cohorts that are either x or children of x (possibly indirect). In other words, how much of resource r can come from 
+the subtree. The value may be negative, what means that the subtree is borrowing from the outside of the subtree (the rest 
+of the hierarchy)
+
+`T(x,r)` can be relatively easily calculated while traversing the Cohort tree.
+
+* `T(x,r)` when x is a ClusterQueue:
+$$T(x,r) = quota(x,r) - usage(x,r)$$
+
+* `T(x,r)` when x is a Cohort:
+$$T(x,r) = quota(x,r) + \sum_{c \in children(x)} min(lendingLimit(c,r), T(c,r))$$
+
+Obviously, with the correct admission process, for any x and r, `T(x,r)  >=  -borrowingLimit(x,r)`
+Otherwise there would be too big debt at level x - some subtree is requesting more than allowed.
+
+Slightly less obvious, but also true is: **If there is no too big debt at any level then the admission is correct**.
+
+Negative `T(x,r)` presents the total amount of resources that a subtree needs. Positive `T(x,r)` presents the total 
+amount of resources that a subtree can deliver (with respect of the `lendingLimit`). At the very top of hierarchy `T(x,r) >=0` 
+(`borrowingLimit` is there 0 since there is no-one to borrow from). `T(x,r)>=0` can occur also within the hierarchy.
+
+`T(x,r)>=0` means that at the level of x, the negative balance of all subtrees can be evened-out by
+other subtrees that have some extra capacity, with respect to their lendingLimit. Extra capacity can be 
+"passed" to the needing subtrees. Then, after this passing, the previously negative subtree becomes positive, and 
+we can re-apply the logic there. All negative sub-sub-trees can be balanced out by positive subtrees and the capacity
+that coming from "above". And so on and so on, up to reaching individual ClusterQueues.
+
+So a new workload can be admitted to a ClusterQueue if and only if, after admission, `T(x,r)  >=  -borrowingLimit(x,r)` 
+stays true at all elements of the hierarchy.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+As the hierarchical cohorts reside entirely inside Kueue, most of the 
+tests will be done as unit and integration tests, checking things like:
+
+* Existing functionality at 2-levels.
+* Long-distance borrowing on multi-level hierarchy.
+* Lending/borrowing limits placed on many levels.
+* Preemptions across hierarchy.
+
+### Graduation Criteria
+
+This is an a core API element and will graduate 
+
+
+## Implementation History
+
+* 2023.12.29 - Initial KEP 
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T10:54:05Z

- **keps/79-hierarchical-cohorts/README.md:47** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T10:54:05Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
```

  We have some ideas for new preemption modes, but they are not formalized into an issue yet.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T10:55:02Z

- **keps/79-hierarchical-cohorts/README.md:52** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T10:55:02Z
```diff
@@ -0,0 +1,238 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce a new, separate API.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T10:55:25Z

- **keps/79-hierarchical-cohorts/README.md:59** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T10:55:25Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T10:57:33Z

- **keps/79-hierarchical-cohorts/README.md:57** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T10:57:33Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
```

  I'm rather against automatic object creation.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T10:58:03Z

- **keps/79-hierarchical-cohorts/README.md:141** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T10:58:03Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
```

  yes, existing ResourceGroup.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2024-01-08T12:41:13Z

- **keps/79-hierarchical-cohorts/README.md:152** by [@KunWuLuan](https://github.com/KunWuLuan) — 2024-01-08T12:41:12Z
```diff
@@ -0,0 +1,242 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Cohorts additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQueue and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+With this proposal, research org's top Cohort will simply set borrowingLimit to 0. Alternatively, production
+org's top Cohort can set lendingLimit to 0. BorrowingLimitis inside production org's ClusterQueues should
+be generous enough to allow borrowing from the research org.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+With this proposal, the cohorts for organizations will set borrowingLimit to 0. Top level Cohort will 
+contain all of these Cohorts, plus the "special" ClusterQueue, with borrowingLimit set to infinity. 
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
```

  Do we need a filed to show the resource borrowed from others?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T13:23:11Z

- **keps/79-hierarchical-cohorts/README.md:100** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T13:23:11Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
```

  I kind-of trust in the common sense of the user. If they create 100 cohorts in a tree structure, so be it. We don't put limits on the number of pods in K8S cluster, or jobs or anything like that.
  
  This is just a note that, with the complex structure, scheduling and preemption will be a bit more expensive. Still, i suspect that the impact will be marginal and the biggest bottleneck will be etcd communication anyway.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T13:27:24Z

- **keps/79-hierarchical-cohorts/README.md:132** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T13:27:24Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T13:27:45Z

- **keps/79-hierarchical-cohorts/README.md:160** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T13:27:45Z
```diff
@@ -0,0 +1,235 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Chorots additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQuota and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
+    // conditions hold the latest available observations of the Conditions
+    // current state.
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
```

  Added.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-08T13:31:52Z

- **keps/79-hierarchical-cohorts/README.md:152** by [@mwielgus](https://github.com/mwielgus) — 2024-01-08T13:31:52Z
```diff
@@ -0,0 +1,242 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Cohorts additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQueue and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+With this proposal, research org's top Cohort will simply set borrowingLimit to 0. Alternatively, production
+org's top Cohort can set lendingLimit to 0. BorrowingLimitis inside production org's ClusterQueues should
+be generous enough to allow borrowing from the research org.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+With this proposal, the cohorts for organizations will set borrowingLimit to 0. Top level Cohort will 
+contain all of these Cohorts, plus the "special" ClusterQueue, with borrowingLimit set to infinity. 
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
```

  This may be problematic as with multi-level hierarchy, a single admission may trigger status updates on many levels, increasing the need for etcd write throughput, which remains one of the biggest Kueue bottlenecks. 
  
  I would consider doing the detailed stats for Cohorts via dedicated api, something similar to: https://github.com/kubernetes-sigs/kueue/blob/main/apis/visibility/v1alpha1/types.go

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2024-01-17T10:01:58Z

- **keps/79-hierarchical-cohorts/README.md:39** by [@KunWuLuan](https://github.com/KunWuLuan) — 2024-01-17T10:01:58Z
```diff
@@ -0,0 +1,246 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
```

  do we have any component to check if the quota tree is valid?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-01-17T10:13:10Z

- **keps/79-hierarchical-cohorts/README.md:39** by [@mwielgus](https://github.com/mwielgus) — 2024-01-17T10:13:10Z
```diff
@@ -0,0 +1,246 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
```

  What do you mean by that? Any non-negative quota is, in theory, valid...

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2024-01-17T11:08:11Z

- **keps/79-hierarchical-cohorts/README.md:39** by [@KunWuLuan](https://github.com/KunWuLuan) — 2024-01-17T11:08:11Z
```diff
@@ -0,0 +1,246 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
```

  For example, if the parent is exist

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-08T20:06:56Z

I think we can merge this as `provisional`.

- **keps/79-hierarchical-cohorts/kep.yaml:5** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-08T19:59:28Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
```

  ```suggestion
  status: provisional
  ```

- **keps/79-hierarchical-cohorts/kep.yaml:19** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-08T20:00:00Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
```

  ```suggestion
  latest-milestone: "v0.8"
  ```
  
  maybe add a comment that it could be added earlier, if time permits

- **keps/79-hierarchical-cohorts/kep.yaml:23** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-08T20:00:08Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.7"
```

  same here.

- **keps/79-hierarchical-cohorts/kep.yaml:27** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-08T20:01:15Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.7"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+disable-supported: false
```

  would it be reasonable to have a feature gate that, when disabled, the cohort objects are ignored? Meaning that only the deepest level of the hierarchy is meaningful.

- **keps/79-hierarchical-cohorts/README.md:39** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-08T20:03:11Z
```diff
@@ -0,0 +1,246 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
```

  That's an implementation detail that is discussed in the risks section.

- **keps/79-hierarchical-cohorts/README.md:152** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-08T20:05:49Z
```diff
@@ -0,0 +1,242 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Cohorts additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQueue and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+With this proposal, research org's top Cohort will simply set borrowingLimit to 0. Alternatively, production
+org's top Cohort can set lendingLimit to 0. BorrowingLimitis inside production org's ClusterQueues should
+be generous enough to allow borrowing from the research org.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+With this proposal, the cohorts for organizations will set borrowingLimit to 0. Top level Cohort will 
+contain all of these Cohorts, plus the "special" ClusterQueue, with borrowingLimit set to infinity. 
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
```

  Please leave a comment about this in the doc.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-02-09T15:14:44Z

- **keps/79-hierarchical-cohorts/kep.yaml:5** by [@mwielgus](https://github.com/mwielgus) — 2024-02-09T15:14:44Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-02-09T15:15:09Z

- **keps/79-hierarchical-cohorts/kep.yaml:19** by [@mwielgus](https://github.com/mwielgus) — 2024-02-09T15:15:09Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-02-09T15:15:17Z

- **keps/79-hierarchical-cohorts/kep.yaml:23** by [@mwielgus](https://github.com/mwielgus) — 2024-02-09T15:15:17Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.7"
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-02-09T15:15:39Z

- **keps/79-hierarchical-cohorts/kep.yaml:27** by [@mwielgus](https://github.com/mwielgus) — 2024-02-09T15:15:38Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.7"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+disable-supported: false
```

  Yes, lets feature-gate it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-09T15:33:46Z

- **keps/79-hierarchical-cohorts/kep.yaml:14** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-09T15:33:46Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
```

  Let's target alpha for 0.7

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-02-09T15:36:50Z

- **keps/79-hierarchical-cohorts/kep.yaml:14** by [@mwielgus](https://github.com/mwielgus) — 2024-02-09T15:36:50Z
```diff
@@ -0,0 +1,32 @@
+title: 
+kep-number: 79
+authors:
+  - "@mwielgus"
+status: draft
+creation-date: 2023-12-28
+reviewers:
+  - "@tenzen-y "
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-02-09T15:38:43Z

- **keps/79-hierarchical-cohorts/README.md:152** by [@mwielgus](https://github.com/mwielgus) — 2024-02-09T15:38:43Z
```diff
@@ -0,0 +1,242 @@
+# KEP-79: Hierarchical Cohorts
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Introduce Cohort top-level object to allow setting multi-level quota 
+hierarchy, with advanced borrowing, lending mechanisms.
+
+## Motivation
+
+The current 2-level hierarchy (ClusterQueues and Cohorts) is not expressive
+enough to handle complex use cases of large organizations with tree-like
+team and quota/budget structures.
+
+### Goals
+
+* Create a multi-level hierarchy for advanced quota management. 
+* Be compatible with the existing ClusterQueue API and mechanics.
+* Allow setting constraints about borrowing and lending at all levels.
+* Provide quota for groups of Queues.
+
+### Non-Goals
+
+* Change the existing API and mechanics in a not backward-compatible way.
+* Introduce an alternative API to ClusterQueue.
+* Introduce new ways of fair sharing, like ratio-based sharing (at least 
+not in this KEP).
+* Introduce additional preemption models (this will be in a separate KEP).
+
+## Proposal
+
+Introduce a new object called Cohort with the similar quota provisioning mechanism
+as ClusterQueue. Cohorts additionally may specify its parent, another Cohort,
+forming together a tree-like organization structure. ClusterQueues will still be able to specify
+the Cohort they belong to. The Cohort mentioned by ClusterQueue doesn't require
+an actual object to be present. If such is not provided, it is understood 
+that the Cohort doesn't provide any quota, has no parent, doesn't belong to any bigger
+structure or has any non-default settings.
+
+The difference between ClusterQueue and Cohort will be that:
+
+* ClusterQueues are leaves in the organization tree, Cohorts are inner nodes. 
+* Cohort doesn't accept any workloads.
+* Nominal quota provided at the Cohort level is to be shared with the entire organization and doesn't 
+  have an owning ClusterQueue.
+* Borrowing limit specified at the Cohort level means that the entire subtree cannot borrow more 
+  from the rest of the organization tree than the given value.
+* Lending limit specified at the Cohort levels means that the rest of the organization tree
+  cannot borrow more from the subtree than the given value.
+
+Preemmptions and resoruce reclamation will happen among the whole cohort structure,
+in the similar fashion as they are executed now.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I have two multi-team organizations in the company. One that does research and one that runs production
+workloads. Both are given some quota that is further distributed among the subteams. I want to grant 
+the production workloads the ability to borrow research quota if needed, but not the other way round.
+
+With this proposal, research org's top Cohort will simply set borrowingLimit to 0. Alternatively, production
+org's top Cohort can set lendingLimit to 0. BorrowingLimitis inside production org's ClusterQueues should
+be generous enough to allow borrowing from the research org.
+
+#### Story 2
+
+I have a couple organizations that have dedicated resources. The organizations should not borrow 
+from each other, however I want to have an additional "special" queue, with low priority jobs, that can 
+borrow unused capacity from any of the organizations.
+
+With this proposal, the cohorts for organizations will set borrowingLimit to 0. Top level Cohort will 
+contain all of these Cohorts, plus the "special" ClusterQueue, with borrowingLimit set to infinity. 
+
+### Risks and Mitigations
+
+* Users may create a cycle in the Cohort hierarchy - Kueue will stop all new admissions within
+the entire tree. The already admitted workloads will be allowed to continue. Appropriate 
+ClusterQueue/Cohort Status Conditions will be set and Events emited.
+
+* Scheduling and preemption may require more computation/resources.
+
+## Design Details
+
+The Cohort API will initially start only with the basic functionality. Additional policies
+regarding sharing Cohort resources can be added later.
+
+```go
+
+type Cohort struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Spec   CohortSpec   `json:"spec,omitempty"`
+    Status CohortStatus `json:"status,omitempty"`
+}
+
+type CohortSpec struct {
+    // Cohort parent name. The parent Cohort object doesn't have to exist.
+    // In such case, it is assumed that parent simply doesn't have any
+    // quota and limits and doesn't have any other custom settings.
+    Parent *string `json:"parent,omitempty"`
+
+    // resourceGroups describes groups of resources that the Cohort can
+    // share with ClusterQueues within the same group of Cohorts/ClusterQueues.
+    // Each resource group defines the list of resources and a list of flavors
+    // that provide quotas for these resources.
+    // Each resource and each flavor can only form part of one resource group.
+    // resourceGroups can be up to 16.
+    //
+    // BorrowingLimit specifies how much ClusterQueues under this Cohort can borrow
+    // from ClusterQueues/Cohorts that are NOT under this Cohort. For Cohorts without
+    // a parent(top of the hierarchy) the BorrowingLimit has to be 0.
+    //
+    // LendingLimit specifies how much ClusterQueues that are NOT under this Cohort
+    // can borrow from the ClusterQueues/Cohorts that are under this Cohort.
+    // If any of the Limits is not specified it means that there is no limit
+    // and ClusterQueues can borrow/lend as much as they want/have.
+    // 
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    ResourceGroups []ResourceGroup `json:"resourceGroups,omitempty"`
+}
+
+
+type CohortStatus struct {
```

  Done.
