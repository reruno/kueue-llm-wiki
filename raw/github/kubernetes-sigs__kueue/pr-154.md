# PR #154: Do ClusterQueue updates after QW is updated in cache

**Summary**: Do ClusterQueue updates after QW is updated in cache

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/154

**Last updated**: 2022-03-28T14:39:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `fix-sync-race` → `main`
- **Created**: 2022-03-25T19:56:57Z
- **Updated**: 2022-03-28T14:39:24Z
- **Merged**: 2022-03-28T14:39:23Z
- **Closed**: 2022-03-28T14:39:24Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 8   **+152 / -129**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

Introduce and expose a Source so that a different controller can push events into the ClusterQueue controller.

This allows the QueuedWorkload controller to trigger CQ updates only after it has updated the cache, preventing race conditions.

#### Which issue(s) this PR fixes:

Fixes #101 

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-25T19:57:10Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/154#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-25T19:58:23Z

Finally got some time to properly fix #101 :smiley: 

I still need to fix the Queue controller, but you can already have a look :)

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-25T19:59:07Z

cc @ahg-g @ArangoGutierrez

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-28T14:17:23Z

/label tide/merge-method-squash

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-28T14:37:17Z

/lgtm

## Reviews

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-25T20:19:25Z

Looking good, just 2 comments

- **pkg/controller/core/core.go:27** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-25T20:14:23Z
```diff
@@ -0,0 +1,42 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+// SetupControllers sets up the core controllers. It returns the name of the
+// controller that failed to create and an error, if any.
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache) (string, error) {
```

  Now that we are doing this, what about making them share a recorder 
  `mgr.GetEventRecorderFor())` 
  would make it easier to set events

- **pkg/controller/core/queuedworkload_controller.go:144** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-25T20:19:14Z
```diff
@@ -134,6 +141,7 @@ func (r *QueuedWorkloadReconciler) Delete(e event.DeleteEvent) bool {
 	if wl.Spec.Admission == nil {
 		r.queues.DeleteWorkload(wl)
 	}
+	r.notifyWatchers(wl)
```

  I am a bit confused here, why calling it before `return` if it has been deferred in line 107 already

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-25T20:24:07Z

- **pkg/constants/constants.go:32** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-25T20:23:13Z
```diff
@@ -29,7 +29,7 @@ const (
 
 	// UpdatesBatchPeriod is the batch period to hold queuedworkload updates
 	// before syncing a Queue and CLusterQueue onbjects.
-	UpdatesBatchPeriod = 3 * time.Second
+	UpdatesBatchPeriod = time.Second
```

  👍🏽

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-25T20:28:21Z

- **pkg/controller/core/core.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-25T20:26:59Z
```diff
@@ -0,0 +1,42 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+// SetupControllers sets up the core controllers. It returns the name of the
+// controller that failed to create and an error, if any.
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache) (string, error) {
```

  Only the job controller and the scheduler have event recorders, and they are outside of this package :)
  
  Although we could have another package that instantiates the queue manager, the cache and the scheduler.
  
  I think we should always keep the job controller separate.

- **pkg/controller/core/queuedworkload_controller.go:144** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-25T20:28:14Z
```diff
@@ -134,6 +141,7 @@ func (r *QueuedWorkloadReconciler) Delete(e event.DeleteEvent) bool {
 	if wl.Spec.Admission == nil {
 		r.queues.DeleteWorkload(wl)
 	}
+	r.notifyWatchers(wl)
```

  Different functions :)

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-25T21:02:03Z

- **pkg/controller/core/queuedworkload_controller.go:144** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-25T21:02:03Z
```diff
@@ -134,6 +141,7 @@ func (r *QueuedWorkloadReconciler) Delete(e event.DeleteEvent) bool {
 	if wl.Spec.Admission == nil {
 		r.queues.DeleteWorkload(wl)
 	}
+	r.notifyWatchers(wl)
```

  then why not defer here as well? just so they all look the same

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-25T21:04:10Z

- **pkg/controller/core/core.go:27** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-25T21:04:10Z
```diff
@@ -0,0 +1,42 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/queue"
+)
+
+// SetupControllers sets up the core controllers. It returns the name of the
+// controller that failed to create and an error, if any.
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache) (string, error) {
```

  yup, I am wondering if moving forward it will be of use to have a recorder in the core controllers as well, we might want to set events into the CRD, no?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-25T21:14:35Z

- **pkg/constants/constants.go:32** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T20:54:45Z
```diff
@@ -29,7 +29,7 @@ const (
 
 	// UpdatesBatchPeriod is the batch period to hold queuedworkload updates
 	// before syncing a Queue and CLusterQueue onbjects.
-	UpdatesBatchPeriod = 3 * time.Second
+	UpdatesBatchPeriod = time.Second
```

  what is the reason for the change?

- **pkg/controller/core/clusterqueue_controller.go:168** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T20:57:33Z
```diff
@@ -135,51 +141,35 @@ func (r *ClusterQueue) Update(e event.UpdateEvent) bool {
 	return true
 }
 
-func (r *ClusterQueue) Generic(e event.GenericEvent) bool {
+func (r *ClusterQueueReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
 	return true
 }
 
-// workloadHandler signals the controller to reconcile the ClusterQueue
+// cqWorkloadHandler signals the controller to reconcile the ClusterQueue
 // assigned to the workload in the event.
-type workloadHandler struct {
+type cqWorkloadHandler struct {
 	qManager *queue.Manager
 }
 
-func (h *workloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
-	w := e.Object.(*kueue.QueuedWorkload)
-	req := h.requestForWorkloadClusterQueue(w)
-	if req != nil {
-		q.AddAfter(*req, constants.UpdatesBatchPeriod)
-	}
+func (h *cqWorkloadHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
 }
 
-func (h *workloadHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	oldW := e.ObjectOld.(*kueue.QueuedWorkload)
-	oldReq := h.requestForWorkloadClusterQueue(oldW)
-	if oldReq != nil {
-		q.AddAfter(*oldReq, constants.UpdatesBatchPeriod)
-	}
-	newW := e.ObjectNew.(*kueue.QueuedWorkload)
-	newReq := h.requestForWorkloadClusterQueue(newW)
-	// Enqueue the CQ for the new object if it's different from the old one.
-	if newReq != nil && (oldReq == nil || oldReq.Name != newReq.Name) {
-		q.AddAfter(*newReq, constants.UpdatesBatchPeriod)
-	}
+func (h *cqWorkloadHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
 }
 
-func (h *workloadHandler) Delete(e event.DeleteEvent, q workqueue.RateLimitingInterface) {
+func (h *cqWorkloadHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *cqWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
```

  add a comment on when we are assuming that the Generic callback will be invoked, I am guessing once for every element in the channel?

- **pkg/controller/core/queue_controller.go:55** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T21:00:48Z
```diff
@@ -29,27 +29,33 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/event"
 	"sigs.k8s.io/controller-runtime/pkg/reconcile"
 	"sigs.k8s.io/controller-runtime/pkg/source"
+	"sigs.k8s.io/kueue/pkg/constants"
 
 	kueue "sigs.k8s.io/kueue/api/v1alpha1"
-	"sigs.k8s.io/kueue/pkg/constants"
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
 // QueueReconciler reconciles a Queue object
 type QueueReconciler struct {
-	client client.Client
-	log    logr.Logger
-	queues *queue.Manager
+	client     client.Client
+	log        logr.Logger
+	queues     *queue.Manager
+	qwUpdateCh chan event.GenericEvent
 }
 
 func NewQueueReconciler(client client.Client, queues *queue.Manager) *QueueReconciler {
 	return &QueueReconciler{
-		log:    ctrl.Log.WithName("queue-reconciler"),
-		queues: queues,
-		client: client,
+		log:        ctrl.Log.WithName("queue-reconciler"),
+		queues:     queues,
+		client:     client,
+		qwUpdateCh: make(chan event.GenericEvent),
 	}
 }
 
+func (r *QueueReconciler) NotifyQWUpdate(w *kueue.QueuedWorkload) {
```

  I would avoid using abbreviations in function names, especially exported ones.

- **pkg/controller/core/queue_controller.go:51** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T21:07:04Z
```diff
@@ -29,27 +29,33 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/event"
 	"sigs.k8s.io/controller-runtime/pkg/reconcile"
 	"sigs.k8s.io/controller-runtime/pkg/source"
+	"sigs.k8s.io/kueue/pkg/constants"
 
 	kueue "sigs.k8s.io/kueue/api/v1alpha1"
-	"sigs.k8s.io/kueue/pkg/constants"
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
 // QueueReconciler reconciles a Queue object
 type QueueReconciler struct {
-	client client.Client
-	log    logr.Logger
-	queues *queue.Manager
+	client     client.Client
+	log        logr.Logger
+	queues     *queue.Manager
+	qwUpdateCh chan event.GenericEvent
 }
 
 func NewQueueReconciler(client client.Client, queues *queue.Manager) *QueueReconciler {
 	return &QueueReconciler{
-		log:    ctrl.Log.WithName("queue-reconciler"),
-		queues: queues,
-		client: client,
+		log:        ctrl.Log.WithName("queue-reconciler"),
+		queues:     queues,
+		client:     client,
+		qwUpdateCh: make(chan event.GenericEvent),
```

  should we make the channel buffered so that there is a lower chance that QW handler doesn't block on it?

- **pkg/controller/core/clusterqueue_controller.go:165** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T21:09:11Z
```diff
@@ -135,51 +141,35 @@ func (r *ClusterQueue) Update(e event.UpdateEvent) bool {
 	return true
 }
 
-func (r *ClusterQueue) Generic(e event.GenericEvent) bool {
+func (r *ClusterQueueReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
 	return true
 }
 
-// workloadHandler signals the controller to reconcile the ClusterQueue
+// cqWorkloadHandler signals the controller to reconcile the ClusterQueue
 // assigned to the workload in the event.
-type workloadHandler struct {
+type cqWorkloadHandler struct {
 	qManager *queue.Manager
 }
 
-func (h *workloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
-	w := e.Object.(*kueue.QueuedWorkload)
-	req := h.requestForWorkloadClusterQueue(w)
-	if req != nil {
-		q.AddAfter(*req, constants.UpdatesBatchPeriod)
-	}
+func (h *cqWorkloadHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
 }
 
-func (h *workloadHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	oldW := e.ObjectOld.(*kueue.QueuedWorkload)
-	oldReq := h.requestForWorkloadClusterQueue(oldW)
-	if oldReq != nil {
-		q.AddAfter(*oldReq, constants.UpdatesBatchPeriod)
-	}
-	newW := e.ObjectNew.(*kueue.QueuedWorkload)
-	newReq := h.requestForWorkloadClusterQueue(newW)
-	// Enqueue the CQ for the new object if it's different from the old one.
-	if newReq != nil && (oldReq == nil || oldReq.Name != newReq.Name) {
-		q.AddAfter(*newReq, constants.UpdatesBatchPeriod)
-	}
+func (h *cqWorkloadHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
 }
 
-func (h *workloadHandler) Delete(e event.DeleteEvent, q workqueue.RateLimitingInterface) {
+func (h *cqWorkloadHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
```

  add a comment on whether or not Delete/Create/Update will ever be invoked for the channel we are watching.

- **pkg/controller/core/queuedworkload_controller.go:107** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T21:12:38Z
```diff
@@ -98,6 +104,7 @@ func (r *QueuedWorkloadReconciler) Create(e event.CreateEvent) bool {
 		return true
 	}
 
+	defer r.notifyWatchers(wl)
```

  invoking only for non-finished workloads makes an assumption about what the watchers care about, I don't think we should do that, the watchers should decide what counts and what doesn't.

- **pkg/controller/core/queuedworkload_controller.go:107** by [@ahg-g](https://github.com/ahg-g) — 2022-03-25T21:14:26Z
```diff
@@ -98,6 +104,7 @@ func (r *QueuedWorkloadReconciler) Create(e event.CreateEvent) bool {
 		return true
 	}
 
+	defer r.notifyWatchers(wl)
```

  I suggest we do `defer r.notifyWatchers(wl)` at the beginning for each update/create/delete

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-26T12:50:07Z

- **pkg/constants/constants.go:32** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-26T12:50:07Z
```diff
@@ -29,7 +29,7 @@ const (
 
 	// UpdatesBatchPeriod is the batch period to hold queuedworkload updates
 	// before syncing a Queue and CLusterQueue onbjects.
-	UpdatesBatchPeriod = 3 * time.Second
+	UpdatesBatchPeriod = time.Second
```

  by using channels, we don't need such wait period

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-26T12:50:51Z

- **pkg/controller/core/queuedworkload_controller.go:107** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-26T12:50:51Z
```diff
@@ -98,6 +104,7 @@ func (r *QueuedWorkloadReconciler) Create(e event.CreateEvent) bool {
 		return true
 	}
 
+	defer r.notifyWatchers(wl)
```

  agree

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-28T13:43:39Z

lgtm
