# PR #1542: Make ordering timestamp configurable when Workload evicted due to Pods-ready

**Summary**: Make ordering timestamp configurable when Workload evicted due to Pods-ready

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1542

**Last updated**: 2024-02-13T19:42:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@nstogner](https://github.com/nstogner)
- **Branch**: `pods-ready-requeue-strategy` → `main`
- **Created**: 2024-01-03T16:28:10Z
- **Updated**: 2024-02-13T19:42:33Z
- **Merged**: 2024-01-18T13:40:37Z
- **Closed**: 2024-01-18T13:40:37Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `kind/api-change`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet), [@trasc](https://github.com/trasc)
- **Changed files**: 23   **+511 / -141**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Allow admins to choose the timestamp used for sorting workloads after eviction due to wait-for-pods-ready. See [KEP-1282](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1282-pods-ready-requeue-strategy).

#### Which issue(s) this PR fixes:

Fixes #1282

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
WaitForPodsReady: Add a config field to allow admins to configure the timestamp used when sorting workloads that were evicted due to their Pods not becoming ready on time.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-03T16:28:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | edc8d7d552bc774d57fb6359cbac9ef58761412d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65a84a311158a60008dbd044 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-03T16:28:19Z

Hi @nstogner. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@nstogner](https://github.com/nstogner) — 2024-01-03T16:29:58Z

PS: I still need to add the user-story test case.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-03T17:51:24Z

/ok-to-test

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-05T19:03:25Z

/assign

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T14:10:43Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-10T14:24:27Z

> /assign @mimowo

ack

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T10:50:24Z

/kind api-change

### Comment by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:23:05Z

> I can think of an alternative approach in which we add a timestamp field in `Workload.Info` that is populated by the queue manager as the workloads are added or updated. Then the rest of the system can use this cached timestamp without having to understand where it's coming from.
> 
> It might simplify some code, but I'll leave it up to you to decide which approach to keep.

Ah, I missed you comment and implemented all of the other requested changes. I am looking at what you suggested now.

### Comment by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:29:47Z

> > I can think of an alternative approach in which we add a timestamp field in `Workload.Info` that is populated by the queue manager as the workloads are added or updated. Then the rest of the system can use this cached timestamp without having to understand where it's coming from.
> > It might simplify some code, but I'll leave it up to you to decide which approach to keep.
> 
> Ah, I missed you comment and implemented all of the other requested changes. I am looking at what you suggested now.

While I think this would remove some of the configuration propagation that is occurring at the moment, it seems a bit unnatural to carry a global config value around in each instance of a Workload (via `type Info`). This value determines how multiple workloads get sorted against eachother. It might be a little confusing to pull this value from a given instance of a workload itself to then compare the rest of the workloads for sorting.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T21:34:05Z

/approve
I'm leaving LGTM to @mimowo or @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-17T05:53:55Z

Due to #1593
/test pull-kueue-test-e2e-main-1-28

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-17T18:05:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1542#pullrequestreview-1827881005" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1542#" title="Author self-approved">nstogner</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-18T08:38:19Z

LGTM

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-18T13:35:09Z

LGTM label has been added.  <details>Git tree hash: 2d4a963ffcad71f03c8030b210b27ecb3b7f716e</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T19:35:04Z

/release-note-edit
```release-note
Add a config field to `.waitForPodsReady` to allow admins to configure the timestamp used when sorting workloads that were evicted due to their Pods not becoming ready in time.
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T19:35:21Z

/release-note-edit
```release-note
Add a config field to `.waitForPodsReady` to allow admins to configure the timestamp used when sorting workloads that were evicted due to their Pods not becoming ready on time.
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T19:42:31Z

/release-note-edit
```release-note
WaitForPodsReady: Add a config field to allow admins to configure the timestamp used when sorting workloads that were evicted due to their Pods not becoming ready on time.
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T09:59:15Z

- **apis/config/v1beta1/configuration_types.go:198** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:27:27Z
```diff
@@ -192,8 +192,22 @@ type WaitForPodsReady struct {
 	// until the jobs reach the PodsReady=true condition. It defaults to false if Enable is false
 	// and defaults to true otherwise.
 	BlockAdmission *bool `json:"blockAdmission,omitempty"`
+
+	// RequeuingTimestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction (back of queue).
+	RequeuingTimestamp RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
```

  As this is a new field I think it should be a ptr, and only use Ordering, Eviction, or null (defaulted to Eviction).
  I think otherwise the API clients generated from the new code may crash when dealing with old API, because they may expect the field to be always returned.

- **apis/config/v1beta1/defaults.go:113** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:27:57Z
```diff
@@ -111,12 +110,15 @@ func SetDefaults_Configuration(cfg *Configuration) {
 			}
 			cfg.WaitForPodsReady.BlockAdmission = &defaultBlockAdmission
 		}
+		if cfg.WaitForPodsReady.RequeuingTimestamp == "" {
```

  Let's use checking the nil here

- **apis/config/v1beta1/defaults.go:42** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:30:02Z
```diff
@@ -38,9 +36,10 @@ const (
 	DefaultLeaderElectionID                             = "c1f6bfd2.kueue.x-k8s.io"
 	DefaultClientConnectionQPS                  float32 = 20.0
 	DefaultClientConnectionBurst                int32   = 30
-	defaultPodsReadyTimeout                             = 5 * time.Minute
+	DefaultPodsReadyTimeout                             = 5 * time.Minute
 	DefaultQueueVisibilityUpdateIntervalSeconds int32   = 5
 	DefaultClusterQueuesMaxCount                int32   = 10
+	DefaultJobFrameworkName                             = "batch/job"
```

  This change might be ok, but seems unrelated, so I would suggest reverting it so that the PR is focused.

- **cmd/kueue/main.go:376** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:31:14Z
```diff
@@ -370,6 +371,13 @@ func waitForPodsReady(cfg *configapi.Configuration) bool {
 	return cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Enable
 }
 
+func podsReadyRequeuingTimestamp(cfg *configapi.Configuration) configapi.RequeuingTimestamp {
+	if cfg.WaitForPodsReady != nil {
+		return cfg.WaitForPodsReady.RequeuingTimestamp
```

  If we go for ptr, I think we will also need to check it is not nil here.

- **cmd/kueue/main.go:378** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:31:38Z
```diff
@@ -370,6 +371,13 @@ func waitForPodsReady(cfg *configapi.Configuration) bool {
 	return cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Enable
 }
 
+func podsReadyRequeuingTimestamp(cfg *configapi.Configuration) configapi.RequeuingTimestamp {
+	if cfg.WaitForPodsReady != nil {
+		return cfg.WaitForPodsReady.RequeuingTimestamp
+	}
+	return ""
```

  let's return Eviction here

- **apis/config/v1beta1/defaults_test.go:30** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:35:00Z
```diff
@@ -25,6 +27,7 @@ import (
 	componentconfigv1alpha1 "k8s.io/component-base/config/v1alpha1"
 	"k8s.io/utils/ptr"
 
+	"sigs.k8s.io/kueue/apis/config/v1beta1"
```

  Why this is needed, we are in the package, adding `v1beta1.` throughout the test makes the diff less readable.

- **pkg/queue/cluster_queue_impl_test.go:40** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:36:46Z
```diff
@@ -36,7 +37,9 @@ const (
 )
 
 func Test_PushOrUpdate(t *testing.T) {
-	cq := newClusterQueueImpl(keyFunc, queueOrdering)
+	cq := newClusterQueueImpl(keyFunc, queueOrderingFunc(workload.Ordering{
```

  nit: maybe create a var in the test so that we don't repeat it?
  ```
  defaultQueueOrdering := queueOrderingFunc(workload.Ordering{
  		PodsReadyRequeuingTimestamp: config.Eviction,
  	})
  ```

- **pkg/queue/cluster_queue_strict_fifo_test.go:39** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:48:27Z
```diff
@@ -34,11 +35,14 @@ const (
 )
 
 func TestFIFOClusterQueue(t *testing.T) {
-	q, err := newClusterQueue(&kueue.ClusterQueue{
-		Spec: kueue.ClusterQueueSpec{
-			QueueingStrategy: kueue.StrictFIFO,
+	q, err := newClusterQueue(
+		&kueue.ClusterQueue{
```

  nit: revert formatting changes

- **pkg/queue/cluster_queue_strict_fifo_test.go:286** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:48:44Z
```diff
@@ -240,11 +282,14 @@ func TestStrictFIFORequeueIfNotPresent(t *testing.T) {
 
 	for reason, test := range tests {
 		t.Run(string(reason), func(t *testing.T) {
-			cq, _ := newClusterQueueStrictFIFO(&kueue.ClusterQueue{
-				Spec: kueue.ClusterQueueSpec{
-					QueueingStrategy: kueue.StrictFIFO,
+			cq, _ := newClusterQueueStrictFIFO(
+				&kueue.ClusterQueue{
```

  nit: revert formatting changes

- **pkg/scheduler/scheduler.go:517** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:50:35Z
```diff
@@ -480,23 +503,26 @@ func (s *Scheduler) applyAdmissionWithSSA(ctx context.Context, w *kueue.Workload
 	return workload.ApplyAdmissionStatus(ctx, s.client, w, false)
 }
 
-type entryOrdering []entry
+type entryOrdering struct {
+	entries          []entry
+	workloadOrdering workload.Ordering
+}
 
-func (e entryOrdering) Len() int {
```

  nit: I would suggest reverting the renames, but feel free to keep

- **pkg/workload/workload.go:404** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:52:22Z
```diff
@@ -389,11 +390,19 @@ func ApplyAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
 }
 
+type Ordering struct {
+	PodsReadyRequeuingTimestamp config.RequeuingTimestamp
+}
+
 // GetQueueOrderTimestamp return the timestamp to be used by the scheduler. It could
 // be the workload creation time or the last time a PodsReady timeout has occurred.
-func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
-	if c := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); c != nil && c.Status == metav1.ConditionTrue && c.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
-		return &c.LastTransitionTime
+func (o Ordering) GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
+	if o.PodsReadyRequeuingTimestamp == config.Eviction || o.PodsReadyRequeuingTimestamp == "" {
```

  ```suggestion
  	if o.PodsReadyRequeuingTimestamp == config.Eviction {
  ```

- **pkg/workload/workload.go:404** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:53:29Z
```diff
@@ -389,11 +390,19 @@ func ApplyAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
 }
 
+type Ordering struct {
+	PodsReadyRequeuingTimestamp config.RequeuingTimestamp
+}
+
 // GetQueueOrderTimestamp return the timestamp to be used by the scheduler. It could
 // be the workload creation time or the last time a PodsReady timeout has occurred.
-func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
-	if c := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); c != nil && c.Status == metav1.ConditionTrue && c.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
-		return &c.LastTransitionTime
+func (o Ordering) GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
+	if o.PodsReadyRequeuingTimestamp == config.Eviction || o.PodsReadyRequeuingTimestamp == "" {
```

  This should not be needed, if we adjust fallback in podsReadyRequeuingTimestamp

- **test/integration/controller/jobs/job/job_controller_test.go:1901** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:56:49Z
```diff
@@ -1803,6 +1805,159 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler with waitForPodsReadyx configured", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns                 *corev1.Namespace
+		equivalentFlavorA  *kueue.ResourceFlavor
+		equivalentFlavorB  *kueue.ResourceFlavor
+		equivalentClusterQ *kueue.ClusterQueue
+		equivalentLocalQ   *kueue.LocalQueue
+
+		waitForPodsReadyTimeout = 6 * time.Second
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		ctx, k8sClient = fwk.RunManager(cfg, managerAndSchedulerSetup(&config.Configuration{
+			WaitForPodsReady: &config.WaitForPodsReady{
+				Enable:             true,
+				Timeout:            &metav1.Duration{Duration: waitForPodsReadyTimeout},
+				RequeuingTimestamp: config.Creation,
+			},
+		}))
+	})
+	ginkgo.AfterAll(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		equivalentFlavorA = testing.MakeResourceFlavor("equivalent-flavor-a").
+			Label(instanceKey, "equivalent-flavor-a").Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivalentFlavorA)).Should(gomega.Succeed())
+
+		equivalentFlavorB = testing.MakeResourceFlavor("equivalent-flavor-b").
+			Label(instanceKey, "equivalent-flavor-b").Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivalentFlavorB)).Should(gomega.Succeed())
+
+		equivalentClusterQ = testing.MakeClusterQueue("equivalent-cq").
+			Cohort("equivalent").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("equivalent-flavor-a").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("equivalent-flavor-b").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivalentClusterQ)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, equivalentClusterQ, true)
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, equivalentFlavorA, true)
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, equivalentFlavorB, true)
+	})
+
+	ginkgo.It("Should schedule jobs as they fit in their ClusterQueue", func() {
+		ginkgo.By("creating localQueues")
+		equivalentLocalQ = testing.MakeLocalQueue("equivalent", ns.Name).ClusterQueue(equivalentClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivalentLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking the first job starts")
+		// A request of 5 CPUs will fit in the first flavor.
+		equivJob1 := testingjob.MakeJob("equiv-job1", ns.Name).Queue(equivalentLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivJob1)).Should(gomega.Succeed())
+		lookupKey1 := types.NamespacedName{Name: equivJob1.Name, Namespace: equivJob1.Namespace}
+		createdEquivJob1 := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, lookupKey1, createdEquivJob1)).Should(gomega.Succeed())
+			return createdEquivJob1.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdEquivJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(equivalentFlavorA.Name))
+		util.ExpectPendingWorkloadsMetric(equivalentClusterQ, 0, 0)
+
+		time.Sleep(time.Second)
+
+		ginkgo.By("checking the second job starts")
+		// A request of 5 CPUs will fit in the second flavor.
+		equivJob2 := testingjob.MakeJob("equiv-job2", ns.Name).Queue(equivalentLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivJob2)).Should(gomega.Succeed())
+		lookupKey2 := types.NamespacedName{Name: equivJob2.Name, Namespace: equivJob2.Namespace}
+		createdEquivJob2 := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, lookupKey2, createdEquivJob2)).Should(gomega.Succeed())
+			return createdEquivJob2.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdEquivJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(equivalentFlavorB.Name))
+		util.ExpectPendingWorkloadsMetric(equivalentClusterQ, 0, 0)
+
+		time.Sleep(time.Second)
```

  I see the first sleep is needed to make sure the creation timestamps differ. 
  
  However, can we simplify the scenario, so that it captures the change, but uses at most one `sleep(1s)`?

- **test/integration/scheduler/podsready/scheduler_test.go:399** by [@mimowo](https://github.com/mimowo) — 2024-01-11T09:57:38Z
```diff
@@ -370,18 +374,67 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.Creation
+		})
+
+		ginkgo.It("Should prioritize workloads submitted earlier", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
```

  Similarly here, can we capture the essence with just 1 sleep?

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-11T17:15:33Z

- **apis/config/v1beta1/configuration_types.go:198** by [@nstogner](https://github.com/nstogner) — 2024-01-11T17:15:33Z
```diff
@@ -192,8 +192,22 @@ type WaitForPodsReady struct {
 	// until the jobs reach the PodsReady=true condition. It defaults to false if Enable is false
 	// and defaults to true otherwise.
 	BlockAdmission *bool `json:"blockAdmission,omitempty"`
+
+	// RequeuingTimestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction (back of queue).
+	RequeuingTimestamp RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
```

  Makes sense, will make this change

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-11T17:16:57Z

- **apis/config/v1beta1/defaults.go:42** by [@nstogner](https://github.com/nstogner) — 2024-01-11T17:16:57Z
```diff
@@ -38,9 +36,10 @@ const (
 	DefaultLeaderElectionID                             = "c1f6bfd2.kueue.x-k8s.io"
 	DefaultClientConnectionQPS                  float32 = 20.0
 	DefaultClientConnectionBurst                int32   = 30
-	defaultPodsReadyTimeout                             = 5 * time.Minute
+	DefaultPodsReadyTimeout                             = 5 * time.Minute
 	DefaultQueueVisibilityUpdateIntervalSeconds int32   = 5
 	DefaultClusterQueuesMaxCount                int32   = 10
+	DefaultJobFrameworkName                             = "batch/job"
```

  I think I added this to avoid an import cycle. I updated the tests to be in a dedicated `_test` package to make sure we keep the constants in sync.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T11:43:37Z

- **apis/config/v1beta1/defaults.go:42** by [@mimowo](https://github.com/mimowo) — 2024-01-12T11:43:36Z
```diff
@@ -38,9 +36,10 @@ const (
 	DefaultLeaderElectionID                             = "c1f6bfd2.kueue.x-k8s.io"
 	DefaultClientConnectionQPS                  float32 = 20.0
 	DefaultClientConnectionBurst                int32   = 30
-	defaultPodsReadyTimeout                             = 5 * time.Minute
+	DefaultPodsReadyTimeout                             = 5 * time.Minute
 	DefaultQueueVisibilityUpdateIntervalSeconds int32   = 5
 	DefaultClusterQueuesMaxCount                int32   = 10
+	DefaultJobFrameworkName                             = "batch/job"
```

  sgtm, if required to avoid the cycle

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-12T16:03:44Z

I can think of an alternative approach in which we add a timestamp field in `Workload.Info` that is populated by the queue manager as the workloads are added or updated. Then the rest of the system can use this cached timestamp without having to understand where it's coming from.

It might simplify some code, but I'll leave it up to you to decide which approach to keep.

- **apis/config/v1beta1/configuration_types.go:205** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T15:25:49Z
```diff
@@ -192,8 +192,22 @@ type WaitForPodsReady struct {
 	// until the jobs reach the PodsReady=true condition. It defaults to false if Enable is false
 	// and defaults to true otherwise.
 	BlockAdmission *bool `json:"blockAdmission,omitempty"`
+
+	// RequeuingTimestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction (back of queue).
+	RequeuingTimestamp RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type RequeuingTimestamp string
+
+const (
+	// Creation timestamp (from Workload .metadata.creationTimestamp).
+	Creation RequeuingTimestamp = "Creation"
```

  ```suggestion
  	CreationTimestamp RequeuingTimestamp = "Creation"
  ```

- **apis/config/v1beta1/configuration_types.go:208** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T15:26:07Z
```diff
@@ -192,8 +192,22 @@ type WaitForPodsReady struct {
 	// until the jobs reach the PodsReady=true condition. It defaults to false if Enable is false
 	// and defaults to true otherwise.
 	BlockAdmission *bool `json:"blockAdmission,omitempty"`
+
+	// RequeuingTimestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction (back of queue).
+	RequeuingTimestamp RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type RequeuingTimestamp string
+
+const (
+	// Creation timestamp (from Workload .metadata.creationTimestamp).
+	Creation RequeuingTimestamp = "Creation"
+
+	// Eviction timestamp (from Workload .status.conditions).
+	Eviction RequeuingTimestamp = "Eviction"
```

  ```suggestion
  	EvictionTimestamp RequeuingTimestamp = "Eviction"
  ```

- **apis/config/v1beta1/defaults_test.go:31** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T15:29:25Z
```diff
@@ -25,6 +27,7 @@ import (
 	componentconfigv1alpha1 "k8s.io/component-base/config/v1alpha1"
 	"k8s.io/utils/ptr"
 
+	"sigs.k8s.io/kueue/apis/config/v1beta1"
 	"sigs.k8s.io/kueue/pkg/controller/jobs/job"
```

  I think we can remove this dependency and just have one package for the code and tests.

- **cmd/kueue/main.go:376** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T15:30:44Z
```diff
@@ -370,6 +371,13 @@ func waitForPodsReady(cfg *configapi.Configuration) bool {
 	return cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Enable
 }
 
+func podsReadyRequeuingTimestamp(cfg *configapi.Configuration) configapi.RequeuingTimestamp {
+	if cfg.WaitForPodsReady != nil {
+		return cfg.WaitForPodsReady.RequeuingTimestamp
```

  Or use `ptr.Deref`

- **pkg/queue/manager.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T15:35:14Z
```diff
@@ -28,6 +28,8 @@ import (
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+
```

  remove empty line

- **pkg/scheduler/scheduler.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T15:51:30Z
```diff
@@ -38,6 +38,8 @@ import (
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+
```

  remove empty line

- **test/integration/controller/jobs/job/job_controller_test.go:1808** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T15:54:29Z
```diff
@@ -1803,6 +1805,159 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler with waitForPodsReadyx configured", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  ```suggestion
  var _ = ginkgo.Describe("Job controller interacting with scheduler with waitForPodsReady configured", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T15:58:47Z
  I'm tempted to drop these tests. The scheduler tests are comprehensive enough, as the timestamps come from the Workload object only.
  
  Although I think these tests might be valuable for pod groups, as there is the additional problem that Pods are  deleted on preemption. But we can do that as E2E tests as a follow up.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-15T10:49:06Z

- **apis/config/v1beta1/defaults_test.go:446** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T09:15:40Z
```diff
@@ -389,51 +396,51 @@ func TestSetDefaults_Configuration(t *testing.T) {
 			},
 		},
 		"integrations": {
-			original: &Configuration{
-				InternalCertManagement: &InternalCertManagement{
+			original: &v1beta1.Configuration{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
-				Integrations: &Integrations{
+				Integrations: &v1beta1.Integrations{
 					Frameworks: []string{"a", "b"},
 				},
 			},
-			want: &Configuration{
-				Namespace:         ptr.To(DefaultNamespace),
+			want: &v1beta1.Configuration{
+				Namespace:         ptr.To(v1beta1.DefaultNamespace),
 				ControllerManager: defaultCtrlManagerConfigurationSpec,
-				InternalCertManagement: &InternalCertManagement{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
 				ClientConnection: defaultClientConnection,
-				Integrations: &Integrations{
+				Integrations: &v1beta1.Integrations{
 					Frameworks: []string{"a", "b"},
 					PodOptions: defaultIntegrations.PodOptions,
 				},
 				QueueVisibility: defaultQueueVisibility,
 			},
 		},
 		"queue visibility": {
-			original: &Configuration{
-				InternalCertManagement: &InternalCertManagement{
+			original: &v1beta1.Configuration{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
-				QueueVisibility: &QueueVisibility{
+				QueueVisibility: &v1beta1.QueueVisibility{
 					UpdateIntervalSeconds: 10,
-					ClusterQueues: &ClusterQueueVisibility{
+					ClusterQueues: &v1beta1.ClusterQueueVisibility{
 						MaxCount: 0,
 					},
 				},
 			},
-			want: &Configuration{
-				Namespace:         ptr.To(DefaultNamespace),
+			want: &v1beta1.Configuration{
+				Namespace:         ptr.To(v1beta1.DefaultNamespace),
 				ControllerManager: defaultCtrlManagerConfigurationSpec,
-				InternalCertManagement: &InternalCertManagement{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
 				ClientConnection: defaultClientConnection,
 				Integrations:     defaultIntegrations,
-				QueueVisibility: &QueueVisibility{
+				QueueVisibility: &v1beta1.QueueVisibility{
 					UpdateIntervalSeconds: 10,
-					ClusterQueues: &ClusterQueueVisibility{
+					ClusterQueues: &v1beta1.ClusterQueueVisibility{
 						MaxCount: 0,
 					},
 				},
```

  Should we add a case, "defaulting waitForPodsReady.RequeuingTimestamp"?

- **pkg/workload/workload.go:403** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T09:35:12Z
```diff
@@ -389,11 +390,19 @@ func ApplyAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
 }
 
+type Ordering struct {
+	PodsReadyRequeuingTimestamp config.RequeuingTimestamp
+}
+
 // GetQueueOrderTimestamp return the timestamp to be used by the scheduler. It could
 // be the workload creation time or the last time a PodsReady timeout has occurred.
-func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
-	if c := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); c != nil && c.Status == metav1.ConditionTrue && c.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
-		return &c.LastTransitionTime
+func (o Ordering) GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
```

  Should we add a unit test since this function is exported?

- **pkg/scheduler/scheduler.go:73** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T09:58:55Z
```diff
@@ -67,28 +69,46 @@ type Scheduler struct {
 	preemptor               *preemption.Preemptor
 	// Stubs.
 	applyAdmission func(context.Context, *kueue.Workload) error
+
+	//podsReadyRequeuingTimestamp config.RequeuingTimestamp
```

  ```suggestion
  ```
  
  Did you forget to remove this?

- **pkg/scheduler/scheduler.go:517** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T10:01:44Z
```diff
@@ -480,23 +503,26 @@ func (s *Scheduler) applyAdmissionWithSSA(ctx context.Context, w *kueue.Workload
 	return workload.ApplyAdmissionStatus(ctx, s.client, w, false)
 }
 
-type entryOrdering []entry
+type entryOrdering struct {
+	entries          []entry
+	workloadOrdering workload.Ordering
+}
 
-func (e entryOrdering) Len() int {
```

  I also think that the previous receiver name, `e` would be better.

- **test/integration/controller/jobs/job/job_controller_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T10:47:37Z
  > I'm tempted to drop these tests. The scheduler tests are comprehensive enough, as the timestamps come from the Workload object only.
  > 
  > Although I think these tests might be valuable for pod groups, as there is the additional problem that Pods are deleted on preemption.
  
  I think so, too. These tests look valuable only for pod integration.
  
  > But we can do that as E2E tests as a follow up.
  
  In the [pod-group e2e tests](https://github.com/kubernetes-sigs/kueue/blob/2ee9e0f6eea07e25be6722c314dbb9a7c9a79ca1/test/e2e/singlecluster/pod_test.go), I think we should focus only on cases that we need kube-controller-manager and kube-scheduler to avoid increasing test duration.
  
  So, I think that moving these test cases to the [integration test for the pod-controller](https://github.com/kubernetes-sigs/kueue/blob/2ee9e0f6eea07e25be6722c314dbb9a7c9a79ca1/test/integration/controller/jobs/pod/pod_controller_test.go) would be enough instead of moving these to e2e.
  
  @alculquicondor WDYT?

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T16:05:57Z

- **pkg/queue/cluster_queue_strict_fifo_test.go:39** by [@nstogner](https://github.com/nstogner) — 2024-01-15T16:05:57Z
```diff
@@ -34,11 +35,14 @@ const (
 )
 
 func TestFIFOClusterQueue(t *testing.T) {
-	q, err := newClusterQueue(&kueue.ClusterQueue{
-		Spec: kueue.ClusterQueueSpec{
-			QueueingStrategy: kueue.StrictFIFO,
+	q, err := newClusterQueue(
+		&kueue.ClusterQueue{
```

  Reverting, note: after running `go fmt` this comes out as:
  
  ```go
  func TestFIFOClusterQueue(t *testing.T) {
  	q, err := newClusterQueue(&kueue.ClusterQueue{
  		Spec: kueue.ClusterQueueSpec{
  			QueueingStrategy: kueue.StrictFIFO,
  		},
  	},
  		workload.Ordering{
  			PodsReadyRequeuingTimestamp: config.EvictionTimestamp,
  		})
  	if err != nil {
  		t.Fatalf("Failed creating ClusterQueue %v", err)
  	}
  ```

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T16:07:46Z

- **pkg/queue/cluster_queue_strict_fifo_test.go:286** by [@nstogner](https://github.com/nstogner) — 2024-01-15T16:07:46Z
```diff
@@ -240,11 +282,14 @@ func TestStrictFIFORequeueIfNotPresent(t *testing.T) {
 
 	for reason, test := range tests {
 		t.Run(string(reason), func(t *testing.T) {
-			cq, _ := newClusterQueueStrictFIFO(&kueue.ClusterQueue{
-				Spec: kueue.ClusterQueueSpec{
-					QueueingStrategy: kueue.StrictFIFO,
+			cq, _ := newClusterQueueStrictFIFO(
+				&kueue.ClusterQueue{
```

  Reverting, note, after `go fmt` this comes out as:
  
  ```go
  		t.Run(tt.name, func(t *testing.T) {
  			if tt.workloadOrdering == nil {
  				// The default ordering:
  				tt.workloadOrdering = &workload.Ordering{PodsReadyRequeuingTimestamp: config.EvictionTimestamp}
  			}
  			q, err := newClusterQueue(&kueue.ClusterQueue{
  				Spec: kueue.ClusterQueueSpec{
  					QueueingStrategy: kueue.StrictFIFO,
  				},
  			},
  				*tt.workloadOrdering)
  			if err != nil {
  				t.Fatalf("Failed creating ClusterQueue %v", err)
  			}
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-15T19:04:11Z

- **apis/config/v1beta1/configuration_types.go:198** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T19:04:11Z
```diff
@@ -192,8 +192,22 @@ type WaitForPodsReady struct {
 	// until the jobs reach the PodsReady=true condition. It defaults to false if Enable is false
 	// and defaults to true otherwise.
 	BlockAdmission *bool `json:"blockAdmission,omitempty"`
+
+	// RequeuingTimestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction (back of queue).
+	RequeuingTimestamp RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
```

  This is still unresolved

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-15T19:07:50Z

- **pkg/queue/cluster_queue_impl_test.go:40** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T19:05:09Z
```diff
@@ -36,7 +37,9 @@ const (
 )
 
 func Test_PushOrUpdate(t *testing.T) {
-	cq := newClusterQueueImpl(keyFunc, queueOrdering)
+	cq := newClusterQueueImpl(keyFunc, queueOrderingFunc(workload.Ordering{
```

  Still unresolved

- **pkg/queue/cluster_queue_strict_fifo_test.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T19:06:06Z
```diff
@@ -34,11 +35,14 @@ const (
 )
 
 func TestFIFOClusterQueue(t *testing.T) {
-	q, err := newClusterQueue(&kueue.ClusterQueue{
-		Spec: kueue.ClusterQueueSpec{
-			QueueingStrategy: kueue.StrictFIFO,
+	q, err := newClusterQueue(
+		&kueue.ClusterQueue{
```

  Let's leave the PR as-is

- **pkg/queue/manager.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T19:06:28Z
```diff
@@ -28,6 +28,8 @@ import (
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+
```

  Still unresolved

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-15T19:09:48Z

- **test/integration/controller/jobs/job/job_controller_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T19:09:47Z
  Fine by me

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:07:27Z

- **test/integration/scheduler/podsready/scheduler_test.go:399** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:07:27Z
```diff
@@ -370,18 +374,67 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.Creation
+		})
+
+		ginkgo.It("Should prioritize workloads submitted earlier", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
```

  The expectation here is workload 1 gets admitted (need spacing between 1 & 2), then workload 2 gets admitted (need spacing between 2 & 3 to make this consistent), then workload 1 gets admitted again. I think we need the workloads to be spaced out in time to have trigger this behavior and maintain the conditions being asserted on.

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:09:09Z

- **test/integration/controller/jobs/job/job_controller_test.go:1901** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:09:08Z
```diff
@@ -1803,6 +1805,159 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler with waitForPodsReadyx configured", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns                 *corev1.Namespace
+		equivalentFlavorA  *kueue.ResourceFlavor
+		equivalentFlavorB  *kueue.ResourceFlavor
+		equivalentClusterQ *kueue.ClusterQueue
+		equivalentLocalQ   *kueue.LocalQueue
+
+		waitForPodsReadyTimeout = 6 * time.Second
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		ctx, k8sClient = fwk.RunManager(cfg, managerAndSchedulerSetup(&config.Configuration{
+			WaitForPodsReady: &config.WaitForPodsReady{
+				Enable:             true,
+				Timeout:            &metav1.Duration{Duration: waitForPodsReadyTimeout},
+				RequeuingTimestamp: config.Creation,
+			},
+		}))
+	})
+	ginkgo.AfterAll(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		equivalentFlavorA = testing.MakeResourceFlavor("equivalent-flavor-a").
+			Label(instanceKey, "equivalent-flavor-a").Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivalentFlavorA)).Should(gomega.Succeed())
+
+		equivalentFlavorB = testing.MakeResourceFlavor("equivalent-flavor-b").
+			Label(instanceKey, "equivalent-flavor-b").Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivalentFlavorB)).Should(gomega.Succeed())
+
+		equivalentClusterQ = testing.MakeClusterQueue("equivalent-cq").
+			Cohort("equivalent").
+			ResourceGroup(
+				*testing.MakeFlavorQuotas("equivalent-flavor-a").Resource(corev1.ResourceCPU, "5").Obj(),
+				*testing.MakeFlavorQuotas("equivalent-flavor-b").Resource(corev1.ResourceCPU, "5").Obj(),
+			).Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivalentClusterQ)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, equivalentClusterQ, true)
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, equivalentFlavorA, true)
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, equivalentFlavorB, true)
+	})
+
+	ginkgo.It("Should schedule jobs as they fit in their ClusterQueue", func() {
+		ginkgo.By("creating localQueues")
+		equivalentLocalQ = testing.MakeLocalQueue("equivalent", ns.Name).ClusterQueue(equivalentClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivalentLocalQ)).Should(gomega.Succeed())
+
+		ginkgo.By("checking the first job starts")
+		// A request of 5 CPUs will fit in the first flavor.
+		equivJob1 := testingjob.MakeJob("equiv-job1", ns.Name).Queue(equivalentLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivJob1)).Should(gomega.Succeed())
+		lookupKey1 := types.NamespacedName{Name: equivJob1.Name, Namespace: equivJob1.Namespace}
+		createdEquivJob1 := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, lookupKey1, createdEquivJob1)).Should(gomega.Succeed())
+			return createdEquivJob1.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdEquivJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(equivalentFlavorA.Name))
+		util.ExpectPendingWorkloadsMetric(equivalentClusterQ, 0, 0)
+
+		time.Sleep(time.Second)
+
+		ginkgo.By("checking the second job starts")
+		// A request of 5 CPUs will fit in the second flavor.
+		equivJob2 := testingjob.MakeJob("equiv-job2", ns.Name).Queue(equivalentLocalQ.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, equivJob2)).Should(gomega.Succeed())
+		lookupKey2 := types.NamespacedName{Name: equivJob2.Name, Namespace: equivJob2.Namespace}
+		createdEquivJob2 := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, lookupKey2, createdEquivJob2)).Should(gomega.Succeed())
+			return createdEquivJob2.Spec.Suspend
+		}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(false)))
+		gomega.Expect(createdEquivJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(equivalentFlavorB.Name))
+		util.ExpectPendingWorkloadsMetric(equivalentClusterQ, 0, 0)
+
+		time.Sleep(time.Second)
```

  Removing these tests as per Aldo's comment

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:09:28Z

- **test/integration/controller/jobs/job/job_controller_test.go:1808** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:09:27Z
```diff
@@ -1803,6 +1805,159 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler with waitForPodsReadyx configured", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  Removing these test cases as per the other comment

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:09:41Z

- **pkg/workload/workload.go:404** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:09:40Z
```diff
@@ -389,11 +390,19 @@ func ApplyAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
 }
 
+type Ordering struct {
+	PodsReadyRequeuingTimestamp config.RequeuingTimestamp
+}
+
 // GetQueueOrderTimestamp return the timestamp to be used by the scheduler. It could
 // be the workload creation time or the last time a PodsReady timeout has occurred.
-func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
-	if c := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); c != nil && c.Status == metav1.ConditionTrue && c.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
-		return &c.LastTransitionTime
+func (o Ordering) GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
+	if o.PodsReadyRequeuingTimestamp == config.Eviction || o.PodsReadyRequeuingTimestamp == "" {
```

  Done

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:09:57Z

- **pkg/workload/workload.go:403** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:09:57Z
```diff
@@ -389,11 +390,19 @@ func ApplyAdmissionStatus(ctx context.Context, c client.Client, w *kueue.Workloa
 	return c.Status().Patch(ctx, patch, client.Apply, client.FieldOwner(constants.AdmissionName))
 }
 
+type Ordering struct {
+	PodsReadyRequeuingTimestamp config.RequeuingTimestamp
+}
+
 // GetQueueOrderTimestamp return the timestamp to be used by the scheduler. It could
 // be the workload creation time or the last time a PodsReady timeout has occurred.
-func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
-	if c := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); c != nil && c.Status == metav1.ConditionTrue && c.Reason == kueue.WorkloadEvictedByPodsReadyTimeout {
-		return &c.LastTransitionTime
+func (o Ordering) GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
```

  Test is there, updated it just now

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:10:24Z

- **pkg/scheduler/scheduler.go:517** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:10:24Z
```diff
@@ -480,23 +503,26 @@ func (s *Scheduler) applyAdmissionWithSSA(ctx context.Context, w *kueue.Workload
 	return workload.ApplyAdmissionStatus(ctx, s.client, w, false)
 }
 
-type entryOrdering []entry
+type entryOrdering struct {
+	entries          []entry
+	workloadOrdering workload.Ordering
+}
 
-func (e entryOrdering) Len() int {
```

  Done

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:10:44Z

- **pkg/scheduler/scheduler.go:73** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:10:44Z
```diff
@@ -67,28 +69,46 @@ type Scheduler struct {
 	preemptor               *preemption.Preemptor
 	// Stubs.
 	applyAdmission func(context.Context, *kueue.Workload) error
+
+	//podsReadyRequeuingTimestamp config.RequeuingTimestamp
```

  Yes, good eye

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:11:30Z

- **pkg/scheduler/scheduler.go:42** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:11:29Z
```diff
@@ -38,6 +38,8 @@ import (
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+
```

  Done

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:11:59Z

- **pkg/queue/manager.go:32** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:11:59Z
```diff
@@ -28,6 +28,8 @@ import (
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+
```

  Done

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:12:22Z

- **pkg/queue/cluster_queue_impl_test.go:40** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:12:22Z
```diff
@@ -36,7 +37,9 @@ const (
 )
 
 func Test_PushOrUpdate(t *testing.T) {
-	cq := newClusterQueueImpl(keyFunc, queueOrdering)
+	cq := newClusterQueueImpl(keyFunc, queueOrderingFunc(workload.Ordering{
```

  Added

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:12:30Z

- **cmd/kueue/main.go:378** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:12:30Z
```diff
@@ -370,6 +371,13 @@ func waitForPodsReady(cfg *configapi.Configuration) bool {
 	return cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Enable
 }
 
+func podsReadyRequeuingTimestamp(cfg *configapi.Configuration) configapi.RequeuingTimestamp {
+	if cfg.WaitForPodsReady != nil {
+		return cfg.WaitForPodsReady.RequeuingTimestamp
+	}
+	return ""
```

  Done

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:12:44Z

- **cmd/kueue/main.go:376** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:12:44Z
```diff
@@ -370,6 +371,13 @@ func waitForPodsReady(cfg *configapi.Configuration) bool {
 	return cfg.WaitForPodsReady != nil && cfg.WaitForPodsReady.Enable
 }
 
+func podsReadyRequeuingTimestamp(cfg *configapi.Configuration) configapi.RequeuingTimestamp {
+	if cfg.WaitForPodsReady != nil {
+		return cfg.WaitForPodsReady.RequeuingTimestamp
```

  Updated to pointer and added nil check

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:14:20Z

- **apis/config/v1beta1/defaults.go:42** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:14:20Z
```diff
@@ -38,9 +36,10 @@ const (
 	DefaultLeaderElectionID                             = "c1f6bfd2.kueue.x-k8s.io"
 	DefaultClientConnectionQPS                  float32 = 20.0
 	DefaultClientConnectionBurst                int32   = 30
-	defaultPodsReadyTimeout                             = 5 * time.Minute
+	DefaultPodsReadyTimeout                             = 5 * time.Minute
 	DefaultQueueVisibilityUpdateIntervalSeconds int32   = 5
 	DefaultClusterQueuesMaxCount                int32   = 10
+	DefaultJobFrameworkName                             = "batch/job"
```

  Reverted package change

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:19:02Z

- **apis/config/v1beta1/defaults_test.go:31** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:19:02Z
```diff
@@ -25,6 +27,7 @@ import (
 	componentconfigv1alpha1 "k8s.io/component-base/config/v1alpha1"
 	"k8s.io/utils/ptr"
 
+	"sigs.k8s.io/kueue/apis/config/v1beta1"
 	"sigs.k8s.io/kueue/pkg/controller/jobs/job"
```

  Done

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-15T20:20:35Z

- **apis/config/v1beta1/defaults_test.go:446** by [@nstogner](https://github.com/nstogner) — 2024-01-15T20:20:35Z
```diff
@@ -389,51 +396,51 @@ func TestSetDefaults_Configuration(t *testing.T) {
 			},
 		},
 		"integrations": {
-			original: &Configuration{
-				InternalCertManagement: &InternalCertManagement{
+			original: &v1beta1.Configuration{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
-				Integrations: &Integrations{
+				Integrations: &v1beta1.Integrations{
 					Frameworks: []string{"a", "b"},
 				},
 			},
-			want: &Configuration{
-				Namespace:         ptr.To(DefaultNamespace),
+			want: &v1beta1.Configuration{
+				Namespace:         ptr.To(v1beta1.DefaultNamespace),
 				ControllerManager: defaultCtrlManagerConfigurationSpec,
-				InternalCertManagement: &InternalCertManagement{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
 				ClientConnection: defaultClientConnection,
-				Integrations: &Integrations{
+				Integrations: &v1beta1.Integrations{
 					Frameworks: []string{"a", "b"},
 					PodOptions: defaultIntegrations.PodOptions,
 				},
 				QueueVisibility: defaultQueueVisibility,
 			},
 		},
 		"queue visibility": {
-			original: &Configuration{
-				InternalCertManagement: &InternalCertManagement{
+			original: &v1beta1.Configuration{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
-				QueueVisibility: &QueueVisibility{
+				QueueVisibility: &v1beta1.QueueVisibility{
 					UpdateIntervalSeconds: 10,
-					ClusterQueues: &ClusterQueueVisibility{
+					ClusterQueues: &v1beta1.ClusterQueueVisibility{
 						MaxCount: 0,
 					},
 				},
 			},
-			want: &Configuration{
-				Namespace:         ptr.To(DefaultNamespace),
+			want: &v1beta1.Configuration{
+				Namespace:         ptr.To(v1beta1.DefaultNamespace),
 				ControllerManager: defaultCtrlManagerConfigurationSpec,
-				InternalCertManagement: &InternalCertManagement{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
 				ClientConnection: defaultClientConnection,
 				Integrations:     defaultIntegrations,
-				QueueVisibility: &QueueVisibility{
+				QueueVisibility: &v1beta1.QueueVisibility{
 					UpdateIntervalSeconds: 10,
-					ClusterQueues: &ClusterQueueVisibility{
+					ClusterQueues: &v1beta1.ClusterQueueVisibility{
 						MaxCount: 0,
 					},
 				},
```

  The assertion is being captured in the renamed `defaulting waitForPodsReady.*` case ... it was already asserting on other waitForPodsReady fields.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-15T21:33:43Z

- **apis/config/v1beta1/defaults.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T21:26:24Z
```diff
@@ -111,12 +110,16 @@ func SetDefaults_Configuration(cfg *Configuration) {
 			}
 			cfg.WaitForPodsReady.BlockAdmission = &defaultBlockAdmission
 		}
+		if cfg.WaitForPodsReady.RequeuingTimestamp == nil {
+			defaultRequeuingTimestamp := EvictionTimestamp
+			cfg.WaitForPodsReady.RequeuingTimestamp = &defaultRequeuingTimestamp
```

  ```suggestion
  			cfg.WaitForPodsReady.RequeuingTimestamp = ptr.To(EvictionTimestamp)
  ```

- **pkg/queue/cluster_queue_strict_fifo_test.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T21:28:28Z
```diff
@@ -38,7 +39,10 @@ func TestFIFOClusterQueue(t *testing.T) {
 		Spec: kueue.ClusterQueueSpec{
```

  I actually preferred putting `&kueue.ClusterQueue` in a new line.

- **test/integration/scheduler/podsready/scheduler_test.go:575** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T21:33:07Z
```diff
@@ -484,4 +539,44 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted", func() {
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and readmitted", func() {
+				time.Sleep(podsReadyTimeout)
```

  Use Eventually to detect that the Workload gets the Eviction condition.

- **test/integration/scheduler/podsready/scheduler_test.go:413** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T21:33:25Z
```diff
@@ -370,18 +374,67 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should prioritize workloads submitted earlier", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted", func() {
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and the second should be admitted", func() {
+				time.Sleep(podsReadyTimeout)
```

  Use Eventually to detect that the Workload gets the Eviction condition.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-16T08:57:37Z

just some nits.

- **apis/config/v1beta1/defaults.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-16T08:16:50Z
```diff
@@ -41,6 +39,7 @@ const (
 	defaultPodsReadyTimeout                             = 5 * time.Minute
 	DefaultQueueVisibilityUpdateIntervalSeconds int32   = 5
 	DefaultClusterQueuesMaxCount                int32   = 10
+	DefaultJobFrameworkName                             = "batch/job"
```

  ```suggestion
  	defaultJobFrameworkName                             = "batch/job"
  ```
  
  Is this constant used from external packages?

- **apis/config/v1beta1/defaults_test.go:446** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-16T08:21:12Z
```diff
@@ -389,51 +396,51 @@ func TestSetDefaults_Configuration(t *testing.T) {
 			},
 		},
 		"integrations": {
-			original: &Configuration{
-				InternalCertManagement: &InternalCertManagement{
+			original: &v1beta1.Configuration{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
-				Integrations: &Integrations{
+				Integrations: &v1beta1.Integrations{
 					Frameworks: []string{"a", "b"},
 				},
 			},
-			want: &Configuration{
-				Namespace:         ptr.To(DefaultNamespace),
+			want: &v1beta1.Configuration{
+				Namespace:         ptr.To(v1beta1.DefaultNamespace),
 				ControllerManager: defaultCtrlManagerConfigurationSpec,
-				InternalCertManagement: &InternalCertManagement{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
 				ClientConnection: defaultClientConnection,
-				Integrations: &Integrations{
+				Integrations: &v1beta1.Integrations{
 					Frameworks: []string{"a", "b"},
 					PodOptions: defaultIntegrations.PodOptions,
 				},
 				QueueVisibility: defaultQueueVisibility,
 			},
 		},
 		"queue visibility": {
-			original: &Configuration{
-				InternalCertManagement: &InternalCertManagement{
+			original: &v1beta1.Configuration{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
-				QueueVisibility: &QueueVisibility{
+				QueueVisibility: &v1beta1.QueueVisibility{
 					UpdateIntervalSeconds: 10,
-					ClusterQueues: &ClusterQueueVisibility{
+					ClusterQueues: &v1beta1.ClusterQueueVisibility{
 						MaxCount: 0,
 					},
 				},
 			},
-			want: &Configuration{
-				Namespace:         ptr.To(DefaultNamespace),
+			want: &v1beta1.Configuration{
+				Namespace:         ptr.To(v1beta1.DefaultNamespace),
 				ControllerManager: defaultCtrlManagerConfigurationSpec,
-				InternalCertManagement: &InternalCertManagement{
+				InternalCertManagement: &v1beta1.InternalCertManagement{
 					Enable: ptr.To(false),
 				},
 				ClientConnection: defaultClientConnection,
 				Integrations:     defaultIntegrations,
-				QueueVisibility: &QueueVisibility{
+				QueueVisibility: &v1beta1.QueueVisibility{
 					UpdateIntervalSeconds: 10,
-					ClusterQueues: &ClusterQueueVisibility{
+					ClusterQueues: &v1beta1.ClusterQueueVisibility{
 						MaxCount: 0,
 					},
 				},
```

  It makes sense.

- **apis/config/v1beta1/defaults_test.go:313** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-16T08:21:30Z
```diff
@@ -312,7 +310,7 @@ func TestSetDefaults_Configuration(t *testing.T) {
 				QueueVisibility:  defaultQueueVisibility,
 			},
 		},
-		"defaulting waitForPodsReady.timeout": {
+		"defaulting waitForPodsReady.*": {
```

  ```suggestion
  		"defaulting waitForPodsReady values": {
  ```

- **pkg/queue/cluster_queue_impl_test.go:40** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-16T08:23:51Z
```diff
@@ -35,8 +36,14 @@ const (
 	defaultNamespace = "default"
 )
 
+var (
+	defaultQueueOrdering = queueOrderingFunc(workload.Ordering{
```

  ```suggestion
  	defaultQueueOrderingFunc = queueOrderingFunc(workload.Ordering{
  ```

- **test/integration/scheduler/podsready/scheduler_test.go:386** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-16T08:47:57Z
```diff
@@ -370,18 +374,67 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should prioritize workloads submitted earlier", func() {
+			localQueueName := "eviction-creation-lq"
```

  Why do we need to define a new lq? Should we use `prodQueue`?

- **test/integration/scheduler/podsready/scheduler_test.go:551** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-16T08:48:38Z
```diff
@@ -484,4 +539,44 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			localQueueName := "eviction-creation-lq"
```

  Here is the same comment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-16T10:24:17Z

- **apis/config/v1beta1/configuration_types.go:198** by [@mimowo](https://github.com/mimowo) — 2024-01-16T10:24:16Z
```diff
@@ -192,8 +192,22 @@ type WaitForPodsReady struct {
 	// until the jobs reach the PodsReady=true condition. It defaults to false if Enable is false
 	// and defaults to true otherwise.
 	BlockAdmission *bool `json:"blockAdmission,omitempty"`
+
+	// RequeuingTimestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction (back of queue).
+	RequeuingTimestamp RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
```

  I can see it resolved now, feel free to resolve the comment

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-16T10:31:49Z

- **test/integration/scheduler/podsready/scheduler_test.go:577** by [@mimowo](https://github.com/mimowo) — 2024-01-16T10:31:49Z
```diff
@@ -484,4 +539,44 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted", func() {
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and readmitted", func() {
+				time.Sleep(podsReadyTimeout)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
```

  Should we also verify what happens to wl2, does it remain pending?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-16T10:43:52Z

- **test/integration/scheduler/podsready/scheduler_test.go:399** by [@mimowo](https://github.com/mimowo) — 2024-01-16T10:43:52Z
```diff
@@ -370,18 +374,67 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.Creation
+		})
+
+		ginkgo.It("Should prioritize workloads submitted earlier", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
```

  sgtm

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T16:48:45Z

- **pkg/queue/cluster_queue_strict_fifo_test.go:39** by [@nstogner](https://github.com/nstogner) — 2024-01-16T16:48:45Z
```diff
@@ -38,7 +39,10 @@ func TestFIFOClusterQueue(t *testing.T) {
 		Spec: kueue.ClusterQueueSpec{
```

  Changing back to new line

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T17:02:48Z

- **test/integration/scheduler/podsready/scheduler_test.go:575** by [@nstogner](https://github.com/nstogner) — 2024-01-16T17:02:48Z
```diff
@@ -484,4 +539,44 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted", func() {
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and readmitted", func() {
+				time.Sleep(podsReadyTimeout)
```

  `util.FinishEvictionForWorkloads()` has a retry loop built in - using that and removing the sleep.

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T17:18:17Z

- **test/integration/scheduler/podsready/scheduler_test.go:413** by [@nstogner](https://github.com/nstogner) — 2024-01-16T17:18:16Z
```diff
@@ -370,18 +374,67 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should prioritize workloads submitted earlier", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted", func() {
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and the second should be admitted", func() {
+				time.Sleep(podsReadyTimeout)
```

  Removing the sleep

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T17:19:13Z

- **apis/config/v1beta1/defaults.go:42** by [@nstogner](https://github.com/nstogner) — 2024-01-16T17:19:13Z
```diff
@@ -41,6 +39,7 @@ const (
 	defaultPodsReadyTimeout                             = 5 * time.Minute
 	DefaultQueueVisibilityUpdateIntervalSeconds int32   = 5
 	DefaultClusterQueuesMaxCount                int32   = 10
+	DefaultJobFrameworkName                             = "batch/job"
```

  Good catch, un-exporting

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T17:35:12Z

- **test/integration/scheduler/podsready/scheduler_test.go:386** by [@nstogner](https://github.com/nstogner) — 2024-01-16T17:35:11Z
```diff
@@ -370,18 +374,67 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should prioritize workloads submitted earlier", func() {
+			localQueueName := "eviction-creation-lq"
```

  Good question, removing

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T17:35:29Z

- **test/integration/scheduler/podsready/scheduler_test.go:551** by [@nstogner](https://github.com/nstogner) — 2024-01-16T17:35:29Z
```diff
@@ -484,4 +539,44 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			localQueueName := "eviction-creation-lq"
```

  Removing

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T21:36:52Z

- **test/integration/scheduler/podsready/scheduler_test.go:577** by [@nstogner](https://github.com/nstogner) — 2024-01-16T21:36:52Z
```diff
@@ -484,4 +539,44 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted", func() {
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and readmitted", func() {
+				time.Sleep(podsReadyTimeout)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
```

  Good catch, it gets admitted via borrowed capacity from the dev queue. I am now creating a standalone queue and asserting that it does not get admitted.

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T21:59:19Z

- **pkg/queue/cluster_queue_impl_test.go:40** by [@nstogner](https://github.com/nstogner) — 2024-01-16T21:59:19Z
```diff
@@ -35,8 +36,14 @@ const (
 	defaultNamespace = "default"
 )
 
+var (
+	defaultQueueOrdering = queueOrderingFunc(workload.Ordering{
```

  Updated

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T21:59:25Z

- **apis/config/v1beta1/defaults_test.go:313** by [@nstogner](https://github.com/nstogner) — 2024-01-16T21:59:25Z
```diff
@@ -312,7 +310,7 @@ func TestSetDefaults_Configuration(t *testing.T) {
 				QueueVisibility:  defaultQueueVisibility,
 			},
 		},
-		"defaulting waitForPodsReady.timeout": {
+		"defaulting waitForPodsReady.*": {
```

  Updated

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-16T22:00:22Z

- **test/integration/scheduler/podsready/scheduler_test.go:399** by [@nstogner](https://github.com/nstogner) — 2024-01-16T22:00:21Z
```diff
@@ -370,18 +374,67 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.Creation
+		})
+
+		ginkgo.It("Should prioritize workloads submitted earlier", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl3 := testing.MakeWorkload("prod3", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
```

  Turns out that my test was wrong and wl2 was getting admitted on borrowed capacity. I simplified the test to get rid of borrowed capacity and use 2 workloads and 1 sleep.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-17T05:51:26Z

- **test/integration/scheduler/podsready/scheduler_test.go:563** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-17T05:50:47Z
```diff
@@ -484,4 +543,56 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			// Build a standalone cluster queue with just enough capacity for a single workload.
+			// (Avoid using prod/dev queues to avoid borrowing)
+			standaloneClusterQ := testing.MakeClusterQueue("standalone-cq").
+				ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, standaloneClusterQ)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, standaloneClusterQ)).Should(gomega.Succeed())
+			}()
```

  Should we perform this in `ginkgo.AfterEach` in this `Context` node instead of the `defer`?

- **test/integration/scheduler/podsready/scheduler_test.go:569** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-17T05:51:04Z
```diff
@@ -484,4 +543,56 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			// Build a standalone cluster queue with just enough capacity for a single workload.
+			// (Avoid using prod/dev queues to avoid borrowing)
+			standaloneClusterQ := testing.MakeClusterQueue("standalone-cq").
+				ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, standaloneClusterQ)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, standaloneClusterQ)).Should(gomega.Succeed())
+			}()
+
+			standaloneQueue := testing.MakeLocalQueue("standalone-queue", ns.Name).ClusterQueue(standaloneClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, standaloneQueue)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, standaloneQueue)).Should(gomega.Succeed())
+			}()
```

  ditto.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-17T08:41:14Z

- **test/integration/scheduler/podsready/scheduler_test.go:577** by [@mimowo](https://github.com/mimowo) — 2024-01-17T08:41:14Z
```diff
@@ -484,4 +539,44 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			localQueueName := "eviction-creation-lq"
+
+			// the workloads are created with a 5 cpu resource requirement to ensure only one can fit at a given time
+			wl1 := testing.MakeWorkload("prod1", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+			wl2 := testing.MakeWorkload("prod2", ns.Name).Queue(localQueueName).Request(corev1.ResourceCPU, "5").Obj()
+
+			ginkgo.By("create the workloads", func() {
+				// since metav1.Time has only second resolution, wait one second between
+				// create calls to avoid any potential creation timestamp collision
+				gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+				time.Sleep(time.Second)
+				gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("create the local queue to start admission", func() {
+				lq := testing.MakeLocalQueue(localQueueName, ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+				gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("waiting for the first workload to be admitted", func() {
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
+			})
+
+			ginkgo.By("waiting the timeout, the first workload should be evicted and readmitted", func() {
+				time.Sleep(podsReadyTimeout)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, wl1)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, wl1)
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-17T08:43:00Z

- **test/integration/scheduler/podsready/scheduler_test.go:563** by [@mimowo](https://github.com/mimowo) — 2024-01-17T08:42:59Z
```diff
@@ -484,4 +543,56 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			// Build a standalone cluster queue with just enough capacity for a single workload.
+			// (Avoid using prod/dev queues to avoid borrowing)
+			standaloneClusterQ := testing.MakeClusterQueue("standalone-cq").
+				ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, standaloneClusterQ)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, standaloneClusterQ)).Should(gomega.Succeed())
+			}()
```

  IIUC this is because the queues are declared only within the `It`, while the `AfterEach` is for all tests.
  Alternatively, we could declare the variable for all tests in the context, and in afterEach delete in not nil, but I don't hold a view which one is better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-17T08:48:20Z

- **apis/config/v1beta1/configuration_types.go:197** by [@mimowo](https://github.com/mimowo) — 2024-01-17T08:46:06Z
```diff
@@ -192,8 +192,22 @@ type WaitForPodsReady struct {
 	// until the jobs reach the PodsReady=true condition. It defaults to false if Enable is false
 	// and defaults to true otherwise.
 	BlockAdmission *bool `json:"blockAdmission,omitempty"`
+
+	// RequeuingTimestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction (back of queue).
```

  I'm wondering if "(back of queue)" could be misleading, there could be lower priority workloads that are even more behind in the queue order, because priority is checked first: https://github.com/kubernetes-sigs/kueue/blob/27902866b88d9f90fcb0a6a296946c12bcadcc6d/pkg/queue/cluster_queue_strict_fifo.go#L50-L55. I would suggest to just remove the text in the brackets.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-17T09:43:30Z

- **test/integration/scheduler/podsready/scheduler_test.go:563** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-17T09:43:30Z
```diff
@@ -484,4 +543,56 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			// Build a standalone cluster queue with just enough capacity for a single workload.
+			// (Avoid using prod/dev queues to avoid borrowing)
+			standaloneClusterQ := testing.MakeClusterQueue("standalone-cq").
+				ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, standaloneClusterQ)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, standaloneClusterQ)).Should(gomega.Succeed())
+			}()
```

  > Alternatively, we could declare the variable for all tests in the context, and in afterEach delete in not nil
  
  Yes, if we want to use `AfterEach`, we must put `standaloneClusterQ` on the line 548.
  
  > but I don't hold a view which one is better.
  
  I would suggest using `AfterEach`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-17T18:05:28Z

/approve
Please address the suggestions for the integration test.

- **test/integration/scheduler/podsready/scheduler_test.go:399** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-17T18:02:27Z
```diff
@@ -370,18 +374,71 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
```

  there  should be a corresponding `AfterEach` resetting the original values.

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-17T21:00:07Z

- **test/integration/scheduler/podsready/scheduler_test.go:399** by [@nstogner](https://github.com/nstogner) — 2024-01-17T21:00:07Z
```diff
@@ -370,18 +374,71 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
```

  This is not done in the other tests today (they all set these values in their `BeforeEach()`s). Would you like me to modify the others?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-17T21:07:50Z

- **test/integration/scheduler/podsready/scheduler_test.go:399** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-17T21:07:50Z
```diff
@@ -370,18 +374,71 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
```

  Ideally, yes, but we can leave it for a follow up.
  
  Just add an AfterEach for this new case in this PR.

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-17T21:24:17Z

- **test/integration/scheduler/podsready/scheduler_test.go:399** by [@nstogner](https://github.com/nstogner) — 2024-01-17T21:24:17Z
```diff
@@ -370,18 +374,71 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
```

  Gotcha, I guess it makes sense to add some variables for tracking the original values?

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-17T21:24:59Z

- **apis/config/v1beta1/configuration_types.go:197** by [@nstogner](https://github.com/nstogner) — 2024-01-17T21:24:59Z
```diff
@@ -192,8 +192,22 @@ type WaitForPodsReady struct {
 	// until the jobs reach the PodsReady=true condition. It defaults to false if Enable is false
 	// and defaults to true otherwise.
 	BlockAdmission *bool `json:"blockAdmission,omitempty"`
+
+	// RequeuingTimestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction (back of queue).
```

  Agree, done

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-17T21:25:07Z

- **test/integration/scheduler/podsready/scheduler_test.go:569** by [@nstogner](https://github.com/nstogner) — 2024-01-17T21:25:07Z
```diff
@@ -484,4 +543,56 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			// Build a standalone cluster queue with just enough capacity for a single workload.
+			// (Avoid using prod/dev queues to avoid borrowing)
+			standaloneClusterQ := testing.MakeClusterQueue("standalone-cq").
+				ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, standaloneClusterQ)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, standaloneClusterQ)).Should(gomega.Succeed())
+			}()
+
+			standaloneQueue := testing.MakeLocalQueue("standalone-queue", ns.Name).ClusterQueue(standaloneClusterQ.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, standaloneQueue)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, standaloneQueue)).Should(gomega.Succeed())
+			}()
```

  Done

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-17T21:26:23Z

- **test/integration/scheduler/podsready/scheduler_test.go:563** by [@nstogner](https://github.com/nstogner) — 2024-01-17T21:26:23Z
```diff
@@ -484,4 +543,56 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			// Build a standalone cluster queue with just enough capacity for a single workload.
+			// (Avoid using prod/dev queues to avoid borrowing)
+			standaloneClusterQ := testing.MakeClusterQueue("standalone-cq").
+				ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, standaloneClusterQ)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, standaloneClusterQ)).Should(gomega.Succeed())
+			}()
```

  I updated to use a `JustBeforeEach()` (cant use `BeforeEach()` b/c you need the `k8sClient`) and an `AfterEach()`.

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-17T21:46:00Z

- **test/integration/scheduler/podsready/scheduler_test.go:399** by [@nstogner](https://github.com/nstogner) — 2024-01-17T21:46:00Z
```diff
@@ -370,18 +374,71 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			})
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
```

  Take a look at [this commit](https://github.com/kubernetes-sigs/kueue/pull/1542/commits/edc8d7d552bc774d57fb6359cbac9ef58761412d) to see if that is what you prefer.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-18T13:34:17Z

- **test/integration/scheduler/podsready/scheduler_test.go:563** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-18T13:34:17Z
```diff
@@ -484,4 +543,56 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReadyNonblockingMode", func() {
 			util.ExpectAdmittedWorkloadsTotalMetric(prodClusterQ, 2)
 		})
 	})
+
+	var _ = ginkgo.Context("Requeuing timestamp set to Creation", func() {
+
+		ginkgo.BeforeEach(func() {
+			podsReadyTimeout = 3 * time.Second
+			requeuingTimestamp = config.CreationTimestamp
+		})
+
+		ginkgo.It("Should keep the evicted workload at the front of the queue", func() {
+			// Build a standalone cluster queue with just enough capacity for a single workload.
+			// (Avoid using prod/dev queues to avoid borrowing)
+			standaloneClusterQ := testing.MakeClusterQueue("standalone-cq").
+				ResourceGroup(*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, standaloneClusterQ)).Should(gomega.Succeed())
+			defer func() {
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, standaloneClusterQ)).Should(gomega.Succeed())
+			}()
```

  Ah, we set up k8client in the `JustBeforeEach()` node...
  Ideally, we want to set up the k8sclient in the `BeforeAll()` or `BeforeEach()`. However, I think the investigation is out of scope in this PR.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-18T13:35:04Z

@nstogner Thank you!

/lgtm
/hold cancel
