# PR #1554: Add HA support for the visibility API

**Summary**: Add HA support for the visibility API

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1554

**Last updated**: 2024-02-13T19:59:22Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@astefanutti](https://github.com/astefanutti)
- **Branch**: `pr-06` → `main`
- **Created**: 2024-01-08T15:13:21Z
- **Updated**: 2024-02-13T19:59:22Z
- **Merged**: 2024-01-25T19:48:58Z
- **Closed**: 2024-01-25T19:48:58Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 8   **+138 / -15**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

This PR enables multiple replicas of the Kueue deployment to serve the visibility extension API endpoint.

This is achieved by:

1.  Starting controllers that feed the queue manager unconditionally, with the addition of `controller.Options{NeedLeaderElection: ptr.To(false)}` as extra option to these controllers;
2. Skipping any actual reconciliation for these controllers, with the addition of `WithLeadingManager(mgr, r)`, which discards the reconciliation requests for non-leading replicas (still consuming the requests from the event queues), until one acquires the leader election lease if the leading replica has failed to renew it.

The scheduling decisions process is untouched and left to the responsibility of the leading replica, based on the state of its local queue manager.

#### Which issue(s) this PR fixes:

Fixes #1510

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
Visibility API: Add HA support.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-08T15:13:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b4ae8f06cd92fc019618990cd94c003818e797a0 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65a13105d166370008872524 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-08T15:13:30Z

Hi @astefanutti. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-08T19:18:17Z

/ok-to-test

### Comment by [@astefanutti](https://github.com/astefanutti) — 2024-01-08T19:41:19Z

/test pull-kueue-test-integration-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-10T14:06:34Z

LGTM label has been added.  <details>Git tree hash: c3e81253492630f353045af24a57e2d8843b2ba8</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-10T17:06:30Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-10T17:06:36Z

LGTM label has been added.  <details>Git tree hash: 4c7f8961bd7c01a238dcf2ad6d00abd3d7dd14c0</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-10T17:23:16Z

> /lgtm /assign @alculquicondor @tenzen-y

ACK

### Comment by [@astefanutti](https://github.com/astefanutti) — 2024-01-12T12:44:52Z

/test pull-kueue-test-e2e-main-1-29

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-15T15:20:16Z

Could you add a commit with the basic E2E test for HA?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-25T19:23:11Z

LGTM label has been added.  <details>Git tree hash: e51bd3986332be0b338375e3968e60fc1d2a666b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-25T19:23:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1554#discussion_r1466823618" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1554#" title="Author self-approved">astefanutti</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T19:59:21Z

/release-note-edit
```release-note
Visibility API: Add HA support.
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-09T17:17:48Z

- **pkg/controller/core/admissioncheck_controller.go:71** by [@mimowo](https://github.com/mimowo) — 2024-01-09T17:17:18Z
```diff
@@ -65,9 +67,9 @@ func NewAdmissionCheckReconciler(
 	}
 }
 
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch;create;update;patch;delete
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks/status,verbs=get;update;patch
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch;create;update;patch;delete
```

  nit: I suggest reverting the formatting changes just to make the diff minimal

- **pkg/controller/core/leader_aware_reconciler.go:42** by [@mimowo](https://github.com/mimowo) — 2024-01-09T17:17:38Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that skips reconciliation requests
+// until the manager has been elected leader. Once the manager is elected leader, the returned
+// reconcile.Reconciler simply delegates to the provided reconcile.Reconciler.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
```

  Could this be a boolean?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-09T17:52:02Z

- **pkg/controller/core/leader_aware_reconciler.go:42** by [@astefanutti](https://github.com/astefanutti) — 2024-01-09T17:52:02Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that skips reconciliation requests
+// until the manager has been elected leader. Once the manager is elected leader, the returned
+// reconcile.Reconciler simply delegates to the provided reconcile.Reconciler.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
```

  Leader election can transition from non-leading to leading, which I think explain why `manager.Elected()` returns a channel, so the manager can signal it has been able to acquire the lease, and thus has become the leader.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-09T17:54:54Z

- **pkg/controller/core/admissioncheck_controller.go:71** by [@astefanutti](https://github.com/astefanutti) — 2024-01-09T17:54:54Z
```diff
@@ -65,9 +67,9 @@ func NewAdmissionCheckReconciler(
 	}
 }
 
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch;create;update;patch;delete
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks/status,verbs=get;update;patch
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch;create;update;patch;delete
```

  I've reverted these changes that my IDE did automatically. Do you have any preference for these Kubebuilder markers? I'd be inclined to open a separate PR to have them consistently aligned with Kubebuilder documentation (with leading space). WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-09T17:56:10Z

- **pkg/controller/core/admissioncheck_controller.go:71** by [@mimowo](https://github.com/mimowo) — 2024-01-09T17:56:09Z
```diff
@@ -65,9 +67,9 @@ func NewAdmissionCheckReconciler(
 	}
 }
 
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch;create;update;patch;delete
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks/status,verbs=get;update;patch
-//+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks/finalizers,verbs=update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch;create;update;patch;delete
```

  I see, I don't have a preference, separate PR is fine

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-09T17:56:45Z

- **pkg/controller/core/leader_aware_reconciler.go:42** by [@mimowo](https://github.com/mimowo) — 2024-01-09T17:56:45Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that skips reconciliation requests
+// until the manager has been elected leader. Once the manager is elected leader, the returned
+// reconcile.Reconciler simply delegates to the provided reconcile.Reconciler.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
```

  ok, so this could flip at runtime? sgtm

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-09T17:59:50Z

- **pkg/controller/core/leader_aware_reconciler.go:42** by [@astefanutti](https://github.com/astefanutti) — 2024-01-09T17:59:50Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that skips reconciliation requests
+// until the manager has been elected leader. Once the manager is elected leader, the returned
+// reconcile.Reconciler simply delegates to the provided reconcile.Reconciler.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
```

  > so this could flip at runtime?
  
  Exactly, that's what happens when the leading replica fails to renew the lease, for whatever reason, then another replica will acquire it, and this channel will get closed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-09T18:00:46Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-09T18:00:46Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  Search by NewControllerManagedBy returns: https://github.com/search?q=repo%3Akubernetes-sigs%2Fkueue%20NewControllerManagedBy&type=code.
  
  I suppose we should we also adjust:
  - [pkg/controller/jobframework/reconciler.go](https://github.com/kubernetes-sigs/kueue/blob/1664221b43c20066335f208307bb5a7b6ee2e232/pkg/controller/jobframework/reconciler.go#L935)
  - [pkg/controller/admissionchecks/provisioning/controller.go](https://github.com/kubernetes-sigs/kueue/blob/1664221b43c20066335f208307bb5a7b6ee2e232/pkg/controller/admissionchecks/provisioning/controller.go#L697)

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-09T18:05:48Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@astefanutti](https://github.com/astefanutti) — 2024-01-09T18:05:48Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  My understanding is only controllers that contribute to the `queue.Manager` state, which is the source of truth for the visibility API, have to be adapted so their watchers are started unconditionally (w.r.t. leader election).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-10T09:30:35Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-10T09:30:35Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  I think this SGTM, but let me clarify what I know and ask some questions to better understand, if the issue is just with the visibility API server, or with the scheduling decisions in the HA mode.
  
  IIUC, the Visibility API serves the information from queue.Manager, but we accept there is some delay in the propagation of the information between replicas. 
  
  What is bad is if the information is not just delayed, but wrong? If so, could it go wrong by running multiple replicas of the core reconcilers?
  
  Further, if the information in the queue.Manager is wrong (for a selected replica), then could this also lead to wrong scheduling decisions? If so, then we have a bigger problem than just "visibility API consistency", but correctness of the scheduling decisions.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-10T10:01:49Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@astefanutti](https://github.com/astefanutti) — 2024-01-10T10:01:49Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  With the approach taken in this PR, only the leading replica is still responsible for making scheduling decisions, based on the state of its local queue manager. So this does not change the scheduling behaviour, compared to the single replica scenario.
  
  From the implementation details perspective, the reconcilers are "disabled" for the non-leading replicas, and only the watchers are started, so no "write" changes are made to the resources. Only the state of the queue managers is updated, which only impact the results of the visibility API requests made to these replicas when these are non-leading.
  
  Given the asynchronous nature of the watchers, there may be a delay by the time they all eventually reach the stationary state of the system, which can result in discrepancies during the transition. If some events are missed, there should be a full re-sync, as it's done for the informers. We may have to harden that re-sync mechanism, if it turns out to be a real issue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-10T13:00:38Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-10T13:00:38Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  Thank you for the explanations, I think the PR makes sense, I would like to better understand the status on the current main.
  
  Is the issue (on the current main) just with the visibility API serving inconsistent data (which could be anyway due to delays), or the issue is with scheduling decisions? If the issue is with making scheduling decisions (based on inconsistent cache in different replica caches) then your PR fixes more than just visibility API, so we could adjust the title / release note.
  
  EDIT: OTOH if currently the scheduling decisions are correct, then I would assume that the caches get eventually consistent, and thus the different instances of the visibility servers should.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-10T13:01:04Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-10T13:01:04Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  cc @alculquicondor

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-10T13:40:19Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@astefanutti](https://github.com/astefanutti) — 2024-01-10T13:40:19Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  > Is the issue (on the current main) just with the visibility API serving inconsistent data (which could be anyway due to delays), or the issue is with scheduling decisions?
  
  The issue with current main is the fact that visibility API endpoints that are served by the non-leading replicas return empty results. That's because the watchers that update the queue manager aren't started for these replicas, which is what that PR solves.
  
  For the correctness of scheduling decisions, this stays untouched, as it remains the responsibility of the leading replica alone.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-10T13:44:57Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-10T13:44:57Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  Thanks for explaining, so I guess the only remaining question from me would be where in this change we actually start the watchers in this PR: `That's because the watchers that update the queue manager aren't started for these replicas, which is what that PR solves.`?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-10T13:53:42Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@astefanutti](https://github.com/astefanutti) — 2024-01-10T13:53:42Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  You're welcome.
  
  The watchers are started for non-leading replicas with the addition of `WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)})`.
  
  This actually instructs controller-runtime to start the entire controllers. When a controller starts, both its reconciler and watchers are started. The other addition, `WithLeadingManager(mgr, r)`, prevents the reconciler from doing any actual work for non-leading replicas, until one acquires the leader election lease if the leading replica has failed to renew it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-10T14:04:35Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-10T14:04:35Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  Excellent, got it, the meaning of `WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).` was the missing piece for me. Thinking if there is a good place to note it, maybe summarize this in the PR description in a bit more detail.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-10T14:06:28Z

/lgtm
/assign @alculquicondor @tenzen-y

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-10T14:50:17Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@astefanutti](https://github.com/astefanutti) — 2024-01-10T14:50:17Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  I've updated the PR description as you've suggested. Let me know if there is any other places I could capture this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-10T14:57:45Z

- **pkg/controller/core/leader_aware_reconciler.go:26** by [@mimowo](https://github.com/mimowo) — 2024-01-10T14:57:44Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that skips reconciliation requests
```

  Maybe add here also something along the lines: "It allows to skip reconciliation for the non-leading replicas which are started when the `NeedLeaderElection: false` option is passed. Starting the non-leading replicas
  without reconciliation is needed so that they can serve data for the visibility server."

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-10T14:58:13Z

- **pkg/controller/core/admissioncheck_controller.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-10T14:58:13Z
```diff
@@ -220,7 +222,8 @@ func (r *AdmissionCheckReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.AdmissionCheck{}).
+		WithOptions(controller.Options{NeedLeaderElection: ptr.To(false)}).
 		WatchesRawSource(&source.Channel{Source: r.cqUpdateCh}, &handler).
 		WithEventFilter(r).
-		Complete(r)
+		Complete(WithLeadingManager(mgr, r))
```

  sgtm, proposed in a separate comment

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-10T16:53:55Z

- **pkg/controller/core/leader_aware_reconciler.go:26** by [@astefanutti](https://github.com/astefanutti) — 2024-01-10T16:53:55Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that skips reconciliation requests
```

  I've updated the function comment / documentation. I hope it captures it. PTAL 🙏🏼.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-10T17:06:23Z

- **pkg/controller/core/leader_aware_reconciler.go:26** by [@mimowo](https://github.com/mimowo) — 2024-01-10T17:06:23Z
```diff
@@ -0,0 +1,52 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that skips reconciliation requests
```

  awesome, lgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-10T22:19:59Z

Very clean implementation 😄 

I just have some questions.

- **pkg/controller/core/leader_aware_reconciler.go:2** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T22:14:00Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
```

  2024 :)

- **pkg/controller/core/leader_aware_reconciler.go:61** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-10T22:18:31Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
+		delegate: reconciler,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected  <-chan struct{}
+	delegate reconcile.Reconciler
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, skip reconciliation.
+		return ctrl.Result{}, nil
```

  If there is a delay in the notification, then would we rely on the Resync to queue the objects for reconciliation again?
  
  Is there a way to trigger Resync when becoming the leader, so we don't have to wait?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-11T09:17:15Z

- **pkg/controller/core/leader_aware_reconciler.go:2** by [@astefanutti](https://github.com/astefanutti) — 2024-01-11T09:17:15Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
```

  Updated. Eagle eyes 😄.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-11T09:33:36Z

- **pkg/controller/core/leader_aware_reconciler.go:61** by [@astefanutti](https://github.com/astefanutti) — 2024-01-11T09:33:36Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
+		delegate: reconciler,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected  <-chan struct{}
+	delegate reconcile.Reconciler
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, skip reconciliation.
+		return ctrl.Result{}, nil
```

  There might be a delay between the moment the leading replica becomes "inoperative", fails to renew the leader election lease, and the future leading replica acquires the lease, which creates a window during which events might be missed / not taken into account.
  
  It may be possible to trigger a full resync of the cache, but I think it's more suitable for correcting cache drift / entropy, and be a "hammer" solution for preventing event misses. For the latter, I think re-queuing with a constant duration would be sufficient. This would probably mean the queues for non-leading replicas contain the keys for the entire list of existing resources, but I think that's acceptable.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-11T10:47:12Z

- **pkg/controller/core/leader_aware_reconciler.go:61** by [@astefanutti](https://github.com/astefanutti) — 2024-01-11T10:47:12Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
+		delegate: reconciler,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected  <-chan struct{}
+	delegate reconcile.Reconciler
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, skip reconciliation.
+		return ctrl.Result{}, nil
```

  Added 923e7bb972840a9ea382830104c199838f6d1a70 to requeue the requests in non-leading replicas after a constant duration set to the configuration leader election lease duration. PTAL 🙏🏼.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T11:24:00Z

- **pkg/controller/core/leader_aware_reconciler.go:61** by [@mimowo](https://github.com/mimowo) — 2024-01-11T11:24:00Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
+		delegate: reconciler,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected  <-chan struct{}
+	delegate reconcile.Reconciler
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, skip reconciliation.
+		return ctrl.Result{}, nil
```

  > There might be a delay between the moment the leading replica becomes "inoperative", fails to renew the leader election lease, and the future leading replica acquires the lease, which creates a window during which events might be missed / not taken into account.
  
  Can this also happen on the main branch? If this is scenario of missed reconciles happens on main I think we could move it to a follow up, because it seems to add complexity, so maybe fixing the scenario can be decoupled from the visibility server fix?
  
  EDIT: and so that scenario has a test, if feasible

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T12:21:15Z

- **pkg/controller/core/leader_aware_reconciler.go:61** by [@mimowo](https://github.com/mimowo) — 2024-01-11T12:21:14Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
+		delegate: reconciler,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected  <-chan struct{}
+	delegate reconcile.Reconciler
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, skip reconciliation.
+		return ctrl.Result{}, nil
```

  Ok, I think I understand, and it seems this is a new issue (with delayed reconciles due to the change of leader) on this branch. IIUC on main, we would not start the watches on non-leading replicas, so at the moment the replica transitions to leading, the watches would start and we would get the ADDED events, triggering reconciles.
  
  Here, we have the watches active, so we will not get the ADDED events, and we may wait unnecessarily long the the reconciles. Doing this with 15min timeout seems borderline for acceptable, because it is still regression from main. 
  
  Is there a way to trigger reconciles for the Reconcilers when the replica transitions from non-leading to leading?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-11T13:14:51Z

- **pkg/controller/core/leader_aware_reconciler.go:61** by [@astefanutti](https://github.com/astefanutti) — 2024-01-11T13:14:51Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
+		delegate: reconciler,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected  <-chan struct{}
+	delegate reconcile.Reconciler
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, skip reconciliation.
+		return ctrl.Result{}, nil
```

  > Ok, I think I understand, and it seems this is a new issue (with delayed reconciles due to the change of leader) on this branch. IIUC on main, we would not start the watches on non-leading replicas, so at the moment the replica transitions to leading, the watches would start and we would get the ADDED events, triggering reconciles.
  
  Right, that's my understanding as well.
  
  > Here, we have the watches active, so we will not get the ADDED events, and we may wait unnecessarily long the the reconciles. Doing this with 15min timeout seems borderline for acceptable, because it is still regression from main.
  > 
  > Is there a way to trigger reconciles for the Reconcilers when the replica transitions from non-leading to leading?
  
  Mind it's 15 seconds by default, which is the default value for the lease duration that's waited by the non-leading replicas to periodically attempt to acquire leadership. This duration is configurable, by setting `.leaderElection.leaseDuration` in the Kueue controller ConfigMap, if 15 seconds is consider too long.
   
  On a side note, re-queuing is documented as the recommended approach to mitigate event misses here:
  
  https://github.com/kubernetes-sigs/controller-runtime/blob/8f8247fdc1c3bc75f2189f28079a9efc4e82d06f/pkg/cache/cache.go#L163-L170

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-11T13:25:37Z

- **pkg/controller/core/leader_aware_reconciler.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-11T13:13:33Z
```diff
@@ -0,0 +1,88 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	if le := cfg.LeaderElection; le != nil {
```

  Is this check needed? At my glance, we verify the same condition in Line 47.

- **pkg/controller/core/leader_aware_reconciler.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-11T13:24:59Z
```diff
@@ -0,0 +1,88 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	if le := cfg.LeaderElection; le != nil {
+		zero := metav1.Duration{}
+		if duration := le.LeaseDuration; duration != zero {
+			requeueDuration = duration.Duration
+		}
```

  Is this check and defaults needed?
  
  We already check this duration here: https://github.com/kubernetes-sigs/kueue/blob/4bc332a75fe221e6dc92db269ae3832da884527a/pkg/config/config.go#L119-L121
  
  Also, the controller-runtime defaults to 15sec if this duration is zero here:
  
  https://github.com/kubernetes-sigs/controller-runtime/blob/8f8247fdc1c3bc75f2189f28079a9efc4e82d06f/pkg/manager/manager.go#L204-L206

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-11T13:42:46Z

- **pkg/controller/core/leader_aware_reconciler.go:54** by [@astefanutti](https://github.com/astefanutti) — 2024-01-11T13:42:46Z
```diff
@@ -0,0 +1,88 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	if le := cfg.LeaderElection; le != nil {
```

  You're right, it's not. I've removed it. Thanks.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-11T13:47:11Z

- **pkg/controller/core/leader_aware_reconciler.go:58** by [@astefanutti](https://github.com/astefanutti) — 2024-01-11T13:47:11Z
```diff
@@ -0,0 +1,88 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	if le := cfg.LeaderElection; le != nil {
+		zero := metav1.Duration{}
+		if duration := le.LeaseDuration; duration != zero {
+			requeueDuration = duration.Duration
+		}
```

  I haven't been able to find where we default the value for `cfg.LeaderElection.LeaseDuration.Duration`.
  
  Maybe we should call `RecommendedDefaultLeaderElectionConfiguration` from component-base when we initialise the configuration, but I'd rather to that in a separate PR, WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T14:02:24Z

- **pkg/controller/core/leader_aware_reconciler.go:61** by [@mimowo](https://github.com/mimowo) — 2024-01-11T14:02:24Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
+		delegate: reconciler,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected  <-chan struct{}
+	delegate reconcile.Reconciler
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, skip reconciliation.
+		return ctrl.Result{}, nil
```

  ah, 15s seems ok, not sure why I was mistaken by minutes.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-11T14:04:23Z

- **pkg/controller/core/leader_aware_reconciler.go:52** by [@mimowo](https://github.com/mimowo) — 2024-01-11T14:04:23Z
```diff
@@ -0,0 +1,86 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
```

  I would suggest to define the default as a constant higher up, even if used in only one place. It is nice to have the consts in one place and named in suggestive way, maybe `defaultRequeueDuration`.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-11T14:04:43Z

- **pkg/controller/core/leader_aware_reconciler.go:61** by [@astefanutti](https://github.com/astefanutti) — 2024-01-11T14:04:43Z
```diff
@@ -0,0 +1,63 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler) reconcile.Reconciler {
+	return &leaderAwareReconciler{
+		elected:  mgr.Elected(),
+		delegate: reconciler,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected  <-chan struct{}
+	delegate reconcile.Reconciler
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, skip reconciliation.
+		return ctrl.Result{}, nil
```

  I thought for a second I had made a mistake on my end, but no 😃.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-11T14:05:26Z

- **pkg/controller/core/leader_aware_reconciler.go:52** by [@astefanutti](https://github.com/astefanutti) — 2024-01-11T14:05:26Z
```diff
@@ -0,0 +1,86 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
```

  Ah right, wanted to do it but forgot about it, let me do that.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-11T15:44:14Z

- **pkg/controller/core/leader_aware_reconciler.go:98** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-11T15:44:14Z
```diff
@@ -0,0 +1,86 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		delegate:        reconciler,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	delegate        reconcile.Reconciler
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, requeue the reconciliation request
+		// to prevent against any missed / discarded events over the period it takes
+		// to fail over a new leading replica, which can take as much as the configured
+		// lease duration, for it to acquire leadership.
+		return ctrl.Result{RequeueAfter: r.requeueDuration}, nil
```

  If a replica doesn't get the lease for a long time, we will keep requeueing keys for objects that might have been deleted. This would be a form of memory leak. Could we quickly check if the objects exist in the client?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-12T12:32:21Z

- **pkg/controller/core/leader_aware_reconciler.go:52** by [@astefanutti](https://github.com/astefanutti) — 2024-01-12T12:32:21Z
```diff
@@ -0,0 +1,86 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
```

  Updated. PTAL.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-12T12:33:42Z

- **pkg/controller/core/leader_aware_reconciler.go:98** by [@astefanutti](https://github.com/astefanutti) — 2024-01-12T12:33:42Z
```diff
@@ -0,0 +1,86 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		delegate:        reconciler,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	delegate        reconcile.Reconciler
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
+	select {
+	case <-r.elected:
+		// The manager has been elected leader, delegate reconciliation to the provided reconciler.
+		return r.delegate.Reconcile(ctx, request)
+	default:
+		// The manager hasn't been elected leader yet, requeue the reconciliation request
+		// to prevent against any missed / discarded events over the period it takes
+		// to fail over a new leading replica, which can take as much as the configured
+		// lease duration, for it to acquire leadership.
+		return ctrl.Result{RequeueAfter: r.requeueDuration}, nil
```

  I've added the logic that prevent from re-enqueuing requests indefinitely if the object has been deleted / is not found. PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T12:47:25Z

- **pkg/controller/core/leader_aware_reconciler.go:52** by [@mimowo](https://github.com/mimowo) — 2024-01-12T12:47:25Z
```diff
@@ -0,0 +1,86 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
```

  LGTM, thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T12:50:57Z

- **pkg/controller/core/leader_aware_reconciler.go:84** by [@mimowo](https://github.com/mimowo) — 2024-01-12T12:50:51Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// defaultRequeueDuration defaults the duration used by non-leading replicas
+// to requeue events, so no events are missed over the period it takes for
+// leader election to fail over a new replica.
+const defaultRequeueDuration = 15 * time.Second
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, obj client.Object, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := defaultRequeueDuration
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		client:          mgr.GetClient(),
+		delegate:        reconciler,
+		object:          obj,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	client          client.Client
+	delegate        reconcile.Reconciler
+	object          client.Object
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
```

  I think this isn't very complex, but non-trivial either, and is not fully covered with e2e tests. Should we have some unit tests at least for the function (we could use fake clock and fake client)? WDYT @alculquicondor @astefanutti ?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-12T17:18:40Z

- **pkg/controller/core/leader_aware_reconciler.go:84** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T17:18:40Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// defaultRequeueDuration defaults the duration used by non-leading replicas
+// to requeue events, so no events are missed over the period it takes for
+// leader election to fail over a new replica.
+const defaultRequeueDuration = 15 * time.Second
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, obj client.Object, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := defaultRequeueDuration
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		client:          mgr.GetClient(),
+		delegate:        reconciler,
+		object:          obj,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	client          client.Client
+	delegate        reconcile.Reconciler
+	object          client.Object
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
```

  I think the most valuable thing would be an E2E test that terminates one of the kueue instances and checks that scheduling continues happening.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-15T13:44:33Z

- **pkg/controller/core/leader_aware_reconciler.go:84** by [@astefanutti](https://github.com/astefanutti) — 2024-01-15T13:44:33Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// defaultRequeueDuration defaults the duration used by non-leading replicas
+// to requeue events, so no events are missed over the period it takes for
+// leader election to fail over a new replica.
+const defaultRequeueDuration = 15 * time.Second
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, obj client.Object, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := defaultRequeueDuration
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		client:          mgr.GetClient(),
+		delegate:        reconciler,
+		object:          obj,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	client          client.Client
+	delegate        reconcile.Reconciler
+	object          client.Object
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
```

  I'd also vote for having an e2e test, that'd scale to two replicas, assert the webhooks and the visibility API are correctly served in both replicas, then terminate the leading replica and scale down to one replica, check leadership has failover to the other replica, and assert scheduling and serving work as expected.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-15T14:04:55Z

- **pkg/controller/core/leader_aware_reconciler.go:84** by [@mimowo](https://github.com/mimowo) — 2024-01-15T14:04:55Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// defaultRequeueDuration defaults the duration used by non-leading replicas
+// to requeue events, so no events are missed over the period it takes for
+// leader election to fail over a new replica.
+const defaultRequeueDuration = 15 * time.Second
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, obj client.Object, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := defaultRequeueDuration
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		client:          mgr.GetClient(),
+		delegate:        reconciler,
+		object:          obj,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	client          client.Client
+	delegate        reconcile.Reconciler
+	object          client.Object
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
```

  just a nit question, why "and scale down to one replica" is relevant in the scenario? I guess it would be closer to real-life scenario if we continue to have 2 replicas,

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-15T14:18:13Z

- **pkg/controller/core/leader_aware_reconciler.go:84** by [@astefanutti](https://github.com/astefanutti) — 2024-01-15T14:18:13Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// defaultRequeueDuration defaults the duration used by non-leading replicas
+// to requeue events, so no events are missed over the period it takes for
+// leader election to fail over a new replica.
+const defaultRequeueDuration = 15 * time.Second
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, obj client.Object, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := defaultRequeueDuration
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		client:          mgr.GetClient(),
+		delegate:        reconciler,
+		object:          obj,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	client          client.Client
+	delegate        reconcile.Reconciler
+	object          client.Object
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
```

  I was thinking we could scale down the deployment as an easy way to make sure leadership fails over to an existing replica, to cover the point about event misses, as I think it's possible for the new Pod to acquire leadership before the existing non-leading replica does. 
  
  That being said, I agree it's a testing detail, that departs from the real-work scenario, so we could have a mechanism that retries the leader termination, if leadership hasn't been acquired by the existing replica.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-15T15:59:59Z

- **pkg/controller/core/leader_aware_reconciler.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-15T15:59:59Z
```diff
@@ -0,0 +1,88 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	if le := cfg.LeaderElection; le != nil {
+		zero := metav1.Duration{}
+		if duration := le.LeaseDuration; duration != zero {
+			requeueDuration = duration.Duration
+		}
```

  > I haven't been able to find where we default the value for cfg.LeaderElection.LeaseDuration.Duration.
  
  We default the leaderElection parameters here:
  
  https://github.com/kubernetes-sigs/kueue/blob/727e0b522e0bd3e1a785be13430fcf29a34b3393/apis/config/v1beta1/defaults.go#L76
  
  > Maybe we should call RecommendedDefaultLeaderElectionConfiguration from component-base when we initialise the configuration, but I'd rather to that in a separate PR, WDYT?
  
  You mean the below helper, right? This should work fine. I'm ok with a separate PR.
  
  https://github.com/kubernetes/component-base/blob/d5ddea0e47af10468dcd90e09768b2661caf23f7/config/v1alpha1/defaults.go#L35

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-15T16:01:46Z

lgtm except for e2e.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-15T17:29:23Z

- **pkg/controller/core/leader_aware_reconciler.go:58** by [@astefanutti](https://github.com/astefanutti) — 2024-01-15T17:29:23Z
```diff
@@ -0,0 +1,88 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	if le := cfg.LeaderElection; le != nil {
+		zero := metav1.Duration{}
+		if duration := le.LeaseDuration; duration != zero {
+			requeueDuration = duration.Duration
+		}
```

  > We default the leaderElection parameters here
  
  I've seen that, but I fail to see how this would prevent `cfg.LeaderElection` from being `nil`. Am I missing something?
  
  > You mean the below helper, right? This should work fine. I'm ok with a separate PR.
  
  Right. I'll open a separate PR ASAP then.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-17T11:38:58Z

- **pkg/controller/core/leader_aware_reconciler.go:84** by [@mimowo](https://github.com/mimowo) — 2024-01-17T11:38:57Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// defaultRequeueDuration defaults the duration used by non-leading replicas
+// to requeue events, so no events are missed over the period it takes for
+// leader election to fail over a new replica.
+const defaultRequeueDuration = 15 * time.Second
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, obj client.Object, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := defaultRequeueDuration
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		client:          mgr.GetClient(),
+		delegate:        reconciler,
+		object:          obj,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	client          client.Client
+	delegate        reconcile.Reconciler
+	object          client.Object
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
```

  sgtm, I don't have a strong preference as long as we outline the reason, and being as close as possible to the real-life usage is a plus for e2e tests

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-17T13:45:15Z

- **pkg/controller/core/leader_aware_reconciler.go:58** by [@astefanutti](https://github.com/astefanutti) — 2024-01-17T13:45:15Z
```diff
@@ -0,0 +1,88 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	if le := cfg.LeaderElection; le != nil {
+		zero := metav1.Duration{}
+		if duration := le.LeaseDuration; duration != zero {
+			requeueDuration = duration.Duration
+		}
```

  > You mean the below helper, right? This should work fine. I'm ok with a separate PR.
  
  I've created #1598. I can rebase this one on top once it's merged.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-25T19:23:06Z

- **pkg/controller/core/leader_aware_reconciler.go:84** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T19:23:06Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// defaultRequeueDuration defaults the duration used by non-leading replicas
+// to requeue events, so no events are missed over the period it takes for
+// leader election to fail over a new replica.
+const defaultRequeueDuration = 15 * time.Second
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, obj client.Object, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := defaultRequeueDuration
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		client:          mgr.GetClient(),
+		delegate:        reconciler,
+		object:          obj,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	client          client.Client
+	delegate        reconcile.Reconciler
+	object          client.Object
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
```

  I'm actually happy with the simplicity of this implementation.
  
  I think we can leave the E2E test for a follow up.
  
  /hold cancel
  /lgtm
  /approve

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-26T15:12:26Z

- **pkg/controller/core/leader_aware_reconciler.go:84** by [@mimowo](https://github.com/mimowo) — 2024-01-26T15:12:26Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// defaultRequeueDuration defaults the duration used by non-leading replicas
+// to requeue events, so no events are missed over the period it takes for
+// leader election to fail over a new replica.
+const defaultRequeueDuration = 15 * time.Second
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, obj client.Object, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := defaultRequeueDuration
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		client:          mgr.GetClient(),
+		delegate:        reconciler,
+		object:          obj,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	client          client.Client
+	delegate        reconcile.Reconciler
+	object          client.Object
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
```

  @astefanutti you may want to open an issue for the follow up test, so that we don't forget about it? Unless you are about to open the PR, then probably it does not matter.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-26T16:24:33Z

- **pkg/controller/core/leader_aware_reconciler.go:58** by [@astefanutti](https://github.com/astefanutti) — 2024-01-26T16:24:33Z
```diff
@@ -0,0 +1,88 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := 15 * time.Second
+	// Otherwise used the configured lease duration for the manager
+	if le := cfg.LeaderElection; le != nil {
+		zero := metav1.Duration{}
+		if duration := le.LeaseDuration; duration != zero {
+			requeueDuration = duration.Duration
+		}
```

  Addressed in #1652.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-26T16:42:51Z

- **pkg/controller/core/leader_aware_reconciler.go:84** by [@astefanutti](https://github.com/astefanutti) — 2024-01-26T16:42:51Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+)
+
+// defaultRequeueDuration defaults the duration used by non-leading replicas
+// to requeue events, so no events are missed over the period it takes for
+// leader election to fail over a new replica.
+const defaultRequeueDuration = 15 * time.Second
+
+// WithLeadingManager returns a decorating reconcile.Reconciler that discards reconciliation requests
+// for the controllers that are started with the controller.Options.NeedLeaderElection
+// option set to false in non-leading replicas.
+//
+// Starting controllers in non-leading replicas is needed for these that update the data
+// served by the visibility extension API server.
+//
+// This enables to:
+//   - Keep the scheduling decisions under the responsibility of the leading replica alone,
+//     to prevent any concurrency issues.
+//   - Consume requests from the watch event queues, to prevent them from growing indefinitely
+//     in the non-leading replicas.
+//   - Transition to actually reconciling requests in the replica that may acquire
+//     the leader election lease, in case the previously leading replica failed to renew it.
+func WithLeadingManager(mgr ctrl.Manager, reconciler reconcile.Reconciler, obj client.Object, cfg *config.Configuration) reconcile.Reconciler {
+	// Do not decorate the reconciler if leader election is disabled
+	if cfg.LeaderElection == nil || !ptr.Deref(cfg.LeaderElection.LeaderElect, false) {
+		return reconciler
+	}
+
+	// Default to the recommended lease duration, that's used for core components
+	requeueDuration := defaultRequeueDuration
+	// Otherwise used the configured lease duration for the manager
+	zero := metav1.Duration{}
+	if duration := cfg.LeaderElection.LeaseDuration; duration != zero {
+		requeueDuration = duration.Duration
+	}
+
+	return &leaderAwareReconciler{
+		elected:         mgr.Elected(),
+		client:          mgr.GetClient(),
+		delegate:        reconciler,
+		object:          obj,
+		requeueDuration: requeueDuration,
+	}
+}
+
+type leaderAwareReconciler struct {
+	elected         <-chan struct{}
+	client          client.Client
+	delegate        reconcile.Reconciler
+	object          client.Object
+	requeueDuration time.Duration
+}
+
+var _ reconcile.Reconciler = (*leaderAwareReconciler)(nil)
+
+func (r *leaderAwareReconciler) Reconcile(ctx context.Context, request reconcile.Request) (reconcile.Result, error) {
```

  @mimowo sure, I've just created #1655 to track it.
