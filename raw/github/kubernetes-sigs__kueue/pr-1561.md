# PR #1561: Fix preemption within cohort when there is no borrowingLimit

**Summary**: Fix preemption within cohort when there is no borrowingLimit

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1561

**Last updated**: 2024-02-10T11:48:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `borrowing-while-preemption-fix` → `main`
- **Created**: 2024-01-09T08:07:35Z
- **Updated**: 2024-02-10T11:48:06Z
- **Merged**: 2024-01-09T19:18:06Z
- **Closed**: 2024-01-09T19:18:06Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Changed files**: 2   **+69 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

To fix handling of preemption within a cohort when there is no borrowingLimit. In that case
during preemption the available resources coming from borrowing were calculated as if borrowingLimit=0.


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix handling of preemption within a cohort when there is no borrowingLimit. In that case,
during preemption, the permitted resources to borrow were calculated as if borrowingLimit=0, instead of unlimited.

As a consequence, when using `reclaimWithinCohort`, it was possible that a workload, scheduled to ClusterQueue with no borrowingLimit, would preempt more workloads than needed, even though it could fit by borrowing.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-09T08:07:42Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a550f53f63796595dce5406ca194a79863976f48 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/659d8aa02357770008a83d18 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-09T11:42:40Z

/hold
To investigate a potential bug

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-09T12:04:59Z

/hold cancel
Should be fixed

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-09T12:48:46Z

cc @yaroslava-serdiuk @alculquicondor

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-01-09T14:44:22Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-09T14:44:30Z

LGTM label has been added.  <details>Git tree hash: 27ef62c50e345742b36da1faeb527364b7e42818</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T16:38:33Z

/release-note-edit
```release-note
NONE
```

Because this is a fix for an unreleased feature.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T16:41:36Z

My bad, this is about an existing feature

/release-note-edit
```release-note
Fix handling of preemption within a cohort when there is no borrowingLimit. In that case
during preemption the available resources coming from borrowing were calculated as if borrowingLimit=0.

As a consequence, when using `borrowWithinCohort` it was possible that a workload scheduled to ClusterQueue with no borrowingLimit would not be scheduled, even though preemption while borrowing could let it schedule.

Also, when using `reclaimWithinCohort` it was possible that a workload, scheduled to ClusterQueue with no borrowingLimit, would preempt more workloads than needed, even though it could fit by borrowing. 
```

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-09T17:37:30Z

> My bad, this is about an existing feature

Indeed, this is a bug that affects both the existing feature and the new one. I can clean up the release note to only mention the existing feature.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T19:12:42Z

/release-note-edit
```release-note
Fix handling of preemption within a cohort when there is no borrowingLimit. In that case,
during preemption, the permitted resources to borrow were calculated as if borrowingLimit=0, instead of unlimited.

As a consequence, when using `reclaimWithinCohort`, it was possible that a workload, scheduled to ClusterQueue with no borrowingLimit, would preempt more workloads than needed, even though it could fit by borrowing.
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-09T19:15:21Z

LGTM label has been added.  <details>Git tree hash: 20e4cbe3c067b7a7df474769606211a577266ea9</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T19:15:40Z

Don't forget to create a separate PR for 0.5

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T19:15:46Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-09T19:15:59Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1561#issuecomment-1883636058" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1561#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-09T15:00:20Z

/assign @alculquicondor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-09T16:53:57Z

We'll need to make a separate cherry-pick for 0.5

- **pkg/scheduler/preemption/preemption_test.go:572** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T16:40:02Z
```diff
@@ -540,6 +564,37 @@ func TestPreemption(t *testing.T) {
 			}),
 			wantPreempted: sets.New("/c1-low"),
 		},
+		"preempting locally and borrowing same resource in cohort; no borrowing limit in the cohort": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("d1-med", "").
+					Priority(0).
+					Request(corev1.ResourceCPU, "4").
+					ReserveQuota(utiltesting.MakeAdmission("d1").Assignment(corev1.ResourceCPU, "default", "4000m").Obj()).
```

  ```suggestion
  					ReserveQuota(utiltesting.MakeAdmission("d1").Assignment(corev1.ResourceCPU, "default", "4").Obj()).
  ```

- **pkg/scheduler/preemption/preemption.go:366** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T16:53:20Z
```diff
@@ -361,20 +361,26 @@ func workloadFits(wlReq cache.FlavorResourceQuantities, cq *cache.ClusterQueue,
 				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
 			}
 			for rName, rReq := range flvReq {
-				limit := flvQuotas.Resources[rName].Nominal
-
-				// No need to check whether cohort is nil here because when borrowingLimit
-				// is non nil, cohort is always non nil.
-				if allowBorrowing && flvQuotas.Resources[rName].BorrowingLimit != nil {
-					limit += *flvQuotas.Resources[rName].BorrowingLimit
-				}
-
-				if cqResUsage[rName]+rReq > limit {
-					return false
+				resource := flvQuotas.Resources[rName]
+
+				if cq.Cohort != nil {
+					if allowBorrowing {
+						// When resource.BorrowingLimit == nil there is no borrowing
+						// limit, so we can skip the check.
+						if resource.BorrowingLimit != nil {
+							if cqResUsage[rName]+rReq > resource.Nominal+*resource.BorrowingLimit {
+								return false
+							}
+						}
+					}
+					if cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
+						return false
+					}
 				}
-
-				if cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
-					return false
+				if cq.Cohort == nil || !allowBorrowing {
```

  This check should probably go first:
  
  ```
  if cohort == nil || !allowBorrowing {
    check against nominal
  } else {
    limit := cohortRequestable
    if borrowingLimit != nil {
      limit = min(limit, nominal+borrowingLimit)
    }
    check against limit
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-09T17:36:03Z

- **pkg/scheduler/preemption/preemption.go:366** by [@mimowo](https://github.com/mimowo) — 2024-01-09T17:36:02Z
```diff
@@ -361,20 +361,26 @@ func workloadFits(wlReq cache.FlavorResourceQuantities, cq *cache.ClusterQueue,
 				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
 			}
 			for rName, rReq := range flvReq {
-				limit := flvQuotas.Resources[rName].Nominal
-
-				// No need to check whether cohort is nil here because when borrowingLimit
-				// is non nil, cohort is always non nil.
-				if allowBorrowing && flvQuotas.Resources[rName].BorrowingLimit != nil {
-					limit += *flvQuotas.Resources[rName].BorrowingLimit
-				}
-
-				if cqResUsage[rName]+rReq > limit {
-					return false
+				resource := flvQuotas.Resources[rName]
+
+				if cq.Cohort != nil {
+					if allowBorrowing {
+						// When resource.BorrowingLimit == nil there is no borrowing
+						// limit, so we can skip the check.
+						if resource.BorrowingLimit != nil {
+							if cqResUsage[rName]+rReq > resource.Nominal+*resource.BorrowingLimit {
+								return false
+							}
+						}
+					}
+					if cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
+						return false
+					}
 				}
-
-				if cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
-					return false
+				if cq.Cohort == nil || !allowBorrowing {
```

  When `cohort != nil`, but regardless of `allowBorrowing` we need to check this (because allowBorrowing is CQ-scoped, but there might be other CQs borrowing already):
  ```golang
  	if cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
  	   return false
  	}
  ```
  Yes, we could do something like 
  ```
    limit := cohortRequestable
    if borrowingLimit != nil {
      limit = min(limit, nominal+borrowingLimit)
    }
    ```
    but the point is that if `borrowingLimit == nil` then we don't do the check against the limit, which was source of the bug actually, so I wanted to make it step-by-step. 
    
    Still, I'm open to suggestions to improve readability, just pointing out it is not as simple as it may seem from the pseudocode.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-09T17:44:04Z

- **pkg/scheduler/preemption/preemption.go:366** by [@mimowo](https://github.com/mimowo) — 2024-01-09T17:44:04Z
```diff
@@ -361,20 +361,26 @@ func workloadFits(wlReq cache.FlavorResourceQuantities, cq *cache.ClusterQueue,
 				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
 			}
 			for rName, rReq := range flvReq {
-				limit := flvQuotas.Resources[rName].Nominal
-
-				// No need to check whether cohort is nil here because when borrowingLimit
-				// is non nil, cohort is always non nil.
-				if allowBorrowing && flvQuotas.Resources[rName].BorrowingLimit != nil {
-					limit += *flvQuotas.Resources[rName].BorrowingLimit
-				}
-
-				if cqResUsage[rName]+rReq > limit {
-					return false
+				resource := flvQuotas.Resources[rName]
+
+				if cq.Cohort != nil {
+					if allowBorrowing {
+						// When resource.BorrowingLimit == nil there is no borrowing
+						// limit, so we can skip the check.
+						if resource.BorrowingLimit != nil {
+							if cqResUsage[rName]+rReq > resource.Nominal+*resource.BorrowingLimit {
+								return false
+							}
+						}
+					}
+					if cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
+						return false
+					}
 				}
-
-				if cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
-					return false
+				if cq.Cohort == nil || !allowBorrowing {
```

  Just pushed an attempt to make it more readable sort of meeting in the middle with the suggestion. PTAL.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-09T17:55:09Z

- **pkg/scheduler/preemption/preemption.go:380** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-09T17:55:09Z
```diff
@@ -361,20 +361,25 @@ func workloadFits(wlReq cache.FlavorResourceQuantities, cq *cache.ClusterQueue,
 				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
 			}
 			for rName, rReq := range flvReq {
-				limit := flvQuotas.Resources[rName].Nominal
-
-				// No need to check whether cohort is nil here because when borrowingLimit
-				// is non nil, cohort is always non nil.
-				if allowBorrowing && flvQuotas.Resources[rName].BorrowingLimit != nil {
-					limit += *flvQuotas.Resources[rName].BorrowingLimit
-				}
-
-				if cqResUsage[rName]+rReq > limit {
-					return false
+				resource := flvQuotas.Resources[rName]
+
+				if cq.Cohort == nil || !allowBorrowing {
+					if cqResUsage[rName]+rReq > resource.Nominal {
+						return false
+					}
+				} else {
+					// When resource.BorrowingLimit == nil there is no borrowing
+					// limit, so we can skip the check.
+					if resource.BorrowingLimit != nil {
+						if cqResUsage[rName]+rReq > resource.Nominal+*resource.BorrowingLimit {
+							return false
+						}
+					}
 				}
-
-				if cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
-					return false
+				if cq.Cohort != nil {
+					if cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
```

  This can go inside the `else` above.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-09T18:04:30Z

- **pkg/scheduler/preemption/preemption.go:380** by [@mimowo](https://github.com/mimowo) — 2024-01-09T18:04:30Z
```diff
@@ -361,20 +361,25 @@ func workloadFits(wlReq cache.FlavorResourceQuantities, cq *cache.ClusterQueue,
 				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
 			}
 			for rName, rReq := range flvReq {
-				limit := flvQuotas.Resources[rName].Nominal
-
-				// No need to check whether cohort is nil here because when borrowingLimit
-				// is non nil, cohort is always non nil.
-				if allowBorrowing && flvQuotas.Resources[rName].BorrowingLimit != nil {
-					limit += *flvQuotas.Resources[rName].BorrowingLimit
-				}
-
-				if cqResUsage[rName]+rReq > limit {
-					return false
+				resource := flvQuotas.Resources[rName]
+
+				if cq.Cohort == nil || !allowBorrowing {
+					if cqResUsage[rName]+rReq > resource.Nominal {
+						return false
+					}
+				} else {
+					// When resource.BorrowingLimit == nil there is no borrowing
+					// limit, so we can skip the check.
+					if resource.BorrowingLimit != nil {
+						if cqResUsage[rName]+rReq > resource.Nominal+*resource.BorrowingLimit {
+							return false
+						}
+					}
 				}
-
-				if cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
-					return false
+				if cq.Cohort != nil {
+					if cohortResUsage[rName]+rReq > cohortResRequestable[rName] {
```

  moved

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-09T19:15:16Z

/lgtm
