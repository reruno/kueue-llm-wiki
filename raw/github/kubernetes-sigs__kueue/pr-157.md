# PR #157: Add task for run jobs

**Summary**: Add task for run jobs

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/157

**Last updated**: 2022-03-30T17:40:46Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `doc` → `main`
- **Created**: 2022-03-28T20:12:16Z
- **Updated**: 2022-03-30T17:40:46Z
- **Merged**: 2022-03-29T14:21:49Z
- **Closed**: 2022-03-29T14:21:49Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 4   **+215 / -7**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Shows how to run jobs and monitor them

#### Which issue(s) this PR fixes:

Part of #64 

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-28T20:12:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/157#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-28T20:12:31Z

cc @cortespao

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-28T20:13:27Z

/hold
to prevent premature merge

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-28T20:33:45Z

/retest
opened #158 to investigate

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-29T13:30:48Z

squash pls

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-29T13:44:02Z

done

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-29T13:52:04Z

/retest

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-29T14:04:00Z

/lgtm

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-29T14:20:39Z

/hold cancel

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-28T22:43:49Z

- **docs/tasks/README.md:18** by [@ahg-g](https://github.com/ahg-g) — 2022-03-28T22:36:48Z
```diff
@@ -3,6 +3,21 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer Cluster Quotas](administer_cluster_quotas.md)
+
+Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
+
+### As a batch user
```

  is this supposed to be `##` instead of three?

- **docs/tasks/README.md:23** by [@ahg-g](https://github.com/ahg-g) — 2022-03-28T22:37:29Z
```diff
@@ -3,6 +3,21 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer Cluster Quotas](administer_cluster_quotas.md)
+
+Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
+
+### As a batch user
+
+A _batch user_ runs [workloads](/docs/concepts/queued_workload.md). A typical
+batch user is a researcher, AI/ML engineer, data scientist, among others.
+
+### [Run Jobs](run_jobs.md)
```

  why is this a header with nothing underneath it?

- **docs/tasks/run_jobs.md:40** by [@ahg-g](https://github.com/ahg-g) — 2022-03-28T22:39:23Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
```

  why limits is required?

- **docs/tasks/run_jobs.md:103** by [@ahg-g](https://github.com/ahg-g) — 2022-03-28T22:40:37Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
```

  ```suggestion
  If the ClusterQueue doesn't have enough quota to run the workload, the output will
  ```

- **docs/tasks/run_jobs.md:103** by [@ahg-g](https://github.com/ahg-g) — 2022-03-28T22:41:07Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
```

  or 
  
  ```suggestion
  If there is no enough quota to run the workload, the output will
  ```

- **docs/tasks/run_jobs.md:128** by [@ahg-g](https://github.com/ahg-g) — 2022-03-28T22:41:38Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
+be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the cluster has enough quota to run the workload,
```

  ```suggestion
  After some time, when there is enough quota to run the workload,
  ```

- **docs/tasks/run_jobs.md:136** by [@ahg-g](https://github.com/ahg-g) — 2022-03-28T22:42:54Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
+be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the cluster has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main    cluster-total   45s
+```
+
+And when you run `kubectl -n default describe sample-job-sl4bm` again, you will
```

  is this missing `queuedworkloads` after describe?

- **docs/tasks/run_jobs.md:126** by [@ahg-g](https://github.com/ahg-g) — 2022-03-28T22:43:04Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
+be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the cluster has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
```

  workloads or queuedworkloads?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-29T13:26:49Z

- **docs/tasks/README.md:18** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-29T13:19:47Z
```diff
@@ -3,6 +3,21 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer Cluster Quotas](administer_cluster_quotas.md)
+
+Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
+
+### As a batch user
```

  yes

- **docs/tasks/README.md:23** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-29T13:19:59Z
```diff
@@ -3,6 +3,21 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer Cluster Quotas](administer_cluster_quotas.md)
+
+Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
+
+### As a batch user
+
+A _batch user_ runs [workloads](/docs/concepts/queued_workload.md). A typical
+batch user is a researcher, AI/ML engineer, data scientist, among others.
+
+### [Run Jobs](run_jobs.md)
```

  I forgot :)

- **docs/tasks/run_jobs.md:40** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-29T13:22:03Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
```

  Removed

- **docs/tasks/run_jobs.md:103** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-29T13:23:36Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
```

  let's go with ClusterQueue

- **docs/tasks/run_jobs.md:128** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-29T13:24:27Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
+be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the cluster has enough quota to run the workload,
```

  said ClusterQueue again

- **docs/tasks/run_jobs.md:126** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-29T13:25:00Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
+be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the cluster has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
```

  either works (I added a shortname). Should I use the long name?

- **docs/tasks/run_jobs.md:136** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-29T13:25:43Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
+be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the cluster has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main    cluster-total   45s
+```
+
+And when you run `kubectl -n default describe sample-job-sl4bm` again, you will
```

  yes :)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-29T13:30:07Z

- **docs/tasks/run_jobs.md:126** by [@ahg-g](https://github.com/ahg-g) — 2022-03-29T13:30:07Z
```diff
@@ -0,0 +1,193 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests and limits for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+          limits:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the cluster doesn't have enough quota to run the workload, the output will
+be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the cluster has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
```

  it is fine, I just didn't know that we can use both

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-03-29T20:29:22Z

- **docs/tasks/README.md:8** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:29:22Z
```diff
@@ -3,6 +3,23 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
```

  The following section includes the tasks available for the following user personas:
  
  * Batch administrators
  * Batch users

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-03-29T20:32:51Z

- **docs/tasks/README.md:17** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:32:50Z
```diff
@@ -3,6 +3,23 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer cluster quotas](administer_cluster_quotas.md)
+
+Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
+
```

  Replace [14 - 17]:
  As a batch administator, learn how to [administer quotas in a cluster](administer_cluster_quotas.md) with `Queues` and `ClusterQueue`.

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-03-29T20:35:01Z

- **docs/tasks/README.md:25** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:35:01Z
```diff
@@ -3,6 +3,23 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer cluster quotas](administer_cluster_quotas.md)
+
+Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
+
+## As a batch user
+
+A _batch user_ runs [workloads](/docs/concepts/queued_workload.md). A typical
+batch user is a researcher, AI/ML engineer, data scientist, among others.
+
+### [Run Jobs](run_jobs.md)
+
+Learn how to run a Job on a cluster managed with Kueue.
```

  As a batch user, learn how to [run a Job on a cluster](run_jobs.md) managed with Kueue.

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-03-29T20:35:17Z

- **docs/tasks/README.md:14** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:35:16Z
```diff
@@ -3,6 +3,23 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer cluster quotas](administer_cluster_quotas.md)
```

  You can remove this H3

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-03-29T20:35:22Z

- **docs/tasks/README.md:18** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:35:22Z
```diff
@@ -3,6 +3,23 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer cluster quotas](administer_cluster_quotas.md)
+
+Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
+
+## As a batch user
```

  You can remove this H3

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-03-29T21:23:23Z

Editorial review. Please, feel free to disregard any comment that might affect technical completeness.
Happy to discuss if needed 😃

- **docs/tasks/README.md:8** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:38:27Z
```diff
@@ -3,6 +3,23 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
```

  Replace [3 - 8]
  
  The following  tasks show you how to perform operations based on the Kueue user personas such as  _batch administrators_ and _batch users_.

- **docs/tasks/README.md:17** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:39:53Z
```diff
@@ -3,6 +3,23 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer cluster quotas](administer_cluster_quotas.md)
+
+Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
+
```

  Replace [13 - 17]
  As a batch administrator, you can learn how to [administer cluster quotas](administer_cluster_quotas.md) with `Queues` and `ClusterQueues`.

- **docs/tasks/README.md:25** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:40:50Z
```diff
@@ -3,6 +3,23 @@
 This section contains pages that show how to perform individual tasks.
 A task page shows how to do an operation with a short sequence of steps.
 
-## [Administer Cluster Resources](administer_cluster_resources.md)
+The tasks are designed to be followed by two personas: _batch administrators_
+and _batch users_.
 
-Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
\ No newline at end of file
+## As a batch administrator
+
+A _batch administrator_ manages the cluster infrastructure and establishes
+quotas and queues.
+
+### [Administer cluster quotas](administer_cluster_quotas.md)
+
+Learn how to setup Queues and ClusterQueues to administer quotas in a cluster.
+
+## As a batch user
+
+A _batch user_ runs [workloads](/docs/concepts/queued_workload.md). A typical
+batch user is a researcher, AI/ML engineer, data scientist, among others.
+
+### [Run Jobs](run_jobs.md)
+
+Learn how to run a Job on a cluster managed with Kueue.
```

  Replace [23 - 25]
  As a batch user, you can learn how to [run Jobs](run_jobs.md) on a cluster managed with Kueue.

- **docs/tasks/run_jobs.md:3** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:41:58Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
```

  ~show~ shows

- **docs/tasks/administer_cluster_quotas.md:3** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:48:24Z
```diff
@@ -3,7 +3,7 @@
 This page shows you how to represent your cluster resource quotas and to
```

  I will suggest we use another verb instead of "represent"
  Maybe it could be "manage" or "define"?
  
  If represent is the right term, please disregard.

- **docs/tasks/administer_cluster_quotas.md:9** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:51:12Z
```diff
@@ -3,7 +3,7 @@
 This page shows you how to represent your cluster resource quotas and to
 establish fair sharing rules among the tenants.
 
-The intended audience for this page are cluster administrators.
+The intended audience for this page are [batch administrators](/docs/tasks/README.md#as-a-batch-administrator).
 
 ## Before you begin
 
```

  Although this change is not part of the commit scope, I would suggest:
  
  ## Before you begin
  
  Make sure the following conditions are met:
  
  * A Kubernetes cluster is running.
  * The kubectl command-line tool has communication with your cluster.
  * [Kueue is installed](/README.md#installation).

- **docs/tasks/run_jobs.md:13** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:53:15Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
```

  ## Before you begin
  
  Make sure the following conditions are met:
  
  * A Kubernetes cluster is running.
  * The kubectl command-line tool has communication with your cluster.
  * [Kueue is installed](/README.md#installation).
  * Your cluster has [quotas configured](administer_cluster_quotas.md).

- **docs/tasks/run_jobs.md:5** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:55:47Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
```

  Missing period "."

- **docs/tasks/run_jobs.md:22** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:56:33Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
```

  The output is similar to the following:

- **docs/tasks/run_jobs.md:34** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:58:27Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
```

  ~is not much different~ is similar to

- **docs/tasks/run_jobs.md:35** by [@cortespao](https://github.com/cortespao) — 2022-03-29T20:59:18Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
```

  ~The differences are:~
  However, you must consider the following differences:

- **docs/tasks/run_jobs.md:37** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:00:13Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
```

  Instead of the final "it", could it be "a job"?

- **docs/tasks/run_jobs.md:38** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:01:01Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
```

  ~This is done through~ Use

- **docs/tasks/run_jobs.md:39** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:01:12Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
```

  the `kueue.x-k8s.io/queue-name` annotation

- **docs/tasks/run_jobs.md:50** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:12:45Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
```

  Is it normal that the name ends with "-"?

- **docs/tasks/run_jobs.md:85** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:13:28Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
```

  The output will be similar to the following:

- **docs/tasks/run_jobs.md:94** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:14:06Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
```

  You can see the workload status with the following command:

- **docs/tasks/run_jobs.md:101** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:14:26Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the ClusterQueue doesn't have enough quota to run the workload, the output
+will be similar to:
```

  will be similar to the following:

- **docs/tasks/run_jobs.md:126** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:16:03Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the ClusterQueue doesn't have enough quota to run the workload, the output
+will be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the ClusterQueue has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
```

  When the ClusterQueue has enough quota, the cluster will run the workload.
  
  To see active workloads, run the following command
   `kubectl -n default get workloads` 
  
  The output is similar to the following:

- **docs/tasks/run_jobs.md:135** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:19:15Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the ClusterQueue doesn't have enough quota to run the workload, the output
+will be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the ClusterQueue has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main    cluster-total   45s
+```
+
+And when you run `kubectl -n default describe workload sample-job-sl4bm` again,
+you will observe an event for the workload admission.
+
```

  To view an event for the workload admission, run the following command:
  
   `kubectl -n default describe workload sample-job-sl4bm`
  
  The output is similar to the following:

- **docs/tasks/run_jobs.md:146** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:20:28Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the ClusterQueue doesn't have enough quota to run the workload, the output
+will be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the ClusterQueue has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main    cluster-total   45s
+```
+
+And when you run `kubectl -n default describe workload sample-job-sl4bm` again,
+you will observe an event for the workload admission.
+
+```
+...
+Events:
+  Type    Reason    Age   From           Message
+  ----    ------    ----  ----           -------
+  Normal  Admitted  50s   kueue-manager  Admitted by ClusterQueue cluster-total
+```
+
+Finally, once the workload has finished executing, you will observe a status
+condition.
+
```

  What is the command the user must run to see this output?

- **docs/tasks/run_jobs.md:161** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:20:55Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the ClusterQueue doesn't have enough quota to run the workload, the output
+will be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the ClusterQueue has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main    cluster-total   45s
+```
+
+And when you run `kubectl -n default describe workload sample-job-sl4bm` again,
+you will observe an event for the workload admission.
+
+```
+...
+Events:
+  Type    Reason    Age   From           Message
+  ----    ------    ----  ----           -------
+  Normal  Admitted  50s   kueue-manager  Admitted by ClusterQueue cluster-total
+```
+
+Finally, once the workload has finished executing, you will observe a status
+condition.
+
+```
+...
+Status:
+  Conditions:
+    ...
+    Last Probe Time:       2022-03-28T19:43:37Z                                                                                                                      
+    Last Transition Time:  2022-03-28T19:43:37Z                                                                                                                      
+    Message:               Job finished successfully                                                                                                                 
+    Reason:                JobFinished                                                                                                                               
+    Status:                True                                                                                                                                      
+    Type:                  Finished
+...
+```
+
+You can find other details in the Job status by running:
```

  To review more details about the Job status, run the following command:

- **docs/tasks/run_jobs.md:167** by [@cortespao](https://github.com/cortespao) — 2022-03-29T21:21:13Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
+    kueue.x-k8s.io/queue-name: main
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+        args: ["30s"]
+        resources:
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      restartPolicy: Never
+```
+
+## 2. Run the Job
+
+You can run the Job with the following command:
+
+```shell
+kubectl create -f sample-job.yaml
+```
+
+Internally, Kueue will create a corresponding [QueuedWorkload](/docs/concepts/queued_workload.md)
+for this Job with a matching name.
+
+```shell
+kubectl -n default get workloads
+```
+
+The output will be similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main                    1s
+```
+
+## 3. (Optional) Monitor the status of the workload
+
+You can run the following command to see the status of the workload.
+
+```shell
+kubectl -n default describe workload sample-job-sl4bm
+```
+
+If the ClusterQueue doesn't have enough quota to run the workload, the output
+will be similar to:
+
+```
+Name:         sample-job-jtz5f                                                                                                                                       
+Namespace:    default                                                                                                                                                
+Labels:       <none>                                                                                                                                                 
+Annotations:  <none>                                                                                                                                                 
+API Version:  kueue.x-k8s.io/v1alpha1                                                                                                                                
+Kind:         QueuedWorkload                                                                                                                                         
+Metadata:
+  ...
+Spec:
+  ...
+Status:
+  Conditions:
+    Last Probe Time:       2022-03-28T19:43:03Z
+    Last Transition Time:  2022-03-28T19:43:03Z
+    Message:               workload didn't fit
+    Reason:                Pending
+    Status:                False
+    Type:                  Admitted
+Events:               <none>
+```
+
+After some time, when the ClusterQueue has enough quota to run the workload,
+the output of `kubectl -n default get workloads` will look similar to:
+
+```
+NAME               QUEUE   ADMITTED BY     AGE
+sample-job-sl4bm   main    cluster-total   45s
+```
+
+And when you run `kubectl -n default describe workload sample-job-sl4bm` again,
+you will observe an event for the workload admission.
+
+```
+...
+Events:
+  Type    Reason    Age   From           Message
+  ----    ------    ----  ----           -------
+  Normal  Admitted  50s   kueue-manager  Admitted by ClusterQueue cluster-total
+```
+
+Finally, once the workload has finished executing, you will observe a status
+condition.
+
+```
+...
+Status:
+  Conditions:
+    ...
+    Last Probe Time:       2022-03-28T19:43:37Z                                                                                                                      
+    Last Transition Time:  2022-03-28T19:43:37Z                                                                                                                      
+    Message:               Job finished successfully                                                                                                                 
+    Reason:                JobFinished                                                                                                                               
+    Status:                True                                                                                                                                      
+    Type:                  Finished
+...
+```
+
+You can find other details in the Job status by running:
+
+```shell
+kubectl -n default describe job sample-job-sl4bm
+```
+
+The output will be similar to:
```

  The output is similar to the following:

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-30T17:29:13Z

- **docs/tasks/run_jobs.md:50** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-30T17:29:13Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
```

  yes, because the system will add a string at the end, like: "sample-job-abcd"

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-03-30T17:40:46Z

- **docs/tasks/run_jobs.md:50** by [@cortespao](https://github.com/cortespao) — 2022-03-30T17:40:46Z
```diff
@@ -0,0 +1,190 @@
+# Run Jobs
+
+This page show you how to run a Job in a Kubernetes cluster with Kueue enabled.
+
+The intended audience for this page are [batch users](/docs/tasks/README.md#as-a-batch-user)
+
+## Before you begin
+
+You need to have a Kubernetes cluster, the kubectl command-line tool
+must be configured to communicate with your cluster, and [Kueue installed](/README.md#installation).
+
+The cluster should have [quotas configured](administer_cluster_quotas.md).
+
+## 0. Identify the queues available in your namespace
+
+Run the following command to list the Queues available in your namespace.
+
+```shell
+kubectl -n default get queues
+```
+
+The output is similar to:
+
+```
+NAME   CLUSTERQUEUE    PENDING WORKLOADS
+main   cluster-total   3
+```
+
+The [ClusterQueue](/docs/concepts/cluster_queue.md) defines the quotas for the
+Queue.
+
+## 1. Define the Job
+
+Running a Job in Kueue is not much different from [running a Job in a Kubernetes cluster](https://kubernetes.io/docs/tasks/job/)
+without Kueue. The differences are:
+- You should create the Job in a [suspended state](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+  as Kueue will decide when it's the best time to start it.
+- You have to set the Queue you want to submit the Job to. This is done through
+  the annotation `kueue.x-k8s.io/queue-name`.
+- You should include the resource requests for each Job Pod.
+
+Here is a sample Job with three Pods that just sleep for a few seconds.
+This sample is also available in [config/samples/sample-job.yaml](/config/samples/sample-job.yaml).
+
+```yaml
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
```

  TIL this! Thanks.
