# PR #1573: Finalize deleted gated Pods from groups without a Workload

**Summary**: Finalize deleted gated Pods from groups without a Workload

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1573

**Last updated**: 2024-03-08T15:36:34Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `finalize-incomplete-pod-group` → `main`
- **Created**: 2024-01-11T21:49:56Z
- **Updated**: 2024-03-08T15:36:34Z
- **Merged**: 2024-01-12T15:52:16Z
- **Closed**: 2024-01-12T15:52:16Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 2   **+65 / -15**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

If gated Pods are deleted before a Workload is created, they should be finalized.

This covers a scenario where a group was never complete and the user decides to delete the Pods.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1451

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

No release note because this is an unreleased feature.

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-11T21:50:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 63d8812401038f9422ac989e242239d2e05d040b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65a1559acf4de30008f66f82 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-11T21:50:03Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1573#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-12T15:10:22Z

LGTM label has been added.  <details>Git tree hash: 74741df644767a965acd99ee9fb8589e5ad055a9</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T12:39:05Z

I think the logic is ok, mostly wondering about overloading the term of "active" pods.

- **pkg/controller/jobs/pod/pod_controller.go:662** by [@mimowo](https://github.com/mimowo) — 2024-01-12T12:26:10Z
```diff
@@ -648,19 +647,27 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	return nil
 }
 
-// activePods returns a slice of non-failed pods in the group
+// activePods returns a slice of active pods in the group
 func (p *Pod) activePods() []corev1.Pod {
-	// Filter all failed pods
 	activePods := make([]corev1.Pod, 0, len(p.list.Items))
 	for _, pod := range p.list.Items {
-		if pod.Status.Phase != corev1.PodFailed {
+		if isPodActive(&pod) {
 			activePods = append(activePods, pod)
 		}
 	}
 
 	return activePods
 }
 
+// isPodActive returns whether the Pod can eventually run, is Running or Succeeded.
```

  I'm wondering if naming "active" pods can be misleading, especially because this term has a different meaning in k8s: https://github.com/kubernetes/kubernetes/blob/4b94168c0f724bbf1edfd846a8e788a32d9a5c49/pkg/controller/controller_utils.go#L980. 
  
  I would suggest to call the function "isPodRunnable" to also capture the intention.

- **pkg/controller/jobs/pod/pod_controller.go:962** by [@mimowo](https://github.com/mimowo) — 2024-01-12T12:33:45Z
```diff
@@ -948,6 +959,20 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) finalizeInactivePods(ctx context.Context, c client.Client) error {
```

  Similarly here, I don't have a better name to propose, but the "active/inactive" makes it slightly confusing. Maybe "finalizeNonRunnablePods"?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-12T14:19:21Z

- **pkg/controller/jobs/pod/pod_controller.go:662** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T14:19:21Z
```diff
@@ -648,19 +647,27 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	return nil
 }
 
-// activePods returns a slice of non-failed pods in the group
+// activePods returns a slice of active pods in the group
 func (p *Pod) activePods() []corev1.Pod {
-	// Filter all failed pods
 	activePods := make([]corev1.Pod, 0, len(p.list.Items))
 	for _, pod := range p.list.Items {
-		if pod.Status.Phase != corev1.PodFailed {
+		if isPodActive(&pod) {
 			activePods = append(activePods, pod)
 		}
 	}
 
 	return activePods
 }
 
+// isPodActive returns whether the Pod can eventually run, is Running or Succeeded.
```

  Perhaps `IsRunnableOrSucceeded`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-12T14:33:59Z

- **pkg/controller/jobs/pod/pod_controller_test.go:2180** by [@trasc](https://github.com/trasc) — 2024-01-12T14:31:37Z
```diff
@@ -2155,6 +2154,32 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"deleted pods in incomplete group are finalized": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Name("p1").
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("group").
+					GroupTotalCount("3").
+					StatusPhase(corev1.PodPending).
+					Delete().
+					Obj(),
+				*basePodWrapper.
+					Name("p2").
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("group").
+					GroupTotalCount("3").
+					StatusPhase(corev1.PodPending).
+					Delete().
+					Obj(),
```

  ```suggestion
  				*basePodWrapper.
  					Clone().
  					Name("p1").
  					Label("kueue.x-k8s.io/managed", "true").
  					KueueFinalizer().
  					KueueSchedulingGate().
  					Group("group").
  					GroupTotalCount("3").
  					StatusPhase(corev1.PodPending).
  					Delete().
  					Obj(),
  				*basePodWrapper.
  					Clone().
  					Name("p2").
  					Label("kueue.x-k8s.io/managed", "true").
  					KueueFinalizer().
  					KueueSchedulingGate().
  					Group("group").
  					GroupTotalCount("3").
  					StatusPhase(corev1.PodPending).
  					Delete().
  					Obj(),
  ```
  Othewise future tests that are using basePodWrapper may see an unexpected name.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T14:35:41Z

- **pkg/controller/jobs/pod/pod_controller.go:662** by [@mimowo](https://github.com/mimowo) — 2024-01-12T14:35:41Z
```diff
@@ -648,19 +647,27 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 	return nil
 }
 
-// activePods returns a slice of non-failed pods in the group
+// activePods returns a slice of active pods in the group
 func (p *Pod) activePods() []corev1.Pod {
-	// Filter all failed pods
 	activePods := make([]corev1.Pod, 0, len(p.list.Items))
 	for _, pod := range p.list.Items {
-		if pod.Status.Phase != corev1.PodFailed {
+		if isPodActive(&pod) {
 			activePods = append(activePods, pod)
 		}
 	}
 
 	return activePods
 }
 
+// isPodActive returns whether the Pod can eventually run, is Running or Succeeded.
```

  sgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-12T14:56:54Z

- **pkg/controller/jobs/pod/pod_controller_test.go:2180** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T14:56:54Z
```diff
@@ -2155,6 +2154,32 @@ func TestReconciler(t *testing.T) {
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
 		},
+		"deleted pods in incomplete group are finalized": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Name("p1").
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("group").
+					GroupTotalCount("3").
+					StatusPhase(corev1.PodPending).
+					Delete().
+					Obj(),
+				*basePodWrapper.
+					Name("p2").
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("group").
+					GroupTotalCount("3").
+					StatusPhase(corev1.PodPending).
+					Delete().
+					Obj(),
```

  Oops, fixed

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-12T15:10:16Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T15:21:40Z

/lgtm
