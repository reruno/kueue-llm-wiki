# PR #1575: [jobset] Add prebuilt workload support.

**Summary**: [jobset] Add prebuilt workload support.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1575

**Last updated**: 2024-01-15T18:05:15Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `prebuilt-wl-jobset` → `main`
- **Created**: 2024-01-12T12:49:14Z
- **Updated**: 2024-01-15T18:05:15Z
- **Merged**: 2024-01-15T17:28:30Z
- **Closed**: 2024-01-15T17:28:30Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 8   **+118 / -10**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Adds prebuilt workload support for jobset.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add Prebuilt Workload support for JobSets.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-12T12:49:20Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2cf512bf464437b014114a8cedb79847dae8449e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65a4e04faf8d5600097d61aa |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T17:07:56Z

We can do that as a follow up.

This is assigned, but there hasn't been any progress
https://github.com/kubernetes-sigs/kueue/issues/1463

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-12T17:40:25Z

> We can do that as a follow up.
> 
> This is assigned, but there hasn't been any progress
> https://github.com/kubernetes-sigs/kueue/issues/1463

Sounds good, also the enablement of prebuilt support for JobSet will be indirectly tested on higher level when we have full support for JobSet in MK. 
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-12T17:40:32Z

LGTM label has been added.  <details>Git tree hash: c63dcbb47f37e1ca26911345fe19da6551227406</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-15T08:55:22Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-15T08:55:28Z

LGTM label has been added.  <details>Git tree hash: 6a40b56991918632c06494e4d67442fcc42a1257</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-15T17:02:43Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1575#pullrequestreview-1822045777" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1575#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [tenzen-y]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T13:15:08Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:68** by [@mimowo](https://github.com/mimowo) — 2024-01-12T13:04:22Z
```diff
@@ -58,6 +59,20 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	jobSet := fromObject(obj)
 	log := ctrl.LoggerFrom(ctx).WithName("jobset-webhook")
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
+
+	if wlName, hasPrebuilt := jobframework.PrebuiltWorkload(jobSet); hasPrebuilt {
+		for i := range jobSet.Spec.ReplicatedJobs {
+			rjTemplate := &jobSet.Spec.ReplicatedJobs[i].Template
+			if rjTemplate.Annotations == nil {
+				rjTemplate.Annotations = map[string]string{
+					constants.ParentWorkloadAnnotation: wlName,
```

  just initialize the map in this branch to empty, and set the value outside else. I think it would read better.

- **pkg/controller/jobs/job/job_webhook.go:79** by [@mimowo](https://github.com/mimowo) — 2024-01-12T13:05:01Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  Can you explain why this is needed? Especially since it wasn't necessary for the Job, or maybe it was necessary and this is a drive-by fix? If this is a drive-by fix, can we have a dedicated PR to make it clear?

- **pkg/controller/jobs/jobset/jobset_webhook.go:63** by [@mimowo](https://github.com/mimowo) — 2024-01-12T13:08:19Z
```diff
@@ -58,6 +59,20 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	jobSet := fromObject(obj)
 	log := ctrl.LoggerFrom(ctx).WithName("jobset-webhook")
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
+
+	if wlName, hasPrebuilt := jobframework.PrebuiltWorkload(jobSet); hasPrebuilt {
```

  Can you explain why this is needed? Also in a comment. I'm puzzled because we only support parent-workload annotation for Job at the moment. If this is a drive-by fix, or feature please move it to a dedicated PR.

- **pkg/util/testingjobs/jobset/wrappers.go:87** by [@mimowo](https://github.com/mimowo) — 2024-01-12T13:13:58Z
```diff
@@ -81,15 +83,20 @@ func (j *JobSetWrapper) Suspend(s bool) *JobSetWrapper {
 	return j
 }
 
-// Queue updates the queue name of the JobSet.
-func (j *JobSetWrapper) Queue(queue string) *JobSetWrapper {
+// Label sets a label to the JobSet.
+func (j *JobSetWrapper) Label(k, v string) *JobSetWrapper {
```

  Are these changes in use in any tests, or is it WIP to enable adding integration tests?
  
  I would like to have at least a basaline scenario integration test for the prebuilt-workload, similar to the Job.
  
  We care about JobSet, because this is the first Job CRD that will be fully usable in MultiKueue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T13:16:42Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@mimowo](https://github.com/mimowo) — 2024-01-12T13:16:42Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  Also, if this is a fix it needs to be reflected in a release note, because the parent-workload feature is released already.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-12T14:12:50Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@trasc](https://github.com/trasc) — 2024-01-12T13:58:02Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  Comment added.
  
  It's not a fix since is only relevant for prebuilt workloads.

- **pkg/controller/jobs/jobset/jobset_webhook.go:63** by [@trasc](https://github.com/trasc) — 2024-01-12T14:03:56Z
```diff
@@ -58,6 +59,20 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	jobSet := fromObject(obj)
 	log := ctrl.LoggerFrom(ctx).WithName("jobset-webhook")
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
+
+	if wlName, hasPrebuilt := jobframework.PrebuiltWorkload(jobSet); hasPrebuilt {
```

  > I'm puzzled because we only support parent-workload annotation for Job at the moment.
  
  Jobs is the user of the annotation, however Jobsets  are one of the potential owners of such Jobs.
  
  > If this is a drive-by fix, or feature please move it to a dedicated PR.
  
  It's not.

- **pkg/util/testingjobs/jobset/wrappers.go:87** by [@trasc](https://github.com/trasc) — 2024-01-12T14:05:59Z
```diff
@@ -81,15 +83,20 @@ func (j *JobSetWrapper) Suspend(s bool) *JobSetWrapper {
 	return j
 }
 
-// Queue updates the queue name of the JobSet.
-func (j *JobSetWrapper) Queue(queue string) *JobSetWrapper {
+// Label sets a label to the JobSet.
+func (j *JobSetWrapper) Label(k, v string) *JobSetWrapper {
```

  used in `pkg/controller/jobs/jobset/jobset_webhook_test.go`
  
  > I would like to have at least a basaline scenario integration test for the prebuilt-workload, similar to the Job.
  
  The prebuilt workload support is mainly implemented in the jobfamwork and validated in the batch/Job integration tests. The bits that are specific to Jobset are tested in unit tests.

- **pkg/controller/jobs/jobset/jobset_webhook.go:68** by [@trasc](https://github.com/trasc) — 2024-01-12T14:11:06Z
```diff
@@ -58,6 +59,20 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	jobSet := fromObject(obj)
 	log := ctrl.LoggerFrom(ctx).WithName("jobset-webhook")
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
+
+	if wlName, hasPrebuilt := jobframework.PrebuiltWorkload(jobSet); hasPrebuilt {
+		for i := range jobSet.Spec.ReplicatedJobs {
+			rjTemplate := &jobSet.Spec.ReplicatedJobs[i].Template
+			if rjTemplate.Annotations == nil {
+				rjTemplate.Annotations = map[string]string{
+					constants.ParentWorkloadAnnotation: wlName,
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T14:26:50Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@mimowo](https://github.com/mimowo) — 2024-01-12T14:26:50Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  This is tricky scenario, please expand the comment when that would happen? Does it mean that the name of the JobSet on the management cluster, and the worker cluster might be different?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T14:28:54Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@mimowo](https://github.com/mimowo) — 2024-01-12T14:28:54Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  TBH I still don't know when that happens. Can we have an integration test for it? Maybe for now explain what is the user journey leading to it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T14:34:09Z

- **pkg/util/testingjobs/jobset/wrappers.go:87** by [@mimowo](https://github.com/mimowo) — 2024-01-12T14:34:09Z
```diff
@@ -81,15 +83,20 @@ func (j *JobSetWrapper) Suspend(s bool) *JobSetWrapper {
 	return j
 }
 
-// Queue updates the queue name of the JobSet.
-func (j *JobSetWrapper) Queue(queue string) *JobSetWrapper {
+// Label sets a label to the JobSet.
+func (j *JobSetWrapper) Label(k, v string) *JobSetWrapper {
```

  I don't see how the unit tests show correctness of the code changes scattered in webhooks.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-12T14:43:28Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@trasc](https://github.com/trasc) — 2024-01-12T14:43:28Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  > This is tricky scenario, please expand the comment when that would happen? Does it mean that the name of the JobSet on the management cluster, and the worker cluster might be different?
  
  `Prebuilt workload` workload allows us to specify a workload to be used by the Jobset. Indeed we are currently mostly using it in multikueue where the name is the same, however we cannot look at prebuilt workload only from this angle.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-12T14:44:50Z

- **pkg/util/testingjobs/jobset/wrappers.go:87** by [@trasc](https://github.com/trasc) — 2024-01-12T14:44:50Z
```diff
@@ -81,15 +83,20 @@ func (j *JobSetWrapper) Suspend(s bool) *JobSetWrapper {
 	return j
 }
 
-// Queue updates the queue name of the JobSet.
-func (j *JobSetWrapper) Queue(queue string) *JobSetWrapper {
+// Label sets a label to the JobSet.
+func (j *JobSetWrapper) Label(k, v string) *JobSetWrapper {
```

  Both the changes and the tests are in the webhooks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T14:55:49Z

- **pkg/util/testingjobs/jobset/wrappers.go:87** by [@mimowo](https://github.com/mimowo) — 2024-01-12T14:55:49Z
```diff
@@ -81,15 +83,20 @@ func (j *JobSetWrapper) Suspend(s bool) *JobSetWrapper {
 	return j
 }
 
-// Queue updates the queue name of the JobSet.
-func (j *JobSetWrapper) Queue(queue string) *JobSetWrapper {
+// Label sets a label to the JobSet.
+func (j *JobSetWrapper) Label(k, v string) *JobSetWrapper {
```

  Sure, but I think we need some higher level test (maybe e2e) to prove the solution works as a whole. Technically, every PR could be covered with just unit tests, but for changes scattered in various compontents it is good to have integration tests. The integration tests have also the documenting role. They show *why* changes in individual files are needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T15:02:22Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@mimowo](https://github.com/mimowo) — 2024-01-12T15:02:21Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  I still don't understand what is the scenario. Is it that a user manually creates prebuilt workload that does not match the JobSet name? We generally don't support workloads as objects modified by users, and there are probably multiple things that can go wrong. 
  
  If so, this is not needed for MultiKueue, I suggest to separate this out to a dedicated PR.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-12T15:07:34Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@trasc](https://github.com/trasc) — 2024-01-12T15:07:34Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  I don't understand...

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T15:24:37Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@mimowo](https://github.com/mimowo) — 2024-01-12T15:24:37Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  1. Explain what is the scenario step-by-step for which the webhook changes are needed.
  2. Is the support for this scenario needed for MultiKueue?
  3. If not needed for MultiKueue, then it is a corner-case, we can fix is a dedicated PR. Let's focus this PR on the support needed for MultiKueue.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-12T15:35:11Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@trasc](https://github.com/trasc) — 2024-01-12T15:35:11Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  This PR is about using Prebuilt Workloads with JobSets. We need to be able to do this in order to add MultiKueue support for JobSets.
  
  If we want to talk about a corner case here , that will be the fact that in case of MultiKueue, the prebuilt workload in the worker has the same name that we can generate from the JobSets name. But we should not.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T15:49:36Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@mimowo](https://github.com/mimowo) — 2024-01-12T15:49:36Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  Workloads are not user-facing APIs of Kueue, we don't expect users create or manipulate manually workloads, or prebuilt-workloads for that matter. I'm not sure we should support such use cases. WDYT @alculquicondor ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T16:07:09Z

- **pkg/controller/jobframework/validation.go:34** by [@mimowo](https://github.com/mimowo) — 2024-01-12T16:07:09Z
```diff
@@ -31,7 +31,7 @@ var (
 	parentWorkloadKeyPath         = annotationsPath.Key(constants.ParentWorkloadAnnotation)
 	queueNameLabelPath            = labelsPath.Key(constants.QueueLabel)
 	workloadPriorityClassNamePath = labelsPath.Key(constants.WorkloadPriorityClassLabel)
-	supportedPrebuiltWlJobGVKs    = sets.New("batch/v1, Kind=Job")
+	supportedPrebuiltWlJobGVKs    = sets.New("batch/v1, Kind=Job", "jobset.x-k8s.io/v1alpha2, Kind=JobSet")
```

  Is this change actually tested currently?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-12T16:08:31Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T16:08:31Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  I see Prebuilt support as a pre-requisite for MK, but the feature should make sense on its own as well.
  
  I agree that assuming that the workload name matches is kind of arbitrary, so if supporting any name is viable with minimal changes, we could do it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T16:12:56Z

- **pkg/controller/jobs/job/job_webhook.go:79** by [@mimowo](https://github.com/mimowo) — 2024-01-12T16:12:56Z
```diff
@@ -74,7 +74,7 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) && jobframework.ParentWorkloadName(job) == "" {
```

  Ok, let's keep it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T16:28:43Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:63** by [@mimowo](https://github.com/mimowo) — 2024-01-12T16:28:43Z
```diff
@@ -58,6 +59,20 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	jobSet := fromObject(obj)
 	log := ctrl.LoggerFrom(ctx).WithName("jobset-webhook")
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
+
+	if wlName, hasPrebuilt := jobframework.PrebuiltWorkload(jobSet); hasPrebuilt {
```

  ok, I see.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-12T16:34:43Z

The changes to the logic LGTM. 

One remaining comment is about testing. The fact that the prebuilt-workload is supported does not seem tested (asked to clarify in https://github.com/kubernetes-sigs/kueue/pull/1575#discussion_r1450650803). 

The preferred way to do for me would be a baseline scenario integration test. JobSet is a key CRD we will use for MultiKueue, so seems desired to have an integration test for it, but unit test is acceptable to me.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-12T18:22:27Z

- **pkg/controller/jobframework/interface.go:138** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T18:12:08Z
```diff
@@ -135,7 +135,7 @@ func workloadPriorityClassName(job GenericJob) string {
 	return ""
 }
 
-func prebuiltWorkload(job GenericJob) (string, bool) {
+func PrebuiltWorkload(job GenericJob) (string, bool) {
```

  ```suggestion
  func PrebuiltWorkloadFor(job GenericJob) (string, bool) {
  ```

- **pkg/controller/jobs/job/job_webhook.go:77** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T18:14:48Z
```diff
@@ -74,7 +74,9 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	// While using prebuilt workloads, the owner job may sat the parent workload to a different one
```

  ```suggestion
  	// While using prebuilt workloads, the owner job may set the parent workload to a different one
  ```

- **pkg/controller/jobframework/validation.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-12T18:18:28Z
```diff
@@ -31,7 +31,7 @@ var (
 	parentWorkloadKeyPath         = annotationsPath.Key(constants.ParentWorkloadAnnotation)
 	queueNameLabelPath            = labelsPath.Key(constants.QueueLabel)
 	workloadPriorityClassNamePath = labelsPath.Key(constants.WorkloadPriorityClassLabel)
-	supportedPrebuiltWlJobGVKs    = sets.New("batch/v1, Kind=Job")
+	supportedPrebuiltWlJobGVKs    = sets.New("batch/v1, Kind=Job", "jobset.x-k8s.io/v1alpha2, Kind=JobSet")
```

  Add a unit test for this in jobset_webhook_test.go

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-15T07:35:51Z

- **pkg/controller/jobframework/interface.go:138** by [@trasc](https://github.com/trasc) — 2024-01-15T07:24:56Z
```diff
@@ -135,7 +135,7 @@ func workloadPriorityClassName(job GenericJob) string {
 	return ""
 }
 
-func prebuiltWorkload(job GenericJob) (string, bool) {
+func PrebuiltWorkload(job GenericJob) (string, bool) {
```

  done

- **pkg/controller/jobframework/validation.go:34** by [@trasc](https://github.com/trasc) — 2024-01-15T07:32:29Z
```diff
@@ -31,7 +31,7 @@ var (
 	parentWorkloadKeyPath         = annotationsPath.Key(constants.ParentWorkloadAnnotation)
 	queueNameLabelPath            = labelsPath.Key(constants.QueueLabel)
 	workloadPriorityClassNamePath = labelsPath.Key(constants.WorkloadPriorityClassLabel)
-	supportedPrebuiltWlJobGVKs    = sets.New("batch/v1, Kind=Job")
+	supportedPrebuiltWlJobGVKs    = sets.New("batch/v1, Kind=Job", "jobset.x-k8s.io/v1alpha2, Kind=JobSet")
```

  done

- **pkg/controller/jobs/job/job_webhook.go:77** by [@trasc](https://github.com/trasc) — 2024-01-15T07:33:39Z
```diff
@@ -74,7 +74,9 @@ func (w *JobWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Applying defaults", "job", klog.KObj(job))
 
-	if owner := metav1.GetControllerOf(job); owner != nil && jobframework.IsOwnerManagedByKueue(owner) {
+	// While using prebuilt workloads, the owner job may sat the parent workload to a different one
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-15T17:02:35Z

Thanks!

/lgtm
/approve
