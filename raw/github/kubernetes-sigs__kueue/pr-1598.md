# PR #1598: Set default leader election configuration

**Summary**: Set default leader election configuration

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1598

**Last updated**: 2024-02-13T20:13:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@astefanutti](https://github.com/astefanutti)
- **Branch**: `pr-08` → `main`
- **Created**: 2024-01-17T13:43:53Z
- **Updated**: 2024-02-13T20:13:14Z
- **Merged**: 2024-01-18T17:04:50Z
- **Closed**: 2024-01-18T17:04:50Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 7   **+153 / -27**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

The leader election section of the Kueue configuration is not defaulted.

Kubernetes components, like the scheduler, usually delegate to component-base to set sensible defaults:

https://github.com/kubernetes/component-base/blob/d5ddea0e47af10468dcd90e09768b2661caf23f7/config/v1alpha1/defaults.go#L35

Implementing that recommended defaulting has been suggested in the following discussion: https://github.com/kubernetes-sigs/kueue/pull/1554#discussion_r1452547334.

#### Does this PR introduce a user-facing change?

```release-note
The leaderElection field in the Configuration API is now defaulted.
Leader election is now enabled by default.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-17T13:43:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 21a2512d0680221535a4ad64578ff802d2f1e613 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65a8f09e35ed320008d7e4a9 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-17T13:44:02Z

Hi @astefanutti. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-17T17:33:11Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-18T10:01:49Z

LGTM label has been added.  <details>Git tree hash: d17270b277bc93330482b2f483ade14d255eea37</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-18T16:41:06Z

/release-note-edit

```release-note
The leaderElection field in the Configuration API is now defaulted.
Leader election is now enabled by default.
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-18T16:41:11Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-18T16:41:19Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1598#issuecomment-1898836602" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1598#" title="Author self-approved">astefanutti</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T20:13:11Z

/kind api-change

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-18T08:48:25Z

LGTM overall

- **apis/config/v1beta1/groupversion_info.go:36** by [@mimowo](https://github.com/mimowo) — 2024-01-18T08:40:24Z
```diff
@@ -31,6 +31,10 @@ var (
 	// SchemeBuilder is used to add go types to the GroupVersionKind scheme
 	SchemeBuilder = &scheme.Builder{GroupVersion: GroupVersion}
 
+	// localSchemeBuilder is used to register autogenerated conversion and defaults functions
+	// It is required by ./zz_generated.conversion.go and ./zz_generated.defaults.go
+	localSchemeBuilder = &SchemeBuilder.SchemeBuilder
```

  What is the reason to declare `localSchemeBuilder`, maybe just refer via `SchemeBuilder.SchemeBuilder`  (as before)?

- **pkg/config/config_test.go:351** by [@mimowo](https://github.com/mimowo) — 2024-01-18T08:43:10Z
```diff
@@ -340,8 +345,12 @@ integrations:
 				Metrics: metricsserver.Options{
 					BindAddress: configapi.DefaultMetricsBindAddress,
 				},
-				LeaderElectionID: "",
-				LeaderElection:   false,
+				LeaderElection:             true,
+				LeaderElectionID:           configapi.DefaultLeaderElectionID,
+				LeaderElectionResourceLock: resourcelock.LeasesResourceLock,
+				LeaseDuration:              ptr.To(15 * time.Second),
```

  Maybe instead of the magic values refer to values in `defaultControlOption`, so that is easier to identify which fields deviate from defaults?

- **apis/config/v1beta1/defaults.go:83** by [@mimowo](https://github.com/mimowo) — 2024-01-18T08:47:22Z
```diff
@@ -73,10 +67,21 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if len(cfg.Health.HealthProbeBindAddress) == 0 {
 		cfg.Health.HealthProbeBindAddress = DefaultHealthProbeBindAddress
 	}
-	if cfg.LeaderElection != nil && cfg.LeaderElection.LeaderElect != nil &&
-		*cfg.LeaderElection.LeaderElect && len(cfg.LeaderElection.ResourceName) == 0 {
+
+	if cfg.LeaderElection == nil {
+		cfg.LeaderElection = &configv1alpha1.LeaderElectionConfiguration{}
+	}
+	if len(cfg.LeaderElection.ResourceName) == 0 {
 		cfg.LeaderElection.ResourceName = DefaultLeaderElectionID
 	}
+	if len(cfg.LeaderElection.ResourceLock) == 0 {
+		// Default to Lease as component-base still defaults to endpoint resources
+		// until core components migrate to using Leases. See k/k #80289 for more details.
+		cfg.LeaderElection.ResourceLock = resourcelock.LeasesResourceLock
```

  Is this a change compared to what is happening on the main branch? I'm wondering if this deserves a release note explaining what are the user-facing differences when using defaults.

- **apis/config/v1beta1/defaults.go:86** by [@mimowo](https://github.com/mimowo) — 2024-01-18T08:47:46Z
```diff
@@ -73,10 +67,21 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if len(cfg.Health.HealthProbeBindAddress) == 0 {
 		cfg.Health.HealthProbeBindAddress = DefaultHealthProbeBindAddress
 	}
-	if cfg.LeaderElection != nil && cfg.LeaderElection.LeaderElect != nil &&
-		*cfg.LeaderElection.LeaderElect && len(cfg.LeaderElection.ResourceName) == 0 {
+
+	if cfg.LeaderElection == nil {
+		cfg.LeaderElection = &configv1alpha1.LeaderElectionConfiguration{}
+	}
+	if len(cfg.LeaderElection.ResourceName) == 0 {
 		cfg.LeaderElection.ResourceName = DefaultLeaderElectionID
 	}
+	if len(cfg.LeaderElection.ResourceLock) == 0 {
+		// Default to Lease as component-base still defaults to endpoint resources
+		// until core components migrate to using Leases. See k/k #80289 for more details.
+		cfg.LeaderElection.ResourceLock = resourcelock.LeasesResourceLock
+	}
+	// Use the default LeaderElectionConfiguration options
+	configv1alpha1.RecommendedDefaultLeaderElectionConfiguration(cfg.LeaderElection)
```

  As before, question about user-facing consequences of setting the defaults.

- **apis/config/v1beta1/defaults.go:46** by [@mimowo](https://github.com/mimowo) — 2024-01-18T08:48:22Z
```diff
@@ -43,13 +44,6 @@ const (
 	DefaultClusterQueuesMaxCount                int32   = 10
 )
 
-func addDefaultingFuncs(scheme *runtime.Scheme) error {
```

  nice win

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-18T08:53:56Z

- **apis/config/v1beta1/groupversion_info.go:36** by [@astefanutti](https://github.com/astefanutti) — 2024-01-18T08:53:56Z
```diff
@@ -31,6 +31,10 @@ var (
 	// SchemeBuilder is used to add go types to the GroupVersionKind scheme
 	SchemeBuilder = &scheme.Builder{GroupVersion: GroupVersion}
 
+	// localSchemeBuilder is used to register autogenerated conversion and defaults functions
+	// It is required by ./zz_generated.conversion.go and ./zz_generated.defaults.go
+	localSchemeBuilder = &SchemeBuilder.SchemeBuilder
```

  It seems the convention is to declare that `localSchemeBuilder` variable. This is what's done in /apis/visibility/v1alpha1` and also in Kubernetes components, like the scheduler for example. So it's mostly for consistency.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-18T08:57:53Z

- **apis/config/v1beta1/defaults.go:83** by [@astefanutti](https://github.com/astefanutti) — 2024-01-18T08:57:52Z
```diff
@@ -73,10 +67,21 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if len(cfg.Health.HealthProbeBindAddress) == 0 {
 		cfg.Health.HealthProbeBindAddress = DefaultHealthProbeBindAddress
 	}
-	if cfg.LeaderElection != nil && cfg.LeaderElection.LeaderElect != nil &&
-		*cfg.LeaderElection.LeaderElect && len(cfg.LeaderElection.ResourceName) == 0 {
+
+	if cfg.LeaderElection == nil {
+		cfg.LeaderElection = &configv1alpha1.LeaderElectionConfiguration{}
+	}
+	if len(cfg.LeaderElection.ResourceName) == 0 {
 		cfg.LeaderElection.ResourceName = DefaultLeaderElectionID
 	}
+	if len(cfg.LeaderElection.ResourceLock) == 0 {
+		// Default to Lease as component-base still defaults to endpoint resources
+		// until core components migrate to using Leases. See k/k #80289 for more details.
+		cfg.LeaderElection.ResourceLock = resourcelock.LeasesResourceLock
```

  No, it doesn't change the default value compared to main. It's necessary so we can delegate to `RecommendedDefaultLeaderElectionConfiguration`, but that still defaults to endpoints as resource lock, because some Kubernetes components haven't migrated yet, and maintain the current default value for Kueue.
  This can be removed once kubernetes/kubernetes#80289 is fixed.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-18T09:02:52Z

- **apis/config/v1beta1/defaults.go:86** by [@astefanutti](https://github.com/astefanutti) — 2024-01-18T09:02:52Z
```diff
@@ -73,10 +67,21 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if len(cfg.Health.HealthProbeBindAddress) == 0 {
 		cfg.Health.HealthProbeBindAddress = DefaultHealthProbeBindAddress
 	}
-	if cfg.LeaderElection != nil && cfg.LeaderElection.LeaderElect != nil &&
-		*cfg.LeaderElection.LeaderElect && len(cfg.LeaderElection.ResourceName) == 0 {
+
+	if cfg.LeaderElection == nil {
+		cfg.LeaderElection = &configv1alpha1.LeaderElectionConfiguration{}
+	}
+	if len(cfg.LeaderElection.ResourceName) == 0 {
 		cfg.LeaderElection.ResourceName = DefaultLeaderElectionID
 	}
+	if len(cfg.LeaderElection.ResourceLock) == 0 {
+		// Default to Lease as component-base still defaults to endpoint resources
+		// until core components migrate to using Leases. See k/k #80289 for more details.
+		cfg.LeaderElection.ResourceLock = resourcelock.LeasesResourceLock
+	}
+	// Use the default LeaderElectionConfiguration options
+	configv1alpha1.RecommendedDefaultLeaderElectionConfiguration(cfg.LeaderElection)
```

  With the values already set in the default Kueue configuration, i.e.: 
  
  https://github.com/kubernetes-sigs/kueue/blob/d4d3f3be091eea414ae8d7018108ff9a391dc14d/config/components/manager/controller_manager_config.yaml#L10-L12
  
  That PR is "idem-potent".
  
  The only change would be if an existing user would use a configuration with `.leaderElection` or `.leaderElection.leaderElect` not defined, then the new default would be true, instead of false.
  
  But I'd be inclined to think enabling leader election by default is desirable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-18T09:06:21Z

- **apis/config/v1beta1/groupversion_info.go:36** by [@mimowo](https://github.com/mimowo) — 2024-01-18T09:06:21Z
```diff
@@ -31,6 +31,10 @@ var (
 	// SchemeBuilder is used to add go types to the GroupVersionKind scheme
 	SchemeBuilder = &scheme.Builder{GroupVersion: GroupVersion}
 
+	// localSchemeBuilder is used to register autogenerated conversion and defaults functions
+	// It is required by ./zz_generated.conversion.go and ./zz_generated.defaults.go
+	localSchemeBuilder = &SchemeBuilder.SchemeBuilder
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-18T09:12:12Z

- **apis/config/v1beta1/defaults.go:86** by [@mimowo](https://github.com/mimowo) — 2024-01-18T09:12:12Z
```diff
@@ -73,10 +67,21 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if len(cfg.Health.HealthProbeBindAddress) == 0 {
 		cfg.Health.HealthProbeBindAddress = DefaultHealthProbeBindAddress
 	}
-	if cfg.LeaderElection != nil && cfg.LeaderElection.LeaderElect != nil &&
-		*cfg.LeaderElection.LeaderElect && len(cfg.LeaderElection.ResourceName) == 0 {
+
+	if cfg.LeaderElection == nil {
+		cfg.LeaderElection = &configv1alpha1.LeaderElectionConfiguration{}
+	}
+	if len(cfg.LeaderElection.ResourceName) == 0 {
 		cfg.LeaderElection.ResourceName = DefaultLeaderElectionID
 	}
+	if len(cfg.LeaderElection.ResourceLock) == 0 {
+		// Default to Lease as component-base still defaults to endpoint resources
+		// until core components migrate to using Leases. See k/k #80289 for more details.
+		cfg.LeaderElection.ResourceLock = resourcelock.LeasesResourceLock
+	}
+	// Use the default LeaderElectionConfiguration options
+	configv1alpha1.RecommendedDefaultLeaderElectionConfiguration(cfg.LeaderElection)
```

  How about the values of `LeaseDuration`, `RenewDeadline`, `RetryPeriod`, as the values are now generated, are they ignored, or their values are as implicit defaults used before?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-18T09:14:19Z

- **apis/config/v1beta1/defaults.go:86** by [@astefanutti](https://github.com/astefanutti) — 2024-01-18T09:14:19Z
```diff
@@ -73,10 +67,21 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if len(cfg.Health.HealthProbeBindAddress) == 0 {
 		cfg.Health.HealthProbeBindAddress = DefaultHealthProbeBindAddress
 	}
-	if cfg.LeaderElection != nil && cfg.LeaderElection.LeaderElect != nil &&
-		*cfg.LeaderElection.LeaderElect && len(cfg.LeaderElection.ResourceName) == 0 {
+
+	if cfg.LeaderElection == nil {
+		cfg.LeaderElection = &configv1alpha1.LeaderElectionConfiguration{}
+	}
+	if len(cfg.LeaderElection.ResourceName) == 0 {
 		cfg.LeaderElection.ResourceName = DefaultLeaderElectionID
 	}
+	if len(cfg.LeaderElection.ResourceLock) == 0 {
+		// Default to Lease as component-base still defaults to endpoint resources
+		// until core components migrate to using Leases. See k/k #80289 for more details.
+		cfg.LeaderElection.ResourceLock = resourcelock.LeasesResourceLock
+	}
+	// Use the default LeaderElectionConfiguration options
+	configv1alpha1.RecommendedDefaultLeaderElectionConfiguration(cfg.LeaderElection)
```

  Yes, the latter, their values are equal to the implicit defaults used before.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-01-18T09:22:42Z

- **pkg/config/config_test.go:351** by [@astefanutti](https://github.com/astefanutti) — 2024-01-18T09:22:41Z
```diff
@@ -340,8 +345,12 @@ integrations:
 				Metrics: metricsserver.Options{
 					BindAddress: configapi.DefaultMetricsBindAddress,
 				},
-				LeaderElectionID: "",
-				LeaderElection:   false,
+				LeaderElection:             true,
+				LeaderElectionID:           configapi.DefaultLeaderElectionID,
+				LeaderElectionResourceLock: resourcelock.LeasesResourceLock,
+				LeaseDuration:              ptr.To(15 * time.Second),
```

  Good idea. Updated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-18T10:01:43Z

/lgtm 
/assign @tenzen-y @alculquicondor
