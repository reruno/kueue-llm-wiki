# PR #1600: Retry removing Pod finalizers on cleanup

**Summary**: Retry removing Pod finalizers on cleanup

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1600

**Last updated**: 2024-01-17T18:56:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `retry-rm-finalizer` → `main`
- **Created**: 2024-01-17T17:13:16Z
- **Updated**: 2024-01-17T18:56:48Z
- **Merged**: 2024-01-17T18:48:29Z
- **Closed**: 2024-01-17T18:48:29Z
- **Labels**: `kind/bug`, `lgtm`, `kind/flake`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 1   **+23 / -29**

## Description

#### What type of PR is this?

/kind bug
/kind flake

#### What this PR does / why we need it:

Retry removing finalizers on cleanup, as the operation might fail when racing with other object updates or deletes.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1591

#### Special notes for your reviewer:

I wanted to use generics to remove finalizers from Workloads and Pods, but given that `ObjectList` doesn't provide a way to access the items, it was not possible to do so.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-17T17:13:23Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | dbd57a60fc3ab68eb04aff426c2dfc2fb2698f53 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65a80aaf9d3bf1000832dff9 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1600--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1600--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE2MDAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.pNfEAUPpdKyCD2rY7XvBiEnyelgz9hyQ6ooEVQnXOT4)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-17T18:11:09Z

/assign @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-17T18:45:34Z

LGTM label has been added.  <details>Git tree hash: 7e262fe0cf7ef51e95c372dc283382c011118037</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-17T18:45:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1600#" title="Author self-approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1600#pullrequestreview-1827959884" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-17T18:45:29Z

Thank you!
/lgtm
/approve

- **test/util/util.go:147** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-17T18:45:07Z
```diff
@@ -140,25 +140,24 @@ func DeleteAllPodsInNamespace(ctx context.Context, c client.Client, ns *corev1.N
 		return fmt.Errorf("deleting all Pods in namespace %q: %w", ns.Name, err)
 	}
 
-	lst := corev1.PodList{}
-	err = c.List(ctx, &lst, client.InNamespace(ns.Name))
-	if err != nil && !apierrors.IsNotFound(err) {
-		return fmt.Errorf("listing Pods with a finalizer in namespace %q: %w", ns.Name, err)
-	}
-
-	if len(lst.Items) == 0 {
-		return nil
-	}
+	gomega.Eventually(func() error {
+		lst := corev1.PodList{}
+		err := c.List(ctx, &lst, client.InNamespace(ns.Name))
+		if err != nil && !errors.IsNotFound(err) {
+			return fmt.Errorf("listing Pods with a finalizer in namespace %q: %w", ns.Name, err)
```

  Although I believe that we can improve error logging by using `Or()` and `func(g gomega.Gomega)`, we can work on such a cleanup at another PR.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-17T18:48:48Z

- **test/util/util.go:147** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-17T18:48:48Z
```diff
@@ -140,25 +140,24 @@ func DeleteAllPodsInNamespace(ctx context.Context, c client.Client, ns *corev1.N
 		return fmt.Errorf("deleting all Pods in namespace %q: %w", ns.Name, err)
 	}
 
-	lst := corev1.PodList{}
-	err = c.List(ctx, &lst, client.InNamespace(ns.Name))
-	if err != nil && !apierrors.IsNotFound(err) {
-		return fmt.Errorf("listing Pods with a finalizer in namespace %q: %w", ns.Name, err)
-	}
-
-	if len(lst.Items) == 0 {
-		return nil
-	}
+	gomega.Eventually(func() error {
+		lst := corev1.PodList{}
+		err := c.List(ctx, &lst, client.InNamespace(ns.Name))
+		if err != nil && !errors.IsNotFound(err) {
+			return fmt.Errorf("listing Pods with a finalizer in namespace %q: %w", ns.Name, err)
```

  I thought about it, but it we still need to check `IsNotFound` in the loop below, so I decided against it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-17T18:56:48Z

- **test/util/util.go:147** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-17T18:56:47Z
```diff
@@ -140,25 +140,24 @@ func DeleteAllPodsInNamespace(ctx context.Context, c client.Client, ns *corev1.N
 		return fmt.Errorf("deleting all Pods in namespace %q: %w", ns.Name, err)
 	}
 
-	lst := corev1.PodList{}
-	err = c.List(ctx, &lst, client.InNamespace(ns.Name))
-	if err != nil && !apierrors.IsNotFound(err) {
-		return fmt.Errorf("listing Pods with a finalizer in namespace %q: %w", ns.Name, err)
-	}
-
-	if len(lst.Items) == 0 {
-		return nil
-	}
+	gomega.Eventually(func() error {
+		lst := corev1.PodList{}
+		err := c.List(ctx, &lst, client.InNamespace(ns.Name))
+		if err != nil && !errors.IsNotFound(err) {
+			return fmt.Errorf("listing Pods with a finalizer in namespace %q: %w", ns.Name, err)
```

  I meant the following:
  
  ```golang
  err := c.List(ctx, &lst, client.InNamespace(ns.Name))
  g.Expect(err).Should(g.Or(g.BeNil(), g.And(g.HaveOccur(), g.BeTrue(errors.IsNotFound(err)))))
  ```
