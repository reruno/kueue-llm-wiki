# PR #1607: [site] [website] Kueue RayCluster documentation

**Summary**: [site] [website] Kueue RayCluster documentation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1607

**Last updated**: 2024-02-13T19:32:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vicentefb](https://github.com/vicentefb)
- **Branch**: `RayClusterDocumentation` → `main`
- **Created**: 2024-01-18T05:25:52Z
- **Updated**: 2024-02-13T19:32:48Z
- **Merged**: 2024-01-31T19:25:10Z
- **Closed**: 2024-01-31T19:25:10Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@andrewsykim](https://github.com/andrewsykim)
- **Requested reviewers**: [@astefanutti](https://github.com/astefanutti), [@kerthcet](https://github.com/kerthcet), [@trasc](https://github.com/trasc)
- **Changed files**: 4   **+166 / -2**

## Description

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:
Adds documentation for RayCluster integration with Kueue

#### Which issue(s) this PR fixes:
Fixes https://github.com/kubernetes-sigs/kueue/issues/1272

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
Adds documentation for RayCluster integration with Kueue

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-18T05:25:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5eeebe6e5f0880d768c514d060e6801087926cd8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65ba8f60ddfa2400073642f4 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-18T05:26:02Z

Hi @vicentefb. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-26T18:04:16Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-31T13:28:39Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1607#pullrequestreview-1852491770" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1607#pullrequestreview-1853732981" title="Approved">astefanutti</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1607#" title="Author self-approved">vicentefb</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@vicentefb](https://github.com/vicentefb) — 2024-01-31T18:35:13Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-31T19:21:05Z

LGTM label has been added.  <details>Git tree hash: 20621a6512de31b192065278ca46bc263ae3f091</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T19:32:43Z

/release-note-edit
```release-note
NONE
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-26T18:19:48Z

- **site/content/en/docs/tasks/run_rayclusters.md:83** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-26T18:14:22Z
```diff
@@ -0,0 +1,91 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
+  Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+  and enable the `ray.io/raycluster` integration.
+
+2. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+3. See [KubeRay Installation](https://ray-project.github.io/kuberay/deploy/installation/) for installation and configuration details of KubeRay.
+
+## RayCluster definition
+
+When running [RayClusters](https://docs.ray.io/en/latest/cluster/getting-started.html) on
+Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  name: raycluster-sample
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: local-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec`.
+
+```yaml
+    headGroupSpec:
+       spec:
+        affinity: {}
+        containers:
+        - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-head
+          resources:
+            limits:
+              cpu: "1"
+              memory: 2G
+            requests:
+              cpu: "1"
+              memory: 2G
+          securityContext: {}
+          volumeMounts:
+          - mountPath: /tmp/ray
+            name: log-volume
+    workerGroupSpecs:
+      template:
+        spec:
+          affinity: {}
+          containers:
+          - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-worker
+          resources:
+            limits:
+            cpu: "1"
+            memory: 1G
+            requests:
+            cpu: "1"
+            memory: 1G
+```
+
+### c. Limitations
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.workerGroupSpecs` is 7.
```

  I thought there were other limitations, like disabling autoscaling.

- **site/content/en/docs/tasks/run_rayclusters.md:79** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-26T18:15:09Z
```diff
@@ -0,0 +1,91 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
+  Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+  and enable the `ray.io/raycluster` integration.
+
+2. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+3. See [KubeRay Installation](https://ray-project.github.io/kuberay/deploy/installation/) for installation and configuration details of KubeRay.
+
+## RayCluster definition
+
+When running [RayClusters](https://docs.ray.io/en/latest/cluster/getting-started.html) on
+Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  name: raycluster-sample
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: local-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec`.
+
+```yaml
+    headGroupSpec:
+       spec:
+        affinity: {}
+        containers:
+        - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-head
+          resources:
+            limits:
+              cpu: "1"
+              memory: 2G
+            requests:
+              cpu: "1"
+              memory: 2G
+          securityContext: {}
+          volumeMounts:
+          - mountPath: /tmp/ray
+            name: log-volume
+    workerGroupSpecs:
+      template:
+        spec:
+          affinity: {}
+          containers:
+          - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-worker
+          resources:
+            limits:
+            cpu: "1"
+            memory: 1G
+            requests:
+            cpu: "1"
+            memory: 1G
+```
+
```

  Not a limitation, but please explain that a RayCluster holds quota until its deleted

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-01-26T23:11:50Z

- **site/content/en/docs/tasks/run_rayclusters.md:83** by [@vicentefb](https://github.com/vicentefb) — 2024-01-26T23:11:50Z
```diff
@@ -0,0 +1,91 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
+  Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+  and enable the `ray.io/raycluster` integration.
+
+2. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+3. See [KubeRay Installation](https://ray-project.github.io/kuberay/deploy/installation/) for installation and configuration details of KubeRay.
+
+## RayCluster definition
+
+When running [RayClusters](https://docs.ray.io/en/latest/cluster/getting-started.html) on
+Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  name: raycluster-sample
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: local-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec`.
+
+```yaml
+    headGroupSpec:
+       spec:
+        affinity: {}
+        containers:
+        - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-head
+          resources:
+            limits:
+              cpu: "1"
+              memory: 2G
+            requests:
+              cpu: "1"
+              memory: 2G
+          securityContext: {}
+          volumeMounts:
+          - mountPath: /tmp/ray
+            name: log-volume
+    workerGroupSpecs:
+      template:
+        spec:
+          affinity: {}
+          containers:
+          - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-worker
+          resources:
+            limits:
+            cpu: "1"
+            memory: 1G
+            requests:
+            cpu: "1"
+            memory: 1G
+```
+
+### c. Limitations
+- Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.workerGroupSpecs` is 7.
```

  Added more limitations

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-01-26T23:11:54Z

- **site/content/en/docs/tasks/run_rayclusters.md:79** by [@vicentefb](https://github.com/vicentefb) — 2024-01-26T23:11:54Z
```diff
@@ -0,0 +1,91 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
+  Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+  and enable the `ray.io/raycluster` integration.
+
+2. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+3. See [KubeRay Installation](https://ray-project.github.io/kuberay/deploy/installation/) for installation and configuration details of KubeRay.
+
+## RayCluster definition
+
+When running [RayClusters](https://docs.ray.io/en/latest/cluster/getting-started.html) on
+Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayJob configuration.
+
+```yaml
+metadata:
+  name: raycluster-sample
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: local-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec`.
+
+```yaml
+    headGroupSpec:
+       spec:
+        affinity: {}
+        containers:
+        - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-head
+          resources:
+            limits:
+              cpu: "1"
+              memory: 2G
+            requests:
+              cpu: "1"
+              memory: 2G
+          securityContext: {}
+          volumeMounts:
+          - mountPath: /tmp/ray
+            name: log-volume
+    workerGroupSpecs:
+      template:
+        spec:
+          affinity: {}
+          containers:
+          - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-worker
+          resources:
+            limits:
+            cpu: "1"
+            memory: 1G
+            requests:
+            cpu: "1"
+            memory: 1G
+```
+
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-30T22:20:40Z

/approve
@astefanutti or @andrewsykim can you give an LGTM?

- **site/content/en/docs/tasks/run_rayclusters.md:82** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-30T22:18:51Z
```diff
@@ -0,0 +1,95 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
+  Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+  and enable the `ray.io/raycluster` integration.
+
+2. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+3. See [KubeRay Installation](https://ray-project.github.io/kuberay/deploy/installation/) for installation and configuration details of KubeRay.
+
+## RayCluster definition
+
+When running [RayClusters](https://docs.ray.io/en/latest/cluster/getting-started.html) on
+Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayCluster configuration.
+
+```yaml
+metadata:
+  name: raycluster-sample
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: local-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec`.
+
+```yaml
+    headGroupSpec:
+       spec:
+        affinity: {}
+        containers:
+        - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-head
+          resources:
+            limits:
+              cpu: "1"
+              memory: 2G
+            requests:
+              cpu: "1"
+              memory: 2G
+          securityContext: {}
+          volumeMounts:
+          - mountPath: /tmp/ray
+            name: log-volume
+    workerGroupSpecs:
+      template:
+        spec:
+          affinity: {}
+          containers:
+          - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-worker
+          resources:
+            limits:
+            cpu: "1"
+            memory: 1G
+            requests:
+            cpu: "1"
+            memory: 1G
+```
+
+Mandatory Cluster Deletion: A RayCluster will hold resource quota, so for optimal resource management Kueue requires that the RayCluster created for a RayJob is deleted after the job completes.
```

  ```suggestion
  Note that a RayCluster will hold resource quotas while it exists. For optimal resource management, you should delete a RayCluster that is no longer in use.
  ```

- **site/content/en/docs/tasks/run_rayclusters.md:86** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-30T22:20:13Z
```diff
@@ -0,0 +1,95 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
+  Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+  and enable the `ray.io/raycluster` integration.
+
+2. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+3. See [KubeRay Installation](https://ray-project.github.io/kuberay/deploy/installation/) for installation and configuration details of KubeRay.
+
+## RayCluster definition
+
+When running [RayClusters](https://docs.ray.io/en/latest/cluster/getting-started.html) on
+Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayCluster configuration.
+
+```yaml
+metadata:
+  name: raycluster-sample
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: local-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec`.
+
+```yaml
+    headGroupSpec:
+       spec:
+        affinity: {}
+        containers:
+        - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-head
+          resources:
+            limits:
+              cpu: "1"
+              memory: 2G
+            requests:
+              cpu: "1"
+              memory: 2G
+          securityContext: {}
+          volumeMounts:
+          - mountPath: /tmp/ray
+            name: log-volume
+    workerGroupSpecs:
+      template:
+        spec:
+          affinity: {}
+          containers:
+          - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-worker
+          resources:
+            limits:
+            cpu: "1"
+            memory: 1G
+            requests:
+            cpu: "1"
+            memory: 1G
+```
+
+Mandatory Cluster Deletion: A RayCluster will hold resource quota, so for optimal resource management Kueue requires that the RayCluster created for a RayJob is deleted after the job completes.
+
+### c. Limitations
+- Limited Worker Groups: Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.workerGroupSpecs` is 7
+- No Integration with Existing Ray Clusters: You cannot submit a RayJob to Kueue that targets a pre-existing, independently created RayCluster
```

  This is a limitation for RayJob (which is documented here: https://kueue.sigs.k8s.io/docs/tasks/run_rayjobs/#c-limitations), not a limitation for RayCluster.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-01-31T01:03:07Z

- **site/content/en/docs/tasks/run_rayclusters.md:86** by [@vicentefb](https://github.com/vicentefb) — 2024-01-31T01:03:07Z
```diff
@@ -0,0 +1,95 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
+  Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+  and enable the `ray.io/raycluster` integration.
+
+2. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+3. See [KubeRay Installation](https://ray-project.github.io/kuberay/deploy/installation/) for installation and configuration details of KubeRay.
+
+## RayCluster definition
+
+When running [RayClusters](https://docs.ray.io/en/latest/cluster/getting-started.html) on
+Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayCluster configuration.
+
+```yaml
+metadata:
+  name: raycluster-sample
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: local-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec`.
+
+```yaml
+    headGroupSpec:
+       spec:
+        affinity: {}
+        containers:
+        - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-head
+          resources:
+            limits:
+              cpu: "1"
+              memory: 2G
+            requests:
+              cpu: "1"
+              memory: 2G
+          securityContext: {}
+          volumeMounts:
+          - mountPath: /tmp/ray
+            name: log-volume
+    workerGroupSpecs:
+      template:
+        spec:
+          affinity: {}
+          containers:
+          - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-worker
+          resources:
+            limits:
+            cpu: "1"
+            memory: 1G
+            requests:
+            cpu: "1"
+            memory: 1G
+```
+
+Mandatory Cluster Deletion: A RayCluster will hold resource quota, so for optimal resource management Kueue requires that the RayCluster created for a RayJob is deleted after the job completes.
+
+### c. Limitations
+- Limited Worker Groups: Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.workerGroupSpecs` is 7
+- No Integration with Existing Ray Clusters: You cannot submit a RayJob to Kueue that targets a pre-existing, independently created RayCluster
```

  Ohhhh that's true, okok got cofused, thanks!

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-01-31T01:24:58Z

- **site/content/en/docs/tasks/run_rayclusters.md:88** by [@andrewsykim](https://github.com/andrewsykim) — 2024-01-31T01:24:44Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
+  Learn how to [install Kueue with a custom manager configuration](/docs/installation/#install-a-custom-configured-released-version)
+  and enable the `ray.io/raycluster` integration.
+
+2. Check [Administer cluster quotas](/docs/tasks/administer_cluster_quotas) for details on the initial Kueue setup.
+
+3. See [KubeRay Installation](https://ray-project.github.io/kuberay/deploy/installation/) for installation and configuration details of KubeRay.
+
+## RayCluster definition
+
+When running [RayClusters](https://docs.ray.io/en/latest/cluster/getting-started.html) on
+Kueue, take into consideration the following aspects:
+
+### a. Queue selection
+
+The target [local queue](/docs/concepts/local_queue) should be specified in the `metadata.labels` section of the RayCluster configuration.
+
+```yaml
+metadata:
+  name: raycluster-sample
+  namespace: default
+  labels:
+    kueue.x-k8s.io/queue-name: local-queue
+```
+
+### b. Configure the resource needs
+
+The resource needs of the workload can be configured in the `spec`.
+
+```yaml
+    headGroupSpec:
+       spec:
+        affinity: {}
+        containers:
+        - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-head
+          resources:
+            limits:
+              cpu: "1"
+              memory: 2G
+            requests:
+              cpu: "1"
+              memory: 2G
+          securityContext: {}
+          volumeMounts:
+          - mountPath: /tmp/ray
+            name: log-volume
+    workerGroupSpecs:
+      template:
+        spec:
+          affinity: {}
+          containers:
+          - env: []
+          image: rayproject/ray:2.7.0
+          imagePullPolicy: IfNotPresent
+          name: ray-worker
+          resources:
+            limits:
+            cpu: "1"
+            memory: 1G
+            requests:
+            cpu: "1"
+            memory: 1G
+```
+
+Note that a RayCluster will hold resource quotas while it exists. For optimal resource management, you should delete a RayCluster that is no longer in use.
+
+### c. Limitations
+- Limited Worker Groups: Because a Kueue workload can have a maximum of 8 PodSets, the maximum number of `spec.workerGroupSpecs` is 7
+- In-Tree Autoscaling Disabled: Kueue manages resource allocation for the RayCluster; therefore, the cluster's internal autoscaling mechanisms need to be disabled
+
+## Example RayJob
```

  Example RayCluster

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-01-31T04:28:09Z

- **site/content/en/docs/tasks/run_rayclusters.md:13** by [@andrewsykim](https://github.com/andrewsykim) — 2024-01-31T04:28:06Z
```diff
@@ -0,0 +1,94 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
```

  We should probably document the Kueue version required for this as well as the Kuberay version required (v1.1 which introduced the Suspend field in RayCluster)

### Review by [@astefanutti](https://github.com/astefanutti) (approved) — 2024-01-31T13:28:34Z

LGTM. Thanks!

- **site/content/en/docs/tasks/run_rayclusters.md:17** by [@astefanutti](https://github.com/astefanutti) — 2024-01-31T13:27:57Z
```diff
@@ -0,0 +1,96 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+Make sure you are using Kueue v0.6.0 version or newer and Kuberay 1.1.0 or newer.
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
```

  Is it really disabled by default. The `config/components/manager/controller_manager_config.yaml` has it enabled right?

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-01-31T19:20:26Z

- **site/content/en/docs/tasks/run_rayclusters.md:15** by [@andrewsykim](https://github.com/andrewsykim) — 2024-01-31T19:20:23Z
```diff
@@ -0,0 +1,92 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+1. Make sure you are using Kueue v0.6.0 version or newer and Kuberay 1.1.0 or newer.
```

  Not sure how we care that v1.1.0 is not release yet. Probably not a big deal since v1.1.0 will be out sometime in Feb

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-01-31T19:20:51Z

- **site/content/en/docs/tasks/run_rayclusters.md:17** by [@andrewsykim](https://github.com/andrewsykim) — 2024-01-31T19:20:51Z
```diff
@@ -0,0 +1,96 @@
+---
+title: "Run A RayCluster"
+date: 2024-01-17
+weight: 6
+description: >
+  Run a RayCluster on Kueue.
+---
+
+This page shows how to leverage Kueue's scheduling and resource management capabilities when running [RayCluster](https://docs.ray.io/en/latest/cluster/getting-started.html).
+
+This guide is for [batch users](/docs/tasks#batch-user) that have a basic understanding of Kueue. For more information, see [Kueue's overview](/docs/overview).
+
+## Before you begin
+
+Make sure you are using Kueue v0.6.0 version or newer and Kuberay 1.1.0 or newer.
+
+1. By default, the integration for `ray.io/raycluster` is not enabled.
```

  I think this is true for RayJob too, we should probably update both docs as a follow-up

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-01-31T19:20:59Z

/lgtm
