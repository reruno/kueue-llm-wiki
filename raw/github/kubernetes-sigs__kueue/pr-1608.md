# PR #1608: KEP: Add an exponential backoff mechanism to the requeueing strategy

**Summary**: KEP: Add an exponential backoff mechanism to the requeueing strategy

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1608

**Last updated**: 2024-02-12T00:16:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `add-beckoff-limit-for-requeue` → `main`
- **Created**: 2024-01-18T18:39:04Z
- **Updated**: 2024-02-12T00:16:47Z
- **Merged**: 2024-02-06T21:55:24Z
- **Closed**: 2024-02-06T21:55:24Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet), [@trasc](https://github.com/trasc)
- **Changed files**: 2   **+167 / -17**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part-of: #1282

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-18T18:39:06Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-18T18:39:10Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3829f798c5474df010fea9757056ede44039b1a8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65c2a5799ecbe20008f5e012 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T01:02:39Z

/assign @alculquicondor @nstogner @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-26T01:02:42Z

@tenzen-y: GitHub didn't allow me to assign the following users: nstogner.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) with read permissions, repo collaborators and people who have commented on this issue/PR can be assigned. Additionally, issues/PRs can only have 10 assignees at the same time.
For more information please see [the contributor guide](https://git.k8s.io/community/contributors/guide/first-contribution.md#issue-assignment-in-github)

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1608#issuecomment-1911246018):

>/assign @alculquicondor @nstogner @mimowo 
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T20:04:08Z

/hold
for reviews.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T23:30:08Z

Due to the failure to set up the webhook server: 

> A BeforeSuite node failed so all tests were skipped.

https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1608/pull-kueue-test-e2e-main-1-29/1751021175854600192

/test pull-kueue-test-e2e-main-1-29

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T21:34:47Z

Pending is only here: https://github.com/kubernetes-sigs/kueue/pull/1608#discussion_r1478419459

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T21:34:59Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-06T21:35:04Z

LGTM label has been added.  <details>Git tree hash: 3982a805d53cc49ef1ad9117f0ced1a31f9cfed8</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-06T21:35:06Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1608#issuecomment-1930789067" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1608#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T21:50:20Z

/hold cancel

## Reviews

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-20T00:13:43Z

- **keps/1282-pods-ready-requeue-strategy/README.md:61** by [@nstogner](https://github.com/nstogner) — 2024-01-20T00:13:43Z
```diff
@@ -56,6 +56,22 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingTimestamp=Eviction`, 
```

  I am guessing this should be `waitForPodsReady.requeuingTimestamp=Creation` ... which would preserve the pre-eviction ordering (i.e. put the evicted workload at the front of the queue again).

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-01-20T00:24:53Z

- **keps/1282-pods-ready-requeue-strategy/README.md:75** by [@nstogner](https://github.com/nstogner) — 2024-01-20T00:24:53Z
```diff
@@ -56,6 +56,22 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingTimestamp=Eviction`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
```

  3. If there are multiple resource flavors that match the workload (flavors 1 & 2) and the workload is running on flavor 2 (where there happens to be a quota issue - i.e. flavor 2 is backed by on-demand capacity): there is a high likelihood that the workload will be requeued on the same flavor - with the same capacity issue (because other workloads will have taken up resource flavor 1 while the requeued workload was running.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T19:05:13Z

- **keps/1282-pods-ready-requeue-strategy/README.md:61** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T19:05:13Z
```diff
@@ -56,6 +56,22 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingTimestamp=Eviction`, 
```

  Oops, it's a great catch! Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T19:18:30Z

- **keps/1282-pods-ready-requeue-strategy/README.md:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T19:18:30Z
```diff
@@ -56,6 +56,22 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingTimestamp=Eviction`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
```

  @nstogner Sorry, I couldn't understand when eviction happens, who is the preemptor, and who is the evictor.
  
  Could you clarify?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T22:34:01Z

- **keps/1282-pods-ready-requeue-strategy/README.md:61** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T22:34:01Z
```diff
@@ -56,6 +56,22 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingTimestamp=Eviction`, 
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-26T01:11:34Z

- **keps/1282-pods-ready-requeue-strategy/README.md:129** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T01:11:34Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
```

  I think that we can break this API for now since we haven't yet released this API.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-01T21:18:26Z

- **keps/1282-pods-ready-requeue-strategy/README.md:75** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T21:00:13Z
```diff
@@ -56,6 +56,22 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingTimestamp=Eviction`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
```

  @nstogner case is still about waitForPodsReady timeout. Once the workload is evicted because it didn't become ready, there is the possibility that it won't fit in the first flavor, so it will be assigned back to the second flavor.

- **keps/1282-pods-ready-requeue-strategy/README.md:75** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T21:01:40Z
```diff
@@ -56,6 +64,28 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+
+Specifically, the second reason will often occur in the workloads required gpus.
```

  ```suggestion
  Specifically, the second reason will often occur if the available quota is fragmented across multiple nodes,
  such that the workload can't be scheduled in a node even though there is enough
  quota in the cluster.
  For example,
  ```

- **keps/1282-pods-ready-requeue-strategy/README.md:86** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T21:03:51Z
```diff
@@ -56,6 +64,28 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+
+Specifically, the second reason will often occur in the workloads required gpus.
+Given that the workload with a request of 2 gpus is submitted to the cluster that
+has 2 worker nodes with 4 gpus, and 3 gpus are used (which means 1 gpu is free in each node),
+the workload will be repeatedly evicted because of the lack of resources in each node even though the cluster has enough capacities.
+
+In this case, to avoid rapid repetition of the admission and eviction cycle,
+the administrator would like to use an exponential backoff mechanism and add a maximum number of retries.
+
+#### Story 3
+
+In the story 3 scenario, after the evicted workload reached the maximum retry condition, 
+we want to easily requeue the workload to the queue without recreating the job. 
```

  And what is the high level solution for this?

- **keps/1282-pods-ready-requeue-strategy/README.md:132** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T21:06:24Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
```

  What do you mean by deactivated?

- **keps/1282-pods-ready-requeue-strategy/README.md:144** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T21:08:11Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
```

  I don't understand what this is for. Can you rephrase?

- **keps/1282-pods-ready-requeue-strategy/README.md:165** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T21:09:27Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount determines the number of times a workload has been requeued.
```

  ```suggestion
  	// requeuedCount records the number of times a workload has been requeued.
  ```

- **keps/1282-pods-ready-requeue-strategy/README.md:190** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T21:11:05Z
```diff
@@ -104,10 +178,40 @@ Currently, workloads within a ClusterQueue are sorted based on 1. Priority and 2
 
 #### Proposed Sorting
 
-The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) that controls which timestamp to return based on the configured ordering strategy. The same sorting logic would also be used when sorting the heads of queues.
+The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) 
+that controls which timestamp to return based on the configured ordering strategy.
+The same sorting logic would also be used when sorting the heads of queues.
 
 Update the `apis/config/<version>` package to include `Creation` and `Eviction` constants.
 
+### Exponential Backoff Mechanism
+
+When the kueueConfig `backOffLimitCount` or `backOffLimitTimeout` is set and there are evicted workloads by waitForPodsReady,
+the queueManager returns evicted workloads that an exponential backoff duration finished and other workloads as a headWorkloads.
```

  ```suggestion
  the queueManager holds evicted workloads with an exponential backoff. During this time, other workloads will have a chance to be admitted.
  ```

- **keps/1282-pods-ready-requeue-strategy/README.md:204** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T21:18:21Z
```diff
@@ -104,10 +178,40 @@ Currently, workloads within a ClusterQueue are sorted based on 1. Priority and 2
 
 #### Proposed Sorting
 
-The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) that controls which timestamp to return based on the configured ordering strategy. The same sorting logic would also be used when sorting the heads of queues.
+The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) 
+that controls which timestamp to return based on the configured ordering strategy.
+The same sorting logic would also be used when sorting the heads of queues.
 
 Update the `apis/config/<version>` package to include `Creation` and `Eviction` constants.
 
+### Exponential Backoff Mechanism
+
+When the kueueConfig `backOffLimitCount` or `backOffLimitTimeout` is set and there are evicted workloads by waitForPodsReady,
+the queueManager returns evicted workloads that an exponential backoff duration finished and other workloads as a headWorkloads.
+
+The queueManager calculates an exponential backoff duration by [the Step function](https://pkg.go.dev/k8s.io/apimachinery/pkg/util/wait@v0.29.1#Backoff.Step).
+
+### Evaluation of BackOffLimits
+
+#### backOffLimitCount
+
+When a workload eviction is issued with `PodsReadyTimeout` condition, 
+a workload `.status.requeuedCount` is incremented by 1 each time　in the workload controller.
+
+After that, when a workload `.status.requeudCount` reaches the kueueConfig `.waitForPodsReady.requeueingStrategy.backOffLimitCount`,
+a workload is deactivated by setting false to `.spec.active` instead of be suspended in the jobframework reconciler.
+
+#### backOffLimitTimeout
```

  What is the use case here?
  
  Why isn't the backoffLimitCount enough?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-01T21:24:55Z

- **keps/1282-pods-ready-requeue-strategy/README.md:86** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-01T21:24:55Z
```diff
@@ -56,6 +64,28 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+
+Specifically, the second reason will often occur in the workloads required gpus.
+Given that the workload with a request of 2 gpus is submitted to the cluster that
+has 2 worker nodes with 4 gpus, and 3 gpus are used (which means 1 gpu is free in each node),
+the workload will be repeatedly evicted because of the lack of resources in each node even though the cluster has enough capacities.
+
+In this case, to avoid rapid repetition of the admission and eviction cycle,
+the administrator would like to use an exponential backoff mechanism and add a maximum number of retries.
+
+#### Story 3
+
+In the story 3 scenario, after the evicted workload reached the maximum retry condition, 
+we want to easily requeue the workload to the queue without recreating the job. 
```

  I meant that using "Suspend a running Job without requeueing".
  
  https://github.com/kubernetes-sigs/kueue/blob/a8c11f351858cd159a3f823b15860918c0490cbc/apis/kueue/v1beta1/workload_types.go#L63-L72

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-01T21:25:27Z

- **keps/1282-pods-ready-requeue-strategy/README.md:132** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-01T21:25:27Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
```

  I  meant using "Suspend a running Job without requeueing" feature.

### Review by [@nstogner](https://github.com/nstogner) (commented) — 2024-02-01T21:29:40Z

- **keps/1282-pods-ready-requeue-strategy/README.md:75** by [@nstogner](https://github.com/nstogner) — 2024-02-01T21:29:40Z
```diff
@@ -56,6 +56,22 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingTimestamp=Eviction`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
```

  Yes, my comment was in regards to the situation where eviction was due to waitForPodsReady. If the requeuing timestamp is configured to be the creation timestamp, and all resource flavors are in use, without backoff there is a very high likelihood that the freshly evicted workload will be readmitted into the same resource flavor, resulting in flapping.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-01T21:33:43Z

- **keps/1282-pods-ready-requeue-strategy/README.md:204** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-01T21:33:43Z
```diff
@@ -104,10 +178,40 @@ Currently, workloads within a ClusterQueue are sorted based on 1. Priority and 2
 
 #### Proposed Sorting
 
-The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) that controls which timestamp to return based on the configured ordering strategy. The same sorting logic would also be used when sorting the heads of queues.
+The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) 
+that controls which timestamp to return based on the configured ordering strategy.
+The same sorting logic would also be used when sorting the heads of queues.
 
 Update the `apis/config/<version>` package to include `Creation` and `Eviction` constants.
 
+### Exponential Backoff Mechanism
+
+When the kueueConfig `backOffLimitCount` or `backOffLimitTimeout` is set and there are evicted workloads by waitForPodsReady,
+the queueManager returns evicted workloads that an exponential backoff duration finished and other workloads as a headWorkloads.
+
+The queueManager calculates an exponential backoff duration by [the Step function](https://pkg.go.dev/k8s.io/apimachinery/pkg/util/wait@v0.29.1#Backoff.Step).
+
+### Evaluation of BackOffLimits
+
+#### backOffLimitCount
+
+When a workload eviction is issued with `PodsReadyTimeout` condition, 
+a workload `.status.requeuedCount` is incremented by 1 each time　in the workload controller.
+
+After that, when a workload `.status.requeudCount` reaches the kueueConfig `.waitForPodsReady.requeueingStrategy.backOffLimitCount`,
+a workload is deactivated by setting false to `.spec.active` instead of be suspended in the jobframework reconciler.
+
+#### backOffLimitTimeout
```

  > Why isn't the backoffLimitCount enough?
  
  Because `backoffCount` is difficult to estimate how many hours jobs will actually be retrying. So, to improve the possibility for estimation, I added timeout separate from count.
  
  If you think that we shouldn't have `backoffLimitTimeout` separate from `backoffLimitCount`, I'm ok with removing `backoffLimitTimeout`.
  
  @alculquicondor WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-01T21:36:15Z

- **keps/1282-pods-ready-requeue-strategy/README.md:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-01T21:36:15Z
```diff
@@ -56,6 +56,22 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingTimestamp=Eviction`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
```

  >  case is still about waitForPodsReady timeout. Once the workload is evicted because it didn't become ready, there is the possibility that it won't fit in the first flavor, so it will be assigned back to the second flavor.
  
  Oh, it makes sense. I will add this story here.
  @alculquicondor @nstogner Thank you.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-01T21:51:27Z

- **keps/1282-pods-ready-requeue-strategy/README.md:204** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T21:51:27Z
```diff
@@ -104,10 +178,40 @@ Currently, workloads within a ClusterQueue are sorted based on 1. Priority and 2
 
 #### Proposed Sorting
 
-The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) that controls which timestamp to return based on the configured ordering strategy. The same sorting logic would also be used when sorting the heads of queues.
+The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) 
+that controls which timestamp to return based on the configured ordering strategy.
+The same sorting logic would also be used when sorting the heads of queues.
 
 Update the `apis/config/<version>` package to include `Creation` and `Eviction` constants.
 
+### Exponential Backoff Mechanism
+
+When the kueueConfig `backOffLimitCount` or `backOffLimitTimeout` is set and there are evicted workloads by waitForPodsReady,
+the queueManager returns evicted workloads that an exponential backoff duration finished and other workloads as a headWorkloads.
+
+The queueManager calculates an exponential backoff duration by [the Step function](https://pkg.go.dev/k8s.io/apimachinery/pkg/util/wait@v0.29.1#Backoff.Step).
+
+### Evaluation of BackOffLimits
+
+#### backOffLimitCount
+
+When a workload eviction is issued with `PodsReadyTimeout` condition, 
+a workload `.status.requeuedCount` is incremented by 1 each time　in the workload controller.
+
+After that, when a workload `.status.requeudCount` reaches the kueueConfig `.waitForPodsReady.requeueingStrategy.backOffLimitCount`,
+a workload is deactivated by setting false to `.spec.active` instead of be suspended in the jobframework reconciler.
+
+#### backOffLimitTimeout
```

  Let's start with less knobs.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-01T21:52:50Z

- **keps/1282-pods-ready-requeue-strategy/README.md:204** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-01T21:52:50Z
```diff
@@ -104,10 +178,40 @@ Currently, workloads within a ClusterQueue are sorted based on 1. Priority and 2
 
 #### Proposed Sorting
 
-The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) that controls which timestamp to return based on the configured ordering strategy. The same sorting logic would also be used when sorting the heads of queues.
+The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) 
+that controls which timestamp to return based on the configured ordering strategy.
+The same sorting logic would also be used when sorting the heads of queues.
 
 Update the `apis/config/<version>` package to include `Creation` and `Eviction` constants.
 
+### Exponential Backoff Mechanism
+
+When the kueueConfig `backOffLimitCount` or `backOffLimitTimeout` is set and there are evicted workloads by waitForPodsReady,
+the queueManager returns evicted workloads that an exponential backoff duration finished and other workloads as a headWorkloads.
+
+The queueManager calculates an exponential backoff duration by [the Step function](https://pkg.go.dev/k8s.io/apimachinery/pkg/util/wait@v0.29.1#Backoff.Step).
+
+### Evaluation of BackOffLimits
+
+#### backOffLimitCount
+
+When a workload eviction is issued with `PodsReadyTimeout` condition, 
+a workload `.status.requeuedCount` is incremented by 1 each time　in the workload controller.
+
+After that, when a workload `.status.requeudCount` reaches the kueueConfig `.waitForPodsReady.requeueingStrategy.backOffLimitCount`,
+a workload is deactivated by setting false to `.spec.active` instead of be suspended in the jobframework reconciler.
+
+#### backOffLimitTimeout
```

  SGTM
  We can start only support `backoffCount` for now.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-01T22:30:28Z

- **keps/1282-pods-ready-requeue-strategy/README.md:144** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-01T22:30:28Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
```

  I will remove this knob as we discussed here: https://github.com/kubernetes-sigs/kueue/pull/1608#discussion_r1475161093

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-02T20:05:10Z

- **keps/1282-pods-ready-requeue-strategy/README.md:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T20:05:10Z
```diff
@@ -56,6 +64,28 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+
+Specifically, the second reason will often occur in the workloads required gpus.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-02T20:05:32Z

- **keps/1282-pods-ready-requeue-strategy/README.md:165** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T20:05:32Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount determines the number of times a workload has been requeued.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-02T20:05:41Z

- **keps/1282-pods-ready-requeue-strategy/README.md:190** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T20:05:41Z
```diff
@@ -104,10 +178,40 @@ Currently, workloads within a ClusterQueue are sorted based on 1. Priority and 2
 
 #### Proposed Sorting
 
-The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) that controls which timestamp to return based on the configured ordering strategy. The same sorting logic would also be used when sorting the heads of queues.
+The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) 
+that controls which timestamp to return based on the configured ordering strategy.
+The same sorting logic would also be used when sorting the heads of queues.
 
 Update the `apis/config/<version>` package to include `Creation` and `Eviction` constants.
 
+### Exponential Backoff Mechanism
+
+When the kueueConfig `backOffLimitCount` or `backOffLimitTimeout` is set and there are evicted workloads by waitForPodsReady,
+the queueManager returns evicted workloads that an exponential backoff duration finished and other workloads as a headWorkloads.
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-05T15:17:34Z

- **keps/1282-pods-ready-requeue-strategy/README.md:86** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T15:16:40Z
```diff
@@ -56,6 +64,28 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+
+Specifically, the second reason will often occur in the workloads required gpus.
+Given that the workload with a request of 2 gpus is submitted to the cluster that
+has 2 worker nodes with 4 gpus, and 3 gpus are used (which means 1 gpu is free in each node),
+the workload will be repeatedly evicted because of the lack of resources in each node even though the cluster has enough capacities.
+
+In this case, to avoid rapid repetition of the admission and eviction cycle,
+the administrator would like to use an exponential backoff mechanism and add a maximum number of retries.
+
+#### Story 3
+
+In the story 3 scenario, after the evicted workload reached the maximum retry condition, 
+we want to easily requeue the workload to the queue without recreating the job. 
```

  Maybe put in the story something like:
  
  "This is possible if the Workload is deactivated (active=false), as opposed to deleting it".

- **keps/1282-pods-ready-requeue-strategy/README.md:132** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T15:17:12Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
```

  ```suggestion
  	// When the number is reached, the workload is deactivated (`.spec.active=false`).	
  ```

- **keps/1282-pods-ready-requeue-strategy/README.md:144** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T15:17:32Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
```

  Please remove so we can move forward with the KEP.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-05T15:18:09Z

- **keps/1282-pods-ready-requeue-strategy/README.md:138** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T15:18:09Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
```

  Please describe the implicit behavior of the backoff. What is the base backoff, and what is the exponent?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-05T18:12:00Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@astefanutti](https://github.com/astefanutti) — 2024-02-05T18:06:37Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  How about adding the last requeue / backoff timestamp and the backoff duration, so it's possible to know when the workload will be "retried", without recomputing the exponential backoff logic for an external observer?

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@astefanutti](https://github.com/astefanutti) — 2024-02-05T18:10:52Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  What are the invariants that make a workload in "backoff" state? Is it a combination of `PodsReadyTimeout` condition and `.status.requeuedCount > 0`?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-05T18:30:44Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T18:30:44Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  +1
  I wanted to keep the API as minimal as possible, but I agree that usability can be compromised without a `maxBackoff`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T18:49:28Z

- **keps/1282-pods-ready-requeue-strategy/README.md:86** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T18:49:28Z
```diff
@@ -56,6 +64,28 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+
+Specifically, the second reason will often occur in the workloads required gpus.
+Given that the workload with a request of 2 gpus is submitted to the cluster that
+has 2 worker nodes with 4 gpus, and 3 gpus are used (which means 1 gpu is free in each node),
+the workload will be repeatedly evicted because of the lack of resources in each node even though the cluster has enough capacities.
+
+In this case, to avoid rapid repetition of the admission and eviction cycle,
+the administrator would like to use an exponential backoff mechanism and add a maximum number of retries.
+
+#### Story 3
+
+In the story 3 scenario, after the evicted workload reached the maximum retry condition, 
+we want to easily requeue the workload to the queue without recreating the job. 
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T18:50:13Z

- **keps/1282-pods-ready-requeue-strategy/README.md:138** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T18:50:13Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T18:51:22Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T18:51:22Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  SGTM
  I will extend this API.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T22:13:28Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T22:13:28Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  Yes, that's right. Those parameters aren't configurable.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T23:24:47Z

- **keps/1282-pods-ready-requeue-strategy/README.md:144** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T23:24:47Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T23:25:19Z

- **keps/1282-pods-ready-requeue-strategy/README.md:204** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T23:25:19Z
```diff
@@ -104,10 +178,40 @@ Currently, workloads within a ClusterQueue are sorted based on 1. Priority and 2
 
 #### Proposed Sorting
 
-The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) that controls which timestamp to return based on the configured ordering strategy. The same sorting logic would also be used when sorting the heads of queues.
+The `pkg/workload` package could be modified to include a conditional (`if evictionReason == kueue.WorkloadEvictedByPodsReadyTimeout`) 
+that controls which timestamp to return based on the configured ordering strategy.
+The same sorting logic would also be used when sorting the heads of queues.
 
 Update the `apis/config/<version>` package to include `Creation` and `Eviction` constants.
 
+### Exponential Backoff Mechanism
+
+When the kueueConfig `backOffLimitCount` or `backOffLimitTimeout` is set and there are evicted workloads by waitForPodsReady,
+the queueManager returns evicted workloads that an exponential backoff duration finished and other workloads as a headWorkloads.
+
+The queueManager calculates an exponential backoff duration by [the Step function](https://pkg.go.dev/k8s.io/apimachinery/pkg/util/wait@v0.29.1#Backoff.Step).
+
+### Evaluation of BackOffLimits
+
+#### backOffLimitCount
+
+When a workload eviction is issued with `PodsReadyTimeout` condition, 
+a workload `.status.requeuedCount` is incremented by 1 each time　in the workload controller.
+
+After that, when a workload `.status.requeudCount` reaches the kueueConfig `.waitForPodsReady.requeueingStrategy.backOffLimitCount`,
+a workload is deactivated by setting false to `.spec.active` instead of be suspended in the jobframework reconciler.
+
+#### backOffLimitTimeout
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T23:42:14Z

- **keps/1282-pods-ready-requeue-strategy/README.md:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T23:42:14Z
```diff
@@ -56,6 +56,22 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingTimestamp=Eviction`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T23:45:04Z

- **keps/1282-pods-ready-requeue-strategy/README.md:132** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T23:45:04Z
```diff
@@ -75,25 +105,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T23:56:59Z

- **keps/1282-pods-ready-requeue-strategy/README.md:86** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T23:56:59Z
```diff
@@ -56,6 +64,28 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+
+Specifically, the second reason will often occur in the workloads required gpus.
+Given that the workload with a request of 2 gpus is submitted to the cluster that
+has 2 worker nodes with 4 gpus, and 3 gpus are used (which means 1 gpu is free in each node),
+the workload will be repeatedly evicted because of the lack of resources in each node even though the cluster has enough capacities.
+
+In this case, to avoid rapid repetition of the admission and eviction cycle,
+the administrator would like to use an exponential backoff mechanism and add a maximum number of retries.
+
+#### Story 3
+
+In the story 3 scenario, after the evicted workload reached the maximum retry condition, 
+we want to easily requeue the workload to the queue without recreating the job. 
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-06T14:22:30Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T14:22:29Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  Ah, there is one more thing. We won't expose the parameters to initialize Backoff: https://pkg.go.dev/k8s.io/apimachinery/pkg/util/wait@v0.29.1#Backoff

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-06T19:58:47Z

- **keps/1282-pods-ready-requeue-strategy/README.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:55:24Z
```diff
@@ -56,6 +66,31 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+3. Multiple flavors are matched for the workload, but the workload can not be launched on the backed flavors (which means non-primary flavor).
```

  was this always there? I don't really understand it.

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:58:18Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  can you add it? We are almost closing the release.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-06T20:02:25Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T20:02:25Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  I should do ASAP...

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-06T20:03:31Z

- **keps/1282-pods-ready-requeue-strategy/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T20:03:31Z
```diff
@@ -56,6 +66,31 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+3. Multiple flavors are matched for the workload, but the workload can not be launched on the backed flavors (which means non-primary flavor).
```

  I added this story built with https://github.com/kubernetes-sigs/kueue/pull/1608/files#r1460075048.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-06T20:45:26Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T20:45:26Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  @alculquicondor Here are quick API design sharing: Please let me know if the API design wouldn't be expected.
  
  ```golang
  type WorkloadStatus struct {
  	...
  	// requeueState records 
  	// 
  	// +optional
  	RequeueState *RequeueState `json:"requeueState,omitempty"` 
  }
  
  type RequeueState struct {
  	// count records the number of times a workload has been requeued.
  	// When a deactivated workload is reactivated, this count is reset to 0. 
  	//
  	// +optional
  	Count *int32 `json:"requeuedCount,omitempty"`
  
  	// +optional
  	LastBackoffTime metav1.Time `json:"lastRequeuedTime,omitempty"`
  	
  	// +optional
  	BackoffDuration metav1.Duration `json:"backoffDuration,omitempty"`
  }
  ```
  
  I'm organizing the above API.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-06T20:53:34Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T20:53:34Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  instead of `BackoffDuration` and `lastBackoffTime`, it might be better to have just `requeueAt metav1.Time` that indicates the time at which Kueue will consider this workload again for admission.
  
  The time when the workload was evicted is already visible in the condition.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-06T20:57:43Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T20:57:43Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  > instead of BackoffDuration and lastBackoffTime, it might be better to have just requeueAt metav1.Time that indicates the time at which Kueue will consider this workload again for admission.
  > 
  > The time when the workload was evicted is already visible in the condition.
  
  That makes sense. Even if we add only `requeueAt`, we can avoid recomputing the exponential backoff logic.
  After we truly want to have `duration` based on user feedback, we can extend API.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-06T21:17:43Z

- **keps/1282-pods-ready-requeue-strategy/README.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T21:17:43Z
```diff
@@ -56,6 +66,31 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+3. Multiple flavors are matched for the workload, but the workload can not be launched on the backed flavors (which means non-primary flavor).
```

  oh right. Let me rephrase:
  
  ```
  If there are multiple resource flavors that match the workload (for example, flavors 1 & 2)
  and the workload was running on flavor 2, it's likely that the workload will be readmitted
  on the same flavor indefinitely.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-06T21:19:25Z

- **keps/1282-pods-ready-requeue-strategy/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T21:19:24Z
```diff
@@ -56,6 +66,31 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+3. Multiple flavors are matched for the workload, but the workload can not be launched on the backed flavors (which means non-primary flavor).
```

  Thank you for this suggestion!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-06T21:34:11Z

- **keps/1282-pods-ready-requeue-strategy/README.md:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T21:34:11Z
```diff
@@ -75,25 +107,69 @@ Consider including folks who also work outside the SIG or subproject.
 
 ### API Changes
 
-Add an additional field to the Kueue ConfigMap to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
+#### KueueConfig
+
+Add an additional field to the KueueConfig to allow administrators to specify what timestamp to consider during queue sorting (under the pre-existing waitForPodsReady block).
 
 Possible settings:
 
 * `Eviction` (Back of queue)
 * `Creation` (Front of queue)
 
-```yaml
-kind: ConfigMap
-metadata:
-  name: kueue-manager-config
-  namespace: kueue-system
-data:
-  controller_manager_config.yaml: |
-    apiVersion: config.kueue.x-k8s.io/v1beta1
-    kind: Configuration
-    # ...
-    waitForPodsReady:
-      requeuingTimestamp: Creation | Eviction # <-- New field
+```go
+type WaitForPodsReady struct {
+	...
+	// requeuingStrategy defines the strategy for requeuing a Workload
+	// +optional
+	RequeuingStrategy *RequeuingStrategy `json:"requeuingStrategy,omitempty"`
+}
+
+type RequeuingStrategy struct {
+	// timestamp defines the timestamp used for requeuing a Workload
+	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// +optional
+	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+	
+	// backOffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated.	
+	//
+	// Defaults to null. 
+	// +optional
+	BackOffLimitCount *int32 `json:"backOffLimitCount,omitempty"`
+    
+	// backOffLimitTimeout defines the time for a workload that 
+	// has once been admitted to reach the PodsReady=true condition. 
+	// When the time is reached, the workload is deactivated.
+	// 	
+	// Defaults to null.
+	// +optional
+	BackOffLimitTimeout *int32 `json:"backOffLimitTimeout,omitempty"`
+}
+
+type RequeuingTimestamp string
+
+const (
+	// creationTimestamp timestamp (from Workload .metadata.creationTimestamp).
+	CreationTimestamp RequeuingTimestamp = "Creation"
+    
+	// evictionTimestamp timestamp (from Workload .status.conditions).
+	EvictionTimestamp RequeuingTimestamp = "Eviction"
+)
+```
+
+#### Workload
+
+Add a new field, "requeuedCount", to the Workload to allow recording the number of times a workload is requeued.
+
+```go
+type WorkloadStatus struct {
+	...
+	// requeuedCount records the number of times a workload has been requeued.
+	// When a deactivated workload is reactivated, this count is reset to 0. 
+	//
+	// +optional
+	RequeuedCount *int32 `json:"requeuedCount,omitempty"`
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-06T21:34:16Z

- **keps/1282-pods-ready-requeue-strategy/README.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-06T21:34:16Z
```diff
@@ -56,6 +66,31 @@ Consider the following scenario:
 
 In this case, the administrator would like the evicted workload to be requeued as soon as possible on the newly available capacity.
 
+#### Story 2
+
+In the story 1 scenario, when we set `waitForPodsReady.requeuingStrategy.timestamp=Creation`, 
+the workload endlessly or repeatedly can be put in front of the queue after eviction in the following eviction reasons:
+
+1. The workload don't have the proper configurations like image pull credential and pvc name, etc.
+2. The cluster can meet flavorQuotas, but each node doesn't have the resources that each podSet requests.  
+3. Multiple flavors are matched for the workload, but the workload can not be launched on the backed flavors (which means non-primary flavor).
```

  Done.
