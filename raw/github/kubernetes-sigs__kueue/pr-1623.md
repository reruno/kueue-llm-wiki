# PR #1623: Do not cleanup excess pods when awaiting to observe previous clean ups

**Summary**: Do not cleanup excess pods when awaiting to observe previous clean ups

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1623

**Last updated**: 2024-01-23T17:29:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `debug-pod-group` → `main`
- **Created**: 2024-01-19T19:15:05Z
- **Updated**: 2024-01-23T17:29:06Z
- **Merged**: 2024-01-23T17:29:03Z
- **Closed**: 2024-01-23T17:29:03Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 5   **+443 / -22**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

- After cleaning up pods, await to observe them in the event handlers before cleaning up more pods.
- When sorting pods for clean up, prefer the ones that are still gated or have lost their finalizer.

This prevents unnecessary cleanups in two scenarios:
- The user creates an excess pod when the group just was started. Previously, the reconciler might not have observed the pods that it ungated, deleting one of these pods.
- The user creates two replacement pods to fill up a Failed Pod. Previously, the reconciler might delete one the Pods in the first reconcile, and the other one in the second reconcile.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1593

#### Special notes for your reviewer:

Adding parallelism simplified handling of expectations when there are failures. But I extracted the changes specific to parallelism into a separate PR #1632

#### Does this PR introduce a user-facing change?

Unreleased feature

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-19T19:15:11Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | fd0c86d92c278c42815ac2ea4164f2e709ae325e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65afedaf73aba90008b735ab |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-22T17:03:45Z

Passed 1/5

/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-22T22:26:34Z

Passed 3/5

/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-22T22:31:14Z

/assign mimowo

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T13:47:51Z

Passed 4/5

/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-23T14:04:10Z

> /assign mimowo

ack

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T15:03:15Z

Passed 5/5

Bonus run:

/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T16:13:20Z

Rebased

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-23T16:13:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1623#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T17:02:37Z

/retest
Failure during building of the image, due to a temporary network problem.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-23T17:23:27Z

I suggested a follow up to demonstrate the calls to `ObservedUID` are necessary and correct when there is an error for the Update or Delete operations: https://github.com/kubernetes-sigs/kueue/pull/1623#discussion_r1463639856
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-23T17:23:33Z

LGTM label has been added.  <details>Git tree hash: d9e69ca2ece204ffa7b6483cfbe32f48b9b0cf5c</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:02:46Z

- **pkg/controller/jobs/pod/expectations.go:34** by [@mimowo](https://github.com/mimowo) — 2024-01-23T14:20:00Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package pod
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+)
+
+type uids = sets.Set[types.UID]
+
+// expectationsStore contains UIDs for which we are waiting to observe some change through event handlers.
+type expectationsStore struct {
+	sync.Mutex
+	name string
+
+	store map[types.NamespacedName]uids
```

  Could we reuse the `cache/thread_safe_store.go` which is used in the core k8s for expectations?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T16:12:35Z

- **pkg/controller/jobs/pod/expectations.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T16:12:35Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package pod
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+)
+
+type uids = sets.Set[types.UID]
+
+// expectationsStore contains UIDs for which we are waiting to observe some change through event handlers.
+type expectationsStore struct {
+	sync.Mutex
+	name string
+
+	store map[types.NamespacedName]uids
```

  I considered it, but it doesn't add much value, and it rather adds unnecessary castings.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:17:28Z

- **pkg/controller/jobs/pod/expectations.go:34** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:17:28Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package pod
+
+import (
+	"sync"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+)
+
+type uids = sets.Set[types.UID]
+
+// expectationsStore contains UIDs for which we are waiting to observe some change through event handlers.
+type expectationsStore struct {
+	sync.Mutex
+	name string
+
+	store map[types.NamespacedName]uids
```

  sgtm, it could be helpful if we were to use more functions from it, but the code is quite simple indeed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:31:11Z

- **pkg/controller/jobs/pod/pod_controller.go:731** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:27:29Z
```diff
@@ -705,14 +717,34 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 	if extraPodsCount <= 0 {
 		return nil
 	}
+	// Do not clean up more pods until observing previous operations
+	if !p.satisfiedExcessPods {
+		return errPendingOps
+	}
 
 	// Sort active pods by creation timestamp
 	sort.Slice(activePods, func(i, j int) bool {
-		return activePods[i].ObjectMeta.CreationTimestamp.Before(&activePods[j].ObjectMeta.CreationTimestamp)
+		pi := &activePods[i]
+		pj := &activePods[j]
+		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+		// Keep pods that have a finalizer
```

  Does it really mean `keep` or, just prioritize removal of pods without finalizer?

- **pkg/controller/jobs/pod/pod_controller.go:741** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:28:11Z
```diff
@@ -705,14 +717,34 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 	if extraPodsCount <= 0 {
 		return nil
 	}
+	// Do not clean up more pods until observing previous operations
+	if !p.satisfiedExcessPods {
+		return errPendingOps
+	}
 
 	// Sort active pods by creation timestamp
 	sort.Slice(activePods, func(i, j int) bool {
-		return activePods[i].ObjectMeta.CreationTimestamp.Before(&activePods[j].ObjectMeta.CreationTimestamp)
+		pi := &activePods[i]
+		pj := &activePods[j]
+		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+		// Keep pods that have a finalizer
+		if iFin != jFin {
+			return iFin
+		}
+		iGated := gateIndex(pi) != gateNotFound
+		jGated := gateIndex(pj) != gateNotFound
+		// keep pods that aren't gated.
+		if iGated != jGated {
+			return !iGated
+		}
+		return pi.ObjectMeta.CreationTimestamp.Before(&pj.ObjectMeta.CreationTimestamp)
```

  This would not break the tie if we have 2 excess pods, created in the same second. All pods have a finalizer, and all aren't gated. Can you confirm this is not an issue, and explain why?

- **pkg/controller/jobs/pod/pod_controller_test.go:2193** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:29:29Z
```diff
@@ -2091,6 +2092,180 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "another-group", Namespace: "ns"},
+				uids: []types.UID{"pod"},
+			}},
+		},
+		"waiting to observe previous deletion of excess pod, no pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "test-group", Namespace: "ns"},
+				uids: []types.UID{"pod2"},
+			}},
+		},
+		"delete excess pod that is gated": {
```

  Could we ever delete an excess pod if it is not gated? Possibly not related to the PR directly, just prior work, but would like to know.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T16:39:27Z

- **pkg/controller/jobs/pod/pod_controller.go:731** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T16:35:39Z
```diff
@@ -705,14 +717,34 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 	if extraPodsCount <= 0 {
 		return nil
 	}
+	// Do not clean up more pods until observing previous operations
+	if !p.satisfiedExcessPods {
+		return errPendingOps
+	}
 
 	// Sort active pods by creation timestamp
 	sort.Slice(activePods, func(i, j int) bool {
-		return activePods[i].ObjectMeta.CreationTimestamp.Before(&activePods[j].ObjectMeta.CreationTimestamp)
+		pi := &activePods[i]
+		pj := &activePods[j]
+		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+		// Keep pods that have a finalizer
```

  updated comment

- **pkg/controller/jobs/pod/pod_controller.go:741** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T16:38:33Z
```diff
@@ -705,14 +717,34 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 	if extraPodsCount <= 0 {
 		return nil
 	}
+	// Do not clean up more pods until observing previous operations
+	if !p.satisfiedExcessPods {
+		return errPendingOps
+	}
 
 	// Sort active pods by creation timestamp
 	sort.Slice(activePods, func(i, j int) bool {
-		return activePods[i].ObjectMeta.CreationTimestamp.Before(&activePods[j].ObjectMeta.CreationTimestamp)
+		pi := &activePods[i]
+		pj := &activePods[j]
+		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+		// Keep pods that have a finalizer
+		if iFin != jFin {
+			return iFin
+		}
+		iGated := gateIndex(pi) != gateNotFound
+		jGated := gateIndex(pj) != gateNotFound
+		// keep pods that aren't gated.
+		if iGated != jGated {
+			return !iGated
+		}
+		return pi.ObjectMeta.CreationTimestamp.Before(&pj.ObjectMeta.CreationTimestamp)
```

  In the first iteration, any of the 2 pods will be picked. In the second iteration, if we haven't observed the finalizer removal or the pod disappearing, then will return before deleting more pods.

- **pkg/controller/jobs/pod/pod_controller_test.go:2193** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T16:39:03Z
```diff
@@ -2091,6 +2092,180 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "another-group", Namespace: "ns"},
+				uids: []types.UID{"pod"},
+			}},
+		},
+		"waiting to observe previous deletion of excess pod, no pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "test-group", Namespace: "ns"},
+				uids: []types.UID{"pod2"},
+			}},
+		},
+		"delete excess pod that is gated": {
```

  yes, it can be deleted too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:41:05Z

- **pkg/controller/jobs/pod/pod_controller.go:731** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:41:05Z
```diff
@@ -705,14 +717,34 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 	if extraPodsCount <= 0 {
 		return nil
 	}
+	// Do not clean up more pods until observing previous operations
+	if !p.satisfiedExcessPods {
+		return errPendingOps
+	}
 
 	// Sort active pods by creation timestamp
 	sort.Slice(activePods, func(i, j int) bool {
-		return activePods[i].ObjectMeta.CreationTimestamp.Before(&activePods[j].ObjectMeta.CreationTimestamp)
+		pi := &activePods[i]
+		pj := &activePods[j]
+		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+		// Keep pods that have a finalizer
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:41:42Z

- **pkg/controller/jobs/pod/pod_controller.go:741** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:41:42Z
```diff
@@ -705,14 +717,34 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 	if extraPodsCount <= 0 {
 		return nil
 	}
+	// Do not clean up more pods until observing previous operations
+	if !p.satisfiedExcessPods {
+		return errPendingOps
+	}
 
 	// Sort active pods by creation timestamp
 	sort.Slice(activePods, func(i, j int) bool {
-		return activePods[i].ObjectMeta.CreationTimestamp.Before(&activePods[j].ObjectMeta.CreationTimestamp)
+		pi := &activePods[i]
+		pj := &activePods[j]
+		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+		// Keep pods that have a finalizer
+		if iFin != jFin {
+			return iFin
+		}
+		iGated := gateIndex(pi) != gateNotFound
+		jGated := gateIndex(pj) != gateNotFound
+		// keep pods that aren't gated.
+		if iGated != jGated {
+			return !iGated
+		}
+		return pi.ObjectMeta.CreationTimestamp.Before(&pj.ObjectMeta.CreationTimestamp)
```

  I see, sounds good

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:47:11Z

- **pkg/controller/jobs/pod/pod_controller_test.go:2193** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:47:11Z
```diff
@@ -2091,6 +2092,180 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "another-group", Namespace: "ns"},
+				uids: []types.UID{"pod"},
+			}},
+		},
+		"waiting to observe previous deletion of excess pod, no pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "test-group", Namespace: "ns"},
+				uids: []types.UID{"pod2"},
+			}},
+		},
+		"delete excess pod that is gated": {
```

  I see, makes sense. So, from correctness point of view we could delete any of them, but it is preferable to delete the one that is gated, because it hasn't started computations yet. The aim of this test case is to demonstrate that out of the candidates we pick the one that is gated. sgtm.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T16:48:16Z

- **pkg/controller/jobs/pod/event_handlers.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T16:48:16Z
```diff
@@ -54,11 +57,25 @@ func (h *podEventHandler) Update(ctx context.Context, e event.UpdateEvent, q wor
 }
 
 func (h *podEventHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	h.queueReconcileForPod(ctx, e.Object, q)
+	p, ok := e.Object.(*corev1.Pod)
+	if !ok {
+		return
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("pod", klog.KObj(p))
+
+	if g, isGroup := p.Labels[GroupNameLabel]; isGroup {
+		// If the watch was temporarily unavailable, it is possible that the object reported in the event still
+		// has a finalizer, but we can consider this Pod cleaned up, as it is being deleted.
+		h.cleanedUpPodsExpectations.ObservedUID(log, types.NamespacedName{Namespace: p.Namespace, Name: g}, p.UID)
```

  I decided to not check the finalizer in Delete, just in case.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T16:57:24Z

- **pkg/controller/jobs/pod/pod_controller_test.go:2193** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T16:57:24Z
```diff
@@ -2091,6 +2092,180 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "another-group", Namespace: "ns"},
+				uids: []types.UID{"pod"},
+			}},
+		},
+		"waiting to observe previous deletion of excess pod, no pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "test-group", Namespace: "ns"},
+				uids: []types.UID{"pod2"},
+			}},
+		},
+		"delete excess pod that is gated": {
```

  Exactly.
  
  Imagine the following scenario:
  1. 1 replacement pod is created. The reconciler ungates it.
  2. The user creates another replacement pod by mistake (so it's excess). This has the same timestamp as the previous pod.
  3. Kueue should delete the gated pod.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:57:58Z

- **pkg/controller/jobs/pod/pod_controller.go:764** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:57:57Z
```diff
@@ -721,12 +753,18 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 		if controllerutil.RemoveFinalizer(&pod, PodFinalizer) {
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
 			if err := c.Update(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
+				p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 				return err
 			}
 		}
 		if pod.ObjectMeta.DeletionTimestamp.IsZero() {
 			log.V(3).Info("Deleting excess pod in group", "excessPod", klog.KObj(&pod))
-			return c.Delete(ctx, &pod)
+			if err := c.Delete(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
```

  Is this comment accurate if we also call `ObservedUID` in reaction to Delete?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T16:58:05Z

- **pkg/controller/jobs/pod/pod_controller_test.go:2193** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T16:58:05Z
```diff
@@ -2091,6 +2092,180 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "another-group", Namespace: "ns"},
+				uids: []types.UID{"pod"},
+			}},
+		},
+		"waiting to observe previous deletion of excess pod, no pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "test-group", Namespace: "ns"},
+				uids: []types.UID{"pod2"},
+			}},
+		},
+		"delete excess pod that is gated": {
```

  Also, this is covered by the existing E2E test, as we create replacement pods without waiting.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:58:28Z

- **pkg/controller/jobs/pod/event_handlers.go:61** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:58:28Z
```diff
@@ -54,11 +57,25 @@ func (h *podEventHandler) Update(ctx context.Context, e event.UpdateEvent, q wor
 }
 
 func (h *podEventHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	h.queueReconcileForPod(ctx, e.Object, q)
+	p, ok := e.Object.(*corev1.Pod)
+	if !ok {
+		return
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("pod", klog.KObj(p))
+
+	if g, isGroup := p.Labels[GroupNameLabel]; isGroup {
+		// If the watch was temporarily unavailable, it is possible that the object reported in the event still
+		// has a finalizer, but we can consider this Pod cleaned up, as it is being deleted.
+		h.cleanedUpPodsExpectations.ObservedUID(log, types.NamespacedName{Namespace: p.Namespace, Name: g}, p.UID)
+	}
+
+	log.V(5).Info("Queueing reconcile for pod")
+
+	q.Add(reconcileRequestForPod(p))
 }
 
 func (h *podEventHandler) Generic(ctx context.Context, e event.GenericEvent, q workqueue.RateLimitingInterface) {
-	h.queueReconcileForPod(ctx, e.Object, q)
```

  Any reason to delete this?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T17:00:50Z

- **pkg/controller/jobs/pod/event_handlers.go:61** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T17:00:50Z
```diff
@@ -54,11 +57,25 @@ func (h *podEventHandler) Update(ctx context.Context, e event.UpdateEvent, q wor
 }
 
 func (h *podEventHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	h.queueReconcileForPod(ctx, e.Object, q)
+	p, ok := e.Object.(*corev1.Pod)
+	if !ok {
+		return
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("pod", klog.KObj(p))
+
+	if g, isGroup := p.Labels[GroupNameLabel]; isGroup {
+		// If the watch was temporarily unavailable, it is possible that the object reported in the event still
+		// has a finalizer, but we can consider this Pod cleaned up, as it is being deleted.
+		h.cleanedUpPodsExpectations.ObservedUID(log, types.NamespacedName{Namespace: p.Namespace, Name: g}, p.UID)
+	}
+
+	log.V(5).Info("Queueing reconcile for pod")
+
+	q.Add(reconcileRequestForPod(p))
 }
 
 func (h *podEventHandler) Generic(ctx context.Context, e event.GenericEvent, q workqueue.RateLimitingInterface) {
-	h.queueReconcileForPod(ctx, e.Object, q)
```

  It's just useless

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T17:01:58Z

- **pkg/controller/jobs/pod/pod_controller.go:764** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T17:01:58Z
```diff
@@ -721,12 +753,18 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 		if controllerutil.RemoveFinalizer(&pod, PodFinalizer) {
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
 			if err := c.Update(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
+				p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 				return err
 			}
 		}
 		if pod.ObjectMeta.DeletionTimestamp.IsZero() {
 			log.V(3).Info("Deleting excess pod in group", "excessPod", klog.KObj(&pod))
-			return c.Delete(ctx, &pod)
+			if err := c.Delete(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
```

  If we call Delete, we should expect to observe it. If the Delete call fails, then we won't observe it. The next reconcile will try to delete again.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T17:02:09Z

- **pkg/controller/jobs/pod/event_handlers.go:61** by [@mimowo](https://github.com/mimowo) — 2024-01-23T17:02:09Z
```diff
@@ -54,11 +57,25 @@ func (h *podEventHandler) Update(ctx context.Context, e event.UpdateEvent, q wor
 }
 
 func (h *podEventHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	h.queueReconcileForPod(ctx, e.Object, q)
+	p, ok := e.Object.(*corev1.Pod)
+	if !ok {
+		return
+	}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("pod", klog.KObj(p))
+
+	if g, isGroup := p.Labels[GroupNameLabel]; isGroup {
+		// If the watch was temporarily unavailable, it is possible that the object reported in the event still
+		// has a finalizer, but we can consider this Pod cleaned up, as it is being deleted.
+		h.cleanedUpPodsExpectations.ObservedUID(log, types.NamespacedName{Namespace: p.Namespace, Name: g}, p.UID)
+	}
+
+	log.V(5).Info("Queueing reconcile for pod")
+
+	q.Add(reconcileRequestForPod(p))
 }
 
 func (h *podEventHandler) Generic(ctx context.Context, e event.GenericEvent, q workqueue.RateLimitingInterface) {
-	h.queueReconcileForPod(ctx, e.Object, q)
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T17:02:17Z

- **pkg/controller/jobs/pod/pod_controller_test.go:2193** by [@mimowo](https://github.com/mimowo) — 2024-01-23T17:02:17Z
```diff
@@ -2091,6 +2092,180 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "another-group", Namespace: "ns"},
+				uids: []types.UID{"pod"},
+			}},
+		},
+		"waiting to observe previous deletion of excess pod, no pods are deleted": {
+			pods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			wantPods: []corev1.Pod{
+				*basePodWrapper.
+					Clone().
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					Group("test-group").
+					GroupTotalCount("1").
+					CreationTimestamp(now).
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod2").
+					UID("pod2").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+				*basePodWrapper.
+					Clone().
+					Name("pod3").
+					Label("kueue.x-k8s.io/managed", "true").
+					KueueFinalizer().
+					KueueSchedulingGate().
+					Group("test-group").
+					CreationTimestamp(now.Add(time.Minute)).
+					GroupTotalCount("1").
+					Obj(),
+			},
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("test-group", "ns").
+					PodSets(
+						*utiltesting.MakePodSet("b990493b", 1).
+							Request(corev1.ResourceCPU, "1").
+							Obj(),
+					).
+					Queue("user-queue").
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+					Admitted(true).
+					Obj(),
+			},
+			workloadCmpOpts: defaultWorkloadCmpOpts,
+			excessPodsExpectations: []keyUIDs{{
+				key:  types.NamespacedName{Name: "test-group", Namespace: "ns"},
+				uids: []types.UID{"pod2"},
+			}},
+		},
+		"delete excess pod that is gated": {
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T17:05:31Z

- **pkg/controller/jobs/pod/pod_controller.go:764** by [@mimowo](https://github.com/mimowo) — 2024-01-23T17:05:31Z
```diff
@@ -721,12 +753,18 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 		if controllerutil.RemoveFinalizer(&pod, PodFinalizer) {
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
 			if err := c.Update(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
+				p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 				return err
 			}
 		}
 		if pod.ObjectMeta.DeletionTimestamp.IsZero() {
 			log.V(3).Info("Deleting excess pod in group", "excessPod", klog.KObj(&pod))
-			return c.Delete(ctx, &pod)
+			if err := c.Delete(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
```

  ah, ok this a little bit tricky and does not seem covered by unit tests currently. I think it would be nice to cover, possibly in a follow up. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T17:06:34Z

- **pkg/controller/jobs/pod/pod_controller.go:764** by [@mimowo](https://github.com/mimowo) — 2024-01-23T17:06:34Z
```diff
@@ -721,12 +753,18 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 		if controllerutil.RemoveFinalizer(&pod, PodFinalizer) {
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
 			if err := c.Update(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
+				p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 				return err
 			}
 		}
 		if pod.ObjectMeta.DeletionTimestamp.IsZero() {
 			log.V(3).Info("Deleting excess pod in group", "excessPod", klog.KObj(&pod))
-			return c.Delete(ctx, &pod)
+			if err := c.Delete(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
```

  I mean the scenario when the Delete fails, so we wait for expectations, then when we re-attempt deleting the same pod. IOW: that this line is needed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T17:10:57Z

- **pkg/controller/jobs/pod/pod_controller.go:764** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T17:10:57Z
```diff
@@ -721,12 +753,18 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 		if controllerutil.RemoveFinalizer(&pod, PodFinalizer) {
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
 			if err := c.Update(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
+				p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 				return err
 			}
 		}
 		if pod.ObjectMeta.DeletionTimestamp.IsZero() {
 			log.V(3).Info("Deleting excess pod in group", "excessPod", klog.KObj(&pod))
-			return c.Delete(ctx, &pod)
+			if err := c.Delete(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
```

  If the Delete fails, we won't be waiting for expectations. This is a call to `ObservedUID`, not `ExpectUIDs`.
  
  But yes, we can follow up with unit tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T17:20:36Z

- **pkg/controller/jobs/pod/pod_controller.go:764** by [@mimowo](https://github.com/mimowo) — 2024-01-23T17:20:36Z
```diff
@@ -721,12 +753,18 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, totalCount
 		if controllerutil.RemoveFinalizer(&pod, PodFinalizer) {
 			log.V(3).Info("Finalizing excess pod in group", "excessPod", klog.KObj(&pod))
 			if err := c.Update(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
+				p.excessPodExpectations.ObservedUID(log, p.key, pod.UID)
 				return err
 			}
 		}
 		if pod.ObjectMeta.DeletionTimestamp.IsZero() {
 			log.V(3).Info("Deleting excess pod in group", "excessPod", klog.KObj(&pod))
-			return c.Delete(ctx, &pod)
+			if err := c.Delete(ctx, &pod); err != nil {
+				// We won't observe this cleanup in the event handler.
```

   Right, my mistake. sgtm
