# PR #1630: Utilize jobframework setups

**Summary**: Utilize jobframework setups

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1630

**Last updated**: 2024-01-24T21:35:46Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `utilize-jobframework-setup` → `main`
- **Created**: 2024-01-22T11:20:10Z
- **Updated**: 2024-01-24T21:35:46Z
- **Merged**: 2024-01-24T21:35:30Z
- **Closed**: 2024-01-24T21:35:30Z
- **Labels**: `release-note`, `lgtm`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 22   **+399 / -115**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:
I utilized setup functions for the jobframeworks so that the platform developers can avoid to copy those functions to separate in-house controllers.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part-of #1601

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Expose utilization functions to setup jobframework reconcilers and webhooks
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-22T11:20:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1630#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-22T11:20:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4ab6b5f225dc9661789aff57c85e1da0e0788c39 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65b16fabb36a49000820f33b |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-23T11:49:10Z

/lgtm
The remaining comment from me https://github.com/kubernetes-sigs/kueue/pull/1630/#discussion_r1462008437 isn't blocking.
/assign @alculquicondor

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-23T12:12:05Z

/hold 
As the unit tests fail

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-23T12:12:13Z

/lgtm cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T17:21:47Z

> @tenzen-y: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:
> 
> Test name	Commit	Details	Required	Rerun command
> pull-kueue-verify-main	[67ef390](https://github.com/kubernetes-sigs/kueue/commit/67ef390db19ac1df14714964e79dc3bd91b67b97)	[link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1630/pull-kueue-verify-main/1749767089805070336)	unknown	`/test pull-kueue-verify-main`
> pull-kueue-test-unit-main	[67ef390](https://github.com/kubernetes-sigs/kueue/commit/67ef390db19ac1df14714964e79dc3bd91b67b97)	[link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1630/pull-kueue-test-unit-main/1749767089754738688)	unknown	`/test pull-kueue-test-unit-main`
> [Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=1630). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Atenzen-y). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

It seems that I need to rebase this PR.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T17:57:15Z

/assign @alculquicondor

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T18:20:29Z

/hold cancel

@alculquicondor I rebased this PR. Please take another look.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T20:16:08Z

The above CI error occurred due to a connection error with the go proxy server.

> ../../../pkg/mod/github.com/onsi/ginkgo/v2@v2.15.0/ginkgo/labels/labels_command.go:15:2: golang.org/x/tools@v0.16.1: read "https:/proxy.golang.org/@v/v0.16.1.zip": stream error: stream ID 23; INTERNAL_ERROR; received from peer
make: *** [Makefile:322: ginkgo] Error 1

https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1630/pull-kueue-test-e2e-main-1-29/1750246773613400064

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T20:21:18Z

@alculquicondor I addressed all comments. PTAL, thanks.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-24T21:29:10Z

LGTM label has been added.  <details>Git tree hash: f0805ea43f86cb0c0af3ba4d381d4e41ebe05325</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-22T15:18:26Z

- **pkg/config/config.go:180** by [@mimowo](https://github.com/mimowo) — 2024-01-22T14:05:05Z
```diff
@@ -168,3 +169,14 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 	addTo(&options, &cfg)
 	return options, cfg, err
 }
+
+func EnabledFrameworks(cfg *configapi.Configuration) sets.Set[string] {
+	if cfg.Integrations == nil || len(cfg.Integrations.Frameworks) == 0 {
+		return nil
+	}
+	return sets.New(cfg.Integrations.Frameworks...)
+}
+
+func IsWaitForPodsReadyEnable(cfg *configapi.Configuration) bool {
```

  ```suggestion
  func IsWaitForPodsReadyEnabled(cfg *configapi.Configuration) bool {
  ```
  maybe this will read better

- **pkg/controller/jobframework/jobframework_test.go:158** by [@mimowo](https://github.com/mimowo) — 2024-01-22T14:30:38Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			builder := utiltesting.NewClientBuilder()
+			builder.WithObjects(append(tc.wls, &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: testNamespace}})...)
+
+			gotIndexerErr := SetupIndexes(ctx, utiltesting.AsIndexer(builder), tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotIndexerErr, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Fatalf("Unexpected setupIndexer error (-want,+got):\n%s", diff)
+			}
+
+			k8sClient := builder.Build()
+
+			gotWls := &kueue.WorkloadList{}
+			if tc.wantFieldMatcherError {
+				// Given that the `wantFieldMatcherError` is `true`, a list operation without fieldMatcher should succeed.
+				if gotListErr := k8sClient.List(ctx, gotWls, client.InNamespace(testNamespace)); gotListErr != nil {
```

  IIUC, this operation would succeed also when wantFieldMatcherError=false, right? If so, what is the purpose of this check and making it conditional?

- **pkg/controller/jobframework/jobframework_test.go:144** by [@mimowo](https://github.com/mimowo) — 2024-01-22T15:13:36Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
```

  Make sure the ctx is closed at the end of each test case

- **pkg/controller/jobframework/jobframework_test.go:66** by [@mimowo](https://github.com/mimowo) — 2024-01-22T15:15:29Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
```

  can we add a test case when kubeflow is enabled, but there is no mapper for it?

- **pkg/controller/jobframework/jobframework_test.go:109** by [@mimowo](https://github.com/mimowo) — 2024-01-22T15:17:30Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
```

  ```suggestion
  		wantWorkloads              []string
  ```
  nit

- **pkg/controller/jobframework/jobframework_test.go:105** by [@mimowo](https://github.com/mimowo) — 2024-01-22T15:18:07Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
```

  ```suggestion
  		workloads                   []client.Object
  ```
  nit

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T16:15:00Z

- **pkg/config/config.go:180** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T16:14:59Z
```diff
@@ -168,3 +169,14 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 	addTo(&options, &cfg)
 	return options, cfg, err
 }
+
+func EnabledFrameworks(cfg *configapi.Configuration) sets.Set[string] {
+	if cfg.Integrations == nil || len(cfg.Integrations.Frameworks) == 0 {
+		return nil
+	}
+	return sets.New(cfg.Integrations.Frameworks...)
+}
+
+func IsWaitForPodsReadyEnable(cfg *configapi.Configuration) bool {
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T16:23:54Z

- **pkg/controller/jobframework/jobframework_test.go:158** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T16:23:53Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			builder := utiltesting.NewClientBuilder()
+			builder.WithObjects(append(tc.wls, &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: testNamespace}})...)
+
+			gotIndexerErr := SetupIndexes(ctx, utiltesting.AsIndexer(builder), tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotIndexerErr, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Fatalf("Unexpected setupIndexer error (-want,+got):\n%s", diff)
+			}
+
+			k8sClient := builder.Build()
+
+			gotWls := &kueue.WorkloadList{}
+			if tc.wantFieldMatcherError {
+				// Given that the `wantFieldMatcherError` is `true`, a list operation without fieldMatcher should succeed.
+				if gotListErr := k8sClient.List(ctx, gotWls, client.InNamespace(testNamespace)); gotListErr != nil {
```

  > IIUC, this operation would succeed also when wantFieldMatcherError=false, right?
  
  Yes, that's right.
  
  > the purpose of this check
  
  I wanted to verify that this operation would succeeded when we don't use the `fieldMatcher`.
  We probably also should verify that the `gotWls` has the proper workloads here.
  
  > making it conditional?
  
  I wanted to avoid unnecessary checks when the `wantFieldMatcherError=false`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T16:27:35Z

- **pkg/controller/jobframework/jobframework_test.go:66** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T16:27:35Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
```

  Proving no errors occur would be valuable In that case. Thanks!
  
  https://github.com/kubernetes-sigs/kueue/blob/5d177736896c2bf56eb4dfdb0ce437f30d73e53d/pkg/controller/jobframework/jobframework.go#L52

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-22T16:36:42Z

- **pkg/controller/jobframework/jobframework_test.go:158** by [@mimowo](https://github.com/mimowo) — 2024-01-22T16:36:42Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			builder := utiltesting.NewClientBuilder()
+			builder.WithObjects(append(tc.wls, &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: testNamespace}})...)
+
+			gotIndexerErr := SetupIndexes(ctx, utiltesting.AsIndexer(builder), tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotIndexerErr, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Fatalf("Unexpected setupIndexer error (-want,+got):\n%s", diff)
+			}
+
+			k8sClient := builder.Build()
+
+			gotWls := &kueue.WorkloadList{}
+			if tc.wantFieldMatcherError {
+				// Given that the `wantFieldMatcherError` is `true`, a list operation without fieldMatcher should succeed.
+				if gotListErr := k8sClient.List(ctx, gotWls, client.InNamespace(testNamespace)); gotListErr != nil {
```

  > We probably also should verify that the gotWls has the proper workloads here.
  
  I think so.
  
  > I wanted to avoid unnecessary checks when the wantFieldMatcherError=false.
  
  I see, but maybe we can remove the conditional for simplicity. Or, alternatively, add a comment why it is checked.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T16:41:58Z

- **pkg/controller/jobframework/jobframework_test.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T16:41:58Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
```

  Does this mean the following?
  
  ```golang
  ctx, cancel := context.WithCancel(context.Background())
  defer cancel()
  ```
  
  IIUC, the `context.Background()` generates an empty context.
  So I think that we need not such a check.
  If my understanding isn't correct, let me know.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T16:44:57Z

- **pkg/controller/jobframework/jobframework_test.go:158** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T16:44:57Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			builder := utiltesting.NewClientBuilder()
+			builder.WithObjects(append(tc.wls, &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: testNamespace}})...)
+
+			gotIndexerErr := SetupIndexes(ctx, utiltesting.AsIndexer(builder), tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotIndexerErr, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Fatalf("Unexpected setupIndexer error (-want,+got):\n%s", diff)
+			}
+
+			k8sClient := builder.Build()
+
+			gotWls := &kueue.WorkloadList{}
+			if tc.wantFieldMatcherError {
+				// Given that the `wantFieldMatcherError` is `true`, a list operation without fieldMatcher should succeed.
+				if gotListErr := k8sClient.List(ctx, gotWls, client.InNamespace(testNamespace)); gotListErr != nil {
```

  > I see, but maybe we can remove the conditional for simplicity.
  
  I'm ok with removing the conditional since this is a unit test, and increasing the test execution time almost wouldn't happen.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T18:36:49Z

- **pkg/controller/jobframework/jobframework_test.go:158** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T18:36:49Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			builder := utiltesting.NewClientBuilder()
+			builder.WithObjects(append(tc.wls, &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: testNamespace}})...)
+
+			gotIndexerErr := SetupIndexes(ctx, utiltesting.AsIndexer(builder), tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotIndexerErr, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Fatalf("Unexpected setupIndexer error (-want,+got):\n%s", diff)
+			}
+
+			k8sClient := builder.Build()
+
+			gotWls := &kueue.WorkloadList{}
+			if tc.wantFieldMatcherError {
+				// Given that the `wantFieldMatcherError` is `true`, a list operation without fieldMatcher should succeed.
+				if gotListErr := k8sClient.List(ctx, gotWls, client.InNamespace(testNamespace)); gotListErr != nil {
```

  updated.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T18:36:55Z

- **pkg/controller/jobframework/jobframework_test.go:66** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T18:36:55Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T18:36:58Z

- **pkg/controller/jobframework/jobframework_test.go:109** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T18:36:57Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-22T18:37:00Z

- **pkg/controller/jobframework/jobframework_test.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-22T18:37:00Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T10:18:44Z

- **pkg/controller/jobframework/jobframework_test.go:144** by [@mimowo](https://github.com/mimowo) — 2024-01-23T10:18:44Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
```

  I see, it is empty. Still, we need to be careful passing it to other functions. When a function starts goroutines using the context they may leak because the context is not closed. Thus, it seems a safe practice to use the snippet as you pasted to make sure the context passed to any functions is closed after the test case.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T15:15:48Z

- **pkg/config/config.go:173** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T15:11:13Z
```diff
@@ -168,3 +169,14 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 	addTo(&options, &cfg)
 	return options, cfg, err
 }
+
+func EnabledFrameworks(cfg *configapi.Configuration) sets.Set[string] {
```

  ```suggestion
  func EnabledFrameworks(integrations *configapi.Integrations) sets.Set[string] {
  ```

- **pkg/config/config.go:180** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T15:11:52Z
```diff
@@ -168,3 +169,14 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 	addTo(&options, &cfg)
 	return options, cfg, err
 }
+
+func EnabledFrameworks(cfg *configapi.Configuration) sets.Set[string] {
+	if cfg.Integrations == nil || len(cfg.Integrations.Frameworks) == 0 {
+		return nil
+	}
+	return sets.New(cfg.Integrations.Frameworks...)
+}
+
+func IsWaitForPodsReadyEnabled(cfg *configapi.Configuration) bool {
```

  ```suggestion
  func IsWaitForPodsReadyEnabled(cfg *configapi.WaitForPodsReady) bool {
  ```

- **pkg/util/cert/cert.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T15:15:26Z
```diff
@@ -65,3 +66,9 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		RequireLeaderElection: false,
 	})
 }
+
+func WaitForCertsReady(setupLog logr.Logger, certsReady chan struct{}) {
```

  ```suggestion
  func WaitForCertsReady(log logr.Logger, certsReady chan struct{}) {
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T17:13:50Z

- **pkg/controller/jobframework/jobframework_test.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T17:13:49Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
```

  It makes sense. Let's make sure that the context is closed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T17:20:40Z

- **pkg/controller/jobframework/jobframework_test.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T17:20:40Z
```diff
@@ -0,0 +1,175 @@
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"net/http"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kubeflow "github.com/kubeflow/mpi-operator/pkg/apis/kubeflow/v2beta1"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayjobapi "github.com/ray-project/kuberay/ray-operator/apis/ray/v1alpha1"
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/rest"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	ctrlmgr "sigs.k8s.io/controller-runtime/pkg/manager"
+	jobset "sigs.k8s.io/jobset/api/jobset/v1alpha2"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestSetupControllers(t *testing.T) {
+	var errModifyOpts = errors.New("test error")
+
+	cases := map[string]struct {
+		opts       []Option
+		modifyOpts modifyOptions
+		mapperGVKs []schema.GroupVersionKind
+		wantError  error
+	}{
+		"setup controllers succeed": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) { return opts, nil },
+		},
+		"modifyOptions returns errors": {
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job", "kubeflow.org/mpijob")),
+			},
+			mapperGVKs: []schema.GroupVersionKind{
+				batchv1.SchemeGroupVersion.WithKind("Job"),
+				kubeflow.SchemeGroupVersionKind,
+			},
+			modifyOpts: func(fwkName string, opts ...Option) ([]Option, error) {
+				return opts, errModifyOpts
+			},
+			wantError: errModifyOpts,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, logger := utiltesting.ContextWithLog(t)
+			k8sClient := utiltesting.NewClientBuilder(jobset.AddToScheme, kubeflow.AddToScheme, rayjobapi.AddToScheme, kftraining.AddToScheme).Build()
+
+			mgrOpts := ctrlmgr.Options{
+				Scheme: k8sClient.Scheme(),
+				NewClient: func(*rest.Config, client.Options) (client.Client, error) {
+					return k8sClient, nil
+				},
+				MapperProvider: func(*rest.Config, *http.Client) (apimeta.RESTMapper, error) {
+					gvs := make([]schema.GroupVersion, len(tc.mapperGVKs))
+					for _, gvk := range tc.mapperGVKs {
+						gvs = append(gvs, gvk.GroupVersion())
+					}
+					mapper := apimeta.NewDefaultRESTMapper(gvs)
+					for _, gvk := range tc.mapperGVKs {
+						mapper.Add(gvk, apimeta.RESTScopeNamespace)
+					}
+					return mapper, nil
+				},
+			}
+			mgr, err := ctrlmgr.New(&rest.Config{}, mgrOpts)
+			if err != nil {
+				t.Fatalf("Failed to setup manager: %v", err)
+			}
+
+			certsReady := make(chan struct{})
+			close(certsReady)
+			gotError := SetupControllers(mgr, logger, tc.modifyOpts, certsReady, tc.opts...)
+			if diff := cmp.Diff(tc.wantError, gotError, cmpopts.EquateErrors()); len(diff) != 0 {
+				t.Errorf("Unexpected error from SetupControllers (-want,+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestSetupIndexes(t *testing.T) {
+	testNamespace := "test"
+
+	cases := map[string]struct {
+		opts                  []Option
+		wls                   []client.Object
+		filter                client.ListOption
+		wantError             error
+		wantFieldMatcherError bool
+		wantList              []string
+	}{
+		"proper indexes are set": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "alpha", "job", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "beta", "job", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:   client.MatchingFields{GetOwnerKey(batchv1.SchemeGroupVersion.WithKind("Job")): "alpha"},
+			wantList: []string{"alpha-wl"},
+		},
+		"kubeflow.org/mpijob is disabled in the configAPI": {
+			wls: []client.Object{
+				utiltesting.MakeWorkload("alpha-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "alpha", "mpijob", true, true).
+					Obj(),
+				utiltesting.MakeWorkload("beta-wl", testNamespace).
+					OwnerReference(kubeflow.SchemeGroupVersionKind, "beta", "mpijob", true, true).
+					Obj(),
+			},
+			opts: []Option{
+				WithEnabledFrameworks(sets.New("batch/job")),
+			},
+			filter:                client.MatchingFields{GetOwnerKey(kubeflow.SchemeGroupVersionKind): "alpha"},
+			wantFieldMatcherError: true,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T17:20:43Z

- **pkg/config/config.go:173** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T17:20:43Z
```diff
@@ -168,3 +169,14 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 	addTo(&options, &cfg)
 	return options, cfg, err
 }
+
+func EnabledFrameworks(cfg *configapi.Configuration) sets.Set[string] {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T17:20:45Z

- **pkg/config/config.go:180** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T17:20:45Z
```diff
@@ -168,3 +169,14 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 	addTo(&options, &cfg)
 	return options, cfg, err
 }
+
+func EnabledFrameworks(cfg *configapi.Configuration) sets.Set[string] {
+	if cfg.Integrations == nil || len(cfg.Integrations.Frameworks) == 0 {
+		return nil
+	}
+	return sets.New(cfg.Integrations.Frameworks...)
+}
+
+func IsWaitForPodsReadyEnabled(cfg *configapi.Configuration) bool {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T17:20:49Z

- **pkg/util/cert/cert.go:70** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T17:20:49Z
```diff
@@ -65,3 +66,9 @@ func ManageCerts(mgr ctrl.Manager, cfg config.Configuration, setupFinished chan
 		RequireLeaderElection: false,
 	})
 }
+
+func WaitForCertsReady(setupLog logr.Logger, certsReady chan struct{}) {
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T18:56:42Z

- **cmd/kueue/main.go:263** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T18:46:55Z
```diff
@@ -262,69 +253,38 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		}
 	}
 
-	manageJobsWithoutQueueName := cfg.ManageJobsWithoutQueueName
-
 	if failedWebhook, err := webhooks.Setup(mgr); err != nil {
 		setupLog.Error(err, "Unable to create webhook", "webhook", failedWebhook)
 		os.Exit(1)
 	}
 
 	opts := []jobframework.Option{
-		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		jobframework.WithManageJobsWithoutQueueName(cfg.ManageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(config.IsWaitForPodsReadyEnabled(cfg.WaitForPodsReady)),
```

  What about something like this?
  I don't think we should need two separate functions to process the configuration.
  
  ```suggestion
  		jobframework.WithWaitForPodsReady(cfg.WaitForPodsReady),
  ```

- **pkg/controller/jobframework/jobframework.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T18:47:45Z
```diff
@@ -0,0 +1,109 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+type modifyOptions func(fwkName string, opts ...Option) ([]Option, error)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(
+	mgr ctrl.Manager,
+	setupLog logr.Logger,
```

  ```suggestion
  	log logr.Logger,
  ```

- **cmd/kueue/main.go:282** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T18:56:21Z
```diff
@@ -262,69 +253,38 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		}
 	}
 
-	manageJobsWithoutQueueName := cfg.ManageJobsWithoutQueueName
-
 	if failedWebhook, err := webhooks.Setup(mgr); err != nil {
 		setupLog.Error(err, "Unable to create webhook", "webhook", failedWebhook)
 		os.Exit(1)
 	}
 
 	opts := []jobframework.Option{
-		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		jobframework.WithManageJobsWithoutQueueName(cfg.ManageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(config.IsWaitForPodsReadyEnabled(cfg.WaitForPodsReady)),
 		jobframework.WithKubeServerVersion(serverVersionFetcher),
+		jobframework.WithEnabledFrameworks(config.EnabledFrameworks(cfg.Integrations)),
+		jobframework.WithManagerName(constants.KueueName),
 	}
-	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
-		log := setupLog.WithValues("jobFrameworkName", name)
-		if isFrameworkEnabled(cfg, name) {
-			gvk, err := apiutil.GVKForObject(cb.JobType, mgr.GetScheme())
-			if err != nil {
-				return err
-			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
-				if !meta.IsNoMatchError(err) {
-					return err
-				}
-				log.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
-			} else {
-				if err = cb.NewReconciler(
-					mgr.GetClient(),
-					mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, constants.KueueName)),
-					opts...,
-				).SetupWithManager(mgr); err != nil {
-					log.Error(err, "Unable to create controller")
-					return err
-				}
-				if name == "pod" {
-					v := serverVersionFetcher.GetServerVersion()
-					if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
-						setupLog.Error(errPodIntegration,
-							"Failed to configure reconcilers",
-							"kubernetesVersion", v)
-						os.Exit(1)
-					}
-
-					opts = append(
-						opts,
-						jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
-						jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
-					)
-				}
-				if err = cb.SetupWebhook(mgr, opts...); err != nil {
-					log.Error(err, "Unable to create webhook")
-					return err
-				}
-				log.Info("Set up controller and webhook for job framework")
-				return nil
-			}
+	err := jobframework.SetupControllers(mgr, setupLog, func(fwkName string, opts ...jobframework.Option) ([]jobframework.Option, error) {
+		options := jobframework.DefaultOptions
+		for _, opt := range opts {
+			opt(&options)
 		}
-		if err := noop.SetupWebhook(mgr, cb.JobType); err != nil {
-			log.Error(err, "Unable to create noop webhook")
-			return err
+		if fwkName == "pod" {
+			v := options.KubeServerVersion.GetServerVersion()
+			if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
+				return opts, errPodIntegration
+			}
+			return append(
+				opts,
+				jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
+				jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
+			), nil
```

  I really don't like this... It might be better if `SetupWebhook` accepts an `any` argument that it can then cast to whatever struct it understands.
  
  Then we could have something like: `jobframework.WithIntegrationOptions("pod", cfg.Integrations.PodOptions)`.
  
  We did something similar to pass arguments to different plugins in kube-scheduler https://github.com/kubernetes/kubernetes/blob/0f817dcd6577b663f6cb22cd5080f14138e8b6ac/pkg/scheduler/framework/plugins/interpodaffinity/plugin.go#L73
  https://github.com/kubernetes/kubernetes/blob/0f817dcd6577b663f6cb22cd5080f14138e8b6ac/pkg/scheduler/framework/plugins/interpodaffinity/plugin.go#L95

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T19:06:10Z

- **cmd/kueue/main.go:263** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T19:06:10Z
```diff
@@ -262,69 +253,38 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		}
 	}
 
-	manageJobsWithoutQueueName := cfg.ManageJobsWithoutQueueName
-
 	if failedWebhook, err := webhooks.Setup(mgr); err != nil {
 		setupLog.Error(err, "Unable to create webhook", "webhook", failedWebhook)
 		os.Exit(1)
 	}
 
 	opts := []jobframework.Option{
-		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		jobframework.WithManageJobsWithoutQueueName(cfg.ManageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(config.IsWaitForPodsReadyEnabled(cfg.WaitForPodsReady)),
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T19:14:02Z

- **cmd/kueue/main.go:282** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T19:14:02Z
```diff
@@ -262,69 +253,38 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		}
 	}
 
-	manageJobsWithoutQueueName := cfg.ManageJobsWithoutQueueName
-
 	if failedWebhook, err := webhooks.Setup(mgr); err != nil {
 		setupLog.Error(err, "Unable to create webhook", "webhook", failedWebhook)
 		os.Exit(1)
 	}
 
 	opts := []jobframework.Option{
-		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		jobframework.WithManageJobsWithoutQueueName(cfg.ManageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(config.IsWaitForPodsReadyEnabled(cfg.WaitForPodsReady)),
 		jobframework.WithKubeServerVersion(serverVersionFetcher),
+		jobframework.WithEnabledFrameworks(config.EnabledFrameworks(cfg.Integrations)),
+		jobframework.WithManagerName(constants.KueueName),
 	}
-	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
-		log := setupLog.WithValues("jobFrameworkName", name)
-		if isFrameworkEnabled(cfg, name) {
-			gvk, err := apiutil.GVKForObject(cb.JobType, mgr.GetScheme())
-			if err != nil {
-				return err
-			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
-				if !meta.IsNoMatchError(err) {
-					return err
-				}
-				log.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
-			} else {
-				if err = cb.NewReconciler(
-					mgr.GetClient(),
-					mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, constants.KueueName)),
-					opts...,
-				).SetupWithManager(mgr); err != nil {
-					log.Error(err, "Unable to create controller")
-					return err
-				}
-				if name == "pod" {
-					v := serverVersionFetcher.GetServerVersion()
-					if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
-						setupLog.Error(errPodIntegration,
-							"Failed to configure reconcilers",
-							"kubernetesVersion", v)
-						os.Exit(1)
-					}
-
-					opts = append(
-						opts,
-						jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
-						jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
-					)
-				}
-				if err = cb.SetupWebhook(mgr, opts...); err != nil {
-					log.Error(err, "Unable to create webhook")
-					return err
-				}
-				log.Info("Set up controller and webhook for job framework")
-				return nil
-			}
+	err := jobframework.SetupControllers(mgr, setupLog, func(fwkName string, opts ...jobframework.Option) ([]jobframework.Option, error) {
+		options := jobframework.DefaultOptions
+		for _, opt := range opts {
+			opt(&options)
 		}
-		if err := noop.SetupWebhook(mgr, cb.JobType); err != nil {
-			log.Error(err, "Unable to create noop webhook")
-			return err
+		if fwkName == "pod" {
+			v := options.KubeServerVersion.GetServerVersion()
+			if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
+				return opts, errPodIntegration
+			}
+			return append(
+				opts,
+				jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
+				jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
+			), nil
```

  It sounds reasonable, but I think that working on its refactoring in another PR (before or after) would be better. 
  WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-23T19:20:41Z

- **cmd/kueue/main.go:282** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-23T19:20:41Z
```diff
@@ -262,69 +253,38 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		}
 	}
 
-	manageJobsWithoutQueueName := cfg.ManageJobsWithoutQueueName
-
 	if failedWebhook, err := webhooks.Setup(mgr); err != nil {
 		setupLog.Error(err, "Unable to create webhook", "webhook", failedWebhook)
 		os.Exit(1)
 	}
 
 	opts := []jobframework.Option{
-		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		jobframework.WithManageJobsWithoutQueueName(cfg.ManageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(config.IsWaitForPodsReadyEnabled(cfg.WaitForPodsReady)),
 		jobframework.WithKubeServerVersion(serverVersionFetcher),
+		jobframework.WithEnabledFrameworks(config.EnabledFrameworks(cfg.Integrations)),
+		jobframework.WithManagerName(constants.KueueName),
 	}
-	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
-		log := setupLog.WithValues("jobFrameworkName", name)
-		if isFrameworkEnabled(cfg, name) {
-			gvk, err := apiutil.GVKForObject(cb.JobType, mgr.GetScheme())
-			if err != nil {
-				return err
-			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
-				if !meta.IsNoMatchError(err) {
-					return err
-				}
-				log.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
-			} else {
-				if err = cb.NewReconciler(
-					mgr.GetClient(),
-					mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, constants.KueueName)),
-					opts...,
-				).SetupWithManager(mgr); err != nil {
-					log.Error(err, "Unable to create controller")
-					return err
-				}
-				if name == "pod" {
-					v := serverVersionFetcher.GetServerVersion()
-					if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
-						setupLog.Error(errPodIntegration,
-							"Failed to configure reconcilers",
-							"kubernetesVersion", v)
-						os.Exit(1)
-					}
-
-					opts = append(
-						opts,
-						jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
-						jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
-					)
-				}
-				if err = cb.SetupWebhook(mgr, opts...); err != nil {
-					log.Error(err, "Unable to create webhook")
-					return err
-				}
-				log.Info("Set up controller and webhook for job framework")
-				return nil
-			}
+	err := jobframework.SetupControllers(mgr, setupLog, func(fwkName string, opts ...jobframework.Option) ([]jobframework.Option, error) {
+		options := jobframework.DefaultOptions
+		for _, opt := range opts {
+			opt(&options)
 		}
-		if err := noop.SetupWebhook(mgr, cb.JobType); err != nil {
-			log.Error(err, "Unable to create noop webhook")
-			return err
+		if fwkName == "pod" {
+			v := options.KubeServerVersion.GetServerVersion()
+			if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
+				return opts, errPodIntegration
+			}
+			return append(
+				opts,
+				jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
+				jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
+			), nil
```

  I see it more as a prerequisite PR. Unless you can commit to get it done this week, I prefer not to merge this PR.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T19:23:48Z

- **cmd/kueue/main.go:282** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T19:23:48Z
```diff
@@ -262,69 +253,38 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		}
 	}
 
-	manageJobsWithoutQueueName := cfg.ManageJobsWithoutQueueName
-
 	if failedWebhook, err := webhooks.Setup(mgr); err != nil {
 		setupLog.Error(err, "Unable to create webhook", "webhook", failedWebhook)
 		os.Exit(1)
 	}
 
 	opts := []jobframework.Option{
-		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		jobframework.WithManageJobsWithoutQueueName(cfg.ManageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(config.IsWaitForPodsReadyEnabled(cfg.WaitForPodsReady)),
 		jobframework.WithKubeServerVersion(serverVersionFetcher),
+		jobframework.WithEnabledFrameworks(config.EnabledFrameworks(cfg.Integrations)),
+		jobframework.WithManagerName(constants.KueueName),
 	}
-	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
-		log := setupLog.WithValues("jobFrameworkName", name)
-		if isFrameworkEnabled(cfg, name) {
-			gvk, err := apiutil.GVKForObject(cb.JobType, mgr.GetScheme())
-			if err != nil {
-				return err
-			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
-				if !meta.IsNoMatchError(err) {
-					return err
-				}
-				log.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
-			} else {
-				if err = cb.NewReconciler(
-					mgr.GetClient(),
-					mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, constants.KueueName)),
-					opts...,
-				).SetupWithManager(mgr); err != nil {
-					log.Error(err, "Unable to create controller")
-					return err
-				}
-				if name == "pod" {
-					v := serverVersionFetcher.GetServerVersion()
-					if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
-						setupLog.Error(errPodIntegration,
-							"Failed to configure reconcilers",
-							"kubernetesVersion", v)
-						os.Exit(1)
-					}
-
-					opts = append(
-						opts,
-						jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
-						jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
-					)
-				}
-				if err = cb.SetupWebhook(mgr, opts...); err != nil {
-					log.Error(err, "Unable to create webhook")
-					return err
-				}
-				log.Info("Set up controller and webhook for job framework")
-				return nil
-			}
+	err := jobframework.SetupControllers(mgr, setupLog, func(fwkName string, opts ...jobframework.Option) ([]jobframework.Option, error) {
+		options := jobframework.DefaultOptions
+		for _, opt := range opts {
+			opt(&options)
 		}
-		if err := noop.SetupWebhook(mgr, cb.JobType); err != nil {
-			log.Error(err, "Unable to create noop webhook")
-			return err
+		if fwkName == "pod" {
+			v := options.KubeServerVersion.GetServerVersion()
+			if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
+				return opts, errPodIntegration
+			}
+			return append(
+				opts,
+				jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
+				jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
+			), nil
```

  I'm ok with taking this as a prerequisite. Let's hold this PR until we finalize this refactoring.
  /hold

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T19:49:45Z

- **cmd/kueue/main.go:263** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T19:49:45Z
```diff
@@ -262,69 +253,38 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		}
 	}
 
-	manageJobsWithoutQueueName := cfg.ManageJobsWithoutQueueName
-
 	if failedWebhook, err := webhooks.Setup(mgr); err != nil {
 		setupLog.Error(err, "Unable to create webhook", "webhook", failedWebhook)
 		os.Exit(1)
 	}
 
 	opts := []jobframework.Option{
-		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		jobframework.WithManageJobsWithoutQueueName(cfg.ManageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(config.IsWaitForPodsReadyEnabled(cfg.WaitForPodsReady)),
```

  Also I think we can apply the same way to `EnabledFrameworks`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T20:58:26Z

- **cmd/kueue/main.go:263** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T20:58:26Z
```diff
@@ -262,69 +253,38 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		}
 	}
 
-	manageJobsWithoutQueueName := cfg.ManageJobsWithoutQueueName
-
 	if failedWebhook, err := webhooks.Setup(mgr); err != nil {
 		setupLog.Error(err, "Unable to create webhook", "webhook", failedWebhook)
 		os.Exit(1)
 	}
 
 	opts := []jobframework.Option{
-		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		jobframework.WithManageJobsWithoutQueueName(cfg.ManageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(config.IsWaitForPodsReadyEnabled(cfg.WaitForPodsReady)),
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-23T21:00:24Z

- **pkg/controller/jobframework/jobframework.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-23T21:00:24Z
```diff
@@ -0,0 +1,109 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+type modifyOptions func(fwkName string, opts ...Option) ([]Option, error)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(
+	mgr ctrl.Manager,
+	setupLog logr.Logger,
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-24T13:58:48Z

- **cmd/kueue/main.go:282** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T13:58:48Z
```diff
@@ -262,69 +253,38 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 		}
 	}
 
-	manageJobsWithoutQueueName := cfg.ManageJobsWithoutQueueName
-
 	if failedWebhook, err := webhooks.Setup(mgr); err != nil {
 		setupLog.Error(err, "Unable to create webhook", "webhook", failedWebhook)
 		os.Exit(1)
 	}
 
 	opts := []jobframework.Option{
-		jobframework.WithManageJobsWithoutQueueName(manageJobsWithoutQueueName),
-		jobframework.WithWaitForPodsReady(waitForPodsReady(cfg)),
+		jobframework.WithManageJobsWithoutQueueName(cfg.ManageJobsWithoutQueueName),
+		jobframework.WithWaitForPodsReady(config.IsWaitForPodsReadyEnabled(cfg.WaitForPodsReady)),
 		jobframework.WithKubeServerVersion(serverVersionFetcher),
+		jobframework.WithEnabledFrameworks(config.EnabledFrameworks(cfg.Integrations)),
+		jobframework.WithManagerName(constants.KueueName),
 	}
-	err := jobframework.ForEachIntegration(func(name string, cb jobframework.IntegrationCallbacks) error {
-		log := setupLog.WithValues("jobFrameworkName", name)
-		if isFrameworkEnabled(cfg, name) {
-			gvk, err := apiutil.GVKForObject(cb.JobType, mgr.GetScheme())
-			if err != nil {
-				return err
-			}
-			if _, err = mgr.GetRESTMapper().RESTMapping(gvk.GroupKind(), gvk.Version); err != nil {
-				if !meta.IsNoMatchError(err) {
-					return err
-				}
-				log.Info("No matching API in the server for job framework, skipped setup of controller and webhook")
-			} else {
-				if err = cb.NewReconciler(
-					mgr.GetClient(),
-					mgr.GetEventRecorderFor(fmt.Sprintf("%s-%s-controller", name, constants.KueueName)),
-					opts...,
-				).SetupWithManager(mgr); err != nil {
-					log.Error(err, "Unable to create controller")
-					return err
-				}
-				if name == "pod" {
-					v := serverVersionFetcher.GetServerVersion()
-					if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
-						setupLog.Error(errPodIntegration,
-							"Failed to configure reconcilers",
-							"kubernetesVersion", v)
-						os.Exit(1)
-					}
-
-					opts = append(
-						opts,
-						jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
-						jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
-					)
-				}
-				if err = cb.SetupWebhook(mgr, opts...); err != nil {
-					log.Error(err, "Unable to create webhook")
-					return err
-				}
-				log.Info("Set up controller and webhook for job framework")
-				return nil
-			}
+	err := jobframework.SetupControllers(mgr, setupLog, func(fwkName string, opts ...jobframework.Option) ([]jobframework.Option, error) {
+		options := jobframework.DefaultOptions
+		for _, opt := range opts {
+			opt(&options)
 		}
-		if err := noop.SetupWebhook(mgr, cb.JobType); err != nil {
-			log.Error(err, "Unable to create noop webhook")
-			return err
+		if fwkName == "pod" {
+			v := options.KubeServerVersion.GetServerVersion()
+			if v.String() == "" || v.LessThan(kubeversion.KubeVersion1_27) {
+				return opts, errPodIntegration
+			}
+			return append(
+				opts,
+				jobframework.WithPodNamespaceSelector(cfg.Integrations.PodOptions.NamespaceSelector),
+				jobframework.WithPodSelector(cfg.Integrations.PodOptions.PodSelector),
+			), nil
```

  @alculquicondor I opened a PR here: #1637

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-24T19:23:37Z

- **pkg/controller/jobframework/jobframework.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-24T19:19:45Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(mgr ctrl.Manager, log logr.Logger, certsReady chan struct{}, opts ...Option) error {
+	// The controllers won't work until the webhooks are operating, and the webhook won't work until the
+	// certs are all in place.
+	cert.WaitForCertsReady(log, certsReady)
```

  Did you forget to remove this from main?
  
  Or maybe it shouldn't be here.

- **pkg/controller/jobframework/jobframework.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-24T19:21:08Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(mgr ctrl.Manager, log logr.Logger, certsReady chan struct{}, opts ...Option) error {
```

  Add comments for how/when this should be used

- **pkg/controller/jobframework/jobframework.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-24T19:22:45Z
  maybe this file should be named `setup.go`?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-24T19:34:11Z

- **pkg/controller/jobframework/jobframework.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T19:34:11Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(mgr ctrl.Manager, log logr.Logger, certsReady chan struct{}, opts ...Option) error {
+	// The controllers won't work until the webhooks are operating, and the webhook won't work until the
+	// certs are all in place.
+	cert.WaitForCertsReady(log, certsReady)
```

  Both here and the main one is intended although I should replace https://github.com/kubernetes-sigs/kueue/blob/78acd5305dc023002f87a221afc32d1cd5ca32c3/cmd/kueue/main.go#L222-L226 with `cert.WaitForCertsReady().`
  
  I think we need to perform `cert.WaitForCertsReady()` here since in the in-house custom job kueue-manager, only this function launches controllers and webhooks.
  
  However, the kueue-manager launches controllers and webooks in this function and main's `setupControllers()`.
  
  So, I think both Is needed. @alculquicondor WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-24T19:34:24Z

- **pkg/controller/jobframework/jobframework.go:39** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T19:34:24Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(mgr ctrl.Manager, log logr.Logger, certsReady chan struct{}, opts ...Option) error {
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-24T19:34:46Z

- **pkg/controller/jobframework/jobframework.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T19:34:45Z
  SGTM

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-24T19:52:31Z

- **pkg/controller/jobframework/jobframework.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-24T19:52:30Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(mgr ctrl.Manager, log logr.Logger, certsReady chan struct{}, opts ...Option) error {
+	// The controllers won't work until the webhooks are operating, and the webhook won't work until the
+	// certs are all in place.
+	cert.WaitForCertsReady(log, certsReady)
```

  yeah, but maybe they didn't setup certs at all? I think they can call the function if they need it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-24T19:56:54Z

- **pkg/controller/jobframework/jobframework.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T19:56:54Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(mgr ctrl.Manager, log logr.Logger, certsReady chan struct{}, opts ...Option) error {
+	// The controllers won't work until the webhooks are operating, and the webhook won't work until the
+	// certs are all in place.
+	cert.WaitForCertsReady(log, certsReady)
```

  It makes sense. Let's remove it from here. 
  They can call `WaitForCertsReady()` since it is an exported one.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-24T20:14:37Z

- **pkg/controller/jobframework/jobframework.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T20:14:37Z
  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-24T20:14:40Z

- **pkg/controller/jobframework/jobframework.go:39** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T20:14:40Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(mgr ctrl.Manager, log logr.Logger, certsReady chan struct{}, opts ...Option) error {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-24T20:14:44Z

- **pkg/controller/jobframework/jobframework.go:42** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-24T20:14:44Z
```diff
@@ -0,0 +1,97 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"os"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/meta"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobs/noop"
+	"sigs.k8s.io/kueue/pkg/util/cert"
+)
+
+var (
+	errFailedMappingResource = errors.New("restMapper failed mapping resource")
+)
+
+func SetupControllers(mgr ctrl.Manager, log logr.Logger, certsReady chan struct{}, opts ...Option) error {
+	// The controllers won't work until the webhooks are operating, and the webhook won't work until the
+	// certs are all in place.
+	cert.WaitForCertsReady(log, certsReady)
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-24T21:29:05Z

/lgtm
