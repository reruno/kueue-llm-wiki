# PR #1631: Top-level MultiKueue cluster API

**Summary**: Top-level MultiKueue cluster API

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1631

**Last updated**: 2024-02-08T07:00:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `multikueue-ckuster-object` → `main`
- **Created**: 2024-01-22T13:15:27Z
- **Updated**: 2024-02-08T07:00:25Z
- **Merged**: 2024-01-29T08:19:49Z
- **Closed**: 2024-01-29T08:19:49Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 42   **+2493 / -971**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Makes MultiKueueCluster a top level API object and adds a reconciler for it.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to  #693 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-22T13:15:30Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-22T13:15:34Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1643981cb878cd71c9a7903fe8e4621a55aa3d83 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65b744dd7f7d8e000807549e |

### Comment by [@trasc](https://github.com/trasc) — 2024-01-22T14:17:48Z

/test all

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:37:15Z

/retitle Top-level MultiKueue cluster API

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-26T17:45:11Z

/approve

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-26T18:22:40Z

Maybe this solves the flakiness of #1658

/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-26T18:35:40Z

Another pass:

/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T19:55:51Z

@trasc Remaining comment only here: https://github.com/kubernetes-sigs/kueue/pull/1631#discussion_r1468079754

### Comment by [@trasc](https://github.com/trasc) — 2024-01-26T20:12:44Z

> @trasc Remaining comment only here: https://github.com/kubernetes-sigs/kueue/pull/1631#discussion_r1468079754

Is that a blocker?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T20:15:16Z

> > @trasc Remaining comment only here: [#1631 (comment)](https://github.com/kubernetes-sigs/kueue/pull/1631#discussion_r1468079754)
> 
> Is that a blocker?

I'm ok with follow-ups for implementations. However, I want to complete its discussion here.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-26T20:59:04Z

Let's do 2 more passes:

/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-29T05:05:46Z

/lgtm cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-29T08:06:03Z

The last pass:

/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-29T08:11:44Z

LGTM label has been added.  <details>Git tree hash: 1d40942fa5d4562ce973ea6c296646731e079a3a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-29T08:11:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1631#issuecomment-1912444297" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1631#pullrequestreview-1848046835" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1631#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T09:35:46Z

First pass, looks pretty good.

- **apis/kueue/v1alpha1/multikueueconfig_types.go:96** by [@mimowo](https://github.com/mimowo) — 2024-01-23T08:34:54Z
```diff
@@ -62,13 +50,56 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
+type MultiKueueClusterSpec struct {
+	KubeconfigRef KubeconfigRef `json:"kubeconfigRef"`
+}
+
+type MultiKueueClusterStatus struct {
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+//+genclient
+//+genclient:nonNamespaced
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:subresource:status
+//+kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   MultiKueueClusterSpec   `json:"spec,omitempty"`
+	Status MultiKueueClusterStatus `json:"status,omitempty"`
+}
+
+//+kubebuilder:object:root=true
+
+// MultiKueueClusterList contains a list of MultiKueueCluster
+type MultiKueueClusterList struct {
+	metav1.TypeMeta `json:",inline"`
+	metav1.ListMeta `json:"metadata,omitempty"`
+	Items           []MultiKueueCluster `json:"items"`
+}
+
+// MultiKueueConfigSpec defines the desired state of MultiKueueConfig
+type MultiKueueConfigSpec struct {
+	// List of MultiKueueClusters names where the workloads from the ClusterQueue should be distributed.
+	//
+	// +listType=set
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=100
+	Clusters []string `json:"clusters"`
```

  ```suggestion
  	Clusters []string `json:"clusters,omitempty"`
  ```

- **apis/kueue/v1alpha1/multikueueconfig_types.go:54** by [@mimowo](https://github.com/mimowo) — 2024-01-23T08:37:05Z
```diff
@@ -62,13 +50,56 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
+type MultiKueueClusterSpec struct {
+	KubeconfigRef KubeconfigRef `json:"kubeconfigRef"`
```

  ```suggestion
          // Information how to connect to the cluster.
  	KubeConfig KubeConfig `json:"kubeConfig"`
  ```

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:68** by [@mimowo](https://github.com/mimowo) — 2024-01-23T08:50:34Z
```diff
@@ -54,105 +49,59 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
+	active := true
```

  Do not introduce redundant var if the same can be checked based on `len(inactiveReason)`. If this is non-obvious, then add a comment above `inactiveReason`

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:74** by [@mimowo](https://github.com/mimowo) — 2024-01-23T08:51:29Z
```diff
@@ -54,105 +49,59 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
+	active := true
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
+		active = false
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
```

  ```suggestion
  		var unreachableClusters []string
  ```

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:86** by [@mimowo](https://github.com/mimowo) — 2024-01-23T08:52:43Z
```diff
@@ -54,105 +49,59 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
+	active := true
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
+		active = false
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				return reconcile.Result{}, err
 			}
 
-			err := remoteCtrl.UpdateConfig(kubeconfigs)
 			if err != nil {
-				inactiveReason = fmt.Sprintf("Cannot start MultiKueueClusters controller: %s", err.Error())
-				a.setControllerFor(ac.Name, nil)
+				missingClusters = append(missingClusters, clusterName)
```

  Log the error at default level, as it might be useful for debugging.

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:255** by [@mimowo](https://github.com/mimowo) — 2024-01-23T08:55:03Z
```diff
@@ -308,49 +224,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldMKC, isOldMKC := event.ObjectOld.(*kueuealpha.MultiKueueCluster)
+	newMKC, isNewMKC := event.ObjectNew.(*kueuealpha.MultiKueueCluster)
+	if !isOldMKC || !isNewMKC {
+		return
+	}
+
+	oldActive := apimeta.IsStatusConditionTrue(oldMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	newActive := apimeta.IsStatusConditionTrue(newMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if oldActive != newActive {
+		if err := m.queue(ctx, newMKC, q); err != nil {
+			ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "multiKueueCluster", klog.KObj(oldMKC))
```

  ditto

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:240** by [@mimowo](https://github.com/mimowo) — 2024-01-23T08:55:32Z
```diff
@@ -308,49 +224,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
```

  Errors / warnings should go at higher level, preferably default (or 2)

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:267** by [@mimowo](https://github.com/mimowo) — 2024-01-23T08:57:45Z
```diff
@@ -308,49 +224,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldMKC, isOldMKC := event.ObjectOld.(*kueuealpha.MultiKueueCluster)
+	newMKC, isNewMKC := event.ObjectNew.(*kueuealpha.MultiKueueCluster)
+	if !isOldMKC || !isNewMKC {
+		return
+	}
+
+	oldActive := apimeta.IsStatusConditionTrue(oldMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	newActive := apimeta.IsStatusConditionTrue(newMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if oldActive != newActive {
+		if err := m.queue(ctx, newMKC, q); err != nil {
+			ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "multiKueueCluster", klog.KObj(oldMKC))
+		}
 	}
 }
 
-func (s *secretHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on generic event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "multiKueueCluster", klog.KObj(mkc))
```

  ditto

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:278** by [@mimowo](https://github.com/mimowo) — 2024-01-23T08:58:56Z
```diff
@@ -308,49 +224,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldMKC, isOldMKC := event.ObjectOld.(*kueuealpha.MultiKueueCluster)
+	newMKC, isNewMKC := event.ObjectNew.(*kueuealpha.MultiKueueCluster)
+	if !isOldMKC || !isNewMKC {
+		return
+	}
+
+	oldActive := apimeta.IsStatusConditionTrue(oldMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	newActive := apimeta.IsStatusConditionTrue(newMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if oldActive != newActive {
+		if err := m.queue(ctx, newMKC, q); err != nil {
+			ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "multiKueueCluster", klog.KObj(oldMKC))
+		}
 	}
 }
 
-func (s *secretHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on generic event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "multiKueueCluster", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) queue(ctx context.Context, obj client.Object, q workqueue.RateLimitingInterface) error {
-	users := &kueuealpha.MultiKueueConfigList{}
-	secret, isSecret := obj.(*corev1.Secret)
-	if !isSecret {
-		return errors.New("not a secret")
+func (m *mkClusterHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
 	}
 
-	if err := s.client.List(ctx, users, client.MatchingFields{UsingKubeConfigs: strings.Join([]string{secret.Namespace, secret.Name}, "/")}); err != nil {
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on generic event", "multiKueueCluster", klog.KObj(mkc))
```

  ditto

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:64** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:04:45Z
```diff
@@ -63,13 +63,13 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 
 func TestMultikueConfigUsingKubeconfig(t *testing.T) {
```

  ```suggestion
  func TestListMultikueClustersUsingKubeconfig(t *testing.T) {
  ```

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:66** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:05:12Z
```diff
@@ -63,13 +63,13 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 
 func TestMultikueConfigUsingKubeconfig(t *testing.T) {
 	cases := map[string]struct {
-		configs       []*kueuealpha.MultiKueueConfig
+		configs       []*kueuealpha.MultiKueueCluster
```

  ```suggestion
  		clusters       []*kueuealpha.MultiKueueCluster
  ```

- **pkg/controller/admissionchecks/multikueue/indexer.go:63** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:16:51Z
```diff
@@ -40,19 +40,28 @@ var (
 
 func getIndexUsingKubeConfigs(configNamespace string) func(obj client.Object) []string {
 	return func(obj client.Object) []string {
-		cfg, isCfg := obj.(*kueuealpha.MultiKueueConfig)
-		if !isCfg || len(cfg.Spec.Clusters) == 0 {
+		cluster, isCluster := obj.(*kueuealpha.MultiKueueCluster)
+		if !isCluster {
 			return nil
 		}
-		return slices.Map(cfg.Spec.Clusters, func(c *kueuealpha.MultiKueueCluster) string {
-			return strings.Join([]string{configNamespace, c.KubeconfigRef.Location}, "/")
-		})
+		return []string{strings.Join([]string{configNamespace, cluster.Spec.KubeconfigRef.Location}, "/")}
 	}
 }
 
+func indexUsingMultiKueueClusters(obj client.Object) []string {
+	config, isConfig := obj.(*kueuealpha.MultiKueueConfig)
+	if !isConfig {
+		return nil
+	}
+	return config.Spec.Clusters
+}
+
 func SetupIndexer(ctx context.Context, indexer client.FieldIndexer, configNamespace string) error {
-	if err := indexer.IndexField(ctx, &kueuealpha.MultiKueueConfig{}, UsingKubeConfigs, getIndexUsingKubeConfigs(configNamespace)); err != nil {
-		return fmt.Errorf("setting index on checks config used kubeconfig: %w", err)
+	if err := indexer.IndexField(ctx, &kueuealpha.MultiKueueCluster{}, UsingKubeConfigs, getIndexUsingKubeConfigs(configNamespace)); err != nil {
+		return fmt.Errorf("setting index on clusters config used kubeconfig: %w", err)
+	}
+	if err := indexer.IndexField(ctx, &kueuealpha.MultiKueueConfig{}, UsingMultiKueueClusters, indexUsingMultiKueueClusters); err != nil {
```

  Add unit tests for this indexer.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:123** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:19:26Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
```

  Log if there is an error

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:184** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:20:25Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
```

  Log the error

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:205** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:23:49Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
```

  Is this accurate? IIUC it just re-triggers reconciliation of the `clustersReconciler`.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:223** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:26:46Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeconfigRef)
+	if retry {
+		return reconcile.Result{}, nil
+	}
+	if err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
```

  I'm wondering if we will get the original err logged if `updateStatus` errors. So, I would suggest logging the err as warning before calling `updateStatus`.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:27:26Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeconfigRef)
+	if retry {
+		return reconcile.Result{}, nil
+	}
+	if err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(cluster.Name, kubeConfig); err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
```

  similarly here, I think we might be losing the original error in case `updateStatus` errors too.

- **test/integration/multikueue/multikueue_test.go:52** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:33:09Z
```diff
@@ -45,6 +45,8 @@ var _ = ginkgo.Describe("Multikueue", func() {
 
 		managerMultikueueSecret1 *corev1.Secret
 		managerMultikueueSecret2 *corev1.Secret
+		managerClusterWorker1    *kueuealpha.MultiKueueCluster
+		managerClusterWorker2    *kueuealpha.MultiKueueCluster
```

  ```suggestion
  		managerClusterWorker1    *kueuealpha.MultiKueueCluster
  		managerClusterWorker2    *kueuealpha.MultiKueueCluster
  ```
  ```suggestion
  		workerCluster1    *kueuealpha.MultiKueueCluster
  		workerCluster2    *kueuealpha.MultiKueueCluster
  ```

- **test/e2e/multikueue/e2e_test.go:53** by [@mimowo](https://github.com/mimowo) — 2024-01-23T09:34:18Z
```diff
@@ -46,11 +46,13 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		worker1Ns *corev1.Namespace
 		worker2Ns *corev1.Namespace
 
-		multiKueueConfig *kueuealpha.MultiKueueConfig
-		multiKueueAc     *kueue.AdmissionCheck
-		managerFlavor    *kueue.ResourceFlavor
-		managerCq        *kueue.ClusterQueue
-		managerLq        *kueue.LocalQueue
+		multiKueueCluster1 *kueuealpha.MultiKueueCluster
+		multiKueueCluster2 *kueuealpha.MultiKueueCluster
```

  ```suggestion
  		workerCluster1 *kueuealpha.MultiKueueCluster
  		workerCluster2 *kueuealpha.MultiKueueCluster
  ```
  it feels saying multiKueue is redundant here. Cluster is semi-redundant, "worker1" and "worker2" would work as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T10:30:08Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:61** by [@mimowo](https://github.com/mimowo) — 2024-01-23T10:30:07Z
```diff
@@ -62,13 +50,56 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
+type MultiKueueClusterSpec struct {
+	KubeconfigRef KubeconfigRef `json:"kubeconfigRef"`
+}
+
+type MultiKueueClusterStatus struct {
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+//+genclient
```

  use " " before markers to be consistent with the style we currently have https://github.com/kubernetes-sigs/kueue/pull/1563

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-23T13:14:51Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:96** by [@trasc](https://github.com/trasc) — 2024-01-23T13:14:50Z
```diff
@@ -62,13 +50,56 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
+type MultiKueueClusterSpec struct {
+	KubeconfigRef KubeconfigRef `json:"kubeconfigRef"`
+}
+
+type MultiKueueClusterStatus struct {
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+//+genclient
+//+genclient:nonNamespaced
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:subresource:status
+//+kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   MultiKueueClusterSpec   `json:"spec,omitempty"`
+	Status MultiKueueClusterStatus `json:"status,omitempty"`
+}
+
+//+kubebuilder:object:root=true
+
+// MultiKueueClusterList contains a list of MultiKueueCluster
+type MultiKueueClusterList struct {
+	metav1.TypeMeta `json:",inline"`
+	metav1.ListMeta `json:"metadata,omitempty"`
+	Items           []MultiKueueCluster `json:"items"`
+}
+
+// MultiKueueConfigSpec defines the desired state of MultiKueueConfig
+type MultiKueueConfigSpec struct {
+	// List of MultiKueueClusters names where the workloads from the ClusterQueue should be distributed.
+	//
+	// +listType=set
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=100
+	Clusters []string `json:"clusters"`
```

  it will conflict semantically with 
  `// +kubebuilder:validation:MinItems=1`,

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-23T13:17:29Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:74** by [@trasc](https://github.com/trasc) — 2024-01-23T13:17:29Z
```diff
@@ -54,105 +49,59 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
+	active := true
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
+		active = false
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
```

  `missingClusters` are actually missing in etcd .
  
  `inactiveClusters ` are those with configuration issues or unreachable targets.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T13:42:32Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:96** by [@mimowo](https://github.com/mimowo) — 2024-01-23T13:42:32Z
```diff
@@ -62,13 +50,56 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
+type MultiKueueClusterSpec struct {
+	KubeconfigRef KubeconfigRef `json:"kubeconfigRef"`
+}
+
+type MultiKueueClusterStatus struct {
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+//+genclient
+//+genclient:nonNamespaced
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:subresource:status
+//+kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   MultiKueueClusterSpec   `json:"spec,omitempty"`
+	Status MultiKueueClusterStatus `json:"status,omitempty"`
+}
+
+//+kubebuilder:object:root=true
+
+// MultiKueueClusterList contains a list of MultiKueueCluster
+type MultiKueueClusterList struct {
+	metav1.TypeMeta `json:",inline"`
+	metav1.ListMeta `json:"metadata,omitempty"`
+	Items           []MultiKueueCluster `json:"items"`
+}
+
+// MultiKueueConfigSpec defines the desired state of MultiKueueConfig
+type MultiKueueConfigSpec struct {
+	// List of MultiKueueClusters names where the workloads from the ClusterQueue should be distributed.
+	//
+	// +listType=set
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=100
+	Clusters []string `json:"clusters"`
```

  not sure if there is a semantic conflict, but I see that since we expect it to be non-empty, then `omitempty` doesn't add much value

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T13:52:20Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:74** by [@mimowo](https://github.com/mimowo) — 2024-01-23T13:52:20Z
```diff
@@ -54,105 +49,59 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
+	active := true
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
+		active = false
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
```

  Got it, makes sense

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T14:02:07Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-23T14:02:07Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeconfigRef)
+	if retry {
+		return reconcile.Result{}, nil
+	}
+	if err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(cluster.Name, kubeConfig); err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
```

  I think in this case this might be due to a transient connectivity issue (these could be of various reasons). 
  
  I think it also makes sense to re-try with exponential backoff, which is the approach used for example when pulling images by kubelet. 
  
  However, I think we can work on the retries as a follow up. Still. it should be fine to just requeue here after 1min constant, but up to you.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-23T15:37:16Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:54** by [@trasc](https://github.com/trasc) — 2024-01-23T14:04:31Z
```diff
@@ -62,13 +50,56 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
+type MultiKueueClusterSpec struct {
+	KubeconfigRef KubeconfigRef `json:"kubeconfigRef"`
```

  done

- **apis/kueue/v1alpha1/multikueueconfig_types.go:61** by [@trasc](https://github.com/trasc) — 2024-01-23T14:04:43Z
```diff
@@ -62,13 +50,56 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
+type MultiKueueClusterSpec struct {
+	KubeconfigRef KubeconfigRef `json:"kubeconfigRef"`
+}
+
+type MultiKueueClusterStatus struct {
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+//+genclient
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:68** by [@trasc](https://github.com/trasc) — 2024-01-23T14:14:41Z
```diff
@@ -54,105 +49,59 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
+	active := true
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:86** by [@trasc](https://github.com/trasc) — 2024-01-23T14:19:40Z
```diff
@@ -54,105 +49,59 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
+	active := true
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
+		active = false
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				return reconcile.Result{}, err
 			}
 
-			err := remoteCtrl.UpdateConfig(kubeconfigs)
 			if err != nil {
-				inactiveReason = fmt.Sprintf("Cannot start MultiKueueClusters controller: %s", err.Error())
-				a.setControllerFor(ac.Name, nil)
+				missingClusters = append(missingClusters, clusterName)
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:240** by [@trasc](https://github.com/trasc) — 2024-01-23T14:23:01Z
```diff
@@ -308,49 +224,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:255** by [@trasc](https://github.com/trasc) — 2024-01-23T14:23:10Z
```diff
@@ -308,49 +224,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldMKC, isOldMKC := event.ObjectOld.(*kueuealpha.MultiKueueCluster)
+	newMKC, isNewMKC := event.ObjectNew.(*kueuealpha.MultiKueueCluster)
+	if !isOldMKC || !isNewMKC {
+		return
+	}
+
+	oldActive := apimeta.IsStatusConditionTrue(oldMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	newActive := apimeta.IsStatusConditionTrue(newMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if oldActive != newActive {
+		if err := m.queue(ctx, newMKC, q); err != nil {
+			ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "multiKueueCluster", klog.KObj(oldMKC))
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:267** by [@trasc](https://github.com/trasc) — 2024-01-23T14:23:19Z
```diff
@@ -308,49 +224,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldMKC, isOldMKC := event.ObjectOld.(*kueuealpha.MultiKueueCluster)
+	newMKC, isNewMKC := event.ObjectNew.(*kueuealpha.MultiKueueCluster)
+	if !isOldMKC || !isNewMKC {
+		return
+	}
+
+	oldActive := apimeta.IsStatusConditionTrue(oldMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	newActive := apimeta.IsStatusConditionTrue(newMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if oldActive != newActive {
+		if err := m.queue(ctx, newMKC, q); err != nil {
+			ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "multiKueueCluster", klog.KObj(oldMKC))
+		}
 	}
 }
 
-func (s *secretHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on generic event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "multiKueueCluster", klog.KObj(mkc))
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:278** by [@trasc](https://github.com/trasc) — 2024-01-23T14:23:30Z
```diff
@@ -308,49 +224,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldMKC, isOldMKC := event.ObjectOld.(*kueuealpha.MultiKueueCluster)
+	newMKC, isNewMKC := event.ObjectNew.(*kueuealpha.MultiKueueCluster)
+	if !isOldMKC || !isNewMKC {
+		return
+	}
+
+	oldActive := apimeta.IsStatusConditionTrue(oldMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	newActive := apimeta.IsStatusConditionTrue(newMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if oldActive != newActive {
+		if err := m.queue(ctx, newMKC, q); err != nil {
+			ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "multiKueueCluster", klog.KObj(oldMKC))
+		}
 	}
 }
 
-func (s *secretHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on generic event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "multiKueueCluster", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) queue(ctx context.Context, obj client.Object, q workqueue.RateLimitingInterface) error {
-	users := &kueuealpha.MultiKueueConfigList{}
-	secret, isSecret := obj.(*corev1.Secret)
-	if !isSecret {
-		return errors.New("not a secret")
+func (m *mkClusterHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
 	}
 
-	if err := s.client.List(ctx, users, client.MatchingFields{UsingKubeConfigs: strings.Join([]string{secret.Namespace, secret.Name}, "/")}); err != nil {
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on generic event", "multiKueueCluster", klog.KObj(mkc))
```

  done

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:64** by [@trasc](https://github.com/trasc) — 2024-01-23T14:27:58Z
```diff
@@ -63,13 +63,13 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 
 func TestMultikueConfigUsingKubeconfig(t *testing.T) {
```

  done

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:66** by [@trasc](https://github.com/trasc) — 2024-01-23T14:28:55Z
```diff
@@ -63,13 +63,13 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 
 func TestMultikueConfigUsingKubeconfig(t *testing.T) {
 	cases := map[string]struct {
-		configs       []*kueuealpha.MultiKueueConfig
+		configs       []*kueuealpha.MultiKueueCluster
```

  done

- **pkg/controller/admissionchecks/multikueue/indexer.go:63** by [@trasc](https://github.com/trasc) — 2024-01-23T14:45:45Z
```diff
@@ -40,19 +40,28 @@ var (
 
 func getIndexUsingKubeConfigs(configNamespace string) func(obj client.Object) []string {
 	return func(obj client.Object) []string {
-		cfg, isCfg := obj.(*kueuealpha.MultiKueueConfig)
-		if !isCfg || len(cfg.Spec.Clusters) == 0 {
+		cluster, isCluster := obj.(*kueuealpha.MultiKueueCluster)
+		if !isCluster {
 			return nil
 		}
-		return slices.Map(cfg.Spec.Clusters, func(c *kueuealpha.MultiKueueCluster) string {
-			return strings.Join([]string{configNamespace, c.KubeconfigRef.Location}, "/")
-		})
+		return []string{strings.Join([]string{configNamespace, cluster.Spec.KubeconfigRef.Location}, "/")}
 	}
 }
 
+func indexUsingMultiKueueClusters(obj client.Object) []string {
+	config, isConfig := obj.(*kueuealpha.MultiKueueConfig)
+	if !isConfig {
+		return nil
+	}
+	return config.Spec.Clusters
+}
+
 func SetupIndexer(ctx context.Context, indexer client.FieldIndexer, configNamespace string) error {
-	if err := indexer.IndexField(ctx, &kueuealpha.MultiKueueConfig{}, UsingKubeConfigs, getIndexUsingKubeConfigs(configNamespace)); err != nil {
-		return fmt.Errorf("setting index on checks config used kubeconfig: %w", err)
+	if err := indexer.IndexField(ctx, &kueuealpha.MultiKueueCluster{}, UsingKubeConfigs, getIndexUsingKubeConfigs(configNamespace)); err != nil {
+		return fmt.Errorf("setting index on clusters config used kubeconfig: %w", err)
+	}
+	if err := indexer.IndexField(ctx, &kueuealpha.MultiKueueConfig{}, UsingMultiKueueClusters, indexUsingMultiKueueClusters); err != nil {
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:123** by [@trasc](https://github.com/trasc) — 2024-01-23T14:56:01Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:184** by [@trasc](https://github.com/trasc) — 2024-01-23T14:58:05Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:205** by [@trasc](https://github.com/trasc) — 2024-01-23T15:00:05Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
```

  No longer, the admission check reconciler watches the clusters and will get the event from the status update .
  
  removed

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:223** by [@trasc](https://github.com/trasc) — 2024-01-23T15:04:02Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeconfigRef)
+	if retry {
+		return reconcile.Result{}, nil
+	}
+	if err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
```

  logged before

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:228** by [@trasc](https://github.com/trasc) — 2024-01-23T15:04:09Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeconfigRef)
+	if retry {
+		return reconcile.Result{}, nil
+	}
+	if err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(cluster.Name, kubeConfig); err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
```

  logged before

- **test/e2e/multikueue/e2e_test.go:53** by [@trasc](https://github.com/trasc) — 2024-01-23T15:08:58Z
```diff
@@ -46,11 +46,13 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		worker1Ns *corev1.Namespace
 		worker2Ns *corev1.Namespace
 
-		multiKueueConfig *kueuealpha.MultiKueueConfig
-		multiKueueAc     *kueue.AdmissionCheck
-		managerFlavor    *kueue.ResourceFlavor
-		managerCq        *kueue.ClusterQueue
-		managerLq        *kueue.LocalQueue
+		multiKueueCluster1 *kueuealpha.MultiKueueCluster
+		multiKueueCluster2 *kueuealpha.MultiKueueCluster
```

  In the integration tests, `workerCluster1`  and  `workerCluster1` are a different object type holding the client and context used in the testing.

- **test/integration/multikueue/multikueue_test.go:52** by [@trasc](https://github.com/trasc) — 2024-01-23T15:11:01Z
```diff
@@ -45,6 +45,8 @@ var _ = ginkgo.Describe("Multikueue", func() {
 
 		managerMultikueueSecret1 *corev1.Secret
 		managerMultikueueSecret2 *corev1.Secret
+		managerClusterWorker1    *kueuealpha.MultiKueueCluster
+		managerClusterWorker2    *kueuealpha.MultiKueueCluster
```

  `workerCluster1`  and  `workerCluster1` are a different object type holding the client and context used in the testing.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:228** by [@trasc](https://github.com/trasc) — 2024-01-23T15:13:33Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeconfigRef)
+	if retry {
+		return reconcile.Result{}, nil
+	}
+	if err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(cluster.Name, kubeConfig); err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
```

  Yes, there is a follow-up item for connection retry.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T15:59:43Z

- **test/integration/multikueue/multikueue_test.go:52** by [@mimowo](https://github.com/mimowo) — 2024-01-23T15:59:43Z
```diff
@@ -45,6 +45,8 @@ var _ = ginkgo.Describe("Multikueue", func() {
 
 		managerMultikueueSecret1 *corev1.Secret
 		managerMultikueueSecret2 *corev1.Secret
+		managerClusterWorker1    *kueuealpha.MultiKueueCluster
+		managerClusterWorker2    *kueuealpha.MultiKueueCluster
```

  would it be better naming if the clients were called `clientWorkerCluster1`, or `clientForWorker1`? I think when naming the client vars we typically say "client" in the name, like `k8sClient`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:00:40Z

- **test/e2e/multikueue/e2e_test.go:53** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:00:40Z
```diff
@@ -46,11 +46,13 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		worker1Ns *corev1.Namespace
 		worker2Ns *corev1.Namespace
 
-		multiKueueConfig *kueuealpha.MultiKueueConfig
-		multiKueueAc     *kueue.AdmissionCheck
-		managerFlavor    *kueue.ResourceFlavor
-		managerCq        *kueue.ClusterQueue
-		managerLq        *kueue.LocalQueue
+		multiKueueCluster1 *kueuealpha.MultiKueueCluster
+		multiKueueCluster2 *kueuealpha.MultiKueueCluster
```

  no strong view on naming, but it feels like these names are fitting well, maybe actually we could rename the clients to have the "client" in name, as in the other comment. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:01:20Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:223** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:01:20Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeconfigRef)
+	if retry {
+		return reconcile.Result{}, nil
+	}
+	if err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
```

  lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:01:25Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:01:25Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeconfigRef)
+	if retry {
+		return reconcile.Result{}, nil
+	}
+	if err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(cluster.Name, kubeConfig); err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
```

  lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:01:50Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:205** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:01:50Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
```

  ok

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-23T16:02:02Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:228** by [@mimowo](https://github.com/mimowo) — 2024-01-23T16:02:01Z
```diff
@@ -0,0 +1,339 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		// notify ac reconcilere
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeconfigRef)
+	if retry {
+		return reconcile.Result{}, nil
+	}
+	if err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(cluster.Name, kubeConfig); err != nil {
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-24T09:10:18Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:55** by [@mimowo](https://github.com/mimowo) — 2024-01-24T08:41:58Z
```diff
@@ -62,21 +50,65 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeconfigRef"`
```

  ```suggestion
  	KubeConfig KubeConfig `json:"kubeConfig"`
  ```

- **apis/kueue/v1alpha1/multikueueconfig_types.go:61** by [@mimowo](https://github.com/mimowo) — 2024-01-24T08:45:18Z
```diff
@@ -62,21 +50,65 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeconfigRef"`
+}
+
+type MultiKueueClusterStatus struct {
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
```

  I see that for other `Conditions` fields we also add the markers:
  ```
  	// +optional
  	// +listType=map
  	// +listMapKey=type
  	// +patchStrategy=merge
  	// +patchMergeKey=type
  ```
  should we add them here, or they are not necessary since we added the comment? Still, I see the comment added too in the other places.

- **pkg/controller/admissionchecks/multikueue/admissioncheck_test.go:104** by [@mimowo](https://github.com/mimowo) — 2024-01-24T08:52:37Z
```diff
@@ -124,12 +96,12 @@ func TestReconcile(t *testing.T) {
 						Type:    kueue.AdmissionCheckActive,
 						Status:  metav1.ConditionFalse,
 						Reason:  "Inactive",
-						Message: `Cannot load kubeconfigs: getting kubeconfig secret for "worker1": secrets "secret1" not found`,
+						Message: "Missing clusters: [worker1]",
 					}).
 					Obj(),
 			},
 		},
-		"missing kubeconfig key in secret": {
+		"inactive cluster": {
```

  Please add a test case with missing and inactive clusters returned in the same message

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:163** by [@mimowo](https://github.com/mimowo) — 2024-01-24T08:54:54Z
```diff
@@ -65,105 +65,108 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 	return builder, ctx
 }
 
-func TestMultikueConfigUsingKubeconfig(t *testing.T) {
+func TestListMultikueClustersUsingKubeconfig(t *testing.T) {
 	cases := map[string]struct {
-		configs       []*kueuealpha.MultiKueueConfig
+		clusters      []*kueuealpha.MultiKueueCluster
 		filter        client.ListOption
 		wantListError error
 		wantList      []string
 	}{
 		"no clusters": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "name",
-					},
-				},
-			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"single cluster, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 		"single cluster, no match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret2",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"multiple clusters, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret0",
-								},
-							},
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			builder, ctx := getClientBuilder()
 			k8sclient := builder.Build()
-			for _, req := range tc.configs {
+			for _, req := range tc.clusters {
 				if err := k8sclient.Create(ctx, req); err != nil {
 					t.Errorf("Unable to create %s request: %v", client.ObjectKeyFromObject(req), err)
 				}
 			}
 
+			lst := &kueuealpha.MultiKueueClusterList{}
+
+			gotListErr := k8sclient.List(ctx, lst, tc.filter)
+			if diff := cmp.Diff(tc.wantListError, gotListErr); diff != "" {
+				t.Errorf("unexpected list error (-want/+got):\n%s", diff)
+			}
+
+			gotList := slices.Map(lst.Items, func(mkc *kueuealpha.MultiKueueCluster) string { return mkc.Name })
+			if diff := cmp.Diff(tc.wantList, gotList, cmpopts.EquateEmpty(), cmpopts.SortSlices(func(a, b string) bool { return a < b })); diff != "" {
+				t.Errorf("unexpected list (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestListMultikueConfigsUsingMultikueueClusters(t *testing.T) {
+	cases := map[string]struct {
+		configs       []*kueuealpha.MultiKueueConfig
+		filter        client.ListOption
+		wantListError error
+		wantList      []string
+	}{
+		"no configs": {
+			filter: client.MatchingFields{UsingMultiKueueClusters: "cluster1"},
+		},
+		"single config, single match": {
+			configs: []*kueuealpha.MultiKueueConfig{
+				utiltesting.MakeMultiKueueConfig("config1").Clusters("cluster1", "cluster2").Obj(),
+			},
+			filter:   client.MatchingFields{UsingMultiKueueClusters: "cluster2"},
+			wantList: []string{"config1"},
+		},
+		"single config, no match": {
+			configs: []*kueuealpha.MultiKueueConfig{
+				utiltesting.MakeMultiKueueConfig("config2").Clusters("cluster2").Obj(),
+			},
+			filter: client.MatchingFields{UsingMultiKueueClusters: "cluster1"},
+		},
+		"multiple configs, single match": {
+			configs: []*kueuealpha.MultiKueueConfig{
+				utiltesting.MakeMultiKueueConfig("config1").Clusters("cluster1", "cluster2").Obj(),
+				utiltesting.MakeMultiKueueConfig("config2").Clusters("cluster2").Obj(),
+			},
+			filter:   client.MatchingFields{UsingMultiKueueClusters: "cluster1"},
+			wantList: []string{"config1"},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			builder, ctx := getClientBuilder()
+			k8sclient := builder.Build()
+			for _, config := range tc.configs {
+				if err := k8sclient.Create(ctx, config); err != nil {
+					t.Errorf("Unable to create %s config: %v", client.ObjectKeyFromObject(config), err)
```

  ```suggestion
  					t.Fatalf("Unable to create %s config: %v", client.ObjectKeyFromObject(config), err)
  ```
  if the setup fails no need to continue with the test

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:216** by [@mimowo](https://github.com/mimowo) — 2024-01-24T09:04:58Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, nil
```

  Retry like this (with error=nil) may result in infinite fast loop of retries, generating unnecessary load, and spamming logs. I think it is better to return err if we retry so that the controller-runtime retries with exponential delay.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-25T06:30:15Z

- **test/integration/multikueue/multikueue_test.go:52** by [@trasc](https://github.com/trasc) — 2024-01-25T06:30:15Z
```diff
@@ -45,6 +45,8 @@ var _ = ginkgo.Describe("Multikueue", func() {
 
 		managerMultikueueSecret1 *corev1.Secret
 		managerMultikueueSecret2 *corev1.Secret
+		managerClusterWorker1    *kueuealpha.MultiKueueCluster
+		managerClusterWorker2    *kueuealpha.MultiKueueCluster
```

  they are in fact structures containing a client and a context.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-25T07:54:28Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:61** by [@trasc](https://github.com/trasc) — 2024-01-25T06:42:45Z
```diff
@@ -62,21 +50,65 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeconfigRef"`
+}
+
+type MultiKueueClusterStatus struct {
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
```

  done

- **apis/kueue/v1alpha1/multikueueconfig_types.go:55** by [@trasc](https://github.com/trasc) — 2024-01-25T06:45:08Z
```diff
@@ -62,21 +50,65 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeconfigRef"`
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck_test.go:104** by [@trasc](https://github.com/trasc) — 2024-01-25T06:48:56Z
```diff
@@ -124,12 +96,12 @@ func TestReconcile(t *testing.T) {
 						Type:    kueue.AdmissionCheckActive,
 						Status:  metav1.ConditionFalse,
 						Reason:  "Inactive",
-						Message: `Cannot load kubeconfigs: getting kubeconfig secret for "worker1": secrets "secret1" not found`,
+						Message: "Missing clusters: [worker1]",
 					}).
 					Obj(),
 			},
 		},
-		"missing kubeconfig key in secret": {
+		"inactive cluster": {
```

  done

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:163** by [@trasc](https://github.com/trasc) — 2024-01-25T06:50:21Z
```diff
@@ -65,105 +65,108 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 	return builder, ctx
 }
 
-func TestMultikueConfigUsingKubeconfig(t *testing.T) {
+func TestListMultikueClustersUsingKubeconfig(t *testing.T) {
 	cases := map[string]struct {
-		configs       []*kueuealpha.MultiKueueConfig
+		clusters      []*kueuealpha.MultiKueueCluster
 		filter        client.ListOption
 		wantListError error
 		wantList      []string
 	}{
 		"no clusters": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "name",
-					},
-				},
-			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"single cluster, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 		"single cluster, no match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret2",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"multiple clusters, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret0",
-								},
-							},
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			builder, ctx := getClientBuilder()
 			k8sclient := builder.Build()
-			for _, req := range tc.configs {
+			for _, req := range tc.clusters {
 				if err := k8sclient.Create(ctx, req); err != nil {
 					t.Errorf("Unable to create %s request: %v", client.ObjectKeyFromObject(req), err)
 				}
 			}
 
+			lst := &kueuealpha.MultiKueueClusterList{}
+
+			gotListErr := k8sclient.List(ctx, lst, tc.filter)
+			if diff := cmp.Diff(tc.wantListError, gotListErr); diff != "" {
+				t.Errorf("unexpected list error (-want/+got):\n%s", diff)
+			}
+
+			gotList := slices.Map(lst.Items, func(mkc *kueuealpha.MultiKueueCluster) string { return mkc.Name })
+			if diff := cmp.Diff(tc.wantList, gotList, cmpopts.EquateEmpty(), cmpopts.SortSlices(func(a, b string) bool { return a < b })); diff != "" {
+				t.Errorf("unexpected list (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestListMultikueConfigsUsingMultikueueClusters(t *testing.T) {
+	cases := map[string]struct {
+		configs       []*kueuealpha.MultiKueueConfig
+		filter        client.ListOption
+		wantListError error
+		wantList      []string
+	}{
+		"no configs": {
+			filter: client.MatchingFields{UsingMultiKueueClusters: "cluster1"},
+		},
+		"single config, single match": {
+			configs: []*kueuealpha.MultiKueueConfig{
+				utiltesting.MakeMultiKueueConfig("config1").Clusters("cluster1", "cluster2").Obj(),
+			},
+			filter:   client.MatchingFields{UsingMultiKueueClusters: "cluster2"},
+			wantList: []string{"config1"},
+		},
+		"single config, no match": {
+			configs: []*kueuealpha.MultiKueueConfig{
+				utiltesting.MakeMultiKueueConfig("config2").Clusters("cluster2").Obj(),
+			},
+			filter: client.MatchingFields{UsingMultiKueueClusters: "cluster1"},
+		},
+		"multiple configs, single match": {
+			configs: []*kueuealpha.MultiKueueConfig{
+				utiltesting.MakeMultiKueueConfig("config1").Clusters("cluster1", "cluster2").Obj(),
+				utiltesting.MakeMultiKueueConfig("config2").Clusters("cluster2").Obj(),
+			},
+			filter:   client.MatchingFields{UsingMultiKueueClusters: "cluster1"},
+			wantList: []string{"config1"},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			builder, ctx := getClientBuilder()
+			k8sclient := builder.Build()
+			for _, config := range tc.configs {
+				if err := k8sclient.Create(ctx, config); err != nil {
+					t.Errorf("Unable to create %s config: %v", client.ObjectKeyFromObject(config), err)
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:216** by [@trasc](https://github.com/trasc) — 2024-01-25T06:54:26Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, nil
```

  done

- **test/integration/multikueue/multikueue_test.go:52** by [@trasc](https://github.com/trasc) — 2024-01-25T06:57:14Z
```diff
@@ -45,6 +45,8 @@ var _ = ginkgo.Describe("Multikueue", func() {
 
 		managerMultikueueSecret1 *corev1.Secret
 		managerMultikueueSecret2 *corev1.Secret
+		managerClusterWorker1    *kueuealpha.MultiKueueCluster
+		managerClusterWorker2    *kueuealpha.MultiKueueCluster
```

  remaned

- **test/e2e/multikueue/e2e_test.go:53** by [@trasc](https://github.com/trasc) — 2024-01-25T06:58:45Z
```diff
@@ -46,11 +46,13 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		worker1Ns *corev1.Namespace
 		worker2Ns *corev1.Namespace
 
-		multiKueueConfig *kueuealpha.MultiKueueConfig
-		multiKueueAc     *kueue.AdmissionCheck
-		managerFlavor    *kueue.ResourceFlavor
-		managerCq        *kueue.ClusterQueue
-		managerLq        *kueue.LocalQueue
+		multiKueueCluster1 *kueuealpha.MultiKueueCluster
+		multiKueueCluster2 *kueuealpha.MultiKueueCluster
```

  renamed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-25T08:54:07Z

/lgtm
/assign @tenzen-y @alculquicondor

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-25T15:53:26Z

Give my first pass. Also can you update this PR description with which [follow-ups ](https://github.com/kubernetes-sigs/kueue/pull/1313)are completed in this PR?

BTW, We make both MultiKueueCluster and MultiKueueConfig shorter to mkc 😵‍💫 
It was a bit confusing, although I'm ok now.

- **apis/kueue/v1alpha1/multikueueconfig_types.go:72** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T13:30:16Z
```diff
@@ -62,21 +50,70 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeConfig"`
+}
+
+type MultiKueueClusterStatus struct {
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	// +patchStrategy=merge
+	// +patchMergeKey=type
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+// +genclient
+// +genclient:nonNamespaced
+// +kubebuilder:object:root=true
+// +kubebuilder:storageversion
+// +kubebuilder:subresource:status
+// +kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
```

  Can we put this object on the `multikueuecluster_types.go` as well as other objects?

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:81** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T14:13:55Z
```diff
@@ -54,114 +49,62 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				log.Error(err, "reading cluster", "multiKueueCluster", clusterName)
```

  ```suggestion
  ```
  
  I don't think that this logging would be valuable since `err` returned `a.client.Get` already has the similar error, right?.

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:101** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T14:28:57Z
```diff
@@ -54,114 +49,62 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				log.Error(err, "reading cluster", "multiKueueCluster", clusterName)
+				return reconcile.Result{}, err
 			}
 
-			err := remoteCtrl.UpdateConfig(kubeconfigs)
 			if err != nil {
-				inactiveReason = fmt.Sprintf("Cannot start MultiKueueClusters controller: %s", err.Error())
-				a.setControllerFor(ac.Name, nil)
+				missingClusters = append(missingClusters, clusterName)
 			} else {
-				remCtrl = remoteCtrl
+				if !apimeta.IsStatusConditionTrue(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive) {
+					inactiveClusters = append(inactiveClusters, clusterName)
+				}
 			}
 		}
+
+		var messageParts []string
+		if len(missingClusters) > 0 {
+			messageParts = []string{fmt.Sprintf("Missing clusters: %v", missingClusters)}
+		}
+		if len(inactiveClusters) > 0 {
+			messageParts = append(messageParts, fmt.Sprintf("Inactive clusters: %v", inactiveClusters))
+		}
+		inactiveReason = strings.Join(messageParts, ", ")
```

  Clarifying what we can not do would be better.
  
  ```suggestion
  		inactiveReason = fmt.Sprintf("Can not start multiKueueCliuster controller: %v", strings.Join(messageParts, ", "))
  ```
  
  Is this message right?

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:228** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T14:38:48Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
```

  ```suggestion
  	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
  ```
  
  Isn't this `MultiKueueCluster`?

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:234** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T14:39:16Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
```

  ```suggestion
  		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on create event", "multiKueueCluster", klog.KObj(mkc))
  ```

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:278** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T14:45:41Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldMKC, isOldMKC := event.ObjectOld.(*kueuealpha.MultiKueueCluster)
+	newMKC, isNewMKC := event.ObjectNew.(*kueuealpha.MultiKueueCluster)
+	if !isOldMKC || !isNewMKC {
+		return
+	}
+
+	oldActive := apimeta.IsStatusConditionTrue(oldMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	newActive := apimeta.IsStatusConditionTrue(newMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if oldActive != newActive {
+		if err := m.queue(ctx, newMKC, q); err != nil {
+			ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on update event", "multiKueueCluster", klog.KObj(oldMKC))
+		}
 	}
 }
 
-func (s *secretHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on generic event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on delete event", "multiKueueCluster", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) queue(ctx context.Context, obj client.Object, q workqueue.RateLimitingInterface) error {
-	users := &kueuealpha.MultiKueueConfigList{}
-	secret, isSecret := obj.(*corev1.Secret)
-	if !isSecret {
-		return errors.New("not a secret")
+func (m *mkClusterHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on generic event", "multiKueueCluster", klog.KObj(mkc))
 	}
+}
 
-	if err := s.client.List(ctx, users, client.MatchingFields{UsingKubeConfigs: strings.Join([]string{secret.Namespace, secret.Name}, "/")}); err != nil {
+func (m *mkClusterHandler) queue(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, q workqueue.RateLimitingInterface) error {
+	users := &kueuealpha.MultiKueueConfigList{}
+	if err := m.client.List(ctx, users, client.MatchingFields{UsingMultiKueueClusters: cluster.Name}); err != nil {
```

  When the `.spec.clusters` has multiple cluster names in this MultiKueueConfig, can this lister get the `MultiKueueConfig`?
  
  I meant the following the MultiKueueConfig:
  
  ```yaml
  Kind: MultiKueueConfig
  spec:
    clusters: ["a", "b", "c"]
  ```

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:228** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T14:47:45Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
```

  Also, can you verify if this handler fires reconcile when the `MultiKueueCluster` is created in tests?

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:68** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:04:47Z
```diff
@@ -65,105 +65,108 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 	return builder, ctx
 }
 
-func TestMultikueConfigUsingKubeconfig(t *testing.T) {
+func TestListMultikueClustersUsingKubeconfig(t *testing.T) {
```

  ```suggestion
  func TestListMultiKueueClustersUsingKubeConfig(t *testing.T) {
  ```

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:106** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:06:36Z
```diff
@@ -65,105 +65,108 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 	return builder, ctx
 }
 
-func TestMultikueConfigUsingKubeconfig(t *testing.T) {
+func TestListMultikueClustersUsingKubeconfig(t *testing.T) {
 	cases := map[string]struct {
-		configs       []*kueuealpha.MultiKueueConfig
+		clusters      []*kueuealpha.MultiKueueCluster
 		filter        client.ListOption
 		wantListError error
 		wantList      []string
 	}{
 		"no clusters": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "name",
-					},
-				},
-			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"single cluster, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 		"single cluster, no match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret2",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"multiple clusters, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret0",
-								},
-							},
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			builder, ctx := getClientBuilder()
 			k8sclient := builder.Build()
-			for _, req := range tc.configs {
+			for _, req := range tc.clusters {
 				if err := k8sclient.Create(ctx, req); err != nil {
-					t.Errorf("Unable to create %s request: %v", client.ObjectKeyFromObject(req), err)
+					t.Fatalf("Unable to create %s cluster: %v", client.ObjectKeyFromObject(req), err)
```

  ```suggestion
  					t.Fatalf("Unable to create %q cluster: %v", client.ObjectKeyFromObject(req), err)
  ```

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:125** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:07:35Z
```diff
@@ -65,105 +65,108 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 	return builder, ctx
 }
 
-func TestMultikueConfigUsingKubeconfig(t *testing.T) {
+func TestListMultikueClustersUsingKubeconfig(t *testing.T) {
 	cases := map[string]struct {
-		configs       []*kueuealpha.MultiKueueConfig
+		clusters      []*kueuealpha.MultiKueueCluster
 		filter        client.ListOption
 		wantListError error
 		wantList      []string
 	}{
 		"no clusters": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "name",
-					},
-				},
-			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"single cluster, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 		"single cluster, no match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret2",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"multiple clusters, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret0",
-								},
-							},
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			builder, ctx := getClientBuilder()
 			k8sclient := builder.Build()
-			for _, req := range tc.configs {
+			for _, req := range tc.clusters {
 				if err := k8sclient.Create(ctx, req); err != nil {
-					t.Errorf("Unable to create %s request: %v", client.ObjectKeyFromObject(req), err)
+					t.Fatalf("Unable to create %s cluster: %v", client.ObjectKeyFromObject(req), err)
+				}
+			}
+
+			lst := &kueuealpha.MultiKueueClusterList{}
+
+			gotListErr := k8sclient.List(ctx, lst, tc.filter)
+			if diff := cmp.Diff(tc.wantListError, gotListErr); diff != "" {
+				t.Errorf("unexpected list error (-want/+got):\n%s", diff)
+			}
+
+			gotList := slices.Map(lst.Items, func(mkc *kueuealpha.MultiKueueCluster) string { return mkc.Name })
+			if diff := cmp.Diff(tc.wantList, gotList, cmpopts.EquateEmpty(), cmpopts.SortSlices(func(a, b string) bool { return a < b })); diff != "" {
+				t.Errorf("unexpected list (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestListMultikueConfigsUsingMultikueueClusters(t *testing.T) {
```

  ```suggestion
  func TestListMultiKueueConfigsUsingMultiKueueClusters(t *testing.T) {
  ```

- **pkg/controller/admissionchecks/multikueue/jobset_adapter_test.go:229** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:12:12Z
```diff
@@ -223,12 +223,16 @@ func TestWlReconcileJobset(t *testing.T) {
 			manageBuilder = manageBuilder.WithLists(&kueue.WorkloadList{Items: tc.managersWorkloads}, &jobset.JobSetList{Items: tc.managersJobSets})
 			manageBuilder = manageBuilder.WithStatusSubresource(slices.Map(tc.managersWorkloads, func(w *kueue.Workload) client.Object { return w })...)
 			manageBuilder = manageBuilder.WithStatusSubresource(slices.Map(tc.managersJobSets, func(w *jobset.JobSet) client.Object { return w })...)
-			manageBuilder = manageBuilder.WithObjects(utiltesting.MakeAdmissionCheck("ac1").ControllerName(ControllerName).Obj())
+			manageBuilder = manageBuilder.WithObjects(
+				utiltesting.MakeMultiKueueConfig("config1").Clusters("worker1").Obj(),
+				utiltesting.MakeAdmissionCheck("ac1").ControllerName(ControllerName).
+					Parameters(kueue.GroupVersion.Group, "MultiKueueConfig", "config1").
```

  ```suggestion
  					Parameters(kueuealpha.GroupVersion.Group, "MultiKueueConfig", "config1").
  ```
  
  I understand that both kueuealpha.GroupVersion.Group and kueue.GroupVersion.Group return the same value. However, using `kueuealpha` would be proper.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:184** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:19:05Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
```

  ```suggestion
  		ctrl.LoggerFrom(ctx).Error(err, "failed to set kubeConfig in the remote client")
  ```

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:208** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:21:19Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
```

  ```suggestion
  	}
  
  	log.V(2).Info("Reconcile MultiKueueCluster")
  
  ```

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:22:17Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
```

  IIUC, this error will never occurred.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:129** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:24:55Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
```

  ```suggestion
  		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
  		return
  	}
  	if client.IgnoreNotFound(err) {
  		ctrl.LoggerFrom(ctx).Error(err, "failed to get local workload", "workload", klog.KObj(wl))
  	}
  
  ```

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:139** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:29:41Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
```

  ```suggestion
  	remoteClients    map[string]*remoteClient
  ```
  
  To clarify the differences between both clients in this structure.
  
  Or
  
  ```golang
  type clusterReconciler struct {
  	localClient client.Client
  ...
  ```

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:241** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:35:01Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
```

  Uhm, this could cause the eternally repeated retring, right?
  If so, introducing the timeout mechanism would be better. However, we can work on the mechanism in another PR.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:244** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:35:54Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
```

  Is there a case that sec.Data is nil?

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:282** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:36:49Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
+	oldCondition := apimeta.FindStatusCondition(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if active && oldCondition != nil && oldCondition.Status == metav1.ConditionTrue {
+		return nil
+	}
+
+	apimeta.SetStatusCondition(&cluster.Status.Conditions, newCondition)
+	return c.client.Status().Update(ctx, cluster)
+}
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters/status,verbs=get;update;patch
+
+func newClustersReconciler(c client.Client, namespace string) *clustersReconciler {
+	return &clustersReconciler{
+		client:          c,
+		configNamespace: namespace,
+		clients:         make(map[string]*remoteClient),
+		wlUpdateCh:      make(chan event.GenericEvent, 10),
```

  Why do we create 10 channels?

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:305** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:38:34Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
+	oldCondition := apimeta.FindStatusCondition(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if active && oldCondition != nil && oldCondition.Status == metav1.ConditionTrue {
+		return nil
+	}
+
+	apimeta.SetStatusCondition(&cluster.Status.Conditions, newCondition)
+	return c.client.Status().Update(ctx, cluster)
+}
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters/status,verbs=get;update;patch
+
+func newClustersReconciler(c client.Client, namespace string) *clustersReconciler {
+	return &clustersReconciler{
+		client:          c,
+		configNamespace: namespace,
+		clients:         make(map[string]*remoteClient),
+		wlUpdateCh:      make(chan event.GenericEvent, 10),
+	}
+}
+
+func (c *clustersReconciler) setupWithManager(mgr ctrl.Manager) error {
+	err := mgr.Add(c)
+	if err != nil {
+		return err
+	}
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&kueuealpha.MultiKueueCluster{}).
+		Watches(&corev1.Secret{}, &secretHandler{client: c.client}).
+		Complete(c)
+}
+
+type secretHandler struct {
+	client client.Client
+}
+
+var _ handler.EventHandler = (*secretHandler)(nil)
+
+func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	if err := s.queue(ctx, event.Object, q); err != nil {
```

  Can we check `event.Object` here? Or can we output error logs in `queue` function?

- **pkg/controller/admissionchecks/multikueue/workload.go:201** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:48:36Z
```diff
@@ -177,13 +198,9 @@ func (a *wlReconciler) readGroup(ctx context.Context, local *kueue.Workload) (*w
 		return nil, nil
 	}
 
-	rController, found := a.acr.controllerFor(relevantChecks[0])
-	if !found {
-		return nil, errors.New("remote controller not found")
-	}
-
-	if !rController.IsActive() {
-		return nil, errors.New("remote controller is not active")
+	rClients, err := a.remoteClientsForAC(ctx, relevantChecks[0])
```

  Why do we get a remote client only for admissionCheck with index=0?

- **pkg/controller/admissionchecks/multikueue/workload_test.go:297** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:49:10Z
```diff
@@ -291,27 +291,27 @@ func TestWlReconcile(t *testing.T) {
 			manageBuilder = manageBuilder.WithLists(&kueue.WorkloadList{Items: tc.managersWorkloads}, &batchv1.JobList{Items: tc.managersJobs})
 			manageBuilder = manageBuilder.WithStatusSubresource(slices.Map(tc.managersWorkloads, func(w *kueue.Workload) client.Object { return w })...)
 			manageBuilder = manageBuilder.WithStatusSubresource(slices.Map(tc.managersJobs, func(w *batchv1.Job) client.Object { return w })...)
-			manageBuilder = manageBuilder.WithObjects(utiltesting.MakeAdmissionCheck("ac1").ControllerName(ControllerName).Obj())
+			manageBuilder = manageBuilder.WithObjects(
+				utiltesting.MakeMultiKueueConfig("config1").Clusters("worker1").Obj(),
+				utiltesting.MakeAdmissionCheck("ac1").ControllerName(ControllerName).
+					Parameters(kueue.GroupVersion.Group, "MultiKueueConfig", "config1").
```

  ```suggestion
  					Parameters(kueuealpha.GroupVersion.Group, "MultiKueueConfig", "config1").
  ```

- **pkg/util/testing/wrappers.go:792** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T15:50:21Z
```diff
@@ -746,3 +747,64 @@ func (p *WorkloadPriorityClassWrapper) PriorityValue(v int32) *WorkloadPriorityC
 func (p *WorkloadPriorityClassWrapper) Obj() *kueue.WorkloadPriorityClass {
 	return &p.WorkloadPriorityClass
 }
+
+type MultiKueueConfigWrapper struct {
+	kueuealpha.MultiKueueConfig
+}
+
+func MakeMultiKueueConfig(name string) *MultiKueueConfigWrapper {
+	return &MultiKueueConfigWrapper{
+		MultiKueueConfig: kueuealpha.MultiKueueConfig{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: name,
+			},
+		},
+	}
+}
+
+func (mkc *MultiKueueConfigWrapper) Obj() *kueuealpha.MultiKueueConfig {
+	return &mkc.MultiKueueConfig
+}
+
+func (mkc *MultiKueueConfigWrapper) Clusters(clusters ...string) *MultiKueueConfigWrapper {
+	mkc.Spec.Clusters = append(mkc.Spec.Clusters, clusters...)
+	return mkc
+}
+
+type MultiKueueClusterWrapper struct {
+	kueuealpha.MultiKueueCluster
+}
+
+func MakeMultiKueueCluster(name string) *MultiKueueClusterWrapper {
+	return &MultiKueueClusterWrapper{
+		MultiKueueCluster: kueuealpha.MultiKueueCluster{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: name,
+			},
+		},
+	}
+}
+
+func (mkc *MultiKueueClusterWrapper) Obj() *kueuealpha.MultiKueueCluster {
+	return &mkc.MultiKueueCluster
+}
+
+func (mkc *MultiKueueClusterWrapper) Secret(name, secretName string) *MultiKueueClusterWrapper {
```

  ```suggestion
  func (mkc *MultiKueueClusterWrapper) KubeConfig(name, secretName string) *MultiKueueClusterWrapper {
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-25T17:21:43Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:28** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T16:51:24Z
```diff
@@ -24,21 +24,9 @@ const (
 	MultiKueueConfigSecretKey = "kubeconfig"
 )
 
-// MultiKueueConfigSpec defines the desired state of MultiKueueConfig
-type MultiKueueConfigSpec struct {
-	// clusters contains the list of configurations for using worker clusters.
-	//
-	// +listType=map
-	// +listMapKey=name
-	// +kubebuilder:validation:MinItems=1
-	// +kubebuilder:validation:MaxItems=100
-	Clusters []MultiKueueCluster `json:"clusters"`
-}
-
-type MultiKueueCluster struct {
-	Name          string        `json:"name"`
-	KubeconfigRef KubeconfigRef `json:"kubeconfigRef"`
-}
+const (
+	MultiKueueClusterActive = "Active"
```

  merge the const blocks

- **apis/kueue/v1alpha1/multikueueconfig_types.go:38** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T16:55:38Z
```diff
@@ -48,7 +36,7 @@ const (
 	SecretLocationType LocationType = "Secret"
 )
 
-type KubeconfigRef struct {
+type KubeConfig struct {
 	// Name of the cluster inside the given KubeConfig.
 	Name string `json:"name"`
```

  I'm on the fence about this field. The name of the MultiKueueCluster object might be enough. Maybe we can make it optional?

- **apis/kueue/v1alpha1/multikueueconfig_types.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T16:58:07Z
```diff
@@ -48,7 +36,7 @@ const (
 	SecretLocationType LocationType = "Secret"
 )
 
-type KubeconfigRef struct {
+type KubeConfig struct {
 	// Name of the cluster inside the given KubeConfig.
 	Name string `json:"name"`
 
```

  Let's improve the comment for Location. When locationType=Secret, location is the name of the Secret, in the kueue-system namespace.

- **apis/kueue/v1alpha1/multikueueconfig_types.go:72** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T16:59:42Z
```diff
@@ -62,21 +50,70 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeConfig"`
+}
+
+type MultiKueueClusterStatus struct {
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	// +patchStrategy=merge
+	// +patchMergeKey=type
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+// +genclient
+// +genclient:nonNamespaced
+// +kubebuilder:object:root=true
+// +kubebuilder:storageversion
+// +kubebuilder:subresource:status
+// +kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
```

  Or we can rename this file as `multikueue_types.go`. The overall file is not that big, so splitting would rather deteriorate readability.

- **apis/kueue/v1alpha1/multikueueconfig_types.go:98** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T17:02:13Z
```diff
@@ -62,21 +50,70 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeConfig"`
+}
+
+type MultiKueueClusterStatus struct {
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	// +patchStrategy=merge
+	// +patchMergeKey=type
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+// +genclient
+// +genclient:nonNamespaced
+// +kubebuilder:object:root=true
+// +kubebuilder:storageversion
+// +kubebuilder:subresource:status
+// +kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   MultiKueueClusterSpec   `json:"spec,omitempty"`
+	Status MultiKueueClusterStatus `json:"status,omitempty"`
+}
+
+// +kubebuilder:object:root=true
+
+// MultiKueueClusterList contains a list of MultiKueueCluster
+type MultiKueueClusterList struct {
+	metav1.TypeMeta `json:",inline"`
+	metav1.ListMeta `json:"metadata,omitempty"`
+	Items           []MultiKueueCluster `json:"items"`
+}
+
+// MultiKueueConfigSpec defines the desired state of MultiKueueConfig
+type MultiKueueConfigSpec struct {
+	// List of MultiKueueClusters names where the workloads from the ClusterQueue should be distributed.
+	//
+	// +listType=set
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=100
```

  I would be surprised if MK can scale that much. Let's start more conservative (10) and we can always relax validation in the future.

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:228** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T17:07:44Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
```

  This might be a sign that we are missing an integration test for the case where a MKCluster is added after the MKConfig is created.

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:89** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T17:10:20Z
```diff
@@ -54,114 +49,62 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				log.Error(err, "reading cluster", "multiKueueCluster", clusterName)
+				return reconcile.Result{}, err
 			}
 
-			err := remoteCtrl.UpdateConfig(kubeconfigs)
 			if err != nil {
-				inactiveReason = fmt.Sprintf("Cannot start MultiKueueClusters controller: %s", err.Error())
-				a.setControllerFor(ac.Name, nil)
+				missingClusters = append(missingClusters, clusterName)
 			} else {
-				remCtrl = remoteCtrl
+				if !apimeta.IsStatusConditionTrue(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive) {
+					inactiveClusters = append(inactiveClusters, clusterName)
```

  @Maybe we can revisit this in the future, but if one cluster is inactive or even missing, we should be able to continue admitting workloads in other clusters.
  
  Please add to the tracking list.

- **apis/kueue/v1alpha1/multikueueconfig_types.go:93** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T17:11:47Z
```diff
@@ -62,21 +50,70 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeConfig"`
+}
+
+type MultiKueueClusterStatus struct {
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	// +patchStrategy=merge
+	// +patchMergeKey=type
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+// +genclient
+// +genclient:nonNamespaced
+// +kubebuilder:object:root=true
+// +kubebuilder:storageversion
+// +kubebuilder:subresource:status
+// +kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   MultiKueueClusterSpec   `json:"spec,omitempty"`
+	Status MultiKueueClusterStatus `json:"status,omitempty"`
+}
+
+// +kubebuilder:object:root=true
+
+// MultiKueueClusterList contains a list of MultiKueueCluster
+type MultiKueueClusterList struct {
+	metav1.TypeMeta `json:",inline"`
+	metav1.ListMeta `json:"metadata,omitempty"`
+	Items           []MultiKueueCluster `json:"items"`
+}
+
+// MultiKueueConfigSpec defines the desired state of MultiKueueConfig
+type MultiKueueConfigSpec struct {
+	// List of MultiKueueClusters names where the workloads from the ClusterQueue should be distributed.
+	//
+	// +listType=set
```

  Add an integration test that shows that repeating the same name is rejected by validation.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:260** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T17:19:25Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
```

  Are you still going to call update if the Cluster is disconnected and remains that way?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-25T17:25:40Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:228** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T17:25:40Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
```

  > This might be a sign that we are missing an integration test for the case where a MKCluster is added after the MKConfig is created.
  
  Yes, that's right. That is what I wanted to say here: `Also, can you verify if this handler fires reconcile when the MultiKueueCluster is created in tests?`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-25T17:28:08Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:72** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-25T17:28:08Z
```diff
@@ -62,21 +50,70 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeConfig"`
+}
+
+type MultiKueueClusterStatus struct {
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	// +patchStrategy=merge
+	// +patchMergeKey=type
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+// +genclient
+// +genclient:nonNamespaced
+// +kubebuilder:object:root=true
+// +kubebuilder:storageversion
+// +kubebuilder:subresource:status
+// +kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
```

  I'm fine with either way.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-25T17:41:30Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:260** by [@trasc](https://github.com/trasc) — 2024-01-25T17:41:30Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
```

  yes, it can be disconnected for multiple reasons and it should be updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-25T18:16:50Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:260** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T18:16:50Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
```

  but if the reason is the same, we don't need to waste the API call.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-26T10:27:54Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:28** by [@trasc](https://github.com/trasc) — 2024-01-26T06:43:04Z
```diff
@@ -24,21 +24,9 @@ const (
 	MultiKueueConfigSecretKey = "kubeconfig"
 )
 
-// MultiKueueConfigSpec defines the desired state of MultiKueueConfig
-type MultiKueueConfigSpec struct {
-	// clusters contains the list of configurations for using worker clusters.
-	//
-	// +listType=map
-	// +listMapKey=name
-	// +kubebuilder:validation:MinItems=1
-	// +kubebuilder:validation:MaxItems=100
-	Clusters []MultiKueueCluster `json:"clusters"`
-}
-
-type MultiKueueCluster struct {
-	Name          string        `json:"name"`
-	KubeconfigRef KubeconfigRef `json:"kubeconfigRef"`
-}
+const (
+	MultiKueueClusterActive = "Active"
```

  done

- **apis/kueue/v1alpha1/multikueueconfig_types.go:38** by [@trasc](https://github.com/trasc) — 2024-01-26T06:45:32Z
```diff
@@ -48,7 +36,7 @@ const (
 	SecretLocationType LocationType = "Secret"
 )
 
-type KubeconfigRef struct {
+type KubeConfig struct {
 	// Name of the cluster inside the given KubeConfig.
 	Name string `json:"name"`
```

  Indeed it's not  adding too much value but is part of the KEP. 
  
  @mimowo @mwielgus Do you think this is still useful?

- **apis/kueue/v1alpha1/multikueueconfig_types.go:39** by [@trasc](https://github.com/trasc) — 2024-01-26T06:49:04Z
```diff
@@ -48,7 +36,7 @@ const (
 	SecretLocationType LocationType = "Secret"
 )
 
-type KubeconfigRef struct {
+type KubeConfig struct {
 	// Name of the cluster inside the given KubeConfig.
 	Name string `json:"name"`
 
```

  done

- **apis/kueue/v1alpha1/multikueueconfig_types.go:72** by [@trasc](https://github.com/trasc) — 2024-01-26T06:49:13Z
```diff
@@ -62,21 +50,70 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeConfig"`
+}
+
+type MultiKueueClusterStatus struct {
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	// +patchStrategy=merge
+	// +patchMergeKey=type
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+// +genclient
+// +genclient:nonNamespaced
+// +kubebuilder:object:root=true
+// +kubebuilder:storageversion
+// +kubebuilder:subresource:status
+// +kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
```

  done

- **apis/kueue/v1alpha1/multikueueconfig_types.go:98** by [@trasc](https://github.com/trasc) — 2024-01-26T06:50:55Z
```diff
@@ -62,21 +50,70 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeConfig"`
+}
+
+type MultiKueueClusterStatus struct {
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	// +patchStrategy=merge
+	// +patchMergeKey=type
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+// +genclient
+// +genclient:nonNamespaced
+// +kubebuilder:object:root=true
+// +kubebuilder:storageversion
+// +kubebuilder:subresource:status
+// +kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   MultiKueueClusterSpec   `json:"spec,omitempty"`
+	Status MultiKueueClusterStatus `json:"status,omitempty"`
+}
+
+// +kubebuilder:object:root=true
+
+// MultiKueueClusterList contains a list of MultiKueueCluster
+type MultiKueueClusterList struct {
+	metav1.TypeMeta `json:",inline"`
+	metav1.ListMeta `json:"metadata,omitempty"`
+	Items           []MultiKueueCluster `json:"items"`
+}
+
+// MultiKueueConfigSpec defines the desired state of MultiKueueConfig
+type MultiKueueConfigSpec struct {
+	// List of MultiKueueClusters names where the workloads from the ClusterQueue should be distributed.
+	//
+	// +listType=set
+	// +kubebuilder:validation:MinItems=1
+	// +kubebuilder:validation:MaxItems=100
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:81** by [@trasc](https://github.com/trasc) — 2024-01-26T06:58:44Z
```diff
@@ -54,114 +49,62 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				log.Error(err, "reading cluster", "multiKueueCluster", clusterName)
```

  The reporting location will be different. 
  (this was requested in a previous review https://github.com/kubernetes-sigs/kueue/pull/1631#discussion_r1462927037)

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:89** by [@trasc](https://github.com/trasc) — 2024-01-26T07:02:47Z
```diff
@@ -54,114 +49,62 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				log.Error(err, "reading cluster", "multiKueueCluster", clusterName)
+				return reconcile.Result{}, err
 			}
 
-			err := remoteCtrl.UpdateConfig(kubeconfigs)
 			if err != nil {
-				inactiveReason = fmt.Sprintf("Cannot start MultiKueueClusters controller: %s", err.Error())
-				a.setControllerFor(ac.Name, nil)
+				missingClusters = append(missingClusters, clusterName)
 			} else {
-				remCtrl = remoteCtrl
+				if !apimeta.IsStatusConditionTrue(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive) {
+					inactiveClusters = append(inactiveClusters, clusterName)
```

  added

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:101** by [@trasc](https://github.com/trasc) — 2024-01-26T07:10:51Z
```diff
@@ -54,114 +49,62 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				log.Error(err, "reading cluster", "multiKueueCluster", clusterName)
+				return reconcile.Result{}, err
 			}
 
-			err := remoteCtrl.UpdateConfig(kubeconfigs)
 			if err != nil {
-				inactiveReason = fmt.Sprintf("Cannot start MultiKueueClusters controller: %s", err.Error())
-				a.setControllerFor(ac.Name, nil)
+				missingClusters = append(missingClusters, clusterName)
 			} else {
-				remCtrl = remoteCtrl
+				if !apimeta.IsStatusConditionTrue(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive) {
+					inactiveClusters = append(inactiveClusters, clusterName)
+				}
 			}
 		}
+
+		var messageParts []string
+		if len(missingClusters) > 0 {
+			messageParts = []string{fmt.Sprintf("Missing clusters: %v", missingClusters)}
+		}
+		if len(inactiveClusters) > 0 {
+			messageParts = append(messageParts, fmt.Sprintf("Inactive clusters: %v", inactiveClusters))
+		}
+		inactiveReason = strings.Join(messageParts, ", ")
```

  no, this is used in the check's active condition not the cluster and I think `Missing clusters: [worker3], Inactive clusters: [worker1]` is descriptive enough. (The `Active` condition being set to `False`)

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:228** by [@trasc](https://github.com/trasc) — 2024-01-26T07:13:45Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
```

  it is a cluster.

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:234** by [@trasc](https://github.com/trasc) — 2024-01-26T07:14:22Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:278** by [@trasc](https://github.com/trasc) — 2024-01-26T07:15:11Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldMKC, isOldMKC := event.ObjectOld.(*kueuealpha.MultiKueueCluster)
+	newMKC, isNewMKC := event.ObjectNew.(*kueuealpha.MultiKueueCluster)
+	if !isOldMKC || !isNewMKC {
+		return
+	}
+
+	oldActive := apimeta.IsStatusConditionTrue(oldMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	newActive := apimeta.IsStatusConditionTrue(newMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if oldActive != newActive {
+		if err := m.queue(ctx, newMKC, q); err != nil {
+			ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on update event", "multiKueueCluster", klog.KObj(oldMKC))
+		}
 	}
 }
 
-func (s *secretHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on generic event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on delete event", "multiKueueCluster", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) queue(ctx context.Context, obj client.Object, q workqueue.RateLimitingInterface) error {
-	users := &kueuealpha.MultiKueueConfigList{}
-	secret, isSecret := obj.(*corev1.Secret)
-	if !isSecret {
-		return errors.New("not a secret")
+func (m *mkClusterHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on generic event", "multiKueueCluster", klog.KObj(mkc))
 	}
+}
 
-	if err := s.client.List(ctx, users, client.MatchingFields{UsingKubeConfigs: strings.Join([]string{secret.Namespace, secret.Name}, "/")}); err != nil {
+func (m *mkClusterHandler) queue(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, q workqueue.RateLimitingInterface) error {
+	users := &kueuealpha.MultiKueueConfigList{}
+	if err := m.client.List(ctx, users, client.MatchingFields{UsingMultiKueueClusters: cluster.Name}); err != nil {
```

  yes. it's validated in "multiple configs, single match" TC.

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:68** by [@trasc](https://github.com/trasc) — 2024-01-26T07:20:33Z
```diff
@@ -65,105 +65,108 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 	return builder, ctx
 }
 
-func TestMultikueConfigUsingKubeconfig(t *testing.T) {
+func TestListMultikueClustersUsingKubeconfig(t *testing.T) {
```

  done

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:106** by [@trasc](https://github.com/trasc) — 2024-01-26T07:22:22Z
```diff
@@ -65,105 +65,108 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 	return builder, ctx
 }
 
-func TestMultikueConfigUsingKubeconfig(t *testing.T) {
+func TestListMultikueClustersUsingKubeconfig(t *testing.T) {
 	cases := map[string]struct {
-		configs       []*kueuealpha.MultiKueueConfig
+		clusters      []*kueuealpha.MultiKueueCluster
 		filter        client.ListOption
 		wantListError error
 		wantList      []string
 	}{
 		"no clusters": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "name",
-					},
-				},
-			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"single cluster, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 		"single cluster, no match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret2",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"multiple clusters, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret0",
-								},
-							},
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			builder, ctx := getClientBuilder()
 			k8sclient := builder.Build()
-			for _, req := range tc.configs {
+			for _, req := range tc.clusters {
 				if err := k8sclient.Create(ctx, req); err != nil {
-					t.Errorf("Unable to create %s request: %v", client.ObjectKeyFromObject(req), err)
+					t.Fatalf("Unable to create %s cluster: %v", client.ObjectKeyFromObject(req), err)
```

  done

- **pkg/controller/admissionchecks/multikueue/indexer_test.go:125** by [@trasc](https://github.com/trasc) — 2024-01-26T07:22:58Z
```diff
@@ -65,105 +65,108 @@ func getClientBuilder() (*fake.ClientBuilder, context.Context) {
 	return builder, ctx
 }
 
-func TestMultikueConfigUsingKubeconfig(t *testing.T) {
+func TestListMultikueClustersUsingKubeconfig(t *testing.T) {
 	cases := map[string]struct {
-		configs       []*kueuealpha.MultiKueueConfig
+		clusters      []*kueuealpha.MultiKueueCluster
 		filter        client.ListOption
 		wantListError error
 		wantList      []string
 	}{
 		"no clusters": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "name",
-					},
-				},
-			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"single cluster, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 		"single cluster, no match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret2",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter: client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
 		},
 		"multiple clusters, single match": {
-			configs: []*kueuealpha.MultiKueueConfig{
-				{
-					ObjectMeta: metav1.ObjectMeta{
-						Namespace: "default",
-						Name:      "config1",
-					},
-					Spec: kueuealpha.MultiKueueConfigSpec{
-						Clusters: []kueuealpha.MultiKueueCluster{
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret0",
-								},
-							},
-							{
-								KubeconfigRef: kueuealpha.KubeconfigRef{
-									Location: "secret1",
-								},
-							},
-						},
-					},
-				},
+			clusters: []*kueuealpha.MultiKueueCluster{
+				utiltesting.MakeMultiKueueCluster("cluster1").Secret("", "secret1").Obj(),
+				utiltesting.MakeMultiKueueCluster("cluster2").Secret("", "secret2").Obj(),
 			},
 			filter:   client.MatchingFields{UsingKubeConfigs: TestNamespace + "/secret1"},
-			wantList: []string{"config1"},
+			wantList: []string{"cluster1"},
 		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
 			builder, ctx := getClientBuilder()
 			k8sclient := builder.Build()
-			for _, req := range tc.configs {
+			for _, req := range tc.clusters {
 				if err := k8sclient.Create(ctx, req); err != nil {
-					t.Errorf("Unable to create %s request: %v", client.ObjectKeyFromObject(req), err)
+					t.Fatalf("Unable to create %s cluster: %v", client.ObjectKeyFromObject(req), err)
+				}
+			}
+
+			lst := &kueuealpha.MultiKueueClusterList{}
+
+			gotListErr := k8sclient.List(ctx, lst, tc.filter)
+			if diff := cmp.Diff(tc.wantListError, gotListErr); diff != "" {
+				t.Errorf("unexpected list error (-want/+got):\n%s", diff)
+			}
+
+			gotList := slices.Map(lst.Items, func(mkc *kueuealpha.MultiKueueCluster) string { return mkc.Name })
+			if diff := cmp.Diff(tc.wantList, gotList, cmpopts.EquateEmpty(), cmpopts.SortSlices(func(a, b string) bool { return a < b })); diff != "" {
+				t.Errorf("unexpected list (-want/+got):\n%s", diff)
+			}
+		})
+	}
+}
+
+func TestListMultikueConfigsUsingMultikueueClusters(t *testing.T) {
```

  done

- **pkg/controller/admissionchecks/multikueue/jobset_adapter_test.go:229** by [@trasc](https://github.com/trasc) — 2024-01-26T07:32:41Z
```diff
@@ -223,12 +223,16 @@ func TestWlReconcileJobset(t *testing.T) {
 			manageBuilder = manageBuilder.WithLists(&kueue.WorkloadList{Items: tc.managersWorkloads}, &jobset.JobSetList{Items: tc.managersJobSets})
 			manageBuilder = manageBuilder.WithStatusSubresource(slices.Map(tc.managersWorkloads, func(w *kueue.Workload) client.Object { return w })...)
 			manageBuilder = manageBuilder.WithStatusSubresource(slices.Map(tc.managersJobSets, func(w *jobset.JobSet) client.Object { return w })...)
-			manageBuilder = manageBuilder.WithObjects(utiltesting.MakeAdmissionCheck("ac1").ControllerName(ControllerName).Obj())
+			manageBuilder = manageBuilder.WithObjects(
+				utiltesting.MakeMultiKueueConfig("config1").Clusters("worker1").Obj(),
+				utiltesting.MakeAdmissionCheck("ac1").ControllerName(ControllerName).
+					Parameters(kueue.GroupVersion.Group, "MultiKueueConfig", "config1").
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@trasc](https://github.com/trasc) — 2024-01-26T07:35:04Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
```

  :), done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:129** by [@trasc](https://github.com/trasc) — 2024-01-26T07:36:02Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:139** by [@trasc](https://github.com/trasc) — 2024-01-26T07:38:17Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:184** by [@trasc](https://github.com/trasc) — 2024-01-26T07:39:47Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:208** by [@trasc](https://github.com/trasc) — 2024-01-26T07:40:23Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:241** by [@trasc](https://github.com/trasc) — 2024-01-26T07:50:24Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
```

  If not fond , it will not retry since the watch for secrets will trigger when the missing secret is created. 
  For other errors , I thing it's safe to trust the controller-runtime retry backoff mechanism.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:244** by [@trasc](https://github.com/trasc) — 2024-01-26T08:11:50Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
```

  .... I have no idea, but reading from a nil map is safe.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:260** by [@trasc](https://github.com/trasc) — 2024-01-26T08:19:30Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
```

  let's check the full condition

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:282** by [@trasc](https://github.com/trasc) — 2024-01-26T08:26:31Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
+	oldCondition := apimeta.FindStatusCondition(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if active && oldCondition != nil && oldCondition.Status == metav1.ConditionTrue {
+		return nil
+	}
+
+	apimeta.SetStatusCondition(&cluster.Status.Conditions, newCondition)
+	return c.client.Status().Update(ctx, cluster)
+}
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters/status,verbs=get;update;patch
+
+func newClustersReconciler(c client.Client, namespace string) *clustersReconciler {
+	return &clustersReconciler{
+		client:          c,
+		configNamespace: namespace,
+		clients:         make(map[string]*remoteClient),
+		wlUpdateCh:      make(chan event.GenericEvent, 10),
```

  It's just one channel with 10 sapce buffer, the same approach as in other controllers (https://github.com/kubernetes-sigs/kueue/blob/1adca158dcb0aa77e00987a8cf37c35d17c64744/pkg/controller/core/clusterqueue_controller.go#L133)

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:305** by [@trasc](https://github.com/trasc) — 2024-01-26T08:29:15Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
+	oldCondition := apimeta.FindStatusCondition(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if active && oldCondition != nil && oldCondition.Status == metav1.ConditionTrue {
+		return nil
+	}
+
+	apimeta.SetStatusCondition(&cluster.Status.Conditions, newCondition)
+	return c.client.Status().Update(ctx, cluster)
+}
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters/status,verbs=get;update;patch
+
+func newClustersReconciler(c client.Client, namespace string) *clustersReconciler {
+	return &clustersReconciler{
+		client:          c,
+		configNamespace: namespace,
+		clients:         make(map[string]*remoteClient),
+		wlUpdateCh:      make(chan event.GenericEvent, 10),
+	}
+}
+
+func (c *clustersReconciler) setupWithManager(mgr ctrl.Manager) error {
+	err := mgr.Add(c)
+	if err != nil {
+		return err
+	}
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&kueuealpha.MultiKueueCluster{}).
+		Watches(&corev1.Secret{}, &secretHandler{client: c.client}).
+		Complete(c)
+}
+
+type secretHandler struct {
+	client client.Client
+}
+
+var _ handler.EventHandler = (*secretHandler)(nil)
+
+func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	if err := s.queue(ctx, event.Object, q); err != nil {
```

  `s.queue` will return `return errors.New("not a secret")` if the check fails

- **pkg/controller/admissionchecks/multikueue/workload.go:201** by [@trasc](https://github.com/trasc) — 2024-01-26T08:31:56Z
```diff
@@ -177,13 +198,9 @@ func (a *wlReconciler) readGroup(ctx context.Context, local *kueue.Workload) (*w
 		return nil, nil
 	}
 
-	rController, found := a.acr.controllerFor(relevantChecks[0])
-	if !found {
-		return nil, errors.New("remote controller not found")
-	}
-
-	if !rController.IsActive() {
-		return nil, errors.New("remote controller is not active")
+	rClients, err := a.remoteClientsForAC(ctx, relevantChecks[0])
```

  There should only be one admission check managed by multikueue set up in one cluster queue, #1635 will try to enforce that.

- **pkg/util/testing/wrappers.go:792** by [@trasc](https://github.com/trasc) — 2024-01-26T08:33:27Z
```diff
@@ -746,3 +747,64 @@ func (p *WorkloadPriorityClassWrapper) PriorityValue(v int32) *WorkloadPriorityC
 func (p *WorkloadPriorityClassWrapper) Obj() *kueue.WorkloadPriorityClass {
 	return &p.WorkloadPriorityClass
 }
+
+type MultiKueueConfigWrapper struct {
+	kueuealpha.MultiKueueConfig
+}
+
+func MakeMultiKueueConfig(name string) *MultiKueueConfigWrapper {
+	return &MultiKueueConfigWrapper{
+		MultiKueueConfig: kueuealpha.MultiKueueConfig{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: name,
+			},
+		},
+	}
+}
+
+func (mkc *MultiKueueConfigWrapper) Obj() *kueuealpha.MultiKueueConfig {
+	return &mkc.MultiKueueConfig
+}
+
+func (mkc *MultiKueueConfigWrapper) Clusters(clusters ...string) *MultiKueueConfigWrapper {
+	mkc.Spec.Clusters = append(mkc.Spec.Clusters, clusters...)
+	return mkc
+}
+
+type MultiKueueClusterWrapper struct {
+	kueuealpha.MultiKueueCluster
+}
+
+func MakeMultiKueueCluster(name string) *MultiKueueClusterWrapper {
+	return &MultiKueueClusterWrapper{
+		MultiKueueCluster: kueuealpha.MultiKueueCluster{
+			ObjectMeta: metav1.ObjectMeta{
+				Name: name,
+			},
+		},
+	}
+}
+
+func (mkc *MultiKueueClusterWrapper) Obj() *kueuealpha.MultiKueueCluster {
+	return &mkc.MultiKueueCluster
+}
+
+func (mkc *MultiKueueClusterWrapper) Secret(name, secretName string) *MultiKueueClusterWrapper {
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:228** by [@trasc](https://github.com/trasc) — 2024-01-26T08:36:33Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
```

  > This might be a sign that we are missing an integration test for the case where a MKCluster is added after the MKConfig is created.
  
  An integration test wold not have been able to catch this because immediately after creation the Active condition of the cluster wold have been updated triggering an Update .

- **apis/kueue/v1alpha1/multikueueconfig_types.go:93** by [@trasc](https://github.com/trasc) — 2024-01-26T10:13:47Z
```diff
@@ -62,21 +50,70 @@ type KubeconfigRef struct {
 	LocationType LocationType `json:"locationType"`
 }
 
-//+genclient
-//+genclient:nonNamespaced
-//+kubebuilder:object:root=true
-//+kubebuilder:storageversion
-//+kubebuilder:resource:scope=Cluster
+type MultiKueueClusterSpec struct {
+	// Information how to connect to the cluster.
+	KubeConfig KubeConfig `json:"kubeConfig"`
+}
+
+type MultiKueueClusterStatus struct {
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	// +patchStrategy=merge
+	// +patchMergeKey=type
+	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+}
+
+// +genclient
+// +genclient:nonNamespaced
+// +kubebuilder:object:root=true
+// +kubebuilder:storageversion
+// +kubebuilder:subresource:status
+// +kubebuilder:resource:scope=Cluster
+
+// MultiKueueCluster is the Schema for the multikueue API
+type MultiKueueCluster struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   MultiKueueClusterSpec   `json:"spec,omitempty"`
+	Status MultiKueueClusterStatus `json:"status,omitempty"`
+}
+
+// +kubebuilder:object:root=true
+
+// MultiKueueClusterList contains a list of MultiKueueCluster
+type MultiKueueClusterList struct {
+	metav1.TypeMeta `json:",inline"`
+	metav1.ListMeta `json:"metadata,omitempty"`
+	Items           []MultiKueueCluster `json:"items"`
+}
+
+// MultiKueueConfigSpec defines the desired state of MultiKueueConfig
+type MultiKueueConfigSpec struct {
+	// List of MultiKueueClusters names where the workloads from the ClusterQueue should be distributed.
+	//
+	// +listType=set
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:228** by [@trasc](https://github.com/trasc) — 2024-01-26T10:14:21Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
```

  added a test

- **pkg/controller/admissionchecks/multikueue/workload_test.go:297** by [@trasc](https://github.com/trasc) — 2024-01-26T10:17:16Z
```diff
@@ -291,27 +291,27 @@ func TestWlReconcile(t *testing.T) {
 			manageBuilder = manageBuilder.WithLists(&kueue.WorkloadList{Items: tc.managersWorkloads}, &batchv1.JobList{Items: tc.managersJobs})
 			manageBuilder = manageBuilder.WithStatusSubresource(slices.Map(tc.managersWorkloads, func(w *kueue.Workload) client.Object { return w })...)
 			manageBuilder = manageBuilder.WithStatusSubresource(slices.Map(tc.managersJobs, func(w *batchv1.Job) client.Object { return w })...)
-			manageBuilder = manageBuilder.WithObjects(utiltesting.MakeAdmissionCheck("ac1").ControllerName(ControllerName).Obj())
+			manageBuilder = manageBuilder.WithObjects(
+				utiltesting.MakeMultiKueueConfig("config1").Clusters("worker1").Obj(),
+				utiltesting.MakeAdmissionCheck("ac1").ControllerName(ControllerName).
+					Parameters(kueue.GroupVersion.Group, "MultiKueueConfig", "config1").
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-26T10:39:53Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:38** by [@mimowo](https://github.com/mimowo) — 2024-01-26T10:39:53Z
```diff
@@ -48,7 +36,7 @@ const (
 	SecretLocationType LocationType = "Secret"
 )
 
-type KubeconfigRef struct {
+type KubeConfig struct {
 	// Name of the cluster inside the given KubeConfig.
 	Name string `json:"name"`
```

  Good spot, I think the intention is this value equals the name of the cluster. Allowing to configure this differently can be confusing to users IMO. 
  
   I suggest dropping it entirely, but also suggest to remove it from KEP for consistency (can be the same PR).

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-26T10:52:36Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:38** by [@trasc](https://github.com/trasc) — 2024-01-26T10:52:36Z
```diff
@@ -48,7 +36,7 @@ const (
 	SecretLocationType LocationType = "Secret"
 )
 
-type KubeconfigRef struct {
+type KubeConfig struct {
 	// Name of the cluster inside the given KubeConfig.
 	Name string `json:"name"`
```

  removed

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-26T15:47:08Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:81** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T15:47:08Z
```diff
@@ -54,114 +49,62 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				log.Error(err, "reading cluster", "multiKueueCluster", clusterName)
```

  I see. I don't have strong objections. So, I'm ok with this.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-26T15:47:47Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:101** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T15:47:47Z
```diff
@@ -54,114 +49,62 @@ func newMultiKueueStoreHelper(c client.Client) (*multiKueueStoreHelper, error) {
 }
 
 // ACReconciler implements the reconciler for all the admission checks controlled by multikueue.
-// Its main tasks being to:
-// - Maintain the list of remote controllers associated to each admission checks.
-// - Maintain the active state of the admission checks.
+// Its main task being to maintain the active state of the admission checks based on the heath
+// of its referenced MultiKueueClusters.
 type ACReconciler struct {
-	client          client.Client
-	helper          *multiKueueStoreHelper
-	configNamespace string
-
-	lock sync.RWMutex
-	// The list of remote controllers, indexed by the admission checks name.
-	controllers map[string]*remoteController
-	wlUpdateCh  chan event.GenericEvent
-
-	// rootContext - holds the context passed by the controller-runtime on Start.
-	// It's used to create child contexts for MultiKueueClusters client watch routines
-	// that will gracefully end when the controller-manager stops.
-	rootContext context.Context
-
-	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
-	// and creating valid kubeconfig content is not trivial.
-	// The full client creation and usage is validated in the integration and e2e tests.
-	builderOverride clientWithWatchBuilder
+	client client.Client
+	helper *multiKueueStoreHelper
 }
 
 var _ reconcile.Reconciler = (*ACReconciler)(nil)
-var _ manager.Runnable = (*ACReconciler)(nil)
-
-func (a *ACReconciler) controllerFor(acName string) (*remoteController, bool) {
-	a.lock.RLock()
-	defer a.lock.RUnlock()
-
-	c, f := a.controllers[acName]
-	return c, f
-}
-
-func (a *ACReconciler) setControllerFor(acName string, c *remoteController) {
-	a.lock.Lock()
-	defer a.lock.Unlock()
-
-	if old, found := a.controllers[acName]; found {
-		old.watchCancel()
-	}
-	if c != nil {
-		a.controllers[acName] = c
-		return
-	}
-	delete(a.controllers, acName)
-}
-
-func (a *ACReconciler) Start(ctx context.Context) error {
-	a.rootContext = ctx
-	return nil
-}
 
 func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
 	ac := &kueue.AdmissionCheck{}
 	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
-		if apierrors.IsNotFound(err) || !ac.DeletionTimestamp.IsZero() {
-			// stop/deleted a potential check controller
-			if cc, existing := a.controllerFor(req.Name); existing {
-				cc.watchCancel()
-				a.setControllerFor(req.Name, nil)
-				log.V(2).Info("Controller removed")
-			}
-			return reconcile.Result{}, nil
-		}
-		return reconcile.Result{}, err
+		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	var remCtrl *remoteController
 	inactiveReason := ""
 
 	log.V(2).Info("Reconcile AdmissionCheck")
 	if cfg, err := a.helper.ConfigFromRef(ctx, ac.Spec.Parameters); err != nil {
 		inactiveReason = fmt.Sprintf("Cannot load the AdmissionChecks parameters: %s", err.Error())
 	} else {
-		kubeconfigs, err := a.getKubeConfigs(ctx, &cfg.Spec)
-		if err != nil {
-			a.setControllerFor(ac.Name, nil)
-			inactiveReason = fmt.Sprintf("Cannot load kubeconfigs: %s", err.Error())
-		} else {
-			remoteCtrl, existing := a.controllerFor(ac.Name)
-			if !existing {
-				remoteCtrl = newRemoteController(a.rootContext, a.client, a.wlUpdateCh)
-				if a.builderOverride != nil {
-					remoteCtrl.builderOverride = a.builderOverride
-				}
-				a.setControllerFor(ac.Name, remoteCtrl)
+		var missingClusters []string
+		var inactiveClusters []string
+		// check the status of the clusters
+		for _, clusterName := range cfg.Spec.Clusters {
+			cluster := &kueuealpha.MultiKueueCluster{}
+			err := a.client.Get(ctx, types.NamespacedName{Name: clusterName}, cluster)
+			if client.IgnoreNotFound(err) != nil {
+				log.Error(err, "reading cluster", "multiKueueCluster", clusterName)
+				return reconcile.Result{}, err
 			}
 
-			err := remoteCtrl.UpdateConfig(kubeconfigs)
 			if err != nil {
-				inactiveReason = fmt.Sprintf("Cannot start MultiKueueClusters controller: %s", err.Error())
-				a.setControllerFor(ac.Name, nil)
+				missingClusters = append(missingClusters, clusterName)
 			} else {
-				remCtrl = remoteCtrl
+				if !apimeta.IsStatusConditionTrue(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive) {
+					inactiveClusters = append(inactiveClusters, clusterName)
+				}
 			}
 		}
+
+		var messageParts []string
+		if len(missingClusters) > 0 {
+			messageParts = []string{fmt.Sprintf("Missing clusters: %v", missingClusters)}
+		}
+		if len(inactiveClusters) > 0 {
+			messageParts = append(messageParts, fmt.Sprintf("Inactive clusters: %v", inactiveClusters))
+		}
+		inactiveReason = strings.Join(messageParts, ", ")
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-26T15:48:17Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:278** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T15:48:17Z
```diff
@@ -308,49 +218,69 @@ func queueReconcileForConfigUsers(ctx context.Context, config string, c client.C
 	return nil
 }
 
-type secretHandler struct {
+type mkClusterHandler struct {
 	client client.Client
 }
 
-var _ handler.EventHandler = (*secretHandler)(nil)
+var _ handler.EventHandler = (*mkClusterHandler)(nil)
 
-func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on create event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueConfig)
+	if !isMKC {
+		return
 	}
-}
 
-func (s *secretHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.ObjectOld, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "secret", klog.KObj(event.ObjectOld))
+	if err := queueReconcileForConfigUsers(ctx, mkc.Name, m.client, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on create event", "multiKueueConfig", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldMKC, isOldMKC := event.ObjectOld.(*kueuealpha.MultiKueueCluster)
+	newMKC, isNewMKC := event.ObjectNew.(*kueuealpha.MultiKueueCluster)
+	if !isOldMKC || !isNewMKC {
+		return
+	}
+
+	oldActive := apimeta.IsStatusConditionTrue(oldMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	newActive := apimeta.IsStatusConditionTrue(newMKC.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if oldActive != newActive {
+		if err := m.queue(ctx, newMKC, q); err != nil {
+			ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on update event", "multiKueueCluster", klog.KObj(oldMKC))
+		}
 	}
 }
 
-func (s *secretHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
-	if err := s.queue(ctx, event.Object, q); err != nil {
-		ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on generic event", "secret", klog.KObj(event.Object))
+func (m *mkClusterHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on delete event", "multiKueueCluster", klog.KObj(mkc))
 	}
 }
 
-func (s *secretHandler) queue(ctx context.Context, obj client.Object, q workqueue.RateLimitingInterface) error {
-	users := &kueuealpha.MultiKueueConfigList{}
-	secret, isSecret := obj.(*corev1.Secret)
-	if !isSecret {
-		return errors.New("not a secret")
+func (m *mkClusterHandler) Generic(ctx context.Context, event event.GenericEvent, q workqueue.RateLimitingInterface) {
+	mkc, isMKC := event.Object.(*kueuealpha.MultiKueueCluster)
+	if !isMKC {
+		return
+	}
+
+	if err := m.queue(ctx, mkc, q); err != nil {
+		ctrl.LoggerFrom(ctx).V(2).Error(err, "Failure on generic event", "multiKueueCluster", klog.KObj(mkc))
 	}
+}
 
-	if err := s.client.List(ctx, users, client.MatchingFields{UsingKubeConfigs: strings.Join([]string{secret.Namespace, secret.Name}, "/")}); err != nil {
+func (m *mkClusterHandler) queue(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, q workqueue.RateLimitingInterface) error {
+	users := &kueuealpha.MultiKueueConfigList{}
+	if err := m.client.List(ctx, users, client.MatchingFields{UsingMultiKueueClusters: cluster.Name}); err != nil {
```

  Thank you for clarifying!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-26T15:50:24Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:241** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T15:50:24Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
```

  It makes sense. The controller-runtime retry mechanism would be safe.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-26T15:50:37Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:244** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T15:50:36Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
```

  It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-26T15:51:44Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:282** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T15:51:44Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
+	oldCondition := apimeta.FindStatusCondition(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if active && oldCondition != nil && oldCondition.Status == metav1.ConditionTrue {
+		return nil
+	}
+
+	apimeta.SetStatusCondition(&cluster.Status.Conditions, newCondition)
+	return c.client.Status().Update(ctx, cluster)
+}
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters/status,verbs=get;update;patch
+
+func newClustersReconciler(c client.Client, namespace string) *clustersReconciler {
+	return &clustersReconciler{
+		client:          c,
+		configNamespace: namespace,
+		clients:         make(map[string]*remoteClient),
+		wlUpdateCh:      make(chan event.GenericEvent, 10),
```

  Ah, I see. We use 10 as a buffer size: https://github.com/kubernetes-sigs/kueue/blob/59980e245f202da838de890613155bf962f069bb/pkg/controller/core/core.go#L30
  
  Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-26T19:45:14Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:305** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T19:45:14Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
+	oldCondition := apimeta.FindStatusCondition(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if active && oldCondition != nil && oldCondition.Status == metav1.ConditionTrue {
+		return nil
+	}
+
+	apimeta.SetStatusCondition(&cluster.Status.Conditions, newCondition)
+	return c.client.Status().Update(ctx, cluster)
+}
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters/status,verbs=get;update;patch
+
+func newClustersReconciler(c client.Client, namespace string) *clustersReconciler {
+	return &clustersReconciler{
+		client:          c,
+		configNamespace: namespace,
+		clients:         make(map[string]*remoteClient),
+		wlUpdateCh:      make(chan event.GenericEvent, 10),
+	}
+}
+
+func (c *clustersReconciler) setupWithManager(mgr ctrl.Manager) error {
+	err := mgr.Add(c)
+	if err != nil {
+		return err
+	}
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&kueuealpha.MultiKueueCluster{}).
+		Watches(&corev1.Secret{}, &secretHandler{client: c.client}).
+		Complete(c)
+}
+
+type secretHandler struct {
+	client client.Client
+}
+
+var _ handler.EventHandler = (*secretHandler)(nil)
+
+func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	if err := s.queue(ctx, event.Object, q); err != nil {
```

  @trasc Yes, that's right, but I still think that the additional key/value is odd in that case.
  
  > "secret", klog.KObj(event.ObjectOld)
  
  So, I still would suggest applying my comment.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-26T19:55:04Z

- **pkg/controller/admissionchecks/multikueue/workload.go:201** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-26T19:55:04Z
```diff
@@ -177,13 +198,9 @@ func (a *wlReconciler) readGroup(ctx context.Context, local *kueue.Workload) (*w
 		return nil, nil
 	}
 
-	rController, found := a.acr.controllerFor(relevantChecks[0])
-	if !found {
-		return nil, errors.New("remote controller not found")
-	}
-
-	if !rController.IsActive() {
-		return nil, errors.New("remote controller is not active")
+	rClients, err := a.remoteClientsForAC(ctx, relevantChecks[0])
```

  That makes sense. Thanks.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-27T08:25:10Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:305** by [@trasc](https://github.com/trasc) — 2024-01-27T08:25:10Z
```diff
@@ -0,0 +1,345 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	client          client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote clients, indexed by the cluster name.
+	clients    map[string]*remoteClient
+	wlUpdateCh chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.clients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.clients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.clients[clusterName]
+	if !found {
+		client = newRemoteClient(c.client, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.clients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		delete(c.clients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.clients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.client.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.client.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the cluster is connected and remains that way, skip the status update
+	oldCondition := apimeta.FindStatusCondition(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if active && oldCondition != nil && oldCondition.Status == metav1.ConditionTrue {
+		return nil
+	}
+
+	apimeta.SetStatusCondition(&cluster.Status.Conditions, newCondition)
+	return c.client.Status().Update(ctx, cluster)
+}
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters/status,verbs=get;update;patch
+
+func newClustersReconciler(c client.Client, namespace string) *clustersReconciler {
+	return &clustersReconciler{
+		client:          c,
+		configNamespace: namespace,
+		clients:         make(map[string]*remoteClient),
+		wlUpdateCh:      make(chan event.GenericEvent, 10),
+	}
+}
+
+func (c *clustersReconciler) setupWithManager(mgr ctrl.Manager) error {
+	err := mgr.Add(c)
+	if err != nil {
+		return err
+	}
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&kueuealpha.MultiKueueCluster{}).
+		Watches(&corev1.Secret{}, &secretHandler{client: c.client}).
+		Complete(c)
+}
+
+type secretHandler struct {
+	client client.Client
+}
+
+var _ handler.EventHandler = (*secretHandler)(nil)
+
+func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	if err := s.queue(ctx, event.Object, q); err != nil {
```

  @tenzen-y done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-29T05:04:50Z

/lgtm

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:306** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-29T05:04:35Z
```diff
@@ -0,0 +1,358 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	} else {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	localClient     client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote remoteClients, indexed by the cluster name.
+	remoteClients map[string]*remoteClient
+	wlUpdateCh    chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.remoteClients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.remoteClients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.remoteClients[clusterName]
+	if !found {
+		client = newRemoteClient(c.localClient, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.remoteClients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "failed to set kubeConfig in the remote client")
+		delete(c.remoteClients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.remoteClients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.localClient.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	log.V(2).Info("Reconcile MultiKueueCluster")
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.localClient.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the condition is up to date
+	oldCondition := apimeta.FindStatusCondition(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if cmpConditionState(oldCondition, &newCondition) {
+		return nil
+	}
+
+	apimeta.SetStatusCondition(&cluster.Status.Conditions, newCondition)
+	return c.localClient.Status().Update(ctx, cluster)
+}
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters/status,verbs=get;update;patch
+
+func newClustersReconciler(c client.Client, namespace string) *clustersReconciler {
+	return &clustersReconciler{
+		localClient:     c,
+		configNamespace: namespace,
+		remoteClients:   make(map[string]*remoteClient),
+		wlUpdateCh:      make(chan event.GenericEvent, 10),
+	}
+}
+
+func (c *clustersReconciler) setupWithManager(mgr ctrl.Manager) error {
+	err := mgr.Add(c)
+	if err != nil {
+		return err
+	}
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&kueuealpha.MultiKueueCluster{}).
+		Watches(&corev1.Secret{}, &secretHandler{client: c.localClient}).
+		Complete(c)
+}
+
+type secretHandler struct {
+	client client.Client
+}
+
+var _ handler.EventHandler = (*secretHandler)(nil)
+
+func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	secret, isSecret := event.Object.(*corev1.Secret)
+	if !isSecret {
+		ctrl.LoggerFrom(ctx).V(5).Info("Failure on create event, not a secret")
```

  ```suggestion
  		ctrl.LoggerFrom(ctx).V(5)Error("Failure on create event, not a secret")
  		return
  ```
  
  IIUC, if this error occurs here, `s.queue` isn't needed to perform, right?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-29T06:25:40Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:306** by [@trasc](https://github.com/trasc) — 2024-01-29T06:25:40Z
```diff
@@ -0,0 +1,358 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package multikueue
+
+import (
+	"context"
+	"fmt"
+	"strings"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/watch"
+	"k8s.io/client-go/tools/clientcmd"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueuealpha "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type clientWithWatchBuilder func(config []byte, options client.Options) (client.WithWatch, error)
+
+type remoteClient struct {
+	localClient client.Client
+	client      client.WithWatch
+	wlUpdateCh  chan<- event.GenericEvent
+	watchCancel func()
+	kubeconfig  []byte
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+func newRemoteClient(localClient client.Client, wlUpdateCh chan<- event.GenericEvent) *remoteClient {
+	rc := &remoteClient{
+		wlUpdateCh:  wlUpdateCh,
+		localClient: localClient,
+	}
+	return rc
+}
+
+func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithWatch, error) {
+	restConfig, err := clientcmd.RESTConfigFromKubeConfig(kubeconfig)
+	if err != nil {
+		return nil, err
+	}
+	return client.NewWithWatch(restConfig, options)
+}
+
+// setConfig - will try to recreate the k8s client and restart watching if the new config is different than
+// the one currently used.
+func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) error {
+	if equality.Semantic.DeepEqual(kubeconfig, rc.kubeconfig) {
+		return nil
+	}
+
+	if rc.watchCancel != nil {
+		rc.watchCancel()
+		rc.watchCancel = nil
+	}
+
+	builder := newClientWithWatch
+	if rc.builderOverride != nil {
+		builder = rc.builderOverride
+	}
+	remoteClient, err := builder(kubeconfig, client.Options{Scheme: rc.localClient.Scheme()})
+	if err != nil {
+		return err
+	}
+	rc.client = remoteClient
+
+	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	if err != nil {
+		return nil
+	}
+
+	go func() {
+		for r := range newWatcher.ResultChan() {
+			rc.queueWorkloadEvent(watchCtx, r)
+		}
+	}()
+
+	rc.watchCancel = newWatcher.Stop
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event) {
+	wl, isWl := ev.Object.(*kueue.Workload)
+	if !isWl {
+		return
+	}
+
+	localWl := &kueue.Workload{}
+	if err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(wl), localWl); err == nil {
+		rc.wlUpdateCh <- event.GenericEvent{Object: localWl}
+	} else {
+		if !apierrors.IsNotFound(err) {
+			ctrl.LoggerFrom(ctx).Error(err, "reading local workload")
+		}
+	}
+}
+
+// clustersReconciler implements the reconciler for all MultiKueueClusters.
+// Its main task being to maintain the list of remote clients associated to each MultiKueueCluster.
+type clustersReconciler struct {
+	localClient     client.Client
+	configNamespace string
+
+	lock sync.RWMutex
+	// The list of remote remoteClients, indexed by the cluster name.
+	remoteClients map[string]*remoteClient
+	wlUpdateCh    chan event.GenericEvent
+
+	// rootContext - holds the context passed by the controller-runtime on Start.
+	// It's used to create child contexts for MultiKueueClusters client watch routines
+	// that will gracefully end when the controller-manager stops.
+	rootContext context.Context
+
+	// For unit testing only. There is now need of creating fully functional remote clients in the unit tests
+	// and creating valid kubeconfig content is not trivial.
+	// The full client creation and usage is validated in the integration and e2e tests.
+	builderOverride clientWithWatchBuilder
+}
+
+var _ manager.Runnable = (*clustersReconciler)(nil)
+var _ reconcile.Reconciler = (*clustersReconciler)(nil)
+
+func (c *clustersReconciler) Start(ctx context.Context) error {
+	c.rootContext = ctx
+	return nil
+}
+
+func (c *clustersReconciler) stopAndRemoveCluster(clusterName string) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	if rc, found := c.remoteClients[clusterName]; found {
+		rc.watchCancel()
+		delete(c.remoteClients, clusterName)
+	}
+}
+
+func (c *clustersReconciler) setRemoteClientConfig(ctx context.Context, clusterName string, kubeconfig []byte) error {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+
+	client, found := c.remoteClients[clusterName]
+	if !found {
+		client = newRemoteClient(c.localClient, c.wlUpdateCh)
+		if c.builderOverride != nil {
+			client.builderOverride = c.builderOverride
+		}
+		c.remoteClients[clusterName] = client
+	}
+
+	if err := client.setConfig(c.rootContext, kubeconfig); err != nil {
+		ctrl.LoggerFrom(ctx).Error(err, "failed to set kubeConfig in the remote client")
+		delete(c.remoteClients, clusterName)
+		return err
+	}
+	return nil
+}
+
+func (a *clustersReconciler) controllerFor(acName string) (*remoteClient, bool) {
+	a.lock.RLock()
+	defer a.lock.RUnlock()
+
+	c, f := a.remoteClients[acName]
+	return c, f
+}
+
+func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	cluster := &kueuealpha.MultiKueueCluster{}
+	log := ctrl.LoggerFrom(ctx)
+
+	err := c.localClient.Get(ctx, req.NamespacedName, cluster)
+	if client.IgnoreNotFound(err) != nil {
+		return reconcile.Result{}, err
+	}
+
+	log.V(2).Info("Reconcile MultiKueueCluster")
+
+	if err != nil || !cluster.DeletionTimestamp.IsZero() {
+		c.stopAndRemoveCluster(req.Name)
+		return reconcile.Result{}, nil
+	}
+
+	// get the kubeconfig
+	kubeConfig, retry, err := c.getKubeConfig(ctx, &cluster.Spec.KubeConfig)
+	if retry {
+		return reconcile.Result{}, err
+	}
+	if err != nil {
+		log.Error(err, "reading kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "BadConfig", err.Error())
+	}
+
+	if err := c.setRemoteClientConfig(ctx, cluster.Name, kubeConfig); err != nil {
+		log.Error(err, "setting kubeconfig")
+		return reconcile.Result{}, c.updateStatus(ctx, cluster, false, "ClientConnectionFailed", err.Error())
+	}
+	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+}
+
+func (c *clustersReconciler) getKubeConfig(ctx context.Context, ref *kueuealpha.KubeConfig) ([]byte, bool, error) {
+	sec := corev1.Secret{}
+	secretObjKey := types.NamespacedName{
+		Namespace: c.configNamespace,
+		Name:      ref.Location,
+	}
+	err := c.localClient.Get(ctx, secretObjKey, &sec)
+	if err != nil {
+		return nil, !apierrors.IsNotFound(err), err
+	}
+
+	kconfigBytes, found := sec.Data[kueuealpha.MultiKueueConfigSecretKey]
+	if !found {
+		return nil, false, fmt.Errorf("key %q not found in secret %q", kueuealpha.MultiKueueConfigSecretKey, ref.Location)
+	}
+
+	return kconfigBytes, false, nil
+}
+
+func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealpha.MultiKueueCluster, active bool, reason, message string) error {
+	newCondition := metav1.Condition{
+		Type:    kueuealpha.MultiKueueClusterActive,
+		Status:  metav1.ConditionFalse,
+		Reason:  reason,
+		Message: message,
+	}
+	if active {
+		newCondition.Status = metav1.ConditionTrue
+	}
+
+	// if the condition is up to date
+	oldCondition := apimeta.FindStatusCondition(cluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+	if cmpConditionState(oldCondition, &newCondition) {
+		return nil
+	}
+
+	apimeta.SetStatusCondition(&cluster.Status.Conditions, newCondition)
+	return c.localClient.Status().Update(ctx, cluster)
+}
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=multikueueclusters/status,verbs=get;update;patch
+
+func newClustersReconciler(c client.Client, namespace string) *clustersReconciler {
+	return &clustersReconciler{
+		localClient:     c,
+		configNamespace: namespace,
+		remoteClients:   make(map[string]*remoteClient),
+		wlUpdateCh:      make(chan event.GenericEvent, 10),
+	}
+}
+
+func (c *clustersReconciler) setupWithManager(mgr ctrl.Manager) error {
+	err := mgr.Add(c)
+	if err != nil {
+		return err
+	}
+
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&kueuealpha.MultiKueueCluster{}).
+		Watches(&corev1.Secret{}, &secretHandler{client: c.localClient}).
+		Complete(c)
+}
+
+type secretHandler struct {
+	client client.Client
+}
+
+var _ handler.EventHandler = (*secretHandler)(nil)
+
+func (s *secretHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	secret, isSecret := event.Object.(*corev1.Secret)
+	if !isSecret {
+		ctrl.LoggerFrom(ctx).V(5).Info("Failure on create event, not a secret")
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-29T08:11:40Z

/lgtm
/approve
