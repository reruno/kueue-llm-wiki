# PR #1635: Single instance admission check.

**Summary**: Single instance admission check.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1635

**Last updated**: 2024-03-11T14:05:56Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `ac-single-instance` → `main`
- **Created**: 2024-01-23T10:17:40Z
- **Updated**: 2024-03-11T14:05:56Z
- **Merged**: 2024-03-11T14:05:08Z
- **Closed**: 2024-03-11T14:05:08Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 10   **+268 / -20**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

- Add the possibility for an AdmissionCheck controller to mark the AdmissionCheck manage by it for single usage within a ClusterQueue.
- Only allow one `multikueue` managed AdmissionCheck to be added to one ClusterQueue.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-23T10:17:42Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-23T10:17:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f086d3c14cf335cd4424ffeaa3ae38ad50a2ce63 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65eea836a6699f0008522c5c |

### Comment by [@trasc](https://github.com/trasc) — 2024-01-23T10:17:50Z

/test all

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-30T22:51:28Z

The release note should be something that is easy to understand for an end-user.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-31T18:11:38Z

/hold
I don't like how we are using Status as a "declarative" API.

Let's leave this discussion for the next release.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-31T18:15:53Z

> /hold I don't like how we are using Status as a "declarative" API.
> 
> Let's leave this discussion for the next release.

SGTM.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-01T06:14:02Z

> I don't like how we are using Status as a "declarative" API.

Why declarative, the admission check controller can decide the value of  the field by it's  own logic.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-16T13:26:57Z

/unhold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-16T14:30:02Z

I still think we shouldn't have any API for this.
If it's about external admission checks not having to reimplement the logic, maybe we can offer an admission check framework and put it there.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-16T15:27:44Z

> I still think we shouldn't have any API for this.
> If it's about external admission checks not having to reimplement the logic, maybe we can offer an admission check framework and put it there.

I'm out of ideas for this,  we can just close it.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-20T11:01:05Z

> > I still think we shouldn't have any API for this.
> > If it's about external admission checks not having to reimplement the logic, maybe we can offer an admission check framework and put it there.
> 
> I'm out of ideas for this, we can just close it.

In the first approach you had a configurable list of admission checks that can have at most one instance, with MultiKueue admission check being on the list. I think the mechanism itself is ok.

What I didn't like is that we surface this list as configurable, because this is an intrinsic nature of the AdmissionCheck, so it is not clear to me why to confuse users with the option to disable it. 

Sure, we may have other custom admission checks, for which the mechanics is the same, for those we could have "extraAdmissionChecksWithAtMostOneInstance", but not sure we need to be exposing this day one.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-20T11:26:10Z

> > > I still think we shouldn't have any API for this.
> > > If it's about external admission checks not having to reimplement the logic, maybe we can offer an admission check framework and put it there.
> > 
> > 
> > I'm out of ideas for this, we can just close it.
> 
> In the first approach you had a configurable list of admission checks that can have at most one instance, with MultiKueue admission check being on the list. I think the mechanism itself is ok.
> 
> What I didn't like is that we surface this list as configurable, because this is an intrinsic nature of the AdmissionCheck, so it is not clear to me why to confuse users with the option to disable it.
> 
> Sure, we may have other custom admission checks, for which the mechanics is the same, for those we could have "extraAdmissionChecksWithAtMostOneInstance", but not sure we need to be exposing this day one.

I don't think it's a good idea hard-code a list of admission-check controllers in the cache given the plug-in nature of these controllers. (I think we have already discussed this).

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-20T11:36:03Z

> I don't think it's a good idea hard-code a list of admission-check controllers in the cache given the plug-in nature of these controllers. (I think we have already discussed this).

I see, I don't recall this discussion though. Anyway, I will defer the decision for API reviewers / approvers in Kueue. IMO making such decisions on PRs without a KEP is tricky, because it requires reverted order of reviews - first by api approvers, then other reviewers (at least for the field).

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-20T14:18:49Z

> > I don't think it's a good idea hard-code a list of admission-check controllers in the cache given the plug-in nature of these controllers. (I think we have already discussed this).
> 
> I see, I don't recall this discussion though. Anyway, I will defer the decision for API reviewers / approvers in Kueue. IMO making such decisions on PRs without a KEP is tricky, because it requires reverted order of reviews - first by api approvers, then other reviewers (at least for the field).

I fully agree with you. As much as possible, I'd like to have the dedicated KEP, but if @trasc thinks that discussion points are self-evident, we can discuss in the issue. Although, I guess that it would be better to create small KEP or update existing admission-check KEP.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-20T15:14:55Z

To decrease the impact on the API we can use a condition instead of adding a new status field.  It's a bit less clear in my opinion but it could do the job.

@alculquicondor @mimowo @tenzen-y WDYT?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-20T16:51:34Z

I agree with having a hardcoded list.
And we can leave having this configurable based on user feedback.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-20T16:56:06Z

> I agree with having a hardcoded list. And we can leave having this configurable based on user feedback.

The hard-coded list needs to be in cache or passed to cache and needs to contain the name of the multikueue admission check controller. The cache implementation should be agnostic in regards of the existence of a specific admission check controller.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-20T17:19:11Z

Why the cache? isn't the workload reconcilier enough?

Passing the list of admission checks as an argument seems reasonable.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-20T17:34:05Z

> Why the cache? isn't the workload reconcilier enough?
> 
> Passing the list of admission checks as an argument seems reasonable.

Having two multikueue AC set in a ClusterQueue is a ClusterQueue configuration issue.
We manage the active state o the cluster Queues in cache.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-20T18:10:11Z

Oops, sorry, I meant to say the Clusterqueue reconciler. But ok, I can understand that the logic needs to be in the cache.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-11T13:43:49Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-11T13:43:54Z

LGTM label has been added.  <details>Git tree hash: d9a577d92a48bd9811c87078cb46b79aac8b67dc</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-11T13:43:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1635#issuecomment-1988476553" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1635#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-24T09:30:40Z

- **apis/config/v1beta1/configuration_types.go:74** by [@mimowo](https://github.com/mimowo) — 2024-01-24T09:27:44Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  Why do we need this? Can we just verify that there is at most one MultiKueue admission check per ClusterQueue? Alternatively, (stronger) if there is at least one MultiKueue admission check, there are no other admission checks for the ClusterQueue. 
  
  It feels not many users will fully understand how to set this field, and might be afraid it can block some of their use cases.
  
  If we need a new API field I think we should update the KEP (to discuss where the knob should be first, it is completely non-obvious to me the proposed place is the right one). WDYT @mwielgus ?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-25T08:39:18Z

- **apis/config/v1beta1/configuration_types.go:74** by [@trasc](https://github.com/trasc) — 2024-01-25T08:39:18Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  There is no knowledge of MultiKueue and is should not be at ClusterQueue level. Also the need of having at most one admission check managed by a specific admission check controller in a queue is an attribute of that controller. Currently we don't have an API object for the Admission Check Controllers, adding one might be to much for what we need now and will add an extra configuration step for the average kueue user.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-25T09:04:52Z

- **apis/config/v1beta1/configuration_types.go:74** by [@mimowo](https://github.com/mimowo) — 2024-01-25T09:04:52Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  I think if this change impacts the API it should be discussed in KEP. 
  
  The other potential options would be:
  1. keep the mechanism, but hard-code the multi-kueue controller to require single instance (no API change so we could move on without KEP, I don't see much value asking the user to configure it, leaving room for error)
  2. if needed, should this not be the configuration on AdmissionCheck, rather than global?
  
  WDYT @alculquicondor ?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-25T09:25:44Z

- **apis/config/v1beta1/configuration_types.go:74** by [@trasc](https://github.com/trasc) — 2024-01-25T09:25:43Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  > 2\. if needed, should this not be the configuration on AdmissionCheck, rather than global?
  
  It need to have the same value for all the AdmissionChecks managed by a specific controller.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-25T09:39:15Z

- **apis/config/v1beta1/configuration_types.go:74** by [@mimowo](https://github.com/mimowo) — 2024-01-25T09:39:14Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  > It need to have the same value for all the AdmissionChecks managed by a specific controller.
  
  Makes sense, still non-obvious to me we prefer to expose this as a user-facing configuration.
  
  I would like to hear form @alculquicondor or @mwielgus if we proceed with this first since it is not part of the KEP currently.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-25T16:47:02Z

- **apis/config/v1beta1/configuration_types.go:74** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T16:47:02Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  This should not be configurable. This is an inherit limitation of MultiKueue.
  
  Then, it should be the MultiKueue controller that should somehow detect the problem and inform the ClusterQueue that there is a problem.
  
  But let's leave this discussion for the next release.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-30T10:35:10Z

First pass to make progress, but I think this should not be configurable

- **pkg/cache/clusterqueue_test.go:379** by [@mimowo](https://github.com/mimowo) — 2024-01-30T10:28:24Z
```diff
@@ -367,3 +368,163 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		curStatus                 metrics.ClusterQueueStatus
```

  ```suggestion
  		cqStatus                 metrics.ClusterQueueStatus
  ```

- **pkg/cache/clusterqueue_test.go:388** by [@mimowo](https://github.com/mimowo) — 2024-01-30T10:29:40Z
```diff
@@ -367,3 +368,163 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		curStatus                 metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantInactiveReason        string
+	}{
+		{
+			name:       "Pending clusterQueue updated valid AC list",
+			curStatus:  pending,
+			wantStatus: active,
```

  move the `want` params to the bottom

- **pkg/cache/clusterqueue_test.go:403** by [@mimowo](https://github.com/mimowo) — 2024-01-30T10:32:11Z
```diff
@@ -367,3 +368,163 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		curStatus                 metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantInactiveReason        string
+	}{
+		{
+			name:       "Pending clusterQueue updated valid AC list",
+			curStatus:  pending,
+			wantStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantInactiveReason: "Ready",
```

  ```suggestion
  			wantReason: "Ready",
  ```
  It looks weird to say inactiveReason: Ready

- **pkg/cache/clusterqueue_test.go:511** by [@mimowo](https://github.com/mimowo) — 2024-01-30T10:32:57Z
```diff
@@ -367,3 +368,163 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		curStatus                 metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantInactiveReason        string
+	}{
+		{
+			name:       "Pending clusterQueue updated valid AC list",
+			curStatus:  pending,
+			wantStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantInactiveReason: "Ready",
+		},
+		{
+			name:       "Active clusterQueue updated with not found AC",
+			curStatus:  active,
+			wantStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantInactiveReason:        "CheckNotFoundOrInactive",
+		},
+		{
+			name:       "Active clusterQueue updated with inactive AC",
+			curStatus:  active,
+			wantStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     false,
+					Controller: "controller3",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantInactiveReason:        "CheckNotFoundOrInactive",
+		},
+		{
+			name:       "Active clusterQueue updated with duplicate single instance AC Controller",
+			curStatus:  active,
+			wantStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantInactiveReason:        "MultipleSingleInstanceControllerChecks",
+		},
+		{
+			name:       "Terminating clusterQueue updated with valid AC list",
+			curStatus:  terminating,
+			wantStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantInactiveReason: "Terminating",
+		},
+		{
+			name:       "Terminating clusterQueue updated with not found AC",
+			curStatus:  terminating,
+			wantStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			wantInactiveReason: "Terminating",
+		},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(utiltesting.NewFakeClient())
+			cq, err := cache.newClusterQueue(cq)
+			if err != nil {
+				t.Fatalf("failed to new clusterQueue %v", err)
+			}
+
+			cq.Status = tc.curStatus
+			if tc.curStatus == active {
```

  What does it do, why this is needed?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-30T15:08:41Z

- **pkg/cache/clusterqueue_test.go:379** by [@trasc](https://github.com/trasc) — 2024-01-30T14:59:44Z
```diff
@@ -367,3 +368,163 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		curStatus                 metrics.ClusterQueueStatus
```

  done

- **pkg/cache/clusterqueue_test.go:388** by [@trasc](https://github.com/trasc) — 2024-01-30T15:01:12Z
```diff
@@ -367,3 +368,163 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		curStatus                 metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantInactiveReason        string
+	}{
+		{
+			name:       "Pending clusterQueue updated valid AC list",
+			curStatus:  pending,
+			wantStatus: active,
```

  done

- **pkg/cache/clusterqueue_test.go:403** by [@trasc](https://github.com/trasc) — 2024-01-30T15:01:52Z
```diff
@@ -367,3 +368,163 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		curStatus                 metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantInactiveReason        string
+	}{
+		{
+			name:       "Pending clusterQueue updated valid AC list",
+			curStatus:  pending,
+			wantStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantInactiveReason: "Ready",
```

  done

- **pkg/cache/clusterqueue_test.go:511** by [@trasc](https://github.com/trasc) — 2024-01-30T15:05:07Z
```diff
@@ -367,3 +368,163 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		curStatus                 metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantInactiveReason        string
+	}{
+		{
+			name:       "Pending clusterQueue updated valid AC list",
+			curStatus:  pending,
+			wantStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantInactiveReason: "Ready",
+		},
+		{
+			name:       "Active clusterQueue updated with not found AC",
+			curStatus:  active,
+			wantStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantInactiveReason:        "CheckNotFoundOrInactive",
+		},
+		{
+			name:       "Active clusterQueue updated with inactive AC",
+			curStatus:  active,
+			wantStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     false,
+					Controller: "controller3",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantInactiveReason:        "CheckNotFoundOrInactive",
+		},
+		{
+			name:       "Active clusterQueue updated with duplicate single instance AC Controller",
+			curStatus:  active,
+			wantStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantInactiveReason:        "MultipleSingleInstanceControllerChecks",
+		},
+		{
+			name:       "Terminating clusterQueue updated with valid AC list",
+			curStatus:  terminating,
+			wantStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantInactiveReason: "Terminating",
+		},
+		{
+			name:       "Terminating clusterQueue updated with not found AC",
+			curStatus:  terminating,
+			wantStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			wantInactiveReason: "Terminating",
+		},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(utiltesting.NewFakeClient())
+			cq, err := cache.newClusterQueue(cq)
+			if err != nil {
+				t.Fatalf("failed to new clusterQueue %v", err)
+			}
+
+			cq.Status = tc.curStatus
+			if tc.curStatus == active {
```

  It aligns the internal "substates" to the desired initial cq state.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-30T15:09:14Z

- **pkg/cache/clusterqueue_test.go:511** by [@trasc](https://github.com/trasc) — 2024-01-30T15:09:13Z
```diff
@@ -367,3 +368,163 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		curStatus                 metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantInactiveReason        string
+	}{
+		{
+			name:       "Pending clusterQueue updated valid AC list",
+			curStatus:  pending,
+			wantStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantInactiveReason: "Ready",
+		},
+		{
+			name:       "Active clusterQueue updated with not found AC",
+			curStatus:  active,
+			wantStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantInactiveReason:        "CheckNotFoundOrInactive",
+		},
+		{
+			name:       "Active clusterQueue updated with inactive AC",
+			curStatus:  active,
+			wantStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     false,
+					Controller: "controller3",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantInactiveReason:        "CheckNotFoundOrInactive",
+		},
+		{
+			name:       "Active clusterQueue updated with duplicate single instance AC Controller",
+			curStatus:  active,
+			wantStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantInactiveReason:        "MultipleSingleInstanceControllerChecks",
+		},
+		{
+			name:       "Terminating clusterQueue updated with valid AC list",
+			curStatus:  terminating,
+			wantStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantInactiveReason: "Terminating",
+		},
+		{
+			name:       "Terminating clusterQueue updated with not found AC",
+			curStatus:  terminating,
+			wantStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			wantInactiveReason: "Terminating",
+		},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(utiltesting.NewFakeClient())
+			cq, err := cache.newClusterQueue(cq)
+			if err != nil {
+				t.Fatalf("failed to new clusterQueue %v", err)
+			}
+
+			cq.Status = tc.curStatus
+			if tc.curStatus == active {
```

  comment added

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-30T15:32:57Z

Minor test question / ask. The code LGTM.

However, I still think this could be simpler for us (in code), and to users if we have just "MultipleMultiKueueAdmissionChecks" as the reason for being inactive, and not make this thing configurable, because as pointed out, this is MultiKueue specific. I think seeing "MultipleSingleInstanceControllerChecks" would be unclear to users. 

/assign @tenzen-y @alculquicondor

- **pkg/cache/clusterqueue_test.go:607** by [@mimowo](https://github.com/mimowo) — 2024-01-30T15:27:19Z
```diff
@@ -367,3 +368,165 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		cqStatus                  metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantReason                string
+	}{
+		{
+			name:     "Pending clusterQueue updated valid AC list",
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: active,
+			wantReason: "Ready",
+		},
+		{
+			name:     "Active clusterQueue updated with not found AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with inactive AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     false,
+					Controller: "controller3",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with duplicate single instance AC Controller",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "MultipleSingleInstanceControllerChecks",
+		},
+		{
+			name:     "Terminating clusterQueue updated with valid AC list",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+		{
+			name:     "Terminating clusterQueue updated with not found AC",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(utiltesting.NewFakeClient())
+			cq, err := cache.newClusterQueue(cq)
+			if err != nil {
+				t.Fatalf("failed to new clusterQueue %v", err)
+			}
+
+			cq.Status = tc.cqStatus
+
+			// Align the admission check related internals to the desired Status.
+			if tc.cqStatus == active {
+				cq.hasMultipleSingleInstanceControllersChecks = false
+				cq.hasMissingOrInactiveAdmissionChecks = false
+			} else {
+				cq.hasMultipleSingleInstanceControllersChecks = true
```

  I'm still puzzled by this, why status != active implies `hasMultipleSingleInstanceControllersChecks: true`, could this be inactive for other reasons, right? This way we cannot test what happens if inactive for reason A, but both `has` are true (or one of them).

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-30T16:02:13Z

- **pkg/cache/clusterqueue_test.go:607** by [@trasc](https://github.com/trasc) — 2024-01-30T16:02:13Z
```diff
@@ -367,3 +368,165 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		cqStatus                  metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantReason                string
+	}{
+		{
+			name:     "Pending clusterQueue updated valid AC list",
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: active,
+			wantReason: "Ready",
+		},
+		{
+			name:     "Active clusterQueue updated with not found AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with inactive AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     false,
+					Controller: "controller3",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with duplicate single instance AC Controller",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "MultipleSingleInstanceControllerChecks",
+		},
+		{
+			name:     "Terminating clusterQueue updated with valid AC list",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+		{
+			name:     "Terminating clusterQueue updated with not found AC",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(utiltesting.NewFakeClient())
+			cq, err := cache.newClusterQueue(cq)
+			if err != nil {
+				t.Fatalf("failed to new clusterQueue %v", err)
+			}
+
+			cq.Status = tc.cqStatus
+
+			// Align the admission check related internals to the desired Status.
+			if tc.cqStatus == active {
+				cq.hasMultipleSingleInstanceControllersChecks = false
+				cq.hasMissingOrInactiveAdmissionChecks = false
+			} else {
+				cq.hasMultipleSingleInstanceControllersChecks = true
```

  both of them are reevaluated during the call to cq.updateWithAdmissionChecks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-30T16:17:05Z

- **pkg/cache/clusterqueue_test.go:607** by [@mimowo](https://github.com/mimowo) — 2024-01-30T16:17:05Z
```diff
@@ -367,3 +368,165 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		cqStatus                  metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantReason                string
+	}{
+		{
+			name:     "Pending clusterQueue updated valid AC list",
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: active,
+			wantReason: "Ready",
+		},
+		{
+			name:     "Active clusterQueue updated with not found AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with inactive AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     false,
+					Controller: "controller3",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with duplicate single instance AC Controller",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "MultipleSingleInstanceControllerChecks",
+		},
+		{
+			name:     "Terminating clusterQueue updated with valid AC list",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+		{
+			name:     "Terminating clusterQueue updated with not found AC",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(utiltesting.NewFakeClient())
+			cq, err := cache.newClusterQueue(cq)
+			if err != nil {
+				t.Fatalf("failed to new clusterQueue %v", err)
+			}
+
+			cq.Status = tc.cqStatus
+
+			// Align the admission check related internals to the desired Status.
+			if tc.cqStatus == active {
+				cq.hasMultipleSingleInstanceControllersChecks = false
+				cq.hasMissingOrInactiveAdmissionChecks = false
+			} else {
+				cq.hasMultipleSingleInstanceControllersChecks = true
```

  I know, and inside this function we check the previous values, so I thought it makes sense to have the flexibility of setting the old values as we want. Let me ask another question, do the current test cases cover the function in 100%?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-30T22:53:15Z

- **apis/config/v1beta1/configuration_types.go:74** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-30T22:53:14Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  To clarify, what I mean by `But let's leave this discussion for the next release` is that we can leave this PR for the next release.
  
  If you want to merge it in this release, then you should address the fact that this should NOT be configurable.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-30T22:53:29Z

- **apis/config/v1beta1/configuration_types.go:74** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-30T22:53:29Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  /hold

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T08:46:06Z

- **pkg/cache/clusterqueue_test.go:607** by [@trasc](https://github.com/trasc) — 2024-01-31T08:46:06Z
```diff
@@ -367,3 +368,165 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		cqStatus                  metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantReason                string
+	}{
+		{
+			name:     "Pending clusterQueue updated valid AC list",
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: active,
+			wantReason: "Ready",
+		},
+		{
+			name:     "Active clusterQueue updated with not found AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with inactive AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     false,
+					Controller: "controller3",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with duplicate single instance AC Controller",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "MultipleSingleInstanceControllerChecks",
+		},
+		{
+			name:     "Terminating clusterQueue updated with valid AC list",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+		{
+			name:     "Terminating clusterQueue updated with not found AC",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(utiltesting.NewFakeClient())
+			cq, err := cache.newClusterQueue(cq)
+			if err != nil {
+				t.Fatalf("failed to new clusterQueue %v", err)
+			}
+
+			cq.Status = tc.cqStatus
+
+			// Align the admission check related internals to the desired Status.
+			if tc.cqStatus == active {
+				cq.hasMultipleSingleInstanceControllersChecks = false
+				cq.hasMissingOrInactiveAdmissionChecks = false
+			} else {
+				cq.hasMultipleSingleInstanceControllersChecks = true
```

  It is covered 100%.
  Having them separated can only give the option to skip `c.updateQueueStatus()`  in the function, case in which   `updateWithAdmissionChecks` has no effect on the cq.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T09:02:51Z

- **apis/config/v1beta1/configuration_types.go:74** by [@trasc](https://github.com/trasc) — 2024-01-31T09:02:51Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  Let's control it from the admission check controller, via a new AC Status field.
  /unhold

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T10:28:55Z

- **pkg/cache/clusterqueue_test.go:607** by [@mimowo](https://github.com/mimowo) — 2024-01-31T10:28:55Z
```diff
@@ -367,3 +368,165 @@ func TestClusterQueueUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
+	cq := utiltesting.MakeClusterQueue("cq").
+		AdmissionChecks("check1", "check2", "check3").
+		Obj()
+
+	testcases := []struct {
+		name                      string
+		cqStatus                  metrics.ClusterQueueStatus
+		admissionChecks           map[string]AdmissionCheck
+		singleInstanceControllers []string
+		wantStatus                metrics.ClusterQueueStatus
+		wantReason                string
+	}{
+		{
+			name:     "Pending clusterQueue updated valid AC list",
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: active,
+			wantReason: "Ready",
+		},
+		{
+			name:     "Active clusterQueue updated with not found AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with inactive AC",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     false,
+					Controller: "controller3",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "CheckNotFoundOrInactive",
+		},
+		{
+			name:     "Active clusterQueue updated with duplicate single instance AC Controller",
+			cqStatus: active,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			singleInstanceControllers: []string{"controller1", "controller2"},
+			wantStatus:                pending,
+			wantReason:                "MultipleSingleInstanceControllerChecks",
+		},
+		{
+			name:     "Terminating clusterQueue updated with valid AC list",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:     true,
+					Controller: "controller3",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+		{
+			name:     "Terminating clusterQueue updated with not found AC",
+			cqStatus: terminating,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:     true,
+					Controller: "controller1",
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+			},
+			wantStatus: terminating,
+			wantReason: "Terminating",
+		},
+	}
+
+	for _, tc := range testcases {
+		t.Run(tc.name, func(t *testing.T) {
+			cache := New(utiltesting.NewFakeClient())
+			cq, err := cache.newClusterQueue(cq)
+			if err != nil {
+				t.Fatalf("failed to new clusterQueue %v", err)
+			}
+
+			cq.Status = tc.cqStatus
+
+			// Align the admission check related internals to the desired Status.
+			if tc.cqStatus == active {
+				cq.hasMultipleSingleInstanceControllersChecks = false
+				cq.hasMissingOrInactiveAdmissionChecks = false
+			} else {
+				cq.hasMultipleSingleInstanceControllersChecks = true
```

  I see, sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T10:29:52Z

- **apis/config/v1beta1/configuration_types.go:74** by [@mimowo](https://github.com/mimowo) — 2024-01-31T10:29:52Z
```diff
@@ -67,6 +67,11 @@ type Configuration struct {
 	// QueueVisibility is configuration to expose the information about the top
 	// pending workloads.
 	QueueVisibility *QueueVisibility `json:"queueVisibility,omitempty"`
+
+	// SingleInstanceAdmissionCheckControllers contains a list of AdmissionCheck controller names for which
+	// at most one AdmissionCheck managed by it can be configured in a ClusterQueue.
+	// A ClusterQueue that violates this rule is marked as Inactive.
+	SingleInstanceAdmissionCheckControllers []string `json:"singleInstanceAdmissionCheckControllers,omitempty"`
```

  Thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T10:33:50Z

LGTM with the caveat that it is still surfaced at the API level, in status. I think we could hardcode this entirely, but don't want to block it.

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:71** by [@mimowo](https://github.com/mimowo) — 2024-01-31T10:33:30Z
```diff
@@ -65,6 +66,15 @@ func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
+	if !ptr.Deref(ac.Status.SingleInstannceInClusterQueue, false) {
+		ac.Status.SingleInstannceInClusterQueue = ptr.To(true)
+		err := a.client.Status().Update(ctx, ac)
```

  I prefer to do not update here, to have the request. It will be updated lower down the function. Only set to true if nil here,

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T10:34:53Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:71** by [@trasc](https://github.com/trasc) — 2024-01-31T10:34:53Z
```diff
@@ -65,6 +66,15 @@ func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
+	if !ptr.Deref(ac.Status.SingleInstannceInClusterQueue, false) {
+		ac.Status.SingleInstannceInClusterQueue = ptr.To(true)
+		err := a.client.Status().Update(ctx, ac)
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-31T17:46:41Z

- **apis/kueue/v1beta1/admissioncheck_types.go:90** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-31T17:01:35Z
```diff
@@ -86,6 +86,13 @@ type AdmissionCheckStatus struct {
 	// +patchStrategy=merge
 	// +patchMergeKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+	// singleInstannceInClusterQueue indicates if the AdmissionCheck should be the only one managed by
```

  ```suggestion
  	// singleInstanceInClusterQueue indicates if the AdmissionCheck should be the only one managed by
  ```

- **apis/kueue/v1beta1/admissioncheck_types.go:95** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-31T17:01:50Z
```diff
@@ -86,6 +86,13 @@ type AdmissionCheckStatus struct {
 	// +patchStrategy=merge
 	// +patchMergeKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+	// singleInstannceInClusterQueue indicates if the AdmissionCheck should be the only one managed by
+	// the same Controller in a ClusterQueue.
+	// Having multiple AdmissionChecks managed by the same Controller of which at least one has this
+	// set to true will cause the ClusterQueue to be marked as Inactive.
+	// +optional
+	SingleInstannceInClusterQueue *bool `json:"singleInstannceInClusterQueue,omitempty"`
```

  ```suggestion
  	SingleInstanceInClusterQueue *bool `json:"singleInstanceInClusterQueue,omitempty"`
  ```

- **apis/kueue/v1beta1/admissioncheck_types.go:95** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-31T17:19:48Z
```diff
@@ -86,6 +86,13 @@ type AdmissionCheckStatus struct {
 	// +patchStrategy=merge
 	// +patchMergeKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+	// singleInstannceInClusterQueue indicates if the AdmissionCheck should be the only one managed by
+	// the same Controller in a ClusterQueue.
+	// Having multiple AdmissionChecks managed by the same Controller of which at least one has this
+	// set to true will cause the ClusterQueue to be marked as Inactive.
+	// +optional
+	SingleInstannceInClusterQueue *bool `json:"singleInstannceInClusterQueue,omitempty"`
```

  Here, what's meant by `instance`? The `instance` means admissionCheck?

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:120** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-31T17:34:15Z
```diff
@@ -115,7 +116,8 @@ func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 	}
 
 	oldCondition := apimeta.FindStatusCondition(ac.Status.Conditions, kueue.AdmissionCheckActive)
-	if !cmpConditionState(oldCondition, &newCondition) {
+	if !ptr.Deref(ac.Status.SingleInstannceInClusterQueue, false) || !cmpConditionState(oldCondition, &newCondition) {
+		ac.Status.SingleInstannceInClusterQueue = ptr.To(true)
```

  Can we enforce `singleInstanceInClusterQueue=true` in advance like webhooks if the admissionChecks is for multikueue?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T06:28:29Z

- **apis/kueue/v1beta1/admissioncheck_types.go:90** by [@trasc](https://github.com/trasc) — 2024-02-01T06:14:24Z
```diff
@@ -86,6 +86,13 @@ type AdmissionCheckStatus struct {
 	// +patchStrategy=merge
 	// +patchMergeKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+	// singleInstannceInClusterQueue indicates if the AdmissionCheck should be the only one managed by
```

  done

- **apis/kueue/v1beta1/admissioncheck_types.go:95** by [@trasc](https://github.com/trasc) — 2024-02-01T06:14:37Z
```diff
@@ -86,6 +86,13 @@ type AdmissionCheckStatus struct {
 	// +patchStrategy=merge
 	// +patchMergeKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+	// singleInstannceInClusterQueue indicates if the AdmissionCheck should be the only one managed by
+	// the same Controller in a ClusterQueue.
+	// Having multiple AdmissionChecks managed by the same Controller of which at least one has this
+	// set to true will cause the ClusterQueue to be marked as Inactive.
+	// +optional
+	SingleInstannceInClusterQueue *bool `json:"singleInstannceInClusterQueue,omitempty"`
```

  done

- **apis/kueue/v1beta1/admissioncheck_types.go:95** by [@trasc](https://github.com/trasc) — 2024-02-01T06:18:08Z
```diff
@@ -86,6 +86,13 @@ type AdmissionCheckStatus struct {
 	// +patchStrategy=merge
 	// +patchMergeKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+	// singleInstannceInClusterQueue indicates if the AdmissionCheck should be the only one managed by
+	// the same Controller in a ClusterQueue.
+	// Having multiple AdmissionChecks managed by the same Controller of which at least one has this
+	// set to true will cause the ClusterQueue to be marked as Inactive.
+	// +optional
+	SingleInstannceInClusterQueue *bool `json:"singleInstannceInClusterQueue,omitempty"`
```

  If admission check `ac1`  and `ac2` are borhn managed by the same controller `c1` and at least one of them (`ac1`  ,`ac2`) hase `Status.SingleInstanceInClusterQueue=true` adding them both to the list of admission checks for a ClusterQueue `cq1` will make `q1` inactive.

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:120** by [@trasc](https://github.com/trasc) — 2024-02-01T06:23:52Z
```diff
@@ -115,7 +116,8 @@ func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 	}
 
 	oldCondition := apimeta.FindStatusCondition(ac.Status.Conditions, kueue.AdmissionCheckActive)
-	if !cmpConditionState(oldCondition, &newCondition) {
+	if !ptr.Deref(ac.Status.SingleInstannceInClusterQueue, false) || !cmpConditionState(oldCondition, &newCondition) {
+		ac.Status.SingleInstannceInClusterQueue = ptr.To(true)
```

  No, the decision should be specific to multikueue, the initial implementation was based on a config list of controller names that need this but: https://github.com/kubernetes-sigs/kueue/pull/1635#discussion_r1466659290

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-08T18:58:41Z

ProvisioningRequest should also be unique, although that can be done in a follow up

/approve

- **apis/kueue/v1beta1/admissioncheck_types.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T18:38:21Z
```diff
@@ -90,6 +90,12 @@ const (
 	// AdmissionCheckActive indicates that the controller of the admission check is
 	// ready to evaluate the checks states
 	AdmissionCheckActive string = "Active"
+
+	// AdmissionChecksSingleInstanceInClusterQueue indicates if the AdmissionCheck should be the only
+	// one managed by the same Controller in a ClusterQueue.
```

  ```suggestion
  	// one managed by the same controller (as determined by the controllerName field) in a ClusterQueue.
  ```

- **apis/kueue/v1beta1/admissioncheck_types.go:96** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T18:38:54Z
```diff
@@ -90,6 +90,12 @@ const (
 	// AdmissionCheckActive indicates that the controller of the admission check is
 	// ready to evaluate the checks states
 	AdmissionCheckActive string = "Active"
+
+	// AdmissionChecksSingleInstanceInClusterQueue indicates if the AdmissionCheck should be the only
+	// one managed by the same Controller in a ClusterQueue.
+	// Having multiple AdmissionChecks managed by the same Controller of which at least one has this
```

  ```suggestion
  	// Having multiple AdmissionChecks managed by the same controller where at least one has this condition
  ```

- **pkg/cache/clusterqueue.go:376** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T18:52:03Z
```diff
@@ -368,15 +373,41 @@ func (c *ClusterQueue) updateLabelKeys(flavors map[kueue.ResourceFlavorReference
 // updateWithAdmissionChecks updates a ClusterQueue based on the passed AdmissionChecks set.
 func (c *ClusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionCheck) {
 	hasMissing := false
+	controllersChecks := make(map[string][]string, len(c.AdmissionChecks))
```

  ```suggestion
  	checksPerController := make(map[string]int, len(c.AdmissionChecks))
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-11T06:24:01Z

- **apis/kueue/v1beta1/admissioncheck_types.go:95** by [@trasc](https://github.com/trasc) — 2024-03-11T06:24:01Z
```diff
@@ -90,6 +90,12 @@ const (
 	// AdmissionCheckActive indicates that the controller of the admission check is
 	// ready to evaluate the checks states
 	AdmissionCheckActive string = "Active"
+
+	// AdmissionChecksSingleInstanceInClusterQueue indicates if the AdmissionCheck should be the only
+	// one managed by the same Controller in a ClusterQueue.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-11T06:24:08Z

- **apis/kueue/v1beta1/admissioncheck_types.go:96** by [@trasc](https://github.com/trasc) — 2024-03-11T06:24:08Z
```diff
@@ -90,6 +90,12 @@ const (
 	// AdmissionCheckActive indicates that the controller of the admission check is
 	// ready to evaluate the checks states
 	AdmissionCheckActive string = "Active"
+
+	// AdmissionChecksSingleInstanceInClusterQueue indicates if the AdmissionCheck should be the only
+	// one managed by the same Controller in a ClusterQueue.
+	// Having multiple AdmissionChecks managed by the same Controller of which at least one has this
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-11T06:45:27Z

- **pkg/cache/clusterqueue.go:376** by [@trasc](https://github.com/trasc) — 2024-03-11T06:27:07Z
```diff
@@ -368,15 +373,41 @@ func (c *ClusterQueue) updateLabelKeys(flavors map[kueue.ResourceFlavorReference
 // updateWithAdmissionChecks updates a ClusterQueue based on the passed AdmissionChecks set.
 func (c *ClusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionCheck) {
 	hasMissing := false
+	controllersChecks := make(map[string][]string, len(c.AdmissionChecks))
```

  done
