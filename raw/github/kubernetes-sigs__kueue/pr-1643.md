# PR #1643: [multikueue] Add garbage collector.

**Summary**: [multikueue] Add garbage collector.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1643

**Last updated**: 2024-02-13T19:40:12Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `multikueue-gc` → `main`
- **Created**: 2024-01-25T13:03:57Z
- **Updated**: 2024-02-13T19:40:12Z
- **Merged**: 2024-02-05T18:37:55Z
- **Closed**: 2024-02-05T18:37:55Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 18   **+607 / -27**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add MultiKueue garbage collection.
A component that periodically, based on `kueueManagerConfig.MultiKueue.GCTimeout` , lists the remote objects created on the remote `MultiKueueClusters` ,  having the label `kueue.x-k8s.io/multikueue-origin` set to `config.multiKueue,origin`  , and delete those who no longer have a local counterpart.


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
MultiKueue: Add garbage collection of deleted Workloads.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-01-25T13:03:59Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-25T13:04:02Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ebe1e328048158d3b59e8803eeaaba60f8190d4f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65c1159dacc5850008f19fbc |

### Comment by [@trasc](https://github.com/trasc) — 2024-01-25T16:44:32Z

/test all

### Comment by [@trasc](https://github.com/trasc) — 2024-01-26T12:11:12Z

/hold
for #1631

### Comment by [@trasc](https://github.com/trasc) — 2024-01-29T08:52:59Z

/unhold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-31T18:15:58Z

The API comments and PR description don't explain what we are garbage collecting exactly.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-01T07:18:08Z

> The API comments and PR description don't explain what we are garbage collecting exactly.

done

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-02T12:04:06Z

LGTM label has been added.  <details>Git tree hash: 254effaa8d7d6cc18541fa549154eb106d19e808</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-02T12:56:57Z

BTW, I don't think this warrants a separate release note, because this is a non-released feature yet. I think one generic release note saying "Support for MultiKueue at Alpha level" (from one PR) would be enough for users, but this is a nit.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T16:51:21Z

/approve
/hold
for comments

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-05T16:51:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1643#issuecomment-1927461755" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1643#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T18:13:32Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-05T18:13:38Z

LGTM label has been added.  <details>Git tree hash: 6121d5ba125ce1f6e953d6aaf37b35fe10980ea2</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T18:31:13Z

/hold cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T20:52:29Z

/kind api-change

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T19:40:10Z

/release-note-edit
```release-note
MultiKueue: Add garbage collection of deleted Workloads.
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-25T13:53:03Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:63** by [@mimowo](https://github.com/mimowo) — 2024-01-25T13:47:13Z
```diff
@@ -53,6 +53,14 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	GarbageCollectionKey string `json:"garbageCollectionKey,omitempty"`
```

  Does the label key need to be configurable? Does it bring any more value that just true / false? I see the value is taken from the controllerName, but this is hard-coded for multikueue.

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@mimowo](https://github.com/mimowo) — 2024-01-25T13:50:46Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueGCLabelKey = "kueue.x-k8s.io/multikueue-gc"
```

  I think there is no reason to couple the label name to GC, is there? It might be useful in other contexts, say when debugging to answer questions like: "what is the number of workloads managed by multikueue", "are there any workloads not managed by multikueue". Such questions aren't directly related to GC.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-25T16:36:26Z

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@trasc](https://github.com/trasc) — 2024-01-25T16:36:25Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueGCLabelKey = "kueue.x-k8s.io/multikueue-gc"
```

  I think we should try to not delete workloads that ware not created by us ("us" being the instance of kueue running the garbage collection).

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-25T16:39:11Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:63** by [@trasc](https://github.com/trasc) — 2024-01-25T16:39:11Z
```diff
@@ -53,6 +53,14 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	GarbageCollectionKey string `json:"garbageCollectionKey,omitempty"`
```

  It could be useful.
  
  An alternative to the new API field could be a kueue config key. (We'll need a multikueue section to make the interval on which the GC runs configurable anyway).
  
  @alculquicondor  WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-25T16:43:39Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-25T16:43:39Z
```diff
@@ -53,6 +53,14 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	GarbageCollectionKey string `json:"garbageCollectionKey,omitempty"`
```

  I think it could be useful, but I wouldn't make it just about GC. It can be useful more generally, just to identify the jobs in the worker clusters.
  
  I'm fine starting with no configuration too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-26T10:22:24Z

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@mimowo](https://github.com/mimowo) — 2024-01-26T10:22:24Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueGCLabelKey = "kueue.x-k8s.io/multikueue-gc"
```

  Sure, I'm just suggesting the name of the label should be `kueue.x-k8s.io/multikueue` (or such, but not coupled to GC)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-26T12:14:12Z

- **apis/kueue/v1alpha1/multikueueconfig_types.go:63** by [@trasc](https://github.com/trasc) — 2024-01-26T12:14:12Z
```diff
@@ -53,6 +53,14 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	GarbageCollectionKey string `json:"garbageCollectionKey,omitempty"`
```

  From multikueue internals perspective it's configurable and there is no point in hard-coding  the value now.
  
  The question in my opinion is if we want to configure it at a MultiKueueCluster level or at a kueue config level (like the GCTimeout).

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-26T12:14:31Z

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@trasc](https://github.com/trasc) — 2024-01-26T12:14:31Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueGCLabelKey = "kueue.x-k8s.io/multikueue-gc"
```

  changed to ` kueue.x-k8s.io/multikueue-origin`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-29T09:24:08Z

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-01-29T09:22:23Z
```diff
@@ -199,6 +202,13 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  Typically I prefer not to expose configuration options we are unsure of being needed. One reason is making things configurable we risk that users without good understanding would tweak this option (by disabling, setting too often or too rare) running into issues, and require OnCall help. Another is that maybe in the future we prefer to have an adaptative interval rather than a constant one. This is still alpha, so I think it is ok to wait for feedback from Alpha testers and see if they need it configurable.
  
  This is analogous to the core k8s [gcCheckPeriod](https://github.com/kubernetes/kubernetes/blob/eb0fcf9e216f83e200e1c04e24d0484394da6b86/pkg/controller/podgc/gc_controller.go#L50C2-L50C15), which is not exposed to users. 
  
  Still. I see some benefits, so I'm fine with this configuration option if @alculquicondor or @tenzen-y give thumbs up for the option.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-29T12:59:38Z

- **apis/kueue/v1alpha1/multikueue_types.go:65** by [@mimowo](https://github.com/mimowo) — 2024-01-29T12:59:34Z
```diff
@@ -50,6 +50,14 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	Origin string `json:"origin,omitempty"`
```

  Why would we make this configurable? Can we just hardcode "multikueue" or "true"?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-30T11:43:04Z

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-01-30T11:43:03Z
```diff
@@ -199,6 +202,13 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  In case this is exposed just for testing we can have two strategies, as in the code k8s using `NewPodGCInternal` to override the defaults, or as we do for example with there parameters: https://github.com/kubernetes-sigs/kueue/blob/b7fc93f5ba42ebbcd54345702be24c3f76d7ea26/pkg/controller/admissionchecks/provisioning/controller.go#L66-L69.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T09:05:55Z

- **apis/config/v1beta1/configuration_types.go:209** by [@trasc](https://github.com/trasc) — 2024-01-31T09:05:55Z
```diff
@@ -199,6 +202,13 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  In my experience the use of global state is usually a very bad idea.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T11:04:33Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:158** by [@mimowo](https://github.com/mimowo) — 2024-01-30T11:48:41Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
```

  use continue in that case to safe indentation level

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:163** by [@mimowo](https://github.com/mimowo) — 2024-01-30T11:49:47Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
+			// if the remote wl has a controller, delete that as well
+			if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+				adapterKey := schema.FromAPIVersionAndKind(controller.APIVersion, controller.Kind).String()
+				if adapter, found := adapters[adapterKey]; !found {
+					log.V(2).Info("No adapter found", "adapterKey", adapterKey, "controller", controller.Name)
```

  Please include in the log the workload which we are currently inspecting

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:159** by [@mimowo](https://github.com/mimowo) — 2024-01-30T11:54:46Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
+			// if the remote wl has a controller, delete that as well
```

  ```suggestion
  			// if the remote wl has a controller (owning Job), delete the Job to trigger deletion of the Job and the workload
  ```

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:160** by [@mimowo](https://github.com/mimowo) — 2024-01-30T11:55:11Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
+			// if the remote wl has a controller, delete that as well
+			if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
```

  What if a workload does not have a controller, isn't this possible?

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:167** by [@mimowo](https://github.com/mimowo) — 2024-01-30T11:55:52Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
+			// if the remote wl has a controller, delete that as well
+			if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+				adapterKey := schema.FromAPIVersionAndKind(controller.APIVersion, controller.Kind).String()
+				if adapter, found := adapters[adapterKey]; !found {
+					log.V(2).Info("No adapter found", "adapterKey", adapterKey, "controller", controller.Name)
+				} else {
+					err := adapter.DeleteRemoteObject(ctx, rc.client, types.NamespacedName{Name: controller.Name, Namespace: remoteWl.Namespace})
+					if client.IgnoreNotFound(err) != nil {
+						log.V(2).Error(err, "Deleting remote workload's owner", "remoteWl", klog.KObj(&remoteWl))
```

  Please remove in the log the name and namespace of the Job that is actually being deleted.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:140** by [@mimowo](https://github.com/mimowo) — 2024-01-30T11:58:52Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
```

  How do we actually tell a workload is garbage. Please describe this in the function comment and above the actual statement that checks this.

- **apis/kueue/v1alpha1/multikueue_types.go:65** by [@mimowo](https://github.com/mimowo) — 2024-01-31T11:04:30Z
```diff
@@ -50,6 +50,14 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	Origin string `json:"origin,omitempty"`
```

  I see, we had an in-private discussion about this. This is introduced to allow multiple MultiKueue to reuse the same worker clusters by different instance of MultiKueue. Having configurable origin allows for that, however, with the default of `multikueue` this requires anyway a change in the config. I'm ok with the change, but also think it might be better to just say straight that this worker sharing is not supported. WDYT @alculquicondor @mwielgus ?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T12:23:06Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:140** by [@trasc](https://github.com/trasc) — 2024-01-31T11:29:29Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:158** by [@trasc](https://github.com/trasc) — 2024-01-31T11:29:36Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:160** by [@trasc](https://github.com/trasc) — 2024-01-31T11:31:47Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
+			// if the remote wl has a controller, delete that as well
+			if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
```

  (reworked), it is  and we only delete the workload

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:163** by [@trasc](https://github.com/trasc) — 2024-01-31T11:33:53Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
+			// if the remote wl has a controller, delete that as well
+			if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+				adapterKey := schema.FromAPIVersionAndKind(controller.APIVersion, controller.Kind).String()
+				if adapter, found := adapters[adapterKey]; !found {
+					log.V(2).Info("No adapter found", "adapterKey", adapterKey, "controller", controller.Name)
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:167** by [@trasc](https://github.com/trasc) — 2024-01-31T11:37:57Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
+			// if the remote wl has a controller, delete that as well
+			if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+				adapterKey := schema.FromAPIVersionAndKind(controller.APIVersion, controller.Kind).String()
+				if adapter, found := adapters[adapterKey]; !found {
+					log.V(2).Info("No adapter found", "adapterKey", adapterKey, "controller", controller.Name)
+				} else {
+					err := adapter.DeleteRemoteObject(ctx, rc.client, types.NamespacedName{Name: controller.Name, Namespace: remoteWl.Namespace})
+					if client.IgnoreNotFound(err) != nil {
+						log.V(2).Error(err, "Deleting remote workload's owner", "remoteWl", klog.KObj(&remoteWl))
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:159** by [@trasc](https://github.com/trasc) — 2024-01-31T11:40:37Z
```diff
@@ -129,6 +137,44 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			log.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		// if it's not found, or pending deletion
+		if err != nil || !localWl.DeletionTimestamp.IsZero() {
+			// if the remote wl has a controller, delete that as well
```

  updated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T12:29:34Z

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-01-31T12:29:34Z
```diff
@@ -199,6 +202,13 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  yes, there is a trade off indeed, I'm ok keeping it configurable

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T12:32:05Z

- **apis/kueue/v1alpha1/multikueue_types.go:65** by [@mimowo](https://github.com/mimowo) — 2024-01-31T12:32:04Z
```diff
@@ -50,6 +50,14 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	Origin string `json:"origin,omitempty"`
```

  I have discussed this with @mwielgus  in-private. We are ok keeping this configurable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T08:58:50Z

- **apis/kueue/v1alpha1/multikueue_types.go:65** by [@mimowo](https://github.com/mimowo) — 2024-02-01T08:58:46Z
```diff
@@ -53,6 +53,16 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	// This is used by the garbage collector to identify remote objects that ware created via
+	// this cluster and delete them if their local counterpart no longer exists.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	Origin string `json:"origin,omitempty"`
```

  I believe however  this should be configurable at the global Kueue level. As discussed here https://github.com/kubernetes-sigs/kueue/pull/1643/files#r1472664011 - the only purpose of making this configurable is to separate workloads and jobs if multiple MultiKueue instances share the same worker. This can be achieved at the global MultiKueue configuration level. No need to set it per cluster. Setting this per cluster would add configuration churn on admins and more room for error.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T10:27:23Z

- **apis/kueue/v1alpha1/multikueue_types.go:65** by [@trasc](https://github.com/trasc) — 2024-02-01T10:27:23Z
```diff
@@ -53,6 +53,16 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	// This is used by the garbage collector to identify remote objects that ware created via
+	// this cluster and delete them if their local counterpart no longer exists.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	Origin string `json:"origin,omitempty"`
```

  I'm fine with that, @alculquicondor WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-01T19:33:02Z

- **apis/kueue/v1alpha1/multikueue_types.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T19:33:02Z
```diff
@@ -53,6 +53,16 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	// This is used by the garbage collector to identify remote objects that ware created via
+	// this cluster and delete them if their local counterpart no longer exists.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	Origin string `json:"origin,omitempty"`
```

  global sounds more appropriate

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T07:33:28Z

- **apis/kueue/v1alpha1/multikueue_types.go:65** by [@trasc](https://github.com/trasc) — 2024-02-02T07:33:28Z
```diff
@@ -53,6 +53,16 @@ type KubeConfig struct {
 type MultiKueueClusterSpec struct {
 	// Information how to connect to the cluster.
 	KubeConfig KubeConfig `json:"kubeConfig"`
+
+	// A label value used to track the creator of workloads in the worker cluster.
+	// This is used by the garbage collector to identify remote objects that ware created via
+	// this cluster and delete them if their local counterpart no longer exists.
+	//
+	// +kubebuilder:validation:Optional
+	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="Value is immutable"
+	// +kubebuilder:validation:MaxLength=63
+	// +kubebuilder:default=multikueue
+	Origin string `json:"origin,omitempty"`
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T09:33:57Z

Almost LGTM

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:16:04Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  Why a pointer? I see two alternatives:
  1. make it value (no ptr), interpret 0 as disabled
  2. make it ptr, validate > 0, interpret null as disabled
  
  Both are fine to me. I find (2.) more readable, but this only slight preference, because it means extra validation.

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:21:24Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueOriginLabelKey = "kueue.x-k8s.io/multikueue-origin"
```

  Do we have some dedicated package or file for consts? It would be nice to group it together with other labels / annotations, for code organization purposes.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:159** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:25:11Z
```diff
@@ -130,6 +134,51 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local corespondent (missing or awaiting deletion). If the remote workload is
+// is owned by a job,also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The remote workload is still relevant.
```

  ```suggestion
  			// The local workload exists and isn't being deleted, so the remote workload is still relevant.
  ```

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:163** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:29:26Z
```diff
@@ -130,6 +134,51 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local corespondent (missing or awaiting deletion). If the remote workload is
+// is owned by a job,also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The remote workload is still relevant.
+			continue
+		}
+
+		// if the remote wl has a controller(owning Job), delete the job
```

  I think it could be useful for debugging if we log that deletion is being attempted. This is what the k8s GC does: https://github.com/kubernetes/kubernetes/blob/23728378380ec853676c4d5bf7b39ae8dbc7a74f/pkg/controller/podgc/gc_controller.go#L344.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:357** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:31:34Z
```diff
@@ -285,16 +341,39 @@ func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealp
 	return c.localClient.Status().Update(ctx, cluster)
 }
 
+func (c *clustersReconciler) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	if c.gcInterval == 0 {
+		log.V(2).Info("Garbage Collection is disabled")
+		return
+	}
+	log.V(2).Info("Starting Garbage Collector")
+	for {
+		select {
+		case <-ctx.Done():
+			log.V(2).Info("Garbage Collector Stopped")
+			return
+		case <-time.After(c.gcInterval):
+			log.V(5).Info("Run Garbage Collection")
```

  ```suggestion
  			log.V(4).Info("Run Garbage Collection for Lost Remote Workloads")
  ```
  Nit, following the log level from core k8s: https://github.com/kubernetes/kubernetes/blob/23728378380ec853676c4d5bf7b39ae8dbc7a74f/pkg/controller/podgc/gc_controller.go#L147

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T09:49:17Z

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@trasc](https://github.com/trasc) — 2024-02-02T09:49:16Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueOriginLabelKey = "kueue.x-k8s.io/multikueue-origin"
```

  there are a couple `pkg/constants` ,`pkg/controller/constants`  but I don't see this fitting in any, being very speiffic to multikueue.
  
  We could add a cleanup follow-up to group the multikueue constants in a dedicate file in this package.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T09:51:46Z

- **apis/config/v1beta1/configuration_types.go:209** by [@trasc](https://github.com/trasc) — 2024-02-02T09:51:46Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  nil is interpreted as not specified in the config and subject to defaulting.
  0  is disabled.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T09:58:00Z

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:58:00Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  Ok, but if we do defaulting anyway why not just get rid of the *?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T10:01:09Z

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@mimowo](https://github.com/mimowo) — 2024-02-02T10:01:09Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueOriginLabelKey = "kueue.x-k8s.io/multikueue-origin"
```

  I see, I would like to have then an analogous constants package for multikueue. Can be a follow up if we have already some other API-facing consts from other PRs

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T10:02:35Z

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@mimowo](https://github.com/mimowo) — 2024-02-02T10:02:35Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueOriginLabelKey = "kueue.x-k8s.io/multikueue-origin"
```

  Similarly, in the core k8s the API-facing labels are put together: https://github.com/kubernetes/kubernetes/blob/master/pkg/apis/batch/types.go#L25-L54. I think this helps a lot with readability to have one go-to place rather than scattered.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T10:07:05Z

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-02-02T10:07:05Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  To be clear, I think this is enough `GCInterval metav1.Duration`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T10:08:04Z

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-02-02T10:08:04Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  Please rename to `GCInterval` or `GCPeriod`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T10:08:53Z

- **apis/config/v1beta1/configuration_types.go:206** by [@mimowo](https://github.com/mimowo) — 2024-02-02T10:08:52Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
```

  ```suggestion
  	// GCTimeout defines the time interval between two consecutive garbage collection runs.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T10:22:37Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:159** by [@trasc](https://github.com/trasc) — 2024-02-02T09:56:12Z
```diff
@@ -130,6 +134,51 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local corespondent (missing or awaiting deletion). If the remote workload is
+// is owned by a job,also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The remote workload is still relevant.
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:163** by [@trasc](https://github.com/trasc) — 2024-02-02T10:02:22Z
```diff
@@ -130,6 +134,51 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local corespondent (missing or awaiting deletion). If the remote workload is
+// is owned by a job,also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{MultiKueueOriginLabelKey: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The remote workload is still relevant.
+			continue
+		}
+
+		// if the remote wl has a controller(owning Job), delete the job
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:357** by [@trasc](https://github.com/trasc) — 2024-02-02T10:04:33Z
```diff
@@ -285,16 +341,39 @@ func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealp
 	return c.localClient.Status().Update(ctx, cluster)
 }
 
+func (c *clustersReconciler) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	if c.gcInterval == 0 {
+		log.V(2).Info("Garbage Collection is disabled")
+		return
+	}
+	log.V(2).Info("Starting Garbage Collector")
+	for {
+		select {
+		case <-ctx.Done():
+			log.V(2).Info("Garbage Collector Stopped")
+			return
+		case <-time.After(c.gcInterval):
+			log.V(5).Info("Run Garbage Collection")
```

  done

- **apis/config/v1beta1/configuration_types.go:209** by [@trasc](https://github.com/trasc) — 2024-02-02T10:06:32Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  If it's not a pointer we will not know if the user did not specified the value and wants to go with the default or the user wants the GC disabled.

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@trasc](https://github.com/trasc) — 2024-02-02T10:15:57Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueOriginLabelKey = "kueue.x-k8s.io/multikueue-origin"
```

  moved in API

- **apis/config/v1beta1/configuration_types.go:206** by [@trasc](https://github.com/trasc) — 2024-02-02T10:18:12Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
```

  done

- **apis/config/v1beta1/configuration_types.go:209** by [@trasc](https://github.com/trasc) — 2024-02-02T10:18:43Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  why?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T10:38:44Z

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-02-02T10:38:43Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  Because the words `Interval` and `Period` capture the periodic nature of the mechanism. 
  
  Timeout suggests it is the maximal amount of time for the GC to complete a single cycle.
  
  Note that in the code k8s the analogous parameter is called `gcCheckPeriod`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T11:12:52Z

- **apis/config/v1beta1/configuration_types.go:209** by [@trasc](https://github.com/trasc) — 2024-02-02T11:12:52Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T11:13:32Z

- **apis/config/v1beta1/configuration_types.go:209** by [@trasc](https://github.com/trasc) — 2024-02-02T11:13:32Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

   If it's not a pointer we will not know if the user did not specified the value and wants to go with the default or the user wants the GC disabled.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T11:21:29Z

- **pkg/controller/admissionchecks/multikueue/workload.go:56** by [@mimowo](https://github.com/mimowo) — 2024-02-02T11:21:29Z
```diff
@@ -52,6 +52,10 @@ var (
 	errNoActiveClusters = errors.New("no active clusters")
 )
 
+const (
+	MultiKueueOriginLabelKey = "kueue.x-k8s.io/multikueue-origin"
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T11:27:14Z

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-02-02T11:27:14Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCTimeout defines the timeout between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCTimeout *metav1.Duration `json:"gcTimeout,omitempty"`
```

  Ah, I think I know what you mean, so that on existing on upgrade the user may set:
  ```
  multikueue:
    origin: multikeue-a
  ```
  forgetting to set the interval param, then it will be left as 0, which would be generally not desired. This makes sense, but it is tricky. My option 2. has similar issue - we would get it disabled, which is probably not the intention. The intention is most likely to use the default.
  
  I think the current version make sense, otherwise we would need something like `gcEnabled` extra field, but might be overkill.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T11:29:04Z

- **apis/config/v1beta1/configuration_types.go:209** by [@mimowo](https://github.com/mimowo) — 2024-02-02T11:29:03Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcTimeout,omitempty"`
```

  ```suggestion
  	GCInterval *metav1.Duration `json:"gcInterval,omitempty"`
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T11:36:07Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster_test.go:280** by [@mimowo](https://github.com/mimowo) — 2024-02-02T11:36:07Z
```diff
@@ -222,3 +225,157 @@ func TestUpdateConfig(t *testing.T) {
 		})
 	}
 }
+
+func TestRemoteClientGC(t *testing.T) {
+	baseJobBuilder := testingjob.MakeJob("job1", TestNamespace)
+	baseWlBuilder := utiltesting.MakeWorkload("wl1", TestNamespace).OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "test-uuid", true, true)
+
+	cases := map[string]struct {
+		managersWorkloads []kueue.Workload
+		workersWorkloads  []kueue.Workload
+		managersJobs      []batchv1.Job
+		workersJobs       []batchv1.Job
+
+		wantManagersWorkloads []kueue.Workload
+		wantWorkersWorkloads  []kueue.Workload
+		wantManagersJobs      []batchv1.Job
+		wantWorkersJobs       []batchv1.Job
+	}{
+		"existing workers and jobs are not deleted": {
+			managersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Obj(),
+			},
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			workersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
+		"missing workers and their owner jobs are deleted": {
```

  Please also add a test case for the scenario when there is only worker workload (no worker Job). IIRC there was an issue with the original implementation that we only deleted workloads if there was the controller job.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T11:46:33Z

- **apis/config/v1beta1/configuration_types.go:209** by [@trasc](https://github.com/trasc) — 2024-02-02T11:46:33Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcTimeout,omitempty"`
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T11:50:39Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster_test.go:280** by [@trasc](https://github.com/trasc) — 2024-02-02T11:50:38Z
```diff
@@ -222,3 +225,157 @@ func TestUpdateConfig(t *testing.T) {
 		})
 	}
 }
+
+func TestRemoteClientGC(t *testing.T) {
+	baseJobBuilder := testingjob.MakeJob("job1", TestNamespace)
+	baseWlBuilder := utiltesting.MakeWorkload("wl1", TestNamespace).OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "test-uuid", true, true)
+
+	cases := map[string]struct {
+		managersWorkloads []kueue.Workload
+		workersWorkloads  []kueue.Workload
+		managersJobs      []batchv1.Job
+		workersJobs       []batchv1.Job
+
+		wantManagersWorkloads []kueue.Workload
+		wantWorkersWorkloads  []kueue.Workload
+		wantManagersJobs      []batchv1.Job
+		wantWorkersJobs       []batchv1.Job
+	}{
+		"existing workers and jobs are not deleted": {
+			managersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Obj(),
+			},
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			workersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
+		"missing workers and their owner jobs are deleted": {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T12:04:01Z

/lgtm
/assign @tenzen-y @alculquicondor

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-02T18:19:23Z

I have not yet reviewed integration tests.

- **apis/config/v1beta1/configuration_types.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T17:34:48Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcInterval,omitempty"`
+
+	// A label value used to track the creator of workloads in the worker clusters.
+	// This is used by multikueue in components like its garbage collector to identify
+	// remote objects that ware created by this multikueue  manager cluster and delete
+	// them if their local counterpart no longer exists.
+	// +optional
+	Origin string `json:"origin,omitempty"`
```

  ```suggestion
  	Origin *string `json:"origin,omitempty"`
  ```
  
  Because this is an optional.

- **apis/config/v1beta1/configuration_types.go:213** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T17:42:17Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcInterval,omitempty"`
+
+	// A label value used to track the creator of workloads in the worker clusters.
+	// This is used by multikueue in components like its garbage collector to identify
+	// remote objects that ware created by this multikueue  manager cluster and delete
```

  ```suggestion
  	// remote objects that ware created by this multikueue manager cluster and delete
  ```

- **apis/config/v1beta1/defaults.go:170** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T17:42:47Z
```diff
@@ -161,4 +163,14 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if cfg.Integrations.PodOptions.PodSelector == nil {
 		cfg.Integrations.PodOptions.PodSelector = &metav1.LabelSelector{}
 	}
+
+	if cfg.MultiKueue == nil {
+		cfg.MultiKueue = &MultiKueue{}
+	}
+	if cfg.MultiKueue.GCInterval == nil {
```

  ```suggestion
  	if cfg.MultiKueue.GCInterval == nil || cfg.MultiKueue.GCInterval.Duration.Seconds() == 0 {
  ```
  
  > Defaults to 1min. If 0, the garbage collection is disabled.
  
  Should we need this defaulting?

- **apis/config/v1beta1/configuration_types.go:211** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T17:45:23Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcInterval,omitempty"`
+
+	// A label value used to track the creator of workloads in the worker clusters.
```

  ```suggestion
  	// Origin defines a label value used to track the creator of workloads in the worker clusters.
  ```

- **apis/config/v1beta1/defaults_test.go:535** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T17:47:59Z
```diff
@@ -464,6 +481,32 @@ func TestSetDefaults_Configuration(t *testing.T) {
 						MaxCount: 0,
 					},
 				},
+				MultiKueue: defaultMultiKueue,
+			},
+		},
+		"multiKueue": {
+			original: &Configuration{
+				InternalCertManagement: &InternalCertManagement{
+					Enable: ptr.To(false),
+				},
+				MultiKueue: &MultiKueue{
+					GCInterval: &metav1.Duration{Duration: time.Second},
+					Origin:     "multikueue-manager1",
+				},
+			},
+			want: &Configuration{
+				Namespace:         ptr.To(DefaultNamespace),
+				ControllerManager: defaultCtrlManagerConfigurationSpec,
+				InternalCertManagement: &InternalCertManagement{
+					Enable: ptr.To(false),
+				},
+				ClientConnection: defaultClientConnection,
+				Integrations:     defaultIntegrations,
+				QueueVisibility:  defaultQueueVisibility,
+				MultiKueue: &MultiKueue{
+					GCInterval: &metav1.Duration{Duration: time.Second},
+					Origin:     "multikueue-manager1",
+				},
 			},
```

  Can you add a case where the multikueue.gcInterval.duration is 0?

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:137** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T17:51:06Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
```

  ```suggestion
  // runGC - lists all the remote workloads having the same multikueue-origin and remove those who
  ```

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:138** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T17:59:29Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local corespondent (missing or awaiting deletion). If the remote workload is
```

  ```suggestion
  // no longer have a local correspondent (missing or awaiting deletion). If the remote workload
  ```

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:178** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T18:12:36Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local corespondent (missing or awaiting deletion). If the remote workload is
+// is owned by a job,also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{kueuealpha.MultiKueueOriginLabel: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The local workload exists and isn't being deleted, so the remote workload is still relevant.
+			continue
+		}
+
+		// if the remote wl has a controller(owning Job), delete the job
+		if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+			ownerKey := types.NamespacedName{Name: controller.Name, Namespace: remoteWl.Namespace}
+			adapterKey := schema.FromAPIVersionAndKind(controller.APIVersion, controller.Kind).String()
+			if adapter, found := adapters[adapterKey]; !found {
+				wlLog.V(2).Info("No adapter found", "adapterKey", adapterKey, "ownerKey", ownerKey)
+			} else {
+				wlLog.V(5).Info("MultiKueueGC deleting workload owner", "ownerKey", ownerKey, "ownnerKind", controller)
+				err := adapter.DeleteRemoteObject(ctx, rc.client, ownerKey)
+				if client.IgnoreNotFound(err) != nil {
+					wlLog.V(2).Error(err, "Deleting remote workload's owner", "ownerKey", ownerKey)
+				}
+			}
+		}
+		wlLog.V(5).Info("MultiKueueGC deleting remote workload")
+		if err := rc.client.Delete(ctx, &remoteWl); client.IgnoreNotFound(err) != nil {
```

  When we delete workloads despite we don't remove jobs due to the dedicated adapter is not found (Line 168), 
  the job resource will be orphaned, right?

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:217** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T18:14:06Z
```diff
@@ -157,6 +214,7 @@ var _ reconcile.Reconciler = (*clustersReconciler)(nil)
 
 func (c *clustersReconciler) Start(ctx context.Context) error {
 	c.rootContext = ctx
+	go c.runGC(ctx)
```

  For future work, I guess that it is possible to register runGC to the manager as a separate runnable.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster_test.go:323** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T18:19:04Z
```diff
@@ -222,3 +225,172 @@ func TestUpdateConfig(t *testing.T) {
 		})
 	}
 }
+
+func TestRemoteClientGC(t *testing.T) {
+	baseJobBuilder := testingjob.MakeJob("job1", TestNamespace)
+	baseWlBuilder := utiltesting.MakeWorkload("wl1", TestNamespace).OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "test-uuid", true, true)
+
+	cases := map[string]struct {
+		managersWorkloads []kueue.Workload
+		workersWorkloads  []kueue.Workload
+		managersJobs      []batchv1.Job
+		workersJobs       []batchv1.Job
+
+		wantManagersWorkloads []kueue.Workload
+		wantWorkersWorkloads  []kueue.Workload
+		wantManagersJobs      []batchv1.Job
+		wantWorkersJobs       []batchv1.Job
+	}{
+		"existing workers and jobs are not deleted": {
+			managersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Obj(),
+			},
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			workersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
+		"missing worker workloads are deleted": {
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
+		"missing worker workloads and their owner jobs are deleted": {
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			workersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
+		"unrelated workers and jobs are not deleted": {
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, "other-gc-key").
+					Obj(),
+			},
+			workersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, "other-gc-key").
+					Obj(),
+			},
+			wantWorkersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
```

  Can we test the case where the dedicated adapter isn't found?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-03T09:08:03Z

- **apis/config/v1beta1/configuration_types.go:216** by [@trasc](https://github.com/trasc) — 2024-02-03T09:08:02Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcInterval,omitempty"`
+
+	// A label value used to track the creator of workloads in the worker clusters.
+	// This is used by multikueue in components like its garbage collector to identify
+	// remote objects that ware created by this multikueue  manager cluster and delete
+	// them if their local counterpart no longer exists.
+	// +optional
+	Origin string `json:"origin,omitempty"`
```

  it is optional but "" is not a value option, we default from that

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-05T07:23:37Z

- **apis/config/v1beta1/configuration_types.go:211** by [@trasc](https://github.com/trasc) — 2024-02-05T06:45:18Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcInterval,omitempty"`
+
+	// A label value used to track the creator of workloads in the worker clusters.
```

  done

- **apis/config/v1beta1/configuration_types.go:213** by [@trasc](https://github.com/trasc) — 2024-02-05T06:45:36Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcInterval,omitempty"`
+
+	// A label value used to track the creator of workloads in the worker clusters.
+	// This is used by multikueue in components like its garbage collector to identify
+	// remote objects that ware created by this multikueue  manager cluster and delete
```

  done

- **apis/config/v1beta1/defaults.go:170** by [@trasc](https://github.com/trasc) — 2024-02-05T06:46:40Z
```diff
@@ -161,4 +163,14 @@ func SetDefaults_Configuration(cfg *Configuration) {
 	if cfg.Integrations.PodOptions.PodSelector == nil {
 		cfg.Integrations.PodOptions.PodSelector = &metav1.LabelSelector{}
 	}
+
+	if cfg.MultiKueue == nil {
+		cfg.MultiKueue = &MultiKueue{}
+	}
+	if cfg.MultiKueue.GCInterval == nil {
```

  no

- **apis/config/v1beta1/defaults_test.go:535** by [@trasc](https://github.com/trasc) — 2024-02-05T06:50:07Z
```diff
@@ -464,6 +481,32 @@ func TestSetDefaults_Configuration(t *testing.T) {
 						MaxCount: 0,
 					},
 				},
+				MultiKueue: defaultMultiKueue,
+			},
+		},
+		"multiKueue": {
+			original: &Configuration{
+				InternalCertManagement: &InternalCertManagement{
+					Enable: ptr.To(false),
+				},
+				MultiKueue: &MultiKueue{
+					GCInterval: &metav1.Duration{Duration: time.Second},
+					Origin:     "multikueue-manager1",
+				},
+			},
+			want: &Configuration{
+				Namespace:         ptr.To(DefaultNamespace),
+				ControllerManager: defaultCtrlManagerConfigurationSpec,
+				InternalCertManagement: &InternalCertManagement{
+					Enable: ptr.To(false),
+				},
+				ClientConnection: defaultClientConnection,
+				Integrations:     defaultIntegrations,
+				QueueVisibility:  defaultQueueVisibility,
+				MultiKueue: &MultiKueue{
+					GCInterval: &metav1.Duration{Duration: time.Second},
+					Origin:     "multikueue-manager1",
+				},
 			},
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:137** by [@trasc](https://github.com/trasc) — 2024-02-05T06:51:00Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:138** by [@trasc](https://github.com/trasc) — 2024-02-05T06:54:49Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local corespondent (missing or awaiting deletion). If the remote workload is
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:178** by [@trasc](https://github.com/trasc) — 2024-02-05T06:59:21Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGc - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local corespondent (missing or awaiting deletion). If the remote workload is
+// is owned by a job,also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{kueuealpha.MultiKueueOriginLabel: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The local workload exists and isn't being deleted, so the remote workload is still relevant.
+			continue
+		}
+
+		// if the remote wl has a controller(owning Job), delete the job
+		if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+			ownerKey := types.NamespacedName{Name: controller.Name, Namespace: remoteWl.Namespace}
+			adapterKey := schema.FromAPIVersionAndKind(controller.APIVersion, controller.Kind).String()
+			if adapter, found := adapters[adapterKey]; !found {
+				wlLog.V(2).Info("No adapter found", "adapterKey", adapterKey, "ownerKey", ownerKey)
+			} else {
+				wlLog.V(5).Info("MultiKueueGC deleting workload owner", "ownerKey", ownerKey, "ownnerKind", controller)
+				err := adapter.DeleteRemoteObject(ctx, rc.client, ownerKey)
+				if client.IgnoreNotFound(err) != nil {
+					wlLog.V(2).Error(err, "Deleting remote workload's owner", "ownerKey", ownerKey)
+				}
+			}
+		}
+		wlLog.V(5).Info("MultiKueueGC deleting remote workload")
+		if err := rc.client.Delete(ctx, &remoteWl); client.IgnoreNotFound(err) != nil {
```

  Without having an adapter for its type, the job would not have been created.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:217** by [@trasc](https://github.com/trasc) — 2024-02-05T07:01:46Z
```diff
@@ -157,6 +214,7 @@ var _ reconcile.Reconciler = (*clustersReconciler)(nil)
 
 func (c *clustersReconciler) Start(ctx context.Context) error {
 	c.rootContext = ctx
+	go c.runGC(ctx)
```

  ... maybe, if we want to split out GC part

- **pkg/controller/admissionchecks/multikueue/multikueuecluster_test.go:323** by [@trasc](https://github.com/trasc) — 2024-02-05T07:13:17Z
```diff
@@ -222,3 +225,172 @@ func TestUpdateConfig(t *testing.T) {
 		})
 	}
 }
+
+func TestRemoteClientGC(t *testing.T) {
+	baseJobBuilder := testingjob.MakeJob("job1", TestNamespace)
+	baseWlBuilder := utiltesting.MakeWorkload("wl1", TestNamespace).OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "test-uuid", true, true)
+
+	cases := map[string]struct {
+		managersWorkloads []kueue.Workload
+		workersWorkloads  []kueue.Workload
+		managersJobs      []batchv1.Job
+		workersJobs       []batchv1.Job
+
+		wantManagersWorkloads []kueue.Workload
+		wantWorkersWorkloads  []kueue.Workload
+		wantManagersJobs      []batchv1.Job
+		wantWorkersJobs       []batchv1.Job
+	}{
+		"existing workers and jobs are not deleted": {
+			managersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Obj(),
+			},
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			workersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
+		"missing worker workloads are deleted": {
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
+		"missing worker workloads and their owner jobs are deleted": {
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, defaultOrigin).
+					Obj(),
+			},
+			managersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			workersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantManagersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
+		"unrelated workers and jobs are not deleted": {
+			workersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, "other-gc-key").
+					Obj(),
+			},
+			workersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+			wantWorkersWorkloads: []kueue.Workload{
+				*baseWlBuilder.Clone().
+					Label(kueuealpha.MultiKueueOriginLabel, "other-gc-key").
+					Obj(),
+			},
+			wantWorkersJobs: []batchv1.Job{
+				*baseJobBuilder.Clone().
+					Obj(),
+			},
+		},
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-05T15:24:30Z

- **apis/config/v1beta1/configuration_types.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-05T15:24:30Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcInterval,omitempty"`
+
+	// A label value used to track the creator of workloads in the worker clusters.
+	// This is used by multikueue in components like its garbage collector to identify
+	// remote objects that ware created by this multikueue  manager cluster and delete
+	// them if their local counterpart no longer exists.
+	// +optional
+	Origin string `json:"origin,omitempty"`
```

  I still think that a pointer is better based on https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#optional-vs-required

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-05T16:39:19Z

- **apis/config/v1beta1/configuration_types.go:216** by [@trasc](https://github.com/trasc) — 2024-02-05T16:39:19Z
```diff
@@ -199,6 +202,20 @@ type WaitForPodsReady struct {
 	RequeuingTimestamp *RequeuingTimestamp `json:"requeuingTimestamp,omitempty"`
 }
 
+type MultiKueue struct {
+	// GCInterval defines the time interval between two consecutive garbage collection runs.
+	// Defaults to 1min. If 0, the garbage collection is disabled.
+	// +optional
+	GCInterval *metav1.Duration `json:"gcInterval,omitempty"`
+
+	// A label value used to track the creator of workloads in the worker clusters.
+	// This is used by multikueue in components like its garbage collector to identify
+	// remote objects that ware created by this multikueue  manager cluster and delete
+	// them if their local counterpart no longer exists.
+	// +optional
+	Origin string `json:"origin,omitempty"`
```

  it adds no benefits in this case , only some additional unnecessary allocations I suspect , however done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-05T16:50:19Z

- **cmd/kueue/main.go:28** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T16:42:06Z
```diff
@@ -25,6 +25,7 @@ import (
 	// Import all Kubernetes client auth plugins (e.g. Azure, GCP, OIDC, etc.)
 	// to ensure that exec-entrypoint and run can make use of them.
 	_ "k8s.io/client-go/plugin/pkg/client/auth"
+	"k8s.io/utils/ptr"
```

  move this to the next block

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:165** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T16:46:41Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGC - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local correspondent (missing or awaiting deletion). If the remote workload
+// is owned by a job, also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{kueuealpha.MultiKueueOriginLabel: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The local workload exists and isn't being deleted, so the remote workload is still relevant.
+			continue
+		}
+
+		// if the remote wl has a controller(owning Job), delete the job
+		if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+			ownerKey := types.NamespacedName{Name: controller.Name, Namespace: remoteWl.Namespace}
```

  use `klog.KRef` instead, which implements different formats.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:177** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T16:47:59Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGC - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local correspondent (missing or awaiting deletion). If the remote workload
+// is owned by a job, also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{kueuealpha.MultiKueueOriginLabel: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The local workload exists and isn't being deleted, so the remote workload is still relevant.
+			continue
+		}
+
+		// if the remote wl has a controller(owning Job), delete the job
+		if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+			ownerKey := types.NamespacedName{Name: controller.Name, Namespace: remoteWl.Namespace}
+			adapterKey := schema.FromAPIVersionAndKind(controller.APIVersion, controller.Kind).String()
+			if adapter, found := adapters[adapterKey]; !found {
+				wlLog.V(2).Info("No adapter found", "adapterKey", adapterKey, "ownerKey", ownerKey)
+			} else {
+				wlLog.V(5).Info("MultiKueueGC deleting workload owner", "ownerKey", ownerKey, "ownnerKind", controller)
+				err := adapter.DeleteRemoteObject(ctx, rc.client, ownerKey)
+				if client.IgnoreNotFound(err) != nil {
+					wlLog.V(2).Error(err, "Deleting remote workload's owner", "ownerKey", ownerKey)
+				}
+			}
+		}
+		wlLog.V(5).Info("MultiKueueGC deleting remote workload")
```

  this can be inside an "else", as deleting the owner would cascade the deletion of the workload without an additional API call.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:347** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T16:49:27Z
```diff
@@ -285,16 +343,39 @@ func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealp
 	return c.localClient.Status().Update(ctx, cluster)
 }
 
+func (c *clustersReconciler) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
```

  ```suggestion
  	log := ctrl.LoggerFrom(ctx).WithName("MultiKueueGC")
  ```
  
  Then you can simplify some messages in the rest of the code.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-05T17:07:06Z

- **cmd/kueue/main.go:28** by [@trasc](https://github.com/trasc) — 2024-02-05T16:54:05Z
```diff
@@ -25,6 +25,7 @@ import (
 	// Import all Kubernetes client auth plugins (e.g. Azure, GCP, OIDC, etc.)
 	// to ensure that exec-entrypoint and run can make use of them.
 	_ "k8s.io/client-go/plugin/pkg/client/auth"
+	"k8s.io/utils/ptr"
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:165** by [@trasc](https://github.com/trasc) — 2024-02-05T17:00:23Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGC - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local correspondent (missing or awaiting deletion). If the remote workload
+// is owned by a job, also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{kueuealpha.MultiKueueOriginLabel: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The local workload exists and isn't being deleted, so the remote workload is still relevant.
+			continue
+		}
+
+		// if the remote wl has a controller(owning Job), delete the job
+		if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+			ownerKey := types.NamespacedName{Name: controller.Name, Namespace: remoteWl.Namespace}
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:177** by [@trasc](https://github.com/trasc) — 2024-02-05T17:04:08Z
```diff
@@ -130,6 +134,53 @@ func (rc *remoteClient) queueWorkloadEvent(ctx context.Context, ev watch.Event)
 	}
 }
 
+// runGC - lists all the remote workloads having the same multikueue-origin and remove those who
+// no longer have a local correspondent (missing or awaiting deletion). If the remote workload
+// is owned by a job, also delete the job.
+func (rc *remoteClient) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := &kueue.WorkloadList{}
+	err := rc.client.List(ctx, lst, client.MatchingLabels{kueuealpha.MultiKueueOriginLabel: rc.origin})
+	if err != nil {
+		log.V(2).Error(err, "Listing remote workloads")
+		return
+	}
+
+	for _, remoteWl := range lst.Items {
+		localWl := &kueue.Workload{}
+		wlLog := log.WithValues("remoteWl", klog.KObj(&remoteWl))
+		err := rc.localClient.Get(ctx, client.ObjectKeyFromObject(&remoteWl), localWl)
+		if client.IgnoreNotFound(err) != nil {
+			wlLog.V(2).Error(err, "Reading local workload")
+			continue
+		}
+
+		if err == nil && localWl.DeletionTimestamp.IsZero() {
+			// The local workload exists and isn't being deleted, so the remote workload is still relevant.
+			continue
+		}
+
+		// if the remote wl has a controller(owning Job), delete the job
+		if controller := metav1.GetControllerOf(&remoteWl); controller != nil {
+			ownerKey := types.NamespacedName{Name: controller.Name, Namespace: remoteWl.Namespace}
+			adapterKey := schema.FromAPIVersionAndKind(controller.APIVersion, controller.Kind).String()
+			if adapter, found := adapters[adapterKey]; !found {
+				wlLog.V(2).Info("No adapter found", "adapterKey", adapterKey, "ownerKey", ownerKey)
+			} else {
+				wlLog.V(5).Info("MultiKueueGC deleting workload owner", "ownerKey", ownerKey, "ownnerKind", controller)
+				err := adapter.DeleteRemoteObject(ctx, rc.client, ownerKey)
+				if client.IgnoreNotFound(err) != nil {
+					wlLog.V(2).Error(err, "Deleting remote workload's owner", "ownerKey", ownerKey)
+				}
+			}
+		}
+		wlLog.V(5).Info("MultiKueueGC deleting remote workload")
```

  I prefer to keep it as is, I find it easier to follow.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:347** by [@trasc](https://github.com/trasc) — 2024-02-05T17:06:41Z
```diff
@@ -285,16 +343,39 @@ func (c *clustersReconciler) updateStatus(ctx context.Context, cluster *kueuealp
 	return c.localClient.Status().Update(ctx, cluster)
 }
 
+func (c *clustersReconciler) runGC(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
```

  done
