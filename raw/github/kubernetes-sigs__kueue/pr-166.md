# PR #166: Add details for ResourceFlavor concept and task steps for multiple flavors

**Summary**: Add details for ResourceFlavor concept and task steps for multiple flavors

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/166

**Last updated**: 2022-04-05T15:07:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `doc` → `main`
- **Created**: 2022-03-30T19:25:49Z
- **Updated**: 2022-04-05T15:07:37Z
- **Merged**: 2022-04-05T15:07:37Z
- **Closed**: 2022-04-05T15:07:37Z
- **Labels**: `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 4   **+289 / -23**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #64 

#### Special notes for your reviewer:

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-30T20:51:06Z

cc @cortespao

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-05T14:48:44Z

/hold
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-05T14:59:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/166#" title="Author self-approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/166#pullrequestreview-932013299" title="Approved">cortespao</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T15:02:49Z

/label tide/merge-method-squash

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T15:03:03Z

/hold cancel

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-31T17:59:17Z

- **docs/concepts/cluster_queue.md:82** by [@ahg-g](https://github.com/ahg-g) — 2022-03-31T17:50:18Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: spot
+labels:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a flavor from a ClusterQueue.
+
+### ResourceFlavor labels
```

  we need to mention that nodes with matching labels should exist for the workload to actually schedule.

- **docs/tasks/administer_cluster_quotas.md:119** by [@ahg-g](https://github.com/ahg-g) — 2022-03-31T17:55:59Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
```

  stress the need to have nodes with the below mentioned labels

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-03-31T20:31:02Z

Happy to elaborate on any comment if needed 😄

- **docs/concepts/cluster_queue.md:41** by [@cortespao](https://github.com/cortespao) — 2022-03-31T19:40:33Z
```diff
@@ -7,6 +7,51 @@ such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
 
 Only [cluster administrators](/docs/tasks#batch-administrator) should create `ClusterQueue` objects.
 
+A sample ClusterQueue looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+This ClusterQueue admits [workloads](queued_workload.md) if and only if:
+- The sum of the CPU requests is less than or equal to 9.
+- The sum of the memory requests is less than or equal to 36Gi.
+
+## Namespace selector
+
+The `.spec.namespaceSelector` field allows you to limit which namespaces
+can have workloads admitted in the ClusterQueue, using a
+[label selector](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/label-selector/#LabelSelector).
+
```

  Suggestion for lines 38 - 40:
  You can limit which namespaces can have workloads admitted in the ClusterQueue, by assigning a [label selector](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/label-selector/#LabelSelector) to the `.spec.namespaceSelector` field.

- **docs/concepts/cluster_queue.md:42** by [@cortespao](https://github.com/cortespao) — 2022-03-31T19:41:46Z
```diff
@@ -7,6 +7,51 @@ such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
 
 Only [cluster administrators](/docs/tasks#batch-administrator) should create `ClusterQueue` objects.
 
+A sample ClusterQueue looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+This ClusterQueue admits [workloads](queued_workload.md) if and only if:
+- The sum of the CPU requests is less than or equal to 9.
+- The sum of the memory requests is less than or equal to 36Gi.
+
+## Namespace selector
+
+The `.spec.namespaceSelector` field allows you to limit which namespaces
+can have workloads admitted in the ClusterQueue, using a
+[label selector](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/label-selector/#LabelSelector).
+
+The empty selector `{}` allows workloads from all the namespaces.
```

  To allow workloads from all the namespaces, assign the empty selector `{}`  to the `.spec.namespaceSelector` field.

- **docs/concepts/cluster_queue.md:63** by [@cortespao](https://github.com/cortespao) — 2022-03-31T19:45:27Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
```

  "(...) variations and allows you to associate them (...)"

- **docs/concepts/cluster_queue.md:69** by [@cortespao](https://github.com/cortespao) — 2022-03-31T19:45:42Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
```

  `ResourceFlavor`

- **docs/concepts/cluster_queue.md:55** by [@cortespao](https://github.com/cortespao) — 2022-03-31T19:47:36Z
```diff
@@ -7,6 +7,51 @@ such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
 
 Only [cluster administrators](/docs/tasks#batch-administrator) should create `ClusterQueue` objects.
 
+A sample ClusterQueue looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+This ClusterQueue admits [workloads](queued_workload.md) if and only if:
+- The sum of the CPU requests is less than or equal to 9.
+- The sum of the memory requests is less than or equal to 36Gi.
+
+## Namespace selector
+
+The `.spec.namespaceSelector` field allows you to limit which namespaces
+can have workloads admitted in the ClusterQueue, using a
+[label selector](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/label-selector/#LabelSelector).
+
+The empty selector `{}` allows workloads from all the namespaces.
+
+A sample `namespaceSelector` looks like the following:
+
+```yaml
+namespaceSelector:
+  matchExpressions:
+  - key: team
+    operator: In
+    values:
+    - team-a
+```
+
 ## Resource Flavors
```

  Resource Flavor object
  
  (If the term "object" applies)

- **docs/concepts/cluster_queue.md:116** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:00:48Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: spot
+labels:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a flavor from a ClusterQueue.
+
+### ResourceFlavor labels
+
+When admitting a workload, Kueue evaluates the
+[`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+fields in the PodSpecs of your [QueuedWorkloads](queued_workload.md) against the
+ResourceFlavor labels.
+
+Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+`.nodeSelector` of the underlying workload Pod templates, if the workload didn't
+specify them already. For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+Kueue adds the labels to `.spec.template.spec.nodeSelector`. This guarantees
+that the workload Pods run on the nodes associated to the flavor that Kueue
+decided that the workload should use.
+
```

  Suggestion for lines 84 - 95:
  
  To guarantee that the workload Pods run on the nodes associated to the flavor that Kueue
  decided that the workload should use, Kueue performs the following steps:
  
  1. When admitting a workload, Kueue evaluates the
  [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
  and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
  fields in the PodSpecs of your [QueuedWorkloads](queued_workload.md) against the
  `ResourceFlavor` labels.
  
  2. Once the workload is admitted, Kueue adds the `ResourceFlavor` labels to the
  `.nodeSelector` of the underlying workload Pod templates, if the workload didn't
  specify them already. 
  
  For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
  Kueue adds the labels to `.spec.template.spec.nodeSelector`.

- **docs/concepts/cluster_queue.md:62** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:10:36Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
```

  Before this line, I would add a note to help a homogeneous cluster audience to skip to the specific section:
  
  Note: If your cluster is homogeneous, you can use a [default ResourceFlavor](#a-default-resourceflavor) instead of assigning custom ResourceFlavor labels.

- **docs/concepts/cluster_queue.md:108** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:12:31Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: spot
+labels:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a flavor from a ClusterQueue.
+
+### ResourceFlavor labels
+
+When admitting a workload, Kueue evaluates the
+[`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+fields in the PodSpecs of your [QueuedWorkloads](queued_workload.md) against the
+ResourceFlavor labels.
+
+Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+`.nodeSelector` of the underlying workload Pod templates, if the workload didn't
+specify them already. For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+Kueue adds the labels to `.spec.template.spec.nodeSelector`. This guarantees
+that the workload Pods run on the nodes associated to the flavor that Kueue
+decided that the workload should use.
+
+### ResourceFlavor taints
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
+workload should have a toleration for it. As opposed to ResourceFlavor labels,
+Kueue will not add tolerations for the flavor taints.
+
+### A default ResourceFlavor
+
+If your cluster has homogeneous resources, or if you don't need to manage
+quotas for the different flavors of a resource separately, you can create a
+ResourceFlavor without any labels or taints. Such ResourceFlavor looks like the
```

  Such ResourceFlavor is called default ResourceFlavor and its sample looks like the following:

- **docs/concepts/cluster_queue.md:122** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:13:52Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: spot
+labels:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a flavor from a ClusterQueue.
+
+### ResourceFlavor labels
+
+When admitting a workload, Kueue evaluates the
+[`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+fields in the PodSpecs of your [QueuedWorkloads](queued_workload.md) against the
+ResourceFlavor labels.
+
+Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+`.nodeSelector` of the underlying workload Pod templates, if the workload didn't
+specify them already. For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+Kueue adds the labels to `.spec.template.spec.nodeSelector`. This guarantees
+that the workload Pods run on the nodes associated to the flavor that Kueue
+decided that the workload should use.
+
+### ResourceFlavor taints
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
+workload should have a toleration for it. As opposed to ResourceFlavor labels,
+Kueue will not add tolerations for the flavor taints.
+
+### A default ResourceFlavor
+
+If your cluster has homogeneous resources, or if you don't need to manage
+quotas for the different flavors of a resource separately, you can create a
+ResourceFlavor without any labels or taints. Such ResourceFlavor looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: default
+```
+
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
 same cohort can borrow unused quota from each other, if they have matching
 resource flavors.
```

  Could it be?
  ", if they have matching [ResourceFlavor labels](#resourceflavor-labels)"

- **docs/tasks/administer_cluster_quotas.md:16** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:20:09Z
```diff
@@ -13,7 +13,7 @@ Make sure the following conditions are met:
 - The kubectl command-line tool has communication with your cluster.
 - [Kueue is installed](/README.md#installation).
 
-## Single ClusterQueue setup
+## Single ClusterQueue, single flavor setup {#single-queue-single-flavor}
```

  Not sure about the comma (,) reason, but I would suggest we change this to:
  
  Single ClusterQueue and single ResourceFlavor setup {#single-queue-single-flavor}

- **docs/tasks/administer_cluster_quotas.md:19** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:21:20Z
```diff
@@ -13,7 +13,7 @@ Make sure the following conditions are met:
 - The kubectl command-line tool has communication with your cluster.
 - [Kueue is installed](/README.md#installation).
 
-## Single ClusterQueue setup
+## Single ClusterQueue, single flavor setup {#single-queue-single-flavor}
 
 In the following steps, you will create a queuing system with a single
 ClusterQueue to govern the quota of your cluster.
```

   with a single ClusterQueue and a single [ResourceFlavor](https://github.com/alculquicondor/kueue/blob/doc/docs/concepts/cluster_queue.md#resource-flavors) to govern the quota of your cluster.

- **docs/tasks/administer_cluster_quotas.md:16** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:23:12Z
```diff
@@ -13,7 +13,7 @@ Make sure the following conditions are met:
 - The kubectl command-line tool has communication with your cluster.
 - [Kueue is installed](/README.md#installation).
 
-## Single ClusterQueue setup
+## Single ClusterQueue, single flavor setup {#single-queue-single-flavor}
```

  Or it could be "and empty  ResourceFlavor setup {#single-queue-single-flavor}"
  
  But it depends on the tech consideration that I might be missing.

- **docs/tasks/administer_cluster_quotas.md:115** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:24:05Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
```

  Missing period at the end.

- **docs/tasks/administer_cluster_quotas.md:139** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:26:19Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f flavor-x86.yaml flavor-arm.yaml
+```
+
```

  Would the following manifests be shown after user runs "apply" command?
  If so, we can say:
  "The output looks similar to the following:"
  Otherwise, we could say:
  "The applied ResourceFlavors manifests look similar to the following:"

- **docs/tasks/administer_cluster_quotas.md:165** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:26:38Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f flavor-x86.yaml flavor-arm.yaml
+```
+
+```yaml
+# flavor-x86.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: x86
+labels:
+  cpu-arch: x86
+```
+
+```yaml
+# flavor-arm.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: arm
+labels:
+  cpu-arch: arm
+```
+
+### 2. Create a ClusterQueue referencing the flavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
```

  Same comment as in line 124.

- **docs/tasks/administer_cluster_quotas.md:180** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:28:09Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f flavor-x86.yaml flavor-arm.yaml
+```
+
+```yaml
+# flavor-x86.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: x86
+labels:
+  cpu-arch: x86
+```
+
+```yaml
+# flavor-arm.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: arm
+labels:
+  cpu-arch: arm
+```
+
+### 2. Create a ClusterQueue referencing the flavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 9
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 84Gi
+```
+
+The flavor names in the fields `.spec.requestableResources[*].flavors[*].resourceFlavor`
+should match the names of the ResourceFlavors created earlier.
+
+Note that `memory` is referencing the `default` flavor created in the [single flavor setup](#single-queue-single-flavor).
```

  #single-queue-single-flavor seems to be broken. Not sure.

- **docs/tasks/administer_cluster_quotas.md:212** by [@cortespao](https://github.com/cortespao) — 2022-03-31T20:30:18Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f flavor-x86.yaml flavor-arm.yaml
+```
+
+```yaml
+# flavor-x86.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: x86
+labels:
+  cpu-arch: x86
+```
+
+```yaml
+# flavor-arm.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: arm
+labels:
+  cpu-arch: arm
+```
+
+### 2. Create a ClusterQueue referencing the flavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 9
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 84Gi
+```
+
+The flavor names in the fields `.spec.requestableResources[*].flavors[*].resourceFlavor`
+should match the names of the ResourceFlavors created earlier.
+
+Note that `memory` is referencing the `default` flavor created in the [single flavor setup](#single-queue-single-flavor).
+This means that you don't want to distinguish if the memory is given from `x86`
+or `arm` nodes.
+
+**Warning**
+
+Using the same flavors in multiple resources is [not supported](https://github.com/kubernetes-sigs/kueue/issues/167).
+
```

  Suggestion: I would move up the warning to line 113, and would change the **Warning** title to **Limitations**.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-01T18:14:48Z

- **docs/concepts/cluster_queue.md:41** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T16:17:41Z
```diff
@@ -7,6 +7,51 @@ such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
 
 Only [cluster administrators](/docs/tasks#batch-administrator) should create `ClusterQueue` objects.
 
+A sample ClusterQueue looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+This ClusterQueue admits [workloads](queued_workload.md) if and only if:
+- The sum of the CPU requests is less than or equal to 9.
+- The sum of the memory requests is less than or equal to 36Gi.
+
+## Namespace selector
+
+The `.spec.namespaceSelector` field allows you to limit which namespaces
+can have workloads admitted in the ClusterQueue, using a
+[label selector](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/label-selector/#LabelSelector).
+
```

  Done

- **docs/concepts/cluster_queue.md:42** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T16:17:46Z
```diff
@@ -7,6 +7,51 @@ such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
 
 Only [cluster administrators](/docs/tasks#batch-administrator) should create `ClusterQueue` objects.
 
+A sample ClusterQueue looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+This ClusterQueue admits [workloads](queued_workload.md) if and only if:
+- The sum of the CPU requests is less than or equal to 9.
+- The sum of the memory requests is less than or equal to 36Gi.
+
+## Namespace selector
+
+The `.spec.namespaceSelector` field allows you to limit which namespaces
+can have workloads admitted in the ClusterQueue, using a
+[label selector](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/label-selector/#LabelSelector).
+
+The empty selector `{}` allows workloads from all the namespaces.
```

  Done

- **docs/concepts/cluster_queue.md:55** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T16:17:53Z
```diff
@@ -7,6 +7,51 @@ such as CPU, memory and hardware accelerators. A `ClusterQueue` defines:
 
 Only [cluster administrators](/docs/tasks#batch-administrator) should create `ClusterQueue` objects.
 
+A sample ClusterQueue looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+This ClusterQueue admits [workloads](queued_workload.md) if and only if:
+- The sum of the CPU requests is less than or equal to 9.
+- The sum of the memory requests is less than or equal to 36Gi.
+
+## Namespace selector
+
+The `.spec.namespaceSelector` field allows you to limit which namespaces
+can have workloads admitted in the ClusterQueue, using a
+[label selector](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/label-selector/#LabelSelector).
+
+The empty selector `{}` allows workloads from all the namespaces.
+
+A sample `namespaceSelector` looks like the following:
+
+```yaml
+namespaceSelector:
+  matchExpressions:
+  - key: team
+    operator: In
+    values:
+    - team-a
+```
+
 ## Resource Flavors
```

  Done

- **docs/concepts/cluster_queue.md:62** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T16:26:35Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
```

  Done, but I think it's better below this line.

- **docs/concepts/cluster_queue.md:69** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T16:29:26Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
```

  throughout kuberentes docs, we just use CamelCase to refer to API objects.
  Example: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/

- **docs/concepts/cluster_queue.md:82** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:23:28Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: spot
+labels:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a flavor from a ClusterQueue.
+
+### ResourceFlavor labels
```

  Done

- **docs/concepts/cluster_queue.md:116** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:23:38Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: spot
+labels:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a flavor from a ClusterQueue.
+
+### ResourceFlavor labels
+
+When admitting a workload, Kueue evaluates the
+[`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+fields in the PodSpecs of your [QueuedWorkloads](queued_workload.md) against the
+ResourceFlavor labels.
+
+Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+`.nodeSelector` of the underlying workload Pod templates, if the workload didn't
+specify them already. For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+Kueue adds the labels to `.spec.template.spec.nodeSelector`. This guarantees
+that the workload Pods run on the nodes associated to the flavor that Kueue
+decided that the workload should use.
+
```

  Done

- **docs/concepts/cluster_queue.md:108** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:38:15Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: spot
+labels:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a flavor from a ClusterQueue.
+
+### ResourceFlavor labels
+
+When admitting a workload, Kueue evaluates the
+[`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+fields in the PodSpecs of your [QueuedWorkloads](queued_workload.md) against the
+ResourceFlavor labels.
+
+Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+`.nodeSelector` of the underlying workload Pod templates, if the workload didn't
+specify them already. For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+Kueue adds the labels to `.spec.template.spec.nodeSelector`. This guarantees
+that the workload Pods run on the nodes associated to the flavor that Kueue
+decided that the workload should use.
+
+### ResourceFlavor taints
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
+workload should have a toleration for it. As opposed to ResourceFlavor labels,
+Kueue will not add tolerations for the flavor taints.
+
+### A default ResourceFlavor
+
+If your cluster has homogeneous resources, or if you don't need to manage
+quotas for the different flavors of a resource separately, you can create a
+ResourceFlavor without any labels or taints. Such ResourceFlavor looks like the
```

  That makes it sound like Kueue adds it. I decided to rename it "empty ResourceFlavor"

- **docs/concepts/cluster_queue.md:122** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:44:58Z
```diff
@@ -17,8 +62,65 @@ Resources in a cluster are typically not homogeneous. Resources could differ in:
 A `ResourceFlavor` is an object that represents these variations and allows
 administrators to associate them with node labels and taints.
 
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: spot
+labels:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a flavor from a ClusterQueue.
+
+### ResourceFlavor labels
+
+When admitting a workload, Kueue evaluates the
+[`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+fields in the PodSpecs of your [QueuedWorkloads](queued_workload.md) against the
+ResourceFlavor labels.
+
+Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+`.nodeSelector` of the underlying workload Pod templates, if the workload didn't
+specify them already. For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+Kueue adds the labels to `.spec.template.spec.nodeSelector`. This guarantees
+that the workload Pods run on the nodes associated to the flavor that Kueue
+decided that the workload should use.
+
+### ResourceFlavor taints
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
+workload should have a toleration for it. As opposed to ResourceFlavor labels,
+Kueue will not add tolerations for the flavor taints.
+
+### A default ResourceFlavor
+
+If your cluster has homogeneous resources, or if you don't need to manage
+quotas for the different flavors of a resource separately, you can create a
+ResourceFlavor without any labels or taints. Such ResourceFlavor looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: default
+```
+
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
 same cohort can borrow unused quota from each other, if they have matching
 resource flavors.
```

  No, let me know if the update is better. Although I'm gonna expand the cohort concept in the next PR.

- **docs/tasks/administer_cluster_quotas.md:16** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:50:58Z
```diff
@@ -13,7 +13,7 @@ Make sure the following conditions are met:
 - The kubectl command-line tool has communication with your cluster.
 - [Kueue is installed](/README.md#installation).
 
-## Single ClusterQueue setup
+## Single ClusterQueue, single flavor setup {#single-queue-single-flavor}
```

  I think "single" is an easier concept to grasp without looking at the yaml first

- **docs/tasks/administer_cluster_quotas.md:19** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:52:03Z
```diff
@@ -13,7 +13,7 @@ Make sure the following conditions are met:
 - The kubectl command-line tool has communication with your cluster.
 - [Kueue is installed](/README.md#installation).
 
-## Single ClusterQueue setup
+## Single ClusterQueue, single flavor setup {#single-queue-single-flavor}
 
 In the following steps, you will create a queuing system with a single
 ClusterQueue to govern the quota of your cluster.
```

  Done

- **docs/tasks/administer_cluster_quotas.md:115** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:52:52Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
```

  Done

- **docs/tasks/administer_cluster_quotas.md:119** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:54:45Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
```

  Done, at the end of the section

- **docs/tasks/administer_cluster_quotas.md:139** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:59:24Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f flavor-x86.yaml flavor-arm.yaml
+```
+
```

  Reordered

- **docs/tasks/administer_cluster_quotas.md:165** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T17:59:32Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f flavor-x86.yaml flavor-arm.yaml
+```
+
+```yaml
+# flavor-x86.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: x86
+labels:
+  cpu-arch: x86
+```
+
+```yaml
+# flavor-arm.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: arm
+labels:
+  cpu-arch: arm
+```
+
+### 2. Create a ClusterQueue referencing the flavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
```

  Done

- **docs/tasks/administer_cluster_quotas.md:180** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T18:07:09Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f flavor-x86.yaml flavor-arm.yaml
+```
+
+```yaml
+# flavor-x86.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: x86
+labels:
+  cpu-arch: x86
+```
+
+```yaml
+# flavor-arm.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: arm
+labels:
+  cpu-arch: arm
+```
+
+### 2. Create a ClusterQueue referencing the flavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 9
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 84Gi
+```
+
+The flavor names in the fields `.spec.requestableResources[*].flavors[*].resourceFlavor`
+should match the names of the ResourceFlavors created earlier.
+
+Note that `memory` is referencing the `default` flavor created in the [single flavor setup](#single-queue-single-flavor).
```

  Oh yeah. It looks like github doesn't support custom IDs :cry:

- **docs/tasks/administer_cluster_quotas.md:212** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-01T18:12:07Z
```diff
@@ -107,6 +107,84 @@ spec:
   clusterQueue: cluster-total
 ```
 
+## Multiple ResourceFlavors setup
+
+You can define quotas for different [resource flavors](/docs/concepts/cluster_queue.md#resource-flavors).
+
+For the rest of this section, assume that your cluster has nodes with two CPU
+architectures, namely `x86` and `arm`, specified in the node label `cpu-arch`
+
+### 1. Create ResourceFlavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f flavor-x86.yaml flavor-arm.yaml
+```
+
+```yaml
+# flavor-x86.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: x86
+labels:
+  cpu-arch: x86
+```
+
+```yaml
+# flavor-arm.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ResourceFlavor
+metadata:
+  name: arm
+labels:
+  cpu-arch: arm
+```
+
+### 2. Create a ClusterQueue referencing the flavors
+
+To create the ResourceFlavors, run the following command:
+
+```shell
+kubectl apply -f cluster-total.yaml
+```
+
+```yaml
+# cluster-total.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: cluster-total
+spec:
+  namespaceSelector: {}
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 9
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 84Gi
+```
+
+The flavor names in the fields `.spec.requestableResources[*].flavors[*].resourceFlavor`
+should match the names of the ResourceFlavors created earlier.
+
+Note that `memory` is referencing the `default` flavor created in the [single flavor setup](#single-queue-single-flavor).
+This means that you don't want to distinguish if the memory is given from `x86`
+or `arm` nodes.
+
+**Warning**
+
+Using the same flavors in multiple resources is [not supported](https://github.com/kubernetes-sigs/kueue/issues/167).
+
```

  Done

### Review by [@cortespao](https://github.com/cortespao) (approved) — 2022-04-05T14:59:05Z
