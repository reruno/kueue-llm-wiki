# PR #1668: [multikueue] Job live status update

**Summary**: [multikueue] Job live status update

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1668

**Last updated**: 2024-02-13T20:01:04Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `multikueue-live-status-update` → `main`
- **Created**: 2024-01-29T14:20:06Z
- **Updated**: 2024-02-13T20:01:04Z
- **Merged**: 2024-02-06T15:33:25Z
- **Closed**: 2024-02-06T15:33:25Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 11   **+307 / -80**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add live status updates for multikueue jobs

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
MultiKueue: Add live status updates for multikueue JobSets
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-29T14:20:11Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 7a365020ca9c9547eaff60f7df058c95555e14c2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65c1fc05b89e4b00096d4220 |

### Comment by [@trasc](https://github.com/trasc) — 2024-02-01T13:20:11Z

/test pull-kueue-test-e2e-main-1-26

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T19:33:38Z

is this for jobs or jobsets?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-01T20:20:18Z

> is this for jobs or jobsets?

At first glance, this PR is for both job and jobset since this PR is extended job_adapter and jobset_adapter.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-05T19:01:49Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1668#pullrequestreview-1863576560" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1668#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-06T15:03:56Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-06T15:04:04Z

LGTM label has been added.  <details>Git tree hash: e6285fce52db704a8f0fe411394096ed7562d7a5</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-06T15:04:39Z

/hold cancel

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T20:01:02Z

/release-note-edit
```release-note
MultiKueue: Add live status updates for multikueue JobSets
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-30T17:25:22Z

Initial comment about the watch scope. I think we should have a label to make the filtering fast.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:153** by [@mimowo](https://github.com/mimowo) — 2024-01-30T17:23:32Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher)
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
```

  When doing the watcher for jobs please use a label to filter out jobs not-owner by MultiKueue. The analogous approach as we do for GC. This could be the same label reused. IMO no need for a different label key for Job and workload, because the intention is the same. Same label keys can be generally used on different objects.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T09:17:15Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@mimowo](https://github.com/mimowo) — 2024-02-01T09:17:14Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
```

  Why are we watching workloads, and not Jobs directly? If we are only watching workloads, how do we get status updates on the remote Job status update?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T09:18:29Z

- **test/integration/multikueue/multikueue_test.go:466** by [@mimowo](https://github.com/mimowo) — 2024-02-01T09:18:29Z
```diff
@@ -456,6 +456,20 @@ var _ = ginkgo.Describe("Multikueue", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("changing the status of the jobset in the worker, updates the manager's jobset status", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJobSet := jobset.JobSet{}
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, client.ObjectKeyFromObject(jobSet), &createdJobSet)).To(gomega.Succeed())
+				createdJobSet.Status.Restarts = 10
+				g.Expect(worker2TestCluster.client.Status().Update(worker2TestCluster.ctx, &createdJobSet)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
```

  I don't see yet the flow, could you explain. If in the code we watch for updates on the workloads, how do we get notification about the remote job status update (`createdJobSet.Status.Restarts = 10`)?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T09:25:59Z

- **pkg/controller/admissionchecks/multikueue/workload.go:280** by [@mimowo](https://github.com/mimowo) — 2024-02-01T09:24:30Z
```diff
@@ -276,7 +277,7 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Type:    kueue.WorkloadFinished,
 			Status:  metav1.ConditionTrue,
 			Reason:  remoteFinishedCond.Reason,
-			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
```

  Why changing this? it could be useful for users to know immediately which worker is processing the job.

- **test/e2e/multikueue/e2e_test.go:258** by [@mimowo](https://github.com/mimowo) — 2024-02-01T09:25:40Z
```diff
@@ -255,7 +255,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						Args:        []string{"2s"}, // give it the time to be observed Active in the live status update step.
```

  I would prefer to give it 10min, and the just terminate the pods, to avoid any risk for flakes.

- **test/e2e/multikueue/e2e_test.go:221** by [@mimowo](https://github.com/mimowo) — 2024-02-01T09:25:55Z
```diff
@@ -218,7 +218,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Type:    kueue.WorkloadFinished,
 						Status:  metav1.ConditionTrue,
 						Reason:  "JobFinished",
-						Message: `From remote "worker1": Job finished successfully`,
```

  Why changing this? It could be useful for users

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T12:57:46Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@trasc](https://github.com/trasc) — 2024-02-01T10:57:48Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
```

  we are also watching jobs that have an adapter implementing `multiKueueWatcher`.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:153** by [@trasc](https://github.com/trasc) — 2024-02-01T11:05:27Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher)
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
```

  Filter for having a prebuilt-workload lebel, for now, so I don't create dependency to the GC PR

- **pkg/controller/admissionchecks/multikueue/workload.go:280** by [@trasc](https://github.com/trasc) — 2024-02-01T11:09:06Z
```diff
@@ -276,7 +277,7 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Type:    kueue.WorkloadFinished,
 			Status:  metav1.ConditionTrue,
 			Reason:  remoteFinishedCond.Reason,
-			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
```

  There is a race condition between having the finish condition copied from the remote to the local workload and having the local job reconciler, as described in L161.
  
  It's tricky to work around this since we cannot fully relay on the local job controller to finish the workload since the remote workload can be marked as finished for other reasons that it's job ending.
  
  Another way  to keep the message prefix would be to sync the condition once more  at L250. WDYT?
  
  The message prefix has very little added value since the remote used will remain recorded in the admission check state.

- **test/e2e/multikueue/e2e_test.go:221** by [@trasc](https://github.com/trasc) — 2024-02-01T11:09:19Z
```diff
@@ -218,7 +218,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Type:    kueue.WorkloadFinished,
 						Status:  metav1.ConditionTrue,
 						Reason:  "JobFinished",
-						Message: `From remote "worker1": Job finished successfully`,
```

  same

- **test/integration/multikueue/multikueue_test.go:466** by [@trasc](https://github.com/trasc) — 2024-02-01T11:11:07Z
```diff
@@ -456,6 +456,20 @@ var _ = ginkgo.Describe("Multikueue", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("changing the status of the jobset in the worker, updates the manager's jobset status", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJobSet := jobset.JobSet{}
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, client.ObjectKeyFromObject(jobSet), &createdJobSet)).To(gomega.Succeed())
+				createdJobSet.Status.Restarts = 10
+				g.Expect(worker2TestCluster.client.Status().Update(worker2TestCluster.ctx, &createdJobSet)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
```

  We get a watch event for the remote job , from that we get its prebuilt workload name and trigger the reconcile for it.

- **test/e2e/multikueue/e2e_test.go:258** by [@trasc](https://github.com/trasc) — 2024-02-01T12:54:41Z
```diff
@@ -255,7 +255,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						Args:        []string{"2s"}, // give it the time to be observed Active in the live status update step.
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T13:07:42Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:153** by [@mimowo](https://github.com/mimowo) — 2024-02-01T13:07:42Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher)
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
```

  I see, but this means we may actually get events from another MultiKueue instance sharing worker, or other jobs using the prebuilt-workload. I would suggest to add a follow up to align this and use the origin label later on.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T13:14:52Z

- **pkg/controller/admissionchecks/multikueue/workload.go:280** by [@mimowo](https://github.com/mimowo) — 2024-02-01T13:14:52Z
```diff
@@ -276,7 +277,7 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Type:    kueue.WorkloadFinished,
 			Status:  metav1.ConditionTrue,
 			Reason:  remoteFinishedCond.Reason,
-			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
```

  > The message prefix has very little added value since the remote used will remain recorded in the admission check state.
  
  sgtm, in that case I think we could drop the prefix indeed in that case. However, I would like to make to ensure there is a way users can get to know the worker doing computations. Do we have an assert for this in e2e tests? Maybe extend this test to show the information is surfaced.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T13:18:07Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:153** by [@trasc](https://github.com/trasc) — 2024-02-01T13:18:07Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher)
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
```

  I'll align them in the last pr  I marge (this or GC)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T13:19:47Z

- **test/e2e/multikueue/e2e_test.go:258** by [@trasc](https://github.com/trasc) — 2024-02-01T13:19:47Z
```diff
@@ -255,7 +255,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						Args:        []string{"2s"}, // give it the time to be observed Active in the live status update step.
```

  looks like we have different behaviors for different k8s versions ... , may I should revert

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T13:23:41Z

- **test/e2e/multikueue/e2e_test.go:258** by [@mimowo](https://github.com/mimowo) — 2024-02-01T13:23:41Z
```diff
@@ -255,7 +255,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						Args:        []string{"2s"}, // give it the time to be observed Active in the live status update step.
```

  can you elaborate, what is the difference, why does it affect us?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T13:25:34Z

- **test/e2e/multikueue/e2e_test.go:258** by [@mimowo](https://github.com/mimowo) — 2024-02-01T13:25:34Z
```diff
@@ -255,7 +255,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						Args:        []string{"2s"}, // give it the time to be observed Active in the live status update step.
```

  If needed to unblock / move forward, change to 5s, we have another effort ticketed to get rid of the delays which could be used to clean this up. I prefer 5s for consistency, and 2s may be little on a loaded system.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T13:37:27Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@mimowo](https://github.com/mimowo) — 2024-02-01T13:37:26Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
```

  Hm, here: `err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})`?
  
  Why do we then actually watch both the workload and job? I would imagine we watch the job and update the local status. It is not clear to me why we watch both. Please elaborate / add comment.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T15:25:04Z

- **test/e2e/multikueue/e2e_test.go:258** by [@trasc](https://github.com/trasc) — 2024-02-01T15:25:04Z
```diff
@@ -255,7 +255,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						Args:        []string{"2s"}, // give it the time to be observed Active in the live status update step.
```

  I needed to change a couple of job template params , now it works  just like the Jobs in singlecluster e2e.
  
  The ugly part is that the same setup finishes the successfully in k8s 1.28 and 1.29 but with failure in 1.26 and 1.27.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T15:31:01Z

- **pkg/controller/admissionchecks/multikueue/workload.go:280** by [@trasc](https://github.com/trasc) — 2024-02-01T15:31:01Z
```diff
@@ -276,7 +277,7 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Type:    kueue.WorkloadFinished,
 			Status:  metav1.ConditionTrue,
 			Reason:  remoteFinishedCond.Reason,
-			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
```

  It is checked in e2e, integration and unit tests.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T15:33:59Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@trasc](https://github.com/trasc) — 2024-02-01T15:33:59Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
```

  We are reconciling workloads only, a job event triggers a reconcile  for its workload which will result in updating the status of the local job.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:14:30Z

- **test/e2e/multikueue/e2e_test.go:260** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:14:30Z
```diff
@@ -255,7 +255,9 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						// Give it the time to be observed Active in the live status update step.
+						// Delete all the pods once active state is detected.
+						Args: []string{"10s"},
```

  Make it high, like 10min. Two reasons:
  1. to clearly indicate the intention that we want to check something before finished
  2. to make sure the test would fail if the pods weren't actually deleted

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:18:40Z

- **test/e2e/multikueue/e2e_test.go:260** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:18:40Z
```diff
@@ -255,7 +255,9 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						// Give it the time to be observed Active in the live status update step.
+						// Delete all the pods once active state is detected.
+						Args: []string{"10s"},
```

  Again, I'm ok to set it to 5s for simplicity for now, if there are issues with 10min + deletion.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:26:26Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:153** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:26:26Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher)
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:28:13Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:28:13Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
```

  So, to confirm IIUC we watch jobs for the live status updates.
  
  Still, we also watch remote workloads to trigger local workload reconciliation. This is needed for example when the workload is finished.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:29:02Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:136** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:29:01Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
```

  Add temporary TODO to use the origin label once available, so that we don't forget.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:30:47Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:138** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:30:47Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
```

  I would say this is a hard error, to fail fast. Otherwise it may seem like working but the important feature of live updates will be discovered as broken late.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:31:04Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:140** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:31:03Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
```

  ```suggestion
  			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unable to start the watcher", "kind", kind)
  ```
  However, I think this is a hard error, as in the other comment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:33:47Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:157** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:33:47Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(3).Info("Starting watch")
+		defer log.V(3).Info("Watch ended")
```

  ```suggestion
  		log.V(2).Info("Starting watch")
  		defer log.V(2).Info("Watch ended")
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:36:43Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:87** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:36:42Z
```diff
@@ -76,17 +78,39 @@ func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithW
 	return client.NewWithWatch(restConfig, options)
 }
 
+type multiKueueWatcher interface {
+	// returns an empty list of objets
+	GetEmptyList() client.ObjectList
+	// returns the key of the workload of interest
+	// - the object name for workloads
+	// - the prebuilt workload for job types
+	GetEventsWorkloadKey(runtime.Object) (types.NamespacedName, error)
```

  ```suggestion
  	GetJobKey(runtime.Object) (types.NamespacedName, error)
  ```
  I think the Events is not helping here. Also, we return Job key.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:37:05Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:88** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:37:05Z
```diff
@@ -76,17 +78,39 @@ func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithW
 	return client.NewWithWatch(restConfig, options)
 }
 
+type multiKueueWatcher interface {
+	// returns an empty list of objets
+	GetEmptyList() client.ObjectList
+	// returns the key of the workload of interest
+	// - the object name for workloads
```

  I think for workloads it fails looking at the impl?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:43:16Z

- **test/e2e/multikueue/e2e_test.go:296** by [@mimowo](https://github.com/mimowo) — 2024-02-01T16:43:16Z
```diff
@@ -287,16 +289,36 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.By("Waiting for the jobSet to get status updates", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdJobset := &jobset.JobSet{}
+					g.Expect(k8sManagerClient.Get(ctx, client.ObjectKeyFromObject(jobSet), createdJobset)).To(gomega.Succeed())
+
+					g.Expect(createdJobset.Status.ReplicatedJobsStatus).To(gomega.BeComparableTo([]jobset.ReplicatedJobStatus{
```

  I'm wondering this could be rather hard to maintain so that it does not flake with kubernetes or JobSet changes. 
  
  No strong view, but I think it is enough to check a stable subset of fields, just to prove there was an update. We could say use opts to limit the compare just to these fields (or even a subset of them).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T16:44:23Z

First pass, looking good overall, we need to have the label to restrict the watch for Jobs. This could be a follow up, or we wait for the GC PR.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T07:35:41Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@trasc](https://github.com/trasc) — 2024-02-02T07:35:41Z
```diff
@@ -97,30 +120,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
```

  yes

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T08:26:24Z

- **test/e2e/multikueue/e2e_test.go:260** by [@trasc](https://github.com/trasc) — 2024-02-02T07:46:08Z
```diff
@@ -255,7 +255,9 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						// Give it the time to be observed Active in the live status update step.
+						// Delete all the pods once active state is detected.
+						Args: []string{"10s"},
```

  ok lets go 5s full execution

- **test/e2e/multikueue/e2e_test.go:296** by [@trasc](https://github.com/trasc) — 2024-02-02T07:46:16Z
```diff
@@ -287,16 +289,36 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
+			ginkgo.By("Waiting for the jobSet to get status updates", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					createdJobset := &jobset.JobSet{}
+					g.Expect(k8sManagerClient.Get(ctx, client.ObjectKeyFromObject(jobSet), createdJobset)).To(gomega.Succeed())
+
+					g.Expect(createdJobset.Status.ReplicatedJobsStatus).To(gomega.BeComparableTo([]jobset.ReplicatedJobStatus{
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:87** by [@trasc](https://github.com/trasc) — 2024-02-02T07:48:57Z
```diff
@@ -76,17 +78,39 @@ func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithW
 	return client.NewWithWatch(restConfig, options)
 }
 
+type multiKueueWatcher interface {
+	// returns an empty list of objets
+	GetEmptyList() client.ObjectList
+	// returns the key of the workload of interest
+	// - the object name for workloads
+	// - the prebuilt workload for job types
+	GetEventsWorkloadKey(runtime.Object) (types.NamespacedName, error)
```

  done, (the key will always be for a workload)

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:88** by [@trasc](https://github.com/trasc) — 2024-02-02T07:50:01Z
```diff
@@ -76,17 +78,39 @@ func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithW
 	return client.NewWithWatch(restConfig, options)
 }
 
+type multiKueueWatcher interface {
+	// returns an empty list of objets
+	GetEmptyList() client.ObjectList
+	// returns the key of the workload of interest
+	// - the object name for workloads
```

  I don't follow.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:136** by [@trasc](https://github.com/trasc) — 2024-02-02T07:51:56Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:138** by [@trasc](https://github.com/trasc) — 2024-02-02T07:55:09Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
```

  ok, let's stop here then.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:140** by [@trasc](https://github.com/trasc) — 2024-02-02T07:55:15Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:157** by [@trasc](https://github.com/trasc) — 2024-02-02T07:55:53Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(3).Info("Starting watch")
+		defer log.V(3).Info("Watch ended")
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T08:28:10Z

- **test/e2e/multikueue/e2e_test.go:258** by [@trasc](https://github.com/trasc) — 2024-02-02T08:28:10Z
```diff
@@ -255,7 +255,7 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						Args:        []string{"2s"}, // give it the time to be observed Active in the live status update step.
```

  reverted

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T09:12:02Z

- **test/integration/multikueue/multikueue_test.go:466** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:12:02Z
```diff
@@ -456,6 +456,20 @@ var _ = ginkgo.Describe("Multikueue", func() {
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 
+		ginkgo.By("changing the status of the jobset in the worker, updates the manager's jobset status", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdJobSet := jobset.JobSet{}
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, client.ObjectKeyFromObject(jobSet), &createdJobSet)).To(gomega.Succeed())
+				createdJobSet.Status.Restarts = 10
+				g.Expect(worker2TestCluster.client.Status().Update(worker2TestCluster.ctx, &createdJobSet)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
```

  gotcha, we will prefer to use the origin label ultimately

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T09:55:42Z

Almost LGTM, just nits + we need to migrate to the origin label.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:88** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:40:09Z
```diff
@@ -76,17 +78,39 @@ func newClientWithWatch(kubeconfig []byte, options client.Options) (client.WithW
 	return client.NewWithWatch(restConfig, options)
 }
 
+type multiKueueWatcher interface {
+	// returns an empty list of objets
+	GetEmptyList() client.ObjectList
+	// returns the key of the workload of interest
+	// - the object name for workloads
```

  Oh, sorry, I got confused by the same function implemented both for Workloads and Jobs. I looked at the Jobs implementations and thought it would error for Workloads. 
  
  Now I see what you do, though the extra layer of indirection makes it slightly hard to follow, but I see there are trade-offs, and this way you can commonize the code for Workloads and Jobs.

- **pkg/controller/admissionchecks/multikueue/workload.go:280** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:45:58Z
```diff
@@ -276,7 +277,7 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Type:    kueue.WorkloadFinished,
 			Status:  metav1.ConditionTrue,
 			Reason:  remoteFinishedCond.Reason,
-			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
```

  I see, it is here in e2e tests: https://github.com/kubernetes-sigs/kueue/blob/a8c11f351858cd159a3f823b15860918c0490cbc/test/e2e/multikueue/e2e_test.go#L208
  
  In that case, I'm ok to revmove it from the condition message.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:161** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:49:49Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(3).Info("Starting watch")
+		defer log.V(3).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetEventsWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key")
```

  Log the namespace/name for the object for which we got the error, so that we can identify it

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@mimowo](https://github.com/mimowo) — 2024-02-02T09:55:17Z
```diff
@@ -98,30 +122,65 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
```

  Add the comment here as well to use the origin label when introduced

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T10:45:03Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@trasc](https://github.com/trasc) — 2024-02-02T10:35:26Z
```diff
@@ -98,30 +122,65 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:161** by [@trasc](https://github.com/trasc) — 2024-02-02T10:43:07Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(3).Info("Starting watch")
+		defer log.V(3).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetEventsWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key")
```

  `r.Object` is a runtime object and is not exposing the name, however I made it part of the error message for JobSet.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T10:56:50Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:161** by [@mimowo](https://github.com/mimowo) — 2024-02-02T10:56:50Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(3).Info("Starting watch")
+		defer log.V(3).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetEventsWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key")
```

  Ok, it is acceptable if there is no easy way to know the name / namespace of the object here. 
  
  However, if possible I prefer to log it here so that it does not need to be done per adapter. Let see what are the options to make it generic.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T11:01:04Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:161** by [@mimowo](https://github.com/mimowo) — 2024-02-02T11:01:04Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(3).Info("Starting watch")
+		defer log.V(3).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetEventsWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key")
```

  I guess we could at least log its gvk in a generic way.
  Maybe we could also cast it on a Object containing name / namespace

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-02T11:31:17Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:161** by [@trasc](https://github.com/trasc) — 2024-02-02T11:31:17Z
```diff
@@ -98,30 +122,63 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function
+			// with only thw wl watcher, just log the error.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unaable to start the watcher", "kind", kind)
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(3).Info("Starting watch")
+		defer log.V(3).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetEventsWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key")
```

  gvk added

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-05T19:01:41Z

/approve
/hold
for comments
Leaving LGTM to @mimowo

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T18:52:30Z
```diff
@@ -30,14 +31,20 @@ type batchJobAdapter struct{}
 
 var _ jobAdapter = (*batchJobAdapter)(nil)
 
-func (b *batchJobAdapter) CreateRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error {
+func (b *batchJobAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error {
```

  Add a comment about what this SyncJob function does and does not do.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:141** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T18:55:19Z
```diff
@@ -98,30 +122,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function with only the wl watcher.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unable to start the watcher", "kind", kind)
```

  ```suggestion
  			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "Unable to start the watcher", "kind", kind)
  ```

- **pkg/controller/admissionchecks/multikueue/workload.go:68** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T18:58:11Z
```diff
@@ -61,8 +61,9 @@ type wlReconciler struct {
 var _ reconcile.Reconciler = (*wlReconciler)(nil)
 
 type jobAdapter interface {
-	// Creates the Job object in the worker cluster using remote client.
-	CreateRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error
+	// Creates the Job object in the worker cluster using remote client, if not already created.
+	// Copy the status from the remote job if already exists.
+	SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error
 	// Copy the status from the job in the worker cluster to the local one.
 	CopyStatusRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName) error
```

  Can we get rid of this function? It sounds pretty similar to SyncJob

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:167** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T18:59:26Z
```diff
@@ -98,30 +122,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function with only the wl watcher.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unable to start the watcher", "kind", kind)
+			// however let's not accept this for now.
+			return err
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(2).Info("Starting watch")
+		defer log.V(2).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
+			} else {
+				rc.queueWorkloadEvent(ctx, wlKey)
```

  there might be an opportunity to use `EnqueueAfter` here, but we can leave it for a follow up.

- **test/e2e/multikueue/e2e_test.go:259** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-05T19:01:09Z
```diff
@@ -255,7 +255,8 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						// Give it the time to be observed Active in the live status update step.
+						Args: []string{"5s"},
```

  Can it be less? Maybe 3s?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-06T07:06:04Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:34** by [@trasc](https://github.com/trasc) — 2024-02-06T06:21:25Z
```diff
@@ -30,14 +31,20 @@ type batchJobAdapter struct{}
 
 var _ jobAdapter = (*batchJobAdapter)(nil)
 
-func (b *batchJobAdapter) CreateRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error {
+func (b *batchJobAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error {
```

  It is described at interface level, the batch job specifics are described inline.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:141** by [@trasc](https://github.com/trasc) — 2024-02-06T06:23:28Z
```diff
@@ -98,30 +122,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function with only the wl watcher.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unable to start the watcher", "kind", kind)
```

  done

- **pkg/controller/admissionchecks/multikueue/workload.go:68** by [@trasc](https://github.com/trasc) — 2024-02-06T06:25:50Z
```diff
@@ -61,8 +61,9 @@ type wlReconciler struct {
 var _ reconcile.Reconciler = (*wlReconciler)(nil)
 
 type jobAdapter interface {
-	// Creates the Job object in the worker cluster using remote client.
-	CreateRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error
+	// Creates the Job object in the worker cluster using remote client, if not already created.
+	// Copy the status from the remote job if already exists.
+	SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error
 	// Copy the status from the job in the worker cluster to the local one.
 	CopyStatusRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName) error
```

  done

- **test/e2e/multikueue/e2e_test.go:259** by [@trasc](https://github.com/trasc) — 2024-02-06T06:27:00Z
```diff
@@ -255,7 +255,8 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						// Give it the time to be observed Active in the live status update step.
+						Args: []string{"5s"},
```

  It used to, it's 5s to avoid potential flaky behavior.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:167** by [@trasc](https://github.com/trasc) — 2024-02-06T07:05:57Z
```diff
@@ -98,30 +122,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function with only the wl watcher.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unable to start the watcher", "kind", kind)
+			// however let's not accept this for now.
+			return err
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(2).Info("Starting watch")
+		defer log.V(2).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
+			} else {
+				rc.queueWorkloadEvent(ctx, wlKey)
```

  In fact it needs to be done in workload.go, but let's keep it as is for now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-06T08:37:09Z

- **test/e2e/multikueue/e2e_test.go:259** by [@mimowo](https://github.com/mimowo) — 2024-02-06T08:37:09Z
```diff
@@ -255,7 +255,8 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						// Give it the time to be observed Active in the live status update step.
+						Args: []string{"5s"},
```

  Yeah, I suggested 5s for consistency with other places. Ideally, I would like not to have 10min timeout, and once the checks are done, then finish the job, but this is ticketed under https://github.com/kubernetes-sigs/kueue/issues/1449

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-06T08:38:43Z

- **pkg/controller/admissionchecks/multikueue/workload.go:68** by [@mimowo](https://github.com/mimowo) — 2024-02-06T08:38:43Z
```diff
@@ -61,8 +61,9 @@ type wlReconciler struct {
 var _ reconcile.Reconciler = (*wlReconciler)(nil)
 
 type jobAdapter interface {
-	// Creates the Job object in the worker cluster using remote client.
-	CreateRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error
+	// Creates the Job object in the worker cluster using remote client, if not already created.
+	// Copy the status from the remote job if already exists.
+	SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error
 	// Copy the status from the job in the worker cluster to the local one.
 	CopyStatusRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName) error
```

  nice

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-06T08:53:48Z

LGTM, just some nits / quesiton

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@mimowo](https://github.com/mimowo) — 2024-02-06T08:44:32Z
```diff
@@ -102,30 +125,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
```

  Remove the TODO since the code already is using the label

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:140** by [@mimowo](https://github.com/mimowo) — 2024-02-06T08:44:56Z
```diff
@@ -102,30 +125,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
```

  Remove TODO

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:53** by [@mimowo](https://github.com/mimowo) — 2024-02-06T08:50:53Z
```diff
@@ -30,14 +32,41 @@ type batchJobAdapter struct{}
 
 var _ jobAdapter = (*batchJobAdapter)(nil)
 
-func (b *batchJobAdapter) CreateRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error {
+func (b *batchJobAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
 	localJob := batchv1.Job{}
 	err := localClient.Get(ctx, key, &localJob)
 	if err != nil {
 		return err
 	}
 
-	remoteJob := batchv1.Job{
+	remoteJob := batchv1.Job{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// the remote job exists
+	if err == nil {
+		// This will no longer be necessary when batchJob will support live status update, by then
+		// we should only sync the Status of the job if it's "Finished".
+		remoteFinished := false
+		for _, c := range remoteJob.Status.Conditions {
```

  nit: Please use utility function (IsStatusConditionTrue) instead of looping here

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:167** by [@mimowo](https://github.com/mimowo) — 2024-02-06T08:53:37Z
```diff
@@ -98,30 +122,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function with only the wl watcher.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unable to start the watcher", "kind", kind)
+			// however let's not accept this for now.
+			return err
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(2).Info("Starting watch")
+		defer log.V(2).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
+			} else {
+				rc.queueWorkloadEvent(ctx, wlKey)
```

  IIUC the purpose is to batch multiple workload updates by time? Is the refactoring planned as one of the follow ups?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-06T09:34:11Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:128** by [@trasc](https://github.com/trasc) — 2024-02-06T09:28:10Z
```diff
@@ -102,30 +125,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:140** by [@trasc](https://github.com/trasc) — 2024-02-06T09:28:18Z
```diff
@@ -102,30 +125,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
```

  done

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:167** by [@trasc](https://github.com/trasc) — 2024-02-06T09:28:44Z
```diff
@@ -98,30 +122,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function with only the wl watcher.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unable to start the watcher", "kind", kind)
+			// however let's not accept this for now.
+			return err
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(2).Info("Starting watch")
+		defer log.V(2).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
+			} else {
+				rc.queueWorkloadEvent(ctx, wlKey)
```

  I guess it could be added in the follow-up list.

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:53** by [@trasc](https://github.com/trasc) — 2024-02-06T09:32:24Z
```diff
@@ -30,14 +32,41 @@ type batchJobAdapter struct{}
 
 var _ jobAdapter = (*batchJobAdapter)(nil)
 
-func (b *batchJobAdapter) CreateRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error {
+func (b *batchJobAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
 	localJob := batchv1.Job{}
 	err := localClient.Get(ctx, key, &localJob)
 	if err != nil {
 		return err
 	}
 
-	remoteJob := batchv1.Job{
+	remoteJob := batchv1.Job{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// the remote job exists
+	if err == nil {
+		// This will no longer be necessary when batchJob will support live status update, by then
+		// we should only sync the Status of the job if it's "Finished".
+		remoteFinished := false
+		for _, c := range remoteJob.Status.Conditions {
```

  these are not metav1.Conditions

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-06T14:19:03Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:167** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T14:19:03Z
```diff
@@ -98,30 +122,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function with only the wl watcher.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unable to start the watcher", "kind", kind)
+			// however let's not accept this for now.
+			return err
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(2).Info("Starting watch")
+		defer log.V(2).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
+			} else {
+				rc.queueWorkloadEvent(ctx, wlKey)
```

  Yes, please. Add it to the follow up list.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-06T14:19:27Z

- **test/e2e/multikueue/e2e_test.go:259** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T14:19:26Z
```diff
@@ -255,7 +255,8 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 						Parallelism: 2,
 						Completions: 2,
 						Image:       "gcr.io/k8s-staging-perf-tests/sleep:v0.1.0",
-						Args:        []string{"1ms"},
+						// Give it the time to be observed Active in the live status update step.
+						Args: []string{"5s"},
```

  sg

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-06T14:24:54Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:167** by [@trasc](https://github.com/trasc) — 2024-02-06T14:24:54Z
```diff
@@ -98,30 +122,66 @@ func (rc *remoteClient) setConfig(watchCtx context.Context, kubeconfig []byte) e
 	}
 	rc.client = remoteClient
 
-	newWatcher, err := rc.client.Watch(watchCtx, &kueue.WorkloadList{})
+	// TODO: Filter based on multikueue-origin label when available.
+	err = rc.startWatcher(watchCtx, kueue.GroupVersion.WithKind("Workload").GroupKind().String(), &workloadKueueWatcher{})
 	if err != nil {
-		return nil
+		return err
+	}
+
+	// add a watch for all the adapters implementing multiKueueWatcher
+	for kind, adapter := range adapters {
+		watcher, implementsWatcher := adapter.(multiKueueWatcher)
+		if !implementsWatcher {
+			continue
+		}
+		// TODO: Filter based on multikueue-origin label when available.
+		err := rc.startWatcher(watchCtx, kind, watcher, client.HasLabels{constants.PrebuiltWorkloadLabel})
+		if err != nil {
+			// not being able to setup a watcher is not ideal but we can function with only the wl watcher.
+			ctrl.LoggerFrom(watchCtx).V(2).Error(err, "unable to start the watcher", "kind", kind)
+			// however let's not accept this for now.
+			return err
+		}
+	}
+
+	rc.kubeconfig = kubeconfig
+	return nil
+}
+
+func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKueueWatcher, listopts ...client.ListOption) error {
+	log := ctrl.LoggerFrom(ctx).WithValues("watchKind", kind)
+	newWatcher, err := rc.client.Watch(ctx, w.GetEmptyList())
+	if err != nil {
+		return err
 	}
 
 	go func() {
+		log.V(2).Info("Starting watch")
+		defer log.V(2).Info("Watch ended")
 		for r := range newWatcher.ResultChan() {
-			rc.queueWorkloadEvent(watchCtx, r)
+			wlKey, err := w.GetWorkloadKey(r.Object)
+			if err != nil {
+				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
+			} else {
+				rc.queueWorkloadEvent(ctx, wlKey)
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-06T15:03:42Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:53** by [@mimowo](https://github.com/mimowo) — 2024-02-06T15:03:42Z
```diff
@@ -30,14 +32,41 @@ type batchJobAdapter struct{}
 
 var _ jobAdapter = (*batchJobAdapter)(nil)
 
-func (b *batchJobAdapter) CreateRemoteObject(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName string) error {
+func (b *batchJobAdapter) SyncJob(ctx context.Context, localClient client.Client, remoteClient client.Client, key types.NamespacedName, workloadName, origin string) error {
 	localJob := batchv1.Job{}
 	err := localClient.Get(ctx, key, &localJob)
 	if err != nil {
 		return err
 	}
 
-	remoteJob := batchv1.Job{
+	remoteJob := batchv1.Job{}
+	err = remoteClient.Get(ctx, key, &remoteJob)
+	if client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	// the remote job exists
+	if err == nil {
+		// This will no longer be necessary when batchJob will support live status update, by then
+		// we should only sync the Status of the job if it's "Finished".
+		remoteFinished := false
+		for _, c := range remoteJob.Status.Conditions {
```

  sg
