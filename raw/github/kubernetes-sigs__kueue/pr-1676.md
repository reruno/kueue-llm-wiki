# PR #1676: Fix Kueue startup by waiting for webhooks server using probes

**Summary**: Fix Kueue startup by waiting for webhooks server using probes

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1676

**Last updated**: 2024-02-10T11:47:50Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `wait-for-webhooks-using-probes` → `main`
- **Created**: 2024-01-31T08:48:28Z
- **Updated**: 2024-02-10T11:47:50Z
- **Merged**: 2024-02-01T20:26:50Z
- **Closed**: 2024-02-01T20:26:50Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 6   **+13 / -32**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

To fix the flaky test.
To also let users wait for Kueue webhooks ready without the need for creating / deleting objects as in `KueueReadyForTesting`

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1658

#### Special notes for your reviewer:

Tested locally, trying to see what will happen on GH CI.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Kueue replicas are advertised as Ready only once the webhooks are functional.

This allows users to wait with the first requests until the Kueue deployment is available, so that the 
early requests don't fail.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-01-31T08:48:34Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d2e420bee8dc7030d4258cc8fe12a6be86c3f1ed |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65bbadbc7ccacf0009d468e2 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-31T09:04:55Z

/cc @trasc @alculquicondor

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-31T09:26:13Z

All green, retrying to get more signal:
/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-01-31T10:04:55Z

/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-01-31T10:24:11Z

/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-31T12:47:21Z

All green, retrying to get more signal:
/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@mimowo](https://github.com/mimowo) — 2024-01-31T13:00:16Z

All green, retrying to get more signal:
/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-01T19:45:32Z

LGTM label has been added.  <details>Git tree hash: 672838e0922a62f82b328cd35c211a88005ba5c6</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-01T19:45:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1676#pullrequestreview-1857465413" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1676#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T19:45:42Z

/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T19:45:52Z

/hold cancel

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-02T08:27:44Z

@alculquicondor @tenzen-y 
I have added a release note, because this is actually a user-facing change IMO. Previously it was really hard to tell when Kueue can accept early requests, especially in the HA mode. Should we consider cherry-picking?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-02T11:06:32Z

> @alculquicondor @tenzen-y I have added a release note, because this is actually a user-facing change IMO. Previously it was really hard to tell when Kueue can accept early requests, especially in the HA mode. Should we consider cherry-picking?

I'm fine with cherry-picking.
This patch would be helpful for both HA and non-HA users. Without this patch, we need to manually check if the manager has started since the readinessprove server starts in spite of not being ready.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-02T17:19:12Z

+1 on cherry pick

On Fri, Feb 2, 2024, 3:06 a.m. Yuki Iwai ***@***.***> wrote:

> @alculquicondor <https://github.com/alculquicondor> @tenzen-y
> <https://github.com/tenzen-y> I have added a release note, because this
> is actually a user-facing change IMO. Previously it was really hard to tell
> when Kueue can accept early requests, especially in the HA mode. Should we
> consider cherry-picking?
>
> I'm fine with cherry-picking.
> This patch would be helpful for both HA and non-HA users. Without this
> patch, we need to manually check if the manager has started since the
> readinessprove server starts in spite of not being ready.
>
> —
> Reply to this email directly, view it on GitHub
> <https://github.com/kubernetes-sigs/kueue/pull/1676#issuecomment-1923582638>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/AAJ5E6GLLY7GQNNQ4DFVGCDYRTCEJAVCNFSM6AAAAABCSUVZYGVHI2DSMVQWIX3LMV43OSLTON2WKQ3PNVWWK3TUHMYTSMRTGU4DENRTHA>
> .
> You are receiving this because you were mentioned.Message ID:
> ***@***.***>
>

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T10:12:33Z

- **cmd/kueue/main.go:285** by [@trasc](https://github.com/trasc) — 2024-01-31T10:05:45Z
```diff
@@ -274,11 +274,15 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 func setupProbeEndpoints(mgr ctrl.Manager) {
 	defer setupLog.Info("Probe endpoints are configured on healthz and readyz")
 
-	if err := mgr.AddHealthzCheck("healthz", healthz.Ping); err != nil {
+	readyz := func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}
+
+	if err := mgr.AddHealthzCheck("healthz", readyz); err != nil {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", readyz); err != nil {
```

  ```suggestion
  	if err := mgr.AddReadyzCheck("readyz",  mgr.GetWebhookServer().StartedChecker()); err != nil {
  ```

- **cmd/kueue/main.go:280** by [@trasc](https://github.com/trasc) — 2024-01-31T10:06:31Z
```diff
@@ -274,11 +274,15 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 func setupProbeEndpoints(mgr ctrl.Manager) {
 	defer setupLog.Info("Probe endpoints are configured on healthz and readyz")
 
-	if err := mgr.AddHealthzCheck("healthz", healthz.Ping); err != nil {
+	readyz := func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}
+
```

  ```suggestion
  ```

- **cmd/kueue/main.go:281** by [@trasc](https://github.com/trasc) — 2024-01-31T10:07:21Z
```diff
@@ -274,11 +274,15 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 func setupProbeEndpoints(mgr ctrl.Manager) {
 	defer setupLog.Info("Probe endpoints are configured on healthz and readyz")
 
-	if err := mgr.AddHealthzCheck("healthz", healthz.Ping); err != nil {
+	readyz := func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}
+
+	if err := mgr.AddHealthzCheck("healthz", readyz); err != nil {
```

  why change the `healthz`?

- **hack/e2e-common.sh:58** by [@trasc](https://github.com/trasc) — 2024-01-31T10:12:20Z
```diff
@@ -55,5 +55,6 @@ function cluster_kueue_deploy {
     else
         kubectl apply --server-side -k test/e2e/config
     fi
+    kubectl wait --for=condition=available --timeout=3m deployment/kueue-controller-manager -n kueue-system
```

  Can we do this somewhere else ?  
  
  It could help if in case of multikue , we deploy in manager, and instead of waiting to be ready now, we also deploy in the workers , and check the availability later on for all of them.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T10:27:37Z

- **hack/e2e-common.sh:58** by [@mimowo](https://github.com/mimowo) — 2024-01-31T10:27:37Z
```diff
@@ -55,5 +55,6 @@ function cluster_kueue_deploy {
     else
         kubectl apply --server-side -k test/e2e/config
     fi
+    kubectl wait --for=condition=available --timeout=3m deployment/kueue-controller-manager -n kueue-system
```

  I see, this is a simple way of waiting for Kueue ready. I imagine users would like to use in their setup scripts. Typically it takes a couple of seconds.
  
  One alternative is to revert the removal of  `KueueReadyForTesting`, but I don't like it, because it recreates objects, this takes time and could cover some issues (as it is actually currently in case of single cluster). Actually, in this PR I wanted to remove it to test that it is not necessary.
  
  Other alternatives I see modify `KueueReadyForTesting` to check the deployment status as this line, or run the entire setup of different clusters for MK in parallel at the script level.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T10:28:24Z

- **cmd/kueue/main.go:281** by [@mimowo](https://github.com/mimowo) — 2024-01-31T10:28:24Z
```diff
@@ -274,11 +274,15 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 func setupProbeEndpoints(mgr ctrl.Manager) {
 	defer setupLog.Info("Probe endpoints are configured on healthz and readyz")
 
-	if err := mgr.AddHealthzCheck("healthz", healthz.Ping); err != nil {
+	readyz := func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}
+
+	if err := mgr.AddHealthzCheck("healthz", readyz); err != nil {
```

  I'm not sure the change to healthz is required. I will test if this can be simplified. Do you know what healthz is used for?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T10:45:47Z

- **cmd/kueue/main.go:281** by [@trasc](https://github.com/trasc) — 2024-01-31T10:45:47Z
```diff
@@ -274,11 +274,15 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 func setupProbeEndpoints(mgr ctrl.Manager) {
 	defer setupLog.Info("Probe endpoints are configured on healthz and readyz")
 
-	if err := mgr.AddHealthzCheck("healthz", healthz.Ping); err != nil {
+	readyz := func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}
+
+	if err := mgr.AddHealthzCheck("healthz", readyz); err != nil {
```

  it should indicate if the container is running, https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#types-of-probe

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T12:26:13Z

- **cmd/kueue/main.go:281** by [@mimowo](https://github.com/mimowo) — 2024-01-31T12:26:13Z
```diff
@@ -274,11 +274,15 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 func setupProbeEndpoints(mgr ctrl.Manager) {
 	defer setupLog.Info("Probe endpoints are configured on healthz and readyz")
 
-	if err := mgr.AddHealthzCheck("healthz", healthz.Ping); err != nil {
+	readyz := func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}
+
+	if err := mgr.AddHealthzCheck("healthz", readyz); err != nil {
```

  Good call, reverted setting it, going to test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T12:26:41Z

- **cmd/kueue/main.go:280** by [@mimowo](https://github.com/mimowo) — 2024-01-31T12:26:41Z
```diff
@@ -274,11 +274,15 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 func setupProbeEndpoints(mgr ctrl.Manager) {
 	defer setupLog.Info("Probe endpoints are configured on healthz and readyz")
 
-	if err := mgr.AddHealthzCheck("healthz", healthz.Ping); err != nil {
+	readyz := func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}
+
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T12:26:49Z

- **cmd/kueue/main.go:285** by [@mimowo](https://github.com/mimowo) — 2024-01-31T12:26:49Z
```diff
@@ -274,11 +274,15 @@ func setupControllers(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manag
 func setupProbeEndpoints(mgr ctrl.Manager) {
 	defer setupLog.Info("Probe endpoints are configured on healthz and readyz")
 
-	if err := mgr.AddHealthzCheck("healthz", healthz.Ping); err != nil {
+	readyz := func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}
+
+	if err := mgr.AddHealthzCheck("healthz", readyz); err != nil {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", readyz); err != nil {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T12:27:11Z

- **cmd/kueue/main.go:292** by [@trasc](https://github.com/trasc) — 2024-01-31T12:27:11Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  ```suggestion
  	if err := mgr.AddReadyzCheck("readyz",mgr.GetWebhookServer().StartedChecker()); err != nil {
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T12:39:13Z

- **cmd/kueue/main.go:292** by [@trasc](https://github.com/trasc) — 2024-01-31T12:39:13Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  or could the `GetWebhookServer()` return different servers on different calls?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T12:47:09Z

- **cmd/kueue/main.go:292** by [@mimowo](https://github.com/mimowo) — 2024-01-31T12:47:09Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  I cannot do this, then the replicas never get ready. The function needs to get the webhook server once ready. I think the webhook server is set asynchronously in controller-runtime.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T13:09:54Z

- **cmd/kueue/main.go:292** by [@trasc](https://github.com/trasc) — 2024-01-31T13:09:54Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  .... very strange, maybe the cert manager is is doing it ... 
  maybe add a comment about this if confirmed.
  
  all other references in sigs are using it directly. 
  https://github.com/search?q=org%3Akubernetes-sigs%20StartedChecker&type=code

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T15:29:29Z

- **cmd/kueue/main.go:292** by [@mimowo](https://github.com/mimowo) — 2024-01-31T15:29:29Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  I think it is indeed about the certs not being ready. One of the projects actually solves this problem too: https://github.com/kubernetes-sigs/hierarchical-namespaces/blob/d367fc08a261135b63d22aeb01c688317d9b7e02/cmd/manager/main.go#L296-L307.
  
  Here they wait explicitly for the certs to be ready, I guess we could do the same so that we don't get the transient errors logged. WDYT?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-01-31T16:11:06Z

- **cmd/kueue/main.go:292** by [@trasc](https://github.com/trasc) — 2024-01-31T16:11:06Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  > , I guess we could do the same so that we don't get the transient errors logged.
  
  What kind of errors? 
  
  `GetWebhookServer().StartedChecker` should be the way to go since it actually attempts to connect the server.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-31T16:33:29Z

- **cmd/kueue/main.go:292** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-31T16:33:29Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  > I guess we could do the same so that we don't get the transient errors logged. WDYT?
  
  I agree with you. We probably could use this:
  
  https://github.com/kubernetes-sigs/kueue/blob/0b0961db512b5ee91047eaba6ff88694e46ff8c3/pkg/util/cert/cert.go#L70
  
  > GetWebhookServer().StartedChecker should be the way to go since it actually attempts to connect the server.
  
  IIUC, The `GetWebhookServer().StartedChecker` returns 200 only if the checker gets a positive response with tls from the webhook servers. So, we need to wait to be ready for webhook servers, which means we need to wait to be ready for certs.
  
  In the case of `healthz.Ping` (our current checker), it always returns `true` since it doesn't check whether or not the webhook servers can actually work fine.
  
  - `StartedChecker` : https://github.com/kubernetes-sigs/controller-runtime/blob/8475c55f3e00e5611de0880eccd785efa85e8e38/pkg/webhook/server.go#L271-L273
  - `healthz.Ping()`: https://github.com/kubernetes-sigs/controller-runtime/blob/8475c55f3e00e5611de0880eccd785efa85e8e38/pkg/healthz/healthz.go#L186-L187

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-01-31T17:01:56Z

- **cmd/kueue/main.go:292** by [@mimowo](https://github.com/mimowo) — 2024-01-31T17:01:56Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  I was thinking about the transient errors before the webhooks is ready, but they are very nice anyway:
  ```
  2024-01-31T13:09:29.832263236Z stderr F 2024-01-31T13:09:29.832081013Z	DEBUG	controller-runtime.healthz	healthz/healthz.go:60	healthz check failed	{"checker": "readyz", "error": "webhook server has not been started yet"}
  ```
  Maybe it doesn't actually make sense to wait in the checker for ready certs, because it does not attempt to connect if the started=false: https://github.com/kubernetes-sigs/controller-runtime/blob/8475c55f3e00e5611de0880eccd785efa85e8e38/pkg/webhook/server.go#L281C9-L281C16
  
  And it changes started=true only after getting the certs: https://github.com/kubernetes-sigs/controller-runtime/blob/8475c55f3e00e5611de0880eccd785efa85e8e38/pkg/webhook/server.go#L261 (just before listening)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-01-31T17:57:55Z

- **cmd/kueue/main.go:292** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-31T17:56:39Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  GetWebhookServer probably doesn't return a meaningful server until the first webhooks is registered, which only happens after the certs are ready.
  
  Just leave a comment for it.

- **hack/e2e-common.sh:58** by [@alculquicondor](https://github.com/alculquicondor) — 2024-01-31T17:57:45Z
```diff
@@ -55,5 +55,6 @@ function cluster_kueue_deploy {
     else
         kubectl apply --server-side -k test/e2e/config
     fi
+    kubectl wait --for=condition=available --timeout=3m deployment/kueue-controller-manager -n kueue-system
```

  +1 on changing `KueueReadyForTesting` with an availability check for the deployment.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-01-31T18:14:25Z

- **cmd/kueue/main.go:292** by [@tenzen-y](https://github.com/tenzen-y) — 2024-01-31T18:14:25Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  > Maybe it doesn't actually make sense to wait in the checker for ready certs, because it does not attempt to connect if the started=false
  
  I think that waiting for certs to get ready makes sense since the  `StartedChecker` returns a meaningful server only after the certs are ready:  https://github.com/kubernetes-sigs/controller-runtime/blob/8475c55f3e00e5611de0880eccd785efa85e8e38/pkg/webhook/server.go#L237-L240

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T07:37:36Z

- **cmd/kueue/main.go:292** by [@trasc](https://github.com/trasc) — 2024-02-01T07:37:36Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  `GetWebhookServer().StartedChecker` is what we need to determine the readiness.
  
  I'm only not sure why we need a closure for calling it , since from what I can see the field  holding the webhook is private  and only stable whit an option when crating the manager,  however it's not a huge problem.
  
  and
  
  >>, I guess we could do the same so that we don't get the transient errors logged.
  
  >What kind of errors?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T08:25:35Z

- **cmd/kueue/main.go:292** by [@mimowo](https://github.com/mimowo) — 2024-02-01T08:25:35Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  > `GetWebhookServer().StartedChecker` is what we need to determine the readiness.
  > 
  > I'm only not sure why we need a closure for calling it , since from what I can see the field holding the webhook is private and only stable whit an option when crating the manager, however it's not a huge problem.
  > 
  
  TBH, I'm not sure why either. What I verified experimentally is that if I just call `GetWebhookServer` before `WaitForCertsReady` then the webhook is not initialized, and it stays that way. 
  
  I get the following logs then even if keeping the readyz probe like currently, but just invoking `GetWebookServer()` before registering the probe:
  
  ```
  2024-01-31T19:49:50.466048439Z	INFO	setup	kueue/main.go:291	readyz	{"ws": {"Options":{"Host":"","Port":9443,"CertDir":"/tmp/k8s-webhook-server/serving-certs","CertName":"tls.crt","KeyName":"tls.key","ClientCAName":"","TLSOpts":null,"WebhookMux":{}}}, "err": "webhook server has not been started yet"}
  ```
  
  I suspect this has something to do with the one-time memorization of the not initialized webhook as runnable here: https://github.com/kubernetes-sigs/controller-runtime/blob/73519a939e27c8465c6c49694356cbd66daf1677/pkg/manager/internal.go#L258.
  
  However, I'm not sure of the details. I would need to further investigate probably with debugger. Let me know if you have some ideas how to get to the bottom of it.
  
  Let me put in the comment what I know so far.
  
  > and
  > 
  > > > , I guess we could do the same so that we don't get the transient errors logged.
  > 
  > > What kind of errors?
  
  I meant transient errors before the webhook server is listening, but I didn't how the look like in practice before writing. I was thinking maybe we have many errors about certs not ready. For what I can see we get these errors: 
  ```
  2024-01-31T13:09:29.832263236Z stderr F 2024-01-31T13:09:29.832081013Z	DEBUG	controller-runtime.healthz	healthz/healthz.go:60	healthz check failed	{"checker": "readyz", "error": "webhook server has not been started yet"}
  ```
  before the checker checks the `started` field before attempting the connection. So, there is only a small window where we can get transient errors, between setting `started: true`, and actually listening: https://github.com/kubernetes-sigs/controller-runtime/blob/8475c55f3e00e5611de0880eccd785efa85e8e38/pkg/webhook/server.go#L261-L263. However, I have never seen this errors so far.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-01T10:19:51Z

- **cmd/kueue/main.go:292** by [@trasc](https://github.com/trasc) — 2024-02-01T10:19:51Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  > I have never seen this errors so far.
  
  I'ts normal behavior when the pod is not yet ready, (previously we ware "ready" from the start). 
  
  Maybe something like: 
  ```suggestion
  	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
  		select{
  		case <-certsReady:
  			return mgr.GetWebhookServer().StartedChecker()(req)
  		case <-req.Context().Done():
  			return errors.New("request canceled")
  		}
  	}); err != nil {
  ```
  Could decrease the umber of messages.
  
  But i find it a bit odd to block the http handler.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T10:43:37Z

- **cmd/kueue/main.go:292** by [@mimowo](https://github.com/mimowo) — 2024-02-01T10:43:37Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  I was thinking about waiting explicitly for `certsReady` (it is done in the `hierarchical-namespaces` project as pointed out above). 
  
  However, I think it doesn't improve messages., because we already wait for certs ready as `mgr.GetWebhookServer().StartedChecker()` first checks if `started=true` (`started=true` implicitly means that certs are ready). If certs aren't ready we get the nice message: `{"checker": "readyz", "error": "webhook server has not been started yet"}`. I think this is enough, and no reason to complicate the code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T13:32:30Z

- **cmd/kueue/main.go:292** by [@mimowo](https://github.com/mimowo) — 2024-02-01T13:32:30Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  Ok, I think I have some new insights into :
  
  > I'm only not sure why we need a closure for calling it , since from what I can see the field holding the webhook is private and only stable whit an option when crating the manager, however it's not a huge problem.
  
  if we call `GetWebhookServer` early, then the not fully initialized webhook server runnable is registered here: https://github.com/kubernetes-sigs/controller-runtime/blob/73519a939e27c8465c6c49694356cbd66daf1677/pkg/manager/internal.go#L257. 
  
  When manager starts it starts all runnables, including the webhook server, but it fails, in this case I see in logs early on:
  ```
  2024-02-01T12:44:13.308211223Z	INFO	controller-runtime.webhook	webhook/server.go:191	Starting webhook server
  2024-02-01T12:44:13.40926745Z	INFO	controller/controller.go:178	Starting EventSource	{"controller": "cert-rotator", "source": "kind source: *v1.Secret"}
  2024-02-01T12:44:13.40932954Z	INFO	controller/controller.go:178	Starting EventSource	{"controller": "cert-rotator", "source": "kind source: *unstructured.Unstructured"}
  2024-02-01T12:44:13.40933832Z	INFO	manager/internal.go:516	Stopping and waiting for non leader election runnables
  ```
  
  Note the presence of "Stopping and waiting for non leader election runnables" just after the start.
  
  OTOH (as in this PR) If we delay calling `GetWebhookServer` then the runnables are added after the certs are ready, and only then the webhook server is started, then it starts successfully. We the log `Starting webhook server` is much later (after certs ready).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-01T14:43:48Z

- **cmd/kueue/main.go:292** by [@mimowo](https://github.com/mimowo) — 2024-02-01T14:43:48Z
```diff
@@ -278,7 +279,9 @@ func setupProbeEndpoints(mgr ctrl.Manager) {
 		setupLog.Error(err, "unable to set up health check")
 		os.Exit(1)
 	}
-	if err := mgr.AddReadyzCheck("readyz", healthz.Ping); err != nil {
+	if err := mgr.AddReadyzCheck("readyz", func(req *http.Request) error {
+		return mgr.GetWebhookServer().StartedChecker()(req)
+	}); err != nil {
```

  slightly polished the comment

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-01T19:45:26Z

/lgtm
/approve
/hold
for a couple more re-runs just in case.

- **hack/e2e-common.sh:58** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-01T19:45:10Z
```diff
@@ -55,5 +55,6 @@ function cluster_kueue_deploy {
     else
         kubectl apply --server-side -k test/e2e/config
     fi
+    kubectl wait --for=condition=available --timeout=3m deployment/kueue-controller-manager -n kueue-system
```

  Let's leave this for a follow up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-02T08:22:02Z

- **hack/e2e-common.sh:58** by [@mimowo](https://github.com/mimowo) — 2024-02-02T08:22:02Z
```diff
@@ -55,5 +55,6 @@ function cluster_kueue_deploy {
     else
         kubectl apply --server-side -k test/e2e/config
     fi
+    kubectl wait --for=condition=available --timeout=3m deployment/kueue-controller-manager -n kueue-system
```

  Sure, but I'm not sure what is the preferred option as a follow up. Is it returning to the `KueueReadyForTesting`, but waiting for deployment available, as in the command line?
