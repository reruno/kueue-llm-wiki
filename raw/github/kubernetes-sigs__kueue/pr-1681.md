# PR #1681: [multikueue] Manage worker cluster unavailability

**Summary**: [multikueue] Manage worker cluster unavailability

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1681

**Last updated**: 2024-05-29T20:29:12Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `multikueue-keep-ready-timeout` → `main`
- **Created**: 2024-02-02T14:31:41Z
- **Updated**: 2024-05-29T20:29:12Z
- **Merged**: 2024-03-06T18:04:01Z
- **Closed**: 2024-03-06T18:04:01Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 17   **+542 / -86**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

- Keep the AdmissionCheck Active even if not all it's clusters are usable.
- Add a mechanism which keeps a workload's MultiKueue AdmissionCheckState as `Ready`  for a configured time even if the manager cannot connect to its reserving worker.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
MuliKueue: Manage worker cluster unavailability
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-02T14:31:43Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-02T14:31:47Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c9a4984c0c2577ab3f0c23c5f81f67e5438dfa4b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65e8a07790b35c00082bd89f |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1681--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1681--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE2ODEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.xDeKdyLt6O2lPEFl2nFpK1gsk7Si5PQeJeR7LnumHME)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-28T10:29:45Z

> I'll approve the implementation when @mimowo gives LGTM

Before lgtm for the implementation I would yet like to confirm the implementation approach. IIUC in the current approach the 15min timeout is evaluated between checks, but if there is nothing to trigger the checks in the period of 15min, then it is possible that the cluster is available, but the check after 15min fails (might be even rejected by the P&F). Then, the workload is requeued. 

I would like to have some mechanism that (1) gives more confidence the cluster is really unavailable, by retrying the check periodically, (2) gives users something more tangible to verify the timeout really passed. I think currently the users don't have a good visibility of when the checks happen, so to verify for them why the requeue happened based on logs will be very hard.

I would like to have some approach that monitors periodically the state of the cluster, say every 30s. The first check unavailable timestamp is recorded in the cluster status. Then, the admission check reconciler verifies if 15min passed since the unavailable timestamp. The timestamp in the status would make it easier to verify / track down the decision for without logs. Also, it would mean that in the 15min period we do more checks, so we don't risk saying "cluster unavailable" if after 15min the call fails for a reason which might be just overloaded API server.

Having said that, I'm ok if we conclude this is overkill, or if we decide this is subject to follow up. However, I would like to have a decision first. WDYT @alculquicondor @mwielgus ?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T15:33:55Z

I'm ok with the current behavior for this PR. We can follow up in another one with periodic checks.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-06T16:09:17Z

Ok, let's go as is, we may iterate on the solution if needed in the future.

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-06T16:09:22Z

LGTM label has been added.  <details>Git tree hash: bcfc2d4afb40670dce1616d770436f38010b7772</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-06T16:18:10Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-06T16:18:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1681#issuecomment-1981246927" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1681#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-06T17:02:28Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-06T17:02:33Z

LGTM label has been added.  <details>Git tree hash: 84e29c2ae7f85cc205d9c5b83ca2e2d00be71647</details>

### Comment by [@trasc](https://github.com/trasc) — 2024-03-06T17:14:18Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-06T17:45:21Z

/test pull-kueue-verify-main

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-06T17:45:51Z

/test pull-kueue-test-integration-main

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-29T20:29:11Z

/release-note-edit
```release-note
MuliKueue: Manage worker cluster unavailability
```

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-05T14:37:43Z

- **apis/config/v1beta1/defaults.go:49** by [@trasc](https://github.com/trasc) — 2024-02-05T14:37:43Z
```diff
@@ -44,6 +44,9 @@ const (
 	DefaultQueueVisibilityUpdateIntervalSeconds int32   = 5
 	DefaultClusterQueuesMaxCount                int32   = 10
 	defaultJobFrameworkName                             = "batch/job"
+	DefaultMultiKueueGCTimeout                          = time.Minute
+	DefaultMultiKueueOrigin                             = "multikueue"
+	DefaultMultiKueueKeepReadyTimeout                   = 5 * time.Minute
```

  15 min

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-06T10:09:34Z

Initial review, API field name suggestion, and questions to understand the flow.

- **apis/config/v1beta1/configuration_types.go:222** by [@mimowo](https://github.com/mimowo) — 2024-02-06T09:54:24Z
```diff
@@ -215,6 +215,11 @@ type MultiKueue struct {
 	// them if their local counterpart no longer exists.
 	// +optional
 	Origin *string `json:"origin,omitempty"`
+
+	// KeepReadyTimeout defines the time a local workload's multikueue admission check state is kept Ready
+	// if the connection with its reserving worker cluster is lost.
+	// +optional
+	KeepReadyTimeout *metav1.Duration `json:"keepReadyTimeout,omitempty"`
```

  I would suggest some alternative name, the current one somehow suggests me this is relevant for the happy path. Maybe something like `WorkerLostTimeout`, `RequeueAfterWorkerLostTimeout`?

- **pkg/controller/admissionchecks/multikueue/workload.go:319** by [@mimowo](https://github.com/mimowo) — 2024-02-06T09:58:54Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
```

  I wonder that `retryAfter` could be negative here if the line above it was just before the timeout, and in this line it is just after. I would suggest to compute remainingTime line above and reuse here

- **pkg/controller/admissionchecks/multikueue/workload.go:377** by [@mimowo](https://github.com/mimowo) — 2024-02-06T10:02:59Z
```diff
@@ -316,36 +362,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			}
 			// update the message
 			acs.Message = fmt.Sprintf("The workload got reservation on %q", reservingRemote)
+			// update the transition time
+			acs.LastTransitionTime = metav1.NewTime(time.Now())
+
 			wlPatch := workload.BaseSSAWorkload(group.local)
 			workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, *acs)
 			err := a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
 			if err != nil {
-				return err
+				return reconcile.Result{}, err
 			}
 		}
-		// drop this if we want to create new remote workloads while holding a reservation
-		return nil
+		// drop this if we want to create new remote workloads while holding a reservation.
+		// check again the connection to the remote in half the keepReadyTimeout.
+		return reconcile.Result{RequeueAfter: a.keepReadyTimeout / 2}, nil
```

  Does this /2 mean we do two checks before giving up? If so, maybe we should make this two a named constant, and I would suggest having 3 checks, so /3?

- **pkg/controller/admissionchecks/multikueue/workload.go:323** by [@mimowo](https://github.com/mimowo) — 2024-02-06T10:03:25Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", retryAfter)
+			return reconcile.Result{RequeueAfter: retryAfter}, nil
+		} else {
+			acs.State = kueue.CheckStateRetry
```

  I'm not sure this is the purpose of the `CheckStateRetry`, could we just keep it Ready?

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@mimowo](https://github.com/mimowo) — 2024-02-06T10:06:17Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  Will this not be done by GC?

- **pkg/controller/admissionchecks/multikueue/workload.go:317** by [@mimowo](https://github.com/mimowo) — 2024-02-06T10:09:00Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
```

  Why do we differ the logic based on !hasReserving?
  I think the main use case for the timeout is to prevent requeue of a running workload (admitted locally and remotely), in case of transient connectivity loss. I thought in case of such workloads we have hasReserving=true.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T14:03:40Z

- **test/integration/multikueue/multikueue_test.go:708** by [@mimowo](https://github.com/mimowo) — 2024-02-08T14:01:01Z
```diff
@@ -560,4 +562,205 @@ var _ = ginkgo.Describe("Multikueue", func() {
 		})
 	})
 
+	ginkgo.It("Should requeue the workload with a delay when the connection to the admitting worker is lost", func() {
+		jobSet := testingjobset.MakeJobSet("job-set", managerNs.Name).
+			Queue(managerLq.Name).
+			ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Parallelism: 1,
+					Completions: 1,
+				}, testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    3,
+					Parallelism: 1,
+					Completions: 1,
+				},
+			).
+			Obj()
+		gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, jobSet)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjobset.GetWorkloadNameForJobSet(jobSet.Name), Namespace: managerNs.Name}
+
+		admission := utiltesting.MakeAdmission(managerCq.Name).PodSets(
+			kueue.PodSetAssignment{
+				Name: "replicated-job-1",
+			}, kueue.PodSetAssignment{
+				Name: "replicated-job-2",
+			},
+		).Obj()
+
+		ginkgo.By("setting workload reservation in the management cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(managerTestCluster.ctx, managerTestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("checking the workload creation in the worker clusters", func() {
+			managerWl := &kueue.Workload{}
+			gomega.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("setting workload reservation in worker2, the workload is admitted in manager amd worker1 wl is removed", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(worker2TestCluster.ctx, worker2TestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				acs := workload.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, multikueueAC.Name)
+				g.Expect(acs).NotTo(gomega.BeNil())
+				g.Expect(acs.State).To(gomega.Equal(kueue.CheckStateReady))
+				g.Expect(acs.Message).To(gomega.Equal(`The workload got reservation on "worker2"`))
+
+				g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The workload is admitted",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")))
+
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		var disconnectedTime time.Time
+		ginkgo.By("breaking the connection to worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				createdCluster.Spec.KubeConfig.Location = "bad-secret"
+				g.Expect(managerTestCluster.client.Update(managerTestCluster.ctx, createdCluster)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				activeCondition := apimeta.FindStatusCondition(createdCluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+				g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:   kueuealpha.MultiKueueClusterActive,
+					Status: metav1.ConditionFalse,
+					Reason: "BadConfig",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "Message", "LastTransitionTime")))
+				disconnectedTime = activeCondition.LastTransitionTime.Time
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("waiting for the local workload admission check state to be set to pending and quotaReservatio removed", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				acs := workload.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, multikueueAC.Name)
+				g.Expect(acs).To(gomega.BeComparableTo(&kueue.AdmissionCheckState{
+					Name:  multikueueAC.Name,
+					State: kueue.CheckStatePending,
+				}, cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime", "Message")))
+
+				// The transition interval should be close to testingKeepReadyTimeout (taking into account the resolution of the LastTransitionTime field)
+				g.Expect(acs.LastTransitionTime.Time.Sub(disconnectedTime)).To(gomega.BeComparableTo(testingKeepReadyTimeout,
+					cmpopts.EquateApproxTime(time.Second)))
+
+				g.Expect(apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadQuotaReserved)).To(gomega.BeFalse())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("restoring the connection to worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				createdCluster.Spec.KubeConfig.Location = managerMultikueueSecret2.Name
+				g.Expect(managerTestCluster.client.Update(managerTestCluster.ctx, createdCluster)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				activeCondition := apimeta.FindStatusCondition(createdCluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+				g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:   kueuealpha.MultiKueueClusterActive,
+					Status: metav1.ConditionTrue,
+					Reason: "Active",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "Message", "LastTransitionTime")))
+				disconnectedTime = activeCondition.LastTransitionTime.Time
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("the worker2 wl is removed since the local one no longer has a reservation", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWorkload := &kueue.Workload{}
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Should remove the worker's workload and job when managers job is deleted", func() {
```

  Can you explain how this test is related to the code changes? If unrelated, maybe a separate PR

- **test/integration/multikueue/multikueue_test.go:662** by [@mimowo](https://github.com/mimowo) — 2024-02-08T14:03:31Z
```diff
@@ -560,4 +562,205 @@ var _ = ginkgo.Describe("Multikueue", func() {
 		})
 	})
 
+	ginkgo.It("Should requeue the workload with a delay when the connection to the admitting worker is lost", func() {
+		jobSet := testingjobset.MakeJobSet("job-set", managerNs.Name).
+			Queue(managerLq.Name).
+			ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Parallelism: 1,
+					Completions: 1,
+				}, testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    3,
+					Parallelism: 1,
+					Completions: 1,
+				},
+			).
+			Obj()
+		gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, jobSet)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjobset.GetWorkloadNameForJobSet(jobSet.Name), Namespace: managerNs.Name}
+
+		admission := utiltesting.MakeAdmission(managerCq.Name).PodSets(
+			kueue.PodSetAssignment{
+				Name: "replicated-job-1",
+			}, kueue.PodSetAssignment{
+				Name: "replicated-job-2",
+			},
+		).Obj()
+
+		ginkgo.By("setting workload reservation in the management cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(managerTestCluster.ctx, managerTestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("checking the workload creation in the worker clusters", func() {
+			managerWl := &kueue.Workload{}
+			gomega.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("setting workload reservation in worker2, the workload is admitted in manager amd worker1 wl is removed", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(worker2TestCluster.ctx, worker2TestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				acs := workload.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, multikueueAC.Name)
+				g.Expect(acs).NotTo(gomega.BeNil())
+				g.Expect(acs.State).To(gomega.Equal(kueue.CheckStateReady))
+				g.Expect(acs.Message).To(gomega.Equal(`The workload got reservation on "worker2"`))
+
+				g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The workload is admitted",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")))
+
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		var disconnectedTime time.Time
+		ginkgo.By("breaking the connection to worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				createdCluster.Spec.KubeConfig.Location = "bad-secret"
+				g.Expect(managerTestCluster.client.Update(managerTestCluster.ctx, createdCluster)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				activeCondition := apimeta.FindStatusCondition(createdCluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+				g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:   kueuealpha.MultiKueueClusterActive,
+					Status: metav1.ConditionFalse,
+					Reason: "BadConfig",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "Message", "LastTransitionTime")))
+				disconnectedTime = activeCondition.LastTransitionTime.Time
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("waiting for the local workload admission check state to be set to pending and quotaReservatio removed", func() {
```

  I'm wondering about using Consistency to verify the workload is not created on worker1 as long as the timeout is not reached, this would require increasing the timeout though.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-19T14:33:37Z

- **apis/config/v1beta1/configuration_types.go:222** by [@trasc](https://github.com/trasc) — 2024-02-19T13:04:53Z
```diff
@@ -215,6 +215,11 @@ type MultiKueue struct {
 	// them if their local counterpart no longer exists.
 	// +optional
 	Origin *string `json:"origin,omitempty"`
+
+	// KeepReadyTimeout defines the time a local workload's multikueue admission check state is kept Ready
+	// if the connection with its reserving worker cluster is lost.
+	// +optional
+	KeepReadyTimeout *metav1.Duration `json:"keepReadyTimeout,omitempty"`
```

  done

- **apis/config/v1beta1/defaults.go:49** by [@trasc](https://github.com/trasc) — 2024-02-19T13:08:25Z
```diff
@@ -44,6 +44,9 @@ const (
 	DefaultQueueVisibilityUpdateIntervalSeconds int32   = 5
 	DefaultClusterQueuesMaxCount                int32   = 10
 	defaultJobFrameworkName                             = "batch/job"
+	DefaultMultiKueueGCTimeout                          = time.Minute
+	DefaultMultiKueueOrigin                             = "multikueue"
+	DefaultMultiKueueKeepReadyTimeout                   = 5 * time.Minute
```

  done

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@trasc](https://github.com/trasc) — 2024-02-19T13:09:28Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  no

- **pkg/controller/admissionchecks/multikueue/workload.go:317** by [@trasc](https://github.com/trasc) — 2024-02-19T13:14:32Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
```

  `hasReserving` - checks if any of the remotes workloads that we currently see has reservation.
  
  This could be false if none of them has reservation or the worker on which the remote workload that has reservation is unreachable - this being the case in which we start counting.

- **pkg/controller/admissionchecks/multikueue/workload.go:319** by [@trasc](https://github.com/trasc) — 2024-02-19T13:17:44Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
```

  done

- **pkg/controller/admissionchecks/multikueue/workload.go:323** by [@trasc](https://github.com/trasc) — 2024-02-19T13:18:28Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", retryAfter)
+			return reconcile.Result{RequeueAfter: retryAfter}, nil
+		} else {
+			acs.State = kueue.CheckStateRetry
```

  This is the purpose of the `CheckStateRetry`.

- **pkg/controller/admissionchecks/multikueue/workload.go:377** by [@trasc](https://github.com/trasc) — 2024-02-19T13:22:54Z
```diff
@@ -316,36 +362,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			}
 			// update the message
 			acs.Message = fmt.Sprintf("The workload got reservation on %q", reservingRemote)
+			// update the transition time
+			acs.LastTransitionTime = metav1.NewTime(time.Now())
+
 			wlPatch := workload.BaseSSAWorkload(group.local)
 			workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, *acs)
 			err := a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
 			if err != nil {
-				return err
+				return reconcile.Result{}, err
 			}
 		}
-		// drop this if we want to create new remote workloads while holding a reservation
-		return nil
+		// drop this if we want to create new remote workloads while holding a reservation.
+		// check again the connection to the remote in half the keepReadyTimeout.
+		return reconcile.Result{RequeueAfter: a.keepReadyTimeout / 2}, nil
```

  Let's just use full timeout

- **test/integration/multikueue/multikueue_test.go:662** by [@trasc](https://github.com/trasc) — 2024-02-19T13:28:09Z
```diff
@@ -560,4 +562,205 @@ var _ = ginkgo.Describe("Multikueue", func() {
 		})
 	})
 
+	ginkgo.It("Should requeue the workload with a delay when the connection to the admitting worker is lost", func() {
+		jobSet := testingjobset.MakeJobSet("job-set", managerNs.Name).
+			Queue(managerLq.Name).
+			ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Parallelism: 1,
+					Completions: 1,
+				}, testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    3,
+					Parallelism: 1,
+					Completions: 1,
+				},
+			).
+			Obj()
+		gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, jobSet)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjobset.GetWorkloadNameForJobSet(jobSet.Name), Namespace: managerNs.Name}
+
+		admission := utiltesting.MakeAdmission(managerCq.Name).PodSets(
+			kueue.PodSetAssignment{
+				Name: "replicated-job-1",
+			}, kueue.PodSetAssignment{
+				Name: "replicated-job-2",
+			},
+		).Obj()
+
+		ginkgo.By("setting workload reservation in the management cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(managerTestCluster.ctx, managerTestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("checking the workload creation in the worker clusters", func() {
+			managerWl := &kueue.Workload{}
+			gomega.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("setting workload reservation in worker2, the workload is admitted in manager amd worker1 wl is removed", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(worker2TestCluster.ctx, worker2TestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				acs := workload.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, multikueueAC.Name)
+				g.Expect(acs).NotTo(gomega.BeNil())
+				g.Expect(acs.State).To(gomega.Equal(kueue.CheckStateReady))
+				g.Expect(acs.Message).To(gomega.Equal(`The workload got reservation on "worker2"`))
+
+				g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The workload is admitted",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")))
+
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		var disconnectedTime time.Time
+		ginkgo.By("breaking the connection to worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				createdCluster.Spec.KubeConfig.Location = "bad-secret"
+				g.Expect(managerTestCluster.client.Update(managerTestCluster.ctx, createdCluster)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				activeCondition := apimeta.FindStatusCondition(createdCluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+				g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:   kueuealpha.MultiKueueClusterActive,
+					Status: metav1.ConditionFalse,
+					Reason: "BadConfig",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "Message", "LastTransitionTime")))
+				disconnectedTime = activeCondition.LastTransitionTime.Time
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("waiting for the local workload admission check state to be set to pending and quotaReservatio removed", func() {
```

  let's keep it as is

- **test/integration/multikueue/multikueue_test.go:708** by [@trasc](https://github.com/trasc) — 2024-02-19T13:32:31Z
```diff
@@ -560,4 +562,205 @@ var _ = ginkgo.Describe("Multikueue", func() {
 		})
 	})
 
+	ginkgo.It("Should requeue the workload with a delay when the connection to the admitting worker is lost", func() {
+		jobSet := testingjobset.MakeJobSet("job-set", managerNs.Name).
+			Queue(managerLq.Name).
+			ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Parallelism: 1,
+					Completions: 1,
+				}, testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    3,
+					Parallelism: 1,
+					Completions: 1,
+				},
+			).
+			Obj()
+		gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, jobSet)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjobset.GetWorkloadNameForJobSet(jobSet.Name), Namespace: managerNs.Name}
+
+		admission := utiltesting.MakeAdmission(managerCq.Name).PodSets(
+			kueue.PodSetAssignment{
+				Name: "replicated-job-1",
+			}, kueue.PodSetAssignment{
+				Name: "replicated-job-2",
+			},
+		).Obj()
+
+		ginkgo.By("setting workload reservation in the management cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(managerTestCluster.ctx, managerTestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("checking the workload creation in the worker clusters", func() {
+			managerWl := &kueue.Workload{}
+			gomega.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("setting workload reservation in worker2, the workload is admitted in manager amd worker1 wl is removed", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(worker2TestCluster.ctx, worker2TestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				acs := workload.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, multikueueAC.Name)
+				g.Expect(acs).NotTo(gomega.BeNil())
+				g.Expect(acs.State).To(gomega.Equal(kueue.CheckStateReady))
+				g.Expect(acs.Message).To(gomega.Equal(`The workload got reservation on "worker2"`))
+
+				g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The workload is admitted",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")))
+
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		var disconnectedTime time.Time
+		ginkgo.By("breaking the connection to worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				createdCluster.Spec.KubeConfig.Location = "bad-secret"
+				g.Expect(managerTestCluster.client.Update(managerTestCluster.ctx, createdCluster)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				activeCondition := apimeta.FindStatusCondition(createdCluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+				g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:   kueuealpha.MultiKueueClusterActive,
+					Status: metav1.ConditionFalse,
+					Reason: "BadConfig",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "Message", "LastTransitionTime")))
+				disconnectedTime = activeCondition.LastTransitionTime.Time
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("waiting for the local workload admission check state to be set to pending and quotaReservatio removed", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				acs := workload.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, multikueueAC.Name)
+				g.Expect(acs).To(gomega.BeComparableTo(&kueue.AdmissionCheckState{
+					Name:  multikueueAC.Name,
+					State: kueue.CheckStatePending,
+				}, cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime", "Message")))
+
+				// The transition interval should be close to testingKeepReadyTimeout (taking into account the resolution of the LastTransitionTime field)
+				g.Expect(acs.LastTransitionTime.Time.Sub(disconnectedTime)).To(gomega.BeComparableTo(testingKeepReadyTimeout,
+					cmpopts.EquateApproxTime(time.Second)))
+
+				g.Expect(apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadQuotaReserved)).To(gomega.BeFalse())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("restoring the connection to worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				createdCluster.Spec.KubeConfig.Location = managerMultikueueSecret2.Name
+				g.Expect(managerTestCluster.client.Update(managerTestCluster.ctx, createdCluster)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				activeCondition := apimeta.FindStatusCondition(createdCluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+				g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:   kueuealpha.MultiKueueClusterActive,
+					Status: metav1.ConditionTrue,
+					Reason: "Active",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "Message", "LastTransitionTime")))
+				disconnectedTime = activeCondition.LastTransitionTime.Time
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("the worker2 wl is removed since the local one no longer has a reservation", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWorkload := &kueue.Workload{}
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Should remove the worker's workload and job when managers job is deleted", func() {
```

  unrelated, (leftover from a rebase conflict resolution)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T14:39:53Z

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:04:56Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  Isn't this part `group.local == nil` covered by the MK GC? IIRC MK GC removes workloads on workers which don't have the local counterpart, so seems to be the case here.

- **pkg/controller/admissionchecks/multikueue/workload.go:311** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:07:25Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
```

  Should we not repeat the outOfSync check for the new pair: `hasReserving, reservingRemote `?

- **pkg/controller/admissionchecks/multikueue/workload.go:323** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:14:20Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", retryAfter)
+			return reconcile.Result{RequeueAfter: retryAfter}, nil
+		} else {
+			acs.State = kueue.CheckStateRetry
```

  Ok, I have two concerns with that:
  1. in the [docs for Retry](https://github.com/kubernetes-sigs/kueue/blob/fa96518c293473480b0d1f45f642af23e433302e/apis/kueue/v1beta1/admissioncheck_types.go#L28C2-L30C19
  ) we say "A workload having at least one check in the state, will be evicted if admitted will not be considered for admission.". However, in the case of workload being temporarily unreachable it is already admitted, so I'm not sure it fits the definition, does it? 
  
  2. In the [docs for Rejected](https://github.com/kubernetes-sigs/kueue/blob/fa96518c293473480b0d1f45f642af23e433302e/apis/kueue/v1beta1/admissioncheck_types.go#L35-L37. ) we have "NOTE: The admission behaviour is currently the same as for retry, we can consider marking the workload as "Finished" with a failure description.". Should we update the note then? I'm also concerned about the checks in code which make `Retry` and `Rejected` equivalent, like [here](https://github.com/kubernetes-sigs/kueue/blob/fa96518c293473480b0d1f45f642af23e433302e/pkg/workload/admissionchecks.go#L145), so we may need to review all the places if this still makes sense.

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:22:14Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  Requeue after 15min to check sounds bad. I think by analogy to `--retry` in curl we could have exponential backoff, or fixed delay (probably at 30s by default). Then wait for `min(remainingTime, delay)`

- **pkg/controller/admissionchecks/multikueue/workload_test.go:427** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:23:49Z
```diff
@@ -423,6 +424,185 @@ func TestWlReconcile(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the local workload is kept Ready if the keepReadyTimeout did not pass since last successful sync": {
```

  ```suggestion
  		"the local workload admission check Ready if the remote WorkerLostTimeout is not exceeded": {
  ```

- **pkg/controller/admissionchecks/multikueue/workload.go:348** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:30:35Z
```diff
@@ -316,15 +362,19 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			}
 			// update the message
 			acs.Message = fmt.Sprintf("The workload got reservation on %q", reservingRemote)
+			// update the transition time
+			acs.LastTransitionTime = metav1.NewTime(time.Now())
+
 			wlPatch := workload.BaseSSAWorkload(group.local)
 			workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, *acs)
 			err := a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
 			if err != nil {
-				return err
+				return reconcile.Result{}, err
 			}
 		}
-		// drop this if we want to create new remote workloads while holding a reservation
-		return nil
+		// drop this if we want to create new remote workloads while holding a reservation.
+		// check again the connection to the remote in workerLostTimeout.
+		return reconcile.Result{RequeueAfter: a.workerLostTimeout}, nil
```

  As in the other comment, I think we should check the connection more often than in 15min.

- **pkg/controller/admissionchecks/multikueue/workload_test.go:453** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:34:19Z
```diff
@@ -423,6 +424,185 @@ func TestWlReconcile(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the local workload is kept Ready if the keepReadyTimeout did not pass since last successful sync": {
+			reconcileFor: "wl1",
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:               "ac1",
+						State:              kueue.CheckStateReady,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-defaulWorkerLostTimeout / 2)), //50% of the timeout
+						Message:            `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:    "ac1",
+						State:   kueue.CheckStateReady,
+						Message: `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+		},
+		"the local workload is set for Retry if the keepReadyTimeout did pass since last successful sync": {
```

  ```suggestion
  		"the local workload's admission check is set to Retry if the WorkerLostTimeout is exceeded": {
  ```

- **pkg/controller/admissionchecks/multikueue/workload_test.go:479** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:35:04Z
```diff
@@ -423,6 +424,185 @@ func TestWlReconcile(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the local workload is kept Ready if the keepReadyTimeout did not pass since last successful sync": {
+			reconcileFor: "wl1",
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:               "ac1",
+						State:              kueue.CheckStateReady,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-defaulWorkerLostTimeout / 2)), //50% of the timeout
+						Message:            `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:    "ac1",
+						State:   kueue.CheckStateReady,
+						Message: `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+		},
+		"the local workload is set for Retry if the keepReadyTimeout did pass since last successful sync": {
+			reconcileFor: "wl1",
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:               "ac1",
+						State:              kueue.CheckStateReady,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-defaulWorkerLostTimeout * 3 / 2)), // 150% of the timeout
+						Message:            `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:    "ac1",
+						State:   kueue.CheckStateRetry,
+						Message: `Reserving remote lost`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+		},
+		"the local workload is set to pendin when it is in Retry without quota": {
```

  ```suggestion
  		"the local workload's admission check is set to Pending when it is in Retry without quota": {
  ```

- **test/integration/multikueue/multikueue_test.go:613** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:37:21Z
```diff
@@ -560,4 +562,146 @@ var _ = ginkgo.Describe("Multikueue", func() {
 		})
 	})
 
+	ginkgo.It("Should requeue the workload with a delay when the connection to the admitting worker is lost", func() {
+		jobSet := testingjobset.MakeJobSet("job-set", managerNs.Name).
+			Queue(managerLq.Name).
+			ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Parallelism: 1,
+					Completions: 1,
+				}, testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    3,
+					Parallelism: 1,
+					Completions: 1,
+				},
+			).
+			Obj()
+		gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, jobSet)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjobset.GetWorkloadNameForJobSet(jobSet.Name), Namespace: managerNs.Name}
+
+		admission := utiltesting.MakeAdmission(managerCq.Name).PodSets(
+			kueue.PodSetAssignment{
+				Name: "replicated-job-1",
+			}, kueue.PodSetAssignment{
+				Name: "replicated-job-2",
+			},
+		).Obj()
+
+		ginkgo.By("setting workload reservation in the management cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(managerTestCluster.ctx, managerTestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("checking the workload creation in the worker clusters", func() {
+			managerWl := &kueue.Workload{}
+			gomega.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("setting workload reservation in worker2, the workload is admitted in manager amd worker1 wl is removed", func() {
```

  ```suggestion
  		ginkgo.By("setting workload reservation in worker2, the workload is admitted in manager and worker1 wl is removed", func() {
  ```

- **test/integration/multikueue/suite_test.go:50** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:39:20Z
```diff
@@ -46,6 +46,10 @@ import (
 	// +kubebuilder:scaffold:imports
 )
 
+const (
+	testingKeepReadyTimeout = 3 * time.Second
```

  ```suggestion
  	workerLostTimeout = 3 * time.Second
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T14:58:54Z

- **pkg/controller/admissionchecks/multikueue/workload.go:323** by [@mimowo](https://github.com/mimowo) — 2024-02-21T14:58:54Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", retryAfter)
+			return reconcile.Result{RequeueAfter: retryAfter}, nil
+		} else {
+			acs.State = kueue.CheckStateRetry
```

  In other words, I'm not understanding yet was is the difference between `Rejected` and `Retry`, and why `Retry` is preferred. 
  
  After the `WorkerLostTimeout` is exceeded we should requeue the workload. Can you elaborate what is the difference between `Rejected` and `Retry`, and how the choice impacts the scenario.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T15:32:36Z

- **pkg/controller/admissionchecks/multikueue/workload.go:323** by [@trasc](https://github.com/trasc) — 2024-02-21T15:32:36Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", retryAfter)
+			return reconcile.Result{RequeueAfter: retryAfter}, nil
+		} else {
+			acs.State = kueue.CheckStateRetry
```

  `Rejected` is permanent, the workload is marked as `Finished` and should never go back in the queue

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T15:36:50Z

- **test/integration/multikueue/multikueue_test.go:662** by [@mimowo](https://github.com/mimowo) — 2024-02-21T15:36:50Z
```diff
@@ -560,4 +562,205 @@ var _ = ginkgo.Describe("Multikueue", func() {
 		})
 	})
 
+	ginkgo.It("Should requeue the workload with a delay when the connection to the admitting worker is lost", func() {
+		jobSet := testingjobset.MakeJobSet("job-set", managerNs.Name).
+			Queue(managerLq.Name).
+			ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Parallelism: 1,
+					Completions: 1,
+				}, testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    3,
+					Parallelism: 1,
+					Completions: 1,
+				},
+			).
+			Obj()
+		gomega.Expect(managerTestCluster.client.Create(managerTestCluster.ctx, jobSet)).Should(gomega.Succeed())
+
+		createdWorkload := &kueue.Workload{}
+		wlLookupKey := types.NamespacedName{Name: workloadjobset.GetWorkloadNameForJobSet(jobSet.Name), Namespace: managerNs.Name}
+
+		admission := utiltesting.MakeAdmission(managerCq.Name).PodSets(
+			kueue.PodSetAssignment{
+				Name: "replicated-job-1",
+			}, kueue.PodSetAssignment{
+				Name: "replicated-job-2",
+			},
+		).Obj()
+
+		ginkgo.By("setting workload reservation in the management cluster", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(managerTestCluster.ctx, managerTestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("checking the workload creation in the worker clusters", func() {
+			managerWl := &kueue.Workload{}
+			gomega.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, managerWl)).To(gomega.Succeed())
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(createdWorkload.Spec).To(gomega.BeComparableTo(managerWl.Spec))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("setting workload reservation in worker2, the workload is admitted in manager amd worker1 wl is removed", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker2TestCluster.client.Get(worker2TestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				g.Expect(util.SetQuotaReservation(worker2TestCluster.ctx, worker2TestCluster.client, createdWorkload, admission)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+				acs := workload.FindAdmissionCheck(createdWorkload.Status.AdmissionChecks, multikueueAC.Name)
+				g.Expect(acs).NotTo(gomega.BeNil())
+				g.Expect(acs.State).To(gomega.Equal(kueue.CheckStateReady))
+				g.Expect(acs.Message).To(gomega.Equal(`The workload got reservation on "worker2"`))
+
+				g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The workload is admitted",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")))
+
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		var disconnectedTime time.Time
+		ginkgo.By("breaking the connection to worker2", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				createdCluster.Spec.KubeConfig.Location = "bad-secret"
+				g.Expect(managerTestCluster.client.Update(managerTestCluster.ctx, createdCluster)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdCluster := &kueuealpha.MultiKueueCluster{}
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, client.ObjectKeyFromObject(workerCluster2), createdCluster)).To(gomega.Succeed())
+				activeCondition := apimeta.FindStatusCondition(createdCluster.Status.Conditions, kueuealpha.MultiKueueClusterActive)
+				g.Expect(activeCondition).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:   kueuealpha.MultiKueueClusterActive,
+					Status: metav1.ConditionFalse,
+					Reason: "BadConfig",
+				}, cmpopts.IgnoreFields(metav1.Condition{}, "Message", "LastTransitionTime")))
+				disconnectedTime = activeCondition.LastTransitionTime.Time
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("waiting for the local workload admission check state to be set to pending and quotaReservatio removed", func() {
```

  Before we say "let's keep it as is" - is it covered at the unit test level though? I mean, do we have some test that verifies that a workload is not requeued before the timeout is reached?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T15:40:37Z

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@trasc](https://github.com/trasc) — 2024-02-21T15:40:37Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  One other purpose of that is avoid a nil pointer deference while calling `!equality.Semantic.DeepEqual(group.local.Spec..` ,
  
  Even ignoring that.
  
  I cannot understand why we wold like to wait fro the periodic GC to run when we can do the cleanup now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T15:41:01Z

- **pkg/controller/admissionchecks/multikueue/workload.go:323** by [@mimowo](https://github.com/mimowo) — 2024-02-21T15:41:01Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", retryAfter)
+			return reconcile.Result{RequeueAfter: retryAfter}, nil
+		} else {
+			acs.State = kueue.CheckStateRetry
```

  Got it, thanks for explaining. Should we update the descriptions to indicate the difference (potentially in this PR, preparatory, or follow up)? This "A workload having at least one check in the state, will be evicted if admitted **will not be considered for admission.**" suggests to me that the workload is not retried also. Similarly, the note suggests there is no difference "NOTE: The admission behaviour is currently the same as for retry".

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T15:45:45Z

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@trasc](https://github.com/trasc) — 2024-02-21T15:45:45Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  The 15 min will apply only if no other event triggers the reconcile, then and only then we are interested in requeuing if the reserving worker is not back.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T15:46:09Z

- **pkg/controller/admissionchecks/multikueue/workload.go:348** by [@trasc](https://github.com/trasc) — 2024-02-21T15:46:09Z
```diff
@@ -316,15 +362,19 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			}
 			// update the message
 			acs.Message = fmt.Sprintf("The workload got reservation on %q", reservingRemote)
+			// update the transition time
+			acs.LastTransitionTime = metav1.NewTime(time.Now())
+
 			wlPatch := workload.BaseSSAWorkload(group.local)
 			workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, *acs)
 			err := a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
 			if err != nil {
-				return err
+				return reconcile.Result{}, err
 			}
 		}
-		// drop this if we want to create new remote workloads while holding a reservation
-		return nil
+		// drop this if we want to create new remote workloads while holding a reservation.
+		// check again the connection to the remote in workerLostTimeout.
+		return reconcile.Result{RequeueAfter: a.workerLostTimeout}, nil
```

  same

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T15:48:19Z

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@mimowo](https://github.com/mimowo) — 2024-02-21T15:48:19Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  To avoid nil pointer we can have `group.local != nil && !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)`, so it is not convincing argument.
  
  > I cannot understand why we wold like to wait fro the periodic GC to run when we can do the cleanup now.
  
  Sorry, for now I just asked the question to understand what is happening (not claiming which one is better in the comments above) because I wasn't sure of my understanding. Can you summarize the pros & cons for deleting now and leaving this to MKGC?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T15:50:44Z

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@trasc](https://github.com/trasc) — 2024-02-21T15:50:44Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  Can you?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T15:53:48Z

- **pkg/controller/admissionchecks/multikueue/workload.go:311** by [@trasc](https://github.com/trasc) — 2024-02-21T15:53:48Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
```

  previously , L308 we remove the out of sync one, we check if another also has reservation.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T15:57:24Z

- **pkg/controller/admissionchecks/multikueue/workload.go:323** by [@trasc](https://github.com/trasc) — 2024-02-21T15:57:24Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", retryAfter)
+			return reconcile.Result{RequeueAfter: retryAfter}, nil
+		} else {
+			acs.State = kueue.CheckStateRetry
```

  indeed 
  
  https://github.com/kubernetes-sigs/kueue/blob/fa96518c293473480b0d1f45f642af23e433302e/apis/kueue/v1beta1/admissioncheck_types.go#L35-L38
  
  is outdated, it should be changed in a cleanup  PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T16:14:11Z

- **pkg/controller/admissionchecks/multikueue/workload.go:311** by [@mimowo](https://github.com/mimowo) — 2024-02-21T16:14:10Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
```

  What guarantees the second will not be out of sync as well?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T16:23:00Z

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@mimowo](https://github.com/mimowo) — 2024-02-21T16:23:00Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  The pro for leaving to MKGC is that the go worker which handles admission check reconciliation can take new tasks earlier (not need to wait for the Delete, which could potentially hang). 
  
  There is also a conceptual difficulty IMO that we have 2 mechanisms overlapping in the responsibility of handling the scenario.
  
  I think these are no major concerns, but if there is no good argument for deleting early, I would just leave it to MKGC.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T16:30:36Z

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@mimowo](https://github.com/mimowo) — 2024-02-21T16:30:36Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  What triggers the admitted workload to lose its reservation after the WorkerLostTimeout? I'm wondering about this check `!workload.HasQuotaReservation(group.local)`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T16:41:06Z

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@mimowo](https://github.com/mimowo) — 2024-02-21T16:41:06Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  Maybe I still miss some piece of information. Some question: if we have a workload running on worker1, and we lose connectivity for 5min, to worker1 (and worker1 is marked inactive). What will trigger the check to include the cluster back as active?
  
  Also, if we don't get connectivity for 15min, do we mark such cluster as inactive?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T18:17:49Z

- **pkg/controller/admissionchecks/multikueue/workload.go:311** by [@trasc](https://github.com/trasc) — 2024-02-21T18:17:49Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
```

  Reworked

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T18:21:08Z

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@trasc](https://github.com/trasc) — 2024-02-21T18:21:08Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  > Maybe I still miss some piece of information. Some question: if we have a workload running on worker1, and we lose connectivity for 5min, to worker1 (and worker1 is marked inactive). What will trigger the check to include the cluster back as active?
  
  Nothing for now we have a follow-up for connection monitoring and reconnects.
  
  > Also, if we don't get connectivity for 15min, do we mark such cluster as inactive?
  
   Here we are talking about workloads.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T18:22:17Z

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@trasc](https://github.com/trasc) — 2024-02-21T18:22:17Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  The setting of `Retry` as AC status.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T20:08:08Z

- **pkg/controller/admissionchecks/multikueue/workload_test.go:427** by [@trasc](https://github.com/trasc) — 2024-02-21T19:15:18Z
```diff
@@ -423,6 +424,185 @@ func TestWlReconcile(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the local workload is kept Ready if the keepReadyTimeout did not pass since last successful sync": {
```

  done

- **pkg/controller/admissionchecks/multikueue/workload_test.go:453** by [@trasc](https://github.com/trasc) — 2024-02-21T19:16:10Z
```diff
@@ -423,6 +424,185 @@ func TestWlReconcile(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the local workload is kept Ready if the keepReadyTimeout did not pass since last successful sync": {
+			reconcileFor: "wl1",
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:               "ac1",
+						State:              kueue.CheckStateReady,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-defaulWorkerLostTimeout / 2)), //50% of the timeout
+						Message:            `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:    "ac1",
+						State:   kueue.CheckStateReady,
+						Message: `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+		},
+		"the local workload is set for Retry if the keepReadyTimeout did pass since last successful sync": {
```

  done

- **pkg/controller/admissionchecks/multikueue/workload_test.go:479** by [@trasc](https://github.com/trasc) — 2024-02-21T19:16:48Z
```diff
@@ -423,6 +424,185 @@ func TestWlReconcile(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the local workload is kept Ready if the keepReadyTimeout did not pass since last successful sync": {
+			reconcileFor: "wl1",
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:               "ac1",
+						State:              kueue.CheckStateReady,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-defaulWorkerLostTimeout / 2)), //50% of the timeout
+						Message:            `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:    "ac1",
+						State:   kueue.CheckStateReady,
+						Message: `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+		},
+		"the local workload is set for Retry if the keepReadyTimeout did pass since last successful sync": {
+			reconcileFor: "wl1",
+			managersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:               "ac1",
+						State:              kueue.CheckStateReady,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-defaulWorkerLostTimeout * 3 / 2)), // 150% of the timeout
+						Message:            `The workload got reservation on "worker1"`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+			wantManagersWorkloads: []kueue.Workload{
+				*baseWorkloadBuilder.Clone().
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:    "ac1",
+						State:   kueue.CheckStateRetry,
+						Message: `Reserving remote lost`,
+					}).
+					ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "job1", "uid1").
+					ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+					Obj(),
+			},
+		},
+		"the local workload is set to pendin when it is in Retry without quota": {
```

  done

- **test/integration/multikueue/suite_test.go:50** by [@trasc](https://github.com/trasc) — 2024-02-21T19:18:40Z
```diff
@@ -46,6 +46,10 @@ import (
 	// +kubebuilder:scaffold:imports
 )
 
+const (
+	testingKeepReadyTimeout = 3 * time.Second
```

  done

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@trasc](https://github.com/trasc) — 2024-02-21T19:34:32Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  1. The MKGC was implementing for other usecases like:
    - the set of admissionchecks of the CQ changes
    - the workload is reconnected after long time and the source wl was removed 
   2. If left, the workload will remain queued in the worker cluster and can potentially block quota
   3. We cannot consider a non-nil wl being  `Semantic.DeepEqual` to a nil.
  
  Reworked (for other reasons)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T06:42:21Z

- **pkg/controller/admissionchecks/multikueue/workload.go:323** by [@trasc](https://github.com/trasc) — 2024-02-22T06:42:21Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		if time.Now().Before(acs.LastTransitionTime.Add(a.keepReadyTimeout)) {
+			retryAfter := a.keepReadyTimeout - time.Since(acs.LastTransitionTime.Time)
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", retryAfter)
+			return reconcile.Result{RequeueAfter: retryAfter}, nil
+		} else {
+			acs.State = kueue.CheckStateRetry
```

  #1763

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T08:14:55Z

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@mimowo](https://github.com/mimowo) — 2024-02-22T08:14:55Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  Ok, I see, but there is an issue with delaying the check, that if we get unlucky once and 15min later, then re requeue. However, the cluster might have been available in-between for 14min. I don't think this is the intention. I think we should monitor if the cluster is connected, and switch its status based on that. 
  
  If the problem is solved under " connection monitoring and reconnects.", then maybe it makes sense to combine to see the solution to temporary loss of connectivity holistically. At least update the "special notes to reviewers" with what will be covered in the coming follow up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T08:21:17Z

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@mimowo](https://github.com/mimowo) — 2024-02-22T08:21:17Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  > If left, the workload will remain queued in the worker cluster and can potentially block quota
  
  Right, this is true in the other cases as well, I'm wondering what makes this situation different. What is the scenario leading to "group.local == nil"?
  
  > We cannot consider a non-nil wl being Semantic.DeepEqual to a nil.
  
  I don't think I suggested that (at least not intentionally). I'm saying (1) nil local workload -> MKGC, (2)  !Semantic.DeepEqual -> Delete remote. It is actually the current code that interprets (1) and (2) as the same (outOfSync). Unless I'm missing something.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T08:34:56Z

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@mimowo](https://github.com/mimowo) — 2024-02-22T08:34:56Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  So IIUC this code is to clear the admission check status as Pending after the workload is evicted and requeued here: https://github.com/kubernetes-sigs/kueue/blob/7ed44becf8bee6f894cb0e2a6f9cd37c98d7b575/pkg/controller/core/workload_controller.go#L237C30-L246. 
  
  Do we currently keep the state of admission checks on eviction?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T10:39:00Z

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@trasc](https://github.com/trasc) — 2024-02-22T10:39:00Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  
  
  > Right, this is true in the other cases as well, I'm wondering what makes this situation different.
  
  We can do something to avoid it. 
  
  The function was refactored, please check it.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T10:43:01Z

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@trasc](https://github.com/trasc) — 2024-02-22T10:43:00Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  Only the admission check controller is managing the admission check state.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T10:45:27Z

- **pkg/controller/admissionchecks/multikueue/workload.go:338** by [@mimowo](https://github.com/mimowo) — 2024-02-22T10:45:27Z
```diff
@@ -316,15 +335,32 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			}
 			// update the message
 			acs.Message = fmt.Sprintf("The workload got reservation on %q", reservingRemote)
+			// update the transition time
```

  The comment explains perfectly readable line, but it is more important to write **why**, because it often depends on interaction with other parts of the system, which is not clear by looking at the lines below. Tbh, I don't know why it is updated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T10:46:55Z

- **pkg/controller/admissionchecks/multikueue/workload.go:302** by [@mimowo](https://github.com/mimowo) — 2024-02-22T10:46:55Z
```diff
@@ -279,11 +293,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: fmt.Sprintf("From remote %q: %s", remote, remoteFinishedCond.Message),
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
```

  Oh, the code with the check is gone, feel free to resolve.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T10:48:08Z

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@mimowo](https://github.com/mimowo) — 2024-02-22T10:48:08Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  Hm, setting Retry to Pending feels like a job the workload_controller could do on eviction. Is it discussed in KEP?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T11:42:16Z

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@trasc](https://github.com/trasc) — 2024-02-22T11:42:16Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  When the watch starts, events are generated for the already exiting objects, hence at that point in time when the worker is alive for some time we wold get a reconcile also.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T11:51:56Z

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@mimowo](https://github.com/mimowo) — 2024-02-22T11:51:56Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  But we still would not observe the cluster being available for 10min in between, This seems fragile to me. I think it is better to periodically check the status, then if the ping fails, we count 15min since the last successful status check.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T12:09:32Z

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@trasc](https://github.com/trasc) — 2024-02-22T12:09:32Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  This is not in the scope of this PR. 
  
  `Retry` is the way of keeping a wl for being requeued based on a admission check controller logic.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T12:22:44Z

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@trasc](https://github.com/trasc) — 2024-02-22T12:22:44Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  This is the best I can do.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T12:32:46Z

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@mimowo](https://github.com/mimowo) — 2024-02-22T12:32:46Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  You mean in this PR / approach on in general we should not track the availability of a worker?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T12:36:25Z

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@mimowo](https://github.com/mimowo) — 2024-02-22T12:36:25Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  I'm wondering if this is needed in this PR. If we moved the logic to workload_controller, it would not be necessary here. 
  
  @alculquicondor which component should be responsible to transitioning Retry to Pending when a workload is evicted: the individual AC controllers, or this is something the workload_controller could handle?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T12:39:59Z

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@trasc](https://github.com/trasc) — 2024-02-22T12:39:59Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  **This is not in the scope of this PR.**
  
  Doing so will prevent other admission check controllers from being able to hold the workload out of the queue.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T12:41:01Z

- **pkg/controller/admissionchecks/multikueue/workload.go:321** by [@trasc](https://github.com/trasc) — 2024-02-22T12:41:01Z
```diff
@@ -279,11 +292,43 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			Reason:  remoteFinishedCond.Reason,
 			Message: remoteFinishedCond.Message,
 		})
-		return a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
+		return reconcile.Result{}, a.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName+"-finish"), client.ForceOwnership)
 	}
 
 	hasReserving, reservingRemote := group.FirstReserving()
 
+	// if the reserving workload is out of sync, remove it
+	if hasReserving {
+		outOfSync := group.local == nil || !equality.Semantic.DeepEqual(group.local.Spec, group.remotes[reservingRemote].Spec)
+		if outOfSync {
+			if err := client.IgnoreNotFound(group.RemoveRemoteObjects(ctx, reservingRemote)); err != nil {
+				log.V(2).Error(err, "Deleting out of sync remote objects", "remote", reservingRemote)
+				return reconcile.Result{}, err
+			}
+			group.remotes[reservingRemote] = nil
+
+			// check if another one exists
+			hasReserving, reservingRemote = group.FirstReserving()
+		}
+	}
+
+	// If there is no reserving and the AC is ready, the connection with the reserving remote might
+	// be lost, keep the workload admitted for keepReadyTimeout and put it back in the queue after that.
+	if !hasReserving && acs.State == kueue.CheckStateReady {
+		remainingWaitTime := a.workerLostTimeout - time.Since(acs.LastTransitionTime.Time)
+		if remainingWaitTime > 0 {
+			log.V(3).Info("Reserving remote lost, retry", "retryAfter", remainingWaitTime)
+			return reconcile.Result{RequeueAfter: remainingWaitTime}, nil
```

  In this PR, we should talk about this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T13:54:04Z

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@mimowo](https://github.com/mimowo) — 2024-02-22T13:54:04Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  We discussed this offline, and I looked in the KEP, The Retry seems to be associated with `RetryDelayMinutes` which determines the time after which the workload is re-admitted (this could probably be done by workload controller, but TBD). Since we don't support the mechanism yet changing the Retry -> Pending is responsibility of the individual AC controllers. Feel free to resolve.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-22T18:55:47Z

- **apis/config/v1beta1/configuration_types.go:220** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T18:37:57Z
```diff
@@ -215,6 +215,11 @@ type MultiKueue struct {
 	// them if their local counterpart no longer exists.
 	// +optional
 	Origin *string `json:"origin,omitempty"`
+
+	// WorkerLostTimeout defines the time a local workload's multikueue admission check state is kept Ready
+	// if the connection with its reserving worker cluster is lost.
```

  explain the behavior when the field is left null, or the default.

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:106** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T18:42:23Z
```diff
@@ -90,28 +99,25 @@ func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 				}
 			}
 		}
+		unusableClustersCount := len(missingClusters) + len(inactiveClusters)
+		if unusableClustersCount > 0 {
+			if unusableClustersCount < len(cfg.Spec.Clusters) {
+				// keep it partially active
+				newCondition.Reason = "PartiallyActive"
```

  ```suggestion
  				newCondition.Reason = "SomeActiveClusters"
  ```

- **pkg/controller/admissionchecks/multikueue/workload.go:261** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T18:53:24Z
```diff
@@ -254,7 +257,17 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 				log.V(2).Error(err, "Deleting remote workload", "workerCluster", rem)
 			}
 		}
-		return errors.Join(errs...)
+
+		if !workload.HasQuotaReservation(group.local) && acs.State == kueue.CheckStateRetry {
```

  ack

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T20:56:35Z

- **apis/config/v1beta1/configuration_types.go:220** by [@trasc](https://github.com/trasc) — 2024-02-22T20:47:52Z
```diff
@@ -215,6 +215,11 @@ type MultiKueue struct {
 	// them if their local counterpart no longer exists.
 	// +optional
 	Origin *string `json:"origin,omitempty"`
+
+	// WorkerLostTimeout defines the time a local workload's multikueue admission check state is kept Ready
+	// if the connection with its reserving worker cluster is lost.
```

  done

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:106** by [@trasc](https://github.com/trasc) — 2024-02-22T20:52:13Z
```diff
@@ -90,28 +99,25 @@ func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 				}
 			}
 		}
+		unusableClustersCount := len(missingClusters) + len(inactiveClusters)
+		if unusableClustersCount > 0 {
+			if unusableClustersCount < len(cfg.Spec.Clusters) {
+				// keep it partially active
+				newCondition.Reason = "PartiallyActive"
```

  done , but why?

- **pkg/controller/admissionchecks/multikueue/workload.go:338** by [@trasc](https://github.com/trasc) — 2024-02-22T20:54:07Z
```diff
@@ -316,15 +335,32 @@ func (a *wlReconciler) reconcileGroup(ctx context.Context, group *wlGroup) error
 			}
 			// update the message
 			acs.Message = fmt.Sprintf("The workload got reservation on %q", reservingRemote)
+			// update the transition time
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-23T14:52:05Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:106** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-23T14:52:05Z
```diff
@@ -90,28 +99,25 @@ func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 				}
 			}
 		}
+		unusableClustersCount := len(missingClusters) + len(inactiveClusters)
+		if unusableClustersCount > 0 {
+			if unusableClustersCount < len(cfg.Spec.Clusters) {
+				// keep it partially active
+				newCondition.Reason = "PartiallyActive"
```

  Active=True, Reason=SomeActiveClusters seems more readable without having to dig into the details.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-23T14:54:54Z

API LGTM
I'll approve the implementation when @mimowo gives LGTM
