# PR #1684: Extend documentation on pod group support [pod groups]

**Summary**: Extend documentation on pod group support [pod groups]

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1684

**Last updated**: 2024-02-10T11:47:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `pod-groups-docs` → `main`
- **Created**: 2024-02-05T11:27:53Z
- **Updated**: 2024-02-10T11:47:39Z
- **Merged**: 2024-02-09T14:20:00Z
- **Closed**: 2024-02-09T14:20:00Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 2   **+108 / -4**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #976

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-05T11:27:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | aa9fb104582ad0b63eaa99176bb45adaf6765b45 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65c5f6512795bc0008e1c113 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1684--kubernetes-sigs-kueue.netlify.app/docs/tasks/run_plain_pods](https://deploy-preview-1684--kubernetes-sigs-kueue.netlify.app/docs/tasks/run_plain_pods) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE2ODQtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.SvkO74OFT91yS69f7dO4lknlKLzjuU8XM5ICJQnQ9zY)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-05T11:37:40Z

/assign @tenzen-y @alculquicondor

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-02-05T14:13:18Z

lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-08T19:44:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1684#pullrequestreview-1871121143" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1684#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1684#pullrequestreview-1869954006" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-09T09:55:50Z

> /hold for comments

/hold cancel 

Comments addressed, PTAL.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-09T14:15:18Z

LGTM label has been added.  <details>Git tree hash: 17e670abb3552cbea3c6a5e1c15d760489a2c931</details>

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-05T13:29:29Z

- **site/content/en/docs/tasks/run_plain_pods.md:6** by [@trasc](https://github.com/trasc) — 2024-02-05T13:28:44Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run Jobs represented by plain pods, either single pods, or pod groups.
```

  maybe
  ```suggestion
    Run a group of Pods as a Kueue managed Job.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-06T12:15:52Z

- **site/content/en/docs/tasks/run_plain_pods.md:6** by [@mimowo](https://github.com/mimowo) — 2024-02-06T12:15:52Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run Jobs represented by plain pods, either single pods, or pod groups.
```

  I would like to emphasize that you can run either a single pod, or a group of pods.
  Maybe: "Run a single Pod, or a group of Pods, as a Kueue-managed Job.". WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-07T14:47:59Z

Also, should we rephrase Line 97 with "Example single Pod"?

- **site/content/en/docs/tasks/run_plain_pods.md:6** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-07T13:59:22Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run a single Pod, or a group of Pods as a Kueue-managed Job.
```

  It looks like a bit long. This paragraph may be folded in many environments.
  Should we keep this title and extend explanations in the Line 9-11?

- **site/content/en/docs/tasks/run_plain_pods.md:125** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-07T14:41:39Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
```

  ```suggestion
  Kueue provides only the minimal required functionality of running pod groups,
  ```

- **site/content/en/docs/tasks/run_plain_pods.md:138** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-07T14:42:54Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
```

  Great note

- **site/content/en/docs/tasks/run_plain_pods.md:140** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-07T14:45:56Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
+
+**NOTE:** We recommend migration to using Job-level APIs for managing sets of pods.
```

  Should we mention that `Job-level` means using already adapted Jobs like batch/job and day job, or adapting custom resources to the kueue?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-02-07T14:58:22Z

- **site/static/examples/pods-kueue/kueue-pod-group.yaml:38** by [@PBundyra](https://github.com/PBundyra) — 2024-02-07T14:57:03Z
```diff
@@ -0,0 +1,42 @@
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  generateName: sample-pod-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: "sample-group"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+spec:
+  containers:
+  - name: sleep
+    image: busybox
+    command:
+      - sleep
+    args:
+      - 3s
+    resources:
+      requests:
+        cpu: 3
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  generateName: sample-pod-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: "sample-group"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+spec:
+  containers:
+  - name: sleep
+    image: busybox
+    command:
+      - sleep
+    args:
+      - 3s
+    resources:
+      requests:
+        cpu: 3
```

  Should the definition of the pod group be there twice?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-02-07T14:59:48Z

- **site/content/en/docs/tasks/run_plain_pods.md:144** by [@PBundyra](https://github.com/PBundyra) — 2024-02-07T14:59:48Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionality of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
+
+**NOTE:** We recommend migration to using Job-level APIs for managing sets of pods.
+
+## Example Pod Group
+
+Here is a sample Pod that just sleeps for a few seconds:
```

  ```suggestion
  Here is a sample Pod Group that just sleeps for a few seconds:
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-02-07T15:00:00Z

- **site/content/en/docs/tasks/run_plain_pods.md:148** by [@PBundyra](https://github.com/PBundyra) — 2024-02-07T15:00:00Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionality of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
+
+**NOTE:** We recommend migration to using Job-level APIs for managing sets of pods.
+
+## Example Pod Group
+
+Here is a sample Pod that just sleeps for a few seconds:
+
+{{< include "examples/pods-kueue/kueue-pod-group.yaml" "yaml" >}}
+
+You can create the Pod using the following command:
```

  ```suggestion
  You can create the Pod Group using the following command:
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-07T16:23:20Z

- **site/content/en/docs/tasks/run_plain_pods.md:6** by [@mimowo](https://github.com/mimowo) — 2024-02-07T16:23:20Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run a single Pod, or a group of Pods as a Kueue-managed Job.
```

  Note this is description field, not title. For descriptions I think it is ok to be longer, we have longer descriptions for other pages, for example: https://kueue.sigs.k8s.io/docs/concepts/admission_check/

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-07T16:30:18Z

- **site/content/en/docs/tasks/run_plain_pods.md:140** by [@mimowo](https://github.com/mimowo) — 2024-02-07T16:30:18Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
+
+**NOTE:** We recommend migration to using Job-level APIs for managing sets of pods.
```

  It does not matter if custom in-house CRD or already supported, what matters is that there is the job API object that maintains the pods. I guess we could list some supported integrations, but not sure this would be distraction here. I'm ok either way.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-07T16:32:41Z

- **site/content/en/docs/tasks/run_plain_pods.md:6** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-07T16:32:41Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run a single Pod, or a group of Pods as a Kueue-managed Job.
```

  Oh, I see. It makes sense. Never mind this comment.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-07T16:33:08Z

- **site/content/en/docs/tasks/run_plain_pods.md:6** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-07T16:33:08Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run a single Pod, or a group of Pods as a Kueue-managed Job.
```

  I thought this is used in the title.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-07T16:36:13Z

- **site/static/examples/pods-kueue/kueue-pod-group.yaml:38** by [@mimowo](https://github.com/mimowo) — 2024-02-07T16:36:12Z
```diff
@@ -0,0 +1,42 @@
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  generateName: sample-pod-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: "sample-group"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+spec:
+  containers:
+  - name: sleep
+    image: busybox
+    command:
+      - sleep
+    args:
+      - 3s
+    resources:
+      requests:
+        cpu: 3
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  generateName: sample-pod-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: "sample-group"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+spec:
+  containers:
+  - name: sleep
+    image: busybox
+    command:
+      - sleep
+    args:
+      - 3s
+    resources:
+      requests:
+        cpu: 3
```

  It does not need to be, we use `generateName`, so we could just ask to run the kubectl create twice. 
  
  However, I think from the docs point of view it can be beneficial to show they may differ, and play slightly different roles. I will suggest a change to modify one of them.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-07T16:40:46Z

- **site/static/examples/pods-kueue/kueue-pod-group.yaml:38** by [@mimowo](https://github.com/mimowo) — 2024-02-07T16:40:46Z
```diff
@@ -0,0 +1,42 @@
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  generateName: sample-pod-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: "sample-group"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+spec:
+  containers:
+  - name: sleep
+    image: busybox
+    command:
+      - sleep
+    args:
+      - 3s
+    resources:
+      requests:
+        cpu: 3
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  generateName: sample-pod-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: "sample-group"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+spec:
+  containers:
+  - name: sleep
+    image: busybox
+    command:
+      - sleep
+    args:
+      - 3s
+    resources:
+      requests:
+        cpu: 3
```

  Updated, PTAL

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-07T16:44:53Z

- **site/content/en/docs/tasks/run_plain_pods.md:140** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-07T16:44:53Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
+
+**NOTE:** We recommend migration to using Job-level APIs for managing sets of pods.
```

  I often receive questions: what is `Job level`? Does `Job level` mean batch/job?
  So, I'd like to add explanations that the `Job-level` means higher level API (CRD) rather than pods, and it directly or indirectly manages pods.
  
  Also, as you say, putting already supported jobs isn't needed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-07T19:03:12Z

I forgot to send my comments yesterday :(

- **site/content/en/docs/tasks/run_plain_pods.md:6** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:08:37Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run a single Pod, or a group of Pods as a Kueue-managed Job.
```

  ```suggestion
    Run a single Pod, or a group of Pods as a Kueue-managed job.
  ```

- **site/content/en/docs/tasks/run_plain_pods.md:109** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:19:11Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
```

  ```suggestion
  ## Running a group of Pods to be admitted together
  ```
  
  Also change the subtitle for the single pod scenario to match this format.

- **site/content/en/docs/tasks/run_plain_pods.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:19:47Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run a single Pod, or a group of Pods as a Kueue-managed Job.
 ---
 
 This page shows how to leverage Kueue's scheduling and resource management capabilities when running plain Pods.
```

  explain that there are 2 possible patterns, and link to the respective subtitles.

- **site/content/en/docs/tasks/run_plain_pods.md:130** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:22:21Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
```

  ```suggestion
  As a consequence of this design decision, Kueue does not re-implement core
  functionalities that are available in the Kubernetes Job API, such as advanced retry
  ```

- **site/content/en/docs/tasks/run_plain_pods.md:133** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:22:40Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
```

  ```suggestion
  This design choice impacts the scenario of
  ```

- **site/content/en/docs/tasks/run_plain_pods.md:136** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:24:38Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
```

  ```suggestion
  When a Kueue needs to preempt a workload that represents a pod group, kueue sends delete requests
  for all of the pods in the group. It is the responsibility of the user or controller that created the original
  pods to create replacement Pods.
  ```

- **site/content/en/docs/tasks/run_plain_pods.md:138** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:25:42Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
```

  Also explain how to terminate a group: delete the Workload or add the annotation `kueue.x-k8s.io/retriable-in-group: false` to at least one of the Pods (original or replacement).

- **site/content/en/docs/tasks/run_plain_pods.md:167** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-06T19:26:40Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
+
+**NOTE:** We recommend migration to using Job-level APIs for managing sets of pods.
+
+## Example Pod Group
+
+Here is a sample Pod that just sleeps for a few seconds:
+
+{{< include "examples/pods-kueue/kueue-pod-group.yaml" "yaml" >}}
+
+You can create the Pod using the following command:
+```sh
+kubectl apply -f kueue-pod-group.yaml
+```
```

  Also explain the format of the Workload name, and how to query it, as it might be relevant to check for the user to check the Workload status or delete it.

- **site/content/en/docs/tasks/run_plain_pods.md:140** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-07T19:01:33Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
+
+**NOTE:** We recommend migration to using Job-level APIs for managing sets of pods.
```

  I agree with @tenzen-y. Maybe something like:
  
  `We recommend using the kubernetes Job API or similar CRDs such as JobSet, MPIJob, RayJob, etc.` and link to the documentation for all supported CRDs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T08:46:17Z

- **site/content/en/docs/tasks/run_plain_pods.md:140** by [@mimowo](https://github.com/mimowo) — 2024-02-08T08:46:17Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
+
+**NOTE:** We recommend migration to using Job-level APIs for managing sets of pods.
```

  ok, sgtm, adjusting

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T08:47:39Z

- **site/content/en/docs/tasks/run_plain_pods.md:6** by [@mimowo](https://github.com/mimowo) — 2024-02-08T08:47:39Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run a single Pod, or a group of Pods as a Kueue-managed Job.
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T08:50:36Z

- **site/content/en/docs/tasks/run_plain_pods.md:109** by [@mimowo](https://github.com/mimowo) — 2024-02-08T08:50:36Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T08:53:36Z

- **site/content/en/docs/tasks/run_plain_pods.md:9** by [@mimowo](https://github.com/mimowo) — 2024-02-08T08:53:36Z
```diff
@@ -1,9 +1,9 @@
 ---
-title: "Run A Plain Pod"
+title: "Run Plain Pods"
 date: 2023-09-27
 weight: 6
 description: >
-  Run a Kueue scheduled Pod.
+  Run a single Pod, or a group of Pods as a Kueue-managed Job.
 ---
 
 This page shows how to leverage Kueue's scheduling and resource management capabilities when running plain Pods.
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T08:55:11Z

- **site/content/en/docs/tasks/run_plain_pods.md:130** by [@mimowo](https://github.com/mimowo) — 2024-02-08T08:55:11Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T08:55:49Z

- **site/content/en/docs/tasks/run_plain_pods.md:133** by [@mimowo](https://github.com/mimowo) — 2024-02-08T08:55:49Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T08:57:14Z

- **site/content/en/docs/tasks/run_plain_pods.md:136** by [@mimowo](https://github.com/mimowo) — 2024-02-08T08:57:14Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T09:28:01Z

- **site/content/en/docs/tasks/run_plain_pods.md:167** by [@mimowo](https://github.com/mimowo) — 2024-02-08T09:28:01Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
+
+**NOTE:** We recommend migration to using Job-level APIs for managing sets of pods.
+
+## Example Pod Group
+
+Here is a sample Pod that just sleeps for a few seconds:
+
+{{< include "examples/pods-kueue/kueue-pod-group.yaml" "yaml" >}}
+
+You can create the Pod using the following command:
+```sh
+kubectl apply -f kueue-pod-group.yaml
+```
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T09:59:53Z

- **site/content/en/docs/tasks/run_plain_pods.md:138** by [@mimowo](https://github.com/mimowo) — 2024-02-08T09:59:53Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
```

  Added a note on termination. 
  
  However, when I tested the annotation `kueue.x-k8s.io/retriable-in-group: false` it didn't terminate my workload. I will investigate why.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T10:23:41Z

- **site/content/en/docs/tasks/run_plain_pods.md:138** by [@mimowo](https://github.com/mimowo) — 2024-02-08T10:23:41Z
```diff
@@ -105,3 +105,47 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Pod Group definition
+
+In order to run a set of pods as a single unit, called Pod Group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionallity of running pod groups,
+just for the need of environments where the pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision Kueue does not re-implement core
+functionalities that are available at the Job-level API, such as advanced retry
+policies. In particular, Kueue does not re-create failed pods.
+
+Note that, this design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a workload represented by the pod group is preempted all of its pods
+are killed by Kueue (by delete requests). However, later, when the workload is
+re-admitted, Kueue will not re-create the terminated pods. This task is left to
+the user (or the external controller).
```

  oh, I see, it works, but I have to terminate all pods in the group, not just the one with annotation. 
  Updated the docs accordingly

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-08T11:36:30Z

Otherwise, lgtm

/approve
leave lgtm on @alculquicondor

- **site/content/en/docs/tasks/run_plain_pods.md:143** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-08T11:35:15Z
```diff
@@ -105,3 +108,66 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Running a group of Pods to be admitted together
+
+In order to run a set of Pods as a single unit, called Pod group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionality of running Pod groups,
+just for the need of environments where the Pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision, Kueue does not re-implement core
+functionalities that are available in the Kubernetes Job API, such as advanced retry
+policies. In particular, Kueue does not re-create failed Pods.
+
+This design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a Kueue needs to preempt a workload that represents a Pod group, kueue sends
+delete requests for all of the Pods in the group. It is the responsibility of the
+user or controller that created the original Pods to create replacement Pods.
+
+**NOTE:** We recommend using the kubernetes Job API or similar CRDs such as
+JobSet, MPIJob, RayJob, etc.
```

  Tied https://kueue.sigs.k8s.io/docs/tasks/#batch-user here would be better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T12:01:16Z

- **site/content/en/docs/tasks/run_plain_pods.md:143** by [@mimowo](https://github.com/mimowo) — 2024-02-08T12:01:16Z
```diff
@@ -105,3 +108,66 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Running a group of Pods to be admitted together
+
+In order to run a set of Pods as a single unit, called Pod group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
+
+Kueue provides only the minimal required functionality of running Pod groups,
+just for the need of environments where the Pods are managed by external
+controllers directly, without a Job-level CRD.
+
+As a consequence of this design decision, Kueue does not re-implement core
+functionalities that are available in the Kubernetes Job API, such as advanced retry
+policies. In particular, Kueue does not re-create failed Pods.
+
+This design choice impacts the scenario of
+[preemption](/docs/concepts/cluster_queue/#preemption).
+When a Kueue needs to preempt a workload that represents a Pod group, kueue sends
+delete requests for all of the Pods in the group. It is the responsibility of the
+user or controller that created the original Pods to create replacement Pods.
+
+**NOTE:** We recommend using the kubernetes Job API or similar CRDs such as
+JobSet, MPIJob, RayJob, etc.
```

  Updated. PTAL.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-08T19:44:47Z

/approve
/hold for comments

- **site/content/en/docs/tasks/run_plain_pods.md:126** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-08T19:41:11Z
```diff
@@ -105,3 +108,66 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Running a group of Pods to be admitted together
+
+In order to run a set of Pods as a single unit, called Pod group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
```

  ```suggestion
  ### Feature limitations
  ```
  
  Same below

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-09T09:54:48Z

- **site/content/en/docs/tasks/run_plain_pods.md:126** by [@mimowo](https://github.com/mimowo) — 2024-02-09T09:54:48Z
```diff
@@ -105,3 +108,66 @@ You can create the Pod using the following command:
 # Create the pod
 kubectl apply -f kueue-pod.yaml
 ```
+
+## Running a group of Pods to be admitted together
+
+In order to run a set of Pods as a single unit, called Pod group, add the
+"pod-group-name" label, and the "pod-group-total-count" annotation to all
+members of the group, consistently:
+
+```yaml
+metadata:
+  labels:
+    kueue.x-k8s.io/pod-group-name: "group-name"
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "2"
+```
+
+## Feature limitations
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-09T14:15:13Z

/lgtm
