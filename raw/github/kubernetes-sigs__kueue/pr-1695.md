# PR #1695: Use a uniquely identifying selector based on recommended labels for Kueue deployment

**Summary**: Use a uniquely identifying selector based on recommended labels for Kueue deployment

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1695

**Last updated**: 2024-05-28T18:33:42Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@astefanutti](https://github.com/astefanutti)
- **Branch**: `pr-12` → `main`
- **Created**: 2024-02-06T16:06:34Z
- **Updated**: 2024-05-28T18:33:42Z
- **Merged**: 2024-03-05T08:32:09Z
- **Closed**: 2024-03-05T08:32:09Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 71   **+123 / -38**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

This PR proposes to change the default label selector currently used by the Kueue deployment from:

```yaml
control-plane: controller-manager
```

to the following selector, that uses Kubernetes recommended labels to uniquely identify Kueue Pods:

```yaml
app.kubernetes.io/name: kueue
app.kubernetes.io/component: controller
```

#### Which issue(s) this PR fixes:

Fixes #1685

#### Special notes for your reviewer:

The script that updates the Helm charts has to be adapted to apply parameterised labels selectors.

#### Does this PR introduce a user-facing change?

```release-note
Add recommended Kubernetes labels to uniquely identify Pods and other resources installed with Kueue.
The Deployment selector remains unchanged to allow for a seamless upgrade.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-06T16:06:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0915e3b60fa8c63ecb1a2ac611eda3abd8ab387f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65e6d12828f25d00084a6b53 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-06T16:06:43Z

Hi @astefanutti. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-09T20:01:15Z

/ok-to-test

### Comment by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T15:46:55Z

/test pull-kueue-test-e2e-main-1-27

### Comment by [@astefanutti](https://github.com/astefanutti) — 2024-02-27T08:37:30Z

/test pull-kueue-test-integration-main

### Comment by [@astefanutti](https://github.com/astefanutti) — 2024-03-04T17:44:12Z

/retest

### Comment by [@astefanutti](https://github.com/astefanutti) — 2024-03-04T18:13:19Z

/test pull-kueue-verify-main

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T18:15:20Z

It looks like something went rogue in the bot.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T18:24:24Z

PTAL https://github.com/kubernetes/test-infra/pull/32152

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-05T08:29:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1695#" title="Author self-approved">astefanutti</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1695#pullrequestreview-1916224076" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-05T08:29:15Z

LGTM label has been added.  <details>Git tree hash: da7d427c1e61473d95ef960c610b8dfd6c313c96</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-08T17:55:12Z

I'm worried that this change can cause significant friction to upgrade.

Should we consider a revert? Is anyone familiar with how other projects have achieved this change with minimal disruption?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-08T18:00:12Z

> I'm worried that this change can cause significant friction to upgrade.
> 
> Should we consider a revert? Is anyone familiar with how other projects have achieved this change with minimal disruption?

Your concern is right. Actually, the `apply` command should be failed since updating the label is not allowed.
In my OSS experience, I usually follow the deprecation steps like:

1. Support both (old and new) labels for 1 or 2 releases and notice breaking changes to users.
2. After 1 or 2 releases pass away, old labels are removed.

@alculquicondor I'm curious about whether we can do a similar approach.
WDYT?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-08T18:56:25Z

Overall, that sounds good, but: at some point, we will have to update the Deployment object. And that will always be a breaking change.
Unless, in addition to your plan, we create a new Deployment and change the existing one to have 0 replicas?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T06:30:06Z

> Overall, that sounds good, but: at some point, we will have to update the Deployment object. And that will always be a breaking change.

You're right.

> Unless, in addition to your plan, we create a new Deployment and change the existing one to have 0 replicas?

That makes sense.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-09T18:18:01Z

So for now, let's revert the selector change and add the old and new labels?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-10T15:19:28Z

> So for now, let's revert the selector change and add the old and new labels?

SGTM Just confirmation. After reverting this PR, we will create a new PR with manifests using new labels, right?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-10T15:26:17Z

Well, this PR has a few more changes.

Is it easier to just manually undo the changes to the Deployment selector?

Is this something you can take @tenzen-y ?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-23T16:48:58Z

The partial revert is not trivial. I think we should do a full revert https://github.com/kubernetes-sigs/kueue/pull/2246#issuecomment-2127624427

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-24T04:01:26Z

> The partial revert is not trivial. I think we should do a full revert [#2246 (comment)](https://github.com/kubernetes-sigs/kueue/pull/2246#issuecomment-2127624427)

I'm ok with completely reverting.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-28T18:33:36Z

Because we merged #1695 

/release-note-edit
```release-note
Add recommended Kubernetes labels to uniquely identify Pods and other resources installed with Kueue.
The Deployment selector remains unchanged to allow for a seamless upgrade.
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-14T09:57:16Z

Generally lgtm, some asks for explanations as I'm not that familiar with the helm scripts yet

- **charts/kueue/templates/_helpers.tpl:50** by [@mimowo](https://github.com/mimowo) — 2024-02-14T09:27:13Z
```diff
@@ -47,7 +47,7 @@ Selector labels
 */}}
 {{- define "kueue.selectorLabels" -}}
 app.kubernetes.io/name: {{ include "kueue.name" . }}
-app.kubernetes.io/instance: {{ .Release.Name }}
```

  Can you explain why this is removed?

- **charts/kueue/templates/prometheus/monitor.yaml:19** by [@mimowo](https://github.com/mimowo) — 2024-02-14T09:34:57Z
```diff
@@ -16,5 +16,5 @@ spec:
       insecureSkipVerify: true
   selector:
     matchLabels:
-      control-plane: controller-manager
+    {{- include "kueue.selectorLabels" . | nindent 6 }}
```

  Is this actually a fix? It seems that we were already doing `include "kueue.selectorLabels"` in other places where this PR removes `control-plane: controller-manager`?

- **charts/kueue/templates/rbac/batch_admin_role.yaml:5** by [@mimowo](https://github.com/mimowo) — 2024-02-14T09:36:16Z
```diff
@@ -2,6 +2,8 @@
 apiVersion: rbac.authorization.k8s.io/v1
 kind: ClusterRole
 metadata:
+  labels:
```

  Could you explain why this is needed (and for other RBAC files)?

- **config/components/prometheus/monitor.yaml:20** by [@mimowo](https://github.com/mimowo) — 2024-02-14T09:44:11Z
```diff
@@ -17,4 +14,5 @@ spec:
         insecureSkipVerify: true
   selector:
     matchLabels:
-      control-plane: controller-manager
```

  IIUC we can remove the `labels.contro-plane` label thanks to the use of commenLabels in the `kustomization.yaml`, but if so, why the labels are added here explicitly?

- **hack/update-helm.sh:160** by [@mimowo](https://github.com/mimowo) — 2024-02-14T09:56:36Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  The sed manipulations are tricky. Can this corrupt the output file if we already have some metadata.labels? 
  I'm thinking if there are other tools which understand yaml, like YQ which could be employed to do this, but not sure, can you make a short summary of alternatives?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-15T10:49:35Z

- **charts/kueue/templates/prometheus/monitor.yaml:19** by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T10:49:35Z
```diff
@@ -16,5 +16,5 @@ spec:
       insecureSkipVerify: true
   selector:
     matchLabels:
-      control-plane: controller-manager
+    {{- include "kueue.selectorLabels" . | nindent 6 }}
```

  Yes, `{{- include "kueue.selectorLabels" . | nindent 6 }}` should have been there already.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-15T10:52:12Z

- **charts/kueue/templates/prometheus/monitor.yaml:19** by [@mimowo](https://github.com/mimowo) — 2024-02-15T10:52:12Z
```diff
@@ -16,5 +16,5 @@ spec:
       insecureSkipVerify: true
   selector:
     matchLabels:
-      control-plane: controller-manager
+    {{- include "kueue.selectorLabels" . | nindent 6 }}
```

  I see, could the fact it was not there cause some user-facing impact? I'm wondering if this fix should go in a separate PR, potentially as part of the 0.6 release (cherry-picked). 
  
  EDIT: Maybe we merge this fix first to handle it independently from this PR?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-15T10:54:17Z

- **charts/kueue/templates/rbac/batch_admin_role.yaml:5** by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T10:54:17Z
```diff
@@ -2,6 +2,8 @@
 apiVersion: rbac.authorization.k8s.io/v1
 kind: ClusterRole
 metadata:
+  labels:
```

  It's not strictly needed, though this can be convenient to have these labels added, for example, for clean-up, so it's possible to perform a delete collection with the appropriate label selector.
  
  Also, it makes the Helm charts consistent with Kustomize, that applies the common labels to all resources, including RBAC ones.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-15T10:57:14Z

- **charts/kueue/templates/rbac/batch_admin_role.yaml:5** by [@mimowo](https://github.com/mimowo) — 2024-02-15T10:57:14Z
```diff
@@ -2,6 +2,8 @@
 apiVersion: rbac.authorization.k8s.io/v1
 kind: ClusterRole
 metadata:
+  labels:
```

  Sgtm, thanks for explaining

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-15T10:58:27Z

- **config/components/prometheus/monitor.yaml:20** by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T10:58:27Z
```diff
@@ -17,4 +14,5 @@ spec:
         insecureSkipVerify: true
   selector:
     matchLabels:
-      control-plane: controller-manager
```

  You're right, these labels should not theoretically be added explicitly. Unfortunately, Kustomize common labels plugin does not currently process label selectors in custom resources like ServiceMonitor.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-15T11:07:03Z

- **hack/update-helm.sh:160** by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T11:07:02Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  I agree. I tried first to use YQ, but could not find a solution. I think the fundamental issue is that Helm templates are not valid YAML, so in that particular case - adding a Helm template entry in a map - I've failed to implement it using JQ.
  
  An alternative might be to revisit how the Kustomize manifests -> Helm charts script works, by taking the "rendered" Kustomize resources as input instead of the "raw" files themselves.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-15T11:17:57Z

- **config/components/prometheus/monitor.yaml:20** by [@mimowo](https://github.com/mimowo) — 2024-02-15T11:17:57Z
```diff
@@ -17,4 +14,5 @@ spec:
         insecureSkipVerify: true
   selector:
     matchLabels:
-      control-plane: controller-manager
```

  sgtm, thanks for explaining

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-15T11:34:20Z

- **hack/update-helm.sh:160** by [@mimowo](https://github.com/mimowo) — 2024-02-15T11:34:20Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  > I agree. I tried first to use YQ, but could not find a solution. I think the fundamental issue is that Helm templates are not valid templates, so in that particular case - adding a Helm template entry in a map - I've failed to implement it using JQ.
  
  I see, but this is interesting why we can use YQ a couple of lines above in the same script, using `$YQ -N -i`. Is it because the yaml files get invalid yaml's while being processed?
  
  > An alternative might be to revisit how the Kustomize manifests -> Helm charts script works, by taking the "rendered" Kustomize resources as input instead of the "raw" files themselves.
  
  Not sure I understand the idea, IIUC if we used rendered files, then wouldn't we hit the same issue, that some replacements corrupt the file, and yq cannot be used more?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-15T11:35:41Z

- **charts/kueue/templates/_helpers.tpl:50** by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T11:35:40Z
```diff
@@ -47,7 +47,7 @@ Selector labels
 */}}
 {{- define "kueue.selectorLabels" -}}
 app.kubernetes.io/name: {{ include "kueue.name" . }}
-app.kubernetes.io/instance: {{ .Release.Name }}
```

  Mostly for consistency with the Kustomize manifests. Also the only case where I could this be needed would be with two different Kueue Deployment instances in the same namespace, which I doubt is something is supported nor we want to support?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-15T11:47:10Z

- **hack/update-helm.sh:160** by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T11:47:10Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  > I see, but this is interesting why we can use YQ a couple of lines above in the same script, using $YQ -N -i. Is it because the yaml files get invalid yaml's while being processed?
  
  You're right, the existing `$YQ -N -I` set single field values and that works fine. But for inlining labels, it touches the structure of the YAML document itself. It possibly is my fairly limited knowledge about YQ, but an explanation why that might not be possible is that for the former it's valid YAML being processed, e.g.:
  
  ```
  metadata:
    namespace: "{{ .Release.Namespace }}"
  ```
  
  While for the latter, it's not:
  
  ```
  labels:
  {{- include "kueue.labels" . | nindent 4 }}
  ```

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-15T11:51:25Z

- **hack/update-helm.sh:160** by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T11:51:24Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  > Not sure I understand the idea, IIUC if we used rendered files, then wouldn't we hit the same issue, that some replacements corrupt the file, and yq cannot be used more?
  
  You're right, I realise my idea would be more the other way, i.e., rendered Helm Charts  -> Kustomize manifests, so it's always valid YAML that's processed. But using Helm manifests as the source of truth is probably very debatable and not worth the effort :).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-15T11:54:08Z

- **hack/update-helm.sh:160** by [@mimowo](https://github.com/mimowo) — 2024-02-15T11:54:08Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  This makes sense, but I'm also not sure how to do it better, let's see if @tenzen-y or @alculquicondor have some ideas.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-15T12:03:40Z

- **charts/kueue/templates/_helpers.tpl:50** by [@mimowo](https://github.com/mimowo) — 2024-02-15T12:03:40Z
```diff
@@ -47,7 +47,7 @@ Selector labels
 */}}
 {{- define "kueue.selectorLabels" -}}
 app.kubernetes.io/name: {{ include "kueue.name" . }}
-app.kubernetes.io/instance: {{ .Release.Name }}
```

  I see, I'm not sure we want to support it (I would prefer not too, but didn't do archeology on this). 
  
  If the change is not strictly needed for the functionality of this PR I would suggest to drop it here, and move to a dedicated cleanup PR, maybe there are other inconsistencies which could be handled there?

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-15T13:19:53Z

- **charts/kueue/templates/prometheus/monitor.yaml:19** by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T13:19:53Z
```diff
@@ -16,5 +16,5 @@ spec:
       insecureSkipVerify: true
   selector:
     matchLabels:
-      control-plane: controller-manager
+    {{- include "kueue.selectorLabels" . | nindent 6 }}
```

  I think it works in main as it's also hard-coded into the corresponding Service chart.
  
  Actually that makes me realise that should be the `kueue.labels` parameter used here, as it targets the metrics Service (as opposed to Pods for PodMonitors). I've fixed that in f2030d40ba8300add8f68ace645503d2cf6d2c2a.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-02-15T13:28:42Z

- **charts/kueue/templates/_helpers.tpl:50** by [@astefanutti](https://github.com/astefanutti) — 2024-02-15T13:28:42Z
```diff
@@ -47,7 +47,7 @@ Selector labels
 */}}
 {{- define "kueue.selectorLabels" -}}
 app.kubernetes.io/name: {{ include "kueue.name" . }}
-app.kubernetes.io/instance: {{ .Release.Name }}
```

  Good idea. Added back in cc5dc1b1eeb710393f23b3ebff2c97c9612fdf9f.
  
  There are the namespace fields set for cluster-scoped resources, like MutatingWebhookConfiguration, that could be cleaned up as well.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-15T14:02:44Z

- **hack/update-helm.sh:160** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-15T14:02:44Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  TBH, I'm hesitating this way (rendered Helm Charts -> Kustomize manifests) since IIUC, the controller-tools, don't support automatic generation of Helm Charts. Also, I suspect that using Helm Chart (programmable YAML) as the source of truth would increase maintenance costs.
  
  As an alternative way, we might be possible to implement a simple tool to generate the Helm Charts from kustomize in Go or Python instead of this shell script.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-16T12:55:52Z

- **hack/update-helm.sh:160** by [@mimowo](https://github.com/mimowo) — 2024-02-16T12:55:52Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  IIUC, then in the script we could load the entire valid yaml, do all replacements and output the helm chart, which is no longer valid yaml, but we don't care at this point, because we already did all processing.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-23T15:25:47Z

- **hack/update-helm.sh:160** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-23T15:25:47Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  TBH, this script is quite unreadable already.
  It would be nice to migrate it to golang, but for the time being, I'm ok with this.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-23T16:48:07Z

- **hack/update-helm.sh:160** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-23T16:48:06Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  I agree with you.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-27T08:54:33Z

- **hack/update-helm.sh:160** by [@mimowo](https://github.com/mimowo) — 2024-02-27T08:54:33Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  I'm ok with this for now as well, but I think this PR with the sed replacements increases the complexity even further. So, ideally I would like to make some follow up to refactor it, or do a preparatory refactoring PR.
  
  EDIT: it is also ok for me if we just open an issue about this for now.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-02T16:30:30Z

- **hack/update-helm.sh:160** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-02T16:30:30Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  @astefanutti Could you open dedicated issue?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-02T16:41:41Z

Overall lgtm
I left comments for some nits.
/approve

- **Makefile:132** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-02T16:31:56Z
```diff
@@ -125,7 +129,7 @@ manifests: controller-gen ## Generate WebhookConfiguration, ClusterRole and Cust
 
 .PHONY: update-helm
 update-helm: manifests yq
-	./hack/update-helm.sh
+	SED=${SED} ./hack/update-helm.sh
```

  ```suggestion
  	SED=$(SED) ./hack/update-helm.sh
  ```

- **config/components/visibility/service.yaml:11** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-02T16:37:28Z
```diff
@@ -9,6 +9,3 @@ spec:
     port: 443
     protocol: TCP
     targetPort: 8082
```

  ```suggestion
      targetPort: 8082
  
  ```

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-03-04T17:29:01Z

- **config/components/visibility/service.yaml:11** by [@astefanutti](https://github.com/astefanutti) — 2024-03-04T17:29:01Z
```diff
@@ -9,6 +9,3 @@ spec:
     port: 443
     protocol: TCP
     targetPort: 8082
```

  Updated. Thanks.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-03-04T17:30:24Z

- **Makefile:132** by [@astefanutti](https://github.com/astefanutti) — 2024-03-04T17:30:24Z
```diff
@@ -125,7 +129,7 @@ manifests: controller-gen ## Generate WebhookConfiguration, ClusterRole and Cust
 
 .PHONY: update-helm
 update-helm: manifests yq
-	./hack/update-helm.sh
+	SED=${SED} ./hack/update-helm.sh
```

  Updated. Thank you.

### Review by [@astefanutti](https://github.com/astefanutti) (commented) — 2024-03-04T17:43:33Z

- **hack/update-helm.sh:160** by [@astefanutti](https://github.com/astefanutti) — 2024-03-04T17:43:33Z
```diff
@@ -157,6 +157,7 @@ for output_file in ${DEST_CRD_DIR}/*.yaml; do
     fi
   done <"$input_file"
   rm $input_file
+  $SED -i '/^metadata:.*/a\  labels:\n  {{- include "kueue.labels" . | nindent 4 }}' $output_file
```

  Sure, I've just created #1798 to capture it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-05T08:29:05Z

Thank you!
/lgtm
/approve
