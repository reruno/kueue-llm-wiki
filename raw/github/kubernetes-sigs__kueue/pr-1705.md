# PR #1705: [doc] MultiKueue

**Summary**: [doc] MultiKueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1705

**Last updated**: 2024-02-20T07:04:36Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `multikueue-doc` → `main`
- **Created**: 2024-02-08T08:30:40Z
- **Updated**: 2024-02-20T07:04:36Z
- **Merged**: 2024-02-14T17:59:34Z
- **Closed**: 2024-02-14T17:59:34Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mwielgus](https://github.com/mwielgus)
- **Changed files**: 4   **+353 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add documentation for MultiKueue..

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-08T08:30:43Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-08T08:30:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5fbd35a92775b4322b0c08ca0a27b19500ba84cb |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65cce1c1ced6d00008c4ed7f |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1705--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1705--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE3MDUtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.DD1SMDsjwV2J16uo_3uEynoyyTRrilgAI7Mychi6uuc)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-08T13:24:15Z

/cc @alculquicondor @tenzen-y @mwielgus

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-14T17:21:58Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-14T17:22:04Z

LGTM label has been added.  <details>Git tree hash: 3cc4b4bd35e08a29c84426c15a7a4b31f7bb79ed</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-14T17:22:04Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-14T17:22:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1705#issuecomment-1944275295" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1705#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T13:40:04Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:3** by [@mimowo](https://github.com/mimowo) — 2024-02-08T13:02:46Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
```

  update

- **site/content/en/docs/admission-check-controllers/multikueue.md:92** by [@mimowo](https://github.com/mimowo) — 2024-02-08T13:17:35Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Maintain the `Active` status of the Admission Checks controlled by `multikueue`
+- Crate and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Jobs main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
+## Parameters
+
+An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
+
+## Example
+
+### Setup
+
+{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+
+For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, the 
+`worker1-secret` secret ca by created with:
+
+```bash
+ kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
```

  Maybe this is another section / page, but I think it would be very useful to extend the example with :
  - setup for the worker clusters
  - running an example jobset - demonstrate how a user to monitor it, in particular figure out which cluster it is running on,  demonstrate

- **site/content/en/docs/admission-check-controllers/multikueue.md:20** by [@mimowo](https://github.com/mimowo) — 2024-02-08T13:22:50Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Maintain the `Active` status of the Admission Checks controlled by `multikueue`
+- Crate and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
```

  ```suggestion
  - Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:17** by [@mimowo](https://github.com/mimowo) — 2024-02-08T13:35:54Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
```

  Maybe we should have something more user / admin - oriented. I'm thinking, what would be particularly useful:
  1  (admin pov) demonstrate step-by-step how admin would setup the clusters (installing jobset etc. on the workers, but only CRDs), with example configs for the manager and worker clusters 
  2. (user pov) explain how user can interact with the environment, explaining that users interacts only with the manager cluster, but should not expect the pods to be created there. If they are interested in debugging the job they need to figure out the worker the job is actually running on, connect to the cluster and see pods.
  
  This could probably be follow ups for the admin / user tasks. WDYT @tenzen-y @alculquicondor @mwielgus ?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-08T15:26:33Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:3** by [@trasc](https://github.com/trasc) — 2024-02-08T15:19:37Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:20** by [@trasc](https://github.com/trasc) — 2024-02-08T15:20:20Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Maintain the `Active` status of the Admission Checks controlled by `multikueue`
+- Crate and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:92** by [@trasc](https://github.com/trasc) — 2024-02-08T15:25:06Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Maintain the `Active` status of the Admission Checks controlled by `multikueue`
+- Crate and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Jobs main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
+## Parameters
+
+An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
+
+## Example
+
+### Setup
+
+{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+
+For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, the 
+`worker1-secret` secret ca by created with:
+
+```bash
+ kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
```

  It could be a multikue specific section in `Instalation`, but when we'll support "managed-by" , there will be no additional steps to do.
  
  Strictly for how to create a secret out of a file ... I don't think it's very special .. we can also drop it from here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-08T15:33:07Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:92** by [@mimowo](https://github.com/mimowo) — 2024-02-08T15:33:07Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Maintain the `Active` status of the Admission Checks controlled by `multikueue`
+- Crate and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Jobs main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
+## Parameters
+
+An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
+
+## Example
+
+### Setup
+
+{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+
+For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, the 
+`worker1-secret` secret ca by created with:
+
+```bash
+ kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
```

  > It could be a multikue specific section in Instalation
  
  Something analogous to this page I'm thinking https://kueue.sigs.k8s.io/docs/tasks/administer_cluster_quotas/, but for MultiKueue.
  
  I wouldn't  mix this in regular installation. I think MultiKueue deserves its own page in tasks for admins. Maybe from the installation we could reference the task. 
  
  I think we should show example configurations for the manager and worker clusters. Describe what is the relationship between the quotas, at least briefly. It is far from obvious if the quotas should be the same or different.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T13:49:08Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:17** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T13:49:07Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
```

  > This could probably be follow ups for the admin / user tasks. WDYT @tenzen-y @alculquicondor @mwielgus ?
  
  That makes sense. 
  Also, I think we should clarify the responsibilities of each role (user, admin, kueue-controller-manager), like kubeconfig, in the `task` section. as a follow-up.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-12T16:46:39Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:39:21Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
```

  ```suggestion
  The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
  ```
  Let's avoid linking to the KEP, it's not meant for end users

- **site/content/en/docs/admission-check-controllers/multikueue.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:39:57Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
```

  ```suggestion
  You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:17** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:44:02Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
```

  Indeed, the current paragraph contains implementation details.
  
  We need to briefly start with the architecture (manager, workers) and when jobs are sent to the workers (first they need to pass quota checks in the manager, then the manager makes the worker clusters compete to run the job).
  
  We should also mention that the manager cluster should have a quota roughly equal to the total quota among all clusters.

- **site/content/en/docs/admission-check-controllers/multikueue.md:78** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:45:42Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Maintain the `Active` status of the Admission Checks controlled by `multikueue`
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
+## Parameters
+
+An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
+
+## Example
+
+### Setup
```

  Before giving an example, we also need to explain how the user needs to configure the clusters to have the same namespaces and clusterqueues.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-13T16:56:36Z

Mostly comments about using active voice

- **site/content/en/docs/admission-check-controllers/multikueue.md:6** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:31:32Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
```

  ```suggestion
    An admission check controller for multi cluster job dispatching.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:32:45Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
```

  ```suggestion
  MultiKueue is an [admission check](/docs/concepts/admission_check/) controller designed to provide the manager side functionality for multi cluster job dispatching.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:24** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:34:11Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
```

  ```suggestion
  The manager's main responsibilities are:
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:30** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:38:40Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
```

  ```suggestion
  The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time.
  Ideally, the quota in the manager cluster should be equal to the total quotas in the worker clusters.
  If significantly lower, the worker clusters will be under utilized.
  If significantly higher, the manager will dispatch and monitor workloads in the worker clusters that don't have a chance to be admitted.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:34** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:40:06Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
```

  ```suggestion
  The worker cluster acts like a standalone Kueue cluster.
  The workloads and jobs in the worker cluster are created or deleted by the
  MultiKueue Admission Check Controller running in the manager cluster.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:38** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:42:34Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
```

  ```suggestion
  For a job to be subject to multi cluster dispatching, you need to assign it to a ClusterQueue 
  that uses a MultiKueue AdmissionCheck. The Multikueue system works as follows:
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:40** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:43:11Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
```

  ```suggestion
  - When the job's Workload gets a QuotaReservation in the manager cluster, a copy of that workload will be created in all the configured worker clusters.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:41** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:44:25Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
```

  ```suggestion
  - When one of the worker clusters admits the remote workload sent to it:
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:42** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:44:39Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
```

  ```suggestion
    - The manager removes all the other remote workloads.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:43** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:45:42Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
```

  ```suggestion
    - The manager creates a copy of the job in the selected worker cluster, configured to use the quota reserved by the admitted Workload.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:44** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:46:22Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
```

  ```suggestion
    - The manager sets the AdmissionCheckState of the local workload to `Ready`, resulting in its Admission in the manager cluster.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:44** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:46:39Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
```

  Although we could remove this line. It's an implementation detail.

- **site/content/en/docs/admission-check-controllers/multikueue.md:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:47:37Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
```

  ```suggestion
  - The manager monitors the remote objects, workload and job, and syncs any changes in their status into the local objects.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:47** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:47:59Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
```

  ```suggestion
    - The manager does a last sync for the objects status.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:48** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:48:18Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
```

  ```suggestion
    - The manager remotes the objects from the worker cluster.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:57** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:48:33Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
```

  and empty lines after titles

- **site/content/en/docs/admission-check-controllers/multikueue.md:55** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:51:11Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
```

  ```suggestion
  - Since updating the status of a local Job could conflict with the Kubernetes Job controller, the manager
    does not sync the Job status during the job execution.
    The manager copies the final status of the remote Job when the remote workload is marked as `Finished`.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:57** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:52:17Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
```

  ```suggestion
  There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the Kubernetes `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:61** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:53:20Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
```

  ```suggestion
  - Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, you should only install the JobSet CRDs, but not the controller.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:74** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:55:18Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
+## Parameters
+
+An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
+
+## Example
+
+### Setup
+
+#### Worker Cluster
+
+When a workload is dispatched from the manager cluster to a worker cluster it is expected that its namespace and LocalQueue to be already created, hence the worker cluster configuration should mirror the one of the manager cluster in terms of namespaces and LocalQueues.
```

  ```suggestion
  When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that
  the job's namespace and LocalQueue also exist in the worker cluster.
  In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
  ```

- **site/content/en/docs/admission-check-controllers/multikueue.md:81** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T16:56:09Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
+## Parameters
+
+An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
+
+## Example
+
+### Setup
+
+#### Worker Cluster
+
+When a workload is dispatched from the manager cluster to a worker cluster it is expected that its namespace and LocalQueue to be already created, hence the worker cluster configuration should mirror the one of the manager cluster in terms of namespaces and LocalQueues.
+
+#### Manager Cluster
+
+{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+
+For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, the 
+`worker1-secret` secret ca by created with:
```

  ```suggestion
  For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`,
  you can create the 
  `worker1-secret` secret by running the following command:
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T18:06:49Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:8** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T17:56:15Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
```

  ```suggestion
  {{% alert title="Warning" color="warning" %}}
  _Available in Kueue v0.6.0 and later_
  {{% /alert %}}
  ```
  
  Mentioning available versions would be worth it.

- **site/content/en/docs/admission-check-controllers/multikueue.md:30** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T17:58:40Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
```

  ```suggestion
  The FlavorQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
  ```
  
  Does this mean the `flavorQuota`? I think that ResourceQuota indicates one of the k8 core APIs.

- **site/content/en/docs/admission-check-controllers/multikueue.md:43** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T18:00:41Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
```

  Can we mention the prebuild annotation here?

- **site/content/en/docs/admission-check-controllers/multikueue.md:71** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T18:04:57Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
```

  I think that we can say that deploying jobset-controller to the manager cluster is possible when we use the jobset with the main branch, right?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-13T18:44:18Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:30** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:44:18Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
```

  flavorQuota is not a field name either, so maybe we should say "The quota set for the flavors of a ClusterQueue"

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T18:52:19Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:30** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T18:52:19Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
```

  That makes sense.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-14T12:31:06Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:9** by [@trasc](https://github.com/trasc) — 2024-02-13T06:18:24Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:11** by [@trasc](https://github.com/trasc) — 2024-02-13T06:19:10Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:78** by [@trasc](https://github.com/trasc) — 2024-02-13T07:05:27Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Maintain the `Active` status of the Admission Checks controlled by `multikueue`
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
+## Parameters
+
+An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
+
+## Example
+
+### Setup
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:17** by [@trasc](https://github.com/trasc) — 2024-02-13T07:50:29Z
```diff
@@ -0,0 +1,50 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2023-10-23
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for [MultiKueue](https://github.com/kubernetes-sigs/kueue/blob/main/keps/693-multikueue/README.md). 
+
+The controller is part of kueue. You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+The controller's main attributes are:
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-14T15:52:36Z

- **site/content/en/docs/admission-check-controllers/multikueue.md:6** by [@trasc](https://github.com/trasc) — 2024-02-14T15:24:31Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:8** by [@trasc](https://github.com/trasc) — 2024-02-14T15:25:34Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:9** by [@trasc](https://github.com/trasc) — 2024-02-14T15:25:40Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:24** by [@trasc](https://github.com/trasc) — 2024-02-14T15:26:29Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:30** by [@trasc](https://github.com/trasc) — 2024-02-14T15:28:17Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:30** by [@trasc](https://github.com/trasc) — 2024-02-14T15:29:16Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:34** by [@trasc](https://github.com/trasc) — 2024-02-14T15:31:46Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:38** by [@trasc](https://github.com/trasc) — 2024-02-14T15:32:38Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:40** by [@trasc](https://github.com/trasc) — 2024-02-14T15:33:35Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:41** by [@trasc](https://github.com/trasc) — 2024-02-14T15:34:28Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:42** by [@trasc](https://github.com/trasc) — 2024-02-14T15:35:27Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:43** by [@trasc](https://github.com/trasc) — 2024-02-14T15:39:43Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:43** by [@trasc](https://github.com/trasc) — 2024-02-14T15:39:45Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:44** by [@trasc](https://github.com/trasc) — 2024-02-14T15:41:34Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:45** by [@trasc](https://github.com/trasc) — 2024-02-14T15:42:06Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:47** by [@trasc](https://github.com/trasc) — 2024-02-14T15:42:46Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:48** by [@trasc](https://github.com/trasc) — 2024-02-14T15:43:55Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:57** by [@trasc](https://github.com/trasc) — 2024-02-14T15:44:10Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:55** by [@trasc](https://github.com/trasc) — 2024-02-14T15:46:42Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:57** by [@trasc](https://github.com/trasc) — 2024-02-14T15:47:22Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:61** by [@trasc](https://github.com/trasc) — 2024-02-14T15:48:37Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:71** by [@trasc](https://github.com/trasc) — 2024-02-14T15:49:40Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
```

  We don't yet support it in MultiKueue ACC.

- **site/content/en/docs/admission-check-controllers/multikueue.md:74** by [@trasc](https://github.com/trasc) — 2024-02-14T15:50:27Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
+## Parameters
+
+An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
+
+## Example
+
+### Setup
+
+#### Worker Cluster
+
+When a workload is dispatched from the manager cluster to a worker cluster it is expected that its namespace and LocalQueue to be already created, hence the worker cluster configuration should mirror the one of the manager cluster in terms of namespaces and LocalQueues.
```

  done

- **site/content/en/docs/admission-check-controllers/multikueue.md:81** by [@trasc](https://github.com/trasc) — 2024-02-14T15:51:39Z
```diff
@@ -0,0 +1,85 @@
+---
+title: "MultiKueue Admission Check Controller"
+date: 2024-02-08
+weight: 2
+description: >
+  An admission check controller providing the manager side MultiKueue functionality.
+---
+
+The MultiKueue Admission Check Controller is an Admission Check Controller designed to provide the manager side functionality for multi cluster job dispatching.
+
+You can enable it by setting the `MultiKueue` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+{{% alert title="Warning" color="warning" %}}
+MultiKueue is currently an alpha feature and disabled by default.
+{{% /alert %}}
+
+## MultiKueue Overview
+
+A MultiKueue setup is composed of a manager cluster and at least one worker cluster.
+
+### Cluster Roles
+#### Manager Cluster
+
+The manager's main attributes are:
+- Establish and maintain the connection with the worker clusters.
+- Create and monitor remote objects (workloads or jobs) while keeping the local ones in sync.
+
+The MultiKueue Admission Check Controller runs in the manager cluster and will also maintain the `Active` status of the Admission Checks controlled by `multikueue`.
+
+The ResourceQuota set on a ClusterQueue using MultiKueue controls how many jobs are subject for dispatching at a given point in time, ideally it should be less then the total quotas in the worker clusters since otherwise will cause the monitoring of workloads that don't have a chance to be admitted. 
+
+#### Worker Cluster
+
+The worker cluster acts like an usual standalone Kueue cluster in which the Kueue the workloads and jobs are managed by the MultiKueue Admission Check Controller running in the manager cluster.
+
+### Job Flow
+
+If the ClusterQueue a job is assigned to in the manager cluster uses a MultiKueue AdmissionCheck it will be the subject of dispatching, as a result:
+
+- When it's workload gets a QuotaReservation in the manager, a copy of that workload will be created in all the configured worker clusters.
+- When one of the remote workloads is admitted in a worker cluster:
+  - All the other remote workloads are removed.
+  - A copy of the job is created in the selected worker cluster having the remote workload set as its prebuilt-workload.
+  - The AdmissionCheckState of the local workload is set as `Ready` resulting in its Admission in the manager cluster.
+- The remote objects , workload and job, are monitored and the changes in their status are synced back in the manager objects.
+- When the remote workload is marked as `Finished`:
+  - A last sync is done for the objects status.
+  - The remote objects are removed from the worker cluster.
+
+
+## Supported jobs
+### batch/Job
+Known Limitations:
+- Since unsuspending a Job in the manager cluster will lead to its local execution, the AdmissionCheckStates are kept `Pending` during the remote job execution.
+- Since updating the status of a local Job could conflict with the Job's main controller, the Job status is not synced during the job execution, the final status of the remote Job is copied when the remote workload is marked as `Finished`.
+
+There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the main `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
+
+### JobSet
+Known Limitations:
+- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, only the JobSet CRDs should be installed in the manager cluster.
+An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+
+## Parameters
+
+An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
+
+## Example
+
+### Setup
+
+#### Worker Cluster
+
+When a workload is dispatched from the manager cluster to a worker cluster it is expected that its namespace and LocalQueue to be already created, hence the worker cluster configuration should mirror the one of the manager cluster in terms of namespaces and LocalQueues.
+
+#### Manager Cluster
+
+{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+
+For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, the 
+`worker1-secret` secret ca by created with:
```

  done
