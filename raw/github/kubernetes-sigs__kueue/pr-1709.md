# PR #1709: Implement backoff re-queuing mechanism

**Summary**: Implement backoff re-queuing mechanism

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1709

**Last updated**: 2024-02-13T19:43:21Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `implement-backoff-requeue-machanism` → `main`
- **Created**: 2024-02-09T08:50:45Z
- **Updated**: 2024-02-13T19:43:21Z
- **Merged**: 2024-02-12T20:41:16Z
- **Closed**: 2024-02-12T20:41:16Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Changed files**: 36   **+1200 / -157**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
/kind api-change

#### What this PR does / why we need it:
I implemented a backoff re-queue mechanism when a workload meets an Eviction with PodsReadyTimout reason.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part-of #1282

#### Special notes for your reviewer:
KEP-1282: https://github.com/kubernetes-sigs/kueue/blob/3cd0da66f57dc90f9d5e631b218f7a374204d07d/keps/1282-pods-ready-requeue-strategy/README.md
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
WaitForPodsReady: Support a backoff re-queueing mechanism with configurable limit.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-09T08:50:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3bd7ce678dbcab192c48c2ecd6da0d54c1bbc0eb |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65ca79e7ff0cd90008be03a2 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-09T08:50:51Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1709#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-10T13:21:09Z

~~I'm re-implementing core logic for re-queuing with an entirely different approach.~~
~~So, I don't want anyone to review this PR yet.~~

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-11T23:37:08Z

@alculquicondor This PR is ready for the review. PTAL

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-11T23:47:03Z

/hold for review

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:02:28Z

/hold for
#1723

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:06:40Z

Rebased

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T19:30:47Z

Can you fix the verify?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:31:29Z

> Can you fix the verify?

Sure.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T19:36:49Z

LGTM otherwise.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-12T19:47:19Z

LGTM label has been added.  <details>Git tree hash: 3f48edfa9fdae121b674d87983ed3aeae76ff5ab</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:51:02Z

I'm still working on https://github.com/kubernetes-sigs/kueue/pull/1709#discussion_r1486432930 and https://github.com/kubernetes-sigs/kueue/pull/1709#discussion_r1486623786.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T19:53:41Z

/hold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T20:07:43Z

/hold cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T20:08:56Z

> Let's leave for a follow up to remove the stub. I don't think it's necessary, if we rework the unit tests.
> 
> /lgtm

Let me try to rework the unit tests to avoid stubs as a follow-up.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T20:36:32Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-12T20:36:37Z

LGTM label has been added.  <details>Git tree hash: 7113c7a278f1978076e5616e526705aa6ca25257</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T19:43:19Z

/release-note-edit
```release-note
WaitForPodsReady: Support a backoff re-queueing mechanism with configurable limit.
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-12T15:18:17Z

- **apis/config/v1beta1/configuration_types.go:225** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T14:12:27Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
```

  the comments seem reversed. But you should say which condition you are talking about.

- **apis/config/v1beta1/configuration_types.go:233** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T14:14:53Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
```

  what happens during a backoff?

- **apis/config/v1beta1/configuration_types.go:233** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T14:16:41Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
```

  ```suggestion
  	// Every backoff duration is about "1.41284738^(n-1)"
  ```

- **apis/config/v1beta1/configuration_types.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T14:18:01Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
+	// where the "n" represents the "workloadStatus.requeueState.count", and the "Rand" represents the random jitter.
+	// Considering the ".waitForPodsReady.timeout" (default: 300 seconds),
```

  the default is not relevant here.

- **apis/config/v1beta1/configuration_types.go:241** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T14:18:36Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
+	// where the "n" represents the "workloadStatus.requeueState.count", and the "Rand" represents the random jitter.
+	// Considering the ".waitForPodsReady.timeout" (default: 300 seconds),
+	// this indicates that an evicted workload with PodsReadyTimeout reason is continued re-queuing for
+	// the "t(n+1) + Rand + SUM[k=1,n]1.41284738^(k-1)" seconds where the "t" represents "waitForPodsReady.timeout".
+	// Given that the "backoffLimitCount" equals "30" and the "waitForPodsReady.timeout" equals "300" (default),
+	// the result equals 24 hours (+Rand seconds).
+	//
+	// Defaults to null.
```

  what does it mean to be null?

- **apis/config/v1beta1/configuration_types.go:237** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T14:19:11Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
+	// where the "n" represents the "workloadStatus.requeueState.count", and the "Rand" represents the random jitter.
+	// Considering the ".waitForPodsReady.timeout" (default: 300 seconds),
+	// this indicates that an evicted workload with PodsReadyTimeout reason is continued re-queuing for
+	// the "t(n+1) + Rand + SUM[k=1,n]1.41284738^(k-1)" seconds where the "t" represents "waitForPodsReady.timeout".
```

  I think it's more useful if you give how much time each backoff will be approximately.

- **apis/kueue/v1beta1/workload_types.go:202** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T14:21:25Z
```diff
@@ -184,6 +190,23 @@ type WorkloadStatus struct {
 	AdmissionChecks []AdmissionCheckState `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"name"`
 }
 
+type RequeueState struct {
+	// count records the number of times a workload has been requeued
+	// When a deactivated (`.spec.activate`=`false`) workload is reactivated (`.spec.activate`=`true`),
+	// this count would be reset to null.
+	//
+	// +optional
+	// +kubebuilder:validation:Minimum=0
+	Count *int32 `json:"count,omitempty"`
+
+	// requeueAt records the time when a workload is requeued.
```

  ```suggestion
  	// requeueAt records the time when a workload will be requeued.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T15:20:15Z

- **apis/config/v1beta1/configuration_types.go:225** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T15:20:15Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
```

  Oops, nice catch.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T15:22:21Z

- **apis/config/v1beta1/configuration_types.go:233** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T15:22:21Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
```

  The workloads aren't pushed to the heap; they are taken as a inadmissible.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T15:23:25Z

- **apis/config/v1beta1/configuration_types.go:241** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T15:23:25Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
+	// where the "n" represents the "workloadStatus.requeueState.count", and the "Rand" represents the random jitter.
+	// Considering the ".waitForPodsReady.timeout" (default: 300 seconds),
+	// this indicates that an evicted workload with PodsReadyTimeout reason is continued re-queuing for
+	// the "t(n+1) + Rand + SUM[k=1,n]1.41284738^(k-1)" seconds where the "t" represents "waitForPodsReady.timeout".
+	// Given that the "backoffLimitCount" equals "30" and the "waitForPodsReady.timeout" equals "300" (default),
+	// the result equals 24 hours (+Rand seconds).
+	//
+	// Defaults to null.
```

  It means that unlimited re-queueing.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T15:23:55Z

- **apis/config/v1beta1/configuration_types.go:237** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T15:23:55Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
+	// where the "n" represents the "workloadStatus.requeueState.count", and the "Rand" represents the random jitter.
+	// Considering the ".waitForPodsReady.timeout" (default: 300 seconds),
+	// this indicates that an evicted workload with PodsReadyTimeout reason is continued re-queuing for
+	// the "t(n+1) + Rand + SUM[k=1,n]1.41284738^(k-1)" seconds where the "t" represents "waitForPodsReady.timeout".
```

  Alright, I will put some examples here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T15:25:15Z

- **apis/config/v1beta1/configuration_types.go:235** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T15:25:15Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
+	// where the "n" represents the "workloadStatus.requeueState.count", and the "Rand" represents the random jitter.
+	// Considering the ".waitForPodsReady.timeout" (default: 300 seconds),
```

  It makes sense.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-12T16:09:57Z

- **pkg/config/validation.go:75** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T15:20:50Z
```diff
@@ -52,6 +56,19 @@ func validate(c *configapi.Configuration) field.ErrorList {
 	return allErrs
 }
 
+func validateWaitForPodsReady(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if !WaitForPodsReadyIsEnabled(c) {
+		return allErrs
+	}
+	if strategy := c.WaitForPodsReady.RequeuingStrategy; strategy != nil && strategy.Timestamp != nil &&
+		*strategy.Timestamp != configapi.CreationTimestamp && *strategy.Timestamp != configapi.EvictionTimestamp {
+		allErrs = append(allErrs, field.NotSupported(requeuingStrategyPath.Child("timestamp"),
+			strategy.Timestamp, []configapi.RequeuingTimestamp{configapi.CreationTimestamp, configapi.EvictionTimestamp}))
+	}
```

  backoffLimitCount should be positive

- **pkg/controller/core/workload_controller.go:338** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T15:44:58Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
```

  before this, were we calling update without any meaningful changes once the workload was evicted?

- **pkg/controller/core/workload_controller.go:338** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T15:45:35Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
```

  I think just checking if it's evicted (not specifically by PodsReadyTimeout) or inactive at all should have been enough

- **pkg/controller/core/workload_controller.go:374** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T15:48:32Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
+		// the workload has already been evicted by the PodsReadyTimeout or been deactivated.
+		return ctrl.Result{}, nil
+	}
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
 		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
 		return ctrl.Result{RequeueAfter: recheckAfter}, nil
-	} else {
-		log.V(2).Info("Start the eviction of the workload due to exceeding the PodsReady timeout")
-		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
-		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, false)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
+	log.V(2).Info("Start the eviction of the workload due to exceeding the PodsReady timeout")
+	if deactivated, err := r.triggerDeactivationOrBackoffRequeue(ctx, wl); deactivated {
+		return ctrl.Result{}, err
+	}
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	return ctrl.Result{}, client.IgnoreNotFound(err)
+}
+
+// triggerDeactivationOrBackoffRequeue deactivates a workload (".spec.active"="false")
+// if a re-queued number has already exceeded the limit of re-queuing backoff.
+// Otherwise, it increments a re-queueing count and update a time to be re-queued.
+// It returns true as a first value if a workload is deactivated.
+func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Context, wl *kueue.Workload) (bool, error) {
+	if !workload.HasRequeueState(wl) {
+		wl.Status.RequeueState = &kueue.RequeueState{}
+	}
+	// If requeuingBackoffLimitCount equals to null, the workloads is repeatedly and endless re-queued.
+	requeuingCount := ptr.Deref(wl.Status.RequeueState.Count, 0) + 1
+	if r.requeuingBackoffLimitCount != nil && requeuingCount > *r.requeuingBackoffLimitCount {
+		wl.Spec.Active = ptr.To(false)
+		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  the event should go after calling Update.

- **pkg/queue/cluster_queue_impl.go:67** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T15:59:41Z
```diff
@@ -57,16 +62,21 @@ type clusterQueueBase struct {
 	lessFunc func(a, b interface{}) bool
 
 	rwm sync.RWMutex
+
+	// stubs
+	canNotQueueWorkload func(*workload.Info, clock.Clock) bool
```

  ```suggestion
  	canQueueWorkload func(*workload.Info, clock.Clock) bool
  ```
  
  Use the positive name, to avoid double negatives `!canNot`

- **pkg/queue/cluster_queue_impl_test.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:00:35Z
```diff
@@ -40,25 +44,57 @@ var (
 	defaultQueueOrderingFunc = queueOrderingFunc(workload.Ordering{
 		PodsReadyRequeuingTimestamp: config.EvictionTimestamp,
 	})
+	canAlwaysQueueWorkload = func(*workload.Info, clock.Clock) bool {
+		return false
+	}
 )
 
 func Test_PushOrUpdate(t *testing.T) {
-	cq := newClusterQueueImpl(keyFunc, defaultQueueOrderingFunc)
-	wl := utiltesting.MakeWorkload("workload-1", defaultNamespace).Obj()
-	if cq.Pending() != 0 {
-		t.Error("ClusterQueue should be empty")
-	}
-	cq.PushOrUpdate(workload.NewInfo(wl))
-	if cq.Pending() != 1 {
-		t.Error("ClusterQueue should have one workload")
+	wlBase := utiltesting.MakeWorkload("workload-1", defaultNamespace).Clone()
+
+	cases := map[string]struct {
+		canNotQueueWorkloadFunc   func(*workload.Info, clock.Clock) bool
+		wantWorkload              *workload.Info
+		wantInAdmissibleWorkloads map[string]*workload.Info
+	}{
+		"can queue workload": {
+			canNotQueueWorkloadFunc: canAlwaysQueueWorkload,
+			wantWorkload:            workload.NewInfo(wlBase.Clone().ResourceVersion("1").Obj()),
+		},
+		"can not queue workload": {
+			canNotQueueWorkloadFunc: func(*workload.Info, clock.Clock) bool { return true },
```

  Can we use a fake clock instead of needing the stub?

- **pkg/webhooks/workload_webhook.go:79** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:05:37Z
```diff
@@ -75,6 +75,11 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 			wl.Spec.PodSets[i].MinCount = nil
 		}
 	}
+
+	// If a deactivated workload is re-activated, we need to reset the RequeueState.
```

  I'm not convinced this works, because the apiserver doesn't generally allow you to update both the status and spec.
  
  Did you add an integration test for it?

- **pkg/workload/workload.go:380** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:07:13Z
```diff
@@ -375,6 +375,9 @@ func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
 
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if HasRequeueState(w) {
+		wlCopy.Status.RequeueState = w.Status.RequeueState.DeepCopy()
+	}
```

  ```suggestion
  	wlCopy.Status.RequeueState = w.Status.RequeueState.DeepCopy()
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-12T16:25:56Z

- **test/integration/scheduler/podsready/scheduler_test.go:263** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:17:50Z
```diff
@@ -248,9 +259,24 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			time.Sleep(podsReadyTimeout)
 			ginkgo.By("finish the eviction")
 			util.FinishEvictionForWorkloads(ctx, k8sClient, prodWl)
-			ginkgo.By("verify the 'prod' workload gets re-admitted once")
+			ginkgo.By("pending by backoff")
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, prodWl)
```

  could this be flaky? For how long will it stay in Pending?

- **pkg/webhooks/workload_webhook.go:79** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:25:46Z
```diff
@@ -75,6 +75,11 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 			wl.Spec.PodSets[i].MinCount = nil
 		}
 	}
+
+	// If a deactivated workload is re-activated, we need to reset the RequeueState.
```

  you did

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T17:27:29Z

- **pkg/config/validation.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T17:27:29Z
```diff
@@ -52,6 +56,19 @@ func validate(c *configapi.Configuration) field.ErrorList {
 	return allErrs
 }
 
+func validateWaitForPodsReady(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if !WaitForPodsReadyIsEnabled(c) {
+		return allErrs
+	}
+	if strategy := c.WaitForPodsReady.RequeuingStrategy; strategy != nil && strategy.Timestamp != nil &&
+		*strategy.Timestamp != configapi.CreationTimestamp && *strategy.Timestamp != configapi.EvictionTimestamp {
+		allErrs = append(allErrs, field.NotSupported(requeuingStrategyPath.Child("timestamp"),
+			strategy.Timestamp, []configapi.RequeuingTimestamp{configapi.CreationTimestamp, configapi.EvictionTimestamp}))
+	}
```

  That makes sense. I will add it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T17:29:40Z

- **pkg/controller/core/workload_controller.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T17:29:40Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
```

  Yes, that's right. Maybe we should work on this fixing at separate PR, but I'm ok with either way.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T17:31:46Z

- **pkg/controller/core/workload_controller.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T17:31:46Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
```

  Fixing this at another PR has the advantage of the chance to cherry-pick to v0.5.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T17:33:16Z

- **pkg/controller/core/workload_controller.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T17:33:16Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
```

  @alculquicondor WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T17:34:06Z

- **pkg/controller/core/workload_controller.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T17:34:06Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
```

  That makes sense.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-12T17:34:42Z

- **pkg/controller/core/workload_controller.go:338** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T17:34:42Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
```

  yeah, I'm ok with an extra PR that can be cherry-picked

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T17:34:43Z

- **pkg/controller/core/workload_controller.go:374** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T17:34:43Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
+		// the workload has already been evicted by the PodsReadyTimeout or been deactivated.
+		return ctrl.Result{}, nil
+	}
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
 		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
 		return ctrl.Result{RequeueAfter: recheckAfter}, nil
-	} else {
-		log.V(2).Info("Start the eviction of the workload due to exceeding the PodsReady timeout")
-		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
-		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, false)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
+	log.V(2).Info("Start the eviction of the workload due to exceeding the PodsReady timeout")
+	if deactivated, err := r.triggerDeactivationOrBackoffRequeue(ctx, wl); deactivated {
+		return ctrl.Result{}, err
+	}
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	return ctrl.Result{}, client.IgnoreNotFound(err)
+}
+
+// triggerDeactivationOrBackoffRequeue deactivates a workload (".spec.active"="false")
+// if a re-queued number has already exceeded the limit of re-queuing backoff.
+// Otherwise, it increments a re-queueing count and update a time to be re-queued.
+// It returns true as a first value if a workload is deactivated.
+func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Context, wl *kueue.Workload) (bool, error) {
+	if !workload.HasRequeueState(wl) {
+		wl.Status.RequeueState = &kueue.RequeueState{}
+	}
+	// If requeuingBackoffLimitCount equals to null, the workloads is repeatedly and endless re-queued.
+	requeuingCount := ptr.Deref(wl.Status.RequeueState.Count, 0) + 1
+	if r.requeuingBackoffLimitCount != nil && requeuingCount > *r.requeuingBackoffLimitCount {
+		wl.Spec.Active = ptr.To(false)
+		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  Nice catch. You're right.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T17:34:58Z

- **pkg/queue/cluster_queue_impl.go:67** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T17:34:58Z
```diff
@@ -57,16 +62,21 @@ type clusterQueueBase struct {
 	lessFunc func(a, b interface{}) bool
 
 	rwm sync.RWMutex
+
+	// stubs
+	canNotQueueWorkload func(*workload.Info, clock.Clock) bool
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T17:36:46Z

- **pkg/queue/cluster_queue_impl_test.go:65** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T17:36:46Z
```diff
@@ -40,25 +44,57 @@ var (
 	defaultQueueOrderingFunc = queueOrderingFunc(workload.Ordering{
 		PodsReadyRequeuingTimestamp: config.EvictionTimestamp,
 	})
+	canAlwaysQueueWorkload = func(*workload.Info, clock.Clock) bool {
+		return false
+	}
 )
 
 func Test_PushOrUpdate(t *testing.T) {
-	cq := newClusterQueueImpl(keyFunc, defaultQueueOrderingFunc)
-	wl := utiltesting.MakeWorkload("workload-1", defaultNamespace).Obj()
-	if cq.Pending() != 0 {
-		t.Error("ClusterQueue should be empty")
-	}
-	cq.PushOrUpdate(workload.NewInfo(wl))
-	if cq.Pending() != 1 {
-		t.Error("ClusterQueue should have one workload")
+	wlBase := utiltesting.MakeWorkload("workload-1", defaultNamespace).Clone()
+
+	cases := map[string]struct {
+		canNotQueueWorkloadFunc   func(*workload.Info, clock.Clock) bool
+		wantWorkload              *workload.Info
+		wantInAdmissibleWorkloads map[string]*workload.Info
+	}{
+		"can queue workload": {
+			canNotQueueWorkloadFunc: canAlwaysQueueWorkload,
+			wantWorkload:            workload.NewInfo(wlBase.Clone().ResourceVersion("1").Obj()),
+		},
+		"can not queue workload": {
+			canNotQueueWorkloadFunc: func(*workload.Info, clock.Clock) bool { return true },
```

  Yes, we can. Let me try to modify this without stubs.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T17:43:01Z

- **test/integration/scheduler/podsready/scheduler_test.go:263** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T17:43:01Z
```diff
@@ -248,9 +259,24 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			time.Sleep(podsReadyTimeout)
 			ginkgo.By("finish the eviction")
 			util.FinishEvictionForWorkloads(ctx, k8sClient, prodWl)
-			ginkgo.By("verify the 'prod' workload gets re-admitted once")
+			ginkgo.By("pending by backoff")
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, prodWl)
```

  This could be flaky. Since there is the first backoff re-queueing, it takes just 1 sec + jitter.
  Maybe, we should increase the factor and duration here: https://github.com/kubernetes-sigs/kueue/blob/13bb0d6a7514a97feb4fae8c800a2b4febc0d32f/pkg/controller/core/workload_controller.go#L383-L384
  
  Although it makes the integration tests more duration.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T18:33:08Z

- **apis/config/v1beta1/configuration_types.go:225** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T18:33:08Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T18:33:12Z

- **apis/config/v1beta1/configuration_types.go:233** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T18:33:12Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
```

  Added comments here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T18:33:14Z

- **apis/config/v1beta1/configuration_types.go:233** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T18:33:14Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T18:33:17Z

- **apis/config/v1beta1/configuration_types.go:235** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T18:33:16Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
+	// where the "n" represents the "workloadStatus.requeueState.count", and the "Rand" represents the random jitter.
+	// Considering the ".waitForPodsReady.timeout" (default: 300 seconds),
```

  Removed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T18:33:22Z

- **apis/config/v1beta1/configuration_types.go:241** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T18:33:22Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
+	// where the "n" represents the "workloadStatus.requeueState.count", and the "Rand" represents the random jitter.
+	// Considering the ".waitForPodsReady.timeout" (default: 300 seconds),
+	// this indicates that an evicted workload with PodsReadyTimeout reason is continued re-queuing for
+	// the "t(n+1) + Rand + SUM[k=1,n]1.41284738^(k-1)" seconds where the "t" represents "waitForPodsReady.timeout".
+	// Given that the "backoffLimitCount" equals "30" and the "waitForPodsReady.timeout" equals "300" (default),
+	// the result equals 24 hours (+Rand seconds).
+	//
+	// Defaults to null.
```

  Added comments here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T18:33:24Z

- **apis/config/v1beta1/configuration_types.go:237** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T18:33:24Z
```diff
@@ -219,9 +219,28 @@ type MultiKueue struct {
 
 type RequeuingStrategy struct {
 	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default): indicates from Workload .metadata.creationTimestamp.
+	// - `Creation`: indicates from Workload .status.conditions.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of requeuing retries.
+	// When the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	//
+	// Every backoff duration is calculated by "1.41284738^(n-1)+Rand"
+	// where the "n" represents the "workloadStatus.requeueState.count", and the "Rand" represents the random jitter.
+	// Considering the ".waitForPodsReady.timeout" (default: 300 seconds),
+	// this indicates that an evicted workload with PodsReadyTimeout reason is continued re-queuing for
+	// the "t(n+1) + Rand + SUM[k=1,n]1.41284738^(k-1)" seconds where the "t" represents "waitForPodsReady.timeout".
```

  Added some examples here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T18:37:26Z

- **apis/kueue/v1beta1/workload_types.go:202** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T18:37:25Z
```diff
@@ -184,6 +190,23 @@ type WorkloadStatus struct {
 	AdmissionChecks []AdmissionCheckState `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"name"`
 }
 
+type RequeueState struct {
+	// count records the number of times a workload has been requeued
+	// When a deactivated (`.spec.activate`=`false`) workload is reactivated (`.spec.activate`=`true`),
+	// this count would be reset to null.
+	//
+	// +optional
+	// +kubebuilder:validation:Minimum=0
+	Count *int32 `json:"count,omitempty"`
+
+	// requeueAt records the time when a workload is requeued.
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-12T18:47:59Z

- **test/integration/scheduler/podsready/scheduler_test.go:263** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T18:47:59Z
```diff
@@ -248,9 +259,24 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			time.Sleep(podsReadyTimeout)
 			ginkgo.By("finish the eviction")
 			util.FinishEvictionForWorkloads(ctx, k8sClient, prodWl)
-			ginkgo.By("verify the 'prod' workload gets re-admitted once")
+			ginkgo.By("pending by backoff")
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, prodWl)
```

  I would just remove this check and leave a comment.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T18:48:18Z

- **pkg/config/validation.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T18:48:18Z
```diff
@@ -52,6 +56,19 @@ func validate(c *configapi.Configuration) field.ErrorList {
 	return allErrs
 }
 
+func validateWaitForPodsReady(c *configapi.Configuration) field.ErrorList {
+	var allErrs field.ErrorList
+	if !WaitForPodsReadyIsEnabled(c) {
+		return allErrs
+	}
+	if strategy := c.WaitForPodsReady.RequeuingStrategy; strategy != nil && strategy.Timestamp != nil &&
+		*strategy.Timestamp != configapi.CreationTimestamp && *strategy.Timestamp != configapi.EvictionTimestamp {
+		allErrs = append(allErrs, field.NotSupported(requeuingStrategyPath.Child("timestamp"),
+			strategy.Timestamp, []configapi.RequeuingTimestamp{configapi.CreationTimestamp, configapi.EvictionTimestamp}))
+	}
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-12T18:54:39Z

- **apis/config/v1beta1/configuration_types.go:241** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T18:50:34Z
```diff
@@ -218,10 +218,35 @@ type MultiKueue struct {
 }
 
 type RequeuingStrategy struct {
-	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// Timestamp defines the timestamp used for re-queuing a Workload
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default) indicates from Workload `Evicted` condition with `PodsReadyTimeout` reason.
+	// - `Creation` indicates from Workload .metadata.creationTimestamp.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "1.41284738^(n-1)+Rand" where the "n" represents the "workloadStatus.requeueState.count",
+	// and the "Rand" represents the random jitter. During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// Considering the ".waitForPodsReady.timeout",
+	// this indicates that an evicted workload with PodsReadyTimeout reason is continued re-queuing for
+	// the "t(n+1) + SUM[k=1,n](1.41284738^(k-1) + Rand)" seconds where the "t" represents "waitForPodsReady.timeout".
+	// Given that the "backoffLimitCount" equals "30" and the "waitForPodsReady.timeout" equals "300" (default),
+	// the result equals 24 hours (+Rand seconds).
```

  Let's just remove this paragraph.... too confusing and not very useful.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T19:01:25Z

- **pkg/controller/core/workload_controller.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:01:25Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
```

  Created: https://github.com/kubernetes-sigs/kueue/pull/1723

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T19:01:35Z

- **pkg/controller/core/workload_controller.go:338** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:01:35Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T19:02:34Z

- **apis/config/v1beta1/configuration_types.go:241** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:02:34Z
```diff
@@ -218,10 +218,35 @@ type MultiKueue struct {
 }
 
 type RequeuingStrategy struct {
-	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// Timestamp defines the timestamp used for re-queuing a Workload
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default) indicates from Workload `Evicted` condition with `PodsReadyTimeout` reason.
+	// - `Creation` indicates from Workload .metadata.creationTimestamp.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "1.41284738^(n-1)+Rand" where the "n" represents the "workloadStatus.requeueState.count",
+	// and the "Rand" represents the random jitter. During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// Considering the ".waitForPodsReady.timeout",
+	// this indicates that an evicted workload with PodsReadyTimeout reason is continued re-queuing for
+	// the "t(n+1) + SUM[k=1,n](1.41284738^(k-1) + Rand)" seconds where the "t" represents "waitForPodsReady.timeout".
+	// Given that the "backoffLimitCount" equals "30" and the "waitForPodsReady.timeout" equals "300" (default),
+	// the result equals 24 hours (+Rand seconds).
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T19:02:52Z

- **test/integration/scheduler/podsready/scheduler_test.go:263** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:02:52Z
```diff
@@ -248,9 +259,24 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			time.Sleep(podsReadyTimeout)
 			ginkgo.By("finish the eviction")
 			util.FinishEvictionForWorkloads(ctx, k8sClient, prodWl)
-			ginkgo.By("verify the 'prod' workload gets re-admitted once")
+			ginkgo.By("pending by backoff")
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, prodWl)
```

  That makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T19:09:57Z

- **pkg/controller/core/workload_controller.go:374** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:09:57Z
```diff
@@ -322,19 +334,64 @@ func syncAdmissionCheckConditions(conds []kueue.AdmissionCheckState, queueChecks
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
+
+	if _, evictedByTimeout := workload.IsEvictedByPodsReadyTimeout(wl); !ptr.Deref(wl.Spec.Active, true) || evictedByTimeout {
+		// the workload has already been evicted by the PodsReadyTimeout or been deactivated.
+		return ctrl.Result{}, nil
+	}
 	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
 	if !countingTowardsTimeout {
 		return ctrl.Result{}, nil
 	}
 	if recheckAfter > 0 {
 		log.V(4).Info("Workload not yet ready and did not exceed its timeout", "recheckAfter", recheckAfter)
 		return ctrl.Result{RequeueAfter: recheckAfter}, nil
-	} else {
-		log.V(2).Info("Start the eviction of the workload due to exceeding the PodsReady timeout")
-		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
-		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, false)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
+	log.V(2).Info("Start the eviction of the workload due to exceeding the PodsReady timeout")
+	if deactivated, err := r.triggerDeactivationOrBackoffRequeue(ctx, wl); deactivated {
+		return ctrl.Result{}, err
+	}
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
+	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	return ctrl.Result{}, client.IgnoreNotFound(err)
+}
+
+// triggerDeactivationOrBackoffRequeue deactivates a workload (".spec.active"="false")
+// if a re-queued number has already exceeded the limit of re-queuing backoff.
+// Otherwise, it increments a re-queueing count and update a time to be re-queued.
+// It returns true as a first value if a workload is deactivated.
+func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Context, wl *kueue.Workload) (bool, error) {
+	if !workload.HasRequeueState(wl) {
+		wl.Status.RequeueState = &kueue.RequeueState{}
+	}
+	// If requeuingBackoffLimitCount equals to null, the workloads is repeatedly and endless re-queued.
+	requeuingCount := ptr.Deref(wl.Status.RequeueState.Count, 0) + 1
+	if r.requeuingBackoffLimitCount != nil && requeuingCount > *r.requeuingBackoffLimitCount {
+		wl.Spec.Active = ptr.To(false)
+		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T19:30:22Z

- **pkg/queue/cluster_queue_impl_test.go:65** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:30:22Z
```diff
@@ -40,25 +44,57 @@ var (
 	defaultQueueOrderingFunc = queueOrderingFunc(workload.Ordering{
 		PodsReadyRequeuingTimestamp: config.EvictionTimestamp,
 	})
+	canAlwaysQueueWorkload = func(*workload.Info, clock.Clock) bool {
+		return false
+	}
 )
 
 func Test_PushOrUpdate(t *testing.T) {
-	cq := newClusterQueueImpl(keyFunc, defaultQueueOrderingFunc)
-	wl := utiltesting.MakeWorkload("workload-1", defaultNamespace).Obj()
-	if cq.Pending() != 0 {
-		t.Error("ClusterQueue should be empty")
-	}
-	cq.PushOrUpdate(workload.NewInfo(wl))
-	if cq.Pending() != 1 {
-		t.Error("ClusterQueue should have one workload")
+	wlBase := utiltesting.MakeWorkload("workload-1", defaultNamespace).Clone()
+
+	cases := map[string]struct {
+		canNotQueueWorkloadFunc   func(*workload.Info, clock.Clock) bool
+		wantWorkload              *workload.Info
+		wantInAdmissibleWorkloads map[string]*workload.Info
+	}{
+		"can queue workload": {
+			canNotQueueWorkloadFunc: canAlwaysQueueWorkload,
+			wantWorkload:            workload.NewInfo(wlBase.Clone().ResourceVersion("1").Obj()),
+		},
+		"can not queue workload": {
+			canNotQueueWorkloadFunc: func(*workload.Info, clock.Clock) bool { return true },
```

  @alculquicondor I found that we need this stub when we verify behavior on `PushOrUpdate` level.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T19:43:59Z

- **pkg/queue/cluster_queue_impl.go:67** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:43:59Z
```diff
@@ -57,16 +62,21 @@ type clusterQueueBase struct {
 	lessFunc func(a, b interface{}) bool
 
 	rwm sync.RWMutex
+
+	// stubs
+	canNotQueueWorkload func(*workload.Info, clock.Clock) bool
```

  Renamed to `backoffWaitingTimeExpired`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T19:46:51Z

- **pkg/workload/workload.go:380** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:46:51Z
```diff
@@ -375,6 +375,9 @@ func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
 
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if HasRequeueState(w) {
+		wlCopy.Status.RequeueState = w.Status.RequeueState.DeepCopy()
+	}
```

  Oh, I didn't know that this `DeepCopy` func returns null if the receiver is null.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-12T19:47:14Z

Let's leave for a follow up to remove the stub. I don't think it's necessary, if we rework the unit tests.

/lgtm

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T19:47:57Z

- **pkg/workload/workload.go:380** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T19:47:57Z
```diff
@@ -375,6 +375,9 @@ func admissionPatch(w *kueue.Workload) *kueue.Workload {
 	wlCopy := BaseSSAWorkload(w)
 
 	wlCopy.Status.Admission = w.Status.Admission.DeepCopy()
+	if HasRequeueState(w) {
+		wlCopy.Status.RequeueState = w.Status.RequeueState.DeepCopy()
+	}
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T20:05:55Z

- **apis/config/v1beta1/configuration_types.go:241** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T20:05:55Z
```diff
@@ -218,10 +218,35 @@ type MultiKueue struct {
 }
 
 type RequeuingStrategy struct {
-	// Timestamp defines the timestamp used for requeuing a Workload
-	// that was evicted due to Pod readiness. Defaults to Eviction.
+	// Timestamp defines the timestamp used for re-queuing a Workload
+	// that was evicted due to Pod readiness. The possible values are:
+	//
+	// - `Eviction` (default) indicates from Workload `Evicted` condition with `PodsReadyTimeout` reason.
+	// - `Creation` indicates from Workload .metadata.creationTimestamp.
+	//
 	// +optional
 	Timestamp *RequeuingTimestamp `json:"timestamp,omitempty"`
+
+	// BackoffLimitCount defines the maximum number of re-queuing retries.
+	// Once the number is reached, the workload is deactivated (`.spec.activate`=`false`).
+	// When it is null, the workloads will repeatedly and endless re-queueing.
+	//
+	// Every backoff duration is about "1.41284738^(n-1)+Rand" where the "n" represents the "workloadStatus.requeueState.count",
+	// and the "Rand" represents the random jitter. During this time, the workload is taken as an inadmissible and
+	// other workloads will have a chance to be admitted.
+	// Considering the ".waitForPodsReady.timeout",
+	// this indicates that an evicted workload with PodsReadyTimeout reason is continued re-queuing for
+	// the "t(n+1) + SUM[k=1,n](1.41284738^(k-1) + Rand)" seconds where the "t" represents "waitForPodsReady.timeout".
+	// Given that the "backoffLimitCount" equals "30" and the "waitForPodsReady.timeout" equals "300" (default),
+	// the result equals 24 hours (+Rand seconds).
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-12T20:06:01Z

- **test/integration/scheduler/podsready/scheduler_test.go:263** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-12T20:06:00Z
```diff
@@ -248,9 +259,24 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			time.Sleep(podsReadyTimeout)
 			ginkgo.By("finish the eviction")
 			util.FinishEvictionForWorkloads(ctx, k8sClient, prodWl)
-			ginkgo.By("verify the 'prod' workload gets re-admitted once")
+			ginkgo.By("pending by backoff")
+			util.ExpectWorkloadsToBePending(ctx, k8sClient, prodWl)
```

  Done.
