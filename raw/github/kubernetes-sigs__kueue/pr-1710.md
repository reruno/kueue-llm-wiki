# PR #1710: E2e extend startup interval

**Summary**: E2e extend startup interval

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1710

**Last updated**: 2024-02-20T07:04:07Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `e2e-extend-starup-interval` → `main`
- **Created**: 2024-02-09T10:23:59Z
- **Updated**: 2024-02-20T07:04:07Z
- **Merged**: 2024-02-09T15:15:03Z
- **Closed**: 2024-02-09T15:15:03Z
- **Labels**: `kind/bug`, `lgtm`, `kind/flake`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 7   **+82 / -3**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind flake
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

E2e extend startup interval.
Move availability checks in the test suite.
Improve k8s components logging in multikueue clusters.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1700 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-09T10:24:04Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0ac943b90c852fa220f74277256e658cecd1dc61 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65c62c8bdfde5a00083feae1 |

### Comment by [@trasc](https://github.com/trasc) — 2024-02-09T10:46:21Z

Waiting time in the first run

| k8s ver  | singlevluster |  multikueue |
|-|-|-|
|1.26|1m7.641970064s|1m33.772634653s|
|1.27|13.562766ms|**3m30.875595362s**|
|1.28|13.152415ms|1m42.251743066s|
|1.29|12.367304ms|1m54.563073262s|

retest

/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-02-09T11:15:02Z

Waiting time in the second run

| k8s ver  | singlevluster |  multikueue |
|-|-|-|
|1.26|11.09248ms  | 1m40.603293742s  |
|1.27| 13.287003ms |1m56.978570488s  |
|1.28|13.278859ms | 1m42.275907991s  |
|1.29| 11.860044ms | 1m47.009216256s |

retest

/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-02-09T11:40:43Z

/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-02-09T11:54:55Z

/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-02-09T14:08:01Z

/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-09T14:23:44Z

LGTM label has been added.  <details>Git tree hash: de7854735117280b63f664ea58ff29df979eda9c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-09T14:23:46Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1710#pullrequestreview-1872519448" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1710#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-09T14:24:24Z

/test pull-kueue-test-e2e-main-1-26
/test pull-kueue-test-e2e-main-1-27
/test pull-kueue-test-e2e-main-1-28
/test pull-kueue-test-e2e-main-1-29

/hold cancel
(this should be enough retries)

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-09T11:28:11Z

- **test/util/constants.go:33** by [@mimowo](https://github.com/mimowo) — 2024-02-09T11:28:11Z
```diff
@@ -26,8 +26,11 @@ const (
 	// such as running pods to completion.
 	LongTimeout = 45 * time.Second
 	// StartupTimeout is meant to be used for waiting for Kueue to startup, given
-	// that cert updates can take up to 2 minutes to propagate to the filesystem.
-	StartUpTimeout     = 3 * time.Minute
+	// that cert updates can take up to 3 minutes to propagate to the filesystem.
+	// Taken into account that after the certificates are ready, all Kueue's components
+	// need started and the time it takes for a change in ready probe response triggers
+	// a change in the deployment status.
+	StartUpTimeout     = 4 * time.Minute
```

  Maybe even 4min is risky for MultiKueue given that the results show it may take >3min occasionally. Maybe 5min is better to be on the safe side. If we have time-out at 4min we don't know how long it would take it let run longer. Or, alternatively, maybe we can have 2 timeouts - 3min for single cluster and 5min for MK?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-09T11:35:41Z

- **test/util/constants.go:33** by [@trasc](https://github.com/trasc) — 2024-02-09T11:35:40Z
```diff
@@ -26,8 +26,11 @@ const (
 	// such as running pods to completion.
 	LongTimeout = 45 * time.Second
 	// StartupTimeout is meant to be used for waiting for Kueue to startup, given
-	// that cert updates can take up to 2 minutes to propagate to the filesystem.
-	StartUpTimeout     = 3 * time.Minute
+	// that cert updates can take up to 3 minutes to propagate to the filesystem.
+	// Taken into account that after the certificates are ready, all Kueue's components
+	// need started and the time it takes for a change in ready probe response triggers
+	// a change in the deployment status.
+	StartUpTimeout     = 4 * time.Minute
```

  The only problem I see it that the delay is not caused by pod getting ready, (a systematic panic starting kueue) all those 5 min will be lost.
  
  We could also check the pods for restarts an give up early if we see this kind of behavior, but it's a bit overkill.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-09T12:06:08Z

- **test/util/constants.go:33** by [@mimowo](https://github.com/mimowo) — 2024-02-09T12:06:08Z
```diff
@@ -26,8 +26,11 @@ const (
 	// such as running pods to completion.
 	LongTimeout = 45 * time.Second
 	// StartupTimeout is meant to be used for waiting for Kueue to startup, given
-	// that cert updates can take up to 2 minutes to propagate to the filesystem.
-	StartUpTimeout     = 3 * time.Minute
+	// that cert updates can take up to 3 minutes to propagate to the filesystem.
+	// Taken into account that after the certificates are ready, all Kueue's components
+	// need started and the time it takes for a change in ready probe response triggers
+	// a change in the deployment status.
+	StartUpTimeout     = 4 * time.Minute
```

  I think using time to detect a systematic panic on startup is poor approach anyway. I guess ideally we can inspect the replicas and see if they restarted. This way we could detect panics even if the startup took less <3min. 
  
  It is overkill for this PR probably, but I think it is reasonable to follow up with such a diagnostic check. It could be even at the end of e2e tests - just inspect the replicas and see if their containers restarted by inspecting the pod status.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-09T13:44:01Z

- **test/util/constants.go:33** by [@trasc](https://github.com/trasc) — 2024-02-09T13:44:01Z
```diff
@@ -26,8 +26,11 @@ const (
 	// such as running pods to completion.
 	LongTimeout = 45 * time.Second
 	// StartupTimeout is meant to be used for waiting for Kueue to startup, given
-	// that cert updates can take up to 2 minutes to propagate to the filesystem.
-	StartUpTimeout     = 3 * time.Minute
+	// that cert updates can take up to 3 minutes to propagate to the filesystem.
+	// Taken into account that after the certificates are ready, all Kueue's components
+	// need started and the time it takes for a change in ready probe response triggers
+	// a change in the deployment status.
+	StartUpTimeout     = 4 * time.Minute
```

  done, let's see how it works

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-09T14:23:39Z

/lgtm
/approve
/hold
for retests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-09T14:30:06Z

- **test/e2e/multikueue/suite_test.go:71** by [@mimowo](https://github.com/mimowo) — 2024-02-09T14:26:22Z
```diff
@@ -62,4 +63,10 @@ var _ = ginkgo.BeforeSuite(func() {
 	k8sWorker2Client = util.CreateClientUsingCluster("kind-" + worker2ClusterName)
 
 	ctx = context.Background()
+
+	waitForAvailableStart := time.Now()
+	util.WaitForKueueAvailability(ctx, k8sManagerClient)
+	util.WaitForKueueAvailability(ctx, k8sWorker1Client)
+	util.WaitForKueueAvailability(ctx, k8sWorker2Client)
+	ginkgo.GinkgoLogr.Info("Kueue is Available in all the clusters", "waitingTime", time.Since(waitForAvailableStart))
```

  I think this waiting time might be confusing, because it spans 3 clusters, and is delayed since the cluster creation. I think we should move this log line into `WaitForKueueAvailability` and log the wait time between `LastTransitionTime` for the Available condition, and the Deployment `CreationTime`. This way we will see how long it takes per cluster, which is more relevant.

- **test/util/e2e.go:80** by [@mimowo](https://github.com/mimowo) — 2024-02-09T14:28:10Z
```diff
@@ -62,3 +67,31 @@ func CreateVisibilityClient(user string) visibilityv1alpha1.VisibilityV1alpha1In
 	visibilityClient := kueueClient.VisibilityV1alpha1()
 	return visibilityClient
 }
+
+func WaitForKueueAvailability(ctx context.Context, k8sClient client.Client) {
+	kcmKey := types.NamespacedName{
+		Namespace: "kueue-system",
+		Name:      "kueue-controller-manager",
+	}
+	deployment := &appsv1.Deployment{}
+	pods := corev1.PodList{}
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) error {
+		g.Expect(k8sClient.Get(ctx, kcmKey, deployment)).To(gomega.Succeed())
+		g.Expect(k8sClient.List(ctx, &pods, client.InNamespace("kueue-system"), client.MatchingLabels(deployment.Spec.Selector.MatchLabels))).To(gomega.Succeed())
```

  I think the check for pod restarts is independent, and should be moved after the Eventually.
  
  I guess this could even call it from AfterSuite (extracted to the VerifyNoKueueRestarts function), to check that we don't get any failures throughout testing.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-09T14:34:04Z

- **test/e2e/multikueue/suite_test.go:71** by [@trasc](https://github.com/trasc) — 2024-02-09T14:34:04Z
```diff
@@ -62,4 +63,10 @@ var _ = ginkgo.BeforeSuite(func() {
 	k8sWorker2Client = util.CreateClientUsingCluster("kind-" + worker2ClusterName)
 
 	ctx = context.Background()
+
+	waitForAvailableStart := time.Now()
+	util.WaitForKueueAvailability(ctx, k8sManagerClient)
+	util.WaitForKueueAvailability(ctx, k8sWorker1Client)
+	util.WaitForKueueAvailability(ctx, k8sWorker2Client)
+	ginkgo.GinkgoLogr.Info("Kueue is Available in all the clusters", "waitingTime", time.Since(waitForAvailableStart))
```

  This is mostly relevant when we need to recompute the timeout (hopefully never).

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-09T14:38:28Z

- **test/util/e2e.go:80** by [@trasc](https://github.com/trasc) — 2024-02-09T14:38:28Z
```diff
@@ -62,3 +67,31 @@ func CreateVisibilityClient(user string) visibilityv1alpha1.VisibilityV1alpha1In
 	visibilityClient := kueueClient.VisibilityV1alpha1()
 	return visibilityClient
 }
+
+func WaitForKueueAvailability(ctx context.Context, k8sClient client.Client) {
+	kcmKey := types.NamespacedName{
+		Namespace: "kueue-system",
+		Name:      "kueue-controller-manager",
+	}
+	deployment := &appsv1.Deployment{}
+	pods := corev1.PodList{}
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) error {
+		g.Expect(k8sClient.Get(ctx, kcmKey, deployment)).To(gomega.Succeed())
+		g.Expect(k8sClient.List(ctx, &pods, client.InNamespace("kueue-system"), client.MatchingLabels(deployment.Spec.Selector.MatchLabels))).To(gomega.Succeed())
```

  Called after ` Eventually` will not shorten the startup wait time when kueue manager is in a crash loop.
  
  We could think of checking  for crashes in after suite ... but I don't see it connected to this issue.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-09T14:39:38Z

- **test/e2e/multikueue/suite_test.go:71** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-09T14:39:38Z
```diff
@@ -62,4 +63,10 @@ var _ = ginkgo.BeforeSuite(func() {
 	k8sWorker2Client = util.CreateClientUsingCluster("kind-" + worker2ClusterName)
 
 	ctx = context.Background()
+
+	waitForAvailableStart := time.Now()
+	util.WaitForKueueAvailability(ctx, k8sManagerClient)
+	util.WaitForKueueAvailability(ctx, k8sWorker1Client)
+	util.WaitForKueueAvailability(ctx, k8sWorker2Client)
+	ginkgo.GinkgoLogr.Info("Kueue is Available in all the clusters", "waitingTime", time.Since(waitForAvailableStart))
```

  I agree with @trasc

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-09T14:46:01Z

- **test/util/e2e.go:80** by [@mimowo](https://github.com/mimowo) — 2024-02-09T14:46:01Z
```diff
@@ -62,3 +67,31 @@ func CreateVisibilityClient(user string) visibilityv1alpha1.VisibilityV1alpha1In
 	visibilityClient := kueueClient.VisibilityV1alpha1()
 	return visibilityClient
 }
+
+func WaitForKueueAvailability(ctx context.Context, k8sClient client.Client) {
+	kcmKey := types.NamespacedName{
+		Namespace: "kueue-system",
+		Name:      "kueue-controller-manager",
+	}
+	deployment := &appsv1.Deployment{}
+	pods := corev1.PodList{}
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) error {
+		g.Expect(k8sClient.Get(ctx, kcmKey, deployment)).To(gomega.Succeed())
+		g.Expect(k8sClient.List(ctx, &pods, client.InNamespace("kueue-system"), client.MatchingLabels(deployment.Spec.Selector.MatchLabels))).To(gomega.Succeed())
```

  > Called after ` Eventually` will not shorten the startup wait time when kueue manager is in a crash loop.
  
  Yes, but it will be better code organization, because it can be called independently after Eventually. Coupling isn't necessary.
  
  > We could think of checking for crashes in after suite ... but I don't see it connected to this issue.
  
  Sure, we can move do it later as we see need. Still, I think it is better to decouple for the ease of reusing later.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-09T14:48:45Z

- **test/e2e/multikueue/suite_test.go:71** by [@mimowo](https://github.com/mimowo) — 2024-02-09T14:48:44Z
```diff
@@ -62,4 +63,10 @@ var _ = ginkgo.BeforeSuite(func() {
 	k8sWorker2Client = util.CreateClientUsingCluster("kind-" + worker2ClusterName)
 
 	ctx = context.Background()
+
+	waitForAvailableStart := time.Now()
+	util.WaitForKueueAvailability(ctx, k8sManagerClient)
+	util.WaitForKueueAvailability(ctx, k8sWorker1Client)
+	util.WaitForKueueAvailability(ctx, k8sWorker2Client)
+	ginkgo.GinkgoLogr.Info("Kueue is Available in all the clusters", "waitingTime", time.Since(waitForAvailableStart))
```

  Sure, it is a nit but I think the time that is measured might be confusing to interpret when looking in the logs. It isn't really the timeout, because timeout is per cluster, but the log is for all clusters. 
  
  Anyway, it is a nit, so I'm ok to keep as is if you agree.
