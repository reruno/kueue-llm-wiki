# PR #1718: Refactor flavorassigner to reduce number of passed arguments

**Summary**: Refactor flavorassigner to reduce number of passed arguments

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1718

**Last updated**: 2024-02-15T12:19:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `refactor-assigner` → `main`
- **Created**: 2024-02-09T20:08:35Z
- **Updated**: 2024-02-15T12:19:49Z
- **Merged**: 2024-02-15T12:19:47Z
- **Closed**: 2024-02-15T12:19:47Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 4   **+95 / -70**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

Add a FlavorAssigner struct to hold unchanging parameters and reduce the number of arguments passed to multiple functions.

Also doing some variable name changes.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1420

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-09T20:08:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8b99117e227fbc56fae8771b66e433eb494e51bc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65ca4e1ae72dc20008c51d41 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-09T20:08:46Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1718#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-09T20:08:52Z

/hold
for branch cut

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-09T21:12:37Z

/test pull-kueue-test-e2e-main-1-27
Just checking if there are any problems building, because jobset is having some problems

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-13T10:41:53Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-13T10:41:58Z

LGTM label has been added.  <details>Git tree hash: bcf35095a4be5f40be48b9360f0feedd9b27387d</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-15T11:51:33Z

/hold cancel
as 0.6 is released already

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-12T11:10:23Z

LGTM, one suggestion for code organization to discuss or apply

- **pkg/scheduler/flavorassigner/flavorassigner.go:389** by [@mimowo](https://github.com/mimowo) — 2024-02-12T11:09:27Z
```diff
@@ -356,38 +355,48 @@ func (a *Assignment) append(requests workload.Requests, psAssignment *PodSetAssi
 	a.LastState.LastTriedFlavorIdx = append(a.LastState.LastTriedFlavorIdx, flavorIdx)
 }
 
-// findFlavorForResourceGroup finds the flavor which can satisfy the resource
-// request, along with the information about resources that need to be borrowed.
+// findFlavorForPodSetResource finds the flavor which can satisfy the podSet request
+// for all resources in the same group as resName.
+// Returns the chosen flavor, along with the information about resources that need to be borrowed.
 // If the flavor cannot be immediately assigned, it returns a status with
 // reasons or failure.
-func (a *Assignment) findFlavorForResourceGroup(
+func (a *FlavorAssigner) findFlavorForPodSetResource(
 	log logr.Logger,
-	rg *cache.ResourceGroup,
+	psId int,
 	requests workload.Requests,
-	resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor,
-	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec,
-	lastAssignment int) (ResourceAssignment, *Status) {
+	resName corev1.ResourceName,
+	assignmentUsage cache.FlavorResourceQuantities,
+) (ResourceAssignment, *Status) {
+	resourceGroup, found := a.cq.RGByResource[resName]
+	if !found {
+		return nil, &Status{
+			reasons: []string{fmt.Sprintf("resource %s unavailable in ClusterQueue", resName)},
+		}
+	}
+
 	status := &Status{}
-	requests = filterRequestedResources(requests, rg.CoveredResources)
+	requests = filterRequestedResources(requests, resourceGroup.CoveredResources)
+	podSpec := &a.wl.Obj.Spec.PodSets[psId].Template.Spec
 
 	var bestAssignment ResourceAssignment
 	bestAssignmentMode := NoFit
 
 	// We will only check against the flavors' labels for the resource.
-	selector := flavorSelector(spec, rg.LabelKeys)
-	flavorIdx := -1
-	for idx, flvQuotas := range rg.Flavors {
-		if features.Enabled(features.FlavorFungibility) && idx <= lastAssignment {
-			continue
-		}
-		flavor, exist := resourceFlavors[flvQuotas.Name]
+	selector := flavorSelector(podSpec, resourceGroup.LabelKeys)
+	assignedFlavorIdx := -1
+	idx := 0
+	if features.Enabled(features.FlavorFungibility) {
+		idx = a.wl.LastAssignment.LastTriedFlavorForPodSetResource(psId, resName) + 1
```

  nit: the `+ 1` here needs to correspond with the  `-1` returned in `workload.go`. This may require jumping to understand. 
  
  I'm wondering if we could have a function `NextFlavorIdxToTry` that would encapsulate the logic (including the check for `features.Enabled(features.FlavorFungibility)`) and always return >= 0.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-12T16:57:42Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:389** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-12T16:57:42Z
```diff
@@ -356,38 +355,48 @@ func (a *Assignment) append(requests workload.Requests, psAssignment *PodSetAssi
 	a.LastState.LastTriedFlavorIdx = append(a.LastState.LastTriedFlavorIdx, flavorIdx)
 }
 
-// findFlavorForResourceGroup finds the flavor which can satisfy the resource
-// request, along with the information about resources that need to be borrowed.
+// findFlavorForPodSetResource finds the flavor which can satisfy the podSet request
+// for all resources in the same group as resName.
+// Returns the chosen flavor, along with the information about resources that need to be borrowed.
 // If the flavor cannot be immediately assigned, it returns a status with
 // reasons or failure.
-func (a *Assignment) findFlavorForResourceGroup(
+func (a *FlavorAssigner) findFlavorForPodSetResource(
 	log logr.Logger,
-	rg *cache.ResourceGroup,
+	psId int,
 	requests workload.Requests,
-	resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor,
-	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec,
-	lastAssignment int) (ResourceAssignment, *Status) {
+	resName corev1.ResourceName,
+	assignmentUsage cache.FlavorResourceQuantities,
+) (ResourceAssignment, *Status) {
+	resourceGroup, found := a.cq.RGByResource[resName]
+	if !found {
+		return nil, &Status{
+			reasons: []string{fmt.Sprintf("resource %s unavailable in ClusterQueue", resName)},
+		}
+	}
+
 	status := &Status{}
-	requests = filterRequestedResources(requests, rg.CoveredResources)
+	requests = filterRequestedResources(requests, resourceGroup.CoveredResources)
+	podSpec := &a.wl.Obj.Spec.PodSets[psId].Template.Spec
 
 	var bestAssignment ResourceAssignment
 	bestAssignmentMode := NoFit
 
 	// We will only check against the flavors' labels for the resource.
-	selector := flavorSelector(spec, rg.LabelKeys)
-	flavorIdx := -1
-	for idx, flvQuotas := range rg.Flavors {
-		if features.Enabled(features.FlavorFungibility) && idx <= lastAssignment {
-			continue
-		}
-		flavor, exist := resourceFlavors[flvQuotas.Name]
+	selector := flavorSelector(podSpec, resourceGroup.LabelKeys)
+	assignedFlavorIdx := -1
+	idx := 0
+	if features.Enabled(features.FlavorFungibility) {
+		idx = a.wl.LastAssignment.LastTriedFlavorForPodSetResource(psId, resName) + 1
```

  Good idea

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-13T10:41:58Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:389** by [@mimowo](https://github.com/mimowo) — 2024-02-13T10:41:58Z
```diff
@@ -356,38 +355,48 @@ func (a *Assignment) append(requests workload.Requests, psAssignment *PodSetAssi
 	a.LastState.LastTriedFlavorIdx = append(a.LastState.LastTriedFlavorIdx, flavorIdx)
 }
 
-// findFlavorForResourceGroup finds the flavor which can satisfy the resource
-// request, along with the information about resources that need to be borrowed.
+// findFlavorForPodSetResource finds the flavor which can satisfy the podSet request
+// for all resources in the same group as resName.
+// Returns the chosen flavor, along with the information about resources that need to be borrowed.
 // If the flavor cannot be immediately assigned, it returns a status with
 // reasons or failure.
-func (a *Assignment) findFlavorForResourceGroup(
+func (a *FlavorAssigner) findFlavorForPodSetResource(
 	log logr.Logger,
-	rg *cache.ResourceGroup,
+	psId int,
 	requests workload.Requests,
-	resourceFlavors map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor,
-	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec,
-	lastAssignment int) (ResourceAssignment, *Status) {
+	resName corev1.ResourceName,
+	assignmentUsage cache.FlavorResourceQuantities,
+) (ResourceAssignment, *Status) {
+	resourceGroup, found := a.cq.RGByResource[resName]
+	if !found {
+		return nil, &Status{
+			reasons: []string{fmt.Sprintf("resource %s unavailable in ClusterQueue", resName)},
+		}
+	}
+
 	status := &Status{}
-	requests = filterRequestedResources(requests, rg.CoveredResources)
+	requests = filterRequestedResources(requests, resourceGroup.CoveredResources)
+	podSpec := &a.wl.Obj.Spec.PodSets[psId].Template.Spec
 
 	var bestAssignment ResourceAssignment
 	bestAssignmentMode := NoFit
 
 	// We will only check against the flavors' labels for the resource.
-	selector := flavorSelector(spec, rg.LabelKeys)
-	flavorIdx := -1
-	for idx, flvQuotas := range rg.Flavors {
-		if features.Enabled(features.FlavorFungibility) && idx <= lastAssignment {
-			continue
-		}
-		flavor, exist := resourceFlavors[flvQuotas.Name]
+	selector := flavorSelector(podSpec, resourceGroup.LabelKeys)
+	assignedFlavorIdx := -1
+	idx := 0
+	if features.Enabled(features.FlavorFungibility) {
+		idx = a.wl.LastAssignment.LastTriedFlavorForPodSetResource(psId, resName) + 1
```

  thx
