# PR #1721: Test that pods are observed when there are error during the cleanup

**Summary**: Test that pods are observed when there are error during the cleanup

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1721

**Last updated**: 2025-06-10T21:07:57Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kaisoz](https://github.com/kaisoz)
- **Branch**: `add-pod-cleanup-tests` → `main`
- **Created**: 2024-02-12T15:52:45Z
- **Updated**: 2025-06-10T21:07:57Z
- **Merged**: 2024-02-16T10:26:52Z
- **Closed**: 2024-02-16T10:26:52Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 1   **+123 / -0**

## Description


#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:
This PR adds tests to ensure that pods are still observed in case errors occur during the cleanup process's create/update operations


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes [#1636](https://github.com/kubernetes-sigs/kueue/issues/1636)


#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-12T15:52:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a7db49d6fb2c6bfa483d2b349d244b28b826f233 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65cf32e76dc4a300083da67d |

### Comment by [@kaisoz](https://github.com/kaisoz) — 2024-02-12T15:58:07Z

/assign @mimowo 

PTAL 😊 Thanks!

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-12T16:01:24Z

/assign

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-14T09:00:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1721#" title="Author self-approved">kaisoz</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1721#pullrequestreview-1879714480" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-16T10:05:38Z

LGTM label has been added.  <details>Git tree hash: 5f875373677e73d2c1e4bef9c52b7e600f80cccb</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-29T08:37:18Z

It is better to cherry-pick this PR to release-0.6 to avoid #2089 conflicts.
/cherry-pick release-0.6

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-04-29T08:37:54Z

@tenzen-y: new pull request created: #2090

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1721#issuecomment-2082163298):

>It is better to cherry-pick this PR to release-0.6 to avoid #2089 conflicts.
>/cherry-pick release-0.6
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-13T10:39:21Z

Thanks for the PR!

- **pkg/controller/jobs/pod/pod_controller_test.go:3252** by [@mimowo](https://github.com/mimowo) — 2024-02-13T10:23:05Z
```diff
@@ -3178,3 +3179,135 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_PodIsObservedWhenUpdateOrDeleteOpsFail(t *testing.T) {
+
+	reqcount := 0
+	now := time.Now()
+	connRefusedEerrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if reqcount == 0 {
```

  I find this logic tricky with `reqcount`. Instead I would suggest to have explicit: pair of (bool, or error) vars: `mockUpdateErr` and `mockDeleteErr`. Then set the values before the `Reconcile` as we want. This might be more code, but more explicit.

- **pkg/controller/jobs/pod/pod_controller_test.go:3184** by [@mimowo](https://github.com/mimowo) — 2024-02-13T10:23:16Z
```diff
@@ -3178,3 +3179,135 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_PodIsObservedWhenUpdateOrDeleteOpsFail(t *testing.T) {
+
```

  nit: remove empty line

- **pkg/controller/jobs/pod/pod_controller_test.go:3183** by [@mimowo](https://github.com/mimowo) — 2024-02-13T10:24:04Z
```diff
@@ -3178,3 +3179,135 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_PodIsObservedWhenUpdateOrDeleteOpsFail(t *testing.T) {
```

  ```suggestion
  func TestReconciler_DeletePodAfterTransientErrorsOnUpdateOrDeleteOps(t *testing.T) {
  ```
  nit, I think it better captures the intention. If we didn't clean the expectations, then the next Reconcile would hang.

- **pkg/controller/jobs/pod/pod_controller_test.go:3293** by [@mimowo](https://github.com/mimowo) — 2024-02-13T10:33:28Z
```diff
@@ -3178,3 +3179,135 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_PodIsObservedWhenUpdateOrDeleteOpsFail(t *testing.T) {
+
+	reqcount := 0
+	now := time.Now()
+	connRefusedEerrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+
+	reconcilerImpl := reconciler.(*Reconciler)
+	excessPodExpectationsStore := reconcilerImpl.expectationsStore
+
+	reconcileRequest := reconcileRequestForPod(&pods[0])
+	podGroupKey := types.NamespacedName{
+		Name:      reconcileRequest.NamespacedName.Name,
+		Namespace: strings.Split(reconcileRequest.NamespacedName.Namespace, "/")[1],
+	}
+
+	// Reconcile for the first time. It'll try  to remove the finalizers but fail
+	_, err := reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedEerrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// The pod should've been observed even though the update failed
+	if satisfied := excessPodExpectationsStore.Satisfied(ctrl.LoggerFrom(ctx), podGroupKey); !satisfied {
```

  I think checking the expectations is not necessary actually. If they were not cleaned, the next Reconcile would not be able to make progress. 
  
  OTOH these are unit tests, so getting into the weeds is ok. Still, I think we could start simple, will less code, yet covering the bug if the lines were missing.

- **pkg/controller/jobs/pod/pod_controller_test.go:3289** by [@mimowo](https://github.com/mimowo) — 2024-02-13T10:35:00Z
```diff
@@ -3178,3 +3179,135 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_PodIsObservedWhenUpdateOrDeleteOpsFail(t *testing.T) {
+
+	reqcount := 0
+	now := time.Now()
+	connRefusedEerrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+
+	reconcilerImpl := reconciler.(*Reconciler)
+	excessPodExpectationsStore := reconcilerImpl.expectationsStore
+
+	reconcileRequest := reconcileRequestForPod(&pods[0])
+	podGroupKey := types.NamespacedName{
+		Name:      reconcileRequest.NamespacedName.Name,
+		Namespace: strings.Split(reconcileRequest.NamespacedName.Namespace, "/")[1],
+	}
+
+	// Reconcile for the first time. It'll try  to remove the finalizers but fail
+	_, err := reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedEerrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// The pod should've been observed even though the update failed
+	if satisfied := excessPodExpectationsStore.Satisfied(ctrl.LoggerFrom(ctx), podGroupKey); !satisfied {
+		t.Fatalf("Expected '%s' UID to be observed", podGroupKey)
+	}
+
+	// Reconcile for the second time to remove the finalizers. Then it should attempt to delete the pod but fail
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedEerrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// The pod should've been observed even though the deletion failed
+	if satisfied := excessPodExpectationsStore.Satisfied(ctrl.LoggerFrom(ctx), podGroupKey); !satisfied {
+		t.Fatalf("Expected '%s' UID to be observed", podGroupKey)
+	}
+
+	// Reconcile for the third time and delete the pod
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
```

  I would suggest to verify the pod is actually deleted at the end. This way we make it clear the deletion happened. Otherwise, it could not happen due to another bug, potentially.

- **pkg/controller/jobs/pod/pod_controller_test.go:3277** by [@mimowo](https://github.com/mimowo) — 2024-02-13T10:36:53Z
```diff
@@ -3178,3 +3179,135 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_PodIsObservedWhenUpdateOrDeleteOpsFail(t *testing.T) {
+
+	reqcount := 0
+	now := time.Now()
+	connRefusedEerrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+
+	reconcilerImpl := reconciler.(*Reconciler)
```

  This casting could be eliminated if we don't assert expecatations directly, as suggested in the comment lower down.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2024-02-13T21:20:33Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3183** by [@kaisoz](https://github.com/kaisoz) — 2024-02-13T21:20:32Z
```diff
@@ -3178,3 +3179,135 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_PodIsObservedWhenUpdateOrDeleteOpsFail(t *testing.T) {
```

  Makes sense 👍🏻

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2024-02-13T21:46:52Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3277** by [@kaisoz](https://github.com/kaisoz) — 2024-02-13T21:46:52Z
```diff
@@ -3178,3 +3179,135 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_PodIsObservedWhenUpdateOrDeleteOpsFail(t *testing.T) {
+
+	reqcount := 0
+	now := time.Now()
+	connRefusedEerrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+
+	reconcilerImpl := reconciler.(*Reconciler)
```

  Done 👍🏻

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2024-02-13T21:49:00Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3293** by [@kaisoz](https://github.com/kaisoz) — 2024-02-13T21:49:00Z
```diff
@@ -3178,3 +3179,135 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_PodIsObservedWhenUpdateOrDeleteOpsFail(t *testing.T) {
+
+	reqcount := 0
+	now := time.Now()
+	connRefusedEerrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if reqcount == 0 {
+					reqcount += 1
+					return connRefusedEerrMock
+				}
+				reqcount = 0
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+
+	reconcilerImpl := reconciler.(*Reconciler)
+	excessPodExpectationsStore := reconcilerImpl.expectationsStore
+
+	reconcileRequest := reconcileRequestForPod(&pods[0])
+	podGroupKey := types.NamespacedName{
+		Name:      reconcileRequest.NamespacedName.Name,
+		Namespace: strings.Split(reconcileRequest.NamespacedName.Namespace, "/")[1],
+	}
+
+	// Reconcile for the first time. It'll try  to remove the finalizers but fail
+	_, err := reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedEerrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// The pod should've been observed even though the update failed
+	if satisfied := excessPodExpectationsStore.Satisfied(ctrl.LoggerFrom(ctx), podGroupKey); !satisfied {
```

  Yeah, you are right, the expectations check is not necessary since the controller is waiting to observe previous operations on pods 👍🏻

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-14T09:00:00Z

/approve
LGTM, waiting for nits

- **pkg/controller/jobs/pod/pod_controller_test.go:3297** by [@mimowo](https://github.com/mimowo) — 2024-02-14T08:58:03Z
```diff
@@ -3178,3 +3178,123 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_DeletePodAfterTransientErrorsOnUpdateOrDeleteOps(t *testing.T) {
+	now := time.Now()
+	connRefusedErrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+	var triggerUpdateErr, triggerDeleteErr bool
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if triggerUpdateErr {
+					return connRefusedErrMock
+				}
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if triggerDeleteErr {
+					return connRefusedErrMock
+				}
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+	reconcileRequest := reconcileRequestForPod(&pods[0])
+
+	// Reconcile for the first time. It'll try  to remove the finalizers but fail
+	triggerUpdateErr = true
+	_, err := reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the second time to remove the finalizers. Then it should attempt to delete the pod but fail
+	triggerUpdateErr = false
+	triggerDeleteErr = true
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the third time and delete the pod
+	triggerDeleteErr = false
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if err != nil {
+		t.Errorf("Got unexpected error while running reconcile:\n%v", err)
+	}
+
+	// Verify that the pod has been indeed deleted
+	podKey := client.ObjectKeyFromObject(&pods[2])
+	var gotPod corev1.Pod
+	if err := kClient.Get(ctx, podKey, &gotPod); err == nil {
```

  nit, verify there is no other error than NotFound, you should be able to use errors.IsNotFound. Fail the test on any other error.

- **pkg/controller/jobs/pod/pod_controller_test.go:3298** by [@mimowo](https://github.com/mimowo) — 2024-02-14T08:58:56Z
```diff
@@ -3178,3 +3178,123 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_DeletePodAfterTransientErrorsOnUpdateOrDeleteOps(t *testing.T) {
+	now := time.Now()
+	connRefusedErrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+	var triggerUpdateErr, triggerDeleteErr bool
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if triggerUpdateErr {
+					return connRefusedErrMock
+				}
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if triggerDeleteErr {
+					return connRefusedErrMock
+				}
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+	reconcileRequest := reconcileRequestForPod(&pods[0])
+
+	// Reconcile for the first time. It'll try  to remove the finalizers but fail
+	triggerUpdateErr = true
+	_, err := reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the second time to remove the finalizers. Then it should attempt to delete the pod but fail
+	triggerUpdateErr = false
+	triggerDeleteErr = true
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the third time and delete the pod
+	triggerDeleteErr = false
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if err != nil {
+		t.Errorf("Got unexpected error while running reconcile:\n%v", err)
+	}
+
+	// Verify that the pod has been indeed deleted
+	podKey := client.ObjectKeyFromObject(&pods[2])
+	var gotPod corev1.Pod
+	if err := kClient.Get(ctx, podKey, &gotPod); err == nil {
+		t.Fatalf("excessPod shouldn't exist after the last reconciliation")
```

  nit
  ```suggestion
  		t.Fatalf("excessPod %q shouldn't exist after the last reconciliation", podKey.String())
  ```
  ?

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2024-02-15T21:37:34Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3297** by [@kaisoz](https://github.com/kaisoz) — 2024-02-15T21:37:34Z
```diff
@@ -3178,3 +3178,123 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_DeletePodAfterTransientErrorsOnUpdateOrDeleteOps(t *testing.T) {
+	now := time.Now()
+	connRefusedErrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+	var triggerUpdateErr, triggerDeleteErr bool
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if triggerUpdateErr {
+					return connRefusedErrMock
+				}
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if triggerDeleteErr {
+					return connRefusedErrMock
+				}
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+	reconcileRequest := reconcileRequestForPod(&pods[0])
+
+	// Reconcile for the first time. It'll try  to remove the finalizers but fail
+	triggerUpdateErr = true
+	_, err := reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the second time to remove the finalizers. Then it should attempt to delete the pod but fail
+	triggerUpdateErr = false
+	triggerDeleteErr = true
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the third time and delete the pod
+	triggerDeleteErr = false
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if err != nil {
+		t.Errorf("Got unexpected error while running reconcile:\n%v", err)
+	}
+
+	// Verify that the pod has been indeed deleted
+	podKey := client.ObjectKeyFromObject(&pods[2])
+	var gotPod corev1.Pod
+	if err := kClient.Get(ctx, podKey, &gotPod); err == nil {
```

  Done! Thanks!

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2024-02-15T21:39:20Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3298** by [@kaisoz](https://github.com/kaisoz) — 2024-02-15T21:39:20Z
```diff
@@ -3178,3 +3178,123 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_DeletePodAfterTransientErrorsOnUpdateOrDeleteOps(t *testing.T) {
+	now := time.Now()
+	connRefusedErrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+	var triggerUpdateErr, triggerDeleteErr bool
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if triggerUpdateErr {
+					return connRefusedErrMock
+				}
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if triggerDeleteErr {
+					return connRefusedErrMock
+				}
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+	reconcileRequest := reconcileRequestForPod(&pods[0])
+
+	// Reconcile for the first time. It'll try  to remove the finalizers but fail
+	triggerUpdateErr = true
+	_, err := reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the second time to remove the finalizers. Then it should attempt to delete the pod but fail
+	triggerUpdateErr = false
+	triggerDeleteErr = true
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the third time and delete the pod
+	triggerDeleteErr = false
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if err != nil {
+		t.Errorf("Got unexpected error while running reconcile:\n%v", err)
+	}
+
+	// Verify that the pod has been indeed deleted
+	podKey := client.ObjectKeyFromObject(&pods[2])
+	var gotPod corev1.Pod
+	if err := kClient.Get(ctx, podKey, &gotPod); err == nil {
+		t.Fatalf("excessPod shouldn't exist after the last reconciliation")
```

  Thanks for the suggestion! I rephrased it to "Pod %q shouldn't exist after the last reconciliation" because the pod is already named 'excessPod' and it sounded a bit redundant

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-16T09:40:03Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3297** by [@mimowo](https://github.com/mimowo) — 2024-02-16T09:40:02Z
```diff
@@ -3178,3 +3178,123 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_DeletePodAfterTransientErrorsOnUpdateOrDeleteOps(t *testing.T) {
+	now := time.Now()
+	connRefusedErrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+	var triggerUpdateErr, triggerDeleteErr bool
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if triggerUpdateErr {
+					return connRefusedErrMock
+				}
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if triggerDeleteErr {
+					return connRefusedErrMock
+				}
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+	reconcileRequest := reconcileRequestForPod(&pods[0])
+
+	// Reconcile for the first time. It'll try  to remove the finalizers but fail
+	triggerUpdateErr = true
+	_, err := reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the second time to remove the finalizers. Then it should attempt to delete the pod but fail
+	triggerUpdateErr = false
+	triggerDeleteErr = true
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the third time and delete the pod
+	triggerDeleteErr = false
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if err != nil {
+		t.Errorf("Got unexpected error while running reconcile:\n%v", err)
+	}
+
+	// Verify that the pod has been indeed deleted
+	podKey := client.ObjectKeyFromObject(&pods[2])
+	var gotPod corev1.Pod
+	if err := kClient.Get(ctx, podKey, &gotPod); !apierrors.IsNotFound(err) {
```

  nit: We have 3 cases here: 
  1. `err = nil` this is bad, because the pod wasn't removed, 
  2. `apierrors.IsNotFound(err)` - this is expected, 
  3. `!apierrors.IsNotFound(err)` -this is bad, because some unexpected error was returned. 
  
  Make sure the err message is logged in case (3.).

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2024-02-16T10:03:52Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3297** by [@kaisoz](https://github.com/kaisoz) — 2024-02-16T10:03:52Z
```diff
@@ -3178,3 +3178,123 @@ func TestGetWorkloadNameForPod(t *testing.T) {
 		t.Errorf("Expected different workload name\n want: %s\n got: %s", wantWlName, wlName)
 	}
 }
+
+func TestReconciler_DeletePodAfterTransientErrorsOnUpdateOrDeleteOps(t *testing.T) {
+	now := time.Now()
+	connRefusedErrMock := fmt.Errorf("connection refused: %w", syscall.ECONNREFUSED)
+	ctx, _ := utiltesting.ContextWithLog(t)
+	var triggerUpdateErr, triggerDeleteErr bool
+
+	basePodWrapper := testingpod.MakePod("pod", "ns").
+		UID("test-uid").
+		Queue("user-queue").
+		Request(corev1.ResourceCPU, "1").
+		Image("", nil)
+
+	pods := []corev1.Pod{
+		*basePodWrapper.
+			Clone().
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			GroupTotalCount("2").
+			CreationTimestamp(now).
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("pod2").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute)).
+			GroupTotalCount("2").
+			Obj(),
+		*basePodWrapper.
+			Clone().
+			Name("excessPod").
+			Label("kueue.x-k8s.io/managed", "true").
+			KueueFinalizer().
+			Group("test-group").
+			CreationTimestamp(now.Add(time.Minute * 2)).
+			GroupTotalCount("2").
+			Obj(),
+	}
+
+	wl := *utiltesting.MakeWorkload("test-group", "ns").
+		PodSets(
+			*utiltesting.MakePodSet("60bc72d3", 2).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+		).
+		Queue("user-queue").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "pod2", "test-uid").
+		OwnerReference(corev1.SchemeGroupVersion.WithKind("Pod"), "excessPod", "test-uid").
+		ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
+		Admitted(true).
+		Obj()
+
+	clientBuilder := utiltesting.NewClientBuilder()
+	if err := SetupIndexes(ctx, utiltesting.AsIndexer(clientBuilder)); err != nil {
+		t.Fatalf("Could not setup indexes: %v", err)
+	}
+
+	for i := range pods {
+		clientBuilder = clientBuilder.WithObjects(&pods[i])
+	}
+
+	kcBuilder := clientBuilder.
+		WithStatusSubresource(&wl).
+		WithInterceptorFuncs(interceptor.Funcs{
+			Update: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.UpdateOption) error {
+				if triggerUpdateErr {
+					return connRefusedErrMock
+				}
+				return client.Update(ctx, obj, opts...)
+			},
+			Delete: func(ctx context.Context, client client.WithWatch, obj client.Object, opts ...client.DeleteOption) error {
+				if triggerDeleteErr {
+					return connRefusedErrMock
+				}
+				return client.Delete(ctx, obj, opts...)
+			},
+		})
+
+	kClient := kcBuilder.Build()
+	if err := kClient.Create(ctx, &wl); err != nil {
+		t.Fatalf("Could not create workload: %v", err)
+	}
+
+	recorder := record.NewBroadcaster().NewRecorder(kClient.Scheme(), corev1.EventSource{Component: "test"})
+	reconciler := NewReconciler(kClient, recorder)
+	reconcileRequest := reconcileRequestForPod(&pods[0])
+
+	// Reconcile for the first time. It'll try  to remove the finalizers but fail
+	triggerUpdateErr = true
+	_, err := reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the second time to remove the finalizers. Then it should attempt to delete the pod but fail
+	triggerUpdateErr = false
+	triggerDeleteErr = true
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if diff := cmp.Diff(connRefusedErrMock, err, cmpopts.EquateErrors()); diff != "" {
+		t.Errorf("Expected reconcile error (-want,+got):\n%s", diff)
+	}
+
+	// Reconcile for the third time and delete the pod
+	triggerDeleteErr = false
+	_, err = reconciler.Reconcile(ctx, reconcileRequest)
+	if err != nil {
+		t.Errorf("Got unexpected error while running reconcile:\n%v", err)
+	}
+
+	// Verify that the pod has been indeed deleted
+	podKey := client.ObjectKeyFromObject(&pods[2])
+	var gotPod corev1.Pod
+	if err := kClient.Get(ctx, podKey, &gotPod); !apierrors.IsNotFound(err) {
```

  Make sense. I improved the logging to make clear when it fails because of an unexpected error, and when it failed because the pod was actually found

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-16T10:05:34Z

/lgtm
Thanks for the contribution in Kueue
