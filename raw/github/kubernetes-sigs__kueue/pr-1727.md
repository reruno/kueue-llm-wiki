# PR #1727: Doc: Add PodsReadyRequeueStrategy documentation

**Summary**: Doc: Add PodsReadyRequeueStrategy documentation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1727

**Last updated**: 2025-08-15T23:12:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `add-backof-requeuing-docs` → `main`
- **Created**: 2024-02-13T14:34:42Z
- **Updated**: 2025-08-15T23:12:37Z
- **Merged**: 2024-02-14T14:05:46Z
- **Closed**: 2024-02-14T14:05:46Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 2   **+59 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:
I added documentation for the PodsReadyRequeueStrategy.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1282 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-13T14:34:47Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 516f7e7efcf8e4d602a08ffb3e3b64fa318b0bfa |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65cc59323892380008b3eb98 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1727--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1727--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE3MjctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.47pslNLr9Wtk3-SAxmNnXGm8Snk_-bQLBo5-icJ1M30)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-13T14:34:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1727#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T17:51:51Z

/assign @alculquicondor

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:56:20Z

@alculquicondor I addressed all comments. PTAL.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-14T06:10:47Z

@alculquicondor I addressed all comments. PTAL

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-14T14:01:53Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-14T14:01:59Z

LGTM label has been added.  <details>Git tree hash: 249d4445050c65bcda33327edf880b2137e51378</details>

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-13T18:42:13Z

- **site/content/en/docs/concepts/workload.md:131** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:33:05Z
```diff
@@ -128,6 +128,24 @@ status:
 ```
 The `count` can only increase while the workload holds a Quota Reservation.
 
+## Exponential Backoff ReQueueing for Sequential Admission with Ready Pods
```

  Before going into backoffs, we should have a section for ReadyPodsTimeout in general.

- **site/content/en/docs/tasks/setup_sequential_admission.md:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:35:22Z
```diff
@@ -43,16 +43,20 @@ fields:
     waitForPodsReady:
       enable: true
       timeout: 10m
```

  can you add `blockAdmission` as well?

- **site/content/en/docs/tasks/setup_sequential_admission.md:69** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:35:44Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
```

  ```suggestion
  ### Requeuing Strategy
  ```

- **site/content/en/docs/tasks/setup_sequential_admission.md:74** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:36:26Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
```

  ```suggestion
  The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) contains optional parameters:
  `timestamp` and `backoffLimitCount`.
  ```

- **site/content/en/docs/tasks/setup_sequential_admission.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:36:49Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
```

  remove these lines. It could be explained below.

- **site/content/en/docs/tasks/setup_sequential_admission.md:78** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:37:43Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
```

  ```suggestion
  The `timestamp` field defines which timestamp Kueue uses to order the Workloads in the queue:
  ```

- **site/content/en/docs/tasks/setup_sequential_admission.md:80** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:37:53Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
```

  ```suggestion
  - `Eviction` (default): The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
  ```

- **site/content/en/docs/tasks/setup_sequential_admission.md:83** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:38:45Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
```

  ```suggestion
  If you want to re-queue a Workload evicted by the `PodsReadyTimeout` back to its original place in the queue,
  ```

- **site/content/en/docs/tasks/setup_sequential_admission.md:84** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:39:00Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
+you can set the `Creation` to the timestamp. 
```

  ```suggestion
  you should use the `Creation` to the timestamp.
  ```

- **site/content/en/docs/tasks/setup_sequential_admission.md:86** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:40:50Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
+you can set the `Creation` to the timestamp. 
+
+A Workload evicted by the `PodsReadyTimeout` reason is re-queued until the count is reached to backoffLimitCount.
```

  ```suggestion
  Kueue will requeue a Workload evicted by the `PodsReadyTimeout` reason until the number of requeues reaches `backoffLimitCount`.
  ```

- **site/content/en/docs/tasks/setup_sequential_admission.md:87** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:41:01Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
+you can set the `Creation` to the timestamp. 
+
+A Workload evicted by the `PodsReadyTimeout` reason is re-queued until the count is reached to backoffLimitCount.
+If you don't specify any integer for backoffLimitCount, 
```

  ```suggestion
  If you don't specify any value for backoffLimitCount, 
  ```

- **site/content/en/docs/tasks/setup_sequential_admission.md:89** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:41:57Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
+you can set the `Creation` to the timestamp. 
+
+A Workload evicted by the `PodsReadyTimeout` reason is re-queued until the count is reached to backoffLimitCount.
+If you don't specify any integer for backoffLimitCount, 
+a Workload is repeatedly and endlessly re-queued to the queue based on timestamp.
+After the count is reached, a Workload is [deactivated](docs/concepts/workload/#active).
```

  ```suggestion
  Once the the number of requeues reaches the limit, Kueue [deactivates the Workload](docs/concepts/workload/#active).
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T18:55:17Z

- **site/content/en/docs/concepts/workload.md:131** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T18:55:17Z
```diff
@@ -128,6 +128,24 @@ status:
 ```
 The `count` can only increase while the workload holds a Quota Reservation.
 
+## Exponential Backoff ReQueueing for Sequential Admission with Ready Pods
```

  That makes sense.
  We didn't add any docs related to PodsReady when we added the waitForPodsReady feature :(

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T18:55:26Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:45** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T18:55:26Z
```diff
@@ -43,16 +43,20 @@ fields:
     waitForPodsReady:
       enable: true
       timeout: 10m
```

  Sure.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-13T18:58:12Z

- **site/content/en/docs/concepts/workload.md:131** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T18:58:12Z
```diff
@@ -128,6 +128,24 @@ status:
 ```
 The `count` can only increase while the workload holds a Quota Reservation.
 
+## Exponential Backoff ReQueueing for Sequential Admission with Ready Pods
```

  not on this page.... most of it is described in tasks. We can keep the note here minimal.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T18:59:15Z

- **site/content/en/docs/concepts/workload.md:131** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T18:59:15Z
```diff
@@ -128,6 +128,24 @@ status:
 ```
 The `count` can only increase while the workload holds a Quota Reservation.
 
+## Exponential Backoff ReQueueing for Sequential Admission with Ready Pods
```

  alright, I will do it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:52:49Z

- **site/content/en/docs/concepts/workload.md:131** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:52:49Z
```diff
@@ -128,6 +128,24 @@ status:
 ```
 The `count` can only increase while the workload holds a Quota Reservation.
 
+## Exponential Backoff ReQueueing for Sequential Admission with Ready Pods
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:52:53Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:45** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:52:53Z
```diff
@@ -43,16 +43,20 @@ fields:
     waitForPodsReady:
       enable: true
       timeout: 10m
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:52:55Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:69** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:52:55Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:52:58Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:74** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:52:58Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:53:01Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:53:01Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:53:03Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:78** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:53:03Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:53:05Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:80** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:53:05Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:53:07Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:83** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:53:07Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:53:09Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:84** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:53:09Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
+you can set the `Creation` to the timestamp. 
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:53:11Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:86** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:53:11Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
+you can set the `Creation` to the timestamp. 
+
+A Workload evicted by the `PodsReadyTimeout` reason is re-queued until the count is reached to backoffLimitCount.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:53:14Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:87** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:53:14Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
+you can set the `Creation` to the timestamp. 
+
+A Workload evicted by the `PodsReadyTimeout` reason is re-queued until the count is reached to backoffLimitCount.
+If you don't specify any integer for backoffLimitCount, 
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-13T19:53:16Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:89** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-13T19:53:16Z
```diff
@@ -62,6 +66,28 @@ pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
 cancelled, the corresponding job is suspended and the Workload is requeued.
 
+### ReQueuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The requeuingStrategy (`waitForPodsReady.requeuingStrategy`) is optional parameters, 
+timestamp (`waitForPodsReady.requeuingStrategt.timestamp`) is defaulting to `Eviction`, and
+backoffLimitCount (`waitForPodsReady.requeuingStrategt.backoffLimitCount`) is defaulting to null.
+
+When you specify the timestamp, a Workload uses the below time to order the Workloads in the queue:
+
+- `Eviction`: The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` to head of the queue,
+you can set the `Creation` to the timestamp. 
+
+A Workload evicted by the `PodsReadyTimeout` reason is re-queued until the count is reached to backoffLimitCount.
+If you don't specify any integer for backoffLimitCount, 
+a Workload is repeatedly and endlessly re-queued to the queue based on timestamp.
+After the count is reached, a Workload is [deactivated](docs/concepts/workload/#active).
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-13T21:59:04Z

- **site/content/en/docs/concepts/workload.md:133** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T21:56:45Z
```diff
@@ -128,6 +128,29 @@ status:
 ```
 The `count` can only increase while the workload holds a Quota Reservation.
 
+## All or Nothing semantics for Job Resource Assignment
+
+This mechanism allows a Job to be admitted only if the entire requested resource is available. 
```

  ```suggestion
  This mechanism allows a Job to be evicted and requeued if the job doesn't become ready.
  ```

- **site/content/en/docs/tasks/setup_sequential_admission.md:89** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-13T21:58:32Z
```diff
@@ -43,24 +43,55 @@ fields:
     waitForPodsReady:
       enable: true
       timeout: 10m
+      blockAdmission: true
+      requeuingStrategy:
+        timestamp: Eviction | Creation
+        backoffLimitCount: 5
 ```
 
-> **Note**
-Note that, if you update an existing Kueue installation you may need to restart the
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
 `kueue-controller-manager` pod in order for Kueue to pick up the updated
 configuration. In that case run:
 
 ```shell
 kubectl delete pods --all -n kueue-system
 ```
+{{% /alert %}}
 
-The timeout (`waitForPodsReady.timeout`) is an optional parameter, defaulting to
+The `timeout` (`waitForPodsReady.timeout`) is an optional parameter, defaulting to
 5 minutes.
 
-When the timeout expires for an admitted Workload, and the workload's
+When the `timeout` expires for an admitted Workload, and the workload's
 pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
-cancelled, the corresponding job is suspended and the Workload is requeued.
+cancelled, the corresponding job is suspended and the Workload is re-queued.
+
+The `blockAdmission` (`waitForPodsReady.blockAdmission`) is an optional parameter, defaulting to `false`.
+When the `blockAdmission` is set to `true` regardless of the `enable` (`waitForPodsReady.enable`) is set to `false`,
+the `enable` is overridden to `true`.
+When the `blockAdmission` is set to `true`, admitted Workload with not ready pods block admission for other Workloads.
+
+### Requeuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The `requeuingStrategy` (`waitForPodsReady.requeuingStrategy`) contains optional parameters: 
+`timestamp` (`waitForPodsReady.requeuingStrategt.timestamp`) and `backoffLimitCount` (`waitForPodsReady.requeuingStrategt.backoffLimitCount`).
+
+The `timestamp` field defines which timestamp Kueue uses to order the Workloads in the queue:
+
+- `Eviction` (default): The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` back to its original place in the queue,
+you should set the `Creation` to the timestamp.
```

  ```suggestion
  you should set the timestamp to `Creation`.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-14T06:10:00Z

- **site/content/en/docs/concepts/workload.md:133** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-14T06:10:00Z
```diff
@@ -128,6 +128,29 @@ status:
 ```
 The `count` can only increase while the workload holds a Quota Reservation.
 
+## All or Nothing semantics for Job Resource Assignment
+
+This mechanism allows a Job to be admitted only if the entire requested resource is available. 
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-14T06:10:04Z

- **site/content/en/docs/tasks/setup_sequential_admission.md:89** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-14T06:10:04Z
```diff
@@ -43,24 +43,55 @@ fields:
     waitForPodsReady:
       enable: true
       timeout: 10m
+      blockAdmission: true
+      requeuingStrategy:
+        timestamp: Eviction | Creation
+        backoffLimitCount: 5
 ```
 
-> **Note**
-Note that, if you update an existing Kueue installation you may need to restart the
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
 `kueue-controller-manager` pod in order for Kueue to pick up the updated
 configuration. In that case run:
 
 ```shell
 kubectl delete pods --all -n kueue-system
 ```
+{{% /alert %}}
 
-The timeout (`waitForPodsReady.timeout`) is an optional parameter, defaulting to
+The `timeout` (`waitForPodsReady.timeout`) is an optional parameter, defaulting to
 5 minutes.
 
-When the timeout expires for an admitted Workload, and the workload's
+When the `timeout` expires for an admitted Workload, and the workload's
 pods are not all scheduled yet (i.e., the Workload condition remains
 `PodsReady=False`), then the Workload's admission is
-cancelled, the corresponding job is suspended and the Workload is requeued.
+cancelled, the corresponding job is suspended and the Workload is re-queued.
+
+The `blockAdmission` (`waitForPodsReady.blockAdmission`) is an optional parameter, defaulting to `false`.
+When the `blockAdmission` is set to `true` regardless of the `enable` (`waitForPodsReady.enable`) is set to `false`,
+the `enable` is overridden to `true`.
+When the `blockAdmission` is set to `true`, admitted Workload with not ready pods block admission for other Workloads.
+
+### Requeuing Strategy
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+{{% /alert %}}
+
+The `requeuingStrategy` (`waitForPodsReady.requeuingStrategy`) contains optional parameters: 
+`timestamp` (`waitForPodsReady.requeuingStrategt.timestamp`) and `backoffLimitCount` (`waitForPodsReady.requeuingStrategt.backoffLimitCount`).
+
+The `timestamp` field defines which timestamp Kueue uses to order the Workloads in the queue:
+
+- `Eviction` (default): The `lastTransitionTime` of the `Evicted=true` condition with `PodsReadyTimeout` reason in a Workload.
+- `Creation`: The creationTimestamp in a Workload.
+
+If you want to re-queue a Workload evicted by the `PodsReadyTimeout` back to its original place in the queue,
+you should set the `Creation` to the timestamp.
```

  Done.
