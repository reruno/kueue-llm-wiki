# PR #1732: [jobframework] Use the jobs uid in workload's name hash.

**Summary**: [jobframework] Use the jobs uid in workload's name hash.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1732

**Last updated**: 2024-03-12T07:28:10Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `job-uid-wl-hash` → `main`
- **Created**: 2024-02-14T07:43:13Z
- **Updated**: 2024-03-12T07:28:10Z
- **Merged**: 2024-02-23T15:14:02Z
- **Closed**: 2024-02-23T15:14:02Z
- **Labels**: `release-note`, `size/L`, `lgtm`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 26   **+201 / -187**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
[jobframework] Use the jobs uuid in workload's name hash. With this we can avoid name collisions for workloads create for jobs having the same name and type created at different pints in time.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1726

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
The hash suffix of the workload's name are now influenced by the job's object UID. Recreated jobs with the same name and kind will use different workload names.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-14T07:43:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 791e737d5d41cf30cc57bdf478d57ffef3641414 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65d6f74500977600080a03e9 |

### Comment by [@trasc](https://github.com/trasc) — 2024-02-14T07:43:30Z

/cc @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-14T19:00:38Z

I'm ok with this change, but before we merge it, I think we should make sure we are not depending on the predictability of the workload name.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-15T07:07:40Z

What happens if admins do cluster migration? 
We often migrate the existing cluster to a new cluster to update the k8s version with backup and restore etcd.

I guess that workloads are just recreated with a new name since Job UID is changed by migration, right?
So, there is no impact, right?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-15T07:41:57Z

I still have concerns about this approach, summarized here: https://github.com/kubernetes-sigs/kueue/issues/1726#issuecomment-1945514791.

EDIT: Having said that, I'm ok with this if we find it "least" evil, but I think it warrants a more descriptive release-note, describing the user-facing impact.

EDIT2: I checked that if I do in-place upgrade then the jobs are not actually stopped. In that case the concerns aren't that important, I'm ok with this change too.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-15T07:43:38Z

@alculquicondor 
Passed the workload creation the workload name is irrelevant for Kueue, job reconciliation is done based on ownership (with the exception of prebuilt workloads when there is no relationship between the wl and job name).

From a human user perspective the wl names will continue to look similar `<job-prefix>-<job-name>-<hash-segment>`,  where `hash-segment` is an opaque string.

@tenzen-y 
For etcd backup/restore I'd expect the workloads to be part of that process as well, if not, indeed the should just be recreated. (are the UIDs  changed in migration?)

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-15T08:20:52Z

> For etcd backup/restore I'd expect the workloads to be part of that process as well, if not, indeed the should just be recreated. (are the UIDs changed in migration?)

If we directly backup and restore etcd, UID wouldn't be changed, but if we use cluster backup tools like the [`velero`](https://velero.io/), UID will be changed. I'm ok with only recreating the workloads.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-15T14:29:00Z

My worry is this line:

https://github.com/kubernetes-sigs/kueue/blob/e10df68a066f23ee1f6787de6e5cd24be86772df/pkg/controller/jobs/job/job_webhook.go#L80

### Comment by [@trasc](https://github.com/trasc) — 2024-02-15T15:16:58Z

> GetWorkloadNameForOwnerRef

Yes , this could be problematic in case of  kueue upgrades, it could keep the child job Suspended if the workload for the parent was created before the upgrade.

A similar problem we have with prebuilt workloads, but only for jobsets at this point, and we do this https://github.com/kubernetes-sigs/kueue/blob/01bd53214c8bc92b9a1164b224104a2f73397b4a/pkg/controller/jobs/jobset/jobset_webhook.go#L60-L69

to overcome it.

Maybe we could droop the usage of the parentWlAnotation and cover the prebuilt scenario as well.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-15T16:24:50Z

> Maybe we could drop the usage of the parentWlAnotation and cover the prebuilt scenario as well.

Can you expand on this?

### Comment by [@trasc](https://github.com/trasc) — 2024-02-16T11:12:35Z

> > Maybe we could drop the usage of the parentWlAnotation and cover the prebuilt scenario as well.
> 
> Can you expand on this?

We can use an ownership based approach, the child job and the worker have the same parent . https://github.com/kubernetes-sigs/kueue/pull/1732/commits/4dc9ba07b3b255e0ea4c582cc5af36d2b08404d1

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-16T14:13:22Z

I like that. Let's start with a PR just for that.

### Comment by [@trasc](https://github.com/trasc) — 2024-02-16T15:20:36Z

> I like that. Let's start with a PR just for that.

Moved in #1747

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-20T11:10:28Z

Should we update the PR description to note  that it fixes https://github.com/kubernetes-sigs/kueue/issues/1726? Or we believe there is more to do to close it, like the GC? In that case we may just mention "Part of #1726".

### Comment by [@trasc](https://github.com/trasc) — 2024-02-20T12:56:18Z

> Should we update the PR description to note that it fixes #1726? Or we believe there is more to do to close it, like the GC? In that case we may just mention "Part of #1726".

Let's go with Fix, since the issue cannot be reproduce after adding this.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-22T07:56:54Z

/lgtm
IIUC the remaining issue is that when a running Job is deleted with `--cascade orphan`, then the workload remains admitted and continues to consume quota, which we want to tackle by GC (either removal or marking as finished). If this is correct, could you please open a follow up issue?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-22T07:57:00Z

LGTM label has been added.  <details>Git tree hash: fb96d8b5f91182d9714c4384522dfdba0f546a99</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-22T08:48:46Z

For the release note, maybe something more user-oriented:
"The workload name is no longer the same for recreated jobs (with the same namespace and name),
because  Job UID is used to determine the hash-like suffix in the workload name."

WDYT @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-22T09:41:48Z

> For the release note, maybe something more user-oriented: "The workload name is no longer the same for recreated jobs (with the same namespace and name), because Job UID is used to determine the hash-like suffix in the workload name."
> 
> WDYT @tenzen-y

@mimowo Generally, sgtm, but it is better to emphasize that the creating workloads names lose consistency or obtain completely random names. WDYT?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-22T09:53:10Z

> @mimowo Generally, sgtm, but it is better to emphasize that the creating workloads names lose consistency or obtain completely random names. WDYT?

Yeah, it is tricky, they are "completely random" from user perspective, but technically they are predictible, so I wanted to have something which captures the situation from user perspective (different names on recreation), yet don't say "random". However, I'm ok adding a sentence to clarify "The workload names are virtually random now." (or something like that).

### Comment by [@trasc](https://github.com/trasc) — 2024-02-22T10:36:19Z

> "The workload names are virtually random now."

Cannot be added since they are not random.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-22T10:51:53Z

> > "The workload names are virtually random now."
> 
> Cannot be added since they are not random.

Well, I think saying "virtually" random, or "random from user's perspective" is fine (after saying that it is a hash-like in the previous sentence), but I don't have a strong opinion. Still, I think we should make set the release note to something understandable to users. WDYT @tenzen-y ?

### Comment by [@trasc](https://github.com/trasc) — 2024-02-22T12:37:10Z

Release note updated

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-23T14:47:22Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-23T14:47:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1732#issuecomment-1961464355" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1732#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-23T15:01:27Z

> > > "The workload names are virtually random now."
> > 
> > 
> > Cannot be added since they are not random.
> 
> Well, I think saying "virtually" random, or "random from user's perspective" is fine (after saying that it is a hash-like in the previous sentence), but I don't have a strong opinion. Still, I think we should make set the release note to something understandable to users. WDYT @tenzen-y ?

Sorry for the late. It sounds good to me.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-14T07:58:27Z

Actually, I think it would be good to get to the bottom of [1726](https://github.com/kubernetes-sigs/kueue/issues/1726) before adding UID to the hash. At least I still don't fully understand it, so maybe just explaining would be enough if you already know. I'm worried that with this change with risk covering some issues, and complications on upgrade. 

Concrete questions:
1. in case there is a conflict in a scenario when a job is recreated with different pod spec, so that the workload is not longer equivalent, should we not trigger recreation the workload?
2. what is the impact during upgrade, what will happen to user workloads which are pending, and admitted ones?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-20T09:00:52Z

Code LGTM, just some nits. Also, a suggestion for potential follow up.

I'm on a fence regarding the release note saying "to avoid potential name collisions". They are rare at rate 1/16mln they are still sort of possible to observe. Maybe we can rephrase a little not to be definitive.

- **pkg/controller/jobs/pod/pod_controller_test.go:266** by [@mimowo](https://github.com/mimowo) — 2024-02-20T08:47:21Z
```diff
@@ -263,7 +263,7 @@ func TestReconciler(t *testing.T) {
 				Queue("test-queue").
 				Obj()},
 			wantWorkloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("pod-pod-a91e8", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				*utiltesting.MakeWorkload("pod-pod-43ebf", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
```

  nit: Can we call the function which determines the hash here?

- **pkg/controller/jobs/pod/pod_controller_test.go:3174** by [@mimowo](https://github.com/mimowo) — 2024-02-20T08:47:49Z
```diff
@@ -3171,8 +3171,8 @@ func TestIsPodOwnerManagedByQueue(t *testing.T) {
 }
 
 func TestGetWorkloadNameForPod(t *testing.T) {
-	wantWlName := "pod-unit-test-7bb47"
-	wlName := GetWorkloadNameForPod("unit-test")
+	wantWlName := "pod-unit-test-65369"
```

  nit: I think it would be better to call the function which determines the hash.

- **pkg/controller/jobframework/workload_names.go:35** by [@mimowo](https://github.com/mimowo) — 2024-02-20T08:51:57Z
```diff
@@ -31,21 +32,23 @@ const (
 	maxPrefixLength = 252 - hashLength
 )
 
-func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerGVK schema.GroupVersionKind) string {
+func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerGVK schema.GroupVersionKind) string {
```

  I'm ok with dropping the `ownerGVK`, but I would suggest a follow up (if we have a consensus on that) so that the PRs are easier to review.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-20T10:56:37Z

- **pkg/controller/jobframework/workload_names.go:35** by [@trasc](https://github.com/trasc) — 2024-02-20T10:41:12Z
```diff
@@ -31,21 +32,23 @@ const (
 	maxPrefixLength = 252 - hashLength
 )
 
-func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerGVK schema.GroupVersionKind) string {
+func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerGVK schema.GroupVersionKind) string {
```

  sgtm

- **pkg/controller/jobs/pod/pod_controller_test.go:266** by [@trasc](https://github.com/trasc) — 2024-02-20T10:45:23Z
```diff
@@ -263,7 +263,7 @@ func TestReconciler(t *testing.T) {
 				Queue("test-queue").
 				Obj()},
 			wantWorkloads: []kueue.Workload{
-				*utiltesting.MakeWorkload("pod-pod-a91e8", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+				*utiltesting.MakeWorkload("pod-pod-43ebf", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
```

  done

- **pkg/controller/jobs/pod/pod_controller_test.go:3174** by [@trasc](https://github.com/trasc) — 2024-02-20T10:54:00Z
```diff
@@ -3171,8 +3171,8 @@ func TestIsPodOwnerManagedByQueue(t *testing.T) {
 }
 
 func TestGetWorkloadNameForPod(t *testing.T) {
-	wantWlName := "pod-unit-test-7bb47"
-	wlName := GetWorkloadNameForPod("unit-test")
+	wantWlName := "pod-unit-test-65369"
```

  `getHash` is private, and calling `GetWorkloadNameForOwnerWithGVK` will make the test validate just that we are using the correct GVK and pass the parameters in the right order given the implementation:
  
  https://github.com/kubernetes-sigs/kueue/blob/a3acdd5f298502ce74f2fc8004476dc0bc16adb7/pkg/controller/jobs/mpijob/mpijob_controller.go#L221-L223

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-20T11:06:59Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3174** by [@mimowo](https://github.com/mimowo) — 2024-02-20T11:06:59Z
```diff
@@ -3171,8 +3171,8 @@ func TestIsPodOwnerManagedByQueue(t *testing.T) {
 }
 
 func TestGetWorkloadNameForPod(t *testing.T) {
-	wantWlName := "pod-unit-test-7bb47"
-	wlName := GetWorkloadNameForPod("unit-test")
+	wantWlName := "pod-unit-test-65369"
```

  I see, but this is actually all that `GetWorkloadNameForPod` does - the rest is happening underneath. So we may unit test it just by checking it passes the parameters properly IMO. I think if we want to check the hashing, then the unit test for `GetWorkloadNameForOwnerWithGVK` is more appropriate, so that the intention is clear.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-20T11:18:30Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3174** by [@trasc](https://github.com/trasc) — 2024-02-20T11:18:30Z
```diff
@@ -3171,8 +3171,8 @@ func TestIsPodOwnerManagedByQueue(t *testing.T) {
 }
 
 func TestGetWorkloadNameForPod(t *testing.T) {
-	wantWlName := "pod-unit-test-7bb47"
-	wlName := GetWorkloadNameForPod("unit-test")
+	wantWlName := "pod-unit-test-65369"
```

  I genuinely don't know that the intentions of this test are.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-20T11:48:19Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3174** by [@mimowo](https://github.com/mimowo) — 2024-02-20T11:48:19Z
```diff
@@ -3171,8 +3171,8 @@ func TestIsPodOwnerManagedByQueue(t *testing.T) {
 }
 
 func TestGetWorkloadNameForPod(t *testing.T) {
-	wantWlName := "pod-unit-test-7bb47"
-	wlName := GetWorkloadNameForPod("unit-test")
+	wantWlName := "pod-unit-test-65369"
```

  +1 I feel maintaining tests with concrete value of the hash is bad.
  
  For `GetWorkloadNameForPod` I would be fine to drop it entirely (or just test delegation as mentioned above).
  
  For `GetWorkloadNameForOwnerWithGVK` I guess we would like to demonstrate that the hash is stable given the job UID and name. However, this would be tested at integration test level, so also I don't see much of a need. Feel free not to add them.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-20T16:46:17Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3174** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-20T16:46:17Z
```diff
@@ -3171,8 +3171,8 @@ func TestIsPodOwnerManagedByQueue(t *testing.T) {
 }
 
 func TestGetWorkloadNameForPod(t *testing.T) {
-	wantWlName := "pod-unit-test-7bb47"
-	wlName := GetWorkloadNameForPod("unit-test")
+	wantWlName := "pod-unit-test-65369"
```

  The test should focus on the behavior we want to have:
  - The prefix matches the job kind and name
  - (new) two jobs with the same name but different UID get different workload names.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T07:27:27Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3174** by [@trasc](https://github.com/trasc) — 2024-02-22T07:27:27Z
```diff
@@ -3171,8 +3171,8 @@ func TestIsPodOwnerManagedByQueue(t *testing.T) {
 }
 
 func TestGetWorkloadNameForPod(t *testing.T) {
-	wantWlName := "pod-unit-test-7bb47"
-	wlName := GetWorkloadNameForPod("unit-test")
+	wantWlName := "pod-unit-test-65369"
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-22T18:06:41Z

- **pkg/controller/jobs/job/job_controller_test.go:2167** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T18:06:41Z
```diff
@@ -2164,7 +2164,7 @@ func TestReconciler(t *testing.T) {
 				Effect:   corev1.TaintEffectNoSchedule,
 			}).Obj(),
 			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name, baseJobWrapper.GetUID()), "ns").
```

  Can we have another unit test where we put a completely different name, but the reconciler still recognizes it as the workload for the job?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T18:29:38Z

- **pkg/controller/jobs/job/job_controller_test.go:2167** by [@trasc](https://github.com/trasc) — 2024-02-22T18:29:37Z
```diff
@@ -2164,7 +2164,7 @@ func TestReconciler(t *testing.T) {
 				Effect:   corev1.TaintEffectNoSchedule,
 			}).Obj(),
 			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name, baseJobWrapper.GetUID()), "ns").
```

  It will be a bit of a stretch and has nothing to do with the current PR.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-22T18:33:48Z

- **pkg/controller/jobs/job/job_controller_test.go:2167** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T18:33:48Z
```diff
@@ -2164,7 +2164,7 @@ func TestReconciler(t *testing.T) {
 				Effect:   corev1.TaintEffectNoSchedule,
 			}).Obj(),
 			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name, baseJobWrapper.GetUID()), "ns").
```

  It does have to do. It's one minimal safety guard to ensure that existing jobs (before upgrading to the latest version) continue to run.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T19:37:26Z

- **pkg/controller/jobs/job/job_controller_test.go:2167** by [@trasc](https://github.com/trasc) — 2024-02-22T19:37:26Z
```diff
@@ -2164,7 +2164,7 @@ func TestReconciler(t *testing.T) {
 				Effect:   corev1.TaintEffectNoSchedule,
 			}).Obj(),
 			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name, baseJobWrapper.GetUID()), "ns").
```

  Why not add this when owner based workload section was added?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-22T19:40:35Z

- **pkg/controller/jobs/job/job_controller_test.go:2167** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T19:40:35Z
```diff
@@ -2164,7 +2164,7 @@ func TestReconciler(t *testing.T) {
 				Effect:   corev1.TaintEffectNoSchedule,
 			}).Obj(),
 			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name, baseJobWrapper.GetUID()), "ns").
```

  because of bad foresight, but it wasn't that important then.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T19:48:34Z

- **pkg/controller/jobs/job/job_controller_test.go:2167** by [@trasc](https://github.com/trasc) — 2024-02-22T19:48:33Z
```diff
@@ -2164,7 +2164,7 @@ func TestReconciler(t *testing.T) {
 				Effect:   corev1.TaintEffectNoSchedule,
 			}).Obj(),
 			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name, baseJobWrapper.GetUID()), "ns").
```

  for the vast majority of tests the workload has name `a` not the name that is usually generated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-23T14:46:19Z

- **pkg/controller/jobs/job/job_controller_test.go:2167** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-23T14:46:19Z
```diff
@@ -2164,7 +2164,7 @@ func TestReconciler(t *testing.T) {
 				Effect:   corev1.TaintEffectNoSchedule,
 			}).Obj(),
 			workloads: []kueue.Workload{
-				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name), "ns").
+				*utiltesting.MakeWorkload(GetWorkloadNameForJob(baseJobWrapper.Name, baseJobWrapper.GetUID()), "ns").
```

  ok, thanks for checking. That should be enough.
