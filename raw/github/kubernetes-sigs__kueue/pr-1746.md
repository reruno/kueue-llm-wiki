# PR #1746: Don't deploy visibility by default

**Summary**: Don't deploy visibility by default

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1746

**Last updated**: 2024-03-12T08:53:50Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `visibility-no-default-deployment` → `main`
- **Created**: 2024-02-16T15:02:49Z
- **Updated**: 2024-03-12T08:53:50Z
- **Merged**: 2024-02-22T08:09:58Z
- **Closed**: 2024-02-22T08:09:58Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 10   **+41 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Don't deploy visibility by default to avoid 
```
E0216 16:57:43.448953  907490 memcache.go:287] couldn't get resource list for visibility.kueue.x-k8s.io/v1alpha1: the server is currently unable to handle the request
E0216 16:57:43.451994  907490 memcache.go:121] couldn't get resource list for visibility.kueue.x-k8s.io/v1alpha1: the server is currently unable to handle the request
```
in older k8s versions.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1519 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Kueue visibility API is no longer installed by default. Users can install it via helm or applying the visibility-api.yaml artifact.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-16T15:03:08Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e24adfeb6270198d625043c391546e9d0fa6ee98 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65d615bd2eddf400083b2563 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T13:35:14Z

I left lgtm on @mimowo 
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-21T13:35:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1746#issuecomment-1956666604" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1746#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T13:35:38Z

/cherry-pick release-0.6

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-02-21T13:35:41Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of release-0.6 in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1746#issuecomment-1956667360):

>/cherry-pick release-0.6
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@trasc](https://github.com/trasc) — 2024-02-21T15:29:33Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T16:10:06Z

> /test all

Prow EKS cluster seems to be down.
https://kubernetes.slack.com/archives/CCK68P2Q2/p1708527057849979

### Comment by [@trasc](https://github.com/trasc) — 2024-02-21T20:09:45Z

/test all

### Comment by [@mimowo](https://github.com/mimowo) — 2024-02-22T08:05:56Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-22T08:06:02Z

LGTM label has been added.  <details>Git tree hash: eb12c7c82f19b112c851e3763d4be211594c082c</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-02-22T08:10:37Z

@tenzen-y: new pull request created: #1764

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1746#issuecomment-1956667360):

>/cherry-pick release-0.6
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-07T17:57:23Z

/release-note-edit
```release-note
Kueue visibility API is no longer installed by default. Users can install it via helm or applying the visibility-api.yaml artifact.
```

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-16T15:42:20Z

- **charts/kueue/values.yaml:9** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-16T15:42:08Z
```diff
@@ -5,6 +5,8 @@ nameOverride: ""
 fullnameOverride: ""
 # Enable each function, like kustomize https://github.com/kubernetes-sigs/kueue/blob/main/config/default/kustomization.yaml
 enablePrometheus: false
+# Enable visibility, set the default to true when the feature is no longer aplha
+enableVisibility: false
```

  Instead of adding this field, should we extract enabled featuregate from https://github.com/kubernetes-sigs/kueue/blob/d7cb251b16fe4c23cd4f7dc6d38fb68b52663515/charts/kueue/values.yaml#L13-L16?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-16T16:37:18Z

- **charts/kueue/values.yaml:9** by [@mimowo](https://github.com/mimowo) — 2024-02-16T16:37:18Z
```diff
@@ -5,6 +5,8 @@ nameOverride: ""
 fullnameOverride: ""
 # Enable each function, like kustomize https://github.com/kubernetes-sigs/kueue/blob/main/config/default/kustomization.yaml
 enablePrometheus: false
+# Enable visibility, set the default to true when the feature is no longer aplha
+enableVisibility: false
```

  I would like that approach to have a single source of truth, but not sure if this is feasible / how to extract.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-16T16:39:20Z

- **charts/kueue/values.yaml:9** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-16T16:39:20Z
```diff
@@ -5,6 +5,8 @@ nameOverride: ""
 fullnameOverride: ""
 # Enable each function, like kustomize https://github.com/kubernetes-sigs/kueue/blob/main/config/default/kustomization.yaml
 enablePrometheus: false
+# Enable visibility, set the default to true when the feature is no longer aplha
+enableVisibility: false
```

  I guess that we can refer to https://github.com/kubernetes-sigs/kueue/blob/6d57813a52066dab412735deeeb60ebb0cdb8e8e/charts/kueue/templates/_helpers.tpl#L73-L75

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-19T09:46:55Z

- **charts/kueue/values.yaml:9** by [@trasc](https://github.com/trasc) — 2024-02-19T09:46:55Z
```diff
@@ -5,6 +5,8 @@ nameOverride: ""
 fullnameOverride: ""
 # Enable each function, like kustomize https://github.com/kubernetes-sigs/kueue/blob/main/config/default/kustomization.yaml
 enablePrometheus: false
+# Enable visibility, set the default to true when the feature is no longer aplha
+enableVisibility: false
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-20T10:51:08Z

- **charts/kueue/templates/_helpers.tpl:81** by [@mimowo](https://github.com/mimowo) — 2024-02-20T10:47:12Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "Visibility")}}
```

  I'm not very familiar with charts, but this looks very excessive to ask if a feature gate is enabled: `(dict "List" .Values.controllerManager.featureGates "Feature" `. Is it possible to introduce a helper function that takes just one argument - the feature gate name?

- **charts/kueue/templates/visibility/apiservice.yaml:1** by [@mimowo](https://github.com/mimowo) — 2024-02-20T10:50:50Z
```diff
@@ -1,3 +1,4 @@
+{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "Visibility")}}
```

  I'm confused here actually, we pass "Visibility", but we don't have such feature gate: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/features/kube_features.go#L29-L86. 
  
  We only have "QueueVisibility" and "VisibilityOnDemand". IIUC the visbility apiservice should be protected by the `VisibilityOnDemand` gate, and has nothing to do with `QueueVisibility`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-20T11:15:02Z

- **charts/kueue/templates/visibility/apiservice.yaml:1** by [@trasc](https://github.com/trasc) — 2024-02-20T11:14:04Z
```diff
@@ -1,3 +1,4 @@
+{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "Visibility")}}
```

  Good catch. done.

- **charts/kueue/templates/_helpers.tpl:81** by [@trasc](https://github.com/trasc) — 2024-02-20T11:14:06Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "Visibility")}}
```

  The templates always take a single parameter, in the example provided is the result of `(dict ...)`, sure the example can be replaced by something like : 
  
  ```
  {{ $params := (dict "List" .Values.controllerManager.featureGates "Feature"  "VisibilityOnDemand"  }}
  {{- if  include "kueue.isFeatureGateEnabled" $params }}
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-20T11:40:29Z

- **charts/kueue/templates/_helpers.tpl:81** by [@mimowo](https://github.com/mimowo) — 2024-02-20T11:40:29Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "Visibility")}}
```

  Could we have just 
  ```
  {{- if  include "kueue.isFeatureGateEnabled" "VisibilityOnDemand" }}
  ```
  And the `kueue.isFeatureGateEnable` would do the rest - look up the `.Values.controllerManager.featureGates`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-20T11:43:31Z

- **charts/kueue/templates/_helpers.tpl:81** by [@trasc](https://github.com/trasc) — 2024-02-20T11:43:31Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "Visibility")}}
```

  no, the template is stateless , and you need to search for `VisibilityOnDemand` in a list, so we need to parameters packed and sent to the template

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-20T11:58:52Z

- **charts/kueue/templates/_helpers.tpl:81** by [@mimowo](https://github.com/mimowo) — 2024-02-20T11:58:51Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "Visibility")}}
```

  got it, thanks for explaining

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-20T13:06:48Z

- **charts/kueue/templates/_helpers.tpl:81** by [@mimowo](https://github.com/mimowo) — 2024-02-20T13:06:47Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "Visibility")}}
```

  Actually, sorry for re-opening the thread, but I see the function above that iterates directly over `.Values.controllerManager.featureGates` and it is not passed as a param. Is there any reason we cannot do this here?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-20T13:42:23Z

- **charts/kueue/templates/_helpers.tpl:81** by [@trasc](https://github.com/trasc) — 2024-02-20T13:42:23Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "Visibility")}}
```

  The full helm context `.` is passed into that.
  
  https://github.com/kubernetes-sigs/kueue/blob/64e837fbdd0470675625741ef1ebfacabe74cb7a/charts/kueue/templates/manager/manager.yaml#L26
  
  For that use-case you are not interested in a specific gate , you just want them all concatenated, hence you don't need to create a new parameters map.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-21T13:18:21Z

LGTM, just one more nit
/assign @tenzen-y

- **charts/kueue/templates/_helpers.tpl:79** by [@mimowo](https://github.com/mimowo) — 2024-02-21T13:17:59Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
```

  ```suggestion
  IsFeatureGateEnabled - outputs true if the feature gate .Feature is enabled in the .List
  ```
  nit

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-21T13:34:45Z

Basically lgtm. I confirmed that this should work fine in the following:

```shell
# The default values.yaml
$ helm template ./charts/kueue --values ./charts/kueue/values.yaml | grep APIService
# No output
```

```shell
# The default values.yaml with the `VisibilityOnDemand`.
# ...
# controllerManager:
#   featureGates:
#     - name: VisibilityOnDemand
#       enabled: true
# ...
$ helm template ./charts/kueue --values ./charts/kueue/values.yaml | grep -A2 APIService
kind: APIService
metadata:
  name: v1alpha1.visibility.kueue.x-k8s.io
```

- **charts/kueue/templates/_helpers.tpl:81** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T13:32:36Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
```

  ```suggestion
    {{- if include "kueue.isFeatureGateEnabled" (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
  ```
  
  Can we use a single space?

- **charts/kueue/templates/_helpers.tpl:84** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T13:32:49Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
+*/}}
+{{- define "kueue.isFeatureGateEnabled" -}}
+{{- $feature  := .Feature }}
```

  ```suggestion
  {{- $feature := .Feature }}
  ```

- **charts/kueue/templates/_helpers.tpl:87** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T13:33:00Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
+*/}}
+{{- define "kueue.isFeatureGateEnabled" -}}
+{{- $feature  := .Feature }}
+{{- $enabled := false }}
+{{- range .List }}
+{{- if (and (eq .name $feature)  (eq .enabled true))}}
```

  ```suggestion
  {{- if (and (eq .name $feature) (eq .enabled true))}}
  ```

- **charts/kueue/templates/visibility/apiservice.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T13:33:31Z
```diff
@@ -1,3 +1,4 @@
+{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
```

  ```suggestion
  {{- if include "kueue.isFeatureGateEnabled" (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
  ```

- **charts/kueue/templates/visibility/role_binding.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T13:33:39Z
```diff
@@ -1,3 +1,4 @@
+{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
```

  ```suggestion
  {{- if include "kueue.isFeatureGateEnabled" (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
  ```

- **charts/kueue/templates/visibility/service.yaml:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T13:33:53Z
```diff
@@ -1,3 +1,4 @@
+{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
```

  ```suggestion
  {{- if include "kueue.isFeatureGateEnabled" (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
  ```

- **hack/update-helm.sh:274** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-21T13:34:07Z
```diff
@@ -269,4 +269,11 @@ for output_file in ${DEST_VISIBILITY_DIR}/*.yaml; do
   if [ "$(cat $output_file | $YQ '.subjects.[] | has("namespace")')" = "true" ]; then
     $YQ -N -i '.subjects.[].namespace = "{{ .Release.Namespace }}"' $output_file
   fi
+
+  {
+  echo '{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}'
```

  ```suggestion
    echo '{{- if include "kueue.isFeatureGateEnabled" (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}'
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-21T15:25:39Z

- **charts/kueue/templates/_helpers.tpl:79** by [@trasc](https://github.com/trasc) — 2024-02-21T15:15:25Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
```

  done

- **charts/kueue/templates/_helpers.tpl:81** by [@trasc](https://github.com/trasc) — 2024-02-21T15:17:20Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
```

  done

- **charts/kueue/templates/_helpers.tpl:84** by [@trasc](https://github.com/trasc) — 2024-02-21T15:18:28Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
+*/}}
+{{- define "kueue.isFeatureGateEnabled" -}}
+{{- $feature  := .Feature }}
```

  done

- **charts/kueue/templates/_helpers.tpl:87** by [@trasc](https://github.com/trasc) — 2024-02-21T15:18:35Z
```diff
@@ -74,3 +74,21 @@ FeatureGates
 - --feature-gates={{ $features | trimSuffix "," }}
 {{- end }}
 {{- end }}
+
+{{/*
+IsFeatureGateEanabled - outputs true if the feature gate .Feature is enabled in the .List
+Usage:
+  {{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
+*/}}
+{{- define "kueue.isFeatureGateEnabled" -}}
+{{- $feature  := .Feature }}
+{{- $enabled := false }}
+{{- range .List }}
+{{- if (and (eq .name $feature)  (eq .enabled true))}}
```

  done

- **charts/kueue/templates/visibility/apiservice.yaml:1** by [@trasc](https://github.com/trasc) — 2024-02-21T15:18:44Z
```diff
@@ -1,3 +1,4 @@
+{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
```

  done

- **charts/kueue/templates/visibility/role_binding.yaml:1** by [@trasc](https://github.com/trasc) — 2024-02-21T15:18:50Z
```diff
@@ -1,3 +1,4 @@
+{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
```

  done

- **charts/kueue/templates/visibility/service.yaml:1** by [@trasc](https://github.com/trasc) — 2024-02-21T15:19:00Z
```diff
@@ -1,3 +1,4 @@
+{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}
```

  done

- **hack/update-helm.sh:274** by [@trasc](https://github.com/trasc) — 2024-02-21T15:19:12Z
```diff
@@ -269,4 +269,11 @@ for output_file in ${DEST_VISIBILITY_DIR}/*.yaml; do
   if [ "$(cat $output_file | $YQ '.subjects.[] | has("namespace")')" = "true" ]; then
     $YQ -N -i '.subjects.[].namespace = "{{ .Release.Namespace }}"' $output_file
   fi
+
+  {
+  echo '{{- if include "kueue.isFeatureGateEnabled"  (dict "List" .Values.controllerManager.featureGates "Feature" "VisibilityOnDemand")}}'
```

  done
