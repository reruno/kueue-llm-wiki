# PR #1747: Drop `kueue.x-k8s.io/parent-workload` in favor of a object ownership based approach.

**Summary**: Drop `kueue.x-k8s.io/parent-workload` in favor of a object ownership based approach.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1747

**Last updated**: 2024-02-20T07:03:21Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `no-parentwl-annotation` → `main`
- **Created**: 2024-02-16T15:18:22Z
- **Updated**: 2024-02-20T07:03:21Z
- **Merged**: 2024-02-19T15:17:45Z
- **Closed**: 2024-02-19T15:17:45Z
- **Labels**: `release-note`, `lgtm`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 24   **+133 / -408**

## Description


<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Drop the usage of `kueue.x-k8s.io/parent-workload` annotation  in favor of an object ownership based approach.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Dropped the usage of `kueue.x-k8s.io/parent-workload` annotation  in favor of an object ownership based approach.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-16T15:18:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2cdaf226a47c8b900540ce2fdad8f016980dcf6a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65d337e5d0efff00083a8a04 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-16T15:29:14Z

/cc

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-16T15:43:19Z

@trasc Could you share some context for this?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-16T15:43:54Z

I was not sure why this improvement is needed.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-16T16:24:20Z

> @trasc Could you share some context for this?

Oh, I found a context myself here: https://github.com/kubernetes-sigs/kueue/pull/1732#issuecomment-1948199678

### Comment by [@trasc](https://github.com/trasc) — 2024-02-19T11:16:29Z

> Generally, lgtm, but I think we need to add the proper release note since any system might depend on this annotation (although my company doesn't depend on this annotation 🙃 ).

Updated

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-19T13:00:36Z

LGTM label has been added.  <details>Git tree hash: bb765ea025023830ea5155bd0ed026dae0fd0678</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-19T14:51:48Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1747#pullrequestreview-1888601731" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1747#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-16T16:49:40Z

This is much less code, so I'm all in for it :)

- **pkg/controller/core/indexer/indexer.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-16T15:36:48Z
```diff
@@ -110,6 +112,11 @@ func IndexWorkloadRuntimeClass(obj client.Object) []string {
 	return nil
 }
 
+// TODO: Is there a "standard" implementation of something like this??
```

  Remove TODO if it's not actionable.

- **pkg/controller/jobframework/reconciler.go:231** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-16T15:39:56Z
```diff
@@ -217,15 +217,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
-	isStandaloneJob := ParentWorkloadName(job) == ""
+	isStandaloneJob := true
+	objectOwner := metav1.GetControllerOf(object)
+	if objectOwner != nil && IsOwnerManagedByKueue(objectOwner) {
+		isStandaloneJob = false
+	}
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
 	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
 		if isStandaloneJob {
-			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job",
-				"queueName", QueueName(job), "parentWorkload", ParentWorkloadName(job))
+			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job", "queueName", QueueName(job))
```

  message is outdated

- **pkg/controller/jobframework/reconciler.go:459** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-16T15:51:06Z
```diff
@@ -456,7 +459,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 func (r *JobReconciler) IsParentJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
```

  It just occurred to me that there might be a bug when both Pod and RayJob support are enabled.
  
  If you create a RayJob, those Pods might be understood as not having a parent workload and be terminated?
  
  But that's beyond the scope of this issue

- **test/integration/controller/jobs/mpijob/mpijob_controller_test.go:550** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-16T16:49:24Z
```diff
@@ -547,7 +547,7 @@ var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
 
-	ginkgo.It("Should suspend a job if the parent workload does not exist", func() {
+	ginkgo.It("Should suspend a job if the parent's workload does not exist or is not admitted", func() {
```

  Can you add a similar test to this with a RayCluster owned by a RayJob?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-16T16:54:07Z

- **pkg/controller/core/indexer/indexer.go:115** by [@mimowo](https://github.com/mimowo) — 2024-02-16T16:44:50Z
```diff
@@ -110,6 +112,11 @@ func IndexWorkloadRuntimeClass(obj client.Object) []string {
 	return nil
 }
 
+// TODO: Is there a "standard" implementation of something like this??
```

  I'm not aware of such function, not need to keep the TODO: https://github.com/kubernetes/community/blob/master/sig-scheduling/CONTRIBUTING.md#best-practices.

- **pkg/controller/jobframework/reconciler.go:231** by [@mimowo](https://github.com/mimowo) — 2024-02-16T16:48:17Z
```diff
@@ -217,15 +217,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
-	isStandaloneJob := ParentWorkloadName(job) == ""
+	isStandaloneJob := true
+	objectOwner := metav1.GetControllerOf(object)
+	if objectOwner != nil && IsOwnerManagedByKueue(objectOwner) {
+		isStandaloneJob = false
+	}
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
 	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
 		if isStandaloneJob {
-			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job",
-				"queueName", QueueName(job), "parentWorkload", ParentWorkloadName(job))
+			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job", "queueName", QueueName(job))
```

  rephrase as there is no longer parent-workload annotation

- **pkg/controller/jobframework/reconciler.go:485** by [@mimowo](https://github.com/mimowo) — 2024-02-16T16:53:09Z
```diff
@@ -469,16 +472,18 @@ func (r *JobReconciler) IsParentJobManaged(ctx context.Context, jobObj client.Ob
 }
 
 func (r *JobReconciler) getParentWorkload(ctx context.Context, job GenericJob, object client.Object) (*kueue.Workload, error) {
-	pw := kueue.Workload{}
-	namespacedName := types.NamespacedName{
-		Name:      ParentWorkloadName(job),
-		Namespace: object.GetNamespace(),
+	owner := metav1.GetControllerOf(object)
+	if owner == nil {
+		return nil, nil
 	}
-	if err := r.client.Get(ctx, namespacedName, &pw); err != nil {
+	wlList := kueue.WorkloadList{}
+	if err := r.client.List(ctx, &wlList, client.InNamespace(object.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(owner.UID)}); err != nil || len(wlList.Items) == 0 {
 		return nil, client.IgnoreNotFound(err)
-	} else {
-		return &pw, nil
 	}
+
+	// In theory the parent can own multiple Workloads, we cannot do too much about it, maybe log it.
+	return &wlList.Items[0], nil
```

  What if it does not own any workloads, we could fail then. I see this should not happen normally, but may happen is someone removes the workload from parent job manually, probably we don't want risk panicing here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-19T08:14:09Z

- **pkg/controller/jobframework/reconciler.go:485** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-19T08:14:09Z
```diff
@@ -469,16 +472,18 @@ func (r *JobReconciler) IsParentJobManaged(ctx context.Context, jobObj client.Ob
 }
 
 func (r *JobReconciler) getParentWorkload(ctx context.Context, job GenericJob, object client.Object) (*kueue.Workload, error) {
-	pw := kueue.Workload{}
-	namespacedName := types.NamespacedName{
-		Name:      ParentWorkloadName(job),
-		Namespace: object.GetNamespace(),
+	owner := metav1.GetControllerOf(object)
+	if owner == nil {
+		return nil, nil
 	}
-	if err := r.client.Get(ctx, namespacedName, &pw); err != nil {
+	wlList := kueue.WorkloadList{}
+	if err := r.client.List(ctx, &wlList, client.InNamespace(object.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(owner.UID)}); err != nil || len(wlList.Items) == 0 {
 		return nil, client.IgnoreNotFound(err)
-	} else {
-		return &pw, nil
 	}
+
+	// In theory the parent can own multiple Workloads, we cannot do too much about it, maybe log it.
+	return &wlList.Items[0], nil
```

  I guess that there isn't such a failure here. Because if the length of the `wlList` is zero, we return nil in Line 481.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-19T08:26:05Z

Generally, lgtm, but I think we need to add the proper release note since any system might depend on this annotation (although my company doesn't depend on this annotation 🙃 ).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-19T08:37:08Z

- **pkg/controller/jobframework/reconciler.go:485** by [@mimowo](https://github.com/mimowo) — 2024-02-19T08:37:07Z
```diff
@@ -469,16 +472,18 @@ func (r *JobReconciler) IsParentJobManaged(ctx context.Context, jobObj client.Ob
 }
 
 func (r *JobReconciler) getParentWorkload(ctx context.Context, job GenericJob, object client.Object) (*kueue.Workload, error) {
-	pw := kueue.Workload{}
-	namespacedName := types.NamespacedName{
-		Name:      ParentWorkloadName(job),
-		Namespace: object.GetNamespace(),
+	owner := metav1.GetControllerOf(object)
+	if owner == nil {
+		return nil, nil
 	}
-	if err := r.client.Get(ctx, namespacedName, &pw); err != nil {
+	wlList := kueue.WorkloadList{}
+	if err := r.client.List(ctx, &wlList, client.InNamespace(object.GetNamespace()), client.MatchingFields{indexer.OwnerReferenceUID: string(owner.UID)}); err != nil || len(wlList.Items) == 0 {
 		return nil, client.IgnoreNotFound(err)
-	} else {
-		return &pw, nil
 	}
+
+	// In theory the parent can own multiple Workloads, we cannot do too much about it, maybe log it.
+	return &wlList.Items[0], nil
```

  Right, I missed that, thanks

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-19T11:13:56Z

- **pkg/controller/core/indexer/indexer.go:115** by [@trasc](https://github.com/trasc) — 2024-02-19T10:26:10Z
```diff
@@ -110,6 +112,11 @@ func IndexWorkloadRuntimeClass(obj client.Object) []string {
 	return nil
 }
 
+// TODO: Is there a "standard" implementation of something like this??
```

  done

- **pkg/controller/core/indexer/indexer.go:115** by [@trasc](https://github.com/trasc) — 2024-02-19T10:27:01Z
```diff
@@ -110,6 +112,11 @@ func IndexWorkloadRuntimeClass(obj client.Object) []string {
 	return nil
 }
 
+// TODO: Is there a "standard" implementation of something like this??
```

  done

- **pkg/controller/jobframework/reconciler.go:231** by [@trasc](https://github.com/trasc) — 2024-02-19T10:29:08Z
```diff
@@ -217,15 +217,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
-	isStandaloneJob := ParentWorkloadName(job) == ""
+	isStandaloneJob := true
+	objectOwner := metav1.GetControllerOf(object)
+	if objectOwner != nil && IsOwnerManagedByKueue(objectOwner) {
+		isStandaloneJob = false
+	}
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
 	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
 		if isStandaloneJob {
-			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job",
-				"queueName", QueueName(job), "parentWorkload", ParentWorkloadName(job))
+			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job", "queueName", QueueName(job))
```

  done

- **pkg/controller/jobframework/reconciler.go:231** by [@trasc](https://github.com/trasc) — 2024-02-19T10:29:14Z
```diff
@@ -217,15 +217,18 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
-	isStandaloneJob := ParentWorkloadName(job) == ""
+	isStandaloneJob := true
+	objectOwner := metav1.GetControllerOf(object)
+	if objectOwner != nil && IsOwnerManagedByKueue(objectOwner) {
+		isStandaloneJob = false
+	}
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	// If the parent-workload annotation is set, it also checks whether the parent job has queue-name label.
 	if !r.manageJobsWithoutQueueName && QueueName(job) == "" {
 		if isStandaloneJob {
-			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job",
-				"queueName", QueueName(job), "parentWorkload", ParentWorkloadName(job))
+			log.V(3).Info("Neither queue-name label, nor parent-workload annotation is set, ignoring the job", "queueName", QueueName(job))
```

  done

- **test/integration/controller/jobs/mpijob/mpijob_controller_test.go:550** by [@trasc](https://github.com/trasc) — 2024-02-19T11:04:22Z
```diff
@@ -547,7 +547,7 @@ var _ = ginkgo.Describe("Job controller for workloads when only jobs with queue
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
 
-	ginkgo.It("Should suspend a job if the parent workload does not exist", func() {
+	ginkgo.It("Should suspend a job if the parent's workload does not exist or is not admitted", func() {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-19T13:00:30Z

/lgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-19T14:51:32Z

- **pkg/controller/jobframework/reconciler.go:459** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-19T14:51:32Z
```diff
@@ -456,7 +459,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 func (r *JobReconciler) IsParentJobManaged(ctx context.Context, jobObj client.Object, namespace string) (bool, error) {
```

  Can you follow up on this?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-19T14:51:41Z

/approve
