# PR #175: Add details for managing cohorts

**Summary**: Add details for managing cohorts

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/175

**Last updated**: 2022-04-07T15:17:58Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `doc-cohort` → `main`
- **Created**: 2022-04-05T17:10:36Z
- **Updated**: 2022-04-07T15:17:58Z
- **Merged**: 2022-04-07T15:17:58Z
- **Closed**: 2022-04-07T15:17:58Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 2   **+258 / -4**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #64 

#### Special notes for your reviewer:

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T17:19:15Z

cc @cortespao @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-05T22:06:39Z

Can you please add docs related to QueueingStrategy as well to ClusterQueue concepts page?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-07T00:07:55Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/175#" title="Author self-approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/175#pullrequestreview-934298915" title="Approved">cortespao</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:04:35Z

/hold
for squash

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T14:21:09Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:55:36Z

/hold cancel

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T15:15:55Z

/lgtm

## Reviews

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-04-05T19:18:20Z

- **docs/concepts/cluster_queue.md:154** by [@cortespao](https://github.com/cortespao) — 2022-04-05T18:39:42Z
```diff
@@ -141,9 +143,84 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+### Borrowing semantics
```

  Borrowing semantics example

- **docs/concepts/cluster_queue.md:156** by [@cortespao](https://github.com/cortespao) — 2022-04-05T18:40:28Z
```diff
@@ -141,9 +143,84 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+### Borrowing semantics
+
+Assume the following two ClusterQueues:
```

  Assume you created the following two ClusterQueues:

- **docs/concepts/cluster_queue.md:223** by [@cortespao](https://github.com/cortespao) — 2022-04-05T18:48:33Z
```diff
@@ -141,9 +143,84 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+### Borrowing semantics
+
+Assume the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Assume the ClusterQueue `team-b-cq` has no admitted workloads. Then the
+ClusterQueue `team-a-cq` can admit workloads with resources adding up to
+`12+9=21` CPUs and `48+36=84Gi` of memory.
+
+However, once there are pending workloads in `team-b-cq` Kueue will admit
+these workloads before admitting any new workloads in `team-a-cq`, in order to
+satisfy the `guaranteed` quota for `team-b-cq`.
+
```

  Proposal for 200 - 206:
  The ClusterQueue can admit workloads depending the following scenarios:
  * If ClusterQueue `team-b-cq` has no admitted workload, then the
  ClusterQueue `team-a-cq` can admit workloads with resources adding up to
  `12+9=21` CPUs and `48+36=84Gi` of memory.
  *  If ClusterQueue `team-b-cq` has pending workloads, then Kueue will admit
  these workloads before admitting any new workloads in `team-a-cq`. Therefore, Kueue ensure the `guaranteed` quota for `team-b-cq` is met.

- **docs/concepts/cluster_queue.md:214** by [@cortespao](https://github.com/cortespao) — 2022-04-05T18:50:43Z
```diff
@@ -141,9 +143,84 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+### Borrowing semantics
+
+Assume the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Assume the ClusterQueue `team-b-cq` has no admitted workloads. Then the
+ClusterQueue `team-a-cq` can admit workloads with resources adding up to
+`12+9=21` CPUs and `48+36=84Gi` of memory.
+
+However, once there are pending workloads in `team-b-cq` Kueue will admit
+these workloads before admitting any new workloads in `team-a-cq`, in order to
+satisfy the `guaranteed` quota for `team-b-cq`.
+
+**Note**: Kueue [does not support preemption](https://github.com/kubernetes-sigs/kueue/issues/83).
+No admitted workloads will be stopped to make space for new workloads.
+
+Borrowing happens per-flavor. If the ClusterQueues belonging to a cohort
+reference different flavors in `.spec.requestableResources[*].flavors[*].resourceFlavors`,
+they won't be able to share quota among them.
+
```

  I would move this paragraph (211-213) up after line 152

- **docs/tasks/administer_cluster_quotas.md:219** by [@cortespao](https://github.com/cortespao) — 2022-04-05T19:10:03Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
```

  Proposal [218 - 222]:
  
  With the following example, you can establish  and set a common name in the `.spec.cohort` field in all the
  ClusterQueues you want to include in the cohort.

- **docs/tasks/administer_cluster_quotas.md:273** by [@cortespao](https://github.com/cortespao) — 2022-04-05T19:11:06Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
+
+For two ClusterQueues, the manifests look like the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+You can create the ClusterQueues with the following command:
+
```

  To create these ClusterQueues, save the preceding manifests and run the following command:

- **docs/tasks/administer_cluster_quotas.md:282** by [@cortespao](https://github.com/cortespao) — 2022-04-05T19:15:11Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
+
+For two ClusterQueues, the manifests look like the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+You can create the ClusterQueues with the following command:
+
+```shell
+kubectl apply -f team-a-cq.yaml -f team-b-cq.yaml
+```
+
+## Multiple ClusterQueue with dedicated and fallback flavors
+
+A ClusterQueue can borrow resources from the cohort even if it has zero
+guaranteed quota for a flavor. This allows you to give dedicated quota for
```

  > A ClusterQueue can borrow resources from the cohort even if it has zero
  
  In this sentence, what do you mean by "it"? I would suggest you repeat the noun to avoid confusions:
  A ClusterQueue can borrow resources from the cohort even if the ClusterQueue  has zero guaranteed quota for a flavor (...)

- **docs/tasks/administer_cluster_quotas.md:287** by [@cortespao](https://github.com/cortespao) — 2022-04-05T19:15:48Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
+
+For two ClusterQueues, the manifests look like the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+You can create the ClusterQueues with the following command:
+
+```shell
+kubectl apply -f team-a-cq.yaml -f team-b-cq.yaml
+```
+
+## Multiple ClusterQueue with dedicated and fallback flavors
+
+A ClusterQueue can borrow resources from the cohort even if it has zero
+guaranteed quota for a flavor. This allows you to give dedicated quota for
+a flavor and fallback to quota for a different flavor, shared with other
+tenants.
+
+Such setup can be accomplished with a ClusterQueue for each tenant and an extra
+ClusterQueue for the shared resources. The manifests for two tenants look like
```

  ClusterQueue for the shared resources. **For example,** the manifests for two tenants look like

- **docs/tasks/administer_cluster_quotas.md:370** by [@cortespao](https://github.com/cortespao) — 2022-04-05T19:17:42Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
+
+For two ClusterQueues, the manifests look like the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+You can create the ClusterQueues with the following command:
+
+```shell
+kubectl apply -f team-a-cq.yaml -f team-b-cq.yaml
+```
+
+## Multiple ClusterQueue with dedicated and fallback flavors
+
+A ClusterQueue can borrow resources from the cohort even if it has zero
+guaranteed quota for a flavor. This allows you to give dedicated quota for
+a flavor and fallback to quota for a different flavor, shared with other
+tenants.
+
+Such setup can be accomplished with a ClusterQueue for each tenant and an extra
+ClusterQueue for the shared resources. The manifests for two tenants look like
+the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 9
+        ceiling: 9
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 0
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 12
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 0
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+```yaml
+# shared-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: shared-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 6
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 24Gi
+```
+
+Note that `team-a-cq` and `team-b-cq` define a `ceiling` equal to their
+`guaranteed` quota for the `arm` flavor, meaning that they can't borrow this
+flavor from each other. On the other hand, they define a `guaranteed: 0` for
+the `x86` flavor, meaning that they don't have any dedicated quota for the
+flavor and they can only borrow it from `shared-cq`.
+
```

  Note the following setup:
  
  * `team-a-cq` and `team-b-cq` define a `ceiling` equal to their
  `guaranteed` quota for the `arm` flavor. Therefore, they can't borrow this
  flavor from each other.
  * `team-a-cq` and `team-b-cq` define a `guaranteed: 0` for
  the `x86` flavor. Therefore, they don't have any dedicated quota for the
  flavor and they can only borrow it from `shared-cq`.

- **docs/tasks/administer_cluster_quotas.md:369** by [@cortespao](https://github.com/cortespao) — 2022-04-05T19:18:12Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
+
+For two ClusterQueues, the manifests look like the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+You can create the ClusterQueues with the following command:
+
+```shell
+kubectl apply -f team-a-cq.yaml -f team-b-cq.yaml
+```
+
+## Multiple ClusterQueue with dedicated and fallback flavors
+
+A ClusterQueue can borrow resources from the cohort even if it has zero
+guaranteed quota for a flavor. This allows you to give dedicated quota for
+a flavor and fallback to quota for a different flavor, shared with other
+tenants.
+
+Such setup can be accomplished with a ClusterQueue for each tenant and an extra
+ClusterQueue for the shared resources. The manifests for two tenants look like
+the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 9
+        ceiling: 9
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 0
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 12
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 0
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+```yaml
+# shared-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: shared-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 6
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 24Gi
+```
+
+Note that `team-a-cq` and `team-b-cq` define a `ceiling` equal to their
+`guaranteed` quota for the `arm` flavor, meaning that they can't borrow this
+flavor from each other. On the other hand, they define a `guaranteed: 0` for
+the `x86` flavor, meaning that they don't have any dedicated quota for the
+flavor and they can only borrow it from `shared-cq`.
+
+You can create the ClusterQueues with the following command:
```

  To create these ClusterQueues, save the preceding manifests and run the following command:

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-05T19:51:48Z

- **docs/concepts/cluster_queue.md:154** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T19:28:19Z
```diff
@@ -141,9 +143,84 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+### Borrowing semantics
```

  Done

- **docs/concepts/cluster_queue.md:156** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T19:28:22Z
```diff
@@ -141,9 +143,84 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+### Borrowing semantics
+
+Assume the following two ClusterQueues:
```

  Done

- **docs/concepts/cluster_queue.md:223** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T19:35:11Z
```diff
@@ -141,9 +143,84 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+### Borrowing semantics
+
+Assume the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Assume the ClusterQueue `team-b-cq` has no admitted workloads. Then the
+ClusterQueue `team-a-cq` can admit workloads with resources adding up to
+`12+9=21` CPUs and `48+36=84Gi` of memory.
+
+However, once there are pending workloads in `team-b-cq` Kueue will admit
+these workloads before admitting any new workloads in `team-a-cq`, in order to
+satisfy the `guaranteed` quota for `team-b-cq`.
+
```

  Done, with a few tweaks

- **docs/concepts/cluster_queue.md:214** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T19:35:17Z
```diff
@@ -141,9 +143,84 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+### Borrowing semantics
+
+Assume the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Assume the ClusterQueue `team-b-cq` has no admitted workloads. Then the
+ClusterQueue `team-a-cq` can admit workloads with resources adding up to
+`12+9=21` CPUs and `48+36=84Gi` of memory.
+
+However, once there are pending workloads in `team-b-cq` Kueue will admit
+these workloads before admitting any new workloads in `team-a-cq`, in order to
+satisfy the `guaranteed` quota for `team-b-cq`.
+
+**Note**: Kueue [does not support preemption](https://github.com/kubernetes-sigs/kueue/issues/83).
+No admitted workloads will be stopped to make space for new workloads.
+
+Borrowing happens per-flavor. If the ClusterQueues belonging to a cohort
+reference different flavors in `.spec.requestableResources[*].flavors[*].resourceFlavors`,
+they won't be able to share quota among them.
+
```

  Done

- **docs/tasks/administer_cluster_quotas.md:219** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T19:39:59Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
```

  Applied with a tweak

- **docs/tasks/administer_cluster_quotas.md:273** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T19:43:25Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
+
+For two ClusterQueues, the manifests look like the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+You can create the ClusterQueues with the following command:
+
```

  Done

- **docs/tasks/administer_cluster_quotas.md:282** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T19:44:21Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
+
+For two ClusterQueues, the manifests look like the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+You can create the ClusterQueues with the following command:
+
+```shell
+kubectl apply -f team-a-cq.yaml -f team-b-cq.yaml
+```
+
+## Multiple ClusterQueue with dedicated and fallback flavors
+
+A ClusterQueue can borrow resources from the cohort even if it has zero
+guaranteed quota for a flavor. This allows you to give dedicated quota for
```

  I was referring to the ClusterQueue indeed. Changed.

- **docs/tasks/administer_cluster_quotas.md:287** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T19:46:00Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
+
+For two ClusterQueues, the manifests look like the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+You can create the ClusterQueues with the following command:
+
+```shell
+kubectl apply -f team-a-cq.yaml -f team-b-cq.yaml
+```
+
+## Multiple ClusterQueue with dedicated and fallback flavors
+
+A ClusterQueue can borrow resources from the cohort even if it has zero
+guaranteed quota for a flavor. This allows you to give dedicated quota for
+a flavor and fallback to quota for a different flavor, shared with other
+tenants.
+
+Such setup can be accomplished with a ClusterQueue for each tenant and an extra
+ClusterQueue for the shared resources. The manifests for two tenants look like
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-06T19:15:08Z

- **docs/concepts/cluster_queue.md:149** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T18:42:38Z
```diff
@@ -141,9 +143,87 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
```

  ```suggestion
  `.spec.cohort` field. All ClusterQueues that have a matching `spec.cohort`
  ```

- **docs/concepts/cluster_queue.md:222** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T19:02:27Z
```diff
@@ -141,9 +143,87 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+Borrowing happens per-flavor. If the ClusterQueues belonging to a cohort
+reference different flavors in `.spec.requestableResources[*].flavors[*].resourceFlavors`,
+they won't be able to share quota among them.
+
+### Borrowing semantics example
+
+Assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+ClusterQueue `team-a-cq` can admit workloads depending on the following
+scenarios:
+
+- If ClusterQueue `team-b-cq` has no admitted workloads, then ClusterQueue
+  `team-a-cq` can admit workloads with resources adding up to `12+9=21` CPUs and
+  `48+36=84Gi` of memory.
+- If ClusterQueue `team-b-cq` has pending workloads and the ClusterQueue
+  `team-a-cq` has all its `guaranteed` quota used, Kueue will admit workloads in
+  ClusterQueue `team-b-cq` before admitting any new workloads in `team-a-cq`.
+  Therefore, Kueue ensures the `guaranteed` quota for `team-b-cq` is met.
```

  should we stress that there is no preemption?

- **docs/tasks/administer_cluster_quotas.md:278** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T19:15:05Z
```diff
@@ -210,6 +210,171 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+Using the following example, you can establish a cohort `team-ab` that includes
+ClusterQueues `team-a-cq` and `team-b-cq`.
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+To create these ClusterQueues, save the preceding manifests and run the
+following command:
+
+```shell
+kubectl apply -f team-a-cq.yaml -f team-b-cq.yaml
+```
+
+## Multiple ClusterQueue with dedicated and fallback flavors
```

  One thing worth mentioning is that in the case of multiple flavors, borrowing will happen first before falling down to the next flavor even if quota was available for the next flavor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-06T20:32:28Z

- **docs/concepts/cluster_queue.md:149** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T20:13:17Z
```diff
@@ -141,9 +143,87 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
```

  Done

- **docs/concepts/cluster_queue.md:222** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T20:13:31Z
```diff
@@ -141,9 +143,87 @@ metadata:
 ## Cohort
 
 ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
-same cohort can borrow unused quota from each other, if they reference
-matching resource flavors in
-`.spec.requestableResources[*].flavors[*].resourceFlavor`.
+same cohort can borrow unused quota from each other.
+
+To add a ClusterQueue to a cohort, specify the name of the cohort in the
+`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
+are part of the same cohort. If the `spec.cohort` field is empty, the
+ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
+any other ClusterQueue.
+
+Borrowing happens per-flavor. If the ClusterQueues belonging to a cohort
+reference different flavors in `.spec.requestableResources[*].flavors[*].resourceFlavors`,
+they won't be able to share quota among them.
+
+### Borrowing semantics example
+
+Assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+ClusterQueue `team-a-cq` can admit workloads depending on the following
+scenarios:
+
+- If ClusterQueue `team-b-cq` has no admitted workloads, then ClusterQueue
+  `team-a-cq` can admit workloads with resources adding up to `12+9=21` CPUs and
+  `48+36=84Gi` of memory.
+- If ClusterQueue `team-b-cq` has pending workloads and the ClusterQueue
+  `team-a-cq` has all its `guaranteed` quota used, Kueue will admit workloads in
+  ClusterQueue `team-b-cq` before admitting any new workloads in `team-a-cq`.
+  Therefore, Kueue ensures the `guaranteed` quota for `team-b-cq` is met.
```

  It's right in the next paragraph :)

- **docs/tasks/administer_cluster_quotas.md:278** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T20:31:16Z
```diff
@@ -210,6 +210,171 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+Using the following example, you can establish a cohort `team-ab` that includes
+ClusterQueues `team-a-cq` and `team-b-cq`.
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+To create these ClusterQueues, save the preceding manifests and run the
+following command:
+
+```shell
+kubectl apply -f team-a-cq.yaml -f team-b-cq.yaml
+```
+
+## Multiple ClusterQueue with dedicated and fallback flavors
```

  Instead, I decided to add a section in ClusterQueue concept for borrowing semantics

- **docs/tasks/administer_cluster_quotas.md:370** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T20:31:27Z
```diff
@@ -210,6 +210,168 @@ To create the ClusterQueue, run the following command:
 kubectl apply -f cluster-total.yaml
 ```
 
+## Multiple ClusterQueues and borrowing cohorts
+
+Two or more ClusterQueues can borrow unused quota from other ClusterQueues in
+the same cohort.
+
+To establish a cohort, set a common name in the `.spec.cohort` field in all the
+ClusterQueues you want to include in the cohort.
+
+For two ClusterQueues, the manifests look like the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 9
+        ceiling: 15
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+        ceiling: 60Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 12
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+Note that the ClusterQueue `team-a-cq` also defines [ceilings](/docs/concepts/cluster_queue.md#ceilings).
+This restricts the ability of the ClusterQueue to borrow the unused quota from
+the cohort up to the configured ceiling, even if the quota is completely unused.
+
+You can create the ClusterQueues with the following command:
+
+```shell
+kubectl apply -f team-a-cq.yaml -f team-b-cq.yaml
+```
+
+## Multiple ClusterQueue with dedicated and fallback flavors
+
+A ClusterQueue can borrow resources from the cohort even if it has zero
+guaranteed quota for a flavor. This allows you to give dedicated quota for
+a flavor and fallback to quota for a different flavor, shared with other
+tenants.
+
+Such setup can be accomplished with a ClusterQueue for each tenant and an extra
+ClusterQueue for the shared resources. The manifests for two tenants look like
+the following:
+
+```yaml
+# team-a-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 9
+        ceiling: 9
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 0
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 36Gi
+```
+
+```yaml
+# team-b-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: team-b-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: arm
+      quota:
+        guaranteed: 12
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 0
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 48Gi
+```
+
+```yaml
+# shared-cq.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: ClusterQueue
+metadata:
+  name: shared-cq
+spec:
+  namespaceSelector: {}
+  cohort: team-ab
+  requestableResources:
+  - name: "cpu"
+    flavors:
+    - resourceFlavor: x86
+      quota:
+        guaranteed: 6
+  - name: "memory"
+    flavors:
+    - resourceFlavor: default
+      quota:
+        guaranteed: 24Gi
+```
+
+Note that `team-a-cq` and `team-b-cq` define a `ceiling` equal to their
+`guaranteed` quota for the `arm` flavor, meaning that they can't borrow this
+flavor from each other. On the other hand, they define a `guaranteed: 0` for
+the `x86` flavor, meaning that they don't have any dedicated quota for the
+flavor and they can only borrow it from `shared-cq`.
+
```

  Done

### Review by [@cortespao](https://github.com/cortespao) (approved) — 2022-04-07T00:07:43Z

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T12:11:36Z

- **docs/concepts/cluster_queue.md:166** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T12:11:36Z
```diff
@@ -146,16 +147,25 @@ ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
 same cohort can borrow unused quota from each other.
 
 To add a ClusterQueue to a cohort, specify the name of the cohort in the
-`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
-are part of the same cohort. If the `spec.cohort` field is empty, the
-ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
-any other ClusterQueue.
+`.spec.cohort` field. All ClusterQueues that have a matching `spec.cohort` are
+part of the same cohort. If the `spec.cohort` field is empty, the ClusterQueue
+doesn't belong to any cohort, and thus it cannot borrow quota from any other
+ClusterQueue.
 
-Borrowing happens per-flavor. If the ClusterQueues belonging to a cohort
-reference different flavors in `.spec.requestableResources[*].flavors[*].resourceFlavors`,
-they won't be able to share quota among them.
+### Flavors and borrowing semantics
 
-### Borrowing semantics example
+When borrowing, Kueue satisfies the following semantics:
+
+- When assigning flavors, Kueue goes through the list of flavors in
+  `.spec.requestableResources[*].flavors`. For each flavor, Kueue attempts to
+  fit the workload using the guaranteed quota of the ClusterQueue or the unused
+  quota in the cohort. If the workload doesn't fit, Kueue proceeds evaluating
+  the next flavor in the list.
+- Borrowing happens per-flavor. If the ClusterQueues belonging to a cohort
+  reference different flavors in `.spec.requestableResources[*].flavors[*].resourceFlavors`,
+  they won't be able to share quota among them.
```

  The paragraph seems to suggest that a flavor must be defined by all CQs in the cohort for it to be sharable, which is not the case. So perhaps we can rephrase to describe the semantics from a single CQ perspective:
  
  ```
  Borrowing happens per-flavor. A ClusterQueue can only borrow quota of flavors it defines.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T14:04:52Z

- **docs/concepts/cluster_queue.md:166** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:03:03Z
```diff
@@ -146,16 +147,25 @@ ClusterQueues can be grouped in _cohorts_. ClusterQueues that belong to the
 same cohort can borrow unused quota from each other.
 
 To add a ClusterQueue to a cohort, specify the name of the cohort in the
-`.spec.cohort` field. All the ClusterQueues that have a matching `spec.cohort`
-are part of the same cohort. If the `spec.cohort` field is empty, the
-ClusterQueue doesn't belong to any cohort, and thus it cannot borrow quota from
-any other ClusterQueue.
+`.spec.cohort` field. All ClusterQueues that have a matching `spec.cohort` are
+part of the same cohort. If the `spec.cohort` field is empty, the ClusterQueue
+doesn't belong to any cohort, and thus it cannot borrow quota from any other
+ClusterQueue.
 
-Borrowing happens per-flavor. If the ClusterQueues belonging to a cohort
-reference different flavors in `.spec.requestableResources[*].flavors[*].resourceFlavors`,
-they won't be able to share quota among them.
+### Flavors and borrowing semantics
 
-### Borrowing semantics example
+When borrowing, Kueue satisfies the following semantics:
+
+- When assigning flavors, Kueue goes through the list of flavors in
+  `.spec.requestableResources[*].flavors`. For each flavor, Kueue attempts to
+  fit the workload using the guaranteed quota of the ClusterQueue or the unused
+  quota in the cohort. If the workload doesn't fit, Kueue proceeds evaluating
+  the next flavor in the list.
+- Borrowing happens per-flavor. If the ClusterQueues belonging to a cohort
+  reference different flavors in `.spec.requestableResources[*].flavors[*].resourceFlavors`,
+  they won't be able to share quota among them.
```

  I see that I overly complicated the text :)
