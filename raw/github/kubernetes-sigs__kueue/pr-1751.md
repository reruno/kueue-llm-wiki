# PR #1751: [Doc]Add documents for feature LendingLimit

**Summary**: [Doc]Add documents for feature LendingLimit

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1751

**Last updated**: 2024-02-20T09:44:28Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@B1F030](https://github.com/B1F030)
- **Branch**: `doc-lendinglimit` → `main`
- **Created**: 2024-02-18T01:54:58Z
- **Updated**: 2024-02-20T09:44:28Z
- **Merged**: 2024-02-20T09:16:56Z
- **Closed**: 2024-02-20T09:16:56Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: _none_
- **Changed files**: 1   **+60 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
add documents for feature LendingLimit
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
#1224 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-18T01:55:16Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4cca0cfdde338686016ff19f8f87a15268c48466 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65d410b51a0b210008c013c0 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1751--kubernetes-sigs-kueue.netlify.app/docs/concepts/cluster_queue](https://deploy-preview-1751--kubernetes-sigs-kueue.netlify.app/docs/concepts/cluster_queue) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE3NTEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.AjjkttmL9Nn8-7_BAVr9s5bl0XH6ruxIBtsh46fUs-o)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-20T04:55:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1751#" title="Author self-approved">B1F030</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1751#pullrequestreview-1889496751" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-20T04:56:19Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-02-20T04:56:22Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of website in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1751#issuecomment-1953488189):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-20T09:13:22Z

LGTM label has been added.  <details>Git tree hash: 7b1127e279f1e2495371b611de7be84ca745dfa9</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-02-20T09:17:33Z

@tenzen-y: new pull request created: #1759

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1751#issuecomment-1953488189):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2024-02-18T08:34:06Z

- **site/content/en/docs/concepts/cluster_queue.md:361** by [@kerthcet](https://github.com/kerthcet) — 2024-02-18T08:28:35Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
+
+LendingLimit is an `Alpha` feature disabled by default, check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) section of the [Installation](/docs/installation/) for details.
+
+> _Available in Kueue v0.6.0 and later_
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if 
+ClusterQueue `team-b-cq` has no admitted Workloads, then ClusterQueue `team-a-cq` 
+can admit Workloads with resources adding up to `9+1=10` CPUs. 
```

  ```suggestion
  In this case, because we set the lendingLimit in ClusterQueue `team-b-cq`, 
  then ClusterQueue `team-a-cq` can at most admit Workloads with quota adding 
  up to `9+1=10` CPUs. 
  ```

- **site/content/en/docs/concepts/cluster_queue.md:366** by [@kerthcet](https://github.com/kerthcet) — 2024-02-18T08:33:56Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
+
+LendingLimit is an `Alpha` feature disabled by default, check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) section of the [Installation](/docs/installation/) for details.
+
+> _Available in Kueue v0.6.0 and later_
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if 
+ClusterQueue `team-b-cq` has no admitted Workloads, then ClusterQueue `team-a-cq` 
+can admit Workloads with resources adding up to `9+1=10` CPUs. 
+
+If, for a given flavor/resource, the `lendingLimit` field is empty or null, 
+a ClusterQueue can borrow up to the sum of nominal quotas from all the 
+ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can 
+borrow `12+9` CPUs.
```

  ```suggestion
  If, for a given flavor/resource, the `lendingLimit` field is empty or null, 
  a clusterQueue can lend out all of its resources. In the above case, 
  `team-b-cq` can borrow up to `12+9=21` CPUs.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-19T09:15:36Z

After the `lendingLimit` graduates to beta, I think we can update the top of clusterQueue sample here: https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/

@alculquicondor @mimowo WDYT?

- **site/content/en/docs/concepts/cluster_queue.md:323** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-19T09:02:22Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
+
+LendingLimit is an `Alpha` feature disabled by default, check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) section of the [Installation](/docs/installation/) for details.
+
+> _Available in Kueue v0.6.0 and later_
+
```

  ```suggestion
  {{% alert title="Warning" color="warning" %}}
  _Available in Kueue v0.6.0 and later_
  `LendingLimit` is an `Alpha` feature disabled by default.
  You can enable it by setting the `LendingLimit` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
  {{% alert title="Warning" color="warning" %}}
  ```

- **site/content/en/docs/concepts/cluster_queue.md:362** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-19T09:03:07Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
+
+LendingLimit is an `Alpha` feature disabled by default, check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) section of the [Installation](/docs/installation/) for details.
+
+> _Available in Kueue v0.6.0 and later_
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
+ClusterQueue `team-b-cq` has no admitted Workloads using lendingLimit
+(quota usage over guaranteed quota, `12-1=11` CPUs), then ClusterQueue
+`team-a-cq`  can admit Workloads with resources adding up to `9+1=10` CPUs.
```

  ```suggestion
  `team-a-cq` can admit Workloads with resources adding up to `9+1=10` CPUs.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:366** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-19T09:08:50Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
+
+LendingLimit is an `Alpha` feature disabled by default, check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) section of the [Installation](/docs/installation/) for details.
+
+> _Available in Kueue v0.6.0 and later_
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
+ClusterQueue `team-b-cq` has no admitted Workloads using lendingLimit
+(quota usage over guaranteed quota, `12-1=11` CPUs), then ClusterQueue
+`team-a-cq`  can admit Workloads with resources adding up to `9+1=10` CPUs.
+
+If, for a given flavor/resource, the `lendingLimit` field is empty or null,
+a ClusterQueue can lend out all of its resources. So for the yamls listed above,
+`team-b-cq` can  borrow `12+9` CPUs.
```

  ```suggestion
  If the `lendingLimit` field is empty or null,
  a ClusterQueue can lend out all of its resources. So, `team-b-cq` can borrow `12+9` CPUs.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:316** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-19T09:13:43Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
```

  ```suggestion
  ```
  
  I don't think this section level would be useful.

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2024-02-19T10:10:46Z

- **site/content/en/docs/concepts/cluster_queue.md:323** by [@B1F030](https://github.com/B1F030) — 2024-02-19T10:10:46Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
+
+LendingLimit is an `Alpha` feature disabled by default, check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) section of the [Installation](/docs/installation/) for details.
+
+> _Available in Kueue v0.6.0 and later_
+
```

  Done.

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2024-02-19T10:11:04Z

- **site/content/en/docs/concepts/cluster_queue.md:366** by [@B1F030](https://github.com/B1F030) — 2024-02-19T10:11:04Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
+
+LendingLimit is an `Alpha` feature disabled by default, check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) section of the [Installation](/docs/installation/) for details.
+
+> _Available in Kueue v0.6.0 and later_
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
+ClusterQueue `team-b-cq` has no admitted Workloads using lendingLimit
+(quota usage over guaranteed quota, `12-1=11` CPUs), then ClusterQueue
+`team-a-cq`  can admit Workloads with resources adding up to `9+1=10` CPUs.
+
+If, for a given flavor/resource, the `lendingLimit` field is empty or null,
+a ClusterQueue can lend out all of its resources. So for the yamls listed above,
+`team-b-cq` can  borrow `12+9` CPUs.
```

  Done.

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2024-02-19T10:11:26Z

- **site/content/en/docs/concepts/cluster_queue.md:362** by [@B1F030](https://github.com/B1F030) — 2024-02-19T10:11:26Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
+
+LendingLimit is an `Alpha` feature disabled by default, check the [Change the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration) section of the [Installation](/docs/installation/) for details.
+
+> _Available in Kueue v0.6.0 and later_
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
+ClusterQueue `team-b-cq` has no admitted Workloads using lendingLimit
+(quota usage over guaranteed quota, `12-1=11` CPUs), then ClusterQueue
+`team-a-cq`  can admit Workloads with resources adding up to `9+1=10` CPUs.
```

  good catch. thx!

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2024-02-19T10:12:51Z

- **site/content/en/docs/concepts/cluster_queue.md:316** by [@B1F030](https://github.com/B1F030) — 2024-02-19T10:12:51Z
```diff
@@ -307,6 +307,64 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+#### Enabling feature LendingLimit
```

  Yes. I was trying to make it level 4 title, but that doesn't seem to work.
  removed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-19T10:18:00Z

lgtm from myself.

@mimowo Could you help to improve English expressions?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-19T13:10:39Z

Just some nits

- **site/content/en/docs/concepts/cluster_queue.md:312** by [@mimowo](https://github.com/mimowo) — 2024-02-19T13:03:08Z
```diff
@@ -307,6 +307,65 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
```

  ```suggestion
  To limit the amount of resources that a ClusterQueue can lend in the cohort,
  ```

- **site/content/en/docs/concepts/cluster_queue.md:361** by [@mimowo](https://github.com/mimowo) — 2024-02-19T13:07:47Z
```diff
@@ -307,6 +307,65 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+
+`LendingLimit` is an `Alpha` feature disabled by default.
+
+You can enable it by setting the `LendingLimit` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+{{% /alert %}}
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
```

  nit: Some suggestion to maybe make it easier to read:
  ```
  Here, lendingLimit=1 is set in ClusterQueue `team-b-cq`. It means that if all admitted workloads in the 
  ClusterQueue `team-b-cq` have their total quota usage below the `nominalQuota` (less or equal `12-1=11` 
  CPUs), then ClusterQueue `team-a-cq` can admit Workloads with resources adding up to `9+1=10` CPUs.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:366** by [@mimowo](https://github.com/mimowo) — 2024-02-19T13:09:07Z
```diff
@@ -307,6 +307,65 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+
+`LendingLimit` is an `Alpha` feature disabled by default.
+
+You can enable it by setting the `LendingLimit` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+{{% /alert %}}
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
+ClusterQueue `team-b-cq` has no admitted Workloads using lendingLimit
+(quota usage over guaranteed quota, `12-1=11` CPUs), then ClusterQueue
+`team-a-cq` can admit Workloads with resources adding up to `9+1=10` CPUs.
+
+If the `lendingLimit` field is empty or null, a ClusterQueue can lend out
```

  ```suggestion
  If the `lendingLimit` field is not specified, a ClusterQueue can lend out
  ```

- **site/content/en/docs/concepts/cluster_queue.md:367** by [@mimowo](https://github.com/mimowo) — 2024-02-19T13:10:26Z
```diff
@@ -307,6 +307,65 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+
+`LendingLimit` is an `Alpha` feature disabled by default.
+
+You can enable it by setting the `LendingLimit` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+{{% /alert %}}
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
+ClusterQueue `team-b-cq` has no admitted Workloads using lendingLimit
+(quota usage over guaranteed quota, `12-1=11` CPUs), then ClusterQueue
+`team-a-cq` can admit Workloads with resources adding up to `9+1=10` CPUs.
+
+If the `lendingLimit` field is empty or null, a ClusterQueue can lend out
+all of its resources. In this case, `team-b-cq` can  borrow `12+9` CPUs.
```

  ```suggestion
  all of its resources. In this case, `team-a-cq` can use up to `9+12` CPUs.
  ```
  ? I think we want to say from the perspective of `team-a-cq` so that it matches the description in the previous paragraph, just without limit.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-19T14:56:47Z

- **site/content/en/docs/concepts/cluster_queue.md:319** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-19T14:54:45Z
```diff
@@ -307,6 +307,65 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+
+`LendingLimit` is an `Alpha` feature disabled by default.
```

  ```suggestion
  `LendingLimit` is an Alpha feature disabled by default.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:361** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-19T14:56:28Z
```diff
@@ -307,6 +307,65 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+
+`LendingLimit` is an `Alpha` feature disabled by default.
+
+You can enable it by setting the `LendingLimit` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+{{% /alert %}}
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
```

  Use active voice: "you set lendingLimit=1 in ClusterQueue"

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2024-02-20T02:39:23Z

- **site/content/en/docs/concepts/cluster_queue.md:361** by [@B1F030](https://github.com/B1F030) — 2024-02-20T02:39:23Z
```diff
@@ -307,6 +307,65 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+
+`LendingLimit` is an `Alpha` feature disabled by default.
+
+You can enable it by setting the `LendingLimit` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+{{% /alert %}}
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
```

  Done.

### Review by [@B1F030](https://github.com/B1F030) (commented) — 2024-02-20T02:39:52Z

- **site/content/en/docs/concepts/cluster_queue.md:367** by [@B1F030](https://github.com/B1F030) — 2024-02-20T02:39:52Z
```diff
@@ -307,6 +307,65 @@ a ClusterQueue can borrow up to the sum of nominal quotas from all the
 ClusterQueues in the cohort. So for the yamls listed above, `team-b-cq` can
 borrow `12+9` CPUs.
 
+### LendingLimit
+
+To limit the amount of resources that a ClusterQueue can lend to others,
+you can set the `.spec.resourcesGroup[*].flavors[*].resource[*].lendingLimit`
+[quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
+
+{{% alert title="Warning" color="warning" %}}
+_Available in Kueue v0.6.0 and later_
+
+`LendingLimit` is an `Alpha` feature disabled by default.
+
+You can enable it by setting the `LendingLimit` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+{{% /alert %}}
+
+As an example, assume you created the following two ClusterQueues:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+```
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-b-cq"
+spec:
+  namespaceSelector: {} # match all.
+  cohort: "team-ab"
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 12
+        lendingLimit: 1
+```
+
+In this case, because we set lendingLimit in ClusterQueue `team-b-cq`, if
+ClusterQueue `team-b-cq` has no admitted Workloads using lendingLimit
+(quota usage over guaranteed quota, `12-1=11` CPUs), then ClusterQueue
+`team-a-cq` can admit Workloads with resources adding up to `9+1=10` CPUs.
+
+If the `lendingLimit` field is empty or null, a ClusterQueue can lend out
+all of its resources. In this case, `team-b-cq` can  borrow `12+9` CPUs.
```

  SGTM, done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-02-20T04:55:30Z

/approve

leaving lgtm on @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-20T09:13:17Z

/lgtm
thanks
