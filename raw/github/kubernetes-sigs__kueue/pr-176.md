# PR #176: Add more details to the failure to admit event and condition message

**Summary**: Add more details to the failure to admit event and condition message

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/176

**Last updated**: 2022-04-06T00:18:57Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-besteffort-default` → `main`
- **Created**: 2022-04-05T18:18:40Z
- **Updated**: 2022-04-06T00:18:57Z
- **Merged**: 2022-04-06T00:18:57Z
- **Closed**: 2022-04-06T00:18:57Z
- **Labels**: `lgtm`, `tide/merge-method-squash`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@denkensk](https://github.com/denkensk)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 1   **+32 / -26**

## Description


#### What type of PR is this?

Add the reason for failing to admit a workload in the event and condition message. We can still do more by plumbing reason related to failed to match affinity or taints. 

/kind cleanup

## Discussion

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-05T18:18:49Z

/hold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-05T18:18:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/176#" title="Author self-approved">ahg-g</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T21:27:47Z

Please fix `vet`

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-05T23:53:53Z

/lgtm

Maybe you need to squash it.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T00:16:56Z

/hold cancel
/label tide/merge-method-squash

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-05T18:33:48Z

- **pkg/scheduler/scheduler.go:170** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T18:30:42Z
```diff
@@ -153,25 +164,20 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	for _, w := range workloads {
 		log := log.WithValues("queuedWorkload", klog.KObj(w.Obj), "clusterQueue", klog.KRef("", w.ClusterQueue))
 		cq := snap.ClusterQueues[w.ClusterQueue]
-		if cq == nil {
-			log.V(3).Info("ClusterQueue not found when calculating workload admission requirements")
-			continue
-		}
 		ns := corev1.Namespace{}
-		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			log.Error(err, "Looking up namespace")
-			continue
-		}
-		if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			log.V(2).Info("Workload namespace doesn't match clusterQueue selector")
-			continue
-		}
 		e := entry{Info: w}
-		if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			log.V(2).Info("Workload didn't fit in remaining clusterQueue even when borrowing")
-			continue
+		if cq == nil {
+			e.noAdmissionReason = "ClusterQueue not found when calculating workload admission requirements"
```

  ```suggestion
  			e.noAdmissionReason = "ClusterQueue not found"
  ```
  
  I think the rest of the string only make sense as part of the log, not as a user-visible message.

- **pkg/scheduler/scheduler.go:172** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T18:31:55Z
```diff
@@ -153,25 +164,20 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	for _, w := range workloads {
 		log := log.WithValues("queuedWorkload", klog.KObj(w.Obj), "clusterQueue", klog.KRef("", w.ClusterQueue))
 		cq := snap.ClusterQueues[w.ClusterQueue]
-		if cq == nil {
-			log.V(3).Info("ClusterQueue not found when calculating workload admission requirements")
-			continue
-		}
 		ns := corev1.Namespace{}
-		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			log.Error(err, "Looking up namespace")
-			continue
-		}
-		if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			log.V(2).Info("Workload namespace doesn't match clusterQueue selector")
-			continue
-		}
 		e := entry{Info: w}
-		if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			log.V(2).Info("Workload didn't fit in remaining clusterQueue even when borrowing")
-			continue
+		if cq == nil {
+			e.noAdmissionReason = "ClusterQueue not found when calculating workload admission requirements"
+		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
+			e.noAdmissionReason = err.Error()
```

  ```suggestion
  			e.noAdmissionReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
  ```

- **pkg/scheduler/scheduler.go:174** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T18:32:17Z
```diff
@@ -153,25 +164,20 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	for _, w := range workloads {
 		log := log.WithValues("queuedWorkload", klog.KObj(w.Obj), "clusterQueue", klog.KRef("", w.ClusterQueue))
 		cq := snap.ClusterQueues[w.ClusterQueue]
-		if cq == nil {
-			log.V(3).Info("ClusterQueue not found when calculating workload admission requirements")
-			continue
-		}
 		ns := corev1.Namespace{}
-		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			log.Error(err, "Looking up namespace")
-			continue
-		}
-		if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			log.V(2).Info("Workload namespace doesn't match clusterQueue selector")
-			continue
-		}
 		e := entry{Info: w}
-		if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			log.V(2).Info("Workload didn't fit in remaining clusterQueue even when borrowing")
-			continue
+		if cq == nil {
+			e.noAdmissionReason = "ClusterQueue not found when calculating workload admission requirements"
+		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
+			e.noAdmissionReason = err.Error()
+		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
+			e.noAdmissionReason = "Workload namespace doesn't match clusterQueue selector"
```

  ```suggestion
  			e.noAdmissionReason = "Workload namespace doesn't match ClusterQueue selector"
  ```

- **pkg/scheduler/scheduler.go:176** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T18:33:25Z
```diff
@@ -153,25 +164,20 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	for _, w := range workloads {
 		log := log.WithValues("queuedWorkload", klog.KObj(w.Obj), "clusterQueue", klog.KRef("", w.ClusterQueue))
 		cq := snap.ClusterQueues[w.ClusterQueue]
-		if cq == nil {
-			log.V(3).Info("ClusterQueue not found when calculating workload admission requirements")
-			continue
-		}
 		ns := corev1.Namespace{}
-		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			log.Error(err, "Looking up namespace")
-			continue
-		}
-		if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			log.V(2).Info("Workload namespace doesn't match clusterQueue selector")
-			continue
-		}
 		e := entry{Info: w}
-		if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			log.V(2).Info("Workload didn't fit in remaining clusterQueue even when borrowing")
-			continue
+		if cq == nil {
+			e.noAdmissionReason = "ClusterQueue not found when calculating workload admission requirements"
+		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
+			e.noAdmissionReason = err.Error()
+		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
+			e.noAdmissionReason = "Workload namespace doesn't match clusterQueue selector"
+		} else if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
+			e.noAdmissionReason = "Workload didn't fit remaining quota even when borrowing"
```

  ```suggestion
  			e.noAdmissionReason = "Workload didn't fit in the remaining quota"
  ```
  
  some setups might not have cohorts, so borrowing might be confusing.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-05T21:08:46Z

- **pkg/scheduler/scheduler.go:172** by [@ahg-g](https://github.com/ahg-g) — 2022-04-05T21:06:46Z
```diff
@@ -153,25 +164,20 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	for _, w := range workloads {
 		log := log.WithValues("queuedWorkload", klog.KObj(w.Obj), "clusterQueue", klog.KRef("", w.ClusterQueue))
 		cq := snap.ClusterQueues[w.ClusterQueue]
-		if cq == nil {
-			log.V(3).Info("ClusterQueue not found when calculating workload admission requirements")
-			continue
-		}
 		ns := corev1.Namespace{}
-		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			log.Error(err, "Looking up namespace")
-			continue
-		}
-		if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			log.V(2).Info("Workload namespace doesn't match clusterQueue selector")
-			continue
-		}
 		e := entry{Info: w}
-		if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			log.V(2).Info("Workload didn't fit in remaining clusterQueue even when borrowing")
-			continue
+		if cq == nil {
+			e.noAdmissionReason = "ClusterQueue not found when calculating workload admission requirements"
+		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
+			e.noAdmissionReason = err.Error()
```

  the error message should include context, no?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-05T21:27:35Z

- **pkg/scheduler/scheduler.go:172** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-05T21:27:34Z
```diff
@@ -153,25 +164,20 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	for _, w := range workloads {
 		log := log.WithValues("queuedWorkload", klog.KObj(w.Obj), "clusterQueue", klog.KRef("", w.ClusterQueue))
 		cq := snap.ClusterQueues[w.ClusterQueue]
-		if cq == nil {
-			log.V(3).Info("ClusterQueue not found when calculating workload admission requirements")
-			continue
-		}
 		ns := corev1.Namespace{}
-		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			log.Error(err, "Looking up namespace")
-			continue
-		}
-		if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			log.V(2).Info("Workload namespace doesn't match clusterQueue selector")
-			continue
-		}
 		e := entry{Info: w}
-		if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			log.V(2).Info("Workload didn't fit in remaining clusterQueue even when borrowing")
-			continue
+		if cq == nil {
+			e.noAdmissionReason = "ClusterQueue not found when calculating workload admission requirements"
+		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
+			e.noAdmissionReason = err.Error()
```

  sure, but the user might be confused on why there is a message for a namespace failure

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-05T21:35:33Z

- **pkg/scheduler/scheduler.go:172** by [@ahg-g](https://github.com/ahg-g) — 2022-04-05T21:35:33Z
```diff
@@ -153,25 +164,20 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	for _, w := range workloads {
 		log := log.WithValues("queuedWorkload", klog.KObj(w.Obj), "clusterQueue", klog.KRef("", w.ClusterQueue))
 		cq := snap.ClusterQueues[w.ClusterQueue]
-		if cq == nil {
-			log.V(3).Info("ClusterQueue not found when calculating workload admission requirements")
-			continue
-		}
 		ns := corev1.Namespace{}
-		if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			log.Error(err, "Looking up namespace")
-			continue
-		}
-		if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			log.V(2).Info("Workload namespace doesn't match clusterQueue selector")
-			continue
-		}
 		e := entry{Info: w}
-		if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			log.V(2).Info("Workload didn't fit in remaining clusterQueue even when borrowing")
-			continue
+		if cq == nil {
+			e.noAdmissionReason = "ClusterQueue not found when calculating workload admission requirements"
+		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
+			e.noAdmissionReason = err.Error()
```

  not sure if saying "couldn't obtain workload namespace" removes that confusion, but I updated the message as suggested :)
