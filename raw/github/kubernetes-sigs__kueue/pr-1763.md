# PR #1763: [AC] Update description for "Retry" and "Rejected" states.

**Summary**: [AC] Update description for "Retry" and "Rejected" states.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1763

**Last updated**: 2024-03-12T07:27:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `ac-doc-rejected` → `main`
- **Created**: 2024-02-22T06:41:25Z
- **Updated**: 2024-03-12T07:27:40Z
- **Merged**: 2024-02-23T17:01:57Z
- **Closed**: 2024-02-23T17:01:57Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 1   **+4 / -6**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Update description for "Retry" and "Rejected" states. to be in sync with the site documentation.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-22T06:41:42Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cc5749b6855bac07f2571be5381595058ad4f57f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65d8bda8fc17550008c904fb |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-23T14:41:09Z

/approve
LGTM pending for the last comment.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-23T14:41:16Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1763#issuecomment-1961454161" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1763#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-23T16:58:24Z

Anything else to add @mimowo ?

Leaving LGTM to you

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-23T16:58:46Z

Ah sorry, I got confused with the other PR.

This is ready.

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-23T16:58:51Z

LGTM label has been added.  <details>Git tree hash: 8aba440c187a80023dced4c0c55d68dcbbd800c4</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T09:49:37Z

Just rephrase suggestion to make it easier to follow. Generally according to the style guide present tense is preferred: https://kubernetes.io/docs/contribute/style/style-guide/#content-best-practices.

- **apis/kueue/v1beta1/admissioncheck_types.go:28** by [@mimowo](https://github.com/mimowo) — 2024-02-22T09:40:56Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
```

  ```suggestion
  	// A workload having at least one check in the state is requeued. An admitted workload
  	// is evicted before requeuing.
  ```
  I guess it still is considered for admission, once it gets the quota reservation again?

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@mimowo](https://github.com/mimowo) — 2024-02-22T09:42:51Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  ```suggestion
  	// A workload having at least one check in the state is marked finished. An admitted workload
  	// is evicted before marked as finished.
  ```

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@mimowo](https://github.com/mimowo) — 2024-02-22T09:48:16Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  A suggestion, I think it will be easier to follow.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T10:29:14Z

- **apis/kueue/v1beta1/admissioncheck_types.go:28** by [@trasc](https://github.com/trasc) — 2024-02-22T10:29:14Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
```

  It is not, only after the `Retry` is replace with `Pending`

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T10:33:36Z

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@trasc](https://github.com/trasc) — 2024-02-22T10:33:36Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  The order should also matter

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T10:55:33Z

- **apis/kueue/v1beta1/admissioncheck_types.go:28** by [@mimowo](https://github.com/mimowo) — 2024-02-22T10:55:32Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
```

  Ok, but it still can be considered for admission if the admission check is flipped. I think this is a purpose of Retry to re-consider it for admission, contrary to Reject.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T10:56:45Z

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@mimowo](https://github.com/mimowo) — 2024-02-22T10:56:45Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  I didn't get - "order of what"? If my suggestion is wrong in some way, could you please try to propose another update to the comment to clarify.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T12:11:50Z

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@trasc](https://github.com/trasc) — 2024-02-22T12:11:50Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  Of operations, the wl is first evicted after that is marked as finished.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T12:13:20Z

- **apis/kueue/v1beta1/admissioncheck_types.go:28** by [@trasc](https://github.com/trasc) — 2024-02-22T12:13:20Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
```

  This PR only changes the comment to better describe the behavior.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T12:39:50Z

- **apis/kueue/v1beta1/admissioncheck_types.go:28** by [@mimowo](https://github.com/mimowo) — 2024-02-22T12:39:50Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
```

  Ok, maybe then this is enough:
  `// An admitted workload having at least one check in the state is evicted.`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T12:40:35Z

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@mimowo](https://github.com/mimowo) — 2024-02-22T12:40:35Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  Got it, maybe then 
  ```
  // A workload having at least one check in the state is marked finished. An admitted workload
  // is also evicted.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T12:45:43Z

- **apis/kueue/v1beta1/admissioncheck_types.go:28** by [@trasc](https://github.com/trasc) — 2024-02-22T12:45:43Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
```

  Why make it incomplete. 
  
  The changed I've done here is 
  
  ```diff
  - A workload having at least one check in the state, will be evicted if admitted will not be considered for admission.
  + A workload having at least one check in the state, will be evicted if admitted and not be considered for admission.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T12:46:27Z

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@trasc](https://github.com/trasc) — 2024-02-22T12:46:27Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  Wrong order again.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T12:48:37Z

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@mimowo](https://github.com/mimowo) — 2024-02-22T12:48:37Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  Can you please propose an edit, because I'm not sure what you mean.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-22T12:51:42Z

- **apis/kueue/v1beta1/admissioncheck_types.go:28** by [@mimowo](https://github.com/mimowo) — 2024-02-22T12:51:42Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
```

  sounds acceptable, but let me defer to the API reviewers @alculquicondor @tenzen-y

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T12:56:01Z

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@trasc](https://github.com/trasc) — 2024-02-22T12:56:01Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  I did already.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-22T18:15:56Z

- **apis/kueue/v1beta1/admissioncheck_types.go:28** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T18:09:42Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
```

  ```suggestion
  	// A workload having at least one check in this state will be evicted if admitted and
  ```

- **apis/kueue/v1beta1/admissioncheck_types.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T18:10:06Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
```

  ```suggestion
  	// will no longer be considered for admission.
  ```

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T18:11:31Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  ```suggestion
  	// A workload having at least one check in this state will be evicted if admitted and will be
  ```

- **apis/kueue/v1beta1/admissioncheck_types.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-22T18:15:53Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
```

  or is it:
  
  ```suggestion
  	// will not be considered for admission while the check is in this state.
  ```
  ?
  
  Please refresh my memory: how does Kueue get out of this state?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T21:07:42Z

- **apis/kueue/v1beta1/admissioncheck_types.go:28** by [@trasc](https://github.com/trasc) — 2024-02-22T21:07:42Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T21:08:36Z

- **apis/kueue/v1beta1/admissioncheck_types.go:29** by [@trasc](https://github.com/trasc) — 2024-02-22T21:08:35Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
```

  
  
  The AC controller should switch it to any other state by its own logic.
  
  changed to `will not be considered for admission.`

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-22T21:10:24Z

- **apis/kueue/v1beta1/admissioncheck_types.go:34** by [@trasc](https://github.com/trasc) — 2024-02-22T21:10:24Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
 	CheckStateRetry CheckState = "Retry"
 
 	// CheckStateRejected means that the check will not pass in the near future. It is not worth
 	// to retry.
-	// NOTE: The admission behaviour is currently the same as for retry,
-	// we can consider marking the workload as "Finished" with a failure
-	// description.
+	// A workload having at least one check in the state, will be evicted if admitted will and
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-23T10:06:28Z

- **apis/kueue/v1beta1/admissioncheck_types.go:29** by [@mimowo](https://github.com/mimowo) — 2024-02-23T10:06:28Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
```

  Indeed the current status is that when `Retry` is set on a workload's admission check, then it stays, so the workload is not considered for admission, until flipped to `Pending`. 
  
  However, I did some digging into the KEP (as this was confusing to me during review of https://github.com/kubernetes-sigs/kueue/pull/1681 which starts to make use of `Retry`), and at the API level we have `RetryDelayMinutes`, IIUC this should automatically transition the state from `Retry` to `Pending`, so that the workload can be re-admitted after 15min. I think it is currently not supported though. 
  
  Let me know if I'm not missing something. I think it would be good to support `RetryDelayMinutes` as it is part of the API (surely separate PR). If we have other priorities I would suggest mentioning that this is the current state for Alpha.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-23T10:33:55Z

- **apis/kueue/v1beta1/admissioncheck_types.go:29** by [@trasc](https://github.com/trasc) — 2024-02-23T10:33:55Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
```

  I do nor recall the exact circumstances `RetryDelayMinutes` did not make it in the implementation, the preemtion support is not yet merged also being still in review. 
  
  Going back to `RetryDelayMinutes` that approach is very inflexible,  different workloads may need different times to wait before requeue. 
  
  Anyway this discussion is not relevant in the context of this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-23T10:48:34Z

- **apis/kueue/v1beta1/admissioncheck_types.go:29** by [@mimowo](https://github.com/mimowo) — 2024-02-23T10:48:34Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
```

  I find it relevant because if we supported the retry delay field, then the workload is considered for admission after that time. I think the comments for the retry field and the fields you amend in this comment are not compatible currently.
  
  If we don't aim to support retry delay field in near future then I would suggest to update its comment so that the comments are aligned.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-23T14:40:39Z

- **apis/kueue/v1beta1/admissioncheck_types.go:29** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-23T14:40:38Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
```

  Please update to `will not be considered for admission while the check is in this state` to make the wording more clear.
  
  Let's assume that we don't plan to support `retryDealyMinutes` in the near future (possibly throughout 0.7), so let' s not discuss that topic.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-23T15:46:13Z

- **apis/kueue/v1beta1/admissioncheck_types.go:29** by [@trasc](https://github.com/trasc) — 2024-02-23T15:46:13Z
```diff
@@ -25,16 +25,14 @@ type CheckState string
 const (
 	// CheckStateRetry means that the check cannot pass at this moment, back off (possibly
 	// allowing other to try, unblock quota) and retry.
-	// A workload having at least one check in the state,
-	// will be evicted if admitted will not be considered
-	// for admission.
+	// A workload having at least one check in the state, will be evicted if admitted and
+	// not be considered for admission.
```

  done
