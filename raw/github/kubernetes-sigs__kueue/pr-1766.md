# PR #1766: [pods] Finalize the replaced pods

**Summary**: [pods] Finalize the replaced pods

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1766

**Last updated**: 2024-05-30T14:33:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `pods-delete-failed-on-replacement` → `main`
- **Created**: 2024-02-23T13:29:39Z
- **Updated**: 2024-05-30T14:33:31Z
- **Merged**: 2024-03-04T20:30:53Z
- **Closed**: 2024-03-04T20:30:53Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 10   **+647 / -111**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Finalize the Failed pods in a pod-group once a replacement pods are created.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1762 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Pod Integration: Finalize failed pods in a pod-group when replacement pods are created
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-23T13:29:41Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@trasc](https://github.com/trasc) — 2024-02-23T13:29:48Z

/test all

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-23T13:29:54Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e34470c006b60a10b615435a2d2c079b385e77ef |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65e620d525aba300085720b9 |

### Comment by [@trasc](https://github.com/trasc) — 2024-02-23T15:23:02Z

/test all

### Comment by [@trasc](https://github.com/trasc) — 2024-02-23T19:28:20Z

/cc @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-04T18:53:05Z

LGTM label has been added.  <details>Git tree hash: 6a6c6ac0a8df89aad7977fdcf5ea5a1d9557fe1e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-04T18:53:10Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1766#pullrequestreview-1915019319" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1766#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T19:36:06Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-04T19:36:11Z

LGTM label has been added.  <details>Git tree hash: d590e2f299bc666aef97cfee1f971269acecc338</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T19:41:11Z

/cherry-pick release-0.6

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-03-04T19:41:13Z

@alculquicondor: once the present PR merges, I will cherry-pick it on top of release-0.6 in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1766#issuecomment-1977328573):

>/cherry-pick release-0.6


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T19:52:25Z

Maybe the last attempt didn't catch the recently merged fix

/retest

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-03-04T20:31:30Z

@alculquicondor: #1766 failed to apply on top of branch "release-0.6":
```
Applying: Add Pick
Applying: Delete the replaced pods
Applying: KEP Update
Applying: Review Remarks
Using index info to reconstruct a base tree...
M	keps/976-plain-pods/README.md
M	pkg/controller/jobs/pod/pod_controller.go
M	test/e2e/singlecluster/pod_test.go
M	test/integration/controller/jobs/pod/pod_controller_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/controller/jobs/pod/pod_controller_test.go
Auto-merging test/e2e/singlecluster/pod_test.go
CONFLICT (content): Merge conflict in test/e2e/singlecluster/pod_test.go
Auto-merging pkg/controller/jobs/pod/pod_controller.go
Auto-merging keps/976-plain-pods/README.md
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0004 Review Remarks
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1766#issuecomment-1977328573):

>/cherry-pick release-0.6


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T20:51:23Z

@trasc please create a cherry-pick and highlight in the description any significant changes compared to this PR.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-08T17:50:20Z

/kind bug

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T14:33:28Z

/release-note-edit
```release-note
Pod Integration: Finalize failed pods in a pod-group when replacement pods are created
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-24T01:46:58Z

- **pkg/util/slices/slices.go:91** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-24T01:16:54Z
```diff
@@ -82,3 +82,17 @@ func CmpNoOrder[E comparable, S ~[]E](a, b S) bool {
 	}
 	return true
 }
+
+// Pick creates a new slice containing only the elements for which keep return true.
+func Pick[E any, S ~[]E](s S, keep func(*E) bool) S {
+	if s == nil {
+		return nil
+	}
+	ret := S{}
```

  nit:
  ```suggestion
  	var ret S
  ```
  To save one conditional and an allocation if no elements pass `keep`

- **pkg/util/slices/slices_test.go:121** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-24T01:18:33Z
```diff
@@ -102,3 +102,36 @@ func TestToCmpNoOrder(t *testing.T) {
 		})
 	}
 }
+
+func TestPick(t *testing.T) {
+	cases := map[string]struct {
+		testSlice []int
+		want      []int
+	}{
+		"nil input": {
+			testSlice: nil,
+			want:      nil,
+		},
+		"empty input": {
+			testSlice: []int{},
+			want:      []int{},
+		},
+		"no match": {
+			testSlice: []int{1, 3, 5, 7, 9},
+			want:      []int{},
```

  ```suggestion
  			want:      nil,
  ```

- **test/integration/controller/jobs/pod/pod_controller_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-24T01:24:26Z
  Please also update the following E2E tests:
  - `Failed Pod can be replaced in group`
  - `should allow to preempt the lower priority group`
  
  in https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/singlecluster/pod_test.go

- **pkg/controller/jobs/pod/pod_controller.go:711** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-24T01:26:07Z
```diff
@@ -704,14 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 
 // runnableOrSucceededPods returns a slice of active pods in the group
 func (p *Pod) runnableOrSucceededPods() []corev1.Pod {
-	activePods := make([]corev1.Pod, 0, len(p.list.Items))
-	for _, pod := range p.list.Items {
-		if isPodRunnableOrSucceeded(&pod) {
-			activePods = append(activePods, pod)
-		}
-	}
+	return utilslices.Pick(p.list.Items, isPodRunnableOrSucceeded)
+}
 
-	return activePods
+// notRunnableandAndNotSucceededPods returns a slice of inactive pods in the group
+func (p *Pod) notRunnableAndNotSucceededPods() []corev1.Pod {
```

  ```suggestion
  func (p *Pod) notRunnableNorSucceededPods() []corev1.Pod {
  ```

- **pkg/controller/jobs/pod/pod_controller.go:1062** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-24T01:26:57Z
```diff
@@ -971,25 +1035,31 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableAndNotSucceededPods()
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
-			}
-
-			if ps.Name == roleHash {
-				roleActivePods = append(roleActivePods, activePod)
+				roleHashErrors = append(roleHashErrors, err)
+				return false
 			}
+			return hash == ps.Name
+		}
+		roleActivePods := utilslices.Pick(activePods, hasRoleFunc)
+		roleInactivePods := utilslices.Pick(inactivePods, hasRoleFunc)
+		if len(roleHashErrors) > 0 {
+			return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", errors.Join(roleHashErrors...))
 		}
-
 		// Cleanup excess pods of the role
-		err := p.cleanupExcessPods(ctx, c, r, int(ps.Count), roleActivePods)
+		err := p.cleanupExcessPods(ctx, c, r, int(ps.Count), roleActivePods, roleInactivePods)
 		if err != nil {
 			return nil, nil, err
 		}
+
+		// Cleanup replaced inactive pods
```

  Is this a leftover from a previous approach?

- **keps/976-plain-pods/README.md:584** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-24T01:40:54Z
```diff
@@ -565,25 +565,23 @@ in-memory Workload, then it is considered unmatching and the Workload is evicted
 In the Pod-group reconciler:
 1. If the Pod is not terminated and doesn't have a deletionTimestamp,
    create a Workload for the pod group if one does not exist.
-2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
-   timestamp or is finished.
+2. Remove Pod finalizers if:
+  - The Pod is terminated and the Workload is finished, has a deletion timestamp or is finished.
+  - The Pod Failed and a valid replacement pod was created for it.
 3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
    then evict the Workload.
 4. For gated pods:
    - remove the gate, set nodeSelector
 5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
    and remove the finalizers from the Pods.
 
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
-managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated.
-
 ### Retrying Failed Pods
 
 The Pod group will generally only be considered finished if all the Pods finish with a Succeeded
 phase.
 This allows the user to send replacement Pods when a Pod in the group fails or if the group is
 preempted. The replacement Pods can have any name, but they must point to the same pod group.
+Once the replacement Pod is created the old one will be completely removed.
```

  Without reading the implementation, I think this might be less code and easier to maintain (because we don't exactly know which pod is being replaced):
  
  ```suggestion
  Once a replacement Pod is created, and Kueue has added it as an owner of the Workload,
   any Failed pods will be deleted.
  ```

- **keps/976-plain-pods/README.md:584** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-24T01:41:02Z
```diff
@@ -565,25 +565,23 @@ in-memory Workload, then it is considered unmatching and the Workload is evicted
 In the Pod-group reconciler:
 1. If the Pod is not terminated and doesn't have a deletionTimestamp,
    create a Workload for the pod group if one does not exist.
-2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
-   timestamp or is finished.
+2. Remove Pod finalizers if:
+  - The Pod is terminated and the Workload is finished, has a deletion timestamp or is finished.
+  - The Pod Failed and a valid replacement pod was created for it.
 3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
    then evict the Workload.
 4. For gated pods:
    - remove the gate, set nodeSelector
 5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
    and remove the finalizers from the Pods.
 
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
-managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated.
-
 ### Retrying Failed Pods
 
 The Pod group will generally only be considered finished if all the Pods finish with a Succeeded
 phase.
 This allows the user to send replacement Pods when a Pod in the group fails or if the group is
 preempted. The replacement Pods can have any name, but they must point to the same pod group.
+Once the replacement Pod is created the old one will be completely removed.
```

  Without reading the implementation, I think this might be less code and easier to maintain (because we don't exactly know which pod is being replaced):
  
  ```suggestion
  Once a replacement Pod is created, and Kueue has added it as an owner of the Workload,
   any Failed pods will be deleted.
  ```

- **pkg/controller/jobs/pod/pod_controller.go:750** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-24T01:42:20Z
```diff
@@ -723,11 +721,77 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
+// Get the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
+	}
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
+	}
+	return deletionTime.Time
+}
+
 // cleanupExcessPods will delete and finalize pods created last if the number of
 // activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod, inactivePods []corev1.Pod) error {
 	log := ctrl.LoggerFrom(ctx)
 
+	// Remove inactive pods that have a replacement
+	if len(inactivePods) > 0 && (len(inactivePods)+len(activePods)) > totalCount {
```

  I suppose here you are trying to identify which pod was replaced. I prefer we avoid having to maintain this code.

- **test/integration/controller/jobs/pod/pod_controller_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-24T01:46:38Z
  I think we need a stronger test:
  
  A scenario where a Pod fails, gets replaced and the replacement immediately fails.
  We need to safeguard against the case where we prematurely delete pods in a way that could lead to the deletion of the Workload.
  It might be the case that your implementation already is good against this case. But we need to be extra sure, as this might affect production workloads.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-26T09:47:38Z

- **keps/976-plain-pods/README.md:584** by [@trasc](https://github.com/trasc) — 2024-02-26T07:37:22Z
```diff
@@ -565,25 +565,23 @@ in-memory Workload, then it is considered unmatching and the Workload is evicted
 In the Pod-group reconciler:
 1. If the Pod is not terminated and doesn't have a deletionTimestamp,
    create a Workload for the pod group if one does not exist.
-2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
-   timestamp or is finished.
+2. Remove Pod finalizers if:
+  - The Pod is terminated and the Workload is finished, has a deletion timestamp or is finished.
+  - The Pod Failed and a valid replacement pod was created for it.
 3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
    then evict the Workload.
 4. For gated pods:
    - remove the gate, set nodeSelector
 5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
    and remove the finalizers from the Pods.
 
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
-managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated.
-
 ### Retrying Failed Pods
 
 The Pod group will generally only be considered finished if all the Pods finish with a Succeeded
 phase.
 This allows the user to send replacement Pods when a Pod in the group fails or if the group is
 preempted. The replacement Pods can have any name, but they must point to the same pod group.
+Once the replacement Pod is created the old one will be completely removed.
```

  Updated. 
  Going with "any  Failed" , we could at most skip the sorting, and replacing them in the order they failed , so the last that failed is kept, can help in debugging.

- **keps/976-plain-pods/README.md:584** by [@trasc](https://github.com/trasc) — 2024-02-26T07:37:38Z
```diff
@@ -565,25 +565,23 @@ in-memory Workload, then it is considered unmatching and the Workload is evicted
 In the Pod-group reconciler:
 1. If the Pod is not terminated and doesn't have a deletionTimestamp,
    create a Workload for the pod group if one does not exist.
-2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
-   timestamp or is finished.
+2. Remove Pod finalizers if:
+  - The Pod is terminated and the Workload is finished, has a deletion timestamp or is finished.
+  - The Pod Failed and a valid replacement pod was created for it.
 3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
    then evict the Workload.
 4. For gated pods:
    - remove the gate, set nodeSelector
 5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
    and remove the finalizers from the Pods.
 
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
-managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated.
-
 ### Retrying Failed Pods
 
 The Pod group will generally only be considered finished if all the Pods finish with a Succeeded
 phase.
 This allows the user to send replacement Pods when a Pod in the group fails or if the group is
 preempted. The replacement Pods can have any name, but they must point to the same pod group.
+Once the replacement Pod is created the old one will be completely removed.
```

  duplicate

- **pkg/controller/jobs/pod/pod_controller.go:711** by [@trasc](https://github.com/trasc) — 2024-02-26T07:38:35Z
```diff
@@ -704,14 +704,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 
 // runnableOrSucceededPods returns a slice of active pods in the group
 func (p *Pod) runnableOrSucceededPods() []corev1.Pod {
-	activePods := make([]corev1.Pod, 0, len(p.list.Items))
-	for _, pod := range p.list.Items {
-		if isPodRunnableOrSucceeded(&pod) {
-			activePods = append(activePods, pod)
-		}
-	}
+	return utilslices.Pick(p.list.Items, isPodRunnableOrSucceeded)
+}
 
-	return activePods
+// notRunnableandAndNotSucceededPods returns a slice of inactive pods in the group
+func (p *Pod) notRunnableAndNotSucceededPods() []corev1.Pod {
```

  done

- **pkg/controller/jobs/pod/pod_controller.go:750** by [@trasc](https://github.com/trasc) — 2024-02-26T07:40:12Z
```diff
@@ -723,11 +721,77 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
+// Get the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
+	}
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
+	}
+	return deletionTime.Time
+}
+
 // cleanupExcessPods will delete and finalize pods created last if the number of
 // activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod, inactivePods []corev1.Pod) error {
 	log := ctrl.LoggerFrom(ctx)
 
+	// Remove inactive pods that have a replacement
+	if len(inactivePods) > 0 && (len(inactivePods)+len(activePods)) > totalCount {
```

  why? 
  A very similar approach is used to remove the excess of created pods.

- **pkg/controller/jobs/pod/pod_controller.go:1062** by [@trasc](https://github.com/trasc) — 2024-02-26T07:40:43Z
```diff
@@ -971,25 +1035,31 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableAndNotSucceededPods()
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
-			}
-
-			if ps.Name == roleHash {
-				roleActivePods = append(roleActivePods, activePod)
+				roleHashErrors = append(roleHashErrors, err)
+				return false
 			}
+			return hash == ps.Name
+		}
+		roleActivePods := utilslices.Pick(activePods, hasRoleFunc)
+		roleInactivePods := utilslices.Pick(inactivePods, hasRoleFunc)
+		if len(roleHashErrors) > 0 {
+			return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", errors.Join(roleHashErrors...))
 		}
-
 		// Cleanup excess pods of the role
-		err := p.cleanupExcessPods(ctx, c, r, int(ps.Count), roleActivePods)
+		err := p.cleanupExcessPods(ctx, c, r, int(ps.Count), roleActivePods, roleInactivePods)
 		if err != nil {
 			return nil, nil, err
 		}
+
+		// Cleanup replaced inactive pods
```

  it is , removed

- **pkg/util/slices/slices.go:91** by [@trasc](https://github.com/trasc) — 2024-02-26T07:41:00Z
```diff
@@ -82,3 +82,17 @@ func CmpNoOrder[E comparable, S ~[]E](a, b S) bool {
 	}
 	return true
 }
+
+// Pick creates a new slice containing only the elements for which keep return true.
+func Pick[E any, S ~[]E](s S, keep func(*E) bool) S {
+	if s == nil {
+		return nil
+	}
+	ret := S{}
```

  done

- **pkg/util/slices/slices_test.go:121** by [@trasc](https://github.com/trasc) — 2024-02-26T07:41:10Z
```diff
@@ -102,3 +102,36 @@ func TestToCmpNoOrder(t *testing.T) {
 		})
 	}
 }
+
+func TestPick(t *testing.T) {
+	cases := map[string]struct {
+		testSlice []int
+		want      []int
+	}{
+		"nil input": {
+			testSlice: nil,
+			want:      nil,
+		},
+		"empty input": {
+			testSlice: []int{},
+			want:      []int{},
+		},
+		"no match": {
+			testSlice: []int{1, 3, 5, 7, 9},
+			want:      []int{},
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-26T09:48:02Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:1** by [@trasc](https://github.com/trasc) — 2024-02-26T09:48:02Z
  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-26T09:50:12Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:1** by [@trasc](https://github.com/trasc) — 2024-02-26T09:50:12Z
  `"Should keep the existing workload for pod replacement"` updated to fail the replacement after it's created.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-26T10:44:56Z

- **pkg/controller/jobs/pod/pod_controller.go:750** by [@trasc](https://github.com/trasc) — 2024-02-26T10:44:56Z
```diff
@@ -723,11 +721,77 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
+// Get the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
+	}
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
+	}
+	return deletionTime.Time
+}
+
 // cleanupExcessPods will delete and finalize pods created last if the number of
 // activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod, inactivePods []corev1.Pod) error {
 	log := ctrl.LoggerFrom(ctx)
 
+	// Remove inactive pods that have a replacement
+	if len(inactivePods) > 0 && (len(inactivePods)+len(activePods)) > totalCount {
```

  Also refactored to reuse the deletion loop.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-26T19:07:33Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:776** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-26T18:40:03Z
```diff
@@ -769,11 +769,20 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				gomega.Expect(k8sClient.Create(ctx, replacementPod)).Should(gomega.Succeed())
 				replacementPodLookupKey := client.ObjectKeyFromObject(replacementPod)
 
+				ginkgo.By("Failing the replacement", func() {
```

  To make it closer to production, we should wait for the schedulingGate to be removed before setting the phase to Failed.

- **test/integration/controller/jobs/pod/pod_controller_test.go:788** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-26T18:42:05Z
```diff
@@ -769,11 +769,20 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				gomega.Expect(k8sClient.Create(ctx, replacementPod)).Should(gomega.Succeed())
 				replacementPodLookupKey := client.ObjectKeyFromObject(replacementPod)
 
+				ginkgo.By("Failing the replacement", func() {
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodFailed, replacementPod)
+				})
+
 				// Workload shouldn't be deleted after replacement pod has been created
 				gomega.Consistently(func() error {
 					return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
 				}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
 
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, podLookupKey, createdPod)).Should(testing.BeNotFoundError())
+					g.Expect(k8sClient.Get(ctx, replacementPodLookupKey, createdPod)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
 				util.ExpectPodUnsuspendedWithNodeSelectors(ctx, k8sClient, replacementPodLookupKey, map[string]string{"kubernetes.io/arch": "arm64"})
 
 				util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, replacementPod)
```

  this is not accurate. Pods never transition from Failed to Succeeded.

- **keps/976-plain-pods/README.md:569** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-26T18:43:09Z
```diff
@@ -565,25 +565,24 @@ in-memory Workload, then it is considered unmatching and the Workload is evicted
 In the Pod-group reconciler:
 1. If the Pod is not terminated and doesn't have a deletionTimestamp,
    create a Workload for the pod group if one does not exist.
-2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
-   timestamp or is finished.
+2. Remove Pod finalizers if:
+  - The Pod is terminated and the Workload is finished, has a deletion timestamp or is finished.
```

  It looks like it was not well written before, but let's fix it.
  ```suggestion
    - The Pod is terminated and the Workload is finished or has a deletion timestamp.
  ```

- **keps/976-plain-pods/README.md:585** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-26T18:45:41Z
```diff
@@ -565,25 +565,24 @@ in-memory Workload, then it is considered unmatching and the Workload is evicted
 In the Pod-group reconciler:
 1. If the Pod is not terminated and doesn't have a deletionTimestamp,
    create a Workload for the pod group if one does not exist.
-2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
-   timestamp or is finished.
+2. Remove Pod finalizers if:
+  - The Pod is terminated and the Workload is finished, has a deletion timestamp or is finished.
+  - The Pod Failed and a valid replacement pod was created for it.
 3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
    then evict the Workload.
 4. For gated pods:
    - remove the gate, set nodeSelector
 5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
    and remove the finalizers from the Pods.
 
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
-managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated.
-
 ### Retrying Failed Pods
 
 The Pod group will generally only be considered finished if all the Pods finish with a Succeeded
 phase.
 This allows the user to send replacement Pods when a Pod in the group fails or if the group is
 preempted. The replacement Pods can have any name, but they must point to the same pod group.
+Once a replacement Pod is created, and Kueue has added it as an owner of the Workload, the
+Failed pod will be deleted.
```

  Add a note on how we identify which Pod is replaced:
  
  If multiple Pods have Failed, a new Pod is assumed to replace the Pod that failed first.

- **pkg/controller/jobs/pod/pod_controller.go:1029** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-26T19:02:53Z
```diff
@@ -971,22 +1020,26 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
```

  `FindMatchingWorkloads` runs before `EnsureWorkloadOwnedByAllMembers`, so this implementation is **not** satisfying what the KEP update says: "Once a replacement Pod is created, and Kueue has added it as an owner of the Workload, the Failed pod will be deleted."
  
  A replacement Pod cannot possibly enter the Failed state until Kueue has removed its scheduling gate, which only happens after `EnsureWorkloadOwnedByAllMembers`. So this sequence of events is probably ok.
  
  However, the user could still delete the replacement Pod immediately after creating it. In this case, the Pod still has a finalizer, so it wouldn't be really deleted, but since it doesn't own the Workload, the Workload can still be garbage collected. Unless I'm missing something?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-27T13:03:09Z

- **pkg/controller/jobs/pod/pod_controller.go:1029** by [@trasc](https://github.com/trasc) — 2024-02-27T13:03:09Z
```diff
@@ -971,22 +1020,26 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
```

  We could drop the `EnsureWorkloadOwnedByAllMembers` method from the interface and make its functionality part of the `FindMatchingWorkloads` before any excess pods cleanup is done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-27T15:19:48Z

- **pkg/controller/jobs/pod/pod_controller.go:1029** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T15:19:48Z
```diff
@@ -971,22 +1020,26 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
```

  I'm ok with that. I suppose you mean to only make owners the Pods in the active list.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-27T15:50:40Z

- **pkg/controller/jobs/pod/pod_controller.go:1029** by [@trasc](https://github.com/trasc) — 2024-02-27T15:50:39Z
```diff
@@ -971,22 +1020,26 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
```

  Sure, but before deleting the excess.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-27T16:00:03Z

- **pkg/controller/jobs/pod/pod_controller.go:1029** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T16:00:03Z
```diff
@@ -971,22 +1020,26 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
```

  Sounds good.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-27T18:39:57Z

- **pkg/controller/jobs/pod/pod_controller.go:785** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T18:39:05Z
```diff
@@ -723,47 +721,98 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
+// Get the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
+	}
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
+	}
+	return deletionTime.Time
+}
+
 // cleanupExcessPods will delete and finalize pods created last if the number of
 // activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod, inactivePods []corev1.Pod) error {
 	log := ctrl.LoggerFrom(ctx)
 
-	extraPodsCount := len(activePods) - totalCount
+	extraInactivePodsCount := min(len(inactivePods), len(inactivePods)+len(activePods)-totalCount)
+	extraActivePodsCount := len(activePods) - totalCount
 
-	if extraPodsCount <= 0 {
+	if extraActivePodsCount <= 0 && extraInactivePodsCount <= 0 {
 		return nil
 	}
 	// Do not clean up more pods until observing previous operations
 	if !p.satisfiedExcessPods {
 		return errPendingOps
 	}
 
-	// Sort active pods by creation timestamp
-	sort.Slice(activePods, func(i, j int) bool {
-		pi := &activePods[i]
-		pj := &activePods[j]
-		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
-		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
-		// Prefer to keep pods that have a finalizer.
-		if iFin != jFin {
-			return iFin
-		}
-		iGated := gateIndex(pi) != gateNotFound
-		jGated := gateIndex(pj) != gateNotFound
-		// Prefer to keep pods that aren't gated.
-		if iGated != jGated {
-			return !iGated
-		}
-		return pi.ObjectMeta.CreationTimestamp.Before(&pj.ObjectMeta.CreationTimestamp)
-	})
+	var extraPods []corev1.Pod
+
+	// Remove inactive pods that have a replacement
+	if extraInactivePodsCount > 0 {
+		// Sort inactive pods
+		sort.Slice(activePods, func(i, j int) bool {
+			pi := &activePods[i]
+			pj := &activePods[j]
+			iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+			jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+			// Prefer to keep pods that have a finalizer.
+			// Add them to the end of the list.
+			if iFin != jFin {
+				return jFin
+			}
 
-	// Extract all the latest created extra pods
-	extraPods := activePods[len(activePods)-extraPodsCount:]
-	extraPodsUIDs := utilslices.Map(extraPods, func(p *corev1.Pod) types.UID { return p.UID })
-	p.excessPodExpectations.ExpectUIDs(log, p.key, extraPodsUIDs)
+			iLastActive := lastActiveTime(pi)
+			jLastActive := lastActiveTime(pj)
 
-	// Finalize and delete the active pods created last
+			if iLastActive.Equal(jLastActive) {
+				return pi.CreationTimestamp.Before(&pj.CreationTimestamp)
+			}
+			return iLastActive.Before(jLastActive)
+		})
+
+		extraPods = inactivePods[:extraInactivePodsCount]
```

  Can you explore the option of not deleting these pods? Just removing the finalizer.
  
  Then it would be up to the k8s garbage collector to take them.
  
  This behavior would preserve the lifecycle of a Pod that users are accustomed to, avoiding potential surprises, or even allowing for some debugging of the Pod yaml before it's fully deleted.

### Review by [@alexandear](https://github.com/alexandear) (commented) — 2024-02-28T11:44:45Z

- **pkg/controller/jobs/pod/pod_controller.go:724** by [@alexandear](https://github.com/alexandear) — 2024-02-27T13:38:48Z
```diff
@@ -723,47 +721,98 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
+// Get the last timestamp on which the pod was observed active:
```

  We can improve the comment according to the guidelines in [Go Doc Comments](https://go.dev/doc/comment):
  ```suggestion
  // lastActiveTime returns the last timestamp on which the pod was observed active:
  ```

- **pkg/controller/jobs/pod/pod_controller.go:739** by [@alexandear](https://github.com/alexandear) — 2024-02-27T13:48:24Z
```diff
@@ -723,47 +721,98 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
+// Get the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
```

  Can we simplify to the following:
  ```suggestion
  		if c.Type == corev1.ContainersReady && c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
  			lastTransition = c.LastTransitionTime
  			break
  		}
  ```
  ?

- **pkg/util/slices/slices.go:87** by [@alexandear](https://github.com/alexandear) — 2024-02-28T11:44:24Z
```diff
@@ -82,3 +82,14 @@ func CmpNoOrder[E comparable, S ~[]E](a, b S) bool {
 	}
 	return true
 }
+
+// Pick creates a new slice containing only the elements for which keep return true.
+func Pick[E any, S ~[]E](s S, keep func(*E) bool) S {
```

  Maybe we should rename this function to `Filter` because it's more common for this kind of operation.
  
  From the https://github.com/kubernetes/utils/blob/e7106e64919e62d73c0926352134656a22dc2e38/strings/slices/slices.go#L39C1-L52C1

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-28T14:24:41Z

- **keps/976-plain-pods/README.md:569** by [@trasc](https://github.com/trasc) — 2024-02-28T11:54:38Z
```diff
@@ -565,25 +565,24 @@ in-memory Workload, then it is considered unmatching and the Workload is evicted
 In the Pod-group reconciler:
 1. If the Pod is not terminated and doesn't have a deletionTimestamp,
    create a Workload for the pod group if one does not exist.
-2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
-   timestamp or is finished.
+2. Remove Pod finalizers if:
+  - The Pod is terminated and the Workload is finished, has a deletion timestamp or is finished.
```

  done

- **keps/976-plain-pods/README.md:585** by [@trasc](https://github.com/trasc) — 2024-02-28T11:54:44Z
```diff
@@ -565,25 +565,24 @@ in-memory Workload, then it is considered unmatching and the Workload is evicted
 In the Pod-group reconciler:
 1. If the Pod is not terminated and doesn't have a deletionTimestamp,
    create a Workload for the pod group if one does not exist.
-2. Remove Pod finalizers if the Pod is terminated and the Workload is finished, has a deletion
-   timestamp or is finished.
+2. Remove Pod finalizers if:
+  - The Pod is terminated and the Workload is finished, has a deletion timestamp or is finished.
+  - The Pod Failed and a valid replacement pod was created for it.
 3. Build the in-memory Workload. If its podset counters are greater than the stored Workload,
    then evict the Workload.
 4. For gated pods:
    - remove the gate, set nodeSelector
 5. If the number of succeeded pods is equal to the admission count, mark the Workload as Finished
    and remove the finalizers from the Pods.
 
-Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way of
-managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
-terminated.
-
 ### Retrying Failed Pods
 
 The Pod group will generally only be considered finished if all the Pods finish with a Succeeded
 phase.
 This allows the user to send replacement Pods when a Pod in the group fails or if the group is
 preempted. The replacement Pods can have any name, but they must point to the same pod group.
+Once a replacement Pod is created, and Kueue has added it as an owner of the Workload, the
+Failed pod will be deleted.
```

  done

- **pkg/controller/jobs/pod/pod_controller.go:785** by [@trasc](https://github.com/trasc) — 2024-02-28T11:55:11Z
```diff
@@ -723,47 +721,98 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
+// Get the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
+	}
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
+	}
+	return deletionTime.Time
+}
+
 // cleanupExcessPods will delete and finalize pods created last if the number of
 // activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
+func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod, inactivePods []corev1.Pod) error {
 	log := ctrl.LoggerFrom(ctx)
 
-	extraPodsCount := len(activePods) - totalCount
+	extraInactivePodsCount := min(len(inactivePods), len(inactivePods)+len(activePods)-totalCount)
+	extraActivePodsCount := len(activePods) - totalCount
 
-	if extraPodsCount <= 0 {
+	if extraActivePodsCount <= 0 && extraInactivePodsCount <= 0 {
 		return nil
 	}
 	// Do not clean up more pods until observing previous operations
 	if !p.satisfiedExcessPods {
 		return errPendingOps
 	}
 
-	// Sort active pods by creation timestamp
-	sort.Slice(activePods, func(i, j int) bool {
-		pi := &activePods[i]
-		pj := &activePods[j]
-		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
-		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
-		// Prefer to keep pods that have a finalizer.
-		if iFin != jFin {
-			return iFin
-		}
-		iGated := gateIndex(pi) != gateNotFound
-		jGated := gateIndex(pj) != gateNotFound
-		// Prefer to keep pods that aren't gated.
-		if iGated != jGated {
-			return !iGated
-		}
-		return pi.ObjectMeta.CreationTimestamp.Before(&pj.ObjectMeta.CreationTimestamp)
-	})
+	var extraPods []corev1.Pod
+
+	// Remove inactive pods that have a replacement
+	if extraInactivePodsCount > 0 {
+		// Sort inactive pods
+		sort.Slice(activePods, func(i, j int) bool {
+			pi := &activePods[i]
+			pj := &activePods[j]
+			iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+			jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+			// Prefer to keep pods that have a finalizer.
+			// Add them to the end of the list.
+			if iFin != jFin {
+				return jFin
+			}
 
-	// Extract all the latest created extra pods
-	extraPods := activePods[len(activePods)-extraPodsCount:]
-	extraPodsUIDs := utilslices.Map(extraPods, func(p *corev1.Pod) types.UID { return p.UID })
-	p.excessPodExpectations.ExpectUIDs(log, p.key, extraPodsUIDs)
+			iLastActive := lastActiveTime(pi)
+			jLastActive := lastActiveTime(pj)
 
-	// Finalize and delete the active pods created last
+			if iLastActive.Equal(jLastActive) {
+				return pi.CreationTimestamp.Before(&pj.CreationTimestamp)
+			}
+			return iLastActive.Before(jLastActive)
+		})
+
+		extraPods = inactivePods[:extraInactivePodsCount]
```

  done

- **pkg/controller/jobs/pod/pod_controller.go:1029** by [@trasc](https://github.com/trasc) — 2024-02-28T11:55:25Z
```diff
@@ -971,22 +1020,26 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
```

  done

- **test/integration/controller/jobs/pod/pod_controller_test.go:776** by [@trasc](https://github.com/trasc) — 2024-02-28T12:52:19Z
```diff
@@ -769,11 +769,20 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				gomega.Expect(k8sClient.Create(ctx, replacementPod)).Should(gomega.Succeed())
 				replacementPodLookupKey := client.ObjectKeyFromObject(replacementPod)
 
+				ginkgo.By("Failing the replacement", func() {
```

  done

- **test/integration/controller/jobs/pod/pod_controller_test.go:788** by [@trasc](https://github.com/trasc) — 2024-02-28T13:04:57Z
```diff
@@ -769,11 +769,20 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				gomega.Expect(k8sClient.Create(ctx, replacementPod)).Should(gomega.Succeed())
 				replacementPodLookupKey := client.ObjectKeyFromObject(replacementPod)
 
+				ginkgo.By("Failing the replacement", func() {
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodFailed, replacementPod)
+				})
+
 				// Workload shouldn't be deleted after replacement pod has been created
 				gomega.Consistently(func() error {
 					return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
 				}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
 
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, podLookupKey, createdPod)).Should(testing.BeNotFoundError())
+					g.Expect(k8sClient.Get(ctx, replacementPodLookupKey, createdPod)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
 				util.ExpectPodUnsuspendedWithNodeSelectors(ctx, k8sClient, replacementPodLookupKey, map[string]string{"kubernetes.io/arch": "arm64"})
 
 				util.SetPodsPhase(ctx, k8sClient, corev1.PodSucceeded, replacementPod)
```

  done

- **pkg/util/slices/slices.go:87** by [@trasc](https://github.com/trasc) — 2024-02-28T14:07:02Z
```diff
@@ -82,3 +82,14 @@ func CmpNoOrder[E comparable, S ~[]E](a, b S) bool {
 	}
 	return true
 }
+
+// Pick creates a new slice containing only the elements for which keep return true.
+func Pick[E any, S ~[]E](s S, keep func(*E) bool) S {
```

  In my opinion filter sold have an effect on the input, it's not the case here.

- **pkg/controller/jobs/pod/pod_controller.go:739** by [@trasc](https://github.com/trasc) — 2024-02-28T14:07:15Z
```diff
@@ -723,47 +721,98 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
+// Get the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
```

  It's not the same, we should stop on `c.Type == corev1.ContainersReady` regardless of the state.

- **pkg/controller/jobs/pod/pod_controller.go:724** by [@trasc](https://github.com/trasc) — 2024-02-28T14:22:38Z
```diff
@@ -723,47 +721,98 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
+// Get the last timestamp on which the pod was observed active:
```

  can you point me to the part from where it results that `lastActiveTime returns ` should be used instead of `Get` in this context.
  done anyway.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-28T17:09:11Z

- **pkg/controller/jobs/pod/pod_controller.go:798** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T16:30:20Z
```diff
@@ -754,16 +790,22 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.E
 		if iGated != jGated {
 			return !iGated
 		}
-		return pi.ObjectMeta.CreationTimestamp.Before(&pj.ObjectMeta.CreationTimestamp)
+		return pi.CreationTimestamp.Before(&pj.CreationTimestamp)
 	})
+}
+
+func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, extraPods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
```

  this can go after the check for `len(extraPods)`

- **pkg/controller/jobs/pod/pod_controller.go:837** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T16:31:13Z
```diff
@@ -788,31 +830,35 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.E
 	if err != nil {
 		return err
 	}
+	return nil
+}
 
-	// Remove excess pods from the group list
-	newPodsInGroup := make([]corev1.Pod, 0, len(p.list.Items)-len(extraPods))
-	for i := range p.list.Items {
-		found := false
-		for j := range extraPods {
-			if p.list.Items[i].Name == extraPods[j].Name && p.list.Items[i].Namespace == extraPods[j].Namespace {
-				found = true
-				break
-			}
-		}
+func finalizePods(ctx context.Context, c client.Client, extraPods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
```

  same here

- **pkg/controller/jobs/pod/pod_controller.go:806** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T16:42:39Z
```diff
@@ -754,16 +790,22 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.E
 		if iGated != jGated {
 			return !iGated
 		}
-		return pi.ObjectMeta.CreationTimestamp.Before(&pj.ObjectMeta.CreationTimestamp)
+		return pi.CreationTimestamp.Before(&pj.CreationTimestamp)
 	})
+}
+
+func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, extraPods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	if len(extraPods) == 0 {
+		return nil
+	}
 
 	// Extract all the latest created extra pods
-	extraPods := activePods[len(activePods)-extraPodsCount:]
 	extraPodsUIDs := utilslices.Map(extraPods, func(p *corev1.Pod) types.UID { return p.UID })
 	p.excessPodExpectations.ExpectUIDs(log, p.key, extraPodsUIDs)
```

  We need to do the same for `finalizePods`. We don't necessarily need to add a new "expectations" field for finalizers, I believe we can use the same one, perhaps renamed to `finalizedPodsExpectations` (because every delete also requires finalizing first).
  
  So if expectations are not satisfied, we don't finalize or delete any more pods.
  
  Please also add an integration test that exercises 2 Failed pods with the same timestamp, and the user only sends one replacement Pod. We need to ensure only one of them gets its finalizer removed.

- **pkg/controller/jobs/pod/pod_controller.go:843** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T16:49:20Z
```diff
@@ -788,31 +830,35 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.E
 	if err != nil {
 		return err
 	}
+	return nil
+}
 
-	// Remove excess pods from the group list
-	newPodsInGroup := make([]corev1.Pod, 0, len(p.list.Items)-len(extraPods))
-	for i := range p.list.Items {
-		found := false
-		for j := range extraPods {
-			if p.list.Items[i].Name == extraPods[j].Name && p.list.Items[i].Namespace == extraPods[j].Namespace {
-				found = true
-				break
-			}
-		}
+func finalizePods(ctx context.Context, c client.Client, extraPods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
 
-		if !found {
-			newPodsInGroup = append(newPodsInGroup, p.list.Items[i])
-		}
+	if len(extraPods) == 0 {
+		return nil
 	}
-	p.list.Items = newPodsInGroup
 
+	// Finalize and delete the active pods created last
```

  comment is inaccurate, but just remove it.

- **pkg/controller/jobs/pod/pod_controller.go:1058** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T16:55:27Z
```diff
@@ -971,37 +1021,73 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableNorSucceededPods()
+
+	var keptPods []corev1.Pod
+	var excessActivePods []corev1.Pod
+	var replacedInactivePods []corev1.Pod
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
+				roleHashErrors = append(roleHashErrors, err)
+				return false
 			}
+			return hash == ps.Name
+		}
+		roleActivePods := utilslices.Pick(activePods, hasRoleFunc)
+		roleInactivePods := utilslices.Pick(inactivePods, hasRoleFunc)
+		if len(roleHashErrors) > 0 {
+			return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", errors.Join(roleHashErrors...))
+		}
 
-			if ps.Name == roleHash {
-				roleActivePods = append(roleActivePods, activePod)
-			}
+		if excessCount := len(roleActivePods) - int(ps.Count); excessCount > 0 {
+			sortActivePods(roleActivePods)
+			excessActivePods = append(excessActivePods, roleActivePods[len(roleActivePods)-excessCount:]...)
+			keptPods = append(keptPods, roleActivePods[:len(roleActivePods)-excessCount]...)
+		} else {
+			keptPods = append(keptPods, roleActivePods...)
 		}
 
-		// Cleanup excess pods of the role
-		err := p.cleanupExcessPods(ctx, c, r, int(ps.Count), roleActivePods)
-		if err != nil {
-			return nil, nil, err
+		if finalzeblePodsCount := min(len(roleInactivePods), len(roleInactivePods)+len(roleActivePods)-int(ps.Count)); finalzeblePodsCount > 0 {
+			sortInactivePods(roleInactivePods)
+			replacedInactivePods = append(replacedInactivePods, roleInactivePods[len(roleInactivePods)-finalzeblePodsCount:]...)
+			keptPods = append(keptPods, roleInactivePods[:len(roleInactivePods)-finalzeblePodsCount]...)
```

  I think we don't need to keep these. `equivalentToWorkload` is resilient to having less Pods than the original Workload specifies.

- **pkg/controller/jobs/pod/pod_controller_test.go:1361** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T17:01:24Z
```diff
@@ -1359,7 +1358,7 @@ func TestReconciler(t *testing.T) {
 					KueueFinalizer().
 					Group("test-group").
 					GroupTotalCount("2").
-					StatusPhase(corev1.PodFailed).
+					StatusPhase(corev1.PodSucceeded).
```

  why change the status of these 2 pods? If not absolutely necessary, please revert.

- **pkg/controller/jobs/pod/pod_controller_test.go:2922** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T17:02:37Z
```diff
@@ -2920,6 +2919,178 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"the failed pods are finalized in order": {
```

  Add a unit test for not finalizing pods when waiting for expectations.
  
  Another one where there are 2 Pods, 1 Pod already has a finalizer removed and there is 1 replacement Pod. No new pod should get a finalizer removed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-28T17:15:31Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:751** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T17:11:34Z
```diff
@@ -748,6 +748,9 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				podLookupKey := types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}
 				util.ExpectPodUnsuspendedWithNodeSelectors(ctx, k8sClient, podLookupKey, map[string]string{"kubernetes.io/arch": "arm64"})
 
+				// cache the uid of the workload it should be the same until the execution ends otherwise the workload was recreated
```

  nice :)

- **test/integration/controller/jobs/pod/pod_controller_test.go:786** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T17:12:51Z
```diff
@@ -770,15 +773,30 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				gomega.Expect(k8sClient.Create(ctx, replacementPod)).Should(gomega.Succeed())
 				replacementPodLookupKey := client.ObjectKeyFromObject(replacementPod)
 
+				ginkgo.By("Failing the replacement", func() {
+					util.ExpectPodsFinalized(ctx, k8sClient, podLookupKey)
+					util.ExpectPodUnsuspendedWithNodeSelectors(ctx, k8sClient, replacementPodLookupKey, map[string]string{"kubernetes.io/arch": "arm64"})
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodFailed, replacementPod)
+				})
+
 				// Workload shouldn't be deleted after replacement pod has been created
-				gomega.Consistently(func() error {
-					return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(createdWorkload.UID).To(gomega.Equal(wlUid))
 				}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  I think we can remove this "consistently" at this point and just check at the very end of the test that the UID didn't change.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-29T09:07:45Z

- **pkg/controller/jobs/pod/pod_controller.go:798** by [@trasc](https://github.com/trasc) — 2024-02-29T06:48:21Z
```diff
@@ -754,16 +790,22 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.E
 		if iGated != jGated {
 			return !iGated
 		}
-		return pi.ObjectMeta.CreationTimestamp.Before(&pj.ObjectMeta.CreationTimestamp)
+		return pi.CreationTimestamp.Before(&pj.CreationTimestamp)
 	})
+}
+
+func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, extraPods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
```

  done

- **pkg/controller/jobs/pod/pod_controller.go:837** by [@trasc](https://github.com/trasc) — 2024-02-29T06:54:44Z
```diff
@@ -788,31 +830,35 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.E
 	if err != nil {
 		return err
 	}
+	return nil
+}
 
-	// Remove excess pods from the group list
-	newPodsInGroup := make([]corev1.Pod, 0, len(p.list.Items)-len(extraPods))
-	for i := range p.list.Items {
-		found := false
-		for j := range extraPods {
-			if p.list.Items[i].Name == extraPods[j].Name && p.list.Items[i].Namespace == extraPods[j].Namespace {
-				found = true
-				break
-			}
-		}
+func finalizePods(ctx context.Context, c client.Client, extraPods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
```

  done

- **pkg/controller/jobs/pod/pod_controller.go:843** by [@trasc](https://github.com/trasc) — 2024-02-29T06:54:52Z
```diff
@@ -788,31 +830,35 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.E
 	if err != nil {
 		return err
 	}
+	return nil
+}
 
-	// Remove excess pods from the group list
-	newPodsInGroup := make([]corev1.Pod, 0, len(p.list.Items)-len(extraPods))
-	for i := range p.list.Items {
-		found := false
-		for j := range extraPods {
-			if p.list.Items[i].Name == extraPods[j].Name && p.list.Items[i].Namespace == extraPods[j].Namespace {
-				found = true
-				break
-			}
-		}
+func finalizePods(ctx context.Context, c client.Client, extraPods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
 
-		if !found {
-			newPodsInGroup = append(newPodsInGroup, p.list.Items[i])
-		}
+	if len(extraPods) == 0 {
+		return nil
 	}
-	p.list.Items = newPodsInGroup
 
+	// Finalize and delete the active pods created last
```

  removed

- **pkg/controller/jobs/pod/pod_controller.go:1058** by [@trasc](https://github.com/trasc) — 2024-02-29T06:56:45Z
```diff
@@ -971,37 +1021,73 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableNorSucceededPods()
+
+	var keptPods []corev1.Pod
+	var excessActivePods []corev1.Pod
+	var replacedInactivePods []corev1.Pod
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
+				roleHashErrors = append(roleHashErrors, err)
+				return false
 			}
+			return hash == ps.Name
+		}
+		roleActivePods := utilslices.Pick(activePods, hasRoleFunc)
+		roleInactivePods := utilslices.Pick(inactivePods, hasRoleFunc)
+		if len(roleHashErrors) > 0 {
+			return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", errors.Join(roleHashErrors...))
+		}
 
-			if ps.Name == roleHash {
-				roleActivePods = append(roleActivePods, activePod)
-			}
+		if excessCount := len(roleActivePods) - int(ps.Count); excessCount > 0 {
+			sortActivePods(roleActivePods)
+			excessActivePods = append(excessActivePods, roleActivePods[len(roleActivePods)-excessCount:]...)
+			keptPods = append(keptPods, roleActivePods[:len(roleActivePods)-excessCount]...)
+		} else {
+			keptPods = append(keptPods, roleActivePods...)
 		}
 
-		// Cleanup excess pods of the role
-		err := p.cleanupExcessPods(ctx, c, r, int(ps.Count), roleActivePods)
-		if err != nil {
-			return nil, nil, err
+		if finalzeblePodsCount := min(len(roleInactivePods), len(roleInactivePods)+len(roleActivePods)-int(ps.Count)); finalzeblePodsCount > 0 {
+			sortInactivePods(roleInactivePods)
+			replacedInactivePods = append(replacedInactivePods, roleInactivePods[len(roleInactivePods)-finalzeblePodsCount:]...)
+			keptPods = append(keptPods, roleInactivePods[:len(roleInactivePods)-finalzeblePodsCount]...)
```

  the keptPods are used beyond the equivalency test , they are set as the pods group `list.Items` and used in detecting the finish condition for instance.

- **pkg/controller/jobs/pod/pod_controller_test.go:1361** by [@trasc](https://github.com/trasc) — 2024-02-29T07:13:15Z
```diff
@@ -1359,7 +1358,7 @@ func TestReconciler(t *testing.T) {
 					KueueFinalizer().
 					Group("test-group").
 					GroupTotalCount("2").
-					StatusPhase(corev1.PodFailed).
+					StatusPhase(corev1.PodSucceeded).
```

  done

- **pkg/controller/jobs/pod/pod_controller_test.go:2922** by [@trasc](https://github.com/trasc) — 2024-02-29T07:34:26Z
```diff
@@ -2920,6 +2919,178 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"the failed pods are finalized in order": {
```

  done

- **test/integration/controller/jobs/pod/pod_controller_test.go:786** by [@trasc](https://github.com/trasc) — 2024-02-29T07:45:12Z
```diff
@@ -770,15 +773,30 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 				gomega.Expect(k8sClient.Create(ctx, replacementPod)).Should(gomega.Succeed())
 				replacementPodLookupKey := client.ObjectKeyFromObject(replacementPod)
 
+				ginkgo.By("Failing the replacement", func() {
+					util.ExpectPodsFinalized(ctx, k8sClient, podLookupKey)
+					util.ExpectPodUnsuspendedWithNodeSelectors(ctx, k8sClient, replacementPodLookupKey, map[string]string{"kubernetes.io/arch": "arm64"})
+					util.SetPodsPhase(ctx, k8sClient, corev1.PodFailed, replacementPod)
+				})
+
 				// Workload shouldn't be deleted after replacement pod has been created
-				gomega.Consistently(func() error {
-					return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+				gomega.Consistently(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+					g.Expect(createdWorkload.UID).To(gomega.Equal(wlUid))
 				}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
```

  done

- **pkg/controller/jobs/pod/pod_controller.go:806** by [@trasc](https://github.com/trasc) — 2024-02-29T09:01:05Z
```diff
@@ -754,16 +790,22 @@ func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.E
 		if iGated != jGated {
 			return !iGated
 		}
-		return pi.ObjectMeta.CreationTimestamp.Before(&pj.ObjectMeta.CreationTimestamp)
+		return pi.CreationTimestamp.Before(&pj.CreationTimestamp)
 	})
+}
+
+func (p *Pod) removeExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, extraPods []corev1.Pod) error {
+	log := ctrl.LoggerFrom(ctx)
+
+	if len(extraPods) == 0 {
+		return nil
+	}
 
 	// Extract all the latest created extra pods
-	extraPods := activePods[len(activePods)-extraPodsCount:]
 	extraPodsUIDs := utilslices.Map(extraPods, func(p *corev1.Pod) types.UID { return p.UID })
 	p.excessPodExpectations.ExpectUIDs(log, p.key, extraPodsUIDs)
```

  >  We need to do the same for finalizePods. We don't necessarily need to add a new "expectations" field for finalizers....
  
  done
  
  > Please also add an integration test that exercise
  
  The new UT  `no unnecessary additional failed pods are finalized` should cover this, as is not considering `finished-with-error`  to be finalized even if it failed before `finished-with-error-no-finalizer` which has no finalizer (was the chosen one to be finalized in a previous reconciliation).

### Review by [@alexandear](https://github.com/alexandear) (commented) — 2024-02-29T12:33:24Z

- **pkg/controller/jobs/pod/pod_controller.go:714** by [@alexandear](https://github.com/alexandear) — 2024-02-29T12:22:55Z
```diff
@@ -704,14 +708,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 
 // runnableOrSucceededPods returns a slice of active pods in the group
 func (p *Pod) runnableOrSucceededPods() []corev1.Pod {
-	activePods := make([]corev1.Pod, 0, len(p.list.Items))
-	for _, pod := range p.list.Items {
-		if isPodRunnableOrSucceeded(&pod) {
-			activePods = append(activePods, pod)
-		}
-	}
+	return utilslices.Pick(p.list.Items, isPodRunnableOrSucceeded)
+}
 
-	return activePods
+// notRunnableandAndNotSucceededPods returns a slice of inactive pods in the group
```

  Could you be so kind as to fix this comment?
  ```suggestion
  // notRunnableNorSucceededPods returns a slice of inactive pods in the group.
  ```

- **pkg/controller/jobs/pod/pod_controller.go:949** by [@alexandear](https://github.com/alexandear) — 2024-02-29T12:24:40Z
```diff
@@ -893,9 +946,13 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	}
 
 	// Cleanup extra pods if there's any
-	err = p.cleanupExcessPods(ctx, c, r, groupTotalCount, activePods)
-	if err != nil {
-		return nil, err
+	if excesPodsCount := len(activePods) - groupTotalCount; excesPodsCount > 0 {
```

  Please fix a typo:
  ```suggestion
  	if excessPodsCount := len(activePods) - groupTotalCount; excessPodsCount > 0 {
  ```

- **pkg/controller/jobs/pod/pod_controller.go:1043** by [@alexandear](https://github.com/alexandear) — 2024-02-29T12:31:35Z
```diff
@@ -971,37 +1028,73 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableNorSucceededPods()
+
+	var keptPods []corev1.Pod
+	var excessActivePods []corev1.Pod
+	var replacedInactivePods []corev1.Pod
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
+				roleHashErrors = append(roleHashErrors, err)
```

  Looks like we can use `errors.Join` here instead of `[]error`:
  
  ```go
  		var roleHashErr error
  		hasRoleFunc := func(p *corev1.Pod) bool {
  			hash, err := getRoleHash(*p)
  			if err != nil {
  				roleHashErr = errors.Join(roleHashErr, err)
  				return false
  			}
  			return hash == ps.Name
  		}
  		roleActivePods := utilslices.Pick(activePods, hasRoleFunc)
  		roleInactivePods := utilslices.Pick(inactivePods, hasRoleFunc)
  		if roleHashErr != nil {
  			return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", roleHashErr)
  		}
  ```
  
  Of course, we can rename `ruleHashErr` to `ruleHashErrs`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-29T14:11:30Z

- **pkg/controller/jobs/pod/pod_controller.go:1043** by [@trasc](https://github.com/trasc) — 2024-02-29T14:11:30Z
```diff
@@ -971,37 +1028,73 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableNorSucceededPods()
+
+	var keptPods []corev1.Pod
+	var excessActivePods []corev1.Pod
+	var replacedInactivePods []corev1.Pod
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
+				roleHashErrors = append(roleHashErrors, err)
```

  please check the implementation of `errors.Join`, there is no reason to run the extra logic .

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-29T14:53:16Z

- **pkg/controller/jobs/pod/pod_controller.go:714** by [@trasc](https://github.com/trasc) — 2024-02-29T14:53:16Z
```diff
@@ -704,14 +708,12 @@ func (p *Pod) validatePodGroupMetadata(r record.EventRecorder, activePods []core
 
 // runnableOrSucceededPods returns a slice of active pods in the group
 func (p *Pod) runnableOrSucceededPods() []corev1.Pod {
-	activePods := make([]corev1.Pod, 0, len(p.list.Items))
-	for _, pod := range p.list.Items {
-		if isPodRunnableOrSucceeded(&pod) {
-			activePods = append(activePods, pod)
-		}
-	}
+	return utilslices.Pick(p.list.Items, isPodRunnableOrSucceeded)
+}
 
-	return activePods
+// notRunnableandAndNotSucceededPods returns a slice of inactive pods in the group
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-29T14:53:25Z

- **pkg/controller/jobs/pod/pod_controller.go:949** by [@trasc](https://github.com/trasc) — 2024-02-29T14:53:25Z
```diff
@@ -893,9 +946,13 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	}
 
 	// Cleanup extra pods if there's any
-	err = p.cleanupExcessPods(ctx, c, r, groupTotalCount, activePods)
-	if err != nil {
-		return nil, err
+	if excesPodsCount := len(activePods) - groupTotalCount; excesPodsCount > 0 {
```

  done

### Review by [@alexandear](https://github.com/alexandear) (commented) — 2024-02-29T15:42:05Z

- **pkg/controller/jobs/pod/pod_controller.go:1043** by [@alexandear](https://github.com/alexandear) — 2024-02-29T15:42:05Z
```diff
@@ -971,37 +1028,73 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableNorSucceededPods()
+
+	var keptPods []corev1.Pod
+	var excessActivePods []corev1.Pod
+	var replacedInactivePods []corev1.Pod
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
+				roleHashErrors = append(roleHashErrors, err)
```

  In fact, there won't be any additional logic required because we have a minimal number of errors. 
  
  Let's investigate when we encounter `roleHashError`. The `getRoleHash` function only returns an error if `json.Marshal` fails. In our scenario, `json.Marshal` will seldom fail. Therefore, we can use `[]error` or `errors.Join` interchangeably. However, `errors.Join` has advantages - it provides cleaner code and its use will be consistent with the rest of the code.
  
  However, the decision to use `errors.Join` is ultimately up to you.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-04T18:53:00Z

nits just for future reference.

/lgtm
/approve
/cherry-pick release-0.6

- **pkg/controller/jobs/pod/pod_controller.go:748** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T18:33:08Z
```diff
@@ -723,21 +725,55 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
-// cleanupExcessPods will delete and finalize pods created last if the number of
-// activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	extraPodsCount := len(activePods) - totalCount
-
-	if extraPodsCount <= 0 {
-		return nil
+// lastActiveTime returns the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
 	}
-	// Do not clean up more pods until observing previous operations
-	if !p.satisfiedExcessPods {
-		return errPendingOps
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
 	}
+	return deletionTime.Time
+}
 
+// sortInactivePods - sorts the provided pods slice base on:
```

  ```suggestion
  // sortInactivePods sorts the provided pods slice base on:
  ```

- **pkg/controller/jobs/pod/pod_controller.go:750** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T18:33:15Z
```diff
@@ -723,21 +725,55 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
-// cleanupExcessPods will delete and finalize pods created last if the number of
-// activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	extraPodsCount := len(activePods) - totalCount
-
-	if extraPodsCount <= 0 {
-		return nil
+// lastActiveTime returns the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
 	}
-	// Do not clean up more pods until observing previous operations
-	if !p.satisfiedExcessPods {
-		return errPendingOps
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
 	}
+	return deletionTime.Time
+}
 
+// sortInactivePods - sorts the provided pods slice base on:
+// - finalizer state (pods with finalizers are first)
+// - lastActiveTime (pods that ware active last are first)
```

  ```suggestion
  // - lastActiveTime (pods that were active last are first)
  ```

- **pkg/controller/jobs/pod/pod_controller.go:772** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T18:33:47Z
```diff
@@ -723,21 +725,55 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
-// cleanupExcessPods will delete and finalize pods created last if the number of
-// activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	extraPodsCount := len(activePods) - totalCount
-
-	if extraPodsCount <= 0 {
-		return nil
+// lastActiveTime returns the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
 	}
-	// Do not clean up more pods until observing previous operations
-	if !p.satisfiedExcessPods {
-		return errPendingOps
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
 	}
+	return deletionTime.Time
+}
 
+// sortInactivePods - sorts the provided pods slice base on:
+// - finalizer state (pods with finalizers are first)
+// - lastActiveTime (pods that ware active last are first)
+// - creation timestamp (newer pods are first)
+func sortInactivePods(inactivePods []corev1.Pod) {
+	sort.Slice(inactivePods, func(i, j int) bool {
+		pi := &inactivePods[i]
+		pj := &inactivePods[j]
+		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+		if iFin != jFin {
+			return iFin
+		}
+
+		iLastActive := lastActiveTime(pi)
+		jLastActive := lastActiveTime(pj)
+
+		if iLastActive.Equal(jLastActive) {
+			return pi.CreationTimestamp.Before(&pj.CreationTimestamp)
+		}
+		return jLastActive.Before(iLastActive)
+	})
+}
+
+// sortActivePods - sorts the provided pods slice base on:
```

  ```suggestion
  // sortActivePods sorts the provided pods slice based on:
  ```

- **pkg/controller/jobs/pod/pod_controller.go:1043** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T18:42:23Z
```diff
@@ -971,37 +1028,73 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableNorSucceededPods()
+
+	var keptPods []corev1.Pod
+	var excessActivePods []corev1.Pod
+	var replacedInactivePods []corev1.Pod
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
+				roleHashErrors = append(roleHashErrors, err)
```

  It would be nicer if `errors.Join` appended to the existing `joinError` array instead of creating a new one each time you call it. So for now it seems better to use an array (not just here, but in any case were we need to concatenate errors).

- **pkg/controller/jobs/pod/pod_controller.go:1062** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T18:44:23Z
```diff
@@ -971,37 +1028,73 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableNorSucceededPods()
+
+	var keptPods []corev1.Pod
+	var excessActivePods []corev1.Pod
+	var replacedInactivePods []corev1.Pod
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
+				roleHashErrors = append(roleHashErrors, err)
+				return false
 			}
+			return hash == ps.Name
+		}
+		roleActivePods := utilslices.Pick(activePods, hasRoleFunc)
+		roleInactivePods := utilslices.Pick(inactivePods, hasRoleFunc)
+		if len(roleHashErrors) > 0 {
+			return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", errors.Join(roleHashErrors...))
+		}
 
-			if ps.Name == roleHash {
-				roleActivePods = append(roleActivePods, activePod)
-			}
+		if excessCount := len(roleActivePods) - int(ps.Count); excessCount > 0 {
+			sortActivePods(roleActivePods)
+			excessActivePods = append(excessActivePods, roleActivePods[len(roleActivePods)-excessCount:]...)
+			keptPods = append(keptPods, roleActivePods[:len(roleActivePods)-excessCount]...)
+		} else {
+			keptPods = append(keptPods, roleActivePods...)
 		}
 
-		// Cleanup excess pods of the role
-		err := p.cleanupExcessPods(ctx, c, r, int(ps.Count), roleActivePods)
-		if err != nil {
-			return nil, nil, err
+		if finalzeblePodsCount := min(len(roleInactivePods), len(roleInactivePods)+len(roleActivePods)-int(ps.Count)); finalzeblePodsCount > 0 {
```

  nit: `finalizeablePodsCount`

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-04T19:32:46Z

- **pkg/controller/jobs/pod/pod_controller.go:748** by [@trasc](https://github.com/trasc) — 2024-03-04T19:16:41Z
```diff
@@ -723,21 +725,55 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
-// cleanupExcessPods will delete and finalize pods created last if the number of
-// activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	extraPodsCount := len(activePods) - totalCount
-
-	if extraPodsCount <= 0 {
-		return nil
+// lastActiveTime returns the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
 	}
-	// Do not clean up more pods until observing previous operations
-	if !p.satisfiedExcessPods {
-		return errPendingOps
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
 	}
+	return deletionTime.Time
+}
 
+// sortInactivePods - sorts the provided pods slice base on:
```

  done

- **pkg/controller/jobs/pod/pod_controller.go:750** by [@trasc](https://github.com/trasc) — 2024-03-04T19:16:49Z
```diff
@@ -723,21 +725,55 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
-// cleanupExcessPods will delete and finalize pods created last if the number of
-// activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	extraPodsCount := len(activePods) - totalCount
-
-	if extraPodsCount <= 0 {
-		return nil
+// lastActiveTime returns the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
 	}
-	// Do not clean up more pods until observing previous operations
-	if !p.satisfiedExcessPods {
-		return errPendingOps
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
 	}
+	return deletionTime.Time
+}
 
+// sortInactivePods - sorts the provided pods slice base on:
+// - finalizer state (pods with finalizers are first)
+// - lastActiveTime (pods that ware active last are first)
```

  done

- **pkg/controller/jobs/pod/pod_controller.go:772** by [@trasc](https://github.com/trasc) — 2024-03-04T19:17:37Z
```diff
@@ -723,21 +725,55 @@ func isPodRunnableOrSucceeded(p *corev1.Pod) bool {
 	return p.Status.Phase != corev1.PodFailed
 }
 
-// cleanupExcessPods will delete and finalize pods created last if the number of
-// activePods is greater than the totalCount value.
-func (p *Pod) cleanupExcessPods(ctx context.Context, c client.Client, r record.EventRecorder, totalCount int, activePods []corev1.Pod) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	extraPodsCount := len(activePods) - totalCount
-
-	if extraPodsCount <= 0 {
-		return nil
+// lastActiveTime returns the last timestamp on which the pod was observed active:
+// - the time the pod was declared Failed
+// - the deletion time
+func lastActiveTime(p *corev1.Pod) time.Time {
+	lastTransition := metav1.Now()
+	for _, c := range p.Status.Conditions {
+		if c.Type == corev1.ContainersReady {
+			if c.Status == corev1.ConditionFalse && c.Reason == string(corev1.PodFailed) {
+				lastTransition = c.LastTransitionTime
+			}
+			break
+		}
 	}
-	// Do not clean up more pods until observing previous operations
-	if !p.satisfiedExcessPods {
-		return errPendingOps
+	deletionTime := ptr.Deref(p.DeletionTimestamp, metav1.Now())
+	if lastTransition.Before(&deletionTime) {
+		return lastTransition.Time
 	}
+	return deletionTime.Time
+}
 
+// sortInactivePods - sorts the provided pods slice base on:
+// - finalizer state (pods with finalizers are first)
+// - lastActiveTime (pods that ware active last are first)
+// - creation timestamp (newer pods are first)
+func sortInactivePods(inactivePods []corev1.Pod) {
+	sort.Slice(inactivePods, func(i, j int) bool {
+		pi := &inactivePods[i]
+		pj := &inactivePods[j]
+		iFin := slices.Contains(pi.Finalizers, PodFinalizer)
+		jFin := slices.Contains(pj.Finalizers, PodFinalizer)
+		if iFin != jFin {
+			return iFin
+		}
+
+		iLastActive := lastActiveTime(pi)
+		jLastActive := lastActiveTime(pj)
+
+		if iLastActive.Equal(jLastActive) {
+			return pi.CreationTimestamp.Before(&pj.CreationTimestamp)
+		}
+		return jLastActive.Before(iLastActive)
+	})
+}
+
+// sortActivePods - sorts the provided pods slice base on:
```

  done

- **pkg/controller/jobs/pod/pod_controller.go:1062** by [@trasc](https://github.com/trasc) — 2024-03-04T19:18:24Z
```diff
@@ -971,37 +1028,73 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 
 	// Cleanup excess pods for each workload pod set (role)
 	activePods := p.runnableOrSucceededPods()
+	inactivePods := p.notRunnableNorSucceededPods()
+
+	var keptPods []corev1.Pod
+	var excessActivePods []corev1.Pod
+	var replacedInactivePods []corev1.Pod
+
 	for _, ps := range workload.Spec.PodSets {
-		// Find all the active pods of the role
-		var roleActivePods []corev1.Pod
-		for _, activePod := range activePods {
-			roleHash, err := getRoleHash(activePod)
+		// Find all the active and inactive pods of the role
+		var roleHashErrors []error
+		hasRoleFunc := func(p *corev1.Pod) bool {
+			hash, err := getRoleHash(*p)
 			if err != nil {
-				return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", err)
+				roleHashErrors = append(roleHashErrors, err)
+				return false
 			}
+			return hash == ps.Name
+		}
+		roleActivePods := utilslices.Pick(activePods, hasRoleFunc)
+		roleInactivePods := utilslices.Pick(inactivePods, hasRoleFunc)
+		if len(roleHashErrors) > 0 {
+			return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", errors.Join(roleHashErrors...))
+		}
 
-			if ps.Name == roleHash {
-				roleActivePods = append(roleActivePods, activePod)
-			}
+		if excessCount := len(roleActivePods) - int(ps.Count); excessCount > 0 {
+			sortActivePods(roleActivePods)
+			excessActivePods = append(excessActivePods, roleActivePods[len(roleActivePods)-excessCount:]...)
+			keptPods = append(keptPods, roleActivePods[:len(roleActivePods)-excessCount]...)
+		} else {
+			keptPods = append(keptPods, roleActivePods...)
 		}
 
-		// Cleanup excess pods of the role
-		err := p.cleanupExcessPods(ctx, c, r, int(ps.Count), roleActivePods)
-		if err != nil {
-			return nil, nil, err
+		if finalzeblePodsCount := min(len(roleInactivePods), len(roleInactivePods)+len(roleActivePods)-int(ps.Count)); finalzeblePodsCount > 0 {
```

  done
