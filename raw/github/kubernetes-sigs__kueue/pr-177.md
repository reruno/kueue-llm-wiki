# PR #177: Add ClusterRoles and task on how to use them

**Summary**: Add ClusterRoles and task on how to use them

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/177

**Last updated**: 2022-04-06T18:40:26Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `rbac` → `main`
- **Created**: 2022-04-05T21:23:30Z
- **Updated**: 2022-04-06T18:40:26Z
- **Merged**: 2022-04-06T18:40:26Z
- **Closed**: 2022-04-06T18:40:26Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 15   **+198 / -12**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

- Adds roles `kueue-batch-admin-role` and `kueue-batch-user-role` to simplify setting common permissions.
- Add a task that explains how to use them.

#### Which issue(s) this PR fixes:

Fixes #64 

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-05T21:23:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/177#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T18:08:33Z

looks good to me

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T18:10:41Z

squashed into 2 commits

/hold cancel

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T18:38:18Z

/lgtm

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-05T22:05:22Z

- **docs/tasks/setup_rbac.md:20** by [@ahg-g](https://github.com/ahg-g) — 2022-04-05T21:50:17Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
```

  `for use by Kueue controllers`

- **docs/tasks/setup_rbac.md:31** by [@ahg-g](https://github.com/ahg-g) — 2022-04-05T21:51:15Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
```

  `created for use by the two main personas that we assume will interact with Kueue`

- **docs/tasks/setup_rbac.md:33** by [@ahg-g](https://github.com/ahg-g) — 2022-04-05T21:55:46Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
+
+- `kueue-batch-admin-role` includes the permissions of all the other ClusterRoles.
```

  ```suggestion
  - `kueue-batch-admin-role` includes the permissions to manage most related resources such as ClusterQueues, Queues and QueuedWorkloads.
  ```

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2022-04-06T00:09:56Z

- **docs/tasks/setup_rbac.md:4** by [@cortespao](https://github.com/cortespao) — 2022-04-05T23:56:43Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
```

  ```suggestion
  to control the types of users that can view and create Kueue objects.
  ```

- **docs/tasks/setup_rbac.md:6** by [@cortespao](https://github.com/cortespao) — 2022-04-05T23:59:54Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
```

  ```suggestion
  This page is intended for [batch administrator](/docs/tasks#batch-administrator).
  ```

- **docs/tasks/setup_rbac.md:16** by [@cortespao](https://github.com/cortespao) — 2022-04-06T00:00:16Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
```

  ```suggestion
  This page assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
  ```

- **docs/tasks/setup_rbac.md:31** by [@cortespao](https://github.com/cortespao) — 2022-04-06T00:00:53Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
```

  ```suggestion
  And two coarse-grained ClusterRoles are created:
  ```

- **docs/tasks/setup_rbac.md:33** by [@cortespao](https://github.com/cortespao) — 2022-04-06T00:01:38Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
+
+- `kueue-batch-admin-role` includes the permissions of all the other ClusterRoles.
```

  On top of @ahg-g suggestion:
  "ClusterQueues, Queues, and QueuedWorkloads"

- **docs/tasks/setup_rbac.md:44** by [@cortespao](https://github.com/cortespao) — 2022-04-06T00:05:24Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
+
+- `kueue-batch-admin-role` includes the permissions of all the other ClusterRoles.
+- `kueue-batch-user-role` includes the permissions to view and edit [Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/)
+  and the permissions of:
+  - kueue-queue-viewer-role
+  - kueue-queuedworkload-viewer-role
+
+## Giving permissions to a batch administrator
+
+A batch administrator typically requires the `kueue-batch-admin-role` ClusterRole
+for all the namespaces.
+
+To bind the role to a batch administrator, represented by the user
```

  ```suggestion
  To bind the `kueue-batch-admin-role` role to a batch administrator, represented by the user
  ```

- **docs/tasks/setup_rbac.md:78** by [@cortespao](https://github.com/cortespao) — 2022-04-06T00:09:39Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
+
+- `kueue-batch-admin-role` includes the permissions of all the other ClusterRoles.
+- `kueue-batch-user-role` includes the permissions to view and edit [Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/)
+  and the permissions of:
+  - kueue-queue-viewer-role
+  - kueue-queuedworkload-viewer-role
+
+## Giving permissions to a batch administrator
+
+A batch administrator typically requires the `kueue-batch-admin-role` ClusterRole
+for all the namespaces.
+
+To bind the role to a batch administrator, represented by the user
+`admin@example.com`, create a ClusterRoleBinding with a manifest similar to the
+following:
+
+```yaml
+# batch-admin-role-binding.yaml
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRoleBinding
+metadata:
+  name: read-pods
+subjects:
+- kind: User
+  name: admin@example.com
+  apiGroup: rbac.authorization.k8s.io
+roleRef:
+  kind: ClusterRole
+  name: kueue-batch-admin-role
+  apiGroup: rbac.authorization.k8s.io
+```
+
+To create the ClusterRoleBinding, save the preceding manifest and run the
+following command:
+
+```shell
+kubectl apply -f batch-admin-role-binding.yaml
+```
+
+## Giving permissions to a batch user
+
+A batch user typically requires permissions to:
+- Create and view Jobs in their namespace.
+- View the queues available in their namespace.
+- View the status of their [workloads](/docs/concepts/queued_workload.md) in their namespace.
+
+To give this permissions to a group of users `team-a@example.com` for the
```

  ```suggestion
  To give these permissions to a group of users `team-a@example.com` for the
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-06T14:01:05Z

- **docs/tasks/setup_rbac.md:20** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T13:48:43Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
```

  Not really. The kueue controller manager has its own role. These are intended for people who really want to fine-tune RBAC. I added this detail.

- **docs/tasks/setup_rbac.md:4** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T13:48:50Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
```

  Done

- **docs/tasks/setup_rbac.md:6** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T13:48:55Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
```

  Done

- **docs/tasks/setup_rbac.md:16** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T13:49:02Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
```

  Done

- **docs/tasks/setup_rbac.md:31** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T13:55:12Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
```

  Done

- **docs/tasks/setup_rbac.md:31** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T13:55:45Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
```

  Done, in combination with @ahg-g's suggestion

- **docs/tasks/setup_rbac.md:33** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T13:57:42Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
+
+- `kueue-batch-admin-role` includes the permissions of all the other ClusterRoles.
```

  I didn't get the "most related" part. See update.

- **docs/tasks/setup_rbac.md:78** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T14:00:40Z
```diff
@@ -0,0 +1,108 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control what different types of users can view and create.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This doc assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install kueue, the following fine-grained ClusterRoles are created:
+
+- kueue-clusterqueue-editor-role
+- kueue-clusterqueue-viewer-role
+- kueue-queue-editor-role
+- kueue-queue-viewer-role
+- kueue-queuedworkload-editor-role
+- kueue-queuedworkload-viewer-role
+- kueue-resourceflavor-editor-role
+- kueue-resourceflavor-viewer-role
+
+And two coarse-grained ClusterRoles:
+
+- `kueue-batch-admin-role` includes the permissions of all the other ClusterRoles.
+- `kueue-batch-user-role` includes the permissions to view and edit [Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/)
+  and the permissions of:
+  - kueue-queue-viewer-role
+  - kueue-queuedworkload-viewer-role
+
+## Giving permissions to a batch administrator
+
+A batch administrator typically requires the `kueue-batch-admin-role` ClusterRole
+for all the namespaces.
+
+To bind the role to a batch administrator, represented by the user
+`admin@example.com`, create a ClusterRoleBinding with a manifest similar to the
+following:
+
+```yaml
+# batch-admin-role-binding.yaml
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRoleBinding
+metadata:
+  name: read-pods
+subjects:
+- kind: User
+  name: admin@example.com
+  apiGroup: rbac.authorization.k8s.io
+roleRef:
+  kind: ClusterRole
+  name: kueue-batch-admin-role
+  apiGroup: rbac.authorization.k8s.io
+```
+
+To create the ClusterRoleBinding, save the preceding manifest and run the
+following command:
+
+```shell
+kubectl apply -f batch-admin-role-binding.yaml
+```
+
+## Giving permissions to a batch user
+
+A batch user typically requires permissions to:
+- Create and view Jobs in their namespace.
+- View the queues available in their namespace.
+- View the status of their [workloads](/docs/concepts/queued_workload.md) in their namespace.
+
+To give this permissions to a group of users `team-a@example.com` for the
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-06T16:18:17Z

- **docs/tasks/setup_rbac.md:22** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T16:11:11Z
```diff
@@ -0,0 +1,112 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control the types of users that can view and create Kueue objects.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This page assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install Kueue, a set of fine-grained ClusterRoles are created, to
+allow you to assign specific permissions to users. These ClusterRoles are the
+following:
```

  ok, I thought those were the roles created for the controllers; 
  
  We don't need mention those at all, it is simpler and more to the point if we only present the coarse-grained roles related to the two personas we have.

- **docs/tasks/setup_rbac.md:22** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T16:15:28Z
```diff
@@ -0,0 +1,112 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control the types of users that can view and create Kueue objects.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This page assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install Kueue, a set of fine-grained ClusterRoles are created, to
+allow you to assign specific permissions to users. These ClusterRoles are the
+following:
```

  but if you insist on keeping it
  
  ```suggestion
  When you install Kueue, the following set of fine-grained ClusterRoles are created to
  allow you to assign specific permissions to users:
  ```

- **docs/tasks/setup_rbac.md:36** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T16:16:36Z
```diff
@@ -0,0 +1,112 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control the types of users that can view and create Kueue objects.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This page assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install Kueue, a set of fine-grained ClusterRoles are created, to
+allow you to assign specific permissions to users. These ClusterRoles are the
+following:
+
+- `kueue-clusterqueue-editor-role`
+- `kueue-clusterqueue-viewer-role`
+- `kueue-job-editor-role`
+- `kueue-job-viewer-role`
+- `kueue-queue-editor-role`
+- `kueue-queue-viewer-role`
+- `kueue-queuedworkload-editor-role`
+- `kueue-queuedworkload-viewer-role`
+- `kueue-resourceflavor-editor-role`
+- `kueue-resourceflavor-viewer-role`
+
+And two coarse-grained ClusterRoles are created for the two main personas that
+we assume will interact with Kueue. These ClusterRoles are the following:
```

  ```suggestion
  Moreover, the following two coarse-grained ClusterRoles are created for the two main personas that
  we assume will interact with Kueue:
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-06T17:12:55Z

/hold
for squash

- **docs/tasks/setup_rbac.md:22** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T17:11:39Z
```diff
@@ -0,0 +1,112 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control the types of users that can view and create Kueue objects.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This page assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install Kueue, a set of fine-grained ClusterRoles are created, to
+allow you to assign specific permissions to users. These ClusterRoles are the
+following:
```

  Removed

- **docs/tasks/setup_rbac.md:36** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T17:12:22Z
```diff
@@ -0,0 +1,112 @@
+# Manage role-based access control
+
+This page shows you how to setup role-based acess control (RBAC) in your cluster
+to control the types of users that can view and create Kueue objects.
+
+The page is intended for a [batch administrator](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/README.md#installation).
+
+This page assumes you are already familiar with [RBAC in kubernetes](https://kubernetes.io/docs/reference/access-authn-authz/rbac/).
+
+## ClusterRoles included in the installation
+
+When you install Kueue, a set of fine-grained ClusterRoles are created, to
+allow you to assign specific permissions to users. These ClusterRoles are the
+following:
+
+- `kueue-clusterqueue-editor-role`
+- `kueue-clusterqueue-viewer-role`
+- `kueue-job-editor-role`
+- `kueue-job-viewer-role`
+- `kueue-queue-editor-role`
+- `kueue-queue-viewer-role`
+- `kueue-queuedworkload-editor-role`
+- `kueue-queuedworkload-viewer-role`
+- `kueue-resourceflavor-editor-role`
+- `kueue-resourceflavor-viewer-role`
+
+And two coarse-grained ClusterRoles are created for the two main personas that
+we assume will interact with Kueue. These ClusterRoles are the following:
```

  Done
