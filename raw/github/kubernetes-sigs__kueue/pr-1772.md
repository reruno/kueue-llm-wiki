# PR #1772: [doc]Split MultiKueue documentation.

**Summary**: [doc]Split MultiKueue documentation.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1772

**Last updated**: 2024-03-12T07:27:10Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `doc-multikueue-tasks` → `main`
- **Created**: 2024-02-27T11:32:36Z
- **Updated**: 2024-03-12T07:27:10Z
- **Merged**: 2024-03-11T14:50:04Z
- **Closed**: 2024-03-11T14:50:04Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 4   **+274 / -36**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Split the MultiKueue documentation in `Overview`(Concepts) and `Setup`(Tasks) sections.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-27T11:32:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 32747cb7c511ee46c999ecda4180572837c768c2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65eec37a51b2830008b3d0d0 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1772--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1772--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE3NzItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.NA2TPG0-ARWnrnbzUhuVDQUltgTr3xBVtim5acwj71A)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T19:52:08Z

/assign @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-11T14:25:38Z

LGTM label has been added.  <details>Git tree hash: 97a0b21eb24ae8e01b783cde4274b4a6f969ed48</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-11T14:25:41Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1772#pullrequestreview-1927983716" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1772#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-11T14:26:26Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-03-11T14:26:29Z

@alculquicondor: once the present PR merges, I will cherry-pick it on top of website in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1772#issuecomment-1988571151):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-03-11T14:50:43Z

@alculquicondor: new pull request created: #1818

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1772#issuecomment-1988571151):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-28T12:53:03Z

- **site/content/en/docs/tasks/setup_multikueue.md:6** by [@mimowo](https://github.com/mimowo) — 2024-02-28T10:36:34Z
```diff
@@ -0,0 +1,24 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
```

  I think it would be preferable to make the documentation step-by-step (command-by-command) tutorial of what needs to be done. I think it is fine to start assuming the clusters as created and running, but then we should leave the user / admin at the moment when JobSet can be scheduled. So, we should have some configuration for the worker, and explain how to prepare the JobSet installation on different clusters.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-02-28T14:38:29Z

- **site/content/en/docs/tasks/setup_multikueue.md:6** by [@trasc](https://github.com/trasc) — 2024-02-28T14:38:29Z
```diff
@@ -0,0 +1,24 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
```

  I don't think that helps, especially since the end goal for JobSet, for instance, is to not have different configurations .
  
  From a Kueue's perspective I think it's covered.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-28T14:43:44Z

- **site/content/en/docs/tasks/setup_multikueue.md:6** by [@mimowo](https://github.com/mimowo) — 2024-02-28T14:43:44Z
```diff
@@ -0,0 +1,24 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
```

  To have the same configuration for JobSet we need first support for managed by in kueue and in JobSet. I think the docs here should guide the admin step by step what to do given the current versions. We may mention in a note that we are working on simplifying things. Cc @mwielgus

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-02T17:15:49Z

- **site/content/en/docs/tasks/setup_multikueue.md:6** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-02T17:15:49Z
```diff
@@ -0,0 +1,24 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
```

  +1 on @mimowo 
  We used to put set-by-setp introduction this `tasks` section.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-04T10:30:37Z

- **site/content/en/docs/tasks/setup_multikueue.md:64** by [@mimowo](https://github.com/mimowo) — 2024-03-04T10:30:37Z
```diff
@@ -0,0 +1,193 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
+
+1. Create a service account, a restricted role and role binding.
+
+```bash
+MULTIKUEUE_SA=multikueue-sa
+NAMESPACE=kueue-system
+
+echo "Creating a custom MultiKueue Role and Service Account"
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: ServiceAccount
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRole
+metadata:
+  name: ${MULTIKUEUE_SA}-role
+rules:
+- apiGroups:
+  - batch
+  resources:
+  - jobs
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - batch
+  resources:
+  - jobs/status
+  verbs:
+  - get
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets/status
+  verbs:
+  - get
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads/status
+  verbs:
+  - get
+  - patch
+  - update
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRoleBinding
+metadata:
+  name: ${MULTIKUEUE_SA}-crb
+roleRef:
+  apiGroup: rbac.authorization.k8s.io
+  kind: ClusterRole
+  name: ${MULTIKUEUE_SA}-role
+subjects:
+- kind: ServiceAccount
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+EOF
+
+```
+
+2. Get or create a secret associated with the service account.
+
+```bash
+SA_SECRET_NAME=$(kubectl get -n ${NAMESPACE} sa/${MULTIKUEUE_SA} -o "jsonpath={.secrets[0]..name}")
+trasc marked this conversation as resolved.
+if [ -z $SA_SECRET_NAME ]
+then
+# Create the secret and bind it to the desired SA
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: Secret
+type: kubernetes.io/service-account-token
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+  annotations:
+    kubernetes.io/service-account.name: "${MULTIKUEUE_SA}"
+EOF
+
+SA_SECRET_NAME=${MULTIKUEUE_SA}
+fi
+```
+
+3. Create a Kubeconfig file
+
+```bash
+SA_TOKEN=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.data['token']}" | base64 -d)
+CA_CERT=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.data['ca\.crt']}")
+
+# Extract cluster IP from the current context
+CURRENT_CONTEXT=$(kubectl config current-context)
+CURRENT_CLUSTER=$(kubectl config view -o jsonpath="{.contexts[?(@.name == \"${CURRENT_CONTEXT}\"})].context.cluster}")
+CURRENT_CLUSTER_ADDR=$(kubectl config view -o jsonpath="{.clusters[?(@.name == \"${CURRENT_CLUSTER}\"})].cluster.server}")
+
+cat > worker1.kubeconfig <<EOF
+apiVersion: v1
+clusters:
+- cluster:
+    certificate-authority-data: ${CA_CERT}
+    server: ${CURRENT_CLUSTER_ADDR}
+  name: ${CURRENT_CLUSTER}
+contexts:
+- context:
+    cluster: ${CURRENT_CLUSTER}
+    user: ${CURRENT_CLUSTER}-${MULTIKUEUE_SA}
+  name: ${CURRENT_CONTEXT}
+current-context: ${CURRENT_CONTEXT}
+kind: Config
+preferences: {}
+users:
+- name: ${CURRENT_CLUSTER}-${MULTIKUEUE_SA}
+  user:
+    token: ${SA_TOKEN}
+EOF
+```
+
+## Manager Cluster
+
+### Jobset CRD only install
+
+As mentioned in the [MultiKueue Overview](/docs/concepts/multikueue/#jobset) section, in the manager cluster only the JobSet CRDs should be installed, you can do this by running:
+```bash
+kubectl apply --server-side -f  https://raw.githubusercontent.com/kubernetes-sigs/jobset/v0.4.0/config/components/crd/bases/jobset.x-k8s.io_jobsets.yaml
+```
+
+### Enable the MultiKueue feature
+
+Enable the `MultiKueue` feature.
+Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+### Create worker's Kubeconfig secret
+
+For the next example, having the `worker1` cluster Kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+
+```bash
+ kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
+```
+
+Check the [worker](#multikueue-specific-kubeconfig) section for details on Kubeconfig generation.
+
+### Create a sample setup
+
+Apply the following to create a sample setup in which the Jobs submitted in the ClusterQueue `cluster-queue` are delegated to a worker `worker1`
+
+{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
```

  This config has admission check, so it is for manager cluster only. Please also demonstrate how to configure a worker.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-04T12:53:37Z

- **site/content/en/docs/tasks/setup_multikueue.md:64** by [@trasc](https://github.com/trasc) — 2024-03-04T12:53:37Z
```diff
@@ -0,0 +1,193 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
+
+1. Create a service account, a restricted role and role binding.
+
+```bash
+MULTIKUEUE_SA=multikueue-sa
+NAMESPACE=kueue-system
+
+echo "Creating a custom MultiKueue Role and Service Account"
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: ServiceAccount
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRole
+metadata:
+  name: ${MULTIKUEUE_SA}-role
+rules:
+- apiGroups:
+  - batch
+  resources:
+  - jobs
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - batch
+  resources:
+  - jobs/status
+  verbs:
+  - get
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets/status
+  verbs:
+  - get
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads/status
+  verbs:
+  - get
+  - patch
+  - update
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRoleBinding
+metadata:
+  name: ${MULTIKUEUE_SA}-crb
+roleRef:
+  apiGroup: rbac.authorization.k8s.io
+  kind: ClusterRole
+  name: ${MULTIKUEUE_SA}-role
+subjects:
+- kind: ServiceAccount
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+EOF
+
+```
+
+2. Get or create a secret associated with the service account.
+
+```bash
+SA_SECRET_NAME=$(kubectl get -n ${NAMESPACE} sa/${MULTIKUEUE_SA} -o "jsonpath={.secrets[0]..name}")
+trasc marked this conversation as resolved.
+if [ -z $SA_SECRET_NAME ]
+then
+# Create the secret and bind it to the desired SA
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: Secret
+type: kubernetes.io/service-account-token
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+  annotations:
+    kubernetes.io/service-account.name: "${MULTIKUEUE_SA}"
+EOF
+
+SA_SECRET_NAME=${MULTIKUEUE_SA}
+fi
+```
+
+3. Create a Kubeconfig file
+
+```bash
+SA_TOKEN=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.data['token']}" | base64 -d)
+CA_CERT=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.data['ca\.crt']}")
+
+# Extract cluster IP from the current context
+CURRENT_CONTEXT=$(kubectl config current-context)
+CURRENT_CLUSTER=$(kubectl config view -o jsonpath="{.contexts[?(@.name == \"${CURRENT_CONTEXT}\"})].context.cluster}")
+CURRENT_CLUSTER_ADDR=$(kubectl config view -o jsonpath="{.clusters[?(@.name == \"${CURRENT_CLUSTER}\"})].cluster.server}")
+
+cat > worker1.kubeconfig <<EOF
+apiVersion: v1
+clusters:
+- cluster:
+    certificate-authority-data: ${CA_CERT}
+    server: ${CURRENT_CLUSTER_ADDR}
+  name: ${CURRENT_CLUSTER}
+contexts:
+- context:
+    cluster: ${CURRENT_CLUSTER}
+    user: ${CURRENT_CLUSTER}-${MULTIKUEUE_SA}
+  name: ${CURRENT_CONTEXT}
+current-context: ${CURRENT_CONTEXT}
+kind: Config
+preferences: {}
+users:
+- name: ${CURRENT_CLUSTER}-${MULTIKUEUE_SA}
+  user:
+    token: ${SA_TOKEN}
+EOF
+```
+
+## Manager Cluster
+
+### Jobset CRD only install
+
+As mentioned in the [MultiKueue Overview](/docs/concepts/multikueue/#jobset) section, in the manager cluster only the JobSet CRDs should be installed, you can do this by running:
+```bash
+kubectl apply --server-side -f  https://raw.githubusercontent.com/kubernetes-sigs/jobset/v0.4.0/config/components/crd/bases/jobset.x-k8s.io_jobsets.yaml
+```
+
+### Enable the MultiKueue feature
+
+Enable the `MultiKueue` feature.
+Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+### Create worker's Kubeconfig secret
+
+For the next example, having the `worker1` cluster Kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+
+```bash
+ kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
+```
+
+Check the [worker](#multikueue-specific-kubeconfig) section for details on Kubeconfig generation.
+
+### Create a sample setup
+
+Apply the following to create a sample setup in which the Jobs submitted in the ClusterQueue `cluster-queue` are delegated to a worker `worker1`
+
+{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
```

  > This config has admission check, so it is for manager cluster only.
  
  It is in the "Manager Cluster" section.
  
  >  Please also demonstrate how to configure a worker.
  
  There is a worker section, some lines before. The kubeconfig generation is documented there, and also it's mentioned that the worker needs to mirror the manager in terms of namespaces and local queues.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-04T13:03:53Z

- **site/content/en/docs/tasks/setup_multikueue.md:64** by [@mimowo](https://github.com/mimowo) — 2024-03-04T13:03:53Z
```diff
@@ -0,0 +1,193 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
+
+1. Create a service account, a restricted role and role binding.
+
+```bash
+MULTIKUEUE_SA=multikueue-sa
+NAMESPACE=kueue-system
+
+echo "Creating a custom MultiKueue Role and Service Account"
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: ServiceAccount
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRole
+metadata:
+  name: ${MULTIKUEUE_SA}-role
+rules:
+- apiGroups:
+  - batch
+  resources:
+  - jobs
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - batch
+  resources:
+  - jobs/status
+  verbs:
+  - get
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets/status
+  verbs:
+  - get
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads/status
+  verbs:
+  - get
+  - patch
+  - update
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRoleBinding
+metadata:
+  name: ${MULTIKUEUE_SA}-crb
+roleRef:
+  apiGroup: rbac.authorization.k8s.io
+  kind: ClusterRole
+  name: ${MULTIKUEUE_SA}-role
+subjects:
+- kind: ServiceAccount
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+EOF
+
+```
+
+2. Get or create a secret associated with the service account.
+
+```bash
+SA_SECRET_NAME=$(kubectl get -n ${NAMESPACE} sa/${MULTIKUEUE_SA} -o "jsonpath={.secrets[0]..name}")
+trasc marked this conversation as resolved.
+if [ -z $SA_SECRET_NAME ]
+then
+# Create the secret and bind it to the desired SA
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: Secret
+type: kubernetes.io/service-account-token
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+  annotations:
+    kubernetes.io/service-account.name: "${MULTIKUEUE_SA}"
+EOF
+
+SA_SECRET_NAME=${MULTIKUEUE_SA}
+fi
+```
+
+3. Create a Kubeconfig file
+
+```bash
+SA_TOKEN=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.data['token']}" | base64 -d)
+CA_CERT=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.data['ca\.crt']}")
+
+# Extract cluster IP from the current context
+CURRENT_CONTEXT=$(kubectl config current-context)
+CURRENT_CLUSTER=$(kubectl config view -o jsonpath="{.contexts[?(@.name == \"${CURRENT_CONTEXT}\"})].context.cluster}")
+CURRENT_CLUSTER_ADDR=$(kubectl config view -o jsonpath="{.clusters[?(@.name == \"${CURRENT_CLUSTER}\"})].cluster.server}")
+
+cat > worker1.kubeconfig <<EOF
+apiVersion: v1
+clusters:
+- cluster:
+    certificate-authority-data: ${CA_CERT}
+    server: ${CURRENT_CLUSTER_ADDR}
+  name: ${CURRENT_CLUSTER}
+contexts:
+- context:
+    cluster: ${CURRENT_CLUSTER}
+    user: ${CURRENT_CLUSTER}-${MULTIKUEUE_SA}
+  name: ${CURRENT_CONTEXT}
+current-context: ${CURRENT_CONTEXT}
+kind: Config
+preferences: {}
+users:
+- name: ${CURRENT_CLUSTER}-${MULTIKUEUE_SA}
+  user:
+    token: ${SA_TOKEN}
+EOF
+```
+
+## Manager Cluster
+
+### Jobset CRD only install
+
+As mentioned in the [MultiKueue Overview](/docs/concepts/multikueue/#jobset) section, in the manager cluster only the JobSet CRDs should be installed, you can do this by running:
+```bash
+kubectl apply --server-side -f  https://raw.githubusercontent.com/kubernetes-sigs/jobset/v0.4.0/config/components/crd/bases/jobset.x-k8s.io_jobsets.yaml
+```
+
+### Enable the MultiKueue feature
+
+Enable the `MultiKueue` feature.
+Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+### Create worker's Kubeconfig secret
+
+For the next example, having the `worker1` cluster Kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+
+```bash
+ kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
+```
+
+Check the [worker](#multikueue-specific-kubeconfig) section for details on Kubeconfig generation.
+
+### Create a sample setup
+
+Apply the following to create a sample setup in which the Jobs submitted in the ClusterQueue `cluster-queue` are delegated to a worker `worker1`
+
+{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
```

  I know, I put the comment here, just to point out something similar would be useful for the worker section as well. I think it would be beneficial to provide a working configuration along with saying "needs to mirror the manager"

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-04T13:58:32Z

- **site/content/en/docs/tasks/setup_multikueue.md:64** by [@trasc](https://github.com/trasc) — 2024-03-04T13:58:32Z
```diff
@@ -0,0 +1,193 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
+
+1. Create a service account, a restricted role and role binding.
+
+```bash
+MULTIKUEUE_SA=multikueue-sa
+NAMESPACE=kueue-system
+
+echo "Creating a custom MultiKueue Role and Service Account"
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: ServiceAccount
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRole
+metadata:
+  name: ${MULTIKUEUE_SA}-role
+rules:
+- apiGroups:
+  - batch
+  resources:
+  - jobs
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - batch
+  resources:
+  - jobs/status
+  verbs:
+  - get
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets/status
+  verbs:
+  - get
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads/status
+  verbs:
+  - get
+  - patch
+  - update
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRoleBinding
+metadata:
+  name: ${MULTIKUEUE_SA}-crb
+roleRef:
+  apiGroup: rbac.authorization.k8s.io
+  kind: ClusterRole
+  name: ${MULTIKUEUE_SA}-role
+subjects:
+- kind: ServiceAccount
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+EOF
+
+```
+
+2. Get or create a secret associated with the service account.
+
+```bash
+SA_SECRET_NAME=$(kubectl get -n ${NAMESPACE} sa/${MULTIKUEUE_SA} -o "jsonpath={.secrets[0]..name}")
+trasc marked this conversation as resolved.
+if [ -z $SA_SECRET_NAME ]
+then
+# Create the secret and bind it to the desired SA
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: Secret
+type: kubernetes.io/service-account-token
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+  annotations:
+    kubernetes.io/service-account.name: "${MULTIKUEUE_SA}"
+EOF
+
+SA_SECRET_NAME=${MULTIKUEUE_SA}
+fi
+```
+
+3. Create a Kubeconfig file
+
+```bash
+SA_TOKEN=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.data['token']}" | base64 -d)
+CA_CERT=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.data['ca\.crt']}")
+
+# Extract cluster IP from the current context
+CURRENT_CONTEXT=$(kubectl config current-context)
+CURRENT_CLUSTER=$(kubectl config view -o jsonpath="{.contexts[?(@.name == \"${CURRENT_CONTEXT}\"})].context.cluster}")
+CURRENT_CLUSTER_ADDR=$(kubectl config view -o jsonpath="{.clusters[?(@.name == \"${CURRENT_CLUSTER}\"})].cluster.server}")
+
+cat > worker1.kubeconfig <<EOF
+apiVersion: v1
+clusters:
+- cluster:
+    certificate-authority-data: ${CA_CERT}
+    server: ${CURRENT_CLUSTER_ADDR}
+  name: ${CURRENT_CLUSTER}
+contexts:
+- context:
+    cluster: ${CURRENT_CLUSTER}
+    user: ${CURRENT_CLUSTER}-${MULTIKUEUE_SA}
+  name: ${CURRENT_CONTEXT}
+current-context: ${CURRENT_CONTEXT}
+kind: Config
+preferences: {}
+users:
+- name: ${CURRENT_CLUSTER}-${MULTIKUEUE_SA}
+  user:
+    token: ${SA_TOKEN}
+EOF
+```
+
+## Manager Cluster
+
+### Jobset CRD only install
+
+As mentioned in the [MultiKueue Overview](/docs/concepts/multikueue/#jobset) section, in the manager cluster only the JobSet CRDs should be installed, you can do this by running:
+```bash
+kubectl apply --server-side -f  https://raw.githubusercontent.com/kubernetes-sigs/jobset/v0.4.0/config/components/crd/bases/jobset.x-k8s.io_jobsets.yaml
+```
+
+### Enable the MultiKueue feature
+
+Enable the `MultiKueue` feature.
+Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+### Create worker's Kubeconfig secret
+
+For the next example, having the `worker1` cluster Kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+
+```bash
+ kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
+```
+
+Check the [worker](#multikueue-specific-kubeconfig) section for details on Kubeconfig generation.
+
+### Create a sample setup
+
+Apply the following to create a sample setup in which the Jobs submitted in the ClusterQueue `cluster-queue` are delegated to a worker `worker1`
+
+{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
```

  added

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-04T13:58:43Z

- **site/content/en/docs/tasks/setup_multikueue.md:6** by [@trasc](https://github.com/trasc) — 2024-03-04T13:58:43Z
```diff
@@ -0,0 +1,24 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
```

  added

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-04T15:19:42Z

- **site/content/en/docs/tasks/setup_multikueue.md:45** by [@mwielgus](https://github.com/mwielgus) — 2024-03-04T15:19:42Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
+
+1. Create a service account, a restricted role and role binding.
+
+```bash
```

  Can we have this in downloadable version (and included here in the same way as the yaml above)?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-04T15:23:24Z

- **site/content/en/docs/tasks/setup_multikueue.md:29** by [@mwielgus](https://github.com/mwielgus) — 2024-03-04T15:23:24Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
+
+1. Create a service account, a restricted role and role binding.
+
+```bash
+MULTIKUEUE_SA=multikueue-sa
```

  Is this the only reason we have it as a batch script instead of plain yaml file?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-04T15:24:51Z

- **site/content/en/docs/tasks/setup_multikueue.md:29** by [@mwielgus](https://github.com/mwielgus) — 2024-03-04T15:24:51Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
+
+1. Create a service account, a restricted role and role binding.
+
+```bash
+MULTIKUEUE_SA=multikueue-sa
```

  Do we realistically expect users to often have a different value here?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-04T15:56:27Z

- **site/content/en/docs/tasks/setup_multikueue.md:24** by [@mwielgus](https://github.com/mwielgus) — 2024-03-04T15:56:27Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
```

  Please add a step to make sure that the commands are executed against the worker cluster.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-04T19:11:17Z

- **site/content/en/docs/tasks/setup_multikueue.md:24** by [@trasc](https://github.com/trasc) — 2024-03-04T19:11:17Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
```

  I don't follow.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-04T19:13:53Z

- **site/content/en/docs/tasks/setup_multikueue.md:24** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T19:13:53Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
```

  `kubectl config use-cluster`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-04T19:14:10Z

- **site/content/en/docs/tasks/setup_multikueue.md:24** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-04T19:14:10Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
```

  sorry, `set-cluster`

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-05T07:26:52Z

- **site/content/en/docs/tasks/setup_multikueue.md:24** by [@trasc](https://github.com/trasc) — 2024-03-05T07:26:52Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
```

  It should be `kubectl config use-cluster`, what I don't understand is the need to specify it needs to be run in the worker cluster, It's in the worker section of the document.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-05T08:34:35Z

- **site/content/en/docs/tasks/setup_multikueue.md:45** by [@trasc](https://github.com/trasc) — 2024-03-05T08:23:17Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
+
+1. Create a service account, a restricted role and role binding.
+
+```bash
```

  done

- **site/content/en/docs/tasks/setup_multikueue.md:29** by [@trasc](https://github.com/trasc) — 2024-03-05T08:32:35Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
+
+1. Create a service account, a restricted role and role binding.
+
+```bash
+MULTIKUEUE_SA=multikueue-sa
```

  The reason was that that the sections could have bean run in the same terminal to generate the file, however switched to downloadable sh file.

- **site/content/en/docs/tasks/setup_multikueue.md:24** by [@trasc](https://github.com/trasc) — 2024-03-05T08:34:25Z
```diff
@@ -0,0 +1,197 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+Follow these steps to create a Kubeconfig restricted to this use case.
```

  mentioned.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-06T11:18:39Z

- **site/content/en/docs/tasks/setup_multikueue.md:90** by [@mwielgus](https://github.com/mwielgus) — 2024-03-06T11:18:39Z
```diff
@@ -0,0 +1,65 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+While `kubectl` is set up to use the worker cluster, download: 
+{{< include "examples/multikueue/create-multikueue-kubeconfig.sh" "bash" >}}
+
+And run:
+
+```bash
+chmod +x create-multikueue-kubeconfig.sh
+./create-multikueue-kubeconfig.sh worker1.kubeconfig
+```
+
+To create a Kubeconfig that can be used in the manager cluster to delegate Jobs in the current worker.
+
+## Manager Cluster
+
+### Jobset CRD only install
+
+As mentioned in the [MultiKueue Overview](/docs/concepts/multikueue/#jobset) section, in the manager cluster only the JobSet CRDs should be installed, you can do this by running:
+```bash
+kubectl apply --server-side -f  https://raw.githubusercontent.com/kubernetes-sigs/jobset/v0.4.0/config/components/crd/bases/jobset.x-k8s.io_jobsets.yaml
+```
+
+### Enable the MultiKueue feature
+
+Enable the `MultiKueue` feature.
+Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+### Create worker's Kubeconfig secret
+
+For the next example, having the `worker1` cluster Kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+
+```bash
+ kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
+```
+
+Check the [worker](#multikueue-specific-kubeconfig) section for details on Kubeconfig generation.
+
+### Create a sample setup
+
+Apply the following to create a sample setup in which the Jobs submitted in the ClusterQueue `cluster-queue` are delegated to a worker `worker1`
+
+{{< include "examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+
```

  Can you include a simple validation step here?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-06T13:58:28Z

- **site/content/en/docs/tasks/setup_multikueue.md:90** by [@trasc](https://github.com/trasc) — 2024-03-06T13:58:28Z
```diff
@@ -0,0 +1,65 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+While `kubectl` is set up to use the worker cluster, download: 
+{{< include "examples/multikueue/create-multikueue-kubeconfig.sh" "bash" >}}
+
+And run:
+
+```bash
+chmod +x create-multikueue-kubeconfig.sh
+./create-multikueue-kubeconfig.sh worker1.kubeconfig
+```
+
+To create a Kubeconfig that can be used in the manager cluster to delegate Jobs in the current worker.
+
+## Manager Cluster
+
+### Jobset CRD only install
+
+As mentioned in the [MultiKueue Overview](/docs/concepts/multikueue/#jobset) section, in the manager cluster only the JobSet CRDs should be installed, you can do this by running:
+```bash
+kubectl apply --server-side -f  https://raw.githubusercontent.com/kubernetes-sigs/jobset/v0.4.0/config/components/crd/bases/jobset.x-k8s.io_jobsets.yaml
+```
+
+### Enable the MultiKueue feature
+
+Enable the `MultiKueue` feature.
+Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+### Create worker's Kubeconfig secret
+
+For the next example, having the `worker1` cluster Kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+
+```bash
+ kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
+```
+
+Check the [worker](#multikueue-specific-kubeconfig) section for details on Kubeconfig generation.
+
+### Create a sample setup
+
+Apply the following to create a sample setup in which the Jobs submitted in the ClusterQueue `cluster-queue` are delegated to a worker `worker1`
+
+{{< include "examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+
```

  added

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-08T16:55:32Z

- **site/content/en/docs/concepts/multikueue.md:71** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:40:19Z
```diff
@@ -69,25 +63,9 @@ Known Limitations:
 
 An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
 
-## Parameters
-
-An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
-
-## Example
-
-### Setup
-
-#### Worker Cluster
-
-When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
-In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
-
-#### Manager Cluster
-
-{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+## Submitting Jobs
 
-For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+After the [configuration](/docs/tasks/setup_multikueue), any MultiKueue supported job submitted in the Manager cluster which targets a MultiKueue configured ClusterQueue is subject to multikueue job delegation without any additional job configuration changes.
 
-```bash
- kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
-```
+## What’s next? 
+- Learn about [MultiKueue environment setup](/docs/tasks/setup_multikueue/)
```

  ```suggestion
  - Learn how to [setup a MultiKueue environment](/docs/tasks/setup_multikueue/)
  ```

- **site/content/en/docs/concepts/multikueue.md:68** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:44:07Z
```diff
@@ -69,25 +63,9 @@ Known Limitations:
 
 An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
 
-## Parameters
-
-An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
-
-## Example
-
-### Setup
-
-#### Worker Cluster
-
-When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
-In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
-
-#### Manager Cluster
-
-{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+## Submitting Jobs
 
-For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+After the [configuration](/docs/tasks/setup_multikueue), any MultiKueue supported job submitted in the Manager cluster which targets a MultiKueue configured ClusterQueue is subject to multikueue job delegation without any additional job configuration changes.
```

  ```suggestion
  In a [configured MultiKueue environemnt](/docs/tasks/setup_multikueue), you can submit any MultiKueue supported job to the Manager cluster, targeting a ClusterQueue configured for Multikueue.
  Kueue delegates the job to the configured worker clusters without any additional configuration changes.
  ```

- **site/content/en/docs/concepts/multikueue.md:71** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:44:20Z
```diff
@@ -69,25 +63,9 @@ Known Limitations:
 
 An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
 
-## Parameters
-
-An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
-
-## Example
-
-### Setup
-
-#### Worker Cluster
-
-When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
-In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
-
-#### Manager Cluster
-
-{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+## Submitting Jobs
 
-For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+After the [configuration](/docs/tasks/setup_multikueue), any MultiKueue supported job submitted in the Manager cluster which targets a MultiKueue configured ClusterQueue is subject to multikueue job delegation without any additional job configuration changes.
 
-```bash
- kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
-```
+## What’s next? 
+- Learn about [MultiKueue environment setup](/docs/tasks/setup_multikueue/)
```

  Also link to how to submit Jobs and JobSets

- **site/content/en/docs/tasks/setup_multikueue.md:16** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:44:59Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
```

  ```suggestion
  To create the sample queue setup in the `default` namespace, you can apply the following manifest:
  ```

- **site/content/en/docs/tasks/setup_multikueue.md:40** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:50:55Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+While `kubectl` is set up to use the worker cluster, download: 
```

  Maybe you can start the document by saying:
  Let's assume that your manager cluster is named X and your worker clusters are named Y and Z. To follow this tutorial, ensure that the credentials for all these clusters should present in the kubeconfig in your local machine.
  
  Then you can just say, run `kubectl config use-cluster worker-cluster-1` below.

- **site/content/en/docs/tasks/setup_multikueue.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:52:41Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
```

  This tutorial explains how you can configure one management cluster and one worker cluster to run JobSets (link to jobset).

- **site/static/examples/multikueue/create-multikueue-kubeconfig.sh:103** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:53:58Z
```diff
@@ -0,0 +1,152 @@
+#!/bin/bash
+
+# Copyright 2024 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+KUBECONFIG_OUT=${1:-kubeconfig}
+MULTIKUEUE_SA=multikueue-sa
+NAMESPACE=kueue-system
+
+# Creating a restricted MultiKueue role, service account and role binding"
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: ServiceAccount
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRole
+metadata:
+  name: ${MULTIKUEUE_SA}-role
+rules:
+- apiGroups:
+  - batch
+  resources:
+  - jobs
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - batch
+  resources:
+  - jobs/status
+  verbs:
+  - get
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets/status
+  verbs:
+  - get
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads/status
+  verbs:
+  - get
+  - patch
+  - update
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRoleBinding
+metadata:
+  name: ${MULTIKUEUE_SA}-crb
+roleRef:
+  apiGroup: rbac.authorization.k8s.io
+  kind: ClusterRole
+  name: ${MULTIKUEUE_SA}-role
+subjects:
+- kind: ServiceAccount
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+EOF
+
+# Get or create a secret bonded to the new service account.
```

  ```suggestion
  # Get or create a secret bound to the new service account.
  ```

- **site/content/en/docs/tasks/setup_multikueue.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:54:37Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
```

  ```suggestion
  ## In the Worker Cluster
  ```

- **site/content/en/docs/tasks/setup_multikueue.md:36** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:54:45Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+While `kubectl` is set up to use the worker cluster, download: 
+{{< include "examples/multikueue/create-multikueue-kubeconfig.sh" "bash" >}}
+
+And run:
+
+```bash
+chmod +x create-multikueue-kubeconfig.sh
+./create-multikueue-kubeconfig.sh worker1.kubeconfig
+```
+
+To create a Kubeconfig that can be used in the manager cluster to delegate Jobs in the current worker.
+
+## Manager Cluster
```

  ```suggestion
  ## In the Manager Cluster
  ```

- **site/content/en/docs/tasks/setup_multikueue.md:36** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T16:55:25Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+While `kubectl` is set up to use the worker cluster, download: 
+{{< include "examples/multikueue/create-multikueue-kubeconfig.sh" "bash" >}}
+
+And run:
+
+```bash
+chmod +x create-multikueue-kubeconfig.sh
+./create-multikueue-kubeconfig.sh worker1.kubeconfig
+```
+
+To create a Kubeconfig that can be used in the manager cluster to delegate Jobs in the current worker.
+
+## Manager Cluster
```

  And add a note about ensuring that kubectl is pointing to the manager cluster.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-11T06:54:24Z

- **site/content/en/docs/concepts/multikueue.md:68** by [@trasc](https://github.com/trasc) — 2024-03-11T06:54:24Z
```diff
@@ -69,25 +63,9 @@ Known Limitations:
 
 An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
 
-## Parameters
-
-An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
-
-## Example
-
-### Setup
-
-#### Worker Cluster
-
-When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
-In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
-
-#### Manager Cluster
-
-{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+## Submitting Jobs
 
-For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+After the [configuration](/docs/tasks/setup_multikueue), any MultiKueue supported job submitted in the Manager cluster which targets a MultiKueue configured ClusterQueue is subject to multikueue job delegation without any additional job configuration changes.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-11T08:23:20Z

- **site/content/en/docs/concepts/multikueue.md:71** by [@trasc](https://github.com/trasc) — 2024-03-11T06:55:24Z
```diff
@@ -69,25 +63,9 @@ Known Limitations:
 
 An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
 
-## Parameters
-
-An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
-
-## Example
-
-### Setup
-
-#### Worker Cluster
-
-When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
-In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
-
-#### Manager Cluster
-
-{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+## Submitting Jobs
 
-For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+After the [configuration](/docs/tasks/setup_multikueue), any MultiKueue supported job submitted in the Manager cluster which targets a MultiKueue configured ClusterQueue is subject to multikueue job delegation without any additional job configuration changes.
 
-```bash
- kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
-```
+## What’s next? 
+- Learn about [MultiKueue environment setup](/docs/tasks/setup_multikueue/)
```

  done

- **site/content/en/docs/concepts/multikueue.md:71** by [@trasc](https://github.com/trasc) — 2024-03-11T07:20:18Z
```diff
@@ -69,25 +63,9 @@ Known Limitations:
 
 An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
 
-## Parameters
-
-An AdmissionCheck controlled by `multikueue` should use a `kueue.x-k8s.io/v1alpha1` `MultiKueueConfig` parameters object, details of its structure can be found in [Kueue Alpha API reference section](/docs/reference/kueue-alpha.v1alpha1/#kueue-x-k8s-io-v1alpha1-MultiKueueConfig)
-
-## Example
-
-### Setup
-
-#### Worker Cluster
-
-When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
-In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
-
-#### Manager Cluster
-
-{{< include "/examples/multikueue/multikueue-setup.yaml" "yaml" >}}
+## Submitting Jobs
 
-For the example provided, having the worker1 cluster kubeconfig stored in a file called `worker1.kubeconfig`, you can create the `worker1-secret` secret by running the following command:
+After the [configuration](/docs/tasks/setup_multikueue), any MultiKueue supported job submitted in the Manager cluster which targets a MultiKueue configured ClusterQueue is subject to multikueue job delegation without any additional job configuration changes.
 
-```bash
- kubectl create secret generic worker1-secret -n kueue-system --from-file=kubeconfig=worker1.kubeconfig
-```
+## What’s next? 
+- Learn about [MultiKueue environment setup](/docs/tasks/setup_multikueue/)
```

  done

- **site/content/en/docs/tasks/setup_multikueue.md:11** by [@trasc](https://github.com/trasc) — 2024-03-11T07:24:46Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
```

  done

- **site/content/en/docs/tasks/setup_multikueue.md:11** by [@trasc](https://github.com/trasc) — 2024-03-11T07:35:56Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
```

  done

- **site/content/en/docs/tasks/setup_multikueue.md:16** by [@trasc](https://github.com/trasc) — 2024-03-11T07:45:16Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
```

  done

- **site/content/en/docs/tasks/setup_multikueue.md:36** by [@trasc](https://github.com/trasc) — 2024-03-11T08:10:23Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+While `kubectl` is set up to use the worker cluster, download: 
+{{< include "examples/multikueue/create-multikueue-kubeconfig.sh" "bash" >}}
+
+And run:
+
+```bash
+chmod +x create-multikueue-kubeconfig.sh
+./create-multikueue-kubeconfig.sh worker1.kubeconfig
+```
+
+To create a Kubeconfig that can be used in the manager cluster to delegate Jobs in the current worker.
+
+## Manager Cluster
```

  done

- **site/content/en/docs/tasks/setup_multikueue.md:36** by [@trasc](https://github.com/trasc) — 2024-03-11T08:13:02Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+While `kubectl` is set up to use the worker cluster, download: 
+{{< include "examples/multikueue/create-multikueue-kubeconfig.sh" "bash" >}}
+
+And run:
+
+```bash
+chmod +x create-multikueue-kubeconfig.sh
+./create-multikueue-kubeconfig.sh worker1.kubeconfig
+```
+
+To create a Kubeconfig that can be used in the manager cluster to delegate Jobs in the current worker.
+
+## Manager Cluster
```

  done

- **site/content/en/docs/tasks/setup_multikueue.md:40** by [@trasc](https://github.com/trasc) — 2024-03-11T08:13:05Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Setup a MultiKueue environment"
+date: 2024-02-26
+weight: 9
+description: >
+  Additional steps needed to setup the multikueue clusters.
+---
+
+Check the concepts section for a [MultiKueue overview](/docs/concepts/multikueue/). 
+
+## Worker Cluster
+
+When MultiKueue dispatches a workload from the manager cluster to a worker cluster, it expects that the job's namespace and LocalQueue also exist in the worker cluster.
+In other words, you should ensure that the worker cluster configuration mirrors the one of the manager cluster in terms of namespaces and LocalQueues.
+
+To create the sample queue setup in the `default` namespace apply:
+
+{{< include "examples/admin/single-clusterqueue-setup.yaml" "yaml" >}}
+
+### MultiKueue Specific Kubeconfig
+
+In order to delegate the jobs in a worker cluster, the manager cluster needs to be able to create, delete, and watch workloads and their parent Jobs.
+
+While `kubectl` is set up to use the worker cluster, download: 
```

  done

- **site/static/examples/multikueue/create-multikueue-kubeconfig.sh:103** by [@trasc](https://github.com/trasc) — 2024-03-11T08:18:06Z
```diff
@@ -0,0 +1,152 @@
+#!/bin/bash
+
+# Copyright 2024 The Kubernetes Authors.
+#
+# Licensed under the Apache License, Version 2.0 (the "License");
+# you may not use this file except in compliance with the License.
+# You may obtain a copy of the License at
+#
+#     http://www.apache.org/licenses/LICENSE-2.0
+#
+# Unless required by applicable law or agreed to in writing, software
+# distributed under the License is distributed on an "AS IS" BASIS,
+# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+# See the License for the specific language governing permissions and
+# limitations under the License.
+
+set -o errexit
+set -o nounset
+set -o pipefail
+
+KUBECONFIG_OUT=${1:-kubeconfig}
+MULTIKUEUE_SA=multikueue-sa
+NAMESPACE=kueue-system
+
+# Creating a restricted MultiKueue role, service account and role binding"
+kubectl apply -f - <<EOF
+apiVersion: v1
+kind: ServiceAccount
+metadata:
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRole
+metadata:
+  name: ${MULTIKUEUE_SA}-role
+rules:
+- apiGroups:
+  - batch
+  resources:
+  - jobs
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - batch
+  resources:
+  - jobs/status
+  verbs:
+  - get
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - jobset.x-k8s.io
+  resources:
+  - jobsets/status
+  verbs:
+  - get
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads
+  verbs:
+  - create
+  - delete
+  - get
+  - list
+  - watch
+- apiGroups:
+  - kueue.x-k8s.io
+  resources:
+  - workloads/status
+  verbs:
+  - get
+  - patch
+  - update
+---
+apiVersion: rbac.authorization.k8s.io/v1
+kind: ClusterRoleBinding
+metadata:
+  name: ${MULTIKUEUE_SA}-crb
+roleRef:
+  apiGroup: rbac.authorization.k8s.io
+  kind: ClusterRole
+  name: ${MULTIKUEUE_SA}-role
+subjects:
+- kind: ServiceAccount
+  name: ${MULTIKUEUE_SA}
+  namespace: ${NAMESPACE}
+EOF
+
+# Get or create a secret bonded to the new service account.
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-11T14:25:32Z

/lgtm
/approve
Thanks!
