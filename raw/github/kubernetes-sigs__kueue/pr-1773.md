# PR #1773: KEP-1714: Fair Sharing

**Summary**: KEP-1714: Fair Sharing

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1773

**Last updated**: 2024-03-18T15:56:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mwielgus](https://github.com/mwielgus)
- **Branch**: `fs` → `main`
- **Created**: 2024-02-27T11:55:41Z
- **Updated**: 2024-03-18T15:56:40Z
- **Merged**: 2024-03-18T15:56:38Z
- **Closed**: 2024-03-18T15:56:38Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 3   **+421 / -0**

## Description

#### What type of PR is this?

/kind feature
/kind api-change

#### What this PR does / why we need it:

Defines how fair sharing of unused resources will work in Kueue.

#### Which issue(s) this PR fixes:

Part of #1714 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-27T11:55:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3f8dcd3dfd47115c1f13f53d73b1d2b8cede4823 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65f82935d0631f0008d30432 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T18:56:25Z

/cc @KunWuLuan @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-27T18:56:29Z

@alculquicondor: GitHub didn't allow me to request PR reviews from the following users: KunWuLuan.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1773#issuecomment-1967395674):

>/cc @KunWuLuan @tenzen-y 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T18:56:41Z

cc @kerthcet

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-27T18:59:31Z

> /cc @KunWuLuan @tenzen-y

Yea, I should review this KEP.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-18T15:32:12Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-18T15:32:18Z

LGTM label has been added.  <details>Git tree hash: 7e88dcc1f51499d039498371bab9716694e2c9f7</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-18T15:32:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1773#issuecomment-2004228648" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1773#" title="Author self-approved">mwielgus</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-27T19:10:00Z

- **keps/1714-fair-sharing/kep.yaml:12** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T18:58:13Z
```diff
@@ -0,0 +1,34 @@
+title: Fair sharing
+kep-number: 1714
+authors:
+  - "@mwielgus"
+  - "@alculquicondor"
+status: draft
+creation-date: 2024-02-05
+reviewers:
+  - "@kerthcet"
+  - "@mimowo"
+approvers:
+  - "@mimowo"
```

  add a `see-also` for the hierarchical cohorts KEP

- **keps/1714-fair-sharing/README.md:183** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T19:09:49Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
```

  Can you add the proposed configuration API?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-27T19:14:16Z

- **keps/1714-fair-sharing/README.md:198** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T19:14:16Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas in the hierarchy of the parent of C.
```

  ```suggestion
  total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2024-02-28T12:09:56Z

- **keps/1714-fair-sharing/README.md:64** by [@kerthcet](https://github.com/kerthcet) — 2024-02-28T07:47:23Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
```

  Maybe not related to this proposal, but this will still lead to priority abuse and make the system more complicated.

- **keps/1714-fair-sharing/README.md:79** by [@kerthcet](https://github.com/kerthcet) — 2024-02-28T07:49:09Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
```

  Just wonder how to measure this? The scheduling latency?

- **keps/1714-fair-sharing/README.md:136** by [@kerthcet](https://github.com/kerthcet) — 2024-02-28T07:56:46Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
```

  Why here to `ensure equal extra space`, didn't understand the intension here.

- **keps/1714-fair-sharing/README.md:1** by [@kerthcet](https://github.com/kerthcet) — 2024-02-28T10:43:23Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
```

  DRF-like fair sharing?

- **keps/1714-fair-sharing/README.md:224** by [@kerthcet](https://github.com/kerthcet) — 2024-02-28T11:00:19Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
+type struct FairSharing {
+     Weight 	*Quantity  // Default is 1. 
```

  Can you claim what the weight used for ?

- **keps/1714-fair-sharing/README.md:196** by [@kerthcet](https://github.com/kerthcet) — 2024-02-28T12:05:29Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
```

  One general question is: since DRF will try to distribute the workloads to different resource pool(CQ) as a result of favoring the smaller share value, but preemption will try to evict the borrowers, isn't this conflict, which means we'll evict more than we're today.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-02-28T14:31:32Z

First pass mostly with questions to better understand. For now I skipped the preemption part.

- **keps/1714-fair-sharing/README.md:33** by [@mimowo](https://github.com/mimowo) — 2024-02-28T12:59:49Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
```

  ```suggestion
  internally.
  ```

- **keps/1714-fair-sharing/README.md:36** by [@mimowo](https://github.com/mimowo) — 2024-02-28T13:52:04Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
```

  assuming the same priorities, maybe worth adding, because by itself this is not entirely true

- **keps/1714-fair-sharing/README.md:47** by [@mimowo](https://github.com/mimowo) — 2024-02-28T13:54:33Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
```

  Does "equally important" assume with same amount of "nominalQuota" contributed?

- **keps/1714-fair-sharing/README.md:51** by [@mimowo](https://github.com/mimowo) — 2024-02-28T13:56:20Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
```

  "should be given a similar amount of resources" - is this not dependent on the amount of nominalQuota contributed. Say one CQ brings 20, another 80. Are they still considered "equally important" in this case (also question above). if so, it would mean both can use around 50, which does not feel fair.

- **keps/1714-fair-sharing/README.md:116** by [@mimowo](https://github.com/mimowo) — 2024-02-28T14:00:50Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
```

  Can we define at this point what "fair share" is? In particular, is it proportional to "nominalQuota", what is the unit of the values?
  
  I assume it is defined later, but it seems reasonable to define it early, not necessarily fully formally, so that the reader has some intuition what this is already. If not feasible to define here maybe at least reference with the line to the place where the concept is defined later.

- **keps/1714-fair-sharing/README.md:120** by [@mimowo](https://github.com/mimowo) — 2024-02-28T14:03:47Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
```

  What about ClusterQueues which reached the total aggregated quota, aren't they able to preempt? This scenario was one of the motivations for https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing, in which nominalQuota for ClusterQueues is 0 (so already reached), yet they are able to preempt. I'm wondering if we are not losing this scenario here.

- **keps/1714-fair-sharing/README.md:136** by [@mimowo](https://github.com/mimowo) — 2024-02-28T14:05:52Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
```

  ```suggestion
  important) to ensure equal extra space (regardless of their given quota) for both teams. 
  ```
  nit, unless you like to use fancy words :)

- **keps/1714-fair-sharing/README.md:161** by [@mimowo](https://github.com/mimowo) — 2024-02-28T14:22:21Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
```

  ```suggestion
  only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
  ```
  ```suggestion
  only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
  ```
  ?

- **keps/1714-fair-sharing/README.md:190** by [@mimowo](https://github.com/mimowo) — 2024-02-28T14:24:02Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
```

  Is the design based on some state-of-the-art approaches? If so, even if modified (or taken as inspiration), it could be helpful to provide some references to justify the design choice, so that we are not re-inventing the wheel.

- **keps/1714-fair-sharing/README.md:193** by [@mimowo](https://github.com/mimowo) — 2024-02-28T14:27:17Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
```

  Would be good to expand on the DRF acronym, if this is some state-of-the-art approach we could mention some reference we draw inspiration from (as mentioned above).

- **keps/1714-fair-sharing/README.md:218** by [@mimowo](https://github.com/mimowo) — 2024-02-28T14:29:35Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
```

  "The **value** for the CQ or cohort" - is this the fair share value? This paragraph feels quite abstract, I think it would be helpful to back it up with a small example so that we can have some intuition.

- **keps/1714-fair-sharing/README.md:216** by [@mimowo](https://github.com/mimowo) — 2024-02-28T14:30:33Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
+type struct FairSharing {
+     Weight 	*Quantity  // Default is 1. 
+}
+```
+
+### Preemption algorithm
+
+The algorithm could be summarized as follows:
+
+```
+AdmitWorkload(x CQ, w Workload)
+ While w does not fit:
```

  Is this algorithm guaranteed to stop? In particular is it possible there are no workloads satisfying this "Find workloads z that could be preempted while satisfying fairness", while the workloads still does not fit?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-02-28T16:03:28Z

- **keps/1714-fair-sharing/README.md:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T15:52:16Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
```

  yes, see section `Share value function and weights`

- **keps/1714-fair-sharing/README.md:47** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T15:53:22Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
```

  No, it's defined by a weight for the ClusterQueue or cohort.

- **keps/1714-fair-sharing/README.md:51** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T15:53:40Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
```

  answered above

- **keps/1714-fair-sharing/README.md:79** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T15:55:19Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
```

  @mimowo is working on an integration-level benchmark.

- **keps/1714-fair-sharing/README.md:116** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T15:55:41Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
```

  It's the weight, but yes, worth a quick mention in this paragraph.

- **keps/1714-fair-sharing/README.md:120** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T15:57:09Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
```

  KEP #1337 is mentioned in the goals

- **keps/1714-fair-sharing/README.md:134** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T15:57:54Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
```

  ```suggestion
  TeamW is not at work and doesn’t need resources. However, once they arrive, some of
  ```

- **keps/1714-fair-sharing/README.md:136** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T15:58:41Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
```

  space from the company-wide pool, as described in the first paragraph.

- **keps/1714-fair-sharing/README.md:193** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T16:01:00Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
```

  These 2 papers:
  - https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf
  - https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1
  I agree, worth adding.

- **keps/1714-fair-sharing/README.md:224** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T16:01:39Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
+type struct FairSharing {
+     Weight 	*Quantity  // Default is 1. 
```

  line 200

- **keps/1714-fair-sharing/README.md:216** by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-28T16:03:17Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
+type struct FairSharing {
+     Weight 	*Quantity  // Default is 1. 
+}
+```
+
+### Preemption algorithm
+
+The algorithm could be summarized as follows:
+
+```
+AdmitWorkload(x CQ, w Workload)
+ While w does not fit:
```

  ```suggestion
   While w does not fit and there are workloads that can still be preempted:
  ```

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-06T14:33:59Z

- **keps/1714-fair-sharing/kep.yaml:12** by [@mwielgus](https://github.com/mwielgus) — 2024-03-06T14:33:59Z
```diff
@@ -0,0 +1,34 @@
+title: Fair sharing
+kep-number: 1714
+authors:
+  - "@mwielgus"
+  - "@alculquicondor"
+status: draft
+creation-date: 2024-02-05
+reviewers:
+  - "@kerthcet"
+  - "@mimowo"
+approvers:
+  - "@mimowo"
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-06T14:35:38Z

- **keps/1714-fair-sharing/README.md:183** by [@mwielgus](https://github.com/mwielgus) — 2024-03-06T14:35:38Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
```

  This can be updated once we will be implementing the sub-feature.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-06T14:41:49Z

- **keps/1714-fair-sharing/kep.yaml:12** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-06T14:41:49Z
```diff
@@ -0,0 +1,34 @@
+title: Fair sharing
+kep-number: 1714
+authors:
+  - "@mwielgus"
+  - "@alculquicondor"
+status: draft
+creation-date: 2024-02-05
+reviewers:
+  - "@kerthcet"
+  - "@mimowo"
+approvers:
+  - "@mimowo"
```

  @tenzen-y as approver

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-06T14:59:14Z

- **keps/1714-fair-sharing/README.md:64** by [@mwielgus](https://github.com/mwielgus) — 2024-03-06T14:59:14Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
```

  IMHO, only slightly. There should be a way to bypass fair-sharing without turning it off completely.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-06T20:44:54Z

- **keps/1714-fair-sharing/README.md:190** by [@mwielgus](https://github.com/mwielgus) — 2024-03-06T20:44:54Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
```

  The approach is not (up to my best knowledge) directly based on any existing solution. There are some similarities, but adding references may not necessarily benefit the reader.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-06T20:45:54Z

- **keps/1714-fair-sharing/kep.yaml:12** by [@mwielgus](https://github.com/mwielgus) — 2024-03-06T20:45:54Z
```diff
@@ -0,0 +1,34 @@
+title: Fair sharing
+kep-number: 1714
+authors:
+  - "@mwielgus"
+  - "@alculquicondor"
+status: draft
+creation-date: 2024-02-05
+reviewers:
+  - "@kerthcet"
+  - "@mimowo"
+approvers:
+  - "@mimowo"
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-07T09:42:24Z

- **keps/1714-fair-sharing/README.md:36** by [@mwielgus](https://github.com/mwielgus) — 2024-03-07T09:42:24Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
```

  Added.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-07T09:52:53Z

- **keps/1714-fair-sharing/README.md:116** by [@mwielgus](https://github.com/mwielgus) — 2024-03-07T09:52:53Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
```

  Added.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-08T21:26:27Z

- **keps/1714-fair-sharing/README.md:275** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T21:21:01Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
+type struct FairSharing {
+     Weight 	*Quantity  // Default is 1. 
+}
+```
+
+### Preemption algorithm
+
+The algorithm could be summarized as follows:
+
+```
+AdmitWorkload(x CQ, w Workload)
+ While w does not fit and there are workloads that can still be preempted:
+   Find CQs that are borrowing resources in the hierarchy
+   For each CQ y:
+	  Find workloads z that could be preempted while satisfying fairness
+   Choose a workload from the biggest offender for preemption
+ Possibly undo some preemption, while w still fits
+```
+
+#### Choosing ClusterQueues to preempt from
+
+When a CQ x fails to admit a workload w, one of the following scenarios may occur:
+
+* [S1] A sub-organization to which CQ x belongs (its cohort, parent cohort, or CQ itself)
+ still has enough resources to admit the workload within its nominal quota, as the total
+ amount of its workload consumption is lower than the quota. However, as there are not
+ enough resources in the system, someone must be borrowing resources. Thus, if we find
+ a potential borrower, we can consider preempting some of its workloads, up to the point
+ when it stops borrowing. This is equivalent to “reclaimWithinCohort” in ClusterQueues.
+
+
+* [S2] A sub-organization to which CQ x belongs is borrowing, however it seems that it is
+borrowing too little compared to other sub-organizations that are also borrowing, so some
+action is needed to enforce fair sharing. We should compare how much the sub-orgs are
+borrowing compared to each other, and preempt some workloads up to the point when its fair
+share would be smaller than the sub-org for which preemptions are executed.
+
+Let’s formalize the above description.
+
+By LCA(x,y) we define the lowest common ancestor of two ClusterQueues x and y in the cohort
+hierarchy. AlmostLCA(x,y) is the last but one element on the path from x to LCA(x,y) in the cohort hierarchy.
+
+When trying to preempt some workloads to admit workload w to CQ x we consider all other
+CQs that are in the same hierarchy. For each of such CQ y we check whether scenario S1
+or S2 occur. To do that we compare sub-organizations that are rooted in AlmostLCA(x,y) and AlmostLCA(y,x).
+
+![LCA diagram](lca.png)
+
+First of all we validate if y is not using its local nominal quota, somewhere on the path.
+If y was using some local nominal quota that lies entirely under AlmostLCA(y,x) then x should
+not be allowed to preempt any of its workload. So we check whether both y and all cohorts on
+the way up to AlmostLCA(y,x) are borrowing. If that happens then y may have something to
+do with x being unable to admit the workload. 
+
+We trigger scenario S1 if AlmostLCA(x,y) is not borrowing but x still has too little
+resources. We put all y’s workloads on a list of potential preemptions, out of which
+we will later (once we check all other CQs) select one that is most reasonable. 
+
+
+We trigger scenario S2 if AlmostLCA(x,y) is borrowing. We check whether AlmostLCA(x,y)
+and AlmostLCA(y,x) are borrowing roughly the same amount. To do this we use a value 
+function (to be defined later) that returns a value of the borrowed capacity.
```

  oops, the value is already defined above

- **keps/1714-fair-sharing/README.md:286** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T21:23:23Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
+type struct FairSharing {
+     Weight 	*Quantity  // Default is 1. 
+}
+```
+
+### Preemption algorithm
+
+The algorithm could be summarized as follows:
+
+```
+AdmitWorkload(x CQ, w Workload)
+ While w does not fit and there are workloads that can still be preempted:
+   Find CQs that are borrowing resources in the hierarchy
+   For each CQ y:
+	  Find workloads z that could be preempted while satisfying fairness
+   Choose a workload from the biggest offender for preemption
+ Possibly undo some preemption, while w still fits
+```
+
+#### Choosing ClusterQueues to preempt from
+
+When a CQ x fails to admit a workload w, one of the following scenarios may occur:
+
+* [S1] A sub-organization to which CQ x belongs (its cohort, parent cohort, or CQ itself)
+ still has enough resources to admit the workload within its nominal quota, as the total
+ amount of its workload consumption is lower than the quota. However, as there are not
+ enough resources in the system, someone must be borrowing resources. Thus, if we find
+ a potential borrower, we can consider preempting some of its workloads, up to the point
+ when it stops borrowing. This is equivalent to “reclaimWithinCohort” in ClusterQueues.
+
+
+* [S2] A sub-organization to which CQ x belongs is borrowing, however it seems that it is
+borrowing too little compared to other sub-organizations that are also borrowing, so some
+action is needed to enforce fair sharing. We should compare how much the sub-orgs are
+borrowing compared to each other, and preempt some workloads up to the point when its fair
+share would be smaller than the sub-org for which preemptions are executed.
+
+Let’s formalize the above description.
+
+By LCA(x,y) we define the lowest common ancestor of two ClusterQueues x and y in the cohort
+hierarchy. AlmostLCA(x,y) is the last but one element on the path from x to LCA(x,y) in the cohort hierarchy.
+
+When trying to preempt some workloads to admit workload w to CQ x we consider all other
+CQs that are in the same hierarchy. For each of such CQ y we check whether scenario S1
+or S2 occur. To do that we compare sub-organizations that are rooted in AlmostLCA(x,y) and AlmostLCA(y,x).
+
+![LCA diagram](lca.png)
+
+First of all we validate if y is not using its local nominal quota, somewhere on the path.
+If y was using some local nominal quota that lies entirely under AlmostLCA(y,x) then x should
+not be allowed to preempt any of its workload. So we check whether both y and all cohorts on
+the way up to AlmostLCA(y,x) are borrowing. If that happens then y may have something to
+do with x being unable to admit the workload. 
+
+We trigger scenario S1 if AlmostLCA(x,y) is not borrowing but x still has too little
+resources. We put all y’s workloads on a list of potential preemptions, out of which
+we will later (once we check all other CQs) select one that is most reasonable. 
+
+
+We trigger scenario S2 if AlmostLCA(x,y) is borrowing. We check whether AlmostLCA(x,y)
+and AlmostLCA(y,x) are borrowing roughly the same amount. To do this we use a value 
+function (to be defined later) that returns a value of the borrowed capacity.
+
+#### Choosing workloads from ClusterQueues for preemption
+
+For each workload z in y we check whether if:
+
+[S2-a] value of AlmostLCA(y,x) without z is still higher than value of AlmostLCA(x,y)
+with admitted workload x. Y’s sub-orgs will still be better than X’s sub-org after we
+preempt z and admit w, thus z is a reasonable candidate to re-balance fair sharing.
+
+
+[S2-b] value of AlmostLCA(y,x) is lower than AlmostLCA(x,y) with admitted workload x.
```

  this seems to be a typo
  ```suggestion
  [S2-b] value of AlmostLCA(y,x) is strictly higher than AlmostLCA(x,y) with admitted workload x.
  ```
  
  Also clarify that this implies that, using this rule, the maximum share value in the cluster monotonically decreases, eventually stopping.

- **keps/1714-fair-sharing/README.md:196** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-08T21:26:06Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
```

  There is no conflict. See the rules S2-a and S2-b in `Choosing workloads from ClusterQueues for preemption`.
  
  It's easy to see that S2-a increases the value of the smallest CQ but it remains being the one with smallest share.
  
  S2-b has a typo, but see my comment below.
  
  That said, fair sharing will indeed increase the number of preemptions overall, but the algorithm should reach a steady state. In other words, there wouldn't be flapping.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-11T12:19:16Z

- **keps/1714-fair-sharing/README.md:39** by [@tenzen-y](https://github.com/tenzen-y) — 2024-02-29T21:58:18Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
```

  ```suggestion
  KEP [#1337](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing) helps a bit in this situation, by introducing priority based preemptions
  ```

- **keps/1714-fair-sharing/README.md:65** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-11T10:35:08Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
```

  ```suggestion
  priority workloads overrule fair sharing and are treated according to KEP [#1337](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing).
  ```

- **keps/1714-fair-sharing/README.md:96** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-11T10:42:59Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
```

  Mentioning https://github.com/kubernetes-sigs/kueue/issues/28 would be worth it.

- **keps/1714-fair-sharing/README.md:142** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-11T10:47:03Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
```

  ```suggestion
  the already submitted workloads from TeamE may be preempted (preferably the least
  ```

- **keps/1714-fair-sharing/README.md:183** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-11T10:50:53Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
```

  @mwielgus Does that mean you will create another KEP? 
  Or, after you implement it, will you update this KEP?

- **keps/1714-fair-sharing/README.md:218** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-11T11:05:45Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
```

  +1

- **keps/1714-fair-sharing/README.md:321** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-11T11:14:33Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
+type struct FairSharing {
+     Weight 	*Quantity  // Default is 1. 
+}
+```
+
+### Preemption algorithm
+
+The algorithm could be summarized as follows:
+
+```
+AdmitWorkload(x CQ, w Workload)
+ While w does not fit and there are workloads that can still be preempted:
+   Find CQs that are borrowing resources in the hierarchy
+   For each CQ y:
+	  Find workloads z that could be preempted while satisfying fairness
+   Choose a workload from the biggest offender for preemption
+ Possibly undo some preemption, while w still fits
+```
+
+#### Choosing ClusterQueues to preempt from
+
+When a CQ x fails to admit a workload w, one of the following scenarios may occur:
+
+* [S1] A sub-organization to which CQ x belongs (its cohort, parent cohort, or CQ itself)
+ still has enough resources to admit the workload within its nominal quota, as the total
+ amount of its workload consumption is lower than the quota. However, as there are not
+ enough resources in the system, someone must be borrowing resources. Thus, if we find
+ a potential borrower, we can consider preempting some of its workloads, up to the point
+ when it stops borrowing. This is equivalent to “reclaimWithinCohort” in ClusterQueues.
+
+
+* [S2] A sub-organization to which CQ x belongs is borrowing, however it seems that it is
+borrowing too little compared to other sub-organizations that are also borrowing, so some
+action is needed to enforce fair sharing. We should compare how much the sub-orgs are
+borrowing compared to each other, and preempt some workloads up to the point when its fair
+share would be smaller than the sub-org for which preemptions are executed.
+
+Let’s formalize the above description.
+
+By LCA(x,y) we define the lowest common ancestor of two ClusterQueues x and y in the cohort
+hierarchy. AlmostLCA(x,y) is the last but one element on the path from x to LCA(x,y) in the cohort hierarchy.
+
+When trying to preempt some workloads to admit workload w to CQ x we consider all other
+CQs that are in the same hierarchy. For each of such CQ y we check whether scenario S1
+or S2 occur. To do that we compare sub-organizations that are rooted in AlmostLCA(x,y) and AlmostLCA(y,x).
+
+![LCA diagram](lca.png)
+
+First of all we validate if y is not using its local nominal quota, somewhere on the path.
+If y was using some local nominal quota that lies entirely under AlmostLCA(y,x) then x should
+not be allowed to preempt any of its workload. So we check whether both y and all cohorts on
+the way up to AlmostLCA(y,x) are borrowing. If that happens then y may have something to
+do with x being unable to admit the workload. 
+
+We trigger scenario S1 if AlmostLCA(x,y) is not borrowing but x still has too little
+resources. We put all y’s workloads on a list of potential preemptions, out of which
+we will later (once we check all other CQs) select one that is most reasonable. 
+
+
+We trigger scenario S2 if AlmostLCA(x,y) is borrowing. We check whether AlmostLCA(x,y)
+and AlmostLCA(y,x) are borrowing roughly the same amount. To do this we use a value 
+function (to be defined later) that returns a value of the borrowed capacity.
+
+#### Choosing workloads from ClusterQueues for preemption
+
+For each workload z in y we check whether if:
+
+[S2-a] value of AlmostLCA(y,x) without z is still higher than value of AlmostLCA(x,y)
+with admitted workload x. Y’s sub-orgs will still be better than X’s sub-org after we
+preempt z and admit w, thus z is a reasonable candidate to re-balance fair sharing.
+
+
+[S2-b] value of AlmostLCA(y,x) is lower than AlmostLCA(x,y) with admitted workload x.
+Here the case is that Y’s sub-org is greedy, and doesn’t allow smaller requests on the
+x’s size. If no other preemption option is possible inside any other CQ, z should be
+considered for preemption. Maybe y has a smaller workload that could be admitted, and
+then both x and y will be ”happy”. In practice this rule prevents huge workloads from
+taking the whole unused capacity, blocking everyone else and claiming that this is fair-sharing. 
+
+After we gathered all possible preemption candidates form all relevant CQs we sort them
+by the following criteria:
+
+* [C1] Values of cohorts/cq on the way from root cohort to y. In descending order, so the
+biggest “offenders” are first.
+* [C2] Priority of the workload inside of CQ - least important ones are favored.
+* [C3] Size of the workload inside of CQ - smallest workloads (based on the dominant resource) are favored.
+
+We pick the best candidate from that ranking and provisionally preempt it. If the
+workload w can be admitted to x then we are done (almost), if not, we search for the
+next candidate starting back from ClusterQueue selection.
+
+#### Algorithmic Optimization
+
+Implemented naively, the proposed algorithm is O(n^2) on the number of workloads.
+
+In practice, we don’t create one list of candidates and sort it every time. It can be noticed that after this provisional preemption.
+
+* Order based on [C3] is not changed - no workload is resized.
+* Order based on [C2] is not changed - no priority is changed. 
+* Order based on [C1] may change a bit, but it applies to the whole groups that could be reshuffled. 
+
+Thus finding the best candidate can be done via:
+
+Finding the CQ with the highest value according to [C1] - this can be done by
+sorting cq’s instead of workloads, their number is much smaller (some of them can be
+dropped as not borrowing).
+ 
+Picking the already (on the first iteration)  sorted list of workloads and checking which of
```

  ```suggestion
  Picking the already (on the first iteration) sorted list of workloads and checking which of
  ```

- **keps/1714-fair-sharing/README.md:220** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-11T12:11:50Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
```

  When we set FairSharing configuration, what happens?

- **keps/1714-fair-sharing/README.md:222** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-11T12:15:06Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
```

  As I can remember, we have a plan to have DRF as another queueingStarategy like this: https://docs.google.com/document/d/1VQ0qxWA-jwgvLq_WYG46OkXWW00O6q7b1BsR_Uv-acs/edit?usp=sharing
  
  So, did you compare the pros and cons of the following options?
  
  1. Exptend CohortSpec and ClusterQueueSpec (current your approach)
  2. Introduce a new queueing strategy, "DRF", and have a new CRD, "FairSharing".
  
  @mwielgus @alculquicondor

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-11T13:25:11Z

- **keps/1714-fair-sharing/README.md:183** by [@mwielgus](https://github.com/mwielgus) — 2024-03-11T13:25:11Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
```

  Either way. This KEP is complex enough to skip any additional and optional apis.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-11T13:32:36Z

- **keps/1714-fair-sharing/README.md:183** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-11T13:32:36Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
```

  If we skip these options in the first iteration, I'm ok with this.
  We can update this KEP before the second iteration.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-12T15:08:37Z

Just a few notes to grasp the high level idea of the algorithms.

- **keps/1714-fair-sharing/README.md:64** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-12T14:52:23Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
```

  The current semantics are the opposite:
  If the running workloads have a priority lower than a defined threshold, they can be evicted.

- **keps/1714-fair-sharing/README.md:222** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-12T14:55:22Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
```

  DRF within a CQ wouldn't easily extrapolate to more complex hierarchies.

- **keps/1714-fair-sharing/README.md:220** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-12T14:56:24Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
```

  That's mentioned in the previous section and detailed in the sections below

- **keps/1714-fair-sharing/README.md:212** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-12T15:02:34Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP [#1337](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing) helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP [#1337](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing).
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted (preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
```

  Let's add another paragraph:
  
  Note that the share value for a sub-organization (a node in the tree) is independent of the share value for its children. In other words, the calculation of the share value only depends on the sum of usages and sum of nominal quotas in the sub-organization.

- **keps/1714-fair-sharing/README.md:197** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-12T15:07:59Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP [#1337](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing) helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP [#1337](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing).
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted (preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
```

  Let's add:
  
  The algorithms take a decision from root-to-leaves. This favors admission from sub-organizations that, as a whole, still have unused resources, or favors preemptions from sub-organizations that, as a whole, are borrowing too much.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-12T15:12:27Z

- **keps/1714-fair-sharing/README.md:220** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-12T15:12:27Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
```

  This is my bad.
  I wanted to say "When we set FairSharing configuration to ClusterQueue without Cohort, what happens?"

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-12T15:24:48Z

- **keps/1714-fair-sharing/README.md:220** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-12T15:24:48Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
```

  Nothing. Fair sharing only applies above CQs.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-12T15:27:48Z

- **keps/1714-fair-sharing/README.md:220** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-12T15:27:48Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
```

  You meant that we will add any validation?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-12T15:58:59Z

- **keps/1714-fair-sharing/README.md:222** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-12T15:58:59Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
```

  That makes sense.
  Recording this discussion as an Alternative approach might be worth it, right?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-18T11:25:02Z

- **keps/1714-fair-sharing/README.md:96** by [@mwielgus](https://github.com/mwielgus) — 2024-03-18T11:25:02Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
```

  Added.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-18T11:27:06Z

- **keps/1714-fair-sharing/README.md:212** by [@mwielgus](https://github.com/mwielgus) — 2024-03-18T11:27:06Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP [#1337](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing) helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP [#1337](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing).
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted (preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-18T11:28:01Z

- **keps/1714-fair-sharing/README.md:275** by [@mwielgus](https://github.com/mwielgus) — 2024-03-18T11:28:01Z
```diff
@@ -0,0 +1,375 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis
+(assuming the same priorities of the upcoming workloads).
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+
+Introduce a concept of fair share as the amount of resources lent to a CQ, 
+on top of the given nominal quota, that doesn't justify "complains" against
+any other similar CQ about excesive extra resources that CQ was given. Basically
+the sharing of unused resources is fair, if no CQ can say it is grossly unfair.
+
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not at work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (regardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2A has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF (see
+[1](https://amplab.cs.berkeley.edu/wp-content/uploads/2011/06/Dominant-Resource-Fairness-Fair-Allocation-of-Multiple-Resource-Types.pdf),
+[2](https://dash.harvard.edu/bitstream/handle/1/11956916/Parkes_BeyondDominant.pdf;jsessionid=AC0D06C2CC07C693BD42008D7AE25D99?sequence=1)):
+
+For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
+requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
+flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
+total nominal quotas (or lendingLimits, if defined) in the hierarchy of the parent of C.
+
+The value for the CQ or cohort is the maximum among the values for each resource, divided by the weight, if defined.
+
+Weights will be added to ClusterQueueSpec and CohortSpec in the following optional struct:
+
+```go
+type struct FairSharing {
+     Weight 	*Quantity  // Default is 1. 
+}
+```
+
+### Preemption algorithm
+
+The algorithm could be summarized as follows:
+
+```
+AdmitWorkload(x CQ, w Workload)
+ While w does not fit and there are workloads that can still be preempted:
+   Find CQs that are borrowing resources in the hierarchy
+   For each CQ y:
+	  Find workloads z that could be preempted while satisfying fairness
+   Choose a workload from the biggest offender for preemption
+ Possibly undo some preemption, while w still fits
+```
+
+#### Choosing ClusterQueues to preempt from
+
+When a CQ x fails to admit a workload w, one of the following scenarios may occur:
+
+* [S1] A sub-organization to which CQ x belongs (its cohort, parent cohort, or CQ itself)
+ still has enough resources to admit the workload within its nominal quota, as the total
+ amount of its workload consumption is lower than the quota. However, as there are not
+ enough resources in the system, someone must be borrowing resources. Thus, if we find
+ a potential borrower, we can consider preempting some of its workloads, up to the point
+ when it stops borrowing. This is equivalent to “reclaimWithinCohort” in ClusterQueues.
+
+
+* [S2] A sub-organization to which CQ x belongs is borrowing, however it seems that it is
+borrowing too little compared to other sub-organizations that are also borrowing, so some
+action is needed to enforce fair sharing. We should compare how much the sub-orgs are
+borrowing compared to each other, and preempt some workloads up to the point when its fair
+share would be smaller than the sub-org for which preemptions are executed.
+
+Let’s formalize the above description.
+
+By LCA(x,y) we define the lowest common ancestor of two ClusterQueues x and y in the cohort
+hierarchy. AlmostLCA(x,y) is the last but one element on the path from x to LCA(x,y) in the cohort hierarchy.
+
+When trying to preempt some workloads to admit workload w to CQ x we consider all other
+CQs that are in the same hierarchy. For each of such CQ y we check whether scenario S1
+or S2 occur. To do that we compare sub-organizations that are rooted in AlmostLCA(x,y) and AlmostLCA(y,x).
+
+![LCA diagram](lca.png)
+
+First of all we validate if y is not using its local nominal quota, somewhere on the path.
+If y was using some local nominal quota that lies entirely under AlmostLCA(y,x) then x should
+not be allowed to preempt any of its workload. So we check whether both y and all cohorts on
+the way up to AlmostLCA(y,x) are borrowing. If that happens then y may have something to
+do with x being unable to admit the workload. 
+
+We trigger scenario S1 if AlmostLCA(x,y) is not borrowing but x still has too little
+resources. We put all y’s workloads on a list of potential preemptions, out of which
+we will later (once we check all other CQs) select one that is most reasonable. 
+
+
+We trigger scenario S2 if AlmostLCA(x,y) is borrowing. We check whether AlmostLCA(x,y)
+and AlmostLCA(y,x) are borrowing roughly the same amount. To do this we use a value 
+function (to be defined later) that returns a value of the borrowed capacity.
```

  Fixed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-03-18T11:45:25Z

- **keps/1714-fair-sharing/README.md:193** by [@mwielgus](https://github.com/mwielgus) — 2024-03-18T11:45:25Z
```diff
@@ -0,0 +1,366 @@
+# KEP-1714: Fair Sharing
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Share value function and weights](#share-value-function-and-weights)
+  - [Preemption algorithm](#preemption-algorithm)
+    - [Choosing ClusterQueues to preempt from](#choosing-clusterqueues-to-preempt-from)
+    - [Choosing workloads from ClusterQueues for preemption](#choosing-workloads-from-clusterqueues-for-preemption)
+    - [Algorithmic Optimization](#algorithmic-optimization)
+    - [Minimum number of preemptions](#minimum-number-of-preemptions)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces weight-based fair sharing of unused resources across 
+needing ClusterQueues, respecting borrowing and lending limits, multi-level
+hierarchy and preferences of cohorts to distribute unused resources 
+Internally.
+
+## Motivation
+Currently unused resources are allocated on the "first come first served" basis.
+This approach is not optimal in large deployments where some users (ClusterQueues)
+could get preferential treatment based solely on the timing of workload submission.
+KEP #1337 helps a bit in this situation, by introducing priority based preemptions
+to borrowing, however in organizations with no strict budget enforcing, priorities
+could be overused (if priorities could guarantee more resources, then everyone would
+use the highest one, breaking the whole idea altogether). Thus a more comprehensive
+solution of distributing unused resources is needed.
+
+### Goals
+* Create a mechanism to enforce fair sharing of resources. Two equally important
+sub-organizations (Cohorts or ClusterQueues), placed in the similar spots in the
+whole organization (hierarchy of Cohorts), actively competing for the same
+resources (having workloads needing more than nominal quota), should 
+be given a similar amount of resources. And any pre existing imbalances should
+be timely resolved by preemption.
+
+* Allow system admin to indicate that a sub-organization is more important than
+their siblings, and should get proportionally more resources than others located
+under the same parent. 
+
+* When distributing resources, prioritize needs of CQ within the same
+sub-organization and only after they have been fulfilled, proceed with distribution
+outside of the suborganization.
+
+* When enforcing fair sharing, ignore workload priorities unless:
+
+** The workload's priority is above admin-defined high priority. Super high
+priority workloads overrule fair sharing and are treated according to KEP #1337.
+
+** There is a need to preempt some non top priority workload from a ClusterQueue.
+Then the lowest priority workloads from a CQ that is over its fair share should
+be selected for preemption.
+
+* The mechanism should be compatible with all existing or proposed Kueue features,
+in particular:
+
+** Borrowing limits 
+** Lending limits
+** Guaranteed/nominal quota
+** Hierarchical cohorts
+
+* Fair sharing should not limit Kueue scalability. Kueue, with fair sharing enabled,
+should be able to handle >1k ClusterQueues, >100 Cohorts and >10k workloads (that
+are either running or queued) within a single hierarchical organization.
+
+* The proposed system should be hard to game, for example by creating big workloads
+that consume all capacity.
+
+* Fair sharing enforcement should not significantly decrease overall utilization,
+however, pathological situations (like a single workload consuming all otherwise
+unused capacity) should be resolved in favor of fair sharing than maximizing the
+utilization (the big greedy workload should be preempted to admit smaller
+workloads from other CQ that consume only their fair share).
+
+### Non-Goals
+* Use historical data (for example CQ A used a lot of shared capacity for the last
+week, so now it should get less because others, who didn't need anything then, have
+pending workloads). Fair sharing should be based on point-in-time situation, although,
+ideally it should be expandable to support history-based fair sharing without major redesign.
+
+* Enable fair sharing only for some part of the resources or Cohort hierarchy.
+Fair sharing will be a global switch (at least initially).
+
+* Maximize utilization at the cost of fair sharing.
+
+## Proposal
+Introduce a global fair sharing mechanism that is based on preemptions. As long
+as there are some free and accessible resources in the cohort hierarchy, Kueue
+will admit workloads without any limits. However, once the capacity is gone,
+new workloads from Cohorts/CQ that have not received their fair share yet will
+be permitted to preempt workloads from Chorots/CQ that received more than their
+fair share would otherwise be (or more than the Cohort/CQ that is in need).
+
+Additionally, we will introduce a reclaim mechanism at the Cohort level. Cohorts
+that are in need, but whose consumption didn’t reach their total aggregated
+nominal quota will be able to preempt workloads from Cohorts/CQ that are over
+their budget and are borrowing from the Cohort/CQ that tries to admit a workload
+within its own nominal quota (very similar to what we currently have for CQ
+within the single level Cohort structure).
+
+We will add an optional weight field to both Cohorts and CQ. The weight will
+indicate how to fair share resources between sub-organizations (CQs or Cohorts)
+under the same Cohort.
+
+Fair sharing will be configured for the whole cluster, using the configuration
+file. In Alpha it will be just a feature gate.
+
+### User Stories (Optional)
+
+#### Story 1
+I have 2 teams in my department in some US-based company, one on the east coast
+(TeamE), one on the west coast (TeamW). Both come to work at 9 am their local time.
+And immediately after that they start submitting their research jobs for execution
+in their assigned CQs. Each of them have a bit of guaranteed resources, however they
+both rely on a company-wide pool or resources that should be fairly shared. 
+
+TeamE can submit as many workloads and consume as many resources as they can while
+TeamW is not a work and doesn’t need resources. However, once they arrive, some of
+the already submitted workloads from TeamE may be preempted(preferably the least
+important) to ensure equal extra space (irregardless of their given quota) for both teams. 
+
+
+#### Story 2
+I have two departments in my company D1 and D2 (modeled as Cohorts, under one
+top-level Cohort). Each of the departments has 3 teams: A, B, C. Each of the teams
+submits their workloads to the appropriate CQ: 1A, 1B, 1C, 2A, 2B, 2C. Each of the
+teams/CQs is given some guaranteed resources within the big ML cluster. Each team
+is also allowed to borrow as many resources from other teams as they want, as long
+as they are not needed by the owner. However, due to internal accounting, company
+internal politics, etc, free resources should first be distributed within the
+department. For example:
+
+* if CQ 1A has free resources, and both 1B and 1C are in need of extra resources 
+to run their workloads, then 2A, 2B and 2C should not be given a share of 1A,
+that would block any workload from 1B or 1C from running. 
+
+* If CQ 1A has free resources and 1B and 1C do not need any of those to start
+a workload (there are no pending workloads or they are too big anyway) then 2A, 2B,
+2C should be given an equal share of the resources that correspond to their needs.
+
+#### Story 3
+I have 3 cohorts inside my organization (top cohort): CS, C1 and C2. CS contains
+a single CQ that is given 300 GPU nominal quota. C1 contains 3 CQ: 1A, 1B and 1C,
+among which 1C is considered to be 3 times more important than others. C2 contains
+only one CQ: 2A.  Each of the queues 1A, 1B, 1C and 2C has a long backlog of jobs
+to execute. I want to distribute the resources from CS in the following way:
+
+* Both C1 and C2 get 150 GPUs each, because they are considered equally important.
+* Inside C1, out of these 150 GPUs, 1A gets 30, 1B gets 30 and 1C gets 90 GPUs. 
+1A and 1B are equally important but 1C is 3x more important than 1A and 1B.
+* Inside C2 there is just a single CQ, so it gets all 150 GPUS.
+
+### Risks and Mitigations
+
+* Fair sharing may increase the number of preemptions in the system vs current
+state where the first workload to acquire unused resources keeps them until it
+finishes. Mitigations include:
+    * Introduce minimum execution time before workloads can be preempted for fair sharing.
+    * Introduce delayed fair share enforcement - new workloads have to wait a bit before preempting others to get their share.
+
+* Fair sharing may decrease utilization of the unused resources while attempting to
+distribute them fairly, vs provide the tighties bin-packing. To avoid these scenarios,
+users should prefer to run massive workloads under nominal quotas.
+
+
+## Design Details
+
+The enforcement of fair-share revolves around the idea of a share value per CQ or
+Cohort and mechanisms to keep the share values among siblings in the hierarchy to
+be as equal as possible. This is achieved via two mechanisms:
+
+* During admission, prioritize admitting workload from the ClusterQueues and cohorts that have the lowest share value.
+* Preempt workloads from ClusterQueues and cohorts that have the highest share value.
+
+### Share value function and weights
+
+The value function is a variation of DRF:
```

  Added.
