# PR #1777: Fair share during admission

**Summary**: Fair share during admission

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1777

**Last updated**: 2024-04-03T19:08:08Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `flat-fair-share` → `main`
- **Created**: 2024-02-27T19:18:12Z
- **Updated**: 2024-04-03T19:08:08Z
- **Merged**: 2024-04-03T19:08:07Z
- **Closed**: 2024-04-03T19:08:07Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 12   **+1251 / -67**

## Description

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Implements the concept of a "fair share" for a ClusterQueue or a ClusterQueue with an additional workload as defined in #1773:

```
For a given resource r provided by a ClusterQueue or cohort c, we calculate T_r as the total
requests consumed by the Workloads for that resource in that CQ or cohort, independent of the
flavor, that are above the nominal quota. The value for a resource is the ratio of T_r and the
total nominal quotas, or lendingLimits if defined, in the hierarchy of the parent of C.

The value for the CQ or cohort is the maximum among the values for each resource,
divided by the weight, if defined.
```

The fair share value is used for sorting the heads of the ClusterQueues during admission.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #1714

#### Special notes for your reviewer:

There is no API for this feature in this PR. That's because we don't want to release this behavior standalone, but accompany it with preemption support, which will come in a subsequent PR.

For reviewers convenience, I roughly divided the PR in 3 commits:
- Keeping stats for a ClusterQueue and cohort about total usage per resource and shareable resources.
- Calculate the share value for a ClusterQueue with or without a new workload
- Admission sorting + tests.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-02-27T19:18:17Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1777#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-02-27T19:18:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cad110e860d17268b938ba1e1b693580768ce5f5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/660d94e7c489300008cd4c07 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-02-27T19:21:22Z

/hold
for #1773

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-26T18:39:45Z

@alculquicondor Is this ready for the review?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-26T19:00:02Z

Yes :)

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-27T10:56:59Z

Ack

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T13:44:41Z

Squashed

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T13:45:07Z

I'm reviewing now.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-03T15:07:19Z

lgtm, leaving the final tag to @tenzen-y

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T15:25:10Z

/hold cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T17:44:45Z

Thank you!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-03T17:44:51Z

LGTM label has been added.  <details>Git tree hash: 4bef7a3a9dc12101c9f6202c2b29996fa85f0188</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T18:36:57Z

It looks like a flaky test. Opened #1943

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T18:46:18Z

/retest

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-27T14:25:43Z

Went through most of the code, still some tests to go.

- **pkg/cache/clusterqueue.go:265** by [@mimowo](https://github.com/mimowo) — 2024-03-27T13:54:04Z
```diff
@@ -246,9 +262,27 @@ func filterQuantities(orig FlavorResourceQuantities, resourceGroups []kueue.Reso
 	return ret
 }
 
+func resetResourceStatsFromResourceGroups(orig ResourceStats, resourceGroups []kueue.ResourceGroup) ResourceStats {
```

  I think the name `reset` suggests that the orig stats are mutated. I would suggest to call it `resourceUsageStatsFromResourceGroups` and move the comment about setting Nominal and Lendable to the function definition

- **pkg/cache/snapshot.go:56** by [@mimowo](https://github.com/mimowo) — 2024-03-27T14:08:03Z
```diff
@@ -41,28 +42,27 @@ type Snapshot struct {
 func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq := s.ClusterQueues[wl.ClusterQueue]
 	delete(cq.Workloads, workload.Key(wl.Obj))
-	updateUsage(wl, cq.Usage, -1)
-	if cq.Cohort != nil {
-		if features.Enabled(features.LendingLimit) {
-			updateCohortUsage(wl, cq, -1)
-		} else {
-			updateUsage(wl, cq.Cohort.Usage, -1)
-		}
-	}
+	cq.addRemoveWorkload(wl, -1)
 }
 
 // AddWorkload adds a workload from its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueues[wl.ClusterQueue]
 	cq.Workloads[workload.Key(wl.Obj)] = wl
-	updateUsage(wl, cq.Usage, 1)
-	if cq.Cohort != nil {
+	cq.addRemoveWorkload(wl, 1)
+}
+
+func (c *ClusterQueue) addRemoveWorkload(wl *workload.Info, m int64) {
```

  maybe `addOrRemoveWorkload`?

- **pkg/scheduler/scheduler.go:597** by [@mimowo](https://github.com/mimowo) — 2024-03-27T14:24:35Z
```diff
@@ -582,7 +589,12 @@ func (e entryOrdering) Less(i, j int) bool {
 		return !aBorrows
 	}
 
-	// 2. Higher priority first if not disabled.
+	// 2. Fair share, if enabled.
+	if e.enableFairSharing && a.dominantResourceShare != b.dominantResourceShare {
+		return a.dominantResourceShare < b.dominantResourceShare
+	}
+
+	// 3. Higher priority first if not disabled.
```

  IIUC here we prioritize scheduling based on fair share, but they are still not fully ignored as suggested in KEP goals: "When enforcing fair sharing, ignore workload priorities unless: (... point about super high prioirites... )"
  
  They are used to break the tie, which will happen when multiple CQs are still have DRF <=0. So, there is still some incentive to set higher priority. Do you think this is acceptable, or should we fallback directly to time creation when Fair Sharing is used?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-28T12:08:38Z

- **pkg/cache/clusterqueue.go:50** by [@mimowo](https://github.com/mimowo) — 2024-03-28T11:42:48Z
```diff
@@ -39,6 +39,16 @@ var (
 	errQueueAlreadyExists = errors.New("queue already exists")
 )
 
+// QuotaStats holds the nominal quota and usage for a resource.
+type QuotaStats struct {
+	Nominal  int64
+	Lendable int64
+	Usage    int64
+}
+
+// ResourceStats holds QuotaStats for resources.
+type ResourceStats map[corev1.ResourceName]*QuotaStats
```

  Why is this a ptr to QuotaStats? Maybe then we could simplify some places which do `ptr.To(*stats)`?

- **pkg/cache/clusterqueue.go:694** by [@mimowo](https://github.com/mimowo) — 2024-03-28T11:47:23Z
```diff
@@ -627,3 +677,26 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 
 	return cohortUsage
 }
+
+// DominantResourceShare returns a value from 0 to 100 representing the maximum of the ratios
+// of usage above nominal quota to the lendable resources in the cohort, among all the resources
+// provided by the ClusterQueue.
+// The function also returns the resource name that yielded this value.
+func (c *ClusterQueue) DominantResourceShare(w *workload.Info) (int, corev1.ResourceName) {
+	if c.Cohort == nil {
+		return 0, ""
+	}
+	var drs int64 = -1
+	var dRes corev1.ResourceName
+	wUsage := w.ResourceUsage()
+	for rName, rStats := range c.ResourceStats {
+		ratio := max(rStats.Usage+wUsage[rName]-rStats.Nominal, 0) * 100 /
+			c.Cohort.ResourceStats[rName].Lendable
```

  Don't we need to protect for division by 0 in case one sets lendingLimit=0?

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:153** by [@mimowo](https://github.com/mimowo) — 2024-03-28T12:02:56Z
```diff
@@ -0,0 +1,159 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"fmt"
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Scheduler", func() {
+
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		defaultFlavor = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteResourceFlavor(ctx, k8sClient, defaultFlavor)).To(gomega.Succeed())
+	})
+
+	ginkgo.When("Preemption is disabled", func() {
+
+		var (
+			cqA      *kueue.ClusterQueue
+			lqA      *kueue.LocalQueue
+			cqB      *kueue.ClusterQueue
+			lqB      *kueue.LocalQueue
+			cqShared *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			cqA = testing.MakeClusterQueue("a").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "3").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqA)).To(gomega.Succeed())
+			lqA = testing.MakeLocalQueue("a", ns.Name).ClusterQueue("a").Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqA)).To(gomega.Succeed())
+
+			cqB = testing.MakeClusterQueue("b").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqB)).To(gomega.Succeed())
+			lqB = testing.MakeLocalQueue("b", ns.Name).ClusterQueue("b").Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqB)).To(gomega.Succeed())
+
+			cqShared = testing.MakeClusterQueue("shared").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "4").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqShared)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqA)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqB)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqShared)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Admits workloads respecting fair share", func() {
+			ginkgo.By("Saturating cq-a")
+
+			aWorkloads := make([]*kueue.Workload, 10)
+			for i := range aWorkloads {
+				aWorkloads[i] = testing.MakeWorkload(fmt.Sprintf("a-%d", i), ns.Name).
+					Queue("a").
+					Request(corev1.ResourceCPU, "1").Obj()
+				gomega.Expect(k8sClient.Create(ctx, aWorkloads[i])).To(gomega.Succeed())
+			}
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 8)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 2)
+
+			ginkgo.By("Creating newer workloads in cq-b")
+			// Ensure workloads in cqB have a newer timestamp.
+			time.Sleep(time.Second)
+			bWorkloads := make([]*kueue.Workload, 5)
+			for i := range bWorkloads {
+				bWorkloads[i] = testing.MakeWorkload(fmt.Sprintf("b-%d", i), ns.Name).
+					Queue("b").
+					Request(corev1.ResourceCPU, "1").Obj()
+				gomega.Expect(k8sClient.Create(ctx, bWorkloads[i])).To(gomega.Succeed())
+			}
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 5)
+
+			ginkgo.By("Terminating 4 running workloads in cqA: shared quota is fair-shared")
+			finishRunningWorkloadsinCQ(cqA, 4)
+
+			// Admits 1 from cqA and 3 from cqB.
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 5)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 1)
+			util.ExpectReservingActiveWorkloadsMetric(cqB, 3)
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 2)
+
+			ginkgo.By("Terminating 2 more running workloads in cqA: cqB starts to take over shared quota")
+			finishRunningWorkloadsinCQ(cqA, 2)
+
+			// Admits last 1 from cqA and 1 from cqB.
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 4)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 0)
+			util.ExpectReservingActiveWorkloadsMetric(cqB, 4)
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 1)
+		})
+	})
+
+})
+
+func finishRunningWorkloadsinCQ(cq *kueue.ClusterQueue, n int) {
+	var wList kueue.WorkloadList
+	gomega.ExpectWithOffset(1, k8sClient.List(ctx, &wList)).To(gomega.Succeed())
+	finished := 0
+	for i := 0; i < len(wList.Items) && finished < n; i++ {
+		wl := wList.Items[i]
+		if wl.Status.Admission != nil && wl.Status.Admission.ClusterQueue == "a" && !meta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
```

  ```suggestion
  		if wl.Status.Admission != nil && wl.Status.Admission.ClusterQueue == cq.Name && !meta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
  ```
  ?

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:147** by [@mimowo](https://github.com/mimowo) — 2024-03-28T12:03:10Z
```diff
@@ -0,0 +1,159 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"fmt"
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Scheduler", func() {
+
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		defaultFlavor = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteResourceFlavor(ctx, k8sClient, defaultFlavor)).To(gomega.Succeed())
+	})
+
+	ginkgo.When("Preemption is disabled", func() {
+
+		var (
+			cqA      *kueue.ClusterQueue
+			lqA      *kueue.LocalQueue
+			cqB      *kueue.ClusterQueue
+			lqB      *kueue.LocalQueue
+			cqShared *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			cqA = testing.MakeClusterQueue("a").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "3").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqA)).To(gomega.Succeed())
+			lqA = testing.MakeLocalQueue("a", ns.Name).ClusterQueue("a").Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqA)).To(gomega.Succeed())
+
+			cqB = testing.MakeClusterQueue("b").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqB)).To(gomega.Succeed())
+			lqB = testing.MakeLocalQueue("b", ns.Name).ClusterQueue("b").Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqB)).To(gomega.Succeed())
+
+			cqShared = testing.MakeClusterQueue("shared").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "4").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqShared)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqA)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqB)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqShared)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Admits workloads respecting fair share", func() {
+			ginkgo.By("Saturating cq-a")
+
+			aWorkloads := make([]*kueue.Workload, 10)
+			for i := range aWorkloads {
+				aWorkloads[i] = testing.MakeWorkload(fmt.Sprintf("a-%d", i), ns.Name).
+					Queue("a").
+					Request(corev1.ResourceCPU, "1").Obj()
+				gomega.Expect(k8sClient.Create(ctx, aWorkloads[i])).To(gomega.Succeed())
+			}
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 8)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 2)
+
+			ginkgo.By("Creating newer workloads in cq-b")
+			// Ensure workloads in cqB have a newer timestamp.
+			time.Sleep(time.Second)
+			bWorkloads := make([]*kueue.Workload, 5)
+			for i := range bWorkloads {
+				bWorkloads[i] = testing.MakeWorkload(fmt.Sprintf("b-%d", i), ns.Name).
+					Queue("b").
+					Request(corev1.ResourceCPU, "1").Obj()
+				gomega.Expect(k8sClient.Create(ctx, bWorkloads[i])).To(gomega.Succeed())
+			}
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 5)
+
+			ginkgo.By("Terminating 4 running workloads in cqA: shared quota is fair-shared")
+			finishRunningWorkloadsinCQ(cqA, 4)
+
+			// Admits 1 from cqA and 3 from cqB.
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 5)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 1)
+			util.ExpectReservingActiveWorkloadsMetric(cqB, 3)
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 2)
+
+			ginkgo.By("Terminating 2 more running workloads in cqA: cqB starts to take over shared quota")
+			finishRunningWorkloadsinCQ(cqA, 2)
+
+			// Admits last 1 from cqA and 1 from cqB.
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 4)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 0)
+			util.ExpectReservingActiveWorkloadsMetric(cqB, 4)
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 1)
+		})
+	})
+
+})
+
+func finishRunningWorkloadsinCQ(cq *kueue.ClusterQueue, n int) {
```

  ```suggestion
  func finishRunningWorkloadsInCQ(cq *kueue.ClusterQueue, n int) {
  ```

- **pkg/cache/clusterqueue_test.go:679** by [@mimowo](https://github.com/mimowo) — 2024-03-28T12:08:29Z
```diff
@@ -620,3 +621,143 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 		})
 	}
 }
+
+func TestDominantResourceShare(t *testing.T) {
+	cases := map[string]struct {
+		cq          ClusterQueue
+		workload    *workload.Info
+		wantDRValue int
+		wantDRName  corev1.ResourceName
+	}{
+		"no cohort": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2_000,
+					},
+				},
+			},
+		},
+		"usage below nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    2_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    6,
+						},
+					},
+				},
+			},
+			wantDRName: corev1.ResourceCPU, // due to alphabetical order.
+		},
+		"usage above nominal": {
```

  I think it would be also worth adding a mixed test case - where we are below in one resource, but above in another to show that the max is returned.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-02T19:31:40Z

- **pkg/cache/clusterqueue.go:50** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T19:08:47Z
```diff
@@ -39,6 +39,16 @@ var (
 	errQueueAlreadyExists = errors.New("queue already exists")
 )
 
+// QuotaStats holds the nominal quota and usage for a resource.
+type QuotaStats struct {
+	Nominal  int64
+	Lendable int64
+	Usage    int64
+}
+
+// ResourceStats holds QuotaStats for resources.
+type ResourceStats map[corev1.ResourceName]*QuotaStats
```

  It needs to be a pointer so we can modify the values within the struct. It's a golang limitation (sometimes I miss C++ 🥲)

- **pkg/cache/clusterqueue.go:265** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T19:12:17Z
```diff
@@ -246,9 +262,27 @@ func filterQuantities(orig FlavorResourceQuantities, resourceGroups []kueue.Reso
 	return ret
 }
 
+func resetResourceStatsFromResourceGroups(orig ResourceStats, resourceGroups []kueue.ResourceGroup) ResourceStats {
```

  That's a good point.
  But I prefer to make it a proper reset by making it a method of ClusterQueue instead.

- **pkg/cache/clusterqueue.go:694** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T19:19:09Z
```diff
@@ -627,3 +677,26 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 
 	return cohortUsage
 }
+
+// DominantResourceShare returns a value from 0 to 100 representing the maximum of the ratios
+// of usage above nominal quota to the lendable resources in the cohort, among all the resources
+// provided by the ClusterQueue.
+// The function also returns the resource name that yielded this value.
+func (c *ClusterQueue) DominantResourceShare(w *workload.Info) (int, corev1.ResourceName) {
+	if c.Cohort == nil {
+		return 0, ""
+	}
+	var drs int64 = -1
+	var dRes corev1.ResourceName
+	wUsage := w.ResourceUsage()
+	for rName, rStats := range c.ResourceStats {
+		ratio := max(rStats.Usage+wUsage[rName]-rStats.Nominal, 0) * 100 /
+			c.Cohort.ResourceStats[rName].Lendable
```

  That would likely be a user error, but it could happen (you wouldn't try to share 0 resources in a cohort). Fixed and added unit test.

- **pkg/cache/clusterqueue_test.go:679** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T19:26:35Z
```diff
@@ -620,3 +621,143 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 		})
 	}
 }
+
+func TestDominantResourceShare(t *testing.T) {
+	cases := map[string]struct {
+		cq          ClusterQueue
+		workload    *workload.Info
+		wantDRValue int
+		wantDRName  corev1.ResourceName
+	}{
+		"no cohort": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2_000,
+					},
+				},
+			},
+		},
+		"usage below nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    2_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    6,
+						},
+					},
+				},
+			},
+			wantDRName: corev1.ResourceCPU, // due to alphabetical order.
+		},
+		"usage above nominal": {
```

  Done

- **pkg/cache/snapshot.go:56** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T19:27:09Z
```diff
@@ -41,28 +42,27 @@ type Snapshot struct {
 func (s *Snapshot) RemoveWorkload(wl *workload.Info) {
 	cq := s.ClusterQueues[wl.ClusterQueue]
 	delete(cq.Workloads, workload.Key(wl.Obj))
-	updateUsage(wl, cq.Usage, -1)
-	if cq.Cohort != nil {
-		if features.Enabled(features.LendingLimit) {
-			updateCohortUsage(wl, cq, -1)
-		} else {
-			updateUsage(wl, cq.Cohort.Usage, -1)
-		}
-	}
+	cq.addRemoveWorkload(wl, -1)
 }
 
 // AddWorkload adds a workload from its corresponding ClusterQueue and
 // updates resource usage.
 func (s *Snapshot) AddWorkload(wl *workload.Info) {
 	cq := s.ClusterQueues[wl.ClusterQueue]
 	cq.Workloads[workload.Key(wl.Obj)] = wl
-	updateUsage(wl, cq.Usage, 1)
-	if cq.Cohort != nil {
+	cq.addRemoveWorkload(wl, 1)
+}
+
+func (c *ClusterQueue) addRemoveWorkload(wl *workload.Info, m int64) {
```

  +1

- **pkg/scheduler/scheduler.go:597** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T19:29:01Z
```diff
@@ -582,7 +589,12 @@ func (e entryOrdering) Less(i, j int) bool {
 		return !aBorrows
 	}
 
-	// 2. Higher priority first if not disabled.
+	// 2. Fair share, if enabled.
+	if e.enableFairSharing && a.dominantResourceShare != b.dominantResourceShare {
+		return a.dominantResourceShare < b.dominantResourceShare
+	}
+
+	// 3. Higher priority first if not disabled.
```

  For some organizations, it might still be useful to allow higher priority jobs to be admitted first.
  
  It all depends on whether the organization has a centralized set of priorities, and somehow they are able to restrict their usage.

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:147** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T19:29:38Z
```diff
@@ -0,0 +1,159 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"fmt"
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Scheduler", func() {
+
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		defaultFlavor = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteResourceFlavor(ctx, k8sClient, defaultFlavor)).To(gomega.Succeed())
+	})
+
+	ginkgo.When("Preemption is disabled", func() {
+
+		var (
+			cqA      *kueue.ClusterQueue
+			lqA      *kueue.LocalQueue
+			cqB      *kueue.ClusterQueue
+			lqB      *kueue.LocalQueue
+			cqShared *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			cqA = testing.MakeClusterQueue("a").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "3").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqA)).To(gomega.Succeed())
+			lqA = testing.MakeLocalQueue("a", ns.Name).ClusterQueue("a").Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqA)).To(gomega.Succeed())
+
+			cqB = testing.MakeClusterQueue("b").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqB)).To(gomega.Succeed())
+			lqB = testing.MakeLocalQueue("b", ns.Name).ClusterQueue("b").Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqB)).To(gomega.Succeed())
+
+			cqShared = testing.MakeClusterQueue("shared").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "4").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqShared)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqA)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqB)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqShared)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Admits workloads respecting fair share", func() {
+			ginkgo.By("Saturating cq-a")
+
+			aWorkloads := make([]*kueue.Workload, 10)
+			for i := range aWorkloads {
+				aWorkloads[i] = testing.MakeWorkload(fmt.Sprintf("a-%d", i), ns.Name).
+					Queue("a").
+					Request(corev1.ResourceCPU, "1").Obj()
+				gomega.Expect(k8sClient.Create(ctx, aWorkloads[i])).To(gomega.Succeed())
+			}
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 8)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 2)
+
+			ginkgo.By("Creating newer workloads in cq-b")
+			// Ensure workloads in cqB have a newer timestamp.
+			time.Sleep(time.Second)
+			bWorkloads := make([]*kueue.Workload, 5)
+			for i := range bWorkloads {
+				bWorkloads[i] = testing.MakeWorkload(fmt.Sprintf("b-%d", i), ns.Name).
+					Queue("b").
+					Request(corev1.ResourceCPU, "1").Obj()
+				gomega.Expect(k8sClient.Create(ctx, bWorkloads[i])).To(gomega.Succeed())
+			}
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 5)
+
+			ginkgo.By("Terminating 4 running workloads in cqA: shared quota is fair-shared")
+			finishRunningWorkloadsinCQ(cqA, 4)
+
+			// Admits 1 from cqA and 3 from cqB.
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 5)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 1)
+			util.ExpectReservingActiveWorkloadsMetric(cqB, 3)
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 2)
+
+			ginkgo.By("Terminating 2 more running workloads in cqA: cqB starts to take over shared quota")
+			finishRunningWorkloadsinCQ(cqA, 2)
+
+			// Admits last 1 from cqA and 1 from cqB.
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 4)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 0)
+			util.ExpectReservingActiveWorkloadsMetric(cqB, 4)
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 1)
+		})
+	})
+
+})
+
+func finishRunningWorkloadsinCQ(cq *kueue.ClusterQueue, n int) {
```

  Done

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:153** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T19:31:07Z
```diff
@@ -0,0 +1,159 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"fmt"
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Scheduler", func() {
+
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		defaultFlavor = testing.MakeResourceFlavor("default").Obj()
+		gomega.Expect(k8sClient.Create(ctx, defaultFlavor)).To(gomega.Succeed())
+
+		ns = &corev1.Namespace{
+			ObjectMeta: metav1.ObjectMeta{
+				GenerateName: "core-",
+			},
+		}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteResourceFlavor(ctx, k8sClient, defaultFlavor)).To(gomega.Succeed())
+	})
+
+	ginkgo.When("Preemption is disabled", func() {
+
+		var (
+			cqA      *kueue.ClusterQueue
+			lqA      *kueue.LocalQueue
+			cqB      *kueue.ClusterQueue
+			lqB      *kueue.LocalQueue
+			cqShared *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			cqA = testing.MakeClusterQueue("a").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "3").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqA)).To(gomega.Succeed())
+			lqA = testing.MakeLocalQueue("a", ns.Name).ClusterQueue("a").Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqA)).To(gomega.Succeed())
+
+			cqB = testing.MakeClusterQueue("b").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "1").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqB)).To(gomega.Succeed())
+			lqB = testing.MakeLocalQueue("b", ns.Name).ClusterQueue("b").Obj()
+			gomega.Expect(k8sClient.Create(ctx, lqB)).To(gomega.Succeed())
+
+			cqShared = testing.MakeClusterQueue("shared").
+				Cohort("all").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("default").Resource(corev1.ResourceCPU, "4").Obj(),
+				).Obj()
+			gomega.Expect(k8sClient.Create(ctx, cqShared)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqA)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqB)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteClusterQueue(ctx, k8sClient, cqShared)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Admits workloads respecting fair share", func() {
+			ginkgo.By("Saturating cq-a")
+
+			aWorkloads := make([]*kueue.Workload, 10)
+			for i := range aWorkloads {
+				aWorkloads[i] = testing.MakeWorkload(fmt.Sprintf("a-%d", i), ns.Name).
+					Queue("a").
+					Request(corev1.ResourceCPU, "1").Obj()
+				gomega.Expect(k8sClient.Create(ctx, aWorkloads[i])).To(gomega.Succeed())
+			}
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 8)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 2)
+
+			ginkgo.By("Creating newer workloads in cq-b")
+			// Ensure workloads in cqB have a newer timestamp.
+			time.Sleep(time.Second)
+			bWorkloads := make([]*kueue.Workload, 5)
+			for i := range bWorkloads {
+				bWorkloads[i] = testing.MakeWorkload(fmt.Sprintf("b-%d", i), ns.Name).
+					Queue("b").
+					Request(corev1.ResourceCPU, "1").Obj()
+				gomega.Expect(k8sClient.Create(ctx, bWorkloads[i])).To(gomega.Succeed())
+			}
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 5)
+
+			ginkgo.By("Terminating 4 running workloads in cqA: shared quota is fair-shared")
+			finishRunningWorkloadsinCQ(cqA, 4)
+
+			// Admits 1 from cqA and 3 from cqB.
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 5)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 1)
+			util.ExpectReservingActiveWorkloadsMetric(cqB, 3)
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 2)
+
+			ginkgo.By("Terminating 2 more running workloads in cqA: cqB starts to take over shared quota")
+			finishRunningWorkloadsinCQ(cqA, 2)
+
+			// Admits last 1 from cqA and 1 from cqB.
+			util.ExpectReservingActiveWorkloadsMetric(cqA, 4)
+			util.ExpectPendingWorkloadsMetric(cqA, 0, 0)
+			util.ExpectReservingActiveWorkloadsMetric(cqB, 4)
+			util.ExpectPendingWorkloadsMetric(cqB, 0, 1)
+		})
+	})
+
+})
+
+func finishRunningWorkloadsinCQ(cq *kueue.ClusterQueue, n int) {
+	var wList kueue.WorkloadList
+	gomega.ExpectWithOffset(1, k8sClient.List(ctx, &wList)).To(gomega.Succeed())
+	finished := 0
+	for i := 0; i < len(wList.Items) && finished < n; i++ {
+		wl := wList.Items[i]
+		if wl.Status.Admission != nil && wl.Status.Admission.ClusterQueue == "a" && !meta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
```

  Definitely :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-03T08:53:38Z

- **pkg/cache/clusterqueue.go:265** by [@mimowo](https://github.com/mimowo) — 2024-04-03T08:53:38Z
```diff
@@ -246,9 +262,27 @@ func filterQuantities(orig FlavorResourceQuantities, resourceGroups []kueue.Reso
 	return ret
 }
 
+func resetResourceStatsFromResourceGroups(orig ResourceStats, resourceGroups []kueue.ResourceGroup) ResourceStats {
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-03T08:58:37Z

- **pkg/scheduler/scheduler.go:597** by [@mimowo](https://github.com/mimowo) — 2024-04-03T08:58:36Z
```diff
@@ -582,7 +589,12 @@ func (e entryOrdering) Less(i, j int) bool {
 		return !aBorrows
 	}
 
-	// 2. Higher priority first if not disabled.
+	// 2. Fair share, if enabled.
+	if e.enableFairSharing && a.dominantResourceShare != b.dominantResourceShare {
+		return a.dominantResourceShare < b.dominantResourceShare
+	}
+
+	// 3. Higher priority first if not disabled.
```

  > For some organizations, it might still be useful to allow higher priority jobs to be admitted first.
  
  Right, certainly there is room for some configuration in the future. For now I'm just questioning if the tie-breaking by priority is desired when fair sharing is used, and dominantResourceShare are equal. The KEP goals say: https://github.com/kubernetes-sigs/kueue/tree/main/keps/1714-fair-sharing#goals
  ```
  When enforcing fair sharing, ignore workload priorities unless:
  
  ** The workload's priority is above admin-defined high priority. Super high priority workloads overrule fair sharing and are treated according to KEP [#1337](https://github.com/kubernetes-sigs/kueue/tree/main/keps/1337-preempt-within-cohort-while-borrowing).
  ```
  This suggests to me that the tie breaker is creation time (bypassing priority) completely. I'm fine either way, just checking if this is not an omission.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-03T09:00:20Z

- **pkg/cache/clusterqueue.go:50** by [@mimowo](https://github.com/mimowo) — 2024-04-03T09:00:20Z
```diff
@@ -39,6 +39,16 @@ var (
 	errQueueAlreadyExists = errors.New("queue already exists")
 )
 
+// QuotaStats holds the nominal quota and usage for a resource.
+type QuotaStats struct {
+	Nominal  int64
+	Lendable int64
+	Usage    int64
+}
+
+// ResourceStats holds QuotaStats for resources.
+type ResourceStats map[corev1.ResourceName]*QuotaStats
```

  ah, I see so that we can write `.Lendable += nominal`, sg.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-03T09:02:05Z

- **pkg/cache/clusterqueue.go:694** by [@mimowo](https://github.com/mimowo) — 2024-04-03T09:02:05Z
```diff
@@ -627,3 +677,26 @@ func (c *ClusterQueue) UsedCohortQuota(fName kueue.ResourceFlavorReference, rNam
 
 	return cohortUsage
 }
+
+// DominantResourceShare returns a value from 0 to 100 representing the maximum of the ratios
+// of usage above nominal quota to the lendable resources in the cohort, among all the resources
+// provided by the ClusterQueue.
+// The function also returns the resource name that yielded this value.
+func (c *ClusterQueue) DominantResourceShare(w *workload.Info) (int, corev1.ResourceName) {
+	if c.Cohort == nil {
+		return 0, ""
+	}
+	var drs int64 = -1
+	var dRes corev1.ResourceName
+	wUsage := w.ResourceUsage()
+	for rName, rStats := range c.ResourceStats {
+		ratio := max(rStats.Usage+wUsage[rName]-rStats.Nominal, 0) * 100 /
+			c.Cohort.ResourceStats[rName].Lendable
```

  I suppose there might be some legitimate scenarios. For example you may want to use sharing for one resource (say CPU), but set `lendingLimit=0` for GPU to prevent sharing of them, in a different resource flavor.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-03T09:04:03Z

- **pkg/cache/clusterqueue_test.go:783** by [@mimowo](https://github.com/mimowo) — 2024-04-03T09:04:03Z
```diff
@@ -620,3 +621,207 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 		})
 	}
 }
+
+func TestDominantResourceShare(t *testing.T) {
+	cases := map[string]struct {
+		cq          ClusterQueue
+		workload    *workload.Info
+		wantDRValue int
+		wantDRName  corev1.ResourceName
+	}{
+		"no cohort": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2_000,
+					},
+				},
+			},
+		},
+		"usage below nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    2_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    6,
+						},
+					},
+				},
+			},
+			wantDRName: corev1.ResourceCPU, // due to alphabetical order.
+		},
+		"usage above nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    3_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    7,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    10_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    10,
+						},
+					},
+				},
+			},
+			wantDRName:  "example.com/gpu",
+			wantDRValue: 20, // (7-5)/10
+		},
+		"one resource above nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    3_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    3,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    10_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    10,
+						},
+					},
+				},
+			},
+			wantDRName:  corev1.ResourceCPU,
+			wantDRValue: 10, // (3-2)/10
+		},
+		"usage with workload above nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    2_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    6,
+						},
+					},
+				},
+			},
+			workload: &workload.Info{
+				TotalRequests: []workload.PodSetResources{{
+					Requests: workload.Requests{
+						corev1.ResourceCPU: 4_000,
+						"example.com/gpu":  4,
+					},
+				}},
+			},
+			wantDRName:  corev1.ResourceCPU,
+			wantDRValue: 30, // (1+4-2)/10
+		},
+		"A resource with zero nominal": {
```

  I see this triggers division by 0 scenario, but maybe a slightly more realistic scenario is to set `lendingLimit: 0` and `nominal>0` (as mentioned in the other comment)?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-03T13:35:43Z

- **pkg/scheduler/scheduler.go:597** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T13:35:43Z
```diff
@@ -582,7 +589,12 @@ func (e entryOrdering) Less(i, j int) bool {
 		return !aBorrows
 	}
 
-	// 2. Higher priority first if not disabled.
+	// 2. Fair share, if enabled.
+	if e.enableFairSharing && a.dominantResourceShare != b.dominantResourceShare {
+		return a.dominantResourceShare < b.dominantResourceShare
+	}
+
+	// 3. Higher priority first if not disabled.
```

  The KEP missed the point that whether to use priority as a tie-breaker is guarded by a feature gate already.
  
  I'm not sure what to do with the priority long term, but for now, it makes sense to keep it guarded.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-03T13:43:50Z

- **pkg/cache/clusterqueue_test.go:783** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T13:43:50Z
```diff
@@ -620,3 +621,207 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 		})
 	}
 }
+
+func TestDominantResourceShare(t *testing.T) {
+	cases := map[string]struct {
+		cq          ClusterQueue
+		workload    *workload.Info
+		wantDRValue int
+		wantDRName  corev1.ResourceName
+	}{
+		"no cohort": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2_000,
+					},
+				},
+			},
+		},
+		"usage below nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    2_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    6,
+						},
+					},
+				},
+			},
+			wantDRName: corev1.ResourceCPU, // due to alphabetical order.
+		},
+		"usage above nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    3_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    7,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    10_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    10,
+						},
+					},
+				},
+			},
+			wantDRName:  "example.com/gpu",
+			wantDRValue: 20, // (7-5)/10
+		},
+		"one resource above nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    3_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    3,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    10_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    10,
+						},
+					},
+				},
+			},
+			wantDRName:  corev1.ResourceCPU,
+			wantDRValue: 10, // (3-2)/10
+		},
+		"usage with workload above nominal": {
+			cq: ClusterQueue{
+				ResourceStats: ResourceStats{
+					corev1.ResourceCPU: {
+						Nominal:  2_000,
+						Lendable: 2_000,
+						Usage:    1_000,
+					},
+					"example.com/gpu": {
+						Nominal:  5,
+						Lendable: 5,
+						Usage:    2,
+					},
+				},
+				Cohort: &Cohort{
+					ResourceStats: ResourceStats{
+						corev1.ResourceCPU: {
+							Nominal:  10_000,
+							Lendable: 10_000,
+							Usage:    2_000,
+						},
+						"example.com/gpu": {
+							Nominal:  10,
+							Lendable: 10,
+							Usage:    6,
+						},
+					},
+				},
+			},
+			workload: &workload.Info{
+				TotalRequests: []workload.PodSetResources{{
+					Requests: workload.Requests{
+						corev1.ResourceCPU: 4_000,
+						"example.com/gpu":  4,
+					},
+				}},
+			},
+			wantDRName:  corev1.ResourceCPU,
+			wantDRValue: 30, // (1+4-2)/10
+		},
+		"A resource with zero nominal": {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-03T14:03:59Z

- **pkg/scheduler/scheduler.go:597** by [@mimowo](https://github.com/mimowo) — 2024-04-03T14:03:58Z
```diff
@@ -582,7 +589,12 @@ func (e entryOrdering) Less(i, j int) bool {
 		return !aBorrows
 	}
 
-	// 2. Higher priority first if not disabled.
+	// 2. Fair share, if enabled.
+	if e.enableFairSharing && a.dominantResourceShare != b.dominantResourceShare {
+		return a.dominantResourceShare < b.dominantResourceShare
+	}
+
+	// 3. Higher priority first if not disabled.
```

  Sgtm, so one can use `PrioritySortingWithinCohort` independently,

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-03T14:19:27Z

Implementations are lgtm
I didn't review integration test, yet.

- **pkg/cache/clusterqueue.go:271** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T13:39:10Z
```diff
@@ -246,9 +262,29 @@ func filterQuantities(orig FlavorResourceQuantities, resourceGroups []kueue.Reso
 	return ret
 }
 
+// resetResourceStatsFromResourceGroups maintains the Usage stats for the given resource groups
+// and resets Nominal and Lendable values. They are calculated again in updateResourceGroups.
+func (c *ClusterQueue) resetResourceStatsFromResourceGroups(resourceGroups []kueue.ResourceGroup) {
+	updatedResourceStats := make(ResourceStats, len(resourceGroups))
+	for _, rg := range resourceGroups {
+		for _, res := range rg.CoveredResources {
+			if oStats := c.ResourceStats[res]; oStats != nil {
```

  ```suggestion
  			if _, exist := updatedResourceStats; exist {
  						continue
  			}
  			if oStats := c.ResourceStats[res]; oStats != nil {
  ```
  
  Is it possible to avoid updating the same resource many times?

- **pkg/cache/clusterqueue.go:519** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T13:44:35Z
```diff
@@ -454,23 +495,34 @@ func (c *ClusterQueue) reportActiveWorkloads() {
 // and the number of admitted workloads for local queues.
 func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	admitted := workload.IsAdmitted(wi.Obj)
-	updateUsage(wi, c.Usage, m)
+	updateFlavorUsage(wi, c.Usage, m)
+	updateResourceStats(wi, c.ResourceStats, m)
 	if admitted {
-		updateUsage(wi, c.AdmittedUsage, m)
+		updateFlavorUsage(wi, c.AdmittedUsage, m)
 		c.admittedWorkloadsCount += int(m)
 	}
 	qKey := workload.QueueKey(wi.Obj)
 	if lq, ok := c.localQueues[qKey]; ok {
-		updateUsage(wi, lq.usage, m)
+		updateFlavorUsage(wi, lq.usage, m)
 		lq.reservingWorkloads += int(m)
 		if admitted {
-			updateUsage(wi, lq.admittedUsage, m)
+			updateFlavorUsage(wi, lq.admittedUsage, m)
 			lq.admittedWorkloads += int(m)
 		}
 	}
 }
 
-func updateUsage(wi *workload.Info, flvUsage FlavorResourceQuantities, m int64) {
+func updateResourceStats(wi *workload.Info, rStats ResourceStats, m int64) {
+	for _, ps := range wi.TotalRequests {
+		for res, v := range ps.Requests {
+			if _, exists := rStats[res]; exists {
+				rStats[res].Usage += v * m
```

  ```suggestion
  				if rStarts[res] == nil {
  					rStats[res].Usage = &QuotaStats{Usage: v * m}
  				}
  				rStats[res].Usage += v * m
  ```
  
  Should we avoid nil pointer errors?

- **pkg/scheduler/scheduler.go:597** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T14:16:38Z
```diff
@@ -582,7 +589,12 @@ func (e entryOrdering) Less(i, j int) bool {
 		return !aBorrows
 	}
 
-	// 2. Higher priority first if not disabled.
+	// 2. Fair share, if enabled.
+	if e.enableFairSharing && a.dominantResourceShare != b.dominantResourceShare {
+		return a.dominantResourceShare < b.dominantResourceShare
+	}
+
+	// 3. Higher priority first if not disabled.
```

  > The KEP missed the point that whether to use priority as a tie-breaker is guarded by a feature gate already.
  > 
  > I'm not sure what to do with the priority long term, but for now, it makes sense to keep it guarded.
  
  +1

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-03T14:59:31Z

- **pkg/cache/clusterqueue.go:271** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T14:56:46Z
```diff
@@ -246,9 +262,29 @@ func filterQuantities(orig FlavorResourceQuantities, resourceGroups []kueue.Reso
 	return ret
 }
 
+// resetResourceStatsFromResourceGroups maintains the Usage stats for the given resource groups
+// and resets Nominal and Lendable values. They are calculated again in updateResourceGroups.
+func (c *ClusterQueue) resetResourceStatsFromResourceGroups(resourceGroups []kueue.ResourceGroup) {
+	updatedResourceStats := make(ResourceStats, len(resourceGroups))
+	for _, rg := range resourceGroups {
+		for _, res := range rg.CoveredResources {
+			if oStats := c.ResourceStats[res]; oStats != nil {
```

  Each resource can only exist in one resourceGroup, so this additional check is not necessary

- **pkg/cache/clusterqueue.go:519** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T14:59:13Z
```diff
@@ -454,23 +495,34 @@ func (c *ClusterQueue) reportActiveWorkloads() {
 // and the number of admitted workloads for local queues.
 func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	admitted := workload.IsAdmitted(wi.Obj)
-	updateUsage(wi, c.Usage, m)
+	updateFlavorUsage(wi, c.Usage, m)
+	updateResourceStats(wi, c.ResourceStats, m)
 	if admitted {
-		updateUsage(wi, c.AdmittedUsage, m)
+		updateFlavorUsage(wi, c.AdmittedUsage, m)
 		c.admittedWorkloadsCount += int(m)
 	}
 	qKey := workload.QueueKey(wi.Obj)
 	if lq, ok := c.localQueues[qKey]; ok {
-		updateUsage(wi, lq.usage, m)
+		updateFlavorUsage(wi, lq.usage, m)
 		lq.reservingWorkloads += int(m)
 		if admitted {
-			updateUsage(wi, lq.admittedUsage, m)
+			updateFlavorUsage(wi, lq.admittedUsage, m)
 			lq.admittedWorkloads += int(m)
 		}
 	}
 }
 
-func updateUsage(wi *workload.Info, flvUsage FlavorResourceQuantities, m int64) {
+func updateResourceStats(wi *workload.Info, rStats ResourceStats, m int64) {
+	for _, ps := range wi.TotalRequests {
+		for res, v := range ps.Requests {
+			if _, exists := rStats[res]; exists {
+				rStats[res].Usage += v * m
```

  Line 518 already checks this.
  
  Also, rStats should have all the resources that the Workload requests (otherwise the Workload wouldn't have been admitted there).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-03T15:21:26Z

- **pkg/cache/clusterqueue.go:271** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T15:21:26Z
```diff
@@ -246,9 +262,29 @@ func filterQuantities(orig FlavorResourceQuantities, resourceGroups []kueue.Reso
 	return ret
 }
 
+// resetResourceStatsFromResourceGroups maintains the Usage stats for the given resource groups
+// and resets Nominal and Lendable values. They are calculated again in updateResourceGroups.
+func (c *ClusterQueue) resetResourceStatsFromResourceGroups(resourceGroups []kueue.ResourceGroup) {
+	updatedResourceStats := make(ResourceStats, len(resourceGroups))
+	for _, rg := range resourceGroups {
+		for _, res := range rg.CoveredResources {
+			if oStats := c.ResourceStats[res]; oStats != nil {
```

  I see. That makes sense. Thank you for this clarification :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-03T15:22:17Z

- **pkg/cache/clusterqueue.go:519** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T15:22:17Z
```diff
@@ -454,23 +495,34 @@ func (c *ClusterQueue) reportActiveWorkloads() {
 // and the number of admitted workloads for local queues.
 func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	admitted := workload.IsAdmitted(wi.Obj)
-	updateUsage(wi, c.Usage, m)
+	updateFlavorUsage(wi, c.Usage, m)
+	updateResourceStats(wi, c.ResourceStats, m)
 	if admitted {
-		updateUsage(wi, c.AdmittedUsage, m)
+		updateFlavorUsage(wi, c.AdmittedUsage, m)
 		c.admittedWorkloadsCount += int(m)
 	}
 	qKey := workload.QueueKey(wi.Obj)
 	if lq, ok := c.localQueues[qKey]; ok {
-		updateUsage(wi, lq.usage, m)
+		updateFlavorUsage(wi, lq.usage, m)
 		lq.reservingWorkloads += int(m)
 		if admitted {
-			updateUsage(wi, lq.admittedUsage, m)
+			updateFlavorUsage(wi, lq.admittedUsage, m)
 			lq.admittedWorkloads += int(m)
 		}
 	}
 }
 
-func updateUsage(wi *workload.Info, flvUsage FlavorResourceQuantities, m int64) {
+func updateResourceStats(wi *workload.Info, rStats ResourceStats, m int64) {
+	for _, ps := range wi.TotalRequests {
+		for res, v := range ps.Requests {
+			if _, exists := rStats[res]; exists {
+				rStats[res].Usage += v * m
```

  That makes sense. That is a great pointer.
  Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-03T17:28:32Z

Oops. I forgot to submit my comment.
Just a nit :)

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:2** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T15:26:13Z
```diff
@@ -0,0 +1,159 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  ```suggestion
  Copyright 2024 The Kubernetes Authors.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-03T17:42:15Z

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:2** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T17:42:15Z
```diff
@@ -0,0 +1,159 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  I'm living in the past :)
  Fixed

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-03T17:44:10Z

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:2** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T17:44:10Z
```diff
@@ -0,0 +1,159 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  🙃
