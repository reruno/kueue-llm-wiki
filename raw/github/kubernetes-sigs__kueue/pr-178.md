# PR #178: Distinguish workload type before requeue

**Summary**: Distinguish workload type before requeue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/178

**Last updated**: 2022-04-12T16:49:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@denkensk](https://github.com/denkensk)
- **Branch**: `requeue-reason` → `main`
- **Created**: 2022-04-06T07:17:15Z
- **Updated**: 2022-04-12T16:49:23Z
- **Merged**: 2022-04-12T16:49:23Z
- **Closed**: 2022-04-12T16:49:23Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 9   **+182 / -35**

## Description

Signed-off-by: Alex Wang <wangqingcan1990@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
https://github.com/kubernetes-sigs/kueue/pull/135#discussion_r840729523

We have a case where we requeue because the workload didn't fit. But we also have a case where we requeue because we admitted workloads in another CQ that had higher priority, even though they might still fit in the next scheduling cycle. I don't think we should punish those workloads. They should skip the inadmissible stage.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:

## Discussion

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-06T08:17:22Z

/assign @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T12:18:37Z

/lgtm

leaving the approve to Aldo

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T13:07:32Z

I sent https://github.com/kubernetes-sigs/kueue/pull/185 so we are consistent with using the term `inadmissible`

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-12T16:40:50Z

squashed

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T16:43:34Z

/lgtm
/hold cancel

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-12T16:47:01Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-12T16:47:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/178#issuecomment-1096961092" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/178#" title="Author self-approved">denkensk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-06T13:50:36Z

- **pkg/scheduler/scheduler.go:414** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T13:50:36Z
```diff
@@ -411,6 +411,13 @@ func (e entryOrdering) Less(i, j int) bool {
 }
 
 func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
+	if message == "cohort used in this cycle" {
```

  lets add a flag to `entry` and pass entry instead of workload.Info.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T14:14:30Z

- **pkg/scheduler/scheduler.go:414** by [@denkensk](https://github.com/denkensk) — 2022-04-06T14:14:30Z
```diff
@@ -411,6 +411,13 @@ func (e entryOrdering) Less(i, j int) bool {
 }
 
 func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
+	if message == "cohort used in this cycle" {
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-06T14:26:25Z

- **pkg/scheduler/scheduler.go:419** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T14:15:13Z
```diff
@@ -410,10 +414,17 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateWorkloadStatus(ctx, s.client, w.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry) {
+	if e.skipped {
+		if !s.queues.AddOrUpdateWorkload(e.Obj) {
```

  hmm, I think we need to `AddIfNotExist` here, we shouldn't update if there is an existing version because if the workload is already back in the queue, it means we received an update event.

- **pkg/scheduler/scheduler.go:159** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T14:24:01Z
```diff
@@ -154,6 +155,9 @@ type entry struct {
 	admissible bool
 	// the reason if the workload wasn't admitted
 	noAdmissionReason string
+	// indicates if the workload is skipped because the cohort it wants to
+	// borrow from has been used in the same scheduling cycle.
```

  ```suggestion
  	// indicates if the workload was skipped in this cycle.
  ```

- **pkg/scheduler/scheduler.go:422** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T14:25:01Z
```diff
@@ -410,10 +414,17 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateWorkloadStatus(ctx, s.client, w.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry) {
+	if e.skipped {
+		if !s.queues.AddOrUpdateWorkload(e.Obj) {
+			log.V(2).Info("Queue for workload didn't exist; ignored for now", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName))
+		}
+		return
+	}
+
+	added := s.queues.RequeueWorkload(ctx, &e.Info)
+	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added)
+	err := workload.UpdateWorkloadStatus(ctx, s.client, e.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", e.noAdmissionReason)
```

  I think we should not update the workload status if the workload was skipped;

- **pkg/scheduler/scheduler.go:267** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T14:25:59Z
```diff
@@ -263,7 +267,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			return
 		}
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
+		s.requeueAndUpdate(log, ctx, *e)
 		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
```

  we shouldn't post an event if the workload was skipped, same thing with status update below.

- **pkg/scheduler/scheduler.go:115** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T14:26:22Z
```diff
@@ -110,6 +110,7 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
 			e.admissible = false
+			e.skipped = true
 			// TODO(): we shouldn't emit an event or update the workload condition in this
 			// case, just re-queue directly.
```

  we can fix this in this PR now that we have the skipped flag.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T14:34:49Z

- **pkg/scheduler/scheduler.go:422** by [@denkensk](https://github.com/denkensk) — 2022-04-06T14:34:48Z
```diff
@@ -410,10 +414,17 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateWorkloadStatus(ctx, s.client, w.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry) {
+	if e.skipped {
+		if !s.queues.AddOrUpdateWorkload(e.Obj) {
+			log.V(2).Info("Queue for workload didn't exist; ignored for now", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName))
+		}
+		return
+	}
+
+	added := s.queues.RequeueWorkload(ctx, &e.Info)
+	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added)
+	err := workload.UpdateWorkloadStatus(ctx, s.client, e.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", e.noAdmissionReason)
```

  it returns in line 422

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-06T14:38:34Z

- **pkg/scheduler/scheduler.go:267** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T14:35:14Z
```diff
@@ -263,7 +267,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			return
 		}
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
+		s.requeueAndUpdate(log, ctx, *e)
 		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
```

  I think we can bring the Event recording inside `requeueAndUpdate` (and skip it accordingly)

- **test/integration/scheduler/scheduler_test.go:526** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T14:38:25Z
```diff
@@ -512,4 +522,40 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
+
+	ginkgo.It("Should schedule jobs using borrowed ClusterQueue in the same Cohort", func() {
```

  ```suggestion
  	ginkgo.It("Should schedule jobs borrowing quota from ClusterQueues in the same Cohort", func() {
  ```

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T14:56:35Z

- **pkg/scheduler/scheduler.go:419** by [@denkensk](https://github.com/denkensk) — 2022-04-06T14:56:35Z
```diff
@@ -410,10 +414,17 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateWorkloadStatus(ctx, s.client, w.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry) {
+	if e.skipped {
+		if !s.queues.AddOrUpdateWorkload(e.Obj) {
```

  Added.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T14:56:42Z

- **pkg/scheduler/scheduler.go:159** by [@denkensk](https://github.com/denkensk) — 2022-04-06T14:56:41Z
```diff
@@ -154,6 +155,9 @@ type entry struct {
 	admissible bool
 	// the reason if the workload wasn't admitted
 	noAdmissionReason string
+	// indicates if the workload is skipped because the cohort it wants to
+	// borrow from has been used in the same scheduling cycle.
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T14:56:52Z

- **pkg/scheduler/scheduler.go:115** by [@denkensk](https://github.com/denkensk) — 2022-04-06T14:56:51Z
```diff
@@ -110,6 +110,7 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
 			e.admissible = false
+			e.skipped = true
 			// TODO(): we shouldn't emit an event or update the workload condition in this
 			// case, just re-queue directly.
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T14:57:57Z

- **pkg/scheduler/scheduler.go:267** by [@denkensk](https://github.com/denkensk) — 2022-04-06T14:57:56Z
```diff
@@ -263,7 +267,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			return
 		}
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
+		s.requeueAndUpdate(log, ctx, *e)
 		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
```

  Yes. Moved them into `requeueAndUpdate `

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-06T15:01:15Z

- **test/integration/scheduler/scheduler_test.go:115** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T14:49:25Z
```diff
@@ -103,19 +104,27 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		prodBEClusterQ = testing.MakeClusterQueue("prod-be-cq").
 			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
 			Resource(testing.MakeResource(corev1.ResourceCPU).
-				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("10").Obj()).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, prodBEClusterQ)).Should(gomega.Succeed())
 
 		devBEClusterQ = testing.MakeClusterQueue("dev-be-cq").
 			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
 			Resource(testing.MakeResource(corev1.ResourceCPU).
-				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("10").Obj()).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("15").Obj()).
```

  this and the change above to 15 should make the jobs admissible from the get go, so we need to reverse them.

- **test/integration/scheduler/scheduler_test.go:530** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T14:51:32Z
```diff
@@ -512,4 +522,40 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
+
+	ginkgo.It("Should schedule jobs using borrowed ClusterQueue in the same Cohort", func() {
+		queue1 := testing.MakeQueue("queue1", ns.Name).ClusterQueue(prodBEClusterQ.Name).Obj()
+		queue2 := testing.MakeQueue("queue2", ns.Name).ClusterQueue(devBEClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue1)).Should(gomega.Succeed())
+		gomega.Expect(k8sClient.Create(ctx, queue2)).Should(gomega.Succeed())
```

  do we need those? there is already `prodBEQueue` and `devBEQueue` created in `BeforeEach`

- **test/integration/scheduler/scheduler_test.go:565** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T14:52:37Z
```diff
@@ -512,4 +522,40 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
+
+	ginkgo.It("Should schedule jobs using borrowed ClusterQueue in the same Cohort", func() {
+		queue1 := testing.MakeQueue("queue1", ns.Name).ClusterQueue(prodBEClusterQ.Name).Obj()
+		queue2 := testing.MakeQueue("queue2", ns.Name).ClusterQueue(devBEClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue1)).Should(gomega.Succeed())
+		gomega.Expect(k8sClient.Create(ctx, queue2)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue1.Name).Request(corev1.ResourceCPU, "11").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue2.Name).Request(corev1.ResourceCPU, "11").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
```

  since the quota was increased to 15, I expect the jobs already started, we need a `Consistently` check here verifying that the jobs continues to be suspended.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-06T15:14:25Z

- **pkg/queue/manager.go:270** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T15:12:59Z
```diff
@@ -265,6 +265,31 @@ func (m *Manager) AddOrUpdateWorkload(w *kueue.QueuedWorkload) bool {
 	return m.addOrUpdateWorkload(w)
 }
 
+// AddWorkloadIfNotExist adds workload to the corresponding queue.
+// Returns whether the queue existed.
+func (m *Manager) AddWorkloadIfNotExist(w *workload.Info) bool {
```

  What if you add a boolean parameter to `RequeueWorkload` instead?
  
  Otherwise there is too much repeated code.
  
  And maybe you can do the same in the `ClusterQueue` interface.
  
  something like: `RequeueIfNotPresent(*workload.Info, bool)`

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T15:20:50Z

- **test/integration/scheduler/scheduler_test.go:565** by [@denkensk](https://github.com/denkensk) — 2022-04-06T15:20:50Z
```diff
@@ -512,4 +522,40 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
+
+	ginkgo.It("Should schedule jobs using borrowed ClusterQueue in the same Cohort", func() {
+		queue1 := testing.MakeQueue("queue1", ns.Name).ClusterQueue(prodBEClusterQ.Name).Obj()
+		queue2 := testing.MakeQueue("queue2", ns.Name).ClusterQueue(devBEClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue1)).Should(gomega.Succeed())
+		gomega.Expect(k8sClient.Create(ctx, queue2)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue1.Name).Request(corev1.ResourceCPU, "11").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue2.Name).Request(corev1.ResourceCPU, "11").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
```

  Added but It won't. Because the request of job1 > prodBEClusterQ.generated +  devBEClusterQ.generated +  testBEClusterQ.generated  at the beginning.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T15:23:31Z

- **pkg/queue/manager.go:270** by [@denkensk](https://github.com/denkensk) — 2022-04-06T15:23:31Z
```diff
@@ -265,6 +265,31 @@ func (m *Manager) AddOrUpdateWorkload(w *kueue.QueuedWorkload) bool {
 	return m.addOrUpdateWorkload(w)
 }
 
+// AddWorkloadIfNotExist adds workload to the corresponding queue.
+// Returns whether the queue existed.
+func (m *Manager) AddWorkloadIfNotExist(w *workload.Info) bool {
```

  Not only the `ClusterQueue` but also the queue.
  ```go
  func (m *Manager) addWorkloadIfNotExist(w *workload.Info) bool {
  	qKey := queueKeyForWorkload(w.Obj)
  	q := m.queues[qKey]
  	if q == nil {
  		return false
  	}
  	q.AddIfNotPresent(w)   ---> here
  	cq := m.clusterQueues[q.ClusterQueue]
  	if cq == nil {
  		return false
  	}
  	if cq.PushIfNotPresent(w) { ---> here
  		m.cond.Broadcast()
  	}
  	return true
  }
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-06T15:30:13Z

- **test/integration/scheduler/scheduler_test.go:115** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T15:30:13Z
```diff
@@ -103,19 +104,27 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		prodBEClusterQ = testing.MakeClusterQueue("prod-be-cq").
 			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
 			Resource(testing.MakeResource(corev1.ResourceCPU).
-				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("10").Obj()).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, prodBEClusterQ)).Should(gomega.Succeed())
 
 		devBEClusterQ = testing.MakeClusterQueue("dev-be-cq").
 			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
 			Resource(testing.MakeResource(corev1.ResourceCPU).
-				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("10").Obj()).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("15").Obj()).
```

  ah, sorry, missed that this is raising the ceiling.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-06T15:38:11Z

- **pkg/scheduler/scheduler.go:415** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T15:34:21Z
```diff
@@ -410,11 +409,19 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateWorkloadStatus(ctx, s.client, w.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	if e.skipped {
+		if !s.queues.AddWorkloadIfNotExist(&e.Info) {
+			log.V(2).Info("Queue for workload didn't exist; ignored for now", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName))
```

  ```suggestion
  			log.V(2).Info("Queue or ClusterQueue for workload didn't exist; ignored for now", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName))
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-06T15:38:42Z

- **test/integration/scheduler/scheduler_test.go:565** by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T15:38:42Z
```diff
@@ -512,4 +522,40 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
+
+	ginkgo.It("Should schedule jobs using borrowed ClusterQueue in the same Cohort", func() {
+		queue1 := testing.MakeQueue("queue1", ns.Name).ClusterQueue(prodBEClusterQ.Name).Obj()
+		queue2 := testing.MakeQueue("queue2", ns.Name).ClusterQueue(devBEClusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue1)).Should(gomega.Succeed())
+		gomega.Expect(k8sClient.Create(ctx, queue2)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue1.Name).Request(corev1.ResourceCPU, "11").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue2.Name).Request(corev1.ResourceCPU, "11").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
```

  right, I missed that the increase was for the ceiling, I thought it was for the guaranteed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-06T16:02:40Z

- **pkg/queue/manager.go:270** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T16:02:39Z
```diff
@@ -265,6 +265,31 @@ func (m *Manager) AddOrUpdateWorkload(w *kueue.QueuedWorkload) bool {
 	return m.addOrUpdateWorkload(w)
 }
 
+// AddWorkloadIfNotExist adds workload to the corresponding queue.
+// Returns whether the queue existed.
+func (m *Manager) AddWorkloadIfNotExist(w *workload.Info) bool {
```

  Queue might not need the distinction. At least not for now.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T16:33:39Z

- **pkg/queue/manager.go:270** by [@denkensk](https://github.com/denkensk) — 2022-04-06T16:33:39Z
```diff
@@ -265,6 +265,31 @@ func (m *Manager) AddOrUpdateWorkload(w *kueue.QueuedWorkload) bool {
 	return m.addOrUpdateWorkload(w)
 }
 
+// AddWorkloadIfNotExist adds workload to the corresponding queue.
+// Returns whether the queue existed.
+func (m *Manager) AddWorkloadIfNotExist(w *workload.Info) bool {
```

  Maybe we also need to distinguish the queue.AddOrUpdate and queue.AddIfNotPresent here?
  It also can be updated if we received an update event..

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T16:36:49Z

- **pkg/queue/manager.go:270** by [@denkensk](https://github.com/denkensk) — 2022-04-06T16:36:49Z
```diff
@@ -265,6 +265,31 @@ func (m *Manager) AddOrUpdateWorkload(w *kueue.QueuedWorkload) bool {
 	return m.addOrUpdateWorkload(w)
 }
 
+// AddWorkloadIfNotExist adds workload to the corresponding queue.
+// Returns whether the queue existed.
+func (m *Manager) AddWorkloadIfNotExist(w *workload.Info) bool {
```

  IIUC. Your suggestion is to add  a boolean parameter in`RequeueWorkload`? And call AddOrUpdate or queue.AddIfNotPresent based on the bool? Right?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T16:46:57Z

- **pkg/queue/manager.go:270** by [@denkensk](https://github.com/denkensk) — 2022-04-06T16:46:57Z
```diff
@@ -265,6 +265,31 @@ func (m *Manager) AddOrUpdateWorkload(w *kueue.QueuedWorkload) bool {
 	return m.addOrUpdateWorkload(w)
 }
 
+// AddWorkloadIfNotExist adds workload to the corresponding queue.
+// Returns whether the queue existed.
+func (m *Manager) AddWorkloadIfNotExist(w *workload.Info) bool {
```

  Updated. Please take a look.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-06T16:47:54Z

- **pkg/scheduler/scheduler.go:415** by [@denkensk](https://github.com/denkensk) — 2022-04-06T16:47:54Z
```diff
@@ -410,11 +409,19 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateWorkloadStatus(ctx, s.client, w.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	if e.skipped {
+		if !s.queues.AddWorkloadIfNotExist(&e.Info) {
+			log.V(2).Info("Queue for workload didn't exist; ignored for now", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName))
```

  updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-06T17:32:36Z

- **pkg/queue/cluster_queue_interface.go:48** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T17:30:09Z
```diff
@@ -43,6 +43,10 @@ type ClusterQueue interface {
 	// PushOrUpdate pushes the workload to ClusterQueue.
 	// If the workload is already present, updates with the new one.
 	PushOrUpdate(*kueue.QueuedWorkload)
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, returns false. Otherwise returns true.
+	PushIfNotPresent(*workload.Info) bool
```

  Instead of adding this function, rename `AddInadmissibleIfNotPresent` to `RequeueIfNotPresent(workload *workload.Info, inadmissible bool)`

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-07T03:38:32Z

- **pkg/queue/cluster_queue_interface.go:48** by [@denkensk](https://github.com/denkensk) — 2022-04-07T03:38:32Z
```diff
@@ -43,6 +43,10 @@ type ClusterQueue interface {
 	// PushOrUpdate pushes the workload to ClusterQueue.
 	// If the workload is already present, updates with the new one.
 	PushOrUpdate(*kueue.QueuedWorkload)
+	// PushIfNotPresent pushes the workload to ClusterQueue.
+	// If the workload is already present, returns false. Otherwise returns true.
+	PushIfNotPresent(*workload.Info) bool
```

  Updated.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T11:41:33Z

- **pkg/queue/manager.go:287** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T11:41:33Z
```diff
@@ -284,7 +284,7 @@ func (m *Manager) addOrUpdateWorkload(w *kueue.QueuedWorkload) bool {
 // RequeueWorkload requeues the workload ensuring that the queue and the
 // workload still exist in the client cache and it's not admitted. It won't
 // requeue if the workload is already in the queue (possible if the workload was updated).
-func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info) bool {
+func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, skipped bool) bool {
```

  lets unify the interface, flip the flag semantics and name it `inadmissible` like  `RequeueIfNotPresent`

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T11:43:19Z

- **pkg/scheduler/scheduler.go:414** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T11:43:19Z
```diff
@@ -410,11 +409,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update QueuedWorkload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.skipped)
+	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added)
```

  ```suggestion
  	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "skipped", e.skipped)
  ```

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-07T11:49:31Z

- **pkg/queue/manager.go:287** by [@denkensk](https://github.com/denkensk) — 2022-04-07T11:49:31Z
```diff
@@ -284,7 +284,7 @@ func (m *Manager) addOrUpdateWorkload(w *kueue.QueuedWorkload) bool {
 // RequeueWorkload requeues the workload ensuring that the queue and the
 // workload still exist in the client cache and it's not admitted. It won't
 // requeue if the workload is already in the queue (possible if the workload was updated).
-func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info) bool {
+func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, skipped bool) bool {
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-07T11:49:44Z

- **pkg/scheduler/scheduler.go:414** by [@denkensk](https://github.com/denkensk) — 2022-04-07T11:49:43Z
```diff
@@ -410,11 +409,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.QueuedWorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update QueuedWorkload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.skipped)
+	log.V(2).Info("Workload re-queued", "queuedWorkload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added)
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T14:22:10Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:22:10Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  sorry for the back-and-forth. I think using `admissible` will be more readable.
  
  Otherwise we run into double negative situations `inadmissible=false` which can be harder to reason about, potentially leading to bugs.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T14:25:21Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T14:25:21Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  I don't think admissible makes sense though here, because the workload was simply not tried.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T14:27:45Z

- **pkg/scheduler/scheduler.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:25:10Z
```diff
@@ -110,8 +110,7 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
 			e.admissible = false
```

  it should be `e.admitted`

- **pkg/scheduler/scheduler.go:114** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:25:40Z
```diff
@@ -110,8 +110,7 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
 			e.admissible = false
-			// TODO(): we shouldn't emit an event or update the workload condition in this
-			// case, just re-queue directly.
+			e.skipped = true
 			e.noAdmissionReason = "cohort used in this cycle"
```

  inadmissibleReason

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T14:32:05Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:32:05Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  I guess it's more like "potentially admissible"
  
  Maybe `skipped` wasn't so bad?
  
  But `admissible` is not the same as `admitted`.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T14:33:37Z

- **pkg/scheduler/scheduler.go:112** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T14:33:36Z
```diff
@@ -110,8 +110,7 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
 			e.admissible = false
```

  during nominate, it would only be admissible not admitted. I would keep this variable name.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T14:40:36Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T14:40:36Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  inadmissible matches the `inadmissibleWorkloads` variable, although the double negative is not good, you can think of it as: does this workload belong to `inadmissibleWorkloads`

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T14:41:56Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T14:41:56Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  basically we are working with inadmissible workloads, it is not the same as naming a variable `disableFeature`

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-07T14:58:00Z

- **pkg/scheduler/scheduler.go:114** by [@denkensk](https://github.com/denkensk) — 2022-04-07T14:58:00Z
```diff
@@ -110,8 +110,7 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
 			e.admissible = false
-			// TODO(): we shouldn't emit an event or update the workload condition in this
-			// case, just re-queue directly.
+			e.skipped = true
 			e.noAdmissionReason = "cohort used in this cycle"
```

  I think it can be renamed in other pr https://github.com/kubernetes-sigs/kueue/pull/185

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T15:00:31Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T15:00:30Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  Yeah, that's a good way to reason about it.
  
  Then the conclusion is that we keep it as `inadmissible`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T15:09:33Z

- **pkg/queue/cluster_queue_interface.go:53** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T15:07:43Z
```diff
@@ -49,12 +49,12 @@ type ClusterQueue interface {
 	// queue is empty.
 	Pop() *workload.Info
 
-	// AddInadmissibleIfNotPresent inserts a workload that could not be
+	// RequeueIfNotPresent inserts a workload that could not be
 	// admitted back into the ClusterQueue. The implementation might choose to
```

  ```suggestion
  	// RequeueIfNotPresent inserts a workload that was not
  	// admitted back into the ClusterQueue. If the boolean is false,
  	// it means that we couldn't determine if the workload was admissible
  	// in the last cycle. If the boolean is true, the implementation might choose to
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T15:13:26Z

- **test/integration/scheduler/scheduler_test.go:557** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T15:13:25Z
```diff
@@ -538,4 +548,50 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, smallWl1, smallWl2)
 	})
+
+	ginkgo.It("Should schedule jobs borrowing quota from ClusterQueues in the same Cohort", func() {
+		job1 := testing.MakeJob("job1", ns.Name).Queue(prodBEQueue.Name).Request(
+			corev1.ResourceCPU, "11").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		createdJob1 := &batchv1.Job{}
+		gomega.Consistently(func() *bool {
```

  use https://github.com/kubernetes-sigs/kueue/blob/425187b36a282851e08ad25d0fd51fc0ee5fc2d9/test/integration/framework/framework.go#L151-L168,
  which should be faster

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T16:43:16Z

- **test/integration/scheduler/scheduler_test.go:557** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T16:43:16Z
```diff
@@ -538,4 +548,50 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, smallWl1, smallWl2)
 	})
+
+	ginkgo.It("Should schedule jobs borrowing quota from ClusterQueues in the same Cohort", func() {
+		job1 := testing.MakeJob("job1", ns.Name).Queue(prodBEQueue.Name).Request(
+			corev1.ResourceCPU, "11").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		createdJob1 := &batchv1.Job{}
+		gomega.Consistently(func() *bool {
```

  only if the test is changed to use QueuedWorkload, otherwise we still have to refactor it again.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-08T11:56:22Z

- **test/integration/scheduler/scheduler_test.go:557** by [@denkensk](https://github.com/denkensk) — 2022-04-08T11:56:22Z
```diff
@@ -538,4 +548,50 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, smallWl1, smallWl2)
 	})
+
+	ginkgo.It("Should schedule jobs borrowing quota from ClusterQueues in the same Cohort", func() {
+		job1 := testing.MakeJob("job1", ns.Name).Queue(prodBEQueue.Name).Request(
+			corev1.ResourceCPU, "11").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		createdJob1 := &batchv1.Job{}
+		gomega.Consistently(func() *bool {
```

  Yes. This function may be added after I implemented this pr.
  I refactor this again to use `ExpectWorkloadsToBePending`

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-08T12:51:32Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@ahg-g](https://github.com/ahg-g) — 2022-04-08T12:51:32Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  btw, StrictFIFO is currently super aggressive in retries, if this mode turned out to be popular, then we need to optimize it: if the workload was inadmissible, then we put it back into the queue but shouldn't pop any workloads from the clusterqueue until an event happens for example.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-08T13:01:59Z

- **test/integration/scheduler/scheduler_test.go:120** by [@ahg-g](https://github.com/ahg-g) — 2022-04-08T13:01:58Z
```diff
@@ -103,19 +104,27 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		prodBEClusterQ = testing.MakeClusterQueue("prod-be-cq").
 			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
 			Resource(testing.MakeResource(corev1.ResourceCPU).
-				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("10").Obj()).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, prodBEClusterQ)).Should(gomega.Succeed())
 
 		devBEClusterQ = testing.MakeClusterQueue("dev-be-cq").
 			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
 			Resource(testing.MakeResource(corev1.ResourceCPU).
-				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("10").Obj()).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, devBEClusterQ)).Should(gomega.Succeed())
 
+		testBEClusterQ = testing.MakeClusterQueue("test-be-cq").
```

  sorry, I just noticed this, since this CQ is being updated, lets move it to be created and deleted inside the test; otherwise, it will be surprising if another test uses it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T13:33:23Z

- **pkg/queue/cluster_queue_interface.go:59** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T13:25:49Z
```diff
@@ -49,12 +49,14 @@ type ClusterQueue interface {
 	// queue is empty.
 	Pop() *workload.Info
 
-	// AddInadmissibleIfNotPresent inserts a workload that could not be
-	// admitted back into the ClusterQueue. The implementation might choose to
-	// keep it in temporary placeholder stage where it doesn't compete with
-	// other workloads, until cluster events free up quota. The workload should
-	// not be reinserted if it's already in the ClusterQueue.
-	AddInadmissibleIfNotPresent(*workload.Info) bool
+	// RequeueIfNotPresent inserts a workload that was not
+	// admitted back into the ClusterQueue. If the boolean is false,
+	// it means that we couldn't determine if the workload was admissible
+	// in the last cycle. If the boolean is true, the implementation might
+	// choose to keep it in temporary placeholder stage where it doesn't
+	// compete with other workloads, until cluster events free up quota.
+	// The workload should not be reinserted if it's already in the ClusterQueue.
```

  ```suggestion
  	// The workload should not be reinserted if it's already in the ClusterQueue.
  	// Returns true if the workload was inserted.
  ```

- **pkg/queue/manager_test.go:417** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T13:28:12Z
```diff
@@ -414,7 +414,7 @@ func TestRequeueWorkload(t *testing.T) {
 				_ = manager.AddOrUpdateWorkload(tc.workload)
 			}
 			info := workload.NewInfo(tc.workload)
-			if requeued := manager.RequeueWorkload(ctx, info); requeued != tc.wantRequeued {
```

  can you rename the test to `TestRequeueWorkloadStrictFIFO`? do we have one for BestEffort?

- **pkg/queue/manager_test.go:801** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T13:30:00Z
```diff
@@ -798,7 +798,7 @@ func TestHeadsAsync(t *testing.T) {
 				// Remove the initial workload from the manager.
 				mgr.Heads(ctx)
 				go func() {
-					mgr.RequeueWorkload(ctx, workload.NewInfo(&wl))
+					mgr.RequeueWorkload(ctx, workload.NewInfo(&wl), true)
```

  Even though we are using StrictFIFO here, false is more appropriate (we want immediate requeue).

- **pkg/scheduler/scheduler.go:277** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T13:31:57Z
```diff
@@ -276,8 +274,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			return
 		}
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
-		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
+		s.requeueAndUpdate(log, ctx, *e, fmt.Sprintf("%s: %v", errCouldNotAdmitWL, err))
```

  ```suggestion
  		e.inadmissibleReason = fmt.Sprintf("%s: %v", errCouldNotAdmitWL, err)
  		s.requeueAndUpdate(log, ctx, *e)
  ```

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-08T13:55:57Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@denkensk](https://github.com/denkensk) — 2022-04-08T13:55:57Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  if so. We also need to distinguish the two type of workloads.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-08T14:08:08Z

- **pkg/scheduler/scheduler.go:277** by [@denkensk](https://github.com/denkensk) — 2022-04-08T14:08:08Z
```diff
@@ -276,8 +274,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			return
 		}
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
-		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
+		s.requeueAndUpdate(log, ctx, *e, fmt.Sprintf("%s: %v", errCouldNotAdmitWL, err))
```

  How about the status of entry? If it's failed because like  timeout of apiserver. This workload should be pushed back to heap directly, right? Shall we need to add the event and update status for it?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-08T14:12:44Z

- **pkg/queue/manager_test.go:417** by [@denkensk](https://github.com/denkensk) — 2022-04-08T14:12:44Z
```diff
@@ -414,7 +414,7 @@ func TestRequeueWorkload(t *testing.T) {
 				_ = manager.AddOrUpdateWorkload(tc.workload)
 			}
 			info := workload.NewInfo(tc.workload)
-			if requeued := manager.RequeueWorkload(ctx, info); requeued != tc.wantRequeued {
```

  sg. The one for `BestEffort` may can be added in follow up pr.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-08T14:14:37Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@ahg-g](https://github.com/ahg-g) — 2022-04-08T14:14:37Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  > if so. We also need to distinguish the two type of workloads.
  
  the inadmissible flag?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T14:26:52Z

- **pkg/scheduler/scheduler.go:277** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T14:19:57Z
```diff
@@ -276,8 +274,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			return
 		}
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
-		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
+		s.requeueAndUpdate(log, ctx, *e, fmt.Sprintf("%s: %v", errCouldNotAdmitWL, err))
```

  You are right, we should update the status or produce an event in that case. And we should push to the heap directly.
  
  I confirm that we don't produce an event if there is a bind failure in scheduler
  
  https://github.com/kubernetes/kubernetes/blob/9984048bd5d13d73ae09250a6f93202c322f9e2a/pkg/scheduler/schedule_one.go#L793-L796

- **pkg/scheduler/scheduler.go:424** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T14:23:04Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
```

  my brain is hurting again thinking about the double negative.
  
  I think we should rename the variable to `immediate` (for immediate requeue).
  
  then `immediate = e.status == skipped` reads pretty well.

- **pkg/scheduler/scheduler.go:423** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T14:24:51Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  ```suggestion
  	if e.status == nominated {
  ```
  
  So that we only do it if it didn't fit.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-08T14:27:15Z

- **pkg/queue/cluster_queue_best_effort_fifo.go:82** by [@denkensk](https://github.com/denkensk) — 2022-04-08T14:27:15Z
```diff
@@ -75,9 +75,15 @@ func (cq *ClusterQueueBestEffortFIFO) Delete(w *kueue.QueuedWorkload) {
 	cq.ClusterQueueImpl.Delete(w)
 }
 
-// AddInadmissibleIfNotPresent inserts a workload that cannot be admitted into
-// the inadmissibleWorkloads in ClusterQueue, unless it is already in the queue.
-func (cq *ClusterQueueBestEffortFIFO) AddInadmissibleIfNotPresent(wInfo *workload.Info) bool {
+// RequeueIfNotPresent inserts a workload that cannot be admitted into
+// ClusterQueue, unless it is already in the queue. If inadmissible is true,
+// the workload will be put into the inadmissibleWorkloads. If not,
+// the workload will be pushed back to heap directly.
+func (cq *ClusterQueueBestEffortFIFO) RequeueIfNotPresent(wInfo *workload.Info, inadmissible bool) bool {
```

  o.. yes  We just need to distinguish the head of the queue if it's inadmissible or not.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-08T14:28:50Z

- **pkg/scheduler/scheduler.go:277** by [@denkensk](https://github.com/denkensk) — 2022-04-08T14:28:50Z
```diff
@@ -276,8 +274,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			return
 		}
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
-		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
+		s.requeueAndUpdate(log, ctx, *e, fmt.Sprintf("%s: %v", errCouldNotAdmitWL, err))
```

  or add a new status for it?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T14:35:18Z

- **pkg/scheduler/scheduler.go:277** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T14:35:17Z
```diff
@@ -276,8 +274,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			return
 		}
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
-		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
+		s.requeueAndUpdate(log, ctx, *e, fmt.Sprintf("%s: %v", errCouldNotAdmitWL, err))
```

  The status in this case is currently "assumed", which I think is fine.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-08T14:42:29Z

- **pkg/scheduler/scheduler.go:424** by [@ahg-g](https://github.com/ahg-g) — 2022-04-08T14:42:29Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
```

  at this point `nominated` means inadmissible, so this check could be `e.status == nominated`; but renaming the `inadmissible` flag to `immediate` sgtm as well.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-08T14:48:40Z

- **pkg/scheduler/scheduler.go:423** by [@denkensk](https://github.com/denkensk) — 2022-04-08T14:48:39Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  if so we can't update the status and event for assumed  workload.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-08T14:48:56Z

- **pkg/scheduler/scheduler.go:424** by [@denkensk](https://github.com/denkensk) — 2022-04-08T14:48:56Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
```

  `e.status == nominated` is ok here.
  so we decide to rename `inadmissible` to `immediate`?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-08T14:50:16Z

- **pkg/scheduler/scheduler.go:423** by [@denkensk](https://github.com/denkensk) — 2022-04-08T14:50:16Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  maybe keep `e.status != skipped` .

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T14:50:21Z

- **pkg/scheduler/scheduler.go:423** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T14:50:21Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  We don't do that. We just do the spec update in that case.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T14:51:12Z

- **pkg/scheduler/scheduler.go:424** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T14:51:12Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
```

  I'm fine with either after Abdullah's comment. Your call, Alex :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T14:52:26Z

- **pkg/scheduler/scheduler.go:423** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T14:52:26Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  We actually want to avoid the status update and event for `assumed` status. The only way we get here is when the spec update failed.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-08T15:27:46Z

- **pkg/scheduler/scheduler.go:424** by [@ahg-g](https://github.com/ahg-g) — 2022-04-08T15:27:46Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
```

  actually no, `e.status == nominated` isn't the only inadmissible case, it is also when the status is not set at all! So lets do aldo's suggestion.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-08T15:32:04Z

- **pkg/scheduler/scheduler.go:423** by [@ahg-g](https://github.com/ahg-g) — 2022-04-08T15:32:04Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  wait, we should keep this as is, or introduce an explicit inadmissible entryStatus because entries with no status set (meaning they weren't even nominated) should be included here.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T15:51:54Z

- **pkg/scheduler/scheduler.go:424** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T15:51:53Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
```

  Also, if the spec update fails in the API, we should also make immediate requeue. So it should be:
  
  `immediate = (e.status == skipped || e.status == assumed)`
  
  Btw, the only way it stays nominated is when it fails to be assumed. This happens if the workload was already assumed or the clusterqueue is gone. In which case it doesn't matter which type of requeue we do.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T15:52:28Z

- **pkg/scheduler/scheduler.go:424** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T15:52:28Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
```

  2nd BTW: given all these problems trying to reason about these states, we should probably unit test this logic :)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-08T16:16:49Z

- **pkg/scheduler/scheduler.go:423** by [@ahg-g](https://github.com/ahg-g) — 2022-04-08T16:16:49Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  also, we should remove the status update that happens here: https://github.com/kubernetes-sigs/kueue/blob/e9f4c4dc3e6758d031a3adabf4cbd80d29de3256/pkg/scheduler/scheduler.go#L124
  
  and the log message above it: https://github.com/kubernetes-sigs/kueue/blob/e9f4c4dc3e6758d031a3adabf4cbd80d29de3256/pkg/scheduler/scheduler.go#L122

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-11T12:22:07Z

- **pkg/scheduler/scheduler.go:423** by [@denkensk](https://github.com/denkensk) — 2022-04-11T12:22:07Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  Removed

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-11T12:22:17Z

- **pkg/queue/manager_test.go:417** by [@denkensk](https://github.com/denkensk) — 2022-04-11T12:22:16Z
```diff
@@ -414,7 +414,7 @@ func TestRequeueWorkload(t *testing.T) {
 				_ = manager.AddOrUpdateWorkload(tc.workload)
 			}
 			info := workload.NewInfo(tc.workload)
-			if requeued := manager.RequeueWorkload(ctx, info); requeued != tc.wantRequeued {
```

  updated.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-11T12:22:45Z

- **pkg/queue/cluster_queue_interface.go:59** by [@denkensk](https://github.com/denkensk) — 2022-04-11T12:22:45Z
```diff
@@ -49,12 +49,14 @@ type ClusterQueue interface {
 	// queue is empty.
 	Pop() *workload.Info
 
-	// AddInadmissibleIfNotPresent inserts a workload that could not be
-	// admitted back into the ClusterQueue. The implementation might choose to
-	// keep it in temporary placeholder stage where it doesn't compete with
-	// other workloads, until cluster events free up quota. The workload should
-	// not be reinserted if it's already in the ClusterQueue.
-	AddInadmissibleIfNotPresent(*workload.Info) bool
+	// RequeueIfNotPresent inserts a workload that was not
+	// admitted back into the ClusterQueue. If the boolean is false,
+	// it means that we couldn't determine if the workload was admissible
+	// in the last cycle. If the boolean is true, the implementation might
+	// choose to keep it in temporary placeholder stage where it doesn't
+	// compete with other workloads, until cluster events free up quota.
+	// The workload should not be reinserted if it's already in the ClusterQueue.
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-11T12:22:56Z

- **test/integration/scheduler/scheduler_test.go:120** by [@denkensk](https://github.com/denkensk) — 2022-04-11T12:22:56Z
```diff
@@ -103,19 +104,27 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		prodBEClusterQ = testing.MakeClusterQueue("prod-be-cq").
 			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
 			Resource(testing.MakeResource(corev1.ResourceCPU).
-				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("10").Obj()).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, prodBEClusterQ)).Should(gomega.Succeed())
 
 		devBEClusterQ = testing.MakeClusterQueue("dev-be-cq").
 			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
 			Resource(testing.MakeResource(corev1.ResourceCPU).
-				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("10").Obj()).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Ceiling("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, devBEClusterQ)).Should(gomega.Succeed())
 
+		testBEClusterQ = testing.MakeClusterQueue("test-be-cq").
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-11T12:39:14Z

- **pkg/scheduler/scheduler.go:138** by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T12:19:13Z
```diff
@@ -141,7 +134,7 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			"status", e.status,
 			"reason", e.inadmissibleReason)
 		if e.status != assumed {
-			s.requeueAndUpdate(log, ctx, &e.Info, e.inadmissibleReason)
+			s.requeueAndUpdate(log, ctx, e)
 			s.recorder.Eventf(e.Obj, corev1.EventTypeNormal, "Pending", e.inadmissibleReason)
```

  Remove this event

- **test/integration/scheduler/scheduler_test.go:560** by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T12:23:11Z
```diff
@@ -538,4 +538,29 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, smallWl1, smallWl2)
 	})
+
+	ginkgo.It("Should schedule workloads borrowing quota from ClusterQueues in the same Cohort", func() {
+		wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(prodBEQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+		wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(devBEQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+
+		ginkgo.By("Creating two workloads")
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		framework.ExpectWorkloadsToBePending(ctx, k8sClient, wl1, wl2)
+
+		// Make sure workloads are in the same scheduling cycle.
+		testBEClusterQ := testing.MakeClusterQueue("test-be-cq").
+			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"15").Max("15").Obj()).
+				Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, testBEClusterQ)).Should(gomega.Succeed())
+
```

  add:
  
  ```
  defer func() {
  	gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, testBEClusterQ)).Should(gomega.Succeed())
  }()
  ```

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-11T12:50:40Z

- **pkg/scheduler/scheduler.go:138** by [@denkensk](https://github.com/denkensk) — 2022-04-11T12:50:40Z
```diff
@@ -141,7 +134,7 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			"status", e.status,
 			"reason", e.inadmissibleReason)
 		if e.status != assumed {
-			s.requeueAndUpdate(log, ctx, &e.Info, e.inadmissibleReason)
+			s.requeueAndUpdate(log, ctx, e)
 			s.recorder.Eventf(e.Obj, corev1.EventTypeNormal, "Pending", e.inadmissibleReason)
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-11T12:50:42Z

- **test/integration/scheduler/scheduler_test.go:560** by [@denkensk](https://github.com/denkensk) — 2022-04-11T12:50:42Z
```diff
@@ -538,4 +538,29 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodClusterQ.Name, smallWl1, smallWl2)
 	})
+
+	ginkgo.It("Should schedule workloads borrowing quota from ClusterQueues in the same Cohort", func() {
+		wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(prodBEQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+		wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(devBEQueue.Name).Request(corev1.ResourceCPU, "11").Obj()
+
+		ginkgo.By("Creating two workloads")
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		framework.ExpectWorkloadsToBePending(ctx, k8sClient, wl1, wl2)
+
+		// Make sure workloads are in the same scheduling cycle.
+		testBEClusterQ := testing.MakeClusterQueue("test-be-cq").
+			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"15").Max("15").Obj()).
+				Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, testBEClusterQ)).Should(gomega.Succeed())
+
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-11T12:57:33Z

- **pkg/scheduler/scheduler.go:423** by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T12:57:33Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  To summarize, we get to `requeueAndUpdate` in the following statuses
  
  1. status not set: workload didn't fit at all.
  2. skipped: workload was nominated but then skipped because the cohort was used.
  3. nominated: admit failed because of Spec update failure.
  4. we never get here in assumed status

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-11T14:22:37Z

- **pkg/queue/cluster_queue_best_effort_fifo_test.go:103** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-11T13:57:56Z
```diff
@@ -100,7 +100,7 @@ func TestClusterQueueBestEffortFIFO(t *testing.T) {
 			}
 
 			for _, w := range test.inadmissibleWorkloadsToAdd {
-				cq.AddInadmissibleIfNotPresent(w)
+				cq.RequeueIfNotPresent(w, true)
```

  false, because it's inadmissible.
  
  since the test didn't fail, can you add an extra check for the workloads not to be there?

- **pkg/queue/cluster_queue_interface.go:56** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-11T14:14:04Z
```diff
@@ -49,12 +49,15 @@ type ClusterQueue interface {
 	// queue is empty.
 	Pop() *workload.Info
 
-	// AddInadmissibleIfNotPresent inserts a workload that could not be
-	// admitted back into the ClusterQueue. The implementation might choose to
-	// keep it in temporary placeholder stage where it doesn't compete with
-	// other workloads, until cluster events free up quota. The workload should
-	// not be reinserted if it's already in the ClusterQueue.
-	AddInadmissibleIfNotPresent(*workload.Info) bool
+	// RequeueIfNotPresent inserts a workload that was not
+	// admitted back into the ClusterQueue. If the boolean is true,
+	// it means that we couldn't determine if the workload was admissible
+	// in the last cycle. If the boolean is false, the implementation might
```

  ```suggestion
  	// admitted back into the ClusterQueue. If the boolean is true,
  	// the workloads should be put back in the queue immediately,
  	// because we couldn't determine if the workload was admissible
  	// in the last cycle. If the boolean is false, the implementation might
  ```

- **pkg/scheduler/scheduler.go:423** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-11T14:18:17Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  3 only happens if we are trying to assume an already assumed workload or the clusterqueue was removed while we were scheduling.
  
  we do get here with 4, but from the `admit` sub-routine.
  
  I think we only should do the update + event in case 1.

- **pkg/scheduler/scheduler.go:423** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-11T14:22:32Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  unit tests? :)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-11T14:50:33Z

- **pkg/scheduler/scheduler.go:423** by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T14:50:33Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  right, admit is non-blocking and starts a go routine.
  
  +1 to unit tests and only update status + event on the inadmissible case only

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-11T16:06:12Z

- **pkg/queue/cluster_queue_best_effort_fifo_test.go:103** by [@denkensk](https://github.com/denkensk) — 2022-04-11T16:06:12Z
```diff
@@ -100,7 +100,7 @@ func TestClusterQueueBestEffortFIFO(t *testing.T) {
 			}
 
 			for _, w := range test.inadmissibleWorkloadsToAdd {
-				cq.AddInadmissibleIfNotPresent(w)
+				cq.RequeueIfNotPresent(w, true)
```

  It has been changed to be immediate.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-12T06:49:07Z

- **pkg/scheduler/scheduler.go:423** by [@denkensk](https://github.com/denkensk) — 2022-04-12T06:49:07Z
```diff
@@ -423,11 +420,15 @@ func (e entryOrdering) Less(i, j int) bool {
 	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
 }
 
-func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, w *workload.Info, message string) {
-	added := s.queues.RequeueWorkload(ctx, w)
-	log.V(2).Info("Workload re-queued", "workload", klog.KObj(w.Obj), "queue", klog.KRef(w.Obj.Namespace, w.Obj.Spec.QueueName), "added", added)
-	err := workload.UpdateStatus(ctx, s.client, w.Obj, kueue.WorkloadAdmitted, corev1.ConditionFalse, "Pending", message)
-	if err != nil {
-		log.Error(err, "Could not update Workload status")
+func (s *Scheduler) requeueAndUpdate(log logr.Logger, ctx context.Context, e entry, message string) {
+	added := s.queues.RequeueWorkload(ctx, &e.Info, e.status != skipped)
+	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "queue", klog.KRef(e.Obj.Namespace, e.Obj.Spec.QueueName), "added", added, "status", e.status)
+
+	if e.status != skipped {
```

  Added unit tests to the changed part.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-12T06:55:31Z

- **pkg/queue/cluster_queue_best_effort_fifo_test.go:103** by [@denkensk](https://github.com/denkensk) — 2022-04-12T06:55:31Z
```diff
@@ -100,7 +100,7 @@ func TestClusterQueueBestEffortFIFO(t *testing.T) {
 			}
 
 			for _, w := range test.inadmissibleWorkloadsToAdd {
-				cq.AddInadmissibleIfNotPresent(w)
+				cq.RequeueIfNotPresent(w, true)
```

  True is to push it back immediately.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-12T13:35:29Z

- **pkg/queue/cluster_queue_best_effort_fifo_test.go:103** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T13:35:29Z
```diff
@@ -100,7 +100,7 @@ func TestClusterQueueBestEffortFIFO(t *testing.T) {
 			}
 
 			for _, w := range test.inadmissibleWorkloadsToAdd {
-				cq.AddInadmissibleIfNotPresent(w)
+				cq.RequeueIfNotPresent(w, true)
```

  But the line says `for _, w := range test.inadmissibleWorkloadsToAdd {`, so shouldn't it be immediate=false?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-12T14:33:32Z

- **pkg/scheduler/scheduler.go:272** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T14:16:50Z
```diff
@@ -275,9 +267,10 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			log.V(2).Info("Workload not admitted because it was deleted")
 			return
 		}
+
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
-		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
+		e.inadmissibleReason = fmt.Sprintf("%s: %v", errCouldNotAdmitWL, err)
```

  I don't think we are using this value, are we?
  
  Should we include it in the log?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-12T15:58:32Z

- **pkg/scheduler/scheduler.go:272** by [@denkensk](https://github.com/denkensk) — 2022-04-12T15:58:32Z
```diff
@@ -275,9 +267,10 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 			log.V(2).Info("Workload not admitted because it was deleted")
 			return
 		}
+
 		log.Error(err, errCouldNotAdmitWL)
-		s.requeueAndUpdate(log, ctx, &e.Info, err.Error())
-		s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Pending", "%s: %v", errCouldNotAdmitWL, err)
+		e.inadmissibleReason = fmt.Sprintf("%s: %v", errCouldNotAdmitWL, err)
```

  it may be not useful. I removed it.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-12T15:58:55Z

- **pkg/queue/cluster_queue_best_effort_fifo_test.go:103** by [@denkensk](https://github.com/denkensk) — 2022-04-12T15:58:54Z
```diff
@@ -100,7 +100,7 @@ func TestClusterQueueBestEffortFIFO(t *testing.T) {
 			}
 
 			for _, w := range test.inadmissibleWorkloadsToAdd {
-				cq.AddInadmissibleIfNotPresent(w)
+				cq.RequeueIfNotPresent(w, true)
```

  yes. You are right. I changed it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-12T16:32:09Z

/lgtm
/hold
for squash
