# PR #179: Update tolerated flavors integration test

**Summary**: Update tolerated flavors integration test

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/179

**Last updated**: 2022-05-07T00:58:50Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@denkensk](https://github.com/denkensk)
- **Branch**: `intest` → `main`
- **Created**: 2022-04-06T08:34:23Z
- **Updated**: 2022-05-07T00:58:50Z
- **Merged**: 2022-05-06T16:11:06Z
- **Closed**: 2022-05-06T16:11:06Z
- **Labels**: `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 2   **+33 / -34**

## Description

Signed-off-by: Alex Wang <wangqingcan1990@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T14:40:22Z

Add to description that this is now possible after #135, with it being the default.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T14:42:04Z

Also, should we have a similar test where we ensure that a smaller later workload is not scheduled when using StrictFIFO.
Although it might be flaky before #164

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-06T16:10:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/179#pullrequestreview-964871128" title="LGTM">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/179#" title="Author self-approved">denkensk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-06T14:44:17Z

- **test/integration/scheduler/scheduler_test.go:191** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T14:44:17Z
```diff
@@ -189,7 +190,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
```

  specify the queueing strategy

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-06T18:16:54Z

- **test/integration/scheduler/scheduler_test.go:103** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T18:16:53Z
```diff
@@ -103,6 +103,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		prodBEClusterQ = testing.MakeClusterQueue("prod-be-cq").
```

  MakeClusterQueue uses StrictFIFO by default. We should make it match the API default.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T11:51:16Z

- **test/integration/scheduler/scheduler_test.go:206** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T11:51:16Z
```diff
@@ -199,20 +200,18 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
+		ginkgo.By("checking a second job without toleration doesn't start")
+		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(prodBEQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+		createdJob2 := &batchv1.Job{}
```

  you can put this inside the `Consistently` clause since it is not used later.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T11:53:24Z

- **test/integration/scheduler/scheduler_test.go:103** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T11:53:24Z
```diff
@@ -103,6 +103,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		prodBEClusterQ = testing.MakeClusterQueue("prod-be-cq").
```

  +1, and we should have a test for StrictFIFO testing that a workload continues to be blocked if another fronting it is inadmissible.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T11:54:35Z

- **test/integration/scheduler/scheduler_test.go:103** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T11:54:35Z
```diff
@@ -103,6 +103,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		prodBEClusterQ = testing.MakeClusterQueue("prod-be-cq").
```

  > Also, should we have a similar test where we ensure that a smaller later workload is not scheduled when using StrictFIFO. Although it might be flaky before #164
  
  why will it be flaky?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T13:43:32Z

- **test/integration/scheduler/scheduler_test.go:103** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T13:43:32Z
```diff
@@ -103,6 +103,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		prodBEClusterQ = testing.MakeClusterQueue("prod-be-cq").
```

  I was thinking that we could end up scheduling the second workload while we are asynchronously doing the API update.
  However, since we assume the first (blocking) workload, I guess that shouldn't be possible.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T13:50:51Z

- **test/integration/scheduler/scheduler_test.go:206** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T13:50:51Z
```diff
@@ -199,20 +200,18 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
+		ginkgo.By("checking a second job without toleration doesn't start")
+		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(prodBEQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+		createdJob2 := &batchv1.Job{}
```

  You could use QueuedWorkload (instead of Job) and use this https://github.com/kubernetes-sigs/kueue/blob/425187b36a282851e08ad25d0fd51fc0ee5fc2d9/test/integration/framework/framework.go#L151-L168
  
  The advantage is that it uses `Eventually` instead of `Consistently`, using the QW status.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-19T16:16:48Z

- **test/integration/scheduler/scheduler_test.go:206** by [@ahg-g](https://github.com/ahg-g) — 2022-04-19T16:16:48Z
```diff
@@ -199,20 +200,18 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
+		ginkgo.By("checking a second job without toleration doesn't start")
+		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(prodBEQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+		createdJob2 := &batchv1.Job{}
```

  Can we please move forward with this PR? I think we can keep this test as is without changing to workload, we can do the migration for the whole file separately.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-01T03:52:28Z

- **test/integration/scheduler/scheduler_test.go:103** by [@denkensk](https://github.com/denkensk) — 2022-05-01T03:52:28Z
```diff
@@ -103,6 +103,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		prodBEClusterQ = testing.MakeClusterQueue("prod-be-cq").
```

  > MakeClusterQueue uses StrictFIFO by default. We should make it match the API default.
  
  Added.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-01T03:57:14Z

- **test/integration/scheduler/scheduler_test.go:206** by [@denkensk](https://github.com/denkensk) — 2022-05-01T03:57:14Z
```diff
@@ -199,20 +200,18 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
+		ginkgo.By("checking a second job without toleration doesn't start")
+		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(prodBEQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+		createdJob2 := &batchv1.Job{}
```

  Also change to use Workload (instead of Job) in this test case and use `ExpectWorkloadsToBePending` here.
  
  > we can do the migration for the whole file separately.
  
  +1 We can use workload in new added test case. And migration the old tests for the whole file in a separate pr.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-02T15:33:14Z

- **test/integration/scheduler/scheduler_test.go:171** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T15:28:23Z
```diff
@@ -162,39 +162,31 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			lookupKey := types.NamespacedName{Name: wl1.Name, Namespace: wl1.Namespace}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, wl1)).Should(gomega.Succeed())
```

  ```suggestion
  			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wlCopy)).To(gomega.Succeed())
  ```

- **test/integration/scheduler/scheduler_test.go:172** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T15:31:05Z
```diff
@@ -162,39 +162,31 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			lookupKey := types.NamespacedName{Name: wl1.Name, Namespace: wl1.Namespace}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, wl1)).Should(gomega.Succeed())
+			return wl1.Spec.Admission != nil && wl1.Spec.Admission.
```

  `return wl1.Spec.Admission`
  
  And then you can compare the whole admission object.

- **test/integration/scheduler/scheduler_test.go:171** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T15:32:57Z
```diff
@@ -162,39 +162,31 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
```

  Shouldn't this be probBEQueue?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-03T00:51:25Z

- **test/integration/scheduler/scheduler_test.go:171** by [@denkensk](https://github.com/denkensk) — 2022-05-03T00:51:25Z
```diff
@@ -162,39 +162,31 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
```

  There is no probBEQueue now. prodQueue is changed to `BestEffortFIFO`.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-03T00:54:35Z

- **test/integration/scheduler/scheduler_test.go:171** by [@denkensk](https://github.com/denkensk) — 2022-05-03T00:54:35Z
```diff
@@ -162,39 +162,31 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			lookupKey := types.NamespacedName{Name: wl1.Name, Namespace: wl1.Namespace}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, wl1)).Should(gomega.Succeed())
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-03T00:54:43Z

- **test/integration/scheduler/scheduler_test.go:172** by [@denkensk](https://github.com/denkensk) — 2022-05-03T00:54:43Z
```diff
@@ -162,39 +162,31 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		gomega.Eventually(func() bool {
+			lookupKey := types.NamespacedName{Name: wl1.Name, Namespace: wl1.Namespace}
+			gomega.Expect(k8sClient.Get(ctx, lookupKey, wl1)).Should(gomega.Succeed())
+			return wl1.Spec.Admission != nil && wl1.Spec.Admission.
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-03T13:30:32Z

- **test/integration/scheduler/scheduler_test.go:173** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T13:29:49Z
```diff
@@ -162,39 +162,34 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
-
-		ginkgo.By("checking a third job with toleration starts")
-		job3 := testing.MakeJob("spot-job3", ns.Name).Queue(prodQueue.Name).
-			Toleration(spotToleration).
-			Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job3)).Should(gomega.Succeed())
-		createdJob3 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: job3.Name, Namespace: job3.Namespace}, createdJob3)).Should(gomega.Succeed())
-			return createdJob3.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob3.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotTaintedFlavor.Name))
+		wlCopy := &kueue.Workload{}
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wlCopy)).To(gomega.Succeed())
+			return wlCopy.Spec.Admission != nil
```

  ```suggestion
  			return wlCopy.Spec.Admission
  ```

- **test/integration/scheduler/scheduler_test.go:171** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T13:30:00Z
```diff
@@ -162,39 +162,34 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
-
-		ginkgo.By("checking a third job with toleration starts")
-		job3 := testing.MakeJob("spot-job3", ns.Name).Queue(prodQueue.Name).
-			Toleration(spotToleration).
-			Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job3)).Should(gomega.Succeed())
-		createdJob3 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: job3.Name, Namespace: job3.Namespace}, createdJob3)).Should(gomega.Succeed())
-			return createdJob3.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob3.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotTaintedFlavor.Name))
+		wlCopy := &kueue.Workload{}
+		gomega.Eventually(func() bool {
```

  ```suggestion
  		gomega.Eventually(func() kueue.Admission {
  ```

- **test/integration/scheduler/scheduler_test.go:187** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-03T13:30:27Z
```diff
@@ -162,39 +162,34 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
-
-		ginkgo.By("checking a third job with toleration starts")
-		job3 := testing.MakeJob("spot-job3", ns.Name).Queue(prodQueue.Name).
-			Toleration(spotToleration).
-			Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job3)).Should(gomega.Succeed())
-		createdJob3 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: job3.Name, Namespace: job3.Namespace}, createdJob3)).Should(gomega.Succeed())
-			return createdJob3.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob3.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotTaintedFlavor.Name))
+		wlCopy := &kueue.Workload{}
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wlCopy)).To(gomega.Succeed())
+			return wlCopy.Spec.Admission != nil
+		}, framework.Timeout, framework.Interval).Should(gomega.Equal(true))
+		expectAdmission := testing.MakeAdmission(prodClusterQ.Name).Flavor(corev1.ResourceCPU, onDemandFlavor.Name).Obj()
+		gomega.Expect(wlCopy.Spec.Admission).Should(gomega.Equal(expectAdmission))
+
+		ginkgo.By("checking a second workload without toleration doesn't start")
+		wl2 := testing.MakeWorkload("on-demand-wl2", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		framework.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
+
+		ginkgo.By("checking a third workload with toleration starts")
+		wl3 := testing.MakeWorkload("on-demand-wl3", ns.Name).Queue(prodQueue.Name).Toleration(spotToleration).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+
+		gomega.Eventually(func() bool {
```

  Same here.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-04T13:13:02Z

- **test/integration/scheduler/scheduler_test.go:173** by [@denkensk](https://github.com/denkensk) — 2022-05-04T13:13:02Z
```diff
@@ -162,39 +162,34 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
-
-		ginkgo.By("checking a third job with toleration starts")
-		job3 := testing.MakeJob("spot-job3", ns.Name).Queue(prodQueue.Name).
-			Toleration(spotToleration).
-			Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job3)).Should(gomega.Succeed())
-		createdJob3 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: job3.Name, Namespace: job3.Namespace}, createdJob3)).Should(gomega.Succeed())
-			return createdJob3.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob3.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotTaintedFlavor.Name))
+		wlCopy := &kueue.Workload{}
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wlCopy)).To(gomega.Succeed())
+			return wlCopy.Spec.Admission != nil
```

  Hi Aldo Did you suggest to change it like this?
  ```go
  expectAdmission := testing.MakeAdmission(prodClusterQ.Name).Flavor(corev1.ResourceCPU,onDemandFlavor.Name).Obj()
  gomega.Eventually(func() kueue.Admission {
      gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wlCopy)).To(gomega.Succeed())
      return wlCopy.Spec.Admission or *wlCopy.Spec.Admission
  }, framework.Timeout, framework.Interval).Should(gomega.Equal(expectAdmission))
  ```
  
  
  `wlCopy.Spec.Admission` is a point of `kueue.Admission`. We still need to check if it's nil before getting value of it. And we'd better not continuity to check the value of `Admission`, we just need to check it when the first time it's not nil.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-04T14:22:43Z

- **test/integration/scheduler/scheduler_test.go:173** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-04T14:22:42Z
```diff
@@ -162,39 +162,34 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		gomega.Expect(createdProdJob2.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
-
-		ginkgo.By("checking a third job with toleration starts")
-		job3 := testing.MakeJob("spot-job3", ns.Name).Queue(prodQueue.Name).
-			Toleration(spotToleration).
-			Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job3)).Should(gomega.Succeed())
-		createdJob3 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: job3.Name, Namespace: job3.Namespace}, createdJob3)).Should(gomega.Succeed())
-			return createdJob3.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob3.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotTaintedFlavor.Name))
+		wlCopy := &kueue.Workload{}
+		gomega.Eventually(func() bool {
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wlCopy)).To(gomega.Succeed())
+			return wlCopy.Spec.Admission != nil
```

  Oops. My recommendation is to return `*kueue.Admission`.
  
  `gomega.Equal` and `pkg/util/testing.Equal` should be able to handle it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-06T15:55:55Z

- **test/integration/scheduler/scheduler_test.go:179** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-06T15:55:55Z
```diff
@@ -166,41 +166,35 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		framework.ExpectPendingWorkloadsMetric(prodQueue, 0)
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
-		framework.ExpectPendingWorkloadsMetric(prodQueue, 0)
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
-
-		ginkgo.By("checking a third job with toleration starts")
-		job3 := testing.MakeJob("spot-job3", ns.Name).Queue(prodQueue.Name).
-			Toleration(spotToleration).
-			Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job3)).Should(gomega.Succeed())
-		createdJob3 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: job3.Name, Namespace: job3.Namespace}, createdJob3)).Should(gomega.Succeed())
-			return createdJob3.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob3.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotTaintedFlavor.Name))
+		expectAdmission := testing.MakeAdmission(prodClusterQ.Name).Flavor(corev1.ResourceCPU, onDemandFlavor.Name).Obj()
+		wlCopy := &kueue.Workload{}
+		gomega.Eventually(func() *kueue.Admission {
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wlCopy)).To(gomega.Succeed())
+			return wlCopy.Spec.Admission
+		}, framework.Timeout, framework.Interval).Should(gomega.Equal(expectAdmission))
```

  ```suggestion
  		}, framework.Timeout, framework.Interval).Should(testing.Equal(expectAdmission))
  ```
  
  or `testingutil`, depending on the alias. That reports using cmp.Diff

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-06T16:06:28Z

- **test/integration/scheduler/scheduler_test.go:179** by [@denkensk](https://github.com/denkensk) — 2022-05-06T16:06:27Z
```diff
@@ -166,41 +166,35 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		framework.ExpectPendingWorkloadsMetric(prodQueue, 0)
 	})
 
-	ginkgo.It("Should schedule jobs on tolerated flavors", func() {
-		ginkgo.By("checking a job without toleration starts on the non-tainted flavor")
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
-		createdJob1 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			lookupKey := types.NamespacedName{Name: job1.Name, Namespace: job1.Namespace}
-			gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob1)).Should(gomega.Succeed())
-			return createdJob1.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
-		framework.ExpectPendingWorkloadsMetric(prodQueue, 0)
+	ginkgo.It("Should schedule workloads on tolerated flavors", func() {
+		ginkgo.By("checking a workload without toleration starts on the non-tainted flavor")
+		wl1 := testing.MakeWorkload("on-demand-wl1", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
 
-		// TODO(#8): uncomment the following once we have proper re-queueing.
-		// ginkgo.By("checking a second job without toleration doesn't start")
-		// job2 := testing.MakeJob("on-demand-job2", namespace).Queue(queue.Name).Request(corev1.ResourceCPU, "5").Obj()
-		// gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
-		// createdJob2 := &batchv1.Job{}
-		// gomega.Consistently(func() bool {
-		// 	lookupKey := types.NamespacedName{Name: job2.Name, Namespace: job2.Namespace}
-		// 	return k8sClient.Get(ctx, lookupKey, createdJob2) == nil && *createdJob2.Spec.Suspend
-		// }, consistentDuration, interval).Should(gomega.BeTrue())
-
-		ginkgo.By("checking a third job with toleration starts")
-		job3 := testing.MakeJob("spot-job3", ns.Name).Queue(prodQueue.Name).
-			Toleration(spotToleration).
-			Request(corev1.ResourceCPU, "5").Obj()
-		gomega.Expect(k8sClient.Create(ctx, job3)).Should(gomega.Succeed())
-		createdJob3 := &batchv1.Job{}
-		gomega.Eventually(func() *bool {
-			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: job3.Name, Namespace: job3.Namespace}, createdJob3)).Should(gomega.Succeed())
-			return createdJob3.Spec.Suspend
-		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
-		gomega.Expect(createdJob3.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotTaintedFlavor.Name))
+		expectAdmission := testing.MakeAdmission(prodClusterQ.Name).Flavor(corev1.ResourceCPU, onDemandFlavor.Name).Obj()
+		wlCopy := &kueue.Workload{}
+		gomega.Eventually(func() *kueue.Admission {
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wlCopy)).To(gomega.Succeed())
+			return wlCopy.Spec.Admission
+		}, framework.Timeout, framework.Interval).Should(gomega.Equal(expectAdmission))
```

  Thanks.
  Updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-06T16:10:24Z

Great!

/approve
/lgtm
