# PR #180: Add concept for priority and queueing strategy

**Summary**: Add concept for priority and queueing strategy

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/180

**Last updated**: 2022-04-07T15:31:59Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `doc-q-strategy` → `main`
- **Created**: 2022-04-06T13:38:03Z
- **Updated**: 2022-04-07T15:31:59Z
- **Merged**: 2022-04-07T15:31:58Z
- **Closed**: 2022-04-07T15:31:58Z
- **Labels**: `lgtm`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 2   **+29 / -1**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

Part of #64 

#### Special notes for your reviewer:

## Discussion

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-06T13:38:22Z

/assign @ahg-g @cortespao

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-06T13:38:25Z

@alculquicondor: GitHub didn't allow me to assign the following users: cortespao.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people), repo collaborators and people who have commented on this issue/PR can be assigned. Additionally, issues/PRs can only have 10 assignees at the same time.
For more information please see [the contributor guide](https://git.k8s.io/community/contributors/guide/first-contribution.md#issue-assignment-in-github)

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/180#issuecomment-1090284441):

>/assign @ahg-g @cortespao


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-06T19:36:35Z

/lgtm
/hold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-07T00:46:45Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/180#" title="Author self-approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/180#pullrequestreview-934312952" title="Approved">cortespao</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T15:21:13Z

squashed

/hold cancel

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T15:29:34Z

/lgtm

## Reviews

### Review by [@cortespao](https://github.com/cortespao) (approved) — 2022-04-07T00:46:32Z

- **docs/concepts/cluster_queue.md:67** by [@cortespao](https://github.com/cortespao) — 2022-04-07T00:40:27Z
```diff
@@ -53,6 +53,25 @@ namespaceSelector:
     - team-a
 ```
 
+## Queueing strategy
+
+You can set different queueing strategies in a ClusterQueue using the
+`.spec.queueingStrategy` field. The queueing strategy determines how workloads
+are ordered in the ClusterQueue and how they are re-queued after an unsuccessful
+[admission](.#admission) attempt.
+
+The following are the supported queueing strategies:
+
+- `StrictFIFO`: Workloads are ordered first by [priority](queued_workload.md#priority)
+  and then by `.metadata.creationTimestamp`. Older workloads that can't be
+  admitted will block admitting newer workloads even if they fit in the
```

  ```suggestion
    admitted will block newer workloads, even if the newer workloads fit in the
  ```

- **docs/concepts/cluster_queue.md:70** by [@cortespao](https://github.com/cortespao) — 2022-04-07T00:41:04Z
```diff
@@ -53,6 +53,25 @@ namespaceSelector:
     - team-a
 ```
 
+## Queueing strategy
+
+You can set different queueing strategies in a ClusterQueue using the
+`.spec.queueingStrategy` field. The queueing strategy determines how workloads
+are ordered in the ClusterQueue and how they are re-queued after an unsuccessful
+[admission](.#admission) attempt.
+
+The following are the supported queueing strategies:
+
+- `StrictFIFO`: Workloads are ordered first by [priority](queued_workload.md#priority)
+  and then by `.metadata.creationTimestamp`. Older workloads that can't be
+  admitted will block admitting newer workloads even if they fit in the
+  available quota.
+- `BestEffortFIFO`: Workloads are ordered the same way as `StrictFIFO`. However,
+  older workloads that can't be admitted will not block admitting newer
```

  ```suggestion
    older workloads that can't be admitted will not block newer
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T14:57:55Z

- **docs/concepts/cluster_queue.md:67** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:42:25Z
```diff
@@ -53,6 +53,25 @@ namespaceSelector:
     - team-a
 ```
 
+## Queueing strategy
+
+You can set different queueing strategies in a ClusterQueue using the
+`.spec.queueingStrategy` field. The queueing strategy determines how workloads
+are ordered in the ClusterQueue and how they are re-queued after an unsuccessful
+[admission](.#admission) attempt.
+
+The following are the supported queueing strategies:
+
+- `StrictFIFO`: Workloads are ordered first by [priority](queued_workload.md#priority)
+  and then by `.metadata.creationTimestamp`. Older workloads that can't be
+  admitted will block admitting newer workloads even if they fit in the
```

  Done

- **docs/concepts/cluster_queue.md:70** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:42:34Z
```diff
@@ -53,6 +53,25 @@ namespaceSelector:
     - team-a
 ```
 
+## Queueing strategy
+
+You can set different queueing strategies in a ClusterQueue using the
+`.spec.queueingStrategy` field. The queueing strategy determines how workloads
+are ordered in the ClusterQueue and how they are re-queued after an unsuccessful
+[admission](.#admission) attempt.
+
+The following are the supported queueing strategies:
+
+- `StrictFIFO`: Workloads are ordered first by [priority](queued_workload.md#priority)
+  and then by `.metadata.creationTimestamp`. Older workloads that can't be
+  admitted will block admitting newer workloads even if they fit in the
+  available quota.
+- `BestEffortFIFO`: Workloads are ordered the same way as `StrictFIFO`. However,
+  older workloads that can't be admitted will not block admitting newer
```

  Done
