# PR #1823: [multikueue] Decrease the number logged errors

**Summary**: [multikueue] Decrease the number logged errors

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1823

**Last updated**: 2024-03-28T09:21:22Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `multikueue-less-errs` → `main`
- **Created**: 2024-03-12T08:13:48Z
- **Updated**: 2024-03-28T09:21:22Z
- **Merged**: 2024-03-13T08:00:11Z
- **Closed**: 2024-03-13T08:00:11Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 1   **+16 / -5**

## Description



<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Limit the number and severity of logged errors during normal operations.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1814 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-03-12T08:14:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a74a4cdf8b2ea158f390e9e64a091d67a84a257b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65f1584684aed00009cb3731 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-13T07:46:55Z

/lgtm
/approve
Thanks

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-13T07:47:00Z

LGTM label has been added.  <details>Git tree hash: f73365635f736b611bb4f68a83d37bf6007efdca</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-13T07:47:05Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1823#issuecomment-1993747464" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1823#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-13T08:38:31Z

/cherry-pick release-0.6

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-03-13T08:39:10Z

@mimowo: new pull request created: #1828

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1823#issuecomment-1993825611):

>/cherry-pick release-0.6


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-13T13:51:29Z

Can you post what the "errors" looked like?

Are they completely ignorable?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-13T13:58:24Z

> Can you post what the "errors" looked like?

The main one we care about is under the issue. It is returned when the watch expires:
```
{"level":"error","ts":"2024-03-06T08:21:44.586427722Z","caller":"multikueue/multikueuecluster.go:200","msg":"Cannot get workload key","clusterName":"multikueue-test-worker2","watchKind":"jobset.x-k8s.io/v1alpha2, Kind=JobSet","jobKind":"/, Kind=","error":"not a jobset","stacktrace":"sigs.k8s.io/kueue/pkg/controller/admissionchecks/multikueue.(*remoteClient).startWatcher.func1\n\t/workspace/pkg/controller/admissionchecks/multikueue/multikueuecluster.go:200"}
```
And the watch expires every 30min or so, which is driven by the API server configuration param `--min-request-timeout`.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-13T13:58:59Z

The other one `"log.V(3).Info("Watch error with unexpected type", "type", fmt.Sprintf("%T", s))"` I never observed

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-13T14:04:56Z

^^ This is before the change. I don't have ready-to-paste output after the change. I could generate one easily.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-13T15:14:23Z

This is how kueue logs now:

```
> kubectl logs kueue-controller-manager-5b48c8b59b-wg57n -nkueue-system 2>&1 | grep -i watch
{"level":"info","ts":"2024-03-13T14:38:41.164876413Z","logger":"controller-runtime.certwatcher","caller":"certwatcher/certwatcher.go:161","msg":"Updated current TLS certificate"}
{"level":"info","ts":"2024-03-13T14:38:41.165088008Z","logger":"controller-runtime.certwatcher","caller":"certwatcher/certwatcher.go:115","msg":"Starting certificate watcher"}
{"level":"Level(-2)","ts":"2024-03-13T14:39:05.267615703Z","caller":"multikueue/multikueuecluster.go:198","msg":"Starting watch","clusterName":"multikueue-test-worker2","watchKind":"Workload.kueue.x-k8s.io"}
{"level":"Level(-2)","ts":"2024-03-13T14:39:05.27162791Z","caller":"multikueue/multikueuecluster.go:198","msg":"Starting watch","clusterName":"multikueue-test-worker2","watchKind":"jobset.x-k8s.io/v1alpha2, Kind=JobSet"}
{"level":"Level(-2)","ts":"2024-03-13T14:39:05.285750407Z","caller":"multikueue/multikueuecluster.go:198","msg":"Starting watch","clusterName":"multikueue-test-worker1","watchKind":"Workload.kueue.x-k8s.io"}
{"level":"Level(-2)","ts":"2024-03-13T14:39:05.290619177Z","caller":"multikueue/multikueuecluster.go:198","msg":"Starting watch","clusterName":"multikueue-test-worker1","watchKind":"jobset.x-k8s.io/v1alpha2, Kind=JobSet"}
{"level":"Level(-2)","ts":"2024-03-13T15:12:27.159711619Z","caller":"multikueue/multikueuecluster.go:217","msg":"Watch ended","clusterName":"multikueue-test-worker1","watchKind":"Workload.kueue.x-k8s.io","ctxErr":null}
{"level":"Level(-2)","ts":"2024-03-13T15:12:27.159799974Z","caller":"multikueue/multikueuecluster.go:223","msg":"Queue reconcile for reconnect","clusterName":"multikueue-test-worker1","watchKind":"Workload.kueue.x-k8s.io","cluster":"multikueue-test-worker1"}
{"level":"Level(-3)","ts":"2024-03-13T15:12:27.160910077Z","caller":"multikueue/multikueuecluster.go:204","msg":"Watch error","clusterName":"multikueue-test-worker1","watchKind":"jobset.x-k8s.io/v1alpha2, Kind=JobSet","status":"Failure","message":"an error on the server (\"unable to decode an event from the watch stream: context canceled\") has prevented the request from succeeding","reason":"InternalError"}
{"level":"Level(-2)","ts":"2024-03-13T15:12:27.161043184Z","caller":"multikueue/multikueuecluster.go:217","msg":"Watch ended","clusterName":"multikueue-test-worker1","watchKind":"jobset.x-k8s.io/v1alpha2, Kind=JobSet","ctxErr":"context canceled"}
{"level":"Level(-2)","ts":"2024-03-13T15:12:27.167277052Z","caller":"multikueue/multikueuecluster.go:198","msg":"Starting watch","clusterName":"multikueue-test-worker1","watchKind":"Workload.kueue.x-k8s.io"}
{"level":"Level(-2)","ts":"2024-03-13T15:12:27.171478071Z","caller":"multikueue/multikueuecluster.go:198","msg":"Starting watch","clusterName":"multikueue-test-worker1","watchKind":"jobset.x-k8s.io/v1alpha2, Kind=JobSet"
```
I guess not too bad, note that now the message "Watch error" is `V(3)`. WDYT?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-13T15:28:49Z

I don't like the idea of hiding errors as V(3) Info. If they are expected, I have a hard time understanding why they are marked as `InternalError`.

Maybe @apelisse can provide some guidance?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-14T13:13:12Z

What context are we passing to start the watch? I wonder if controller-runtime has a timeout for the context.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-14T13:59:00Z

@trasc can you help to answer that?

### Comment by [@trasc](https://github.com/trasc) — 2024-03-14T14:16:17Z

Nothing special, a `WithCancel` of the managers `Start` context.
https://github.com/kubernetes-sigs/kueue/blob/a96927de470d985f17b03a328c33bb8547eae9a4/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L162C29-L162C47

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-27T10:47:08Z

I looked at this a little bit more, here is the summary:
* I confirmed that the watch is closed depending on the `min-request-timeout` param. When set to 30s the watch usually ends in 30s - 1min. This is consistent for kind and GKE.
* The watch ends with 200 status code, there is no error server side. Here is an example server-side log by the API server, when the watch was closed after 34s:
```
1 httplog.go:132] "HTTP" verb="WATCH" URI="/apis/jobset.x-k8s.io/v1alpha2/jobsets?labelSelector=kueue.x-k8s.io%2Fmultikueue-origin%3Dmultikueue&watch=true" latency="34.525929114s" userAgent="manager/v0.0.0 (linux/amd64) kubernetes/$Format" audit-ID="55d611cc-e3b5-4643-b5b1-92182703a262" srcIP="192.168.8.1:36440" apf_pl="workload-low" apf_fs="service-accounts" apf_iseats=1 apf_fseats=0 apf_additionalLatency="0s" apf_init_latency="320.89µs" apf_execution_time="323.26µs" resp=200
```

I think I traced down a little more what happens at the client side.
1. the client creates a new watcher with InternalError (500) as the default: https://github.com/kubernetes/kubernetes/blob/03ce04584437624840ad78edac1b772e47e78dc2/staging/src/k8s.io/client-go/rest/request.go#L789-L791
2. the watcher wraps the closed connection with the error that we see in the logs: https://github.com/kubernetes/kubernetes/blob/03ce04584437624840ad78edac1b772e47e78dc2/staging/src/k8s.io/apimachinery/pkg/watch/streamwatcher.go#L120
3. this is further wrapped by the `AsObject` function which delegates to `NewGenericServerResponse`: https://github.com/kubernetes/kubernetes/blob/03ce04584437624840ad78edac1b772e47e78dc2/staging/src/k8s.io/apimachinery/pkg/api/errors/errors.go#L843-L857
4. The `NewGenericServerResponse` does the final wrapping (because 500 is the defualt): https://github.com/kubernetes/kubernetes/blob/03ce04584437624840ad78edac1b772e47e78dc2/staging/src/k8s.io/apimachinery/pkg/api/errors/errors.go#L487-L490

This seems also reported here: https://github.com/kubernetes/client-go/issues/12, and the suggestion seems to just break the loop when the channel is closed, regardless of the event. 

Finally. handling of the nuances of watcher recreation seems to be already covered by the `RetryWatcher` here: https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/client-go/tools/watch/retrywatcher.go#L149-L236. WDYT @alculquicondor? I can open a follow up issue to investigate / clean up.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-27T15:05:56Z

The immediate conclusion for me is that we shouldn't bother with a cherry-pick.

Whereas for an appropriate solution, I think we should report this as a bug in the client-go, because this should not be tagged as an "InternalError".

But for the time being, we'll have to live with the silencing.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-27T15:16:10Z

Sg, closing the cherry pick. I will open the issue for client-go. I'm yet wondering about using the RetryWatcher to abstract out this craft. Any view, I think it makes sense to open an issue, maybe someone will pick this up.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-28T09:14:09Z

Open the issue upstream: https://github.com/kubernetes/client-go/issues/1340.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-28T09:21:21Z

Also, opened the follow up issue to investigate and possibly use the RetryWatcher: https://github.com/kubernetes-sigs/kueue/issues/1924

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-12T09:19:39Z

- **pkg/controller/admissionchecks/multikueue/workload.go:183** by [@mimowo](https://github.com/mimowo) — 2024-03-12T09:17:00Z
```diff
@@ -180,7 +180,10 @@ func (w *wlReconciler) remoteClientsForAC(ctx context.Context, acName string) (m
 	clients := make(map[string]*remoteClient, len(cfg.Spec.Clusters))
 	for _, clusterName := range cfg.Spec.Clusters {
 		if client, found := w.clusters.controllerFor(clusterName); found {
-			clients[clusterName] = client
+			// Skip the client if its reconnect is ongoing.
```

  Some questions:
  1. Is this related to the errors, or drive by fix? 
  2. What is the user-visible impact (if any)?

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:206** by [@mimowo](https://github.com/mimowo) — 2024-03-12T09:18:38Z
```diff
@@ -196,11 +197,21 @@ func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKu
 	go func() {
 		log.V(2).Info("Starting watch")
 		for r := range newWatcher.ResultChan() {
-			wlKey, err := w.GetWorkloadKey(r.Object)
-			if err != nil {
-				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
-			} else {
-				rc.queueWorkloadEvent(ctx, wlKey)
+			switch r.Type {
+			case watch.Error:
+				switch s := r.Object.(type) {
+				case *metav1.Status:
+					log.V(5).Info("Watch error", "status", s.Status, "message", s.Message, "reason", s.Reason)
+				default:
+					log.V(5).Info("Watch error with unexpected type", "type", fmt.Sprintf("%T", s))
```

  ```suggestion
  					log.V(2).Error("Watch error with unexpected type", "type", fmt.Sprintf("%T", s))
  ```
  If this is unexpected, we prefer to get it logged at high level.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-12T15:14:29Z

- **pkg/controller/admissionchecks/multikueue/workload.go:183** by [@trasc](https://github.com/trasc) — 2024-03-12T15:14:29Z
```diff
@@ -180,7 +180,10 @@ func (w *wlReconciler) remoteClientsForAC(ctx context.Context, acName string) (m
 	clients := make(map[string]*remoteClient, len(cfg.Spec.Clusters))
 	for _, clusterName := range cfg.Spec.Clusters {
 		if client, found := w.clusters.controllerFor(clusterName); found {
-			clients[clusterName] = client
+			// Skip the client if its reconnect is ongoing.
```

  I't not the error in the issue, but if the watch has failed and the client tries to reconnect and the, say network connection is still down, trying to get workloads from the worker will also fail. So in my opinion we should skip  that client for the time being.
  
  Alternatively we can just skip the reporting of the error in `readGroup`, but it looks strange.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-12T15:16:32Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:206** by [@trasc](https://github.com/trasc) — 2024-03-12T15:16:31Z
```diff
@@ -196,11 +197,21 @@ func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKu
 	go func() {
 		log.V(2).Info("Starting watch")
 		for r := range newWatcher.ResultChan() {
-			wlKey, err := w.GetWorkloadKey(r.Object)
-			if err != nil {
-				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
-			} else {
-				rc.queueWorkloadEvent(ctx, wlKey)
+			switch r.Type {
+			case watch.Error:
+				switch s := r.Object.(type) {
+				case *metav1.Status:
+					log.V(5).Info("Watch error", "status", s.Status, "message", s.Message, "reason", s.Reason)
+				default:
+					log.V(5).Info("Watch error with unexpected type", "type", fmt.Sprintf("%T", s))
```

  The type of the error is unexpected, the fact that the error is reported is as expected as two lines above.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-12T15:26:15Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:206** by [@mimowo](https://github.com/mimowo) — 2024-03-12T15:26:15Z
```diff
@@ -196,11 +197,21 @@ func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKu
 	go func() {
 		log.V(2).Info("Starting watch")
 		for r := range newWatcher.ResultChan() {
-			wlKey, err := w.GetWorkloadKey(r.Object)
-			if err != nil {
-				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
-			} else {
-				rc.queueWorkloadEvent(ctx, wlKey)
+			switch r.Type {
+			case watch.Error:
+				switch s := r.Object.(type) {
+				case *metav1.Status:
+					log.V(5).Info("Watch error", "status", s.Status, "message", s.Message, "reason", s.Reason)
+				default:
+					log.V(5).Info("Watch error with unexpected type", "type", fmt.Sprintf("%T", s))
```

   The message "Watch error with unexpected type" suggests something is going seriously wrong, so setting (5) seems too low. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-12T15:34:36Z

- **pkg/controller/admissionchecks/multikueue/workload.go:183** by [@mimowo](https://github.com/mimowo) — 2024-03-12T15:34:36Z
```diff
@@ -180,7 +180,10 @@ func (w *wlReconciler) remoteClientsForAC(ctx context.Context, acName string) (m
 	clients := make(map[string]*remoteClient, len(cfg.Spec.Clusters))
 	for _, clusterName := range cfg.Spec.Clusters {
 		if client, found := w.clusters.controllerFor(clusterName); found {
-			clients[clusterName] = client
+			// Skip the client if its reconnect is ongoing.
```

  IIUC there is no user-facing impact except for skipping some requests which are likely to fail. 
  
  Could we have this maybe as a separate PR if not related, and with some unit test?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-12T15:37:11Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:206** by [@mimowo](https://github.com/mimowo) — 2024-03-12T15:37:11Z
```diff
@@ -196,11 +197,21 @@ func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKu
 	go func() {
 		log.V(2).Info("Starting watch")
 		for r := range newWatcher.ResultChan() {
-			wlKey, err := w.GetWorkloadKey(r.Object)
-			if err != nil {
-				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
-			} else {
-				rc.queueWorkloadEvent(ctx, wlKey)
+			switch r.Type {
+			case watch.Error:
+				switch s := r.Object.(type) {
+				case *metav1.Status:
+					log.V(5).Info("Watch error", "status", s.Status, "message", s.Message, "reason", s.Reason)
+				default:
+					log.V(5).Info("Watch error with unexpected type", "type", fmt.Sprintf("%T", s))
```

  Oh, I see you don't have err at hand here to call `Error`, still, v(2) seems more appropriate

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-13T07:43:19Z

- **pkg/controller/admissionchecks/multikueue/workload.go:183** by [@trasc](https://github.com/trasc) — 2024-03-13T07:43:19Z
```diff
@@ -180,7 +180,10 @@ func (w *wlReconciler) remoteClientsForAC(ctx context.Context, acName string) (m
 	clients := make(map[string]*remoteClient, len(cfg.Spec.Clusters))
 	for _, clusterName := range cfg.Spec.Clusters {
 		if client, found := w.clusters.controllerFor(clusterName); found {
-			clients[clusterName] = client
+			// Skip the client if its reconnect is ongoing.
```

  Will be done in another PR

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-13T07:43:50Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:206** by [@trasc](https://github.com/trasc) — 2024-03-13T07:43:49Z
```diff
@@ -196,11 +197,21 @@ func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKu
 	go func() {
 		log.V(2).Info("Starting watch")
 		for r := range newWatcher.ResultChan() {
-			wlKey, err := w.GetWorkloadKey(r.Object)
-			if err != nil {
-				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
-			} else {
-				rc.queueWorkloadEvent(ctx, wlKey)
+			switch r.Type {
+			case watch.Error:
+				switch s := r.Object.(type) {
+				case *metav1.Status:
+					log.V(5).Info("Watch error", "status", s.Status, "message", s.Message, "reason", s.Reason)
+				default:
+					log.V(5).Info("Watch error with unexpected type", "type", fmt.Sprintf("%T", s))
```

  Let's go with V(3)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-13T07:44:06Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:206** by [@trasc](https://github.com/trasc) — 2024-03-13T07:44:06Z
```diff
@@ -196,11 +197,21 @@ func (rc *remoteClient) startWatcher(ctx context.Context, kind string, w multiKu
 	go func() {
 		log.V(2).Info("Starting watch")
 		for r := range newWatcher.ResultChan() {
-			wlKey, err := w.GetWorkloadKey(r.Object)
-			if err != nil {
-				log.V(2).Error(err, "Cannot get workload key", "jobKind", r.Object.GetObjectKind().GroupVersionKind())
-			} else {
-				rc.queueWorkloadEvent(ctx, wlKey)
+			switch r.Type {
+			case watch.Error:
+				switch s := r.Object.(type) {
+				case *metav1.Status:
+					log.V(5).Info("Watch error", "status", s.Status, "message", s.Message, "reason", s.Reason)
+				default:
+					log.V(5).Info("Watch error with unexpected type", "type", fmt.Sprintf("%T", s))
```

  Let's go with V(3)
