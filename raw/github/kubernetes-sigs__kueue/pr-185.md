# PR #185: Cleanup: variables renaming

**Summary**: Cleanup: variables renaming

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/185

**Last updated**: 2022-04-07T19:39:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-cleanup` → `main`
- **Created**: 2022-04-07T13:05:34Z
- **Updated**: 2022-04-07T19:39:24Z
- **Merged**: 2022-04-07T19:39:24Z
- **Closed**: 2022-04-07T19:39:24Z
- **Labels**: `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: _none_
- **Changed files**: 1   **+32 / -22**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

variables renaming for consistency

#### Which issue(s) this PR fixes:
Fixes #

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-07T13:08:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/185#" title="Author self-approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/185#pullrequestreview-935036425" title="LGTM">ArangoGutierrez</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-04-07T14:06:47Z

/assign alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:15:23Z

not so sure about this one, maybe we should name all the booleans `admissible`.

otherwise you run into double-negative situations `inadmissible = false`, which can be confusing.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T14:23:28Z

ok, lets use admissible for the boolean and inadmissibleMessage for the message

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T14:56:58Z

I was suggesting in the other PR that we use `admitted` and `inadmissibleMessage`

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T15:28:46Z

> I was suggesting in the other PR that we use `admitted` and `inadmissibleMessage`

during nominate, it would only be admissible not admitted.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T19:27:32Z

> LGTM squash?

done

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T19:34:37Z

/lgtm

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T19:38:15Z

/hold cancel

## Reviews

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (approved) — 2022-04-07T13:08:18Z

/lgtm
/hold

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-07T14:19:04Z

- **pkg/scheduler/scheduler.go:112** by [@denkensk](https://github.com/denkensk) — 2022-04-07T14:19:00Z
```diff
@@ -104,22 +104,22 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	usedCohorts := sets.NewString()
 	for i := range entries {
 		e := &entries[i]
-		if !e.admissible {
+		if e.inadmissible {
 			continue
 		}
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
-			e.admissible = false
+			e.inadmissible = true
```

  There is a bit of inconsistency here.
  We mark the workloads with ("cohort used in this cycle") as not inadmissible when re-enqueue.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T15:34:49Z

- **pkg/scheduler/scheduler.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T15:34:49Z
```diff
@@ -104,22 +104,22 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	usedCohorts := sets.NewString()
 	for i := range entries {
 		e := &entries[i]
-		if !e.admissible {
+		if e.inadmissible {
 			continue
 		}
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
-			e.admissible = false
+			e.inadmissible = true
```

  I agree that this is inconsistent.
  That's why I preferred `admitted`. What about `nominated`?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T17:59:34Z

- **pkg/scheduler/scheduler.go:112** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T17:59:33Z
```diff
@@ -104,22 +104,22 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	usedCohorts := sets.NewString()
 	for i := range entries {
 		e := &entries[i]
-		if !e.admissible {
+		if e.inadmissible {
 			continue
 		}
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
-			e.admissible = false
+			e.inadmissible = true
```

  good catch, I guess we can use nominated, I hesitated initially because it didn't make sense when reversing this flag in case admit fails (few lines below)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T18:01:21Z

- **pkg/scheduler/scheduler.go:112** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T18:01:21Z
```diff
@@ -104,22 +104,22 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	usedCohorts := sets.NewString()
 	for i := range entries {
 		e := &entries[i]
-		if !e.admissible {
+		if e.inadmissible {
 			continue
 		}
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
-			e.admissible = false
+			e.inadmissible = true
```

  actually we can have both flags, and that will solve the problem

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T18:17:15Z

- **pkg/scheduler/scheduler.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T18:17:15Z
```diff
@@ -104,22 +104,22 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	usedCohorts := sets.NewString()
 	for i := range entries {
 		e := &entries[i]
-		if !e.admissible {
+		if e.inadmissible {
 			continue
 		}
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
-			e.admissible = false
+			e.inadmissible = true
```

  In that case, we can have `inadmissible` and `nominated`, and get rid of `skipped`?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T18:20:01Z

- **pkg/scheduler/scheduler.go:112** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T18:20:01Z
```diff
@@ -104,22 +104,22 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	usedCohorts := sets.NewString()
 	for i := range entries {
 		e := &entries[i]
-		if !e.admissible {
+		if e.inadmissible {
 			continue
 		}
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
-			e.admissible = false
+			e.inadmissible = true
```

  I changed to enum, take a look :)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T18:27:10Z

- **pkg/scheduler/scheduler.go:112** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T18:27:10Z
```diff
@@ -104,22 +104,22 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	usedCohorts := sets.NewString()
 	for i := range entries {
 		e := &entries[i]
-		if !e.admissible {
+		if e.inadmissible {
 			continue
 		}
 		c := snapshot.ClusterQueues[e.ClusterQueue]
 		if len(e.borrows) > 0 && c.Cohort != nil && usedCohorts.Has(c.Cohort.Name) {
-			e.admissible = false
+			e.inadmissible = true
```

  Two inadmissible and nominated flags would look nice too, i am ok with either, wdyt?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T18:45:37Z

- **pkg/scheduler/scheduler.go:153** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T18:44:34Z
```diff
@@ -134,14 +135,24 @@ func (s *Scheduler) schedule(ctx context.Context) {
 
 	// 6. Requeue the heads that were not scheduled.
 	for _, e := range entries {
-		if e.admissible {
-			continue
+		if e.status != admitted {
+			s.requeueAndUpdate(log, ctx, &e.Info, e.inadmissibleReason)
+			s.recorder.Eventf(e.Obj, corev1.EventTypeNormal, "Pending", e.inadmissibleReason)
 		}
-		s.requeueAndUpdate(log, ctx, &e.Info, e.noAdmissionReason)
-		s.recorder.Eventf(e.Obj, corev1.EventTypeNormal, "Pending", e.noAdmissionReason)
 	}
 }
 
+type entryStatus string
+
+const (
+	// indicates if the workload is nominated for admission.
+	nominated entryStatus = "nominated"
+	// indicates if the workload was nominated but skipped in this cycle.
+	skipped entryStatus = "skipped"
+	// indicates if the workload was successfully admitted.
+	admitted entryStatus = "admitted"
```

  `assumed` is more accurate, because the API call is async.
  
  but that's a nit. I'm ok with this too.

- **pkg/scheduler/scheduler.go:189** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-07T18:45:26Z
```diff
@@ -167,15 +177,15 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if cq == nil {
-			e.noAdmissionReason = "ClusterQueue not found"
+			e.inadmissibleReason = "ClusterQueue not found"
 		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			e.noAdmissionReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
+			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			e.noAdmissionReason = "Workload namespace doesn't match ClusterQueue selector"
+			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
 		} else if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			e.noAdmissionReason = "Workload didn't fit in the remaining quota"
+			e.inadmissibleReason = "Workload didn't fit in the remaining quota"
 		} else {
-			e.admissible = true
+			e.status = nominated
 			log.V(3).Info("Nominated workload for admission")
```

  Should we remove this line here and log every time for every entry at the end? Including the status and reason.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-07T19:14:55Z

- **pkg/scheduler/scheduler.go:189** by [@ahg-g](https://github.com/ahg-g) — 2022-04-07T19:14:54Z
```diff
@@ -167,15 +177,15 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if cq == nil {
-			e.noAdmissionReason = "ClusterQueue not found"
+			e.inadmissibleReason = "ClusterQueue not found"
 		} else if err := s.client.Get(ctx, types.NamespacedName{Name: w.Obj.Namespace}, &ns); err != nil {
-			e.noAdmissionReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
+			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
-			e.noAdmissionReason = "Workload namespace doesn't match ClusterQueue selector"
+			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
 		} else if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			e.noAdmissionReason = "Workload didn't fit in the remaining quota"
+			e.inadmissibleReason = "Workload didn't fit in the remaining quota"
 		} else {
-			e.admissible = true
+			e.status = nominated
 			log.V(3).Info("Nominated workload for admission")
```

  moved the log to the loop at the end of the `schedule` func

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-07T19:16:35Z

LGTM
squash?
