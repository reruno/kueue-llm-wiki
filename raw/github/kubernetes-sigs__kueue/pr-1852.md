# PR #1852: Dynamically Sized Jobs - Scale Down

**Summary**: Dynamically Sized Jobs - Scale Down

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1852

**Last updated**: 2025-08-28T16:14:23Z

---

## Metadata

- **State**: closed
- **Author**: [@vicentefb](https://github.com/vicentefb)
- **Branch**: `ElasticJobs` → `main`
- **Created**: 2024-03-15T02:51:37Z
- **Updated**: 2025-08-28T16:14:23Z
- **Merged**: —
- **Closed**: 2025-08-28T16:14:22Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `needs-rebase`, `do-not-merge/hold`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@andrewsykim](https://github.com/andrewsykim), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 11   **+374 / -9**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:
This includes Phase 1 implementation for Dynamically Sized Jobs KEP #1851 

#### Which issue(s) this PR fixes:
Part of #77 

#### Special notes for your reviewer: 
Scale Down only implementation for RayClusters

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Scale Down implementation for RayClusters
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-15T02:51:47Z

Hi @vicentefb. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-03-15T02:51:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/667c66c547e9ff0007fc8ebf |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1852--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1852--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE4NTItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.JzribNUoaEBfVTJIPMVGlqjNE0LVgjEVgURU7CYtGOo)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@vicentefb](https://github.com/vicentefb) — 2024-03-15T02:55:00Z

cc @andrewsykim @astefanutti

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-15T22:56:39Z

/ok-to-test

### Comment by [@kerthcet](https://github.com/kerthcet) — 2024-05-06T09:58:38Z

/cc

### Comment by [@vicentefb](https://github.com/vicentefb) — 2024-05-06T18:00:44Z

/retest

### Comment by [@vicentefb](https://github.com/vicentefb) — 2024-05-31T00:05:21Z

/retest

### Comment by [@vicentefb](https://github.com/vicentefb) — 2024-05-31T02:23:19Z

friendly ping :) @kerthcet @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-31T13:48:50Z

/hold

Waiting for the release of 0.7 before merging this

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-06T23:34:50Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1852#" title="Author self-approved">vicentefb</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@vicentefb](https://github.com/vicentefb) — 2024-06-26T20:41:59Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-07-20T09:38:40Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@akram](https://github.com/akram) — 2024-10-13T20:50:44Z

any update about this PR ?
It was looking as a good start

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-10-15T17:08:06Z

This needs a contributor to pick up the work and complete the scale up design and implementation.

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-01-16T15:49:17Z

My understanding is that Kueue supports plain pods (https://kueue.sigs.k8s.io/docs/tasks/run/plain_pods/#example-pod) therefore couldn't we have the head node and pod-workers have a `kueue.x-k8s.io/queue-name` label ? 

Then instead of supporting special logic for scaling up/down a RayCluster we could just make sure that the RayCluster contains workers which have a label that's the same as the one in the RayCluster object. Then the inTreeAutoScaler of Ray would just create pod objects and they would be scheduled based on the capacity of the queue they point to.

Edit: the head node should also have the same label

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2025-01-16T16:20:37Z

That's a valid approach too. You would probably want the head nodes to have a higher priority than the workers, to reduce risk of preemption.

### Comment by [@troychiu](https://github.com/troychiu) — 2025-02-03T09:01:53Z

> My understanding is that Kueue supports plain pods (https://kueue.sigs.k8s.io/docs/tasks/run/plain_pods/#example-pod) therefore couldn't we have the head node and pod-workers have a `kueue.x-k8s.io/queue-name` label ?
> 
> Then instead of supporting special logic for scaling up/down a RayCluster we could just make sure that the RayCluster contains workers which have a label that's the same as the one in the RayCluster object. Then the inTreeAutoScaler of Ray would just create pod objects and they would be scheduled based on the capacity of the queue they point to.
> 
> Edit: the head node should also have the same label

Does this mean that we would need a workload CR for each Ray worker? I think that would be a heavy load for the cluster. Also, users have to enable plain pods integration if they want to use ray autoscaling in this case?

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-02-06T12:41:53Z

> Does this mean that we would need a workload CR for each Ray worker? I think that would be a heavy load for the cluster. 

I don't think this is a big problem. For example, I expect that we currently have 1 Workload CR per managed Job and the number of jobs on a cluster is greater than the number of Ray Workers pods. This is because your average ray worker pod tends to have several tasks running on it.

What would be the number of Ray worker pods that would cause kueue to show signs of heavy load ?

> Also, users have to enable plain pods integration if they want to use ray autoscaling in this case?

Yeah, I think cluster admins - or people with privileges to configure Kueue - would need to enable pod integration (https://kueue.sigs.k8s.io/docs/tasks/run/plain_pods/#before-you-begin). It's probably best they restrict Kueue to managing the pods for just a limited number of namespaces - those which contain RayClusters managed by Kueue.

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-02-06T12:47:21Z

The problem I'm currently facing is that I am using RayClusters in a Kueue managed K8s cluster. Kueue stops me from dynamically scaling my RayClusters which in turn makes my hardware utilization poor :(. 

Ray, with the help of KubeRay and the inTreeAutoScaler, already has support for that. So I was thinking that we can just configure Kueue to react to the decisions of Ray/KubeRay by just looking at the Pods that Ray/Kuberay delete/create. In turn, that would simplify the RayCluster-specific code in Kueue.

Of course, you rarely get something for free. This suggestion would need Kueue to manage pods.

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2025-02-07T11:40:56Z

> Then instead of supporting special logic for scaling up/down a RayCluster we could just make sure that the RayCluster contains workers which have a label that's the same as the one in the RayCluster object. Then the inTreeAutoScaler of Ray would just create pod objects and they would be scheduled based on the capacity of the queue they point to.

Does this mean that each pod in the RayCluster will be considered as a separate workload? Can we use TAS (topology-aware scheduling) for RayCluster when we submit RayCluster like this?

### Comment by [@KunWuLuan](https://github.com/KunWuLuan) — 2025-02-07T11:43:57Z

@vicentefb @alculquicondor Do you need contributors? Let me see what can I help.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2025-02-07T13:21:45Z

I'm not involved in this project anymore.
cc @mimowo @tenzen-y

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-02-07T13:25:59Z

> Does this mean that each pod in the RayCluster will be considered as a separate workload? 

In my proposal, yes.

> Can we use TAS (topology-aware scheduling) for RayCluster when we submit RayCluster like this?

Not sure about that. A RayCluster is effectively a collection of pod definitions. As long Kueue supports using the TAS-related annotations/labels (https://kueue.sigs.k8s.io/docs/concepts/topology_aware_scheduling/#example-1) in a Pod, then it should work with the RayCluster workers/head-node too.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-07T13:35:08Z

ack. IIUC @VassilisVassiliadis your proposal is to have a Workload per Pod to achieve scaling of RayClusters? 

We apply that approach for Deployments. However, Deployments by nature are meant for serving and thus they typically support partial preemption and working at partial capacity. For workloads managed by Ray this is less obvious. Some users who use Ray for training would not want that granularity, I think.

So my questions that come first to my mind:
1. what would be the determining factor between the classic Workload per RayCluster  and Workload per Pod approaches? Some annotation?
2. what are the advantages of this approach over the scale down support as proposed in the KEP https://github.com/kubernetes-sigs/kueue/issues/77? Is it "time to market" / "ease of implementation" or are there additional use cases supported thanks to that?
3. do you only care about scale down? If so, then it probably could be implemented just by using dynamic reclaim of quota for finished pods? If so, then it should also be relatively easy to implement and a more generic solution (applicable to also training).

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-02-07T14:49:51Z

> ack. IIUC @VassilisVassiliadis your proposal is to have a Workload per Pod to achieve scaling of RayClusters?

Yes that's it. Instead of switching of the inTreeAutoScaler off ray and handling scaling of workers as an all or nothing static decision within Kueue we could let the autoScaler do its thing and just react to requests for individual pods. 

Effectively, we're letting KubeRay just submit pods to K8s whenever it decides to do that. The plain pods are then intercepted by Kueue and handled accordingly.

> 
> We apply that approach for Deployments. However, Deployments by nature are meant for serving and thus they typically support partial preemption and working at partial capacity. For workloads managed by Ray this is less obvious. Some users who use Ray for training would not want that granularity, I think.

That's understandable.

> 
> So my questions that come first to my mind:
> 
>     1. what would be the determining factor between the classic Workload per RayCluster  and Workload per Pod approaches? Some annotation?

I think you're asking how Kueue would tell between scheduling the entire RayCluster as 1 thing or allowing KubeRay to dynamically scale the Ray workers up/down i.e. Kueue just deals with the objects that KubeRay creates, not the RayCluster object itself. Perhaps, there is a label at the level of the RayCluster CR to configure which method to use.

>     2. what are the advantages of this approach over the scale down support as proposed in the KEP [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)? Is it "time to market" / "ease of implementation" or are there additional use cases supported thanks to that?

The proposal can be considered as "don't treat a RayCluster as a resource Kueue manages, instead handle the objects it creates".

The advantages are:

1. Kueue does not need extra code to support dynamically resized RayClusters -> Faster time to market, Less code to manage inside Kueue which is a kind of superset for "ease of implementation". This is because KubeRay/ray take care of deciding when to scale up/down the cluster resources. Kueue doesn't need to actually have logic for the RayCluster objects.
1. We don't have to switch off the inTreeAutoScaler of KubeRay - we can also make use of Ray's experimental v2 Autoscaler which comes with a couple of bugfixes not present in v1 (e.g. strict-spread policies)
1. A RayCluster that's (indirectly) managed by Kueue works the same way as a cluster not managed by kueue --> users don't get surprised by unexpected behaviour of their RayCluster

Downside:

1. ClusterAdmins must ask Kueue to monitor plain pods
1. This approach doesn't take into account future plans for dynamically scaled Kueue workloads -> this isn't really a disadvantage just that the approach for a RayCluster wouldn't work for a generic workload.

An alternative would be to modify KubeRay so that it works with Job objects instead of Pod objects. That would get rid of the disadvantage. But of course it would need someone to update KubeRay first.

>     3. do you only care about scale down? If so, then it probably could be implemented just by using dynamic reclaim of quota for finished pods? If so, then it should also be relatively easy to implement and a more generic solution (applicable to also training).

I'm interested in both scale up and down. I'd like to create a cluster that starts with 0 workers, just the head node and have it scale to the appropriate size while reacting to the number and nature of tasks I create. This would be the behaviour I'd get on a vanilla k8s environment.

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-02-07T15:54:39Z

Another thing that came to mind is that if the entire RayCluster is scheduled as 1 then in the event of preemption the entire thing goes down. If you don't persist your Ray logs, which is something you might do to avoid dealing with garbage collection, you risk losing them.

### Comment by [@troychiu](https://github.com/troychiu) — 2025-02-10T19:02:51Z

If we are considering what @VassilisVassiliadis proposed, I am wondering if KEP-77 can also be done. In my case and possibly same for other users, we don't want to enable pod integration since a CR is created for every pod and this can greatly slow down the cluster.
If yes, I am going to create a PR to resume KEP-77.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-02-14T12:02:54Z

Sorry for the delayed response, folks.

First of all, I could understand both concerns and use cases mentioned by @VassilisVassiliadis @troychiu.
Those sound reasonable.

> If we are considering what @VassilisVassiliadis proposed, I am wondering if KEP-77 can also be done. In my case and possibly same for other users, we don't want to enable pod integration since a CR is created for every pod and this can greatly slow down the cluster.
If yes, I am going to create a PR to resume KEP-77.

However, if we move this (and scale up as well) forward, I'm wondering if we should revisit design and investigation since our JobFramework is much more evolved and complicated rather than the day when we designed this. Additionally, the next minor release is closing. So, I do not contain such big things currently.

So, my proposal is to support Ray's autoscaling step by step.

Step 1. Support known ownerReference standalone feature only for Plain Pod Integration discussed in https://github.com/kubernetes-sigs/kueue/issues/4106 (I know this can not resolve @troychiu concerns) 
This could resolve @VassilisVassiliadis use cases, IIUC, since you could enqueue Ray nodes by Pod Integraion instead of RayCluster integration to Kueue.

Step 2. Consider if we should expand the known ownerReference standalone mechanism to other frameworks, including RayCluster. I mean that we want to investigate if we should support another RayCluster enqueue mode to handle RayWorker as standalone pods as opposed to a member of RayCluster. In this new mode, we create the Workload object only with the head Node, and the worker Node will be handled by just the Pod. 

For Ray, this is mostly same behavior with `Step 1`. But, for other frameworks, this is a good starting point on how to handle the known owners. Actually, the kubeflow/trainer has similar use case.

This might be problematic since we can not guarantee when the Ray worker node Pods will be admitted by Kueue even though the Ray Head has been admitted already since the Workload objects corresponding to the Ray worker node will be created when the RayCluster Head node is admitted.

I know this is still not resolved @troychiu concerns since this offers cluster admins to enable Pod Integration.

Step 3. We redesign this straightforward dynamic approach. But, I think we can perform investigations and designing (not including impl) during step 2 so that we can delivery this faster.

In my mind, `step 1` will be able to do in v0.11 (next release) or v0.12, `step 2` will be able to do in v0.12, and `step 3` will be able to do in v0.13. But `step 3` is a much tentative candidate since it is a much bigger feature, and we currently focus on other bigger Workload scheduling evolving like fairSharing, HierarcyCohort, and TopologyScheduling.

cc @mimowo

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-02-18T16:54:52Z

I think that as long as we can have Kueue monitor pods, but not do anything to those that don't have the `kueue.x-k8s.io/queue-name` label, my suggestion should be "good enough" for now. When Kueue gets more complete support for dynamically scaling workloads it could also switch to using that for managing the RayCluster custom resources.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-18T17:01:07Z

I support Step 1 from https://github.com/kubernetes-sigs/kueue/pull/1852#issuecomment-2659158040, basically create workloads for Pods with queue-name. However, to avoid creating a workload for KubeRay APIs (RayJob or RayCluster), introduce the "standalone" annotation.

Is it aligned with your proposal  @VassilisVassiliadis?

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-02-19T20:49:47Z

hi @mimowo I had a look at https://github.com/kubernetes-sigs/kueue/issues/4106 and I think that step 1 in https://github.com/kubernetes-sigs/kueue/pull/1852#issuecomment-2659158040 makes sense. Though to be fair I think I'm mising some background information to understand the details regarding the delta between step 1 and 2.

My understanding is that step 1 implies the following changes:
1.  Kueue should not reject RayCluster objects which switch on their inTreeAutoScaler. That would enable the inTreeAutoScaler to behave similar to the Spark example in #4106. That is, the autoScaler container would create/delete pods based on the volume/requests of the ray tasks.
2. somebody must insert the appropriate `kueue.x-k8s.io/queue-name` to the worker sections of the RayCluster object (could either be left to people creating the RayCluster, or as a step in Kueue's webhook for RayCluster objects)
3. (this is totally optional) might be worth supporting the current way Kueue handles RayClusters too, via some kind of switch. This is just in case there are some unforeseen side-effects to using pod integration as opposed to the current way (no ray autoscaler and static clusters).
4. the docs should be updated to reflect the above

### Comment by [@mimowo](https://github.com/mimowo) — 2025-02-21T16:56:09Z

I think we are discussing very similar approaches, where the integration is at the Pod level. Trying to marry the ideas.

> Kueue should not reject RayCluster objects which switch on their inTreeAutoScaler.

How can Kueue tell if RayCluster is using inTreeAutoScaler?

> somebody must insert the appropriate kueue.x-k8s.io/queue-name

This could be Kueue webhook when Kueue realizes this mode of integration is used. Then it could inject that label into Pod template along with the kueue.x-k8s.io/standalone annotation. The combination of these two in the PodTemplate will make the newly created Pods to create Workloads and be managed by Kueue independently. Just as we do for Deployments.

So, I believe 2 things are to be done:
- figure out a way to tell Kueue should support the RayCluster in this mode for inTreeAutoScaler (can this be done in Kueue webhook)
- implement support for the kueue.x-k8s.io/standalone annotation for Pods, and probably for Jobs (as RayCluster also creates the submitter Job), so at this point we can just support across all CRDs.

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-02-25T10:52:06Z

> I think we are discussing very similar approaches, where the integration is at the Pod level. Trying to marry the ideas.
> 
> > Kueue should not reject RayCluster objects which switch on their inTreeAutoScaler.
> 
> How can Kueue tell if RayCluster is using inTreeAutoScaler?
> 
I was referring to these:
- 2nd limitation: https://kueue.sigs.k8s.io/docs/tasks/run/rayclusters/#c-limitations
- The code that enforces the limitation: https://github.com/kubernetes-sigs/kueue/blob/f7437963455e846cc7f681218c9891721f7bf0e8/pkg/controller/jobs/raycluster/raycluster_webhook.go#L105-L107

> > somebody must insert the appropriate kueue.x-k8s.io/queue-name

Perhaps this condition could inform Kueue whether the user expects Kueue to schedule the entire RayCluster as 1 workload ( current approach with inTreeAutoScaler disabled) or whether they prefer that KubeRay manages the workloads (i.e. the inTreeAutoScaler is enabled and KubeRay creates pods). For the latter, Kueue could just raise the "Invalid workload" exception when inTreeAutoScaler is enabled but at least 1 of the worker definitions doesn't have the `kueue.x-k8s.io/queue-name` label. 

This way, users understand how their workers are getting scheduled and potentially simplify this:

> This could be Kueue webhook when Kueue realizes this mode of integration is used. Then it could inject that label into Pod template along with the kueue.x-k8s.io/standalone annotation. The combination of these two in the PodTemplate will make the newly created Pods to create Workloads and be managed by Kueue independently. Just as we do for Deployments.


> So, I believe 2 things are to be done:
> 
>     * figure out a way to tell Kueue should support the RayCluster in this mode for inTreeAutoScaler (can this be done in Kueue webhook)
> 
>     * implement support for the kueue.x-k8s.io/standalone annotation for Pods, and probably for Jobs (as RayCluster also creates the submitter Job), so at this point we can just support across all CRDs.

these make sense to me

### Comment by [@kevin85421](https://github.com/kevin85421) — 2025-03-24T19:06:47Z

cc @andrewsykim @mimowo, is there any plan to support Ray Autoscaling? One Ray user is considering adopting Kueue, but autoscaling is essential for them.

### Comment by [@VassilisVassiliadis](https://github.com/VassilisVassiliadis) — 2025-03-25T10:19:28Z

I can take a stab at my suggestion.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-29T09:44:40Z

@vicentefb we are moving forward with the updated version of the KEP77 (so far only for Jobs).
We want to follow up with RayCluster, but I think this PR needs to be aligned then in terms of feature gate, and most likely the approach. Do you want to work on adapting the PR?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-29T09:44:45Z

cc @ichekrygin

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-28T16:01:33Z

@vicentefb: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-main-1-29 | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-main-1-29/1864018092221272064) | true | `/test pull-kueue-test-e2e-main-1-29`
pull-kueue-test-e2e-main-1-30 | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-main-1-30/1864018092288380928) | true | `/test pull-kueue-test-e2e-main-1-30`
pull-kueue-verify-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-verify-main/1864018092506484736) | true | `/test pull-kueue-verify-main`
pull-kueue-build-image-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-build-image-main/1864018092561010688) | true | `/test pull-kueue-build-image-main`
pull-kueue-test-unit-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-unit-main/1864018092108025856) | true | `/test pull-kueue-test-unit-main`
pull-kueue-test-scheduling-perf-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-scheduling-perf-main/1864018092712005632) | true | `/test pull-kueue-test-scheduling-perf-main`
pull-kueue-test-multikueue-e2e-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-multikueue-e2e-main/1864018092393238528) | true | `/test pull-kueue-test-multikueue-e2e-main`
pull-kueue-test-integration-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-integration-main/1864018092166746112) | true | `/test pull-kueue-test-integration-main`
pull-kueue-test-e2e-main-1-31 | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-main-1-31/1864018092338712576) | true | `/test pull-kueue-test-e2e-main-1-31`
pull-kueue-test-tas-e2e-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-tas-e2e-main/1864018092447764480) | true | `/test pull-kueue-test-tas-e2e-main`
pull-kueue-test-e2e-main-1-32 | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-main-1-32/1878734930482040832) | true | `/test pull-kueue-test-e2e-main-1-32`
pull-kueue-test-multikueue-integration-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-multikueue-integration-main/1883921393498722304) | true | `/test pull-kueue-test-multikueue-integration-main`
pull-kueue-test-customconfigs-e2e-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-customconfigs-e2e-main/1889321536989433856) | true | `/test pull-kueue-test-customconfigs-e2e-main`
pull-kueue-test-e2e-tas-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-tas-main/1890345756175372288) | true | `/test pull-kueue-test-e2e-tas-main`
pull-kueue-test-integration-multikueue-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-integration-multikueue-main/1890345756036960256) | true | `/test pull-kueue-test-integration-multikueue-main`
pull-kueue-test-e2e-customconfigs-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-customconfigs-main/1890345756246675456) | true | `/test pull-kueue-test-e2e-customconfigs-main`
pull-kueue-test-e2e-multikueue-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-multikueue-main/1890345756099874816) | true | `/test pull-kueue-test-e2e-multikueue-main`
pull-kueue-test-e2e-certmanager-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-certmanager-main/1902052433698230272) | true | `/test pull-kueue-test-e2e-certmanager-main`
pull-kueue-test-e2e-kueueviz-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-kueueviz-main/1903038723021344768) | true | `/test pull-kueue-test-e2e-kueueviz-main`
pull-kueue-test-integration-baseline-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-integration-baseline-main/1915413891307802624) | true | `/test pull-kueue-test-integration-baseline-main`
pull-kueue-test-integration-extended-main | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-integration-extended-main/1915425796994895872) | true | `/test pull-kueue-test-integration-extended-main`
pull-kueue-test-e2e-main-1-33 | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-main-1-33/1927360473196400640) | true | `/test pull-kueue-test-e2e-main-1-33`
pull-kueue-test-e2e-main-1-34 | 64edc8668af96192f5784b1c7b5b7c1e65c0f5ba | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/1852/pull-kueue-test-e2e-main-1-34/1961096757043531776) | true | `/test pull-kueue-test-e2e-main-1-34`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=1852). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Avicentefb). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-28T16:14:17Z

In favor of https://github.com/kubernetes-sigs/kueue/pull/5510.
@vicentefb Thank you!
/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-28T16:14:23Z

@tenzen-y: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1852#issuecomment-3234126357):

>In favor of https://github.com/kubernetes-sigs/kueue/pull/5510.
>@vicentefb Thank you!
>/close
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-03-18T14:22:25Z

- **pkg/controller/core/workload_controller.go:595** by [@andrewsykim](https://github.com/andrewsykim) — 2024-03-18T14:19:35Z
```diff
@@ -587,11 +588,31 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 		if err := r.cache.UpdateWorkload(oldWl, wlCopy); err != nil {
 			log.Error(err, "Updating workload in cache")
 		}
+		// This forces you to go through the scheduler to update PodSetAssignments if there's a difference between the
+		// worker group podSetAssignments.Count of the old and new workload
+		updatePsa := compareAdmissionPodSetAssignmentCount(oldWl.Status.Admission, wlCopy.Status.Admission)
+		if features.Enabled(features.DynamicallySizedJobs) && updatePsa && !r.queues.UpdateWorkload(oldWl, wlCopy) {
+			log.V(2).Info("Queue for updated workload didn't exist; ignoring for now")
```

  This check just adds a log message and doesn't change any behavior of the controller. Am I missing something?

- **pkg/controller/core/workload_controller.go:593** by [@andrewsykim](https://github.com/andrewsykim) — 2024-03-18T14:19:52Z
```diff
@@ -587,11 +588,31 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 		if err := r.cache.UpdateWorkload(oldWl, wlCopy); err != nil {
 			log.Error(err, "Updating workload in cache")
 		}
+		// This forces you to go through the scheduler to update PodSetAssignments if there's a difference between the
+		// worker group podSetAssignments.Count of the old and new workload
+		updatePsa := compareAdmissionPodSetAssignmentCount(oldWl.Status.Admission, wlCopy.Status.Admission)
```

  Move this check inside the feature gate check since you don't need to check it otherwise

- **pkg/controller/core/workload_controller.go:606** by [@andrewsykim](https://github.com/andrewsykim) — 2024-03-18T14:20:17Z
```diff
@@ -587,11 +588,31 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 		if err := r.cache.UpdateWorkload(oldWl, wlCopy); err != nil {
 			log.Error(err, "Updating workload in cache")
 		}
+		// This forces you to go through the scheduler to update PodSetAssignments if there's a difference between the
+		// worker group podSetAssignments.Count of the old and new workload
+		updatePsa := compareAdmissionPodSetAssignmentCount(oldWl.Status.Admission, wlCopy.Status.Admission)
+		if features.Enabled(features.DynamicallySizedJobs) && updatePsa && !r.queues.UpdateWorkload(oldWl, wlCopy) {
+			log.V(2).Info("Queue for updated workload didn't exist; ignoring for now")
+		}
 	}
 
 	return true
 }
 
+func compareAdmissionPodSetAssignmentCount(oldWlAdmission *kueue.Admission, newWlAdmisson *kueue.Admission) bool {
+	// this is specific to RayClusters, it contains a PodSet of length 2, containing head and workers
+	oldWlPsa := len(oldWlAdmission.PodSetAssignments)
+	newWlPsa := len(newWlAdmisson.PodSetAssignments)
+	if oldWlPsa == 2 && oldWlPsa == newWlPsa {
```

  Is there a way to make this check generic without checking length 2?

- **pkg/scheduler/scheduler.go:261** by [@andrewsykim](https://github.com/andrewsykim) — 2024-03-18T14:21:01Z
```diff
@@ -258,6 +258,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			s.cache.WaitForPodsReady(ctx)
 			log.V(5).Info("Finished waiting for all admitted workloads to be in the PodsReady condition")
 		}
+		if e.status == assumed {
```

  Should this be feature gated?

- **pkg/webhooks/workload_webhook.go:275** by [@andrewsykim](https://github.com/andrewsykim) — 2024-03-18T14:21:46Z
```diff
@@ -347,29 +347,18 @@ func ValidateWorkloadUpdate(newObj, oldObj *kueue.Workload) field.ErrorList {
 	allErrs = append(allErrs, ValidateWorkload(newObj)...)
 
 	if workload.HasQuotaReservation(oldObj) {
-		allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PodSets, oldObj.Spec.PodSets, specPath.Child("podSets"))...)
```

  Why remove these?

- **pkg/webhooks/workload_webhook.go:275** by [@andrewsykim](https://github.com/andrewsykim) — 2024-03-18T14:22:18Z
```diff
@@ -347,29 +347,18 @@ func ValidateWorkloadUpdate(newObj, oldObj *kueue.Workload) field.ErrorList {
 	allErrs = append(allErrs, ValidateWorkload(newObj)...)
 
 	if workload.HasQuotaReservation(oldObj) {
-		allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PodSets, oldObj.Spec.PodSets, specPath.Child("podSets"))...)
```

  (if you need to change any behavior that is specific to the feature, ensure that it is behind the feature gate, otherwise you will break functionality for other jobs)

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-03-18T17:43:56Z

- **pkg/controller/core/workload_controller.go:595** by [@vicentefb](https://github.com/vicentefb) — 2024-03-18T17:43:56Z
```diff
@@ -587,11 +588,31 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 		if err := r.cache.UpdateWorkload(oldWl, wlCopy); err != nil {
 			log.Error(err, "Updating workload in cache")
 		}
+		// This forces you to go through the scheduler to update PodSetAssignments if there's a difference between the
+		// worker group podSetAssignments.Count of the old and new workload
+		updatePsa := compareAdmissionPodSetAssignmentCount(oldWl.Status.Admission, wlCopy.Status.Admission)
+		if features.Enabled(features.DynamicallySizedJobs) && updatePsa && !r.queues.UpdateWorkload(oldWl, wlCopy) {
+			log.V(2).Info("Queue for updated workload didn't exist; ignoring for now")
```

  It's checking if the feature is enabled and if there's a diff in podset count, if so it calls `!r.queues.UpdateWorkload(oldWl, wlCopy)`, I can change the log message

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-03-18T18:15:46Z

- **pkg/controller/core/workload_controller.go:595** by [@andrewsykim](https://github.com/andrewsykim) — 2024-03-18T18:15:42Z
```diff
@@ -587,11 +588,31 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 		if err := r.cache.UpdateWorkload(oldWl, wlCopy); err != nil {
 			log.Error(err, "Updating workload in cache")
 		}
+		// This forces you to go through the scheduler to update PodSetAssignments if there's a difference between the
+		// worker group podSetAssignments.Count of the old and new workload
+		updatePsa := compareAdmissionPodSetAssignmentCount(oldWl.Status.Admission, wlCopy.Status.Admission)
+		if features.Enabled(features.DynamicallySizedJobs) && updatePsa && !r.queues.UpdateWorkload(oldWl, wlCopy) {
+			log.V(2).Info("Queue for updated workload didn't exist; ignoring for now")
```

  Oh I see, I think the readability can improve like this:
  ```
  if features.Enabled(features.DynamicallySizedJobs) && compareAdmissionPodSetAssignmentCount(oldWl.Status.Admission, wlCopy.Status.Admission) {
    if !r.queues.UpdateWorkload(oldWl, wlCopy)  {
      log.V(2).Info("Queue for updated workload didn't exist; ignoring for now")
    }
  }
  ```

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-03-18T18:59:04Z

- **pkg/scheduler/scheduler.go:261** by [@vicentefb](https://github.com/vicentefb) — 2024-03-18T18:59:04Z
```diff
@@ -258,6 +258,9 @@ func (s *Scheduler) schedule(ctx context.Context) {
 			s.cache.WaitForPodsReady(ctx)
 			log.V(5).Info("Finished waiting for all admitted workloads to be in the PodsReady condition")
 		}
+		if e.status == assumed {
```

  Done

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-03-18T18:59:11Z

- **pkg/controller/core/workload_controller.go:606** by [@vicentefb](https://github.com/vicentefb) — 2024-03-18T18:59:11Z
```diff
@@ -587,11 +588,31 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 		if err := r.cache.UpdateWorkload(oldWl, wlCopy); err != nil {
 			log.Error(err, "Updating workload in cache")
 		}
+		// This forces you to go through the scheduler to update PodSetAssignments if there's a difference between the
+		// worker group podSetAssignments.Count of the old and new workload
+		updatePsa := compareAdmissionPodSetAssignmentCount(oldWl.Status.Admission, wlCopy.Status.Admission)
+		if features.Enabled(features.DynamicallySizedJobs) && updatePsa && !r.queues.UpdateWorkload(oldWl, wlCopy) {
+			log.V(2).Info("Queue for updated workload didn't exist; ignoring for now")
+		}
 	}
 
 	return true
 }
 
+func compareAdmissionPodSetAssignmentCount(oldWlAdmission *kueue.Admission, newWlAdmisson *kueue.Admission) bool {
+	// this is specific to RayClusters, it contains a PodSet of length 2, containing head and workers
+	oldWlPsa := len(oldWlAdmission.PodSetAssignments)
+	newWlPsa := len(newWlAdmisson.PodSetAssignments)
+	if oldWlPsa == 2 && oldWlPsa == newWlPsa {
```

  Added a for loop

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-03-18T18:59:32Z

- **pkg/webhooks/workload_webhook.go:275** by [@vicentefb](https://github.com/vicentefb) — 2024-03-18T18:59:32Z
```diff
@@ -347,29 +347,18 @@ func ValidateWorkloadUpdate(newObj, oldObj *kueue.Workload) field.ErrorList {
 	allErrs = append(allErrs, ValidateWorkload(newObj)...)
 
 	if workload.HasQuotaReservation(oldObj) {
-		allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PodSets, oldObj.Spec.PodSets, specPath.Child("podSets"))...)
```

  Sg added feature gate check

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-09T18:26:23Z

- **pkg/controller/core/workload_controller.go:196** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T18:17:17Z
```diff
@@ -192,6 +193,33 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, err
 		}
 
+		if features.Enabled(features.DynamicallySizedJobs) && ptr.Deref(wl.Status.Admission.PodSetAssignments[1].Count, 0) != wl.Spec.PodSets[1].Count {
```

  Please put all of this inside a method.
  
  Maybe `downSizeJobIfNecessary`

- **pkg/controller/core/workload_controller.go:196** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T18:17:45Z
```diff
@@ -192,6 +193,33 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, err
 		}
 
+		if features.Enabled(features.DynamicallySizedJobs) && ptr.Deref(wl.Status.Admission.PodSetAssignments[1].Count, 0) != wl.Spec.PodSets[1].Count {
```

  why only check one podset? AFAIK, RayCluster can have multiple podsets.

- **pkg/controller/core/workload_controller.go:201** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T18:18:48Z
```diff
@@ -192,6 +193,33 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, err
 		}
 
+		if features.Enabled(features.DynamicallySizedJobs) && ptr.Deref(wl.Status.Admission.PodSetAssignments[1].Count, 0) != wl.Spec.PodSets[1].Count {
+			// Get Memory and CPU values
+			originalMemoryPerPod := wl.Spec.PodSets[1].Template.Spec.Containers[0].Resources.Requests.Memory()
+			originalCpuPerPod := wl.Spec.PodSets[1].Template.Spec.Containers[0].Resources.Requests.Cpu()
+			currentAssignedMemoryPerPod := wl.Status.Admission.PodSetAssignments[1].ResourceUsage.Memory()
+			currentAssignedCpuPerPod := wl.Status.Admission.PodSetAssignments[1].ResourceUsage.Cpu()
```

  Why only CPU/memory? This should be generic for all the resources that the job requires.

- **pkg/controller/jobframework/reconciler.go:349** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T18:22:56Z
```diff
@@ -346,6 +346,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		}
 	}
 
+	// 4.1 update podSetCount for RayCluster resize
```

  This is generic code, so it shouldn't have any specifics to RayCluster.
  
  Maybe you can add an interface for SizableJobs with a method `IsDownsizable` that returns true/false.

- **pkg/controller/jobframework/reconciler.go:352** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T18:24:56Z
```diff
@@ -346,6 +346,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		}
 	}
 
+	// 4.1 update podSetCount for RayCluster resize
+	if features.Enabled(features.DynamicallySizedJobs) && workload.IsAdmitted(wl) && job.GVK().Kind == "RayCluster" {
+		podSets := job.PodSets()
+		jobPodSetCount := podSets[1].Count
```

  It should not be specific to one podset.

- **pkg/controller/jobframework/reconciler.go:696** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T18:26:19Z
```diff
@@ -677,7 +692,8 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		jobPodSetCount := job.PodSets()
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) || (features.Enabled(features.DynamicallySizedJobs) && job.GVK().Kind == "RayCluster" && jobPodSetCount[1].Count < wl.Spec.PodSets[1].Count) {
```

  No specific code please.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-04-10T00:05:10Z

- **pkg/controller/jobframework/reconciler.go:696** by [@vicentefb](https://github.com/vicentefb) — 2024-04-10T00:05:10Z
```diff
@@ -677,7 +692,8 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		jobPodSetCount := job.PodSets()
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) || (features.Enabled(features.DynamicallySizedJobs) && job.GVK().Kind == "RayCluster" && jobPodSetCount[1].Count < wl.Spec.PodSets[1].Count) {
```

  Sg i have used optional interface.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-04-10T00:05:55Z

- **pkg/controller/jobframework/reconciler.go:352** by [@vicentefb](https://github.com/vicentefb) — 2024-04-10T00:05:55Z
```diff
@@ -346,6 +346,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		}
 	}
 
+	// 4.1 update podSetCount for RayCluster resize
+	if features.Enabled(features.DynamicallySizedJobs) && workload.IsAdmitted(wl) && job.GVK().Kind == "RayCluster" {
+		podSets := job.PodSets()
+		jobPodSetCount := podSets[1].Count
```

  Sg, I have used the optional interface to iterate through the podsets.

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-04-10T00:06:01Z

- **pkg/controller/jobframework/reconciler.go:349** by [@vicentefb](https://github.com/vicentefb) — 2024-04-10T00:06:01Z
```diff
@@ -346,6 +346,21 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		}
 	}
 
+	// 4.1 update podSetCount for RayCluster resize
```

  Added

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-04-10T00:07:14Z

- **pkg/controller/core/workload_controller.go:201** by [@vicentefb](https://github.com/vicentefb) — 2024-04-10T00:07:14Z
```diff
@@ -192,6 +193,33 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, err
 		}
 
+		if features.Enabled(features.DynamicallySizedJobs) && ptr.Deref(wl.Status.Admission.PodSetAssignments[1].Count, 0) != wl.Spec.PodSets[1].Count {
+			// Get Memory and CPU values
+			originalMemoryPerPod := wl.Spec.PodSets[1].Template.Spec.Containers[0].Resources.Requests.Memory()
+			originalCpuPerPod := wl.Spec.PodSets[1].Template.Spec.Containers[0].Resources.Requests.Cpu()
+			currentAssignedMemoryPerPod := wl.Status.Admission.PodSetAssignments[1].ResourceUsage.Memory()
+			currentAssignedCpuPerPod := wl.Status.Admission.PodSetAssignments[1].ResourceUsage.Cpu()
```

  Changed implementation to iterate through Requests and Resource Usage lists

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-04-10T00:07:33Z

- **pkg/controller/core/workload_controller.go:196** by [@vicentefb](https://github.com/vicentefb) — 2024-04-10T00:07:33Z
```diff
@@ -192,6 +193,33 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, err
 		}
 
+		if features.Enabled(features.DynamicallySizedJobs) && ptr.Deref(wl.Status.Admission.PodSetAssignments[1].Count, 0) != wl.Spec.PodSets[1].Count {
```

  Checking for multiple podsets now

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-11T19:43:27Z

- **pkg/controller/core/workload_controller.go:284** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:25:10Z
```diff
@@ -191,6 +192,13 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if updated, err := r.reconcileOnClusterQueueActiveState(ctx, &wl, cqName); updated || err != nil {
 			return ctrl.Result{}, err
 		}
+		if features.Enabled(features.DynamicallySizedJobs) && downSizeJobIfNecessary(&wl) {
+			// Update Status
+			workload.SyncAdmittedCondition(&wl)
+			if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true); err != nil {
+				return ctrl.Result{}, err
+			}
```

  I would also put this inside downSizeJobIfNecessary

- **pkg/controller/core/workload_controller.go:258** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:30:40Z
```diff
@@ -241,6 +249,35 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	return ctrl.Result{}, nil
 }
 
+func downSizeJobIfNecessary(wl *kueue.Workload) bool {
+	statusUpdate := false
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
+			// Get Resource Requests and Usage values
+			originalResourceRequests := wl.Spec.PodSets[i].Template.Spec.Containers[0].Resources.Requests
```

  What about requests from other containers?
  
  I think you can use this function https://github.com/kubernetes-sigs/kueue/blob/3347afb5a8681f4fd2a5f3b1a5be8c5c0ebac488/pkg/util/limitrange/limitrange.go#L85

- **pkg/controller/core/workload_controller.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:31:59Z
  Please add unit tests

- **pkg/controller/jobs/raycluster/raycluster_controller.go:92** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:33:52Z
```diff
@@ -87,6 +88,20 @@ func (j *RayCluster) GVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *RayCluster) IsDownsizable(wl *kueue.Workload) bool {
+	return j.GVK().Kind == "RayCluster" && features.Enabled(features.DynamicallySizedJobs) && JobHasDownsize(j.PodSets(), wl)
```

  ```suggestion
  	return features.Enabled(features.DynamicallySizedJobs) && JobHasDownsize(j.PodSets(), wl)
  ```

- **pkg/controller/jobs/raycluster/raycluster_controller.go:96** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:34:24Z
```diff
@@ -87,6 +88,20 @@ func (j *RayCluster) GVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *RayCluster) IsDownsizable(wl *kueue.Workload) bool {
+	return j.GVK().Kind == "RayCluster" && features.Enabled(features.DynamicallySizedJobs) && JobHasDownsize(j.PodSets(), wl)
+}
+
+// JobHasDownsize checks whether a job has a scale down
+func JobHasDownsize(podSets []kueue.PodSet, wl *kueue.Workload) bool {
```

  ```suggestion
  func jobHasDownsize(podSets []kueue.PodSet, wl *kueue.Workload) bool {
  ```
  
  Or is there a reason to export it?
  
  Also, why not just implement inside `IsDownsizable`?

- **pkg/controller/jobs/raycluster/raycluster_controller.go:91** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:34:45Z
```diff
@@ -87,6 +88,20 @@ func (j *RayCluster) GVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *RayCluster) IsDownsizable(wl *kueue.Workload) bool {
```

  ```suggestion
  func (j *RayCluster) IsDownsized(wl *kueue.Workload) bool {
  ```
  ?

- **pkg/features/kube_features.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:35:08Z
```diff
@@ -83,6 +83,10 @@ const (
 	//
 	// Enables lending limit.
 	LendingLimit featuregate.Feature = "LendingLimit"
+	// owner: @vicenteferrara
+	// kep: <TODO>
```

  please add the kep link

- **pkg/util/testingjobs/raycluster/wrappers.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:38:00Z
```diff
@@ -80,6 +80,12 @@ func (j *ClusterWrapper) NodeSelectorHeadGroup(k, v string) *ClusterWrapper {
 	return j
 }
 
+// Set replica count
+func (j *ClusterWrapper) SetReplicaCount(c int32) *ClusterWrapper {
+	j.Spec.WorkerGroupSpecs[0].Replicas = ptr.To(c)
```

  ```suggestion
  	j.Spec.WorkerGroupSpecs[1].Replicas = ptr.To(c)
  ```
  
  ?

- **pkg/util/testingjobs/raycluster/wrappers.go:84** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:39:17Z
```diff
@@ -80,6 +80,12 @@ func (j *ClusterWrapper) NodeSelectorHeadGroup(k, v string) *ClusterWrapper {
 	return j
 }
 
+// Set replica count
+func (j *ClusterWrapper) SetReplicaCount(c int32) *ClusterWrapper {
```

  ```suggestion
  func (j *ClusterWrapper) WorkerCount(c int32) *ClusterWrapper {
  ```
  
  Avoid Set/Get in golang https://go.dev/doc/effective_go#getters

- **test/integration/controller/jobs/raycluster/raycluster_controller_test.go:638** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:43:22Z
```diff
@@ -632,6 +633,69 @@ var _ = ginkgo.Describe("RayCluster Job controller interacting with scheduler",
 		util.ExpectPendingWorkloadsMetric(clusterQueue, 0, 0)
 		util.ExpectReservingActiveWorkloadsMetric(clusterQueue, 1)
 	})
+
+	gomega.Eventually(func() bool {
+		if err := features.SetEnable(features.DynamicallySizedJobs, true); err != nil {
+			return false
+		}
+		return features.Enabled(features.DynamicallySizedJobs)
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.It("Should not suspend job when there's a scale down", func() {
```

  The test should also make sure that another RayCluster that fits within the freed quota is admitted.

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-04-12T01:05:06Z

- **pkg/controller/jobframework/interface.go:65** by [@andrewsykim](https://github.com/andrewsykim) — 2024-04-12T00:46:52Z
```diff
@@ -62,6 +62,11 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
+type SizableJobs interface {
```

  ResizableJobs?

- **pkg/controller/jobframework/interface.go:67** by [@andrewsykim](https://github.com/andrewsykim) — 2024-04-12T00:47:38Z
```diff
@@ -62,6 +62,11 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
+type SizableJobs interface {
+	// IsDownsizable returns the true/false depending if the job is being downsized.
+	IsDownsizable(wl *kueue.Workload) bool
```

  I would instead rename this function `IsResizable` but leave a comment saying it only supports downsizing for now

- **pkg/controller/jobframework/reconciler.go:350** by [@andrewsykim](https://github.com/andrewsykim) — 2024-04-12T01:01:51Z
```diff
@@ -346,6 +346,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		}
 	}
 
+	// 4.1 update podSetCount for RayCluster resize (downsize)
+	if jobSizeable, implementsSizable := job.(SizableJobs); implementsSizable && jobSizeable.IsDownsizable(wl) && workload.IsAdmitted(wl) {
```

  Should this logic also be feature gated?

- **pkg/features/kube_features.go:89** by [@andrewsykim](https://github.com/andrewsykim) — 2024-04-12T01:03:12Z
```diff
@@ -83,6 +83,10 @@ const (
 	//
 	// Enables lending limit.
 	LendingLimit featuregate.Feature = "LendingLimit"
+	// owner: @vicenteferrara
+	// kep: <TODO>
+	// alpha: v0.8
+	DynamicallySizedJobs featuregate.Feature = "DynamicallySizedJobs"
```

  I like `ResizableJobs` or `ElasticJobs` better

- **pkg/features/kube_features.go:89** by [@andrewsykim](https://github.com/andrewsykim) — 2024-04-12T01:03:27Z
```diff
@@ -83,6 +83,10 @@ const (
 	//
 	// Enables lending limit.
 	LendingLimit featuregate.Feature = "LendingLimit"
+	// owner: @vicenteferrara
+	// kep: <TODO>
+	// alpha: v0.8
+	DynamicallySizedJobs featuregate.Feature = "DynamicallySizedJobs"
```

  Actually ElasticJobs might be confused with ElasticcJobs feature in k/k, so maybe ResizableJobs is better

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-04-30T15:33:30Z

- **pkg/controller/core/workload_controller.go:295** by [@andrewsykim](https://github.com/andrewsykim) — 2024-04-30T15:32:23Z
```diff
@@ -248,6 +256,52 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	return ctrl.Result{}, nil
 }
 
+func (r *WorkloadReconciler) isScaledDown(wl *kueue.Workload) bool {
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
+			return true
+		}
+	}
+	return false
+}
+
+func (r *WorkloadReconciler) downSizeJobIfNecessary(wl *kueue.Workload, ctx context.Context) error {
+	statusUpdate := false
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
+			// Get Resource Requests and Usage values
+			originalResourceRequests := limitrange.TotalRequests(&wl.Spec.PodSets[i].Template.Spec)
+			currentAssignedResourceUsage := wl.Status.Admission.PodSetAssignments[i].ResourceUsage
+
+			diff := ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) - wl.Spec.PodSets[i].Count
+			for k := range currentAssignedResourceUsage {
+				resourceQuantity := originalResourceRequests[k]
+				resourceQuantity.Mul(int64(diff))
+				originalResourceRequests[k] = resourceQuantity
+
+				assignedResourceQuantity := currentAssignedResourceUsage[k]
+				assignedResourceQuantity.Sub(originalResourceRequests[k])
+				currentAssignedResourceUsage[k] = assignedResourceQuantity
+			}
+
+			wl.Status.Admission.PodSetAssignments[i].Count = ptr.To(wl.Spec.PodSets[i].Count)
+			wl.Status.Admission.PodSetAssignments[i].ResourceUsage = currentAssignedResourceUsage
+
+			statusUpdate = true
+		}
+	}
+	if statusUpdate {
```

  nit: updateStatus

- **pkg/controller/core/workload_controller.go:273** by [@andrewsykim](https://github.com/andrewsykim) — 2024-04-30T15:33:19Z
```diff
@@ -248,6 +256,52 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	return ctrl.Result{}, nil
 }
 
+func (r *WorkloadReconciler) isScaledDown(wl *kueue.Workload) bool {
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
+			return true
+		}
+	}
+	return false
+}
+
+func (r *WorkloadReconciler) downSizeJobIfNecessary(wl *kueue.Workload, ctx context.Context) error {
+	statusUpdate := false
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
```

  reads slight better as:
  ```
  if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) <= wl.Spec.PodSets[i].Count {
      continue
  }
  // Get Resource Requests and Usage values
  originalResourceRequests := limitrange.TotalRequests(&wl.Spec.PodSets[i].Template.Spec)
  currentAssignedResourceUsage := wl.Status.Admission.PodSetAssignments[i].ResourceUsage
  
  diff := ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) - wl.Spec.PodSets[i].Count
  for k := range currentAssignedResourceUsage {
    resourceQuantity := originalResourceRequests[k]
    resourceQuantity.Mul(int64(diff))
    originalResourceRequests[k] = resourceQuantity
    
    assignedResourceQuantity := currentAssignedResourceUsage[k]
    assignedResourceQuantity.Sub(originalResourceRequests[k])
    currentAssignedResourceUsage[k] = assignedResourceQuantity
  }
  
  wl.Status.Admission.PodSetAssignments[i].Count = ptr.To(wl.Spec.PodSets[i].Count)
  wl.Status.Admission.PodSetAssignments[i].ResourceUsage = currentAssignedResourceUsage
  
  ```

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-04-30T15:37:38Z

- **pkg/controller/jobs/raycluster/raycluster_controller.go:92** by [@andrewsykim](https://github.com/andrewsykim) — 2024-04-30T15:35:11Z
```diff
@@ -87,6 +88,18 @@ func (j *RayCluster) GVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *RayCluster) IsResizable(wl *kueue.Workload) bool {
+	if features.Enabled(features.ResizableJobs) {
```

  ```go
  if !features.Enabled(features.ResizableJobs) {
     return false
  }
  
  pods := j.PodSets()
  for i := 1; i < len(pods); i++ {
  	if wl.Spec.PodSets[i].Count > pods[i].Count {
  		return true
  	}
  }
  
  return false
  ```

- **pkg/controller/core/workload_controller.go:702** by [@andrewsykim](https://github.com/andrewsykim) — 2024-04-30T15:37:20Z
```diff
@@ -582,7 +637,7 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 				}
 			})
 		}
-	case prevStatus == admitted && status == admitted && !equality.Semantic.DeepEqual(oldWl.Status.ReclaimablePods, wl.Status.ReclaimablePods):
+	case prevStatus == admitted && status == admitted && !equality.Semantic.DeepEqual(oldWl.Status.ReclaimablePods, wl.Status.ReclaimablePods) || r.isScaledDown(oldWl):
```

  This check isn't gated by the feature gate, is it okay to do?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-10T19:14:21Z

- **pkg/controller/core/workload_controller.go:317** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-10T19:04:39Z
```diff
@@ -293,6 +301,57 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 	return cond != nil && cond.Status == metav1.ConditionFalse && cond.Reason == kueue.WorkloadEvictedByClusterQueueStopped
 }
 
+func (r *WorkloadReconciler) isScaledDown(wl *kueue.Workload) bool {
+	if !features.Enabled(features.ResizableJobs) {
+		return false
+	}
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
+			return true
+		}
+	}
+	return false
+}
+
+func (r *WorkloadReconciler) downSizeJobIfNecessary(wl *kueue.Workload, ctx context.Context) error {
```

  ```suggestion
  func (r *WorkloadReconciler) downSizePodSetAssigmentsIfNecessary(wl *kueue.Workload, ctx context.Context) error {
  ```

- **pkg/controller/jobframework/interface.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-10T19:06:26Z
```diff
@@ -62,6 +62,12 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
+type ReizableJobs interface {
```

  ```suggestion
  type ResizableJobs interface {
  ```

- **pkg/controller/jobframework/reconciler.go:760** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-10T19:09:28Z
```diff
@@ -698,7 +709,8 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		jobSizeable, implementsSizable := job.(ReizableJobs)
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) || (implementsSizable && jobSizeable.IsResizable(wl)) {
```

  This check is not enough.
  Since we don't support upsizing, we should return false if upsizing happens here.
  
  But overall, we should check that the shapes are the same (only counters changed).

- **pkg/controller/jobframework/interface.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-10T19:12:41Z
```diff
@@ -62,6 +62,12 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
+type ReizableJobs interface {
+	// IsResizable returns the true/false depending if the job is being downsized.
+	// It only supports downsizing for now, see KEP:77
+	IsResizable(wl *kueue.Workload) bool
```

  ```suggestion
  	// IsResizable returns the true/false supports resizing.
  	// It only supports downsizing for now, see KEP:77
  	IsResizable(wl *kueue.Workload) bool
  ```

- **pkg/controller/jobframework/reconciler.go:760** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-10T19:14:04Z
```diff
@@ -698,7 +709,8 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		jobSizeable, implementsSizable := job.(ReizableJobs)
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) || (implementsSizable && jobSizeable.IsResizable(wl)) {
```

  It looks like you are partially checking that inside `IsResizable`, but we shouldn't be doing that in the integration.
  
  It can be generic... just by looking at the podsets.
  
  So, in this package, there could be a function `isDownsized(oldPodsets, newPodSets []PodSet) bool`

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2024-05-13T11:54:42Z

Leave few comments.

- **pkg/controller/jobframework/interface.go:65** by [@kerthcet](https://github.com/kerthcet) — 2024-05-13T11:35:03Z
```diff
@@ -62,6 +62,12 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
+type ReizableJobs interface {
```

  I think singular is enough here.

- **pkg/util/testing/wrappers.go:90** by [@kerthcet](https://github.com/kerthcet) — 2024-05-13T11:38:11Z
```diff
@@ -74,6 +74,29 @@ func MakeWorkload(name, ns string) *WorkloadWrapper {
 	}}
 }
 
+// MakeWorkload creates a wrapper for a Workload with a single
+// pod with a single container.
+func MakeWorkloadWithMorePodSets(name, ns string) *WorkloadWrapper {
+	return &WorkloadWrapper{kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{Name: name, Namespace: ns},
+		Spec: kueue.WorkloadSpec{
+			PodSets: []kueue.PodSet{
+				*MakePodSet("main", 3).Obj(),
+			},
+		},
+	}}
+}
+
+func (w *WorkloadWrapper) ScaleDown() *WorkloadWrapper {
```

  Why not combine to `Scale(count int32)`?

- **pkg/util/testing/wrappers.go:79** by [@kerthcet](https://github.com/kerthcet) — 2024-05-13T11:39:18Z
```diff
@@ -74,6 +74,29 @@ func MakeWorkload(name, ns string) *WorkloadWrapper {
 	}}
 }
 
+// MakeWorkload creates a wrapper for a Workload with a single
+// pod with a single container.
+func MakeWorkloadWithMorePodSets(name, ns string) *WorkloadWrapper {
```

  A little confusion about the function name, what do you mean More here, or specify the count directly?

- **pkg/controller/core/workload_controller.go:702** by [@kerthcet](https://github.com/kerthcet) — 2024-05-13T11:44:08Z
```diff
@@ -620,7 +680,7 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 				}
 			})
 		}
-	case prevStatus == admitted && status == admitted && !equality.Semantic.DeepEqual(oldWl.Status.ReclaimablePods, wl.Status.ReclaimablePods):
+	case prevStatus == admitted && status == admitted && !equality.Semantic.DeepEqual(oldWl.Status.ReclaimablePods, wl.Status.ReclaimablePods) || r.isScaledDown(oldWl):
```

  We'll verify the oldWl or newWl?

- **pkg/controller/core/workload_controller.go:309** by [@kerthcet](https://github.com/kerthcet) — 2024-05-13T11:47:05Z
```diff
@@ -293,6 +301,57 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 	return cond != nil && cond.Status == metav1.ConditionFalse && cond.Reason == kueue.WorkloadEvictedByClusterQueueStopped
 }
 
+func (r *WorkloadReconciler) isScaledDown(wl *kueue.Workload) bool {
+	if !features.Enabled(features.ResizableJobs) {
+		return false
+	}
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
```

  Can you help me to understand why index 0 not possible here?

- **pkg/controller/core/workload_controller.go:332** by [@kerthcet](https://github.com/kerthcet) — 2024-05-13T11:53:55Z
```diff
@@ -293,6 +301,57 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 	return cond != nil && cond.Status == metav1.ConditionFalse && cond.Reason == kueue.WorkloadEvictedByClusterQueueStopped
 }
 
+func (r *WorkloadReconciler) isScaledDown(wl *kueue.Workload) bool {
+	if !features.Enabled(features.ResizableJobs) {
+		return false
+	}
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
+			return true
+		}
+	}
+	return false
+}
+
+func (r *WorkloadReconciler) downSizeJobIfNecessary(wl *kueue.Workload, ctx context.Context) error {
+	updateStatus := false
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) <= wl.Spec.PodSets[i].Count {
+			continue
+		}
+		// Get Resource Requests and Usage values
+		originalResourceRequests := limitrange.TotalRequests(&wl.Spec.PodSets[i].Template.Spec)
+		currentAssignedResourceUsage := wl.Status.Admission.PodSetAssignments[i].ResourceUsage
+
+		diff := ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) - wl.Spec.PodSets[i].Count
+		for k := range currentAssignedResourceUsage {
+			resourceQuantity := originalResourceRequests[k]
+			resourceQuantity.Mul(int64(diff))
+			originalResourceRequests[k] = resourceQuantity
```

  I suppose not change the original resources, although will not impact the result but may lead to confusion?

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2024-05-30T14:13:44Z

- **pkg/util/equality/podset.go:80** by [@andrewsykim](https://github.com/andrewsykim) — 2024-05-30T14:11:52Z
```diff
@@ -58,3 +58,35 @@ func ComparePodSetSlices(a, b []kueue.PodSet, ignoreTolerations bool) bool {
 	}
 	return true
 }
+
+func isDownsized(oldPodsets, newPodSets []kueue.PodSet) bool {
+	for i := 0; i < len(oldPodsets); i++ {
+		if oldPodsets[i].Count > newPodSets[i].Count {
+			return true
+		}
+	}
+	return false
+}
+
+// First check is to see if there was an upsize which we don't support yet
+// Second check is to see if there was a downsize
+func IsResized(oldPodsets, newPodSets []kueue.PodSet) bool {
+	return !isDownsized(newPodSets, oldPodsets) && isDownsized(oldPodsets, newPodSets)
+}
+
+// This check is intended for workload PodSets and PodSetAssignments
+// First check is to see if there was an upsize which we don't support yet
+// Second check is to see if there was a downsize
+func IsResizedPsa(oldPodsetsAssignments []kueue.PodSetAssignment, newPodSets []kueue.PodSet) bool {
```

  IsResizedPodSetAssignment

- **pkg/util/equality/podset.go:83** by [@andrewsykim](https://github.com/andrewsykim) — 2024-05-30T14:12:28Z
```diff
@@ -58,3 +58,35 @@ func ComparePodSetSlices(a, b []kueue.PodSet, ignoreTolerations bool) bool {
 	}
 	return true
 }
+
+func isDownsized(oldPodsets, newPodSets []kueue.PodSet) bool {
+	for i := 0; i < len(oldPodsets); i++ {
+		if oldPodsets[i].Count > newPodSets[i].Count {
+			return true
+		}
+	}
+	return false
+}
+
+// First check is to see if there was an upsize which we don't support yet
+// Second check is to see if there was a downsize
+func IsResized(oldPodsets, newPodSets []kueue.PodSet) bool {
+	return !isDownsized(newPodSets, oldPodsets) && isDownsized(oldPodsets, newPodSets)
+}
+
+// This check is intended for workload PodSets and PodSetAssignments
+// First check is to see if there was an upsize which we don't support yet
+// Second check is to see if there was a downsize
+func IsResizedPsa(oldPodsetsAssignments []kueue.PodSetAssignment, newPodSets []kueue.PodSet) bool {
+	for i := 0; i < len(oldPodsetsAssignments); i++ {
+		if ptr.Deref(oldPodsetsAssignments[i].Count, 0) < newPodSets[i].Count {
+			return false
```

  Add a comment here and a TODO that we are ignoring scale up for now

- **pkg/util/equality/podset.go:79** by [@andrewsykim](https://github.com/andrewsykim) — 2024-05-30T14:13:10Z
```diff
@@ -58,3 +58,35 @@ func ComparePodSetSlices(a, b []kueue.PodSet, ignoreTolerations bool) bool {
 	}
 	return true
 }
+
+func isDownsized(oldPodsets, newPodSets []kueue.PodSet) bool {
+	for i := 0; i < len(oldPodsets); i++ {
+		if oldPodsets[i].Count > newPodSets[i].Count {
+			return true
+		}
+	}
+	return false
+}
+
+// First check is to see if there was an upsize which we don't support yet
+// Second check is to see if there was a downsize
+func IsResized(oldPodsets, newPodSets []kueue.PodSet) bool {
+	return !isDownsized(newPodSets, oldPodsets) && isDownsized(oldPodsets, newPodSets)
+}
+
+// This check is intended for workload PodSets and PodSetAssignments
+// First check is to see if there was an upsize which we don't support yet
+// Second check is to see if there was a downsize
+func IsResizedPsa(oldPodsetsAssignments []kueue.PodSetAssignment, newPodSets []kueue.PodSet) bool {
+	for i := 0; i < len(oldPodsetsAssignments); i++ {
+		if ptr.Deref(oldPodsetsAssignments[i].Count, 0) < newPodSets[i].Count {
+			return false
+		}
+	}
+	for i := 0; i < len(oldPodsetsAssignments); i++ {
```

  This whole function can be simpler:
  ```
  for i := 0; i < len(oldPodsetsAssignments); i++ {
      if ptr.Deref(oldPodsetsAssignments[i].Count, 0) > newPodSets[i].Count {
          return true
      }
  }
  
  return false
  ```

### Review by [@vicentefb](https://github.com/vicentefb) (commented) — 2024-06-26T18:16:18Z

- **pkg/controller/jobs/raycluster/raycluster_controller.go:96** by [@vicentefb](https://github.com/vicentefb) — 2024-04-11T23:37:42Z
```diff
@@ -87,6 +88,20 @@ func (j *RayCluster) GVK() schema.GroupVersionKind {
 	return gvk
 }
 
+func (j *RayCluster) IsDownsizable(wl *kueue.Workload) bool {
+	return j.GVK().Kind == "RayCluster" && features.Enabled(features.DynamicallySizedJobs) && JobHasDownsize(j.PodSets(), wl)
+}
+
+// JobHasDownsize checks whether a job has a scale down
+func JobHasDownsize(podSets []kueue.PodSet, wl *kueue.Workload) bool {
```

  No reason to export it, it's actually a helper function, yeah i can include it inside `IsDownsized`

- **pkg/util/testingjobs/raycluster/wrappers.go:85** by [@vicentefb](https://github.com/vicentefb) — 2024-04-11T23:44:28Z
```diff
@@ -80,6 +80,12 @@ func (j *ClusterWrapper) NodeSelectorHeadGroup(k, v string) *ClusterWrapper {
 	return j
 }
 
+// Set replica count
+func (j *ClusterWrapper) SetReplicaCount(c int32) *ClusterWrapper {
+	j.Spec.WorkerGroupSpecs[0].Replicas = ptr.To(c)
```

  Ohh this is already in the `WorkerGroupSpec` so it's the first one since we also have the `HeadGroupSpec`

- **pkg/controller/jobframework/reconciler.go:350** by [@vicentefb](https://github.com/vicentefb) — 2024-04-12T23:10:24Z
```diff
@@ -346,6 +346,17 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		}
 	}
 
+	// 4.1 update podSetCount for RayCluster resize (downsize)
+	if jobSizeable, implementsSizable := job.(SizableJobs); implementsSizable && jobSizeable.IsDownsizable(wl) && workload.IsAdmitted(wl) {
```

  The implementation of the interface method `IsResizable` which is inside the `raycluster_controller.go` has the feature gate check as a first step.

- **pkg/features/kube_features.go:89** by [@vicentefb](https://github.com/vicentefb) — 2024-04-25T05:07:33Z
```diff
@@ -83,6 +83,10 @@ const (
 	//
 	// Enables lending limit.
 	LendingLimit featuregate.Feature = "LendingLimit"
+	// owner: @vicenteferrara
+	// kep: <TODO>
+	// alpha: v0.8
+	DynamicallySizedJobs featuregate.Feature = "DynamicallySizedJobs"
```

  Okok done

- **pkg/controller/core/workload_controller.go:273** by [@vicentefb](https://github.com/vicentefb) — 2024-04-30T18:11:25Z
```diff
@@ -248,6 +256,52 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	return ctrl.Result{}, nil
 }
 
+func (r *WorkloadReconciler) isScaledDown(wl *kueue.Workload) bool {
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
+			return true
+		}
+	}
+	return false
+}
+
+func (r *WorkloadReconciler) downSizeJobIfNecessary(wl *kueue.Workload, ctx context.Context) error {
+	statusUpdate := false
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
```

  Done

- **pkg/controller/core/workload_controller.go:702** by [@vicentefb](https://github.com/vicentefb) — 2024-04-30T18:18:02Z
```diff
@@ -582,7 +637,7 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 				}
 			})
 		}
-	case prevStatus == admitted && status == admitted && !equality.Semantic.DeepEqual(oldWl.Status.ReclaimablePods, wl.Status.ReclaimablePods):
+	case prevStatus == admitted && status == admitted && !equality.Semantic.DeepEqual(oldWl.Status.ReclaimablePods, wl.Status.ReclaimablePods) || r.isScaledDown(oldWl):
```

  True! I added the feature gate check inside the method `isScaledDown`, thanks!

- **pkg/util/testing/wrappers.go:90** by [@vicentefb](https://github.com/vicentefb) — 2024-05-13T23:04:55Z
```diff
@@ -74,6 +74,29 @@ func MakeWorkload(name, ns string) *WorkloadWrapper {
 	}}
 }
 
+// MakeWorkload creates a wrapper for a Workload with a single
+// pod with a single container.
+func MakeWorkloadWithMorePodSets(name, ns string) *WorkloadWrapper {
+	return &WorkloadWrapper{kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{Name: name, Namespace: ns},
+		Spec: kueue.WorkloadSpec{
+			PodSets: []kueue.PodSet{
+				*MakePodSet("main", 3).Obj(),
+			},
+		},
+	}}
+}
+
+func (w *WorkloadWrapper) ScaleDown() *WorkloadWrapper {
```

  I realized I wasn't even using them now. I removed `MakeWorkloadWithMorePodSets(name, ns string)`, ` ScaleDown()`, ` IncreasePodSetCount()` and `RequestScaleDown(r corev1.ResourceName, q string)` sorry for the confusion. 
  
  I'm only using `MakeAdmissionScaleDown()` and I'm aware that we already have  [MakeAdmission(cq string, podSetNames ...string)](https://github.com/kubernetes-sigs/kueue/blob/1f63a55769a7dd22bebade37cb30f6e26a50a4f9/pkg/util/testing/wrappers.go#L457) and i could change this to accept an integer value for PodSetAssignmentCount, a string value for resourceUsage amount and corev1.ResourceName but that would change a lot of files in the repo and might be better suited for a different PR.  I could change the name of the method to be `MakeCustomAdmission` since we are specifying these 3 things in the parameters.

- **pkg/util/testing/wrappers.go:79** by [@vicentefb](https://github.com/vicentefb) — 2024-05-13T23:05:23Z
```diff
@@ -74,6 +74,29 @@ func MakeWorkload(name, ns string) *WorkloadWrapper {
 	}}
 }
 
+// MakeWorkload creates a wrapper for a Workload with a single
+// pod with a single container.
+func MakeWorkloadWithMorePodSets(name, ns string) *WorkloadWrapper {
```

  See comment: https://github.com/kubernetes-sigs/kueue/pull/1852/files/97babe786ff6d2d64c34855acc6a5dc37cfd014b#r1598327603

- **pkg/controller/jobframework/reconciler.go:760** by [@vicentefb](https://github.com/vicentefb) — 2024-05-13T23:09:10Z
```diff
@@ -698,7 +709,8 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		jobSizeable, implementsSizable := job.(ReizableJobs)
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) || (implementsSizable && jobSizeable.IsResizable(wl)) {
```

  Okok i moved the logic to happen inside the `equality` package and i'm checking for upsizing and downsizing but i'm still making the check from the integration because I send in the podSets[1:] since we don't care about the RayCluster head.

- **pkg/controller/core/workload_controller.go:309** by [@vicentefb](https://github.com/vicentefb) — 2024-05-13T23:12:43Z
```diff
@@ -293,6 +301,57 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 	return cond != nil && cond.Status == metav1.ConditionFalse && cond.Reason == kueue.WorkloadEvictedByClusterQueueStopped
 }
 
+func (r *WorkloadReconciler) isScaledDown(wl *kueue.Workload) bool {
+	if !features.Enabled(features.ResizableJobs) {
+		return false
+	}
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
```

  In this case the feature of Scale Down works for RayClusters and the first element in the PodSet is always the head which always has a count of 1 and we care about the RayCluster workers which happen to be [1:] in a PodSet.

- **pkg/controller/core/workload_controller.go:309** by [@vicentefb](https://github.com/vicentefb) — 2024-05-13T23:44:23Z
```diff
@@ -293,6 +301,57 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 	return cond != nil && cond.Status == metav1.ConditionFalse && cond.Reason == kueue.WorkloadEvictedByClusterQueueStopped
 }
 
+func (r *WorkloadReconciler) isScaledDown(wl *kueue.Workload) bool {
+	if !features.Enabled(features.ResizableJobs) {
+		return false
+	}
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
```

  I included  the implementation logic inside the package `util/equality`

- **pkg/controller/core/workload_controller.go:702** by [@vicentefb](https://github.com/vicentefb) — 2024-05-14T00:28:26Z
```diff
@@ -620,7 +680,7 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 				}
 			})
 		}
-	case prevStatus == admitted && status == admitted && !equality.Semantic.DeepEqual(oldWl.Status.ReclaimablePods, wl.Status.ReclaimablePods):
+	case prevStatus == admitted && status == admitted && !equality.Semantic.DeepEqual(oldWl.Status.ReclaimablePods, wl.Status.ReclaimablePods) || r.isScaledDown(oldWl):
```

  The `oldWl` will still have the original PodSetAssignments and we can check the difference between that and the workload's PodSet count to see if there was a downsize.

- **pkg/controller/core/workload_controller.go:332** by [@vicentefb](https://github.com/vicentefb) — 2024-05-14T00:45:55Z
```diff
@@ -293,6 +301,57 @@ func isDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
 	return cond != nil && cond.Status == metav1.ConditionFalse && cond.Reason == kueue.WorkloadEvictedByClusterQueueStopped
 }
 
+func (r *WorkloadReconciler) isScaledDown(wl *kueue.Workload) bool {
+	if !features.Enabled(features.ResizableJobs) {
+		return false
+	}
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) > wl.Spec.PodSets[i].Count {
+			return true
+		}
+	}
+	return false
+}
+
+func (r *WorkloadReconciler) downSizeJobIfNecessary(wl *kueue.Workload, ctx context.Context) error {
+	updateStatus := false
+	podSetSize := len(wl.Spec.PodSets)
+	for i := 1; i < podSetSize; i++ {
+		if ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) <= wl.Spec.PodSets[i].Count {
+			continue
+		}
+		// Get Resource Requests and Usage values
+		originalResourceRequests := limitrange.TotalRequests(&wl.Spec.PodSets[i].Template.Spec)
+		currentAssignedResourceUsage := wl.Status.Admission.PodSetAssignments[i].ResourceUsage
+
+		diff := ptr.Deref(wl.Status.Admission.PodSetAssignments[i].Count, 0) - wl.Spec.PodSets[i].Count
+		for k := range currentAssignedResourceUsage {
+			resourceQuantity := originalResourceRequests[k]
+			resourceQuantity.Mul(int64(diff))
+			originalResourceRequests[k] = resourceQuantity
```

  Okok sounds good, removed that line and used resourceQuantity directly below.
