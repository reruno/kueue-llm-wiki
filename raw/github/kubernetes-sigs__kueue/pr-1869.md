# PR #1869: Passing parameters to ProvisioningRequest from annotations

**Summary**: Passing parameters to ProvisioningRequest from annotations

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1869

**Last updated**: 2024-04-26T09:52:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `prov-req-annotations` → `main`
- **Created**: 2024-03-20T10:35:37Z
- **Updated**: 2024-04-26T09:52:38Z
- **Merged**: 2024-03-26T11:31:20Z
- **Closed**: 2024-03-26T11:31:20Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 16   **+277 / -36**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
/kind api-change 

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This implements changes introduced in #1788 and #1793 

#### Which issue(s) this PR fixes:
Fixes #1788
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:
I've done manual e2e testing

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
User can now pass parameters to ProvisioningRequest using job's annotations
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-03-20T10:35:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2b8b01c8998487409016dc0449a321c5635db2b1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6602ac62474abf0008679552 |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-03-21T14:11:38Z

/assign @yaroslava-serdiuk

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-21T19:13:34Z

Please include integration tests for jobs and pods

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-03-22T11:35:38Z

/retest

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-03-22T11:58:41Z

/retest

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-03-22T12:39:02Z

I'll reverse changes regarding setup-envtest version when #1883  is merged

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-22T17:39:34Z

@gabesaba: changing LGTM is restricted to collaborators

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1869#pullrequestreview-1955169975):

>other than a few nitpicks, looks good
>
>/lgtm


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T18:35:22Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-22T18:35:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1869#issuecomment-2015691916" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1869#pullrequestreview-1955169975" title="LGTM">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1869#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-26T11:30:54Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-26T11:31:00Z

LGTM label has been added.  <details>Git tree hash: b4492ec0256b05671735ff07ea7339d1e273bc73</details>

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-03-20T14:59:36Z

- **pkg/controller/admissionchecks/provisioning/controller.go:168** by [@gabesaba](https://github.com/gabesaba) — 2024-03-20T13:43:24Z
```diff
@@ -162,6 +165,7 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if requeAfter != nil {
 		return reconcile.Result{RequeueAfter: *requeAfter}, nil
 	}
+
```

  nit: delete
  
  could you revert other whitespace changes too please, to minimize the diff? e.g. reconciler_test.go and last 3 changes of controller_test.go

- **pkg/controller/admissionchecks/provisioning/controller.go:461** by [@gabesaba](https://github.com/gabesaba) — 2024-03-20T14:04:41Z
```diff
@@ -438,17 +443,27 @@ func requestHasParameters(req *autoscaling.ProvisioningRequest, prc *kueue.Provi
 	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
 		return false
 	}
-	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
-		return false
-	}
-	for k, vReq := range req.Spec.Parameters {
-		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+	for k, vCfg := range prc.Spec.Parameters {
+		if vReq, found := req.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
 			return false
 		}
 	}
 	return true
 }
 
+// passProvReqParams extracts from Workload's annotations ones that should be passed to ProvisioningRequest
+func passProvReqParams(wl *kueue.Workload, req *autoscaling.ProvisioningRequest) {
+	if req.Spec.Parameters == nil {
+		req.Spec.Parameters = make(map[string]autoscaling.Parameter, 0)
+	}
+	for annotation, val := range wl.Annotations {
+		if strings.HasPrefix(annotation, provReqAnnotationPrefix) {
+			paramName := strings.Split(annotation, "/")[1]
```

  you may consider https://pkg.go.dev/strings#TrimPrefix

- **pkg/controller/jobframework/reconciler_test.go:42** by [@gabesaba](https://github.com/gabesaba) — 2024-03-20T14:06:40Z
```diff
@@ -38,6 +38,10 @@ import (
 	. "sigs.k8s.io/kueue/pkg/controller/jobframework"
 )
 
+const (
+	testNamespace = "ns"
```

  is this used?

- **pkg/controller/admissionchecks/provisioning/controller.go:446** by [@gabesaba](https://github.com/gabesaba) — 2024-03-20T14:19:52Z
```diff
@@ -438,17 +443,27 @@ func requestHasParameters(req *autoscaling.ProvisioningRequest, prc *kueue.Provi
 	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
 		return false
 	}
-	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
-		return false
-	}
-	for k, vReq := range req.Spec.Parameters {
-		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+	for k, vCfg := range prc.Spec.Parameters {
```

  could you update the function name and/or add a comment please? It is not clear from the function name what this check is doing.
  
  especially now that we'r no longer just checking for equality of the two maps.

- **pkg/controller/jobs/pod/pod_controller.go:903** by [@gabesaba](https://github.com/gabesaba) — 2024-03-20T14:39:36Z
```diff
@@ -897,9 +897,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 
 	wl := &kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{
-			Namespace:  p.pod.GetNamespace(),
-			Labels:     map[string]string{},
-			Finalizers: []string{kueue.ResourceInUseFinalizerName},
+			Namespace:   p.pod.GetNamespace(),
+			Labels:      map[string]string{},
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: p.pod.Annotations,
```

  nit: GetAnnotations()

- **pkg/controller/jobs/pod/pod_controller.go:903** by [@gabesaba](https://github.com/gabesaba) — 2024-03-20T14:42:36Z
```diff
@@ -897,9 +897,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 
 	wl := &kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{
-			Namespace:  p.pod.GetNamespace(),
-			Labels:     map[string]string{},
-			Finalizers: []string{kueue.ResourceInUseFinalizerName},
+			Namespace:   p.pod.GetNamespace(),
+			Labels:      map[string]string{},
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: p.pod.Annotations,
```

  So that I understand this better: who is responsible for setting annotations on the pod (or job)? I suppose that will be as part of the pod/job config that users submit to kueue?

- **pkg/controller/jobs/pod/pod_controller.go:903** by [@gabesaba](https://github.com/gabesaba) — 2024-03-20T14:42:59Z
```diff
@@ -897,9 +897,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 
 	wl := &kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{
-			Namespace:  p.pod.GetNamespace(),
-			Labels:     map[string]string{},
-			Finalizers: []string{kueue.ResourceInUseFinalizerName},
+			Namespace:   p.pod.GetNamespace(),
+			Labels:      map[string]string{},
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: p.pod.Annotations,
```

  Do we want to filter this to just pass the ones with the provisioning request prefix for now? then this logic may be updated when someone wants to use annotations for something else in the future

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller_test.go:1** by [@gabesaba](https://github.com/gabesaba) — 2024-03-20T14:55:49Z
  are these (paddlejob, mxjob, pytorchjob, tfjob, xgboostjob, mpijob) tests needed, or can we be confident with just the job_controller_test and pod_controller_test?
  
  if it is needed, can the setup be simplified? e.g. in this test, a for-loop for many cases is created, but we just have one case.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-21T07:17:35Z

- **pkg/controller/admissionchecks/provisioning/controller.go:461** by [@PBundyra](https://github.com/PBundyra) — 2024-03-21T07:17:35Z
```diff
@@ -438,17 +443,27 @@ func requestHasParameters(req *autoscaling.ProvisioningRequest, prc *kueue.Provi
 	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
 		return false
 	}
-	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
-		return false
-	}
-	for k, vReq := range req.Spec.Parameters {
-		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+	for k, vCfg := range prc.Spec.Parameters {
+		if vReq, found := req.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
 			return false
 		}
 	}
 	return true
 }
 
+// passProvReqParams extracts from Workload's annotations ones that should be passed to ProvisioningRequest
+func passProvReqParams(wl *kueue.Workload, req *autoscaling.ProvisioningRequest) {
+	if req.Spec.Parameters == nil {
+		req.Spec.Parameters = make(map[string]autoscaling.Parameter, 0)
+	}
+	for annotation, val := range wl.Annotations {
+		if strings.HasPrefix(annotation, provReqAnnotationPrefix) {
+			paramName := strings.Split(annotation, "/")[1]
```

  Thank you, I'll fix that

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-21T07:46:44Z

- **pkg/controller/jobs/pod/pod_controller.go:903** by [@PBundyra](https://github.com/PBundyra) — 2024-03-21T07:46:44Z
```diff
@@ -897,9 +897,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 
 	wl := &kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{
-			Namespace:  p.pod.GetNamespace(),
-			Labels:     map[string]string{},
-			Finalizers: []string{kueue.ResourceInUseFinalizerName},
+			Namespace:   p.pod.GetNamespace(),
+			Labels:      map[string]string{},
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: p.pod.Annotations,
```

  Yes, exactly

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-21T08:01:55Z

- **pkg/controller/admissionchecks/provisioning/controller.go:446** by [@PBundyra](https://github.com/PBundyra) — 2024-03-21T08:01:55Z
```diff
@@ -438,17 +443,27 @@ func requestHasParameters(req *autoscaling.ProvisioningRequest, prc *kueue.Provi
 	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
 		return false
 	}
-	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
-		return false
-	}
-	for k, vReq := range req.Spec.Parameters {
-		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+	for k, vCfg := range prc.Spec.Parameters {
```

  Of course, fixed!

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-21T08:02:08Z

- **pkg/controller/admissionchecks/provisioning/controller.go:168** by [@PBundyra](https://github.com/PBundyra) — 2024-03-21T08:02:08Z
```diff
@@ -162,6 +165,7 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if requeAfter != nil {
 		return reconcile.Result{RequeueAfter: *requeAfter}, nil
 	}
+
```

  Thanks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-21T08:13:58Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-03-21T08:13:58Z
  >> are these (paddlejob, mxjob, pytorchjob, tfjob, xgboostjob, mpijob) tests needed, or can we be confident with just the job_controller_test and pod_controller_test?
  
  I am not strongly opinionated on that so I can reverse those tests if you think it's a better call. I just thought it' was better to have them than not, as I was not sure how confident we could be.
  
  >> if it is needed, can the setup be simplified? e.g. in this test, a for-loop for many cases is created, but we just have one case.
  
  I've decided to use the for-loop setup so it's easier to extend in the future.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-21T09:00:10Z

- **pkg/controller/jobs/pod/pod_controller.go:903** by [@PBundyra](https://github.com/PBundyra) — 2024-03-21T09:00:09Z
```diff
@@ -897,9 +897,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 
 	wl := &kueue.Workload{
 		ObjectMeta: metav1.ObjectMeta{
-			Namespace:  p.pod.GetNamespace(),
-			Labels:     map[string]string{},
-			Finalizers: []string{kueue.ResourceInUseFinalizerName},
+			Namespace:   p.pod.GetNamespace(),
+			Labels:      map[string]string{},
+			Finalizers:  []string{kueue.ResourceInUseFinalizerName},
+			Annotations: p.pod.Annotations,
```

  Sure, fixed

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-03-21T13:36:38Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller_test.go:1** by [@gabesaba](https://github.com/gabesaba) — 2024-03-21T13:36:38Z
  1) Coverage is good, but these tests are accounting for the majority of lines of code of this change, and this is code that will have to be maintained.  This approach is not very scalable, as tests are proportional to features * types of jobs in the system. Is it possible to write a test such as "when we create a job of type X, it delegates to the jobframework/pod_controller to create a workload"? And then, have tests for the PodController/JobReconciler that check that those properly propagate the annotations. In this case, tests are proportional to features + types of jobs. @alculquicondor, wdyt?
  
  2) I would bias towards simplicity. These tests may never be extended, and the current version is more complex, which will make it harder to update/maintain.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-21T19:11:23Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-21T19:11:22Z
  I'm with @gabesaba here.
  These additional tests don't add much value. We started some effort some time ago to add a generic testing suite, but it's now stale (although focused on integration: #1191).
  For now, some tests just for job, pods and maybe one of the kubeflow jobs should be enough.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-21T19:24:02Z

- **pkg/controller/admissionchecks/provisioning/controller.go:272** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-21T19:18:21Z
```diff
@@ -267,6 +269,7 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
 				},
 			}
+			passProvReqParams(wl, req)
```

  What if the user modifies the annotations?
  Is there a resync?

- **pkg/controller/admissionchecks/provisioning/controller_test.go:320** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-21T19:20:12Z
```diff
@@ -311,6 +313,46 @@ func TestReconcile(t *testing.T) {
 				},
 			},
 		},
+		"workload with provreq annotation": {
+			workload: utiltesting.MakeWorkload("wl", TestNamespace).
+				SetAnnotations(map[string]string{
+					"provreq.kueue.x-k8s.io/ValidUntilSeconds": "0",
+					"invalid-provreq-prefix/MyAnnotation":      "MyValue"}).
```

  Add another annotation that doesn't have the prefix, to make sure it's skipped

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-22T08:12:16Z

- **pkg/controller/admissionchecks/provisioning/controller.go:272** by [@PBundyra](https://github.com/PBundyra) — 2024-03-22T08:12:16Z
```diff
@@ -267,6 +269,7 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
 				},
 			}
+			passProvReqParams(wl, req)
```

  Once ProvReq is created there is no way to modify it

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-22T10:19:26Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:320** by [@PBundyra](https://github.com/PBundyra) — 2024-03-22T10:19:26Z
```diff
@@ -311,6 +313,46 @@ func TestReconcile(t *testing.T) {
 				},
 			},
 		},
+		"workload with provreq annotation": {
+			workload: utiltesting.MakeWorkload("wl", TestNamespace).
+				SetAnnotations(map[string]string{
+					"provreq.kueue.x-k8s.io/ValidUntilSeconds": "0",
+					"invalid-provreq-prefix/MyAnnotation":      "MyValue"}).
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-22T11:18:48Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-03-22T11:18:48Z
  Thanks for the clarification. I've reversed the discussed changes.

### Review by [@gabesaba](https://github.com/gabesaba) (approved) — 2024-03-22T17:39:31Z

other than a few nitpicks, looks good

/lgtm

- **pkg/controller/admissionchecks/provisioning/controller.go:440** by [@gabesaba](https://github.com/gabesaba) — 2024-03-22T15:52:47Z
```diff
@@ -434,21 +437,33 @@ func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]aut
 	return out
 }
 
-func requestHasParameters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+// provReqSyncedWithConfig checks if the provisioning request has the same provisioningClassName as the provisioning request config
```

  nit: s/provReqSyncedWithConfig //

- **pkg/controller/admissionchecks/provisioning/controller.go:447** by [@gabesaba](https://github.com/gabesaba) — 2024-03-22T17:11:51Z
```diff
@@ -434,21 +437,33 @@ func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]aut
 	return out
 }
 
-func requestHasParameters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+// provReqSyncedWithConfig checks if the provisioning request has the same provisioningClassName as the provisioning request config
+// and contains all the parameters from the config
+func provReqSyncedWithConfig(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
 	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
 		return false
 	}
-	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
-		return false
-	}
-	for k, vReq := range req.Spec.Parameters {
-		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+	for k, vCfg := range prc.Spec.Parameters {
+		if vReq, found := req.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
```

  nit: consider converting both types back to underlying type before comparison, as vCfg isn't a autoscaling.Parameter, nor vice versa.
  
  
  `string(vReq) != string(vCfg)`

- **pkg/controller/constants/constants.go:40** by [@gabesaba](https://github.com/gabesaba) — 2024-03-22T17:17:41Z
```diff
@@ -36,4 +36,7 @@ const (
 	// workloadPriorityClass name.
 	// This label is always mutable because it might be useful for the preemption.
 	WorkloadPriorityClassLabel = "kueue.x-k8s.io/priority-class"
+
+	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
```

  nit: s/ProvReqAnnotationPrefix is the //

- **pkg/controller/jobframework/reconciler.go:1026** by [@gabesaba](https://github.com/gabesaba) — 2024-03-22T17:26:16Z
```diff
@@ -1020,3 +1022,14 @@ func clearMinCountsIfFeatureDisabled(in []kueue.PodSet) []kueue.PodSet {
 	}
 	return in
 }
+
+// FilterAnnotationsWithPrefix filters out annotations containing prefix.
```

  nit: `// filters for annotations containing prefix`
  
  I think that out implies that you're removing annotations with the prefix

- **pkg/controller/jobframework/reconciler.go:1027** by [@gabesaba](https://github.com/gabesaba) — 2024-03-22T17:30:26Z
```diff
@@ -1020,3 +1022,14 @@ func clearMinCountsIfFeatureDisabled(in []kueue.PodSet) []kueue.PodSet {
 	}
 	return in
 }
+
+// FilterAnnotationsWithPrefix filters out annotations containing prefix.
+func FilterAnnotationsWithPrefix(annotations map[string]string, prefix string) map[string]string {
```

  nit: this function doesn't need to be specialized (drop the second parameter). rename accordingly

- **pkg/controller/admissionchecks/provisioning/controller.go:460** by [@gabesaba](https://github.com/gabesaba) — 2024-03-22T17:36:12Z
```diff
@@ -434,21 +437,33 @@ func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]aut
 	return out
 }
 
-func requestHasParameters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+// provReqSyncedWithConfig checks if the provisioning request has the same provisioningClassName as the provisioning request config
+// and contains all the parameters from the config
+func provReqSyncedWithConfig(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
 	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
 		return false
 	}
-	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
-		return false
-	}
-	for k, vReq := range req.Spec.Parameters {
-		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+	for k, vCfg := range prc.Spec.Parameters {
+		if vReq, found := req.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
 			return false
 		}
 	}
 	return true
 }
 
+// passProvReqParams extracts from Workload's annotations ones that should be passed to ProvisioningRequest
+func passProvReqParams(wl *kueue.Workload, req *autoscaling.ProvisioningRequest) {
+	if req.Spec.Parameters == nil {
+		req.Spec.Parameters = make(map[string]autoscaling.Parameter, 0)
+	}
+	for annotation, val := range wl.Annotations {
+		if strings.HasPrefix(annotation, constants.ProvReqAnnotationPrefix) {
```

  nit: if it's easy to refactor to a common location, consider using FilterAnnotationsWithPrefix here, to keep the logic in one place
  
  e.g. `for annotation, val := range FilterAnnotationsWithPrefix(wl.Annotations) {`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T18:29:38Z

- **pkg/controller/admissionchecks/provisioning/controller.go:272** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T18:29:38Z
```diff
@@ -267,6 +269,7 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
 				},
 			}
+			passProvReqParams(wl, req)
```

  That's ok, but let's point it out in the documentation.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T18:31:30Z

- **pkg/controller/jobframework/reconciler.go:1026** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T18:31:30Z
```diff
@@ -1020,3 +1022,14 @@ func clearMinCountsIfFeatureDisabled(in []kueue.PodSet) []kueue.PodSet {
 	}
 	return in
 }
+
+// FilterAnnotationsWithPrefix filters out annotations containing prefix.
```

  https://go.dev/wiki/CodeReviewComments#comment-sentences

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T18:35:14Z

- **test/integration/controller/jobs/job/job_controller_test.go:133** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T18:34:41Z
```diff
@@ -126,9 +130,10 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 
 		createdTime := createdWorkload.CreationTimestamp
 
-		ginkgo.By("checking the workload is created with priority and priorityName")
+		ginkgo.By("checking the workload is created with priority, priorityName, and ProvisioningRequest qannotations")
```

  ```suggestion
  		ginkgo.By("checking the workload is created with priority, priorityName, and ProvisioningRequest annotations")
  ```

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-03-25T08:38:42Z

- **pkg/controller/admissionchecks/provisioning/controller.go:440** by [@gabesaba](https://github.com/gabesaba) — 2024-03-25T08:38:41Z
```diff
@@ -434,21 +437,33 @@ func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]aut
 	return out
 }
 
-func requestHasParameters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+// provReqSyncedWithConfig checks if the provisioning request has the same provisioningClassName as the provisioning request config
```

  ignore this please

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-03-25T08:40:42Z

- **pkg/controller/constants/constants.go:40** by [@gabesaba](https://github.com/gabesaba) — 2024-03-25T08:40:41Z
```diff
@@ -36,4 +36,7 @@ const (
 	// workloadPriorityClass name.
 	// This label is always mutable because it might be useful for the preemption.
 	WorkloadPriorityClassLabel = "kueue.x-k8s.io/priority-class"
+
+	// ProvReqAnnotationPrefix is the prefix for annotations that should be pass to ProvisioningRequest as Parameters.
```

  ignore this please

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-25T09:35:21Z

LGTM, just a naming nit

- **pkg/util/testing/wrappers.go:285** by [@mimowo](https://github.com/mimowo) — 2024-03-25T09:32:47Z
```diff
@@ -277,8 +282,8 @@ func (w *WorkloadWrapper) appendOwnerReference(gvk schema.GroupVersionKind, name
 	return w
 }
 
-func (w *WorkloadWrapper) Annotations(kv map[string]string) *WorkloadWrapper {
-	w.ObjectMeta.Annotations = kv
+func (w *WorkloadWrapper) SetAnnotations(annotations map[string]string) *WorkloadWrapper {
```

  nit, in golang we usually don't prefix setters with `Set` so I would suggest to revert the rename.

- **pkg/util/testingjobs/jobset/wrappers.go:113** by [@mimowo](https://github.com/mimowo) — 2024-03-25T09:33:47Z
```diff
@@ -109,6 +109,12 @@ func (j *JobSetWrapper) Label(k, v string) *JobSetWrapper {
 	return j
 }
 
+// Annotation sets an annotation to the JobSet.
+func (j *JobSetWrapper) SetAnnotations(annotations map[string]string) *JobSetWrapper {
```

  ```suggestion
  func (j *JobSetWrapper) Annotations(annotations map[string]string) *JobSetWrapper {
  ```
  Or just `Annotation` function as for `pkg/util/testingjobs/pod/wrappers.go`

- **pkg/util/testingjobs/mxjob/wrappers.go:136** by [@mimowo](https://github.com/mimowo) — 2024-03-25T09:34:10Z
```diff
@@ -132,6 +132,12 @@ func (j *MXJobWrapper) Queue(queue string) *MXJobWrapper {
 	return j
 }
 
+// SetAnnotations updates annotations of the job.
+func (j *MXJobWrapper) SetAnnotations(annotations map[string]string) *MXJobWrapper {
```

  `Annotations` or `Annotation` (for setting annotations one by one)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-03-25T10:19:51Z

- **pkg/controller/admissionchecks/provisioning/controller.go:460** by [@PBundyra](https://github.com/PBundyra) — 2024-03-25T10:19:51Z
```diff
@@ -434,21 +437,33 @@ func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]aut
 	return out
 }
 
-func requestHasParameters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+// provReqSyncedWithConfig checks if the provisioning request has the same provisioningClassName as the provisioning request config
+// and contains all the parameters from the config
+func provReqSyncedWithConfig(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
 	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
 		return false
 	}
-	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
-		return false
-	}
-	for k, vReq := range req.Spec.Parameters {
-		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+	for k, vCfg := range prc.Spec.Parameters {
+		if vReq, found := req.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
 			return false
 		}
 	}
 	return true
 }
 
+// passProvReqParams extracts from Workload's annotations ones that should be passed to ProvisioningRequest
+func passProvReqParams(wl *kueue.Workload, req *autoscaling.ProvisioningRequest) {
+	if req.Spec.Parameters == nil {
+		req.Spec.Parameters = make(map[string]autoscaling.Parameter, 0)
+	}
+	for annotation, val := range wl.Annotations {
+		if strings.HasPrefix(annotation, constants.ProvReqAnnotationPrefix) {
```

  Fixed, thanks

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-25T13:42:15Z

- **pkg/controller/jobframework/reconciler.go:48** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-25T13:41:09Z
```diff
@@ -45,6 +45,7 @@ import (
 	"sigs.k8s.io/kueue/pkg/util/maps"
 	utilpriority "sigs.k8s.io/kueue/pkg/util/priority"
 	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
```

  This is not sorted alphabetically

- **pkg/util/admissioncheck/admissioncheck.go:148** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-25T13:41:50Z
```diff
@@ -141,3 +143,14 @@ func FilterForController(ctx context.Context, c client.Client, states []kueue.Ad
 	}
 	return retActive, nil
 }
+
+// GetProvReqAnnotations returns annotations containing the Provisioning Request annotation prefix.
+func GetProvReqAnnotations(annotations map[string]string) map[string]string {
```

  ```suggestion
  func FilterProvReqAnnotations(annotations map[string]string) map[string]string {
  ```
