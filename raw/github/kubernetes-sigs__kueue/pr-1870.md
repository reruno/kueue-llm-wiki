# PR #1870: MultiKueue JobSet managedBy

**Summary**: MultiKueue JobSet managedBy

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1870

**Last updated**: 2024-05-29T20:23:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `multikueue-jobset-managed-by` → `main`
- **Created**: 2024-03-20T13:44:15Z
- **Updated**: 2024-05-29T20:23:03Z
- **Merged**: 2024-04-16T13:34:14Z
- **Closed**: 2024-04-16T13:34:14Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 11   **+178 / -62**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add support for JobSet `spec.managedBy`.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #693 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
MultiKueue: Add support for  JobSet  `spec.managedBy` field
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-03-20T13:44:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 901d8ea02038d22e8e7e56c527e3b207432447dc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/661e7b25e49ce40008f6a612 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1870--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1870--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE4NzAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.5S_QKcj-aYBvsvf6uU-EeDWEdtFveAzRkuyh3TwSIs8)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2024-03-25T10:11:09Z

/hold
Let's wait for the JobSet 0.5 release which migrates to field, which is tentatively planned for April per https://github.com/kubernetes-sigs/kueue/pull/1870#discussion_r1536007381

### Comment by [@trasc](https://github.com/trasc) — 2024-04-16T07:53:23Z

/unhold

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-16T10:06:55Z

/lgtm
/assign @alculquicondor @tenzen-y 
Please also adjust the PR description and title to refer to the field.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-16T10:07:00Z

LGTM label has been added.  <details>Git tree hash: 5fc7622b2e3abc96d956242b83803c311396cc22</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T12:13:59Z

/unassign

@mimowo can you handle this one?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-16T12:20:37Z

> @mimowo can you handle this one?

I think extra approve will be needed for docs, but I can approve the rest.
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-16T13:22:29Z

LGTM label has been added.  <details>Git tree hash: f7fb744a7e9a8aee5175e456fa919aba2a27958d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-16T13:22:30Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1870#pullrequestreview-2003625277" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1870#issuecomment-2058958154" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1870#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[hack/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/hack/OWNERS)~~ [alculquicondor,mimowo]
- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor,mimowo]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor,mimowo]
- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-29T20:23:01Z

/release-note-edit
```release-note
MultiKueue: Add support for  JobSet  `spec.managedBy` field
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T14:40:27Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T14:39:47Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  Without going more into code  I think we should first migrate JobSet to use managedBy field to be consistent with batch Job. Could you please send the JobSet PR first?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-20T15:14:01Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@trasc](https://github.com/trasc) — 2024-03-20T15:14:01Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  
  -  kubernetes-sigs/jobset#407
  
  Merged and part of v0.4.0

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T15:21:47Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T15:21:47Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  Yeah but this is based on label and we decided to use field upstream

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-20T15:32:33Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@trasc](https://github.com/trasc) — 2024-03-20T15:32:33Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  The current state of the jobset fits our needs , if at some point  they decide to switch to field we can adapt. But for now I don't see that need.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-20T15:36:45Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-20T15:36:45Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  I agree that there is no need, but being a kubernetes-sig project, it should follow the same pattern as kubernetes.
  
  So with the goal of standardizing and incentivizing other projects to follow suit, we can do the change in jobset.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T15:48:47Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T15:48:47Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  Yes, I think we want the clear path for other JobCRDs to follow, so we prefer consistence in the ecosystem, which is a goal of batch wg. 
  
  Second the same argument applies as for k8s job, that with label you may use it on old versions, and it may have unexpected consequences later on. With field we fail fast.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T16:02:54Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T16:02:54Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  There is an issue for this already https://github.com/kubernetes-sigs/jobset/issues/440

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-20T16:15:01Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@trasc](https://github.com/trasc) — 2024-03-20T16:15:00Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  I could do that also, but it should not block this PR. And we should realign when a new versin of jobset is release.
  
  It will also help if we are sure the approach is final for Jobs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T16:18:06Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T16:18:05Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  The approach for Job was agreed by API approvers so assume it is final.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T16:20:29Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T16:20:29Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  I think we should kick off the JobSet effort as soon as possible. If JobSet can support it quickly then my preference is to adapt this PR.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-20T16:25:39Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@trasc](https://github.com/trasc) — 2024-03-20T16:25:39Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  I see no valid reason to hold this PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T16:30:33Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T16:30:33Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  The reason is that we don't want to support label in 0.7, because it would mean we need to maintain it in the future. If we merge this PR, in the current form, we need to be careful to remember to use field before the release.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-03-20T16:39:17Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@tenzen-y](https://github.com/tenzen-y) — 2024-03-20T16:39:17Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  I'm ok with maintaining both approaches (label and field), but we need to follow appropriate deprecation steps.
  @mimowo @alculquicondor @trasc Can we pay such maintenance costs?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-20T16:43:10Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@trasc](https://github.com/trasc) — 2024-03-20T16:43:10Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  > The reason is that we don't want to support label in 0.7, because it would mean we need to maintain it in the future. If we merge this PR, in the current form, we need to be careful to remember to use field before the release.
  
  Is JobSet going to support both at any point?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-20T16:48:13Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@trasc](https://github.com/trasc) — 2024-03-20T16:48:13Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  > I'm ok with maintaining both approaches (label and field), but we need to follow appropriate deprecation steps.
  > @mimowo @alculquicondor @trasc Can we pay such maintenance costs?
  
  When the field is available, we can switch to that, we can also keep a fallback to the label if it is still available.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T17:04:11Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T17:04:11Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  There is a deprecation cost that needs to be paid for the label. I think we already have that at the JobSet level, but not yet at the Kueue level.
  
   I think it is preferred not to support label in Kueue at all, to avoid the cost for kueue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T17:14:25Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T17:14:25Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  Consider that Kueue 0.7 is using label to support JobSet 0.4, then a future JobSet drops the support for the alpha label. 
  
  Then the user is broken after upgrading jobset.
  
   So I think if we can it is better to wait for field in JobSet and not go into the issues on upgrades.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-20T17:19:51Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-03-20T17:19:51Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  I think alpha features are mostly for experimentation so jobset is free to drop the support (in the next release) actually for the alpha level label. 
  
  If there is a user that urgently (before jobset 0.5 is released) needs multikueue, which I doubt, they will be able to use Kueue 0.7 with main version of jobset.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-03-22T18:05:58Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@kannon92](https://github.com/kannon92) — 2024-03-22T18:05:58Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  @danielvegamyhre and I are discussing this https://github.com/kubernetes-sigs/jobset/issues/440#issuecomment-2015616035.
  
  Ideally we would like to drop this ASAP as long as no users are using it.

### Review by [@danielvegamyhre](https://github.com/danielvegamyhre) (commented) — 2024-03-22T18:15:52Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@danielvegamyhre](https://github.com/danielvegamyhre) — 2024-03-22T18:15:52Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  > I think alpha features are mostly for experimentation so jobset is free to drop the support (in the next release) actually for the alpha level label.
  
  This is true but JobSet is a special case because all JobSet features are technically still alpha but several companies depend on it for their production workloads, making it more like v1/GA is that regard.
  
  For this particular label though, I'm not aware of any users that actually use it, and it's only existed for 3 weeks, so perhaps we can just drop it in the April release.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T18:19:14Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T18:19:14Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  It sounds unlikely that anyone would use the managed-by label in a production environment. The only known use case is MultiKueue, and it's currently not depending on the label.
  
  So, I think it would be ok just to drop the support for the label in the next release of jobset.

### Review by [@danielvegamyhre](https://github.com/danielvegamyhre) (commented) — 2024-03-22T18:39:16Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@danielvegamyhre](https://github.com/danielvegamyhre) — 2024-03-22T18:39:16Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  I see, ok sounds good to me.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-03-25T10:09:38Z

- **site/content/en/docs/concepts/multikueue.md:62** by [@mimowo](https://github.com/mimowo) — 2024-03-25T10:08:00Z
```diff
@@ -58,10 +58,8 @@ Known Limitations:
 There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the Kubernetes `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
 
 ### JobSet
-Known Limitations:
-- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, you should only install the JobSet CRDs, but not the controller.
 
-An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, MultiKueue expects the JobSets submitted to a ClusterQueue using it to have `alpha.jobset.sigs.k8s.io/managed-by` label set to `kueue`.
```

  ```suggestion
  MultiKueue expects the JobSets submitted to a ClusterQueue to have the `spec.managedBy` field set to `kueue.x-k8s.io/multikueue`.
  ```
  Once we migrate to use the field, I would suggest `kueue.x-k8s.io/multikueue` as the reserved value, by analogy to k8s, where the value is `kubernetes.io/job-controller`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-02T06:49:11Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-04-02T06:49:10Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  @trasc please adapt this PR as the Jobset PR is merged: https://github.com/kubernetes-sigs/jobset/pull/487.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-02T11:12:58Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@trasc](https://github.com/trasc) — 2024-04-02T11:12:58Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  We should wait for the change to be released, since we need to also install a compatible version of  JobSet operator.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-02T11:20:24Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@mimowo](https://github.com/mimowo) — 2024-04-02T11:20:24Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  I think we could for now install main version to move forward, then we just need to bump to 0.5 once JobSet is released. @danielvegamyhre any plans for the release of 0.5?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-04-02T12:20:27Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@kannon92](https://github.com/kannon92) — 2024-04-02T12:20:27Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  https://github.com/kubernetes-sigs/jobset/issues/279
  
  @danielvegamyhre is reviewing the ttl PR. I asked about the website work also as I'm not sure if that is considered done or not. But I think release 0.5 should be soon. Issue says target was in April.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-04-02T12:21:28Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@kannon92](https://github.com/kannon92) — 2024-04-02T12:21:28Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  https://github.com/kubernetes-sigs/jobset/issues/438
  
  April 10th supposely.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2024-04-16T02:12:57Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@kannon92](https://github.com/kannon92) — 2024-04-16T02:12:57Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  Jobset 0.5 was released today!

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-16T07:52:42Z

- **pkg/controller/admissionchecks/multikueue/jobset_adapter.go:70** by [@trasc](https://github.com/trasc) — 2024-04-16T07:52:42Z
```diff
@@ -62,9 +63,12 @@ func (b *jobsetAdapter) SyncJob(ctx context.Context, localClient client.Client,
 	if remoteJob.Labels == nil {
 		remoteJob.Labels = map[string]string{}
 	}
-	remoteJob.Labels[constants.PrebuiltWorkloadLabel] = workloadName
+	remoteJob.Labels[controllerconstants.PrebuiltWorkloadLabel] = workloadName
 	remoteJob.Labels[kueuealpha.MultiKueueOriginLabel] = origin
 
+	// set the manager
+	remoteJob.Labels[jobset.LabelManagedBy] = jobset.JobSetManager
```

  thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-16T08:26:54Z

Almost lgtm, just nits.

- **pkg/controller/admissionchecks/multikueue/workload.go:79** by [@mimowo](https://github.com/mimowo) — 2024-04-16T08:16:39Z
```diff
@@ -73,6 +73,11 @@ type jobAdapter interface {
 	// kept Pending while the job runs in a worker. This might be needed to keep the managers job
 	// suspended and not start the execution locally.
 	KeepAdmissionCheckPending() bool
+	// IsJobManagedByKueue returns
+	// a bool indicating if the job object identified by key is managed by kueue and can be delegated.
+	// a reason message
+	// any API error encountered during the check
```

  ```suggestion
  	// IsJobManagedByKueue returns:
  	// - a bool indicating if the job object identified by key is managed by kueue and can be delegated.
  	// - a reason indicating why the job is not managed by Kueue
  	// - any API error encountered during the check
  ```

- **site/content/en/docs/tasks/run/jobsets.md:37** by [@mimowo](https://github.com/mimowo) — 2024-04-16T08:21:51Z
```diff
@@ -32,7 +32,15 @@ metadata:
     kueue.x-k8s.io/queue-name: user-queue
 ```
 
-### b. Configure the resource needs
+### b. MultiKueue
+
+If the JobSet is submitted to a queue using [MultiKueue](/docs/concepts/multikueue) one additional label needs to be specified.
```

  Please update

- **site/content/en/docs/concepts/multikueue.md:62** by [@mimowo](https://github.com/mimowo) — 2024-04-16T08:23:07Z
```diff
@@ -58,10 +58,8 @@ Known Limitations:
 There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the Kubernetes `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
 
 ### JobSet
-Known Limitations:
-- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, you should only install the JobSet CRDs, but not the controller.
 
-An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, MultiKueue expects the JobSets submitted to a ClusterQueue using it to have `spec.managedBy` set to `kueue.x-k8s.io/multikueue`. The JobSet `managedBy` field is available since JobSet v0.5.0.
```

  ```suggestion
  MultiKueue expects the JobSets, submitted to a ClusterQueue using the MultiKueue amission check, to have `spec.managedBy` set to `kueue.x-k8s.io/multikueue`. The JobSet `managedBy` field is available since JobSet v0.5.0.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-16T09:12:08Z

- **pkg/controller/admissionchecks/multikueue/workload.go:79** by [@trasc](https://github.com/trasc) — 2024-04-16T09:06:40Z
```diff
@@ -73,6 +73,11 @@ type jobAdapter interface {
 	// kept Pending while the job runs in a worker. This might be needed to keep the managers job
 	// suspended and not start the execution locally.
 	KeepAdmissionCheckPending() bool
+	// IsJobManagedByKueue returns
+	// a bool indicating if the job object identified by key is managed by kueue and can be delegated.
+	// a reason message
+	// any API error encountered during the check
```

  done

- **site/content/en/docs/concepts/multikueue.md:62** by [@trasc](https://github.com/trasc) — 2024-04-16T09:09:09Z
```diff
@@ -58,10 +58,8 @@ Known Limitations:
 There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the Kubernetes `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
 
 ### JobSet
-Known Limitations:
-- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, you should only install the JobSet CRDs, but not the controller.
 
-An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, MultiKueue expects the JobSets submitted to a ClusterQueue using it to have `spec.managedBy` set to `kueue.x-k8s.io/multikueue`. The JobSet `managedBy` field is available since JobSet v0.5.0.
```

  done

- **site/content/en/docs/tasks/run/jobsets.md:37** by [@trasc](https://github.com/trasc) — 2024-04-16T09:10:52Z
```diff
@@ -32,7 +32,15 @@ metadata:
     kueue.x-k8s.io/queue-name: user-queue
 ```
 
-### b. Configure the resource needs
+### b. MultiKueue
+
+If the JobSet is submitted to a queue using [MultiKueue](/docs/concepts/multikueue) one additional label needs to be specified.
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-16T12:22:09Z

- **site/content/en/docs/tasks/run/jobsets.md:41** by [@mimowo](https://github.com/mimowo) — 2024-04-16T12:21:57Z
```diff
@@ -32,7 +32,15 @@ metadata:
     kueue.x-k8s.io/queue-name: user-queue
 ```
 
-### b. Configure the resource needs
+### b. MultiKueue
+
+If the JobSet is submitted to a queue using [MultiKueue](/docs/concepts/multikueue) its `spec.managedBy` field needs to be set to `kueue.x-k8s.io/multikueue`.
+
+```yaml
+    alpha.jobset.sigs.k8s.io/managed-by: kueue
+```
```

  Oh I missed that, please drop or adjust.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-16T12:25:49Z

- **site/content/en/docs/tasks/run/jobsets.md:41** by [@trasc](https://github.com/trasc) — 2024-04-16T12:25:49Z
```diff
@@ -32,7 +32,15 @@ metadata:
     kueue.x-k8s.io/queue-name: user-queue
 ```
 
-### b. Configure the resource needs
+### b. MultiKueue
+
+If the JobSet is submitted to a queue using [MultiKueue](/docs/concepts/multikueue) its `spec.managedBy` field needs to be set to `kueue.x-k8s.io/multikueue`.
+
+```yaml
+    alpha.jobset.sigs.k8s.io/managed-by: kueue
+```
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T12:29:43Z

- **site/content/en/docs/concepts/multikueue.md:62** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T12:29:43Z
```diff
@@ -58,10 +58,8 @@ Known Limitations:
 There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the Kubernetes `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
 
 ### JobSet
-Known Limitations:
-- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, you should only install the JobSet CRDs, but not the controller.
 
-An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+MultiKueue expects the JobSets, submitted to a ClusterQueue using a MultiKueue admission check, to have `spec.managedBy` set to `kueue.x-k8s.io/multikueue`. The JobSet `managedBy` field is available since JobSet v0.5.0.
```

  ```suggestion
  When you want to submit JobSets to a ClusterQueue with a MultiKueue admission check, you should set the `spec.managedBy` field to `kueue.x-k8s.io/multikueue`. The `managedBy` field is available in JobSet v0.5.0 and newer.
  ```
  
  what happens if you don't?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-16T13:21:00Z

- **site/content/en/docs/concepts/multikueue.md:62** by [@trasc](https://github.com/trasc) — 2024-04-16T13:20:57Z
```diff
@@ -58,10 +58,8 @@ Known Limitations:
 There is an ongoing effort to overcome these limitations by adding the possibility to disable the reconciliation of some jobs by the Kubernetes `batch/Job` controller. Details in `kubernetes/enhancements` [KEP-4368](https://github.com/kubernetes/enhancements/tree/master/keps/sig-apps/4368-support-managed-by-label-for-batch-jobs#readme).
 
 ### JobSet
-Known Limitations:
-- Since unsuspending a JobSet in the manager cluster will lead to its local execution and updating the status of a local JobSet could conflict with its main controller, you should only install the JobSet CRDs, but not the controller.
 
-An approach similar to the one described for [`batch/Job`](#batchjob) is taken into account to overcome this. 
+MultiKueue expects the JobSets, submitted to a ClusterQueue using a MultiKueue admission check, to have `spec.managedBy` set to `kueue.x-k8s.io/multikueue`. The JobSet `managedBy` field is available since JobSet v0.5.0.
```

  done + added some "or else ..."

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T13:22:24Z

/lgtm
/approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-16T14:27:19Z

Totally, lgtm
I left a comment for a nit.
Thank you!

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T14:21:45Z
```diff
@@ -101,3 +101,7 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
 	return true
 }
+
+func (b *batchJobAdapter) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
```

  ```suggestion
  func (b *batchJobAdapter) IsJobManagedByKueue(context.Context, client.Client, types.NamespacedName) (bool, string, error) {
  ```
  
  This is a minor nit.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-17T10:02:52Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:105** by [@trasc](https://github.com/trasc) — 2024-04-17T10:02:52Z
```diff
@@ -101,3 +101,7 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
 	return true
 }
+
+func (b *batchJobAdapter) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
```

  We should have the `managedBy` field available for Jobs soon and we'll need to properly implement the method.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-17T10:57:07Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:105** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-17T10:57:07Z
```diff
@@ -101,3 +101,7 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
 	return true
 }
+
+func (b *batchJobAdapter) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
```

  That makes sense.
