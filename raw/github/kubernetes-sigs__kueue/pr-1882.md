# PR #1882: [importer] Run in cluster

**Summary**: [importer] Run in cluster

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1882

**Last updated**: 2024-03-22T18:17:46Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `importer-in-cluster` → `main`
- **Created**: 2024-03-22T12:14:21Z
- **Updated**: 2024-03-22T18:17:46Z
- **Merged**: 2024-03-22T18:13:33Z
- **Closed**: 2024-03-22T18:13:33Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 8   **+264 / -3**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
[importer] Run in cluster
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
none
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-22T12:14:23Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-03-22T12:14:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 418336e5985bb92a93789abaa8d0066338626ab6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65fdc5b62accba000948d915 |

### Comment by [@trasc](https://github.com/trasc) — 2024-03-22T12:14:48Z

/test all

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-22T18:10:43Z

LGTM label has been added.  <details>Git tree hash: b57cf1529428a898db3967191e27b73174bc1a92</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-22T18:10:45Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1882#pullrequestreview-1955462950" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1882#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T15:19:10Z

- **cloudbuild.yaml:30** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:06:49Z
```diff
@@ -22,6 +22,15 @@ steps:
       - GIT_TAG=$_GIT_TAG
       - EXTRA_TAG=$_PULL_BASE_REF
       - DOCKER_BUILDX_CMD=/buildx-entrypoint
+  - name: 'gcr.io/k8s-staging-test-infra/gcb-docker-gcloud:v20221214-1b4dd4d69a'
+    entrypoint: make
+    args:
+      - importer-image-push
+    env:
+      - IMAGE_REGISTRY=gcr.io/$PROJECT_ID
```

  I just realized that this is wrong. We should be overriding STAGING_IMAGE_REGISTRY

- **Makefile:326** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:08:54Z
```diff
@@ -317,6 +317,34 @@ debug-image-push:
 		--platform=$(PLATFORMS) \
 		--push ./hack/debugpod
 
+# Build the importer binary
+.PHONY: importer-build
+importer-build:
+	$(GO_BUILD_ENV) $(GO_CMD) build -ldflags="$(LD_FLAGS)" -o bin/importer cmd/importer/main.go
+
+.PHONY: importer-image-build
+importer-image-build:
```

  As a follow up, can you add this target to the `build` presubmit?

- **Makefile:337** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:11:17Z
```diff
@@ -317,6 +317,34 @@ debug-image-push:
 		--platform=$(PLATFORMS) \
 		--push ./hack/debugpod
 
+# Build the importer binary
+.PHONY: importer-build
+importer-build:
+	$(GO_BUILD_ENV) $(GO_CMD) build -ldflags="$(LD_FLAGS)" -o bin/importer cmd/importer/main.go
+
+.PHONY: importer-image-build
+importer-image-build:
+	$(IMAGE_BUILD_CMD) \
+		-t $(STAGING_IMAGE_REGISTRY)/importer:$(GIT_TAG) \
+		-t $(STAGING_IMAGE_REGISTRY)/importer:$(RELEASE_BRANCH)-latest \
+		--platform=$(PLATFORMS) \
+		--build-arg BASE_IMAGE=$(BASE_IMAGE) \
+		--build-arg BUILDER_IMAGE=$(BUILDER_IMAGE) \
+		--build-arg CGO_ENABLED=$(CGO_ENABLED) \
+		$(PUSH) \
+		-f ./cmd/importer/Dockerfile ./
+
+# Developers don't need to build this image, as it will be available as gcr.io/k8s-staging-kueue/importer
```

  I'd remove this comment.
  The debug image is unlikely to change, but the importer might change fairly often, so it's more likely that developers will need to build locally.

- **cmd/importer/README.md:103** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:13:17Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
```

  ```suggestion
  `cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster.
  In order to use the manifests, you should:
  ```

- **cmd/importer/README.md:138** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:15:09Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
+
+1. Update the used image
+
+A minimal image containing the importer can be built by
+
+```bash
+make importer-image
+```
+
+Make the created image accessible by your cluster.
+
+Note: Importer images will be available in `gcr.io/k8s-staging-kueue/importer` soon.
+
+And run
+```bash
+(cd cmd/importer/run-in-cluster && kustomize edit set image importer=<image:tag>)
+```
+
+2. Updated the importer args in `cmd/importer/run-in-cluster/importer.yaml`
+3. Update the mapping configuration in `cmd/importer/run-in-cluster/mapping.yaml`
+4. Deploy the configuration:
+
+```bash
+ kubectl apply -k cmd/importer/run-in-cluster/
+```
+
+And check  the logs
+
+```yaml
+kubectl -n kueue-importer logs kueue-importer -f
```

  this reminds me: can you make the debug setting for zap logger optional?
  When running in cluster, it's useful to have json logging instead.

- **cmd/importer/run-in-cluster/deps.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:15:42Z
  nit: Is this autogenerated? If not, could it be using kubebuilder tags?

- **cmd/importer/run-in-cluster/importer.yaml:2** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:16:30Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: v1
+kind: Pod
```

  Use a kubernetes Job instead, because it provides retries

- **cmd/importer/README.md:133** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:17:52Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
+
+1. Update the used image
+
+A minimal image containing the importer can be built by
+
+```bash
+make importer-image
+```
+
+Make the created image accessible by your cluster.
+
+Note: Importer images will be available in `gcr.io/k8s-staging-kueue/importer` soon.
+
+And run
+```bash
+(cd cmd/importer/run-in-cluster && kustomize edit set image importer=<image:tag>)
+```
+
+2. Updated the importer args in `cmd/importer/run-in-cluster/importer.yaml`
+3. Update the mapping configuration in `cmd/importer/run-in-cluster/mapping.yaml`
+4. Deploy the configuration:
+
+```bash
+ kubectl apply -k cmd/importer/run-in-cluster/
+```
```

  Add a note that this is configured to run with dry-run=false

- **cmd/importer/README.md:122** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:18:01Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
+
+1. Update the used image
+
+A minimal image containing the importer can be built by
+
+```bash
+make importer-image
+```
+
+Make the created image accessible by your cluster.
+
+Note: Importer images will be available in `gcr.io/k8s-staging-kueue/importer` soon.
+
+And run
+```bash
+(cd cmd/importer/run-in-cluster && kustomize edit set image importer=<image:tag>)
+```
+
+2. Updated the importer args in `cmd/importer/run-in-cluster/importer.yaml`
```

  ```suggestion
  2. Update the importer args in `cmd/importer/run-in-cluster/importer.yaml`
  ```

- **cmd/importer/run-in-cluster/importer.yaml:15** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:18:17Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: v1
+kind: Pod
+metadata:
+  name: kueue-importer
+spec:
+  containers:
+  - name: importer
+    image: importer
+    imagePullPolicy: IfNotPresent
+    args:
+    - import
+    - -n=ns1,ns2
+    - --queuemapping-file=/mapping.yaml
+    - --dry-run=false
+    - -v
```

  ```suggestion
      - -v=2
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-22T15:24:48Z

- **cmd/importer/run-in-cluster/deps.yaml:1** by [@trasc](https://github.com/trasc) — 2024-03-22T15:24:48Z
  I guess we could do it .... but it might be overkill. my initial intent was to just use the kueue-controller-manager's SA.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T15:27:48Z

- **cmd/importer/run-in-cluster/deps.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T15:27:48Z
  could we just reference that or use a `ln`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-22T15:47:32Z

- **Makefile:326** by [@trasc](https://github.com/trasc) — 2024-03-22T15:35:34Z
```diff
@@ -317,6 +317,34 @@ debug-image-push:
 		--platform=$(PLATFORMS) \
 		--push ./hack/debugpod
 
+# Build the importer binary
+.PHONY: importer-build
+importer-build:
+	$(GO_BUILD_ENV) $(GO_CMD) build -ldflags="$(LD_FLAGS)" -o bin/importer cmd/importer/main.go
+
+.PHONY: importer-image-build
+importer-image-build:
```

  we could

- **Makefile:337** by [@trasc](https://github.com/trasc) — 2024-03-22T15:35:59Z
```diff
@@ -317,6 +317,34 @@ debug-image-push:
 		--platform=$(PLATFORMS) \
 		--push ./hack/debugpod
 
+# Build the importer binary
+.PHONY: importer-build
+importer-build:
+	$(GO_BUILD_ENV) $(GO_CMD) build -ldflags="$(LD_FLAGS)" -o bin/importer cmd/importer/main.go
+
+.PHONY: importer-image-build
+importer-image-build:
+	$(IMAGE_BUILD_CMD) \
+		-t $(STAGING_IMAGE_REGISTRY)/importer:$(GIT_TAG) \
+		-t $(STAGING_IMAGE_REGISTRY)/importer:$(RELEASE_BRANCH)-latest \
+		--platform=$(PLATFORMS) \
+		--build-arg BASE_IMAGE=$(BASE_IMAGE) \
+		--build-arg BUILDER_IMAGE=$(BUILDER_IMAGE) \
+		--build-arg CGO_ENABLED=$(CGO_ENABLED) \
+		$(PUSH) \
+		-f ./cmd/importer/Dockerfile ./
+
+# Developers don't need to build this image, as it will be available as gcr.io/k8s-staging-kueue/importer
```

  done

- **cloudbuild.yaml:30** by [@trasc](https://github.com/trasc) — 2024-03-22T15:38:55Z
```diff
@@ -22,6 +22,15 @@ steps:
       - GIT_TAG=$_GIT_TAG
       - EXTRA_TAG=$_PULL_BASE_REF
       - DOCKER_BUILDX_CMD=/buildx-entrypoint
+  - name: 'gcr.io/k8s-staging-test-infra/gcb-docker-gcloud:v20221214-1b4dd4d69a'
+    entrypoint: make
+    args:
+      - importer-image-push
+    env:
+      - IMAGE_REGISTRY=gcr.io/$PROJECT_ID
```

  sounds right

- **cmd/importer/README.md:103** by [@trasc](https://github.com/trasc) — 2024-03-22T15:40:24Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
```

  done

- **cmd/importer/README.md:122** by [@trasc](https://github.com/trasc) — 2024-03-22T15:40:56Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
+
+1. Update the used image
+
+A minimal image containing the importer can be built by
+
+```bash
+make importer-image
+```
+
+Make the created image accessible by your cluster.
+
+Note: Importer images will be available in `gcr.io/k8s-staging-kueue/importer` soon.
+
+And run
+```bash
+(cd cmd/importer/run-in-cluster && kustomize edit set image importer=<image:tag>)
+```
+
+2. Updated the importer args in `cmd/importer/run-in-cluster/importer.yaml`
```

  done

- **cmd/importer/README.md:138** by [@trasc](https://github.com/trasc) — 2024-03-22T15:42:05Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
+
+1. Update the used image
+
+A minimal image containing the importer can be built by
+
+```bash
+make importer-image
+```
+
+Make the created image accessible by your cluster.
+
+Note: Importer images will be available in `gcr.io/k8s-staging-kueue/importer` soon.
+
+And run
+```bash
+(cd cmd/importer/run-in-cluster && kustomize edit set image importer=<image:tag>)
+```
+
+2. Updated the importer args in `cmd/importer/run-in-cluster/importer.yaml`
+3. Update the mapping configuration in `cmd/importer/run-in-cluster/mapping.yaml`
+4. Deploy the configuration:
+
+```bash
+ kubectl apply -k cmd/importer/run-in-cluster/
+```
+
+And check  the logs
+
+```yaml
+kubectl -n kueue-importer logs kueue-importer -f
```

  Let's do it in a cleanup follow-up.

- **cmd/importer/README.md:133** by [@trasc](https://github.com/trasc) — 2024-03-22T15:43:41Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
+
+1. Update the used image
+
+A minimal image containing the importer can be built by
+
+```bash
+make importer-image
+```
+
+Make the created image accessible by your cluster.
+
+Note: Importer images will be available in `gcr.io/k8s-staging-kueue/importer` soon.
+
+And run
+```bash
+(cd cmd/importer/run-in-cluster && kustomize edit set image importer=<image:tag>)
+```
+
+2. Updated the importer args in `cmd/importer/run-in-cluster/importer.yaml`
+3. Update the mapping configuration in `cmd/importer/run-in-cluster/mapping.yaml`
+4. Deploy the configuration:
+
+```bash
+ kubectl apply -k cmd/importer/run-in-cluster/
+```
```

  It will depend on/if the arguments are changed.

- **cmd/importer/run-in-cluster/importer.yaml:2** by [@trasc](https://github.com/trasc) — 2024-03-22T15:45:07Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: v1
+kind: Pod
```

  The pod will most likely fail due to a bad mapping, I don't see a value in retrying it.

- **cmd/importer/run-in-cluster/importer.yaml:15** by [@trasc](https://github.com/trasc) — 2024-03-22T15:45:41Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: v1
+kind: Pod
+metadata:
+  name: kueue-importer
+spec:
+  containers:
+  - name: importer
+    image: importer
+    imagePullPolicy: IfNotPresent
+    args:
+    - import
+    - -n=ns1,ns2
+    - --queuemapping-file=/mapping.yaml
+    - --dry-run=false
+    - -v
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-22T15:49:52Z

- **cmd/importer/run-in-cluster/deps.yaml:1** by [@trasc](https://github.com/trasc) — 2024-03-22T15:49:52Z
  maybe yes but it will be too hacky.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T16:05:43Z

- **cmd/importer/README.md:133** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T16:05:43Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
+
+1. Update the used image
+
+A minimal image containing the importer can be built by
+
+```bash
+make importer-image
+```
+
+Make the created image accessible by your cluster.
+
+Note: Importer images will be available in `gcr.io/k8s-staging-kueue/importer` soon.
+
+And run
+```bash
+(cd cmd/importer/run-in-cluster && kustomize edit set image importer=<image:tag>)
+```
+
+2. Updated the importer args in `cmd/importer/run-in-cluster/importer.yaml`
+3. Update the mapping configuration in `cmd/importer/run-in-cluster/mapping.yaml`
+4. Deploy the configuration:
+
+```bash
+ kubectl apply -k cmd/importer/run-in-cluster/
+```
```

  Right, but we should warn people what the default is.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T16:06:33Z

- **cloudbuild.yaml:30** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T16:06:33Z
```diff
@@ -27,7 +27,7 @@ steps:
     args:
       - importer-image-push
     env:
-      - IMAGE_REGISTRY=gcr.io/$PROJECT_ID
+      - STAGING_IMAGE_REGISTRY=gcr.io/$PROJECT_ID
```

  The Makefile needs to be changed to accept this as a value. It's currently a constant

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T16:07:38Z

- **cmd/importer/run-in-cluster/importer.yaml:15** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-22T16:07:38Z
```diff
@@ -12,7 +12,7 @@ spec:
     - -n=ns1,ns2
     - --queuemapping-file=/mapping.yaml
     - --dry-run=false
-    - -v
+    - -vv
```

  use `=2`. It's easier to understand and modify.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-03-22T17:53:57Z

- **cloudbuild.yaml:30** by [@trasc](https://github.com/trasc) — 2024-03-22T17:52:04Z
```diff
@@ -27,7 +27,7 @@ steps:
     args:
       - importer-image-push
     env:
-      - IMAGE_REGISTRY=gcr.io/$PROJECT_ID
+      - STAGING_IMAGE_REGISTRY=gcr.io/$PROJECT_ID
```

  done

- **cmd/importer/README.md:133** by [@trasc](https://github.com/trasc) — 2024-03-22T17:52:25Z
```diff
@@ -97,3 +97,38 @@ After which, if `--dry-run=false` was specified, for each selected Pod the impor
 
  Will import all the pods in namespace `ns1` or `ns2` having the label `src.lbl` set to `src-val` in LocalQueue `user-queue` regardless of their priorityClassName and those with `src.lbl==src-val2` ,`src2.lbl==src2-val` and `priorityClassName==p-class`in `user-queue2`.
 
+
+#### Run in cluster
+
+`cmd/importer/run-in-cluster` provides the necessary kustomize manifests needed to run the importer from within the cluster, In order to use them you should:
+
+1. Update the used image
+
+A minimal image containing the importer can be built by
+
+```bash
+make importer-image
+```
+
+Make the created image accessible by your cluster.
+
+Note: Importer images will be available in `gcr.io/k8s-staging-kueue/importer` soon.
+
+And run
+```bash
+(cd cmd/importer/run-in-cluster && kustomize edit set image importer=<image:tag>)
+```
+
+2. Updated the importer args in `cmd/importer/run-in-cluster/importer.yaml`
+3. Update the mapping configuration in `cmd/importer/run-in-cluster/mapping.yaml`
+4. Deploy the configuration:
+
+```bash
+ kubectl apply -k cmd/importer/run-in-cluster/
+```
```

  ... done

- **cmd/importer/run-in-cluster/importer.yaml:15** by [@trasc](https://github.com/trasc) — 2024-03-22T17:53:13Z
```diff
@@ -12,7 +12,7 @@ spec:
     - -n=ns1,ns2
     - --queuemapping-file=/mapping.yaml
     - --dry-run=false
-    - -v
+    - -vv
```

  it's not ,  however done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-22T18:10:38Z

/lgtm
/approve
