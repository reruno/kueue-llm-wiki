# PR #191: feat: handle pod overhead

**Summary**: feat: handle pod overhead

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/191

**Last updated**: 2022-04-28T00:42:42Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feature/handle-pod-overhead` → `main`
- **Created**: 2022-04-08T15:59:45Z
- **Updated**: 2022-04-28T00:42:42Z
- **Merged**: 2022-04-27T18:10:12Z
- **Closed**: 2022-04-27T18:10:12Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 8   **+229 / -29**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/119

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-08T15:59:53Z

Hi @kerthcet. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-08T16:15:44Z

cc @alculquicondor please help to check whether this is on the right way. If so, I'll add tests then. Thanks a lot.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-10T02:47:37Z

@denkensk would you kind to add a ok-to-test here.

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-10T14:26:16Z

/ok-to-test

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-11T12:57:53Z

/retest

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-11T13:18:06Z

PTAL @alculquicondor

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-11T14:23:28Z

/retest

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-14T04:57:56Z

Sorry for squashed the commits to help solve the huge conflicts.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-26T02:09:58Z

Addressed comments and left some for further discussing.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-26T15:19:18Z

Addressed @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-26T18:14:51Z

/approve

Please squash

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-26T18:15:06Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/191#issuecomment-1110106812" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/191#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-27T01:10:27Z

squashed.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-27T18:09:48Z

/lgtm

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T16:58:15Z

- **pkg/controller/core/workload_controller.go:274** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T16:58:01Z
```diff
@@ -252,3 +266,27 @@ func workloadFinished(w *kueue.Workload) bool {
 	}
 	return false
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+func handlePodOverhead(wl *kueue.Workload, classes nodev1.RuntimeClassList) {
+	for _, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			for _, item := range classes.Items {
```

  It might be faster to just do a Get for each podset (in general there would be 1 or 2 podsets)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-08T16:59:33Z

- **pkg/controller/core/workload_controller.go:281** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-08T16:59:31Z
```diff
@@ -252,3 +266,27 @@ func workloadFinished(w *kueue.Workload) bool {
 	}
 	return false
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+func handlePodOverhead(wl *kueue.Workload, classes nodev1.RuntimeClassList) {
+	for _, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			for _, item := range classes.Items {
+				if item.Name == *pod.Spec.RuntimeClassName && item.Overhead != nil {
+					pod.Spec.Overhead = item.Overhead.PodFixed
+					break
+				}
+			}
+		} else if pod.Spec.RuntimeClassName == nil && len(pod.Spec.Overhead) > 0 {
+			pod.Spec.Overhead = make(corev1.ResourceList)
```

  This is modifying the object from the informer Cache. Maybe consider doing it in `NewInfo`?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-09T02:01:38Z

- **pkg/controller/core/workload_controller.go:274** by [@kerthcet](https://github.com/kerthcet) — 2022-04-09T02:01:38Z
```diff
@@ -252,3 +266,27 @@ func workloadFinished(w *kueue.Workload) bool {
 	}
 	return false
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+func handlePodOverhead(wl *kueue.Workload, classes nodev1.RuntimeClassList) {
+	for _, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			for _, item := range classes.Items {
```

  make sense.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-09T07:29:00Z

- **pkg/controller/core/workload_controller.go:281** by [@kerthcet](https://github.com/kerthcet) — 2022-04-09T07:28:59Z
```diff
@@ -252,3 +266,27 @@ func workloadFinished(w *kueue.Workload) bool {
 	}
 	return false
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+func handlePodOverhead(wl *kueue.Workload, classes nodev1.RuntimeClassList) {
+	for _, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			for _, item := range classes.Items {
+				if item.Name == *pod.Spec.RuntimeClassName && item.Overhead != nil {
+					pod.Spec.Overhead = item.Overhead.PodFixed
+					break
+				}
+			}
+		} else if pod.Spec.RuntimeClassName == nil && len(pod.Spec.Overhead) > 0 {
+			pod.Spec.Overhead = make(corev1.ResourceList)
```

  Oh yes, you're right. In this case, pod creation will be rejected original, but now it seems legally.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-13T14:30:57Z

- **pkg/controller/core/workload_controller.go:180** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T14:30:57Z
```diff
@@ -172,6 +176,9 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	}
 	log.V(2).Info("Workload update event")
 
+	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
+	handlePodOverhead(wl, r.client)
```

  This is still modifying the object coming from the event.
  
  Notice that there is a `DeepCopy` below. You should modify that object.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T15:33:37Z

- **pkg/controller/core/workload_controller.go:180** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T15:33:37Z
```diff
@@ -172,6 +176,9 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	}
 	log.V(2).Info("Workload update event")
 
+	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
+	handlePodOverhead(wl, r.client)
```

  thanks.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-13T18:12:39Z

- **pkg/controller/core/workload_controller.go:110** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T17:55:09Z
```diff
@@ -105,13 +107,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 		return true
 	}
 
+	copiedWl := wl.DeepCopy()
```

  nit: I slightly prefer `wlCopy`

- **pkg/controller/core/workload_controller.go:268** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T17:58:37Z
```diff
@@ -252,3 +261,26 @@ func workloadFinished(w *kueue.Workload) bool {
 	}
 	return false
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+// As a result, the pod's Overhead is not always correct. E.g. we set a non-exist runtime class name to
+// `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, in real world, the pod creation will be
+// rejected as the dismatch with RuntimeClass, but we will still assume they are correct here.
```

  ```suggestion
  // As a result, the pod's Overhead is not always correct. E.g. if we set a non-existent runtime class name to
  // `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, the pod creation will be
  // rejected due to the mismatch with RuntimeClass. However, in this function we assume that they are correct.
  ```

- **pkg/controller/core/workload_controller.go:272** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T17:59:16Z
```diff
@@ -252,3 +261,26 @@ func workloadFinished(w *kueue.Workload) bool {
 	}
 	return false
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+// As a result, the pod's Overhead is not always correct. E.g. we set a non-exist runtime class name to
+// `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, in real world, the pod creation will be
+// rejected as the dismatch with RuntimeClass, but we will still assume they are correct here.
+func handlePodOverhead(wl *kueue.Workload, c client.Client) {
+	for i, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			var runtimeClass nodev1.RuntimeClass
```

  Did you add permissions for the controller to access the runtimeclass?

- **pkg/controller/core/workload_controller.go:273** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T17:59:42Z
```diff
@@ -252,3 +261,26 @@ func workloadFinished(w *kueue.Workload) bool {
 	}
 	return false
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+// As a result, the pod's Overhead is not always correct. E.g. we set a non-exist runtime class name to
+// `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, in real world, the pod creation will be
+// rejected as the dismatch with RuntimeClass, but we will still assume they are correct here.
+func handlePodOverhead(wl *kueue.Workload, c client.Client) {
+	for i, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			var runtimeClass nodev1.RuntimeClass
+			if err := c.Get(context.TODO(), types.NamespacedName{Name: *pod.Spec.RuntimeClassName}, &runtimeClass); err != nil {
```

  use a single context for both calls. And make it `context.Background`

- **test/integration/framework/framework.go:147** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T18:05:55Z
```diff
@@ -134,6 +135,24 @@ func DeleteNamespace(ctx context.Context, c client.Client, ns *corev1.Namespace)
 	return nil
 }
 
+func DeleteRuntimeClass(ctx context.Context, c client.Client, runtimeClass *nodev1.RuntimeClass) error {
+	if runtimeClass != nil {
+		if err := c.Delete(ctx, runtimeClass); err != nil && !apierrors.IsNotFound(err) {
+			return err
+		}
+	}
+	return nil
+}
+
+func DeleteWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) error {
```

  This is already part of `DeleteNamespace`

- **test/integration/controller/core/workload_controller_test.go:129** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T18:08:41Z
```diff
@@ -105,4 +107,150 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("Workload with RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			resources := corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			}
+			runtimeClass = testing.MakeRuntimeClass("kata", "bar-handler").PodOverhead(resources).Obj()
+			gomega.Expect(k8sClient.Create(ctx, runtimeClass)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteWorkload(ctx, k8sClient, wl)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
```

  already part of `DeleteNamespace`

- **test/integration/controller/core/workload_controller_test.go:206** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T18:10:28Z
```diff
@@ -105,4 +107,150 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("Workload with RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			resources := corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			}
+			runtimeClass = testing.MakeRuntimeClass("kata", "bar-handler").PodOverhead(resources).Obj()
+			gomega.Expect(k8sClient.Create(ctx, runtimeClass)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteWorkload(ctx, k8sClient, wl)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
+				Queue(queue.Name).
+				Request(corev1.ResourceCPU, "1").
+				Admit(testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+				RuntimeClass("kata").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Got ClusterQueueStatus")
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCQ)).To(gomega.Succeed())
+				return updatedCQ.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				PendingWorkloads:  0,
+				AdmittedWorkloads: 1,
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						flavorOnDemand: {
+							Total:    pointer.Quantity(resource.MustParse("2")),
+							Borrowed: nil,
+						},
+					},
+				},
+			}))
+		})
+	})
+
+	ginkgo.When("Workload with non-exists RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteWorkload(ctx, k8sClient, wl)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
```

  The test is so small that it should be fine to not have `By` calls.

- **test/integration/controller/core/workload_controller_test.go:163** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T18:12:37Z
```diff
@@ -105,4 +107,150 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("Workload with RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			resources := corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			}
+			runtimeClass = testing.MakeRuntimeClass("kata", "bar-handler").PodOverhead(resources).Obj()
+			gomega.Expect(k8sClient.Create(ctx, runtimeClass)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteWorkload(ctx, k8sClient, wl)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
```

  Make you can create the workload using `JustBeforeEach` https://onsi.github.io/ginkgo/#separating-creation-and-configuration-justbeforeeach

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-14T03:25:20Z

- **pkg/controller/core/workload_controller.go:272** by [@kerthcet](https://github.com/kerthcet) — 2022-04-14T03:25:19Z
```diff
@@ -252,3 +261,26 @@ func workloadFinished(w *kueue.Workload) bool {
 	}
 	return false
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+// As a result, the pod's Overhead is not always correct. E.g. we set a non-exist runtime class name to
+// `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, in real world, the pod creation will be
+// rejected as the dismatch with RuntimeClass, but we will still assume they are correct here.
+func handlePodOverhead(wl *kueue.Workload, c client.Client) {
+	for i, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			var runtimeClass nodev1.RuntimeClass
```

  Haven't notice this. Added.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-14T03:25:28Z

- **test/integration/framework/framework.go:147** by [@kerthcet](https://github.com/kerthcet) — 2022-04-14T03:25:27Z
```diff
@@ -134,6 +135,24 @@ func DeleteNamespace(ctx context.Context, c client.Client, ns *corev1.Namespace)
 	return nil
 }
 
+func DeleteRuntimeClass(ctx context.Context, c client.Client, runtimeClass *nodev1.RuntimeClass) error {
+	if runtimeClass != nil {
+		if err := c.Delete(ctx, runtimeClass); err != nil && !apierrors.IsNotFound(err) {
+			return err
+		}
+	}
+	return nil
+}
+
+func DeleteWorkload(ctx context.Context, c client.Client, wl *kueue.Workload) error {
```

  Deleted.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-14T03:26:02Z

- **test/integration/controller/core/workload_controller_test.go:206** by [@kerthcet](https://github.com/kerthcet) — 2022-04-14T03:26:02Z
```diff
@@ -105,4 +107,150 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("Workload with RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			resources := corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			}
+			runtimeClass = testing.MakeRuntimeClass("kata", "bar-handler").PodOverhead(resources).Obj()
+			gomega.Expect(k8sClient.Create(ctx, runtimeClass)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteWorkload(ctx, k8sClient, wl)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
+				Queue(queue.Name).
+				Request(corev1.ResourceCPU, "1").
+				Admit(testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+				RuntimeClass("kata").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Got ClusterQueueStatus")
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCQ)).To(gomega.Succeed())
+				return updatedCQ.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				PendingWorkloads:  0,
+				AdmittedWorkloads: 1,
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						flavorOnDemand: {
+							Total:    pointer.Quantity(resource.MustParse("2")),
+							Borrowed: nil,
+						},
+					},
+				},
+			}))
+		})
+	})
+
+	ginkgo.When("Workload with non-exists RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteWorkload(ctx, k8sClient, wl)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
```

  Prefer to leave it behind. More clear to read the tests.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-14T03:26:13Z

- **test/integration/controller/core/workload_controller_test.go:163** by [@kerthcet](https://github.com/kerthcet) — 2022-04-14T03:26:13Z
```diff
@@ -105,4 +107,150 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("Workload with RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			resources := corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("1"),
+			}
+			runtimeClass = testing.MakeRuntimeClass("kata", "bar-handler").PodOverhead(resources).Obj()
+			gomega.Expect(k8sClient.Create(ctx, runtimeClass)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteWorkload(ctx, k8sClient, wl)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
```

  New skill got, thanks for sharing. But considering workloads' configurations in this node are quite different, especially with the former ones, I just don't want to introduce more complexities.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-14T05:03:05Z

- **pkg/controller/core/workload_controller.go:110** by [@kerthcet](https://github.com/kerthcet) — 2022-04-14T05:03:05Z
```diff
@@ -105,13 +107,16 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 		return true
 	}
 
+	copiedWl := wl.DeepCopy()
```

  yes!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-25T14:54:40Z

- **pkg/controller/core/workload_controller.go:274** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T14:46:17Z
```diff
@@ -249,3 +259,28 @@ func workloadStatus(w *kueue.Workload) string {
 	}
 	return pending
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+// As a result, the pod's Overhead is not always correct. E.g. if we set a non-existent runtime class name to
+// `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, in real world, the pod creation will be
+// rejected due to the mismatch with RuntimeClass. However, in the future we assume that they are correct.
+func handlePodOverhead(wl *kueue.Workload, c client.Client) {
+	ctx := context.Background()
+
+	for i, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			var runtimeClass nodev1.RuntimeClass
+			if err := c.Get(ctx, types.NamespacedName{Name: *pod.Spec.RuntimeClassName}, &runtimeClass); err != nil {
+				klog.Error(err, "failed to get RuntimeClass")
```

  ```suggestion
  				klog.Error(err, "Could not get RuntimeClass")
  ```

- **pkg/controller/core/workload_controller.go:281** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T14:48:02Z
```diff
@@ -249,3 +259,28 @@ func workloadStatus(w *kueue.Workload) string {
 	}
 	return pending
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+// As a result, the pod's Overhead is not always correct. E.g. if we set a non-existent runtime class name to
+// `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, in real world, the pod creation will be
+// rejected due to the mismatch with RuntimeClass. However, in the future we assume that they are correct.
+func handlePodOverhead(wl *kueue.Workload, c client.Client) {
+	ctx := context.Background()
+
+	for i, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			var runtimeClass nodev1.RuntimeClass
+			if err := c.Get(ctx, types.NamespacedName{Name: *pod.Spec.RuntimeClassName}, &runtimeClass); err != nil {
+				klog.Error(err, "failed to get RuntimeClass")
+				continue
+			}
+			if runtimeClass.Overhead != nil {
+				wl.Spec.PodSets[i].Spec.Overhead = runtimeClass.Overhead.PodFixed
+			}
+		} else if pod.Spec.RuntimeClassName == nil && len(pod.Spec.Overhead) > 0 {
+			// In real world, pod creation will be rejected for setting Pod Overhead without corresponding RuntimeClass,
```

  Why do this here?
  
  Maybe just leave it as is and let the pod creation fail.

- **pkg/util/testing/wrappers.go:202** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T14:49:13Z
```diff
@@ -191,6 +192,20 @@ func (w *WorkloadWrapper) PriorityClass(priorityClassName string) *WorkloadWrapp
 	return w
 }
 
+func (w *WorkloadWrapper) RuntimeClass(name string) *WorkloadWrapper {
+	if len(w.Spec.PodSets) > 0 {
+		w.Spec.PodSets[0].Spec.RuntimeClassName = &name
+	}
+	return w
+}
+
+func (w *WorkloadWrapper) Overhead(resources corev1.ResourceList) *WorkloadWrapper {
```

  Remove this if it's not being used.

- **pkg/util/testing/wrappers.go:197** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T14:49:28Z
```diff
@@ -191,6 +192,20 @@ func (w *WorkloadWrapper) PriorityClass(priorityClassName string) *WorkloadWrapp
 	return w
 }
 
+func (w *WorkloadWrapper) RuntimeClass(name string) *WorkloadWrapper {
+	if len(w.Spec.PodSets) > 0 {
+		w.Spec.PodSets[0].Spec.RuntimeClassName = &name
```

  Should we add it to all Podsets?

- **pkg/util/testing/wrappers.go:375** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T14:49:59Z
```diff
@@ -359,3 +374,29 @@ func (rf *ResourceFlavorWrapper) Taint(t corev1.Taint) *ResourceFlavorWrapper {
 	rf.Taints = append(rf.Taints, t)
 	return rf
 }
+
+// RuntimeClassWrapper wraps a RuntimeClass.
+type RuntimeClassWrapper struct{ nodev1.RuntimeClass }
+
+// MakeRuntimeClass creates a wrapper for a Runtime.
+func MakeRuntimeClass(name, handler string) *RuntimeClassWrapper {
```

  Is the handler required?

- **test/integration/controller/core/workload_controller_test.go:191** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T14:53:27Z
```diff
@@ -142,4 +140,141 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
 		})
 	})
+
+	ginkgo.When("Workload with RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			runtimeClass = testing.MakeRuntimeClass("kata", "bar-handler").PodOverhead(resources).Obj()
+			gomega.Expect(k8sClient.Create(ctx, runtimeClass)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
+				Queue(queue.Name).
+				Request(corev1.ResourceCPU, "1").
+				Admit(testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+				RuntimeClass("kata").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Got ClusterQueueStatus")
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCQ)).To(gomega.Succeed())
+				return updatedCQ.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				PendingWorkloads:  0,
+				AdmittedWorkloads: 1,
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						flavorOnDemand: {
+							Total:    pointer.Quantity(resource.MustParse("2")),
+							Borrowed: nil,
+						},
+					},
+				},
+			}))
+		})
+	})
+
+	ginkgo.When("Workload with non-exists RuntimeClass defined", func() {
```

  ```suggestion
  	ginkgo.When("Workload with non-existent RuntimeClass defined", func() {
  ```

- **test/integration/controller/core/workload_controller_test.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T14:54:28Z
```diff
@@ -142,4 +140,141 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
 		})
 	})
+
+	ginkgo.When("Workload with RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			runtimeClass = testing.MakeRuntimeClass("kata", "bar-handler").PodOverhead(resources).Obj()
+			gomega.Expect(k8sClient.Create(ctx, runtimeClass)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
+				Queue(queue.Name).
+				Request(corev1.ResourceCPU, "1").
+				Admit(testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+				RuntimeClass("kata").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Got ClusterQueueStatus")
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCQ)).To(gomega.Succeed())
+				return updatedCQ.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				PendingWorkloads:  0,
+				AdmittedWorkloads: 1,
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						flavorOnDemand: {
+							Total:    pointer.Quantity(resource.MustParse("2")),
+							Borrowed: nil,
+						},
+					},
+				},
+			}))
+		})
+	})
+
+	ginkgo.When("Workload with non-exists RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
+				Queue(queue.Name).
+				Request(corev1.ResourceCPU, "1").
+				Admit(testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+				RuntimeClass("kata").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Got ClusterQueueStatus")
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCQ)).To(gomega.Succeed())
+				return updatedCQ.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				PendingWorkloads:  0,
+				AdmittedWorkloads: 1,
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						flavorOnDemand: {
+							Total:    pointer.Quantity(resource.MustParse("1")),
+							Borrowed: nil,
+						},
+					},
+				},
+			}))
+		})
+	})
+
+	ginkgo.When("workload with PodOverhead defined, RuntimeClass undefined", func() {
```

  I would just remove this test.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-25T15:33:10Z

- **pkg/controller/core/workload_controller.go:281** by [@kerthcet](https://github.com/kerthcet) — 2022-04-25T15:23:41Z
```diff
@@ -249,3 +259,28 @@ func workloadStatus(w *kueue.Workload) string {
 	}
 	return pending
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+// As a result, the pod's Overhead is not always correct. E.g. if we set a non-existent runtime class name to
+// `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, in real world, the pod creation will be
+// rejected due to the mismatch with RuntimeClass. However, in the future we assume that they are correct.
+func handlePodOverhead(wl *kueue.Workload, c client.Client) {
+	ctx := context.Background()
+
+	for i, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			var runtimeClass nodev1.RuntimeClass
+			if err := c.Get(ctx, types.NamespacedName{Name: *pod.Spec.RuntimeClassName}, &runtimeClass); err != nil {
+				klog.Error(err, "failed to get RuntimeClass")
+				continue
+			}
+			if runtimeClass.Overhead != nil {
+				wl.Spec.PodSets[i].Spec.Overhead = runtimeClass.Overhead.PodFixed
+			}
+		} else if pod.Spec.RuntimeClassName == nil && len(pod.Spec.Overhead) > 0 {
+			// In real world, pod creation will be rejected for setting Pod Overhead without corresponding RuntimeClass,
```

  We just want to calculate more accurately, pod here is just a copied obj, will not change the origin behavior(reject pod creation).

- **pkg/util/testing/wrappers.go:375** by [@kerthcet](https://github.com/kerthcet) — 2022-04-25T15:31:01Z
```diff
@@ -359,3 +374,29 @@ func (rf *ResourceFlavorWrapper) Taint(t corev1.Taint) *ResourceFlavorWrapper {
 	rf.Taints = append(rf.Taints, t)
 	return rf
 }
+
+// RuntimeClassWrapper wraps a RuntimeClass.
+type RuntimeClassWrapper struct{ nodev1.RuntimeClass }
+
+// MakeRuntimeClass creates a wrapper for a Runtime.
+func MakeRuntimeClass(name, handler string) *RuntimeClassWrapper {
```

  Yes, an empty handler will be rejected in creation.

- **test/integration/controller/core/workload_controller_test.go:235** by [@kerthcet](https://github.com/kerthcet) — 2022-04-25T15:33:07Z
```diff
@@ -142,4 +140,141 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
 		})
 	})
+
+	ginkgo.When("Workload with RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			runtimeClass = testing.MakeRuntimeClass("kata", "bar-handler").PodOverhead(resources).Obj()
+			gomega.Expect(k8sClient.Create(ctx, runtimeClass)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
+				Queue(queue.Name).
+				Request(corev1.ResourceCPU, "1").
+				Admit(testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+				RuntimeClass("kata").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Got ClusterQueueStatus")
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCQ)).To(gomega.Succeed())
+				return updatedCQ.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				PendingWorkloads:  0,
+				AdmittedWorkloads: 1,
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						flavorOnDemand: {
+							Total:    pointer.Quantity(resource.MustParse("2")),
+							Borrowed: nil,
+						},
+					},
+				},
+			}))
+		})
+	})
+
+	ginkgo.When("Workload with non-exists RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
+				Queue(queue.Name).
+				Request(corev1.ResourceCPU, "1").
+				Admit(testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+				RuntimeClass("kata").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Got ClusterQueueStatus")
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCQ)).To(gomega.Succeed())
+				return updatedCQ.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				PendingWorkloads:  0,
+				AdmittedWorkloads: 1,
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						flavorOnDemand: {
+							Total:    pointer.Quantity(resource.MustParse("1")),
+							Borrowed: nil,
+						},
+					},
+				},
+			}))
+		})
+	})
+
+	ginkgo.When("workload with PodOverhead defined, RuntimeClass undefined", func() {
```

  It was used to cover this snippet https://github.com/kubernetes-sigs/kueue/pull/191/files#r857714581

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-25T15:50:33Z

- **pkg/util/testing/wrappers.go:197** by [@kerthcet](https://github.com/kerthcet) — 2022-04-25T15:50:33Z
```diff
@@ -191,6 +192,20 @@ func (w *WorkloadWrapper) PriorityClass(priorityClassName string) *WorkloadWrapp
 	return w
 }
 
+func (w *WorkloadWrapper) RuntimeClass(name string) *WorkloadWrapper {
+	if len(w.Spec.PodSets) > 0 {
+		w.Spec.PodSets[0].Spec.RuntimeClassName = &name
```

  good catch.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-26T02:06:58Z

- **pkg/util/testing/wrappers.go:202** by [@kerthcet](https://github.com/kerthcet) — 2022-04-26T02:06:58Z
```diff
@@ -191,6 +192,20 @@ func (w *WorkloadWrapper) PriorityClass(priorityClassName string) *WorkloadWrapp
 	return w
 }
 
+func (w *WorkloadWrapper) RuntimeClass(name string) *WorkloadWrapper {
+	if len(w.Spec.PodSets) > 0 {
+		w.Spec.PodSets[0].Spec.RuntimeClassName = &name
+	}
+	return w
+}
+
+func (w *WorkloadWrapper) Overhead(resources corev1.ResourceList) *WorkloadWrapper {
```

  It's actually uesd.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-26T14:57:58Z

- **pkg/controller/core/workload_controller.go:281** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-26T14:54:56Z
```diff
@@ -249,3 +259,28 @@ func workloadStatus(w *kueue.Workload) string {
 	}
 	return pending
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+// As a result, the pod's Overhead is not always correct. E.g. if we set a non-existent runtime class name to
+// `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, in real world, the pod creation will be
+// rejected due to the mismatch with RuntimeClass. However, in the future we assume that they are correct.
+func handlePodOverhead(wl *kueue.Workload, c client.Client) {
+	ctx := context.Background()
+
+	for i, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			var runtimeClass nodev1.RuntimeClass
+			if err := c.Get(ctx, types.NamespacedName{Name: *pod.Spec.RuntimeClassName}, &runtimeClass); err != nil {
+				klog.Error(err, "failed to get RuntimeClass")
+				continue
+			}
+			if runtimeClass.Overhead != nil {
+				wl.Spec.PodSets[i].Spec.Overhead = runtimeClass.Overhead.PodFixed
+			}
+		} else if pod.Spec.RuntimeClassName == nil && len(pod.Spec.Overhead) > 0 {
+			// In real world, pod creation will be rejected for setting Pod Overhead without corresponding RuntimeClass,
```

  But this shouldn't happen, so we might as well not have code (and tests) for this.

- **pkg/util/testing/wrappers.go:202** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-26T14:56:40Z
```diff
@@ -191,6 +192,20 @@ func (w *WorkloadWrapper) PriorityClass(priorityClassName string) *WorkloadWrapp
 	return w
 }
 
+func (w *WorkloadWrapper) RuntimeClass(name string) *WorkloadWrapper {
+	if len(w.Spec.PodSets) > 0 {
+		w.Spec.PodSets[0].Spec.RuntimeClassName = &name
+	}
+	return w
+}
+
+func (w *WorkloadWrapper) Overhead(resources corev1.ResourceList) *WorkloadWrapper {
```

  I can't find any usage.

- **test/integration/controller/core/workload_controller_test.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-26T14:57:52Z
```diff
@@ -142,4 +140,141 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
 		})
 	})
+
+	ginkgo.When("Workload with RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			runtimeClass = testing.MakeRuntimeClass("kata", "bar-handler").PodOverhead(resources).Obj()
+			gomega.Expect(k8sClient.Create(ctx, runtimeClass)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
+				Queue(queue.Name).
+				Request(corev1.ResourceCPU, "1").
+				Admit(testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+				RuntimeClass("kata").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Got ClusterQueueStatus")
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCQ)).To(gomega.Succeed())
+				return updatedCQ.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				PendingWorkloads:  0,
+				AdmittedWorkloads: 1,
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						flavorOnDemand: {
+							Total:    pointer.Quantity(resource.MustParse("2")),
+							Borrowed: nil,
+						},
+					},
+				},
+			}))
+		})
+	})
+
+	ginkgo.When("Workload with non-exists RuntimeClass defined", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not accumulate RuntimeClass's overhead", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).
+				Queue(queue.Name).
+				Request(corev1.ResourceCPU, "1").
+				Admit(testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()).
+				RuntimeClass("kata").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Got ClusterQueueStatus")
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), &updatedCQ)).To(gomega.Succeed())
+				return updatedCQ.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				PendingWorkloads:  0,
+				AdmittedWorkloads: 1,
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						flavorOnDemand: {
+							Total:    pointer.Quantity(resource.MustParse("1")),
+							Borrowed: nil,
+						},
+					},
+				},
+			}))
+		})
+	})
+
+	ginkgo.When("workload with PodOverhead defined, RuntimeClass undefined", func() {
```

  That's why we should remove it :)
  
  It's just non-useful code we shouldn't have to maintain.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-26T15:17:30Z

- **pkg/util/testing/wrappers.go:202** by [@kerthcet](https://github.com/kerthcet) — 2022-04-26T15:12:35Z
```diff
@@ -191,6 +192,20 @@ func (w *WorkloadWrapper) PriorityClass(priorityClassName string) *WorkloadWrapp
 	return w
 }
 
+func (w *WorkloadWrapper) RuntimeClass(name string) *WorkloadWrapper {
+	if len(w.Spec.PodSets) > 0 {
+		w.Spec.PodSets[0].Spec.RuntimeClassName = &name
+	}
+	return w
+}
+
+func (w *WorkloadWrapper) Overhead(resources corev1.ResourceList) *WorkloadWrapper {
```

  Here's  where it locates at , but since I'll remove this test,  I'll remove this also. https://github.com/kubernetes-sigs/kueue/blob/342578788b7bb20941b80d6959e2acee993f1570/test/integration/controller/core/workload_controller_test.go#L249-L259

- **pkg/controller/core/workload_controller.go:281** by [@kerthcet](https://github.com/kerthcet) — 2022-04-26T15:16:45Z
```diff
@@ -249,3 +259,28 @@ func workloadStatus(w *kueue.Workload) string {
 	}
 	return pending
 }
+
+// We do not verify Pod's RuntimeClass legality here as this will be performed in admission controller.
+// As a result, the pod's Overhead is not always correct. E.g. if we set a non-existent runtime class name to
+// `pod.Spec.RuntimeClassName` and we also set the `pod.Spec.Overhead`, in real world, the pod creation will be
+// rejected due to the mismatch with RuntimeClass. However, in the future we assume that they are correct.
+func handlePodOverhead(wl *kueue.Workload, c client.Client) {
+	ctx := context.Background()
+
+	for i, pod := range wl.Spec.PodSets {
+		if pod.Spec.RuntimeClassName != nil && len(pod.Spec.Overhead) == 0 {
+			var runtimeClass nodev1.RuntimeClass
+			if err := c.Get(ctx, types.NamespacedName{Name: *pod.Spec.RuntimeClassName}, &runtimeClass); err != nil {
+				klog.Error(err, "failed to get RuntimeClass")
+				continue
+			}
+			if runtimeClass.Overhead != nil {
+				wl.Spec.PodSets[i].Spec.Overhead = runtimeClass.Overhead.PodFixed
+			}
+		} else if pod.Spec.RuntimeClassName == nil && len(pod.Spec.Overhead) > 0 {
+			// In real world, pod creation will be rejected for setting Pod Overhead without corresponding RuntimeClass,
```

  Yes, normally, users will not set the `Overhead` but leave the `pod.Spec.RuntimeClassName` empty. I'll modify it as you advised.
