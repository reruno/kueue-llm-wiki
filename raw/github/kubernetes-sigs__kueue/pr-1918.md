# PR #1918: Update ProvisioningRequest documentation by adding a new way of passing Parameters to ProvisioningRequest

**Summary**: Update ProvisioningRequest documentation by adding a new way of passing Parameters to ProvisioningRequest

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1918

**Last updated**: 2024-04-26T09:54:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `provreq-doc-update` → `main`
- **Created**: 2024-03-26T15:37:41Z
- **Updated**: 2024-04-26T09:54:13Z
- **Merged**: 2024-03-28T14:54:46Z
- **Closed**: 2024-03-28T14:54:46Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 2   **+24 / -4**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1788

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-03-26T15:37:51Z

/assign @alculquicondor

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-03-26T15:37:57Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 28c58cbb6a949e700d6f07dbacd2cb94858eee23 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6605652373b2840008cfe467 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1918--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1918--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE5MTgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.FgZ9wDPP5zg4WKW6MSJWnTBfQ--LC8p9KoU0aQWNBdk)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-28T14:35:06Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-28T14:35:11Z

LGTM label has been added.  <details>Git tree hash: 3e77b8af5c4791505d0b44d57be4d3905f99775c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-28T14:35:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1918#issuecomment-2025367000" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1918#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-26T15:42:55Z

- **site/content/en/docs/admission-check-controllers/provisioning.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-26T15:40:50Z
```diff
@@ -14,6 +14,7 @@ The Provisioning Admission Check Controller is supported on [Kubernetes cluster-
 
```

  It looks like we are missing a section that explains how to configure the ProvisioningRequest into a ClusterQueue.

- **site/content/en/docs/admission-check-controllers/provisioning.md:15** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-26T15:42:50Z
```diff
@@ -14,6 +14,7 @@ The Provisioning Admission Check Controller is supported on [Kubernetes cluster-
 
 ## Parameters
```

  Add a preamble along this lines:
  
  There are two ways to configure the parameters to be sent to a ProvisioningRequest for your Jobs. You can configure via `ProvisioningRequestConfig` to set parameters to all jobs that land(are assigned?) on it. Or you can configure it per job using annotations.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-27T14:35:32Z

We use "job" when referring to jobs generically. We use Job when referring to the k8s Job.

We might say Kubernetes Job or batch/v1 Job when we really want to refer to them specifically.

- **site/content/en/docs/admission-check-controllers/provisioning.md:17** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-27T14:30:58Z
```diff
@@ -6,14 +6,24 @@ description: >
   An admission check controller providing kueue integration with cluster autoscaler.
 ---
 
-The Provisioning Admission Check Controller is an Admission Check Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
+The Provisioning AdmissionCheck Controller is an AdmissionCheck Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
 
-The controller is part of kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+The controller is part of Kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
 
 The Provisioning Admission Check Controller is supported on [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) versions 1.29 and later. However, some cloud-providers may not have an implementation for it.
 
-## Parameters
+## Usage
 
+To use the Provisioning AdmissionCheck create an AdmissionCheck with `kueue.x-k8s.io/provisioning-request` as a `.spec.controllerName` and provide ProvisioningRequest configuration with `ProvisioningRequestConfig` as `.spec.parameters`. See an example below.
```

  ```suggestion
  To use the Provisioning AdmissionCheck, create an [AdmissionCheck](docs/concepts/admission_check)
  with `kueue.x-k8s.io/provisioning-request` as a `.spec.controllerName` and create a ProvisioningRequest configuration usign a `ProvisioningRequestConfig` object. See an example below.
  ```

- **site/content/en/docs/admission-check-controllers/provisioning.md:23** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-27T14:31:52Z
```diff
@@ -6,14 +6,24 @@ description: >
   An admission check controller providing kueue integration with cluster autoscaler.
 ---
 
-The Provisioning Admission Check Controller is an Admission Check Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
+The Provisioning AdmissionCheck Controller is an AdmissionCheck Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
 
-The controller is part of kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+The controller is part of Kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
 
 The Provisioning Admission Check Controller is supported on [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) versions 1.29 and later. However, some cloud-providers may not have an implementation for it.
 
-## Parameters
+## Usage
 
+To use the Provisioning AdmissionCheck create an AdmissionCheck with `kueue.x-k8s.io/provisioning-request` as a `.spec.controllerName` and provide ProvisioningRequest configuration with `ProvisioningRequestConfig` as `.spec.parameters`. See an example below.
+
+## ProvisioningRequest configuration
+
+There are two ways to configure ProvisioningRequest sent for your Jobs.
+
+- **ProvisioningRequestConfig:** This configuration in the AdmissionCheck applies to all Jobs it validates. It enables you to set `ProvisioningClassName`, `managedResources`, and `Parameters`
```

  ```suggestion
  - **ProvisioningRequestConfig:** This configuration in the AdmissionCheck applies to all the jobs that go through this check.
  It enables you to set `provisioningClassName`, `managedResources`, and `parameters`
  ```

- **site/content/en/docs/admission-check-controllers/provisioning.md:24** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-27T14:32:08Z
```diff
@@ -6,14 +6,24 @@ description: >
   An admission check controller providing kueue integration with cluster autoscaler.
 ---
 
-The Provisioning Admission Check Controller is an Admission Check Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
+The Provisioning AdmissionCheck Controller is an AdmissionCheck Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
 
-The controller is part of kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+The controller is part of Kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
 
 The Provisioning Admission Check Controller is supported on [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) versions 1.29 and later. However, some cloud-providers may not have an implementation for it.
 
-## Parameters
+## Usage
 
+To use the Provisioning AdmissionCheck create an AdmissionCheck with `kueue.x-k8s.io/provisioning-request` as a `.spec.controllerName` and provide ProvisioningRequest configuration with `ProvisioningRequestConfig` as `.spec.parameters`. See an example below.
+
+## ProvisioningRequest configuration
+
+There are two ways to configure ProvisioningRequest sent for your Jobs.
+
+- **ProvisioningRequestConfig:** This configuration in the AdmissionCheck applies to all Jobs it validates. It enables you to set `ProvisioningClassName`, `managedResources`, and `Parameters`
+- **Job annotation**: This configuration applies only to a specific Job. It enables you to set `Parameters`, and if both the annotation and the ProvisioningRequestConfig refer same one, annotation overrides it.
```

  ```suggestion
  - **Job annotation**: This configuration applies only to a specific job. It enables you to set `parameters`, and if both the annotation and the ProvisioningRequestConfig refer same one, annotation overrides it.
  ```

- **site/content/en/docs/admission-check-controllers/provisioning.md:48** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-27T14:33:25Z
```diff
@@ -33,6 +43,12 @@ Where:
 
 Check the [API definition](https://github.com/kubernetes-sigs/kueue/blob/main/apis/kueue/v1beta1/provisioningrequestconfig_types.go) for more details.
 
+### Job annotation
+
+Another way to pass ProvisioningRequest's [Parameters](https://github.com/kubernetes/autoscaler/blob/0130d33747bb329b790ccb6e8962eedb6ffdd0a8/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1/types.go#L115) is by using Job annotations. Every annotation with the ***provreq.kueue.x-k8s.io/*** prefix will be directly passed to created ProvisioningRequest. E.g. `provreq.kueue.x-k8s.io/ValidUntilSeconds: "60"` will pass `ValidUntilSeconds` parameter with the value of `60`. See more examples below.
```

  ```suggestion
  Another way to pass ProvisioningRequest's [parameters](https://github.com/kubernetes/autoscaler/blob/0130d33747bb329b790ccb6e8962eedb6ffdd0a8/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1/types.go#L115) is by using Job annotations. Every annotation with the ***provreq.kueue.x-k8s.io/*** prefix will be directly passed to created ProvisioningRequest. E.g. `provreq.kueue.x-k8s.io/ValidUntilSeconds: "60"` will pass `ValidUntilSeconds` parameter with the value of `60`. See more examples below.
  ```

- **site/content/en/docs/admission-check-controllers/provisioning.md:50** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-27T14:34:25Z
```diff
@@ -33,6 +43,12 @@ Where:
 
 Check the [API definition](https://github.com/kubernetes-sigs/kueue/blob/main/apis/kueue/v1beta1/provisioningrequestconfig_types.go) for more details.
 
+### Job annotation
+
+Another way to pass ProvisioningRequest's [Parameters](https://github.com/kubernetes/autoscaler/blob/0130d33747bb329b790ccb6e8962eedb6ffdd0a8/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1/types.go#L115) is by using Job annotations. Every annotation with the ***provreq.kueue.x-k8s.io/*** prefix will be directly passed to created ProvisioningRequest. E.g. `provreq.kueue.x-k8s.io/ValidUntilSeconds: "60"` will pass `ValidUntilSeconds` parameter with the value of `60`. See more examples below.
+
+Once a ProvisioningRequest is created, modifying value of annotations will have no effect, similarly to using ProvisioningRequest without Kueue.
```

  ```suggestion
  Once Kueue creates a ProvisioningRequest for the job you submitted, modifying the value of annotations in the job will have no effect in the ProvisioningRequest.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-03-27T16:22:12Z

ok last nits :)

- **site/content/en/docs/admission-check-controllers/provisioning.md:29** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-27T16:20:54Z
```diff
@@ -6,14 +6,26 @@ description: >
   An admission check controller providing kueue integration with cluster autoscaler.
 ---
 
-The Provisioning Admission Check Controller is an Admission Check Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
+The Provisioning AdmissionCheck Controller is an AdmissionCheck Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
 
-The controller is part of kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+The controller is part of Kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
 
 The Provisioning Admission Check Controller is supported on [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) versions 1.29 and later. However, some cloud-providers may not have an implementation for it.
 
-## Parameters
+## Usage
 
+To use the Provisioning AdmissionCheck, create an [AdmissionCheck](docs/concepts/admission_check)
+with `kueue.x-k8s.io/provisioning-request` as a `.spec.controllerName` and create a ProvisioningRequest configuration usign a `ProvisioningRequestConfig` object. See an example below.
+
+## ProvisioningRequest configuration
+
+There are two ways to configure ProvisioningRequest sent for your Jobs.
+
+- **ProvisioningRequestConfig:** This configuration in the AdmissionCheck applies to all the jobs that go through this check.
+It enables you to set `provisioningClassName`, `managedResources`, and `parameters`
+- **Job annotation**: This configuration applies only to a specific job. It enables you to set `parameters`, and if both the annotation and the ProvisioningRequestConfig refer same one, annotation overrides it.
+
+### ProvisioningRequestConfig
 This controller uses a `ProvisioningRequestConfig` as parameters, like:
```

  This doesn't read well.
  
  Maybe just:
  
  ```suggestion
  A `ProvisioningRequestConfig` looks like the following:
  ```

- **site/content/en/docs/admission-check-controllers/provisioning.md:26** by [@alculquicondor](https://github.com/alculquicondor) — 2024-03-27T16:22:03Z
```diff
@@ -6,14 +6,26 @@ description: >
   An admission check controller providing kueue integration with cluster autoscaler.
 ---
 
-The Provisioning Admission Check Controller is an Admission Check Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
+The Provisioning AdmissionCheck Controller is an AdmissionCheck Controller designed to integrate Kueue with [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler). Its primary function is to create [ProvisioningRequests](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41) for the workloads holding [Quota Reservation](/docs/concepts/#quota-reservation) and keeping the [AdmissionCheckState](/docs/concepts/admission_check/#admissioncheckstate) in sync.
 
-The controller is part of kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+The controller is part of Kueue. You can enable it by setting the `ProvisioningACC` feature gate. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
 
 The Provisioning Admission Check Controller is supported on [Kubernetes cluster-autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) versions 1.29 and later. However, some cloud-providers may not have an implementation for it.
 
-## Parameters
+## Usage
 
+To use the Provisioning AdmissionCheck, create an [AdmissionCheck](docs/concepts/admission_check)
+with `kueue.x-k8s.io/provisioning-request` as a `.spec.controllerName` and create a ProvisioningRequest configuration usign a `ProvisioningRequestConfig` object. See an example below.
+
+## ProvisioningRequest configuration
+
+There are two ways to configure ProvisioningRequest sent for your Jobs.
+
+- **ProvisioningRequestConfig:** This configuration in the AdmissionCheck applies to all the jobs that go through this check.
+It enables you to set `provisioningClassName`, `managedResources`, and `parameters`
+- **Job annotation**: This configuration applies only to a specific job. It enables you to set `parameters`, and if both the annotation and the ProvisioningRequestConfig refer same one, annotation overrides it.
```

  ```suggestion
  - **Job annotation**: This configuration enables you to set `parameters` to a specific job. If both the annotation and the ProvisioningRequestConfig refer to the same parameter, the annotation value takes precedence.
  ```
