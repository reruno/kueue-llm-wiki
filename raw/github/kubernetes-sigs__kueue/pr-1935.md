# PR #1935: KEP-1432: AdmissionCheck per ResourceFlavor

**Summary**: KEP-1432: AdmissionCheck per ResourceFlavor

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1935

**Last updated**: 2024-04-26T09:52:08Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `KEP-1432` → `main`
- **Created**: 2024-04-02T09:03:53Z
- **Updated**: 2024-04-26T09:52:08Z
- **Merged**: 2024-04-05T12:41:27Z
- **Closed**: 2024-04-05T12:41:27Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mwielgus](https://github.com/mwielgus)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 2   **+136 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Design for support for AdmissionChecks per ResourceFlavor

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #1432 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-02T09:04:10Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 32bb924e4040ec6ef5bf372d3c60d8feeeaf7a6c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/660feb7425114900084d4469 |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-04-02T09:04:36Z

/assign @alculquicondor

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-04-02T09:06:23Z

Could you take a look please @mimowo @gabesaba?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-04-02T13:12:07Z

/assign @mwielgus

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-05T12:18:13Z

LGTM label has been added.  <details>Git tree hash: b3079a157caab13ba1113f659a19be60f9473027</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-05T12:18:16Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1935#pullrequestreview-1983011764" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1935#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-02T18:54:54Z

- **keps/1432-admission-check-per-resource-flavor/README.md:42** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:32:58Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
```

  Please remove comments

- **keps/1432-admission-check-per-resource-flavor/README.md:77** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:34:06Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
```

  ```suggestion
  - Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
  ```

- **keps/1432-admission-check-per-resource-flavor/README.md:64** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:36:13Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
```

  It's not that much about efficiency. It's just about flexibility and expressiveness.
  
  Right now, you can't say things like: use my reservation first, and if not possible, do a ProvReq.

- **keps/1432-admission-check-per-resource-flavor/README.md:78** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:37:03Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
```

  I don't think this should be a goal.
  
  What about running different an AdmissionCheck with different settings depending on the flavor? That could be a goal.

- **keps/1432-admission-check-per-resource-flavor/README.md:94** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:38:12Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
```

  ```suggestion
  Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when a Workload
  is admited for this ResourceFlavor.
  ```

- **keps/1432-admission-check-per-resource-flavor/README.md:121** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:39:19Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
```

  Why? Please expand.
  
  Will this be validated?

- **keps/1432-admission-check-per-resource-flavor/README.md:163** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:49:06Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
```

  what happens if they have the same controller? Is the workload not able to be admitted, is it marked as Failed somehow?

- **keps/1432-admission-check-per-resource-flavor/README.md:231** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:52:17Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload should have
+assigned `AdmissionChecks` both from `ClusterQueue` and `FlavorQuota` APIs. In case two `AdmissionChecks` have the same
+controller, the one from `FlavorQuota` API should take over.
+
+This enables having default `AdmissionChecks` that apply to all Workloads submitted to a `ClusterQueue`, and more
+specific ones that apply only to Workloads using a specific `ResourceFlavor`.
+
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[X] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+<!-- ##### Prerequisite testing updates -->
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+Test Workload's Controller's function that assigns AdmissionChecks to a Workload
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+<!-- - `<package>`: `<date>` - `<test coverage>` -->
+
+#### Integration tests
+- Create 2 ResourceFlavors with associated 2 AdmissionCheck and test if a Workload contains the AdmissionCheck associated with one of the ResourceFlavors;
```

  ```suggestion
  - Create 2 ResourceFlavors each with different AdmissionChecks and test if a Workload contains the AdmissionCheck associated with one of the ResourceFlavors;
  ```
  ?

- **keps/1432-admission-check-per-resource-flavor/kep.yaml:9** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:54:23Z
```diff
@@ -0,0 +1,41 @@
+title: AdmissionCheck per ResourceFlavor
+kep-number: 1913
+authors:
+  - "@pbundyra"
+status: implementable
+creation-date: 2024-03-26
+reviewers:
+  - "@mimowo"
+  - "@gabesaba"
```

  I think @mwielgus wanted to review this one.
  
  @tenzen-y do you have anything to say about this feature?

- **keps/1432-admission-check-per-resource-flavor/kep.yaml:25** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-02T18:54:42Z
```diff
@@ -0,0 +1,41 @@
+title: AdmissionCheck per ResourceFlavor
+kep-number: 1913
+authors:
+  - "@pbundyra"
+status: implementable
+creation-date: 2024-03-26
+reviewers:
+  - "@mimowo"
+  - "@gabesaba"
+approvers:
+  - "@alculquicondor"
+
+# see-also:
+#   - "/keps/1234-we-heard-you-like-keps"
+#   - "/keps/2345-everyone-gets-a-kep"
+# replaces:
+#   - "/keps/3456-replaced-kep"
+
+# The target maturity stage in the current dev cycle for this KEP.
+# stage: alpha|beta|stable
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+# latest-milestone: "v0.2"
```

  Please uncomment and set to v0.7

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-03T06:43:17Z

- **keps/1432-admission-check-per-resource-flavor/kep.yaml:9** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-03T06:43:17Z
```diff
@@ -0,0 +1,41 @@
+title: AdmissionCheck per ResourceFlavor
+kep-number: 1913
+authors:
+  - "@pbundyra"
+status: implementable
+creation-date: 2024-03-26
+reviewers:
+  - "@mimowo"
+  - "@gabesaba"
```

  Let me check.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-04-03T10:18:29Z

- **keps/1432-admission-check-per-resource-flavor/README.md:63** by [@gabesaba](https://github.com/gabesaba) — 2024-04-03T08:33:15Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
```

  ```suggestion
  the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
  ```

- **keps/1432-admission-check-per-resource-flavor/README.md:94** by [@gabesaba](https://github.com/gabesaba) — 2024-04-03T08:54:39Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
```

  ```suggestion
  Add AdmissionChecks to the FlavorQuota API,  which guard the admission of a Workload to a ResourceFlavor.
  ```

- **keps/1432-admission-check-per-resource-flavor/README.md:149** by [@gabesaba](https://github.com/gabesaba) — 2024-04-03T09:00:59Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
```

  please hide existing fields e.g. [[...]](https://github.com/kubernetes-sigs/kueue/blob/af116ab42e2595db86df65eb20953034316cb1d7/keps/993-two-phase-admission/README.md?plain=1#L107)

- **keps/1432-admission-check-per-resource-flavor/README.md:169** by [@gabesaba](https://github.com/gabesaba) — 2024-04-03T09:25:00Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload should have
```

  ```suggestion
  At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload may have
  ```

- **keps/1432-admission-check-per-resource-flavor/README.md:162** by [@gabesaba](https://github.com/gabesaba) — 2024-04-03T09:40:38Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
```

  could you document that each AdmissionCheck is "foreign key", referencing the (metadata.name?) field of AdmissionCheck?

- **keps/1432-admission-check-per-resource-flavor/README.md:169** by [@gabesaba](https://github.com/gabesaba) — 2024-04-03T10:03:32Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload should have
```

  @mwielgus Apart from maintaining backwards compatibility (which I don't want to discount, though AdmissionChecks are still relatively young), is there a good reason to have AdmissionChecks at both the CQ and Flavor level?
  
  I suppose not having to reason about AdmissionCheck override makes the API simpler to understand, and easier to debug when something is misbehaving

- **keps/1432-admission-check-per-resource-flavor/README.md:1** by [@gabesaba](https://github.com/gabesaba) — 2024-04-03T10:07:19Z
  please reference kep-993

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-03T11:14:24Z

- **keps/1432-admission-check-per-resource-flavor/README.md:94** by [@mimowo](https://github.com/mimowo) — 2024-04-03T11:03:53Z
```diff
@@ -0,0 +1,288 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when a Workload
```

  ```suggestion
  Add new field to FlavorQuota API, so it contains a list of AdmissionChecks that should run when a Workload
  ```

- **keps/1432-admission-check-per-resource-flavor/README.md:80** by [@mimowo](https://github.com/mimowo) — 2024-04-03T11:11:24Z
```diff
@@ -0,0 +1,288 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when a Workload
+is admited for this ResourceFlavor.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload may have
+assigned `AdmissionChecks` both from `ClusterQueue` and `FlavorQuota` APIs. In case two `AdmissionChecks` have the same
+controller, the one from `FlavorQuota` API should take over.
+
+This enables having default `AdmissionChecks` that apply to all Workloads submitted to a `ClusterQueue`, and more
+specific ones that apply only to Workloads using a specific `ResourceFlavor`.
```

  Do we have a good story for supporting both levels at the same time? If so please add to the stories section. Otherwise I would suggest to simplify and only allow them to be present at one level. We can relax this in the future if needed. 
  
  It feels that setting admission checks at both levels might complicate debugging for the "why my workload is not starting?" question.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-03T19:57:18Z

- **keps/1432-admission-check-per-resource-flavor/README.md:169** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-03T19:57:18Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload should have
```

  Even though it's new, it's already being used in production, so I definitely would leave it supported.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-04T07:49:46Z

- **keps/1432-admission-check-per-resource-flavor/README.md:169** by [@mimowo](https://github.com/mimowo) — 2024-04-04T07:49:46Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload should have
```

  Right, but we still could consider allowing only for one level of AdmissionChecks - either CQ or RF, as suggested also in https://github.com/kubernetes-sigs/kueue/pull/1935#discussion_r1549503754

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-04T09:00:47Z

- **keps/1432-admission-check-per-resource-flavor/README.md:169** by [@PBundyra](https://github.com/PBundyra) — 2024-04-04T09:00:47Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload should have
```

  Maybe we could simplify it and skip overriding as there are not that many use cases behind it. However, I would keep having AdmissionChecks both at the CQ and Flavor level, and I've added a Story to show an example. WDYT?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-04T09:00:54Z

- **keps/1432-admission-check-per-resource-flavor/README.md:163** by [@PBundyra](https://github.com/PBundyra) — 2024-04-04T09:00:54Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
```

  WDYT about introducing a new condition and setting it if this happens?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-04T09:01:40Z

- **keps/1432-admission-check-per-resource-flavor/README.md:80** by [@PBundyra](https://github.com/PBundyra) — 2024-04-04T09:01:40Z
```diff
@@ -0,0 +1,288 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when a Workload
+is admited for this ResourceFlavor.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload may have
+assigned `AdmissionChecks` both from `ClusterQueue` and `FlavorQuota` APIs. In case two `AdmissionChecks` have the same
+controller, the one from `FlavorQuota` API should take over.
+
+This enables having default `AdmissionChecks` that apply to all Workloads submitted to a `ClusterQueue`, and more
+specific ones that apply only to Workloads using a specific `ResourceFlavor`.
```

  I've added a story for supporting this. Maybe we could simplify the mechanism though and discard overriding AdmissionChecks. WDYT

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-04T09:45:50Z

- **keps/1432-admission-check-per-resource-flavor/README.md:80** by [@mimowo](https://github.com/mimowo) — 2024-04-04T09:45:50Z
```diff
@@ -0,0 +1,288 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when a Workload
+is admited for this ResourceFlavor.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload may have
+assigned `AdmissionChecks` both from `ClusterQueue` and `FlavorQuota` APIs. In case two `AdmissionChecks` have the same
+controller, the one from `FlavorQuota` API should take over.
+
+This enables having default `AdmissionChecks` that apply to all Workloads submitted to a `ClusterQueue`, and more
+specific ones that apply only to Workloads using a specific `ResourceFlavor`.
```

  If the story is not yet motivated by a user request, then I would suggest to simplify. 
  
  In case we have ACs only at one level, then we can apply the same validation rules, but scoped.
  
  If we allow setting the ACs at two levels the semantic of merging and validation gets tricky. Some questions to emphasize the complications: 
  1. why override, if we allow some admission checks (like ProvReq) to have multiple instances we could append rather than override,
  2. why override rather than validation error for admission checks that require single instance, say for MK, user might get surprised which one "wins"
  3. are the answers above different if the ACs have different / the same name?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-04T12:49:37Z

- **keps/1432-admission-check-per-resource-flavor/README.md:80** by [@PBundyra](https://github.com/PBundyra) — 2024-04-04T12:49:37Z
```diff
@@ -0,0 +1,288 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when a Workload
+is admited for this ResourceFlavor.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload may have
+assigned `AdmissionChecks` both from `ClusterQueue` and `FlavorQuota` APIs. In case two `AdmissionChecks` have the same
+controller, the one from `FlavorQuota` API should take over.
+
+This enables having default `AdmissionChecks` that apply to all Workloads submitted to a `ClusterQueue`, and more
+specific ones that apply only to Workloads using a specific `ResourceFlavor`.
```

  Thanks for clarifying. I took that into consideration and overall agree with it. I've committed changes that aligns with that approach

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-04T12:51:00Z

- **keps/1432-admission-check-per-resource-flavor/README.md:169** by [@PBundyra](https://github.com/PBundyra) — 2024-04-04T12:51:00Z
```diff
@@ -0,0 +1,287 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when its unnecessary. We would like to have an ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and efficient.
+
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+### Goals
+- Support running AdmissionChecks only when the ResourceFlavor is used.
+- Reduce the number of unnecessary AdmissionChecks.
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+## Proposal
+
+Add new field to FlavorQuota API, so it contains list of AdmissionChecks thatshould run when the ResourceFlavor is used.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+
+
+### Notes/Constraints/Caveats (Optional)
+All `AdmissionChecks` assigned to a Workload must have different controllers.
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+We propose adding a new field to `FlavorQuota` API called `AdmissionChecks` in following manner:
+
+```
+type FlavorQuotas struct {
+  // name of this flavor. The name should match the .metadata.name of a
+  // ResourceFlavor. If a matching ResourceFlavor does not exist, the
+  // ClusterQueue will have an Active condition set to False.
+  Name ResourceFlavorReference `json:"name"`  
+
+  // resources is the list of quotas for this flavor per resource.
+  // There could be up to 16 resources.
+  // +listType=map
+  // +listMapKey=name
+  // +kubebuilder:validation:MinItems=1
+  // +kubebuilder:validation:MaxItems=16
+  Resources []ResourceQuota `json:"resources"`
+
+  // admissionChecks is the list of AdmissionChecks that should run when the ResourceFlavor is used.
+  // No two AdmissionChecks should have the same controller.
+  // +listType=set
+  AdmissionChecks []string `json:"admissionchecks"`
+}
+```
+
+At the same time, we want to preserve the existing `AdmissionChecks` field in `ClusterQueue` API. A Workload should have
```

  I agree with @gabesaba and @mimowo https://github.com/kubernetes-sigs/kueue/pull/1935#discussion_r1551617863

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-04T13:12:17Z

Overall lgtm

- **keps/1432-admission-check-per-resource-flavor/README.md:86** by [@mimowo](https://github.com/mimowo) — 2024-04-04T13:12:02Z
```diff
@@ -87,16 +77,19 @@ to implement this enhancement.
 
 #### Unit Tests
 
-Test Workload's Controller's function that assigns AdmissionChecks to a Workload
+- Test Workload's Controller's function that assigns AdmissionChecks to a Workload
+
+- Test Cache that checks ClusterQueue's status
+
 
 #### Integration tests
 - Create 2 ResourceFlavors each with a different AdmissionCheck and test if a Workload contains the AdmissionCheck associated with one of the ResourceFlavors;
```

  nit: I think it makes sense to add a test for the baseline scenario, for the use case we have: no AC for reservation flavor, and one AC for the spot/on-demand flavor.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-04T14:58:32Z

- **keps/1432-admission-check-per-resource-flavor/README.md:86** by [@PBundyra](https://github.com/PBundyra) — 2024-04-04T14:58:32Z
```diff
@@ -87,16 +77,19 @@ to implement this enhancement.
 
 #### Unit Tests
 
-Test Workload's Controller's function that assigns AdmissionChecks to a Workload
+- Test Workload's Controller's function that assigns AdmissionChecks to a Workload
+
+- Test Cache that checks ClusterQueue's status
+
 
 #### Integration tests
 - Create 2 ResourceFlavors each with a different AdmissionCheck and test if a Workload contains the AdmissionCheck associated with one of the ResourceFlavors;
```

  Added

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-04T15:43:24Z

LGTM on this new feature.

- **keps/1432-admission-check-per-resource-flavor/README.md:39** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-04T15:10:47Z
```diff
@@ -0,0 +1,94 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and expressive.
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Running an AdmissionCheck with different settings depending on the flavor
+
+### Non-Goals
+
+## Proposal
+
+Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that  should run when a Workload
```

  ```suggestion
  Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that should run when a Workload
  ```

- **keps/1432-admission-check-per-resource-flavor/README.md:51** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-04T15:40:52Z
```diff
@@ -0,0 +1,94 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and expressive.
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Running an AdmissionCheck with different settings depending on the flavor
+
+### Non-Goals
+
+## Proposal
+
+Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that  should run when a Workload
+is admitted for this ResourceFlavor.
+
+### User Stories (Optional)
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+### Notes/Constraints/Caveats (Optional)
+User cannot define AdmissionChecks both at ClusterQueue and ResourceFlavor level. This will be validated by
+a ClusterQueue webhook.
+
+### Risks and Mitigations
+
+## Design Details
```

  I think that this API design would bring AdmissionChecks fragmentation.
  So, I'm wondering if we can add configurations under the `ClusterQueueSpec` field like this.
  In theory, once we introduce the `AdmissionCheckStrategy`, the existing `AdmissionChecks` field is not needed, but we keep the existing field for backward compatibility.
  
  @alculquicondor @PBundyra WDYT?
  
  ```diff
  type ClusterQueueSpec struct {
  ...
      AdmissionChecks []string
  +   AdmissionCheckStrategy []AdmissionCheckStrategy
  }
  
  +type AdmissionCheckPolicy struct {
  +    // AdmissionCheck name.
  +    Name string
  +    Strategy AdmissionCheckStrategyKind
  +    Selector  *metav1.LabelSelector OR []types.NamespacedName
  +}
  
  +type AdmissionCheckStrategyKind string
  +constants (
  +    AdmissionCheckStrategyKindClusterQueue AdmissionCheckStrategyKind = "ClusterQueue" // default
  +    AdmissionCheckStrategyKindResourceFlavor AdmissionCheckStrategyKind = "ResourceFlavor"
  +)
  +
  ```

- **keps/1432-admission-check-per-resource-flavor/README.md:52** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-04T15:42:34Z
```diff
@@ -0,0 +1,94 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and expressive.
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Running an AdmissionCheck with different settings depending on the flavor
+
+### Non-Goals
+
+## Proposal
+
+Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that  should run when a Workload
+is admitted for this ResourceFlavor.
+
+### User Stories (Optional)
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+### Notes/Constraints/Caveats (Optional)
+User cannot define AdmissionChecks both at ClusterQueue and ResourceFlavor level. This will be validated by
+a ClusterQueue webhook.
+
+### Risks and Mitigations
```

  Are there any risks? If there are no risks, should we remove this section?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-04T15:56:51Z

- **keps/1432-admission-check-per-resource-flavor/README.md:51** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T15:56:51Z
```diff
@@ -0,0 +1,94 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and expressive.
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Running an AdmissionCheck with different settings depending on the flavor
+
+### Non-Goals
+
+## Proposal
+
+Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that  should run when a Workload
+is admitted for this ResourceFlavor.
+
+### User Stories (Optional)
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+### Notes/Constraints/Caveats (Optional)
+User cannot define AdmissionChecks both at ClusterQueue and ResourceFlavor level. This will be validated by
+a ClusterQueue webhook.
+
+### Risks and Mitigations
+
+## Design Details
```

  What is the label selector for? Is it for workload labels?
  
  I'm failing to see what would be the use case at the moment, but I agree that we might need to add more "options" in the future.
  
  OTOH, just a `strategy` is not enough. Maybe it could be something like:
  
  ```diff
  type ClusterQueueSpec struct {
  ...
      AdmissionChecks []string
  +   AdmissionCheckStrategy []AdmissionCheckStrategy
  }
  
  +type AdmissionCheckStrategy struct {
  +    // AdmissionCheck name.
  +    Name string
  +    ForFlavors []string
  +}
  ```
  
  And if `forFlavors` is empty, then the admission check applies to all of the ClusterQueue.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-04T15:59:24Z

- **keps/1432-admission-check-per-resource-flavor/README.md:51** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T15:59:24Z
```diff
@@ -0,0 +1,94 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and expressive.
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Running an AdmissionCheck with different settings depending on the flavor
+
+### Non-Goals
+
+## Proposal
+
+Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that  should run when a Workload
+is admitted for this ResourceFlavor.
+
+### User Stories (Optional)
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+### Notes/Constraints/Caveats (Optional)
+User cannot define AdmissionChecks both at ClusterQueue and ResourceFlavor level. This will be validated by
+a ClusterQueue webhook.
+
+### Risks and Mitigations
+
+## Design Details
```

  The advantage of the above API, compared to the original proposal, is that it's more future proof.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-04T16:28:40Z

- **keps/1432-admission-check-per-resource-flavor/README.md:51** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-04T16:28:40Z
```diff
@@ -0,0 +1,94 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and expressive.
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Running an AdmissionCheck with different settings depending on the flavor
+
+### Non-Goals
+
+## Proposal
+
+Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that  should run when a Workload
+is admitted for this ResourceFlavor.
+
+### User Stories (Optional)
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+### Notes/Constraints/Caveats (Optional)
+User cannot define AdmissionChecks both at ClusterQueue and ResourceFlavor level. This will be validated by
+a ClusterQueue webhook.
+
+### Risks and Mitigations
+
+## Design Details
```

  > What is the label selector for? Is it for workload labels?
  
  It is for ResourceFlavor, but we can use it for Workloads in the future.
  But I don't have a strong point of view against LabelSelector, and I just wanted to express somehow generic filters.
  
  > I'm failing to see what would be the use case at the moment, but I agree that we might need to add more "options" in the future.
  
  Yes, that's right. That is the main motivation for my proposed API.
  
  > OTOH, just a strategy is not enough. Maybe it could be something like:
  And if forFlavors is empty, then the admission check applies to all of the ClusterQueue.
  
  I like your proposed API rather than my proposal, but the `ForFlavors` sounds slightly weired.
  How about the `CheckForFlavors` or `ActionForFlavors`?
  
  Also, should we limit the number of `forFlavors`? I guess it is possible to set `16`.
  WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-04T16:40:27Z

- **keps/1432-admission-check-per-resource-flavor/README.md:51** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T16:40:27Z
```diff
@@ -0,0 +1,94 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and expressive.
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Running an AdmissionCheck with different settings depending on the flavor
+
+### Non-Goals
+
+## Proposal
+
+Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that  should run when a Workload
+is admitted for this ResourceFlavor.
+
+### User Stories (Optional)
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+### Notes/Constraints/Caveats (Optional)
+User cannot define AdmissionChecks both at ClusterQueue and ResourceFlavor level. This will be validated by
+a ClusterQueue webhook.
+
+### Risks and Mitigations
+
+## Design Details
```

  How about this?
  ```yaml
  admissionCheckStrategies:
  - name: foo
    onFlavors: [bar]
  ```
  
  I think more words is just redundant.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-04T16:41:38Z

- **keps/1432-admission-check-per-resource-flavor/README.md:51** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-04T16:41:37Z
```diff
@@ -0,0 +1,94 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and expressive.
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Running an AdmissionCheck with different settings depending on the flavor
+
+### Non-Goals
+
+## Proposal
+
+Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that  should run when a Workload
+is admitted for this ResourceFlavor.
+
+### User Stories (Optional)
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+### Notes/Constraints/Caveats (Optional)
+User cannot define AdmissionChecks both at ClusterQueue and ResourceFlavor level. This will be validated by
+a ClusterQueue webhook.
+
+### Risks and Mitigations
+
+## Design Details
```

  it looks great! Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-04T17:16:35Z

- **keps/1432-admission-check-per-resource-flavor/kep.yaml:10** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-04T17:16:35Z
```diff
@@ -0,0 +1,17 @@
+title: AdmissionCheck per ResourceFlavor
+kep-number: 1913
+authors:
+  - "@pbundyra"
+status: implementable
+creation-date: 2024-03-26
+reviewers:
+  - "@mimowo"
+  - "@gabesaba"
+  - "@mwielgus"
```

  ```suggestion
    - "@mwielgus"
    - "@tenzen-y"
  ```
  
  :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-05T12:18:09Z

/lgtm
/approve

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-05T12:19:01Z

- **keps/1432-admission-check-per-resource-flavor/README.md:51** by [@PBundyra](https://github.com/PBundyra) — 2024-04-05T12:19:01Z
```diff
@@ -0,0 +1,94 @@
+# KEP-1432: AdmissionChecks per ResouceFlavor
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces assigning AdmissionChecks to a ResourceFlavor.
+
+## Motivation
+
+Currently, AdmissionChecks are only assigned to a ClusterQueue. This results in running `AdmissionChecks` regardless of
+the ResourceFlavor used by a Workload, even when it is unnecessary. We would like to have the ability to assign AdmissionChecks
+to a specific ResourceFlavor, so the whole mechanism is more flexible and expressive.
+
+### Goals
+- Support running AdmissionChecks only when a particular ResourceFlavor is assigned to the Workload.
+- Running an AdmissionCheck with different settings depending on the flavor
+
+### Non-Goals
+
+## Proposal
+
+Add a new field to FlavorQuota API, so it contains a list of AdmissionChecks that  should run when a Workload
+is admitted for this ResourceFlavor.
+
+### User Stories (Optional)
+
+#### Story 1
+As a user who has reserved machines at my cloud provider I would like to use them first. If they are not obtainable then
+switch to spot machines, using ProvisioningRequest. It only makes sense to run Provisioning AdmissionCheck when the ResourceFlavor is Spot.
+
+### Notes/Constraints/Caveats (Optional)
+User cannot define AdmissionChecks both at ClusterQueue and ResourceFlavor level. This will be validated by
+a ClusterQueue webhook.
+
+### Risks and Mitigations
+
+## Design Details
```

  I like this idea, especially as it's more extendable. I've included it in my proposal. Thank you!
