# PR #1942: Distinguish preemption reasons

**Summary**: Distinguish preemption reasons

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1942

**Last updated**: 2024-05-29T14:53:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `distinguish-preemption-reasons` → `main`
- **Created**: 2024-04-03T17:23:22Z
- **Updated**: 2024-05-29T14:53:51Z
- **Merged**: 2024-04-10T15:19:59Z
- **Closed**: 2024-04-10T15:19:59Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@astefanutti](https://github.com/astefanutti), [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 7   **+113 / -19**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1874

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
A new condition with type Preempted allows to distinguish different reasons for the preemption to happen
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-03T17:23:37Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3a4fc2dbe1531f37e03d1ea09d2d8fcfcd9143f0 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6616614b2f909d000869a295 |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-04T17:42:47Z

/assign @alculquicondor @astefanutti @tenzen-y

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T18:13:47Z

/release-note-edit
```release-note
A new condition with type Preempted allows to distinguish different reasons for the preemption to happen
```

Maybe also include the names and how they might be expanded in the future?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-10T15:02:33Z

LGTM label has been added.  <details>Git tree hash: 1c5d2f5a818c1f699c4c742bb7b8d8325d3c55f9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-10T15:02:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1942#pullrequestreview-1991970384" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1942#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-04T18:23:21Z

- **pkg/scheduler/preemption/preemption.go:147** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T18:16:19Z
```diff
@@ -135,18 +136,23 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, targets []*workload.In
 	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
 		target := targets[i]
 		if !meta.IsStatusConditionTrue(target.Obj.Status.Conditions, kueue.WorkloadEvicted) {
-			err := p.applyPreemption(ctx, target.Obj)
-			if err != nil {
-				errCh.SendErrorWithCancel(err, cancel)
-				return
-			}
 
 			origin := "ClusterQueue"
+			reason := "InClusterQueue"
 			if cq.Name != target.ClusterQueue {
 				origin = "cohort"
+				reason = "InCohort"
+			}
+
+			message := fmt.Sprintf("Preempted to accommodate the workload (UID: %s) in the %s", preemptor.Obj.UID, origin)
```

  ```suggestion
  			message := fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the %s", preemptor.Obj.UID, origin)
  ```

- **pkg/scheduler/preemption/preemption.go:154** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T18:16:45Z
```diff
@@ -135,18 +136,23 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, targets []*workload.In
 	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
 		target := targets[i]
 		if !meta.IsStatusConditionTrue(target.Obj.Status.Conditions, kueue.WorkloadEvicted) {
-			err := p.applyPreemption(ctx, target.Obj)
-			if err != nil {
-				errCh.SendErrorWithCancel(err, cancel)
-				return
-			}
 
 			origin := "ClusterQueue"
+			reason := "InClusterQueue"
 			if cq.Name != target.ClusterQueue {
 				origin = "cohort"
+				reason = "InCohort"
+			}
+
+			message := fmt.Sprintf("Preempted to accommodate the workload (UID: %s) in the %s", preemptor.Obj.UID, origin)
+			err := p.applyPreemption(ctx, target.Obj, reason, message)
+			if err != nil {
+				errCh.SendErrorWithCancel(err, cancel)
+				return
 			}
+
 			log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
```

  ```suggestion
  			log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj), "reason", reason)
  ```

- **apis/kueue/v1beta1/workload_types.go:288** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T18:17:35Z
```diff
@@ -287,6 +287,13 @@ const (
 
 	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue
```

  Update comment to include that if the reason is Preempted, another condition accompanies it with more details.

- **pkg/workload/workload.go:402** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T18:18:33Z
```diff
@@ -399,6 +399,22 @@ func SetQuotaReservation(w *kueue.Workload, admission *kueue.Admission) {
 		evictedCond.Status = metav1.ConditionFalse
 		evictedCond.LastTransitionTime = metav1.Now()
 	}
+	//reset Preempted condition if present.
```

  ```suggestion
  	// reset Preempted condition if present.
  ```

- **pkg/workload/workload.go:413** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T18:21:09Z
```diff
@@ -399,6 +399,22 @@ func SetQuotaReservation(w *kueue.Workload, admission *kueue.Admission) {
 		evictedCond.Status = metav1.ConditionFalse
 		evictedCond.LastTransitionTime = metav1.Now()
 	}
+	//reset Preempted condition if present.
+	if preemptedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadPreempted); preemptedCond != nil {
+		preemptedCond.Status = metav1.ConditionFalse
+		preemptedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetPreemptedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadPreempted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
```

  This is already set by `apimeta.SetStatusCondition`.

- **test/integration/scheduler/preemption_test.go:411** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-04T18:23:18Z
```diff
@@ -404,6 +407,104 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, alphaWl, gammaWl)
 			util.ExpectWorkloadsToBeAdmittedCount(ctx, k8sClient, 1, betaWls...)
 		})
+
+		ginkgo.It("Should add a dedicated Preempted condition indicating the preemption reason", func() {
```

  Could we just check the new condition in the existing tests?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-05T06:10:48Z

- **pkg/scheduler/preemption/preemption.go:147** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-05T05:57:58Z
```diff
@@ -135,18 +136,23 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, targets []*workload.In
 	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
 		target := targets[i]
 		if !meta.IsStatusConditionTrue(target.Obj.Status.Conditions, kueue.WorkloadEvicted) {
-			err := p.applyPreemption(ctx, target.Obj)
-			if err != nil {
-				errCh.SendErrorWithCancel(err, cancel)
-				return
-			}
 
 			origin := "ClusterQueue"
+			reason := "InClusterQueue"
 			if cq.Name != target.ClusterQueue {
 				origin = "cohort"
+				reason = "InCohort"
+			}
+
+			message := fmt.Sprintf("Preempted to accommodate the workload (UID: %s) in the %s", preemptor.Obj.UID, origin)
```

  ```suggestion
  			message := fmt.Sprintf("Preempted to accommodate the workload %q in the %s", klog.KObj(preemptor.Obj), origin)
  ```
  
  I'm wondering if we should use "namespace/name" in the message since this message is user-facing, and UID is slightly hard for users to understand. WDYT?

- **test/integration/scheduler/preemption_test.go:411** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-05T05:59:53Z
```diff
@@ -404,6 +407,104 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, alphaWl, gammaWl)
 			util.ExpectWorkloadsToBeAdmittedCount(ctx, k8sClient, 1, betaWls...)
 		})
+
+		ginkgo.It("Should add a dedicated Preempted condition indicating the preemption reason", func() {
```

  +1

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-05T12:15:23Z

- **pkg/scheduler/preemption/preemption.go:147** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-05T12:15:23Z
```diff
@@ -135,18 +136,23 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, targets []*workload.In
 	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
 		target := targets[i]
 		if !meta.IsStatusConditionTrue(target.Obj.Status.Conditions, kueue.WorkloadEvicted) {
-			err := p.applyPreemption(ctx, target.Obj)
-			if err != nil {
-				errCh.SendErrorWithCancel(err, cancel)
-				return
-			}
 
 			origin := "ClusterQueue"
+			reason := "InClusterQueue"
 			if cq.Name != target.ClusterQueue {
 				origin = "cohort"
+				reason = "InCohort"
+			}
+
+			message := fmt.Sprintf("Preempted to accommodate the workload (UID: %s) in the %s", preemptor.Obj.UID, origin)
```

  We had this same discussion in kube-scheduler and we concluded that the most we can share is a UID. Pod names and namespaces are considered PII. Leaking this kind of information is undesirable in multi-tenant environments.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-08T07:14:29Z

- **pkg/scheduler/preemption/preemption.go:147** by [@mimowo](https://github.com/mimowo) — 2024-04-08T07:14:29Z
```diff
@@ -135,18 +136,23 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, targets []*workload.In
 	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
 		target := targets[i]
 		if !meta.IsStatusConditionTrue(target.Obj.Status.Conditions, kueue.WorkloadEvicted) {
-			err := p.applyPreemption(ctx, target.Obj)
-			if err != nil {
-				errCh.SendErrorWithCancel(err, cancel)
-				return
-			}
 
 			origin := "ClusterQueue"
+			reason := "InClusterQueue"
 			if cq.Name != target.ClusterQueue {
 				origin = "cohort"
+				reason = "InCohort"
+			}
+
+			message := fmt.Sprintf("Preempted to accommodate the workload (UID: %s) in the %s", preemptor.Obj.UID, origin)
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-08T07:15:09Z

- **pkg/scheduler/preemption/preemption.go:154** by [@mimowo](https://github.com/mimowo) — 2024-04-08T07:15:09Z
```diff
@@ -135,18 +136,23 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, targets []*workload.In
 	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
 		target := targets[i]
 		if !meta.IsStatusConditionTrue(target.Obj.Status.Conditions, kueue.WorkloadEvicted) {
-			err := p.applyPreemption(ctx, target.Obj)
-			if err != nil {
-				errCh.SendErrorWithCancel(err, cancel)
-				return
-			}
 
 			origin := "ClusterQueue"
+			reason := "InClusterQueue"
 			if cq.Name != target.ClusterQueue {
 				origin = "cohort"
+				reason = "InCohort"
+			}
+
+			message := fmt.Sprintf("Preempted to accommodate the workload (UID: %s) in the %s", preemptor.Obj.UID, origin)
+			err := p.applyPreemption(ctx, target.Obj, reason, message)
+			if err != nil {
+				errCh.SendErrorWithCancel(err, cancel)
+				return
 			}
+
 			log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.Obj))
```

  Done, plus added the message field which might be helpful for debugging I suppose

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-08T07:15:19Z

- **apis/kueue/v1beta1/workload_types.go:288** by [@mimowo](https://github.com/mimowo) — 2024-04-08T07:15:19Z
```diff
@@ -287,6 +287,13 @@ const (
 
 	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-08T07:15:28Z

- **pkg/workload/workload.go:402** by [@mimowo](https://github.com/mimowo) — 2024-04-08T07:15:28Z
```diff
@@ -399,6 +399,22 @@ func SetQuotaReservation(w *kueue.Workload, admission *kueue.Admission) {
 		evictedCond.Status = metav1.ConditionFalse
 		evictedCond.LastTransitionTime = metav1.Now()
 	}
+	//reset Preempted condition if present.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-08T07:20:40Z

- **pkg/workload/workload.go:413** by [@mimowo](https://github.com/mimowo) — 2024-04-08T07:20:40Z
```diff
@@ -399,6 +399,22 @@ func SetQuotaReservation(w *kueue.Workload, admission *kueue.Admission) {
 		evictedCond.Status = metav1.ConditionFalse
 		evictedCond.LastTransitionTime = metav1.Now()
 	}
+	//reset Preempted condition if present.
+	if preemptedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadPreempted); preemptedCond != nil {
+		preemptedCond.Status = metav1.ConditionFalse
+		preemptedCond.LastTransitionTime = metav1.Now()
+	}
+}
+
+func SetPreemptedCondition(w *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadPreempted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-08T07:20:49Z

- **test/integration/scheduler/preemption_test.go:411** by [@mimowo](https://github.com/mimowo) — 2024-04-08T07:20:49Z
```diff
@@ -404,6 +407,104 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, alphaWl, gammaWl)
 			util.ExpectWorkloadsToBeAdmittedCount(ctx, k8sClient, 1, betaWls...)
 		})
+
+		ginkgo.It("Should add a dedicated Preempted condition indicating the preemption reason", func() {
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-08T10:13:35Z

- **pkg/scheduler/preemption/preemption.go:147** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-08T10:13:35Z
```diff
@@ -135,18 +136,23 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, targets []*workload.In
 	workqueue.ParallelizeUntil(ctx, parallelPreemptions, len(targets), func(i int) {
 		target := targets[i]
 		if !meta.IsStatusConditionTrue(target.Obj.Status.Conditions, kueue.WorkloadEvicted) {
-			err := p.applyPreemption(ctx, target.Obj)
-			if err != nil {
-				errCh.SendErrorWithCancel(err, cancel)
-				return
-			}
 
 			origin := "ClusterQueue"
+			reason := "InClusterQueue"
 			if cq.Name != target.ClusterQueue {
 				origin = "cohort"
+				reason = "InCohort"
+			}
+
+			message := fmt.Sprintf("Preempted to accommodate the workload (UID: %s) in the %s", preemptor.Obj.UID, origin)
```

  I didn't know that. 
  That makes sense. Thank you for this clarification!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-09T17:05:31Z

- **apis/kueue/v1beta1/workload_types.go:288** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T16:09:54Z
```diff
@@ -285,8 +285,17 @@ const (
 	// ready or have succeeded.
 	WorkloadPodsReady = "PodsReady"
 
-	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue
+	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue.
```

  Not sure why we were saying ClusterQueue.
  
  ```suggestion
  	// WorkloadEvicted means that the Workload was evicted.
  ```
  
  Maybe we can list the reasons too? Just so that people understand what are the possible evictions.

- **apis/kueue/v1beta1/workload_types.go:289** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T16:10:05Z
```diff
@@ -285,8 +285,17 @@ const (
 	// ready or have succeeded.
 	WorkloadPodsReady = "PodsReady"
 
-	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue
+	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue.
+	// When a workload is preempted this condition is accompanied by the "Preempted"
```

  ```suggestion
  	// When a workload is preempted, this condition is accompanied by the "Preempted"
  ```

- **test/integration/scheduler/preemption_test.go:314** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:05:25Z
```diff
@@ -274,7 +274,57 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.FinishEvictionForWorkloads(ctx, k8sClient, alphaLowWl, betaMidWl)
 
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaMidWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaHighWl)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, alphaLowWl, betaMidWl)
+
+			conditionCmpOpts := cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+			ginkgo.By("Verify the Preempted condition", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(betaMidWl), betaMidWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(betaMidWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InCohort",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the cohort", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the Preempted condition on re-admission, as the preemptor is finished", func() {
+				util.FinishWorkloads(ctx, k8sClient, alphaMidWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaLowWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
```

  uhm... this is rather confusing, because now the workload isn't preempted.
  
  Should we add a prefix? Like: `Previously: `

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-10T09:25:39Z

- **test/integration/scheduler/preemption_test.go:314** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T09:25:39Z
```diff
@@ -274,7 +274,57 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.FinishEvictionForWorkloads(ctx, k8sClient, alphaLowWl, betaMidWl)
 
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaMidWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaHighWl)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, alphaLowWl, betaMidWl)
+
+			conditionCmpOpts := cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+			ginkgo.By("Verify the Preempted condition", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(betaMidWl), betaMidWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(betaMidWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InCohort",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the cohort", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the Preempted condition on re-admission, as the preemptor is finished", func() {
+				util.FinishWorkloads(ctx, k8sClient, alphaMidWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaLowWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
```

  When we see only the message, it may bring any confusion. But doesn't avoid confusion since we have a `Status: False` field?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-10T09:25:55Z

LGTM

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T09:33:01Z

- **apis/kueue/v1beta1/workload_types.go:288** by [@mimowo](https://github.com/mimowo) — 2024-04-10T09:33:00Z
```diff
@@ -285,8 +285,17 @@ const (
 	// ready or have succeeded.
 	WorkloadPodsReady = "PodsReady"
 
-	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue
+	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue.
```

  Makes sense, I proposed the change for the list of reasons also

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T09:33:44Z

- **apis/kueue/v1beta1/workload_types.go:289** by [@mimowo](https://github.com/mimowo) — 2024-04-10T09:33:43Z
```diff
@@ -285,8 +285,17 @@ const (
 	// ready or have succeeded.
 	WorkloadPodsReady = "PodsReady"
 
-	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue
+	// WorkloadEvicted means that the Workload was evicted by a ClusterQueue.
+	// When a workload is preempted this condition is accompanied by the "Preempted"
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T09:42:46Z

- **test/integration/scheduler/preemption_test.go:314** by [@mimowo](https://github.com/mimowo) — 2024-04-10T09:42:46Z
```diff
@@ -274,7 +274,57 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.FinishEvictionForWorkloads(ctx, k8sClient, alphaLowWl, betaMidWl)
 
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaMidWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaHighWl)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, alphaLowWl, betaMidWl)
+
+			conditionCmpOpts := cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+			ginkgo.By("Verify the Preempted condition", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(betaMidWl), betaMidWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(betaMidWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InCohort",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the cohort", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the Preempted condition on re-admission, as the preemptor is finished", func() {
+				util.FinishWorkloads(ctx, k8sClient, alphaMidWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaLowWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
```

  Ok, so I only flipped status for consistency with how it was done for the "Evicted" condition.
  
  However, I think ideally, the condition should indicate the reason for the "last" transition.
  The [API comment](https://github.com/kubernetes/kubernetes/blob/9791f0d1f39f3f1e0796add7833c1059325d5098/pkg/apis/batch/types.go#L641-L643), Also, an example in the Job controller when transitioning from True to False (see [here](https://github.com/kubernetes/kubernetes/blob/9791f0d1f39f3f1e0796add7833c1059325d5098/pkg/controller/job/job_controller.go#L937C128-L937C138).
  
  In this case of transitoning Evicted / Preempted conditions from `True->False`, the reason for this transition is that it has now QuotaReserved. OTOH, it might be handy to still keep the information why the workload was preempted in the first place.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T10:30:16Z

- **test/integration/scheduler/preemption_test.go:314** by [@mimowo](https://github.com/mimowo) — 2024-04-10T10:30:16Z
```diff
@@ -274,7 +274,57 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.FinishEvictionForWorkloads(ctx, k8sClient, alphaLowWl, betaMidWl)
 
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaMidWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaHighWl)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, alphaLowWl, betaMidWl)
+
+			conditionCmpOpts := cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+			ginkgo.By("Verify the Preempted condition", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(betaMidWl), betaMidWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(betaMidWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InCohort",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the cohort", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the Preempted condition on re-admission, as the preemptor is finished", func() {
+				util.FinishWorkloads(ctx, k8sClient, alphaMidWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaLowWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
```

  Proposed a change along the lines in the last commit. PTAL.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-10T14:52:32Z

- **test/integration/scheduler/preemption_test.go:314** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T14:52:32Z
```diff
@@ -274,7 +274,57 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.FinishEvictionForWorkloads(ctx, k8sClient, alphaLowWl, betaMidWl)
 
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaMidWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaHighWl)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, alphaLowWl, betaMidWl)
+
+			conditionCmpOpts := cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+			ginkgo.By("Verify the Preempted condition", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(betaMidWl), betaMidWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(betaMidWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InCohort",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the cohort", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the Preempted condition on re-admission, as the preemptor is finished", func() {
+				util.FinishWorkloads(ctx, k8sClient, alphaMidWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaLowWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
```

  > When we see only the message, it may bring any confusion. But doesn't avoid confusion since we have a `Status: False` field?
  
  I have already received tickets from customers that missed the status: false line. So better avoid the confusion.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-10T15:02:28Z

/lgtm
/approve

- **pkg/workload/workload.go:402** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T15:02:17Z
```diff
@@ -397,8 +397,27 @@ func SetQuotaReservation(w *kueue.Workload, admission *kueue.Admission) {
 	//reset Evicted condition if present.
 	if evictedCond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadEvicted); evictedCond != nil {
 		evictedCond.Status = metav1.ConditionFalse
+		evictedCond.Reason = "QuotaReserved"
+		evictedCond.Message = "Previously: " + evictedCond.Message
 		evictedCond.LastTransitionTime = metav1.Now()
```

  FYI @vladikkuzn, given that you are working on #1939

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-10T15:05:33Z

- **test/integration/scheduler/preemption_test.go:314** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T15:05:32Z
```diff
@@ -274,7 +274,57 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.FinishEvictionForWorkloads(ctx, k8sClient, alphaLowWl, betaMidWl)
 
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaMidWl)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaHighWl)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, alphaLowWl, betaMidWl)
+
+			conditionCmpOpts := cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+			ginkgo.By("Verify the Preempted condition", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(betaMidWl), betaMidWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(betaMidWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionTrue,
+						Reason:  "InCohort",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the cohort", alphaMidWl.UID),
+					}, conditionCmpOpts))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Verify the Preempted condition on re-admission, as the preemptor is finished", func() {
+				util.FinishWorkloads(ctx, k8sClient, alphaMidWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, alphaCQ.Name, alphaLowWl)
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, betaCQ.Name, betaMidWl, betaHighWl)
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(alphaLowWl), alphaLowWl)).To(gomega.Succeed())
+					g.Expect(apimeta.FindStatusCondition(alphaLowWl.Status.Conditions, kueue.WorkloadPreempted)).To(gomega.BeComparableTo(&metav1.Condition{
+						Type:    kueue.WorkloadPreempted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "InClusterQueue",
+						Message: fmt.Sprintf("Preempted to accommodate a workload (UID: %s) in the ClusterQueue", alphaMidWl.UID),
```

  > I have already received tickets from customers that missed the status: false line. So better avoid the confusion.
  
  I see. End-user's feedback is important. Thank you for sharing it with me.
  /lgtm
