# PR #1955: add a metric for eviction number in cluster queue

**Summary**: add a metric for eviction number in cluster queue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1955

**Last updated**: 2024-05-30T14:35:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@lowang-bh](https://github.com/lowang-bh)
- **Branch**: `evictionMetric` → `main`
- **Created**: 2024-04-06T04:46:01Z
- **Updated**: 2024-05-30T14:35:43Z
- **Merged**: 2024-05-08T19:12:54Z
- **Closed**: 2024-05-08T19:12:54Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `needs-rebase`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@astefanutti](https://github.com/astefanutti), [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 15   **+163 / -55**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:

Expose a metrics to record workloads eviction number in cluster queue

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1741

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add metric `evicted_workloads`: the number of evicted workloads per 'cluster_queue'
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-06T04:46:11Z

Hi @lowang-bh. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-06T04:46:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c3dce09e3e811e65dd529ee2c2bfbd98b64d8d71 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/663b87a37fbbb70008d61228 |

### Comment by [@lowang-bh](https://github.com/lowang-bh) — 2024-04-06T04:49:14Z

/assign @alculquicondor @astefanutti

### Comment by [@kannon92](https://github.com/kannon92) — 2024-04-08T15:45:31Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T13:56:55Z

cc @mimowo

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-22T12:55:08Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-22T13:08:19Z

@lowang-bh is there any progress on the remaining comments:
- https://github.com/kubernetes-sigs/kueue/pull/1955#discussion_r1557639786
- https://github.com/kubernetes-sigs/kueue/pull/1955#discussion_r1559181845

Let us know if there are any complications with them, we would like to include this feature in 0.7

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-23T15:23:34Z

@lowang-bh the changes LGTM, the remaining asks from me are about the integration tests coverage. The idea is to find existing tests and inject asserts into them. Let me know if there are complications, we can move it to a follow up.

### Comment by [@lowang-bh](https://github.com/lowang-bh) — 2024-04-23T23:41:02Z

> Let me know if there are complications, we can move it to a follow up.

Hi, I need some time to find out where and how to assert them in integration test.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-24T09:27:46Z

> Hi, I need some time to find out where and how to assert them in integration test.

I've taken another look to suggest the places, and this revealed some bugs in the code. More details in comments. PTAL

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-08T08:19:17Z

@lowang-bh friendly ping if you can find time to rebase and address the remaining comments.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-08T15:15:17Z

LGTM label has been added.  <details>Git tree hash: ecc5316bb277453c56086e3b0ab44fcc0eee124f</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-08T15:15:36Z

/approve
/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-08T18:48:05Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1955#pullrequestreview-2046413694" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1955#" title="Author self-approved">lowang-bh</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1955#issuecomment-2100820530" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/OWNERS)~~ [alculquicondor,mimowo]
- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T14:35:41Z

/kind feature
/release-note-edit
```release-note
Add metric `evicted_workloads`: the number of evicted workloads per 'cluster_queue'
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-09T13:32:53Z

- **pkg/metrics/metrics.go:114** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T13:29:39Z
```diff
@@ -107,21 +107,34 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	EvictedWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Name: "evicted_workloads",
+			Help: `The number of evicted workloads per 'cluster_queue',
+The label 'result' can have the following values:
```

  ```suggestion
  The label 'reason' can have the following values:
  ```

- **pkg/metrics/metrics.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T13:30:02Z
```diff
@@ -107,21 +107,34 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	EvictedWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Name: "evicted_workloads",
+			Help: `The number of evicted workloads per 'cluster_queue',
+The label 'result' can have the following values:
+- "Preempted" means that the workload was evicted in order to free resources for a workload with a higher priority.
```

  ```suggestion
  - "Preempted" means that the workload was evicted in order to free resources for a workload with a higher priority or reclamation of nominal quota.
  ```

- **pkg/metrics/metrics.go:129** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T13:30:55Z
```diff
@@ -107,21 +107,34 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	EvictedWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Name: "evicted_workloads",
+			Help: `The number of evicted workloads per 'cluster_queue',
+The label 'result' can have the following values:
+- "Preempted" means that the workload was evicted in order to free resources for a workload with a higher priority.
+- "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
+- "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
+- "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
+- "InactiveWorkload" means that the workload was evicted because spec.active is set to false`,
+		}, []string{"cluster_queue", "reason"},
+	)
+
 	// Metrics tied to the cache.
 
 	ReservingActiveWorkloads = prometheus.NewGaugeVec(
 		prometheus.GaugeOpts{
 			Subsystem: constants.KueueName,
 			Name:      "reserving_active_workloads",
-			Help:      "The number of Workloads that are reserving quota, per 'cluster_queue'",
+			Help:      "The number of workloads that are reserving quota, per 'cluster_queue'",
```

  We actually try to use capitalized words for API objects

- **pkg/controller/core/workload_controller.go:257** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T13:32:50Z
```diff
@@ -248,6 +249,8 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+	cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
+	metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
```

  Always report metrics after the Apply or Update call has succeeded. Otherwise, if there is failure with the API update, we will be counting double.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T10:05:19Z

- **pkg/metrics/metrics.go:110** by [@mimowo](https://github.com/mimowo) — 2024-04-10T09:59:20Z
```diff
@@ -107,21 +107,34 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	EvictedWorkloads = prometheus.NewGaugeVec(
```

  Is this really a guage metric? It seems more like a counter.

- **pkg/metrics/metrics.go:112** by [@mimowo](https://github.com/mimowo) — 2024-04-10T10:03:09Z
```diff
@@ -107,21 +107,34 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	EvictedWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Name: "evicted_workloads",
```

  If this is a counter metric add the _total suffix, following the convention: https://prometheus.io/docs/practices/naming/

- **pkg/metrics/metrics.go:110** by [@mimowo](https://github.com/mimowo) — 2024-04-10T10:05:17Z
```diff
@@ -107,21 +107,34 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	EvictedWorkloads = prometheus.NewGaugeVec(
```

  Please add some test for the metric, either unit test or integration. I know we have some integration tests for metrics already, so extending an existing integration test to assert of the metrics sounds good to me.

### Review by [@lowang-bh](https://github.com/lowang-bh) (commented) — 2024-04-10T13:14:51Z

- **pkg/metrics/metrics.go:110** by [@lowang-bh](https://github.com/lowang-bh) — 2024-04-10T13:14:51Z
```diff
@@ -107,21 +107,34 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	EvictedWorkloads = prometheus.NewGaugeVec(
```

  Have changed it to a counter metric.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-10T20:18:59Z

- **pkg/controller/core/workload_controller.go:257** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T20:18:59Z
```diff
@@ -248,6 +249,8 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+	cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
+	metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
```

  this is still pending.

### Review by [@lowang-bh](https://github.com/lowang-bh) (commented) — 2024-04-23T14:29:18Z

- **pkg/metrics/metrics.go:110** by [@lowang-bh](https://github.com/lowang-bh) — 2024-04-23T14:29:18Z
```diff
@@ -107,21 +107,34 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	EvictedWorkloads = prometheus.NewGaugeVec(
```

  Have added the unit test.

### Review by [@lowang-bh](https://github.com/lowang-bh) (commented) — 2024-04-23T14:30:25Z

- **pkg/controller/core/workload_controller.go:257** by [@lowang-bh](https://github.com/lowang-bh) — 2024-04-23T14:30:25Z
```diff
@@ -248,6 +249,8 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+	cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
+	metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
```

  All metrics is report after the patch is succeed now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T15:19:08Z

- **pkg/metrics/metrics.go:110** by [@mimowo](https://github.com/mimowo) — 2024-04-23T15:19:08Z
```diff
@@ -107,21 +107,34 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	EvictedWorkloads = prometheus.NewGaugeVec(
```

  Thanks. My last ask would be that the unit test verifies `ReportEvictedWorkloads` works ok, still it would be good to assert in integration tests to make sure it is called.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T15:20:31Z

- **pkg/controller/core/workload_controller.go:314** by [@mimowo](https://github.com/mimowo) — 2024-04-23T15:20:31Z
```diff
@@ -269,6 +269,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	log.V(3).Info("Workload is evicted due to admission checks")
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
 	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	if err == nil {
+		cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
+		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
```

  Please cover in integration test that it is bumped (by asserting in an existing test).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T15:20:59Z

- **pkg/controller/core/workload_controller.go:354** by [@mimowo](https://github.com/mimowo) — 2024-04-23T15:20:59Z
```diff
@@ -309,6 +313,9 @@ func (r *WorkloadReconciler) reconcileOnClusterQueueActiveState(ctx context.Cont
 		log.V(3).Info("Workload is evicted because the ClusterQueue is stopped", "clusterQueue", klog.KRef("", cqName))
 		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByClusterQueueStopped, "The ClusterQueue is stopped")
 		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByClusterQueueStopped)
```

  Please cover in an integration test that it is bumped (in an existing test).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T15:22:02Z

- **pkg/controller/core/workload_controller.go:432** by [@mimowo](https://github.com/mimowo) — 2024-04-23T15:22:02Z
```diff
@@ -383,6 +390,10 @@ func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req c
 	}
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
 	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	if err == nil {
+		cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
+		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByPodsReadyTimeout)
```

  Please cover in an integration test that it is bumped, by extending an existing test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-24T08:07:34Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:322** by [@mimowo](https://github.com/mimowo) — 2024-04-24T08:07:34Z
```diff
@@ -319,6 +319,7 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 							kueue.WorkloadEvictedByPreemption, "By test", "evict"),
 					).Should(gomega.Succeed())
 					util.FinishEvictionForWorkloads(ctx, k8sClient, createdWorkload)
+					util.ExpectEvictedWorkloadsTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByPreemption, 1)
```

  I think this [failure](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1955/pull-kueue-test-integration-main/1782940296334544896) is caused by this assert, because this integration test does not run scheduler, and the metric is bumped in scheduler code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-24T08:55:21Z

- **pkg/controller/core/workload_controller.go:354** by [@mimowo](https://github.com/mimowo) — 2024-04-24T08:55:21Z
```diff
@@ -309,6 +313,9 @@ func (r *WorkloadReconciler) reconcileOnClusterQueueActiveState(ctx context.Cont
 		log.V(3).Info("Workload is evicted because the ClusterQueue is stopped", "clusterQueue", klog.KRef("", cqName))
 		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByClusterQueueStopped, "The ClusterQueue is stopped")
 		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByClusterQueueStopped)
```

  This one you could check [here](https://github.com/kubernetes-sigs/kueue/blob/36b340e396341c905444576bf797a221cb5f92ac/test/integration/scheduler/scheduler_test.go#L1778), by `util.ExpectEvictedWorkloadsTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByClusterQueueStopped, 1)`. 
  
  However, it turns out that the metric counts as 2 at this point (I checked that), which suggests a bug, because we trigger the patch twice. 
  
  The second time is called when the Evicted=true condition is already there, I think if the `Evicted` condition is already there, then we can skip sending the patch again (and thus bumping the metric). 
  
  If this is the case, then the PR already revealed a performance improvement in this scenario. cc @alculquicondor

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-24T08:58:41Z

- **pkg/controller/core/workload_controller.go:354** by [@mimowo](https://github.com/mimowo) — 2024-04-24T08:58:41Z
```diff
@@ -309,6 +313,9 @@ func (r *WorkloadReconciler) reconcileOnClusterQueueActiveState(ctx context.Cont
 		log.V(3).Info("Workload is evicted because the ClusterQueue is stopped", "clusterQueue", klog.KRef("", cqName))
 		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByClusterQueueStopped, "The ClusterQueue is stopped")
 		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByClusterQueueStopped)
```

  Note how we avoid sending eviction request twice in case of preemption: https://github.com/kubernetes-sigs/kueue/blob/36b340e396341c905444576bf797a221cb5f92ac/pkg/scheduler/preemption/preemption.go#L156

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-24T09:00:54Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:322** by [@mimowo](https://github.com/mimowo) — 2024-04-24T09:00:54Z
```diff
@@ -319,6 +319,7 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 							kueue.WorkloadEvictedByPreemption, "By test", "evict"),
 					).Should(gomega.Succeed())
 					util.FinishEvictionForWorkloads(ctx, k8sClient, createdWorkload)
+					util.ExpectEvictedWorkloadsTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByPreemption, 1)
```

  You can cover this one [here](https://github.com/kubernetes-sigs/kueue/blob/36b340e396341c905444576bf797a221cb5f92ac/test/integration/scheduler/preemption_test.go#L139) instead with `util.ExpectEvictedWorkloadsTotalMetric(cq.Name, kueue.WorkloadEvictedByPreemption, 2)`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-24T09:22:59Z

- **pkg/controller/core/workload_controller.go:314** by [@mimowo](https://github.com/mimowo) — 2024-04-24T09:22:59Z
```diff
@@ -269,6 +269,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	log.V(3).Info("Workload is evicted due to admission checks")
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
 	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	if err == nil {
+		cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
+		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
```

  This one you could cover [here](https://github.com/kubernetes-sigs/kueue/blob/9b25d998b4cc317ab9539e55497be49fa19588d3/test/integration/controller/core/workload_controller_test.go#L382) with `util.ExpectEvictedWorkloadsTotalMetric(clusterQueue.Name, kueue.WorkloadEvictedByAdmissionCheck, 1)`.
  
  However, it reveals a bug that `ClusterQueueFromLocalQueue` expects the namespace/LQ name. 
  To fix that I suggest to create a function: `ClusterQueueForWorkload` which would call `ClusterQueueFromLocalQueue` for `fmt.Sprintf("%s/%s", wl.Namespace, wl.Spec.QueueName)`.
  
  Then use this function here, and in the other place.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-24T09:23:19Z

- **pkg/controller/core/workload_controller.go:394** by [@mimowo](https://github.com/mimowo) — 2024-04-24T09:23:19Z
```diff
@@ -383,6 +390,10 @@ func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req c
 	}
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
 	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	if err == nil {
+		cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
```

  This needs a fix, as in the other comment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-24T09:26:21Z

- **pkg/controller/core/workload_controller.go:432** by [@mimowo](https://github.com/mimowo) — 2024-04-24T09:26:21Z
```diff
@@ -383,6 +390,10 @@ func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req c
 	}
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByPodsReadyTimeout, fmt.Sprintf("Exceeded the PodsReady timeout %s", req.NamespacedName.String()))
 	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	if err == nil {
+		cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
+		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByPodsReadyTimeout)
```

  This you can cover [here](https://github.com/kubernetes-sigs/kueue/blob/9b25d998b4cc317ab9539e55497be49fa19588d3/test/integration/scheduler/podsready/scheduler_test.go#L245) with `util.ExpectEvictedWorkloadsTotalMetric(prodClusterQ.Name, kueue.WorkloadEvictedByPodsReadyTimeout, 1)`

### Review by [@lowang-bh](https://github.com/lowang-bh) (commented) — 2024-04-27T07:50:48Z

- **pkg/controller/core/workload_controller.go:354** by [@lowang-bh](https://github.com/lowang-bh) — 2024-04-27T07:50:48Z
```diff
@@ -309,6 +313,9 @@ func (r *WorkloadReconciler) reconcileOnClusterQueueActiveState(ctx context.Cont
 		log.V(3).Info("Workload is evicted because the ClusterQueue is stopped", "clusterQueue", klog.KRef("", cqName))
 		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByClusterQueueStopped, "The ClusterQueue is stopped")
 		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByClusterQueueStopped)
```

  I have add a conditon check before do patch:
  ```go
  		if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted)  {
  			log.V(3).Info("Workload is already evicted due to clusterQueue stopped.")
  			return false, nil
  		}
  ```

### Review by [@lowang-bh](https://github.com/lowang-bh) (commented) — 2024-04-27T08:08:03Z

- **pkg/controller/core/workload_controller.go:314** by [@lowang-bh](https://github.com/lowang-bh) — 2024-04-27T08:08:03Z
```diff
@@ -269,6 +269,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	log.V(3).Info("Workload is evicted due to admission checks")
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
 	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	if err == nil {
+		cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
+		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
```

  There is already a func `ClusterQueueForWorkload`.
  ```go
  func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (string, bool) {
  	m.RLock()
  	defer m.RUnlock()
  	q, ok := m.localQueues[workload.QueueKey(wl)]
  	if !ok {
  		return "", false
  	}
  	_, ok = m.clusterQueues[q.ClusterQueue]
  	return q.ClusterQueue, ok
  }
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-30T15:30:57Z

- **pkg/util/testing/metrics/metrics.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T15:30:57Z
```diff
@@ -27,8 +27,9 @@ import (
 )
 
 type GaugeDataPoint struct {
-	Labels map[string]string
-	Value  float64
+	Labels       map[string]string
+	GaugeValue   float64
+	CounterValue float64
```

  This type is called `GaugeDataPoint`, yet it has two types of values.
  
  Maybe it should be renamed to just `DataPoint` instead?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-30T15:31:21Z

- **pkg/util/testing/metrics/metrics.go:32** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T15:31:21Z
```diff
@@ -27,8 +27,9 @@ import (
 )
 
 type GaugeDataPoint struct {
-	Labels map[string]string
-	Value  float64
+	Labels       map[string]string
+	GaugeValue   float64
+	CounterValue float64
```

  And do we really need the two separate fields?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T07:01:50Z

- **pkg/controller/core/workload_controller.go:314** by [@mimowo](https://github.com/mimowo) — 2024-05-07T07:01:50Z
```diff
@@ -269,6 +269,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	log.V(3).Info("Workload is evicted due to admission checks")
 	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
 	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	if err == nil {
+		cqName, _ := r.queues.ClusterQueueFromLocalQueue(wl.Spec.QueueName)
+		metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByAdmissionCheck)
```

  oh, this is even better then :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T07:04:41Z

- **pkg/controller/jobframework/reconciler.go:406** by [@mimowo](https://github.com/mimowo) — 2024-05-07T07:04:41Z
```diff
@@ -398,7 +403,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if job.IsSuspended() {
 		// start the job if the workload has been admitted, and the job is still suspended
 		if workload.IsAdmitted(wl) {
-			log.V(2).Info("Job admitted, unsuspending")
+			log.V(5).Info("Job admitted, unsuspending")
```

  I would suggest not to change this in the PR, because level 2 is often used as default.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T08:00:11Z

- **test/integration/scheduler/podsready/scheduler_test.go:299** by [@mimowo](https://github.com/mimowo) — 2024-05-07T08:00:11Z
```diff
@@ -293,6 +294,9 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 				})
 				g.Expect(k8sClient.Status().Update(ctx, prodWl)).Should(gomega.Succeed())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed(), "Job reconciler should add an Evicted condition with InactiveWorkload to the Workload")
+
+			// ReconcileGenericJob didn't run here
+			// util.ExpectEvictedWorkloadsTotalMetric(prodClusterQ.Name, kueue.WorkloadEvictedByDeactivation, 1)
```

  I think this requires some investigation, or explanation why we don't see the metric bumped.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T09:01:48Z

- **test/integration/scheduler/podsready/scheduler_test.go:299** by [@mimowo](https://github.com/mimowo) — 2024-05-07T09:01:48Z
```diff
@@ -293,6 +294,9 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 				})
 				g.Expect(k8sClient.Status().Update(ctx, prodWl)).Should(gomega.Succeed())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed(), "Job reconciler should add an Evicted condition with InactiveWorkload to the Workload")
+
+			// ReconcileGenericJob didn't run here
+			// util.ExpectEvictedWorkloadsTotalMetric(prodClusterQ.Name, kueue.WorkloadEvictedByDeactivation, 1)
```

  l looked closer into this, and it appears to be fixed here: https://github.com/kubernetes-sigs/kueue/pull/2131/. The metric should also be bumped into the new section in the `workload_controller` now. 
  
  Additionally, IIUC we can now remove the block above, please do so: `TODO: Once we move a logic to issue the Eviction with InactiveWorkload reason, we need to remove the below updates.`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T09:03:38Z

- **pkg/util/testing/metrics/metrics.go:32** by [@mimowo](https://github.com/mimowo) — 2024-05-07T09:03:37Z
```diff
@@ -27,8 +27,9 @@ import (
 )
 
 type GaugeDataPoint struct {
-	Labels map[string]string
-	Value  float64
+	Labels       map[string]string
+	GaugeValue   float64
+	CounterValue float64
```

  Looking at the code this looks addressed now. @lowang-bh please resolve if you can confirm.

### Review by [@lowang-bh](https://github.com/lowang-bh) (commented) — 2024-05-08T13:54:41Z

- **pkg/controller/jobframework/reconciler.go:406** by [@lowang-bh](https://github.com/lowang-bh) — 2024-05-08T13:54:41Z
```diff
@@ -398,7 +403,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	if job.IsSuspended() {
 		// start the job if the workload has been admitted, and the job is still suspended
 		if workload.IsAdmitted(wl) {
-			log.V(2).Info("Job admitted, unsuspending")
+			log.V(5).Info("Job admitted, unsuspending")
```

  Reverted.

### Review by [@lowang-bh](https://github.com/lowang-bh) (commented) — 2024-05-08T13:55:27Z

- **test/integration/scheduler/podsready/scheduler_test.go:299** by [@lowang-bh](https://github.com/lowang-bh) — 2024-05-08T13:55:27Z
```diff
@@ -293,6 +294,9 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 				})
 				g.Expect(k8sClient.Status().Update(ctx, prodWl)).Should(gomega.Succeed())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed(), "Job reconciler should add an Evicted condition with InactiveWorkload to the Workload")
+
+			// ReconcileGenericJob didn't run here
+			// util.ExpectEvictedWorkloadsTotalMetric(prodClusterQ.Name, kueue.WorkloadEvictedByDeactivation, 1)
```

  > l looked closer into this, and it appears to be fixed here: #2131. The metric should also be bumped into the new section in the `workload_controller` now.
  
  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-08T15:15:12Z

/lgtm
Thanks

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-08T18:47:58Z

/approve
