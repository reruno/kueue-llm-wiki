# PR #1959: Copy some labels from Pods/Jobs into the Workload object

**Summary**: Copy some labels from Pods/Jobs into the Workload object

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1959

**Last updated**: 2024-04-15T05:04:34Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `copy_labels` → `main`
- **Created**: 2024-04-09T09:39:16Z
- **Updated**: 2024-04-15T05:04:34Z
- **Merged**: 2024-04-12T15:04:50Z
- **Closed**: 2024-04-12T15:04:50Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 16   **+505 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature
#### What this PR does / why we need it:
When creating a Workload object from Pod/Job, copy labels of the Pod/Job into the Workload object. It will allow to easily identify and filter the Workload objects. 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1834

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Added label copying from Pod/Job into the Kueue Workload.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-09T09:39:26Z

Hi @pajakd. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-09T09:39:34Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4130abff20d44c32d324fda0d46ebb97dbb45879 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6619304a69a5e40008d6e42d |

### Comment by [@kannon92](https://github.com/kannon92) — 2024-04-09T11:06:07Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-12T14:53:13Z

LGTM label has been added.  <details>Git tree hash: eb17443205045edb36e8a055bd337161a9d89c32</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-12T14:53:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1959#pullrequestreview-1997476089" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1959#" title="Author self-approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-09T16:03:02Z

- **apis/config/v1beta1/configuration_types.go:306** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T15:43:33Z
```diff
@@ -301,6 +301,10 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from job
+	// into the workload object
```

  Explain what happens if the job doesn't have the label.
  
  And, in the case of composable jobs, what happens if there are different values in different objects.

- **keps/1834-copy-labels-into-workload/README.md:44** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T15:46:05Z
```diff
@@ -0,0 +1,101 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
```

  What happens if the jobs don't have the label?

- **keps/1834-copy-labels-into-workload/kep.yaml:11** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T15:46:31Z
```diff
@@ -0,0 +1,30 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: draft
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@gabesaba"
+approvers:
+  - TBD
```

  remove this line

- **keps/1834-copy-labels-into-workload/kep.yaml:16** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T15:47:26Z
```diff
@@ -0,0 +1,30 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: draft
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@gabesaba"
+approvers:
+  - TBD
+  - "@alculquicondor"
+
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
```

  Since there is no feature gate, maybe we should call this as beta (to match the API version).

- **keps/1834-copy-labels-into-workload/kep.yaml:30** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T15:47:28Z
```diff
@@ -0,0 +1,30 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: draft
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@gabesaba"
+approvers:
+  - TBD
+  - "@alculquicondor"
+
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.7"
+  beta: "v0.8"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+disable-supported: true
```

  is it?
  There is no feature gate (I wouldn't add one).

- **pkg/controller/jobframework/reconciler.go:802** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T15:51:20Z
```diff
@@ -784,16 +794,29 @@ func (r *JobReconciler) finalizeJob(ctx context.Context, job GenericJob) error {
 	return nil
 }
 
+func (r *JobReconciler) getWorkloadLabels(job GenericJob) map[string]string {
+	jobLabels := job.Object().GetLabels()
+	workloadLabels := make(map[string]string)
+	if r.labelKeysToCopy == nil {
+		return workloadLabels
+	}
```

  Reducing allocations:
  ```suggestion
  	if len(r.labelKeysToCopy) == 0 {
  		return nil
  	}
  	workloadLabels := make(map[string]string, len(r.labelKeysToCopy))
  ```

- **pkg/controller/jobs/pod/pod_controller.go:906** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T15:58:43Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	jobLabels := p.Object().GetLabels()
+	workloadLabels := make(map[string]string)
+	if labelKeysToCopy == nil {
```

  Similar recommendation as the job controller.

- **pkg/controller/jobs/pod/pod_controller.go:82** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T15:59:37Z
```diff
@@ -78,6 +79,7 @@ var (
 	errIncorrectReconcileRequest = fmt.Errorf("event handler error: got a single pod reconcile request for a pod group")
 	errPendingOps                = jobframework.UnretryableError("waiting to observe previous operations on pods")
 	errPodNoSupportKubeVersion   = errors.New("pod integration only supported in Kubernetes 1.27 or newer")
+	errPodGroupLabelsMismatch    = errors.New("error when constructing workload: pods have different label values")
```

  It's already an error, so no need to put it in the message
  ```suggestion
  	errPodGroupLabelsMismatch    = errors.New("constructing workload: pods have different label values")
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:141** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T16:02:09Z
```diff
@@ -135,6 +138,10 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
 		gomega.Expect(createdWorkload.Annotations).Should(gomega.Equal(map[string]string{"provreq.kueue.x-k8s.io/ValidUntilSeconds": "0"}))
 
+		ginkgo.By("checking the workload gets assigned the correct labels")
```

  remove this line. Calling `By` is only useful when you are producing a change that the system has to react to.

- **test/integration/controller/jobs/pod/pod_controller_test.go:521** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T16:02:59Z
```diff
@@ -509,12 +510,15 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 					Group("test-group").
 					GroupTotalCount("2").
 					Queue("test-queue").
+					Label("dontCopyKey", "dontCopyValue").
 					Obj()
 				pod2 := testingpod.MakePod("test-pod2", ns.Name).
 					Group("test-group").
 					GroupTotalCount("2").
 					Request(corev1.ResourceCPU, "1").
 					Queue("test-queue").
+					Label("toCopyKey", "toCopyValue").
+					Label("dontCopyKey", "dontCopyValue").
```

  nit
  ```suggestion
  					Label("dontCopyKey", "dontCopyAnotherValue").
  ```
  
  To make sure different values here don't prevent the Workload creation.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-10T07:56:21Z

- **apis/config/v1beta1/configuration_types.go:306** by [@pajakd](https://github.com/pajakd) — 2024-04-10T07:56:21Z
```diff
@@ -301,6 +301,10 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from job
+	// into the workload object
```

  When the label is not there it is not copied (no error, no warning). Added an explanation in the comment (also about the case of mismatched labels).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-10T08:27:54Z

Thank you for the comments. I think I addressed them. Please take a look if everything looks ok.

- **keps/1834-copy-labels-into-workload/README.md:44** by [@pajakd](https://github.com/pajakd) — 2024-04-10T07:59:15Z
```diff
@@ -0,0 +1,101 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
```

  Added an explanation.

- **keps/1834-copy-labels-into-workload/kep.yaml:11** by [@pajakd](https://github.com/pajakd) — 2024-04-10T07:59:51Z
```diff
@@ -0,0 +1,30 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: draft
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@gabesaba"
+approvers:
+  - TBD
```

  Done.

- **keps/1834-copy-labels-into-workload/kep.yaml:16** by [@pajakd](https://github.com/pajakd) — 2024-04-10T08:00:21Z
```diff
@@ -0,0 +1,30 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: draft
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@gabesaba"
+approvers:
+  - TBD
+  - "@alculquicondor"
+
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
```

  Done.

- **keps/1834-copy-labels-into-workload/kep.yaml:30** by [@pajakd](https://github.com/pajakd) — 2024-04-10T08:01:52Z
```diff
@@ -0,0 +1,30 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: draft
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@gabesaba"
+approvers:
+  - TBD
+  - "@alculquicondor"
+
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.7"
+  beta: "v0.8"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+disable-supported: true
```

  Oh, sorry.  Removed the `disable-supported` and the comments.

- **pkg/controller/jobframework/reconciler.go:802** by [@pajakd](https://github.com/pajakd) — 2024-04-10T08:23:25Z
```diff
@@ -784,16 +794,29 @@ func (r *JobReconciler) finalizeJob(ctx context.Context, job GenericJob) error {
 	return nil
 }
 
+func (r *JobReconciler) getWorkloadLabels(job GenericJob) map[string]string {
+	jobLabels := job.Object().GetLabels()
+	workloadLabels := make(map[string]string)
+	if r.labelKeysToCopy == nil {
+		return workloadLabels
+	}
```

  Workload must have the `Labels` field initialized because later we use it (assigning JobUIDLabel) for every workload (that has a valid jobuid). 
  
  Since output of this function is used directly as `Labels` in the workload object we (probably) should not return `nil`. 
  
  I did apply the suggested change (but returning empty list instead of nil).

- **pkg/controller/jobs/pod/pod_controller.go:82** by [@pajakd](https://github.com/pajakd) — 2024-04-10T08:23:41Z
```diff
@@ -78,6 +79,7 @@ var (
 	errIncorrectReconcileRequest = fmt.Errorf("event handler error: got a single pod reconcile request for a pod group")
 	errPendingOps                = jobframework.UnretryableError("waiting to observe previous operations on pods")
 	errPodNoSupportKubeVersion   = errors.New("pod integration only supported in Kubernetes 1.27 or newer")
+	errPodGroupLabelsMismatch    = errors.New("error when constructing workload: pods have different label values")
```

  Done

- **pkg/controller/jobs/pod/pod_controller.go:906** by [@pajakd](https://github.com/pajakd) — 2024-04-10T08:25:26Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	jobLabels := p.Object().GetLabels()
+	workloadLabels := make(map[string]string)
+	if labelKeysToCopy == nil {
```

  Here we can return nil, so I'm applying the suggestion entirely.

- **test/integration/controller/jobs/job/job_controller_test.go:141** by [@pajakd](https://github.com/pajakd) — 2024-04-10T08:25:48Z
```diff
@@ -135,6 +138,10 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
 		gomega.Expect(createdWorkload.Annotations).Should(gomega.Equal(map[string]string{"provreq.kueue.x-k8s.io/ValidUntilSeconds": "0"}))
 
+		ginkgo.By("checking the workload gets assigned the correct labels")
```

  Ok, done

- **test/integration/controller/jobs/pod/pod_controller_test.go:521** by [@pajakd](https://github.com/pajakd) — 2024-04-10T08:25:59Z
```diff
@@ -509,12 +510,15 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 					Group("test-group").
 					GroupTotalCount("2").
 					Queue("test-queue").
+					Label("dontCopyKey", "dontCopyValue").
 					Obj()
 				pod2 := testingpod.MakePod("test-pod2", ns.Name).
 					Group("test-group").
 					GroupTotalCount("2").
 					Request(corev1.ResourceCPU, "1").
 					Queue("test-queue").
+					Label("toCopyKey", "toCopyValue").
+					Label("dontCopyKey", "dontCopyValue").
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T11:33:21Z

Some questions to the KEP for now.

- **keps/1834-copy-labels-into-workload/README.md:45** by [@mimowo](https://github.com/mimowo) — 2024-04-10T11:22:24Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
```

  Is the Workload object created only when we have all the pods in a group created? If it is created when the first pod is created, then we may not have a chance to raise the exception. 
  Also, related:
  - when is the exception raised, it is during workload creation, or later? If later, how is the error communicated to the user?
  - what happens when the workload already exists, and the user creates a new pod with a non-matching value?

- **keps/1834-copy-labels-into-workload/README.md:29** by [@mimowo](https://github.com/mimowo) — 2024-04-10T11:30:01Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
```

  What about labels updated for an existing Job? Should we update it , or keep the original only? If we keep the original add it to non-goals.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-10T12:39:00Z

Thank you for the comments!

- **keps/1834-copy-labels-into-workload/README.md:29** by [@pajakd](https://github.com/pajakd) — 2024-04-10T12:19:37Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
```

  I think the idea is to copy the labels only at the moment of creation of the workload. Updating is out of scope (please correct me if I'm wrong). 
  
  Added to non-goals.

- **keps/1834-copy-labels-into-workload/README.md:45** by [@pajakd](https://github.com/pajakd) — 2024-04-10T12:35:57Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
```

  As far as I know the workload object is created when all the pods are created.
  
  The exception is raised during workload creation.  
  
  The new feature is only applied during the creation of a new workload so if the user creates another pod later it will have no effect on the labels on the running workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T13:29:06Z

- **keps/1834-copy-labels-into-workload/README.md:45** by [@mimowo](https://github.com/mimowo) — 2024-04-10T13:29:06Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
```

  > As far as I know the workload object is created when all the pods are created.
  
  This would simplify this indeed. Could you please double check on a running system?
  
  > The exception is raised during workload creation.
  > The new feature is only applied during the creation of a new workload so if the user creates another pod later it will have no effect on the labels on the running workload.
  
  sgtm, please add a sentence along the lines to clarify in KEP for future readers.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T13:29:43Z

- **keps/1834-copy-labels-into-workload/README.md:29** by [@mimowo](https://github.com/mimowo) — 2024-04-10T13:29:43Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
```

  sgtm, thanks for updating the KEP

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-10T13:40:35Z

I reviewed only proposal.

- **keps/1834-copy-labels-into-workload/kep.yaml:5** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T11:57:20Z
```diff
@@ -0,0 +1,25 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: draft
```

  ```suggestion
  status: implementable
  ```
  
  Isn't this `implementable`?

- **keps/1834-copy-labels-into-workload/README.md:29** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T13:26:22Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
```

  @pajakd So, what happens when the users update the label of Job? Will we implement validations to prevent updating job labels?

- **keps/1834-copy-labels-into-workload/README.md:47** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T13:37:36Z
```diff
@@ -0,0 +1,104 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+* This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+* This proposal only concerns copying of labels into newly created workloads. Updating the labels of an existing workload (for example if the label of the underlying job is changed) is out of scope of this proposal.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
+
```

  Could you add a concrete API design here? Other Keps would be helpful to understand how to add an API design here.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-10T17:37:46Z

- **keps/1834-copy-labels-into-workload/README.md:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T17:37:46Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
```

  > As far as I know the workload object is created when all the pods are created.
  
  That is correct.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-10T17:46:42Z

- **keps/1834-copy-labels-into-workload/README.md:55** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T17:38:03Z
```diff
@@ -0,0 +1,104 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+* This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+* This proposal only concerns copying of labels into newly created workloads. Updating the labels of an existing workload (for example if the label of the underlying job is changed) is out of scope of this proposal.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
+
+
+### Risks and Mitigations
+
+None.
+
+## Design Details
+
+We believe that the KEP is rather straightforward and the [Proposal](#proposal) section contains sufficient details of the implementation.
```

  You can add the configuration API detail here.

- **apis/config/v1beta1/configuration_types.go:311** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T17:38:55Z
```diff
@@ -301,6 +301,15 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
+	// object. We don't require the job to have all the labels from this list. If
+	// a job does not have some label with the given key from this list, the
+	// constructed workload object will be created without this label. In the case
+	// of creating a workload from a composable job (pod group), if multiple objects
+	// have labels with some key form  the  list, the values of these labels must
+	// agree or otherwise the workload creation would fail.
```

  Also mention that if the Job labels change after the first Workload object is created, the Workload will not be updated.

- **keps/1834-copy-labels-into-workload/README.md:29** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T17:40:35Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
```

  IIUC, nothing will happen if the user changes the labels. But we should add that to the API documentation.

- **pkg/controller/jobframework/reconciler.go:798** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T17:43:03Z
```diff
@@ -784,16 +794,29 @@ func (r *JobReconciler) finalizeJob(ctx context.Context, job GenericJob) error {
 	return nil
 }
 
+func (r *JobReconciler) getWorkloadLabels(job GenericJob) map[string]string {
+	jobLabels := job.Object().GetLabels()
```

  move this line after the check for `len(r.labelKeysToCopy)`.

- **test/integration/controller/jobs/pod/pod_controller_test.go:576** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T17:46:40Z
```diff
@@ -569,6 +573,9 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 						},
 						wlConditionCmpOpts...,
 					))
+					ginkgo.By("checking the workload gets assigned the correct labels")
```

  ```suggestion
  					// Checking the workload gets assigned the correct labels.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-11T07:51:26Z

- **pkg/controller/jobs/pod/pod_controller.go:904** by [@mimowo](https://github.com/mimowo) — 2024-04-11T07:00:37Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	jobLabels := p.Object().GetLabels()
```

  nit: here also move below the len check

- **pkg/controller/jobs/pod/pod_controller.go:913** by [@mimowo](https://github.com/mimowo) — 2024-04-11T07:06:14Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	jobLabels := p.Object().GetLabels()
+	if len(labelKeysToCopy) == 0 {
+		return nil, nil
+	}
+	workloadLabels := make(map[string]string, len(labelKeysToCopy))
+	for _, labelKey := range labelKeysToCopy {
+		if labelValue, found := jobLabels[labelKey]; found {
+			workloadLabels[labelKey] = labelValue
+		}
+	}
```

  Could we move this pass under the `!p.isGroup` check? It seems the case when this is a group is handled below.

- **pkg/controller/jobs/pod/pod_controller_test.go:3387** by [@mimowo](https://github.com/mimowo) — 2024-04-11T07:23:26Z
```diff
@@ -3371,6 +3372,138 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"workload is created with correct labels for a single pod": {
+			pods: []corev1.Pod{*basePodWrapper.
+				Clone().
+				Label("kueue.x-k8s.io/managed", "true").
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				KueueFinalizer().
+				KueueSchedulingGate().
+				Queue("test-queue").
+				Obj()},
+			wantPods: nil,
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithLabelKeysToCopy([]string{"toCopyKey"}),
```

  nit: add also a label key name that is absent in the pod.

- **pkg/controller/jobs/pod/pod_controller.go:962** by [@mimowo](https://github.com/mimowo) — 2024-04-11T07:51:07Z
```diff
@@ -934,7 +965,11 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 		if err := controllerutil.SetControllerReference(object, wl, c.Scheme()); err != nil {
 			return nil, err
 		}
-
+		labelsToCopy, err := p.getWorkloadLabels(labelKeysToCopy)
```

  This code looks repeated for groups and non groups., but the function already makes the distinction inside. I would suggest to call it early once, and pass the labels when creating the workload object already. This would also match the flow for other job CRDs, and no need to merge.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:34:35Z

- **pkg/controller/jobs/pod/pod_controller.go:962** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:34:35Z
```diff
@@ -934,7 +965,11 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 		if err := controllerutil.SetControllerReference(object, wl, c.Scheme()); err != nil {
 			return nil, err
 		}
-
+		labelsToCopy, err := p.getWorkloadLabels(labelKeysToCopy)
```

  Your idea would make it cleaner but here is the reason why its proposed this way:
  
  In the case of pod group we select only the active pods and cleanup excess pods 
  ````go
  // Cleanup extra pods if there's any
  	if excessPodsCount := len(activePods) - groupTotalCount; excessPodsCount > 0 {
  		sortActivePods(activePods)
  		err = p.removeExcessPods(ctx, c, r, activePods[len(activePods)-excessPodsCount:])
  		if err != nil {
  			return nil, err
  		}
  		p.list.Items = activePods[:len(activePods)-excessPodsCount]
  	}
  ````
  And this happens later in the code (after the workload object is created). So at the moment of workload creation some excess pods potentially could still be there. To be safe, I don't want to copy the labels from the excess pods so I should to my copying after the cleanup

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:34:56Z

- **pkg/controller/jobs/pod/pod_controller_test.go:3387** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:34:56Z
```diff
@@ -3371,6 +3372,138 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"workload is created with correct labels for a single pod": {
+			pods: []corev1.Pod{*basePodWrapper.
+				Clone().
+				Label("kueue.x-k8s.io/managed", "true").
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				KueueFinalizer().
+				KueueSchedulingGate().
+				Queue("test-queue").
+				Obj()},
+			wantPods: nil,
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithLabelKeysToCopy([]string{"toCopyKey"}),
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:35:01Z

- **pkg/controller/jobs/pod/pod_controller.go:913** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:35:01Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	jobLabels := p.Object().GetLabels()
+	if len(labelKeysToCopy) == 0 {
+		return nil, nil
+	}
+	workloadLabels := make(map[string]string, len(labelKeysToCopy))
+	for _, labelKey := range labelKeysToCopy {
+		if labelValue, found := jobLabels[labelKey]; found {
+			workloadLabels[labelKey] = labelValue
+		}
+	}
```

  This is handling the case when the object is a single pod. We have to copy the labels from the single pod into the workload in this case.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:35:05Z

- **pkg/controller/jobs/pod/pod_controller.go:904** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:35:05Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	jobLabels := p.Object().GetLabels()
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:35:09Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:576** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:35:09Z
```diff
@@ -569,6 +573,9 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 						},
 						wlConditionCmpOpts...,
 					))
+					ginkgo.By("checking the workload gets assigned the correct labels")
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:35:13Z

- **pkg/controller/jobframework/reconciler.go:798** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:35:13Z
```diff
@@ -784,16 +794,29 @@ func (r *JobReconciler) finalizeJob(ctx context.Context, job GenericJob) error {
 	return nil
 }
 
+func (r *JobReconciler) getWorkloadLabels(job GenericJob) map[string]string {
+	jobLabels := job.Object().GetLabels()
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:35:17Z

- **keps/1834-copy-labels-into-workload/README.md:55** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:35:17Z
```diff
@@ -0,0 +1,104 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+* This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+* This proposal only concerns copying of labels into newly created workloads. Updating the labels of an existing workload (for example if the label of the underlying job is changed) is out of scope of this proposal.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
+
+
+### Risks and Mitigations
+
+None.
+
+## Design Details
+
+We believe that the KEP is rather straightforward and the [Proposal](#proposal) section contains sufficient details of the implementation.
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:35:36Z

- **keps/1834-copy-labels-into-workload/README.md:47** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:35:36Z
```diff
@@ -0,0 +1,104 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+* This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+* This proposal only concerns copying of labels into newly created workloads. Updating the labels of an existing workload (for example if the label of the underlying job is changed) is out of scope of this proposal.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
+
```

  Done. I added the API change details to the KEP.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:35:42Z

- **keps/1834-copy-labels-into-workload/kep.yaml:5** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:35:42Z
```diff
@@ -0,0 +1,25 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: draft
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:35:50Z

- **keps/1834-copy-labels-into-workload/README.md:29** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:35:50Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
```

  Yes, nothing happens if the user changes the labels. I added this information to the API documentation

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T09:36:04Z

- **keps/1834-copy-labels-into-workload/README.md:45** by [@pajakd](https://github.com/pajakd) — 2024-04-11T09:36:04Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
```

  Done. Added a sentence to KEP clarifying this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-11T09:45:58Z

- **pkg/controller/jobs/pod/pod_controller.go:962** by [@mimowo](https://github.com/mimowo) — 2024-04-11T09:45:58Z
```diff
@@ -934,7 +965,11 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 		if err := controllerutil.SetControllerReference(object, wl, c.Scheme()); err != nil {
 			return nil, err
 		}
-
+		labelsToCopy, err := p.getWorkloadLabels(labelKeysToCopy)
```

  Ah, got it :) So this is to handle a situation when there is a mismatch, but in one of the excess pods.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-11T09:48:28Z

- **pkg/controller/jobs/pod/pod_controller.go:913** by [@mimowo](https://github.com/mimowo) — 2024-04-11T09:48:28Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	jobLabels := p.Object().GetLabels()
+	if len(labelKeysToCopy) == 0 {
+		return nil, nil
+	}
+	workloadLabels := make(map[string]string, len(labelKeysToCopy))
+	for _, labelKey := range labelKeysToCopy {
+		if labelValue, found := jobLabels[labelKey]; found {
+			workloadLabels[labelKey] = labelValue
+		}
+	}
```

  Yes, my idea was:
  ```golang
          workloadLabels := make(map[string]string, len(labelKeysToCopy))
  	if !p.isGroup {
  	        for _, labelKey := range labelKeysToCopy {
  		        if labelValue, found := jobLabels[labelKey]; found {
  			        workloadLabels[labelKey] = labelValue
  		        }
  	        }
  		return workloadLabels, nil
  	}
          // the code for group handling
  ```
  would it work?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T11:19:57Z

- **pkg/controller/jobs/pod/pod_controller.go:913** by [@pajakd](https://github.com/pajakd) — 2024-04-11T11:19:56Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	jobLabels := p.Object().GetLabels()
+	if len(labelKeysToCopy) == 0 {
+		return nil, nil
+	}
+	workloadLabels := make(map[string]string, len(labelKeysToCopy))
+	for _, labelKey := range labelKeysToCopy {
+		if labelValue, found := jobLabels[labelKey]; found {
+			workloadLabels[labelKey] = labelValue
+		}
+	}
```

  yes, I think its correct. Applied the suggested changes

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-11T18:12:22Z

design, implementation and tests LGTM.
The API comment needs a bit of work.

@tenzen-y anything to add?

- **apis/config/v1beta1/configuration_types.go:310** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:06:36Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
+	// object. We don't require the job to have all the labels from this list. If
+	// a job does not have some label with the given key from this list, the
+	// constructed workload object will be created without this label. In the case
+	// of creating a workload from a composable job (pod group), if multiple objects
+	// have labels with some key form  the  list, the values of these labels must
```

  ```suggestion
  	// have labels with some key from the list, the values of these labels must
  ```

- **apis/config/v1beta1/configuration_types.go:311** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:06:47Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
+	// object. We don't require the job to have all the labels from this list. If
+	// a job does not have some label with the given key from this list, the
+	// constructed workload object will be created without this label. In the case
+	// of creating a workload from a composable job (pod group), if multiple objects
+	// have labels with some key form  the  list, the values of these labels must
+	// agree or otherwise the workload creation would fail. The labels are copied only
```

  ```suggestion
  	// match or otherwise the workload creation would fail. The labels are copied only
  ```

- **apis/config/v1beta1/configuration_types.go:306** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:07:14Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
+	// object. We don't require the job to have all the labels from this list. If
```

  ```suggestion
  	// object. It is not required for the job to have all the labels from this list. If
  ```

- **keps/1834-copy-labels-into-workload/kep.yaml:11** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:09:13Z
```diff
@@ -0,0 +1,25 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: implementable
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@gabesaba"
+approvers:
+  - "@alculquicondor"
```

  add @tenzen-y as well

- **apis/config/v1beta1/configuration_types.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:10:26Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
```

  One general recommendation when writing documentation and API comments, is to use active voice https://kubernetes.io/docs/contribute/style/style-guide/#use-active-voice
  
  Try to adhere.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T19:08:06Z

- **apis/config/v1beta1/configuration_types.go:310** by [@pajakd](https://github.com/pajakd) — 2024-04-11T19:08:06Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
+	// object. We don't require the job to have all the labels from this list. If
+	// a job does not have some label with the given key from this list, the
+	// constructed workload object will be created without this label. In the case
+	// of creating a workload from a composable job (pod group), if multiple objects
+	// have labels with some key form  the  list, the values of these labels must
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T19:08:12Z

- **apis/config/v1beta1/configuration_types.go:311** by [@pajakd](https://github.com/pajakd) — 2024-04-11T19:08:12Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
+	// object. We don't require the job to have all the labels from this list. If
+	// a job does not have some label with the given key from this list, the
+	// constructed workload object will be created without this label. In the case
+	// of creating a workload from a composable job (pod group), if multiple objects
+	// have labels with some key form  the  list, the values of these labels must
+	// agree or otherwise the workload creation would fail. The labels are copied only
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T19:08:15Z

- **apis/config/v1beta1/configuration_types.go:306** by [@pajakd](https://github.com/pajakd) — 2024-04-11T19:08:15Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
+	// object. We don't require the job to have all the labels from this list. If
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T19:08:19Z

- **keps/1834-copy-labels-into-workload/kep.yaml:11** by [@pajakd](https://github.com/pajakd) — 2024-04-11T19:08:19Z
```diff
@@ -0,0 +1,25 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: implementable
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@gabesaba"
+approvers:
+  - "@alculquicondor"
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-11T19:09:52Z

- **apis/config/v1beta1/configuration_types.go:305** by [@pajakd](https://github.com/pajakd) — 2024-04-11T19:09:52Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
```

  Ok I'll read and follow the style guide. But here I don't see any good way of rephrasing "should be copied" into active voice...

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-11T19:13:37Z

- **apis/config/v1beta1/configuration_types.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T19:13:37Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
```

  Sounds good, there is the exception `Use passive voice if active voice leads to an awkward construction` :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-11T19:15:25Z

/approve

Leaving LGTM to @tenzen-y

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-11T20:20:39Z

Thank you for this update!
Basically, lgtm. I left a few minor comments.

- **apis/config/v1beta1/configuration_types.go:305** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T19:53:05Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
```

  ```suggestion
  	// labelKeysToCopy is a list of label keys that should be copied from the job into the workload
  ```

- **keps/1834-copy-labels-into-workload/README.md:29** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T19:53:47Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
```

  Thank you for the clarifications :)

- **keps/1834-copy-labels-into-workload/README.md:60** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T19:54:12Z
```diff
@@ -0,0 +1,121 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+* This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+* This proposal only concerns copying of labels into newly created workloads. Updating the labels of an existing workload (for example if the label of the underlying job is changed) is out of scope of this proposal.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception. The exception will be raised during the workload creation.
+
+
+### Risks and Mitigations
+
+None.
+
+## Design Details
+
+
+The proposal contains a single modification to the API. We propose to add a new field `LabelKeysToCopy` to `Integrations` struct in the `Configuration` API. 
+``` go
+type Integrations struct {
+    ...
+	// A list of label keys that should be copied from the job into the workload
```

  ```suggestion
  	// labelKeysToCopy is a list of label keys that should be copied from the job into the workload
  ```

- **keps/1834-copy-labels-into-workload/kep.yaml:25** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T19:57:29Z
```diff
@@ -0,0 +1,25 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: implementable
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@mimowo"
+approvers:
+  - "@alculquicondor"
+  - "@tenzen-y"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.7"
+  beta: "v0.8"
```

  ```suggestion
    beta: "v0.7"
  ```
  
  I'm wondering if we should declare this feature as a beta in v0.7, right?

- **pkg/controller/jobs/job/job_controller_test.go:486** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:01:51Z
```diff
@@ -457,6 +457,42 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"when workload is created, it has correct labels set": {
+			job: *baseJobWrapper.Clone().
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				UID("test-uid").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				UID("test-uid").
+				Suspend(true).
+				Obj(),
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithLabelKeysToCopy([]string{"toCopyKey", "redundantToCopyKey"}),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("job", "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Queue("foo").
+					Priority(0).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "test-uid",
+						"toCopyKey":                  "toCopyValue"}).
+					Obj(),
+			},
+
```

  ```suggestion
  ```

- **pkg/controller/jobframework/reconciler.go:799** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:08:19Z
```diff
@@ -784,16 +794,29 @@ func (r *JobReconciler) finalizeJob(ctx context.Context, job GenericJob) error {
 	return nil
 }
 
+func (r *JobReconciler) getWorkloadLabels(job GenericJob) map[string]string {
+	if len(r.labelKeysToCopy) == 0 {
+		return map[string]string{}
```

  I'm curious about if we can return just `nil`. WDYT?

- **pkg/controller/jobs/pod/pod_controller.go:914** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:09:31Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	if len(labelKeysToCopy) == 0 {
+		return nil, nil
+	}
+	podLabels := p.Object().GetLabels()
+	workloadLabels := make(map[string]string, len(labelKeysToCopy))
+	if !p.isGroup {
+		for _, labelKey := range labelKeysToCopy {
+			if labelValue, found := podLabels[labelKey]; found {
+				workloadLabels[labelKey] = labelValue
+			}
+		}
```

  This is completely the same logic as the generic reconciler:
  So, could we commonize them?
  
  ```go
  	jobLabels := job.Object().GetLabels()
  	workloadLabels := make(map[string]string, len(r.labelKeysToCopy))
  	for _, labelKey := range r.labelKeysToCopy {
  		if labelValue, found := jobLabels[labelKey]; found {
  			workloadLabels[labelKey] = labelValue
  		}
  	}
  ```

- **test/integration/controller/jobs/pod/pod_controller_test.go:576** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:14:58Z
```diff
@@ -569,6 +573,9 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 						},
 						wlConditionCmpOpts...,
 					))
+					// Checking the workload gets assigned the correct labels.
```

  ```suggestion
  					ginkgo.By("Checking the workload gets assigned the correct labels")
  ```
  
  This comment is helpful for the debug.

- **pkg/controller/jobs/job/job_controller_test.go:494** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:18:46Z
```diff
@@ -457,6 +457,42 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"when workload is created, it has correct labels set": {
+			job: *baseJobWrapper.Clone().
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				UID("test-uid").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				UID("test-uid").
+				Suspend(true).
+				Obj(),
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithLabelKeysToCopy([]string{"toCopyKey", "redundantToCopyKey"}),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("job", "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Queue("foo").
+					Priority(0).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "test-uid",
+						"toCopyKey":                  "toCopyValue"}).
+					Obj(),
+			},
+
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "job", Namespace: "ns"},
+					EventType: "Normal",
+					Reason:    "CreatedWorkload",
+					Message:   "Created Workload: ns/" + GetWorkloadNameForJob(baseJobWrapper.Name, types.UID("test-uid")),
+				},
+			},
+		},
```

  Could you add another case to verify if the labels are not populated to Workloads after the Workload is already created?
  
  I'd like to prove this comment: "The labels are copied only during the workload creation and are not updated even if the labels of the underlying job are changed."

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-11T20:33:00Z

- **keps/1834-copy-labels-into-workload/kep.yaml:25** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T20:32:59Z
```diff
@@ -0,0 +1,25 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: implementable
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@mimowo"
+approvers:
+  - "@alculquicondor"
+  - "@tenzen-y"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.7"
+  beta: "v0.8"
```

  +1

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-12T07:55:59Z

- **keps/1834-copy-labels-into-workload/README.md:45** by [@mimowo](https://github.com/mimowo) — 2024-04-12T07:55:59Z
```diff
@@ -0,0 +1,103 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception.
```

  Thanks for clarifying and the update.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-12T07:59:59Z

- **keps/1834-copy-labels-into-workload/kep.yaml:25** by [@mimowo](https://github.com/mimowo) — 2024-04-12T07:59:58Z
```diff
@@ -0,0 +1,25 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: implementable
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@mimowo"
+approvers:
+  - "@alculquicondor"
+  - "@tenzen-y"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.7"
+  beta: "v0.8"
```

  I'm ok without a feature gate since the risk is minimal, but if there is no feature gate, should we declare "beta", or "stable" already?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-12T12:56:34Z

- **pkg/controller/jobs/job/job_controller_test.go:494** by [@pajakd](https://github.com/pajakd) — 2024-04-12T12:56:34Z
```diff
@@ -457,6 +457,42 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"when workload is created, it has correct labels set": {
+			job: *baseJobWrapper.Clone().
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				UID("test-uid").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				UID("test-uid").
+				Suspend(true).
+				Obj(),
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithLabelKeysToCopy([]string{"toCopyKey", "redundantToCopyKey"}),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("job", "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Queue("foo").
+					Priority(0).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "test-uid",
+						"toCopyKey":                  "toCopyValue"}).
+					Obj(),
+			},
+
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "job", Namespace: "ns"},
+					EventType: "Normal",
+					Reason:    "CreatedWorkload",
+					Message:   "Created Workload: ns/" + GetWorkloadNameForJob(baseJobWrapper.Name, types.UID("test-uid")),
+				},
+			},
+		},
```

  I added a check in the job_controller integration test. Please take a look.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-12T12:56:39Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:576** by [@pajakd](https://github.com/pajakd) — 2024-04-12T12:56:39Z
```diff
@@ -569,6 +573,9 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 						},
 						wlConditionCmpOpts...,
 					))
+					// Checking the workload gets assigned the correct labels.
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-12T12:56:43Z

- **pkg/controller/jobs/pod/pod_controller.go:914** by [@pajakd](https://github.com/pajakd) — 2024-04-12T12:56:43Z
```diff
@@ -898,7 +900,36 @@ func (p *Pod) ensureWorkloadOwnedByAllMembers(ctx context.Context, c client.Clie
 	return nil
 }
 
-func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client, r record.EventRecorder) (*kueue.Workload, error) {
+func (p *Pod) getWorkloadLabels(labelKeysToCopy []string) (map[string]string, error) {
+	if len(labelKeysToCopy) == 0 {
+		return nil, nil
+	}
+	podLabels := p.Object().GetLabels()
+	workloadLabels := make(map[string]string, len(labelKeysToCopy))
+	if !p.isGroup {
+		for _, labelKey := range labelKeysToCopy {
+			if labelValue, found := podLabels[labelKey]; found {
+				workloadLabels[labelKey] = labelValue
+			}
+		}
```

  Ok, good idea. So this is in fact filtering a map with a certain list of keys. I extracted this into `maps` library (added a generic `maps.FilterKeys` function and a bunch of unit tests). And I'm using it here and in the generic reconciler. Please take a look if it looks better.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-12T12:56:54Z

- **pkg/controller/jobframework/reconciler.go:799** by [@pajakd](https://github.com/pajakd) — 2024-04-12T12:56:54Z
```diff
@@ -784,16 +794,29 @@ func (r *JobReconciler) finalizeJob(ctx context.Context, job GenericJob) error {
 	return nil
 }
 
+func (r *JobReconciler) getWorkloadLabels(job GenericJob) map[string]string {
+	if len(r.labelKeysToCopy) == 0 {
+		return map[string]string{}
```

  I reworked this logic (see the comment below).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-12T12:57:00Z

- **pkg/controller/jobs/job/job_controller_test.go:486** by [@pajakd](https://github.com/pajakd) — 2024-04-12T12:56:59Z
```diff
@@ -457,6 +457,42 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"when workload is created, it has correct labels set": {
+			job: *baseJobWrapper.Clone().
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				UID("test-uid").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Label("toCopyKey", "toCopyValue").
+				Label("dontCopyKey", "dontCopyValue").
+				UID("test-uid").
+				Suspend(true).
+				Obj(),
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithLabelKeysToCopy([]string{"toCopyKey", "redundantToCopyKey"}),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("job", "ns").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Queue("foo").
+					Priority(0).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "test-uid",
+						"toCopyKey":                  "toCopyValue"}).
+					Obj(),
+			},
+
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-12T12:57:09Z

- **keps/1834-copy-labels-into-workload/README.md:60** by [@pajakd](https://github.com/pajakd) — 2024-04-12T12:57:09Z
```diff
@@ -0,0 +1,121 @@
+# KEP-1834: Copy some labels from Pods/Jobs into the Workload object 
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+When creating a kueue workload, copy the job (or pod) labels into the workload object (see https://github.com/kubernetes-sigs/kueue/issues/1834). Allow to configure which labels should be copied.
+
+## Motivation
+
+Currently the workloads do not "inherit" any of the labels of the jobs based on which they are created. Since workloads in Kueue are internal representations of the Kubernetes jobs, allowing to easily copy the labels of the job object into the workload object is natural and useful. For instance it would facilitate selection/filtering of the workloads by the administrators. 
+
+### Goals
+
+* Establish a mechanism to configure which set of labels should be copied.
+* Copy the selected labels when creating the workload based on a job.
+
+### Non-Goals
+* This proposal does not contain any form of validation or analysis of the labels as they are going to be copied from one object to another.
+* This proposal only concerns copying of labels into newly created workloads. Updating the labels of an existing workload (for example if the label of the underlying job is changed) is out of scope of this proposal.
+
+## Proposal
+
+We want to do the following API change. The proposal is to add a field named `labelKeysToCopy` into the configuration API (under `Integrations`). This field will hold a list of keys of labels that should be copied. This configuration will be global in the sense that it will apply to all the job frameworks. Since the list `labelKeysToCopy` will be empty by default, this change will not affect the existing functionality. 
+
+We will not require for all the labels with keys from `labelKeysToCopy` to be present at the job object. When some of the labels will not be present at the job object, this label will not be assigned to the created workload object.
+
+A case that requires more attention is creating workloads from pod groups because in that case a single workload is based on multiple pods (each of which might have labels). We propose:
+ * When a label from the `labelKeysToCopy` list will be present at some of the pods from the group and the value of this label on all these pods will be identical then this label will be copied into the workload. Note that we do not require that all the pods have the label but all that do have must have the same value.
+ * When multiple pods from the group will have the label with different values, we will raise an exception. The exception will be raised during the workload creation.
+
+
+### Risks and Mitigations
+
+None.
+
+## Design Details
+
+
+The proposal contains a single modification to the API. We propose to add a new field `LabelKeysToCopy` to `Integrations` struct in the `Configuration` API. 
+``` go
+type Integrations struct {
+    ...
+	// A list of label keys that should be copied from the job into the workload
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-12T12:57:15Z

- **apis/config/v1beta1/configuration_types.go:305** by [@pajakd](https://github.com/pajakd) — 2024-04-12T12:57:15Z
```diff
@@ -301,6 +301,17 @@ type Integrations struct {
 	Frameworks []string `json:"frameworks,omitempty"`
 	// PodOptions defines kueue controller behaviour for pod objects
 	PodOptions *PodIntegrationOptions `json:"podOptions,omitempty"`
+
+	// A list of label keys that should be copied from the job into the workload
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-04-12T13:02:37Z

- **keps/1834-copy-labels-into-workload/kep.yaml:25** by [@pajakd](https://github.com/pajakd) — 2024-04-12T13:02:37Z
```diff
@@ -0,0 +1,25 @@
+title: Copy labels into workload
+kep-number: 1834
+authors:
+  - "@pajakd"
+status: implementable
+creation-date: 2024-04-08
+reviewers:
+  - "@alculquicondor"
+  - "@mimowo"
+approvers:
+  - "@alculquicondor"
+  - "@tenzen-y"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.7"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.7"
+  beta: "v0.8"
```

  I don't know (I'll let you decide). For the time being I applied the suggestion above.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-12T14:53:07Z

/lgtm
/approve
