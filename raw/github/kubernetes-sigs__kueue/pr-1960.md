# PR #1960: Introduce AdmissionCheckStrategy API, change assigning AdmissionCheck…

**Summary**: Introduce AdmissionCheckStrategy API, change assigning AdmissionCheck…

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1960

**Last updated**: 2024-05-30T14:13:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `admissioncheck-per-flavor` → `main`
- **Created**: 2024-04-09T10:05:05Z
- **Updated**: 2024-05-30T14:13:14Z
- **Merged**: 2024-04-16T14:57:33Z
- **Closed**: 2024-04-16T14:57:33Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 26   **+885 / -66**

## Description

…s to a Workload

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Implements changes described in #1935 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1432 

#### Special notes for your reviewer:
I've tested it manually e2e. In an incoming PR I'll add webhook validation.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
You can define AdmissionChecks per ResourceFlavor in the ClusterQueue API, using `admissionChecksStrategy`
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-09T10:05:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 181cdcafa0559930c33ebc540c33ec7d76c022fe |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/661e8cd95aa1d6000837408c |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-04-09T10:11:37Z

/assign @alculquicondor 
/assign @tenzen-y 
/assign @mimowo

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:11:42Z

/release-note-edit
```release-note
Users can define AdmissionChecks per ResourceFlavor in the ClusterQueue API.
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:12:08Z

The original note could have been interpreted as a breaking change, which is not the case.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:12:57Z

/release-note-edit
```release-note
Users can define AdmissionChecks per ResourceFlavor in the ClusterQueue API, using admissionChecksStrategy.
```

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T14:07:54Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-15T14:32:32Z

If the above error is a flaky, please open an issue.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T13:20:25Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-16T13:20:32Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1960#issuecomment-2059078784" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1960#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T14:54:43Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-16T14:54:48Z

LGTM label has been added.  <details>Git tree hash: bbfb53bfb4792ccaa7d3d14764d98b4e2c9e1d78</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T14:59:10Z

Oops, this PR already has been merged :(

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T15:06:02Z

@PBundyra Sorry for my late response. Could you address my comments? Thanks.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T14:13:11Z

/release-note-edit

```release-note
You can define AdmissionChecks per ResourceFlavor in the ClusterQueue API, using `admissionChecksStrategy`
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-09T17:47:39Z

- **apis/kueue/v1beta1/clusterqueue_types.go:100** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:15:41Z
```diff
@@ -97,6 +97,9 @@ type ClusterQueueSpec struct {
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
 
+	// admissionCheckStrategy is the list of AdmissionChecks that should run when a particular ResourceFlavor is used.
```

  mention that this cannot be used in combination with `admissionChecks`. Add the same note for admissionChecks

- **apis/kueue/v1beta1/clusterqueue_types.go:122** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:16:19Z
```diff
@@ -112,6 +115,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
+	// name is an AdmissionCheck's metav1.Name
+	Name string `json:"name"`
+	// forFlavors is a list of ResourceFlavors' metav1.Names that this AdmissionCheck should run for.
+	// if empty the AdmissionCheck will run for all workloads submitted to the ClusterQueue.
```

  ```suggestion
  	// If empty, the AdmissionCheck will run for all workloads submitted to the ClusterQueue.
  ```

- **pkg/controller/core/workload_controller.go:285** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:26:57Z
```diff
@@ -259,18 +263,55 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		return false, err
 	}
 
-	queueAdmissionChecks := queue.Spec.AdmissionChecks
-	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+	var admissionChecks []string
+	if len(queue.Spec.AdmissionChecks) == 0 {
+		admissionChecks = getChecksFromACStrategies(ctx, wl, &queue)
+	} else {
+		admissionChecks = queue.Spec.AdmissionChecks
+	}
+
+	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, admissionChecks)
 	if shouldUpdate {
 		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
+		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", admissionChecks)
 		wl.Status.AdmissionChecks = newChecks
 		err := r.client.Status().Update(ctx, wl)
 		return true, client.IgnoreNotFound(err)
 	}
 	return false, nil
 }
 
+// getChecksFromACStrategies return AdmissionChecks that are assigned to ResourceFlavors assigned to the Workload.
+func getChecksFromACStrategies(ctx context.Context, wl *kueue.Workload, queue *kueue.ClusterQueue) []string {
```

  when a context is not needed, pass a log instead.

- **pkg/controller/core/workload_controller.go:293** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:27:20Z
```diff
@@ -259,18 +263,55 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		return false, err
 	}
 
-	queueAdmissionChecks := queue.Spec.AdmissionChecks
-	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+	var admissionChecks []string
+	if len(queue.Spec.AdmissionChecks) == 0 {
+		admissionChecks = getChecksFromACStrategies(ctx, wl, &queue)
+	} else {
+		admissionChecks = queue.Spec.AdmissionChecks
+	}
+
+	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, admissionChecks)
 	if shouldUpdate {
 		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
+		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", admissionChecks)
 		wl.Status.AdmissionChecks = newChecks
 		err := r.client.Status().Update(ctx, wl)
 		return true, client.IgnoreNotFound(err)
 	}
 	return false, nil
 }
 
+// getChecksFromACStrategies return AdmissionChecks that are assigned to ResourceFlavors assigned to the Workload.
+func getChecksFromACStrategies(ctx context.Context, wl *kueue.Workload, queue *kueue.ClusterQueue) []string {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Kueue sets AdmissionChecks first based on ClusterQueue configuration and at this point Workload has no
+	// ResourceFlavors assigned, so we cannot match AdmissionChecks to ResourceFlavor.
+	// After QuotaReservation is, another reconciliation happens and we can match AdmissionChecks to ResourceFlavors
+	if wl.Status.Admission == nil {
+		log.V(2).Info("Workload has no Admission", "Workload", wl)
+		return []string{}
```

  ```suggestion
  		return nil
  ```

- **pkg/controller/core/workload_controller.go:296** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:30:23Z
```diff
@@ -259,18 +263,55 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		return false, err
 	}
 
-	queueAdmissionChecks := queue.Spec.AdmissionChecks
-	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+	var admissionChecks []string
+	if len(queue.Spec.AdmissionChecks) == 0 {
+		admissionChecks = getChecksFromACStrategies(ctx, wl, &queue)
+	} else {
+		admissionChecks = queue.Spec.AdmissionChecks
+	}
+
+	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, admissionChecks)
 	if shouldUpdate {
 		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
+		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", admissionChecks)
 		wl.Status.AdmissionChecks = newChecks
 		err := r.client.Status().Update(ctx, wl)
 		return true, client.IgnoreNotFound(err)
 	}
 	return false, nil
 }
 
+// getChecksFromACStrategies return AdmissionChecks that are assigned to ResourceFlavors assigned to the Workload.
+func getChecksFromACStrategies(ctx context.Context, wl *kueue.Workload, queue *kueue.ClusterQueue) []string {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Kueue sets AdmissionChecks first based on ClusterQueue configuration and at this point Workload has no
+	// ResourceFlavors assigned, so we cannot match AdmissionChecks to ResourceFlavor.
+	// After QuotaReservation is, another reconciliation happens and we can match AdmissionChecks to ResourceFlavors
+	if wl.Status.Admission == nil {
+		log.V(2).Info("Workload has no Admission", "Workload", wl)
+		return []string{}
+	}
+
+	wlFlavors := make([]kueue.ResourceFlavorReference, 0)
```

  ```suggestion
  	wlFlavors := make([]kueue.ResourceFlavorReference)
  ```

- **pkg/controller/core/workload_controller_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:38:57Z
  Please also add one test case for TestReconcile

- **pkg/util/testing/wrappers.go:318** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:40:12Z
```diff
@@ -315,6 +315,11 @@ func (w *WorkloadWrapper) ResourceVersion(v string) *WorkloadWrapper {
 	return w
 }
 
+func (w *WorkloadWrapper) Admission(a *kueue.Admission) *WorkloadWrapper {
```

  Use `ReserveQuota` instead

- **pkg/webhooks/clusterqueue_webhook.go:151** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:41:06Z
```diff
@@ -144,6 +145,15 @@ func validatePreemption(preemption *kueue.ClusterQueuePreemption, path *field.Pa
 	return allErrs
 }
 
+func validateCQAdmissionChecks(spec *kueue.ClusterQueueSpec, path *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	if len(spec.AdmissionCheckStrategy) != 0 && len(spec.AdmissionChecks) != 0 {
+		allErrs = append(allErrs, field.Invalid(path, spec, "Either AdmissionChecks or AdmissionCheckStrategy can be set"))
```

  ```suggestion
  		allErrs = append(allErrs, field.Invalid(path, spec, "Either AdmissionChecks or AdmissionCheckStrategy can be set, but not both"))
  ```

- **site/static/examples/provisioning/plain-prov-req.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:43:12Z
  What is the different with https://github.com/kubernetes-sigs/kueue/blob/main/site/static/examples/provisioning/provisioning-setup.yaml?
  
  Maybe add a comment at the top?

- **site/static/examples/jobs/jobset-sample.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:43:38Z
```diff
@@ -0,0 +1,50 @@
+apiVersion: jobset.x-k8s.io/v1alpha2
```

  Why is this relevant to this PR?
  Maybe it can go in a different PR?

- **test/integration/scheduler/workload_controller_test.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:44:52Z
```diff
@@ -69,6 +70,110 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWorkload   kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and admitting the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  ```suggestion
  				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
  ```

- **test/integration/scheduler/workload_controller_test.go:146** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:46:16Z
```diff
@@ -69,6 +70,110 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWorkload   kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and admitting the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
```

  Although, at this point, the Workload should **not** be admitted.
  
  I don't think that is the case.
  
  I think we are missing a change here https://github.com/kubernetes-sigs/kueue/blob/b9c346a83d965d7849e4e27cf7e06e5ee1ad22df/pkg/scheduler/scheduler.go#L280
  
  Right now, workloads are admitted directly if there are no `.spec.admissionChecks`.

- **test/integration/scheduler/workload_controller_test.go:187** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-09T17:47:36Z
```diff
@@ -69,6 +70,110 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWorkload   kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and admitting the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWorkload)).To(gomega.Succeed())
+					return slices.Map(updatedWorkload.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check2"))
+			})
+
+			ginkgo.By("updating admission check strategies", func() {
+				createdQueue := kueue.ClusterQueue{}
+				queueKey := client.ObjectKeyFromObject(clusterQueue)
+				gomega.Eventually(func() error {
+					gomega.Expect(k8sClient.Get(ctx, queueKey, &createdQueue)).To(gomega.Succeed())
+					createdQueue.Spec.AdmissionCheckStrategy = []kueue.AdmissionCheckStrategy{
+						*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+						*testing.MakeAdmissionCheckStrategy("check2", reservationFlavor).Obj(),
+						*testing.MakeAdmissionCheckStrategy("check3").Obj()}
+					return k8sClient.Update(ctx, &createdQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWorkload)).To(gomega.Succeed())
+					return slices.Map(updatedWorkload.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check3"))
+			})
+		})
```

  Complete the test by marking the checks as passed and verifying that at this point the Workload is admitted.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T07:39:21Z

- **site/static/examples/provisioning/plain-prov-req.yaml:1** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T07:39:21Z
  I didn't want to include the whole file, just the CQ's config

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T09:20:37Z

- **apis/kueue/v1beta1/clusterqueue_types.go:100** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T09:20:37Z
```diff
@@ -97,6 +97,9 @@ type ClusterQueueSpec struct {
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
 
+	// admissionCheckStrategy is the list of AdmissionChecks that should run when a particular ResourceFlavor is used.
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T09:20:47Z

- **pkg/controller/core/workload_controller.go:285** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T09:20:46Z
```diff
@@ -259,18 +263,55 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		return false, err
 	}
 
-	queueAdmissionChecks := queue.Spec.AdmissionChecks
-	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+	var admissionChecks []string
+	if len(queue.Spec.AdmissionChecks) == 0 {
+		admissionChecks = getChecksFromACStrategies(ctx, wl, &queue)
+	} else {
+		admissionChecks = queue.Spec.AdmissionChecks
+	}
+
+	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, admissionChecks)
 	if shouldUpdate {
 		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
+		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", admissionChecks)
 		wl.Status.AdmissionChecks = newChecks
 		err := r.client.Status().Update(ctx, wl)
 		return true, client.IgnoreNotFound(err)
 	}
 	return false, nil
 }
 
+// getChecksFromACStrategies return AdmissionChecks that are assigned to ResourceFlavors assigned to the Workload.
+func getChecksFromACStrategies(ctx context.Context, wl *kueue.Workload, queue *kueue.ClusterQueue) []string {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T09:20:58Z

- **site/static/examples/jobs/jobset-sample.yaml:1** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T09:20:58Z
```diff
@@ -0,0 +1,50 @@
+apiVersion: jobset.x-k8s.io/v1alpha2
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T09:21:20Z

- **test/integration/scheduler/workload_controller_test.go:146** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T09:21:20Z
```diff
@@ -69,6 +70,110 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWorkload   kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and admitting the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
```

  Good catch! Indeed I've missed this change, thanks!

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T09:21:27Z

- **test/integration/scheduler/workload_controller_test.go:187** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T09:21:27Z
```diff
@@ -69,6 +70,110 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWorkload   kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and admitting the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWorkload)).To(gomega.Succeed())
+					return slices.Map(updatedWorkload.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check2"))
+			})
+
+			ginkgo.By("updating admission check strategies", func() {
+				createdQueue := kueue.ClusterQueue{}
+				queueKey := client.ObjectKeyFromObject(clusterQueue)
+				gomega.Eventually(func() error {
+					gomega.Expect(k8sClient.Get(ctx, queueKey, &createdQueue)).To(gomega.Succeed())
+					createdQueue.Spec.AdmissionCheckStrategy = []kueue.AdmissionCheckStrategy{
+						*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+						*testing.MakeAdmissionCheckStrategy("check2", reservationFlavor).Obj(),
+						*testing.MakeAdmissionCheckStrategy("check3").Obj()}
+					return k8sClient.Update(ctx, &createdQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWorkload)).To(gomega.Succeed())
+					return slices.Map(updatedWorkload.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check3"))
+			})
+		})
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T09:21:40Z

- **test/integration/scheduler/workload_controller_test.go:187** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T09:21:40Z
```diff
@@ -69,6 +70,110 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWorkload   kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and admitting the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWorkload); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWorkload)).To(gomega.Succeed())
+					return slices.Map(updatedWorkload.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check2"))
+			})
+
+			ginkgo.By("updating admission check strategies", func() {
+				createdQueue := kueue.ClusterQueue{}
+				queueKey := client.ObjectKeyFromObject(clusterQueue)
+				gomega.Eventually(func() error {
+					gomega.Expect(k8sClient.Get(ctx, queueKey, &createdQueue)).To(gomega.Succeed())
+					createdQueue.Spec.AdmissionCheckStrategy = []kueue.AdmissionCheckStrategy{
+						*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+						*testing.MakeAdmissionCheckStrategy("check2", reservationFlavor).Obj(),
+						*testing.MakeAdmissionCheckStrategy("check3").Obj()}
+					return k8sClient.Update(ctx, &createdQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWorkload)).To(gomega.Succeed())
+					return slices.Map(updatedWorkload.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check3"))
+			})
+		})
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T09:22:19Z

- **pkg/util/testing/wrappers.go:318** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T09:22:18Z
```diff
@@ -315,6 +315,11 @@ func (w *WorkloadWrapper) ResourceVersion(v string) *WorkloadWrapper {
 	return w
 }
 
+func (w *WorkloadWrapper) Admission(a *kueue.Admission) *WorkloadWrapper {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T09:24:29Z

- **pkg/controller/core/workload_controller.go:296** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T09:24:29Z
```diff
@@ -259,18 +263,55 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 		return false, err
 	}
 
-	queueAdmissionChecks := queue.Spec.AdmissionChecks
-	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+	var admissionChecks []string
+	if len(queue.Spec.AdmissionChecks) == 0 {
+		admissionChecks = getChecksFromACStrategies(ctx, wl, &queue)
+	} else {
+		admissionChecks = queue.Spec.AdmissionChecks
+	}
+
+	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, admissionChecks)
 	if shouldUpdate {
 		log := ctrl.LoggerFrom(ctx)
-		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
+		log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", admissionChecks)
 		wl.Status.AdmissionChecks = newChecks
 		err := r.client.Status().Update(ctx, wl)
 		return true, client.IgnoreNotFound(err)
 	}
 	return false, nil
 }
 
+// getChecksFromACStrategies return AdmissionChecks that are assigned to ResourceFlavors assigned to the Workload.
+func getChecksFromACStrategies(ctx context.Context, wl *kueue.Workload, queue *kueue.ClusterQueue) []string {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Kueue sets AdmissionChecks first based on ClusterQueue configuration and at this point Workload has no
+	// ResourceFlavors assigned, so we cannot match AdmissionChecks to ResourceFlavor.
+	// After QuotaReservation is, another reconciliation happens and we can match AdmissionChecks to ResourceFlavors
+	if wl.Status.Admission == nil {
+		log.V(2).Info("Workload has no Admission", "Workload", wl)
+		return []string{}
+	}
+
+	wlFlavors := make([]kueue.ResourceFlavorReference, 0)
```

  Could you clarify that please? It doesn't compile with that change, and the initial capacity of the wlFlavors is a bit tedious to calculate upfront

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T10:21:57Z

- **pkg/controller/core/workload_controller_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T10:21:57Z
  Added

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-10T11:42:37Z

My first path is added.

- **apis/kueue/v1beta1/clusterqueue_types.go:104** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T10:07:28Z
```diff
@@ -93,10 +93,16 @@ type ClusterQueueSpec struct {
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
 
-	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
+	// admissionChecks lists the AdmissionChecks required by this ClusterQueue.
+	// Cannot be used along with AdmissionCheckStrategy.
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
```

  Should we mention that "This field is deprecated. Please use the admissionCheckStrategy field"?
  @alculquicondor @PBundyra WDYT?

- **apis/kueue/v1beta1/clusterqueue_types.go:101** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T10:09:53Z
```diff
@@ -93,10 +93,16 @@ type ClusterQueueSpec struct {
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
 
-	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
+	// admissionChecks lists the AdmissionChecks required by this ClusterQueue.
+	// Cannot be used along with AdmissionCheckStrategy.
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
 
+	// admissionCheckStrategy is the list of AdmissionChecks that should run when a particular ResourceFlavor is used.
```

  ```suggestion
  	// admissionCheckStrategy is the list of AdmissionChecks.
  ```
  
  We don't restrict use cases only particular resourceFlavors. Actually, empty `onFlavors` means applying strategies to clusterQueue.

- **apis/kueue/v1beta1/clusterqueue_types.go:102** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T10:10:27Z
```diff
@@ -93,10 +93,16 @@ type ClusterQueueSpec struct {
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
 
-	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
+	// admissionChecks lists the AdmissionChecks required by this ClusterQueue.
+	// Cannot be used along with AdmissionCheckStrategy.
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
 
+	// admissionCheckStrategy is the list of AdmissionChecks that should run when a particular ResourceFlavor is used.
+	// Cannot be used along with AdmissionChecks.
```

  ```suggestion
  	// Cannot be used along with admissionChecks.
  ```
  
  Let's use the JSON name.

- **apis/kueue/v1beta1/clusterqueue_types.go:122** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T10:11:35Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
+	// name is an AdmissionCheck's metav1.Name
```

  ```suggestion
  	// name is an AdmissionCheck's name.
  ```
  
  I think that mentioning the type of this field is not valuable for end users.

- **apis/kueue/v1beta1/clusterqueue_types.go:124** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T10:11:59Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
+	// name is an AdmissionCheck's metav1.Name
+	Name string `json:"name"`
+	// forFlavors is a list of ResourceFlavors' metav1.Names that this AdmissionCheck should run for.
```

  ```suggestion
  	// onFlavors is a list of ResourceFlavors' names that this AdmissionCheck should run for.
  ```

- **pkg/workload/workload.go:527** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T10:14:30Z
```diff
@@ -520,3 +523,37 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	}
 	return nil
 }
+
+// AdmissionChecksForWorkload return AdmissionChecks that should be assigned to a specific Workload based on
```

  ```suggestion
  // AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
  ```

- **pkg/workload/workload.go:538** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T10:15:34Z
```diff
@@ -520,3 +523,37 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	}
 	return nil
 }
+
+// AdmissionChecksForWorkload return AdmissionChecks that should be assigned to a specific Workload based on
+// ClusterQueue configuration and ResourceFlavors
+func AdmissionChecksForWorkload(log *logr.Logger, wl *kueue.Workload, cq *kueue.ClusterQueue) []string {
+	if len(cq.Spec.AdmissionChecks) != 0 {
+		return cq.Spec.AdmissionChecks
+	}
+
+	// Kueue sets AdmissionChecks first based on ClusterQueue configuration and at this point Workload has no
+	// ResourceFlavors assigned, so we cannot match AdmissionChecks to ResourceFlavor.
+	// After Quota is reserved, another reconciliation happens and we can match AdmissionChecks to ResourceFlavors
+	if wl.Status.Admission == nil {
+		log.V(2).Info("Workload has no Admission", "Workload", wl)
```

  ```suggestion
  		log.V(2).Info("Workload has no Admission", "Workload", klog.KObj(wl))
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:126** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T10:27:42Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
+	// name is an AdmissionCheck's metav1.Name
+	Name string `json:"name"`
+	// forFlavors is a list of ResourceFlavors' metav1.Names that this AdmissionCheck should run for.
+	// If empty, the AdmissionCheck will run for all workloads submitted to the ClusterQueue.
+	OnFlavors []string `json:"onFlavors"`
```

  ```suggestion
  	OnFlavors []ResourceFlavorReference`json:"onFlavors"`
  ```
  
  Should we use the dedicated type to avoid casting to string?
  @alculquicondor WDYT?

- **pkg/workload/workload.go:593** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T11:20:46Z
```diff
@@ -520,3 +523,37 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	}
 	return nil
 }
+
+// AdmissionChecksForWorkload return AdmissionChecks that should be assigned to a specific Workload based on
+// ClusterQueue configuration and ResourceFlavors
+func AdmissionChecksForWorkload(log *logr.Logger, wl *kueue.Workload, cq *kueue.ClusterQueue) []string {
+	if len(cq.Spec.AdmissionChecks) != 0 {
+		return cq.Spec.AdmissionChecks
+	}
+
+	// Kueue sets AdmissionChecks first based on ClusterQueue configuration and at this point Workload has no
+	// ResourceFlavors assigned, so we cannot match AdmissionChecks to ResourceFlavor.
+	// After Quota is reserved, another reconciliation happens and we can match AdmissionChecks to ResourceFlavors
+	if wl.Status.Admission == nil {
+		log.V(2).Info("Workload has no Admission", "Workload", wl)
+		return nil
+	}
+
+	wlFlavors := make([]kueue.ResourceFlavorReference, 0)
+	for _, podSet := range wl.Status.Admission.PodSetAssignments {
+		wlFlavors = append(wlFlavors, expmaps.Values(podSet.Flavors)...)
+	}
+	// Since different resources may have the same ResourceFlavor, we need to remove duplicates
+	slices.Sort(wlFlavors)
+	wlFlavors = slices.Compact(wlFlavors)
+
+	admissionCheckNames := make([]string, 0)
+	for _, acStrategy := range cq.Spec.AdmissionCheckStrategy {
+		for _, flavor := range wlFlavors {
+			if len(acStrategy.OnFlavors) == 0 || slices.Contains(acStrategy.OnFlavors, string(flavor)) {
+				admissionCheckNames = append(admissionCheckNames, acStrategy.Name)
+			}
+		}
+	}
```

  Should we reduce the number of loops? The current implementation has 7 loop points:
  
  1. `for _, podSet := range wl.Status.Admission.PodSetAssignments {`
  2. `expmaps.Values(podSet.Flavors)`
  3. `slices.Sort(wlFlavors)`
  4. `slices.Compact(wlFlavors)`
  5. `for _, acStrategy := range cq.Spec.AdmissionCheckStrategy {`
  6. `for _, flavor := range wlFlavors {`
  7. `slices.Contains(acStrategy.OnFlavors, string(flavor))`
  
  I guess that using `sets.Set` allows us to reduce loops like this.
  Also, I guess that replacing the returned type with `sets.Set` would allow us to avoid creating them in the scheduler.
  
  ```golang
  func AdmissionChecksForWorkload(log *logr.Logger, wl *kueue.Workload, cq *kueue.ClusterQueue) sets.Set[string] {
  [...]
  
  assinedFlvs := sets.New[string]()
  for _, podSet := range wl.Status.Admission.PodSetAssignments {
  	for _, fName := range podSet.Flavor {
  		assinedFlvs.Insert(fName)
  	}
  }
  
  acNames := sets.New[string]()
  for _, acStrategy := range cq.Spec.AdmissionCheckStrategy {
  	if len(acStrategy.OnFlavors) == 0 {
  		acNames.Insert(acStrategy.Name)
  		continue
  	}
  	for _, fName := range acStrategy.OnFlavors {
  		if assignedFlvs.Has(fName) {
  			acNames.Insert(acStrategy.Name)
  		}
  	}
  }
  
  return acNames
  ```
  
  Note that the implementation above is just a reference. So, I think that we need to refactor it.

- **pkg/workload/workload_test.go:697** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T11:34:11Z
```diff
@@ -643,3 +644,63 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks []string
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategy("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: []string{"ac1"},
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategy("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: []string{"ac1"},
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategy("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategy("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: []string{"ac1", "ac2"},
+		},
+		"Workload has no Admission": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategy("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategy("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			log := ctrl.LoggerFrom(ctx)
```

  Should we use our library?
  
  https://github.com/kubernetes-sigs/kueue/blob/b9c346a83d965d7849e4e27cf7e06e5ee1ad22df/pkg/util/testing/context.go#L28

- **pkg/workload/workload_test.go:701** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T11:34:33Z
```diff
@@ -643,3 +644,63 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks []string
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategy("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: []string{"ac1"},
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategy("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: []string{"ac1"},
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategy("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategy("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: []string{"ac1", "ac2"},
+		},
+		"Workload has no Admission": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategy("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategy("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			log := ctrl.LoggerFrom(ctx)
+			gotAdmissionChecks := AdmissionChecksForWorkload(&log, tc.wl, tc.cq)
+
+			if diff := cmp.Diff(tc.wantAdmissionChecks, gotAdmissionChecks); diff != "" {
+				t.Errorf("Unexpected AdmissionChecks, (want-/got+): %s", diff)
```

  ```suggestion
  				t.Errorf("Unexpected AdmissionChecks, (want-/got+):\n%s", diff)
  ```

- **pkg/webhooks/clusterqueue_webhook.go:131** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T11:37:50Z
```diff
@@ -144,6 +145,15 @@ func validatePreemption(preemption *kueue.ClusterQueuePreemption, path *field.Pa
 	return allErrs
 }
 
+func validateCQAdmissionChecks(spec *kueue.ClusterQueueSpec, path *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	if len(spec.AdmissionCheckStrategy) != 0 && len(spec.AdmissionChecks) != 0 {
+		allErrs = append(allErrs, field.Invalid(path, spec, "Either AdmissionChecks or AdmissionCheckStrategy can be set, but not both"))
```

  ```suggestion
  		allErrs = append(allErrs, field.Invalid(path, spec, "must have either admissionChecks or admissionCheckStrategy"))
  ```
  The field.Invalid() adds the path as a prefix.

- **pkg/webhooks/clusterqueue_webhook.go:128** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T11:39:52Z
```diff
@@ -144,6 +145,15 @@ func validatePreemption(preemption *kueue.ClusterQueuePreemption, path *field.Pa
 	return allErrs
 }
 
+func validateCQAdmissionChecks(spec *kueue.ClusterQueueSpec, path *field.Path) field.ErrorList {
```

  Could you implement unit tests here?: https://github.com/kubernetes-sigs/kueue/blob/b9c346a83d965d7849e4e27cf7e06e5ee1ad22df/pkg/webhooks/clusterqueue_webhook_test.go#L34

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-10T11:44:04Z

- **test/integration/scheduler/workload_controller_test.go:133** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T11:43:17Z
```diff
@@ -69,6 +72,138 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.FIt("the workload should have appropriate AdditionalChecks added", func() {
```

  ```suggestion
  		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
  ```
  Isn't this for the debug?

- **test/integration/scheduler/workload_controller_test.go:150** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T11:43:28Z
```diff
@@ -69,6 +72,138 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.FIt("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
+
+			fmt.Printf("PATRYK conditions%#v\n", updatedWl.Status.Conditions)
```

  ```suggestion
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T11:48:04Z

- **test/integration/scheduler/workload_controller_test.go:150** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T11:48:04Z
```diff
@@ -69,6 +72,138 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.FIt("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
+
+			fmt.Printf("PATRYK conditions%#v\n", updatedWl.Status.Conditions)
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T11:48:10Z

- **test/integration/scheduler/workload_controller_test.go:133** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T11:48:10Z
```diff
@@ -69,6 +72,138 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategy("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategy("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategy("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.FIt("the workload should have appropriate AdditionalChecks added", func() {
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T11:52:45Z

- **apis/kueue/v1beta1/clusterqueue_types.go:124** by [@mimowo](https://github.com/mimowo) — 2024-04-10T11:40:24Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
+	// name is an AdmissionCheck's metav1.Name
+	Name string `json:"name"`
+	// forFlavors is a list of ResourceFlavors' metav1.Names that this AdmissionCheck should run for.
```

  ```suggestion
  	// OnFlavors is a list of ResourceFlavors' metav1.Names that this AdmissionCheck should run for.
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@mimowo](https://github.com/mimowo) — 2024-04-10T11:47:04Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  Do we support mutability of the structure? If not, I would suggest to validate it in the webhook.

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@mimowo](https://github.com/mimowo) — 2024-04-10T11:52:08Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  I'm thinking if the strategy should be a struct that wraps the list of admission checks:
  ```yaml
  admissionChecksStrategy:
     admissionChecks:
      - name1
        onFlavors: [a, b, c]
  ```
  The reason being to allow for introducing properties common for all admission checks, analogously as we do for PodFailurePolicy or SuccessPolicy in Job. One example I would see this useful might be to say if the admission checks are "parallel" or "sequential".
  
  WDYT @alculquicondor @tenzen-y ?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T13:07:57Z

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T13:07:57Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  We do

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T13:18:17Z

- **pkg/webhooks/clusterqueue_webhook.go:131** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T13:18:16Z
```diff
@@ -144,6 +145,15 @@ func validatePreemption(preemption *kueue.ClusterQueuePreemption, path *field.Pa
 	return allErrs
 }
 
+func validateCQAdmissionChecks(spec *kueue.ClusterQueueSpec, path *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	if len(spec.AdmissionCheckStrategy) != 0 && len(spec.AdmissionChecks) != 0 {
+		allErrs = append(allErrs, field.Invalid(path, spec, "Either AdmissionChecks or AdmissionCheckStrategy can be set, but not both"))
```

  I think this message can be misleading as it indicates that a user must always define admissionChecks or admissionCheckStrategy which is not necessarily true

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T13:38:25Z

- **apis/kueue/v1beta1/clusterqueue_types.go:101** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T13:38:25Z
```diff
@@ -93,10 +93,16 @@ type ClusterQueueSpec struct {
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
 
-	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
+	// admissionChecks lists the AdmissionChecks required by this ClusterQueue.
+	// Cannot be used along with AdmissionCheckStrategy.
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
 
+	// admissionCheckStrategy is the list of AdmissionChecks that should run when a particular ResourceFlavor is used.
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T13:38:32Z

- **pkg/workload/workload.go:593** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T13:38:32Z
```diff
@@ -520,3 +523,37 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	}
 	return nil
 }
+
+// AdmissionChecksForWorkload return AdmissionChecks that should be assigned to a specific Workload based on
+// ClusterQueue configuration and ResourceFlavors
+func AdmissionChecksForWorkload(log *logr.Logger, wl *kueue.Workload, cq *kueue.ClusterQueue) []string {
+	if len(cq.Spec.AdmissionChecks) != 0 {
+		return cq.Spec.AdmissionChecks
+	}
+
+	// Kueue sets AdmissionChecks first based on ClusterQueue configuration and at this point Workload has no
+	// ResourceFlavors assigned, so we cannot match AdmissionChecks to ResourceFlavor.
+	// After Quota is reserved, another reconciliation happens and we can match AdmissionChecks to ResourceFlavors
+	if wl.Status.Admission == nil {
+		log.V(2).Info("Workload has no Admission", "Workload", wl)
+		return nil
+	}
+
+	wlFlavors := make([]kueue.ResourceFlavorReference, 0)
+	for _, podSet := range wl.Status.Admission.PodSetAssignments {
+		wlFlavors = append(wlFlavors, expmaps.Values(podSet.Flavors)...)
+	}
+	// Since different resources may have the same ResourceFlavor, we need to remove duplicates
+	slices.Sort(wlFlavors)
+	wlFlavors = slices.Compact(wlFlavors)
+
+	admissionCheckNames := make([]string, 0)
+	for _, acStrategy := range cq.Spec.AdmissionCheckStrategy {
+		for _, flavor := range wlFlavors {
+			if len(acStrategy.OnFlavors) == 0 || slices.Contains(acStrategy.OnFlavors, string(flavor)) {
+				admissionCheckNames = append(admissionCheckNames, acStrategy.Name)
+			}
+		}
+	}
```

  Thanks for catching that. Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T13:38:43Z

- **pkg/webhooks/clusterqueue_webhook.go:128** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T13:38:43Z
```diff
@@ -144,6 +145,15 @@ func validatePreemption(preemption *kueue.ClusterQueuePreemption, path *field.Pa
 	return allErrs
 }
 
+func validateCQAdmissionChecks(spec *kueue.ClusterQueueSpec, path *field.Path) field.ErrorList {
```

  Added

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T13:38:56Z

- **pkg/workload/workload_test.go:697** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T13:38:56Z
```diff
@@ -643,3 +644,63 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks []string
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategy("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: []string{"ac1"},
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategy("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: []string{"ac1"},
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategy("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategy("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: []string{"ac1", "ac2"},
+		},
+		"Workload has no Admission": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategy("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategy("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx := context.Background()
+			log := ctrl.LoggerFrom(ctx)
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T13:46:34Z

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@mimowo](https://github.com/mimowo) — 2024-04-10T13:46:34Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  Interesting, so do we reflect the changes at the CQ level into the workload level (`status.admissionCheckStates`), or just let it continue as initialized?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T13:58:36Z

- **site/static/examples/provisioning/plain-prov-req.yaml:1** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T13:58:36Z
  Oh nvm, I didn't want to include this file at all

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-10T14:00:33Z

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@PBundyra](https://github.com/PBundyra) — 2024-04-10T14:00:33Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  We propagate those changes into the workloads

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-10T14:10:11Z

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@mimowo](https://github.com/mimowo) — 2024-04-10T14:10:11Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  I see, makes sense, I'm yet wondering about some scenarios. For example, if an admin adds ProvReq to CQ with a running workload, is the workload evicted or continues?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-10T17:11:17Z

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T17:11:16Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  +1
  It could be useful.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-10T17:18:37Z

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-10T17:18:37Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  +1
  Thank you for pointing this out.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-11T08:30:03Z

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@PBundyra](https://github.com/PBundyra) — 2024-04-11T08:30:03Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  Yes, I've also wondered about scenarios like these. I imagine we wouldn't like to evict already running workload, similarly as it's done right now iiuc. However, introducing the new API also allows us to extend it, and maybe evicting (or not) could be also part of it in the future

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-11T09:19:05Z

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@PBundyra](https://github.com/PBundyra) — 2024-04-11T09:19:05Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  +1 I like this idea, I've added the changes

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-11T09:31:07Z

- **apis/kueue/v1beta1/clusterqueue_types.go:121** by [@mimowo](https://github.com/mimowo) — 2024-04-11T09:31:07Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
```

  sgtm, I'm ok keeping it mutable since the current `admissionChecks` are mutable. I suppose we would prefer not to evict by default indeed, but we can handle this in the future if there is a need / user request.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-11T18:30:05Z

- **pkg/workload/workload.go:530** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:17:39Z
```diff
@@ -520,3 +524,40 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	}
 	return nil
 }
+
+// AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
+// ClusterQueue configuration and ResourceFlavors
+func AdmissionChecksForWorkload(log *logr.Logger, wl *kueue.Workload, cq *kueue.ClusterQueue) sets.Set[string] {
```

  Logger is already an interface
  
  
  ```suggestion
  func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, cq *kueue.ClusterQueue) sets.Set[string] {
  ```

- **pkg/scheduler/scheduler.go:518** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:24:59Z
```diff
@@ -515,7 +515,11 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 	}
 
 	workload.SetQuotaReservation(newWorkload, admission)
-	if workload.HasAllChecks(newWorkload, mustHaveChecks) {
+	queue := &kueue.ClusterQueue{}
```

  For consistency with every other detail about a ClusterQueue within the scheduling cycle, pass it through the cache.ClusterQueue object instead.

- **pkg/workload/workload_test.go:697** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:28:05Z
```diff
@@ -643,3 +644,62 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks sets.Set[string]
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1", "ac2"),
+		},
+		"Workload has no QuotaReserved": {
```

  Add another case where the admission check doesn't match the flavor of the workload assignment, so that we can check that it's not added to the Workload.

- **test/integration/scheduler/workload_controller_test.go:146** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:28:49Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
```

  Add a check that the workload is not admitted here

- **test/integration/scheduler/workload_controller_test.go:193** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T18:29:55Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					return slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check2"))
+			})
+
+			ginkgo.By("updating admission check strategies", func() {
+				createdQueue := kueue.ClusterQueue{}
+				queueKey := client.ObjectKeyFromObject(clusterQueue)
+				gomega.Eventually(func() error {
+					gomega.Expect(k8sClient.Get(ctx, queueKey, &createdQueue)).To(gomega.Succeed())
+
+					createdQueue.Spec.AdmissionChecksStrategy.AdmissionChecks = []kueue.AdmissionCheckStrategyRule{
+						*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check2", reservationFlavor).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check3").Obj()}
+					return k8sClient.Update(ctx, &createdQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					return slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check3"))
+			})
+
+			ginkgo.By("marking the checks as passed", func() {
+				gomega.Eventually(func() error {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStateReady,
+						Message: "check successfully passed",
+					})
+					workload.SetAdmissionCheckState(&updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    "check3",
+						State:   kueue.CheckStateReady,
+						Message: "check successfully passed",
+					})
+					return k8sClient.Status().Update(ctx, &updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("admitting the workload", func() {
```

  You are not "admitting the workload" here, you are just checking.
  
  Prefer to only use a new "by" when the test is changing something, not when it's checking an additional thing.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-11T21:00:20Z

- **apis/kueue/v1beta1/clusterqueue_types.go:126** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:26:45Z
```diff
@@ -112,6 +118,14 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+type AdmissionCheckStrategy struct {
+	// name is an AdmissionCheck's metav1.Name
+	Name string `json:"name"`
+	// forFlavors is a list of ResourceFlavors' metav1.Names that this AdmissionCheck should run for.
+	// If empty, the AdmissionCheck will run for all workloads submitted to the ClusterQueue.
+	OnFlavors []string `json:"onFlavors"`
```

  ```suggestion
  	// +optional
  	OnFlavors []string `json:"onFlavors,omitempty"`
  ```
  
  Also, isn't this an optional parameter?

- **pkg/scheduler/scheduler.go:518** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:29:36Z
```diff
@@ -515,7 +515,11 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 	}
 
 	workload.SetQuotaReservation(newWorkload, admission)
-	if workload.HasAllChecks(newWorkload, mustHaveChecks) {
+	queue := &kueue.ClusterQueue{}
```

  +1

- **pkg/scheduler/scheduler_test.go:1451** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:30:06Z
```diff
@@ -1447,6 +1447,9 @@ func TestSchedule(t *testing.T) {
 				if err := qManager.AddClusterQueue(ctx, &cq); err != nil {
 					t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
 				}
+				if err := cl.Create(ctx, &cq); err != nil {
+					t.Error("couldn't create the cluster queue", err)
```

  ```suggestion
  					t.Errorf("couldn't create the cluster queue: %v", err)
  ```

- **pkg/workload/workload.go:546** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:45:32Z
```diff
@@ -520,3 +524,40 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	}
 	return nil
 }
+
+// AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
+// ClusterQueue configuration and ResourceFlavors
+func AdmissionChecksForWorkload(log *logr.Logger, wl *kueue.Workload, cq *kueue.ClusterQueue) sets.Set[string] {
+	if len(cq.Spec.AdmissionChecks) != 0 {
+		return sets.New(cq.Spec.AdmissionChecks...)
+	}
+
+	// Kueue sets AdmissionChecks first based on ClusterQueue configuration and at this point Workload has no
+	// ResourceFlavors assigned, so we cannot match AdmissionChecks to ResourceFlavor.
+	// After Quota is reserved, another reconciliation happens and we can match AdmissionChecks to ResourceFlavors
+	if wl.Status.Admission == nil {
+		log.V(2).Info("Workload has no Admission", "Workload", klog.KObj(wl))
+		return nil
+	}
+
+	assignedFlavors := sets.New[string]()
+	for _, podSet := range wl.Status.Admission.PodSetAssignments {
+		for _, flavor := range podSet.Flavors {
+			assignedFlavors.Insert(string(flavor))
```

  ```suggestion
  	var assignedFlavors []kueue.ResourceFlavorReference
  	for _, podSet := range wl.Status.Admission.PodSetAssignments {
  		for _, flavor := range podSet.Flavors {
  			assignedFlavors = append(assignedFlavors, flavor)
  ```
  
  If my previous comment brought you any confusion, sorry, but I think that here is slice should be enough.

- **pkg/controller/core/workload_controller_test.go:604** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:49:53Z
```diff
@@ -544,6 +599,18 @@ func TestReconcile(t *testing.T) {
 			ctx, ctxCancel := context.WithCancel(ctxWithLogger)
 			defer ctxCancel()
 
+			if tc.cq != nil {
+				if err := cl.Create(ctx, tc.cq); err != nil {
+					t.Error("couldn't create the cluster queue", err)
```

  ```suggestion
  					t.Errorf("couldn't create the cluster queue: %v", err)
  ```

- **pkg/controller/core/workload_controller_test.go:607** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:50:46Z
```diff
@@ -544,6 +599,18 @@ func TestReconcile(t *testing.T) {
 			ctx, ctxCancel := context.WithCancel(ctxWithLogger)
 			defer ctxCancel()
 
+			if tc.cq != nil {
+				if err := cl.Create(ctx, tc.cq); err != nil {
+					t.Error("couldn't create the cluster queue", err)
+				}
+				if err := qManager.AddClusterQueue(ctx, tc.cq); err != nil {
+					t.Error("couldn't add the cluster queue to the cache", err)
```

  ```suggestion
  					t.Errorf("couldn't add the cluster queue to the cache: %v", err)
  ```

- **pkg/controller/core/workload_controller_test.go:610** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:50:58Z
```diff
@@ -544,6 +599,18 @@ func TestReconcile(t *testing.T) {
 			ctx, ctxCancel := context.WithCancel(ctxWithLogger)
 			defer ctxCancel()
 
+			if tc.cq != nil {
+				if err := cl.Create(ctx, tc.cq); err != nil {
+					t.Error("couldn't create the cluster queue", err)
+				}
+				if err := qManager.AddClusterQueue(ctx, tc.cq); err != nil {
+					t.Error("couldn't add the cluster queue to the cache", err)
+				}
+				if err := qManager.AddLocalQueue(ctx, tc.lq); err != nil {
+					t.Error("couldn't add the local queue to the cache", err)
```

  ```suggestion
  					t.Errorf("couldn't add the local queue to the cache: %v", err)
  ```

- **test/integration/scheduler/workload_controller_test.go:130** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:54:02Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
```

  Could you clean up LocalQueue as well?

- **test/integration/scheduler/workload_controller_test.go:145** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:56:59Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
```

  ```suggestion
  				gomega.Eventually(func(g gomega.Gomega) {
  					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeeded())
  					g.Expect(workload.HasQuotaReservation(&updatedWl)).Should(gomega.BeTrue(), "should have quota reservation")
  				}, util.Timeout, util.Interval).Should(gomega.Succeeded(),)
  ```
  
  This allows us to improve debuggability.

- **test/integration/scheduler/workload_controller_test.go:152** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:58:56Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					return slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check2"))
```

  ```suggestion
  				gomega.Eventually(func(g gomega.Gomega) {
  					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
  					checks := slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
  					gomgea.Expect(checks).Should(gomega.ConsistOf("check1", "check2"))
  				}, util.Timeout, util.Interval).Should(gomega.Succeeded())
  ```

- **test/integration/scheduler/workload_controller_test.go:152** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T20:59:21Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					return slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check2"))
```

  Could you apply the same form to other places?

- **apis/kueue/v1beta1/clusterqueue_types.go:104** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-11T21:00:17Z
```diff
@@ -93,10 +93,16 @@ type ClusterQueueSpec struct {
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
 
-	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
+	// admissionChecks lists the AdmissionChecks required by this ClusterQueue.
+	// Cannot be used along with AdmissionCheckStrategy.
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
```

  friendly ping > @alculquicondor

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:31:20Z

- **pkg/workload/workload.go:530** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:31:20Z
```diff
@@ -520,3 +524,40 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	}
 	return nil
 }
+
+// AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
+// ClusterQueue configuration and ResourceFlavors
+func AdmissionChecksForWorkload(log *logr.Logger, wl *kueue.Workload, cq *kueue.ClusterQueue) sets.Set[string] {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:31:36Z

- **pkg/scheduler/scheduler.go:518** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:31:36Z
```diff
@@ -515,7 +515,11 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 	}
 
 	workload.SetQuotaReservation(newWorkload, admission)
-	if workload.HasAllChecks(newWorkload, mustHaveChecks) {
+	queue := &kueue.ClusterQueue{}
```

  Please take a look if this aligns with your idea

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:32:05Z

- **test/integration/scheduler/workload_controller_test.go:146** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:32:05Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:32:14Z

- **test/integration/scheduler/workload_controller_test.go:193** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:32:14Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					return slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check2"))
+			})
+
+			ginkgo.By("updating admission check strategies", func() {
+				createdQueue := kueue.ClusterQueue{}
+				queueKey := client.ObjectKeyFromObject(clusterQueue)
+				gomega.Eventually(func() error {
+					gomega.Expect(k8sClient.Get(ctx, queueKey, &createdQueue)).To(gomega.Succeed())
+
+					createdQueue.Spec.AdmissionChecksStrategy.AdmissionChecks = []kueue.AdmissionCheckStrategyRule{
+						*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check2", reservationFlavor).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check3").Obj()}
+					return k8sClient.Update(ctx, &createdQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					return slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check3"))
+			})
+
+			ginkgo.By("marking the checks as passed", func() {
+				gomega.Eventually(func() error {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStateReady,
+						Message: "check successfully passed",
+					})
+					workload.SetAdmissionCheckState(&updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    "check3",
+						State:   kueue.CheckStateReady,
+						Message: "check successfully passed",
+					})
+					return k8sClient.Status().Update(ctx, &updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("admitting the workload", func() {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:34:43Z

- **test/integration/scheduler/workload_controller_test.go:130** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:34:42Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:42:31Z

- **pkg/workload/workload_test.go:697** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:42:31Z
```diff
@@ -643,3 +644,62 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks sets.Set[string]
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1", "ac2"),
+		},
+		"Workload has no QuotaReserved": {
```

  Do you mean a different scenario than the `"AdmissionCheckStrategy with an unmatched flavor"`?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:52:12Z

- **pkg/workload/workload.go:546** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:52:12Z
```diff
@@ -520,3 +524,40 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	}
 	return nil
 }
+
+// AdmissionChecksForWorkload returns AdmissionChecks that should be assigned to a specific Workload based on
+// ClusterQueue configuration and ResourceFlavors
+func AdmissionChecksForWorkload(log *logr.Logger, wl *kueue.Workload, cq *kueue.ClusterQueue) sets.Set[string] {
+	if len(cq.Spec.AdmissionChecks) != 0 {
+		return sets.New(cq.Spec.AdmissionChecks...)
+	}
+
+	// Kueue sets AdmissionChecks first based on ClusterQueue configuration and at this point Workload has no
+	// ResourceFlavors assigned, so we cannot match AdmissionChecks to ResourceFlavor.
+	// After Quota is reserved, another reconciliation happens and we can match AdmissionChecks to ResourceFlavors
+	if wl.Status.Admission == nil {
+		log.V(2).Info("Workload has no Admission", "Workload", klog.KObj(wl))
+		return nil
+	}
+
+	assignedFlavors := sets.New[string]()
+	for _, podSet := range wl.Status.Admission.PodSetAssignments {
+		for _, flavor := range podSet.Flavors {
+			assignedFlavors.Insert(string(flavor))
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:52:21Z

- **test/integration/scheduler/workload_controller_test.go:130** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:52:21Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:52:43Z

- **test/integration/scheduler/workload_controller_test.go:152** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:52:43Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					return slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check2"))
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:52:52Z

- **test/integration/scheduler/workload_controller_test.go:145** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:52:52Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T13:55:29Z

- **apis/kueue/v1beta1/clusterqueue_types.go:104** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T13:55:28Z
```diff
@@ -93,10 +93,16 @@ type ClusterQueueSpec struct {
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
 
-	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
+	// admissionChecks lists the AdmissionChecks required by this ClusterQueue.
+	// Cannot be used along with AdmissionCheckStrategy.
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
```

  I dont think this should be part of this PR, maybe we could open another issue for deprecating `.spec.AdmissionChecks`. Please note that this has already been mentioned in #768

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-12T14:56:08Z

- **apis/kueue/v1beta1/clusterqueue_types.go:104** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T14:56:08Z
```diff
@@ -93,10 +93,16 @@ type ClusterQueueSpec struct {
 	// lower priority first.
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
 
-	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
+	// admissionChecks lists the AdmissionChecks required by this ClusterQueue.
+	// Cannot be used along with AdmissionCheckStrategy.
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
```

  I think we will just eventually deprecate the entire v1beta1 API. Then we can write a migration guide.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-12T14:57:31Z

- **pkg/workload/workload_test.go:697** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T14:57:31Z
```diff
@@ -643,3 +644,62 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks sets.Set[string]
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1", "ac2"),
+		},
+		"Workload has no QuotaReserved": {
```

  That sounds like the one I want, but I don't see it in this file. Is it somewhere else?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-12T15:06:14Z

- **pkg/controller/core/workload_controller_test.go:296** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T15:01:20Z
```diff
@@ -285,13 +286,15 @@ func TestSyncCheckStates(t *testing.T) {
 
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
-			gotStates, gotShouldChange := syncAdmissionCheckConditions(tc.states, tc.list)
+			gotStates, gotShouldChange := syncAdmissionCheckConditions(tc.states, sets.New(tc.list...))
 
 			if tc.wantChange != gotShouldChange {
 				t.Errorf("Unexpected should change, want=%v", tc.wantChange)
 			}
 
-			opts := []cmp.Option{}
+			opts := []cmp.Option{
+				cmpopts.SortSlices(func(a, b kueue.AdmissionCheckState) bool { return a.Name < b.Name }),
```

  Maybe we can always produce the list alphabetically?
  Then it will be easier for users to find a particular check.

- **pkg/scheduler/scheduler.go:518** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T15:04:25Z
```diff
@@ -515,7 +515,11 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 	}
 
 	workload.SetQuotaReservation(newWorkload, admission)
-	if workload.HasAllChecks(newWorkload, mustHaveChecks) {
+	queue := &kueue.ClusterQueue{}
```

  I don't think I observe any difference.
  
  What I mean is that you can add another field here, or just replace the existing set with a `map[string]sets.Set[string])` that has the information about flavors. https://github.com/kubernetes-sigs/kueue/blob/99ab81d14eca26b806522a8e1554b5f91591abc3/pkg/cache/clusterqueue.go#L65

- **test/integration/scheduler/workload_controller_test.go:193** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T15:06:11Z
```diff
@@ -69,6 +70,137 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", reservationFlavor).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlKey, &updatedWl); err != nil {
+						return false
+					}
+					return workload.HasQuotaReservation(&updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue(), "should have quota reservation")
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					return slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check2"))
+			})
+
+			ginkgo.By("updating admission check strategies", func() {
+				createdQueue := kueue.ClusterQueue{}
+				queueKey := client.ObjectKeyFromObject(clusterQueue)
+				gomega.Eventually(func() error {
+					gomega.Expect(k8sClient.Get(ctx, queueKey, &createdQueue)).To(gomega.Succeed())
+
+					createdQueue.Spec.AdmissionChecksStrategy.AdmissionChecks = []kueue.AdmissionCheckStrategyRule{
+						*testing.MakeAdmissionCheckStrategyRule("check1", flavorOnDemand).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check2", reservationFlavor).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check3").Obj()}
+					return k8sClient.Update(ctx, &createdQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func() []string {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					return slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+				}, util.Timeout, util.Interval).Should(gomega.ConsistOf("check1", "check3"))
+			})
+
+			ginkgo.By("marking the checks as passed", func() {
+				gomega.Eventually(func() error {
+					gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStateReady,
+						Message: "check successfully passed",
+					})
+					workload.SetAdmissionCheckState(&updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:    "check3",
+						State:   kueue.CheckStateReady,
+						Message: "check successfully passed",
+					})
+					return k8sClient.Status().Update(ctx, &updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("admitting the workload", func() {
```

  Maybe you forgot to push?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-12T15:23:00Z

- **pkg/workload/workload_test.go:697** by [@PBundyra](https://github.com/PBundyra) — 2024-04-12T15:23:00Z
```diff
@@ -643,3 +644,62 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks sets.Set[string]
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1", "ac2"),
+		},
+		"Workload has no QuotaReserved": {
```

  Can you double check if the changes you're looking at are up-to-date?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-12T15:39:49Z

- **pkg/cache/clusterqueue_test.go:602** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T15:32:56Z
```diff
@@ -592,31 +599,33 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 
 	for _, tc := range testcases {
 		t.Run(tc.name, func(t *testing.T) {
-			cache := New(utiltesting.NewFakeClient())
-			cq, err := cache.newClusterQueue(cq)
-			if err != nil {
-				t.Fatalf("failed to new clusterQueue %v", err)
-			}
+			for _, cq := range cqs {
```

  following the DAMP not DRY principle, I propose that you instead add a field `cq` into the test cases struct, add more cases and remove this loop.
  
  That way, we can clearly see what is the input cq and the expected output.
  
  https://abseil.io/resources/swe-book/html/ch12.html#:~:text=Tests%20and%20Code%20Sharing%3A%20DAMP%2C%20Not%20DRY

- **pkg/cache/clusterqueue.go:67** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T15:35:02Z
```diff
@@ -52,18 +53,19 @@ type ResourceStats map[corev1.ResourceName]*QuotaStats
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds admitted workloads.
 type ClusterQueue struct {
-	Name              string
-	Cohort            *Cohort
-	ResourceGroups    []ResourceGroup
-	RGByResource      map[corev1.ResourceName]*ResourceGroup
-	Usage             FlavorResourceQuantities
-	Workloads         map[string]*workload.Info
-	WorkloadsNotReady sets.Set[string]
-	NamespaceSelector labels.Selector
-	Preemption        kueue.ClusterQueuePreemption
-	FlavorFungibility kueue.FlavorFungibility
-	AdmissionChecks   sets.Set[string]
-	Status            metrics.ClusterQueueStatus
+	Name                   string
+	Cohort                 *Cohort
+	ResourceGroups         []ResourceGroup
+	RGByResource           map[corev1.ResourceName]*ResourceGroup
+	Usage                  FlavorResourceQuantities
+	Workloads              map[string]*workload.Info
+	WorkloadsNotReady      sets.Set[string]
+	NamespaceSelector      labels.Selector
+	Preemption             kueue.ClusterQueuePreemption
+	FlavorFungibility      kueue.FlavorFungibility
+	AdmissionChecks        sets.Set[string]
+	AdmissionCheckStrategy kueue.AdmissionChecksStrategy
```

  maybe it's better to fuse these fields. So we only process the API once in `ClusterQueue.update`, translate everything into admissionChecksStrategy (or a map based version of it) and the rest of the code just works with the strategies.

- **test/integration/scheduler/workload_controller_test.go:139** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T15:38:35Z
```diff
@@ -69,6 +70,134 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", kueue.ResourceFlavorReference(flavorOnDemand)).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", kueue.ResourceFlavorReference(reservationFlavor)).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
```

  ```suggestion
  			ginkgo.By("creating and waiting for workload to have a quota reservation", func() {
  ```

- **test/integration/scheduler/workload_controller_test.go:149** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T15:39:08Z
```diff
@@ -69,6 +70,134 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", kueue.ResourceFlavorReference(flavorOnDemand)).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", kueue.ResourceFlavorReference(reservationFlavor)).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(&updatedWl)).Should(gomega.BeTrue(), "should have quota reservation")
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(workload.IsAdmitted(&updatedWl)).To(gomega.BeFalse())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
```

  Can it be in the same eventually for quota reservation? Although I seem to remember that it's added separately.

- **test/integration/scheduler/workload_controller_test.go:156** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T15:39:23Z
```diff
@@ -69,6 +70,134 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", kueue.ResourceFlavorReference(flavorOnDemand)).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", kueue.ResourceFlavorReference(reservationFlavor)).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(&updatedWl)).Should(gomega.BeTrue(), "should have quota reservation")
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(workload.IsAdmitted(&updatedWl)).To(gomega.BeFalse())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					checks := slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+					g.Expect(checks).Should(gomega.ConsistOf("check1", "check2"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("updating admission check strategies", func() {
```

  ```suggestion
  			ginkgo.By("adding an additional admission check to the clusterqueue", func() {
  ```

- **test/integration/scheduler/workload_controller_test.go:169** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-12T15:39:41Z
```diff
@@ -69,6 +70,134 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", kueue.ResourceFlavorReference(flavorOnDemand)).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", kueue.ResourceFlavorReference(reservationFlavor)).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(&updatedWl)).Should(gomega.BeTrue(), "should have quota reservation")
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(workload.IsAdmitted(&updatedWl)).To(gomega.BeFalse())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					checks := slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+					g.Expect(checks).Should(gomega.ConsistOf("check1", "check2"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("updating admission check strategies", func() {
+				createdQueue := kueue.ClusterQueue{}
+				queueKey := client.ObjectKeyFromObject(clusterQueue)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, queueKey, &createdQueue)).To(gomega.Succeed())
+					createdQueue.Spec.AdmissionChecksStrategy.AdmissionChecks = []kueue.AdmissionCheckStrategyRule{
+						*testing.MakeAdmissionCheckStrategyRule("check1", kueue.ResourceFlavorReference(flavorOnDemand)).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check2", kueue.ResourceFlavorReference(reservationFlavor)).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check3").Obj()}
+					g.Expect(k8sClient.Update(ctx, &createdQueue)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
```

  No action from the test, so no worthy of a `By`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-15T18:26:53Z

- **pkg/cache/clusterqueue.go:67** by [@PBundyra](https://github.com/PBundyra) — 2024-04-15T18:26:53Z
```diff
@@ -52,18 +53,19 @@ type ResourceStats map[corev1.ResourceName]*QuotaStats
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds admitted workloads.
 type ClusterQueue struct {
-	Name              string
-	Cohort            *Cohort
-	ResourceGroups    []ResourceGroup
-	RGByResource      map[corev1.ResourceName]*ResourceGroup
-	Usage             FlavorResourceQuantities
-	Workloads         map[string]*workload.Info
-	WorkloadsNotReady sets.Set[string]
-	NamespaceSelector labels.Selector
-	Preemption        kueue.ClusterQueuePreemption
-	FlavorFungibility kueue.FlavorFungibility
-	AdmissionChecks   sets.Set[string]
-	Status            metrics.ClusterQueueStatus
+	Name                   string
+	Cohort                 *Cohort
+	ResourceGroups         []ResourceGroup
+	RGByResource           map[corev1.ResourceName]*ResourceGroup
+	Usage                  FlavorResourceQuantities
+	Workloads              map[string]*workload.Info
+	WorkloadsNotReady      sets.Set[string]
+	NamespaceSelector      labels.Selector
+	Preemption             kueue.ClusterQueuePreemption
+	FlavorFungibility      kueue.FlavorFungibility
+	AdmissionChecks        sets.Set[string]
+	AdmissionCheckStrategy kueue.AdmissionChecksStrategy
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-15T18:27:01Z

- **test/integration/scheduler/workload_controller_test.go:149** by [@PBundyra](https://github.com/PBundyra) — 2024-04-15T18:27:00Z
```diff
@@ -69,6 +70,134 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", kueue.ResourceFlavorReference(flavorOnDemand)).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", kueue.ResourceFlavorReference(reservationFlavor)).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(&updatedWl)).Should(gomega.BeTrue(), "should have quota reservation")
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(workload.IsAdmitted(&updatedWl)).To(gomega.BeFalse())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-15T18:27:07Z

- **test/integration/scheduler/workload_controller_test.go:169** by [@PBundyra](https://github.com/PBundyra) — 2024-04-15T18:27:06Z
```diff
@@ -69,6 +70,134 @@ var _ = ginkgo.Describe("Workload controller with scheduler", func() {
 		updatedCQ = kueue.ClusterQueue{}
 	})
 
+	ginkgo.When("the queue has admission check strategies", func() {
+		var (
+			flavor1           *kueue.ResourceFlavor
+			flavor2           *kueue.ResourceFlavor
+			check1            *kueue.AdmissionCheck
+			check2            *kueue.AdmissionCheck
+			check3            *kueue.AdmissionCheck
+			reservationFlavor string = "reservation"
+			updatedWl         kueue.Workload
+			flavorOnDemand    string              = "on-demand"
+			resourceGPU       corev1.ResourceName = "example.com/gpu"
+		)
+
+		ginkgo.BeforeEach(func() {
+			flavor1 = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor1)).Should(gomega.Succeed())
+
+			flavor2 = testing.MakeResourceFlavor(reservationFlavor).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor2)).Should(gomega.Succeed())
+
+			check1 = testing.MakeAdmissionCheck("check1").ControllerName("ctrl1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check1)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check1, metav1.ConditionTrue)
+
+			check2 = testing.MakeAdmissionCheck("check2").ControllerName("ctrl2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check2)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check2, metav1.ConditionTrue)
+
+			check3 = testing.MakeAdmissionCheck("check3").ControllerName("ctrl3").Obj()
+			gomega.Expect(k8sClient.Create(ctx, check3)).Should(gomega.Succeed())
+			util.SetAdmissionCheckActive(ctx, k8sClient, check3, metav1.ConditionTrue)
+
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				AdmissionCheckStrategy(
+					*testing.MakeAdmissionCheckStrategyRule("check1", kueue.ResourceFlavorReference(flavorOnDemand)).Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check2").Obj(),
+					*testing.MakeAdmissionCheckStrategyRule("check3", kueue.ResourceFlavorReference(reservationFlavor)).Obj()).
+				ResourceGroup(
+					*testing.MakeFlavorQuotas(reservationFlavor).Resource(resourceGPU, "1", "1").Obj(),
+					*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check3, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check2, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, check1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor1, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor2, true)
+		})
+
+		ginkgo.It("the workload should have appropriate AdditionalChecks added", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue("queue").
+				Request(resourceGPU, "3").
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+
+			ginkgo.By("creating and reserving quota the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(workload.HasQuotaReservation(&updatedWl)).Should(gomega.BeTrue(), "should have quota reservation")
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(workload.IsAdmitted(&updatedWl)).To(gomega.BeFalse())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					checks := slices.Map(updatedWl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+					g.Expect(checks).Should(gomega.ConsistOf("check1", "check2"))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("updating admission check strategies", func() {
+				createdQueue := kueue.ClusterQueue{}
+				queueKey := client.ObjectKeyFromObject(clusterQueue)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, queueKey, &createdQueue)).To(gomega.Succeed())
+					createdQueue.Spec.AdmissionChecksStrategy.AdmissionChecks = []kueue.AdmissionCheckStrategyRule{
+						*testing.MakeAdmissionCheckStrategyRule("check1", kueue.ResourceFlavorReference(flavorOnDemand)).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check2", kueue.ResourceFlavorReference(reservationFlavor)).Obj(),
+						*testing.MakeAdmissionCheckStrategyRule("check3").Obj()}
+					g.Expect(k8sClient.Update(ctx, &createdQueue)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("checking workload's admission checks", func() {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-15T18:27:39Z

- **pkg/cache/clusterqueue_test.go:602** by [@PBundyra](https://github.com/PBundyra) — 2024-04-15T18:27:39Z
```diff
@@ -592,31 +599,33 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 
 	for _, tc := range testcases {
 		t.Run(tc.name, func(t *testing.T) {
-			cache := New(utiltesting.NewFakeClient())
-			cq, err := cache.newClusterQueue(cq)
-			if err != nil {
-				t.Fatalf("failed to new clusterQueue %v", err)
-			}
+			for _, cq := range cqs {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-15T18:47:08Z

- **pkg/controller/core/workload_controller_test.go:296** by [@PBundyra](https://github.com/PBundyra) — 2024-04-15T18:47:08Z
```diff
@@ -285,13 +286,15 @@ func TestSyncCheckStates(t *testing.T) {
 
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
-			gotStates, gotShouldChange := syncAdmissionCheckConditions(tc.states, tc.list)
+			gotStates, gotShouldChange := syncAdmissionCheckConditions(tc.states, sets.New(tc.list...))
 
 			if tc.wantChange != gotShouldChange {
 				t.Errorf("Unexpected should change, want=%v", tc.wantChange)
 			}
 
-			opts := []cmp.Option{}
+			opts := []cmp.Option{
+				cmpopts.SortSlices(func(a, b kueue.AdmissionCheckState) bool { return a.Name < b.Name }),
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-15T19:05:08Z

just 2 nits

- **pkg/cache/snapshot.go:147** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-15T19:02:43Z
```diff
@@ -143,9 +143,11 @@ func (c *ClusterQueue) snapshot() *ClusterQueue {
 		Preemption:                    c.Preemption,
 		NamespaceSelector:             c.NamespaceSelector,
 		Status:                        c.Status,
-		AdmissionChecks:               c.AdmissionChecks.Clone(),
-		AdmissionCheckStrategy:        c.AdmissionCheckStrategy,
 	}
+	admissionChecks := make(map[string]sets.Set[string], len(c.AdmissionChecks))
```

  Wouldn't `maps.Clone` accomplish the same?

- **pkg/controller/core/workload_controller_test.go:295** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-15T19:03:52Z
```diff
@@ -292,9 +292,7 @@ func TestSyncCheckStates(t *testing.T) {
 				t.Errorf("Unexpected should change, want=%v", tc.wantChange)
 			}
 
-			opts := []cmp.Option{
-				cmpopts.SortSlices(func(a, b kueue.AdmissionCheckState) bool { return a.Name < b.Name }),
-			}
+			opts := []cmp.Option{}
```

  ```suggestion
  			var opts []cmp.Option
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-16T10:12:34Z

- **pkg/cache/snapshot.go:147** by [@PBundyra](https://github.com/PBundyra) — 2024-04-16T10:12:34Z
```diff
@@ -143,9 +143,11 @@ func (c *ClusterQueue) snapshot() *ClusterQueue {
 		Preemption:                    c.Preemption,
 		NamespaceSelector:             c.NamespaceSelector,
 		Status:                        c.Status,
-		AdmissionChecks:               c.AdmissionChecks.Clone(),
-		AdmissionCheckStrategy:        c.AdmissionCheckStrategy,
 	}
+	admissionChecks := make(map[string]sets.Set[string], len(c.AdmissionChecks))
```

  In this case we needed a deep copy of the set so `maps.Clone` wouldn't be sufficient. I've added a new function that creates a deep copy

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T13:12:42Z

- **pkg/controller/core/workload_controller.go:269** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T13:12:42Z
```diff
@@ -253,17 +257,19 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 }
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cqName string) (bool, error) {
-	// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
-	queue := kueue.ClusterQueue{}
-	if err := r.client.Get(ctx, types.NamespacedName{Name: cqName}, &queue); err != nil {
+	log := ctrl.LoggerFrom(ctx)
+
+	// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
+	cq := kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, types.NamespacedName{Name: cqName}, &cq); err != nil {
 		return false, err
 	}
 
-	queueAdmissionChecks := queue.Spec.AdmissionChecks
-	newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+	admissionChecks := workload.AdmissionChecksForWorkload(log, wl, utilac.NewAdmissionChecks(&cq))
+	log.V(2).Info("PATRYK created ac", "ac", admissionChecks)
```

  hi Patryk :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-16T14:58:35Z

- **apis/kueue/v1beta1/clusterqueue_types.go:140** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T14:38:03Z
```diff
@@ -117,6 +123,23 @@ type ClusterQueueSpec struct {
 	StopPolicy *StopPolicy `json:"stopPolicy,omitempty"`
 }
 
+// AdmissionCheckStrategy defines a strategy for a AdmissionCheck.
+type AdmissionChecksStrategy struct {
+	// admissionChecks is a list of strategies for AdmissionChecks
+	AdmissionChecks []AdmissionCheckStrategyRule `json:"admissionChecks,omitempty"`
+}
+
+// AdmissionCheckStrategyRule defines rules for a single AdmissionCheck
+type AdmissionCheckStrategyRule struct {
+	// name is an AdmissionCheck's name.
+	Name string `json:"name"`
+
+	// onFlavors is a list of ResourceFlavors' names that this AdmissionCheck should run for.
+	// If empty, the AdmissionCheck will run for all workloads submitted to the ClusterQueue.
+	// +optional
+	OnFlavors []ResourceFlavorReference `json:"onFlavors"`
```

  ```suggestion
  	OnFlavors []ResourceFlavorReference `json:"onFlavors,omitempty"`
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:109** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T14:43:10Z
```diff
@@ -98,10 +98,16 @@ type ClusterQueueSpec struct {
 	// +kubebuilder:default={}
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
 
-	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
+	// admissionChecks lists the AdmissionChecks required by this ClusterQueue.
+	// Cannot be used along with AdmissionCheckStrategy.
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
 
+	// admissionCheckStrategy defines a list of strategies to determine which ResourceFlavors require AdmissionChecks.
+	// This property cannot be used in conjunction with the 'admissionChecks' property.
+	// +optional
+	AdmissionChecksStrategy AdmissionChecksStrategy `json:"admissionChecksStrategy,omitempty"`
```

  ```suggestion
  	AdmissionChecksStrategy *AdmissionChecksStrategy `json:"admissionChecksStrategy,omitempty"`
  ```
  
  Based on [the API documentation](https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#optional-vs-required), I'm wondering if we should make this field pointer.
  
  @alculquicondor WDYT?

- **pkg/cache/clusterqueue.go:69** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T14:47:11Z
```diff
@@ -62,8 +63,11 @@ type ClusterQueue struct {
 	NamespaceSelector labels.Selector
 	Preemption        kueue.ClusterQueuePreemption
 	FlavorFungibility kueue.FlavorFungibility
-	AdmissionChecks   sets.Set[string]
-	Status            metrics.ClusterQueueStatus
+	// Aggregates AdmissionChecks from both .spec.AdmissionChecks and .spec.AdmissionCheckStrategy
+	// Sets hold ResourceFlavors to which an AdmissionCheck should apply.
+	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
+	AdmissionChecks map[string]sets.Set[string]
```

  ```suggestion
  	AdmissionChecks map[string]sets.Set[kueue.ResourceFlavorReference]
  ```
  
  I'm wondering if we can avoid casting at some points.
  @PBundyra WDYT?

- **pkg/workload/workload_test.go:713** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T14:52:55Z
```diff
@@ -647,3 +649,71 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks sets.Set[string]
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"AdmissionCheckStrategy with an unmatched flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "unmatched-flavor").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1", "ac2"),
+		},
+		"Workload has no QuotaReserved": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, log := utiltesting.ContextWithLog(t)
+			gotAdmissionChecks := AdmissionChecksForWorkload(log, tc.wl, utilac.NewAdmissionChecks(tc.cq))
+
+			if diff := cmp.Diff(tc.wantAdmissionChecks, gotAdmissionChecks); diff != "" {
```

  ```suggestion
  			if diff := cmp.Diff(tc.wantAdmissionChecks, gotAdmissionChecks, cmpopts.SortMap(...)); diff != "" {
  ```
  
  This could have a potential flaky test since the `map` doesn't guarantee the order.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T15:04:03Z

- **apis/kueue/v1beta1/clusterqueue_types.go:109** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T15:04:03Z
```diff
@@ -98,10 +98,16 @@ type ClusterQueueSpec struct {
 	// +kubebuilder:default={}
 	Preemption *ClusterQueuePreemption `json:"preemption,omitempty"`
 
-	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
+	// admissionChecks lists the AdmissionChecks required by this ClusterQueue.
+	// Cannot be used along with AdmissionCheckStrategy.
 	// +optional
 	AdmissionChecks []string `json:"admissionChecks,omitempty"`
 
+	// admissionCheckStrategy defines a list of strategies to determine which ResourceFlavors require AdmissionChecks.
+	// This property cannot be used in conjunction with the 'admissionChecks' property.
+	// +optional
+	AdmissionChecksStrategy AdmissionChecksStrategy `json:"admissionChecksStrategy,omitempty"`
```

  oh right, now that it's not a list, it's worth making it a pointer.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T15:07:25Z

- **pkg/workload/workload_test.go:713** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T15:07:25Z
```diff
@@ -647,3 +649,71 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks sets.Set[string]
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"AdmissionCheckStrategy with an unmatched flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "unmatched-flavor").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1", "ac2"),
+		},
+		"Workload has no QuotaReserved": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, log := utiltesting.ContextWithLog(t)
+			gotAdmissionChecks := AdmissionChecksForWorkload(log, tc.wl, utilac.NewAdmissionChecks(tc.cq))
+
+			if diff := cmp.Diff(tc.wantAdmissionChecks, gotAdmissionChecks); diff != "" {
```

  but both are sets

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-16T15:09:22Z

- **pkg/workload/workload_test.go:713** by [@PBundyra](https://github.com/PBundyra) — 2024-04-16T15:09:22Z
```diff
@@ -647,3 +649,71 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks sets.Set[string]
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"AdmissionCheckStrategy with an unmatched flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "unmatched-flavor").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1", "ac2"),
+		},
+		"Workload has no QuotaReserved": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, log := utiltesting.ContextWithLog(t)
+			gotAdmissionChecks := AdmissionChecksForWorkload(log, tc.wl, utilac.NewAdmissionChecks(tc.cq))
+
+			if diff := cmp.Diff(tc.wantAdmissionChecks, gotAdmissionChecks); diff != "" {
```

  I don't think this is the case, since we compare `sets.Set` and it implements `Equal` method which is invoked during comparison

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-16T15:09:45Z

- **pkg/workload/workload_test.go:713** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T15:09:45Z
```diff
@@ -647,3 +649,71 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks sets.Set[string]
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"AdmissionCheckStrategy with an unmatched flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "unmatched-flavor").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1", "ac2"),
+		},
+		"Workload has no QuotaReserved": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, log := utiltesting.ContextWithLog(t)
+			gotAdmissionChecks := AdmissionChecksForWorkload(log, tc.wl, utilac.NewAdmissionChecks(tc.cq))
+
+			if diff := cmp.Diff(tc.wantAdmissionChecks, gotAdmissionChecks); diff != "" {
```

  IIUC, the sets are typed `map[T]struct{}`. So, could we re-order by the `cmpopts.SortMap`.
  @PBundyra, could you investigate them?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-16T15:10:55Z

- **pkg/workload/workload_test.go:713** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T15:10:54Z
```diff
@@ -647,3 +649,71 @@ func TestResourceUsage(t *testing.T) {
 		})
 	}
 }
+
+func TestAdmissionCheckStrategy(t *testing.T) {
+	cases := map[string]struct {
+		cq                  *kueue.ClusterQueue
+		wl                  *kueue.Workload
+		wantAdmissionChecks sets.Set[string]
+	}{
+		"AdmissionCheckStrategy with a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"AdmissionCheckStrategy with an unmatched flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "unmatched-flavor").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+		"AdmissionCheckStrategy without a flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(*utiltesting.MakeAdmissionCheckStrategyRule("ac1").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1"),
+		},
+		"Two AdmissionCheckStrategies, one with flavor, one without flavor": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("cq").Assignment("cpu", "flavor1", "1").Obj()).
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: sets.New("ac1", "ac2"),
+		},
+		"Workload has no QuotaReserved": {
+			wl: utiltesting.MakeWorkload("wl", "ns").
+				Obj(),
+			cq: utiltesting.MakeClusterQueue("cq").
+				AdmissionCheckStrategy(
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac1", "flavor1").Obj(),
+					*utiltesting.MakeAdmissionCheckStrategyRule("ac2").Obj()).
+				Obj(),
+			wantAdmissionChecks: nil,
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			_, log := utiltesting.ContextWithLog(t)
+			gotAdmissionChecks := AdmissionChecksForWorkload(log, tc.wl, utilac.NewAdmissionChecks(tc.cq))
+
+			if diff := cmp.Diff(tc.wantAdmissionChecks, gotAdmissionChecks); diff != "" {
```

  > I don't think this is the case, since we compare sets.Set and it implements Equal method which is invoked during comparison
  
  Oh, I didn't know that. Thank you for the clarification!

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-16T15:17:49Z

- **pkg/cache/clusterqueue.go:69** by [@PBundyra](https://github.com/PBundyra) — 2024-04-16T15:17:49Z
```diff
@@ -62,8 +63,11 @@ type ClusterQueue struct {
 	NamespaceSelector labels.Selector
 	Preemption        kueue.ClusterQueuePreemption
 	FlavorFungibility kueue.FlavorFungibility
-	AdmissionChecks   sets.Set[string]
-	Status            metrics.ClusterQueueStatus
+	// Aggregates AdmissionChecks from both .spec.AdmissionChecks and .spec.AdmissionCheckStrategy
+	// Sets hold ResourceFlavors to which an AdmissionCheck should apply.
+	// In case its empty, it means an AdmissionCheck should apply to all ResourceFlavor
+	AdmissionChecks map[string]sets.Set[string]
```

  Good idea, thanks!
