# PR #1971: Allow run test-multikueue-e2e for mac os

**Summary**: Allow run test-multikueue-e2e for mac os

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1971

**Last updated**: 2024-04-23T18:38:32Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/test-e2e-multikueue-cluster` → `main`
- **Created**: 2024-04-10T17:26:25Z
- **Updated**: 2024-04-23T18:38:32Z
- **Merged**: 2024-04-23T06:19:00Z
- **Closed**: 2024-04-23T06:19:00Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 4   **+52 / -29**

## Description

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:
To allow run test-multikueue-e2e on mac os.

#### Which issue(s) this PR fixes:
Related to [#1924](https://github.com/kubernetes-sigs/kueue/issues/1924)

#### Special notes for your reviewer:
The docker on mac os working another way than on linux. So we can't bind port to 178.18.0.1. That's why we need to use internal kind cluster connection to control plane on  MultiKueue secrets. 

#### Does this PR introduce a user-facing change?
No

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2024-04-10T17:26:29Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: mbobrovskyi / name: Mykhailo Bobrovskyi  (3dac8608921c47f8155efd3df345e9e0109d9301, 74d1f7fac5b612d6536dc36ebbb07b63835e274a, 2d94ca517fefae6a4476368ef73ec9d25dc68dc6, 5411d8f705cd360f81b66e6ad80d13ceca0bc3d4, 624c627f4fa80602c20c1f72599b33b159e63d0b, aa4d5bb95faec6ed13a70196a2069f612a73489c, 284a73c6c9fc21bbdbeca965074b694edf299101, f26456158ee1ab1ce5f1ff9113f15006b5077517, e0513ee37a7ac16464174f86e0c0751701ada719, a6bedb71cab32e5215284b038d528f41b6214e70, d09f726aa85de41d7af03f91922559fbdfac9894, 1675fdd2788f38f812714b6d1bf40e0447009441, 1bf0baf102e533cbeea62fcde24c44ffc1193edc, f6b8aaea7153b218ce18a550db310e37d7c74080, 011bb0ec78433359016c89c3c9c063be5ef59925, 360feb5b23920616acdb38fcc0487086750b228e, 0be032bd0ed55f2943cce2c702644641c74d2a29)</li></ul>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-10T17:26:34Z

Welcome @mbobrovskyi! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-10T17:26:34Z

Hi @mbobrovskyi. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-10T17:26:42Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0be032bd0ed55f2943cce2c702644641c74d2a29 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6627493f41154700089174f7 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-10T17:28:10Z

/ok-to-test

### Comment by [@trasc](https://github.com/trasc) — 2024-04-10T18:08:42Z

/assign

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-11T03:32:14Z

Works for me 👍🏻 
We should somehow ensure that timeout works for e2e tests even on low performant env, because right now it times out

### Comment by [@trasc](https://github.com/trasc) — 2024-04-11T09:30:54Z

/test pull-kueue-test-multikueue-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-04-11T09:40:31Z

/test pull-kueue-test-multikueue-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-04-12T16:34:38Z

/test pull-kueue-test-multikueue-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-04-12T16:44:12Z

/test pull-kueue-test-multikueue-e2e-main-1-29

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-22T06:22:05Z

/test pull-kueue-test-multikueue-e2e-main-1-29

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-22T06:49:22Z

/test pull-kueue-test-multikueue-e2e-main-1-29

### Comment by [@trasc](https://github.com/trasc) — 2024-04-22T07:14:35Z

/test pull-kueue-test-multikueue-e2e-main-1-29

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-22T13:28:35Z

@trasc wdyt of the overall approach?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-22T17:15:45Z

/approve

Leaving LGTM to @trasc

### Comment by [@trasc](https://github.com/trasc) — 2024-04-22T17:44:38Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-22T17:44:43Z

LGTM label has been added.  <details>Git tree hash: e1babc6a9425b701cba6af70e908283485147cfe</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-22T18:10:45Z

/release-note-none

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-23T06:15:11Z

LGTM label has been added.  <details>Git tree hash: 9e16709ab058eca720b87a45e9365d58ab1d473c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-23T06:15:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1971#issuecomment-2070298629" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1971#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1971#pullrequestreview-2016323792" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[hack/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/hack/OWNERS)~~ [alculquicondor,tenzen-y]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-23T06:16:03Z

/remove-kind bug
/kind cleanup

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-10T17:32:28Z

@trasc can you give a pass?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-11T12:26:07Z

- **hack/create-multikueue-kubeconfig.sh:136** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-11T12:26:06Z
```diff
@@ -126,15 +126,14 @@ CA_CERT=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.d
 # Extract cluster IP from the current context
 CURRENT_CONTEXT=$(kubectl config current-context)
 CURRENT_CLUSTER=$(kubectl config view -o jsonpath="{.contexts[?(@.name == \"${CURRENT_CONTEXT}\"})].context.cluster}")
-CURRENT_CLUSTER_ADDR=$(kubectl config view -o jsonpath="{.clusters[?(@.name == \"${CURRENT_CLUSTER}\"})].cluster.server}")
 
 echo "Writing kubeconfig in ${KUBECONFIG_OUT}"
 cat > ${KUBECONFIG_OUT} <<EOF
 apiVersion: v1
 clusters:
 - cluster:
     certificate-authority-data: ${CA_CERT}
-    server: ${CURRENT_CLUSTER_ADDR}
+    server: https://${CURRENT_CLUSTER/#kind-}-control-plane:6443
```

  This looks suspiciously hardcoded.
  Are you sure this would work outside of mac os?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-11T12:34:18Z

- **hack/create-multikueue-kubeconfig.sh:136** by [@trasc](https://github.com/trasc) — 2024-04-11T12:34:18Z
```diff
@@ -126,15 +126,14 @@ CA_CERT=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.d
 # Extract cluster IP from the current context
 CURRENT_CONTEXT=$(kubectl config current-context)
 CURRENT_CLUSTER=$(kubectl config view -o jsonpath="{.contexts[?(@.name == \"${CURRENT_CONTEXT}\"})].context.cluster}")
-CURRENT_CLUSTER_ADDR=$(kubectl config view -o jsonpath="{.clusters[?(@.name == \"${CURRENT_CLUSTER}\"})].cluster.server}")
 
 echo "Writing kubeconfig in ${KUBECONFIG_OUT}"
 cat > ${KUBECONFIG_OUT} <<EOF
 apiVersion: v1
 clusters:
 - cluster:
     certificate-authority-data: ${CA_CERT}
-    server: ${CURRENT_CLUSTER_ADDR}
+    server: https://${CURRENT_CLUSTER/#kind-}-control-plane:6443
```

  It usually works , but for some reason after the worker reconnects and manager->worker connection works, the host -> worker client has a strange behavior. (getting a cluster queue takes abnormally long ).

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-11T13:08:07Z

- **hack/create-multikueue-kubeconfig.sh:136** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-11T13:08:07Z
```diff
@@ -126,15 +126,14 @@ CA_CERT=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.d
 # Extract cluster IP from the current context
 CURRENT_CONTEXT=$(kubectl config current-context)
 CURRENT_CLUSTER=$(kubectl config view -o jsonpath="{.contexts[?(@.name == \"${CURRENT_CONTEXT}\"})].context.cluster}")
-CURRENT_CLUSTER_ADDR=$(kubectl config view -o jsonpath="{.clusters[?(@.name == \"${CURRENT_CLUSTER}\"})].cluster.server}")
 
 echo "Writing kubeconfig in ${KUBECONFIG_OUT}"
 cat > ${KUBECONFIG_OUT} <<EOF
 apiVersion: v1
 clusters:
 - cluster:
     certificate-authority-data: ${CA_CERT}
-    server: ${CURRENT_CLUSTER_ADDR}
+    server: https://${CURRENT_CLUSTER/#kind-}-control-plane:6443
```

  Yeap, this should work. This is an internal address for cluster control plane generated by kind. 
  
  To be more clear we can also get config by executing this command `kind get kubeconfig --internal --name ${CLUSTER_NAME} > ${KUBECONFIG_OUT}`. But on this way we need to add ${SA_TOKEN} in some way.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-12T18:02:27Z

- **test/e2e/multikueue/e2e_test.go:384** by [@trasc](https://github.com/trasc) — 2024-04-12T18:02:27Z
```diff
@@ -363,13 +371,17 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 							Reason: "ClientConnectionFailed",
 						},
 						cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime", "Message"))))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Reconnecting worker1 container to the kind network", func() {
-				cmd := exec.Command("docker", "network", "connect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.List(ctx, &corev1.PodList{}, podsListOptions)).To(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+				time.Sleep(util.LongTimeout)
```

  :), no

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-15T08:35:26Z

- **hack/create-multikueue-kubeconfig.sh:136** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-15T08:35:26Z
```diff
@@ -126,15 +126,14 @@ CA_CERT=$(kubectl get -n ${NAMESPACE} secrets/${SA_SECRET_NAME} -o "jsonpath={.d
 # Extract cluster IP from the current context
 CURRENT_CONTEXT=$(kubectl config current-context)
 CURRENT_CLUSTER=$(kubectl config view -o jsonpath="{.contexts[?(@.name == \"${CURRENT_CONTEXT}\"})].context.cluster}")
-CURRENT_CLUSTER_ADDR=$(kubectl config view -o jsonpath="{.clusters[?(@.name == \"${CURRENT_CLUSTER}\"})].cluster.server}")
 
 echo "Writing kubeconfig in ${KUBECONFIG_OUT}"
 cat > ${KUBECONFIG_OUT} <<EOF
 apiVersion: v1
 clusters:
 - cluster:
     certificate-authority-data: ${CA_CERT}
-    server: ${CURRENT_CLUSTER_ADDR}
+    server: https://${CURRENT_CLUSTER/#kind-}-control-plane:6443
```

  This script can be used for kind and external clusters. So take a decision to do not change this script and not restrict it. Instead that move this logic of cluster.server replacement a little bit upper on `multikueue-e2e-test.sh`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-15T09:08:39Z

- **test/e2e/multikueue/e2e_test.go:384** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-15T09:08:39Z
```diff
@@ -363,13 +371,17 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 							Reason: "ClientConnectionFailed",
 						},
 						cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime", "Message"))))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Reconnecting worker1 container to the kind network", func() {
-				cmd := exec.Command("docker", "network", "connect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.List(ctx, &corev1.PodList{}, podsListOptions)).To(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+				time.Sleep(util.LongTimeout)
```

  For some reason, after reconnecting the container to the network, when we try to get pods, we get it with the previous values (as before disconnect). Therefore, it takes some time for the cluster to restore them and we got actually values.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-15T09:47:59Z

- **test/e2e/multikueue/e2e_test.go:384** by [@trasc](https://github.com/trasc) — 2024-04-15T09:47:55Z
```diff
@@ -363,13 +371,17 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 							Reason: "ClientConnectionFailed",
 						},
 						cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime", "Message"))))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Reconnecting worker1 container to the kind network", func() {
-				cmd := exec.Command("docker", "network", "connect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.List(ctx, &corev1.PodList{}, podsListOptions)).To(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+				time.Sleep(util.LongTimeout)
```

  I understand but instead of  just sleeping `util.LongTimeout` we need to wait for an event that indicates the the `some time` has passed and we can continue with our test now.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-22T13:28:23Z

- **test/e2e/multikueue/e2e_test.go:364** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-15T14:27:02Z
```diff
@@ -344,13 +346,20 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 	})
 	ginkgo.When("The connection to a worker cluster is unreliable", func() {
 		ginkgo.It("Should update the cluster status to reflect the connection state", func() {
+			worker1Container := fmt.Sprintf("%s-control-plane", worker1ClusterName)
+			worker1ClusterKey := client.ObjectKeyFromObject(workerCluster1)
+
 			ginkgo.By("Disconnecting worker1 container from the kind network", func() {
-				cmd := exec.Command("docker", "network", "disconnect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "disconnect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
-			})
 
-			worker1ClusterKey := client.ObjectKeyFromObject(workerCluster1)
+				podList := &corev1.PodList{}
+				podListOptions := client.InNamespace("kueue-system")
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.List(ctx, podList, podListOptions)).NotTo(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
 
 			ginkgo.By("Waiting for the cluster do become inactive", func() {
```

  ```suggestion
  			ginkgo.By("Waiting for the cluster to become inactive", func() {
  ```

- **test/e2e/multikueue/e2e_test.go:361** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-15T14:28:54Z
```diff
@@ -344,13 +346,20 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 	})
 	ginkgo.When("The connection to a worker cluster is unreliable", func() {
 		ginkgo.It("Should update the cluster status to reflect the connection state", func() {
+			worker1Container := fmt.Sprintf("%s-control-plane", worker1ClusterName)
+			worker1ClusterKey := client.ObjectKeyFromObject(workerCluster1)
+
 			ginkgo.By("Disconnecting worker1 container from the kind network", func() {
-				cmd := exec.Command("docker", "network", "disconnect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "disconnect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
-			})
 
-			worker1ClusterKey := client.ObjectKeyFromObject(workerCluster1)
+				podList := &corev1.PodList{}
+				podListOptions := client.InNamespace("kueue-system")
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.List(ctx, podList, podListOptions)).NotTo(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  ```suggestion
  				gomega.Eventually(func(g gomega.Gomega) error {
  					return k8sWorker1Client.List(ctx, podList, podListOptions)
  				}, util.LongTimeout, util.Interval).ShouldNot(gomega.Succeed())
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-22T13:58:06Z

- **test/e2e/multikueue/e2e_test.go:384** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-22T13:58:06Z
```diff
@@ -363,13 +371,17 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 							Reason: "ClientConnectionFailed",
 						},
 						cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime", "Message"))))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Reconnecting worker1 container to the kind network", func() {
-				cmd := exec.Command("docker", "network", "connect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sWorker1Client.List(ctx, &corev1.PodList{}, podsListOptions)).To(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+				time.Sleep(util.LongTimeout)
```

  @trasc  Can we resolve it?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-22T15:41:30Z

- **test/e2e/multikueue/e2e_test.go:392** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-22T15:36:09Z
```diff
@@ -364,13 +382,24 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 							Reason: "ClientConnectionFailed",
 						},
 						util.IgnoreConditionTimestampsAndObservedGeneration, util.IgnoreConditionMessage)))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Reconnecting worker1 container to the kind network", func() {
-				cmd := exec.Command("docker", "network", "connect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sWorker1Client, worker1Cq2)).ToNot(gomega.HaveOccurred())
```

  will this always immediately succeed or should we put this inside an "Eventually"?

- **test/e2e/multikueue/e2e_test.go:396** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-22T15:40:05Z
```diff
@@ -364,13 +382,24 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 							Reason: "ClientConnectionFailed",
 						},
 						util.IgnoreConditionTimestampsAndObservedGeneration, util.IgnoreConditionMessage)))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Reconnecting worker1 container to the kind network", func() {
-				cmd := exec.Command("docker", "network", "connect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sWorker1Client, worker1Cq2)).ToNot(gomega.HaveOccurred())
+
+				// After reconnecting the container to the network, when we try to get pods,
```

  That reminds me that clients in controller-runtime are cached.
  
  I wonder if we could just recreate the client or something like that.
  
  But do we really need to check the client? We should be fine just checking that the `MultiKueueClusterActive` condition is set.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-22T16:27:03Z

- **test/e2e/multikueue/e2e_test.go:392** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-22T16:27:03Z
```diff
@@ -364,13 +382,24 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 							Reason: "ClientConnectionFailed",
 						},
 						util.IgnoreConditionTimestampsAndObservedGeneration, util.IgnoreConditionMessage)))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Reconnecting worker1 container to the kind network", func() {
-				cmd := exec.Command("docker", "network", "connect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sWorker1Client, worker1Cq2)).ToNot(gomega.HaveOccurred())
```

  It usually works. But you right, I will add  an "Eventually" to be sure.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-22T17:00:28Z

- **test/e2e/multikueue/e2e_test.go:396** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-22T17:00:28Z
```diff
@@ -364,13 +382,24 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 							Reason: "ClientConnectionFailed",
 						},
 						util.IgnoreConditionTimestampsAndObservedGeneration, util.IgnoreConditionMessage)))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Reconnecting worker1 container to the kind network", func() {
-				cmd := exec.Command("docker", "network", "connect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sWorker1Client, worker1Cq2)).ToNot(gomega.HaveOccurred())
+
+				// After reconnecting the container to the network, when we try to get pods,
```

  I tried to recreate the client and unfortunately that didn't help. From our side, the problem is not in the cache. I think the problem is that the pods cannot save the actual status to the database due to a network issue.
  
  When we disconnect from such a network, the modules cannot connect to the control-plane, crashes and restarts every time. This is not only kueue-control-manager problem, but also modules from the kube-system namespace. After connecting and trying to get pods, we get it with previous values (like before disconnection). And we cannot check the actual state of the modules to wait and be sure that our cluster is running successfully.
  
  This is not a required option for the current test. The Multiqueue cluster becomes active because it is watching the current state of the JobSetList and of course we can connect to it and get that information. But if we don't add it, our AfterEach logic (where we have the deletion processes) will fail because of the timeout reason we already have in the previous commits.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-22T18:09:56Z

/hold
/release-note-none

- **test/e2e/multikueue/e2e_test.go:392** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-22T18:09:40Z
```diff
@@ -364,13 +382,24 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 							Reason: "ClientConnectionFailed",
 						},
 						util.IgnoreConditionTimestampsAndObservedGeneration, util.IgnoreConditionMessage)))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
 			ginkgo.By("Reconnecting worker1 container to the kind network", func() {
-				cmd := exec.Command("docker", "network", "connect", "kind", "kind-worker1-control-plane")
+				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
+				gomega.Expect(util.DeleteClusterQueue(ctx, k8sWorker1Client, worker1Cq2)).ToNot(gomega.HaveOccurred())
```

  The remaining task is only here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-23T06:15:05Z

/lgtm
/approve
/hold cancel
