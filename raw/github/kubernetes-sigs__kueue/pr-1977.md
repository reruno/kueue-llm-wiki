# PR #1977: [metrics] Add quota_reserved_wait_time_seconds

**Summary**: [metrics] Add quota_reserved_wait_time_seconds

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1977

**Last updated**: 2024-04-24T17:28:07Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/admission_wait_time_seconds` → `main`
- **Created**: 2024-04-12T10:45:59Z
- **Updated**: 2024-04-24T17:28:07Z
- **Merged**: 2024-04-23T06:48:56Z
- **Closed**: 2024-04-23T06:48:56Z
- **Labels**: `kind/bug`, `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet), [@trasc](https://github.com/trasc)
- **Changed files**: 14   **+296 / -34**

## Description

#### What type of PR is this?
/kind bug
/kind feature

#### What this PR does / why we need it:
Fixed metrics admitted_workloads_total, admission_wait_time_seconds.
Added metrics quota_reserved_workloads_total, quota_reserved_wait_time_seconds.

#### Which issue(s) this PR fixes:
Fixes #1961 

#### Does this PR introduce a user-facing change?
```release-note
Improve metrics related to workload's quota reservation and admission:
- fix admission_wait_time_seconds - to measure the time to "Admitted" condition since creation time or last requeue (as opposed to the "QuotaReserved" condition as before)
- add quota_reserved_wait_time_seconds - measures time to "QuotaReserved" condition since creation time, or last eviction time
- add quota_reserved_workloads_total - counts the number of workloads that got admitted
- admission_checks_wait_time_seconds - measures the time to admit a workload with admission checks since quota reservation
- use longer buckets (up to 10240s) for histogram metrics: admission_wait_time_seconds, quota_reserved_wait_time_seconds, admission_checks_wait_time_seconds
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-12T10:46:08Z

Hi @mbobrovskyi. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-12T10:46:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a33f33e04201190e8b359e20ba2bb1dddced92b8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66265ea2242094000847962a |

### Comment by [@trasc](https://github.com/trasc) — 2024-04-12T15:31:32Z

/assign
/ok-to-test
/test all

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-12T15:37:25Z

@trasc: /release-note-edit must be used with a release note block.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1977#issuecomment-2051996697):

>/release-note-edit 
>```
>Improve metrics relating to workload's quota reservation and admission
>```


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-12T15:38:50Z

@trasc: /release-note-edit must be used with a single release note block.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1977#issuecomment-2051998917):

>/release-note-edit 
>
>```release-note
>Improve metrics relating to workload's quota reservation and admission
>```


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-12T16:43:07Z

@trasc: /release-note-edit must be used with a release note block.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1977#issuecomment-2052106369):

>/release-note-edit
>
>```
>Improve metrics relating to workload's quota reservation and admission
>```
>
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@trasc](https://github.com/trasc) — 2024-04-15T14:22:01Z

/lgtm
/assign @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-15T14:22:06Z

LGTM label has been added.  <details>Git tree hash: f8afee24b21124b8fda3851ab821281f02d50e92</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-22T07:19:19Z

LGTM label has been added.  <details>Git tree hash: c9c1f4ba99a1d94dc4b267733da06e284f80195a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-22T15:26:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1977#pullrequestreview-2015032819" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1977#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-23T06:29:18Z

/lgtm 
Let's address the remaining comments ([test](https://github.com/kubernetes-sigs/kueue/pull/1977#discussion_r1574811084) and [transitions](https://github.com/kubernetes-sigs/kueue/pull/1977#discussion_r1574949456)) in follow up(s).

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-23T06:29:23Z

LGTM label has been added.  <details>Git tree hash: d813f5fde2705e8a3177eeb00b5269ec4a993645</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-23T06:42:24Z

> /lgtm Let's address the remaining comments ([test](https://github.com/kubernetes-sigs/kueue/pull/1977#discussion_r1574811084) and [transitions](https://github.com/kubernetes-sigs/kueue/pull/1977#discussion_r1574949456)) in follow up(s).

I opened the issue to track the remaining work for transitioning: https://github.com/kubernetes-sigs/kueue/issues/2038.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-23T07:11:33Z

/release-note-edit
```release-note
Improve metrics related to workload's quota reservation and admission:
- fix admission_wait_time_seconds - to measure the time to "Admitted" condition since creation time or last requeue (as opposed to the "QuotaReserved" condition as before)
- add quota_reserved_wait_time_seconds - measures time to "QuotaReserved" condition since creation time, or last eviction time
- add quota_reserved_workloads_total - counts the number of workloads that got admitted
- admission_checks_wait_time_seconds - measures the time to admit a workload with admission checks since quota reservation
- use longer buckets (up to 10240s) for histogram metrics: admission_wait_time_seconds, quota_reserved_wait_time_seconds, admission_checks_wait_time_seconds
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-24T08:12:04Z

@mimowo As I can see this PR, the semantics of the existing metric (`admitted_workloads_total ` and `admission_wait_time_seconds `) are reworked with breaking. So, shouldn't add the `ACTION REQUIRED` in the release note?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-24T08:24:33Z

> @mimowo As I can see this PR, the semantics of the existing metric (`admitted_workloads_total ` and `admission_wait_time_seconds `) are reworked with breaking. So, shouldn't add the `ACTION REQUIRED` in the release note?

I don't think `admitted_workloads_total` changed, we just added `quota_reserved_workloads_total`.
Regarding `admission_wait_time_seconds` I would say it was fixed for workloads with admission checks. 
It does not seem to me user action is needed.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-24T17:28:06Z

> I don't think admitted_workloads_total changed, we just added quota_reserved_workloads_total.

Oh, I see. Thank you for the clarifications. I was thinking that the metric had been changed.

> Regarding admission_wait_time_seconds I would say it was fixed for workloads with admission checks.
It does not seem to me user action is needed.

That makes sense.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-15T17:25:35Z

Overall lgtm, couple of nits. I would also like to conclude on the new name for the new metric.

- **test/integration/scheduler/podsready/scheduler_test.go:257** by [@mimowo](https://github.com/mimowo) — 2024-04-15T17:02:05Z
```diff
@@ -254,6 +254,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
 			ginkgo.By("checking the 'prod' workload is admitted")
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, prodWl)
+			util.ExpectQuotaReservedWorkloadsTotalMetric(prodClusterQ, 1)
```

  It seems that in all cases the metric for reserved equals the metric for admitted. Please add an assert to a test where they differ, a good place might be `test/integration/controller/admissionchecks/provisioning/provisioning_test.go`.

- **pkg/scheduler/scheduler.go:545** by [@mimowo](https://github.com/mimowo) — 2024-04-15T17:02:33Z
```diff
@@ -534,11 +534,15 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 			}
 			waitTime := time.Since(waitStarted)
 			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "QuotaReserved", "Quota reserved in ClusterQueue %v, wait time since queued was %.0fs", admission.ClusterQueue, waitTime.Seconds())
+			metrics.QuotaReservedWorkload(admission.ClusterQueue, waitTime)
+
 			if workload.IsAdmitted(newWorkload) {
 				s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time since reservation was 0s ", admission.ClusterQueue)
+				metrics.AdmittedWorkload(admission.ClusterQueue, 0)
 			}
-			metrics.AdmittedWorkload(admission.ClusterQueue, waitTime)
+
 			log.V(2).Info("Workload successfully admitted and assigned flavors", "assignments", admission.PodSetAssignments)
+
```

  nit: revert new empty line (here and above)

- **pkg/metrics/metrics.go:132** by [@mimowo](https://github.com/mimowo) — 2024-04-15T17:07:11Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  This is a slightly different approach than in the Issue description, but I'm ok with either.
  
  However, with the change of semantics I suggest a rename, maybe: `quota_reserved_to_admission_wait_time_seconds`
  
  Another approach would be to call it `admission_checks_wait_time_seconds`, but then do not count 0 if there are no admission checks.
  
  I'm slightly leaning towards `quota_reserved_to_admission_wait_time_seconds`. WDYT @alculquicondor @tenzen-y ?

- **site/content/en/docs/reference/metrics.md:25** by [@mimowo](https://github.com/mimowo) — 2024-04-15T17:13:44Z
```diff
@@ -22,13 +22,15 @@ Use the following metrics to monitor the health of the kueue controllers:
 
 Use the following metrics to monitor the status of your ClusterQueues:
 
-| Metric name | Type | Description | Labels |
-| ----------- | ---- | ----------- | ------ |
-| `kueue_pending_workloads` | Gauge | The number of pending workloads. | `cluster_queue`: the name of the ClusterQueue<br> `status`: possible values are `active` or `inadmissible` |
-| `kueue_admitted_workloads_total` | Counter | The total number of admitted workloads. | `cluster_queue`: the name of the ClusterQueue |
-| `kueue_admission_wait_time_seconds` | Histogram | The time between a Workload was created until it was admitted. | `cluster_queue`: the name of the ClusterQueue |
+| Metric name | Type | Description                                                                    | Labels |
```

  Could you revert the formatting changes which don't result in the visual display of the table?
  It is not immediate if these are just formatting or content changes.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-16T14:48:04Z

- **test/integration/scheduler/podsready/scheduler_test.go:257** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-16T14:48:03Z
```diff
@@ -254,6 +254,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
 			ginkgo.By("checking the 'prod' workload is admitted")
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, prodWl)
+			util.ExpectQuotaReservedWorkloadsTotalMetric(prodClusterQ, 1)
```

  Added assert on test/integration/scheduler/scheduler_test.go.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-16T14:50:31Z

- **pkg/metrics/metrics.go:132** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-16T14:50:31Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  Can I just rename metric to quota_reserved_to_admission_wait_time_seconds? Or should I need to change logic?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-16T14:52:00Z

- **pkg/metrics/metrics.go:132** by [@mimowo](https://github.com/mimowo) — 2024-04-16T14:52:00Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  `quota_reserved_to_admission_wait_time_seconds` sgtm. @alculquicondor any view?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T15:14:43Z

- **pkg/metrics/metrics.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T15:14:43Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  sgtm. I would also not count a 0 if there are no admission checks

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-16T15:21:00Z

- **test/util/util.go:457** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T15:14:29Z
```diff
@@ -448,6 +448,15 @@ func ExpectAdmittedWorkloadsTotalMetric(cq *kueue.ClusterQueue, v int) {
 	}, Timeout, Interval).Should(gomega.Equal(v))
 }
 
+func ExpectQuotaReservedWorkloadsTotalMetric(cq *kueue.ClusterQueue, v int) {
+	metric := metrics.QuotaReservedWorkloadsTotal.WithLabelValues(cq.Name)
+	gomega.EventuallyWithOffset(1, func() int {
+		v, err := testutil.GetCounterMetricValue(metric)
+		gomega.Expect(err).ToNot(gomega.HaveOccurred())
+		return int(v)
+	}, Timeout, Interval).Should(gomega.Equal(v))
```

  ```suggestion
  	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
  		count, err := testutil.GetCounterMetricValue(metric)
  		g.Expect(err).ToNot(gomega.HaveOccurred())
  		g.Expect(int(count)).Should(gomega.Equal(v))
  	}, Timeout, Interval).Should(gomega.Succeeded())
  ```
  
  Improving debuggability would be better.

- **pkg/metrics/metrics.go:132** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-16T15:19:54Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  SGTM
  
  > I would also not count a 0 if there are no admission checks
  
  This is a good call. I agree with this.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T15:26:34Z

- **pkg/metrics/metrics.go:120** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T15:26:34Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
```

  Let's also add some buckets to both histograms.
  
  The [DefBuckets](https://pkg.go.dev/github.com/prometheus/client_golang/prometheus#pkg-variables) only go up to 10s, which is definitely not enough in a queueing system.
  
  Also, we don't really have resolution below 1s, so maybe we can use something like:
  
  ```
  []float64{1, 2.5, 5, 10, 20, 40, 80, 160, 320, 640, 1280, 2560, 5120, 10240}
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-16T15:31:57Z

- **pkg/metrics/metrics.go:120** by [@mimowo](https://github.com/mimowo) — 2024-04-16T15:31:57Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
```

  Great idea, I saw the cap by 10s already when looking into the metrics for the new scalability tests.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-17T07:52:58Z

- **pkg/metrics/metrics.go:132** by [@trasc](https://github.com/trasc) — 2024-04-17T07:52:58Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  Then , maybe the name of the metric should be something like `admission_check_wait_time_seconds ` or keep two histograms. The transition from reserving to admitted happens with or without admissioncheckes .
  
  In my opinion the way they are now, the counter and the number of recorded histogram records should be the same always.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T08:17:38Z

- **pkg/metrics/metrics.go:132** by [@mimowo](https://github.com/mimowo) — 2024-04-17T08:17:38Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  Even if the number of records in histogram for `quota_reserved_to_admission_wait_time_seconds` and `quota_reserved_wait_time_seconds` is the same we still cannot answer the question: what is the distribution of `admission_wait_time_seconds`, because it is not clear how to add two histograms. 
  
  Having a second thought on this, I would support to return to the issue description: `admission_wait_time_seconds`, but measuring the total time to admission (`quota_reserved` + `admission checks`, per workload).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T09:01:50Z

- **pkg/metrics/metrics.go:132** by [@mimowo](https://github.com/mimowo) — 2024-04-17T09:01:50Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  To follow up on the idea of `admission_check_wait_time_seconds` I opened: https://github.com/kubernetes-sigs/kueue/issues/1994.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-17T15:08:08Z

- **pkg/metrics/metrics.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T15:08:08Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  Can you summarize the full plan including all the involved metrics?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T16:26:57Z

- **test/integration/scheduler/podsready/scheduler_test.go:257** by [@mimowo](https://github.com/mimowo) — 2024-04-17T16:26:57Z
```diff
@@ -254,6 +254,7 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			gomega.Expect(k8sClient.Create(ctx, prodWl)).Should(gomega.Succeed())
 			ginkgo.By("checking the 'prod' workload is admitted")
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, prodClusterQ.Name, prodWl)
+			util.ExpectQuotaReservedWorkloadsTotalMetric(prodClusterQ, 1)
```

  thanks

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T16:40:56Z

- **pkg/metrics/metrics.go:132** by [@mimowo](https://github.com/mimowo) — 2024-04-17T16:40:56Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  1. fix `admissionWaitTime` (`admission_wait_time_seconds`) - measures time to "Admitted" condition since creation time, or last eviction time
  2. add `quotaReservedWaitTime (`quota_reserved_wait_time_seconds`) - measures time to "QuotaReserved" condition since creation time, or last eviction time
  3. keep `quotaReservedWorkloadsTotal` (`quota_reserved_workloads_total`) as implemented currently
  4. update the buckets as suggested in the other comment
  5. add `quotaReservedToAdmissionWaitTime`  (`quota_reserved_to_admission_wait_time_seconds`) - measures time from `QuotaReserved` to `Admitted` condition, 0 counts

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T16:41:45Z

- **site/content/en/docs/reference/metrics.md:25** by [@mimowo](https://github.com/mimowo) — 2024-04-17T16:41:45Z
```diff
@@ -22,13 +22,15 @@ Use the following metrics to monitor the health of the kueue controllers:
 
 Use the following metrics to monitor the status of your ClusterQueues:
 
-| Metric name | Type | Description | Labels |
-| ----------- | ---- | ----------- | ------ |
-| `kueue_pending_workloads` | Gauge | The number of pending workloads. | `cluster_queue`: the name of the ClusterQueue<br> `status`: possible values are `active` or `inadmissible` |
-| `kueue_admitted_workloads_total` | Counter | The total number of admitted workloads. | `cluster_queue`: the name of the ClusterQueue |
-| `kueue_admission_wait_time_seconds` | Histogram | The time between a Workload was created until it was admitted. | `cluster_queue`: the name of the ClusterQueue |
+| Metric name | Type | Description                                                                    | Labels |
```

  this seems done, feel free to mark as resolved

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T16:42:12Z

- **pkg/scheduler/scheduler.go:545** by [@mimowo](https://github.com/mimowo) — 2024-04-17T16:42:12Z
```diff
@@ -534,11 +534,15 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, mustHaveChecks sets.Set
 			}
 			waitTime := time.Since(waitStarted)
 			s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "QuotaReserved", "Quota reserved in ClusterQueue %v, wait time since queued was %.0fs", admission.ClusterQueue, waitTime.Seconds())
+			metrics.QuotaReservedWorkload(admission.ClusterQueue, waitTime)
+
 			if workload.IsAdmitted(newWorkload) {
 				s.recorder.Eventf(newWorkload, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time since reservation was 0s ", admission.ClusterQueue)
+				metrics.AdmittedWorkload(admission.ClusterQueue, 0)
 			}
-			metrics.AdmittedWorkload(admission.ClusterQueue, waitTime)
+
 			log.V(2).Info("Workload successfully admitted and assigned flavors", "assignments", admission.PodSetAssignments)
+
```

  feel free to mark as reasolved

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-17T17:58:38Z

- **pkg/metrics/metrics.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T17:58:37Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  I agree with everything, except counting cases with no admission checks as zero in point 5.
  
  Someone that wants to actually monitor `quota_reserved_to_admission_wait_time_seconds` doesn't care about queues that don't have admission checks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T18:10:09Z

- **pkg/metrics/metrics.go:132** by [@mimowo](https://github.com/mimowo) — 2024-04-17T18:10:09Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  I see, but the name of the metric is sort of agnostic about the presence of admission checks. 
  
  What would you say about calling it `admission_checks_wait_time_seconds`, and only counting when there are admission checks?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-17T18:51:58Z

- **pkg/metrics/metrics.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T18:51:58Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  SGTM

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T09:13:40Z

- **pkg/metrics/metrics.go:132** by [@trasc](https://github.com/trasc) — 2024-04-18T09:13:40Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  > 1. fix `admissionWaitTime` (`admission_wait_time_seconds`) - measures time to "Admitted" condition since creation time, or last eviction time
  
  This is problematic the sense that when a quota reservation is done the eviction condition is altered and we are not able to recover that last evicted time, this is the reason why the event recording is done with the time from reservation to admission.
  
  https://github.com/kubernetes-sigs/kueue/blob/f1f0223fd9079f371bcfee143b4221821923d204/pkg/workload/workload.go#L392-L418

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T09:38:20Z

- **pkg/metrics/metrics.go:132** by [@mimowo](https://github.com/mimowo) — 2024-04-18T09:38:20Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  Good point. So to implement this I see two ways:
  1. have an extra condition like `Queued` (or `Pending` would be another good name) with `LastTransitionTime` pointing to creation or last eviction, which is not updated on quota reservation, but on eviction
  2. extra field in workload status to know how long quota reservation took, like `timeToReserveQuotaMs`
  
  I would be leaning towards (1.) as more flexible and readable from user's perspective. WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-18T13:54:49Z

- **pkg/metrics/metrics.go:132** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T13:54:48Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  I would name it `Requeued`, as this condition wouldn't show up on the first queueing attempt. We can add it alongside when setting QuotaReserved=False

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T14:04:46Z

- **pkg/metrics/metrics.go:132** by [@mimowo](https://github.com/mimowo) — 2024-04-18T14:04:46Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  I like it

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-18T14:13:30Z

- **pkg/metrics/metrics.go:132** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-18T14:13:30Z
```diff
@@ -103,7 +119,7 @@ The label 'result' can have the following values:
 		prometheus.HistogramOpts{
 			Subsystem: constants.KueueName,
 			Name:      "admission_wait_time_seconds",
-			Help:      "The time between a Workload was created until it was admitted, per 'cluster_queue'",
+			Help:      "The time from when a workload got the quota reservation until admission, per 'cluster_queue'",
```

  > I would name it Requeued, as this condition wouldn't show up on the first queueing attempt. We can add it alongside when setting QuotaReserved=False
  
  SGTM

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-22T07:19:15Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-22T08:04:50Z

Almost lgtm

- **pkg/controller/core/workload_controller.go:185** by [@mimowo](https://github.com/mimowo) — 2024-04-22T07:51:14Z
```diff
@@ -177,8 +178,16 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, err
 		}
 		if workload.IsAdmitted(&wl) {
-			c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved)
-			r.recorder.Eventf(&wl, corev1.EventTypeNormal, "Admitted", "Admitted by ClusterQueue %v, wait time since reservation was %.0fs", wl.Status.Admission.ClusterQueue, time.Since(c.LastTransitionTime.Time).Seconds())
+			queuedTime := wl.CreationTimestamp.Time
+			if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); c != nil {
+				queuedTime = c.LastTransitionTime.Time
+			}
+			queuedWaitTime := time.Since(queuedTime)
```

  nit: extract this as a helper in workload.go to commonize the analogous code with scheduler.go. Maybe `QueuedWaitTime(workload)`

- **pkg/workload/workload.go:379** by [@mimowo](https://github.com/mimowo) — 2024-04-22T07:52:00Z
```diff
@@ -361,6 +371,19 @@ func UnsetQuotaReservationWithCondition(wl *kueue.Workload, reason, message stri
 	return changed
 }
 
+// SetRequeuedCondition sets the WorkloadRequeued condition to true
+func SetRequeuedCondition(wl *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadRequeued,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
```

  nit: this is set inside `apimeta.SetStatusCondition(`

- **apis/kueue/v1beta1/workload_types.go:308** by [@mimowo](https://github.com/mimowo) — 2024-04-22T07:53:25Z
```diff
@@ -302,6 +302,11 @@ const (
 	// more detailed information. The more detailed reasons should be prefixed
 	// by one of the "base" reasons.
 	WorkloadPreempted = "Preempted"
+
+	// WorkloadRequeued means that the Workload was requeued.
+	// The possible reasons for this condition are:
+	// - On setting QuotaReserved=False
```

  ```suggestion
  	// WorkloadRequeued means that the Workload was requeued due to eviction.
  ```
  Let's make the comment meaningful for the users who may observe the conditions.
  Kueue will set `QuotaReserved=False` for evicted workloads.

- **test/integration/scheduler/scheduler_test.go:423** by [@mimowo](https://github.com/mimowo) — 2024-04-22T08:00:30Z
```diff
@@ -378,6 +414,22 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			})
 		})
 
+		ginkgo.It("Should admit workloads with admission checks", func() {
+			wl1 := testing.MakeWorkload("admission-check-wl1", ns.Name).
+				Queue(admissionCheckQueue.Name).
+				Request(corev1.ResourceCPU, "2").
+				Obj()
+
+			ginkgo.By("checking the first workload gets created and admitted", func() {
```

  ```suggestion
  			ginkgo.By("checking the first workload gets created and gets quota reserved", func() {
  ```

- **apis/kueue/v1beta1/workload_types.go:306** by [@mimowo](https://github.com/mimowo) — 2024-04-22T08:03:42Z
```diff
@@ -302,6 +302,11 @@ const (
 	// more detailed information. The more detailed reasons should be prefixed
 	// by one of the "base" reasons.
 	WorkloadPreempted = "Preempted"
+
+	// WorkloadRequeued means that the Workload was requeued.
```

  Please make sure we have an integration test which checks that:
  - `Requeued=True` when `QuotaReserved=True` and `Admitted=False`
  - `Requeued=False` when the workload is evicted (IIUC this may still need implementation)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-22T12:52:29Z

- **apis/kueue/v1beta1/workload_types.go:306** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-22T12:52:29Z
```diff
@@ -302,6 +302,11 @@ const (
 	// more detailed information. The more detailed reasons should be prefixed
 	// by one of the "base" reasons.
 	WorkloadPreempted = "Preempted"
+
+	// WorkloadRequeued means that the Workload was requeued.
```

  > Requeued=False when the workload is evicted (IIUC this may still need implementation)
  
  Could you please explain this case? Is it possible to set Requeued to false?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-22T13:12:48Z

- **apis/kueue/v1beta1/workload_types.go:306** by [@mimowo](https://github.com/mimowo) — 2024-04-22T13:12:47Z
```diff
@@ -302,6 +302,11 @@ const (
 	// more detailed information. The more detailed reasons should be prefixed
 	// by one of the "base" reasons.
 	WorkloadPreempted = "Preempted"
+
+	// WorkloadRequeued means that the Workload was requeued.
```

  Sure, we could set it to `false`, and when we set `Admitted=True` condition. We do something similar for the other conditions when settting `QuotaReserved: true`: https://github.com/kubernetes-sigs/kueue/blob/7880819ae316be7bd9d906c75b7eeb15cd26f133/pkg/workload/workload.go#L405-L417

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-22T13:17:34Z

- **apis/kueue/v1beta1/workload_types.go:306** by [@mimowo](https://github.com/mimowo) — 2024-04-22T13:17:34Z
```diff
@@ -302,6 +302,11 @@ const (
 	// more detailed information. The more detailed reasons should be prefixed
 	// by one of the "base" reasons.
 	WorkloadPreempted = "Preempted"
+
+	// WorkloadRequeued means that the Workload was requeued.
```

  OTOH, setting it to `False` is not required to compute the metric, so maybe it does not need to be in this PR. WDYT @alculquicondor ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-22T14:00:13Z

- **apis/kueue/v1beta1/workload_types.go:306** by [@mimowo](https://github.com/mimowo) — 2024-04-22T14:00:13Z
```diff
@@ -302,6 +302,11 @@ const (
 	// more detailed information. The more detailed reasons should be prefixed
 	// by one of the "base" reasons.
 	WorkloadPreempted = "Preempted"
+
+	// WorkloadRequeued means that the Workload was requeued.
```

  As discussed offline, we don't want to change the condition to False on admission.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-22T14:01:20Z

- **apis/kueue/v1beta1/workload_types.go:306** by [@mimowo](https://github.com/mimowo) — 2024-04-22T14:01:20Z
```diff
@@ -302,6 +302,11 @@ const (
 	// more detailed information. The more detailed reasons should be prefixed
 	// by one of the "base" reasons.
 	WorkloadPreempted = "Preempted"
+
+	// WorkloadRequeued means that the Workload was requeued.
```

  Could you just add an assert the it remains True when QuotaReserved=True, and Admitted=True?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-22T15:26:06Z

/approve
Leaving lgtm to @mimowo

- **pkg/controller/jobs/job/job_controller_test.go:638** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-22T15:01:45Z
```diff
@@ -633,6 +633,12 @@ func TestReconciler(t *testing.T) {
 						Reason:  "Pending",
 						Message: "The workload is deactivated",
 					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
```

  This is not correct. If the workload is deactivated, Requeued should be False.

- **apis/kueue/v1beta1/workload_types.go:306** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-22T15:22:54Z
```diff
@@ -302,6 +302,11 @@ const (
 	// more detailed information. The more detailed reasons should be prefixed
 	// by one of the "base" reasons.
 	WorkloadPreempted = "Preempted"
+
+	// WorkloadRequeued means that the Workload was requeued.
```

  The condition should transition to `False` in the following scenarios:
  - When workload.spec.active=false. It should return to `True` when it's reactivated.
  - When evicted due to WaitForPodsReady, the workload will temporarily be in backoff. During this time, `Requeued` should be false.
  
  Although I think we can leave these for a follow up.
