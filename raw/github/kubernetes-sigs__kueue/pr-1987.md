# PR #1987: [test] Scalability runner metrics

**Summary**: [test] Scalability runner metrics

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1987

**Last updated**: 2024-04-19T15:24:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `scalability-runner-metrics` → `main`
- **Created**: 2024-04-16T11:12:08Z
- **Updated**: 2024-04-19T15:24:02Z
- **Merged**: 2024-04-19T14:42:29Z
- **Closed**: 2024-04-19T14:42:29Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo)
- **Changed files**: 6   **+451 / -17**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Adds scalability runner measured metrics, a set of measurements results extracted from the API events monitored by the runner which can be easily used to detect regressions in kueue's scheduling.

Currently a summary is generated like:

```yaml
clusterQueueClasses:
  cq:
    cpuAverageUsage: 358300
    cpuUsed: 123130721000
    firstEventTime: "2024-04-17T13:07:53.831208896+03:00"
    lastEventTime: "2024-04-17T13:13:37.48321136+03:00"
    nominalQuota: 600000
workloadClasses:
  large:
    averageTimeToAdmissionMs: 8288
    averageTimeToFinishMs: 9292
    count: 1500
    totalEvictions: 0
  medium:
    averageTimeToAdmissionMs: 77447
    averageTimeToFinishMs: 77951
    count: 3000
    totalEvictions: 1
  small:
    averageTimeToAdmissionMs: 214757
    averageTimeToFinishMs: 214962
    count: 10500
    totalEvictions: 8
```

The checker will  verify

```yaml
clusterQueueClassesMinUsage:
  cq: 10
# the value needs to be adapted based on a set of CI run results 
# computed as summary.clusterQueueClasses[<cq-class>].cpuAverageUsage * 100 / summary.clusterQueueClasses[<cq-class>].nominalQuota

wlClassesMaxAvgTimeToAdmissionMs:
  large: 3600_000 
  medium: 3600_000
  small: 3600_000 
# the value needs to be adapted based on a set of CI run results 
# compared against summary.workloadClasses[<wl-class>].averageTimeToAdmissionMs
```

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #1912

#### Special notes for your reviewer:

Please let me know if you can think of additional measurements we can do and worth monitoring in CI.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-16T11:12:24Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | de380c45ed6df94f3dabcfea034cd67458247892 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66210966cc6c250008f472a2 |

### Comment by [@trasc](https://github.com/trasc) — 2024-04-16T11:12:36Z

/hold 
for #1931

### Comment by [@trasc](https://github.com/trasc) — 2024-04-18T08:40:09Z

/unhold
/cc @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-18T10:53:40Z

LGTM label has been added.  <details>Git tree hash: 72b256c1b9480040161741eac8dd0508dcdaa023</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-18T12:00:03Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-18T12:00:08Z

LGTM label has been added.  <details>Git tree hash: 4c31362d0d4125016a9fdaf3321c49eccc2c951a</details>

### Comment by [@trasc](https://github.com/trasc) — 2024-04-19T12:20:33Z

> Can you add to the README how I would create a new test case?

Depending on what you want to do you'll just need to change [default_generator_config.yaml](https://github.com/kubernetes-sigs/kueue/blob/main/test/scalability/default_generator_config.yaml)  and probably just run `make run-scalability` since the range spec may not fit. 

Both the runner and checker support working with different files but I did not do that in the Makefile yet, and we are able to define multiple "profiles", however since this pr is not that close to this kind of change  let's do the doc and potentially Makefile update  in a follow up.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T14:22:53Z

Please do

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-19T14:23:00Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1987#issuecomment-2066694634" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1987#pullrequestreview-2008515272" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1987#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T09:17:34Z

I'm ok to move forward with this PR, it does not look like that much code after rebase, and might be helpful in some situations (like knowing average times to admit per workload size).

- **test/scalability/runner/recorder/recorder.go:115** by [@mimowo](https://github.com/mimowo) — 2024-04-18T09:05:01Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
+	GetEventInfo() string
+}
+
 type CQStatus struct {
-	Name               string
+}
+
+type CQEvent struct {
+	Time      time.Time
+	Name      string
+	ClassName string
+	Cohort    string
+	UID       types.UID
+
+	CPUReservation     int64
+	CPUUsage           int64
+	CPUQuota           int64
 	PendingWorkloads   int32
 	ReservingWorkloads int32
+	AdmittedWorkloads  int32
 	Active             bool
 }
 
-type Store map[string]CQStatus
+func (e *CQEvent) GetEventInfo() string {
+	return ""
+}
+
+type CQState struct {
+	FirstEventTime  time.Time
+	FirstActiveTime time.Time
+	CPUUsed         int64
+	CPUMaxUsage     int64
+	LastEvent       *CQEvent
+}
+
+var CQStateCsvHeader = []string{
+	"name",
+	"cohort",
+	"class name",
+	"CPU quota (mCPU)",
+	"CPU used (mCPU * ms)",
+	"CPU max usage (mCPU)",
+	"monitor time (ms)",
+}
+
+func (cqs *CQState) CsvRecord() []string {
+	monitoringTimeMs := cqs.LastEvent.Time.Sub(cqs.FirstEventTime).Milliseconds()
+	return []string{
+		cqs.LastEvent.Name,
+		cqs.LastEvent.Cohort,
+		cqs.LastEvent.ClassName,
+		strconv.FormatInt(cqs.LastEvent.CPUQuota, 10),
+		strconv.FormatInt(cqs.CPUUsed, 10),
+		strconv.FormatInt(cqs.CPUMaxUsage, 10),
+		strconv.FormatInt(monitoringTimeMs, 10),
+	}
+}
+
+type CQStore map[string]*CQState
+
+type WLEvent struct {
+	Time time.Time
+	types.NamespacedName
+	UID       types.UID
+	ClassName string
+	Admitted  bool
+	Evicted   bool
+	Finished  bool
+}
+
+func (e *WLEvent) GetEventInfo() string {
+	return ""
+}
+
+type WLState struct {
+	Id int
+	types.NamespacedName
+	ClassName      string
+	FirstEventTime time.Time
+	MSToAdmitted   int64
+	MSToFinished   int64
```

  ```suggestion
  	TimeToAdmitMs   int64
  	TimeToFinishMs   int64
  ```
  nit

- **test/scalability/runner/recorder/recorder.go:58** by [@mimowo](https://github.com/mimowo) — 2024-04-18T09:06:46Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
+	GetEventInfo() string
+}
+
 type CQStatus struct {
-	Name               string
+}
+
+type CQEvent struct {
+	Time      time.Time
+	Name      string
+	ClassName string
+	Cohort    string
+	UID       types.UID
+
+	CPUReservation     int64
+	CPUUsage           int64
+	CPUQuota           int64
 	PendingWorkloads   int32
 	ReservingWorkloads int32
+	AdmittedWorkloads  int32
 	Active             bool
 }
 
-type Store map[string]CQStatus
+func (e *CQEvent) GetEventInfo() string {
```

  nit: this seems unused?

- **test/scalability/runner/recorder/recorder.go:35** by [@mimowo](https://github.com/mimowo) — 2024-04-18T09:12:44Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  Is this needed?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T09:58:24Z

- **test/scalability/runner/recorder/recorder.go:35** by [@trasc](https://github.com/trasc) — 2024-04-18T09:58:24Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  just not to go `interface{}` on the events channel, same goes for it method.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T10:10:45Z

- **test/scalability/runner/recorder/recorder.go:115** by [@trasc](https://github.com/trasc) — 2024-04-18T10:10:45Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
+	GetEventInfo() string
+}
+
 type CQStatus struct {
-	Name               string
+}
+
+type CQEvent struct {
+	Time      time.Time
+	Name      string
+	ClassName string
+	Cohort    string
+	UID       types.UID
+
+	CPUReservation     int64
+	CPUUsage           int64
+	CPUQuota           int64
 	PendingWorkloads   int32
 	ReservingWorkloads int32
+	AdmittedWorkloads  int32
 	Active             bool
 }
 
-type Store map[string]CQStatus
+func (e *CQEvent) GetEventInfo() string {
+	return ""
+}
+
+type CQState struct {
+	FirstEventTime  time.Time
+	FirstActiveTime time.Time
+	CPUUsed         int64
+	CPUMaxUsage     int64
+	LastEvent       *CQEvent
+}
+
+var CQStateCsvHeader = []string{
+	"name",
+	"cohort",
+	"class name",
+	"CPU quota (mCPU)",
+	"CPU used (mCPU * ms)",
+	"CPU max usage (mCPU)",
+	"monitor time (ms)",
+}
+
+func (cqs *CQState) CsvRecord() []string {
+	monitoringTimeMs := cqs.LastEvent.Time.Sub(cqs.FirstEventTime).Milliseconds()
+	return []string{
+		cqs.LastEvent.Name,
+		cqs.LastEvent.Cohort,
+		cqs.LastEvent.ClassName,
+		strconv.FormatInt(cqs.LastEvent.CPUQuota, 10),
+		strconv.FormatInt(cqs.CPUUsed, 10),
+		strconv.FormatInt(cqs.CPUMaxUsage, 10),
+		strconv.FormatInt(monitoringTimeMs, 10),
+	}
+}
+
+type CQStore map[string]*CQState
+
+type WLEvent struct {
+	Time time.Time
+	types.NamespacedName
+	UID       types.UID
+	ClassName string
+	Admitted  bool
+	Evicted   bool
+	Finished  bool
+}
+
+func (e *WLEvent) GetEventInfo() string {
+	return ""
+}
+
+type WLState struct {
+	Id int
+	types.NamespacedName
+	ClassName      string
+	FirstEventTime time.Time
+	MSToAdmitted   int64
+	MSToFinished   int64
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T10:11:27Z

- **test/scalability/runner/recorder/recorder.go:58** by [@trasc](https://github.com/trasc) — 2024-04-18T10:11:27Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
+	GetEventInfo() string
+}
+
 type CQStatus struct {
-	Name               string
+}
+
+type CQEvent struct {
+	Time      time.Time
+	Name      string
+	ClassName string
+	Cohort    string
+	UID       types.UID
+
+	CPUReservation     int64
+	CPUUsage           int64
+	CPUQuota           int64
 	PendingWorkloads   int32
 	ReservingWorkloads int32
+	AdmittedWorkloads  int32
 	Active             bool
 }
 
-type Store map[string]CQStatus
+func (e *CQEvent) GetEventInfo() string {
```

  It's just to fit the `event` interface

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T10:12:12Z

- **test/scalability/runner/recorder/recorder.go:39** by [@mimowo](https://github.com/mimowo) — 2024-04-18T10:12:11Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
+	GetEventInfo() string
+}
+
 type CQStatus struct {
```

  This `CQStatus` struct seems unused now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T10:13:48Z

- **test/scalability/runner/recorder/recorder.go:35** by [@mimowo](https://github.com/mimowo) — 2024-04-18T10:13:48Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  I see, I'm ok keeping the dedicated `event` interface, but the `GetEventInfo` function does not seem to be used. We can remove the declaration and implementations I suppose.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T10:15:08Z

- **test/scalability/runner/recorder/recorder.go:58** by [@mimowo](https://github.com/mimowo) — 2024-04-18T10:15:08Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
+	GetEventInfo() string
+}
+
 type CQStatus struct {
-	Name               string
+}
+
+type CQEvent struct {
+	Time      time.Time
+	Name      string
+	ClassName string
+	Cohort    string
+	UID       types.UID
+
+	CPUReservation     int64
+	CPUUsage           int64
+	CPUQuota           int64
 	PendingWorkloads   int32
 	ReservingWorkloads int32
+	AdmittedWorkloads  int32
 	Active             bool
 }
 
-type Store map[string]CQStatus
+func (e *CQEvent) GetEventInfo() string {
```

  I see, could we have just `type event interface{}` if we don't use the function I guess.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T10:40:33Z

- **test/scalability/runner/recorder/recorder.go:35** by [@trasc](https://github.com/trasc) — 2024-04-18T10:40:33Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  Making `event` an empty interface will defeat the purpose of having it (restrict what types qualify as an `event`), if you insist we can just use `interface {}`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T10:43:49Z

- **test/scalability/runner/recorder/recorder.go:39** by [@trasc](https://github.com/trasc) — 2024-04-18T10:43:49Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
+	GetEventInfo() string
+}
+
 type CQStatus struct {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T10:52:05Z

- **test/scalability/runner/recorder/recorder.go:35** by [@mimowo](https://github.com/mimowo) — 2024-04-18T10:52:05Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  Having a dedicated interface sgtm as a place for some functions. I prefer to make it empty, but I'm ok with the dummy function. I was just thinking if I'm not missing some uses.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T10:53:36Z

/lgtm
/approve 
/assign @alculquicondor

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T10:58:49Z

- **test/scalability/runner/recorder/recorder.go:35** by [@trasc](https://github.com/trasc) — 2024-04-18T10:58:49Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  It just occurred to me that "|" types within the interface might do it without the need of the dummy func.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T11:24:51Z

- **test/scalability/runner/recorder/recorder.go:35** by [@mimowo](https://github.com/mimowo) — 2024-04-18T11:24:51Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  how would it look like?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T11:30:05Z

- **test/scalability/runner/recorder/recorder.go:35** by [@trasc](https://github.com/trasc) — 2024-04-18T11:30:05Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  Unfortunately it's not working, we can go with different channels (for event type).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T11:34:25Z

- **test/scalability/runner/recorder/recorder.go:35** by [@mimowo](https://github.com/mimowo) — 2024-04-18T11:34:25Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  ok

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T11:53:19Z

- **test/scalability/runner/recorder/recorder.go:35** by [@trasc](https://github.com/trasc) — 2024-04-18T11:53:19Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-18T11:53:35Z

- **test/scalability/runner/recorder/recorder.go:58** by [@trasc](https://github.com/trasc) — 2024-04-18T11:53:35Z
```diff
@@ -18,27 +18,138 @@ package recorder
 
 import (
 	"context"
+	"encoding/csv"
+	"os"
+	"strconv"
 	"sync/atomic"
 	"time"
 
 	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/yaml"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/test/scalability/runner/generator"
 )
 
+type event interface {
+	GetEventInfo() string
+}
+
 type CQStatus struct {
-	Name               string
+}
+
+type CQEvent struct {
+	Time      time.Time
+	Name      string
+	ClassName string
+	Cohort    string
+	UID       types.UID
+
+	CPUReservation     int64
+	CPUUsage           int64
+	CPUQuota           int64
 	PendingWorkloads   int32
 	ReservingWorkloads int32
+	AdmittedWorkloads  int32
 	Active             bool
 }
 
-type Store map[string]CQStatus
+func (e *CQEvent) GetEventInfo() string {
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-18T18:07:30Z

Can you add to the README how I would create a new test case?
