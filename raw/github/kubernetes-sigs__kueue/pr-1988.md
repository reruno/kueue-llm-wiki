# PR #1988: Preemption algorithm described

**Summary**: Preemption algorithm described

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1988

**Last updated**: 2024-05-29T14:53:52Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `preemption-docs` → `main`
- **Created**: 2024-04-16T13:30:04Z
- **Updated**: 2024-05-29T14:53:52Z
- **Merged**: 2024-04-17T15:50:44Z
- **Closed**: 2024-04-17T15:50:44Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk), [@trasc](https://github.com/trasc)
- **Changed files**: 1   **+49 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1974

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-16T13:30:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4680715ffbaadcda8470f59f636a114297dc1bdc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/661fec1189da3c0008936198 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1988--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1988--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTE5ODgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.FgkwpUK5YcNm-MIDXpWnE-zzZhqAhN-s0v45EDfT0dY)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-16T13:34:33Z

/assign @alculquicondor @trasc

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-04-17T15:13:36Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-17T15:13:42Z

LGTM label has been added.  <details>Git tree hash: 07dfa6c34085f867939d862c45a65c3e16e81916</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-17T15:46:58Z

LGTM label has been added.  <details>Git tree hash: 6d238707b96d4f3f4c97f95faa9b2e04d30687cb</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-17T15:46:59Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1988#pullrequestreview-2006426454" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1988#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T15:47:25Z

I guess we can't cherry-pick to website until we release v0.6.3

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-16T14:22:23Z

Maybe we can also specify the fact the the preempting workload will stay in the head of the queue until the evicted workloads free the quota.

- **site/content/en/docs/concepts/cluster_queue.md:438** by [@trasc](https://github.com/trasc) — 2024-04-16T14:01:48Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
```

  `A workload is eligible for preemptions if it is eligible for preemptions` sounds strange.

- **site/content/en/docs/concepts/cluster_queue.md:434** by [@trasc](https://github.com/trasc) — 2024-04-16T14:03:55Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
```

  Maybe find a way to describe the workloads role in the process `preemptor` vs `preemptee`, `evictor workload` vs `evicted workload` or somethung similar.

- **site/content/en/docs/concepts/cluster_queue.md:445** by [@trasc](https://github.com/trasc) — 2024-04-16T14:14:17Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
+selected resource flavor
+(see [FlavorFungibility](/docs/concepts/cluster_queue/#flavorfungibility)).
+
+A workload is only eligible to do preemptions in a given resource flavor if it
+fits fully within the nominal quota of the flavor. Alternatively, if
+ `borrowWithinCohort` is enabled, then any workloads requesting within the
+ cohort limits are eligible for preemptions.
```

  ```suggestion
  A workload is eligible for preemptions if  its target ClusterQueue [FlavorFungibility](/docs/concepts/cluster_queue/#flavorfungibility) allows it and,  if it only fits fully within the nominal quota of a flavor but not in its free quoata, Or if
   `borrowWithinCohort` is enabled, then any workloads requesting within the
   cohort limits are eligible for preemptions.
  ```
  
  this for sure should not be the final version but it should be something like this,  maybe it helps to mention the relevant bits in the api ref /docs/reference/kueue.v1beta1/#kueue-x-k8s-io-v1beta1-ClusterQueuePreemption

- **site/content/en/docs/concepts/cluster_queue.md:457** by [@trasc](https://github.com/trasc) — 2024-04-16T14:16:20Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
+selected resource flavor
+(see [FlavorFungibility](/docs/concepts/cluster_queue/#flavorfungibility)).
+
+A workload is only eligible to do preemptions in a given resource flavor if it
+fits fully within the nominal quota of the flavor. Alternatively, if
+ `borrowWithinCohort` is enabled, then any workloads requesting within the
+ cohort limits are eligible for preemptions.
+
+#### Candidates
+
+The list of peremption candidates is compiled as the list of workloads within
+the cluster queue, satisfying the `withinClusterQueue` policy, and workloads
+within the cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- workloads from other cluster queues
+- workloads with the lowest priority
+- workloads which run the shortest
```

  ```suggestion
  - workloads which ran the shortest
  ```
  ? 
  
  but maybe got admitted last sounds better

- **site/content/en/docs/concepts/cluster_queue.md:476** by [@trasc](https://github.com/trasc) — 2024-04-16T14:20:11Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
+selected resource flavor
+(see [FlavorFungibility](/docs/concepts/cluster_queue/#flavorfungibility)).
+
+A workload is only eligible to do preemptions in a given resource flavor if it
+fits fully within the nominal quota of the flavor. Alternatively, if
+ `borrowWithinCohort` is enabled, then any workloads requesting within the
+ cohort limits are eligible for preemptions.
+
+#### Candidates
+
+The list of peremption candidates is compiled as the list of workloads within
+the cluster queue, satisfying the `withinClusterQueue` policy, and workloads
+within the cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- workloads from other cluster queues
+- workloads with the lowest priority
+- workloads which run the shortest
+
+#### Targets
+
+The algorithm to qualify candidates as targets does the following steps:
+
+1. If all candidates belong to the target queue, then Kueue gradually qualifies
+candidates as targets until the workload can fit. The end result utilization
+might be the above nominal.
+
+2. If `borrowWithinCohort` is enabled, then all candidates are considered in
+order, respecting the `borrowWithinCohort.maxPriorityThreshold` threshold, until
+the workload can fit. The end result utilization might be above the nominal
+quota.
+
+3. If existing workloads are under nominal, then all candidates are considered
+in order. However, the final utilization cannot exceed the nominal quota.
+
+4. Only a subset of candidates from the target queue is considered, but the end
+result utilization is allowed to be above nominal.
```

  I don't think I understand these two.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-16T14:54:49Z

- **site/content/en/docs/concepts/cluster_queue.md:457** by [@mimowo](https://github.com/mimowo) — 2024-04-16T14:54:49Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
+selected resource flavor
+(see [FlavorFungibility](/docs/concepts/cluster_queue/#flavorfungibility)).
+
+A workload is only eligible to do preemptions in a given resource flavor if it
+fits fully within the nominal quota of the flavor. Alternatively, if
+ `borrowWithinCohort` is enabled, then any workloads requesting within the
+ cohort limits are eligible for preemptions.
+
+#### Candidates
+
+The list of peremption candidates is compiled as the list of workloads within
+the cluster queue, satisfying the `withinClusterQueue` policy, and workloads
+within the cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- workloads from other cluster queues
+- workloads with the lowest priority
+- workloads which run the shortest
```

  Right, updated. PTAL

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-16T14:57:07Z

- **site/content/en/docs/concepts/cluster_queue.md:434** by [@mimowo](https://github.com/mimowo) — 2024-04-16T14:57:07Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
```

  I think `preemptor` and `victim` makes sense, as they used for preemption of pods. I'm going to update using this vocab.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-16T14:59:14Z

- **site/content/en/docs/concepts/cluster_queue.md:476** by [@mimowo](https://github.com/mimowo) — 2024-04-16T14:59:14Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
+selected resource flavor
+(see [FlavorFungibility](/docs/concepts/cluster_queue/#flavorfungibility)).
+
+A workload is only eligible to do preemptions in a given resource flavor if it
+fits fully within the nominal quota of the flavor. Alternatively, if
+ `borrowWithinCohort` is enabled, then any workloads requesting within the
+ cohort limits are eligible for preemptions.
+
+#### Candidates
+
+The list of peremption candidates is compiled as the list of workloads within
+the cluster queue, satisfying the `withinClusterQueue` policy, and workloads
+within the cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- workloads from other cluster queues
+- workloads with the lowest priority
+- workloads which run the shortest
+
+#### Targets
+
+The algorithm to qualify candidates as targets does the following steps:
+
+1. If all candidates belong to the target queue, then Kueue gradually qualifies
+candidates as targets until the workload can fit. The end result utilization
+might be the above nominal.
+
+2. If `borrowWithinCohort` is enabled, then all candidates are considered in
+order, respecting the `borrowWithinCohort.maxPriorityThreshold` threshold, until
+the workload can fit. The end result utilization might be above the nominal
+quota.
+
+3. If existing workloads are under nominal, then all candidates are considered
+in order. However, the final utilization cannot exceed the nominal quota.
+
+4. Only a subset of candidates from the target queue is considered, but the end
+result utilization is allowed to be above nominal.
```

  Thanks for the heads up, I'm going to think how to rephrase them. In the meanwhile, they are descriptions of this steps in code: https://github.com/kubernetes-sigs/kueue/blob/ebecc565094f0555903a037502c05e99c081d470/pkg/scheduler/preemption/preemption.go#L122-L130.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T15:12:47Z

- **site/content/en/docs/concepts/cluster_queue.md:430** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T14:46:34Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
```

  This list should be somewhere under "preemption algorithm"

- **site/content/en/docs/concepts/cluster_queue.md:438** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T14:51:44Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
```

  fungibility is not about eligibility... it's about what we prioritize (borrowing vs preemption).

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-16T15:47:01Z

- **site/content/en/docs/concepts/cluster_queue.md:434** by [@trasc](https://github.com/trasc) — 2024-04-16T15:47:01Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
```

  `victim` is way too dark, being evicted is not such a tragedy, and it will add yet another terminology in the context of preemption

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T16:10:08Z

- **site/content/en/docs/concepts/cluster_queue.md:434** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T16:10:08Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
```

  That word is not listed here https://inclusivenaming.org/word-lists/
  
  However, it definitely doesn't sound appropriate. You can use `preemptee` as long as you define it somewhere.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-16T16:10:49Z

- **site/content/en/docs/concepts/cluster_queue.md:434** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-16T16:10:49Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
```

  `target` might work better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-16T16:22:31Z

- **site/content/en/docs/concepts/cluster_queue.md:434** by [@mimowo](https://github.com/mimowo) — 2024-04-16T16:22:31Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
```

  Victim is used in the user-facing docs for pods, so was reviewed by tech writers I assume: https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/.
  
  However, if you feel it is not good in the context here, then I like target, which I actually use in the text currently.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T13:00:11Z

- **site/content/en/docs/concepts/cluster_queue.md:438** by [@mimowo](https://github.com/mimowo) — 2024-04-17T13:00:10Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
```

  Right, I have rephrased the description. PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T13:02:05Z

- **site/content/en/docs/concepts/cluster_queue.md:434** by [@mimowo](https://github.com/mimowo) — 2024-04-17T13:02:05Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
```

  I have rephrased the description using "incoming Workload" (as it was already used in other places in the "Preemption" section of the page). The end result I call "targets", the sorted interim list I call "candidates". PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T13:06:14Z

- **site/content/en/docs/concepts/cluster_queue.md:445** by [@mimowo](https://github.com/mimowo) — 2024-04-17T13:06:14Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
+selected resource flavor
+(see [FlavorFungibility](/docs/concepts/cluster_queue/#flavorfungibility)).
+
+A workload is only eligible to do preemptions in a given resource flavor if it
+fits fully within the nominal quota of the flavor. Alternatively, if
+ `borrowWithinCohort` is enabled, then any workloads requesting within the
+ cohort limits are eligible for preemptions.
```

  I have rephrased the section. PTAL. Regarding the reference to the API, we could, but the API is already described just above the new section.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T13:06:50Z

- **site/content/en/docs/concepts/cluster_queue.md:476** by [@mimowo](https://github.com/mimowo) — 2024-04-17T13:06:50Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+### Preemption Algorithm
+
+The preemption algorithm has the following stages.
+
+#### Eligibility
+
+A workload is eligible for preemptions if it is eligible for preemptions for the
+selected resource flavor
+(see [FlavorFungibility](/docs/concepts/cluster_queue/#flavorfungibility)).
+
+A workload is only eligible to do preemptions in a given resource flavor if it
+fits fully within the nominal quota of the flavor. Alternatively, if
+ `borrowWithinCohort` is enabled, then any workloads requesting within the
+ cohort limits are eligible for preemptions.
+
+#### Candidates
+
+The list of peremption candidates is compiled as the list of workloads within
+the cluster queue, satisfying the `withinClusterQueue` policy, and workloads
+within the cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- workloads from other cluster queues
+- workloads with the lowest priority
+- workloads which run the shortest
+
+#### Targets
+
+The algorithm to qualify candidates as targets does the following steps:
+
+1. If all candidates belong to the target queue, then Kueue gradually qualifies
+candidates as targets until the workload can fit. The end result utilization
+might be the above nominal.
+
+2. If `borrowWithinCohort` is enabled, then all candidates are considered in
+order, respecting the `borrowWithinCohort.maxPriorityThreshold` threshold, until
+the workload can fit. The end result utilization might be above the nominal
+quota.
+
+3. If existing workloads are under nominal, then all candidates are considered
+in order. However, the final utilization cannot exceed the nominal quota.
+
+4. Only a subset of candidates from the target queue is considered, but the end
+result utilization is allowed to be above nominal.
```

  I have rephrased them. PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T13:08:38Z

- **site/content/en/docs/concepts/cluster_queue.md:430** by [@mimowo](https://github.com/mimowo) — 2024-04-17T13:08:38Z
```diff
@@ -429,6 +429,58 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
```

  Moved it below. PTAL.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-17T13:47:04Z

- **site/content/en/docs/concepts/cluster_queue.md:454** by [@trasc](https://github.com/trasc) — 2024-04-17T13:47:03Z
```diff
@@ -422,13 +422,59 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
+for preemptions, within a flavor of the target queue, when one of the following
+is true:
+- the requests of the Workload are below the flavor's nominal quota, or
+- `borrowWithinCohort` is enabled.
+
+Kueue implements heuristics to preempt as few Workloads as possible, preferring
+Workloads with these characteristics:
 
 - Workloads belonging to ClusterQueues that are borrowing quota.
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+Below we present a more detailed description of the algorithm.
+
+#### Candidates
+
+The list of preemption candidates is compiled from Workloads within the Cluster
+Queue satisfying the `withinClusterQueue` policy, and Workloads within the
+cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- Workloads from other borrowing queues in the cohort,
+- Workloads with the lowest priority,
+- Workloads which got admitted the most recently.
```

  We should sync the list at L438 with this, and maybe louse one of them.
  
  Just removing L438-L441 can be the best.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T13:54:57Z

- **site/content/en/docs/concepts/cluster_queue.md:454** by [@mimowo](https://github.com/mimowo) — 2024-04-17T13:54:57Z
```diff
@@ -422,13 +422,59 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
+for preemptions, within a flavor of the target queue, when one of the following
+is true:
+- the requests of the Workload are below the flavor's nominal quota, or
+- `borrowWithinCohort` is enabled.
+
+Kueue implements heuristics to preempt as few Workloads as possible, preferring
+Workloads with these characteristics:
 
 - Workloads belonging to ClusterQueues that are borrowing quota.
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+Below we present a more detailed description of the algorithm.
+
+#### Candidates
+
+The list of preemption candidates is compiled from Workloads within the Cluster
+Queue satisfying the `withinClusterQueue` policy, and Workloads within the
+cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- Workloads from other borrowing queues in the cohort,
+- Workloads with the lowest priority,
+- Workloads which got admitted the most recently.
```

  Indeed, let me tentatively drop  L438-L441, and moved up as intro for the "Preemption Algorithm overview" section.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-17T15:25:25Z

Almost there

- **site/content/en/docs/concepts/cluster_queue.md:432** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T15:15:57Z
```diff
@@ -422,12 +422,52 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
 
-- Workloads belonging to ClusterQueues that are borrowing quota.
-- Workloads with the lowest priority.
-- Workloads that have been admitted more recently.
+Kueue implements heuristics to preempt as few Workloads as possible.
+Below we present a more detailed description of the algorithm.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
```

  ```suggestion
  An incoming Workload, which does not fit within the unused quota, is eligible
  ```

- **site/content/en/docs/concepts/cluster_queue.md:433** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T15:16:12Z
```diff
@@ -422,12 +422,52 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
 
-- Workloads belonging to ClusterQueues that are borrowing quota.
-- Workloads with the lowest priority.
-- Workloads that have been admitted more recently.
+Kueue implements heuristics to preempt as few Workloads as possible.
+Below we present a more detailed description of the algorithm.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
+for preemptions, within a flavor of the target queue, when one of the following
```

  ```suggestion
  to issue preemptions when one of the following
  ```
  
  for simplicity

- **site/content/en/docs/concepts/cluster_queue.md:446** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T15:18:41Z
```diff
@@ -422,12 +422,52 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
 
-- Workloads belonging to ClusterQueues that are borrowing quota.
-- Workloads with the lowest priority.
-- Workloads that have been admitted more recently.
+Kueue implements heuristics to preempt as few Workloads as possible.
+Below we present a more detailed description of the algorithm.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
+for preemptions, within a flavor of the target queue, when one of the following
+is true:
+- the requests of the Workload are below the flavor's nominal quota, or
+- `borrowWithinCohort` is enabled.
+
+#### Candidates
+
+The list of preemption candidates is compiled from Workloads within the Cluster
+Queue satisfying the `withinClusterQueue` policy, and Workloads within the
+cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- Workloads from other borrowing queues in the cohort,
```

  ```suggestion
  - Workloads from borrowing queues in the cohort,
  ```

- **site/content/en/docs/concepts/cluster_queue.md:452** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T15:19:44Z
```diff
@@ -422,12 +422,52 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
 
-- Workloads belonging to ClusterQueues that are borrowing quota.
-- Workloads with the lowest priority.
-- Workloads that have been admitted more recently.
+Kueue implements heuristics to preempt as few Workloads as possible.
+Below we present a more detailed description of the algorithm.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
+for preemptions, within a flavor of the target queue, when one of the following
+is true:
+- the requests of the Workload are below the flavor's nominal quota, or
+- `borrowWithinCohort` is enabled.
+
+#### Candidates
+
+The list of preemption candidates is compiled from Workloads within the Cluster
+Queue satisfying the `withinClusterQueue` policy, and Workloads within the
+cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- Workloads from other borrowing queues in the cohort,
+- Workloads with the lowest priority,
+- Workloads which got admitted the most recently.
+
+#### Targets
+
+The algorithm qualifies the candidates as targets using the following heuristics:
```

  ```suggestion
  The algorithm qualifies the candidates as preemption targets using the following heuristics:
  ```

- **site/content/en/docs/concepts/cluster_queue.md:469** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T15:21:13Z
```diff
@@ -422,12 +422,52 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
 
-- Workloads belonging to ClusterQueues that are borrowing quota.
-- Workloads with the lowest priority.
-- Workloads that have been admitted more recently.
+Kueue implements heuristics to preempt as few Workloads as possible.
+Below we present a more detailed description of the algorithm.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
+for preemptions, within a flavor of the target queue, when one of the following
+is true:
+- the requests of the Workload are below the flavor's nominal quota, or
+- `borrowWithinCohort` is enabled.
+
+#### Candidates
+
+The list of preemption candidates is compiled from Workloads within the Cluster
+Queue satisfying the `withinClusterQueue` policy, and Workloads within the
+cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- Workloads from other borrowing queues in the cohort,
+- Workloads with the lowest priority,
+- Workloads which got admitted the most recently.
+
+#### Targets
+
+The algorithm qualifies the candidates as targets using the following heuristics:
+
+1. If all candidates belong to the target queue, then Kueue greedily
+qualifies candidates until the incoming Workload can fit, allowing for borrowing.
+
+2. If `borrowWithinCohort` is enabled, then Kueue greedily qualifies
+candidates (respecting the `borrowWithinCohort.maxPriorityThreshold` threshold),
+until the incoming Workload can fit, allowing for borrowing.
+
+3. If the current usage of the target queue is below nominal quota, then
+Kueue greedily qualifies the candidates, until the incoming workload can fit
+without borrowing.
+
+4. Kueue tries to greedily qualifies a subset of candidates which belong to the
+target Cluster Queue, until the incoming Workload can fit, allowing for borrowing.
+
+The last step of the algorithm is to optimize the set of targets. For this
+purpose Kueue greedily traverses the list of initial targets (in reverse) and
```

  ```suggestion
  purpose Kueue greedily traverses the list of initial targets in reverse and
  ```

- **site/content/en/docs/concepts/cluster_queue.md:470** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T15:21:59Z
```diff
@@ -422,12 +422,52 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
 
-- Workloads belonging to ClusterQueues that are borrowing quota.
-- Workloads with the lowest priority.
-- Workloads that have been admitted more recently.
+Kueue implements heuristics to preempt as few Workloads as possible.
+Below we present a more detailed description of the algorithm.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
+for preemptions, within a flavor of the target queue, when one of the following
+is true:
+- the requests of the Workload are below the flavor's nominal quota, or
+- `borrowWithinCohort` is enabled.
+
+#### Candidates
+
+The list of preemption candidates is compiled from Workloads within the Cluster
+Queue satisfying the `withinClusterQueue` policy, and Workloads within the
+cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- Workloads from other borrowing queues in the cohort,
+- Workloads with the lowest priority,
+- Workloads which got admitted the most recently.
+
+#### Targets
+
+The algorithm qualifies the candidates as targets using the following heuristics:
+
+1. If all candidates belong to the target queue, then Kueue greedily
+qualifies candidates until the incoming Workload can fit, allowing for borrowing.
+
+2. If `borrowWithinCohort` is enabled, then Kueue greedily qualifies
+candidates (respecting the `borrowWithinCohort.maxPriorityThreshold` threshold),
+until the incoming Workload can fit, allowing for borrowing.
+
+3. If the current usage of the target queue is below nominal quota, then
+Kueue greedily qualifies the candidates, until the incoming workload can fit
+without borrowing.
+
+4. Kueue tries to greedily qualifies a subset of candidates which belong to the
+target Cluster Queue, until the incoming Workload can fit, allowing for borrowing.
+
+The last step of the algorithm is to optimize the set of targets. For this
+purpose Kueue greedily traverses the list of initial targets (in reverse) and
+removes these without which the incoming Workload still can be admitted.
```

  ```suggestion
  removes them from the list of targets if the incoming Workload still can be admitted when they are accounted back for quota usage.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:455** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T15:24:22Z
```diff
@@ -422,12 +422,52 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
 
-- Workloads belonging to ClusterQueues that are borrowing quota.
-- Workloads with the lowest priority.
-- Workloads that have been admitted more recently.
+Kueue implements heuristics to preempt as few Workloads as possible.
+Below we present a more detailed description of the algorithm.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
+for preemptions, within a flavor of the target queue, when one of the following
+is true:
+- the requests of the Workload are below the flavor's nominal quota, or
+- `borrowWithinCohort` is enabled.
+
+#### Candidates
+
+The list of preemption candidates is compiled from Workloads within the Cluster
+Queue satisfying the `withinClusterQueue` policy, and Workloads within the
+cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- Workloads from other borrowing queues in the cohort,
+- Workloads with the lowest priority,
+- Workloads which got admitted the most recently.
+
+#### Targets
+
+The algorithm qualifies the candidates as targets using the following heuristics:
+
+1. If all candidates belong to the target queue, then Kueue greedily
+qualifies candidates until the incoming Workload can fit, allowing for borrowing.
```

  instead of `allowing borrowing`, maybe we can be more explicit.
  
  And then you can refer to "allowing/disallowing borrowing" freely
  
  ```
  allowing the usage of the ClusterQueue to be above the nominal quota, up to the `borrowingLimit`.
  This is called "borrowing".
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-17T15:34:55Z

- **site/content/en/docs/concepts/cluster_queue.md:455** by [@mimowo](https://github.com/mimowo) — 2024-04-17T15:34:55Z
```diff
@@ -422,12 +422,52 @@ The fields above do the following:
   - `LowerOrNewerEqualPriority`: only preempt Workloads in the ClusterQueue that either have a lower priority than the pending workload or equal priority and are newer than the pending workload.
 
 Note that an incoming Workload can preempt Workloads both within the
-ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
-Workloads as possible, preferring Workloads with these characteristics:
+ClusterQueue and the cohort.
 
-- Workloads belonging to ClusterQueues that are borrowing quota.
-- Workloads with the lowest priority.
-- Workloads that have been admitted more recently.
+Kueue implements heuristics to preempt as few Workloads as possible.
+Below we present a more detailed description of the algorithm.
+
+### Preemption Algorithm overview
+
+An incoming Workload, which does not fit within the free capacity, is eligible
+for preemptions, within a flavor of the target queue, when one of the following
+is true:
+- the requests of the Workload are below the flavor's nominal quota, or
+- `borrowWithinCohort` is enabled.
+
+#### Candidates
+
+The list of preemption candidates is compiled from Workloads within the Cluster
+Queue satisfying the `withinClusterQueue` policy, and Workloads within the
+cohort which satisfy the `reclaimWithinCohort` policy.
+
+The list of candidates is sorted based on the following preference checks for
+tie-breaking:
+- Workloads from other borrowing queues in the cohort,
+- Workloads with the lowest priority,
+- Workloads which got admitted the most recently.
+
+#### Targets
+
+The algorithm qualifies the candidates as targets using the following heuristics:
+
+1. If all candidates belong to the target queue, then Kueue greedily
+qualifies candidates until the incoming Workload can fit, allowing for borrowing.
```

  Done along the lines. PTAL.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-17T15:46:54Z

/lgtm
/approve
