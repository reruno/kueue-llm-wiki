# PR #2001: docs: add troubleshooting resource requests

**Summary**: docs: add troubleshooting resource requests

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2001

**Last updated**: 2024-04-19T18:00:21Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vsoch](https://github.com/vsoch)
- **Branch**: `add-troubleshooting-resource-requests` → `main`
- **Created**: 2024-04-17T20:03:46Z
- **Updated**: 2024-04-19T18:00:21Z
- **Merged**: 2024-04-18T14:45:59Z
- **Closed**: 2024-04-18T14:45:59Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 3   **+40 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

There isn't a troubleshooting case for cluster resources not matching what the pod needs.

#### Which issue(s) this PR fixes:

Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
NONE

```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-17T20:03:55Z

Hi @vsoch. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-17T20:04:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3c404297ad6a64acab8636ee5e59383cabb1b0a1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66211591893f3b0008b5d8fa |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2001--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2001--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTIwMDEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.vzopaX0hEKj2TKOD-bo25dWD0E6r_QQQ4D7ueZkA3SA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T12:25:53Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T14:41:22Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-18T14:41:28Z

LGTM label has been added.  <details>Git tree hash: e3665dd925a212738505f5645866069e4936d142</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-18T14:41:30Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2001#issuecomment-2064054725" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2001#" title="Author self-approved">vsoch</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T14:45:01Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-04-18T14:45:03Z

@alculquicondor: once the present PR merges, I will cherry-pick it on top of website in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2001#issuecomment-2064068626):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-04-18T14:46:35Z

@alculquicondor: #2001 failed to apply on top of branch "website":
```
Applying: docs: add note about resources matching cluster-queue
Applying: fix: typos in provisioning and troubleshooting
Using index info to reconstruct a base tree...
M	site/content/en/docs/admission-check-controllers/provisioning.md
Falling back to patching base and 3-way merge...
Auto-merging site/content/en/docs/admission-check-controllers/provisioning.md
CONFLICT (content): Merge conflict in site/content/en/docs/admission-check-controllers/provisioning.md
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0002 fix: typos in provisioning and troubleshooting
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2001#issuecomment-2064068626):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T15:47:11Z

Could you create a manual cherry-pick?

`./hack/cherry_pick_pull.sh website 2001`

### Comment by [@vsoch](https://github.com/vsoch) — 2024-04-18T17:16:11Z

Is that comment for me?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T17:52:37Z

yes, if you have the time.

### Comment by [@vsoch](https://github.com/vsoch) — 2024-04-18T18:35:00Z

Is there a second ref missing there?

```console
$ ./hack/cherry_pick_pull.sh 2001
./hack/cherry_pick_pull.sh <remote branch> <pr-number>...: cherry pick one or more <pr> onto <remote branch> and leave instructions for proposing pull request

  Checks out <remote branch> and handles the cherry-pick of <pr> (possibly multiple) for you.
  Examples:
    ./hack/cherry_pick_pull.sh upstream/release-3.14 12345        # Cherry-picks PR 12345 onto upstream/release-3.14 and proposes that as a PR.
    ./hack/cherry_pick_pull.sh upstream/release-3.14 12345 56789  # Cherry-picks PR 12345, then 56789 and proposes the combination as a single PR.

  Set the DRY_RUN environment var to skip git push and creating PR.
  This is useful for creating patches to a release branch without making a PR.
  When DRY_RUN is set the script will leave you in a branch containing the commits you cherry-picked.

  Set the REGENERATE_DOCS environment var to regenerate documentation for the target branch after picking the specified commits.
  This is useful when picking commits containing changes to API documentation.

  Set UPSTREAM_REMOTE (default: upstream) and FORK_REMOTE (default: origin)
  to override the default remote names to what you have locally.

  For merge process info, see https://git.k8s.io/community/contributors/devel/sig-release/cherry-picks.md

```
And this is from the branch here?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T19:23:55Z

```
./hack/cherry_pick_pull.sh website 2001
```
`website` is the destination branch.

### Comment by [@vsoch](https://github.com/vsoch) — 2024-04-18T19:49:10Z

Sorry @alculquicondor I'm not comfortable with the permissions this gh client is asking for: 

*deleted* had my address :) 

If there is a manual cherry pick command you want to show I'm happy to run that, but I'm not giving this permission at that level to my account.

### Comment by [@vsoch](https://github.com/vsoch) — 2024-04-18T19:52:42Z

Would it be better next time if I open the PR from my personal account instead of researchapps? Is that the issue with the automation not working (because it's an org and not a user)?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T21:11:55Z

You can always do a fully manual cherry-pick

```sh
git checkout upstream/website
git checkout -b my-cherry-pick
git cherry-pick <commit-hash>
<fix conflicts>
<usual git push>
<open pr>
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T21:12:39Z

> Is that the issue with the automation not working (because it's an org and not a user)?

No, that's just because there was a conflict.

### Comment by [@vsoch](https://github.com/vsoch) — 2024-04-18T21:38:09Z

Should I just open the PR again and rebase against the main/master branch?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T21:46:20Z

No, against the website branch

### Comment by [@vsoch](https://github.com/vsoch) — 2024-04-18T21:56:36Z

hmm the rebase won't work because it adds my user to all the previous commits. 
![image](https://github.com/kubernetes-sigs/kueue/assets/814322/3d372d82-3781-4965-be3d-41a23b2ab743)

What is the main issue? Why was there a conflict (not detected before merge) for the content to go into website?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T12:26:41Z

There shouldn't have been a need for a rebase. Let me try.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T14:52:55Z

For reference, this is what I did:

```
git fetch upstream website
git checkout -b cherry-troubleshooting upstream/website
git cherry-pick 472ce6d2c6bbfab2903db065dbb8f117415bc8d5
#resolve conflicts
git cherry-pick --continue
git push -u origin cherry-troubleshooting
```

Then open the PR by following https://github.com/kubernetes-sigs/kueue/compare/website...alculquicondor:kueue:cherry-troubleshooting?expand=1

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T14:53:55Z

/release-note-edit
```release-note
NONE
```

We decided not to add release notes for documentation changes, as they might be updated at the different time from the release.

### Comment by [@vsoch](https://github.com/vsoch) — 2024-04-19T17:00:35Z

Thanks, they look good! Hopefully the automation will work next time so you don't have to do manual work.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T18:00:20Z

It's normal, there was a conflict.

The document for provisioning request that you updated has changes that are not in the website (because they correspond to a feature that is not released yet).

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-17T20:52:09Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:63** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T20:46:49Z
```diff
@@ -60,6 +60,45 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue   cluster-queue   9m45s
    ```
 
+## Does my cluster-queue match the job resource requests?
```

  I would put this as a `###` after the `Pending workload` section.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:63** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T20:47:22Z
```diff
@@ -60,6 +60,45 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue   cluster-queue   9m45s
    ```
 
+## Does my cluster-queue match the job resource requests?
```

  ```suggestion
  ## Does my ClusterQueue have the resource requests that the job requires?
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:68** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T20:49:11Z
```diff
@@ -60,6 +60,45 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue   cluster-queue   9m45s
    ```
 
+## Does my cluster-queue match the job resource requests?
+
+When you submit a job that has a resource request, for example:
+
+```bash
+$ kubectl get jobs job-0-9-size-6 -o json | jq -r .spec.template.spec.containers[0].resources
```

  ```suggestion
  $ kubectl get jobs job-0-9-size-6 -o jsonpath='.spec.template.spec.containers[0].resources'
  ```
  
  Although this wouldn't pretty print

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:101** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T20:50:38Z
```diff
@@ -60,6 +60,45 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue   cluster-queue   9m45s
    ```
 
+## Does my cluster-queue match the job resource requests?
+
+When you submit a job that has a resource request, for example:
+
+```bash
+$ kubectl get jobs job-0-9-size-6 -o json | jq -r .spec.template.spec.containers[0].resources
+```
+```console
+{
+  "limits": {
+    "cpu": "2"
+  },
+  "requests": {
+    "cpu": "2"
+  }
+}
+```
+
+If your cluster queue does not have a definition for the _requests_ the job cannot be admitted. For the job above you would want to ensure that you've defined "cpu" under `resourceGroups`, as shown below:
+
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {}
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 40
+```
+
+Generally speaking, if you find that workloads are not admitted, a good debugging strategy is to check that you've covered resources that are requested, and then carefully describe each of the job and workload, along with looking at logs for kueue. You can typically find a log or condition message that gives a hint to why the queue is not moving.
+
```

  this is already explained in the other sections.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:81** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T20:50:47Z
```diff
@@ -60,6 +60,45 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue   cluster-queue   9m45s
    ```
 
+## Does my cluster-queue match the job resource requests?
+
+When you submit a job that has a resource request, for example:
+
+```bash
+$ kubectl get jobs job-0-9-size-6 -o json | jq -r .spec.template.spec.containers[0].resources
+```
+```console
+{
+  "limits": {
+    "cpu": "2"
+  },
+  "requests": {
+    "cpu": "2"
+  }
+}
+```
+
+If your cluster queue does not have a definition for the _requests_ the job cannot be admitted. For the job above you would want to ensure that you've defined "cpu" under `resourceGroups`, as shown below:
```

  ```suggestion
  If your ClusterQueue does not have a definition for the _requests_, Kueue cannot admit the job. For the job above, you would want to ensure that you've defined `cpu` under `resourceGroups`, as shown below:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:81** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T20:52:06Z
```diff
@@ -60,6 +60,45 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue   cluster-queue   9m45s
    ```
 
+## Does my cluster-queue match the job resource requests?
+
+When you submit a job that has a resource request, for example:
+
+```bash
+$ kubectl get jobs job-0-9-size-6 -o json | jq -r .spec.template.spec.containers[0].resources
+```
+```console
+{
+  "limits": {
+    "cpu": "2"
+  },
+  "requests": {
+    "cpu": "2"
+  }
+}
+```
+
+If your cluster queue does not have a definition for the _requests_ the job cannot be admitted. For the job above you would want to ensure that you've defined "cpu" under `resourceGroups`, as shown below:
```

  Although, I would try to avoid repeating too many things that are already described somewhere else. You can link to https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#resource-groups
  
  The idea of this document is to give a decision tree for debugging, and link to the relevant documentation for the specifics.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2024-04-17T21:23:51Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:68** by [@vsoch](https://github.com/vsoch) — 2024-04-17T21:23:51Z
```diff
@@ -60,6 +60,45 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue   cluster-queue   9m45s
    ```
 
+## Does my cluster-queue match the job resource requests?
+
+When you submit a job that has a resource request, for example:
+
+```bash
+$ kubectl get jobs job-0-9-size-6 -o json | jq -r .spec.template.spec.containers[0].resources
```

  I do like the pretty printing <3 But happy to change it if the docs already have a convention.

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2024-04-17T21:25:04Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:101** by [@vsoch](https://github.com/vsoch) — 2024-04-17T21:25:04Z
```diff
@@ -60,6 +60,45 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue   cluster-queue   9m45s
    ```
 
+## Does my cluster-queue match the job resource requests?
+
+When you submit a job that has a resource request, for example:
+
+```bash
+$ kubectl get jobs job-0-9-size-6 -o json | jq -r .spec.template.spec.containers[0].resources
+```
+```console
+{
+  "limits": {
+    "cpu": "2"
+  },
+  "requests": {
+    "cpu": "2"
+  }
+}
+```
+
+If your cluster queue does not have a definition for the _requests_ the job cannot be admitted. For the job above you would want to ensure that you've defined "cpu" under `resourceGroups`, as shown below:
+
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {}
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 40
+```
+
+Generally speaking, if you find that workloads are not admitted, a good debugging strategy is to check that you've covered resources that are requested, and then carefully describe each of the job and workload, along with looking at logs for kueue. You can typically find a log or condition message that gives a hint to why the queue is not moving.
+
```

  I am going to say here that redundancy is OK, especially in documentation. I don't think we want to be maintaining equivalent information (e.g., some spec or definition) in two places, but reminders about "here is a good way to debug" are always good to have multiple times. The user reader very likely has not seen/read all the "the other sections."

### Review by [@vsoch](https://github.com/vsoch) (commented) — 2024-04-17T21:39:15Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:101** by [@vsoch](https://github.com/vsoch) — 2024-04-17T21:39:15Z
```diff
@@ -60,6 +60,45 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue   cluster-queue   9m45s
    ```
 
+## Does my cluster-queue match the job resource requests?
+
+When you submit a job that has a resource request, for example:
+
+```bash
+$ kubectl get jobs job-0-9-size-6 -o json | jq -r .spec.template.spec.containers[0].resources
+```
+```console
+{
+  "limits": {
+    "cpu": "2"
+  },
+  "requests": {
+    "cpu": "2"
+  }
+}
+```
+
+If your cluster queue does not have a definition for the _requests_ the job cannot be admitted. For the job above you would want to ensure that you've defined "cpu" under `resourceGroups`, as shown below:
+
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {}
+  resourceGroups:
+  - coveredResources: ["cpu"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 40
+```
+
+Generally speaking, if you find that workloads are not admitted, a good debugging strategy is to check that you've covered resources that are requested, and then carefully describe each of the job and workload, along with looking at logs for kueue. You can typically find a log or condition message that gives a hint to why the queue is not moving.
+
```

  I did wind up removing it, resolving!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-18T12:29:03Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:154** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T12:29:02Z
```diff
@@ -133,6 +133,45 @@ status:
     type: QuotaReserved
 ```
 
+### Does my ClusterQueue have the resource requests that the job requires?
+
+When you submit a job that has a resource request, for example:
+
+```bash
+$ kubectl get jobs job-0-9-size-6 -o json | jq -r .spec.template.spec.containers[0].resources
+```
+```console
+{
+  "limits": {
+    "cpu": "2"
+  },
+  "requests": {
+    "cpu": "2"
+  }
+}
+```
+
+If your ClusterQueue does not have a definition for the _requests_, Kueue cannot admit the job. For the job above, you would want to ensure that you've defined `cpu` under `resourceGroups`, as shown below:
```

  ```suggestion
  If your ClusterQueue does not have a definition for the `requests`, Kueue cannot admit the job. For the job above, you should define `cpu` quotas under `resourceGroups`. A ClusterQueue defining `cpu` quota looks like the following:
  ```
