# PR #2002: Rule s2-b for flat fair preemption

**Summary**: Rule s2-b for flat fair preemption

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2002

**Last updated**: 2024-04-22T09:05:54Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `fair-preemption-rule-b` → `main`
- **Created**: 2024-04-17T20:38:14Z
- **Updated**: 2024-04-22T09:05:54Z
- **Merged**: 2024-04-22T09:05:52Z
- **Closed**: 2024-04-22T09:05:52Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 4   **+134 / -29**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Continues #1948, adding rule S2-b from https://github.com/kubernetes-sigs/kueue/tree/main/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption

```
value of AlmostLCA(y,x) (with z) is strictly higher than AlmostLCA(x,y) with admitted workload w. Here the case is that Y’s sub-org is greedy, and doesn’t allow smaller requests on the x’s size. If no other preemption option is possible inside any other CQ, z should be considered for preemption. Maybe y has a smaller workload that could be admitted, and then both x and y will be ”happy”. In practice this rule prevents huge workloads from taking the whole unused capacity, blocking everyone else and claiming that this is fair-sharing.
```

In other words, allow preemption if the highest share value among the CQs will monotonically decrease.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #1714

#### Special notes for your reviewer:

WIP because I want to play a little more with the algorithm and more test cases.

#### Does this PR introduce a user-facing change?

Not yet. The boolean that determines the use of fair sharing is not accessible through any API.

```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-17T20:38:23Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2002#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-17T20:38:27Z

Adding the "do-not-merge/release-note-label-needed" label and removing any existing "release-note-none" label because there is a "kind/deprecation" label on the PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-17T20:38:34Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2648b128fcf7d0ddf603a6cdb81a8e9e9c2cdd12 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6622bdd960334f0008e2d548 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-17T20:39:07Z

/remove-kind failing-test api-change deprecation flake regression

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-18T10:54:29Z

/assign

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T14:37:38Z

/retest
Flaky #2020

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T14:58:24Z

This is ready for review

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-22T06:22:50Z

LGTM label has been added.  <details>Git tree hash: e5c9f26c4cccd126f9a38a393707559552859484</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-22T08:44:30Z

/lgtm
/hold cancel

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T11:33:23Z

- **pkg/scheduler/preemption/preemption.go:305** by [@mimowo](https://github.com/mimowo) — 2024-04-18T11:31:58Z
```diff
@@ -323,12 +301,41 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 				}
 				// Might need to pick a different CQ due to changing values.
 				break
+			} else {
+				retryCandidates = append(retryCandidates, candCQ.workloads[i])
```

  Shouldn't we only retry these that are `belowThreshold`?

- **pkg/queue/cluster_queue.go:98** by [@mimowo](https://github.com/mimowo) — 2024-04-18T11:32:21Z
```diff
@@ -95,7 +95,7 @@ func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueu
 func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
 	lessFunc := queueOrderingFunc(wo)
 	return &ClusterQueue{
-		heap:                   heap.New(workloadKey, lessFunc),
+		heap:                   *heap.New(workloadKey, lessFunc),
```

  Is it a bugfix after the previous PR, or this is specifically needed for this? Or not needed ?:)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-18T14:37:26Z

- **pkg/queue/cluster_queue.go:98** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T14:37:25Z
```diff
@@ -95,7 +95,7 @@ func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueu
 func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
 	lessFunc := queueOrderingFunc(wo)
 	return &ClusterQueue{
-		heap:                   heap.New(workloadKey, lessFunc),
+		heap:                   *heap.New(workloadKey, lessFunc),
```

  Not needed, but I want to stay consistent and always have a `New_` function return a pointer.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-18T14:40:13Z

- **pkg/scheduler/preemption/preemption.go:312** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T14:39:30Z
```diff
@@ -323,12 +301,41 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 				}
 				// Might need to pick a different CQ due to changing values.
 				break
+			} else {
+				retryCandidates = append(retryCandidates, candCQ.workloads[i])
 			}
 		}
 	}
 	if !fits {
-		restoreSnapshot(snapshot, targets)
-		return nil
+		// Try rule S2-b in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+		// if rule S2-A was not enough.
+		cqHeap = cqHeapFromCandidates(retryCandidates, snapshot)
```

  note to self: we need to recheck if the clusterqueues are still borrowing.

- **pkg/scheduler/preemption/preemption.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T14:40:03Z
```diff
@@ -323,12 +301,41 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 				}
 				// Might need to pick a different CQ due to changing values.
 				break
+			} else {
+				retryCandidates = append(retryCandidates, candCQ.workloads[i])
```

  if it's above threshold, it would not come to the `else`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T14:46:08Z

- **pkg/queue/cluster_queue.go:98** by [@mimowo](https://github.com/mimowo) — 2024-04-18T14:46:08Z
```diff
@@ -95,7 +95,7 @@ func newClusterQueue(cq *kueue.ClusterQueue, wo workload.Ordering) (*ClusterQueu
 func newClusterQueueImpl(wo workload.Ordering, clock clock.Clock) *ClusterQueue {
 	lessFunc := queueOrderingFunc(wo)
 	return &ClusterQueue{
-		heap:                   heap.New(workloadKey, lessFunc),
+		heap:                   *heap.New(workloadKey, lessFunc),
```

  Sure, consistency is important

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-18T14:48:14Z

- **pkg/scheduler/preemption/preemption.go:305** by [@mimowo](https://github.com/mimowo) — 2024-04-18T14:48:14Z
```diff
@@ -323,12 +301,41 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 				}
 				// Might need to pick a different CQ due to changing values.
 				break
+			} else {
+				retryCandidates = append(retryCandidates, candCQ.workloads[i])
```

  Hm, the first condition is `belowThreshold || newNominatedShareValue <= newCandShareVal`, so if above threshold it depends on the result of `newNominatedShareValue <= newCandShareVal`, which could be either way. Unless I'm missing something.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-18T15:49:05Z

- **pkg/scheduler/preemption/preemption.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T15:49:05Z
```diff
@@ -323,12 +301,41 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 				}
 				// Might need to pick a different CQ due to changing values.
 				break
+			} else {
+				retryCandidates = append(retryCandidates, candCQ.workloads[i])
```

  True.
  However, it doesn't matter. The workload is still a candidate, under the eye of fair sharing.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-18T20:50:10Z

- **pkg/scheduler/preemption/preemption.go:312** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T19:57:19Z
```diff
@@ -323,12 +301,41 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 				}
 				// Might need to pick a different CQ due to changing values.
 				break
+			} else {
+				retryCandidates = append(retryCandidates, candCQ.workloads[i])
 			}
 		}
 	}
 	if !fits {
-		restoreSnapshot(snapshot, targets)
-		return nil
+		// Try rule S2-b in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+		// if rule S2-A was not enough.
+		cqHeap = cqHeapFromCandidates(retryCandidates, snapshot)
```

  Actually, this is not necessary, because if the CQ is not borrowing, then `candCQ.share` below is zero, thus nothing can preempt anymore.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-19T06:38:36Z

- **pkg/scheduler/preemption/preemption.go:305** by [@mimowo](https://github.com/mimowo) — 2024-04-19T06:38:36Z
```diff
@@ -323,12 +301,41 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 				}
 				// Might need to pick a different CQ due to changing values.
 				break
+			} else {
+				retryCandidates = append(retryCandidates, candCQ.workloads[i])
```

  The scenario I'm thinking about is that it is above the threshold, and `newNominatedShareValue <= newCandShareVal ` is false, then it is added to `retryCandidates`. 
  
  In that case it is considered in rule B, where we no longer check the threshold. So, in the end a workload above threshold could be preempted if selected in rule B. Wondering if this is expected?
  
  EDIT: nvm, I think it is ok, a workload above threshold should be considered for rule B in fair sharing.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-19T17:38:54Z

Generally, lgtm

- **pkg/scheduler/preemption/preemption.go:364** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-19T17:04:02Z
```diff
@@ -341,6 +342,33 @@ type candidateCQ struct {
 	share     int
 }
 
+func cqHeapFromCandidates(candidates []*workload.Info, firstOnly bool, snapshot *cache.Snapshot) *heap.Heap[candidateCQ] {
+	cqHeap := heap.New(
+		func(c *candidateCQ) string {
+			return c.cq.Name
+		},
+		func(c1, c2 *candidateCQ) bool {
+			return c1.share > c2.share
+		},
+	)
+	for _, cand := range candidates {
+		candCQ := cqHeap.GetByKey(cand.ClusterQueue)
+		if candCQ == nil {
+			cq := snapshot.ClusterQueues[cand.ClusterQueue]
+			share, _ := cq.DominantResourceShare()
+			candCQ = &candidateCQ{
+				cq:        cq,
+				share:     share,
+				workloads: []*workload.Info{cand},
+			}
+			_ = cqHeap.PushIfNotPresent(candCQ)
```

  ```suggestion
  			cqHeap.Push(candCQ)
  ```
  
  Since we already checked if the `cqHeap` has `candCQ` in `candCQ == nil`, I'm curious about whether the `Push` function would be enough. WDYT?

- **pkg/scheduler/preemption/preemption_test.go:1536** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-19T17:32:56Z
```diff
@@ -1522,6 +1522,83 @@ func TestFairPreemptions(t *testing.T) {
 			targetCQ:      "a",
 			wantPreempted: sets.New("/a_low", "/b1"),
 		},
+		"preempt huge workload if there is no other option, as long as the target CQ gets a lower share": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "9").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming:      utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "2").Obj(),
+			targetCQ:      "a",
+			wantPreempted: sets.New("/b1"),
+		},
+		"can't preempt huge workload if the incoming is also huge": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("a", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "9").SimpleReserveQuota("b", "default", now).Obj(),
```

  ```suggestion
  				*utiltesting.MakeWorkload("a1", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("a", "default", now).Obj(),
  				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "7").SimpleReserveQuota("b", "default", now).Obj(),
  ```
  
  Is this correct? Because the sum of quota is `9` (3+3+3), I guess that `a1` or `b1` are not admitted, right?

- **pkg/scheduler/preemption/preemption_test.go:1570** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-19T17:37:07Z
```diff
@@ -1522,6 +1522,83 @@ func TestFairPreemptions(t *testing.T) {
 			targetCQ:      "a",
 			wantPreempted: sets.New("/a_low", "/b1"),
 		},
+		"preempt huge workload if there is no other option, as long as the target CQ gets a lower share": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "9").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming:      utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "2").Obj(),
+			targetCQ:      "a",
+			wantPreempted: sets.New("/b1"),
+		},
+		"can't preempt huge workload if the incoming is also huge": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("a", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "9").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming: utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "7").Obj(),
+			targetCQ: "a",
+		},
+		"can't preempt 2 smaller workloads if the incoming is huge": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b2", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b3", "").Request(corev1.ResourceCPU, "3").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming: utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "6").Obj(),
+			targetCQ: "a",
+		},
+		"preempt from target and others even if over nominal": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1_low", "").Priority(-1).Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("a2_low", "").Priority(-1).Request(corev1.ResourceCPU, "1").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "3").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b2", "").Request(corev1.ResourceCPU, "3").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming:      utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "4").Obj(),
+			targetCQ:      "a",
+			wantPreempted: sets.New("/a1_low", "/b1"),
+		},
+		"prefer to preempt workloads that don't make the target CQ have the biggest share": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b2", "").Request(corev1.ResourceCPU, "1").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b3", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("c1", "").Request(corev1.ResourceCPU, "1").SimpleReserveQuota("c", "default", now).Obj(),
+			},
+			incoming: utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "3.5").Obj(),
+			targetCQ: "a",
+			// It would have been possible to preempt "/b1" under rule S2-b, but S2-1 was possible first.
```

  ```suggestion
  			// It would have been possible to preempt "/b1" under rule S2-b, but S2-a was possible first.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-19T18:54:45Z

- **pkg/scheduler/preemption/preemption.go:364** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T18:51:22Z
```diff
@@ -341,6 +342,33 @@ type candidateCQ struct {
 	share     int
 }
 
+func cqHeapFromCandidates(candidates []*workload.Info, firstOnly bool, snapshot *cache.Snapshot) *heap.Heap[candidateCQ] {
+	cqHeap := heap.New(
+		func(c *candidateCQ) string {
+			return c.cq.Name
+		},
+		func(c1, c2 *candidateCQ) bool {
+			return c1.share > c2.share
+		},
+	)
+	for _, cand := range candidates {
+		candCQ := cqHeap.GetByKey(cand.ClusterQueue)
+		if candCQ == nil {
+			cq := snapshot.ClusterQueues[cand.ClusterQueue]
+			share, _ := cq.DominantResourceShare()
+			candCQ = &candidateCQ{
+				cq:        cq,
+				share:     share,
+				workloads: []*workload.Info{cand},
+			}
+			_ = cqHeap.PushIfNotPresent(candCQ)
```

  There is no `Push`, only `PushOrUpdate`. But changed to it, so we can avoid the `_`.

- **pkg/scheduler/preemption/preemption_test.go:1536** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T18:53:27Z
```diff
@@ -1522,6 +1522,83 @@ func TestFairPreemptions(t *testing.T) {
 			targetCQ:      "a",
 			wantPreempted: sets.New("/a_low", "/b1"),
 		},
+		"preempt huge workload if there is no other option, as long as the target CQ gets a lower share": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "9").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming:      utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "2").Obj(),
+			targetCQ:      "a",
+			wantPreempted: sets.New("/b1"),
+		},
+		"can't preempt huge workload if the incoming is also huge": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("a", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "9").SimpleReserveQuota("b", "default", now).Obj(),
```

  That's correct. Also changed the incoming to 5, just to be at the limit.

- **pkg/scheduler/preemption/preemption_test.go:1570** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-19T18:53:57Z
```diff
@@ -1522,6 +1522,83 @@ func TestFairPreemptions(t *testing.T) {
 			targetCQ:      "a",
 			wantPreempted: sets.New("/a_low", "/b1"),
 		},
+		"preempt huge workload if there is no other option, as long as the target CQ gets a lower share": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "9").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming:      utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "2").Obj(),
+			targetCQ:      "a",
+			wantPreempted: sets.New("/b1"),
+		},
+		"can't preempt huge workload if the incoming is also huge": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("a", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "9").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming: utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "7").Obj(),
+			targetCQ: "a",
+		},
+		"can't preempt 2 smaller workloads if the incoming is huge": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b2", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b3", "").Request(corev1.ResourceCPU, "3").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming: utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "6").Obj(),
+			targetCQ: "a",
+		},
+		"preempt from target and others even if over nominal": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("a1_low", "").Priority(-1).Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("a2_low", "").Priority(-1).Request(corev1.ResourceCPU, "1").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "3").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b2", "").Request(corev1.ResourceCPU, "3").SimpleReserveQuota("b", "default", now).Obj(),
+			},
+			incoming:      utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "4").Obj(),
+			targetCQ:      "a",
+			wantPreempted: sets.New("/a1_low", "/b1"),
+		},
+		"prefer to preempt workloads that don't make the target CQ have the biggest share": {
+			admitted: []kueue.Workload{
+				*utiltesting.MakeWorkload("b1", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b2", "").Request(corev1.ResourceCPU, "1").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("b3", "").Request(corev1.ResourceCPU, "2").SimpleReserveQuota("b", "default", now).Obj(),
+				*utiltesting.MakeWorkload("c1", "").Request(corev1.ResourceCPU, "1").SimpleReserveQuota("c", "default", now).Obj(),
+			},
+			incoming: utiltesting.MakeWorkload("a_incoming", "").Request(corev1.ResourceCPU, "3.5").Obj(),
+			targetCQ: "a",
+			// It would have been possible to preempt "/b1" under rule S2-b, but S2-1 was possible first.
```

  Done :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-22T06:22:24Z

- **pkg/scheduler/preemption/preemption.go:364** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-22T06:22:24Z
```diff
@@ -341,6 +342,33 @@ type candidateCQ struct {
 	share     int
 }
 
+func cqHeapFromCandidates(candidates []*workload.Info, firstOnly bool, snapshot *cache.Snapshot) *heap.Heap[candidateCQ] {
+	cqHeap := heap.New(
+		func(c *candidateCQ) string {
+			return c.cq.Name
+		},
+		func(c1, c2 *candidateCQ) bool {
+			return c1.share > c2.share
+		},
+	)
+	for _, cand := range candidates {
+		candCQ := cqHeap.GetByKey(cand.ClusterQueue)
+		if candCQ == nil {
+			cq := snapshot.ClusterQueues[cand.ClusterQueue]
+			share, _ := cq.DominantResourceShare()
+			candCQ = &candidateCQ{
+				cq:        cq,
+				share:     share,
+				workloads: []*workload.Info{cand},
+			}
+			_ = cqHeap.PushIfNotPresent(candCQ)
```

  Oh, that makes sense. Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-22T06:22:45Z

Thank you!
/lgtm
/hold 
for @mimowo
