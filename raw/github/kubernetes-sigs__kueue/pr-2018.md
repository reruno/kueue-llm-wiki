# PR #2018: Scalability scrape

**Summary**: Scalability scrape

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2018

**Last updated**: 2024-04-25T14:33:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `scalability-scrape` → `main`
- **Created**: 2024-04-19T12:04:30Z
- **Updated**: 2024-04-25T14:33:37Z
- **Merged**: 2024-04-24T15:00:34Z
- **Closed**: 2024-04-24T15:00:34Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 5   **+216 / -14**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Make the scalability runner able to periodically scrape the metrics endpoint.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #1912

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
None
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-19T12:04:32Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-19T12:04:48Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 330d2cbfc320687dabd6552efb6845e2fbe983a1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66289b823439e90008018a13 |

### Comment by [@trasc](https://github.com/trasc) — 2024-04-22T10:47:22Z

/cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-23T09:05:47Z

/lgtm
/approve
/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-23T09:05:53Z

LGTM label has been added.  <details>Git tree hash: efb8b51f518e08a29ddfc7a5602bb31002bcccdb</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T18:50:43Z

Do you know how many GBs of data it takes to scrape every 10s?

### Comment by [@trasc](https://github.com/trasc) — 2024-04-24T05:15:40Z

> Do you know how many GBs of data it takes to scrape every 10s?

A scrape is about 90KB, but being text they compress well, 345 of them using around 2.4MB.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-24T14:39:27Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-24T14:39:33Z

LGTM label has been added.  <details>Git tree hash: 59fdf260109bf259aeb088b89f254e7b6a7e2c33</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-24T14:39:34Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2018#issuecomment-2075111806" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2018#issuecomment-2071793828" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2018#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-22T11:46:25Z

LGTM overall

- **Makefile:234** by [@mimowo](https://github.com/mimowo) — 2024-04-22T11:39:19Z
```diff
@@ -230,6 +230,14 @@ ifdef SCALABILITY_KUEUE_LOGS
 SCALABILITY_EXTRA_ARGS +=  --withLogs=true --logToFile=true
 endif
 
+ifdef SCALABILITY_SCRAPE_INTERVAL
+SCALABILITY_SCRAPE_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
```

  Can we add them to `SCALABILITY_EXTRA_ARGS`? I guess one could use them with `kubectl proxy` and scrape from a local kind cluster.

- **test/scalability/README.md:60** by [@mimowo](https://github.com/mimowo) — 2024-04-22T11:40:35Z
```diff
@@ -55,6 +57,8 @@ Setting `SCALABILITY_CPU_PROFILE=1` will generate a cpuprofile of minimalkueue i
 
 Setting `SCALABILITY_KUEUE_LOGS=1` will save the logs of minimalkueue in  `$(PROJECT_DIR)/bin/run-scalability/minimalkueue.out.log` and  `$(PROJECT_DIR)/bin/run-scalability/minimalkueue.err.log`
 
+Setting `SCALABILITY_SCRAPE_INTERVAL` to an interval value (e.g. `1s`) will expose the metrics of `minimalkueue` and have them collected by the scalability runner in `$(PROJECT_DIR)/bin/run-scalability/metricsDump.tgz` every interval. 
```

  Can we also have a small usability script to load the tgz into victoria metrics?

- **test/scalability/runner/scraper/scraper.go:62** by [@mimowo](https://github.com/mimowo) — 2024-04-22T11:41:03Z
```diff
@@ -0,0 +1,124 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scraper
+
+import (
+	"archive/tar"
+	"bytes"
+	"compress/gzip"
+	"context"
+	"errors"
+	"fmt"
+	"io"
+	"net"
+	"net/http"
+	"os"
+	"time"
+)
+
+func GetFreePort() (int, error) {
+	l, err := net.Listen("tcp", ":0")
+	if err != nil {
+		return 0, err
+	}
+	defer l.Close()
+	l.Close()
+	if taddr, isTcp := l.Addr().(*net.TCPAddr); isTcp {
+		return taddr.Port, nil
+	}
+	return 0, errors.New("cannot get a free tcp address")
+}
+
+type Scraper struct {
+	interval       time.Duration
+	url            string
+	fileNameFormat string
+	c              http.Client
+}
+
+func NewScraper(interval time.Duration, url, fileNameFormat string) *Scraper {
+	return &Scraper{
+		interval:       interval,
+		url:            url,
+		fileNameFormat: fileNameFormat,
+		c:              http.Client{},
+	}
+}
+
+func (s *Scraper) doScarpe(ctx context.Context, tw *tar.Writer) error {
```

  ```suggestion
  func (s *Scraper) doScrape(ctx context.Context, tw *tar.Writer) error {
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-22T12:25:27Z

- **test/scalability/README.md:60** by [@trasc](https://github.com/trasc) — 2024-04-22T12:25:27Z
```diff
@@ -55,6 +57,8 @@ Setting `SCALABILITY_CPU_PROFILE=1` will generate a cpuprofile of minimalkueue i
 
 Setting `SCALABILITY_KUEUE_LOGS=1` will save the logs of minimalkueue in  `$(PROJECT_DIR)/bin/run-scalability/minimalkueue.out.log` and  `$(PROJECT_DIR)/bin/run-scalability/minimalkueue.err.log`
 
+Setting `SCALABILITY_SCRAPE_INTERVAL` to an interval value (e.g. `1s`) will expose the metrics of `minimalkueue` and have them collected by the scalability runner in `$(PROJECT_DIR)/bin/run-scalability/metricsDump.tgz` every interval. 
```

  I'm not very familiar with victoria metrics.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-22T12:27:46Z

- **Makefile:234** by [@trasc](https://github.com/trasc) — 2024-04-22T12:27:45Z
```diff
@@ -230,6 +230,14 @@ ifdef SCALABILITY_KUEUE_LOGS
 SCALABILITY_EXTRA_ARGS +=  --withLogs=true --logToFile=true
 endif
 
+ifdef SCALABILITY_SCRAPE_INTERVAL
+SCALABILITY_SCRAPE_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
```

  `SCALABILITY_EXTRA_ARGS` is used for args that are relevant for minimalkueue usecase only.
  
  >  I guess one could use them with kubectl proxy and scrape from a local kind cluster.
  
  I don't understand.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-22T12:29:35Z

- **test/scalability/runner/scraper/scraper.go:62** by [@trasc](https://github.com/trasc) — 2024-04-22T12:29:35Z
```diff
@@ -0,0 +1,124 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package scraper
+
+import (
+	"archive/tar"
+	"bytes"
+	"compress/gzip"
+	"context"
+	"errors"
+	"fmt"
+	"io"
+	"net"
+	"net/http"
+	"os"
+	"time"
+)
+
+func GetFreePort() (int, error) {
+	l, err := net.Listen("tcp", ":0")
+	if err != nil {
+		return 0, err
+	}
+	defer l.Close()
+	l.Close()
+	if taddr, isTcp := l.Addr().(*net.TCPAddr); isTcp {
+		return taddr.Port, nil
+	}
+	return 0, errors.New("cannot get a free tcp address")
+}
+
+type Scraper struct {
+	interval       time.Duration
+	url            string
+	fileNameFormat string
+	c              http.Client
+}
+
+func NewScraper(interval time.Duration, url, fileNameFormat string) *Scraper {
+	return &Scraper{
+		interval:       interval,
+		url:            url,
+		fileNameFormat: fileNameFormat,
+		c:              http.Client{},
+	}
+}
+
+func (s *Scraper) doScarpe(ctx context.Context, tw *tar.Writer) error {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-22T12:30:57Z

- **test/scalability/README.md:60** by [@mimowo](https://github.com/mimowo) — 2024-04-22T12:30:57Z
```diff
@@ -55,6 +57,8 @@ Setting `SCALABILITY_CPU_PROFILE=1` will generate a cpuprofile of minimalkueue i
 
 Setting `SCALABILITY_KUEUE_LOGS=1` will save the logs of minimalkueue in  `$(PROJECT_DIR)/bin/run-scalability/minimalkueue.out.log` and  `$(PROJECT_DIR)/bin/run-scalability/minimalkueue.err.log`
 
+Setting `SCALABILITY_SCRAPE_INTERVAL` to an interval value (e.g. `1s`) will expose the metrics of `minimalkueue` and have them collected by the scalability runner in `$(PROJECT_DIR)/bin/run-scalability/metricsDump.tgz` every interval. 
```

  Me neither, but the API seems simple, I successfully loaded a single file with `curl -vX POST -T "$file" http://localhost:8428/api/v1/import/prometheus?timestamp="$timestamp"`. My idea is to use this API to load the dump, similarly as I prototyped in the [doc](https://docs.google.com/document/d/1iO8CGO6kgav2JL6RKYnNYvkeQIHpFqB-SQqJ7eUsjPs/edit?tab=t.0)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-22T12:36:16Z

- **Makefile:234** by [@mimowo](https://github.com/mimowo) — 2024-04-22T12:36:16Z
```diff
@@ -230,6 +230,14 @@ ifdef SCALABILITY_KUEUE_LOGS
 SCALABILITY_EXTRA_ARGS +=  --withLogs=true --logToFile=true
 endif
 
+ifdef SCALABILITY_SCRAPE_INTERVAL
+SCALABILITY_SCRAPE_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
```

  I was suggesting that we could do:
  ```
  ifdef SCALABILITY_SCRAPE_INTERVAL
  SCALABILITY_EXTRA_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
  endif
  
  ifdef SCALABILITY_SCRAPE_URL
  SCALABILITY_EXTRA_ARGS +=  --metricsScrapeURL=$(SCALABILITY_SCRAPE_URL)
  endif
  ```
  As a consequence the params will be also passed to `run-scalability-in-cluster`, and I think this could be useful.
  If I'm running a local cluster on Kind, then I can use `kubectl proxy` and pass `metricsScrapeURL=http://localhost:8001/metrics`. This will let use scrape the data from the cluster and analyze.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T08:28:03Z

- **test/scalability/README.md:60** by [@trasc](https://github.com/trasc) — 2024-04-23T08:28:03Z
```diff
@@ -55,6 +57,8 @@ Setting `SCALABILITY_CPU_PROFILE=1` will generate a cpuprofile of minimalkueue i
 
 Setting `SCALABILITY_KUEUE_LOGS=1` will save the logs of minimalkueue in  `$(PROJECT_DIR)/bin/run-scalability/minimalkueue.out.log` and  `$(PROJECT_DIR)/bin/run-scalability/minimalkueue.err.log`
 
+Setting `SCALABILITY_SCRAPE_INTERVAL` to an interval value (e.g. `1s`) will expose the metrics of `minimalkueue` and have them collected by the scalability runner in `$(PROJECT_DIR)/bin/run-scalability/metricsDump.tgz` every interval. 
```

  added a short section on this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T08:32:43Z

- **test/scalability/README.md:76** by [@mimowo](https://github.com/mimowo) — 2024-04-23T08:32:43Z
```diff
@@ -66,3 +66,17 @@ make test-scalability
 ```
 
 Runs the scalability with minimalkueue and checks the results against `$(PROJECT_DIR)/test/scalability/default_rangespec.yaml`
+
+## Scrape result
+
+The scrape result `metricsDump.tgz` contains a set of `<ts>.prometheus` files, where `ts` is the millisecond representation of the epoch time at the moment each scrape was stared and can be used during the import in a visualization tool.
+
+If an instance of [VictoriaMetrics](https://docs.victoriametrics.com/) listening at `http://localhost:8428` is used, a metrics dump can be imported like:
+
+```bash
```

  It could be helpful to move it to a separate script, and from README just reference running the script.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T08:34:24Z

- **Makefile:234** by [@trasc](https://github.com/trasc) — 2024-04-23T08:34:24Z
```diff
@@ -230,6 +230,14 @@ ifdef SCALABILITY_KUEUE_LOGS
 SCALABILITY_EXTRA_ARGS +=  --withLogs=true --logToFile=true
 endif
 
+ifdef SCALABILITY_SCRAPE_INTERVAL
+SCALABILITY_SCRAPE_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
```

  Passing potential cpu profile or logging params don't make seance in a run-in-cluster scenario, that is why we have two sets.
  
  Regarding the scrape URL, it's not relevant how it is managed (`kubectl port-forward`  or something else) as long as it's accessible.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T08:36:32Z

- **test/scalability/README.md:76** by [@trasc](https://github.com/trasc) — 2024-04-23T08:36:32Z
```diff
@@ -66,3 +66,17 @@ make test-scalability
 ```
 
 Runs the scalability with minimalkueue and checks the results against `$(PROJECT_DIR)/test/scalability/default_rangespec.yaml`
+
+## Scrape result
+
+The scrape result `metricsDump.tgz` contains a set of `<ts>.prometheus` files, where `ts` is the millisecond representation of the epoch time at the moment each scrape was stared and can be used during the import in a visualization tool.
+
+If an instance of [VictoriaMetrics](https://docs.victoriametrics.com/) listening at `http://localhost:8428` is used, a metrics dump can be imported like:
+
+```bash
```

  It's just an usage example with an almost random tool.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T08:42:48Z

- **test/scalability/README.md:76** by [@mimowo](https://github.com/mimowo) — 2024-04-23T08:42:48Z
```diff
@@ -66,3 +66,17 @@ make test-scalability
 ```
 
 Runs the scalability with minimalkueue and checks the results against `$(PROJECT_DIR)/test/scalability/default_rangespec.yaml`
+
+## Scrape result
+
+The scrape result `metricsDump.tgz` contains a set of `<ts>.prometheus` files, where `ts` is the millisecond representation of the epoch time at the moment each scrape was stared and can be used during the import in a visualization tool.
+
+If an instance of [VictoriaMetrics](https://docs.victoriametrics.com/) listening at `http://localhost:8428` is used, a metrics dump can be imported like:
+
+```bash
```

  We are likely to use it for checking performance with and without fair sharing. 
  
  In the future we may use it for asserting the scalability in CI after the test, based on PromQL queries.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T08:46:10Z

- **Makefile:234** by [@mimowo](https://github.com/mimowo) — 2024-04-23T08:46:10Z
```diff
@@ -230,6 +230,14 @@ ifdef SCALABILITY_KUEUE_LOGS
 SCALABILITY_EXTRA_ARGS +=  --withLogs=true --logToFile=true
 endif
 
+ifdef SCALABILITY_SCRAPE_INTERVAL
+SCALABILITY_SCRAPE_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
```

  > Passing potential cpu profile or logging params don't make seance in a run-in-cluster scenario, that is why we have two sets.
  
  I see, but then they could be just ignored by the script. It feels a complication to have `SCALABILITY_EXTRA_ARGS` and `SCALABILITY_SCRAPE_ARGS` at the top level Makefile.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T08:47:08Z

- **test/scalability/README.md:76** by [@trasc](https://github.com/trasc) — 2024-04-23T08:47:08Z
```diff
@@ -66,3 +66,17 @@ make test-scalability
 ```
 
 Runs the scalability with minimalkueue and checks the results against `$(PROJECT_DIR)/test/scalability/default_rangespec.yaml`
+
+## Scrape result
+
+The scrape result `metricsDump.tgz` contains a set of `<ts>.prometheus` files, where `ts` is the millisecond representation of the epoch time at the moment each scrape was stared and can be used during the import in a visualization tool.
+
+If an instance of [VictoriaMetrics](https://docs.victoriametrics.com/) listening at `http://localhost:8428` is used, a metrics dump can be imported like:
+
+```bash
```

  In that future scenario it could be added, probably,  but for now it's not necessary.
  (we are talking about 4 lines, two of which are more or less optional)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T08:47:53Z

- **Makefile:234** by [@trasc](https://github.com/trasc) — 2024-04-23T08:47:53Z
```diff
@@ -230,6 +230,14 @@ ifdef SCALABILITY_KUEUE_LOGS
 SCALABILITY_EXTRA_ARGS +=  --withLogs=true --logToFile=true
 endif
 
+ifdef SCALABILITY_SCRAPE_INTERVAL
+SCALABILITY_SCRAPE_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
```

  which script?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T08:49:50Z

- **test/scalability/README.md:76** by [@mimowo](https://github.com/mimowo) — 2024-04-23T08:49:50Z
```diff
@@ -66,3 +66,17 @@ make test-scalability
 ```
 
 Runs the scalability with minimalkueue and checks the results against `$(PROJECT_DIR)/test/scalability/default_rangespec.yaml`
+
+## Scrape result
+
+The scrape result `metricsDump.tgz` contains a set of `<ts>.prometheus` files, where `ts` is the millisecond representation of the epoch time at the moment each scrape was stared and can be used during the import in a visualization tool.
+
+If an instance of [VictoriaMetrics](https://docs.victoriametrics.com/) listening at `http://localhost:8428` is used, a metrics dump can be imported like:
+
+```bash
```

  ok, we can move it as future work, and for manual experimentation it is easy to copy-paste if needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T08:50:15Z

- **Makefile:234** by [@mimowo](https://github.com/mimowo) — 2024-04-23T08:50:14Z
```diff
@@ -230,6 +230,14 @@ ifdef SCALABILITY_KUEUE_LOGS
 SCALABILITY_EXTRA_ARGS +=  --withLogs=true --logToFile=true
 endif
 
+ifdef SCALABILITY_SCRAPE_INTERVAL
+SCALABILITY_SCRAPE_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
```

  I meant the scalability runner

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T08:56:26Z

- **Makefile:234** by [@trasc](https://github.com/trasc) — 2024-04-23T08:56:26Z
```diff
@@ -230,6 +230,14 @@ ifdef SCALABILITY_KUEUE_LOGS
 SCALABILITY_EXTRA_ARGS +=  --withLogs=true --logToFile=true
 endif
 
+ifdef SCALABILITY_SCRAPE_INTERVAL
+SCALABILITY_SCRAPE_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
```

  Why mix them here and extract them afterwords when it's that easy to keep the separated?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T09:01:00Z

- **Makefile:234** by [@mimowo](https://github.com/mimowo) — 2024-04-23T09:01:00Z
```diff
@@ -230,6 +230,14 @@ ifdef SCALABILITY_KUEUE_LOGS
 SCALABILITY_EXTRA_ARGS +=  --withLogs=true --logToFile=true
 endif
 
+ifdef SCALABILITY_SCRAPE_INTERVAL
+SCALABILITY_SCRAPE_ARGS +=  --metricsScrapeInterval=$(SCALABILITY_SCRAPE_INTERVAL)
```

  Readability only, I was a little surprised and it wasn't immediately obvious why we have both `SCALABILITY_SCRAPE_ARGS` and `SCALABILITY_EXTRA_ARGS`, because the scrape args are "extra". 
  
  Anyway, this is not a strong opinion, I'm ok as is if this is your preference, I see the reasoning now.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-23T18:48:19Z

- **test/scalability/runner/main.go:203** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T18:40:42Z
```diff
@@ -170,10 +186,21 @@ func main() {
 		os.Exit(1)
 	}
 
+	if *metricsScrapeInterval != 0 && *metricsScrapeURL != "" {
+		dumpTar := path.Join(*outputDir, "metricsDump.tgz")
+		wg.Add(1)
+		err := runScraper(ctx, *metricsScrapeInterval, dumpTar, *metricsScrapeURL, errCh, wg)
+		if err != nil {
+			log.Error(err, "Scraper start")
+			os.Exit(1)
+		}
+
+	}
+
 	wg.Add(1)
 	err = runManager(ctx, cfg, errCh, wg, recorder)
 	if err != nil {
-		log.Error(err, "manager start")
+		log.Error(err, "Manager start")
```

  ```suggestion
  		log.Error(err, "Failed to start manager")
  ```

- **test/scalability/runner/main.go:191** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T18:45:18Z
```diff
@@ -170,10 +186,21 @@ func main() {
 		os.Exit(1)
 	}
 
+	if *metricsScrapeInterval != 0 && *metricsScrapeURL != "" {
+		dumpTar := path.Join(*outputDir, "metricsDump.tgz")
+		wg.Add(1)
```

  why this one? The routine that runScraper launches already increments the wg.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-24T05:45:34Z

- **test/scalability/runner/main.go:203** by [@trasc](https://github.com/trasc) — 2024-04-24T05:17:41Z
```diff
@@ -170,10 +186,21 @@ func main() {
 		os.Exit(1)
 	}
 
+	if *metricsScrapeInterval != 0 && *metricsScrapeURL != "" {
+		dumpTar := path.Join(*outputDir, "metricsDump.tgz")
+		wg.Add(1)
+		err := runScraper(ctx, *metricsScrapeInterval, dumpTar, *metricsScrapeURL, errCh, wg)
+		if err != nil {
+			log.Error(err, "Scraper start")
+			os.Exit(1)
+		}
+
+	}
+
 	wg.Add(1)
 	err = runManager(ctx, cfg, errCh, wg, recorder)
 	if err != nil {
-		log.Error(err, "manager start")
+		log.Error(err, "Manager start")
```

  done

- **test/scalability/runner/main.go:191** by [@trasc](https://github.com/trasc) — 2024-04-24T05:25:14Z
```diff
@@ -170,10 +186,21 @@ func main() {
 		os.Exit(1)
 	}
 
+	if *metricsScrapeInterval != 0 && *metricsScrapeURL != "" {
+		dumpTar := path.Join(*outputDir, "metricsDump.tgz")
+		wg.Add(1)
```

  done
