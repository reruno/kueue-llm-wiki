# PR #202: introduce Heap package

**Summary**: introduce Heap package

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/202

**Last updated**: 2022-04-15T14:13:29Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feature/introduce-heap-package` → `main`
- **Created**: 2022-04-11T12:50:41Z
- **Updated**: 2022-04-15T14:13:29Z
- **Merged**: 2022-04-15T14:13:11Z
- **Closed**: 2022-04-15T14:13:11Z
- **Labels**: `kind/feature`, `lgtm`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 7   **+521 / -107**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-11T12:50:49Z

Hi @kerthcet. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-11T12:52:51Z

/ok-to-test

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-11T13:09:17Z

/retest

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-11T13:15:05Z

/retest

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T01:51:56Z

addressed all the comments. cc @alculquicondor

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-15T05:41:18Z

> few nits, looks good to me otherwise

Addressed all the comments.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T14:04:02Z

squash pls

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-15T14:10:00Z

squashed.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T14:11:52Z

/approve
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-15T14:12:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/202#issuecomment-1100132803" title="LGTM">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/202#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-15T14:13:26Z

I'll add more tests to cover this at higher cluster queue implement level then.

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-12T17:52:20Z

- **pkg/util/heap/heap.go:19** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-11T13:42:16Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
```

  Add comment:
  
  ```golang
  // heap.Interface implementation inspired by
  // https://github.com/kubernetes/kubernetes/blob/master/pkg/scheduler/internal/heap/heap.go
  ```

- **pkg/util/heap/heap_test.go:2** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:26:50Z
```diff
@@ -0,0 +1,269 @@
+/*
+Copyright 2018 The Kubernetes Authors.
```

  2022

- **pkg/queue/cluster_queue_impl.go:23** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:27:34Z
```diff
@@ -17,11 +17,10 @@ limitations under the License.
 package queue
 
 import (
-	"container/heap"
-
 	"k8s.io/apimachinery/pkg/util/sets"
 
 	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	utilheap "sigs.k8s.io/kueue/pkg/util/heap"
```

  just `heap`

- **pkg/util/heap/heap.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:28:33Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+
+	return nil
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool, err error) {
```

  The error is always null, just return the boolean.

- **pkg/util/heap/heap.go:184** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:29:58Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+
+	return nil
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool, err error) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false, nil
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true, nil
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	item, ok := h.data.items[key]
+	if !ok {
+		return fmt.Errorf("object not found")
+	}
+
+	heap.Remove(h.data, item.index)
+	return nil
+}
+
+// Pop returns the head of the heap and removes it.
+func (h *Heap) Pop() (interface{}, error) {
+	obj := heap.Pop(h.data)
+	if obj == nil {
+		return nil, fmt.Errorf("object was already removed from heap data")
+	}
+	return obj, nil
+}
+
+// Peek returns the head of the heap without removing it.
+func (h *Heap) Peek() interface{} {
+	return h.data.Peek()
+}
+
+// Get returns the requested item, exists, error.
+func (h *Heap) Get(obj interface{}) (item interface{}, exists bool) {
+	key := h.data.keyFunc(obj)
+	return h.GetByKey(key)
+}
+
+// GetByKey returns the requested item, or sets exists=false.
+func (h *Heap) GetByKey(key string) (interface{}, bool) {
+	item, exists := h.data.items[key]
+	if !exists {
+		return nil, false
+	}
+	return item.obj, true
+}
+
+// Len returns the number of items in the heap.
+func (h *Heap) Len() int {
+	return len(h.data.keys)
+}
+
+// List returns a list of all the items.
+func (h *Heap) List() []interface{} {
+	list := make([]interface{}, 0, h.Len())
+	for _, item := range h.data.items {
+		list = append(list, item.obj)
+	}
+	return list
+}
+
+// New returns a Heap which can be used to queue up items to process.
+func New(keyFn keyFunc, lessFn lessFunc) *Heap {
+	return &Heap{
+		data: &data{
```

  does it need to be a pointer?

- **pkg/util/heap/heap.go:110** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:45:04Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
```

  Any particular reason why you added this now?

- **pkg/util/heap/heap.go:123** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:46:11Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
```

  if there is no error case, don't return anything.

- **pkg/queue/cluster_queue_impl.go:81** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:47:43Z
```diff
@@ -66,30 +65,21 @@ func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
 // pushIfNotPresent pushes the workload to ClusterQueue.
 // If the workload is already present, returns false. Otherwise returns true.
 func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	item := c.heap.items[workload.Key(info.Obj)]
-	if item != nil {
+	added, err := c.heap.AddIfNotPresent(info)
+	if err != nil {
 		return false
 	}
-	heap.Push(&c.heap, *info)
-	return true
+	return added
 }
 
 func (c *ClusterQueueImpl) PushOrUpdate(w *kueue.Workload) {
-	item := c.heap.items[workload.Key(w)]
-	info := *workload.NewInfo(w)
-	if item == nil {
-		heap.Push(&c.heap, info)
-		return
-	}
-	item.obj = info
-	heap.Fix(&c.heap, item.index)
+	info := workload.NewInfo(w)
+	_ = c.heap.AddOrUpdate(info)
 }
 
 func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	item := c.heap.items[workload.Key(w)]
-	if item != nil {
-		heap.Remove(&c.heap, item.index)
-	}
+	info := workload.NewInfo(w)
```

  Creating this object is not cheap. Can you remove using the key instead?

- **pkg/util/heap/heap.go:148** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:48:10Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+
+	return nil
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool, err error) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false, nil
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true, nil
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(obj interface{}) error {
```

  if we are not going to use the error, don't return it.

- **pkg/util/heap/heap.go:173** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:49:12Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+
+	return nil
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool, err error) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false, nil
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true, nil
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	item, ok := h.data.items[key]
+	if !ok {
+		return fmt.Errorf("object not found")
+	}
+
+	heap.Remove(h.data, item.index)
+	return nil
+}
+
+// Pop returns the head of the heap and removes it.
+func (h *Heap) Pop() (interface{}, error) {
+	obj := heap.Pop(h.data)
+	if obj == nil {
+		return nil, fmt.Errorf("object was already removed from heap data")
+	}
+	return obj, nil
+}
+
+// Peek returns the head of the heap without removing it.
+func (h *Heap) Peek() interface{} {
+	return h.data.Peek()
+}
+
+// Get returns the requested item, exists, error.
+func (h *Heap) Get(obj interface{}) (item interface{}, exists bool) {
```

  Is this used?

- **pkg/util/heap/heap.go:190** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:49:49Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+
+	return nil
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool, err error) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false, nil
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true, nil
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	item, ok := h.data.items[key]
+	if !ok {
+		return fmt.Errorf("object not found")
+	}
+
+	heap.Remove(h.data, item.index)
+	return nil
+}
+
+// Pop returns the head of the heap and removes it.
+func (h *Heap) Pop() (interface{}, error) {
+	obj := heap.Pop(h.data)
+	if obj == nil {
+		return nil, fmt.Errorf("object was already removed from heap data")
+	}
+	return obj, nil
+}
+
+// Peek returns the head of the heap without removing it.
+func (h *Heap) Peek() interface{} {
+	return h.data.Peek()
+}
+
+// Get returns the requested item, exists, error.
+func (h *Heap) Get(obj interface{}) (item interface{}, exists bool) {
+	key := h.data.keyFunc(obj)
+	return h.GetByKey(key)
+}
+
+// GetByKey returns the requested item, or sets exists=false.
+func (h *Heap) GetByKey(key string) (interface{}, bool) {
+	item, exists := h.data.items[key]
+	if !exists {
+		return nil, false
+	}
+	return item.obj, true
+}
+
+// Len returns the number of items in the heap.
+func (h *Heap) Len() int {
+	return len(h.data.keys)
```

  ```suggestion
  	return h.data.Len()
  ```

- **pkg/workload/workload.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:51:44Z
```diff
@@ -38,6 +38,15 @@ type Info struct {
 	ClusterQueue string
 }
 
+func KeyFunc(obj interface{}) string {
```

  this should be part of the `queue` package

- **test/integration/scheduler/scheduler_test.go:26** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T17:52:15Z
```diff
@@ -23,10 +23,10 @@ import (
 	corev1 "k8s.io/api/core/v1"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
-	"k8s.io/utils/pointer"
-	"sigs.k8s.io/controller-runtime/pkg/client"
 
+	"sigs.k8s.io/controller-runtime/pkg/client"
```

  move this one to the previous import block

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T00:48:26Z

- **pkg/util/heap/heap.go:110** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T00:48:26Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
```

  It's a common method for heap AFAIK.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T01:00:23Z

- **pkg/util/heap/heap.go:173** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T01:00:23Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+
+	return nil
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool, err error) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false, nil
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true, nil
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	item, ok := h.data.items[key]
+	if !ok {
+		return fmt.Errorf("object not found")
+	}
+
+	heap.Remove(h.data, item.index)
+	return nil
+}
+
+// Pop returns the head of the heap and removes it.
+func (h *Heap) Pop() (interface{}, error) {
+	obj := heap.Pop(h.data)
+	if obj == nil {
+		return nil, fmt.Errorf("object was already removed from heap data")
+	}
+	return obj, nil
+}
+
+// Peek returns the head of the heap without removing it.
+func (h *Heap) Peek() interface{} {
+	return h.data.Peek()
+}
+
+// Get returns the requested item, exists, error.
+func (h *Heap) Get(obj interface{}) (item interface{}, exists bool) {
```

  Just in tests now. Considering this is an util package, I think it makes sense somehow.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T01:16:07Z

- **pkg/util/heap/heap.go:184** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T01:16:07Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+
+	return nil
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool, err error) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false, nil
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true, nil
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	item, ok := h.data.items[key]
+	if !ok {
+		return fmt.Errorf("object not found")
+	}
+
+	heap.Remove(h.data, item.index)
+	return nil
+}
+
+// Pop returns the head of the heap and removes it.
+func (h *Heap) Pop() (interface{}, error) {
+	obj := heap.Pop(h.data)
+	if obj == nil {
+		return nil, fmt.Errorf("object was already removed from heap data")
+	}
+	return obj, nil
+}
+
+// Peek returns the head of the heap without removing it.
+func (h *Heap) Peek() interface{} {
+	return h.data.Peek()
+}
+
+// Get returns the requested item, exists, error.
+func (h *Heap) Get(obj interface{}) (item interface{}, exists bool) {
+	key := h.data.keyFunc(obj)
+	return h.GetByKey(key)
+}
+
+// GetByKey returns the requested item, or sets exists=false.
+func (h *Heap) GetByKey(key string) (interface{}, bool) {
+	item, exists := h.data.items[key]
+	if !exists {
+		return nil, false
+	}
+	return item.obj, true
+}
+
+// Len returns the number of items in the heap.
+func (h *Heap) Len() int {
+	return len(h.data.keys)
+}
+
+// List returns a list of all the items.
+func (h *Heap) List() []interface{} {
+	list := make([]interface{}, 0, h.Len())
+	for _, item := range h.data.items {
+		list = append(list, item.obj)
+	}
+	return list
+}
+
+// New returns a Heap which can be used to queue up items to process.
+func New(keyFn keyFunc, lessFn lessFunc) *Heap {
+	return &Heap{
+		data: &data{
```

  nope, but the operation of native heap is always need a pointer, like `heap.Fix(&data, i)`, so I prefer pointer here.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T01:19:04Z

- **pkg/queue/cluster_queue_impl.go:81** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T01:19:04Z
```diff
@@ -66,30 +65,21 @@ func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
 // pushIfNotPresent pushes the workload to ClusterQueue.
 // If the workload is already present, returns false. Otherwise returns true.
 func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	item := c.heap.items[workload.Key(info.Obj)]
-	if item != nil {
+	added, err := c.heap.AddIfNotPresent(info)
+	if err != nil {
 		return false
 	}
-	heap.Push(&c.heap, *info)
-	return true
+	return added
 }
 
 func (c *ClusterQueueImpl) PushOrUpdate(w *kueue.Workload) {
-	item := c.heap.items[workload.Key(w)]
-	info := *workload.NewInfo(w)
-	if item == nil {
-		heap.Push(&c.heap, info)
-		return
-	}
-	item.obj = info
-	heap.Fix(&c.heap, item.index)
+	info := workload.NewInfo(w)
+	_ = c.heap.AddOrUpdate(info)
 }
 
 func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	item := c.heap.items[workload.Key(w)]
-	if item != nil {
-		heap.Remove(&c.heap, item.index)
-	}
+	info := workload.NewInfo(w)
```

  good point!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-13T13:26:33Z

- **pkg/queue/cluster_queue_impl.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:10:28Z
```diff
@@ -32,10 +31,16 @@ type ClusterQueueImpl struct {
 	// across the queues in this ClusterQueue.
 	QueueingStrategy kueue.QueueingStrategy
 
-	heap   heapImpl
+	heap   *heap.Heap
```

  does it need to be a pointer?

- **pkg/queue/manager.go:445** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:14:05Z
```diff
@@ -442,6 +442,7 @@ func (m *Manager) heads() []workload.Info {
 	var workloads []workload.Info
 	for cqName, cq := range m.clusterQueues {
 		wl := cq.Pop()
+
```

  revert?

- **pkg/queue/queue.go:26** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:14:44Z
```diff
@@ -23,6 +23,11 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
+func KeyFunc(obj interface{}) string {
```

  It probably doesn't need to be exported?

- **pkg/scheduler/scheduler.go:84** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:17:18Z
```diff
@@ -81,6 +81,7 @@ func (s *Scheduler) schedule(ctx context.Context) {
 	// 1. Get the heads from the queues, including their desired clusterQueue.
 	// This operation blocks while the queues are empty.
 	headWorkloads := s.queues.Heads(ctx)
+
```

  revert

- **pkg/util/heap/heap.go:33** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:19:47Z
```diff
@@ -0,0 +1,210 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// heap.Interface implementation inspired by
+// https://github.com/kubernetes/kubernetes/blob/master/pkg/scheduler/internal/heap/heap.go
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
```

  maybe for a follow up: we could try using generics :smile: 
  
  https://go.dev/doc/tutorial/generics

- **pkg/util/heap/heap.go:110** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:20:42Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
```

  I would remove it if it's not being used.

- **pkg/util/heap/heap.go:179** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:21:32Z
```diff
@@ -0,0 +1,210 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// heap.Interface implementation inspired by
+// https://github.com/kubernetes/kubernetes/blob/master/pkg/scheduler/internal/heap/heap.go
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(key string) error {
+	item, ok := h.data.items[key]
+	if !ok {
+		return fmt.Errorf("object not found")
+	}
+
+	heap.Remove(h.data, item.index)
+	return nil
+}
+
+// Pop returns the head of the heap and removes it.
+func (h *Heap) Pop() (interface{}, error) {
+	obj := heap.Pop(h.data)
+	if obj == nil {
+		return nil, fmt.Errorf("object was already removed from heap data")
+	}
+	return obj, nil
+}
+
+// Peek returns the head of the heap without removing it.
+func (h *Heap) Peek() interface{} {
+	return h.data.Peek()
+}
+
+// Get returns the requested item, exists, error.
+func (h *Heap) Get(obj interface{}) (item interface{}, exists bool) {
+	key := h.data.keyFunc(obj)
+	return h.GetByKey(key)
+}
+
+// GetByKey returns the requested item, or sets exists=false.
+func (h *Heap) GetByKey(key string) (interface{}, bool) {
```

  We could just use `nil` if it doesn't exist.

- **pkg/util/heap/heap.go:159** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:22:19Z
```diff
@@ -0,0 +1,210 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// heap.Interface implementation inspired by
+// https://github.com/kubernetes/kubernetes/blob/master/pkg/scheduler/internal/heap/heap.go
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(key string) error {
+	item, ok := h.data.items[key]
+	if !ok {
+		return fmt.Errorf("object not found")
+	}
+
+	heap.Remove(h.data, item.index)
+	return nil
+}
+
+// Pop returns the head of the heap and removes it.
+func (h *Heap) Pop() (interface{}, error) {
```

  let's just use `nil` to indicate that the heap is empty.

- **pkg/util/heap/heap_test.go:196** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:25:56Z
```diff
@@ -0,0 +1,269 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// This file was copied from client-go/tools/cache/heap.go and modified
+// for our non thread-safe heap
+
+package heap
+
+import (
+	"testing"
+)
+
+func testHeapObjectKeyFunc(obj interface{}) string {
+	return obj.(testHeapObject).name
+}
+
+type testHeapObject struct {
+	name string
+	val  interface{}
+}
+
+func mkHeapObj(name string, val interface{}) testHeapObject {
+	return testHeapObject{name: name, val: val}
+}
+
+func compareInts(val1 interface{}, val2 interface{}) bool {
+	first := val1.(testHeapObject).val.(int)
+	second := val2.(testHeapObject).val.(int)
+	return first < second
+}
+
+// TestHeapBasic tests Heap invariant
+func TestHeapBasic(t *testing.T) {
+	h := New(testHeapObjectKeyFunc, compareInts)
+	const amount = 500
+	var i int
+
+	for i = amount; i > 0; i-- {
+		h.AddOrUpdate(mkHeapObj(string([]rune{'a', rune(i)}), i))
+	}
+
+	// Make sure that the numbers are popped in ascending order.
+	prevNum := 0
+	for i := 0; i < amount; i++ {
+		obj, err := h.Pop()
+		num := obj.(testHeapObject).val.(int)
+		// All the items must be sorted.
+		if err != nil || prevNum > num {
+			t.Errorf("got %v out of order, last was %v", obj, prevNum)
+		}
+		prevNum = num
+	}
+}
+
+// Tests Heap.Add and ensures that heap invariant is preserved after adding items.
+func TestHeap_Add(t *testing.T) {
+	h := New(testHeapObjectKeyFunc, compareInts)
+	h.AddOrUpdate(mkHeapObj("foo", 10))
+	h.AddOrUpdate(mkHeapObj("bar", 1))
+	h.AddOrUpdate(mkHeapObj("baz", 11))
+	h.AddOrUpdate(mkHeapObj("zab", 30))
+	h.AddOrUpdate(mkHeapObj("foo", 13)) // This updates "foo".
+
+	item, err := h.Pop()
+	if e, a := 1, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+	item, err = h.Pop()
+	if e, a := 11, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+	_ = h.Delete("baz")                 // Nothing is deleted.
+	h.AddOrUpdate(mkHeapObj("foo", 14)) // foo is updated.
+	item, err = h.Pop()
+	if e, a := 14, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+	item, err = h.Pop()
+	if e, a := 30, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+}
+
+// TestHeap_AddIfNotPresent tests Heap.AddIfNotPresent and ensures that heap
+// invariant is preserved after adding items.
+func TestHeap_AddIfNotPresent(t *testing.T) {
+	h := New(testHeapObjectKeyFunc, compareInts)
+	_ = h.AddIfNotPresent(mkHeapObj("foo", 10))
+	_ = h.AddIfNotPresent(mkHeapObj("bar", 1))
+	_ = h.AddIfNotPresent(mkHeapObj("baz", 11))
+	_ = h.AddIfNotPresent(mkHeapObj("zab", 30))
+	_ = h.AddIfNotPresent(mkHeapObj("foo", 13)) // This is not added.
+
+	if len := len(h.data.items); len != 4 {
+		t.Errorf("unexpected number of items: %d", len)
+	}
+	if val := h.data.items["foo"].obj.(testHeapObject).val; val != 10 {
+		t.Errorf("unexpected value: %d", val)
+	}
+	item, err := h.Pop()
+	if e, a := 1, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+	item, err = h.Pop()
+	if e, a := 10, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+	// bar is already popped. Let's add another one.
+	_ = h.AddIfNotPresent(mkHeapObj("bar", 14))
+	item, err = h.Pop()
+	if e, a := 11, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+	item, err = h.Pop()
+	if e, a := 14, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+}
+
+// TestHeap_Delete tests Heap.Delete and ensures that heap invariant is
+// preserved after deleting items.
+func TestHeap_Delete(t *testing.T) {
+	h := New(testHeapObjectKeyFunc, compareInts)
+	h.AddOrUpdate(mkHeapObj("foo", 10))
+	h.AddOrUpdate(mkHeapObj("bar", 1))
+	h.AddOrUpdate(mkHeapObj("bal", 31))
+	h.AddOrUpdate(mkHeapObj("baz", 11))
+
+	// Delete head. Delete should work with "key" and doesn't care about the value.
+	if err := h.Delete("bar"); err != nil {
+		t.Fatalf("Failed to delete head.")
+	}
+	item, err := h.Pop()
+	if e, a := 10, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+	h.AddOrUpdate(mkHeapObj("zab", 30))
+	h.AddOrUpdate(mkHeapObj("faz", 30))
+	len := h.data.Len()
+	// Delete non-existing item.
+	if err = h.Delete("non-existent"); err == nil || len != h.data.Len() {
+		t.Fatalf("Didn't expect any item removal")
+	}
+	// Delete tail.
+	if err = h.Delete("bal"); err != nil {
+		t.Fatalf("Failed to delete tail.")
+	}
+	// Delete one of the items with value 30.
+	if err = h.Delete("zab"); err != nil {
+		t.Fatalf("Failed to delete item.")
+	}
+	item, err = h.Pop()
+	if e, a := 11, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+	item, err = h.Pop()
+	if e, a := 30, item.(testHeapObject).val; err != nil || a != e {
+		t.Fatalf("expected %d, got %d", e, a)
+	}
+	if h.data.Len() != 0 {
+		t.Fatalf("expected an empty heap.")
+	}
+}
+
+// TestHeap_Update tests Heap.Update and ensures that heap invariant is
+// preserved after adding items.
+func TestHeap_Update(t *testing.T) {
```

  Just have one test for AddOrUpdate.

- **pkg/workload/workload.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T13:26:27Z
```diff
@@ -38,6 +38,10 @@ type Info struct {
 	ClusterQueue string
 }
 
+func Key(w *kueue.Workload) string {
```

  revert the move.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T17:18:42Z

- **pkg/util/heap/heap.go:33** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T16:14:03Z
```diff
@@ -0,0 +1,210 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// heap.Interface implementation inspired by
+// https://github.com/kubernetes/kubernetes/blob/master/pkg/scheduler/internal/heap/heap.go
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
```

  create a issue  here https://github.com/kubernetes-sigs/kueue/issues/217 for the first step

- **pkg/queue/cluster_queue_impl.go:34** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T17:14:17Z
```diff
@@ -32,10 +31,16 @@ type ClusterQueueImpl struct {
 	// across the queues in this ClusterQueue.
 	QueueingStrategy kueue.QueueingStrategy
 
-	heap   heapImpl
+	heap   *heap.Heap
```

  no, just personal prefer.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-13T17:21:20Z

- **pkg/queue/cluster_queue_impl.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T17:21:20Z
```diff
@@ -32,10 +31,16 @@ type ClusterQueueImpl struct {
 	// across the queues in this ClusterQueue.
 	QueueingStrategy kueue.QueueingStrategy
 
-	heap   heapImpl
+	heap   *heap.Heap
```

  In golang, you should prefer values instead of pointers wherever possible, to reduce work for the GC.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-13T17:31:55Z

- **pkg/queue/cluster_queue_impl.go:83** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-13T17:30:45Z
```diff
@@ -66,30 +71,16 @@ func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
 // pushIfNotPresent pushes the workload to ClusterQueue.
 // If the workload is already present, returns false. Otherwise returns true.
 func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	item := c.heap.items[workload.Key(info.Obj)]
-	if item != nil {
-		return false
-	}
-	heap.Push(&c.heap, *info)
-	return true
+	return c.heap.AddIfNotPresent(info)
 }
 
 func (c *ClusterQueueImpl) PushOrUpdate(w *kueue.Workload) {
-	item := c.heap.items[workload.Key(w)]
-	info := *workload.NewInfo(w)
-	if item == nil {
-		heap.Push(&c.heap, info)
-		return
-	}
-	item.obj = info
-	heap.Fix(&c.heap, item.index)
+	info := workload.NewInfo(w)
+	c.heap.AddOrUpdate(info)
 }
 
 func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	item := c.heap.items[workload.Key(w)]
-	if item != nil {
-		heap.Remove(&c.heap, item.index)
-	}
+	_ = c.heap.Delete(workload.Key(w))
```

  whenever you ignore an error, explain why. But I think we should remove the return value.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-14T08:12:35Z

- **pkg/queue/cluster_queue_impl.go:34** by [@kerthcet](https://github.com/kerthcet) — 2022-04-14T08:12:35Z
```diff
@@ -32,10 +31,16 @@ type ClusterQueueImpl struct {
 	// across the queues in this ClusterQueue.
 	QueueingStrategy kueue.QueueingStrategy
 
-	heap   heapImpl
+	heap   *heap.Heap
```

  Changed to struct.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-14T08:19:46Z

- **pkg/queue/cluster_queue_impl.go:83** by [@kerthcet](https://github.com/kerthcet) — 2022-04-14T08:19:46Z
```diff
@@ -66,30 +71,16 @@ func (c *ClusterQueueImpl) DeleteFromQueue(q *Queue) {
 // pushIfNotPresent pushes the workload to ClusterQueue.
 // If the workload is already present, returns false. Otherwise returns true.
 func (c *ClusterQueueImpl) pushIfNotPresent(info *workload.Info) bool {
-	item := c.heap.items[workload.Key(info.Obj)]
-	if item != nil {
-		return false
-	}
-	heap.Push(&c.heap, *info)
-	return true
+	return c.heap.AddIfNotPresent(info)
 }
 
 func (c *ClusterQueueImpl) PushOrUpdate(w *kueue.Workload) {
-	item := c.heap.items[workload.Key(w)]
-	info := *workload.NewInfo(w)
-	if item == nil {
-		heap.Push(&c.heap, info)
-		return
-	}
-	item.obj = info
-	heap.Fix(&c.heap, item.index)
+	info := workload.NewInfo(w)
+	c.heap.AddOrUpdate(info)
 }
 
 func (c *ClusterQueueImpl) Delete(w *kueue.Workload) {
-	item := c.heap.items[workload.Key(w)]
-	if item != nil {
-		heap.Remove(&c.heap, item.index)
-	}
+	_ = c.heap.Delete(workload.Key(w))
```

  updated as advised.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-14T20:46:38Z

few nits, looks good to me otherwise

- **pkg/queue/cluster_queue_impl.go:104** by [@ahg-g](https://github.com/ahg-g) — 2022-04-14T20:35:32Z
```diff
@@ -104,29 +95,35 @@ func (c *ClusterQueueImpl) Pop() *workload.Info {
 	if c.heap.Len() == 0 {
 		return nil
 	}
-	w := heap.Pop(&c.heap).(workload.Info)
-	return &w
+
+	info := c.heap.Pop()
+	if info == nil {
+		return nil
+	}
+	a := info.(*workload.Info)
+	return a
```

  ```suggestion
  	return info.(*workload.Info)
  ```

- **pkg/util/heap/heap.go:128** by [@ahg-g](https://github.com/ahg-g) — 2022-04-14T20:40:39Z
```diff
@@ -0,0 +1,190 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// heap.Interface implementation inspired by
+// https://github.com/kubernetes/kubernetes/blob/master/pkg/scheduler/internal/heap/heap.go
+package heap
+
+import (
+	"container/heap"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool) {
```

  same thing here, pls rename to `PushIfNotPresent` so we are consistent with `Pop` and the queue functions what are also named `PushIfNotPresent`

- **pkg/util/heap/heap.go:116** by [@ahg-g](https://github.com/ahg-g) — 2022-04-14T20:43:53Z
```diff
@@ -0,0 +1,190 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// heap.Interface implementation inspired by
+// https://github.com/kubernetes/kubernetes/blob/master/pkg/scheduler/internal/heap/heap.go
+package heap
+
+import (
+	"container/heap"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) {
```

  pls rename to `PushOrUpdate` so we are consistent with `Pop` and the queue functions what are also named `PushOrUpdate`

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-15T02:10:29Z

- **pkg/util/heap/heap.go:116** by [@kerthcet](https://github.com/kerthcet) — 2022-04-15T02:10:28Z
```diff
@@ -0,0 +1,190 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// heap.Interface implementation inspired by
+// https://github.com/kubernetes/kubernetes/blob/master/pkg/scheduler/internal/heap/heap.go
+package heap
+
+import (
+	"container/heap"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) {
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-15T02:26:07Z

- **pkg/util/heap/heap.go:184** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T02:26:07Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+
+	return nil
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool, err error) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false, nil
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true, nil
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	item, ok := h.data.items[key]
+	if !ok {
+		return fmt.Errorf("object not found")
+	}
+
+	heap.Remove(h.data, item.index)
+	return nil
+}
+
+// Pop returns the head of the heap and removes it.
+func (h *Heap) Pop() (interface{}, error) {
+	obj := heap.Pop(h.data)
+	if obj == nil {
+		return nil, fmt.Errorf("object was already removed from heap data")
+	}
+	return obj, nil
+}
+
+// Peek returns the head of the heap without removing it.
+func (h *Heap) Peek() interface{} {
+	return h.data.Peek()
+}
+
+// Get returns the requested item, exists, error.
+func (h *Heap) Get(obj interface{}) (item interface{}, exists bool) {
+	key := h.data.keyFunc(obj)
+	return h.GetByKey(key)
+}
+
+// GetByKey returns the requested item, or sets exists=false.
+func (h *Heap) GetByKey(key string) (interface{}, bool) {
+	item, exists := h.data.items[key]
+	if !exists {
+		return nil, false
+	}
+	return item.obj, true
+}
+
+// Len returns the number of items in the heap.
+func (h *Heap) Len() int {
+	return len(h.data.keys)
+}
+
+// List returns a list of all the items.
+func (h *Heap) List() []interface{} {
+	list := make([]interface{}, 0, h.Len())
+	for _, item := range h.data.items {
+		list = append(list, item.obj)
+	}
+	return list
+}
+
+// New returns a Heap which can be used to queue up items to process.
+func New(keyFn keyFunc, lessFn lessFunc) *Heap {
+	return &Heap{
+		data: &data{
```

  similar to the argument below, avoid using pointers if not needed for performance reasons. There is no issue with doing `heap.Fix(&data, i)`

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-15T05:40:39Z

- **pkg/util/heap/heap.go:184** by [@kerthcet](https://github.com/kerthcet) — 2022-04-15T05:40:38Z
```diff
@@ -0,0 +1,211 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package heap
+
+import (
+	"container/heap"
+	"fmt"
+)
+
+// lessFunc is a function that receives two items and returns true if the first
+// item should be placed before the second one when the list is sorted.
+type lessFunc func(a, b interface{}) bool
+
+// KeyFunc is a function type to get the key from an object.
+type keyFunc func(obj interface{}) string
+
+type heapItem struct {
+	obj   interface{}
+	index int
+}
+
+type itemKeyValue struct {
+	key string
+	obj interface{}
+}
+
+// data is an internal struct that implements the standard heap interface
+// and keeps the data stored in the heap.
+type data struct {
+	// items is a map from key of the objects to the objects and their index
+	items map[string]*heapItem
+	// keys keeps the keys of the objects ordered according to the heap invariant.
+	keys     []string
+	keyFunc  keyFunc
+	lessFunc lessFunc
+}
+
+var _ = heap.Interface(&data{})
+
+// Less compares two objects and returns true if the first one should go
+// in front of the second one in the heap.
+func (h *data) Less(i, j int) bool {
+	if i > h.Len() || j > h.Len() {
+		return false
+	}
+	a, ok := h.items[h.keys[i]]
+	if !ok {
+		return false
+	}
+	b, ok := h.items[h.keys[j]]
+	if !ok {
+		return false
+	}
+	return h.lessFunc(a.obj, b.obj)
+}
+
+// Len returns the number of items in the Heap.
+func (h *data) Len() int {
+	return len(h.keys)
+}
+
+// Swap implements swapping of two elements in the heap. This is a part of standard
+// heap interface and should never be called directly.
+func (h *data) Swap(i, j int) {
+	h.keys[i], h.keys[j] = h.keys[j], h.keys[i]
+	h.items[h.keys[i]].index = i
+	h.items[h.keys[j]].index = j
+}
+
+// Push is supposed to be called by heap.Push only.
+func (h *data) Push(kv interface{}) {
+	keyValue := kv.(*itemKeyValue)
+	h.items[keyValue.key] = &heapItem{
+		obj:   keyValue.obj,
+		index: len(h.keys),
+	}
+	h.keys = append(h.keys, keyValue.key)
+}
+
+// Pop is supposed to be called by heap.Pop only.
+func (h *data) Pop() interface{} {
+	key := h.keys[len(h.keys)-1]
+	h.keys = h.keys[:len(h.keys)-1]
+	item, ok := h.items[key]
+	if !ok {
+		// This is an error
+		return nil
+	}
+	delete(h.items, key)
+	return item.obj
+}
+
+// Peek is supposed to return the head of the heap
+func (h *data) Peek() interface{} {
+	if len(h.keys) > 0 {
+		return h.items[h.keys[0]].obj
+	}
+	return nil
+}
+
+// Heap is a producer/consumer queue that implements a heap data structure.
+// It can be used to implement priority queues and similar data structures.
+type Heap struct {
+	data *data
+}
+
+// AddOrUpdate inserts an item to the queue.
+// The item will be updated if it already exists.
+func (h *Heap) AddOrUpdate(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		h.data.items[key].obj = obj
+		heap.Fix(h.data, h.data.items[key].index)
+	} else {
+		heap.Push(h.data, &itemKeyValue{key, obj})
+	}
+
+	return nil
+}
+
+// AddIfNotPresent inserts an item to the queue. If an item with
+// the key is present in the map, no changes is made to the item.
+func (h *Heap) AddIfNotPresent(obj interface{}) (added bool, err error) {
+	key := h.data.keyFunc(obj)
+	if _, exists := h.data.items[key]; exists {
+		return false, nil
+	}
+
+	heap.Push(h.data, &itemKeyValue{key, obj})
+	return true, nil
+}
+
+// Delete removes an item.
+func (h *Heap) Delete(obj interface{}) error {
+	key := h.data.keyFunc(obj)
+	item, ok := h.data.items[key]
+	if !ok {
+		return fmt.Errorf("object not found")
+	}
+
+	heap.Remove(h.data, item.index)
+	return nil
+}
+
+// Pop returns the head of the heap and removes it.
+func (h *Heap) Pop() (interface{}, error) {
+	obj := heap.Pop(h.data)
+	if obj == nil {
+		return nil, fmt.Errorf("object was already removed from heap data")
+	}
+	return obj, nil
+}
+
+// Peek returns the head of the heap without removing it.
+func (h *Heap) Peek() interface{} {
+	return h.data.Peek()
+}
+
+// Get returns the requested item, exists, error.
+func (h *Heap) Get(obj interface{}) (item interface{}, exists bool) {
+	key := h.data.keyFunc(obj)
+	return h.GetByKey(key)
+}
+
+// GetByKey returns the requested item, or sets exists=false.
+func (h *Heap) GetByKey(key string) (interface{}, bool) {
+	item, exists := h.data.items[key]
+	if !exists {
+		return nil, false
+	}
+	return item.obj, true
+}
+
+// Len returns the number of items in the heap.
+func (h *Heap) Len() int {
+	return len(h.data.keys)
+}
+
+// List returns a list of all the items.
+func (h *Heap) List() []interface{} {
+	list := make([]interface{}, 0, h.Len())
+	for _, item := range h.data.items {
+		list = append(list, item.obj)
+	}
+	return list
+}
+
+// New returns a Heap which can be used to queue up items to process.
+func New(keyFn keyFunc, lessFn lessFunc) *Heap {
+	return &Heap{
+		data: &data{
```

  Updated.
