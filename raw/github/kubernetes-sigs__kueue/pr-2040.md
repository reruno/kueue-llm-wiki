# PR #2040: [WaitForPodsReady] Make requeue base delay confiurable

**Summary**: [WaitForPodsReady] Make requeue base delay confiurable

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2040

**Last updated**: 2024-05-29T14:53:56Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `make-requeue-configurable` → `main`
- **Created**: 2024-04-23T10:40:07Z
- **Updated**: 2024-05-29T14:53:56Z
- **Merged**: 2024-04-24T09:01:23Z
- **Closed**: 2024-04-24T09:01:23Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 13   **+144 / -115**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #2009

#### Special notes for your reviewer:

I'm yet wondering about making the exponent and jitter configurable, wdyt?

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Make the PodsReady base delay for requeuing configurable
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-23T10:40:09Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-23T10:40:25Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 86461d9520ab68a81cb926fa706fd95c749a5b4f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6628b7277f782e00082026df |

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-23T10:40:33Z

/test all

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-23T12:33:09Z

/assign @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-23T14:28:31Z

> Basically, lgtm
> Also could you implement validations here:

Added, thanks
/assign @alculquicondor

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-24T07:30:42Z

@mimowo Also, could you open another PR to update the document?
https://kueue.sigs.k8s.io/docs/tasks/manage/setup_sequential_admission/#enabling-waitforpodsready

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-24T07:32:02Z

> @mimowo Also, could you open another PR to update the document?
> https://kueue.sigs.k8s.io/docs/tasks/manage/setup_sequential_admission/#enabling-waitforpodsready

Sure, I will follow up on this.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-24T07:47:13Z

/test pull-kueue-test-e2e-main-1-29 
/test pull-kueue-test-scheduling-perf-main 
/test pull-kueue-test-unit-main
/test pull-kueue-verify-main
not sure why the tests are failed, they don't even seem started
![image](https://github.com/kubernetes-sigs/kueue/assets/10359181/c8a0584e-81f8-43fa-9fa5-882de399e3ed)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-24T08:02:50Z

LGTM label has been added.  <details>Git tree hash: 27145d6920748a7c15ef5d1b55a9fd434e10e386</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-24T08:03:00Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-24T08:03:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2040#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2040#issuecomment-2074327799" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-24T08:20:46Z

> /test pull-kueue-test-e2e-main-1-29 /test pull-kueue-test-scheduling-perf-main /test pull-kueue-test-unit-main /test pull-kueue-verify-main not sure why the tests are failed, they don't even seem started ![image](https://private-user-images.githubusercontent.com/10359181/325127781-c8a0584e-81f8-43fa-9fa5-882de399e3ed.png?jwt=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..Nplqit6S3nz7OOcH1dQut_CK0-CQBm_W3QZjVQmDSzI)

/test pull-kueue-verify-main

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-24T08:34:39Z

/test pull-kueue-build-image-main

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T12:37:32Z

- **apis/config/v1beta1/configuration_types.go:258** by [@mimowo](https://github.com/mimowo) — 2024-04-23T12:37:32Z
```diff
@@ -248,6 +249,13 @@ type RequeuingStrategy struct {
 	// Defaults to null.
 	// +optional
 	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BaseDelaySeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 10.
+	// +optional
+	BaseDelaySeconds *int32 `json:"baseDelaySeconds,omitempty"`
```

  I'm yet wondering about making the exponent and jitter configurable, wdyt @alculquicondor @tenzen-y?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-23T13:30:55Z

- **apis/config/v1beta1/configuration_types.go:258** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-23T13:29:10Z
```diff
@@ -248,6 +249,13 @@ type RequeuingStrategy struct {
 	// Defaults to null.
 	// +optional
 	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BaseDelaySeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 10.
+	// +optional
+	BaseDelaySeconds *int32 `json:"baseDelaySeconds,omitempty"`
```

  I prefer to keep this simple, but @mimowo and/or @alculquicondor have requests from your customers or you have concrete use cases, I'm ok with implementing them.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-23T13:39:47Z

Basically, lgtm
Also could you implement validations here: https://github.com/kubernetes-sigs/kueue/blob/92baacd06e54f57de85a15590a1780eb84455941/pkg/config/validation.go#L60

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T14:37:13Z

- **pkg/controller/core/core.go:36** by [@trasc](https://github.com/trasc) — 2024-04-23T14:36:11Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  I personally like the `Options` pattern better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T14:44:07Z

- **pkg/controller/core/core.go:36** by [@mimowo](https://github.com/mimowo) — 2024-04-23T14:44:07Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  Right, but the new parameter is not needed any longer since the configuration is not passed in ` cfg *configapi.Configuration`. I guess we could replace  ` cfg *configapi.Configuration` with `Options`, but it would be a lot of changes, so I wouldn't like to mix this into this PR.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-23T15:02:11Z

- **apis/config/v1beta1/configuration_types.go:258** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T15:02:11Z
```diff
@@ -248,6 +249,13 @@ type RequeuingStrategy struct {
 	// Defaults to null.
 	// +optional
 	BackoffLimitCount *int32 `json:"backoffLimitCount,omitempty"`
+
+	// BaseDelaySeconds defines the base for the exponential backoff for
+	// re-queuing an evicted workload.
+	//
+	// Defaults to 10.
+	// +optional
+	BaseDelaySeconds *int32 `json:"baseDelaySeconds,omitempty"`
```

  Definitely not the jitter. But I'm also ok leaving the exponent out for now.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T15:30:36Z

- **pkg/controller/core/core.go:36** by [@trasc](https://github.com/trasc) — 2024-04-23T15:30:36Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  for now changing `WithControllerRequeuingBaseDelaySeconds` to something like `WithControllerRequeuingBackoffBaseSeconds`  can work and we can do a clean-up to drop the cfg usage.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T15:43:26Z

- **pkg/controller/core/core.go:36** by [@mimowo](https://github.com/mimowo) — 2024-04-23T15:43:26Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  I see that, but the interim state of passing the params which are part of the cfg both ways would look weird. I'm happy to open a dedicated issue to do the refactoring at once.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T16:05:04Z

- **pkg/controller/core/core.go:36** by [@trasc](https://github.com/trasc) — 2024-04-23T16:05:04Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  If we agree we should go towards `Options` , why start with a step back?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T16:09:25Z

- **pkg/controller/core/core.go:36** by [@mimowo](https://github.com/mimowo) — 2024-04-23T16:09:25Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

   the interim state of passing the params which are part of the cfg both ways would look weird

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-23T16:13:33Z

- **pkg/controller/core/core.go:36** by [@mimowo](https://github.com/mimowo) — 2024-04-23T16:13:33Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  And also I'm not 100% percent sure it will be better. Someone had a reason to make it like that in the first place, so I think it will require an actual experiment to see if there are no unforeseen complications

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-23T17:05:37Z

- **pkg/controller/core/core.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T17:05:37Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  It looks like dropping cfg would require changing a lot of functions (all the SetupWithManager calls).
  It might be worth exploring, but I agree that it's too much for this PR.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-23T17:09:57Z

- **pkg/controller/core/core.go:72** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T17:09:57Z
```diff
@@ -78,19 +65,13 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	if err := cqRec.SetupWithManager(mgr, cfg); err != nil {
 		return "ClusterQueue", err
 	}
-	ctrlOpts := ControllerOptions{
-		requeuingBaseDelaySeconds: defaultRequeuingBaseDelaySeconds,
-	}
-	for _, opt := range controllerOpts {
-		opt(&ctrlOpts)
-	}
 
 	if err := NewWorkloadReconciler(mgr.GetClient(), qManager, cc,
 		mgr.GetEventRecorderFor(constants.WorkloadControllerName),
 		WithWorkloadUpdateWatchers(qRec, cqRec),
 		WithPodsReadyTimeout(podsReadyTimeout(cfg)),
```

  Maybe it would be worth something like:
  
  ```
  WithPodsReadyTimeout(cfg.WaitForPodsReady)
  ```
  
  So we don't need to keep adding options for every single field.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T17:10:43Z

- **pkg/controller/core/core.go:36** by [@trasc](https://github.com/trasc) — 2024-04-23T17:10:43Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  In this PR there is the option to pass the new param as option just like the old one was set.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-23T17:14:01Z

- **pkg/controller/core/core.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T17:14:01Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  Yes, but the cfg is already included.
  
  If options is the path forward, it's beyond the scope of this PR.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T17:22:05Z

- **pkg/controller/core/core.go:36** by [@trasc](https://github.com/trasc) — 2024-04-23T17:22:05Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  We are dropping the ControllerOption type in this PR, we should not do that.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-23T17:29:34Z

- **pkg/controller/core/core.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T17:29:34Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  Right now, there is no use for it. If we can make a case for bringing it back, we will.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T18:13:11Z

- **pkg/controller/core/core.go:36** by [@trasc](https://github.com/trasc) — 2024-04-23T18:13:10Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  It's not needed as a result of this PR implementation.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-23T18:16:02Z

- **pkg/controller/core/core.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T18:16:02Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  Again, going the path of keeping it requires more changes. I don't think refactoring of this function is a priority right now. So we should leave the code in a state that makes sense for the mid-term.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-23T18:54:02Z

- **pkg/controller/core/core.go:36** by [@trasc](https://github.com/trasc) — 2024-04-23T18:54:02Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  > going the path of keeping it requires more changes
  
  I'm not convinced, we used to have for now changing `WithControllerRequeuingBaseDelaySeconds`  we can just replace it with  `WithControllerRequeuingBackoffBaseSeconds` .

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-23T19:03:23Z

- **pkg/controller/core/core.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T19:03:23Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  But then we have the same information flowing through `cfg`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-24T07:12:17Z

- **pkg/controller/core/core.go:72** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-24T07:12:16Z
```diff
@@ -78,19 +65,13 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	if err := cqRec.SetupWithManager(mgr, cfg); err != nil {
 		return "ClusterQueue", err
 	}
-	ctrlOpts := ControllerOptions{
-		requeuingBaseDelaySeconds: defaultRequeuingBaseDelaySeconds,
-	}
-	for _, opt := range controllerOpts {
-		opt(&ctrlOpts)
-	}
 
 	if err := NewWorkloadReconciler(mgr.GetClient(), qManager, cc,
 		mgr.GetEventRecorderFor(constants.WorkloadControllerName),
 		WithWorkloadUpdateWatchers(qRec, cqRec),
 		WithPodsReadyTimeout(podsReadyTimeout(cfg)),
```

  +1

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-24T07:17:51Z

- **pkg/controller/core/core.go:36** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-24T07:17:51Z
```diff
@@ -29,25 +29,12 @@ import (
 )
 
 const (
-	updateChBuffer                   = 10
-	defaultRequeuingBaseDelaySeconds = 10
+	updateChBuffer = 10
 )
 
-type ControllerOptions struct {
-	requeuingBaseDelaySeconds int32
-}
-
-type ControllerOption func(*ControllerOptions)
-
-func WithControllerRequeuingBaseDelaySeconds(value int32) ControllerOption {
-	return func(o *ControllerOptions) {
-		o.requeuingBaseDelaySeconds = value
-	}
-}
-
 // SetupControllers sets up the core controllers. It returns the name of the
 // controller that failed to create and an error, if any.
-func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration, controllerOpts ...ControllerOption) (string, error) {
+func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache, cfg *configapi.Configuration) (string, error) {
```

  Let me put the context of the reason why we added the `controllerOpts`.
  
  We introduced the `controllerOpts` in https://github.com/kubernetes-sigs/kueue/pull/2025 instead of Config API to cherry-pick to the release v0.6 as an interim approach. Then, we try to remove the interim approach in this PR.
  
  I agree with refactoring this function, but as @alculquicondor and @mimowo, I also think that the refactoring is beyond this PR responsibility.
  So, I agree with leaving the refactoring in another PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-24T07:43:25Z

- **pkg/controller/core/core.go:72** by [@mimowo](https://github.com/mimowo) — 2024-04-24T07:43:25Z
```diff
@@ -78,19 +65,13 @@ func SetupControllers(mgr ctrl.Manager, qManager *queue.Manager, cc *cache.Cache
 	if err := cqRec.SetupWithManager(mgr, cfg); err != nil {
 		return "ClusterQueue", err
 	}
-	ctrlOpts := ControllerOptions{
-		requeuingBaseDelaySeconds: defaultRequeuingBaseDelaySeconds,
-	}
-	for _, opt := range controllerOpts {
-		opt(&ctrlOpts)
-	}
 
 	if err := NewWorkloadReconciler(mgr.GetClient(), qManager, cc,
 		mgr.GetEventRecorderFor(constants.WorkloadControllerName),
 		WithWorkloadUpdateWatchers(qRec, cqRec),
 		WithPodsReadyTimeout(podsReadyTimeout(cfg)),
```

  Good idea, I just pushed a commit along the lines, but I use:
  `WithWaitForPodsReady(waitForPodsReady(cfg.WaitForPodsReady)),`
  because the controller needs the entire config. Also I use the `waitForPodsReady` function to convert the underlying config into a structure easier to consume. In particular, I don't need the `enable` field.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-24T08:02:46Z

Thank you!
/lgtm
