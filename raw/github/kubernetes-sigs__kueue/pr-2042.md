# PR #2042: [KEP] Support new ProvisioningRequest's conditions

**Summary**: [KEP] Support new ProvisioningRequest's conditions

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2042

**Last updated**: 2024-07-31T11:43:04Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `provreq-kep-update` → `main`
- **Created**: 2024-04-23T12:29:35Z
- **Updated**: 2024-07-31T11:43:04Z
- **Merged**: 2024-05-08T16:01:12Z
- **Closed**: 2024-05-08T16:01:12Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Changed files**: 1   **+24 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Describes behavior of ProvisioningRequest controller regarding recently introduced new [conditions](https://github.com/kubernetes/autoscaler/blob/349559c32f496900ccd643172c7e4f87a8fecc02/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1/types.go#L177)

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #2041 


#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE

```

## Discussion

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-04-23T12:29:47Z

/assign @alculquicondor 
/assign @yaroslava-serdiuk

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-23T12:29:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3a847457e312ab97e52c538a1b4b6ad5f4588a72 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/663b352c0b7504000720bf19 |

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-04-23T13:04:15Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-23T13:04:21Z

LGTM label has been added.  <details>Git tree hash: 3a0c1db53337ec693007ecfd2de38e1fbae1853d</details>

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-07T11:19:40Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-07T11:19:45Z

LGTM label has been added.  <details>Git tree hash: 616d2645c1f3b658f35404d50c177fa21285f4b1</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-08T15:56:50Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-08T15:56:55Z

LGTM label has been added.  <details>Git tree hash: 5dbafd083b5d0dd1a0e0b6210a0c1118f0d453de</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-08T15:56:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2042#issuecomment-2100900633" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2042#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-04-23T13:03:53Z

- **keps/1136-provisioning-request-support/README.md:103** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-04-23T13:03:53Z
```diff
@@ -95,11 +95,13 @@ The `ProvisioningRequest` should have the owner reference set to the workload.
 To understand what details should it put into `ProvisioningRequest` the controller
 will also need to watch `ProvisioningRequestConfigs`.
 
-* Watch all changes CA makes to `ProvisioningRequests`. If the `Provisioned`
-or `CapacityAvailable` condition is set to `True` then finish the `AdmissionCheck`
-with success (and propagate the information about `ProvisioningRequest` name to
+* Watch all changes CA makes to `ProvisioningRequests`. If the `ProvisioningRequest's` conditions are set to:
+  - `Provisioned=false` controller should surface information about ProvisioningRequest's ETA. It should emit an event regarding that and for every ETA change.
+  - `Provisioned=true` controller should mark the AdmissionCheck as `Ready` and propagate the information about `ProvisioningRequest` name to
 workload pods - [KEP #1145](https://github.com/kubernetes-sigs/kueue/blob/main/keps/1145-additional-labels/kep.yaml) under `"cluster-autoscaler.kubernetes.io/consume-provisioning-request"`.
-If the `ProvisioningRequest` fails, fail the `AdmissionCheck`.
+  - `Failed=true` controller should retry AdmissionCheck with respect to the `RetryConfig` configuration, or mark the AdmissionCheck as `Rejected`
+  - `BookingExpired=true` controller should check if `ProvisioningRequest` needs recreating, and retry if needed with respect to the `RetryConfig` configuration.
```

  nit: Maybe worth specify when ProvisioningRequest needs to be recreated

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-04-23T13:23:01Z

- **keps/1136-provisioning-request-support/README.md:103** by [@PBundyra](https://github.com/PBundyra) — 2024-04-23T13:23:01Z
```diff
@@ -95,11 +95,13 @@ The `ProvisioningRequest` should have the owner reference set to the workload.
 To understand what details should it put into `ProvisioningRequest` the controller
 will also need to watch `ProvisioningRequestConfigs`.
 
-* Watch all changes CA makes to `ProvisioningRequests`. If the `Provisioned`
-or `CapacityAvailable` condition is set to `True` then finish the `AdmissionCheck`
-with success (and propagate the information about `ProvisioningRequest` name to
+* Watch all changes CA makes to `ProvisioningRequests`. If the `ProvisioningRequest's` conditions are set to:
+  - `Provisioned=false` controller should surface information about ProvisioningRequest's ETA. It should emit an event regarding that and for every ETA change.
+  - `Provisioned=true` controller should mark the AdmissionCheck as `Ready` and propagate the information about `ProvisioningRequest` name to
 workload pods - [KEP #1145](https://github.com/kubernetes-sigs/kueue/blob/main/keps/1145-additional-labels/kep.yaml) under `"cluster-autoscaler.kubernetes.io/consume-provisioning-request"`.
-If the `ProvisioningRequest` fails, fail the `AdmissionCheck`.
+  - `Failed=true` controller should retry AdmissionCheck with respect to the `RetryConfig` configuration, or mark the AdmissionCheck as `Rejected`
+  - `BookingExpired=true` controller should check if `ProvisioningRequest` needs recreating, and retry if needed with respect to the `RetryConfig` configuration.
```

  Done, thanks

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-04-23T14:30:57Z

- **keps/1136-provisioning-request-support/README.md:103** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-04-23T14:30:57Z
```diff
@@ -95,11 +95,13 @@ The `ProvisioningRequest` should have the owner reference set to the workload.
 To understand what details should it put into `ProvisioningRequest` the controller
 will also need to watch `ProvisioningRequestConfigs`.
 
-* Watch all changes CA makes to `ProvisioningRequests`. If the `Provisioned`
-or `CapacityAvailable` condition is set to `True` then finish the `AdmissionCheck`
-with success (and propagate the information about `ProvisioningRequest` name to
+* Watch all changes CA makes to `ProvisioningRequests`. If the `ProvisioningRequest's` conditions are set to:
+  - `Provisioned=false` controller should surface information about ProvisioningRequest's ETA. It should emit an event regarding that and for every ETA change.
+  - `Provisioned=true` controller should mark the AdmissionCheck as `Ready` and propagate the information about `ProvisioningRequest` name to
 workload pods - [KEP #1145](https://github.com/kubernetes-sigs/kueue/blob/main/keps/1145-additional-labels/kep.yaml) under `"cluster-autoscaler.kubernetes.io/consume-provisioning-request"`.
-If the `ProvisioningRequest` fails, fail the `AdmissionCheck`.
+  - `Failed=true` controller should retry AdmissionCheck with respect to the `RetryConfig` configuration, or mark the AdmissionCheck as `Rejected`
+  - `BookingExpired=true` controller should check if `ProvisioningRequest` needs recreating - when the provisioned nodes have no pods scheduled, and retry if needed with respect to the `RetryConfig` configuration.
```

  I don't think we need to check provisioned nodes. For example for Check-capacity ProvisioningClass there are no provisioned nodes. 
  I think ProvisioningRequest should be recreated if the pods weren't created yet or if the pods were created but not all of them were scheduled during some time.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-23T16:22:42Z

- **keps/1136-provisioning-request-support/README.md:103** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-23T16:22:42Z
```diff
@@ -95,11 +95,13 @@ The `ProvisioningRequest` should have the owner reference set to the workload.
 To understand what details should it put into `ProvisioningRequest` the controller
 will also need to watch `ProvisioningRequestConfigs`.
 
-* Watch all changes CA makes to `ProvisioningRequests`. If the `Provisioned`
-or `CapacityAvailable` condition is set to `True` then finish the `AdmissionCheck`
-with success (and propagate the information about `ProvisioningRequest` name to
+* Watch all changes CA makes to `ProvisioningRequests`. If the `ProvisioningRequest's` conditions are set to:
+  - `Provisioned=false` controller should surface information about ProvisioningRequest's ETA. It should emit an event regarding that and for every ETA change.
+  - `Provisioned=true` controller should mark the AdmissionCheck as `Ready` and propagate the information about `ProvisioningRequest` name to
 workload pods - [KEP #1145](https://github.com/kubernetes-sigs/kueue/blob/main/keps/1145-additional-labels/kep.yaml) under `"cluster-autoscaler.kubernetes.io/consume-provisioning-request"`.
-If the `ProvisioningRequest` fails, fail the `AdmissionCheck`.
+  - `Failed=true` controller should retry AdmissionCheck with respect to the `RetryConfig` configuration, or mark the AdmissionCheck as `Rejected`
+  - `BookingExpired=true` controller should check if `ProvisioningRequest` needs recreating - when the provisioned nodes have no pods scheduled, and retry if needed with respect to the `RetryConfig` configuration.
```

  +1 on an even simpler approach of checking if the job is unsuspended.
  
  Maybe start a section in Risks and Mitigations to describe when the race condition that could happen. IIRC, it could only happen if the user has more than one admission check or when creating a massive job, where the job controller is not able to create pods fast enough
  For the first case, we have WaitForPodsReady as a possible mitigation (after the timeout, the job is evicted and retried).
  For the second case, the cluster administrator should probably size their control plane appropriately (bigger VMs and/or bigger qps for the controller creating pods).

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-07T19:47:40Z

- **keps/1136-provisioning-request-support/README.md:78** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:41:06Z
```diff
@@ -77,6 +78,21 @@ There doesn't seem to be much risks or mitigations.
 [Two phase admission process](https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission)
```

  this paragraph is no longer true, you can remove it.

- **keps/1136-provisioning-request-support/README.md:119** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:44:03Z
```diff
@@ -95,11 +111,15 @@ The `ProvisioningRequest` should have the owner reference set to the workload.
 To understand what details should it put into `ProvisioningRequest` the controller
 will also need to watch `ProvisioningRequestConfigs`.
 
-* Watch all changes CA makes to `ProvisioningRequests`. If the `Provisioned`
-or `CapacityAvailable` condition is set to `True` then finish the `AdmissionCheck`
-with success (and propagate the information about `ProvisioningRequest` name to
+* Watch all changes CA makes to `ProvisioningRequests`. If the `ProvisioningRequest's` conditions are set to:
+  - `Provisioned=false` controller should surface information about ProvisioningRequest's ETA. It should emit an event regarding that and for every ETA change.
+  - `Provisioned=true` controller should mark the AdmissionCheck as `Ready` and propagate the information about `ProvisioningRequest` name to
 workload pods - [KEP #1145](https://github.com/kubernetes-sigs/kueue/blob/main/keps/1145-additional-labels/kep.yaml) under `"cluster-autoscaler.kubernetes.io/consume-provisioning-request"`.
-If the `ProvisioningRequest` fails, fail the `AdmissionCheck`.
+  - `Failed=true` controller should retry AdmissionCheck with respect to the `RetryConfig` configuration, or mark the AdmissionCheck as `Rejected`
+  - `BookingExpired=true` if a Workload is not `Admitted` controller should act the same as for `Failed=true`.
```

  ```suggestion
    - `BookingExpired=true` if a Workload is not `Admitted`, the controller should act the same as for `Failed=true`.
  ```

- **keps/1136-provisioning-request-support/README.md:120** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:44:20Z
```diff
@@ -95,11 +111,15 @@ The `ProvisioningRequest` should have the owner reference set to the workload.
 To understand what details should it put into `ProvisioningRequest` the controller
 will also need to watch `ProvisioningRequestConfigs`.
 
-* Watch all changes CA makes to `ProvisioningRequests`. If the `Provisioned`
-or `CapacityAvailable` condition is set to `True` then finish the `AdmissionCheck`
-with success (and propagate the information about `ProvisioningRequest` name to
+* Watch all changes CA makes to `ProvisioningRequests`. If the `ProvisioningRequest's` conditions are set to:
+  - `Provisioned=false` controller should surface information about ProvisioningRequest's ETA. It should emit an event regarding that and for every ETA change.
+  - `Provisioned=true` controller should mark the AdmissionCheck as `Ready` and propagate the information about `ProvisioningRequest` name to
 workload pods - [KEP #1145](https://github.com/kubernetes-sigs/kueue/blob/main/keps/1145-additional-labels/kep.yaml) under `"cluster-autoscaler.kubernetes.io/consume-provisioning-request"`.
-If the `ProvisioningRequest` fails, fail the `AdmissionCheck`.
+  - `Failed=true` controller should retry AdmissionCheck with respect to the `RetryConfig` configuration, or mark the AdmissionCheck as `Rejected`
+  - `BookingExpired=true` if a Workload is not `Admitted` controller should act the same as for `Failed=true`.
+  - `CapacityRevoked=true` if a Workload is not `Finished` controller should mark it as `Inactive`, which will evict it.
```

  ```suggestion
    - `CapacityRevoked=true` if a Workload is not `Finished`, the controller should mark it as `Inactive`, which will evict it.
  ```

- **keps/1136-provisioning-request-support/README.md:121** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:45:39Z
```diff
@@ -95,11 +111,15 @@ The `ProvisioningRequest` should have the owner reference set to the workload.
 To understand what details should it put into `ProvisioningRequest` the controller
 will also need to watch `ProvisioningRequestConfigs`.
 
-* Watch all changes CA makes to `ProvisioningRequests`. If the `Provisioned`
-or `CapacityAvailable` condition is set to `True` then finish the `AdmissionCheck`
-with success (and propagate the information about `ProvisioningRequest` name to
+* Watch all changes CA makes to `ProvisioningRequests`. If the `ProvisioningRequest's` conditions are set to:
+  - `Provisioned=false` controller should surface information about ProvisioningRequest's ETA. It should emit an event regarding that and for every ETA change.
+  - `Provisioned=true` controller should mark the AdmissionCheck as `Ready` and propagate the information about `ProvisioningRequest` name to
 workload pods - [KEP #1145](https://github.com/kubernetes-sigs/kueue/blob/main/keps/1145-additional-labels/kep.yaml) under `"cluster-autoscaler.kubernetes.io/consume-provisioning-request"`.
-If the `ProvisioningRequest` fails, fail the `AdmissionCheck`.
+  - `Failed=true` controller should retry AdmissionCheck with respect to the `RetryConfig` configuration, or mark the AdmissionCheck as `Rejected`
+  - `BookingExpired=true` if a Workload is not `Admitted` controller should act the same as for `Failed=true`.
+  - `CapacityRevoked=true` if a Workload is not `Finished` controller should mark it as `Inactive`, which will evict it.
+    Additionally, an event should be emitted to signalize this happening. This can happen only if a user uses `batch.v1/Job` and
```

  ```suggestion
      Additionally, an event should be emitted to signalize this happening. This can happen only if the job
      allows for retries, for example, in the case of `batch.v1/Job`, when the user
  ```

- **keps/1136-provisioning-request-support/README.md:120** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:47:36Z
```diff
@@ -95,11 +111,15 @@ The `ProvisioningRequest` should have the owner reference set to the workload.
 To understand what details should it put into `ProvisioningRequest` the controller
 will also need to watch `ProvisioningRequestConfigs`.
 
-* Watch all changes CA makes to `ProvisioningRequests`. If the `Provisioned`
-or `CapacityAvailable` condition is set to `True` then finish the `AdmissionCheck`
-with success (and propagate the information about `ProvisioningRequest` name to
+* Watch all changes CA makes to `ProvisioningRequests`. If the `ProvisioningRequest's` conditions are set to:
+  - `Provisioned=false` controller should surface information about ProvisioningRequest's ETA. It should emit an event regarding that and for every ETA change.
+  - `Provisioned=true` controller should mark the AdmissionCheck as `Ready` and propagate the information about `ProvisioningRequest` name to
 workload pods - [KEP #1145](https://github.com/kubernetes-sigs/kueue/blob/main/keps/1145-additional-labels/kep.yaml) under `"cluster-autoscaler.kubernetes.io/consume-provisioning-request"`.
-If the `ProvisioningRequest` fails, fail the `AdmissionCheck`.
+  - `Failed=true` controller should retry AdmissionCheck with respect to the `RetryConfig` configuration, or mark the AdmissionCheck as `Rejected`
+  - `BookingExpired=true` if a Workload is not `Admitted` controller should act the same as for `Failed=true`.
+  - `CapacityRevoked=true` if a Workload is not `Finished` controller should mark it as `Inactive`, which will evict it.
```

  Maybe something for the implementation, not the KEP, but we should somehow communicate why the Workload was deactivated.
  Can we leave a message in the admission checks slice or something?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-08T08:18:22Z

- **keps/1136-provisioning-request-support/README.md:78** by [@PBundyra](https://github.com/PBundyra) — 2024-05-08T08:18:21Z
```diff
@@ -77,6 +78,21 @@ There doesn't seem to be much risks or mitigations.
 [Two phase admission process](https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission)
```

  Done
