# PR #2047: Validate admission check applies to all flavors

**Summary**: Validate admission check applies to all flavors

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2047

**Last updated**: 2024-05-08T09:48:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `ac-applied-all-flavors` → `main`
- **Created**: 2024-04-23T22:07:44Z
- **Updated**: 2024-05-08T09:48:13Z
- **Merged**: 2024-04-25T18:11:33Z
- **Closed**: 2024-04-25T18:11:33Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 9   **+94 / -12**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:

- Allow to validate if a MultiKueue admission check is configured only for a subset of flavors.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2021

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-23T22:07:53Z

Hi @IrvingMg. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-23T22:08:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 48e3a106d777e3d49b2154eb4eb20d9b656dfb96 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/662a71871b7b8e00080db7e7 |

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-04-23T22:48:31Z

/assign @trasc

### Comment by [@trasc](https://github.com/trasc) — 2024-04-24T03:50:47Z

/ok-to-test

### Comment by [@trasc](https://github.com/trasc) — 2024-04-24T05:48:18Z

/retest
The failing test is tracked under #2045

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-24T13:53:22Z

LGTM label has been added.  <details>Git tree hash: 59d5cd1e47db569fcf6c11274ba202a2da6d0e74</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-24T16:48:47Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-25T09:52:50Z

@IrvingMg please also update documentation for the MK admission check, can be done in a follow up.

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-04-25T14:35:42Z

/test pull-kueue-test-scheduling-perf-main

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-25T14:42:25Z

LGTM, remaining nit: https://github.com/kubernetes-sigs/kueue/pull/2047#discussion_r1579593642
/approve
/assign @alculquicondor

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-25T15:11:16Z

/lgtm
Thanks

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-25T15:11:21Z

LGTM label has been added.  <details>Git tree hash: 6c81084f542779325e8cdc5db5283070d211f376</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-25T17:47:09Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2047#pullrequestreview-2023195702" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2047#" title="Author self-approved">IrvingMg</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2047#issuecomment-2077388368" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-24T06:06:46Z

- **apis/kueue/v1beta1/admissioncheck_types.go:108** by [@trasc](https://github.com/trasc) — 2024-04-24T05:50:33Z
```diff
@@ -104,6 +104,9 @@ const (
 	// Having multiple AdmissionChecks managed by the same controller where at least one has this condition
 	// set to true will cause the ClusterQueue to be marked as Inactive.
 	AdmissionChecksSingleInstanceInClusterQueue string = "SingleInstanceInClusterQueue"
+
+	// AdmissionCheckApplyToAllFlavors indicates if the AdmissionCheck should apply to all ResourceFlavor.
```

  ```suggestion
  	// AdmissionCheckApplyToAllFlavors indicates if the AdmissionCheck cannot be applied at ResourceFlavor level.
  ```

- **pkg/cache/clusterqueue.go:440** by [@trasc](https://github.com/trasc) — 2024-04-24T05:52:34Z
```diff
@@ -431,6 +437,9 @@ func (c *ClusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 			if ac.SingleInstanceInClusterQueue {
 				singleInstanceControllers.Insert(ac.Controller)
 			}
+			if flavors.Len() != 0 && ac.ApplyToAllFlavors {
```

  ```suggestion
  			if ac.ApplyToAllFlavors  && flavors.Len() != 0 {
  ```
  nit

- **pkg/cache/admissioncheck.go:23** by [@trasc](https://github.com/trasc) — 2024-04-24T06:00:55Z
```diff
@@ -20,4 +20,5 @@ type AdmissionCheck struct {
 	Active                       bool
 	Controller                   string
 	SingleInstanceInClusterQueue bool
+	ApplyToAllFlavors            bool
```

  ```suggestion
  	ApplyOnlyToAllFlavors            bool
  ```

- **pkg/cache/clusterqueue.go:90** by [@trasc](https://github.com/trasc) — 2024-04-24T06:02:55Z
```diff
@@ -87,6 +87,7 @@ type ClusterQueue struct {
 	hasMissingFlavors                          bool
 	hasMissingOrInactiveAdmissionChecks        bool
 	hasMultipleSingleInstanceControllersChecks bool
+	hasSpecificFlavorChecks                    bool
```

  Maybe
  
  ```suggestion
  	hasAllFlavorsChecksAppliedToSpecificFlavor                    bool
  ```

- **pkg/cache/clusterqueue.go:377** by [@trasc](https://github.com/trasc) — 2024-04-24T06:03:50Z
```diff
@@ -372,6 +373,10 @@ func (c *ClusterQueue) inactiveReason() (string, string) {
 			reasons = append(reasons, "MultipleSingleInstanceControllerChecks")
 		}
 
+		if c.hasSpecificFlavorChecks {
+			reasons = append(reasons, "ChecksNotApplyToAllFlavors")
```

  ```suggestion
  			reasons = append(reasons, "AllFlavorsChecksAppliedToSpecificFlavor ")
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-24T13:53:17Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-25T07:08:34Z

The implementation lgtm, but the naming is confusing imo (proposed an alternative). Also, it would be helpful to have a message where we can point out the admission check, but we can leave for follow up.

- **pkg/cache/clusterqueue_test.go:677** by [@mimowo](https://github.com/mimowo) — 2024-04-25T06:17:05Z
```diff
@@ -646,6 +653,29 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantStatus: pending,
 			wantReason: "MultipleSingleInstanceControllerChecks",
 		},
+		{
+			name:     "Active clusterQueue with an AC per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                true,
+					Controller:            "controller1",
+					ApplyOnlyToAllFlavors: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                true,
+					Controller:            "controller3",
+					ApplyOnlyToAllFlavors: true,
+				},
+			},
+			wantStatus: pending,
+			wantReason: "AllFlavorsChecksAppliedToSpecificFlavor",
```

  `AllFlavorsChecksAppliedToSpecificFlavor` I have a trouble understanding this (and I think users would have too). 
  
  Ideally, as a user, I would see something like `MultiKueueAdmissionCheckAppliedPerFlavor`. 
  
  Since the mechanism is generic, we could maybe have `FlavorIndependentAdmissionCheckAppliedPerFlavor`? The details could go into a message (see another comment).

- **pkg/cache/clusterqueue_test.go:481** by [@mimowo](https://github.com/mimowo) — 2024-04-25T06:21:22Z
```diff
@@ -474,6 +474,13 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			*utiltesting.MakeAdmissionCheckStrategyRule("check3").Obj()).
 		Obj()
 
+	cqWithACPerFlavor := utiltesting.MakeClusterQueue("cq3").
+		AdmissionCheckStrategy(
+			*utiltesting.MakeAdmissionCheckStrategyRule("check1", "flavor1").Obj(),
+			*utiltesting.MakeAdmissionCheckStrategyRule("check2", "flavor2").Obj(),
+			*utiltesting.MakeAdmissionCheckStrategyRule("check3", "flavor3").Obj()).
```

  I'm not sure why we have both "check1" and "check3" applied to a specific flavor. I would suggest to make "check3" applied to all flavors.

- **pkg/cache/clusterqueue.go:464** by [@mimowo](https://github.com/mimowo) — 2024-04-25T06:28:56Z
```diff
@@ -452,6 +461,11 @@ func (c *ClusterQueue) updateWithAdmissionChecks(checks map[string]AdmissionChec
 		update = true
 	}
 
+	if c.hasAllFlavorsChecksAppliedToSpecificFlavor != hasSpecificChecks {
```

  It is a bit hard to follow the name. I think the intention is `hasClusterQueueLevelAdmissionCheckAppliedAtFlavorLevel`?

- **pkg/cache/clusterqueue_test.go:677** by [@mimowo](https://github.com/mimowo) — 2024-04-25T07:00:21Z
```diff
@@ -646,6 +653,29 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantStatus: pending,
 			wantReason: "MultipleSingleInstanceControllerChecks",
 		},
+		{
+			name:     "Active clusterQueue with an AC per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                true,
+					Controller:            "controller1",
+					ApplyOnlyToAllFlavors: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                true,
+					Controller:            "controller3",
+					ApplyOnlyToAllFlavors: true,
+				},
+			},
+			wantStatus: pending,
+			wantReason: "AllFlavorsChecksAppliedToSpecificFlavor",
```

  It would be nice to describe more details in the message, for example "The Admission Check {name} (controlled by {controller}) can only be applied to all flavors, but applied to {list of flavors}.".

- **apis/kueue/v1beta1/admissioncheck_types.go:109** by [@mimowo](https://github.com/mimowo) — 2024-04-25T07:03:37Z
```diff
@@ -104,6 +104,9 @@ const (
 	// Having multiple AdmissionChecks managed by the same controller where at least one has this condition
 	// set to true will cause the ClusterQueue to be marked as Inactive.
 	AdmissionChecksSingleInstanceInClusterQueue string = "SingleInstanceInClusterQueue"
+
+	// AdmissionCheckApplyOnlyToAllFlavors indicates if the AdmissionCheck cannot be applied at ResourceFlavor level.
+	AdmissionCheckApplyOnlyToAllFlavors string = "ApplyOnlyToAllFlavors"
```

  Maybe `FlavorIndependentAdmissionCheck` ?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-04-25T10:16:08Z

- **pkg/cache/clusterqueue_test.go:677** by [@IrvingMg](https://github.com/IrvingMg) — 2024-04-25T10:16:08Z
```diff
@@ -646,6 +653,29 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantStatus: pending,
 			wantReason: "MultipleSingleInstanceControllerChecks",
 		},
+		{
+			name:     "Active clusterQueue with an AC per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                true,
+					Controller:            "controller1",
+					ApplyOnlyToAllFlavors: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                true,
+					Controller:            "controller3",
+					ApplyOnlyToAllFlavors: true,
+				},
+			},
+			wantStatus: pending,
+			wantReason: "AllFlavorsChecksAppliedToSpecificFlavor",
```

  Do you mean that we should include the details in the reason string? As in the other cases of  [`inactiveReason()`](https://github.com/kubernetes-sigs/kueue/pull/2047/files#diff-441da8393964615cf5b020d0afb9199588e157e50718e83b527788eb679391ecR356) method, currently we're returning just the reason without extra information.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-25T11:34:58Z

- **pkg/cache/clusterqueue.go:377** by [@mimowo](https://github.com/mimowo) — 2024-04-25T11:32:34Z
```diff
@@ -372,6 +373,10 @@ func (c *ClusterQueue) inactiveReason() (string, string) {
 			reasons = append(reasons, "MultipleSingleInstanceControllerChecks")
 		}
 
+		if c.hasClusterQueueLevelAdmissionCheckAppliedAtFlavorLevel {
+			reasons = append(reasons, "MultiKueueAdmissionCheckAppliedPerFlavor")
```

  ```suggestion
  			reasons = append(reasons, "FlavorIndependentAdmissionCheckAppliedPerFlavor")
  ```
  Let's call it that way since the condition checked in the if is more generic

- **pkg/cache/clusterqueue.go:90** by [@mimowo](https://github.com/mimowo) — 2024-04-25T11:34:01Z
```diff
@@ -82,13 +82,14 @@ type ClusterQueue struct {
 
 	AdmittedUsage FlavorResourceQuantities
 	// localQueues by (namespace/name).
-	localQueues                                map[string]*queue
-	podsReadyTracking                          bool
-	hasMissingFlavors                          bool
-	hasMissingOrInactiveAdmissionChecks        bool
-	hasMultipleSingleInstanceControllersChecks bool
-	admittedWorkloadsCount                     int
-	isStopped                                  bool
+	localQueues                                            map[string]*queue
+	podsReadyTracking                                      bool
+	hasMissingFlavors                                      bool
+	hasMissingOrInactiveAdmissionChecks                    bool
+	hasMultipleSingleInstanceControllersChecks             bool
+	hasClusterQueueLevelAdmissionCheckAppliedAtFlavorLevel bool
```

  ```suggestion
  	hasFlavorIndependentAdmissionCheckAppliedAtFlavorLevel bool
  ```
  for consistency

- **pkg/cache/clusterqueue_test.go:656** by [@mimowo](https://github.com/mimowo) — 2024-04-25T11:34:37Z
```diff
@@ -646,6 +652,20 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantStatus: pending,
 			wantReason: "MultipleSingleInstanceControllerChecks",
 		},
+		{
+			name:     "Active clusterQueue with an AC per ResourceFlavor",
```

  ```suggestion
  			name:     "Active clusterQueue with a FlavorIndependent AC applied per ResourceFlavor",
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-25T11:51:25Z

- **pkg/cache/clusterqueue_test.go:677** by [@mimowo](https://github.com/mimowo) — 2024-04-25T11:51:25Z
```diff
@@ -646,6 +653,29 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantStatus: pending,
 			wantReason: "MultipleSingleInstanceControllerChecks",
 		},
+		{
+			name:     "Active clusterQueue with an AC per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                true,
+					Controller:            "controller1",
+					ApplyOnlyToAllFlavors: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                true,
+					Controller:            "controller3",
+					ApplyOnlyToAllFlavors: true,
+				},
+			},
+			wantStatus: pending,
+			wantReason: "AllFlavorsChecksAppliedToSpecificFlavor",
```

  > Do you mean that we should include the details in the reason string?
  
  The `reason` field is ok, this is just a camel-case summary. 
  
  I was thinking about the `message` field that is set on the ClusterQueue's condition when `Active=False`. 
  This indeed is returned as the second param from `inactiveReason`, and computed here as a concatenation of `reason` fields. However, it does not tell the user details, like which AC we are referring to.
  
  To pass this information we would need to probably compute it inside `updateWithAdmissionChecks` and store [here](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/clusterqueue.go#L87-L91), probably as a map from reason to message.
  
  Since this is a pre-existing limitation of the current structure of the code I'm ok to leave it as a follow up. WDYT?

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-04-25T14:12:08Z

- **pkg/cache/clusterqueue_test.go:677** by [@IrvingMg](https://github.com/IrvingMg) — 2024-04-25T14:12:08Z
```diff
@@ -646,6 +653,29 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantStatus: pending,
 			wantReason: "MultipleSingleInstanceControllerChecks",
 		},
+		{
+			name:     "Active clusterQueue with an AC per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                true,
+					Controller:            "controller1",
+					ApplyOnlyToAllFlavors: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                true,
+					Controller:            "controller3",
+					ApplyOnlyToAllFlavors: true,
+				},
+			},
+			wantStatus: pending,
+			wantReason: "AllFlavorsChecksAppliedToSpecificFlavor",
```

  I agree, I think we can leave it as a follow up and include there the details for this and the rest of the admission checks as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-25T14:36:59Z

- **pkg/cache/clusterqueue_test.go:677** by [@mimowo](https://github.com/mimowo) — 2024-04-25T14:36:59Z
```diff
@@ -646,6 +653,29 @@ func TestClusterQueueUpdateWithAdmissionCheck(t *testing.T) {
 			wantStatus: pending,
 			wantReason: "MultipleSingleInstanceControllerChecks",
 		},
+		{
+			name:     "Active clusterQueue with an AC per ResourceFlavor",
+			cq:       cqWithACPerFlavor,
+			cqStatus: pending,
+			admissionChecks: map[string]AdmissionCheck{
+				"check1": {
+					Active:                true,
+					Controller:            "controller1",
+					ApplyOnlyToAllFlavors: true,
+				},
+				"check2": {
+					Active:     true,
+					Controller: "controller2",
+				},
+				"check3": {
+					Active:                true,
+					Controller:            "controller3",
+					ApplyOnlyToAllFlavors: true,
+				},
+			},
+			wantStatus: pending,
+			wantReason: "AllFlavorsChecksAppliedToSpecificFlavor",
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-25T14:39:12Z

- **pkg/cache/clusterqueue.go:90** by [@mimowo](https://github.com/mimowo) — 2024-04-25T14:39:12Z
```diff
@@ -82,13 +82,14 @@ type ClusterQueue struct {
 
 	AdmittedUsage FlavorResourceQuantities
 	// localQueues by (namespace/name).
-	localQueues                                map[string]*queue
-	podsReadyTracking                          bool
-	hasMissingFlavors                          bool
-	hasMissingOrInactiveAdmissionChecks        bool
-	hasMultipleSingleInstanceControllersChecks bool
-	admittedWorkloadsCount                     int
-	isStopped                                  bool
+	localQueues                                            map[string]*queue
+	podsReadyTracking                                      bool
+	hasMissingFlavors                                      bool
+	hasMissingOrInactiveAdmissionChecks                    bool
+	hasMultipleSingleInstanceControllersChecks             bool
+	hasFlavorIndependentAdmissionCheckAppliedAtFlavorLevel bool
```

  ```suggestion
  	hasFlavorIndependentAdmissionCheckAppliedPerFlavor bool
  ```
  nit, shorter and for consistency with the `FlavorIndependentAdmissionCheckAppliedPerFlavor` reason

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-25T17:47:03Z

/approve
