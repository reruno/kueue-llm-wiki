# PR #2048: [MultiKueue] Default managedBy for ClusterQueues configured to use MultiKueue AC

**Summary**: [MultiKueue] Default managedBy for ClusterQueues configured to use MultiKueue AC

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2048

**Last updated**: 2024-05-30T14:02:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `job-set-default-managed-by` → `main`
- **Created**: 2024-04-24T06:21:17Z
- **Updated**: 2024-05-30T14:02:14Z
- **Merged**: 2024-05-09T16:31:03Z
- **Closed**: 2024-05-09T16:31:03Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@vladikkuzn](https://github.com/vladikkuzn), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Changed files**: 10   **+351 / -68**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Sets jobSet's ManagedBy field by default if it's managed by MultiKueue
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2022

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
MultiKueue: Add the `managedBy` field to JobSets assigned to a ClusterQueue configured for MultiKueue
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-24T06:21:27Z

Hi @vladikkuzn. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-24T06:21:36Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a171146d8b4605ab0001dfffc32daf342c5b0341 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/663cf6648d9ea50008a21470 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2048--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2048--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTIwNDgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.pESOTsMiRkhSouUZaQXfxFu_D8Bs25U1dMNEhz25vTU)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-24T07:12:36Z

/cc @trasc

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-24T07:12:58Z

/assign @trasc

### Comment by [@trasc](https://github.com/trasc) — 2024-04-24T07:20:49Z

/ok-to-test

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-25T19:07:32Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-25T19:33:39Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-26T08:59:36Z

> /assign @mimowo

ack

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-26T10:03:07Z

/assign

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-28T23:37:40Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-07T16:02:28Z

/lgtm
/approve

/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-07T16:02:33Z

LGTM label has been added.  <details>Git tree hash: 0ef782e7398ea5bf06dddedc6ee837a1a98670ae</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-08T08:31:19Z

LGTM label has been added.  <details>Git tree hash: 0b2331f5ea5d38361d828bd3c53210963499ec79</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-09T16:03:43Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2048#issuecomment-2098805223" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2048#pullrequestreview-2048305640" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2048#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-09T16:17:02Z

/lgtm
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-09T16:17:07Z

LGTM label has been added.  <details>Git tree hash: 91d24b27c1ac2773c09c8b4754ea9ca5c39f3841</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T14:02:11Z

/release-note-edit

```release-note
MultiKueue: Add the `managedBy` field to JobSets assigned to a ClusterQueue configured for MultiKueue
```

## Reviews

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-04-25T15:46:27Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:79** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-25T15:46:27Z
```diff
@@ -57,6 +66,31 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+	if features.Enabled(features.MultiKueue) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(fmt.Sprintf("%s/%s", jobSet.ObjectMeta.Namespace, localQueueName))
```

  Is there a better way to do it?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-26T10:58:49Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:79** by [@mimowo](https://github.com/mimowo) — 2024-04-26T10:58:49Z
```diff
@@ -57,6 +66,31 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+	if features.Enabled(features.MultiKueue) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(fmt.Sprintf("%s/%s", jobSet.ObjectMeta.Namespace, localQueueName))
```

  I don't think so. I would vote for changing `ClusterQueueFromLocalQueue` to accept `namespace` and `name`. This would also simplify [here](https://github.com/kubernetes-sigs/kueue/blob/0446cb5e54655d3057e2e96a50de1f9da03b0a0e/pkg/visibility/api/rest/pending_workloads_lq.go#L70).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-26T11:14:28Z

LGTM overall. Just nits.

- **pkg/controller/jobs/jobset/jobset_webhook.go:83** by [@mimowo](https://github.com/mimowo) — 2024-04-26T11:01:22Z
```diff
@@ -57,6 +68,31 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+	if features.Enabled(features.MultiKueue) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(fmt.Sprintf("%s/%s", jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		clusterQueue, found := w.cache.GetClusterQueue(clusterQueueName)
+		if !found {
+			return nil
+		}
+		for admissionCheckName := range clusterQueue.AdmissionChecks {
+			admissionCheck, ok := w.cache.GetAdmissionCheck(admissionCheckName)
+			if !ok {
+				continue
+			}
+			if admissionCheck.Controller == multikueue.ControllerName {
+				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
```

  This may override a user's custom setting. To be on a safe side I would suggest setting it only if it is nil, or JobSetControllerName.

- **pkg/controller/jobs/jobset/jobset_webhook_test.go:94** by [@mimowo](https://github.com/mimowo) — 2024-04-26T11:03:32Z
```diff
@@ -71,3 +81,179 @@ func TestValidateCreate(t *testing.T) {
 		})
 	}
 }
+
+func TestDefault(t *testing.T) {
+	testCases := []struct {
+		name              string
+		jobSet            *jobset.JobSet
+		queues            []kueue.LocalQueue
+		clusterQueues     []kueue.ClusterQueue
+		admissionCheck    *kueue.AdmissionCheck
+		multiKueueEnabled bool
+		expectedManagedBy string
+		expectedError     error
```

  ```suggestion
  		wantManagedBy string
  		wantErr       error
  ```
  nit, to follow the naming we used more commonly

- **pkg/controller/jobs/jobset/jobset_webhook_test.go:329** by [@mimowo](https://github.com/mimowo) — 2024-04-26T11:06:59Z
```diff
@@ -71,3 +81,179 @@ func TestValidateCreate(t *testing.T) {
 		})
 	}
 }
+
+func TestDefault(t *testing.T) {
+	testCases := []struct {
+		name              string
+		jobSet            *jobset.JobSet
+		queues            []kueue.LocalQueue
+		clusterQueues     []kueue.ClusterQueue
+		admissionCheck    *kueue.AdmissionCheck
+		multiKueueEnabled bool
+		expectedManagedBy string
+		expectedError     error
+	}{
+		{
+			name: "TestDefault_WithQueueLabel",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels: map[string]string{
+						constants.QueueLabel: "local-queue",
+					},
+					Namespace: "default",
+				},
+			},
+			queues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("local-queue", "default").
+					ClusterQueue("cluster-queue").
+					Obj(),
+			},
+			clusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("cluster-queue").
+					AdmissionChecks("admission-check").
+					Obj(),
+			},
+			admissionCheck: utiltesting.MakeAdmissionCheck("admission-check").
+				ControllerName(multikueue.ControllerName).
+				Active(metav1.ConditionTrue).
+				Obj(),
+			multiKueueEnabled: true,
+			expectedManagedBy: multikueue.ControllerName,
+		},
+		{
+			name: "TestDefault_WithoutQueueLabel",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{Namespace: "default"},
+			},
+			multiKueueEnabled: true,
+			expectedManagedBy: "",
+		},
+		{
+			name: "TestDefault_InvalidQueueName",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels:    map[string]string{constants.QueueLabel: "invalid-queue"},
+					Namespace: "default",
+				},
+			},
+			multiKueueEnabled: true,
+			expectedError:     errors.New("queue doesn't exist"),
+		},
+		{
+			name: "TestDefault_QueueNotFound",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels: map[string]string{
+						constants.QueueLabel: "non-existent-queue",
+					},
+					Namespace: "default",
+				},
+			},
+			multiKueueEnabled: true,
+			expectedError:     errors.New("queue doesn't exist"),
+		},
+		{
+			name: "TestDefault_AdmissionCheckNotFound",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels: map[string]string{
+						constants.QueueLabel: "local-queue",
+					},
+					Namespace: "default",
+				},
+			},
+			queues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("local-queue", "default").
+					ClusterQueue("cluster-queue").
+					Obj(),
+			},
+			clusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("cluster-queue").
+					AdmissionChecks("non-existent-admission-check").
+					Obj(),
+			},
+			multiKueueEnabled: true,
+			expectedManagedBy: "",
+		},
+		{
+			name: "TestDefault_MultiKueueFeatureDisabled",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels: map[string]string{
+						constants.QueueLabel: "local-queue",
+					},
+					Namespace: "default",
+				},
+			},
+			queues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("local-queue", "default").
+					ClusterQueue("cluster-queue").
+					Obj(),
+			},
+			clusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("cluster-queue").
+					AdmissionChecks("admission-check").
+					Obj(),
+			},
+			admissionCheck: utiltesting.MakeAdmissionCheck("admission-check").
+				ControllerName(multikueue.ControllerName).
+				Active(metav1.ConditionTrue).
+				Obj(),
+			multiKueueEnabled: false,
+			expectedManagedBy: "",
+		},
+	}
+
+	for _, tc := range testCases {
+		t.Run(tc.name, func(t *testing.T) {
+			defer features.SetFeatureGateDuringTest(t, features.MultiKueue, tc.multiKueueEnabled)()
+
+			ctx, _ := utiltesting.ContextWithLog(t)
+
+			clientBuilder := utiltesting.NewClientBuilder().
+				WithObjects(
+					&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: "default"}},
+				)
+			cl := clientBuilder.Build()
+			cqCache := cache.New(cl)
+			queueManager := queue.NewManager(cl, cqCache)
+
+			for _, q := range tc.queues {
+				if err := queueManager.AddLocalQueue(ctx, &q); err != nil {
+					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				}
+			}
+			for _, cq := range tc.clusterQueues {
+				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				}
+				if tc.admissionCheck != nil {
+					cqCache.AddOrUpdateAdmissionCheck(tc.admissionCheck)
+					if err := queueManager.AddClusterQueue(ctx, &cq); err != nil {
+						t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+					}
+				}
+			}
+			webhook := &JobSetWebhook{
+				manageJobsWithoutQueueName: false,
+				queues:                     queueManager,
+				cache:                      cqCache,
+			}
+
+			err := webhook.Default(ctx, tc.jobSet)
+			if err != nil && tc.expectedError == nil {
+				t.Errorf("Unexpected error: %v", err)
+			} else if err == nil && tc.expectedError != nil {
+				t.Errorf("Expected error %v, but got nil", tc.expectedError)
+			} else if err != nil && err.Error() != tc.expectedError.Error() {
+				t.Errorf("Expected error %v, but got %v", tc.expectedError, err)
+			}
```

  nit: Can we compare errors using `cmp`, similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/0446cb5e54655d3057e2e96a50de1f9da03b0a0e/pkg/controller/jobs/job/job_webhook_test.go#L377-L379)?

- **pkg/controller/jobs/jobset/jobset_webhook_test.go:332** by [@mimowo](https://github.com/mimowo) — 2024-04-26T11:07:46Z
```diff
@@ -71,3 +81,179 @@ func TestValidateCreate(t *testing.T) {
 		})
 	}
 }
+
+func TestDefault(t *testing.T) {
+	testCases := []struct {
+		name              string
+		jobSet            *jobset.JobSet
+		queues            []kueue.LocalQueue
+		clusterQueues     []kueue.ClusterQueue
+		admissionCheck    *kueue.AdmissionCheck
+		multiKueueEnabled bool
+		expectedManagedBy string
+		expectedError     error
+	}{
+		{
+			name: "TestDefault_WithQueueLabel",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels: map[string]string{
+						constants.QueueLabel: "local-queue",
+					},
+					Namespace: "default",
+				},
+			},
+			queues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("local-queue", "default").
+					ClusterQueue("cluster-queue").
+					Obj(),
+			},
+			clusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("cluster-queue").
+					AdmissionChecks("admission-check").
+					Obj(),
+			},
+			admissionCheck: utiltesting.MakeAdmissionCheck("admission-check").
+				ControllerName(multikueue.ControllerName).
+				Active(metav1.ConditionTrue).
+				Obj(),
+			multiKueueEnabled: true,
+			expectedManagedBy: multikueue.ControllerName,
+		},
+		{
+			name: "TestDefault_WithoutQueueLabel",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{Namespace: "default"},
+			},
+			multiKueueEnabled: true,
+			expectedManagedBy: "",
+		},
+		{
+			name: "TestDefault_InvalidQueueName",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels:    map[string]string{constants.QueueLabel: "invalid-queue"},
+					Namespace: "default",
+				},
+			},
+			multiKueueEnabled: true,
+			expectedError:     errors.New("queue doesn't exist"),
+		},
+		{
+			name: "TestDefault_QueueNotFound",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels: map[string]string{
+						constants.QueueLabel: "non-existent-queue",
+					},
+					Namespace: "default",
+				},
+			},
+			multiKueueEnabled: true,
+			expectedError:     errors.New("queue doesn't exist"),
+		},
+		{
+			name: "TestDefault_AdmissionCheckNotFound",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels: map[string]string{
+						constants.QueueLabel: "local-queue",
+					},
+					Namespace: "default",
+				},
+			},
+			queues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("local-queue", "default").
+					ClusterQueue("cluster-queue").
+					Obj(),
+			},
+			clusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("cluster-queue").
+					AdmissionChecks("non-existent-admission-check").
+					Obj(),
+			},
+			multiKueueEnabled: true,
+			expectedManagedBy: "",
+		},
+		{
+			name: "TestDefault_MultiKueueFeatureDisabled",
+			jobSet: &jobset.JobSet{
+				ObjectMeta: ctrl.ObjectMeta{
+					Labels: map[string]string{
+						constants.QueueLabel: "local-queue",
+					},
+					Namespace: "default",
+				},
+			},
+			queues: []kueue.LocalQueue{
+				*utiltesting.MakeLocalQueue("local-queue", "default").
+					ClusterQueue("cluster-queue").
+					Obj(),
+			},
+			clusterQueues: []kueue.ClusterQueue{
+				*utiltesting.MakeClusterQueue("cluster-queue").
+					AdmissionChecks("admission-check").
+					Obj(),
+			},
+			admissionCheck: utiltesting.MakeAdmissionCheck("admission-check").
+				ControllerName(multikueue.ControllerName).
+				Active(metav1.ConditionTrue).
+				Obj(),
+			multiKueueEnabled: false,
+			expectedManagedBy: "",
+		},
+	}
+
+	for _, tc := range testCases {
+		t.Run(tc.name, func(t *testing.T) {
+			defer features.SetFeatureGateDuringTest(t, features.MultiKueue, tc.multiKueueEnabled)()
+
+			ctx, _ := utiltesting.ContextWithLog(t)
+
+			clientBuilder := utiltesting.NewClientBuilder().
+				WithObjects(
+					&corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: "default"}},
+				)
+			cl := clientBuilder.Build()
+			cqCache := cache.New(cl)
+			queueManager := queue.NewManager(cl, cqCache)
+
+			for _, q := range tc.queues {
+				if err := queueManager.AddLocalQueue(ctx, &q); err != nil {
+					t.Fatalf("Inserting queue %s/%s in manager: %v", q.Namespace, q.Name, err)
+				}
+			}
+			for _, cq := range tc.clusterQueues {
+				if err := cqCache.AddClusterQueue(ctx, &cq); err != nil {
+					t.Fatalf("Inserting clusterQueue %s in cache: %v", cq.Name, err)
+				}
+				if tc.admissionCheck != nil {
+					cqCache.AddOrUpdateAdmissionCheck(tc.admissionCheck)
+					if err := queueManager.AddClusterQueue(ctx, &cq); err != nil {
+						t.Fatalf("Inserting clusterQueue %s in manager: %v", cq.Name, err)
+					}
+				}
+			}
+			webhook := &JobSetWebhook{
+				manageJobsWithoutQueueName: false,
+				queues:                     queueManager,
+				cache:                      cqCache,
+			}
+
+			err := webhook.Default(ctx, tc.jobSet)
+			if err != nil && tc.expectedError == nil {
+				t.Errorf("Unexpected error: %v", err)
+			} else if err == nil && tc.expectedError != nil {
+				t.Errorf("Expected error %v, but got nil", tc.expectedError)
+			} else if err != nil && err.Error() != tc.expectedError.Error() {
+				t.Errorf("Expected error %v, but got %v", tc.expectedError, err)
+			}
+
+			if tc.jobSet.Spec.ManagedBy == nil && tc.expectedManagedBy != "" {
+				t.Errorf("Expected jobSet.Spec.ManagedBy to be %s, but got nil", tc.expectedManagedBy)
+			} else if tc.jobSet.Spec.ManagedBy != nil && *tc.jobSet.Spec.ManagedBy != tc.expectedManagedBy {
+				t.Errorf("Expected jobSet.Spec.ManagedBy to be %s, but got %v", tc.expectedManagedBy, *tc.jobSet.Spec.ManagedBy)
+			}
```

  Also, try to use `cmp.Diff` for comparison.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-04-29T12:31:04Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:79** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-29T12:31:04Z
```diff
@@ -57,6 +66,31 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+	if features.Enabled(features.MultiKueue) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(fmt.Sprintf("%s/%s", jobSet.ObjectMeta.Namespace, localQueueName))
```

  Added `queue.QueueKey(namespace, lqName)`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-06T13:47:31Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:71** by [@mimowo](https://github.com/mimowo) — 2024-05-06T13:47:31Z
```diff
@@ -57,6 +67,34 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+	if features.Enabled(features.MultiKueue) {
+		if jobSet.Spec.ManagedBy != nil && *jobSet.Spec.ManagedBy == jobsetapi.JobSetControllerName {
```

  ```suggestion
  		if jobSet.Spec.ManagedBy != nil && *jobSet.Spec.ManagedBy != jobsetapi.JobSetControllerName {
  ```
  We want to set `multikueue` controller if: `managedBy == nil || managedBy ==  jobsetapi.JobSetControllerName`, so this should be negation of this condition.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-06T13:48:40Z

- **pkg/controller/jobs/jobset/jobset_webhook_test.go:98** by [@mimowo](https://github.com/mimowo) — 2024-05-06T13:48:40Z
```diff
@@ -71,3 +82,202 @@ func TestValidateCreate(t *testing.T) {
 		})
 	}
 }
+
+func TestDefault(t *testing.T) {
+	testCases := []struct {
+		name              string
+		jobSet            *jobset.JobSet
+		queues            []kueue.LocalQueue
+		clusterQueues     []kueue.ClusterQueue
+		admissionCheck    *kueue.AdmissionCheck
+		multiKueueEnabled bool
+		wantManagedBy     *string
+		wantErr           error
+	}{
+		{
+			name: "TestDefault_JobSetManagedBy_jobsetapi.JobSetControllerName",
```

  Please add a test case when the user creates a jobset with custom managedBy, say: `managedBy: example.com/foo`.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-07T14:27:18Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:71** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-07T14:27:18Z
```diff
@@ -57,6 +67,34 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+	if features.Enabled(features.MultiKueue) {
+		if jobSet.Spec.ManagedBy != nil && *jobSet.Spec.ManagedBy == jobsetapi.JobSetControllerName {
```

  Extracted to https://github.com/kubernetes-sigs/kueue/pull/2048/commits/19824957b0123f253bd478caf25b85427c7ad198#diff-400dfd475cc7c218c95f8918f3b1da14443c724fa2e6669327532c89983e34a9R100-R104

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T14:50:13Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:102** by [@mimowo](https://github.com/mimowo) — 2024-05-07T14:50:13Z
```diff
@@ -57,9 +67,41 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canOverrideManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		clusterQueue, found := w.cache.GetClusterQueue(clusterQueueName)
+		if !found {
+			return nil
+		}
+		for admissionCheckName := range clusterQueue.AdmissionChecks {
+			admissionCheck, ok := w.cache.GetAdmissionCheck(admissionCheckName)
+			if !ok {
+				continue
+			}
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Overriding ManagedBy", "jobset", klog.KObj(jobSet), "managedBy", multikueue.ControllerName)
+				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+				break
+			}
+		}
+	}
+
 	return nil
 }
 
+func canOverrideManagedBy(jobSetSpecManagedBy *string) bool {
+	return features.Enabled(features.MultiKueue) &&
+		(jobSetSpecManagedBy == nil || (jobSetSpecManagedBy != nil && *jobSetSpecManagedBy == jobsetapi.JobSetControllerName))
```

  ```suggestion
  		(jobSetSpecManagedBy == nil || *jobSetSpecManagedBy == jobsetapi.JobSetControllerName)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T14:59:01Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:90** by [@mimowo](https://github.com/mimowo) — 2024-05-07T14:59:00Z
```diff
@@ -57,9 +67,41 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canOverrideManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		clusterQueue, found := w.cache.GetClusterQueue(clusterQueueName)
+		if !found {
+			return nil
+		}
+		for admissionCheckName := range clusterQueue.AdmissionChecks {
+			admissionCheck, ok := w.cache.GetAdmissionCheck(admissionCheckName)
+			if !ok {
+				continue
+			}
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Overriding ManagedBy", "jobset", klog.KObj(jobSet), "managedBy", multikueue.ControllerName)
```

  ```suggestion
  				log.V(5).Info("Defaulting managedBy", "jobset", klog.KObj(jobSet), "managedBy", multikueue.ControllerName)
  ```
  nit: since from 0.5.1 JobSet does not set the managedBy (so it will be nil).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T15:00:25Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:90** by [@mimowo](https://github.com/mimowo) — 2024-05-07T15:00:25Z
```diff
@@ -57,9 +67,41 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canOverrideManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		clusterQueue, found := w.cache.GetClusterQueue(clusterQueueName)
+		if !found {
+			return nil
+		}
+		for admissionCheckName := range clusterQueue.AdmissionChecks {
+			admissionCheck, ok := w.cache.GetAdmissionCheck(admissionCheckName)
+			if !ok {
+				continue
+			}
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Overriding ManagedBy", "jobset", klog.KObj(jobSet), "managedBy", multikueue.ControllerName)
```

  Can we also log the old value (`"oldManagedBy", jobSet.Spec.ManagedBy)`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T15:01:55Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:100** by [@mimowo](https://github.com/mimowo) — 2024-05-07T15:01:55Z
```diff
@@ -57,9 +67,41 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canOverrideManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		clusterQueue, found := w.cache.GetClusterQueue(clusterQueueName)
+		if !found {
+			return nil
+		}
+		for admissionCheckName := range clusterQueue.AdmissionChecks {
+			admissionCheck, ok := w.cache.GetAdmissionCheck(admissionCheckName)
+			if !ok {
+				continue
+			}
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Overriding ManagedBy", "jobset", klog.KObj(jobSet), "managedBy", multikueue.ControllerName)
+				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+				break
+			}
+		}
+	}
+
 	return nil
 }
 
+func canOverrideManagedBy(jobSetSpecManagedBy *string) bool {
```

  ```suggestion
  func canDefaultManagedBy(jobSetSpecManagedBy *string) bool {
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-07T17:35:14Z

- **pkg/cache/cache.go:227** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T17:32:39Z
```diff
@@ -224,6 +224,13 @@ func (c *Cache) DeleteAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string]
 	return c.updateClusterQueues()
 }
 
+func (c *Cache) GetAdmissionCheck(name string) (AdmissionCheck, bool) {
```

  ```suggestion
  func (c *Cache) AdmissionCheck(name string) (AdmissionCheck, bool) {
  ```

- **pkg/cache/cache.go:382** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T17:32:46Z
```diff
@@ -372,6 +379,13 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	metrics.ClearCacheMetrics(cq.Name)
 }
 
+func (c *Cache) GetClusterQueue(name string) (*ClusterQueue, bool) {
```

  ```suggestion
  func (c *Cache) ClusterQueue(name string) (*ClusterQueue, bool) {
  ```

- **pkg/controller/jobs/jobset/jobset_webhook.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T17:34:02Z
```diff
@@ -57,9 +67,41 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		clusterQueue, found := w.cache.GetClusterQueue(clusterQueueName)
+		if !found {
+			return nil
+		}
+		for admissionCheckName := range clusterQueue.AdmissionChecks {
+			admissionCheck, ok := w.cache.GetAdmissionCheck(admissionCheckName)
```

  this is taking multiple locks. Can we have a single function `AdmissionChecksForClusterQueue`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-08T06:17:09Z

- **pkg/cache/cache.go:241** by [@trasc](https://github.com/trasc) — 2024-05-08T06:15:42Z
```diff
@@ -224,6 +224,21 @@ func (c *Cache) DeleteAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string]
 	return c.updateClusterQueues()
 }
 
+func (c *Cache) AdmissionChecksForClusterQueue(cqName string) (acs []AdmissionCheck) {
+	c.RLock()
+	defer c.RUnlock()
+	cq, ok := c.clusterQueues[cqName]
+	if !ok {
+		return nil
+	}
+	for acName := range cq.AdmissionChecks {
+		if ac, ok := c.admissionChecks[acName]; ok {
+			acs = append(acs, ac)
+		}
+	}
+	return acs
+}
```

  nit: 
  ```suggestion
  func (c *Cache) AdmissionChecksForClusterQueue(cqName string) ([]AdmissionCheck) {
  	c.RLock()
  	defer c.RUnlock()
  	cq, ok := c.clusterQueues[cqName]
  	if !ok || len(cq.AdmissionChecks) == 0 {
  		return nil
  	}
  	acs := make([]AdmissionCheck, 0, len(cq.AdmissionChecks))
  	for acName := range cq.AdmissionChecks {
  		if ac, ok := c.admissionChecks[acName]; ok {
  			acs = append(acs, ac)
  		}
  	}
  	return acs
  }
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-08T08:31:14Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-08T08:47:37Z

- **site/content/en/docs/tasks/run/jobsets.md:128** by [@mimowo](https://github.com/mimowo) — 2024-05-08T08:47:37Z
```diff
@@ -128,60 +128,8 @@ spec:
 
 ### MultiKueue Environment
 
-```yaml
-# jobset-sample.yaml
-apiVersion: jobset.x-k8s.io/v1alpha2
-kind: JobSet
-metadata:
-  generateName: sleep-job-
-  labels:
-    kueue.x-k8s.io/queue-name: user-queue
-spec:
-  network:
-    enableDNSHostnames: false
-    subdomain: some-subdomain
-  replicatedJobs:
-    - name: workers
-      replicas: 2
-      template:
-        spec:
-          parallelism: 4
-          completions: 4
-          backoffLimit: 0
-          template:
-            spec:
-              containers:
-                - name: sleep
-                  image: busybox
-                  resources:
-                    requests:
-                      cpu: 1
-                      memory: "200Mi"
-                  command:
-                    - sleep
-                  args:
-                    - 100s
-    - name: driver
-      template:
-        spec:
-          parallelism: 1
-          completions: 1
-          backoffLimit: 0
-          template:
-            spec:
-              containers:
-                - name: sleep
-                  image: busybox
-                  resources:
-                    requests:
-                      cpu: 2
-                      memory: "200Mi"
-                  command:
-                    - sleep
-                  args:
-                    - 100s
-  managedBy: kueue.x-k8s.io/multikueue
-```
+The same `jobset-sample.yaml` file from [single cluster environment](#single-cluster-environment) can be used in a [MultiKueue environment](#multikueue-environment).
```

  nit:  please remove the sections `### Single Cluster Environment` and `### MultiKueue Environment`, and wrap the comment here in a `Note`, like [here](https://github.com/kubernetes-sigs/kueue/blob/85f8c67843fb81a074abe754bf84eda8dcca449e/site/content/en/docs/tasks/manage/setup_multikueue.md?plain=1#L19-L26).

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-08T20:05:17Z

- **site/content/en/docs/tasks/run/jobsets.md:128** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-08T20:05:17Z
```diff
@@ -128,60 +128,8 @@ spec:
 
 ### MultiKueue Environment
 
-```yaml
-# jobset-sample.yaml
-apiVersion: jobset.x-k8s.io/v1alpha2
-kind: JobSet
-metadata:
-  generateName: sleep-job-
-  labels:
-    kueue.x-k8s.io/queue-name: user-queue
-spec:
-  network:
-    enableDNSHostnames: false
-    subdomain: some-subdomain
-  replicatedJobs:
-    - name: workers
-      replicas: 2
-      template:
-        spec:
-          parallelism: 4
-          completions: 4
-          backoffLimit: 0
-          template:
-            spec:
-              containers:
-                - name: sleep
-                  image: busybox
-                  resources:
-                    requests:
-                      cpu: 1
-                      memory: "200Mi"
-                  command:
-                    - sleep
-                  args:
-                    - 100s
-    - name: driver
-      template:
-        spec:
-          parallelism: 1
-          completions: 1
-          backoffLimit: 0
-          template:
-            spec:
-              containers:
-                - name: sleep
-                  image: busybox
-                  resources:
-                    requests:
-                      cpu: 2
-                      memory: "200Mi"
-                  command:
-                    - sleep
-                  args:
-                    - 100s
-  managedBy: kueue.x-k8s.io/multikueue
-```
+The same `jobset-sample.yaml` file from [single cluster environment](#single-cluster-environment) can be used in a [MultiKueue environment](#multikueue-environment).
```

  https://github.com/kubernetes-sigs/kueue/pull/2048/commits/8ba9d7765022d2f81f070d740ce4c2049d3f74ec

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T07:09:34Z

- **pkg/cache/cache.go:397** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T06:47:37Z
```diff
@@ -372,6 +388,13 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	metrics.ClearCacheMetrics(cq.Name)
 }
 
+func (c *Cache) ClusterQueue(name string) (*ClusterQueue, bool) {
+	c.RLock()
+	defer c.RUnlock()
+	cq, ok := c.clusterQueues[name]
+	return cq, ok
+}
+
```

  Where do we use this function? It seems that this is never used anywhere.

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T07:02:08Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  @mimowo @alculquicondor Does this mean that we don't support MultiKueue for the Job without queue name? 
  
  https://github.com/kubernetes-sigs/kueue/blob/0a2c120c037fa9416736f87052fba6f60c4e27c6/apis/config/v1beta1/configuration_types.go#L41-L48

- **pkg/controller/jobs/jobset/jobset_webhook.go:94** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T07:02:57Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		for _, admissionCheck := range w.cache.AdmissionChecksForClusterQueue(clusterQueueName) {
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Defaulting ManagedBy", "jobset", klog.KObj(jobSet), "oldManagedBy", jobSet.Spec.ManagedBy, "managedBy", multikueue.ControllerName)
+				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+				break
+			}
+		}
+	}
+
 	return nil
 }
 
+func canDefaultManagedBy(jobSetSpecManagedBy *string) bool {
+	return features.Enabled(features.MultiKueue) &&
+		(jobSetSpecManagedBy == nil || *jobSetSpecManagedBy == jobsetapi.JobSetControllerName)
```

  ```suggestion
  	return features.Enabled(features.MultiKueue) && ptr.Equal(jobSetSpecManagedBy, jobsetapi.JobSetControllerName)
  ```

- **pkg/controller/jobs/jobset/jobset_webhook.go:84** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T07:03:48Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		for _, admissionCheck := range w.cache.AdmissionChecksForClusterQueue(clusterQueueName) {
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Defaulting ManagedBy", "jobset", klog.KObj(jobSet), "oldManagedBy", jobSet.Spec.ManagedBy, "managedBy", multikueue.ControllerName)
+				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+				break
```

  ```suggestion
  				return nil
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-09T07:41:35Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@mimowo](https://github.com/mimowo) — 2024-05-09T07:41:35Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  If the user does not specify the `queueName` we cannot know if the target CQ is MK, so we don't default `managedBy`. Still, it could work if the user manually specified the `managedBy: multikueue`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-09T07:42:36Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:94** by [@mimowo](https://github.com/mimowo) — 2024-05-09T07:42:36Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		for _, admissionCheck := range w.cache.AdmissionChecksForClusterQueue(clusterQueueName) {
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Defaulting ManagedBy", "jobset", klog.KObj(jobSet), "oldManagedBy", jobSet.Spec.ManagedBy, "managedBy", multikueue.ControllerName)
+				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+				break
+			}
+		}
+	}
+
 	return nil
 }
 
+func canDefaultManagedBy(jobSetSpecManagedBy *string) bool {
+	return features.Enabled(features.MultiKueue) &&
+		(jobSetSpecManagedBy == nil || *jobSetSpecManagedBy == jobsetapi.JobSetControllerName)
```

  Will this work? If `jobSet.spec.managedBy==nil`, then we want to default.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-09T07:43:28Z

- **site/content/en/docs/tasks/run/jobsets.md:128** by [@mimowo](https://github.com/mimowo) — 2024-05-09T07:43:28Z
```diff
@@ -128,60 +128,8 @@ spec:
 
 ### MultiKueue Environment
 
-```yaml
-# jobset-sample.yaml
-apiVersion: jobset.x-k8s.io/v1alpha2
-kind: JobSet
-metadata:
-  generateName: sleep-job-
-  labels:
-    kueue.x-k8s.io/queue-name: user-queue
-spec:
-  network:
-    enableDNSHostnames: false
-    subdomain: some-subdomain
-  replicatedJobs:
-    - name: workers
-      replicas: 2
-      template:
-        spec:
-          parallelism: 4
-          completions: 4
-          backoffLimit: 0
-          template:
-            spec:
-              containers:
-                - name: sleep
-                  image: busybox
-                  resources:
-                    requests:
-                      cpu: 1
-                      memory: "200Mi"
-                  command:
-                    - sleep
-                  args:
-                    - 100s
-    - name: driver
-      template:
-        spec:
-          parallelism: 1
-          completions: 1
-          backoffLimit: 0
-          template:
-            spec:
-              containers:
-                - name: sleep
-                  image: busybox
-                  resources:
-                    requests:
-                      cpu: 2
-                      memory: "200Mi"
-                  command:
-                    - sleep
-                  args:
-                    - 100s
-  managedBy: kueue.x-k8s.io/multikueue
-```
+The same `jobset-sample.yaml` file from [single cluster environment](#single-cluster-environment) can be used in a [MultiKueue environment](#multikueue-environment).
```

  lgtm, thanks

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T07:45:04Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:94** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T07:45:04Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
+		clusterQueueName, err := w.queues.ClusterQueueFromLocalQueue(queue.QueueKey(jobSet.ObjectMeta.Namespace, localQueueName))
+		if err != nil {
+			return err
+		}
+		for _, admissionCheck := range w.cache.AdmissionChecksForClusterQueue(clusterQueueName) {
+			if admissionCheck.Controller == multikueue.ControllerName {
+				log.V(5).Info("Defaulting ManagedBy", "jobset", klog.KObj(jobSet), "oldManagedBy", jobSet.Spec.ManagedBy, "managedBy", multikueue.ControllerName)
+				jobSet.Spec.ManagedBy = ptr.To(multikueue.ControllerName)
+				break
+			}
+		}
+	}
+
 	return nil
 }
 
+func canDefaultManagedBy(jobSetSpecManagedBy *string) bool {
+	return features.Enabled(features.MultiKueue) &&
+		(jobSetSpecManagedBy == nil || *jobSetSpecManagedBy == jobsetapi.JobSetControllerName)
```

  Oh, I see.
  You're right. Thanks for pointing this out!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T07:47:51Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T07:47:51Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  Thank you for the clarifications!
  So, expanding the documentation would be better.
  @vladikkuzn Could you update this docs?
  
  > {{% alert title="Note" color="note" %}} The same jobset-sample.yaml file from [single cluster environment](https://github.com/kubernetes-sigs/kueue/blob/8ba9d7765022d2f81f070d740ce4c2049d3f74ec/site/content/en/docs/tasks/run/jobsets.md#single-cluster-environment) can be used in a [MultiKueue environment](https://github.com/kubernetes-sigs/kueue/blob/8ba9d7765022d2f81f070d740ce4c2049d3f74ec/site/content/en/docs/tasks/run/jobsets.md#multikueue-environment). The spec.managedBy field will be set to kueue.x-k8s.io/multikueue automatically, if not specified. {{% /alert %}}

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-09T09:44:27Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@mimowo](https://github.com/mimowo) — 2024-05-09T09:44:27Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  Makes sense, maybe:
  ```
  The same jobset-sample.yaml file from [single cluster environment] can be used in a
  [MultiKueue environment].  The `spec.managedBy` field will be set to `kueue.x-k8s.io/multikueue`
  automatically, if not specified, as long as  the `kueue.x-k8s.io/queue-name` annotation
  is specified and the corresponding Cluster Queue uses the [Multi Kueue admission check]. 
  ```
  I think we don't need to mention `manageJobsWithoutQueueName: true` specifically, because the field is defaulted regardless, it just cannot be changed. So we could add maybe another sentence: "Please also note that `spec.managedBy` field is immutable". 
  
  WDYT @tenzen-y ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T12:01:06Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T12:01:06Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  > I think we don't need to mention manageJobsWithoutQueueName: true specifically, because the field is defaulted regardless, it just cannot be changed. 
  
  That makes sense. Instead of my recommendation above, how about expanding KueueConfig API documentation?
  
  > "Please also note that spec.managedBy field is immutable"
  
  This limitation doesn't come from kueue; it is brought us from JobSet, right?
  So, it might be better to improve JobSet documentation and just put it link here. HDYT?
  
  https://github.com/kubernetes-sigs/jobset/blob/7752a365fa5c9ab497c5b61d4dcd7c9e3609e9cb/pkg/webhooks/jobset_webhook.go#L231

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-09T12:12:11Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@mimowo](https://github.com/mimowo) — 2024-05-09T12:12:11Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  > That makes sense. Instead of my recommendation above, how about expanding KueueConfig API documentation?
  
  sgtm, do you have a specific suggestion?
  
  > This limitation doesn't come from kueue; it is brought us from JobSet, right?
  
  correct
  
  > So, it might be better to improve JobSet documentation and just put it link here. HDYT?
  
  We could add a note [here](https://github.com/kubernetes-sigs/jobset/blob/7752a365fa5c9ab497c5b61d4dcd7c9e3609e9cb/api/jobset/v1alpha2/jobset_types.go#L97), but it seems there is no JobSet reference built for now: https://jobset.sigs.k8s.io/docs/reference/.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T12:26:00Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T12:26:00Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  > > That makes sense. Instead of my recommendation above, how about expanding KueueConfig API documentation?
  > 
  > sgtm, do you have a specific suggestion?
  
  How about this to replace 
  
  > If set to true, then those jobs will be suspended and never started unless they are assigned a queue and eventually admitted. This also applies to jobs created before starting the kueue controller.
  
  with
  
  > If set to true, then those jobs will be suspended and never started unless they are assigned a queue and eventually admitted. This also applies to jobs created before starting the kueue controller. 
  Moreover, when using the MultiKueue feature, users must specify the ".spec.managedBy" name in the Jobs by themselves.
  
  > We could add a note [here](https://github.com/kubernetes-sigs/jobset/blob/7752a365fa5c9ab497c5b61d4dcd7c9e3609e9cb/api/jobset/v1alpha2/jobset_types.go#L97), but it seems there is no JobSet reference built for now: https://jobset.sigs.k8s.io/docs/reference/.
  
  That makes sense. Regarding reference, creating an issue would be better in the JobSet side.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-09T12:41:53Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@mimowo](https://github.com/mimowo) — 2024-05-09T12:41:53Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  > Moreover, when using the MultiKueue feature, users must specify the ".spec.managedBy" name in the Jobs by themselves.
  
  Maybe, but there is no code in the webhook that depends on `manageJobsWithoutQueueName` currently. Also, the webhook is JobSet-specific at the moment. As long as the queueName annotation is supplied, then `managedBy` is defaulted for JobSet. Maybe the note around [here](https://github.com/kubernetes-sigs/kueue/blob/8ba9d7765022d2f81f070d740ce4c2049d3f74ec/site/content/en/docs/tasks/run/jobsets.md#example-jobset) is enough?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T12:59:34Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T12:59:34Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  > Maybe, but there is no code in the webhook that depends on manageJobsWithoutQueueName currently.
  
  Yeah, you're right... It seems that my understanding for the `manageJobsWithoutQueueName` is not correct. So, my recommendation is no valid.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-09T13:02:51Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@mimowo](https://github.com/mimowo) — 2024-05-09T13:02:51Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  > That makes sense. Regarding reference, creating an issue would be better in the JobSet side.
  
  I have opened the Issue: https://github.com/kubernetes-sigs/jobset/issues/566
  and the PR: https://github.com/kubernetes-sigs/jobset/pull/565

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T14:09:15Z

- **pkg/controller/jobs/jobset/jobset_webhook.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T14:09:15Z
```diff
@@ -57,9 +67,33 @@ func (w *JobSetWebhook) Default(ctx context.Context, obj runtime.Object) error {
 	log.V(5).Info("Applying defaults", "jobset", klog.KObj(jobSet))
 
 	jobframework.ApplyDefaultForSuspend(jobSet, w.manageJobsWithoutQueueName)
+
+	if canDefaultManagedBy(jobSet.Spec.ManagedBy) {
+		localQueueName, found := jobSet.Labels[constants.QueueLabel]
+		if !found {
+			return nil
+		}
```

  Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T16:03:32Z

Thank you!
/approve

Leaving `lgtm` on @mimowo
