# PR #205: Added an option to control processing jobs with no queue name set

**Summary**: Added an option to control processing jobs with no queue name set

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/205

**Last updated**: 2022-04-12T10:28:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ahg-g](https://github.com/ahg-g)
- **Branch**: `ahg-kueue` → `main`
- **Created**: 2022-04-11T18:22:21Z
- **Updated**: 2022-04-12T10:28:49Z
- **Merged**: 2022-04-11T21:05:26Z
- **Closed**: 2022-04-11T21:05:26Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 6   **+141 / -59**

## Description


#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:
Adds an internal flag to control whether or not the job-controller reconciles jobs with no queue name set; by default those jobs are not processed.

This is important so users are not surprised that their existing running jobs get suspended when they first try Kueue.

This flag will be exposed to be configurable via ComponentConfig once we add it.

#### Which issue(s) this PR fixes:
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-11T18:22:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/205#" title="Author self-approved">ahg-g</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T18:22:48Z

/hold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-11T19:04:18Z

Wait, I see the option, but I don't see the command line flag for it.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T20:26:57Z

> Wait, I see the option, but I don't see the command line flag for it.

No command-line flag, this is an option in the controller that we will expose via Component Config. I don't want to add a flag just so we deprecate it right away. I will send a PR after this one for CC, it is not that complicated.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T21:05:06Z

/hold cancel

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-11T18:54:20Z

- **pkg/controller/workload/job/job_controller.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-11T18:38:32Z
```diff
@@ -104,6 +133,11 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
+	if queueName(&job) == "" && !r.processJobsWithoutQueueName {
+		log.V(2).Info(fmt.Sprintf("%s annotation is not set, ignoring the job", constants.QueueAnnotation))
```

  V(3)?

- **test/integration/controller/job/job_controller_test.go:248** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-11T18:43:36Z
```diff
@@ -221,3 +244,38 @@ var _ = ginkgo.Describe("Job controller", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
 	})
 })
+
+var _ = ginkgo.Describe("Job controller for workloads with no queue set", func() {
```

  nit: I would use a single `ginkgo.Describe` with two `ginkgo.Context`, because you are describing the same controller.

- **test/integration/controller/job/job_controller_test.go:260** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-11T18:48:34Z
```diff
@@ -221,3 +244,38 @@ var _ = ginkgo.Describe("Job controller", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
 	})
 })
+
+var _ = ginkgo.Describe("Job controller for workloads with no queue set", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+	ginkgo.It("Should reconcile jobs only when queue is set", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
```

  Did you forget a line for this?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-11T20:24:18Z

- **pkg/controller/workload/job/job_controller.go:137** by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T20:24:17Z
```diff
@@ -104,6 +133,11 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
+	if queueName(&job) == "" && !r.processJobsWithoutQueueName {
+		log.V(2).Info(fmt.Sprintf("%s annotation is not set, ignoring the job", constants.QueueAnnotation))
```

  done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-11T20:24:35Z

- **test/integration/controller/job/job_controller_test.go:248** by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T20:24:35Z
```diff
@@ -221,3 +244,38 @@ var _ = ginkgo.Describe("Job controller", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
 	})
 })
+
+var _ = ginkgo.Describe("Job controller for workloads with no queue set", func() {
```

  the indentation is too much for me :)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-11T20:25:30Z

- **test/integration/controller/job/job_controller_test.go:260** by [@ahg-g](https://github.com/ahg-g) — 2022-04-11T20:25:29Z
```diff
@@ -221,3 +244,38 @@ var _ = ginkgo.Describe("Job controller", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
 	})
 })
+
+var _ = ginkgo.Describe("Job controller for workloads with no queue set", func() {
+	ginkgo.BeforeEach(func() {
+		fwk = &framework.Framework{
+			ManagerSetup: managerSetup(),
+			CRDPath:      crdPath,
+		}
+		ctx, cfg, k8sClient = fwk.Setup()
+	})
+	ginkgo.AfterEach(func() {
+		fwk.Teardown()
+	})
+	ginkgo.It("Should reconcile jobs only when queue is set", func() {
+		ginkgo.By("checking the job gets suspended when created unsuspended")
```

  done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-11T20:57:18Z

/lgtm

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-12T01:38:10Z

- **pkg/controller/workload/job/job_controller.go:136** by [@kerthcet](https://github.com/kerthcet) — 2022-04-12T01:38:05Z
```diff
@@ -104,6 +133,11 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
+	if queueName(&job) == "" && !r.processJobsWithoutQueueName {
```

  Q: Should this be `queueName(&job) || "" && !r.processJobsWithoutQueueName`, if users don't set the queue name, we  do not have a default queue name for them now IIRC.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-12T02:40:38Z

- **pkg/controller/workload/job/job_controller.go:136** by [@ahg-g](https://github.com/ahg-g) — 2022-04-12T02:40:38Z
```diff
@@ -104,6 +133,11 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
+	if queueName(&job) == "" && !r.processJobsWithoutQueueName {
```

  Can you clarify again the question? this basically says if `r.processJobsWithoutQueueName=false`, then don't manage jobs that don't set the queue-name.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-12T02:41:38Z

- **pkg/controller/workload/job/job_controller.go:136** by [@ahg-g](https://github.com/ahg-g) — 2022-04-12T02:41:37Z
```diff
@@ -104,6 +133,11 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
+	if queueName(&job) == "" && !r.processJobsWithoutQueueName {
```

  A default queue name can be added, but it needs to be set via a webhook at job creation time if desired.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-12T02:51:52Z

- **pkg/controller/workload/job/job_controller.go:136** by [@kerthcet](https://github.com/kerthcet) — 2022-04-12T02:51:52Z
```diff
@@ -104,6 +133,11 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
+	if queueName(&job) == "" && !r.processJobsWithoutQueueName {
```

  > Can you clarify again the question? this basically says if `r.processJobsWithoutQueueName=false`, then don't manage jobs that don't set the queue-name.
  
  I mean whether we should only check  ` r.processJobsWithoutQueueName` here and if queueName not set, we will add a default queue name like `default` automatically in `ConstructWorkloadFor`. But this means we should have default queue and default cq in initialization.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-12T05:27:50Z

- **pkg/controller/workload/job/job_controller.go:136** by [@ahg-g](https://github.com/ahg-g) — 2022-04-12T05:27:49Z
```diff
@@ -104,6 +133,11 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
+	if queueName(&job) == "" && !r.processJobsWithoutQueueName {
```

  The problem we are trying to solve here is not that the queue is missing per se; see https://github.com/kubernetes-sigs/kueue/issues/63#issuecomment-1094301433
  
  basically we don't want to miss up with potentially existing jobs when kueue is first installed.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-12T10:28:49Z

- **pkg/controller/workload/job/job_controller.go:136** by [@kerthcet](https://github.com/kerthcet) — 2022-04-12T10:28:49Z
```diff
@@ -104,6 +133,11 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
+	if queueName(&job) == "" && !r.processJobsWithoutQueueName {
```

  moved discussion here https://github.com/kubernetes-sigs/kueue/issues/208
