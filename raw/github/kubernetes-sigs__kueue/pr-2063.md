# PR #2063: Fix transitions of Requeued condition

**Summary**: Fix transitions of Requeued condition

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2063

**Last updated**: 2024-05-30T14:24:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/transitions-of-requeued-conditions` → `main`
- **Created**: 2024-04-25T08:14:45Z
- **Updated**: 2024-05-30T14:24:43Z
- **Merged**: 2024-05-03T19:04:08Z
- **Closed**: 2024-05-03T19:04:09Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 11   **+726 / -42**

## Description

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:
- fixed Requeued condition transition on activate/deactivate workload
- fixed Requeued condition transition on WaitForPodsReady

#### Which issue(s) this PR fixes:
Fixes #2038 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-25T08:14:54Z

Hi @mbobrovskyi. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-25T08:15:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c1fa647b00dc027e75bc358b6f78a67642bc4ed6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66351cc7a09ac500085fc9df |

### Comment by [@trasc](https://github.com/trasc) — 2024-04-25T08:16:45Z

/assign
/ok-to-test
/test all

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-25T18:13:07Z

cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2024-04-26T11:47:52Z

> cc @mimowo

ack

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-26T11:54:17Z

/test pull-kueue-test-scheduling-perf-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T19:27:09Z

/test pull-kueue-test-e2e-main-1-29

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T17:56:33Z

Integration passed 1/5

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T18:07:21Z

Integration passed 2/5

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T18:16:54Z

Integration passed 3/5

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T18:27:39Z

Integration passed 4/5

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T18:40:07Z

Integration passed 5/5

/test pull-kueue-test-integration-main

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T19:01:06Z

/lgtm
/approve

If @mimowo has any suggestions, we can apply them in a follow up.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-03T19:01:12Z

LGTM label has been added.  <details>Git tree hash: e5287e4c21b2efecc94d6bf8fd1dec28022ab3e2</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-03T19:01:17Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2063#issuecomment-2093601723" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2063#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-06T09:32:25Z

> If @mimowo has any suggestions, we can apply them in a follow up.

@mbobrovskyi please verify and address (if needed) in a follow up the following comments: 
- https://github.com/kubernetes-sigs/kueue/pull/2063#discussion_r1590751039
- https://github.com/kubernetes-sigs/kueue/pull/2063#discussion_r1590754789

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-06T11:44:59Z

@mbobrovskyi thanks for handling the comments, so the only follow up would be to clear `RequeueAt` when we set `Requeued: true`.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-06T13:05:24Z

Fixed it on #2143.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T14:24:38Z

/release-note-edit
```release-note
NONE
```

Because this fixes an unreleased feature.

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-04-25T11:38:27Z

- **pkg/controller/core/workload_controller.go:179** by [@trasc](https://github.com/trasc) — 2024-04-25T08:26:23Z
```diff
@@ -165,10 +165,37 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	// If a deactivated workload is re-activated, we need to reset the RequeueState.
-	if wl.Status.RequeueState != nil && ptr.Deref(wl.Spec.Active, true) && workload.IsEvictedByDeactivation(&wl) {
-		wl.Status.RequeueState = nil
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		// If a deactivated workload is re-activated we need to set the WorkloadRequeued condition to true if already unset.
+		if workload.IsUnsetRequeuedByDeactivation(&wl) {
+			workload.SetRequeuedCondition(&wl, "ReactivateWorkload", "The workload is reactivated")
```

  Can we have an API constant for `"ReactivateWorkload"`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-25T11:48:09Z

- **pkg/controller/core/workload_controller.go:179** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-25T11:48:09Z
```diff
@@ -165,10 +165,37 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	// If a deactivated workload is re-activated, we need to reset the RequeueState.
-	if wl.Status.RequeueState != nil && ptr.Deref(wl.Spec.Active, true) && workload.IsEvictedByDeactivation(&wl) {
-		wl.Status.RequeueState = nil
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		// If a deactivated workload is re-activated we need to set the WorkloadRequeued condition to true if already unset.
+		if workload.IsUnsetRequeuedByDeactivation(&wl) {
+			workload.SetRequeuedCondition(&wl, "ReactivateWorkload", "The workload is reactivated")
```

  Added

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-26T12:47:22Z

LGTM overall, I'm yet wondering if we handle properly QueueStopped scenario.

- **pkg/workload/workload.go:600** by [@mimowo](https://github.com/mimowo) — 2024-04-26T12:21:49Z
```diff
@@ -572,6 +586,15 @@ func IsEvictedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
 	return cond, true
 }
 
+// IsUnsetRequeuedByPodsReadyTimeout returns true if the workload is unset requeued by pods ready timeout.
+func IsUnsetRequeuedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
+	cond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadRequeued)
+	if cond == nil || cond.Status != metav1.ConditionFalse || cond.Reason != kueue.WorkloadEvictedByPodsReadyTimeout {
+		return nil, false
+	}
+	return cond, true
```

  ```suggestion
  	return cond != nil && cond.Status == metav1.ConditionFalse && cond.Reason == kueue.WorkloadEvictedByPodsReadyTimeout
  ```
  as above, much easier to read

- **pkg/controller/core/workload_controller.go:174** by [@mimowo](https://github.com/mimowo) — 2024-04-26T12:25:13Z
```diff
@@ -165,10 +165,37 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	// If a deactivated workload is re-activated, we need to reset the RequeueState.
-	if wl.Status.RequeueState != nil && ptr.Deref(wl.Spec.Active, true) && workload.IsEvictedByDeactivation(&wl) {
-		wl.Status.RequeueState = nil
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		// If a deactivated workload is re-activated we need to set the WorkloadRequeued condition to true if already unset.
+		if workload.IsUnsetRequeuedByDeactivation(&wl) {
+			workload.SetRequeuedCondition(&wl, kueue.WorkloadRequeuedByReactivation, "The workload is reactivated")
+			updated = true
+		}
+
+		// Wait for requeueAt and set the WorkloadRequeued condition to true.
+		if condition, isUnset := workload.IsUnsetRequeuedByPodsReadyTimeout(&wl); condition != nil && isUnset {
+			var requeueAfter time.Duration
+			if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+				requeueAfter = time.Until(wl.Status.RequeueState.RequeueAt.Time)
```

  Compute the time using the r.clock. should be available on the main branch. It allows us to use fakeClock for testing.

- **pkg/controller/jobs/job/job_controller_test.go:807** by [@mimowo](https://github.com/mimowo) — 2024-04-26T12:38:32Z
```diff
@@ -668,6 +668,326 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"when workload is evicted due to pods ready timeout, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "job", Namespace: "ns"},
+					EventType: "Normal",
+					Reason:    "Stopped",
+					Message:   "Exceeded the PodsReady timeout",
+				},
+			},
+		},
+		"when workload is evicted due to admission check, job gets suspended": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByAdmissionCheck,
+						Message: "At least one admission check is false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "At least one admission check is false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByAdmissionCheck,
+						Message: "At least one admission check is false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByAdmissionCheck,
+						Message: "At least one admission check is false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "job", Namespace: "ns"},
+					EventType: "Normal",
+					Reason:    "Stopped",
+					Message:   "At least one admission check is false",
+				},
+			},
+		},
+		"when workload is evicted due to cluster queue stopped, job gets suspended": {
```

  Thanks for adding this scenario for completeness. I'm wondering if this is not a scenario that we missed, and we should also say Requeued=false here. Then, set Requeued=true when the CQ is no longer stopped. WDYT @alculquicondor ?

- **apis/kueue/v1beta1/workload_types.go:335** by [@mimowo](https://github.com/mimowo) — 2024-04-26T12:43:37Z
```diff
@@ -329,6 +329,12 @@ const (
 	WorkloadEvictedByDeactivation = "InactiveWorkload"
 )
 
+const (
+	// WorkloadRequeuedByReactivation indicates that the workload was requeued
+	// because spec.active is set to true.
+	WorkloadRequeuedByReactivation = "ReactivateWorkload"
```

  ```suggestion
  	WorkloadReactivated = "WorkloadReactivated"
  ```
  It is a `reason`. It will be clear that it is `Requeued` from the condition type.

- **apis/kueue/v1beta1/workload_types.go:366** by [@mimowo](https://github.com/mimowo) — 2024-04-26T12:46:36Z
```diff
@@ -329,6 +329,12 @@ const (
 	WorkloadEvictedByDeactivation = "InactiveWorkload"
 )
 
+const (
```

  nit: put together with other workload reasons

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-26T16:07:29Z

- **pkg/workload/workload.go:600** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-26T16:07:28Z
```diff
@@ -572,6 +586,15 @@ func IsEvictedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
 	return cond, true
 }
 
+// IsUnsetRequeuedByPodsReadyTimeout returns true if the workload is unset requeued by pods ready timeout.
+func IsUnsetRequeuedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
+	cond := apimeta.FindStatusCondition(w.Status.Conditions, kueue.WorkloadRequeued)
+	if cond == nil || cond.Status != metav1.ConditionFalse || cond.Reason != kueue.WorkloadEvictedByPodsReadyTimeout {
+		return nil, false
+	}
+	return cond, true
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-26T16:09:36Z

- **pkg/controller/core/workload_controller.go:174** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-26T16:09:36Z
```diff
@@ -165,10 +165,37 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	// If a deactivated workload is re-activated, we need to reset the RequeueState.
-	if wl.Status.RequeueState != nil && ptr.Deref(wl.Spec.Active, true) && workload.IsEvictedByDeactivation(&wl) {
-		wl.Status.RequeueState = nil
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		// If a deactivated workload is re-activated we need to set the WorkloadRequeued condition to true if already unset.
+		if workload.IsUnsetRequeuedByDeactivation(&wl) {
+			workload.SetRequeuedCondition(&wl, kueue.WorkloadRequeuedByReactivation, "The workload is reactivated")
+			updated = true
+		}
+
+		// Wait for requeueAt and set the WorkloadRequeued condition to true.
+		if condition, isUnset := workload.IsUnsetRequeuedByPodsReadyTimeout(&wl); condition != nil && isUnset {
+			var requeueAfter time.Duration
+			if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+				requeueAfter = time.Until(wl.Status.RequeueState.RequeueAt.Time)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-26T16:10:28Z

- **apis/kueue/v1beta1/workload_types.go:335** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-26T16:10:28Z
```diff
@@ -329,6 +329,12 @@ const (
 	WorkloadEvictedByDeactivation = "InactiveWorkload"
 )
 
+const (
+	// WorkloadRequeuedByReactivation indicates that the workload was requeued
+	// because spec.active is set to true.
+	WorkloadRequeuedByReactivation = "ReactivateWorkload"
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-26T16:10:48Z

- **apis/kueue/v1beta1/workload_types.go:366** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-26T16:10:48Z
```diff
@@ -329,6 +329,12 @@ const (
 	WorkloadEvictedByDeactivation = "InactiveWorkload"
 )
 
+const (
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-26T16:29:50Z

- **apis/kueue/v1beta1/workload_types.go:333** by [@mimowo](https://github.com/mimowo) — 2024-04-26T16:29:50Z
```diff
@@ -327,6 +327,10 @@ const (
 	// WorkloadEvictedByDeactivation indicates that the workload was evicted
 	// because spec.active is set to false.
 	WorkloadEvictedByDeactivation = "InactiveWorkload"
+
+	// WorkloadReactivated indicates that the workload was requeued because
+	// spec.active is set to true after deactivation.
+	WorkloadReactivated = "WorkloadReactivated"
```

  ```suggestion
  	WorkloadReactivated = "Reactivated"
  ```
  I guess this "Workload" is assumed as subject, since this is a workload's condition.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-26T16:36:09Z

- **apis/kueue/v1beta1/workload_types.go:333** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-26T16:36:09Z
```diff
@@ -327,6 +327,10 @@ const (
 	// WorkloadEvictedByDeactivation indicates that the workload was evicted
 	// because spec.active is set to false.
 	WorkloadEvictedByDeactivation = "InactiveWorkload"
+
+	// WorkloadReactivated indicates that the workload was requeued because
+	// spec.active is set to true after deactivation.
+	WorkloadReactivated = "WorkloadReactivated"
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-04-26T16:42:03Z

- **pkg/controller/core/workload_controller.go:163** by [@mimowo](https://github.com/mimowo) — 2024-04-26T16:42:03Z
```diff
@@ -152,10 +152,37 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	// If a deactivated workload is re-activated, we need to reset the RequeueState.
-	if wl.Status.RequeueState != nil && ptr.Deref(wl.Spec.Active, true) && workload.IsEvictedByDeactivation(&wl) {
-		wl.Status.RequeueState = nil
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+	if ptr.Deref(wl.Spec.Active, true) {
```

  I'm thinking we should move this block below L188 (handling of deletion), and maybe even L192 (handling of workload finished). The reason being, that now we handle here PodsReadyTimeout, and this may result in waiting potentially arbitrarily long for requeue, the waiting times grow exponentially like 10s, 20s, 40s... . 
  
  However, it is possible a user deletes the workload in the meanwhile, and we wait unnecessarily for the requeue before removing the finalizer. Similarly, a user may mark the workload as finished. 
  
  Could you check if this is the case, then please fix (not highest priority, so could be follow up), plus add a test?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-26T16:53:23Z

- **pkg/controller/jobs/job/job_controller_test.go:807** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-26T16:53:23Z
```diff
@@ -668,6 +668,326 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"when workload is evicted due to pods ready timeout, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "job", Namespace: "ns"},
+					EventType: "Normal",
+					Reason:    "Stopped",
+					Message:   "Exceeded the PodsReady timeout",
+				},
+			},
+		},
+		"when workload is evicted due to admission check, job gets suspended": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByAdmissionCheck,
+						Message: "At least one admission check is false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "At least one admission check is false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByAdmissionCheck,
+						Message: "At least one admission check is false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByAdmissionCheck,
+						Message: "At least one admission check is false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "job", Namespace: "ns"},
+					EventType: "Normal",
+					Reason:    "Stopped",
+					Message:   "At least one admission check is false",
+				},
+			},
+		},
+		"when workload is evicted due to cluster queue stopped, job gets suspended": {
```

  Yes, it should be similar to setting Active=false on the workload.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-29T05:47:29Z

- **pkg/controller/core/workload_controller.go:163** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-29T05:47:29Z
```diff
@@ -152,10 +152,37 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling Workload")
 
-	// If a deactivated workload is re-activated, we need to reset the RequeueState.
-	if wl.Status.RequeueState != nil && ptr.Deref(wl.Spec.Active, true) && workload.IsEvictedByDeactivation(&wl) {
-		wl.Status.RequeueState = nil
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+	if ptr.Deref(wl.Spec.Active, true) {
```

  Yes, you right. Moved it and added tests.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-29T05:47:56Z

- **pkg/controller/jobs/job/job_controller_test.go:807** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-29T05:47:56Z
```diff
@@ -668,6 +668,326 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"when workload is evicted due to pods ready timeout, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "job", Namespace: "ns"},
+					EventType: "Normal",
+					Reason:    "Stopped",
+					Message:   "Exceeded the PodsReady timeout",
+				},
+			},
+		},
+		"when workload is evicted due to admission check, job gets suspended": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByAdmissionCheck,
+						Message: "At least one admission check is false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "At least one admission check is false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByAdmissionCheck,
+						Message: "At least one admission check is false",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByAdmissionCheck,
+						Message: "At least one admission check is false",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "job", Namespace: "ns"},
+					EventType: "Normal",
+					Reason:    "Stopped",
+					Message:   "At least one admission check is false",
+				},
+			},
+		},
+		"when workload is evicted due to cluster queue stopped, job gets suspended": {
```

  Added this logic.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-01T18:07:27Z

- **apis/kueue/v1beta1/workload_types.go:341** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:08:22Z
```diff
@@ -327,6 +327,18 @@ const (
 	// WorkloadEvictedByDeactivation indicates that the workload was evicted
 	// because spec.active is set to false.
 	WorkloadEvictedByDeactivation = "InactiveWorkload"
+
+	// WorkloadReactivated indicates that the workload was requeued because
+	// spec.active is set to true after deactivation.
+	WorkloadReactivated = "Reactivated"
+
+	// WorkloadBackoffFinished indicates that the workload was requeued because
+	// backoff finished.
+	WorkloadBackoffFinished = "BackoffFinished"
+
+	// WorkloadClusterQueueStarted indicates that the workload was requeued because
+	// cluster queue started.
+	WorkloadClusterQueueStarted = "ClusterQueueStarted"
```

  ```suggestion
  	// cluster queue was restarted after being stopped.
  	WorkloadClusterQueueRestarted = "ClusterQueueRestarted"
  ```

- **pkg/controller/core/workload_controller.go:173** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:18:02Z
```diff
@@ -166,9 +160,52 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		// If a deactivated workload is re-activated we need to set the WorkloadRequeued condition to true.
+		if workload.IsUnsetRequeuedByDeactivation(&wl) {
```

  `IsUnsetRequeuedByDeactivation` reads very poorly.
  
  I would suggest something like this:
  
  ```golang
  if cond := meta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond.Status == False {
    switch cond.Reason {
      ...
    }
  }
  ```

- **pkg/controller/core/workload_controller_test.go:552** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:25:48Z
```diff
@@ -549,7 +549,7 @@ func TestReconcile(t *testing.T) {
 				RequeueState(ptr.To[int32](4), ptr.To(metav1.NewTime(testStartTime.Add(80*time.Second).Truncate(time.Second)))).
 				Obj(),
 		},
-		"deactivated workload": {
+		"deactivate workload by reached re-queue backoffLimitCount": {
```

  ```suggestion
  		"deactivate workload when reaching backoffLimitCount": {
  ```

- **pkg/controller/core/workload_controller_test.go:552** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:27:19Z
```diff
@@ -549,7 +549,7 @@ func TestReconcile(t *testing.T) {
 				RequeueState(ptr.To[int32](4), ptr.To(metav1.NewTime(testStartTime.Add(80*time.Second).Truncate(time.Second)))).
 				Obj(),
 		},
-		"deactivated workload": {
+		"deactivate workload by reached re-queue backoffLimitCount": {
```

  Also, shouldn't this case get the `Requeued` condition set to false?

- **pkg/controller/core/workload_controller_test.go:613** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:29:08Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByDeactivation,
+					Message: "The workload is deactivated",
+				}).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionTrue,
+					Reason:  kueue.WorkloadReactivated,
+					Message: "The workload is reactivated",
+				}).
+				Obj(),
+		},
+		"should check the WorkloadRequeued condition on pods ready timeout": {
```

  ```suggestion
  		"should keep the WorkloadRequeued condition until the backoff expires": {
  ```
  ?
  Although I'm not sure if I understood the test correctly.

- **pkg/controller/core/workload_controller_test.go:635** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:30:54Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByDeactivation,
+					Message: "The workload is deactivated",
+				}).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionTrue,
+					Reason:  kueue.WorkloadReactivated,
+					Message: "The workload is reactivated",
+				}).
+				Obj(),
+		},
+		"should check the WorkloadRequeued condition on pods ready timeout": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
+				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(60*time.Second).Truncate(time.Second)))).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
+				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(60*time.Second).Truncate(time.Second)))).
+				Obj(),
+		},
+		"should set the WorkloadRequeued condition to true on pods ready timeout": {
```

  ```suggestion
  		"should set the WorkloadRequeued condition when backoff expires": {
  ```

- **pkg/controller/core/workload_controller_test.go:645** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:31:07Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByDeactivation,
+					Message: "The workload is deactivated",
+				}).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionTrue,
+					Reason:  kueue.WorkloadReactivated,
+					Message: "The workload is reactivated",
+				}).
+				Obj(),
+		},
+		"should check the WorkloadRequeued condition on pods ready timeout": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
+				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(60*time.Second).Truncate(time.Second)))).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
+				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(60*time.Second).Truncate(time.Second)))).
+				Obj(),
+		},
+		"should set the WorkloadRequeued condition to true on pods ready timeout": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
```

  Add a RequeueState.

- **pkg/workload/workload.go:588** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:37:22Z
```diff
@@ -577,6 +585,24 @@ func IsEvictedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
 	return cond, true
 }
 
+// IsUnsetRequeuedByDeactivation returns true if the workload is unset requeued by deactivation.
```

  Let's remove these functions

- **test/integration/controller/core/workload_controller_test.go:474** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:39:39Z
```diff
@@ -452,4 +448,144 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 			gomega.Expect(finalQueueWorkload.Spec.Priority).To(gomega.Equal(&initialPriority))
 		})
 	})
+
+	ginkgo.When("the workload is in a backoff state", func() {
+
+		var flavor *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavor = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor, true)
+		})
+
+		ginkgo.It("should handle the workload deletion", func() {
```

  why are you testing workload deletion? Did something change about this behavior?

- **test/integration/controller/core/workload_controller_test.go:520** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T17:40:20Z
```diff
@@ -452,4 +448,144 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 			gomega.Expect(finalQueueWorkload.Spec.Priority).To(gomega.Equal(&initialPriority))
 		})
 	})
+
+	ginkgo.When("the workload is in a backoff state", func() {
+
+		var flavor *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavor = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor, true)
+		})
+
+		ginkgo.It("should handle the workload deletion", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue(localQueue.Name).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+			createdWl := kueue.Workload{}
+
+			ginkgo.By("creating the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &createdWl)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("setting the requeued condition", func() {
+				message := fmt.Sprintf("Exceeded the PodsReady timeout ns %s", ns.Name)
+				requeueAt := metav1.NewTime(time.Now().Add(util.LongTimeout))
+				createdWl.Status.RequeueState = &kueue.RequeueState{
+					RequeueAt: ptr.To(requeueAt),
+					Count:     ptr.To(int32(1)),
+				}
+				workload.UnsetRequeuedCondition(&createdWl, kueue.WorkloadEvictedByPodsReadyTimeout, message)
+				gomega.Expect(k8sClient.Status().Update(ctx, &createdWl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &createdWl)).To(gomega.Succeed())
+					g.Expect(createdWl.Status.RequeueState).ToNot(gomega.BeNil())
+					g.Expect(createdWl.Status.Conditions).To(gomega.ContainElements(
+						gomega.BeComparableTo(metav1.Condition{
+							Type:    kueue.WorkloadRequeued,
+							Status:  metav1.ConditionFalse,
+							Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+							Message: message,
+						}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("deleting the workload", func() {
+				gomega.Expect(k8sClient.Delete(ctx, &createdWl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &createdWl)).ToNot(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("should handle the workload finished", func() {
```

  This looks like something that cab be handled in unit tests.

- **test/integration/controller/jobs/job/job_controller_test.go:1778** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T18:07:24Z
```diff
@@ -1775,6 +1775,38 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 			wll.Spec.Active = ptr.To(false)
 			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
 
+			gomega.Eventually(func(g gomega.Gomega) {
```

  Also add similar validations for existing integration tests for PodsReadyTimeout.
  
  I'm interested to see the whole transition from Admitted, to Evicted (Requeued=false), and then Requeue=True.
  
  IIRC, it should be feasible to control the time passing from the test (without using time.Sleep)

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-01T18:56:08Z

- **pkg/controller/core/workload_controller_test.go:552** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-01T18:56:08Z
```diff
@@ -549,7 +549,7 @@ func TestReconcile(t *testing.T) {
 				RequeueState(ptr.To[int32](4), ptr.To(metav1.NewTime(testStartTime.Add(80*time.Second).Truncate(time.Second)))).
 				Obj(),
 		},
-		"deactivated workload": {
+		"deactivate workload by reached re-queue backoffLimitCount": {
```

  For this case no. Because deactivation handling on ReconcileGenericJob (reconcile.go) and unit test don't cover it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-01T18:57:46Z

- **pkg/controller/core/workload_controller_test.go:613** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-01T18:57:46Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByDeactivation,
+					Message: "The workload is deactivated",
+				}).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionTrue,
+					Reason:  kueue.WorkloadReactivated,
+					Message: "The workload is reactivated",
+				}).
+				Obj(),
+		},
+		"should check the WorkloadRequeued condition on pods ready timeout": {
```

  Yes, correct.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-01T19:06:50Z

- **pkg/controller/core/workload_controller_test.go:552** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T19:06:50Z
```diff
@@ -549,7 +549,7 @@ func TestReconcile(t *testing.T) {
 				RequeueState(ptr.To[int32](4), ptr.To(metav1.NewTime(testStartTime.Add(80*time.Second).Truncate(time.Second)))).
 				Obj(),
 		},
-		"deactivated workload": {
+		"deactivate workload by reached re-queue backoffLimitCount": {
```

  Then we should cover this in integration. There should be a test for it, we just need to add the checks for the conditions.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T08:55:26Z

- **apis/kueue/v1beta1/workload_types.go:341** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T08:55:26Z
```diff
@@ -327,6 +327,18 @@ const (
 	// WorkloadEvictedByDeactivation indicates that the workload was evicted
 	// because spec.active is set to false.
 	WorkloadEvictedByDeactivation = "InactiveWorkload"
+
+	// WorkloadReactivated indicates that the workload was requeued because
+	// spec.active is set to true after deactivation.
+	WorkloadReactivated = "Reactivated"
+
+	// WorkloadBackoffFinished indicates that the workload was requeued because
+	// backoff finished.
+	WorkloadBackoffFinished = "BackoffFinished"
+
+	// WorkloadClusterQueueStarted indicates that the workload was requeued because
+	// cluster queue started.
+	WorkloadClusterQueueStarted = "ClusterQueueStarted"
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T08:55:33Z

- **pkg/controller/core/workload_controller.go:173** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T08:55:32Z
```diff
@@ -166,9 +160,52 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		// If a deactivated workload is re-activated we need to set the WorkloadRequeued condition to true.
+		if workload.IsUnsetRequeuedByDeactivation(&wl) {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T08:56:06Z

- **pkg/controller/core/workload_controller_test.go:613** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T08:56:06Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByDeactivation,
+					Message: "The workload is deactivated",
+				}).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionTrue,
+					Reason:  kueue.WorkloadReactivated,
+					Message: "The workload is reactivated",
+				}).
+				Obj(),
+		},
+		"should check the WorkloadRequeued condition on pods ready timeout": {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T08:56:17Z

- **pkg/controller/core/workload_controller_test.go:635** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T08:56:17Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByDeactivation,
+					Message: "The workload is deactivated",
+				}).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionTrue,
+					Reason:  kueue.WorkloadReactivated,
+					Message: "The workload is reactivated",
+				}).
+				Obj(),
+		},
+		"should check the WorkloadRequeued condition on pods ready timeout": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
+				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(60*time.Second).Truncate(time.Second)))).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
+				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(60*time.Second).Truncate(time.Second)))).
+				Obj(),
+		},
+		"should set the WorkloadRequeued condition to true on pods ready timeout": {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T08:56:44Z

- **pkg/workload/workload.go:588** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T08:56:44Z
```diff
@@ -577,6 +585,24 @@ func IsEvictedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
 	return cond, true
 }
 
+// IsUnsetRequeuedByDeactivation returns true if the workload is unset requeued by deactivation.
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-02T09:28:10Z

- **pkg/controller/core/workload_controller.go:187** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-02T08:24:47Z
```diff
@@ -166,9 +160,52 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		// If a deactivated workload is re-activated we need to set the WorkloadRequeued condition to true.
+		if workload.IsUnsetRequeuedByDeactivation(&wl) {
+			workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload is reactivated")
+			updated = true
+		}
+
+		// Wait for requeueAt and set the WorkloadRequeued condition to true.
+		if workload.IsUnsetRequeuedByPodsReadyTimeout(&wl) {
+			var requeueAfter time.Duration
+			if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+				requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+			}
+			if requeueAfter > 0 {
+				return reconcile.Result{RequeueAfter: requeueAfter}, nil
+			}
```

  If we return nil here, we will miss to reset the `RequeueState`.
  So, we should set the `requeueAfter` in Line 192.

- **pkg/controller/core/workload_controller_test.go:593** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-02T08:54:28Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
```

  Could you verify the `requeueState` as well?

- **pkg/controller/jobs/job/job_controller_test.go:698** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-02T09:01:43Z
```diff
@@ -668,6 +668,326 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"when workload is evicted due to pods ready timeout, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
```

  I guess that this admission check is irrelevant in this case, right?
  If so, could you eliminate this admissionCheck?

- **pkg/controller/jobs/raycluster/raycluster_controller_test.go:331** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-02T09:05:32Z
```diff
@@ -328,7 +328,6 @@ func TestReconciler(t *testing.T) {
 					Condition(metav1.Condition{
 						Type:               kueue.WorkloadRequeued,
 						Status:             metav1.ConditionTrue,
-						Reason:             "Pending",
```

  Why is this reason empty?

- **pkg/workload/workload.go:395** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-02T09:23:48Z
```diff
@@ -394,6 +390,18 @@ func SetRequeuedCondition(wl *kueue.Workload, reason string, message string) {
 	apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
 }
 
+// UnsetRequeuedCondition sets the WorkloadRequeued condition to false
+func UnsetRequeuedCondition(wl *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadRequeued,
+		Status:             metav1.ConditionFalse,
+		Reason:             reason,
+		Message:            api.TruncateConditionMessage(message),
+		ObservedGeneration: wl.Generation,
+	}
+	apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+}
```

  Could you commonize the `SetRequeuedCondition` and the `UnSetRequeuedCondition` functions?
  Also, could you re-title the `UnSetRequeuedCondition` to something like the `DisableRequeuedCondition` since the `UnsetRequeuedCondition` never unset the `Requeued` condition?
  @alculquicondor WDYT about the function name, `DisableRequeuedCondition`? Any recommended name?
  
  ```golang
  func SetRequeuedCondition(wl *kueue.Workload, reason, message string) {
  	updateRequeuedCondition(wl, reason, message, metav1.ConditionTrue)
  }
  
  func DisableRequeuedCondition(wl *Workload, reason, message string) {
  	updateRequeuedCondition(wl, reason, message, metav1.ConditionFalse)
  }
  
  func updateRequeuedCondition(wl *Workload, reason, message string, status metav1.ConditionStatus) {
  	condition := metav1.Condition{
  		Type:               kueue.WorkloadRequeued,
  		Status:             status,
  		Reason:             reason,
  		Message:            api.TruncateConditionMessage(message),
  		ObservedGeneration: wl.Generation,
  	}
  	apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
  }
  ```

- **pkg/workload/workload.go:589** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-02T09:25:08Z
```diff
@@ -577,6 +585,12 @@ func IsEvictedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
 	return cond, true
 }
 
+// IsUnsetRequeuedByClusterQueueStopped returns true if the workload is unset requeued by cluster queue stopped.
+func IsUnsetRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
```

  ```suggestion
  func IsDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
  ```
  
  I mentioned above, could we re-title this function name to smoothing like `disable`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T09:58:14Z

- **pkg/controller/core/workload_controller.go:187** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T09:58:14Z
```diff
@@ -166,9 +160,52 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		// If a deactivated workload is re-activated we need to set the WorkloadRequeued condition to true.
+		if workload.IsUnsetRequeuedByDeactivation(&wl) {
+			workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload is reactivated")
+			updated = true
+		}
+
+		// Wait for requeueAt and set the WorkloadRequeued condition to true.
+		if workload.IsUnsetRequeuedByPodsReadyTimeout(&wl) {
+			var requeueAfter time.Duration
+			if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+				requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+			}
+			if requeueAfter > 0 {
+				return reconcile.Result{RequeueAfter: requeueAfter}, nil
+			}
```

  Should we reset the `RequeueState` on this case?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T10:31:57Z

- **pkg/controller/jobs/raycluster/raycluster_controller_test.go:331** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T10:31:57Z
```diff
@@ -328,7 +328,6 @@ func TestReconciler(t *testing.T) {
 					Condition(metav1.Condition{
 						Type:               kueue.WorkloadRequeued,
 						Status:             metav1.ConditionTrue,
-						Reason:             "Pending",
```

  On the request we are using abstract (without Reason and Message) evicted condition. So thats why on result we get it empty. I will set `WorkloadEvictedByDeactivation`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T18:48:35Z

- **pkg/controller/core/workload_controller_test.go:645** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T18:48:35Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByDeactivation,
+					Message: "The workload is deactivated",
+				}).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionTrue,
+					Reason:  kueue.WorkloadReactivated,
+					Message: "The workload is reactivated",
+				}).
+				Obj(),
+		},
+		"should check the WorkloadRequeued condition on pods ready timeout": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
+				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(60*time.Second).Truncate(time.Second)))).
+				Obj(),
+			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
+				RequeueState(ptr.To[int32](1), ptr.To(metav1.NewTime(testStartTime.Add(60*time.Second).Truncate(time.Second)))).
+				Obj(),
+		},
+		"should set the WorkloadRequeued condition to true on pods ready timeout": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				Active(true).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadRequeued,
+					Status:  metav1.ConditionFalse,
+					Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+					Message: "Exceeded the PodsReady timeout ns",
+				}).
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T18:49:10Z

- **test/integration/controller/core/workload_controller_test.go:520** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T18:49:10Z
```diff
@@ -452,4 +448,144 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 			gomega.Expect(finalQueueWorkload.Spec.Priority).To(gomega.Equal(&initialPriority))
 		})
 	})
+
+	ginkgo.When("the workload is in a backoff state", func() {
+
+		var flavor *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavor = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor, true)
+		})
+
+		ginkgo.It("should handle the workload deletion", func() {
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue(localQueue.Name).
+				Finalizers(kueue.ResourceInUseFinalizerName).
+				Obj()
+			wlKey := client.ObjectKeyFromObject(wl)
+			createdWl := kueue.Workload{}
+
+			ginkgo.By("creating the workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &createdWl)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("setting the requeued condition", func() {
+				message := fmt.Sprintf("Exceeded the PodsReady timeout ns %s", ns.Name)
+				requeueAt := metav1.NewTime(time.Now().Add(util.LongTimeout))
+				createdWl.Status.RequeueState = &kueue.RequeueState{
+					RequeueAt: ptr.To(requeueAt),
+					Count:     ptr.To(int32(1)),
+				}
+				workload.UnsetRequeuedCondition(&createdWl, kueue.WorkloadEvictedByPodsReadyTimeout, message)
+				gomega.Expect(k8sClient.Status().Update(ctx, &createdWl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &createdWl)).To(gomega.Succeed())
+					g.Expect(createdWl.Status.RequeueState).ToNot(gomega.BeNil())
+					g.Expect(createdWl.Status.Conditions).To(gomega.ContainElements(
+						gomega.BeComparableTo(metav1.Condition{
+							Type:    kueue.WorkloadRequeued,
+							Status:  metav1.ConditionFalse,
+							Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+							Message: message,
+						}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("deleting the workload", func() {
+				gomega.Expect(k8sClient.Delete(ctx, &createdWl)).To(gomega.Succeed())
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &createdWl)).ToNot(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("should handle the workload finished", func() {
```

  Moved to unit tests.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T18:49:29Z

- **test/integration/controller/core/workload_controller_test.go:474** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T18:49:29Z
```diff
@@ -452,4 +448,144 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 			gomega.Expect(finalQueueWorkload.Spec.Priority).To(gomega.Equal(&initialPriority))
 		})
 	})
+
+	ginkgo.When("the workload is in a backoff state", func() {
+
+		var flavor *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavor = testing.MakeResourceFlavor(flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Create(ctx, flavor)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, flavor, true)
+		})
+
+		ginkgo.It("should handle the workload deletion", func() {
```

  Removed this test. Already tested on unit tests.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T18:49:41Z

- **test/integration/controller/jobs/job/job_controller_test.go:1778** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T18:49:41Z
```diff
@@ -1775,6 +1775,38 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 			wll.Spec.Active = ptr.To(false)
 			gomega.Expect(k8sClient.Update(ctx, wll)).Should(gomega.Succeed())
 
+			gomega.Eventually(func(g gomega.Gomega) {
```

  Added

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T18:50:19Z

- **pkg/controller/jobs/job/job_controller_test.go:698** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T18:50:19Z
```diff
@@ -668,6 +668,326 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"when workload is evicted due to pods ready timeout, job gets suspended and quota is unset": {
+			job: *baseJobWrapper.Clone().
+				Suspend(false).
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*baseWorkloadWrapper.Clone().
+					Admitted(true).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: "Exceeded the PodsReady timeout",
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
```

  You right. Removed it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T18:50:33Z

- **pkg/controller/jobs/raycluster/raycluster_controller_test.go:331** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T18:50:32Z
```diff
@@ -328,7 +328,6 @@ func TestReconciler(t *testing.T) {
 					Condition(metav1.Condition{
 						Type:               kueue.WorkloadRequeued,
 						Status:             metav1.ConditionTrue,
-						Reason:             "Pending",
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T18:50:45Z

- **pkg/workload/workload.go:589** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T18:50:45Z
```diff
@@ -577,6 +585,12 @@ func IsEvictedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
 	return cond, true
 }
 
+// IsUnsetRequeuedByClusterQueueStopped returns true if the workload is unset requeued by cluster queue stopped.
+func IsUnsetRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
```

  Renamed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-02T18:55:59Z

- **pkg/workload/workload.go:382** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-02T18:55:59Z
```diff
@@ -383,10 +379,19 @@ func UnsetQuotaReservationWithCondition(wl *kueue.Workload, reason, message stri
 }
 
 // SetRequeuedCondition sets the WorkloadRequeued condition to true
-func SetRequeuedCondition(wl *kueue.Workload, reason string, message string) {
+func SetRequeuedCondition(wl *kueue.Workload, reason, message string) {
```

  ```suggestion
  func SetRequeuedCondition(wl *kueue.Workload, reason, message string, status bool) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T19:30:44Z

- **pkg/workload/workload.go:395** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T19:30:43Z
```diff
@@ -394,6 +390,18 @@ func SetRequeuedCondition(wl *kueue.Workload, reason string, message string) {
 	apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
 }
 
+// UnsetRequeuedCondition sets the WorkloadRequeued condition to false
+func UnsetRequeuedCondition(wl *kueue.Workload, reason string, message string) {
+	condition := metav1.Condition{
+		Type:               kueue.WorkloadRequeued,
+		Status:             metav1.ConditionFalse,
+		Reason:             reason,
+		Message:            api.TruncateConditionMessage(message),
+		ObservedGeneration: wl.Generation,
+	}
+	apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+}
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-02T20:40:05Z

- **pkg/workload/workload.go:382** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-02T20:40:05Z
```diff
@@ -383,10 +379,19 @@ func UnsetQuotaReservationWithCondition(wl *kueue.Workload, reason, message stri
 }
 
 // SetRequeuedCondition sets the WorkloadRequeued condition to true
-func SetRequeuedCondition(wl *kueue.Workload, reason string, message string) {
+func SetRequeuedCondition(wl *kueue.Workload, reason, message string) {
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-03T13:23:25Z

- **pkg/controller/core/workload_controller_test.go:593** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T12:12:21Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
```

  This still looks pending. I think the request is to add a requeueState in the input, to verify that it's cleared by the reconcile.

- **pkg/controller/core/workload_controller.go:203** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T12:16:53Z
```diff
@@ -166,9 +160,51 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond != nil && cond.Status == metav1.ConditionFalse {
+			switch cond.Reason {
+			case kueue.WorkloadEvictedByDeactivation:
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
+				updated = true
+			case kueue.WorkloadEvictedByPodsReadyTimeout:
+				var requeueAfter time.Duration
+				if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+					requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+				}
+				if requeueAfter > 0 {
+					return reconcile.Result{RequeueAfter: requeueAfter}, nil
+				}
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)
+				updated = true
+			}
+		}
+
+		if updated {
+			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		}
+	}
+
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
-		if updated, err := r.reconcileSyncAdmissionChecks(ctx, &wl, cqName); updated || err != nil {
+		// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
+		cq := kueue.ClusterQueue{}
+		if err := r.client.Get(ctx, types.NamespacedName{Name: cqName}, &cq); err != nil {
+			return ctrl.Result{}, err
+		}
+		// If stopped cluster queue is started we need to set the WorkloadRequeued condition to true.
+		if workload.IsDisabledRequeuedByClusterQueueStopped(&wl) && ptr.Deref(cq.Spec.StopPolicy, kueue.None) == kueue.None {
```

  what sets this condition to True with that reason in the first place?

- **pkg/workload/workload.go:581** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T12:21:18Z
```diff
@@ -577,6 +577,12 @@ func IsEvictedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
 	return cond, true
 }
 
+// IsDisabledRequeuedByClusterQueueStopped returns true if the workload is unset requeued by cluster queue stopped.
+func IsDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
```

  if there aren't any other users of this function other than the workload reconciler, move this function there and make it unexported.

- **test/integration/controller/core/workload_controller_test.go:408** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T12:22:17Z
```diff
@@ -405,12 +405,6 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 							Reason:  "Pending",
 							Message: "By test",
 						}, util.IgnoreConditionTimestampsAndObservedGeneration),
-						gomega.BeComparableTo(metav1.Condition{
```

  why remove this check?

- **test/integration/controller/jobs/job/job_controller_test.go:1914** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T12:25:13Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+		fl *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		waitForPodsReady := &configapi.WaitForPodsReady{
+			Enable:         true,
+			BlockAdmission: ptr.To(true),
+			Timeout:        &metav1.Duration{Duration: util.Timeout},
```

  ```suggestion
  			Timeout:        &metav1.Duration{Duration: 10 * time.Millisecond},
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:1993** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T13:12:44Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+		fl *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		waitForPodsReady := &configapi.WaitForPodsReady{
+			Enable:         true,
+			BlockAdmission: ptr.To(true),
+			Timeout:        &metav1.Duration{Duration: util.Timeout},
+			RequeuingStrategy: &configapi.RequeuingStrategy{
+				Timestamp:          ptr.To(configapi.EvictionTimestamp),
+				BackoffBaseSeconds: ptr.To[int32](configapi.DefaultRequeuingBackoffBaseSeconds),
+			},
+		}
+		ctx, k8sClient = fwk.RunManager(cfg, managerAndSchedulerSetup(
+			&configapi.Configuration{WaitForPodsReady: waitForPodsReady},
+			jobframework.WithWaitForPodsReady(waitForPodsReady),
+		))
+	})
+	ginkgo.AfterAll(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{GenerateName: "core-"}}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		fl = testing.MakeResourceFlavor("fl").Obj()
+		gomega.Expect(k8sClient.Create(ctx, fl)).Should(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue("cq").
+			ResourceGroup(*testing.MakeFlavorQuotas("fl").Resource(corev1.ResourceCPU, "10").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).Should(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue("lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, fl, true)
+	})
+
+	ginkgo.When("workload evicted due pods ready timeout", func() {
+		ginkgo.It("should requeued after job status ready", func() {
+			ginkgo.By("creating job")
+			job := testingjob.MakeJob("job", ns.Name).Queue(lq.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+
+			wl := &kueue.Workload{}
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
+
+			jobKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("checking the workload is evicted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobKey, createdJob)).Should(gomega.Succeed())
+				g.Expect(createdJob.Status.Ready, 0).Should(gomega.BeNil())
+				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  ```suggestion
  			}, util.Timeout, util.Interval).Should(gomega.Succeed())
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:1917** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T13:13:14Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+		fl *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		waitForPodsReady := &configapi.WaitForPodsReady{
+			Enable:         true,
+			BlockAdmission: ptr.To(true),
+			Timeout:        &metav1.Duration{Duration: util.Timeout},
+			RequeuingStrategy: &configapi.RequeuingStrategy{
+				Timestamp:          ptr.To(configapi.EvictionTimestamp),
+				BackoffBaseSeconds: ptr.To[int32](configapi.DefaultRequeuingBackoffBaseSeconds),
```

  ```suggestion
  				BackoffBaseSeconds: ptr.To[int32](1),
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:2037** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T13:13:29Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+		fl *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		waitForPodsReady := &configapi.WaitForPodsReady{
+			Enable:         true,
+			BlockAdmission: ptr.To(true),
+			Timeout:        &metav1.Duration{Duration: util.Timeout},
+			RequeuingStrategy: &configapi.RequeuingStrategy{
+				Timestamp:          ptr.To(configapi.EvictionTimestamp),
+				BackoffBaseSeconds: ptr.To[int32](configapi.DefaultRequeuingBackoffBaseSeconds),
+			},
+		}
+		ctx, k8sClient = fwk.RunManager(cfg, managerAndSchedulerSetup(
+			&configapi.Configuration{WaitForPodsReady: waitForPodsReady},
+			jobframework.WithWaitForPodsReady(waitForPodsReady),
+		))
+	})
+	ginkgo.AfterAll(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{GenerateName: "core-"}}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		fl = testing.MakeResourceFlavor("fl").Obj()
+		gomega.Expect(k8sClient.Create(ctx, fl)).Should(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue("cq").
+			ResourceGroup(*testing.MakeFlavorQuotas("fl").Resource(corev1.ResourceCPU, "10").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).Should(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue("lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, fl, true)
+	})
+
+	ginkgo.When("workload evicted due pods ready timeout", func() {
+		ginkgo.It("should requeued after job status ready", func() {
+			ginkgo.By("creating job")
+			job := testingjob.MakeJob("job", ns.Name).Queue(lq.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+
+			wl := &kueue.Workload{}
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
+
+			jobKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("checking the workload is evicted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobKey, createdJob)).Should(gomega.Succeed())
+				g.Expect(createdJob.Status.Ready, 0).Should(gomega.BeNil())
+				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("setting job status ready")
+			createdJob.Status.Ready = ptr.To(int32(1))
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("checking the workload is requeued")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobKey, createdJob)).Should(gomega.Succeed())
+				g.Expect(createdJob.Status.Ready).ShouldNot(gomega.BeNil())
+				g.Expect(*createdJob.Status.Ready).Should(gomega.Equal(int32(1)))
+				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadPodsReady,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadPodsReady,
+						Message: "All pods were ready or succeeded since the workload admission",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadQuotaReserved,
+						Message: fmt.Sprintf("Quota reserved in ClusterQueue %s", cq.Name),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadQuotaReserved,
+						Message: fmt.Sprintf("Previously: Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadAdmitted,
+						Message: "The workload is admitted",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadBackoffFinished,
+						Message: "The workload backoff was finished",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  ```suggestion
  			}, util.Timeout, util.Interval).Should(gomega.Succeed())
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:1899** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T13:23:23Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  I fear that there is high potential for flakiness here.
  
  The ideal solution is to manipulate the passing of time in the test, if you can pass a fake time to every controller.
  
  OTOH, we are not too concerned with what the scheduler is doing, so we could potentially remove it from the equation. What we are concerned mostly is the interaction between the job controller and the workload reconciler.
  
  @mimowo do you have any suggestions on how to improve this test?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T14:53:48Z

- **test/integration/controller/core/workload_controller_test.go:408** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T14:53:48Z
```diff
@@ -405,12 +405,6 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 							Reason:  "Pending",
 							Message: "By test",
 						}, util.IgnoreConditionTimestampsAndObservedGeneration),
-						gomega.BeComparableTo(metav1.Condition{
```

  Because this test doesn't execute jobframework reconciler. jobframework reconciler should handle AdmissionCheck eviction and set Requeued condition to true. 
  
  I can add something like this. But I'm not sure that it will give us something.
  
  ```go
  workload.SetRequeuedCondition(&createdWl, "AdmissionCheck", "At least one admission check is false", true)
  gomega.Expect(k8sClient.Status().Update(ctx, &createdWl)).Should(gomega.Succeed())
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-03T15:02:36Z

- **test/integration/controller/core/workload_controller_test.go:408** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T15:02:36Z
```diff
@@ -405,12 +405,6 @@ var _ = ginkgo.Describe("Workload controller", ginkgo.Ordered, ginkgo.ContinueOn
 							Reason:  "Pending",
 							Message: "By test",
 						}, util.IgnoreConditionTimestampsAndObservedGeneration),
-						gomega.BeComparableTo(metav1.Condition{
```

  No, that's ok.
  Thanks for explaining.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T15:12:25Z

- **pkg/controller/core/workload_controller_test.go:593** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T15:12:25Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
```

  Yes, still working on it :(. The problem is that this status doesn't update by Patch/Apply. The `workload.ApplyAdmissionStatus` skipping all nil values.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-03T15:20:34Z

- **pkg/controller/core/workload_controller_test.go:593** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T15:20:33Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
```

  uhm..... I think that might be a limitation of the fake client. Maybe you can leave it, as it is already tested on integration https://github.com/kubernetes-sigs/kueue/pull/1838/files#diff-f4fac662d1029511a4b1ca216fa88a0396663289dda8c022e8fcda59a02deabc

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T15:43:19Z

- **pkg/controller/core/workload_controller.go:203** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T15:43:19Z
```diff
@@ -166,9 +160,51 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond != nil && cond.Status == metav1.ConditionFalse {
+			switch cond.Reason {
+			case kueue.WorkloadEvictedByDeactivation:
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
+				updated = true
+			case kueue.WorkloadEvictedByPodsReadyTimeout:
+				var requeueAfter time.Duration
+				if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+					requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+				}
+				if requeueAfter > 0 {
+					return reconcile.Result{RequeueAfter: requeueAfter}, nil
+				}
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)
+				updated = true
+			}
+		}
+
+		if updated {
+			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		}
+	}
+
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
-		if updated, err := r.reconcileSyncAdmissionChecks(ctx, &wl, cqName); updated || err != nil {
+		// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
+		cq := kueue.ClusterQueue{}
+		if err := r.client.Get(ctx, types.NamespacedName{Name: cqName}, &cq); err != nil {
+			return ctrl.Result{}, err
+		}
+		// If stopped cluster queue is started we need to set the WorkloadRequeued condition to true.
+		if workload.IsDisabledRequeuedByClusterQueueStopped(&wl) && ptr.Deref(cq.Spec.StopPolicy, kueue.None) == kueue.None {
```

  The eviction setting on workload_controller here:
  
  ```go
  if workload.IsAdmitted(wl) {
      if queueStopPolicy != kueue.HoldAndDrain {
          return false, nil
      }
      log.V(3).Info("Workload is evicted because the ClusterQueue is stopped", "clusterQueue", klog.KRef("", cqName))
      workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByClusterQueueStopped, "The ClusterQueue is stopped")
      err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
      return true, client.IgnoreNotFound(err)
  }
  ```
  
  After that on jobframework reconciler this event handling and setting Requeued=true.
  
  ```go
  // 6. handle eviction
  if evCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
      if err := r.stopJob(ctx, job, wl, StopReasonWorkloadEvicted, evCond.Message); err != nil {
          return ctrl.Result{}, err
      }
      if workload.HasQuotaReservation(wl) {
          if !job.IsActive() {
              log.V(6).Info("The job is no longer active, clear the workloads admission")
              if evCond.Reason == kueue.WorkloadEvictedByDeactivation ||
                  evCond.Reason == kueue.WorkloadEvictedByPodsReadyTimeout ||
                  evCond.Reason == kueue.WorkloadEvictedByClusterQueueStopped {
                  workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, false)
              } else {
                  workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
              }
              _ = workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message)
              err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
              if err != nil {
                  return ctrl.Result{}, fmt.Errorf("clearing admission: %w", err)
              }
          }
      }
      return ctrl.Result{}, nil
  }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T17:22:18Z

- **pkg/controller/core/workload_controller_test.go:552** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T17:22:18Z
```diff
@@ -549,7 +549,7 @@ func TestReconcile(t *testing.T) {
 				RequeueState(ptr.To[int32](4), ptr.To(metav1.NewTime(testStartTime.Add(80*time.Second).Truncate(time.Second)))).
 				Obj(),
 		},
-		"deactivated workload": {
+		"deactivate workload by reached re-queue backoffLimitCount": {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T17:23:33Z

- **pkg/controller/core/workload_controller_test.go:593** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T17:23:33Z
```diff
@@ -590,6 +590,92 @@ func TestReconcile(t *testing.T) {
 				Message:   "Deactivated Workload \"ns/wl\" by reached re-queue backoffLimitCount",
 			}},
 		},
+		"should set the WorkloadRequeued condition to true on re-activated": {
```

  Added comment to do not confuse in future.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T17:24:45Z

- **pkg/workload/workload.go:581** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T17:24:45Z
```diff
@@ -577,6 +577,12 @@ func IsEvictedByPodsReadyTimeout(w *kueue.Workload) (*metav1.Condition, bool) {
 	return cond, true
 }
 
+// IsDisabledRequeuedByClusterQueueStopped returns true if the workload is unset requeued by cluster queue stopped.
+func IsDisabledRequeuedByClusterQueueStopped(w *kueue.Workload) bool {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T17:25:04Z

- **test/integration/controller/jobs/job/job_controller_test.go:1914** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T17:25:04Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+		fl *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		waitForPodsReady := &configapi.WaitForPodsReady{
+			Enable:         true,
+			BlockAdmission: ptr.To(true),
+			Timeout:        &metav1.Duration{Duration: util.Timeout},
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T17:25:12Z

- **test/integration/controller/jobs/job/job_controller_test.go:1993** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T17:25:12Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+		fl *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		waitForPodsReady := &configapi.WaitForPodsReady{
+			Enable:         true,
+			BlockAdmission: ptr.To(true),
+			Timeout:        &metav1.Duration{Duration: util.Timeout},
+			RequeuingStrategy: &configapi.RequeuingStrategy{
+				Timestamp:          ptr.To(configapi.EvictionTimestamp),
+				BackoffBaseSeconds: ptr.To[int32](configapi.DefaultRequeuingBackoffBaseSeconds),
+			},
+		}
+		ctx, k8sClient = fwk.RunManager(cfg, managerAndSchedulerSetup(
+			&configapi.Configuration{WaitForPodsReady: waitForPodsReady},
+			jobframework.WithWaitForPodsReady(waitForPodsReady),
+		))
+	})
+	ginkgo.AfterAll(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{GenerateName: "core-"}}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		fl = testing.MakeResourceFlavor("fl").Obj()
+		gomega.Expect(k8sClient.Create(ctx, fl)).Should(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue("cq").
+			ResourceGroup(*testing.MakeFlavorQuotas("fl").Resource(corev1.ResourceCPU, "10").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).Should(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue("lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, fl, true)
+	})
+
+	ginkgo.When("workload evicted due pods ready timeout", func() {
+		ginkgo.It("should requeued after job status ready", func() {
+			ginkgo.By("creating job")
+			job := testingjob.MakeJob("job", ns.Name).Queue(lq.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+
+			wl := &kueue.Workload{}
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
+
+			jobKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("checking the workload is evicted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobKey, createdJob)).Should(gomega.Succeed())
+				g.Expect(createdJob.Status.Ready, 0).Should(gomega.BeNil())
+				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T17:25:21Z

- **test/integration/controller/jobs/job/job_controller_test.go:1917** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T17:25:21Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+		fl *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		waitForPodsReady := &configapi.WaitForPodsReady{
+			Enable:         true,
+			BlockAdmission: ptr.To(true),
+			Timeout:        &metav1.Duration{Duration: util.Timeout},
+			RequeuingStrategy: &configapi.RequeuingStrategy{
+				Timestamp:          ptr.To(configapi.EvictionTimestamp),
+				BackoffBaseSeconds: ptr.To[int32](configapi.DefaultRequeuingBackoffBaseSeconds),
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T17:25:31Z

- **test/integration/controller/jobs/job/job_controller_test.go:2037** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T17:25:31Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+		fl *kueue.ResourceFlavor
+		cq *kueue.ClusterQueue
+		lq *kueue.LocalQueue
+	)
+
+	ginkgo.BeforeAll(func() {
+		fwk = &framework.Framework{
+			CRDPath: crdPath,
+		}
+		cfg = fwk.Init()
+		waitForPodsReady := &configapi.WaitForPodsReady{
+			Enable:         true,
+			BlockAdmission: ptr.To(true),
+			Timeout:        &metav1.Duration{Duration: util.Timeout},
+			RequeuingStrategy: &configapi.RequeuingStrategy{
+				Timestamp:          ptr.To(configapi.EvictionTimestamp),
+				BackoffBaseSeconds: ptr.To[int32](configapi.DefaultRequeuingBackoffBaseSeconds),
+			},
+		}
+		ctx, k8sClient = fwk.RunManager(cfg, managerAndSchedulerSetup(
+			&configapi.Configuration{WaitForPodsReady: waitForPodsReady},
+			jobframework.WithWaitForPodsReady(waitForPodsReady),
+		))
+	})
+	ginkgo.AfterAll(func() {
+		fwk.Teardown()
+	})
+
+	ginkgo.BeforeEach(func() {
+		ns = &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{GenerateName: "core-"}}
+		gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+		fl = testing.MakeResourceFlavor("fl").Obj()
+		gomega.Expect(k8sClient.Create(ctx, fl)).Should(gomega.Succeed())
+
+		cq = testing.MakeClusterQueue("cq").
+			ResourceGroup(*testing.MakeFlavorQuotas("fl").Resource(corev1.ResourceCPU, "10").Obj()).Obj()
+		gomega.Expect(k8sClient.Create(ctx, cq)).Should(gomega.Succeed())
+
+		lq = testing.MakeLocalQueue("lq", ns.Name).ClusterQueue(cq.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lq)).Should(gomega.Succeed())
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, cq, true)
+		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, fl, true)
+	})
+
+	ginkgo.When("workload evicted due pods ready timeout", func() {
+		ginkgo.It("should requeued after job status ready", func() {
+			ginkgo.By("creating job")
+			job := testingjob.MakeJob("job", ns.Name).Queue(lq.Name).Request(corev1.ResourceCPU, "2").Obj()
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+
+			wl := &kueue.Workload{}
+			wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: job.Namespace}
+
+			jobKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("checking the workload is evicted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobKey, createdJob)).Should(gomega.Succeed())
+				g.Expect(createdJob.Status.Ready, 0).Should(gomega.BeNil())
+				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadEvictedByPodsReadyTimeout,
+						Message: fmt.Sprintf("Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("setting job status ready")
+			createdJob.Status.Ready = ptr.To(int32(1))
+			gomega.Expect(k8sClient.Status().Update(ctx, createdJob)).Should(gomega.Succeed())
+
+			ginkgo.By("checking the workload is requeued")
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, jobKey, createdJob)).Should(gomega.Succeed())
+				g.Expect(createdJob.Status.Ready).ShouldNot(gomega.BeNil())
+				g.Expect(*createdJob.Status.Ready).Should(gomega.Equal(int32(1)))
+				g.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				g.Expect(wl.Status.Conditions).To(gomega.ContainElements(
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadPodsReady,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadPodsReady,
+						Message: "All pods were ready or succeeded since the workload admission",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadQuotaReserved,
+						Message: fmt.Sprintf("Quota reserved in ClusterQueue %s", cq.Name),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionFalse,
+						Reason:  kueue.WorkloadQuotaReserved,
+						Message: fmt.Sprintf("Previously: Exceeded the PodsReady timeout %s", wlKey.String()),
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadAdmitted,
+						Message: "The workload is admitted",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+					gomega.BeComparableTo(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadBackoffFinished,
+						Message: "The workload backoff was finished",
+					}, util.IgnoreConditionTimestampsAndObservedGeneration),
+				))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-03T17:52:55Z

- **pkg/controller/core/workload_controller.go:203** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T17:52:55Z
```diff
@@ -166,9 +160,51 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond != nil && cond.Status == metav1.ConditionFalse {
+			switch cond.Reason {
+			case kueue.WorkloadEvictedByDeactivation:
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
+				updated = true
+			case kueue.WorkloadEvictedByPodsReadyTimeout:
+				var requeueAfter time.Duration
+				if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+					requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+				}
+				if requeueAfter > 0 {
+					return reconcile.Result{RequeueAfter: requeueAfter}, nil
+				}
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)
+				updated = true
+			}
+		}
+
+		if updated {
+			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		}
+	}
+
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
-		if updated, err := r.reconcileSyncAdmissionChecks(ctx, &wl, cqName); updated || err != nil {
+		// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
+		cq := kueue.ClusterQueue{}
+		if err := r.client.Get(ctx, types.NamespacedName{Name: cqName}, &cq); err != nil {
+			return ctrl.Result{}, err
+		}
+		// If stopped cluster queue is started we need to set the WorkloadRequeued condition to true.
+		if workload.IsDisabledRequeuedByClusterQueueStopped(&wl) && ptr.Deref(cq.Spec.StopPolicy, kueue.None) == kueue.None {
```

  Got it, thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T18:27:28Z

- **test/integration/controller/jobs/job/job_controller_test.go:1899** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T18:27:28Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  With current solution I think we can't manipulate the passing of time in the test. Inside worload_controller we are using another incapsulated waitForPodsReadyConfig.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-03T18:28:53Z

- **test/integration/controller/jobs/job/job_controller_test.go:1899** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-03T18:28:53Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  With scheduler I think it's more realistic case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-06T09:03:46Z

- **test/integration/controller/jobs/job/job_controller_test.go:1899** by [@mimowo](https://github.com/mimowo) — 2024-05-06T09:03:46Z
```diff
@@ -1832,6 +1895,150 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 	})
 })
 
+var _ = ginkgo.Describe("Job controller interacting with scheduler when waitForPodsReady enabled", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  I think the test itself is fine, but I also believe there is a potential for flakes in this assert "checking the workload is evicted", because historically 1s was not enough for some asserts, and we use  `BackoffBaseSeconds: 1`. 
  
  I think we could pro-actively set the `BackoffBaseSeconds` to `2` or `3`, but I'm also ok to wait until we observe flakes here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-06T09:21:43Z

- **pkg/controller/core/workload_controller.go:185** by [@mimowo](https://github.com/mimowo) — 2024-05-06T09:21:42Z
```diff
@@ -166,9 +160,51 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond != nil && cond.Status == metav1.ConditionFalse {
+			switch cond.Reason {
+			case kueue.WorkloadEvictedByDeactivation:
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
+				updated = true
+			case kueue.WorkloadEvictedByPodsReadyTimeout:
+				var requeueAfter time.Duration
+				if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+					requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+				}
+				if requeueAfter > 0 {
+					return reconcile.Result{RequeueAfter: requeueAfter}, nil
+				}
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)
```

  It looks inconsistent: that we don't clear`wl.Status.RequeueState` for PodsReady timeout, but we clear it on L168 in case of Reactivation. In particular, I'm wondering if this could lead to incrementing the `RequeueState.Count` [here](https://github.com/kubernetes-sigs/kueue/blob/f108fef22eb4532f3918754e5a08226ac366343c/pkg/controller/core/workload_controller.go#L420) on the second requeue. Please verify and fix if this is the case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-06T09:25:34Z

- **pkg/controller/core/workload_controller.go:203** by [@mimowo](https://github.com/mimowo) — 2024-05-06T09:25:34Z
```diff
@@ -166,9 +160,51 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond != nil && cond.Status == metav1.ConditionFalse {
+			switch cond.Reason {
+			case kueue.WorkloadEvictedByDeactivation:
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
+				updated = true
+			case kueue.WorkloadEvictedByPodsReadyTimeout:
+				var requeueAfter time.Duration
+				if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+					requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+				}
+				if requeueAfter > 0 {
+					return reconcile.Result{RequeueAfter: requeueAfter}, nil
+				}
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)
+				updated = true
+			}
+		}
+
+		if updated {
+			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		}
+	}
+
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
-		if updated, err := r.reconcileSyncAdmissionChecks(ctx, &wl, cqName); updated || err != nil {
+		// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
+		cq := kueue.ClusterQueue{}
+		if err := r.client.Get(ctx, types.NamespacedName{Name: cqName}, &cq); err != nil {
+			return ctrl.Result{}, err
+		}
+		// If stopped cluster queue is started we need to set the WorkloadRequeued condition to true.
+		if isDisabledRequeuedByClusterQueueStopped(&wl) && ptr.Deref(cq.Spec.StopPolicy, kueue.None) == kueue.None {
```

  I would suspect it does not matter what was the `StopPolicy` that lead to the setting `Requeued: false` for ClusterQueueStopped. In particular, if the `stopPolicy: HoldAndDrain`, then I believe we would set the `Requeued: false` [here](https://github.com/kubernetes-sigs/kueue/blob/f108fef22eb4532f3918754e5a08226ac366343c/pkg/controller/core/workload_controller.go#L331-L334). However, the extra check `ptr.Deref(cq.Spec.StopPolicy, kueue.None) == kueue.None` would prevent us to set `Requeued: true`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-06T11:01:28Z

- **pkg/controller/core/workload_controller.go:185** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-06T11:01:28Z
```diff
@@ -166,9 +160,51 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond != nil && cond.Status == metav1.ConditionFalse {
+			switch cond.Reason {
+			case kueue.WorkloadEvictedByDeactivation:
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
+				updated = true
+			case kueue.WorkloadEvictedByPodsReadyTimeout:
+				var requeueAfter time.Duration
+				if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+					requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+				}
+				if requeueAfter > 0 {
+					return reconcile.Result{RequeueAfter: requeueAfter}, nil
+				}
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)
```

  I'm not sure that we need to clear `wl.Status.RequeueState` in this case, because we need to know how much attempts we have, to deactivate workload if we exceeded the limit [here](https://github.com/kubernetes-sigs/kueue/blob/f108fef22eb4532f3918754e5a08226ac366343c/pkg/controller/core/workload_controller.go#L421).
  
  ```go
  func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Context, wl *kueue.Workload) (bool, error) {
  ...
          // If requeuingBackoffLimitCount equals to null, the workloads is repeatedly and endless re-queued.
  	requeuingCount := ptr.Deref(wl.Status.RequeueState.Count, 0) + 1
  	if r.waitForPodsReady.requeuingBackoffLimitCount != nil && requeuingCount > *r.waitForPodsReady.requeuingBackoffLimitCount {
  		wl.Spec.Active = ptr.To(false)
  		if err := r.client.Update(ctx, wl); err != nil {
  			return false, err
  		}
  		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
  			"Deactivated Workload %q by reached re-queue backoffLimitCount", klog.KObj(wl))
  		return true, nil
  	}
  ...
  }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-06T11:02:55Z

- **pkg/controller/core/workload_controller.go:185** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-06T11:02:55Z
```diff
@@ -166,9 +160,51 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond != nil && cond.Status == metav1.ConditionFalse {
+			switch cond.Reason {
+			case kueue.WorkloadEvictedByDeactivation:
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
+				updated = true
+			case kueue.WorkloadEvictedByPodsReadyTimeout:
+				var requeueAfter time.Duration
+				if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+					requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+				}
+				if requeueAfter > 0 {
+					return reconcile.Result{RequeueAfter: requeueAfter}, nil
+				}
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)
```

  Or you mean to clear just [RequeueAt](https://github.com/kubernetes-sigs/kueue/blob/f108fef22eb4532f3918754e5a08226ac366343c/apis/kueue/v1beta1/workload_types.go#L222) field?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-06T11:13:19Z

- **pkg/controller/core/workload_controller.go:185** by [@mimowo](https://github.com/mimowo) — 2024-05-06T11:13:19Z
```diff
@@ -166,9 +160,51 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond != nil && cond.Status == metav1.ConditionFalse {
+			switch cond.Reason {
+			case kueue.WorkloadEvictedByDeactivation:
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
+				updated = true
+			case kueue.WorkloadEvictedByPodsReadyTimeout:
+				var requeueAfter time.Duration
+				if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+					requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+				}
+				if requeueAfter > 0 {
+					return reconcile.Result{RequeueAfter: requeueAfter}, nil
+				}
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)
```

  > I'm not sure that we need to clear wl.Status.RequeueState in this case, because we need to know how much attempts we have
  
  Ah, got it, we need to preserve the `count` so that we can increment it if the PodsReady timeout is exceeded for the next time.
  
  > Or you mean to clear just RequeueAt field?
  
  I guess the `RequeueAt` field is harmless, but might be confusing, since the workload is already requeued, so indeed, I would suggest to clear it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-06T11:44:03Z

- **pkg/controller/core/workload_controller.go:203** by [@mimowo](https://github.com/mimowo) — 2024-05-06T11:44:03Z
```diff
@@ -166,9 +160,51 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if ptr.Deref(wl.Spec.Active, true) {
+		var updated bool
+
+		// If a deactivated workload is re-activated we need to reset the RequeueState.
+		if workload.IsEvictedByDeactivation(&wl) && wl.Status.RequeueState != nil {
+			wl.Status.RequeueState = nil
+			updated = true
+		}
+
+		if cond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued); cond != nil && cond.Status == metav1.ConditionFalse {
+			switch cond.Reason {
+			case kueue.WorkloadEvictedByDeactivation:
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadReactivated, "The workload was reactivated", true)
+				updated = true
+			case kueue.WorkloadEvictedByPodsReadyTimeout:
+				var requeueAfter time.Duration
+				if wl.Status.RequeueState != nil && wl.Status.RequeueState.RequeueAt != nil {
+					requeueAfter = wl.Status.RequeueState.RequeueAt.Time.Sub(r.clock.Now())
+				}
+				if requeueAfter > 0 {
+					return reconcile.Result{RequeueAfter: requeueAfter}, nil
+				}
+				workload.SetRequeuedCondition(&wl, kueue.WorkloadBackoffFinished, "The workload backoff was finished", true)
+				updated = true
+			}
+		}
+
+		if updated {
+			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+		}
+	}
+
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
-		if updated, err := r.reconcileSyncAdmissionChecks(ctx, &wl, cqName); updated || err != nil {
+		// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
+		cq := kueue.ClusterQueue{}
+		if err := r.client.Get(ctx, types.NamespacedName{Name: cqName}, &cq); err != nil {
+			return ctrl.Result{}, err
+		}
+		// If stopped cluster queue is started we need to set the WorkloadRequeued condition to true.
+		if isDisabledRequeuedByClusterQueueStopped(&wl) && ptr.Deref(cq.Spec.StopPolicy, kueue.None) == kueue.None {
```

  Oh, I think the implementation is correct (as discussed offline), we only set `Requeue: true` if `stopPolicy: None`.
