# PR #2070: Add APIs for configuring fair sharing

**Summary**: Add APIs for configuring fair sharing

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2070

**Last updated**: 2024-05-08T16:35:17Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `fair-sharing-api` → `main`
- **Created**: 2024-04-25T19:28:42Z
- **Updated**: 2024-05-08T16:35:17Z
- **Merged**: 2024-05-08T16:35:15Z
- **Closed**: 2024-05-08T16:35:15Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mwielgus](https://github.com/mwielgus), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: _none_
- **Changed files**: 32   **+976 / -41**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Add APIs for fair sharing:
- In `KueueConfiguration`, a new `FairSharing` struct to enable the feature and configure strategies.
- In `ClusterQueueSpec`, a new `FairSharing` struct containing the weight.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1714

#### Special notes for your reviewer:

At this point, I'm looking for feedback on the types and field documentation.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add fair sharing when borrowing unused resources from other ClusterQueues in a cohort.

Fair sharing is based on DRF for usage above nominal quotas.
When fair sharing is enabled, Kueue prefers to admit workloads from ClusterQueues with the lowest share first.
Administrators can enable and configure fair sharing preemption using a combination of two policies: `LessThanOrEqualtoFinalShare`, `LessThanInitialShare`.

You can define a fair sharing `weight` for ClusterQueues. The weight determines how much of the unused resources each ClusterQueue can take in comparison to others.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-25T19:29:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 357b4991a0b2d5ba33bfb2a70da3bf9b333e5c17 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/663ba16103576a0008c34782 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-25T19:29:08Z

cc @mwielgus @tenzen-y

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T18:17:14Z

This is now ready for review 😄 

/assign @tenzen-y @mwielgus

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T20:21:25Z

> By the way, it seems that the fairSharing feature overlaps borrowWithinCohort at some points. So, in the v1 API, we might be able to consolidate both APIs into a single one or provide a comprehensive feature somehow.

Did you mean that borrowWithinCohort can be replaced by using `weight: 0`?

If so, I agree. I would like to completely remove `borrowWithinCohort` once we go to `v1`, unless we hear some opposing feedback.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-03T20:22:42Z

> Did you mean that borrowWithinCohort can be replaced by using `weight: 0`?

Yes, exactly.

> If so, I agree. I would like to completely remove borrowWithinCohort once we go to v1, unless we hear some opposing feedback.

SGTM

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-03T20:41:50Z

We should relax the threshold more on the perf-test, right?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-06T09:43:41Z

> We should relax the threshold more on the perf-test, right?

I think so, I opened https://github.com/kubernetes-sigs/kueue/issues/2140 to keep track, I should open a PR later today.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2024-05-06T10:59:55Z

> borrowWithinCohort once we go to v1, unless we

But why, do we have similar capacity covered this?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:35:04Z

> > borrowWithinCohort once we go to v1, unless we
>
> But why, do we have similar capacity covered this?

Yes, the biggest motivation to have borrowWithinCohort was to allow certain ClusterQueues to always be preempted whenever some other CQ needs the resources.

And this can be easily achieved when enabling fair sharing, by setting the weight on these "preemptible" CQs to zero.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-08T13:09:06Z

@tenzen-y anything to add?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-08T13:37:03Z

I did another pass for fair sharing, lgtm, just the two nit-like comments

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-08T16:04:27Z

LGTM label has been added.  <details>Git tree hash: 07fccade5bb30526c1bae6d4b147b6d21d43dcf8</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-08T16:04:30Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2070#" title="Author self-approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2070#pullrequestreview-2046077468" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-26T16:43:23Z

- **apis/kueue/v1beta1/clusterqueue_types.go:478** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-26T16:14:15Z
```diff
@@ -464,6 +467,17 @@ type BorrowWithinCohort struct {
 	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
 }
 
+// FairSharing contains the properties of the ClusterQueue when participating in fair sharing.
+type FairSharing struct {
+	// weight gives a comparative advantage to this ClusterQueue when competing for unused
+	// resources in the cohort against other ClusterQueues.
+	// The share of a ClusterQueue is based on the dominant resource usage above nominal
+	// quotas for each resource, divided by the weight.
+	// Admission prioritizes scheduling workloads from ClusterQueues with the lowest share
+	// and preempting workloads from the ClusterQueues with the highest share.
```

  As per our design, it seems that we need to default the `weight` to `1` if it is empty.
  So, how to default this?
  
  https://github.com/kubernetes-sigs/kueue/tree/main/keps/1714-fair-sharing#share-value-function-and-weights

- **apis/config/v1beta1/configuration_types.go:356** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-26T16:24:03Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
```

  This marker wouldn't work fine since the Config API is not CRD.
  So, we need to implement validations here: https://github.com/kubernetes-sigs/kueue/blob/0cdb8fda4bdf07d6a702cf8160acd71e2a07053b/pkg/config/validation.go

- **apis/config/v1beta1/configuration_types.go:366** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-26T16:25:29Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
```

  ```suggestion
  	Enabled *bool `json:"enabled,omitempty"`
  ```
  
  Because this is optional.

- **apis/config/v1beta1/configuration_types.go:387** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-26T16:39:34Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preempte CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  I have some discussion points here. Which word should we use? Or do we not need to care if both fields are aligned with the same rule?
  
  The first is `Strategy` vs `Policy` since we use the `Policy` for ClusterQueues. 
  
  https://github.com/kubernetes-sigs/kueue/blob/3cbb7fcfbd27bb49964edc84c60c233b45244b30/apis/kueue/v1beta1/clusterqueue_types.go#L354
  
  The second is `Less` vs `Lower` since we use the `Lower` for the ClusterQueue.
  
  https://github.com/kubernetes-sigs/kueue/blob/3cbb7fcfbd27bb49964edc84c60c233b45244b30/apis/kueue/v1beta1/clusterqueue_types.go#L359-L360

- **apis/config/v1beta1/configuration_types.go:371** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-26T16:42:21Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload doesn't fit after using the previous strategies.
```

  ```suggestion
  	// The preemption algorithm will only use the next strategy in the list if the
  	// incoming workload (preemptee) doesn't fit after using the previous strategies.
  ```
  
  It might be helpful to understand the below description. WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-26T16:44:56Z

- **apis/config/v1beta1/configuration_types.go:376** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-26T16:44:55Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preempte CQ before the preemption.
```

  ```suggestion
  	//   than the share of the preemptee CQ before the preemption.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-26T17:40:50Z

- **apis/config/v1beta1/configuration_types.go:356** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-26T17:25:03Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
```

  I was hopeful 😅 
  I'll move it.

- **apis/config/v1beta1/configuration_types.go:366** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-26T17:25:19Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
```

  But the entire struct is already a pointer.

- **apis/config/v1beta1/configuration_types.go:371** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-26T17:25:58Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload doesn't fit after using the previous strategies.
```

  It's preemptor, but fixed.

- **apis/config/v1beta1/configuration_types.go:387** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-26T17:32:09Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preempte CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  I was going for `Strategy` to match `queueingStrategy`, but `Policy` would work too. I think strategies works a little better given than the algorithm iterates through them.
  
  Low/High works well for priority, but I don't think it works well for "share".

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-27T08:03:13Z

- **apis/config/v1beta1/configuration_types.go:387** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-27T08:03:13Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preempte CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  That makes sense.
  I agree with `strategy` and `Less/High`!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-29T12:48:51Z

- **apis/config/v1beta1/configuration_types.go:366** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-29T12:48:51Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
```

  I commented on this based on the `InternalCertManagement` API: https://github.com/kubernetes-sigs/kueue/blob/5d4a7afcf69cefd93eade9761d917233b101ab25/apis/config/v1beta1/configuration_types.go#L271-L275
  
  Also, it seems that `optional` should be defined as a pointer according to https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#optional-vs-required.
  
  But I'm not sure if we should align with all k/k policies. 
  @alculquicondor WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-29T16:54:32Z

- **apis/config/v1beta1/configuration_types.go:366** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T16:54:32Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
```

  But this field is not optional https://github.com/kubernetes-sigs/kueue/pull/2070/files#diff-b8a00d3ff4d5cac56b83519c4b98c4f8d27c6f3855c68327842289ed64f87722R418
  
  It's just omitted when false. Maybe I can just remove `omitempty`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-29T16:58:21Z

- **apis/config/v1beta1/configuration_types.go:366** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-29T16:58:21Z
```diff
@@ -349,3 +352,28 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+// +kubebuilder:validation:Enum=LessThanOrEqualToFinalShare;LessThanInitialShare
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enabled indicates whether fair sharing is enabled for all cohorts.
+	// Defaults to false.
+	Enabled bool `json:"enabled,omitempty"`
```

  > It's just omitted when false. Maybe I can just remove omitempty.
  
  That makes sense!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-29T17:12:01Z

I left a comment for a nit.

- **pkg/config/validation.go:170** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-29T17:05:08Z
```diff
@@ -149,3 +153,27 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 
 	return allErrs
 }
+
+func validateFairSharing(c *configapi.Configuration) field.ErrorList {
+	fs := c.FairSharing
+	if fs == nil {
+		return nil
+	}
+	var allErrs field.ErrorList
+	if !fs.Enable && len(fs.PreemptionStrategies) != 0 {
+		allErrs = append(allErrs, field.Invalid(fsPreemptionStrategiesPath, fs.PreemptionStrategies, "Must be empty when fair sharing is disabled"))
+	}
+	strategies := sets.New[configapi.PreemptionStrategy]()
+	validStrategies := []configapi.PreemptionStrategy{configapi.LessThanInitialShare, configapi.LessThanOrEqualToFinalShare}
+	for i, s := range fs.PreemptionStrategies {
+		path := fsPreemptionStrategiesPath.Index(i)
+		if slices.Index(validStrategies, s) == -1 {
```

  ```suggestion
  		if !slices.Contains(validStrategies, s) {
  ```
  
  I understand that the `Index` and `Contains` have the same computation complexity, but the `Contains` is more direct, right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-29T17:13:03Z

- **apis/config/v1beta1/configuration_types.go:366** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-29T17:13:03Z
```diff
@@ -349,3 +352,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable,omitempty"`
```

  ```suggestion
  	Enable bool `json:"enable"`
  ```
  
  Based on this discussion: https://github.com/kubernetes-sigs/kueue/pull/2070#discussion_r1583413342

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-03T17:05:04Z

- **pkg/config/validation.go:170** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T17:05:03Z
```diff
@@ -149,3 +153,27 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 
 	return allErrs
 }
+
+func validateFairSharing(c *configapi.Configuration) field.ErrorList {
+	fs := c.FairSharing
+	if fs == nil {
+		return nil
+	}
+	var allErrs field.ErrorList
+	if !fs.Enable && len(fs.PreemptionStrategies) != 0 {
+		allErrs = append(allErrs, field.Invalid(fsPreemptionStrategiesPath, fs.PreemptionStrategies, "Must be empty when fair sharing is disabled"))
+	}
+	strategies := sets.New[configapi.PreemptionStrategy]()
+	validStrategies := []configapi.PreemptionStrategy{configapi.LessThanInitialShare, configapi.LessThanOrEqualToFinalShare}
+	for i, s := range fs.PreemptionStrategies {
+		path := fsPreemptionStrategiesPath.Index(i)
+		if slices.Index(validStrategies, s) == -1 {
```

  +1 on readability. Thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-03T18:18:20Z

- **apis/kueue/v1beta1/clusterqueue_types.go:478** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T18:18:20Z
```diff
@@ -464,6 +467,17 @@ type BorrowWithinCohort struct {
 	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
 }
 
+// FairSharing contains the properties of the ClusterQueue when participating in fair sharing.
+type FairSharing struct {
+	// weight gives a comparative advantage to this ClusterQueue when competing for unused
+	// resources in the cohort against other ClusterQueues.
+	// The share of a ClusterQueue is based on the dominant resource usage above nominal
+	// quotas for each resource, divided by the weight.
+	// Admission prioritizes scheduling workloads from ClusterQueues with the lowest share
+	// and preempting workloads from the ClusterQueues with the highest share.
```

  added the default

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-03T20:16:40Z

Totally, lgtm
I left a few comments.
By the way, it seems that the `fairSharing` feature overlaps borrowWithinCohort at some points. So, in the v1 API, we might be able to consolidate both APIs into a single one or provide a comprehensive feature somehow.

- **pkg/cache/clusterqueue.go:689** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-03T19:17:50Z
```diff
@@ -677,6 +686,9 @@ func (c *ClusterQueue) dominantResourceShare(wlReq FlavorResourceQuantities, m i
 	if c.Cohort == nil {
 		return 0, ""
 	}
+	if c.FairWeight.Cmp(resource.Quantity{}) == 0 {
```

  ```suggestion
  	if c.FairWeight.Equal(resource.Quantity{}) {
  ```
  
  ```suggestion
  	if c.FairWeight.IsZero() {
  ```
  I'm curious about whether we can use the `Equal` Or `IsZero` function.

- **pkg/scheduler/preemption/preemption.go:348** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-03T19:50:26Z
```diff
@@ -306,14 +339,13 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 			}
 		}
 	}
-	if !fits {
-		// Try rule S2-b in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
-		// if rule S2-a was not enough.
+	if !fits && len(p.fsStrategies) > 1 {
+		// Try next strategy if the previous strategy wasn't enough
 		cqHeap = cqHeapFromCandidates(retryCandidates, true, snapshot)
 
 		for cqHeap.Len() > 0 && !fits {
 			candCQ := cqHeap.Pop()
-			if newNominatedShareValue < candCQ.share {
+			if p.fsStrategies[1](newNominatedShareValue, candCQ.share, 0 /* irrelevant */) {
```

  I completely understand the reason why `0` is an irrelevant value. Because we could reach here only when the `LessThanOrEqualToFinalShare` is set on the `PreepmptionStarategy`.
  
  But others might feel surprised by this `irrelevant` comment. So, leaving a comment here might be worth it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-03T20:27:36Z

- **pkg/scheduler/preemption/preemption.go:348** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T20:27:11Z
```diff
@@ -306,14 +339,13 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 			}
 		}
 	}
-	if !fits {
-		// Try rule S2-b in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
-		// if rule S2-a was not enough.
+	if !fits && len(p.fsStrategies) > 1 {
+		// Try next strategy if the previous strategy wasn't enough
 		cqHeap = cqHeapFromCandidates(retryCandidates, true, snapshot)
 
 		for cqHeap.Len() > 0 && !fits {
 			candCQ := cqHeap.Pop()
-			if newNominatedShareValue < candCQ.share {
+			if p.fsStrategies[1](newNominatedShareValue, candCQ.share, 0 /* irrelevant */) {
```

  Added :)

- **pkg/cache/clusterqueue.go:689** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T20:27:32Z
```diff
@@ -677,6 +686,9 @@ func (c *ClusterQueue) dominantResourceShare(wlReq FlavorResourceQuantities, m i
 	if c.Cohort == nil {
 		return 0, ""
 	}
+	if c.FairWeight.Cmp(resource.Quantity{}) == 0 {
```

  Clearly, I don't know k8s libraries well enough :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-03T20:34:11Z

- **pkg/cache/clusterqueue.go:689** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-03T20:34:11Z
```diff
@@ -677,6 +686,9 @@ func (c *ClusterQueue) dominantResourceShare(wlReq FlavorResourceQuantities, m i
 	if c.Cohort == nil {
 		return 0, ""
 	}
+	if c.FairWeight.Cmp(resource.Quantity{}) == 0 {
```

  Haha, I know a little bit well Quantity library since I implemented the `Mul` function (multiplication) in the last year :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-03T20:34:40Z

- **pkg/scheduler/preemption/preemption.go:348** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-03T20:34:40Z
```diff
@@ -306,14 +339,13 @@ func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, sn
 			}
 		}
 	}
-	if !fits {
-		// Try rule S2-b in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
-		// if rule S2-a was not enough.
+	if !fits && len(p.fsStrategies) > 1 {
+		// Try next strategy if the previous strategy wasn't enough
 		cqHeap = cqHeapFromCandidates(retryCandidates, true, snapshot)
 
 		for cqHeap.Len() > 0 && !fits {
 			candCQ := cqHeap.Pop()
-			if newNominatedShareValue < candCQ.share {
+			if p.fsStrategies[1](newNominatedShareValue, candCQ.share, 0 /* irrelevant */) {
```

  Thank you!

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2024-05-06T09:52:07Z

- **apis/kueue/v1beta1/clusterqueue_types.go:482** by [@kerthcet](https://github.com/kerthcet) — 2024-05-06T09:34:57Z
```diff
@@ -464,6 +468,20 @@ type BorrowWithinCohort struct {
 	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
 }
 
+// FairSharing contains the properties of the ClusterQueue when participating in fair sharing.
+type FairSharing struct {
+	// weight gives a comparative advantage to this ClusterQueue when competing for unused
+	// resources in the cohort against other ClusterQueues.
+	// The share of a ClusterQueue is based on the dominant resource usage above nominal
+	// quotas for each resource, divided by the weight.
+	// Admission prioritizes scheduling workloads from ClusterQueues with the lowest share
+	// and preempting workloads from the ClusterQueues with the highest share.
+	// A zero weight implies infinite share value, meaning that this ClusterQueue will always
+	// be at disadvantage against other ClusterQueues.
+	// +kubebuilder:default=1
+	Weight *resource.Quantity `json:"weight,omitempty"`
```

  Can't `*int32` fit here? Or `IntOrString`? What other values we'll have?

- **apis/config/v1beta1/configuration_types.go:387** by [@kerthcet](https://github.com/kerthcet) — 2024-05-06T09:51:32Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  I think array is not that expressible but maybe enough for us right now. What we achieved here is somehow equal to `OR`, will we have `AND` or more complicated actions based on different conditions?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-06T10:50:24Z

- **pkg/scheduler/preemption/preemption.go:297** by [@mimowo](https://github.com/mimowo) — 2024-05-06T10:25:04Z
```diff
@@ -255,7 +258,38 @@ func restoreSnapshot(snapshot *cache.Snapshot, targets []*workload.Info) {
 	}
 }
 
-func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, resPerFlv resourcesPerFlavor, candidates []*workload.Info, allowBorrowingBelowPriority *int32) []*workload.Info {
+type fsStrategy func(preemptorNewShare, preempteeOldShare, preempteeNewShare int) bool
+
+// lessThanOrEqualToFinalShare implements Rule S2-a in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+func lessThanOrEqualToFinalShare(preemptorNewShare, _, preempteeNewShare int) bool {
+	return preemptorNewShare <= preempteeNewShare
+}
+
+// lessThanInitialShare implements rule S2-b in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+func lessThanInitialShare(preemptorNewShare, preempteeOldShare, _ int) bool {
+	return preemptorNewShare < preempteeOldShare
+}
+
+// parseStrategies converts an array of strategies into the functions to the used by the algorithm.
+// This function takes advantage of the properties of the preemption algorithm and the strategies.
+// The number of functions returned might not match the input slice.
+func parseStrategies(s []config.PreemptionStrategy) []fsStrategy {
+	result := []fsStrategy{lessThanOrEqualToFinalShare}
+	if len(s) == 0 {
+		return result
+	}
+	if s[0] == config.LessThanInitialShare {
+		result[0] = lessThanInitialShare
+		// This rule is a superset of the other rule, no need to check other strategies.
```

  Are we confident Rule B is superset of Rule A? 
  
  I'm thinking it could be possible that `preempteeOldShare=preempteeNewShare` (for example, there are 2 resources with the same DRF, and the workload being preempted reduces DRF only for one resource, so the DRF for the entire CQ stays the same (because it is a max).
  
  In that case applying Rule A could help, while Rule B does not help.

- **apis/config/v1beta1/configuration_types.go:387** by [@mimowo](https://github.com/mimowo) — 2024-05-06T10:35:03Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  I don't have a strong view here, but I would suggest to consider dropping this configuration, unless we have some evidence users will need it, because:
  1. the validation and parsing adds a substantial amount of code, asking for coverage. In particular we don't have integration tests for only one of the rules enabled
  2. the strategies are quite complex, and I don't think users would like to configure them on their own (unless we have some evidence for that)
  3. I'm not sure the list of strategies is the correct abstraction for the API. Do we foresee more strategies in this form? Maybe just an extra boolean to enable Rule B is enough (say `preemptLargeWorkloads` to more readable to the admins)? Because Rule A should probably be implied if one enables FairSharing.
  
  Also, the API is not discussed in KEP, so please update the KEP API accordingly if we go this path.

- **apis/config/v1beta1/configuration_types.go:378** by [@mimowo](https://github.com/mimowo) — 2024-05-06T10:42:56Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
```

  ```suggestion
  	// - LessThanOrEqualToFinalShare: Only preempt a workload if the share of the preemptor CQ
  	//   is less than or equal to the share of the preemptee CQ without the workload.
  	// - LessThanInitialShare: Only preempt a workload if the share of the preemptor CQ is strictly less
  	//   than the share of the preemptee CQ with the workload.
  ```
  Suggestion to say about preemption of a particular candidate workload rather than ` after the preemption occurs` (as in rule A). I think the current formulation may be confusing that we are talking about the end state of the preemption as a process (including all candidates), and it would not be accurate, because Rule B can add more workloads.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-06T13:03:37Z

- **apis/config/v1beta1/configuration_types.go:363** by [@mwielgus](https://github.com/mwielgus) — 2024-05-06T13:03:37Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
```

  I understand the rationale behind these enums. However, it will be extremely hard for the users to understand what they are actually doing without diving very deep into the algorithm. Maybe we could repackage it as fairness enforcement strength "medium" and "strong"?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-06T14:05:00Z

- **apis/config/v1beta1/configuration_types.go:363** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-06T14:05:00Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
```

  It's not just "medium" and "strong".
  
  There are different semantics if you put them in different orders.
  
  Putting `LessThanInitialShare` might lead to more preemptions. But it also gives you the most predictable behavior, because the algorithm will always try to preempt the lowest priorities first. If you only have `LessThanOrEqualToFinalShare` or you put it first, then you might end up preempting a higher priority job because the low priority one doesn't satisfy the rule.
  
  So yes, having this enums forces the user to understand the algorithm, but anyone that is coming this way has complex use cases for fair sharing already. Otherwise, they would just be using the default.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-06T14:10:56Z

- **apis/config/v1beta1/configuration_types.go:387** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-06T14:10:56Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  It's not OR and it's not (just) AND.
  
  It's one after the other.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-06T14:20:02Z

- **apis/config/v1beta1/configuration_types.go:363** by [@mwielgus](https://github.com/mwielgus) — 2024-05-06T14:20:02Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
```

  I agree that "medium" and "strong" would not be 100% precise. But we need to make it simpler for people to understand what knobs they have to control the behavior and what (more or less) will happen if they change them. 
  The algorithm is HARD to understand and many people will get it wrong and will turn knobs in the wrong direction.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-06T14:23:41Z

- **apis/config/v1beta1/configuration_types.go:387** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-06T14:18:25Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  I do have evidence of a need for a configuration like `["LessThanInitialShare"]`, which was not originally envisioned in the KEP.
  
  Then the question is to @mwielgus and @tenzen-y if you would rather just keep this only configuration, or you still want to keep the other two possibilities (`["LessThanOrEqualToFinalShare"]` and ["LessThanOrEqualToFinalShare", "LessThanInitialShare"]`).
  
  > In particular we don't have integration tests for only one of the rules enabled
  
  That is true, but the different algorithms don't change the mechanics of actually doing the preemptions. Integration tests just make sure that multiple preemptions can occur, regardless of the algorithm.
  
  > Do we foresee more strategies in this form?
  
  I'm not certain of that. I think we might just have the two strategies. But again, this leads to three different configurations, and an array seems to be the easiest way to communicate the semantics.

- **apis/kueue/v1beta1/clusterqueue_types.go:482** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-06T14:20:37Z
```diff
@@ -464,6 +468,20 @@ type BorrowWithinCohort struct {
 	MaxPriorityThreshold *int32 `json:"maxPriorityThreshold,omitempty"`
 }
 
+// FairSharing contains the properties of the ClusterQueue when participating in fair sharing.
+type FairSharing struct {
+	// weight gives a comparative advantage to this ClusterQueue when competing for unused
+	// resources in the cohort against other ClusterQueues.
+	// The share of a ClusterQueue is based on the dominant resource usage above nominal
+	// quotas for each resource, divided by the weight.
+	// Admission prioritizes scheduling workloads from ClusterQueues with the lowest share
+	// and preempting workloads from the ClusterQueues with the highest share.
+	// A zero weight implies infinite share value, meaning that this ClusterQueue will always
+	// be at disadvantage against other ClusterQueues.
+	// +kubebuilder:default=1
+	Weight *resource.Quantity `json:"weight,omitempty"`
```

  The quantity allows you to have integers or decimals, without losing precision.

- **pkg/scheduler/preemption/preemption.go:297** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-06T14:23:25Z
```diff
@@ -255,7 +258,38 @@ func restoreSnapshot(snapshot *cache.Snapshot, targets []*workload.Info) {
 	}
 }
 
-func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, resPerFlv resourcesPerFlavor, candidates []*workload.Info, allowBorrowingBelowPriority *int32) []*workload.Info {
+type fsStrategy func(preemptorNewShare, preempteeOldShare, preempteeNewShare int) bool
+
+// lessThanOrEqualToFinalShare implements Rule S2-a in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+func lessThanOrEqualToFinalShare(preemptorNewShare, _, preempteeNewShare int) bool {
+	return preemptorNewShare <= preempteeNewShare
+}
+
+// lessThanInitialShare implements rule S2-b in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+func lessThanInitialShare(preemptorNewShare, preempteeOldShare, _ int) bool {
+	return preemptorNewShare < preempteeOldShare
+}
+
+// parseStrategies converts an array of strategies into the functions to the used by the algorithm.
+// This function takes advantage of the properties of the preemption algorithm and the strategies.
+// The number of functions returned might not match the input slice.
+func parseStrategies(s []config.PreemptionStrategy) []fsStrategy {
+	result := []fsStrategy{lessThanOrEqualToFinalShare}
+	if len(s) == 0 {
+		return result
+	}
+	if s[0] == config.LessThanInitialShare {
+		result[0] = lessThanInitialShare
+		// This rule is a superset of the other rule, no need to check other strategies.
```

  Yeah, that's a possibility, although unlikely. I could make the 4 combinations work. But Let's see what we decide about the overall topic.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-06T18:59:57Z

- **apis/config/v1beta1/configuration_types.go:387** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-06T18:59:57Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  I had a discussion with @mwielgus and @mimowo. We brainstormed a lot of names and a simplified APIs, but ended up using terms that are not immediately obvious, such as `balance`, `minimalShare`, `soft`.
  
  So, we decided to keep the current proposal, which is more mathematical, assuming that whoever is looking to change the default needs to read the documentation and understand some of the basics of the algorithm.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-06T19:19:17Z

- **apis/config/v1beta1/configuration_types.go:378** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-06T19:14:00Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
```

  I added more details and suggestions on when to use each strategy.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-07T07:27:54Z

- **apis/config/v1beta1/configuration_types.go:386** by [@mimowo](https://github.com/mimowo) — 2024-05-07T07:15:08Z
```diff
@@ -352,3 +355,34 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt a workload if the share of the preemptor CQ
+	//   with the preemptor workload is less than or equal to the share of the preemptee CQ
+	//   without the workload to be preempted.
+	//   This strategy might favor preemption of smaller workloads in the preemptee CQ,
+	//   regardless of priority or start time, in an effort to keep the share of the CQ
+	//   as high as possible.
+	// - LessThanInitialShare: Only preempt a workload if the share of the preemptor CQ
+	//   with the incoming workload is strictly less than the share of the preemptee CQ.
+	//   This strategy doesn't depend on the share usage of the workload being preempted.
+	//   As a result, the strategy chooses to preempt workloads with the lowest priority and
+	//   newest start time first.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
```

  As per offline discussions we consider the two combinations as most likely in use:
  1. `[LessThanOrEqualToFinalShare, LessThanInitialShare]` and 
  2. `[LessThanInitialShare]`
  
  So, I would suggest setting the default to one of the configurations.
  
  I would vote for the `[LessThanOrEqualToFinalShare, LessThanInitialShare]` since it matches the KEP.

- **pkg/scheduler/preemption/preemption.go:297** by [@mimowo](https://github.com/mimowo) — 2024-05-07T07:27:00Z
```diff
@@ -255,7 +258,38 @@ func restoreSnapshot(snapshot *cache.Snapshot, targets []*workload.Info) {
 	}
 }
 
-func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, resPerFlv resourcesPerFlavor, candidates []*workload.Info, allowBorrowingBelowPriority *int32) []*workload.Info {
+type fsStrategy func(preemptorNewShare, preempteeOldShare, preempteeNewShare int) bool
+
+// lessThanOrEqualToFinalShare implements Rule S2-a in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+func lessThanOrEqualToFinalShare(preemptorNewShare, _, preempteeNewShare int) bool {
+	return preemptorNewShare <= preempteeNewShare
+}
+
+// lessThanInitialShare implements rule S2-b in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+func lessThanInitialShare(preemptorNewShare, preempteeOldShare, _ int) bool {
+	return preemptorNewShare < preempteeOldShare
+}
+
+// parseStrategies converts an array of strategies into the functions to the used by the algorithm.
+// This function takes advantage of the properties of the preemption algorithm and the strategies.
+// The number of functions returned might not match the input slice.
+func parseStrategies(s []config.PreemptionStrategy) []fsStrategy {
+	result := []fsStrategy{lessThanOrEqualToFinalShare}
+	if len(s) == 0 {
+		return result
+	}
+	if s[0] == config.LessThanInitialShare {
+		result[0] = lessThanInitialShare
+		// This rule is a superset of the other rule, no need to check other strategies.
```

  Since we go for the "mathematical" approach, I would suggest to be precise, and don't make the assumption that Rule B is a superset of Rule A (since unlikely scenarios may happen).

- **pkg/config/validation.go:190** by [@mimowo](https://github.com/mimowo) — 2024-05-07T07:27:47Z
```diff
@@ -174,3 +176,27 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 
 	return allErrs
 }
+
+func validateFairSharing(c *configapi.Configuration) field.ErrorList {
+	fs := c.FairSharing
+	if fs == nil {
+		return nil
+	}
+	var allErrs field.ErrorList
+	if !fs.Enable && len(fs.PreemptionStrategies) != 0 {
+		allErrs = append(allErrs, field.Invalid(fsPreemptionStrategiesPath, fs.PreemptionStrategies, "Must be empty when fair sharing is disabled"))
+	}
+	strategies := sets.New[configapi.PreemptionStrategy]()
+	validStrategies := []configapi.PreemptionStrategy{configapi.LessThanInitialShare, configapi.LessThanOrEqualToFinalShare}
```

  I think we should either validate against non-supported configurations or support them. I'm leaning towards validation, because it will keep the code simpler. IIUC this is the only non-supported option for now: `[LessThanInitialShare, LessThanOrEqualToFinalShare]` .

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-07T07:38:10Z

- **apis/config/v1beta1/configuration_types.go:387** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-07T07:38:10Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  SGTM
  
  @alculquicondor @mimowo @mwielgus I'm sure that flexibility and understandability are tradeoffs. I'm curious about whether we can provide the `balance`, `minimalShare`, and `soft` via the `kueuectl` since I can imagine that the user of `kueuectl` would prefer to avoid the deep dive into the Kueue.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-07T19:31:42Z

- **apis/config/v1beta1/configuration_types.go:386** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:03:07Z
```diff
@@ -352,3 +355,34 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt a workload if the share of the preemptor CQ
+	//   with the preemptor workload is less than or equal to the share of the preemptee CQ
+	//   without the workload to be preempted.
+	//   This strategy might favor preemption of smaller workloads in the preemptee CQ,
+	//   regardless of priority or start time, in an effort to keep the share of the CQ
+	//   as high as possible.
+	// - LessThanInitialShare: Only preempt a workload if the share of the preemptor CQ
+	//   with the incoming workload is strictly less than the share of the preemptee CQ.
+	//   This strategy doesn't depend on the share usage of the workload being preempted.
+	//   As a result, the strategy chooses to preempt workloads with the lowest priority and
+	//   newest start time first.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
```

  Done

- **apis/config/v1beta1/configuration_types.go:387** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:20:02Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  `kueuectl` is not meant to change Kueue configuration. I will have to do a good job via documentation, instead.

- **pkg/config/validation.go:190** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:20:46Z
```diff
@@ -174,3 +176,27 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 
 	return allErrs
 }
+
+func validateFairSharing(c *configapi.Configuration) field.ErrorList {
+	fs := c.FairSharing
+	if fs == nil {
+		return nil
+	}
+	var allErrs field.ErrorList
+	if !fs.Enable && len(fs.PreemptionStrategies) != 0 {
+		allErrs = append(allErrs, field.Invalid(fsPreemptionStrategiesPath, fs.PreemptionStrategies, "Must be empty when fair sharing is disabled"))
+	}
+	strategies := sets.New[configapi.PreemptionStrategy]()
+	validStrategies := []configapi.PreemptionStrategy{configapi.LessThanInitialShare, configapi.LessThanOrEqualToFinalShare}
```

  Changed validation to have an explicit list of supported combinations.

- **pkg/scheduler/preemption/preemption.go:297** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T19:21:53Z
```diff
@@ -255,7 +258,38 @@ func restoreSnapshot(snapshot *cache.Snapshot, targets []*workload.Info) {
 	}
 }
 
-func fairPreemptions(wl *workload.Info, assignment flavorassigner.Assignment, snapshot *cache.Snapshot, resPerFlv resourcesPerFlavor, candidates []*workload.Info, allowBorrowingBelowPriority *int32) []*workload.Info {
+type fsStrategy func(preemptorNewShare, preempteeOldShare, preempteeNewShare int) bool
+
+// lessThanOrEqualToFinalShare implements Rule S2-a in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+func lessThanOrEqualToFinalShare(preemptorNewShare, _, preempteeNewShare int) bool {
+	return preemptorNewShare <= preempteeNewShare
+}
+
+// lessThanInitialShare implements rule S2-b in https://sigs.k8s.io/kueue/keps/1714-fair-sharing#choosing-workloads-from-clusterqueues-for-preemption
+func lessThanInitialShare(preemptorNewShare, preempteeOldShare, _ int) bool {
+	return preemptorNewShare < preempteeOldShare
+}
+
+// parseStrategies converts an array of strategies into the functions to the used by the algorithm.
+// This function takes advantage of the properties of the preemption algorithm and the strategies.
+// The number of functions returned might not match the input slice.
+func parseStrategies(s []config.PreemptionStrategy) []fsStrategy {
+	result := []fsStrategy{lessThanOrEqualToFinalShare}
+	if len(s) == 0 {
+		return result
+	}
+	if s[0] == config.LessThanInitialShare {
+		result[0] = lessThanInitialShare
+		// This rule is a superset of the other rule, no need to check other strategies.
```

  Removed this return in favor of validation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-08T13:11:48Z

- **config/components/manager/controller_manager_config.yaml:55** by [@mimowo](https://github.com/mimowo) — 2024-05-08T13:11:48Z
```diff
@@ -50,3 +50,6 @@ integrations:
 #       - key: kubernetes.io/metadata.name
 #         operator: NotIn
 #         values: [ kube-system, kueue-system ]
+# fairSharing:
+#   enable: true
+#   preemptionStrategies: [LessThanOrEqualToFinalShare]
```

  nit: Maybe align this with the current default?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-08T13:14:12Z

- **pkg/config/validation.go:213** by [@mimowo](https://github.com/mimowo) — 2024-05-08T13:14:12Z
```diff
@@ -174,3 +176,52 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 
 	return allErrs
 }
+
+var (
+	validStrategySets = [][]configapi.PreemptionStrategy{
+		{
+			configapi.LessThanOrEqualToFinalShare,
+		},
+		{
+			configapi.LessThanInitialShare,
+		},
+		{
+			configapi.LessThanOrEqualToFinalShare,
+			configapi.LessThanInitialShare,
+		},
+	}
+
+	validStrategySetsStr = func() []string {
+		var ss []string
+		for _, s := range validStrategySets {
+			// Casting because strings.Join requires a slice of strings
+			strategies := *(*[]string)(unsafe.Pointer(&s))
+			ss = append(ss, strings.Join(strategies, ","))
+		}
+		return ss
+	}()
+)
+
+func validateFairSharing(c *configapi.Configuration) field.ErrorList {
+	fs := c.FairSharing
+	if fs == nil {
+		return nil
+	}
+	var allErrs field.ErrorList
+	if !fs.Enable && len(fs.PreemptionStrategies) != 0 {
+		allErrs = append(allErrs, field.Invalid(fsPreemptionStrategiesPath, fs.PreemptionStrategies, "Must be empty when fair sharing is disabled"))
+	}
```

  Do we need it? I guess it can be handy to keep th configuration when temporarily disabling the feature. 
  Similarly as for waitForPodsReady.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-08T15:43:24Z

LGTM 
Thank you!

- **apis/config/v1beta1/configuration_types.go:387** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-08T15:38:18Z
```diff
@@ -352,3 +355,27 @@ type ClusterQueueVisibility struct {
 	// Defaults to 10.
 	MaxCount int32 `json:"maxCount,omitempty"`
 }
+
+type PreemptionStrategy string
+
+const (
+	LessThanOrEqualToFinalShare PreemptionStrategy = "LessThanOrEqualToFinalShare"
+	LessThanInitialShare        PreemptionStrategy = "LessThanInitialShare"
+)
+
+type FairSharing struct {
+	// enable indicates whether to enable fair sharing for all cohorts.
+	// Defaults to false.
+	Enable bool `json:"enable"`
+
+	// preemptionStrategies indicates which constraints should a preemption satisfy.
+	// The preemption algorithm will only use the next strategy in the list if the
+	// incoming workload (preemptor) doesn't fit after using the previous strategies.
+	// Possible values are:
+	// - LessThanOrEqualToFinalShare: Only preempt if the share of the preemptor CQ
+	//   will be less than or equal to the share of the preemptee CQ after the preemption occurs.
+	// - LessThanInitialShare: Only preempt if the share of the preemptor CQ will be strictly less
+	//   than the share of the preemptee CQ before the preemption.
+	// The default strategy is ["LessThanOrEqualToFinalShare"].
+	PreemptionStrategies []PreemptionStrategy `json:"preemptionStrategies,omitempty"`
```

  SGTM

- **pkg/config/validation.go:213** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-08T15:42:57Z
```diff
@@ -174,3 +176,52 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 
 	return allErrs
 }
+
+var (
+	validStrategySets = [][]configapi.PreemptionStrategy{
+		{
+			configapi.LessThanOrEqualToFinalShare,
+		},
+		{
+			configapi.LessThanInitialShare,
+		},
+		{
+			configapi.LessThanOrEqualToFinalShare,
+			configapi.LessThanInitialShare,
+		},
+	}
+
+	validStrategySetsStr = func() []string {
+		var ss []string
+		for _, s := range validStrategySets {
+			// Casting because strings.Join requires a slice of strings
+			strategies := *(*[]string)(unsafe.Pointer(&s))
+			ss = append(ss, strings.Join(strategies, ","))
+		}
+		return ss
+	}()
+)
+
+func validateFairSharing(c *configapi.Configuration) field.ErrorList {
+	fs := c.FairSharing
+	if fs == nil {
+		return nil
+	}
+	var allErrs field.ErrorList
+	if !fs.Enable && len(fs.PreemptionStrategies) != 0 {
+		allErrs = append(allErrs, field.Invalid(fsPreemptionStrategiesPath, fs.PreemptionStrategies, "Must be empty when fair sharing is disabled"))
+	}
```

  If there are no drawbacks (similar to additional computation, and potentially bugs in preemptor), I would prefer to apply the @mimowo proposal here.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-08T15:59:27Z

- **config/components/manager/controller_manager_config.yaml:55** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-08T15:58:25Z
```diff
@@ -50,3 +50,6 @@ integrations:
 #       - key: kubernetes.io/metadata.name
 #         operator: NotIn
 #         values: [ kube-system, kueue-system ]
+# fairSharing:
+#   enable: true
+#   preemptionStrategies: [LessThanOrEqualToFinalShare]
```

  Done

- **pkg/config/validation.go:213** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-08T15:58:53Z
```diff
@@ -174,3 +176,52 @@ func validatePodIntegrationOptions(c *configapi.Configuration) field.ErrorList {
 
 	return allErrs
 }
+
+var (
+	validStrategySets = [][]configapi.PreemptionStrategy{
+		{
+			configapi.LessThanOrEqualToFinalShare,
+		},
+		{
+			configapi.LessThanInitialShare,
+		},
+		{
+			configapi.LessThanOrEqualToFinalShare,
+			configapi.LessThanInitialShare,
+		},
+	}
+
+	validStrategySetsStr = func() []string {
+		var ss []string
+		for _, s := range validStrategySets {
+			// Casting because strings.Join requires a slice of strings
+			strategies := *(*[]string)(unsafe.Pointer(&s))
+			ss = append(ss, strings.Join(strategies, ","))
+		}
+		return ss
+	}()
+)
+
+func validateFairSharing(c *configapi.Configuration) field.ErrorList {
+	fs := c.FairSharing
+	if fs == nil {
+		return nil
+	}
+	var allErrs field.ErrorList
+	if !fs.Enable && len(fs.PreemptionStrategies) != 0 {
+		allErrs = append(allErrs, field.Invalid(fsPreemptionStrategiesPath, fs.PreemptionStrategies, "Must be empty when fair sharing is disabled"))
+	}
```

  SG. Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-08T16:04:23Z

Hugely LGTM!
The implementation of the FairSharing feature inspired by Dominant Resource Fairness was not a really easy journey! Awsome 🎉 

/lgtm
/approve
