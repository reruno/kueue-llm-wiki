# PR #209: feat: sync workload condition when admitted

**Summary**: feat: sync workload condition when admitted

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/209

**Last updated**: 2022-04-14T03:59:46Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feature/sync-workload-condition` → `main`
- **Created**: 2022-04-12T13:24:09Z
- **Updated**: 2022-04-14T03:59:46Z
- **Merged**: 2022-04-13T18:22:46Z
- **Closed**: 2022-04-13T18:22:46Z
- **Labels**: `kind/feature`, `lgtm`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 3   **+53 / -17**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes: 
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/201

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-12T13:24:17Z

Hi @kerthcet. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-12T13:24:21Z

cc @ahg-g

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-12T15:42:39Z

We'll probably want to leave this one after we cut the release.
I'm only waiting on #178 to merge to do it.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T01:53:36Z

/ok-to-test

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T02:55:09Z

addressed the comments.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T16:12:59Z

thanks, pls squash

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T17:20:27Z

squashed.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T18:21:26Z

/approve
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-13T18:21:39Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/209#issuecomment-1098350640" title="LGTM">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/209#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-13T01:19:03Z

- **pkg/controller/core/workload_controller.go:253** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T00:46:24Z
```diff
@@ -244,9 +250,9 @@ func workloadStatus(w *kueue.Workload) string {
 	return pending
 }
 
-func workloadFinished(w *kueue.Workload) bool {
+func WorkloadInCondition(w *kueue.Workload, condition kueue.WorkloadConditionType) bool {
```

  pls move to `pkg/workload` pkg; the `controller/...` pkgs should only be imported by main.

- **pkg/controller/core/workload_controller.go:243** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T00:51:59Z
```diff
@@ -234,8 +240,8 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager) error {
 		Complete(r)
 }
 
-func workloadStatus(w *kueue.Workload) string {
-	if workloadFinished(w) {
+func WorkloadStatus(w *kueue.Workload) string {
```

  revert; this doesn't need to be exported.

- **test/integration/controller/core/workload_controller_test.go:111** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T01:11:51Z
```diff
@@ -105,4 +107,40 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("admit workload", func() {
```

  ```suggestion
  	ginkgo.When("the workload is admitted", func() {
  ```

- **test/integration/controller/core/workload_controller_test.go:123** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T01:14:09Z
```diff
@@ -105,4 +107,40 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("admit workload", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(resourceGPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
```

  This is not needed; `DeleteNamespace` takes care of it.

- **test/integration/controller/core/workload_controller_test.go:130** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T01:15:39Z
```diff
@@ -105,4 +107,40 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("admit workload", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(resourceGPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			updatedQueueWorkload = kueue.Workload{}
+			clusterQueue = nil
+		})
+
+		ginkgo.It("Should update status when workloads are admitted", func() {
```

  ```suggestion
  		ginkgo.It("Should update the workload's condition when it gets admitted", func() {
  ```

- **test/integration/controller/core/workload_controller_test.go:131** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T01:16:02Z
```diff
@@ -105,4 +107,40 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("admit workload", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(resourceGPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			updatedQueueWorkload = kueue.Workload{}
+			clusterQueue = nil
+		})
+
+		ginkgo.It("Should update status when workloads are admitted", func() {
+			ginkgo.By("Create workloads")
```

  ```suggestion
  			ginkgo.By("Create workload")
  ```

- **test/integration/controller/core/workload_controller_test.go:135** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T01:16:09Z
```diff
@@ -105,4 +107,40 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("admit workload", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(resourceGPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			updatedQueueWorkload = kueue.Workload{}
+			clusterQueue = nil
+		})
+
+		ginkgo.It("Should update status when workloads are admitted", func() {
+			ginkgo.By("Create workloads")
+			wl = testing.MakeWorkload("one", ns.Name).Queue(queue.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Admit workloads")
```

  ```suggestion
  			ginkgo.By("Admit workload")
  ```

- **test/integration/controller/core/workload_controller_test.go:141** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T01:18:32Z
```diff
@@ -105,4 +107,40 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("admit workload", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(resourceGPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteQueue(ctx, k8sClient, queue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			updatedQueueWorkload = kueue.Workload{}
+			clusterQueue = nil
+		})
+
+		ginkgo.It("Should update status when workloads are admitted", func() {
+			ginkgo.By("Create workloads")
+			wl = testing.MakeWorkload("one", ns.Name).Queue(queue.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Admit workloads")
+			gomega.Eventually(func() bool {
+				var newWL kueue.Workload
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &newWL)).To(gomega.Succeed())
+				newWL.Spec.Admission = testing.MakeAdmission(clusterQueue.Name).
+					Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()
+				gomega.Expect(k8sClient.Update(ctx, &newWL)).To(gomega.Succeed())
```

  do the update once outside eventually; inside eventually we should only get the workload and check the condition

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T02:23:24Z

- **pkg/controller/core/workload_controller.go:253** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T02:23:24Z
```diff
@@ -244,9 +250,9 @@ func workloadStatus(w *kueue.Workload) string {
 	return pending
 }
 
-func workloadFinished(w *kueue.Workload) bool {
+func WorkloadInCondition(w *kueue.Workload, condition kueue.WorkloadConditionType) bool {
```

  This is only be used by workload_controller_test.go and the kueue is an alias of `sigs.k8s.io/kueue/apis/core/v1alpha1`

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-13T04:16:04Z

- **pkg/controller/core/workload_controller.go:253** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T04:16:04Z
```diff
@@ -244,9 +250,9 @@ func workloadStatus(w *kueue.Workload) string {
 	return pending
 }
 
-func workloadFinished(w *kueue.Workload) bool {
+func WorkloadInCondition(w *kueue.Workload, condition kueue.WorkloadConditionType) bool {
```

  Still, this is not a package that is supposed to export utility-like functions. Lets just place it where it belongs.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T05:34:49Z

- **pkg/controller/core/workload_controller.go:253** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T05:34:49Z
```diff
@@ -244,9 +250,9 @@ func workloadStatus(w *kueue.Workload) string {
 	return pending
 }
 
-func workloadFinished(w *kueue.Workload) bool {
+func WorkloadInCondition(w *kueue.Workload, condition kueue.WorkloadConditionType) bool {
```

  So controllers should not manage exported functions. Thanks for sharing. Also addressed.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-13T13:02:36Z

- **test/integration/controller/core/workload_controller_test.go:129** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T12:36:51Z
```diff
@@ -105,4 +107,39 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("the workload is admitted", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(resourceGPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+			updatedQueueWorkload = kueue.Workload{}
+			clusterQueue = nil
+		})
+
+		ginkgo.It("Should update the workload's condition when it gets admitted", func() {
```

  ```suggestion
  		ginkgo.It("Should update the workload's condition", func() {
  ```

- **test/integration/controller/core/workload_controller_test.go:129** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T12:39:46Z
```diff
@@ -105,4 +107,39 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("the workload is admitted", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(resourceGPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+			updatedQueueWorkload = kueue.Workload{}
+			clusterQueue = nil
+		})
+
+		ginkgo.It("Should update the workload's condition when it gets admitted", func() {
```

  so that it reads like this:
  
  ginkgo.When("the workload is admitted") -> ginkgo.It("Should update the workload's condition")

- **pkg/controller/core/workload_controller.go:94** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T12:53:04Z
```diff
@@ -91,6 +91,11 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
+	if status == admitted && !workload.WorkloadInCondition(&wl, kueue.WorkloadAdmitted) {
```

  actually we don't need the `!workload.WorkloadInCondition` check, it is already part of `UpdateWorkloadStatusIfChanged`

- **pkg/workload/workload.go:227** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T12:55:07Z
```diff
@@ -226,3 +223,12 @@ func UpdateWorkloadStatusIfChanged(ctx context.Context,
 	// Updating an existing condition
 	return UpdateStatus(ctx, c, wl, conditionType, conditionStatus, reason, message)
 }
+
+func WorkloadInCondition(w *kueue.Workload, condition kueue.WorkloadConditionType) bool {
```

  I actually don't think we need this function. see the suggestion in the test.

- **test/integration/controller/core/workload_controller_test.go:141** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T12:59:58Z
```diff
@@ -105,4 +107,39 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("the workload is admitted", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(resourceGPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+			updatedQueueWorkload = kueue.Workload{}
+			clusterQueue = nil
+		})
+
+		ginkgo.It("Should update the workload's condition when it gets admitted", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).Queue(queue.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Admit workload")
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+			updatedQueueWorkload.Spec.Admission = testing.MakeAdmission(clusterQueue.Name).
+				Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() bool {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return workload.WorkloadInCondition(&updatedQueueWorkload, kueue.WorkloadAdmitted)
```

  ```suggestion
                                   i := workload.FindConditionIndex(updatedQueueWorkload.Status, kueue.WorkloadAdmitted)
  			        return i != -1 && updatedQueueWorkload.Status.Conditions[i].Status == corev1.ConditionTrue
  ```

- **pkg/controller/core/workload_controller.go:252** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T13:01:51Z
```diff
@@ -235,20 +241,11 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager) error {
 }
 
 func workloadStatus(w *kueue.Workload) string {
-	if workloadFinished(w) {
+	if workload.WorkloadInCondition(w, kueue.WorkloadFinished) {
 		return finished
 	}
 	if w.Spec.Admission != nil {
 		return admitted
 	}
 	return pending
 }
-
-func workloadFinished(w *kueue.Workload) bool {
-	for _, c := range w.Status.Conditions {
-		if c.Type == kueue.WorkloadFinished {
-			return c.Status == corev1.ConditionTrue
-		}
-	}
```

  we can keep this function, but change the implementation to:
  
  ```
     i := workload.FindConditionIndex(w.Status, kueue.WorkloadFinished)
     return i != -1 && w.Status.Conditions[i].Status == corev1.ConditionTrue
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T14:04:28Z

- **pkg/controller/core/workload_controller.go:94** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T14:04:28Z
```diff
@@ -91,6 +91,11 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
+	if status == admitted && !workload.WorkloadInCondition(&wl, kueue.WorkloadAdmitted) {
```

  Just want to make sure only when `w.Spec.Admission != nil` and Admit status is false or not present, then we'll update the status. Although it seems like we can easily remove the `!workload.WorkloadInCondition(&wl, kueue.WorkloadAdmitted)` now and it behaves the same, but as the function name `UpdateWorkloadStatusIfChanged` indicates, even we change the status reason, it will also update. This can prevent some potential bugs in some day, also I think it's better to read.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T14:04:32Z

- **test/integration/controller/core/workload_controller_test.go:141** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T14:04:32Z
```diff
@@ -105,4 +107,39 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			gomega.Expect(updatedQueueWorkload.Status.Conditions[0].Message).To(testing.Equal(message))
 		})
 	})
+
+	ginkgo.When("the workload is admitted", func() {
+		ginkgo.BeforeEach(func() {
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				Resource(testing.MakeResource(resourceGPU).
+					Flavor(testing.MakeFlavor(flavorOnDemand, "5").Max("10").Obj()).Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+
+			queue = testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, queue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQueue)).To(gomega.Succeed())
+			updatedQueueWorkload = kueue.Workload{}
+			clusterQueue = nil
+		})
+
+		ginkgo.It("Should update the workload's condition when it gets admitted", func() {
+			ginkgo.By("Create workload")
+			wl = testing.MakeWorkload("one", ns.Name).Queue(queue.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Admit workload")
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+			updatedQueueWorkload.Spec.Admission = testing.MakeAdmission(clusterQueue.Name).
+				Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() bool {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return workload.WorkloadInCondition(&updatedQueueWorkload, kueue.WorkloadAdmitted)
```

  yes, they behave the same, but I think it's a little hard to read.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T14:04:35Z

- **pkg/controller/core/workload_controller.go:252** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T14:04:35Z
```diff
@@ -235,20 +241,11 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager) error {
 }
 
 func workloadStatus(w *kueue.Workload) string {
-	if workloadFinished(w) {
+	if workload.WorkloadInCondition(w, kueue.WorkloadFinished) {
 		return finished
 	}
 	if w.Spec.Admission != nil {
 		return admitted
 	}
 	return pending
 }
-
-func workloadFinished(w *kueue.Workload) bool {
-	for _, c := range w.Status.Conditions {
-		if c.Type == kueue.WorkloadFinished {
-			return c.Status == corev1.ConditionTrue
-		}
-	}
```

  I'd like to keep the function `WorkloadInCondition` but change the implement as you advised to reduce redundant. WDYT?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-13T14:20:00Z

- **pkg/controller/core/workload_controller.go:94** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T14:18:14Z
```diff
@@ -91,6 +91,11 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
+	if status == admitted && !workload.WorkloadInCondition(&wl, kueue.WorkloadAdmitted) {
```

  >Just want to make sure only when w.Spec.Admission != nil and Admit status is false or not present, then we'll update the status.
  
  The check done in `WorkloadInCondition` is done by `UpdateWorkloadStatusIfChanged`; as the name suggests, the update will happen only if there was a change. We should remove this check.

- **pkg/controller/core/workload_controller.go:252** by [@ahg-g](https://github.com/ahg-g) — 2022-04-13T14:19:19Z
```diff
@@ -235,20 +241,11 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager) error {
 }
 
 func workloadStatus(w *kueue.Workload) string {
-	if workloadFinished(w) {
+	if workload.WorkloadInCondition(w, kueue.WorkloadFinished) {
 		return finished
 	}
 	if w.Spec.Admission != nil {
 		return admitted
 	}
 	return pending
 }
-
-func workloadFinished(w *kueue.Workload) bool {
-	for _, c := range w.Status.Conditions {
-		if c.Type == kueue.WorkloadFinished {
-			return c.Status == corev1.ConditionTrue
-		}
-	}
```

  sg, but please rename it something like `InCondition`, similarly we can rename `UpdateWorkloadStatusIfChanged` to `UpdateStatusIfChanged` since they are in the workload pkg already.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-13T14:58:41Z

- **pkg/controller/core/workload_controller.go:252** by [@kerthcet](https://github.com/kerthcet) — 2022-04-13T14:58:40Z
```diff
@@ -235,20 +241,11 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager) error {
 }
 
 func workloadStatus(w *kueue.Workload) string {
-	if workloadFinished(w) {
+	if workload.WorkloadInCondition(w, kueue.WorkloadFinished) {
 		return finished
 	}
 	if w.Spec.Admission != nil {
 		return admitted
 	}
 	return pending
 }
-
-func workloadFinished(w *kueue.Workload) bool {
-	for _, c := range w.Status.Conditions {
-		if c.Type == kueue.WorkloadFinished {
-			return c.Status == corev1.ConditionTrue
-		}
-	}
```

  thanks, all addressed.
