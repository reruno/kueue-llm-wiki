# PR #2093: KEP 2076: Kueuectl

**Summary**: KEP 2076: Kueuectl

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2093

**Last updated**: 2024-05-01T18:40:57Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mwielgus](https://github.com/mwielgus)
- **Branch**: `kep-kueuectl` → `main`
- **Created**: 2024-04-29T09:48:54Z
- **Updated**: 2024-05-01T18:40:57Z
- **Merged**: 2024-05-01T18:40:55Z
- **Closed**: 2024-05-01T18:40:55Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 3   **+376 / -1**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Introduces command line tool for Kueue management and API listing. 

#### Which issue(s) this PR fixes:

Fixes #2076

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-29T09:49:12Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8b34b12adb0956ecc51975f2733799b6b8fc7200 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/663280f6beddcc00088ddbf1 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:05:01Z

cc @tenzen-y @vsoch

### Comment by [@vsoch](https://github.com/vsoch) — 2024-04-29T18:06:39Z

I think the administrative use case is good, but it seems much smaller than what I hoped for with respect to this tool - a way to manage and understand the running workloads (the user case, for which I think there are many more than administrators). Looking forward (hoping) to see the latter.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-01T16:53:17Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2093#pullrequestreview-2033963888" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2093#" title="Author self-approved">mwielgus</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-01T18:35:04Z

LGTM label has been added.  <details>Git tree hash: 237c9c3803630c6ce3f7c045619e3bcc1cbdc86c</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-29T13:51:58Z

- **keps/2076-kueuectl/README.md:149** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-29T13:51:10Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
```

  What the short name for this flag should we use? Is it ok to use "-c"?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-04-29T16:22:07Z

- **keps/2076-kueuectl/README.md:149** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-29T16:22:07Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
```

  or cq

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-29T16:26:47Z

- **keps/2076-kueuectl/README.md:149** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-29T16:26:47Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
```

  No, short flag must contain only one symbol.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-29T17:08:13Z

- **keps/2076-kueuectl/kep.yaml:12** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:08:13Z
```diff
@@ -0,0 +1,28 @@
+title: Kueuectl
+kep-number: 2076
+authors:
+  - "@mwielgus"
+status: implementable
+creation-date: 2024-04-25
+reviewers:
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+replaces:
+  - "/keps/487-kubectl-plugin"
```

  Please update KEP's yaml to say `replaced` and leave a note there referencing this KEP.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-29T18:01:17Z

- **keps/2076-kueuectl/README.md:40** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:40:24Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
```

  ```suggestion
  * create Local and ClusterQueues without writing yamls,
  ```

- **keps/2076-kueuectl/README.md:47** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:41:06Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
```

  ```suggestion
  tedious without writing a custom mini script or complex pipe processing.
  ```

- **keps/2076-kueuectl/README.md:56** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:41:34Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
```

  ```suggestion
      * Stopping and resuming individual Workloads.
  ```

- **keps/2076-kueuectl/README.md:83** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:45:33Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
```

  We can submit them to the cluster in dryRun mode, to get validation errors, if any https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/client#CreateOptions

- **keps/2076-kueuectl/README.md:118** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:46:46Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
```

  is the policy a string? so no threshold?
  
  Since this is a niche use case, maybe it's better to leave it to a full yaml.

- **keps/2076-kueuectl/README.md:122** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:48:29Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
```

  What if I want another flavor? Do I add another `--nominal-quota` line?

- **keps/2076-kueuectl/README.md:148** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:51:45Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
```

  what is this for?

- **keps/2076-kueuectl/README.md:213** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:53:40Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
```

  maybe `--condition=Pending|Admitted`?

- **keps/2076-kueuectl/README.md:237** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:56:20Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
+--selector=selector
+```
+
+Output:
+
+* Namespace (if -A is used)
+* Workload name
+* CRD type (truncated to 10 chars)
+* CRD name
+* LocalQueue
+* ClusterQueue
+* Status
+* Position in Queue (if Pending)
+* Age
+
+
+### Stop ClusterQueue
+
+Stops admission and execution inside the specified ClusterQueue, possibly
+limiting the action only to the selected ResourceFlavor.
+Format:
+
+```
+kueuectl stop clusterqueue|cq cqname
+--keep-already-running
+--resource-flavor=rfname
```

  We don't currently have an API to do this. Other than setting nominal to zero, but we would lose information.
  Can you remove, expand or open an issue for it?

- **keps/2076-kueuectl/README.md:226** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T17:59:23Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
+--selector=selector
+```
+
+Output:
+
+* Namespace (if -A is used)
+* Workload name
+* CRD type (truncated to 10 chars)
+* CRD name
+* LocalQueue
+* ClusterQueue
+* Status
+* Position in Queue (if Pending)
```

  This will depend on https://github.com/kubernetes-sigs/kueue/issues/1776
  
  cc @KunWuLuan

- **keps/2076-kueuectl/README.md:257** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T18:00:02Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
+--selector=selector
+```
+
+Output:
+
+* Namespace (if -A is used)
+* Workload name
+* CRD type (truncated to 10 chars)
+* CRD name
+* LocalQueue
+* ClusterQueue
+* Status
+* Position in Queue (if Pending)
+* Age
+
+
+### Stop ClusterQueue
+
+Stops admission and execution inside the specified ClusterQueue, possibly
+limiting the action only to the selected ResourceFlavor.
+Format:
+
+```
+kueuectl stop clusterqueue|cq cqname
+--keep-already-running
+--resource-flavor=rfname
+```
+
+Output:
+None.
+
+### Resume ClusterQueue
+
+Resumes admission inside the specified ClusterQueue.
+Format:
+```
+kueuectl resume clusterqueue|cq cqname 
+-–resource-flavor=rfname
+```
+Output:
+None.
+
+### Stop LocalQueue
+
+Stops execution (or just admission) of Workloads coming from the given LocalQueue. 
+This requires adding StopPolicy to LocalQueue and enforcing its changes in ClusterQueue.
```

  Open a separate issue for this.

- **keps/2076-kueuectl/README.md:280** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T18:00:35Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
+--selector=selector
+```
+
+Output:
+
+* Namespace (if -A is used)
+* Workload name
+* CRD type (truncated to 10 chars)
+* CRD name
+* LocalQueue
+* ClusterQueue
+* Status
+* Position in Queue (if Pending)
+* Age
+
+
+### Stop ClusterQueue
+
+Stops admission and execution inside the specified ClusterQueue, possibly
+limiting the action only to the selected ResourceFlavor.
+Format:
+
+```
+kueuectl stop clusterqueue|cq cqname
+--keep-already-running
+--resource-flavor=rfname
+```
+
+Output:
+None.
+
+### Resume ClusterQueue
+
+Resumes admission inside the specified ClusterQueue.
+Format:
+```
+kueuectl resume clusterqueue|cq cqname 
+-–resource-flavor=rfname
+```
+Output:
+None.
+
+### Stop LocalQueue
+
+Stops execution (or just admission) of Workloads coming from the given LocalQueue. 
+This requires adding StopPolicy to LocalQueue and enforcing its changes in ClusterQueue.
+Format:
+```
+kueuectl stop localqueue|lq lqname
+--keep-already-running
+```
+Output:
+None.
+
+
+### Resume LocalQueue
+
+Resumes admission of Workloads coming from the given LocalQueue.
+Format:
+```
+kueuectl resume localqueue|lq lqname 
+```
+Output:
+None.
+
+### Stop Workload
+
+Puts the given Workload on hold. The Workload will not be admitted and 
+if it is already admitted it will be put back to queue just as if it was preempted. 
```

  Clarify that this can use the `.spec.active` Field.

- **keps/2076-kueuectl/README.md:57** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T18:01:06Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
```

  It doesn't look like this is explained below? Remove it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-29T18:03:56Z

- **keps/2076-kueuectl/README.md:149** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-29T18:03:56Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
```

  @alculquicondor WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-29T18:05:39Z

- **keps/2076-kueuectl/README.md:149** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-29T18:05:39Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
```

  `c` should be fine.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-30T08:53:24Z

- **keps/2076-kueuectl/README.md:41** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T08:21:03Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
```

  Are other resources out of scope like resourceFlavor?

- **keps/2076-kueuectl/README.md:54** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T08:22:38Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
```

  ```suggestion
      * Listing Queues and Workloads that meet certain criteria.
  ```

- **keps/2076-kueuectl/README.md:55** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T08:22:58Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
```

  ```suggestion
      * Stopping and resuming execution in ClusteQueues and LocalQueues.
  ```

- **keps/2076-kueuectl/README.md:59** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T08:24:32Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
```

  ```suggestion
  * Build it on top of kubectl (as a [kubectl plugin](https://kubernetes.io/docs/tasks/extend-kubectl/kubectl-plugins/)) to reuse all of
  ```

- **keps/2076-kueuectl/README.md:61** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T08:25:51Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
```

  Could you add exposing the plugin via krew to Goal?

- **keps/2076-kueuectl/README.md:125** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T08:35:12Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
```

  How to specify fair sharing? Will we skip to implement command for the fair sharing in the first iteration?

- **keps/2076-kueuectl/README.md:148** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T08:45:55Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
```

  I'm not sure if it is possible, but couldn't get namespace from root kubectl arg?
  
  ```
  $ kubectl --namespace=foo kueue ...
  ```

- **keps/2076-kueuectl/README.md:213** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T08:50:53Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
```

  Instead of `--condition=Pending|Admitted`, could we provide a field selector for more flexibility?

- **keps/2076-kueuectl/README.md:346** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T08:52:44Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
+--selector=selector
+```
+
+Output:
+
+* Namespace (if -A is used)
+* Workload name
+* CRD type (truncated to 10 chars)
+* CRD name
+* LocalQueue
+* ClusterQueue
+* Status
+* Position in Queue (if Pending)
+* Age
+
+
+### Stop ClusterQueue
+
+Stops admission and execution inside the specified ClusterQueue, possibly
+limiting the action only to the selected ResourceFlavor.
+Format:
+
+```
+kueuectl stop clusterqueue|cq cqname
+--keep-already-running
+--resource-flavor=rfname
+```
+
+Output:
+None.
+
+### Resume ClusterQueue
+
+Resumes admission inside the specified ClusterQueue.
+Format:
+```
+kueuectl resume clusterqueue|cq cqname 
+-–resource-flavor=rfname
+```
+Output:
+None.
+
+### Stop LocalQueue
+
+Stops execution (or just admission) of Workloads coming from the given LocalQueue. 
+This requires adding StopPolicy to LocalQueue and enforcing its changes in ClusterQueue.
+Format:
+```
+kueuectl stop localqueue|lq lqname
+--keep-already-running
+```
+Output:
+None.
+
+
+### Resume LocalQueue
+
+Resumes admission of Workloads coming from the given LocalQueue.
+Format:
+```
+kueuectl resume localqueue|lq lqname 
+```
+Output:
+None.
+
+### Stop Workload
+
+Puts the given Workload on hold. The Workload will not be admitted and 
+if it is already admitted it will be put back to queue just as if it was preempted. 
+Format:
+```
+kueuectl stop workload name --namespace=ns
+```
+Output:
+None.
+
+
+### Resume Workload
+
+Resumes the Workload, allowing its admission according to regular ClusterQueue rules.
+Format:
+```
+kueuectl resume workload name --namespace=ns
+```
+Output:
+None.
+
+### Pass-through
+
+For completeness there will be 4 additional commands that will simply execute regular kubectl
+so that the users won't have to remember to switch the command to kubectl.
+
+* `delete workload|clusterqueue|cq|localqueue|lq`
+* `get workload|clusterqueue|cq|localqueue|lq`
+* `edit workload|clusterqueue|cq|localqueue|lq`
+* `describe workload|clusterqueue|cq|localqueue|lq`
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Regular unit tests for the commands will be provided. 
+
+#### Integration tests
+
+Integration/E2E tests will be provided for all of the commands.
+
+### Graduation Criteria
+
+Beta:
+* Positive feedback from users.
+* All bugs and issues fixed.
+
+GA/Stable:
+* Positive feedback from users
+* No request for column/flags changes for 0.5 year.
+
+## Implementation History
+
+KEP: 2023-04-27.
+
+## Alternatives
+
+* Use existing kubectl functionality and perform management operations via
+API manipulations.
```

  Could we add another approach that we implement cli by fully scratch instead of kubectl plugin?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-04-30T09:57:15Z

- **keps/2076-kueuectl/README.md:40** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-30T09:57:14Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
```

  Do we want to help generate yamls?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:27:14Z

- **keps/2076-kueuectl/README.md:40** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:27:14Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
```

  Yes, with --dry-run you will get just the yaml.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:31:17Z

- **keps/2076-kueuectl/README.md:41** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:31:17Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
```

  For the first version, probably yes. But I will add it here for completeness.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:34:43Z

- **keps/2076-kueuectl/README.md:61** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:34:43Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
```

  Added.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:35:56Z

- **keps/2076-kueuectl/README.md:83** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:35:56Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
```

  Added.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:36:34Z

- **keps/2076-kueuectl/README.md:118** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:36:34Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
```

  Ok, removed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:38:14Z

- **keps/2076-kueuectl/README.md:125** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:38:14Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
```

  I skipped the topic until fair sharing is completed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:38:28Z

- **keps/2076-kueuectl/README.md:122** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:38:28Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
```

  Yes.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:39:44Z

- **keps/2076-kueuectl/README.md:148** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:39:44Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
```

  So that you can create LQ pointing to CQ that doesn't exist yet.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:54:38Z

- **keps/2076-kueuectl/README.md:280** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:54:38Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
+--selector=selector
+```
+
+Output:
+
+* Namespace (if -A is used)
+* Workload name
+* CRD type (truncated to 10 chars)
+* CRD name
+* LocalQueue
+* ClusterQueue
+* Status
+* Position in Queue (if Pending)
+* Age
+
+
+### Stop ClusterQueue
+
+Stops admission and execution inside the specified ClusterQueue, possibly
+limiting the action only to the selected ResourceFlavor.
+Format:
+
+```
+kueuectl stop clusterqueue|cq cqname
+--keep-already-running
+--resource-flavor=rfname
+```
+
+Output:
+None.
+
+### Resume ClusterQueue
+
+Resumes admission inside the specified ClusterQueue.
+Format:
+```
+kueuectl resume clusterqueue|cq cqname 
+-–resource-flavor=rfname
+```
+Output:
+None.
+
+### Stop LocalQueue
+
+Stops execution (or just admission) of Workloads coming from the given LocalQueue. 
+This requires adding StopPolicy to LocalQueue and enforcing its changes in ClusterQueue.
+Format:
+```
+kueuectl stop localqueue|lq lqname
+--keep-already-running
+```
+Output:
+None.
+
+
+### Resume LocalQueue
+
+Resumes admission of Workloads coming from the given LocalQueue.
+Format:
+```
+kueuectl resume localqueue|lq lqname 
+```
+Output:
+None.
+
+### Stop Workload
+
+Puts the given Workload on hold. The Workload will not be admitted and 
+if it is already admitted it will be put back to queue just as if it was preempted. 
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:55:20Z

- **keps/2076-kueuectl/README.md:346** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:55:20Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
+--selector=selector
+```
+
+Output:
+
+* Namespace (if -A is used)
+* Workload name
+* CRD type (truncated to 10 chars)
+* CRD name
+* LocalQueue
+* ClusterQueue
+* Status
+* Position in Queue (if Pending)
+* Age
+
+
+### Stop ClusterQueue
+
+Stops admission and execution inside the specified ClusterQueue, possibly
+limiting the action only to the selected ResourceFlavor.
+Format:
+
+```
+kueuectl stop clusterqueue|cq cqname
+--keep-already-running
+--resource-flavor=rfname
+```
+
+Output:
+None.
+
+### Resume ClusterQueue
+
+Resumes admission inside the specified ClusterQueue.
+Format:
+```
+kueuectl resume clusterqueue|cq cqname 
+-–resource-flavor=rfname
+```
+Output:
+None.
+
+### Stop LocalQueue
+
+Stops execution (or just admission) of Workloads coming from the given LocalQueue. 
+This requires adding StopPolicy to LocalQueue and enforcing its changes in ClusterQueue.
+Format:
+```
+kueuectl stop localqueue|lq lqname
+--keep-already-running
+```
+Output:
+None.
+
+
+### Resume LocalQueue
+
+Resumes admission of Workloads coming from the given LocalQueue.
+Format:
+```
+kueuectl resume localqueue|lq lqname 
+```
+Output:
+None.
+
+### Stop Workload
+
+Puts the given Workload on hold. The Workload will not be admitted and 
+if it is already admitted it will be put back to queue just as if it was preempted. 
+Format:
+```
+kueuectl stop workload name --namespace=ns
+```
+Output:
+None.
+
+
+### Resume Workload
+
+Resumes the Workload, allowing its admission according to regular ClusterQueue rules.
+Format:
+```
+kueuectl resume workload name --namespace=ns
+```
+Output:
+None.
+
+### Pass-through
+
+For completeness there will be 4 additional commands that will simply execute regular kubectl
+so that the users won't have to remember to switch the command to kubectl.
+
+* `delete workload|clusterqueue|cq|localqueue|lq`
+* `get workload|clusterqueue|cq|localqueue|lq`
+* `edit workload|clusterqueue|cq|localqueue|lq`
+* `describe workload|clusterqueue|cq|localqueue|lq`
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Regular unit tests for the commands will be provided. 
+
+#### Integration tests
+
+Integration/E2E tests will be provided for all of the commands.
+
+### Graduation Criteria
+
+Beta:
+* Positive feedback from users.
+* All bugs and issues fixed.
+
+GA/Stable:
+* Positive feedback from users
+* No request for column/flags changes for 0.5 year.
+
+## Implementation History
+
+KEP: 2023-04-27.
+
+## Alternatives
+
+* Use existing kubectl functionality and perform management operations via
+API manipulations.
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-04-30T11:56:56Z

- **keps/2076-kueuectl/kep.yaml:12** by [@mwielgus](https://github.com/mwielgus) — 2024-04-30T11:56:56Z
```diff
@@ -0,0 +1,28 @@
+title: Kueuectl
+kep-number: 2076
+authors:
+  - "@mwielgus"
+status: implementable
+creation-date: 2024-04-25
+reviewers:
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+replaces:
+  - "/keps/487-kubectl-plugin"
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-30T13:24:28Z

- **keps/2076-kueuectl/README.md:54** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T13:24:28Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
```

  I think it was intentional, to refer to both Local and Cluster Queues. But I agree that it's not obvious.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-30T13:59:58Z

- **keps/2076-kueuectl/README.md:41** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T13:59:58Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
```

  Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-30T14:03:50Z

- **keps/2076-kueuectl/README.md:54** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T14:03:50Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
```

  Oh, I didn't find that.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-30T14:04:18Z

- **keps/2076-kueuectl/README.md:125** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T14:04:17Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
```

  SGTM

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-30T14:22:09Z

- **keps/2076-kueuectl/README.md:148** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T14:22:09Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
```

  @mbobrovskyi is writing the first command. Did you try putting `-n` before `kueue`?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-30T14:23:34Z

- **keps/2076-kueuectl/README.md:148** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T14:23:34Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
```

  maybe `--ignore-unknown-cq`?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-30T14:25:51Z

- **keps/2076-kueuectl/README.md:213** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T14:25:22Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
```

  Conditions are multiple fields, though.
  
  Or do you expect some form of jsonpath? I wonder if it could become too complex to write for a common use case such as conditions.
  
  OTOH, do you think we should have other fields?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-30T14:27:37Z

- **keps/2076-kueuectl/README.md:148** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-30T14:27:37Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
```

  Nope. 
  
  `Error: flags cannot be placed before plugin name: --namespace=foo`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-30T14:29:15Z

- **keps/2076-kueuectl/README.md:148** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T14:29:15Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
```

  I guess that's a limitation of the plugin framework.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-04-30T14:31:01Z

- **keps/2076-kueuectl/README.md:148** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-04-30T14:31:01Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
```

  Yes, but we can put it here `kubectl kueue --namespace=foo create lq lq1 -c cq1`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-30T14:35:09Z

- **keps/2076-kueuectl/README.md:148** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T14:35:09Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * (In the future) Migrating workloads between LocalQueues and other avanced operations
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
```

  Uhm, it's interesting. Thank you for investigating that!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-04-30T16:24:10Z

- **keps/2076-kueuectl/README.md:213** by [@tenzen-y](https://github.com/tenzen-y) — 2024-04-30T16:24:10Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
```

  @alculquicondor I was imagined similar to the [`kubectl --field-selector`](https://kubernetes.io/docs/concepts/overview/working-with-objects/field-selectors/).
  
  I didn't mean that JSONPath.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-04-30T17:36:53Z

- **keps/2076-kueuectl/README.md:213** by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T17:36:53Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
```

  we can probably make it work for most fields, but conditions will still look weird.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-01T15:58:24Z

- **keps/2076-kueuectl/README.md:237** by [@mwielgus](https://github.com/mwielgus) — 2024-05-01T15:58:24Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
+--selector=selector
+```
+
+Output:
+
+* Namespace (if -A is used)
+* Workload name
+* CRD type (truncated to 10 chars)
+* CRD name
+* LocalQueue
+* ClusterQueue
+* Status
+* Position in Queue (if Pending)
+* Age
+
+
+### Stop ClusterQueue
+
+Stops admission and execution inside the specified ClusterQueue, possibly
+limiting the action only to the selected ResourceFlavor.
+Format:
+
+```
+kueuectl stop clusterqueue|cq cqname
+--keep-already-running
+--resource-flavor=rfname
```

  Added a comment.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-01T16:02:10Z

- **keps/2076-kueuectl/README.md:257** by [@mwielgus](https://github.com/mwielgus) — 2024-05-01T16:02:10Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
+```
+
+Output:  
+A simple confirmation `localqueue.kueue.x-k8s.io/xxxxx created`
+
+### List ClusterQueue
+
+List all ClusterQueues, potentially limiting output to those that are active/inactive and 
+matching the label selector. Format:
+
+```
+kueuectl list cq|clusterqueue(s)
+--active=*|true|false
+--selector=selector                   # label selector
+```
+
+Output columns:
+
+* Name
+* Cohort
+* Pending Workloads
+* Admitted Workloads
+* Status (active or not)
+* Age
+
+### List LocalQueue 
+
+Lists LocalQueues that match the given criteria: point to a specific CQ, 
+being active/inactive, belonging to the specified namespace or matching the label
+selector.
+Format: 
+
+```
+kueuectl list lq|localqueue(s) 
+–-namespace=ns                        # uses context's default namespaces if not specified
+--all-namespaces | -A
+-–clusterqueue=clusterqueue 
+–-active=*|true|false
+--selector=selector                   # label selector
+```
+
+Outputs columns:
+
+* Namespace (if -A is used)
+* Name
+* ClusterQueue
+* Pending Workloads 
+* Admitted Workloads 
+* Status (active or not)
+* Age
+
+### List Workloads
+
+Lists Workloads that match the provided criteria. Format:
+
+```
+kueuectl list workloads 
+--namespace=ns                        # uses context's default namespace if not specified
+--all-namespaces | -A
+--clusterqueue=cq 
+-–localqueue=lq 
+-—only-pending
+—-only-admitted
+--selector=selector
+```
+
+Output:
+
+* Namespace (if -A is used)
+* Workload name
+* CRD type (truncated to 10 chars)
+* CRD name
+* LocalQueue
+* ClusterQueue
+* Status
+* Position in Queue (if Pending)
+* Age
+
+
+### Stop ClusterQueue
+
+Stops admission and execution inside the specified ClusterQueue, possibly
+limiting the action only to the selected ResourceFlavor.
+Format:
+
+```
+kueuectl stop clusterqueue|cq cqname
+--keep-already-running
+--resource-flavor=rfname
+```
+
+Output:
+None.
+
+### Resume ClusterQueue
+
+Resumes admission inside the specified ClusterQueue.
+Format:
+```
+kueuectl resume clusterqueue|cq cqname 
+-–resource-flavor=rfname
+```
+Output:
+None.
+
+### Stop LocalQueue
+
+Stops execution (or just admission) of Workloads coming from the given LocalQueue. 
+This requires adding StopPolicy to LocalQueue and enforcing its changes in ClusterQueue.
```

  Create #2109

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-01T16:02:34Z

- **keps/2076-kueuectl/README.md:57** by [@mwielgus](https://github.com/mwielgus) — 2024-05-01T16:02:34Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
```

  Fixed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-01T16:03:46Z

- **keps/2076-kueuectl/README.md:148** by [@mwielgus](https://github.com/mwielgus) — 2024-05-01T16:03:46Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
```

  Ol, changed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-01T16:49:54Z

- **keps/2076-kueuectl/README.md:148** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T16:49:54Z
```diff
@@ -0,0 +1,342 @@
+# KEP-2076: Kueuectl
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Create ClusterQueue](#create-clusterqueue)
+  - [Create LocalQueue](#create-localqueue)
+  - [List ClusterQueue](#list-clusterqueue)
+  - [List LocalQueue](#list-localqueue)
+  - [List Workloads](#list-workloads)
+  - [Stop ClusterQueue](#stop-clusterqueue)
+  - [Resume ClusterQueue](#resume-clusterqueue)
+  - [Stop LocalQueue](#stop-localqueue)
+  - [Resume LocalQueue](#resume-localqueue)
+  - [Stop Workload](#stop-workload)
+  - [Resume Workload](#resume-workload)
+  - [Pass-through](#pass-through)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+We want to create a command line tool for Kueue that allows to:
+
+* list Kueue's objects with easy to use Kueue-specific filtering,
+* create Local and ClusterQueues without touching yamls,
+* perform management operations on LQs, CQs and Workloads.
+
+## Motivation
+
+Currently many administrative operations around Kueue are largely inconvenient. 
+They require full API understanding, are relatively error-prone or are simply
+impossible without writing a custom mini script or complex pipe processing.
+
+### Goals
+
+* Provide a command line tool for system administrator to:
+
+    * Create ClusterQueues and LocalQueues.
+    * Listing *Queues and Workloads that meet certain criteria.
+    * Stopping and resuming execution in ClusteQueuesr and LocalQueues.
+    * Stooping and resuming individual Workloads.
+    * Migrating workloads between LocalQueues.
+    
+* Build it on top of kubectl (as a kubectl plugin) to reuse all of
+the authentication/cluster selection methods.
+
+### Non-Goals
+
+* Provide any other interface than a command line (no web ui).
+* Provide a tool targeted at ml researchers to make running jobs on Kubernetes easier.
+* Expose additional metrics or statuses.
+
+## Proposal
+
+Create kueue kubectl plugin with a set of listing and management commands in form of:
+
+```
+kubectl kueue <command> <object> <flags>
+```
+
+Additionally provide a wrapper script to allow shorter syntax like:
+
+```
+kueuectl <command> <object> <flags>
+```
+
+The commands automatically submit all changes unless `--dry-run` option is given - in that 
+case the tool will print out yamls without making any changes in the cluster.
+
+### User Stories (Optional)
+
+#### Story 1
+
+I want to stop admission of new Workloads on a specific ClusterQueue but allow the already
+running Workloads to complete without manually editing the CQ's definition.
+
+#### Story 2
+
+I want to create a LocalQueue pointing to a specific CQ without creating a one-time yaml.
+
+### Risks and Mitigations
+
+* There will be an additional binary placed on sysadmin's machine, whose version will
+have to be keep in sync.
+
+## Design Details
+
+The following commands will be provided within the plugin:
+
+### Create ClusterQueue 
+
+Creates a ClusterQueue with the given name, cohort, specified quota and other details.
+Format:
+
+```
+kueuectl create cq|clusterqueue cqname 
+–-cohort=cohortname                        # defaults to "" - no cohort
+
+--queuing-strategy=strategy                # defaults to BestEffortFIFO
+--namespace-selector=selector              # defaults to {} - all namespaces can use the queue
+--reclaim-within-cohort=policy             # defaults to Never
+--preemption-within-cluster-queue = policy # defaults to Never
+--preemption-when-borrowing = policy       # defaults to Never
+
+–-nominal-quota=rfname1:resource1=value,resource2=value,resource3=value
+–-borrowing-limit=rfname1:resource1=value,resource2=value,resource3=value
+–-lending-limit=rfname1:resource1=value,resource2=value,resource3=value
+```
+
+It is possible to create a ClusterQueue with multiple resource flavors/FlavorQuotas inside
+a ResourceGroup and multiple ResourceGroups covering different sets of resources.
+The command will create the appropriate resource groups with resource flavors 
+in the order they appear in the command line. If two settings have at least 
+one common resource quota specified, they will land in the same ResourceGroup.
+
+Output: 
+A simple confirmation, like in regular kubectl create, 
+`clusterqueue.kueue.x-k8s.io/xxxxx created`
+
+
+### Create LocalQueue
+
+Creates a LocalQueue with the given name pointing to specified ClusterQueue.
+The command validates that the target ClusterQueue exists and 
+its namespace selector matches to LocalQueue's namespace.
+
+Format:
+
+```
+kueuectl create lq|localqueue lqname
+–-namespace=namespace                  # uses context's default namespace if not specified
+–-clusterqueue=cqname
+--skip-validation
```

  did you forget to commit/push?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-01T16:53:12Z

/approve
Leaving LGTM to @tenzen-y

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-01T18:18:20Z

Otherwise lgtm

- **keps/2076-kueuectl/kep.yaml:20** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-01T18:18:08Z
```diff
@@ -0,0 +1,28 @@
+title: Kueuectl
+kep-number: 2076
+authors:
+  - "@mwielgus"
+status: implementable
+creation-date: 2024-04-25
+reviewers:
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+replaces:
+  - "/keps/487-kubectl-plugin"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.8"
```

  ```suggestion
  latest-milestone: "v0.7"
  ```
  
  @mwielgus Isn't this target v0.7? Because we already implemented the part of the command.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-01T18:21:33Z

- **keps/2076-kueuectl/kep.yaml:25** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-01T18:21:31Z
```diff
@@ -0,0 +1,28 @@
+title: Kueuectl
+kep-number: 2076
+authors:
+  - "@mwielgus"
+status: implementable
+creation-date: 2024-04-25
+reviewers:
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+replaces:
+  - "/keps/487-kubectl-plugin"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.8"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.8"
+  beta: "v0.9"
```

  ```suggestion
    alpha: "v0.7"
    beta: "v0.8"
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-01T18:31:47Z

- **keps/2076-kueuectl/kep.yaml:20** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-01T18:31:47Z
```diff
@@ -0,0 +1,28 @@
+title: Kueuectl
+kep-number: 2076
+authors:
+  - "@mwielgus"
+status: implementable
+creation-date: 2024-04-25
+reviewers:
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+replaces:
+  - "/keps/487-kubectl-plugin"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.8"
```

  Although we will likely not finish a meaningful amount of commands. Not sure.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-01T18:34:30Z

- **keps/2076-kueuectl/kep.yaml:20** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-01T18:34:30Z
```diff
@@ -0,0 +1,28 @@
+title: Kueuectl
+kep-number: 2076
+authors:
+  - "@mwielgus"
+status: implementable
+creation-date: 2024-04-25
+reviewers:
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+replaces:
+  - "/keps/487-kubectl-plugin"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.8"
```

  If your team doesn't have enough time to implement commands, I'm ok with postponing it to v0.8.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-01T18:34:58Z

- **keps/2076-kueuectl/kep.yaml:20** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-01T18:34:58Z
```diff
@@ -0,0 +1,28 @@
+title: Kueuectl
+kep-number: 2076
+authors:
+  - "@mwielgus"
+status: implementable
+creation-date: 2024-04-25
+reviewers:
+  - "@alculquicondor"
+approvers:
+  - "@alculquicondor"
+replaces:
+  - "/keps/487-kubectl-plugin"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.8"
```

  /lgtm
