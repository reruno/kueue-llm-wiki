# PR #2099: Add sidecar containers to resource requests computation

**Summary**: Add sidecar containers to resource requests computation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2099

**Last updated**: 2024-05-28T11:33:35Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `support-sidecar-containers` → `main`
- **Created**: 2024-04-29T14:42:23Z
- **Updated**: 2024-05-28T11:33:35Z
- **Merged**: 2024-05-07T15:11:49Z
- **Closed**: 2024-05-07T15:11:50Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 3   **+262 / -89**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind bug

#### What this PR does / why we need it:

Includes `SidecarContainers` in the computation of total resource requests of a pod.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2068 

#### Special notes for your reviewer:

The new formula to calculate the total was extracted from [KEP-753](https://github.com/kubernetes/enhancements/tree/master/keps/sig-node/753-sidecar-containers#resources-calculation-for-scheduling-and-pod-admission).

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix the resource requests computation taking into account sidecar containers.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-04-29T14:42:42Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 053c150ff587524c855e277dfa4f7679acd5dae6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/663a31e1bd605100082ca63e |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T13:18:24Z

Please add a release note.

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-04-30T14:35:37Z

How urgent is this? Is it acceptable to wait until https://github.com/kubernetes/kubernetes/issues/124537 is fixed, and use that library? @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-30T14:58:11Z

No, we cannot wait. That change will only be available in a released module when all of v1.31 is released, which will be ~3 months from now.

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-07T12:41:07Z

/test pull-kueue-test-integration-main

### Comment by [@gabesaba](https://github.com/gabesaba) — 2024-05-07T13:56:34Z

lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-07T14:51:18Z

LGTM label has been added.  <details>Git tree hash: df9454efb9a3ffa1eafdb3d2d48d9edc8dc9219d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-07T14:51:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2099#pullrequestreview-2043433257" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2099#" title="Author self-approved">IrvingMg</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-07T14:51:50Z

/cherry-pick release-0.6

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-05-07T14:51:52Z

@alculquicondor: once the present PR merges, I will cherry-pick it on top of release-0.6 in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2099#issuecomment-2098593857):

>/cherry-pick release-0.6


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-05-07T15:12:25Z

@alculquicondor: #2099 failed to apply on top of branch "release-0.6":
```
Applying: Add sidecar containers to resource requests computation
Applying: Refactor init containers resource computation
Applying: Refactor tests cases using Container wrapper
Using index info to reconstruct a base tree...
M	pkg/util/testing/wrappers.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/util/testing/wrappers.go
CONFLICT (content): Merge conflict in pkg/util/testing/wrappers.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0003 Refactor tests cases using Container wrapper
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2099#issuecomment-2098593857):

>/cherry-pick release-0.6


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-07T15:22:58Z

@IrvingMg Could you open a PR manually?

## Reviews

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-04-30T07:18:22Z

- **pkg/util/limitrange/limitrange.go:84** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-04-30T07:18:22Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
```

  Is that max needed?
  ```suggestion
  // total = Sum(Max( nonSidecarInitContainers + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
  ```

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-04-30T11:14:14Z

- **pkg/util/limitrange/limitrange.go:84** by [@IrvingMg](https://github.com/IrvingMg) — 2024-04-30T11:14:14Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
```

  Yes, to have the first argument of the outer `Max()`, we must first find the `nonSidecarInitContainer` that requires the most resources and then add the result of the `SidecarContainers` resources summation.
  
  More about the resource calculation can be found here: https://github.com/kubernetes/enhancements/tree/master/keps/sig-node/753-sidecar-containers#resources-calculation-for-scheduling-and-pod-admission

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-04-30T14:58:03Z

- **pkg/util/limitrange/limitrange.go:84** by [@gabesaba](https://github.com/gabesaba) — 2024-04-30T14:40:26Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
```

  This is the first of the two formulas in the kep, right? Are we okay with this simplification? As we may end up overestimating resource usage

- **pkg/util/limitrange/limitrange.go:94** by [@gabesaba](https://github.com/gabesaba) — 2024-04-30T14:42:10Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
 func TotalRequests(ps *corev1.PodSpec) corev1.ResourceList {
-	total := corev1.ResourceList{}
-
 	// add the resource from the main containers
+	sumContainers := corev1.ResourceList{}
 	for i := range ps.Containers {
-		total = resource.MergeResourceListKeepSum(total, ps.Containers[i].Resources.Requests)
+		sumContainers = resource.MergeResourceListKeepSum(sumContainers, ps.Containers[i].Resources.Requests)
 	}
 
-	// take into account the maximum of any init containers
+	// take into account the maximum of any non sidecar init containers and add the sidecar containers
+	maxNonSidecarInitContainers := corev1.ResourceList{}
+	sumSidecarContainers := corev1.ResourceList{}
```

  nit: factor this logic out into a function for sidecar and init containers

- **pkg/util/limitrange/limitrange.go:96** by [@gabesaba](https://github.com/gabesaba) — 2024-04-30T14:44:39Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
 func TotalRequests(ps *corev1.PodSpec) corev1.ResourceList {
-	total := corev1.ResourceList{}
-
 	// add the resource from the main containers
+	sumContainers := corev1.ResourceList{}
 	for i := range ps.Containers {
-		total = resource.MergeResourceListKeepSum(total, ps.Containers[i].Resources.Requests)
+		sumContainers = resource.MergeResourceListKeepSum(sumContainers, ps.Containers[i].Resources.Requests)
 	}
 
-	// take into account the maximum of any init containers
+	// take into account the maximum of any non sidecar init containers and add the sidecar containers
+	maxNonSidecarInitContainers := corev1.ResourceList{}
+	sumSidecarContainers := corev1.ResourceList{}
 	for i := range ps.InitContainers {
-		total = resource.MergeResourceListKeepMax(total, ps.InitContainers[i].Resources.Requests)
+		// Is the init container a sidecar container?
```

  nit: remove this comment. it will be clear from the function name (comment above)

- **pkg/util/limitrange/limitrange.go:94** by [@gabesaba](https://github.com/gabesaba) — 2024-04-30T14:45:29Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
 func TotalRequests(ps *corev1.PodSpec) corev1.ResourceList {
-	total := corev1.ResourceList{}
-
 	// add the resource from the main containers
+	sumContainers := corev1.ResourceList{}
 	for i := range ps.Containers {
-		total = resource.MergeResourceListKeepSum(total, ps.Containers[i].Resources.Requests)
+		sumContainers = resource.MergeResourceListKeepSum(sumContainers, ps.Containers[i].Resources.Requests)
 	}
 
-	// take into account the maximum of any init containers
+	// take into account the maximum of any non sidecar init containers and add the sidecar containers
+	maxNonSidecarInitContainers := corev1.ResourceList{}
+	sumSidecarContainers := corev1.ResourceList{}
```

  unless you will implement the second formula in the kep; which seems to require calculation together

- **pkg/util/limitrange/limitrange_test.go:236** by [@gabesaba](https://github.com/gabesaba) — 2024-04-30T14:54:11Z
```diff
@@ -219,6 +230,17 @@ func TestTotalRequest(t *testing.T) {
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
+		"include sidecar container": {
+			podSpec: &corev1.PodSpec{
+				InitContainers: containers[2:],
+				Containers:     containers[:2],
```

  Could we inline these lists for readability? If it is not too much additional effort, please refactor the other tests too
  
  maybe some api like:
  `CreateContainer().WithCpuReqs("1000m").WithMemoryReqs("2Gi").Sidecar()`
  
  or even more simply just a few functions which create a few different pods, that you could mix and match in the various tests

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-04-30T15:06:17Z

- **pkg/util/limitrange/limitrange.go:84** by [@IrvingMg](https://github.com/IrvingMg) — 2024-04-30T15:06:17Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
```

  Exactly. That's the first formula in the KEP. 
  
  @alculquicondor WDYT?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-04-30T15:12:04Z

- **pkg/util/limitrange/limitrange_test.go:236** by [@gabesaba](https://github.com/gabesaba) — 2024-04-30T15:12:03Z
```diff
@@ -219,6 +230,17 @@ func TestTotalRequest(t *testing.T) {
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
+		"include sidecar container": {
+			podSpec: &corev1.PodSpec{
+				InitContainers: containers[2:],
+				Containers:     containers[:2],
```

  Also, please add more test cases, stressing each part of the formula so that we're more confident it is working as intended

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-04-30T15:18:44Z

- **pkg/util/limitrange/limitrange.go:94** by [@IrvingMg](https://github.com/IrvingMg) — 2024-04-30T15:18:43Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
 func TotalRequests(ps *corev1.PodSpec) corev1.ResourceList {
-	total := corev1.ResourceList{}
-
 	// add the resource from the main containers
+	sumContainers := corev1.ResourceList{}
 	for i := range ps.Containers {
-		total = resource.MergeResourceListKeepSum(total, ps.Containers[i].Resources.Requests)
+		sumContainers = resource.MergeResourceListKeepSum(sumContainers, ps.Containers[i].Resources.Requests)
 	}
 
-	// take into account the maximum of any init containers
+	// take into account the maximum of any non sidecar init containers and add the sidecar containers
+	maxNonSidecarInitContainers := corev1.ResourceList{}
+	sumSidecarContainers := corev1.ResourceList{}
```

  Do you mean that there should be a function to compute the total for init containers that returns those two values? Or one function for sidecar and another one for nonSidecar containers?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-04-30T15:39:50Z

- **pkg/util/limitrange/limitrange.go:94** by [@gabesaba](https://github.com/gabesaba) — 2024-04-30T15:39:49Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
 func TotalRequests(ps *corev1.PodSpec) corev1.ResourceList {
-	total := corev1.ResourceList{}
-
 	// add the resource from the main containers
+	sumContainers := corev1.ResourceList{}
 	for i := range ps.Containers {
-		total = resource.MergeResourceListKeepSum(total, ps.Containers[i].Resources.Requests)
+		sumContainers = resource.MergeResourceListKeepSum(sumContainers, ps.Containers[i].Resources.Requests)
 	}
 
-	// take into account the maximum of any init containers
+	// take into account the maximum of any non sidecar init containers and add the sidecar containers
+	maxNonSidecarInitContainers := corev1.ResourceList{}
+	sumSidecarContainers := corev1.ResourceList{}
```

  I'd lean towards two functions, but this is up to you :)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-05-02T15:17:16Z

overall lgtm, just a few nits. thanks for addressing the comments.

- **pkg/util/limitrange/limitrange.go:106** by [@gabesaba](https://github.com/gabesaba) — 2024-05-02T14:56:05Z
```diff
@@ -110,6 +100,26 @@ func TotalRequests(ps *corev1.PodSpec) corev1.ResourceList {
 	return total
 }
 
+func calculateRegularInitContainersResources(initContainers []corev1.Container) corev1.ResourceList {
+	total := corev1.ResourceList{}
+	for i := range initContainers {
+		if initContainers[i].RestartPolicy == nil || *initContainers[i].RestartPolicy != corev1.ContainerRestartPolicyAlways {
```

  nit: consider factoring this logic to isSideCarContainer function
  `if !isSideCarContainer()`

- **pkg/util/limitrange/limitrange.go:116** by [@gabesaba](https://github.com/gabesaba) — 2024-05-02T14:56:18Z
```diff
@@ -110,6 +100,26 @@ func TotalRequests(ps *corev1.PodSpec) corev1.ResourceList {
 	return total
 }
 
+func calculateRegularInitContainersResources(initContainers []corev1.Container) corev1.ResourceList {
+	total := corev1.ResourceList{}
+	for i := range initContainers {
+		if initContainers[i].RestartPolicy == nil || *initContainers[i].RestartPolicy != corev1.ContainerRestartPolicyAlways {
+			total = resource.MergeResourceListKeepMax(total, initContainers[i].Resources.Requests)
+		}
+	}
+	return total
+}
+
+func calculateSidecarContainersResources(initContainers []corev1.Container) corev1.ResourceList {
+	total := corev1.ResourceList{}
+	for i := range initContainers {
+		if initContainers[i].RestartPolicy != nil && *initContainers[i].RestartPolicy == corev1.ContainerRestartPolicyAlways {
```

  nit: same as above
  
  `if isSideCarContainer()`

- **pkg/util/limitrange/limitrange_test.go:1** by [@gabesaba](https://github.com/gabesaba) — 2024-05-02T15:01:30Z
  nit: consider making units consistent. e.g. for CPU either "1" or "1000m" in all places. makes it easier to quickly calculate as a reader

- **pkg/util/limitrange/limitrange_test.go:246** by [@gabesaba](https://github.com/gabesaba) — 2024-05-02T15:02:52Z
```diff
@@ -161,90 +160,184 @@ func TestSummarize(t *testing.T) {
 }
 
 func TestTotalRequest(t *testing.T) {
-	containers := []corev1.Container{
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("1"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("4"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("1"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-					"example.com/gpu":     resource.MustParse("2"),
-				},
-			},
-			RestartPolicy: ptr.To(corev1.ContainerRestartPolicyAlways),
-		},
-	}
 	cases := map[string]struct {
 		podSpec *corev1.PodSpec
 		want    corev1.ResourceList
 	}{
-		"sum up main containers": {
+		"pod without init containers": {
 			podSpec: &corev1.PodSpec{
-				Containers: containers[:2],
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
 				corev1.ResourceCPU:    resource.MustParse("2500m"),
 				corev1.ResourceMemory: resource.MustParse("2Gi"),
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
-		"one init wants more": {
+		"pod only with regular init containers": {
 			podSpec: &corev1.PodSpec{
-				InitContainers: containers[2:4],
-				Containers:     containers[:2],
+				InitContainers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "4").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
 				corev1.ResourceCPU:    resource.MustParse("4000m"),
 				corev1.ResourceMemory: resource.MustParse("2Gi"),
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
-		"include sidecar container": {
+		"pod only with sidecar containers": {
 			podSpec: &corev1.PodSpec{
-				InitContainers: containers[2:],
-				Containers:     containers[:2],
+				InitContainers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "4").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						AsSidecar().
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						AsSidecar().
+						Obj(),
+				},
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
-				corev1.ResourceCPU:    resource.MustParse("5000m"),
+				corev1.ResourceCPU:    resource.MustParse("8000m"),
 				corev1.ResourceMemory: resource.MustParse("4Gi"),
 				"example.com/gpu":     resource.MustParse("4"),
 			},
 		},
+		"pod only with init containers": {
```

  there are sidecars in this test?

- **pkg/util/limitrange/limitrange_test.go:186** by [@gabesaba](https://github.com/gabesaba) — 2024-05-02T15:12:44Z
```diff
@@ -160,79 +160,194 @@ func TestSummarize(t *testing.T) {
 }
 
 func TestTotalRequest(t *testing.T) {
-	containers := []corev1.Container{
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("1"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("4"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-	}
 	cases := map[string]struct {
 		podSpec *corev1.PodSpec
 		want    corev1.ResourceList
 	}{
-		"sum up main containers": {
+		"pod without init containers": {
 			podSpec: &corev1.PodSpec{
-				Containers: containers[:2],
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
 				corev1.ResourceCPU:    resource.MustParse("2500m"),
 				corev1.ResourceMemory: resource.MustParse("2Gi"),
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
-		"one init wants more": {
+		"pod only with regular init containers": {
```

  nit: "pod with init containers"

- **pkg/util/limitrange/limitrange_test.go:186** by [@gabesaba](https://github.com/gabesaba) — 2024-05-02T15:15:46Z
```diff
@@ -160,79 +160,194 @@ func TestSummarize(t *testing.T) {
 }
 
 func TestTotalRequest(t *testing.T) {
-	containers := []corev1.Container{
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("1"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("4"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-	}
 	cases := map[string]struct {
 		podSpec *corev1.PodSpec
 		want    corev1.ResourceList
 	}{
-		"sum up main containers": {
+		"pod without init containers": {
 			podSpec: &corev1.PodSpec{
-				Containers: containers[:2],
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
 				corev1.ResourceCPU:    resource.MustParse("2500m"),
 				corev1.ResourceMemory: resource.MustParse("2Gi"),
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
-		"one init wants more": {
+		"pod only with regular init containers": {
```

  nit: consider a description of not only the setup, but what we hope to verify with a particularly test
  
  e.g "pod with init containers. request max of all init containers"
  
  consider these sorts of descriptions for other tests too.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-02T17:42:18Z

- **pkg/util/limitrange/limitrange_test.go:246** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-02T17:42:18Z
```diff
@@ -161,90 +160,184 @@ func TestSummarize(t *testing.T) {
 }
 
 func TestTotalRequest(t *testing.T) {
-	containers := []corev1.Container{
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("1"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("4"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("1"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-					"example.com/gpu":     resource.MustParse("2"),
-				},
-			},
-			RestartPolicy: ptr.To(corev1.ContainerRestartPolicyAlways),
-		},
-	}
 	cases := map[string]struct {
 		podSpec *corev1.PodSpec
 		want    corev1.ResourceList
 	}{
-		"sum up main containers": {
+		"pod without init containers": {
 			podSpec: &corev1.PodSpec{
-				Containers: containers[:2],
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
 				corev1.ResourceCPU:    resource.MustParse("2500m"),
 				corev1.ResourceMemory: resource.MustParse("2Gi"),
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
-		"one init wants more": {
+		"pod only with regular init containers": {
 			podSpec: &corev1.PodSpec{
-				InitContainers: containers[2:4],
-				Containers:     containers[:2],
+				InitContainers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "4").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
 				corev1.ResourceCPU:    resource.MustParse("4000m"),
 				corev1.ResourceMemory: resource.MustParse("2Gi"),
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
-		"include sidecar container": {
+		"pod only with sidecar containers": {
 			podSpec: &corev1.PodSpec{
-				InitContainers: containers[2:],
-				Containers:     containers[:2],
+				InitContainers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "4").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						AsSidecar().
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						AsSidecar().
+						Obj(),
+				},
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
-				corev1.ResourceCPU:    resource.MustParse("5000m"),
+				corev1.ResourceCPU:    resource.MustParse("8000m"),
 				corev1.ResourceMemory: resource.MustParse("4Gi"),
 				"example.com/gpu":     resource.MustParse("4"),
 			},
 		},
+		"pod only with init containers": {
```

  Yes, the last two items in `InitContainers` field are sidecars, and the first two are regular init containers.  I updated the description for more clarity.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-02T17:52:38Z

- **pkg/util/limitrange/limitrange_test.go:186** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-02T17:52:38Z
```diff
@@ -160,79 +160,194 @@ func TestSummarize(t *testing.T) {
 }
 
 func TestTotalRequest(t *testing.T) {
-	containers := []corev1.Container{
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("1"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("4"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-	}
 	cases := map[string]struct {
 		podSpec *corev1.PodSpec
 		want    corev1.ResourceList
 	}{
-		"sum up main containers": {
+		"pod without init containers": {
 			podSpec: &corev1.PodSpec{
-				Containers: containers[:2],
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
 				corev1.ResourceCPU:    resource.MustParse("2500m"),
 				corev1.ResourceMemory: resource.MustParse("2Gi"),
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
-		"one init wants more": {
+		"pod only with regular init containers": {
```

  > nit: "pod with init containers"
  
  I think it's best to leave `regular init containers` since we aren't passing sidecar containers which might be considered InitContainers as well.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-02T17:52:54Z

- **pkg/util/limitrange/limitrange_test.go:186** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-02T17:52:53Z
```diff
@@ -160,79 +160,194 @@ func TestSummarize(t *testing.T) {
 }
 
 func TestTotalRequest(t *testing.T) {
-	containers := []corev1.Container{
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("1"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU:    resource.MustParse("4"),
-					corev1.ResourceMemory: resource.MustParse("2Gi"),
-				},
-			},
-		},
-		{
-			Resources: corev1.ResourceRequirements{
-				Requests: corev1.ResourceList{
-					corev1.ResourceCPU: resource.MustParse("1500m"),
-					"example.com/gpu":  resource.MustParse("2"),
-				},
-			},
-		},
-	}
 	cases := map[string]struct {
 		podSpec *corev1.PodSpec
 		want    corev1.ResourceList
 	}{
-		"sum up main containers": {
+		"pod without init containers": {
 			podSpec: &corev1.PodSpec{
-				Containers: containers[:2],
+				Containers: []corev1.Container{
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1").
+						WithResourceReq(corev1.ResourceMemory, "2Gi").
+						Obj(),
+					*testingutil.MakeContainer().
+						WithResourceReq(corev1.ResourceCPU, "1500m").
+						WithResourceReq("example.com/gpu", "2").
+						Obj(),
+				},
 			},
 			want: corev1.ResourceList{
 				corev1.ResourceCPU:    resource.MustParse("2500m"),
 				corev1.ResourceMemory: resource.MustParse("2Gi"),
 				"example.com/gpu":     resource.MustParse("2"),
 			},
 		},
-		"one init wants more": {
+		"pod only with regular init containers": {
```

  > nit: consider a description of not only the setup, but what we hope to verify with a particularly test
  > 
  > e.g "pod with init containers. request max of all init containers"
  > 
  > consider these sorts of descriptions for other tests too.
  
  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-03T18:00:38Z

- **pkg/util/limitrange/limitrange.go:84** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-03T18:00:38Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
```

  Let's use the final formula:
  
  ```
  InitContainerUse(i) = Sum(sidecar containers with index < i) + InitContainer(i)
  Max ( Max( each InitContainerUse ) , Sum(Sidecar Containers) + Sum(Containers) )
  ```

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-06T15:18:44Z

- **pkg/util/limitrange/limitrange.go:84** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-06T15:18:44Z
```diff
@@ -81,20 +81,30 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Sum(Max( Max(nonSidecarInitContainers) + Sum(SidecarContainers), Sum(SidecarContainers) + Sum(Containers) ), overhead)
```

  Formula updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-06T19:47:39Z

unit tests look solid :)

- **pkg/util/limitrange/limitrange.go:84** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-06T19:37:03Z
```diff
@@ -81,25 +81,54 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Max( Max(each InitContainerUse) , Sum(SidecarContainers) + Sum(Containers) ) + pod overhead
```

  Add this to the comment too:
  
  ```
  InitContainerUse(i) = Sum(sidecar containers with index < i) + InitContainer(i)
  ```

- **pkg/util/limitrange/limitrange.go:110** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-06T19:41:34Z
```diff
@@ -81,25 +81,54 @@ func (s Summary) validatePodSpecContainers(containers []corev1.Container, path *
 }
 
 // TotalRequests computes the total resource requests of a pod.
-// total = sum(max(sum(.containers[].requests), initContainers[].requests), overhead)
+// total = Max( Max(each InitContainerUse) , Sum(SidecarContainers) + Sum(Containers) ) + pod overhead
 func TotalRequests(ps *corev1.PodSpec) corev1.ResourceList {
-	total := corev1.ResourceList{}
+	sumContainers := calculateMainContainersResources(ps.Containers)
+	maxInitContainers := calculateInitContainersResources(ps.InitContainers)
+	sumSidecarContainers := calculateSidecarContainersResources(ps.InitContainers)
+
+	total := resource.MergeResourceListKeepMax(
+		maxInitContainers,
+		resource.MergeResourceListKeepSum(sumSidecarContainers, sumContainers),
+	)
+	// add the overhead
+	total = resource.MergeResourceListKeepSum(total, ps.Overhead)
+	return total
+}
 
-	// add the resource from the main containers
-	for i := range ps.Containers {
-		total = resource.MergeResourceListKeepSum(total, ps.Containers[i].Resources.Requests)
+func calculateMainContainersResources(containers []corev1.Container) corev1.ResourceList {
+	total := corev1.ResourceList{}
+	for i := range containers {
+		total = resource.MergeResourceListKeepSum(total, containers[i].Resources.Requests)
 	}
+	return total
+}
+
+func calculateInitContainersResources(initContainers []corev1.Container) corev1.ResourceList {
+	total := corev1.ResourceList{}
+	for i := range initContainers {
+		sumSidecarContainers := calculateSidecarContainersResources(initContainers[:i])
```

  This is a $O(n^2)$ algorithm.
  
  Can you make it linear?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-05-07T13:12:31Z

- **pkg/util/limitrange/limitrange.go:133** by [@gabesaba](https://github.com/gabesaba) — 2024-05-07T13:10:26Z
```diff
@@ -105,16 +110,27 @@ func calculateMainContainersResources(containers []corev1.Container) corev1.Reso
 }
 
 func calculateInitContainersResources(initContainers []corev1.Container) corev1.ResourceList {
+	incrementalSum := calculateSidecarContainersIncrementalSum(initContainers)
 	total := corev1.ResourceList{}
 	for i := range initContainers {
-		sumSidecarContainers := calculateSidecarContainersResources(initContainers[:i])
-		initContainerUse := resource.MergeResourceListKeepSum(sumSidecarContainers, initContainers[i].Resources.Requests)
-
+		initContainerUse := resource.MergeResourceListKeepSum(incrementalSum[i], initContainers[i].Resources.Requests)
 		total = resource.MergeResourceListKeepMax(total, initContainerUse)
 	}
 	return total
 }
 
+func calculateSidecarContainersIncrementalSum(initContainers []corev1.Container) []corev1.ResourceList {
+	results := make([]corev1.ResourceList, len(initContainers))
+	lastResult := corev1.ResourceList{}
+	for i := 1; i < len(initContainers); i++ {
+		if isSidecarContainer(initContainers[i-1]) {
+			lastResult = resource.MergeResourceListKeepSum(results[i-1], initContainers[i-1].Resources.Requests)
+		}
+		results[i] = lastResult
+	}
+	return results
+}
+
```

  I think this can be simplified a bit, also with O(1) memory complexity 
  ```
  def calculateInitContainerResources():
    sideCarRunningUsage = resourceUsage()
    maxInitContainerUsage = resourceUsage()
    for container in initContainers:
      if isSideCar(container):
        sideCarRunningUsage = mergeKeepSum(sideCarRunningUsage, usage(container))
      else:
        initContainerUse = usage(container) + sideCarRunningUsage
        maxInitContainerUsage = mergeKeepMax(maxInitContainerUsage, initContainerUse)
    return maxInitContainerUsage
  
  ```

- **pkg/util/limitrange/limitrange.go:1** by [@gabesaba](https://github.com/gabesaba) — 2024-05-07T13:12:12Z
  please add a unit test which would fail for formula1, but passes for formula2 (as it seems unit tests haven't changed?)

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-07T13:24:42Z

- **pkg/util/limitrange/limitrange.go:1** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-07T13:24:42Z
  Unit tests were updated to match the results with the new formula. That was done here 48490a688d791c02239baf7b82994d9f781a8b08  for test cases: `pod only with regular init and sidecar containers` and `adds overhead`.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2024-05-07T13:27:51Z

- **pkg/util/limitrange/limitrange.go:1** by [@gabesaba](https://github.com/gabesaba) — 2024-05-07T13:27:51Z
  ahh, sorry I missed this. I only looked at the most recent commit :)

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-07T13:52:00Z

- **pkg/util/limitrange/limitrange.go:133** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-07T13:52:00Z
```diff
@@ -105,16 +110,27 @@ func calculateMainContainersResources(containers []corev1.Container) corev1.Reso
 }
 
 func calculateInitContainersResources(initContainers []corev1.Container) corev1.ResourceList {
+	incrementalSum := calculateSidecarContainersIncrementalSum(initContainers)
 	total := corev1.ResourceList{}
 	for i := range initContainers {
-		sumSidecarContainers := calculateSidecarContainersResources(initContainers[:i])
-		initContainerUse := resource.MergeResourceListKeepSum(sumSidecarContainers, initContainers[i].Resources.Requests)
-
+		initContainerUse := resource.MergeResourceListKeepSum(incrementalSum[i], initContainers[i].Resources.Requests)
 		total = resource.MergeResourceListKeepMax(total, initContainerUse)
 	}
 	return total
 }
 
+func calculateSidecarContainersIncrementalSum(initContainers []corev1.Container) []corev1.ResourceList {
+	results := make([]corev1.ResourceList, len(initContainers))
+	lastResult := corev1.ResourceList{}
+	for i := 1; i < len(initContainers); i++ {
+		if isSidecarContainer(initContainers[i-1]) {
+			lastResult = resource.MergeResourceListKeepSum(results[i-1], initContainers[i-1].Resources.Requests)
+		}
+		results[i] = lastResult
+	}
+	return results
+}
+
```

  Done. Thanks :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-07T14:51:13Z

/lgtm
/approve
