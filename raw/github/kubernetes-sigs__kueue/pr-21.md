# PR #21: Add verify checks for go code

**Summary**: Add verify checks for go code

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/21

**Last updated**: 2022-02-18T21:38:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Branch**: `devel/verify` → `main`
- **Created**: 2022-02-18T14:27:35Z
- **Updated**: 2022-02-18T21:38:23Z
- **Merged**: 2022-02-18T21:38:23Z
- **Closed**: 2022-02-18T21:38:23Z
- **Labels**: `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 1   **+31 / -8**

## Description

Signed-off-by: Carlos Eduardo Arango Gutierrez <carangog@redhat.com>

#### What type of PR is this?

/kind cleanup


#### What this PR does / why we need it:
Ci-Verify is always welcomed :)

## Discussion

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T14:57:16Z

I'll open an issue for this 
```bash
pkg/capacity/capacity_test.go:37:19: Error return value is not checked (errcheck)                                      
        kueue.AddToScheme(scheme)                                                                                                                                                                                                             
                         ^                  
pkg/capacity/capacity_test.go:66:23: Error return value of `cache.AddCapacity` is not checked (errcheck)               
                                        cache.AddCapacity(context.Background(), &c)                                                                                                                                                           
                                                         ^                                                                                                                                                                                    
pkg/capacity/capacity_test.go:89:26: Error return value of `cache.UpdateCapacity` is not checked (errcheck)            
                                        cache.UpdateCapacity(&c)                                                       
                                                            ^                                                                                                                                                                                 
pkg/capacity/capacity_test.go:203:19: Error return value is not checked (errcheck)
        kueue.AddToScheme(scheme)                                                                                                                                                                                                             
                         ^                                                                                                                                                                                                                    
pkg/capacity/snapshot_test.go:38:19: Error return value is not checked (errcheck)                                                                                                                                                             
        kueue.AddToScheme(scheme)                                                                                                                                                                                                             
                         ^                                                                                                                                                                                                                    
pkg/capacity/snapshot_test.go:122:20: Error return value of `cache.AddCapacity` is not checked (errcheck)              
                cache.AddCapacity(context.Background(), &cap)                                                          
                                 ^                                                                                     
pkg/queue/manager.go:61:21: Error return value of `qImpl.setProperties` is not checked (errcheck)
        qImpl.setProperties(q)                   
                           ^                                                                                           
pkg/queue/manager_test.go:394:19: Error return value of `manager.AddQueue` is not checked (errcheck)                   
                manager.AddQueue(ctx, &q)                                                                              
                                ^                          
pkg/queue/manager_test.go:452:20: Error return value of `manager.AddQueue` is not checked (errcheck)                   
                        manager.AddQueue(ctx, &q)                                                                      
                                        ^                                                                                                                                                                                                     
pkg/queue/manager_test.go:462:19: Error return value of `manager.AddQueue` is not checked (errcheck)
                manager.AddQueue(ctx, &q)                                                                              
                                ^                   
pkg/scheduler/scheduler.go:200:32: Error return value of `s.capacityCache.AssumeWorkload` is not checked (errcheck)
        s.capacityCache.AssumeWorkload(newWorkload)                                                                                                                                                                                           
                                      ^                                                                                
pkg/capacity/capacity.go:120:2: S1023: redundant `return` statement (gosimple)                                         
        return                                                                                                         
        ^                                                                                                                                                                                                                                     
pkg/capacity/snapshot_test.go:292:4: SA9003: empty branch (staticcheck)
                        if m == nil {                                                                                                                                                                                                         
                        ^
make: *** [Makefile:73: ci-lint] Error 1
```

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T21:20:04Z

not wanted binaries removed, maybe add them to .gitignore?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T21:33:08Z

The Makefile rule should add it to `./bin`, which is already ignored.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T21:33:26Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T21:36:15Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-02-18T21:36:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/21#issuecomment-1045215234" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/21#" title="Author self-approved">ArangoGutierrez</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-18T15:03:52Z

- **Makefile:65** by [@denkensk](https://github.com/denkensk) — 2022-02-18T15:03:48Z
```diff
@@ -59,15 +62,28 @@ vet: ## Run go vet against code.
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+gofmt-verify:
```

  maybe better put in hack/verify-gofmt.sh like k/k

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T15:05:26Z

- **Makefile:65** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T15:05:26Z
```diff
@@ -59,15 +62,28 @@ vet: ## Run go vet against code.
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+gofmt-verify:
```

  I hate that `hack` dir created by the operator-framework, I much ratter prefer the scripts path.
  but I am open to suggestions

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-18T15:10:57Z

- **Makefile:14** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T14:55:12Z
```diff
@@ -11,6 +11,9 @@ else
 GOBIN=$(shell go env GOBIN)
 endif
 
+GO_CMD ?= go
```

  why parameterize this?
  
  The rest of the Makefile is using `go` directly. If you think this has a use case, change every usage of `go` with `$(GO_CMD)`

- **Makefile:15** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T14:55:39Z
```diff
@@ -11,6 +11,9 @@ else
 GOBIN=$(shell go env GOBIN)
 endif
 
+GO_CMD ?= go
+GO_FMT ?= gofmt
```

  why not `go fmt`?

- **Makefile:65** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T14:56:06Z
```diff
@@ -59,15 +62,28 @@ vet: ## Run go vet against code.
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+gofmt-verify:
```

  nit: `fmt-verify`
  
  and put it next to the `fmt` directive.

- **Makefile:66** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T15:02:30Z
```diff
@@ -59,15 +62,28 @@ vet: ## Run go vet against code.
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+gofmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
```

  can we just use `./...` instead of using find?

- **Makefile:75** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T15:05:07Z
```diff
@@ -59,15 +62,28 @@ vet: ## Run go vet against code.
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+gofmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
+	if [ -n "$$out" ]; then \
+	    echo "$$out"; \
+	    exit 1; \
+	fi
+
+ci-lint:
+	golangci-lint run --timeout 7m0s
+
+lint:
```

  k8s has given up on golint https://github.com/kubernetes/kubernetes/pull/98063

- **scripts/test-infra/verify.sh:9** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T15:06:13Z
```diff
@@ -0,0 +1,16 @@
+#!/bin/bash -e
+
+# Install deps
+gobinpath="$(go env GOPATH)/bin"
+curl -sfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh| sh -s -- -b "$gobinpath" v1.42.1
+export PATH=$PATH:$(go env GOPATH)/bin
+
+# Run verify steps
+make gofmt-verify
```

  I thought this didn't produce changes, just fail.
  
  Should we add `set -e` at the top?

- **Makefile:83** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T15:08:04Z
```diff
@@ -59,15 +62,28 @@ vet: ## Run go vet against code.
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+gofmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
+	if [ -n "$$out" ]; then \
+	    echo "$$out"; \
+	    exit 1; \
+	fi
+
+ci-lint:
```

  Add a make rule to install golangci-lint, like there is one for `kustomize` and other binaries.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-18T15:12:15Z

- **Makefile:65** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T15:12:15Z
```diff
@@ -59,15 +62,28 @@ vet: ## Run go vet against code.
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+gofmt-verify:
```

  I kind of like the word scripts better, but hack is the k8s standard 🤷

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-18T15:15:48Z

- **Makefile:65** by [@denkensk](https://github.com/denkensk) — 2022-02-18T15:15:48Z
```diff
@@ -59,15 +62,28 @@ vet: ## Run go vet against code.
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+gofmt-verify:
```

  slight suggestion. Happy is the most important 😄

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T15:25:34Z

- **Makefile:65** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T15:25:34Z
```diff
@@ -59,15 +62,28 @@ vet: ## Run go vet against code.
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+gofmt-verify:
```

  ok, by popular demand, will move to ugly hack folder

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T15:27:49Z

- **Makefile:14** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T15:27:49Z
```diff
@@ -11,6 +11,9 @@ else
 GOBIN=$(shell go env GOBIN)
 endif
 
+GO_CMD ?= go
```

  some people, me included, something have defined this env var in our hosts to customize the go version we want to use, or diff test scenarios, so is better to have it as a variable on the Makefile

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T15:28:58Z

- **Makefile:15** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T15:28:58Z
```diff
@@ -11,6 +11,9 @@ else
 GOBIN=$(shell go env GOBIN)
 endif
 
+GO_CMD ?= go
+GO_FMT ?= gofmt
```

  ```bash
  Fmt runs the command 'gofmt -l -w' on the packages named
  by the import paths. It prints the names of the files that are modified.
  ```
  `go fmt` runs `gofmt` on the background

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T15:31:31Z

- **scripts/test-infra/verify.sh:9** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T15:31:30Z
```diff
@@ -0,0 +1,16 @@
+#!/bin/bash -e
+
+# Install deps
+gobinpath="$(go env GOPATH)/bin"
+curl -sfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh| sh -s -- -b "$gobinpath" v1.42.1
+export PATH=$PATH:$(go env GOPATH)/bin
+
+# Run verify steps
+make gofmt-verify
```

  `#!/bin/bash -e` is at the header

### Review by [@jiwq](https://github.com/jiwq) (commented) — 2022-02-18T15:47:29Z

- **hack/verify-gofmt.sh:1** by [@jiwq](https://github.com/jiwq) — 2022-02-18T15:47:28Z
```diff
@@ -0,0 +1,16 @@
+#!/bin/bash -e
```

  Why not use `#!/usr/bin/env bash`?
  
  ```suggestion
  #!/usr/bin/env bash
  
  set -e
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-18T17:00:28Z

- **Makefile:59** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T16:51:29Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
```

  Any reason not to do `./...`?

- **Makefile:55** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T16:52:21Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
```

  `GO_FMT` instead? Just trying to be uniform.

- **Makefile:83** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T16:54:47Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
+	if [ -n "$$out" ]; then \
+	    echo "$$out"; \
+	    exit 1; \
+	fi
 
 .PHONY: vet
 vet: ## Run go vet against code.
-	go vet ./...
+	$(GO_CMD) vet ./...
 
 .PHONY: test
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+GOLANGCI_LINT = $(shell pwd)/bin/golangci-lint
+.PHONY: golangci-lint
+golangci-lint: ## Download golangci-lint locally if necessary.
+	$(call go-get-tool,$(GOLANGCI_LINT),github.com/golangci/golangci-lint/cmd/golangci-lint@v1.44.2)
+
+ci-lint:
```

  the rule should depend on golangci-lint.
  
  Also add `.PHONY`

- **Makefile:58** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T16:54:54Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+fmt-verify:
```

  Add `.PHONY`

- **hack/verify-gofmt.sh:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T16:55:40Z
```diff
@@ -0,0 +1,18 @@
+#!/usr/bin/env bash
```

  The filename should just be `verify.sh`, as it's checking several things :)

- **hack/verify-gofmt.sh:6** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T16:56:26Z
```diff
@@ -0,0 +1,18 @@
+#!/usr/bin/env bash
+
+set -e
+
+# Install deps
+gobinpath="$(go env GOPATH)/bin"
```

  Make should handle the download, no?

- **hack/verify-gofmt.sh:14** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T16:56:45Z
```diff
@@ -0,0 +1,18 @@
+#!/usr/bin/env bash
+
+set -e
+
+# Install deps
+gobinpath="$(go env GOPATH)/bin"
+curl -sfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh| sh -s -- -b "$gobinpath" v1.42.1
+export PATH=$PATH:$(go env GOPATH)/bin
+
+# Run verify steps
+make fmt-verify
+make ci-lint
+
+# Check that repo is clean
```

  Is this "just in case"? The commands above don't modify the code, right?

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T18:00:01Z

- **hack/verify-gofmt.sh:14** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T18:00:01Z
```diff
@@ -0,0 +1,18 @@
+#!/usr/bin/env bash
+
+set -e
+
+# Install deps
+gobinpath="$(go env GOPATH)/bin"
+curl -sfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh| sh -s -- -b "$gobinpath" v1.42.1
+export PATH=$PATH:$(go env GOPATH)/bin
+
+# Run verify steps
+make fmt-verify
+make ci-lint
+
+# Check that repo is clean
```

  yes

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T18:00:12Z

- **hack/verify-gofmt.sh:6** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T18:00:12Z
```diff
@@ -0,0 +1,18 @@
+#!/usr/bin/env bash
+
+set -e
+
+# Install deps
+gobinpath="$(go env GOPATH)/bin"
```

  oh now it does, yes

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T18:00:55Z

- **hack/verify-gofmt.sh:14** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T18:00:55Z
```diff
@@ -0,0 +1,18 @@
+#!/usr/bin/env bash
+
+set -e
+
+# Install deps
+gobinpath="$(go env GOPATH)/bin"
+curl -sfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh| sh -s -- -b "$gobinpath" v1.42.1
+export PATH=$PATH:$(go env GOPATH)/bin
+
+# Run verify steps
+make fmt-verify
+make ci-lint
+
+# Check that repo is clean
```

  if gofmt actually modified something, it means the verify failed, so we can have verify as a test

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T18:01:07Z

- **hack/verify-gofmt.sh:1** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T18:01:07Z
```diff
@@ -0,0 +1,18 @@
+#!/usr/bin/env bash
```

  got it

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T18:05:31Z

- **Makefile:59** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T18:05:31Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
```

  it fails with `./...`

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T18:06:27Z

- **Makefile:59** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T18:06:27Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
```

  ```bash
  stat ./...: no such file or directory                    
  make: *** [Makefile:58: fmt-verify] Error 2   
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-18T18:31:37Z

- **Makefile:83** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T18:31:37Z
```diff
@@ -49,25 +52,42 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_FMT) ./...
+
+.PHONY: fmt-verify
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
+	if [ -n "$$out" ]; then \
+	    echo "$$out"; \
+	    exit 1; \
+	fi
 
 .PHONY: vet
 vet: ## Run go vet against code.
-	go vet ./...
+	$(GO_CMD) vet ./...
 
 .PHONY: test
 test: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./... -coverprofile cover.out
 
+GOLANGCI_LINT = $(shell pwd)/bin/golangci-lint
+.PHONY: golangci-lint
+golangci-lint: ## Download golangci-lint locally if necessary.
+	$(call go-get-tool,$(GOLANGCI_LINT),github.com/golangci/golangci-lint/cmd/golangci-lint@v1.44.2)
+
+.PHONY: ci-lint
+ci-lint:
```

  ```suggestion
  ci-lint: golangci-lint
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-18T18:40:07Z

- **Makefile:55** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T18:37:18Z
```diff
@@ -49,25 +52,42 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_FMT) ./...
```

  Now this doesn't work :)
  
  It looks like `go fmt` is not simply an alias for `gofmt -l -w`. It internally lists all the files in the package. I guess leave it as `$(GO_CMD) fmt`

- **Makefile:59** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T18:40:05Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
```

  Does `-d` only print the diff or actually modify the files? I think it only prints the diff, so we don't need to check the status of the repo in verify.sh

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T20:31:35Z

- **Makefile:59** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T20:31:35Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
```

  ```
    -d    display diffs instead of rewriting files
  ```
  It will print the diff, but the exit code will still be 0.
  
  the small check on verify.sh is to get an exit code, so we can use the script during prow testing

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T20:32:24Z

- **Makefile:55** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T20:32:24Z
```diff
@@ -49,25 +52,42 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_FMT) ./...
```

  back to GO_CMD

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-18T20:36:50Z

- **Makefile:59** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T20:36:50Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
```

  Yeah, I get that. What I'm saying is that we don't need to run `git diff` at the end, because `make fmt-verify` would make the script fail already.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-18T20:37:34Z

- **Makefile:69** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T20:37:33Z
```diff
@@ -49,25 +52,42 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+.PHONY: fmt-verify
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d ./...`; \
+	if [ -n "$$out" ]; then \
+	    echo "$$out"; \
+	    exit 1; \
+	fi
 
 .PHONY: vet
 vet: ## Run go vet against code.
-	go vet ./...
+	$(GO_CMD) vet ./...
 
 .PHONY: test
```

  can you rebase? I just changed something here 😅

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T20:42:44Z

- **Makefile:69** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T20:42:44Z
```diff
@@ -49,25 +52,42 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+.PHONY: fmt-verify
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d ./...`; \
+	if [ -n "$$out" ]; then \
+	    echo "$$out"; \
+	    exit 1; \
+	fi
 
 .PHONY: vet
 vet: ## Run go vet against code.
-	go vet ./...
+	$(GO_CMD) vet ./...
 
 .PHONY: test
```

  yup, 1 sec

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T20:46:37Z

- **Makefile:69** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T20:46:37Z
```diff
@@ -49,25 +52,42 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+.PHONY: fmt-verify
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d ./...`; \
+	if [ -n "$$out" ]; then \
+	    echo "$$out"; \
+	    exit 1; \
+	fi
 
 .PHONY: vet
 vet: ## Run go vet against code.
-	go vet ./...
+	$(GO_CMD) vet ./...
 
 .PHONY: test
```

  rebased

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-18T20:52:50Z

- **Makefile:75** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T20:52:49Z
```diff
@@ -49,29 +52,46 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+.PHONY: fmt-verify
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d ./...`; \
+	if [ -n "$$out" ]; then \
+	    echo "$$out"; \
+	    exit 1; \
+	fi
 
 .PHONY: vet
 vet: ## Run go vet against code.
-	go vet ./...
+	$(GO_CMD) vet ./...
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	go test ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test ./pkg/... -coverprofile cover.out
 
 .PHONY: test-integration
 test-integration: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./test/integration/...
```

  GO_CMD here too :)

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-18T20:54:45Z

- **Makefile:75** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-18T20:54:45Z
```diff
@@ -49,29 +52,46 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+.PHONY: fmt-verify
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d ./...`; \
+	if [ -n "$$out" ]; then \
+	    echo "$$out"; \
+	    exit 1; \
+	fi
 
 .PHONY: vet
 vet: ## Run go vet against code.
-	go vet ./...
+	$(GO_CMD) vet ./...
 
 .PHONY: test
 test: generate fmt vet ## Run tests.
-	go test ./pkg/... -coverprofile cover.out
+	$(GO_CMD) test ./pkg/... -coverprofile cover.out
 
 .PHONY: test-integration
 test-integration: manifests generate fmt vet envtest ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" go test ./test/integration/...
```

  oh damm didn't see that `go`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-18T20:55:02Z

- **Makefile:59** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-18T20:55:02Z
```diff
@@ -49,25 +52,40 @@ generate: controller-gen ## Generate code containing DeepCopy, DeepCopyInto, and
 
 .PHONY: fmt
 fmt: ## Run go fmt against code.
-	go fmt ./...
+	$(GO_CMD) fmt ./...
+
+fmt-verify:
+	@out=`$(GO_FMT) -w -l -d $$(find . -name '*.go')`; \
```

  this?
