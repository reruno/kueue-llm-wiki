# PR #2160: Propagate the reason for Eviction into the pod TerminationTarget condition

**Summary**: Propagate the reason for Eviction into the pod TerminationTarget condition

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2160

**Last updated**: 2024-05-30T14:04:41Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `reason_propagate` → `main`
- **Created**: 2024-05-08T12:01:07Z
- **Updated**: 2024-05-30T14:04:41Z
- **Merged**: 2024-05-13T15:31:18Z
- **Closed**: 2024-05-13T15:31:18Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 5   **+91 / -12**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:
Whenever kueue is stopping a pod, it adds a condition TerminationTarget. However, the reason for termination is always "StoppedByKueue". The goal of this PR is to set a specific reason depending on why the pod is stopped (like workload is evicted or workload is deleted).

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Pod Integration: The reason for stopping a pod is now specified in the pod `TerminationTarget` condition
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-08T12:01:16Z

Hi @pajakd. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-08T12:02:08Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | acc49144855dce0e0652aa310a49c8422b0f5c63 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6641e14759424500086bd947 |

### Comment by [@trasc](https://github.com/trasc) — 2024-05-08T12:18:19Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-13T15:12:51Z

LGTM label has been added.  <details>Git tree hash: d68c24335a60cf1b5fbb031ad04c1635bb7a6b32</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-13T15:12:52Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2160#pullrequestreview-2053000160" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2160#" title="Author self-approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T14:04:39Z

/release-note-edit

```release-note
Pod Integration: The reason for stopping a pod is now specified in the pod `TerminationTarget` condition
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-08T15:20:25Z

- **pkg/controller/jobframework/interface.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-08T15:20:25Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  Uhm... this one might not be enough. We also need to know if it was WaitForPodsReady or Preemption

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-05-09T10:41:21Z

- **pkg/controller/jobframework/interface.go:70** by [@pajakd](https://github.com/pajakd) — 2024-05-09T10:41:21Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  Ok, a second try. Please take a look. I don't know if I should add to this enum all possible Eviction reasons or I can just directly cast and propagate the `evCond.Reason` ?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-09T17:16:18Z

- **pkg/controller/jobframework/interface.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-09T17:16:18Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  As discussed offline, let's see if we can use a hyphen to concatenate the reasons.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T17:47:40Z

- **pkg/controller/jobframework/interface.go:70** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T17:47:40Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  @alculquicondor Could we decide the delimiter based on this discussion https://github.com/kubernetes/enhancements/pull/4479/files#r1581226166 since the reason should be CamelCase?
  
  > In condition types, and everywhere else they appear in the API, Reason is intended to be a one-word, CamelCase representation of the category of cause of the current status
  
  https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#typical-status-properties
  
  That discussion will seem to happen at the next sig apps meeting.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-09T17:48:45Z

- **pkg/controller/jobframework/interface.go:70** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-09T17:48:45Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  > That discussion will seem to happen at the next sig apps meeting.
  
  Oh, there is not the discussion in the agenda...

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-09T20:44:48Z

- **pkg/controller/jobframework/interface.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-09T20:44:48Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  I'm just trying to make the reason make sense.
  
  `WorkloadEvictedPreempted` doesn't read that well.
  
  So, the alternatives would be:
  1. `WorkloadPreempted` (drop the `Evicted` part)
  2. `Preempted` (just use the reason directly, and forget about the Workload bit)
  3. `WorkloadEvictedBecausePreempted` (same proposal as the hyphen, but with a word)
  
  Maybe 1 reads best of all? WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-10T06:00:20Z

- **pkg/controller/jobframework/interface.go:70** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-10T06:00:20Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  I agree with you since we have already the `Evicted` in the `condition.type`, right?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-10T09:04:58Z

- **pkg/controller/jobframework/reconciler.go:823** by [@trasc](https://github.com/trasc) — 2024-05-10T09:03:23Z
```diff
@@ -761,7 +761,13 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, wl *kueue.W
 	}
 
 	if jws, implements := job.(ComposableJob); implements {
-		stoppedNow, err := jws.Stop(ctx, r.client, info, stopReason, eventMsg)
+		reason := stopReason
```

  In my opinion, ignoring the received reason just for ComposableJob when Evicted condition is set is hard to follow, 
  the caller  of `stopJob` should provide the right reason for all the jobs.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-05-10T11:28:33Z

- **pkg/controller/jobframework/interface.go:70** by [@pajakd](https://github.com/pajakd) — 2024-05-10T11:28:33Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  So the possible reasons for eviction are: https://github.com/kubernetes-sigs/kueue/blob/b870144dc272788a755369411052bf5b81e50348/apis/kueue/v1beta1/workload_types.go#L332-L351
  
  And 1. reads definitely good for "Preempted" but for the other ones it would be a bit confusing. In my opinion concatenation with the dash would make the most sense. But if it is not recommended in the guidelines then we can decide on any of the alternatives. Would `WorkloadEvictedReason{Preempted,PodsReadyTimeout ...}` (a variant of 3) be too long?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-10T15:31:53Z

- **pkg/controller/jobframework/interface.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-10T15:31:53Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  I like it, even if it's too long

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-10T15:36:30Z

- **pkg/controller/jobframework/reconciler.go:823** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-10T15:36:30Z
```diff
@@ -761,7 +761,13 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, wl *kueue.W
 	}
 
 	if jws, implements := job.(ComposableJob); implements {
-		stoppedNow, err := jws.Stop(ctx, r.client, info, stopReason, eventMsg)
+		reason := stopReason
```

  I agree, but right now we don't have a condition in every job before suspending.
  
  We could work on that as a follow up. For now, the pain point is in the pod integration, given that they need to be replaced and it's not now immediately obvious when a pod was deleted because of an Eviction (because the Workload condition can change if it's immediately re-admitted).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-05-13T06:29:08Z

- **pkg/controller/jobframework/interface.go:70** by [@pajakd](https://github.com/pajakd) — 2024-05-13T06:29:07Z
```diff
@@ -62,13 +62,13 @@ type JobWithReclaimablePods interface {
 	ReclaimablePods() ([]kueue.ReclaimablePod, error)
 }
 
-type StopReason int
+type StopReason string
 
 const (
-	StopReasonWorkloadDeleted StopReason = iota
-	StopReasonWorkloadEvicted
-	StopReasonNoMatchingWorkload
-	StopReasonNotAdmitted
+	StopReasonWorkloadDeleted    StopReason = "WorkloadDeleted"
+	StopReasonWorkloadEvicted    StopReason = "WorkloadEvicted"
```

  Ok, changed the joining word to "Reason".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-13T08:36:32Z

- **pkg/controller/jobframework/reconciler.go:825** by [@mimowo](https://github.com/mimowo) — 2024-05-13T08:36:32Z
```diff
@@ -820,7 +820,13 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, wl *kueue.W
 	}
 
 	if jws, implements := job.(ComposableJob); implements {
-		stoppedNow, err := jws.Stop(ctx, r.client, info, stopReason, eventMsg)
+		reason := stopReason
+		if stopReason == StopReasonWorkloadEvicted {
+			if evCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
```

  You can use `apimeta.IsStatusConditionTrue`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-13T08:37:17Z

- **pkg/controller/jobframework/reconciler.go:826** by [@mimowo](https://github.com/mimowo) — 2024-05-13T08:37:17Z
```diff
@@ -820,7 +820,13 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, wl *kueue.W
 	}
 
 	if jws, implements := job.(ComposableJob); implements {
-		stoppedNow, err := jws.Stop(ctx, r.client, info, stopReason, eventMsg)
+		reason := stopReason
+		if stopReason == StopReasonWorkloadEvicted {
+			if evCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
+				reason = StopReason(string(StopReasonWorkloadEvicted) + "Reason" + evCond.Reason)
```

  ```suggestion
  				reason = StopReason(string(StopReasonWorkloadEvicted) + "DueTo" + evCond.Reason)
  ```
  Maybe it reads better?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-05-13T09:28:15Z

- **pkg/controller/jobframework/reconciler.go:826** by [@pajakd](https://github.com/pajakd) — 2024-05-13T09:28:15Z
```diff
@@ -820,7 +820,13 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, wl *kueue.W
 	}
 
 	if jws, implements := job.(ComposableJob); implements {
-		stoppedNow, err := jws.Stop(ctx, r.client, info, stopReason, eventMsg)
+		reason := stopReason
+		if stopReason == StopReasonWorkloadEvicted {
+			if evCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
+				reason = StopReason(string(StopReasonWorkloadEvicted) + "Reason" + evCond.Reason)
```

  Ok. It definitely reads better.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2024-05-13T09:31:21Z

- **pkg/controller/jobframework/reconciler.go:825** by [@pajakd](https://github.com/pajakd) — 2024-05-13T09:31:21Z
```diff
@@ -820,7 +820,13 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, wl *kueue.W
 	}
 
 	if jws, implements := job.(ComposableJob); implements {
-		stoppedNow, err := jws.Stop(ctx, r.client, info, stopReason, eventMsg)
+		reason := stopReason
+		if stopReason == StopReasonWorkloadEvicted {
+			if evCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
```

  Right, but I do need the entire `Condition` object (not only its presence/absence) because I have to read the `Reason` from it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-13T10:19:26Z

- **pkg/controller/jobframework/reconciler.go:825** by [@mimowo](https://github.com/mimowo) — 2024-05-13T10:19:26Z
```diff
@@ -820,7 +820,13 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, wl *kueue.W
 	}
 
 	if jws, implements := job.(ComposableJob); implements {
-		stoppedNow, err := jws.Stop(ctx, r.client, info, stopReason, eventMsg)
+		reason := stopReason
+		if stopReason == StopReasonWorkloadEvicted {
+			if evCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
```

  oh, yes

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-13T15:12:46Z

/lgtm
/approve
