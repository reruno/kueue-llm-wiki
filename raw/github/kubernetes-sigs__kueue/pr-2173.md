# PR #2173: Allow stoping admission from a specific LocalQueue.

**Summary**: Allow stoping admission from a specific LocalQueue.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2173

**Last updated**: 2024-06-10T15:09:21Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/allow-stoping-admission-from-a-specific-localqueue` → `main`
- **Created**: 2024-05-09T17:31:21Z
- **Updated**: 2024-06-10T15:09:21Z
- **Merged**: 2024-06-10T15:00:26Z
- **Closed**: 2024-06-10T15:00:27Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mwielgus](https://github.com/mwielgus), [@kerthcet](https://github.com/kerthcet), [@trasc](https://github.com/trasc)
- **Changed files**: 19   **+940 / -95**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
To allow stoping admission from a specific LocalQueue.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2109 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Allow stoping admission from a specific LocalQueue.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-09T17:31:24Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-09T17:31:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9125419389655cd22a963dfd5e21a1de3427f592 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6663588838aa7d00072a86b6 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-09T17:33:41Z

/assign

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-13T15:11:05Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-13T15:42:33Z

/assign @trasc

### Comment by [@trasc](https://github.com/trasc) — 2024-05-15T13:21:19Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-15T13:21:24Z

LGTM label has been added.  <details>Git tree hash: a53233261ab10920026cf68fe39460dbfe419ccd</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T13:23:36Z

/cc @mwielgus 
/cc @alculquicondor

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T19:23:12Z

/assign @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T20:01:07Z

/hold

Let's not merge this until we have released 0.7

Please work on other commands for now.

### Comment by [@trasc](https://github.com/trasc) — 2024-06-07T05:48:51Z

/unhold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-10T14:38:56Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-10T14:39:02Z

LGTM label has been added.  <details>Git tree hash: 30adec9e553f4e1736efc07ac759b1e0a6fb6a29</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-10T14:39:03Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2173#issuecomment-2158548149" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2173#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-14T09:12:45Z

- **apis/kueue/v1beta1/stop_policy.go:1** by [@trasc](https://github.com/trasc) — 2024-05-14T08:41:49Z
  Maybe move this into constants.go

- **pkg/controller/core/workload_controller.go:405** by [@trasc](https://github.com/trasc) — 2024-05-14T08:52:33Z
```diff
@@ -331,6 +364,46 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 	return false, nil
 }
 
+func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Context, wl *kueue.Workload, lq *kueue.LocalQueue) (bool, error) {
+	queueStopPolicy := ptr.Deref(lq.Spec.StopPolicy, kueue.None)
+
+	log := ctrl.LoggerFrom(ctx)
+
+	if workload.IsAdmitted(wl) {
+		if queueStopPolicy != kueue.HoldAndDrain {
+			return false, nil
+		}
+		if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+			log.V(3).Info("Workload is already evicted due to LocalQueue stopped.", "localQueue", klog.KRef("", wl.Spec.QueueName))
+			return false, nil
+		}
+		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, wl.Spec.QueueName))
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, cqExist := r.queues.ClusterQueueForWorkload(wl)
+			if cqExist {
+				metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByLocalQueueStopped)
+			}
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+
+	if lq.Name == "" || !lq.DeletionTimestamp.IsZero() {
+		log.V(3).Info("Workload is inadmissible because the LocalQueue is terminating or missing", "localQueue", klog.KRef("", wl.Spec.QueueName))
+		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("LocalQueue %s is terminating or missing", wl.Spec.QueueName))
+		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	}
```

  What happens if the workload is admitted and the lq is removed, shouldn't we evict?

- **pkg/controller/core/workload_controller.go:892** by [@trasc](https://github.com/trasc) — 2024-05-14T08:55:32Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(lq))
+		ctx = ctrl.LoggerInto(ctx, log)
+		w.queueReconcileForWorkloadsOfLocalQueue(ctx, lq, wq)
```

  We should have the same level of logging for cq and lq.

- **pkg/controller/jobframework/reconciler.go:421** by [@trasc](https://github.com/trasc) — 2024-05-14T09:00:58Z
```diff
@@ -417,13 +417,9 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if workload.HasQuotaReservation(wl) {
 			if !job.IsActive() {
 				log.V(6).Info("The job is no longer active, clear the workloads admission")
-				if evCond.Reason == kueue.WorkloadEvictedByDeactivation ||
-					evCond.Reason == kueue.WorkloadEvictedByPodsReadyTimeout ||
-					evCond.Reason == kueue.WorkloadEvictedByClusterQueueStopped {
-					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, false)
-				} else {
-					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
-				}
+				// The requeued condition status set to true only on EvictedByPreemption or EvictedByAdmissionCheck
+				status := evCond.Reason == kueue.WorkloadEvictedByPreemption || evCond.Reason == kueue.WorkloadEvictedByAdmissionCheck
```

  Rename `status` to something like `setRequeue` or anything more relevant.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T09:58:59Z

- **pkg/controller/core/workload_controller.go:892** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T09:58:59Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(lq))
+		ctx = ctrl.LoggerInto(ctx, log)
+		w.queueReconcileForWorkloadsOfLocalQueue(ctx, lq, wq)
```

  So just `log := ctrl.LoggerFrom(ctx)`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T10:06:54Z

- **pkg/controller/core/workload_controller.go:405** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T10:06:54Z
```diff
@@ -331,6 +364,46 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 	return false, nil
 }
 
+func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Context, wl *kueue.Workload, lq *kueue.LocalQueue) (bool, error) {
+	queueStopPolicy := ptr.Deref(lq.Spec.StopPolicy, kueue.None)
+
+	log := ctrl.LoggerFrom(ctx)
+
+	if workload.IsAdmitted(wl) {
+		if queueStopPolicy != kueue.HoldAndDrain {
+			return false, nil
+		}
+		if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+			log.V(3).Info("Workload is already evicted due to LocalQueue stopped.", "localQueue", klog.KRef("", wl.Spec.QueueName))
+			return false, nil
+		}
+		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, wl.Spec.QueueName))
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, cqExist := r.queues.ClusterQueueForWorkload(wl)
+			if cqExist {
+				metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByLocalQueueStopped)
+			}
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+
+	if lq.Name == "" || !lq.DeletionTimestamp.IsZero() {
+		log.V(3).Info("Workload is inadmissible because the LocalQueue is terminating or missing", "localQueue", klog.KRef("", wl.Spec.QueueName))
+		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("LocalQueue %s is terminating or missing", wl.Spec.QueueName))
+		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	}
```

  For clusterqueue we are evicting only for `kueue.HoldAndDrain` status. On local queue I made the same logic.
  
  @mimowo @alculquicondor could you help?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T10:14:38Z

- **pkg/controller/jobframework/reconciler.go:421** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T10:14:38Z
```diff
@@ -417,13 +417,9 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if workload.HasQuotaReservation(wl) {
 			if !job.IsActive() {
 				log.V(6).Info("The job is no longer active, clear the workloads admission")
-				if evCond.Reason == kueue.WorkloadEvictedByDeactivation ||
-					evCond.Reason == kueue.WorkloadEvictedByPodsReadyTimeout ||
-					evCond.Reason == kueue.WorkloadEvictedByClusterQueueStopped {
-					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, false)
-				} else {
-					workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, true)
-				}
+				// The requeued condition status set to true only on EvictedByPreemption or EvictedByAdmissionCheck
+				status := evCond.Reason == kueue.WorkloadEvictedByPreemption || evCond.Reason == kueue.WorkloadEvictedByAdmissionCheck
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T10:14:54Z

- **apis/kueue/v1beta1/stop_policy.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T10:14:53Z
  Done

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-14T13:32:22Z

- **pkg/controller/core/workload_controller.go:889** by [@trasc](https://github.com/trasc) — 2024-05-14T13:27:25Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
```

  `else if` or `return` after 886, the object cannot be both cq and lq

- **pkg/controller/core/workload_controller.go:892** by [@trasc](https://github.com/trasc) — 2024-05-14T13:29:38Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(lq))
+		ctx = ctrl.LoggerInto(ctx, log)
+		w.queueReconcileForWorkloadsOfLocalQueue(ctx, lq, wq)
```

  not necessarily, but if we set the localqueue for lq we should also set clusterqueue for cq.

- **pkg/controller/core/workload_controller.go:910** by [@trasc](https://github.com/trasc) — 2024-05-14T13:31:35Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(lq))
+		ctx = ctrl.LoggerInto(ctx, log)
+		w.queueReconcileForWorkloadsOfLocalQueue(ctx, lq, wq)
 	}
 }
 
 // Update is called in response to an update event.
-func (w *workloadCqHandler) Update(ctx context.Context, ev event.UpdateEvent, wq workqueue.RateLimitingInterface) {
-	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(ev.ObjectNew))
-	ctx = ctrl.LoggerInto(ctx, log)
-	log.V(5).Info("Workload cluster queue update event")
-	oldCq, oldIsQueue := ev.ObjectOld.(*kueue.ClusterQueue)
-	newCq, newIsQueue := ev.ObjectNew.(*kueue.ClusterQueue)
+func (w *workloadQueueHandler) Update(ctx context.Context, ev event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	oldCq, oldIsCq := ev.ObjectOld.(*kueue.ClusterQueue)
+	newCq, newIsCq := ev.ObjectNew.(*kueue.ClusterQueue)
+	if oldIsCq && newIsCq {
+		log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(ev.ObjectNew))
+		ctx = ctrl.LoggerInto(ctx, log)
+		log.V(5).Info("Workload cluster queue update event")
 
-	if !oldIsQueue || !newIsQueue {
-		return
+		if !newCq.DeletionTimestamp.IsZero() ||
+			!utilslices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks) ||
+			!gocmp.Equal(oldCq.Spec.AdmissionChecksStrategy, newCq.Spec.AdmissionChecksStrategy) ||
+			!ptr.Equal(oldCq.Spec.StopPolicy, newCq.Spec.StopPolicy) {
+			w.queueReconcileForWorkloadsOfClusterQueue(ctx, newCq.Name, wq)
+		}
```

  ```suggestion
  			w.queueReconcileForWorkloadsOfClusterQueue(ctx, newCq.Name, wq)
  		}
  		return
  ```
  ther is no point to check if it's a lq if it is a cq

- **pkg/controller/core/workload_controller.go:933** by [@trasc](https://github.com/trasc) — 2024-05-14T13:31:52Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(lq))
+		ctx = ctrl.LoggerInto(ctx, log)
+		w.queueReconcileForWorkloadsOfLocalQueue(ctx, lq, wq)
 	}
 }
 
 // Update is called in response to an update event.
-func (w *workloadCqHandler) Update(ctx context.Context, ev event.UpdateEvent, wq workqueue.RateLimitingInterface) {
-	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(ev.ObjectNew))
-	ctx = ctrl.LoggerInto(ctx, log)
-	log.V(5).Info("Workload cluster queue update event")
-	oldCq, oldIsQueue := ev.ObjectOld.(*kueue.ClusterQueue)
-	newCq, newIsQueue := ev.ObjectNew.(*kueue.ClusterQueue)
+func (w *workloadQueueHandler) Update(ctx context.Context, ev event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	oldCq, oldIsCq := ev.ObjectOld.(*kueue.ClusterQueue)
+	newCq, newIsCq := ev.ObjectNew.(*kueue.ClusterQueue)
+	if oldIsCq && newIsCq {
+		log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(ev.ObjectNew))
+		ctx = ctrl.LoggerInto(ctx, log)
+		log.V(5).Info("Workload cluster queue update event")
 
-	if !oldIsQueue || !newIsQueue {
-		return
+		if !newCq.DeletionTimestamp.IsZero() ||
+			!utilslices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks) ||
+			!gocmp.Equal(oldCq.Spec.AdmissionChecksStrategy, newCq.Spec.AdmissionChecksStrategy) ||
+			!ptr.Equal(oldCq.Spec.StopPolicy, newCq.Spec.StopPolicy) {
+			w.queueReconcileForWorkloadsOfClusterQueue(ctx, newCq.Name, wq)
+		}
 	}
 
-	if !newCq.DeletionTimestamp.IsZero() ||
-		!utilslices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks) ||
-		!gocmp.Equal(oldCq.Spec.AdmissionChecksStrategy, newCq.Spec.AdmissionChecksStrategy) ||
-		!ptr.Equal(oldCq.Spec.StopPolicy, newCq.Spec.StopPolicy) {
-		w.queueReconcileForWorkloads(ctx, newCq.Name, wq)
+	oldLq, oldIsLq := ev.ObjectOld.(*kueue.LocalQueue)
+	newLq, newIsLq := ev.ObjectNew.(*kueue.LocalQueue)
+	if oldIsLq && newIsLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(ev.ObjectNew))
+		ctx = ctrl.LoggerInto(ctx, log)
+		log.V(5).Info("Workload cluster queue update event")
+
+		if !newLq.DeletionTimestamp.IsZero() || !ptr.Equal(oldLq.Spec.StopPolicy, newLq.Spec.StopPolicy) {
+			w.queueReconcileForWorkloadsOfLocalQueue(ctx, newLq, wq)
+		}
 	}
 }
 
 // Delete is called in response to a delete event.
-func (w *workloadCqHandler) Delete(ctx context.Context, ev event.DeleteEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Delete(ctx context.Context, ev event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
```

  same

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T14:52:04Z

- **pkg/controller/core/workload_controller.go:933** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T14:52:04Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(lq))
+		ctx = ctrl.LoggerInto(ctx, log)
+		w.queueReconcileForWorkloadsOfLocalQueue(ctx, lq, wq)
 	}
 }
 
 // Update is called in response to an update event.
-func (w *workloadCqHandler) Update(ctx context.Context, ev event.UpdateEvent, wq workqueue.RateLimitingInterface) {
-	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(ev.ObjectNew))
-	ctx = ctrl.LoggerInto(ctx, log)
-	log.V(5).Info("Workload cluster queue update event")
-	oldCq, oldIsQueue := ev.ObjectOld.(*kueue.ClusterQueue)
-	newCq, newIsQueue := ev.ObjectNew.(*kueue.ClusterQueue)
+func (w *workloadQueueHandler) Update(ctx context.Context, ev event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	oldCq, oldIsCq := ev.ObjectOld.(*kueue.ClusterQueue)
+	newCq, newIsCq := ev.ObjectNew.(*kueue.ClusterQueue)
+	if oldIsCq && newIsCq {
+		log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(ev.ObjectNew))
+		ctx = ctrl.LoggerInto(ctx, log)
+		log.V(5).Info("Workload cluster queue update event")
 
-	if !oldIsQueue || !newIsQueue {
-		return
+		if !newCq.DeletionTimestamp.IsZero() ||
+			!utilslices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks) ||
+			!gocmp.Equal(oldCq.Spec.AdmissionChecksStrategy, newCq.Spec.AdmissionChecksStrategy) ||
+			!ptr.Equal(oldCq.Spec.StopPolicy, newCq.Spec.StopPolicy) {
+			w.queueReconcileForWorkloadsOfClusterQueue(ctx, newCq.Name, wq)
+		}
 	}
 
-	if !newCq.DeletionTimestamp.IsZero() ||
-		!utilslices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks) ||
-		!gocmp.Equal(oldCq.Spec.AdmissionChecksStrategy, newCq.Spec.AdmissionChecksStrategy) ||
-		!ptr.Equal(oldCq.Spec.StopPolicy, newCq.Spec.StopPolicy) {
-		w.queueReconcileForWorkloads(ctx, newCq.Name, wq)
+	oldLq, oldIsLq := ev.ObjectOld.(*kueue.LocalQueue)
+	newLq, newIsLq := ev.ObjectNew.(*kueue.LocalQueue)
+	if oldIsLq && newIsLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(ev.ObjectNew))
+		ctx = ctrl.LoggerInto(ctx, log)
+		log.V(5).Info("Workload cluster queue update event")
+
+		if !newLq.DeletionTimestamp.IsZero() || !ptr.Equal(oldLq.Spec.StopPolicy, newLq.Spec.StopPolicy) {
+			w.queueReconcileForWorkloadsOfLocalQueue(ctx, newLq, wq)
+		}
 	}
 }
 
 // Delete is called in response to a delete event.
-func (w *workloadCqHandler) Delete(ctx context.Context, ev event.DeleteEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Delete(ctx context.Context, ev event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T14:52:22Z

- **pkg/controller/core/workload_controller.go:910** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T14:52:22Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(lq))
+		ctx = ctrl.LoggerInto(ctx, log)
+		w.queueReconcileForWorkloadsOfLocalQueue(ctx, lq, wq)
 	}
 }
 
 // Update is called in response to an update event.
-func (w *workloadCqHandler) Update(ctx context.Context, ev event.UpdateEvent, wq workqueue.RateLimitingInterface) {
-	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(ev.ObjectNew))
-	ctx = ctrl.LoggerInto(ctx, log)
-	log.V(5).Info("Workload cluster queue update event")
-	oldCq, oldIsQueue := ev.ObjectOld.(*kueue.ClusterQueue)
-	newCq, newIsQueue := ev.ObjectNew.(*kueue.ClusterQueue)
+func (w *workloadQueueHandler) Update(ctx context.Context, ev event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	oldCq, oldIsCq := ev.ObjectOld.(*kueue.ClusterQueue)
+	newCq, newIsCq := ev.ObjectNew.(*kueue.ClusterQueue)
+	if oldIsCq && newIsCq {
+		log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(ev.ObjectNew))
+		ctx = ctrl.LoggerInto(ctx, log)
+		log.V(5).Info("Workload cluster queue update event")
 
-	if !oldIsQueue || !newIsQueue {
-		return
+		if !newCq.DeletionTimestamp.IsZero() ||
+			!utilslices.CmpNoOrder(oldCq.Spec.AdmissionChecks, newCq.Spec.AdmissionChecks) ||
+			!gocmp.Equal(oldCq.Spec.AdmissionChecksStrategy, newCq.Spec.AdmissionChecksStrategy) ||
+			!ptr.Equal(oldCq.Spec.StopPolicy, newCq.Spec.StopPolicy) {
+			w.queueReconcileForWorkloadsOfClusterQueue(ctx, newCq.Name, wq)
+		}
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T14:52:38Z

- **pkg/controller/core/workload_controller.go:889** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T14:52:37Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T14:52:45Z

- **pkg/controller/core/workload_controller.go:892** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T14:52:45Z
```diff
@@ -801,70 +874,90 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 	}
 }
 
-type workloadCqHandler struct {
-	client client.Client
+type workloadQueueHandler struct {
+	r *WorkloadReconciler
 }
 
-var _ handler.EventHandler = (*workloadCqHandler)(nil)
+var _ handler.EventHandler = (*workloadQueueHandler)(nil)
 
 // Create is called in response to a create event.
-func (w *workloadCqHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
-	if cq, isQueue := ev.Object.(*kueue.ClusterQueue); isQueue {
-		w.queueReconcileForWorkloads(ctx, cq.Name, wq)
+func (w *workloadQueueHandler) Create(ctx context.Context, ev event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	if cq, isCq := ev.Object.(*kueue.ClusterQueue); isCq {
+		w.queueReconcileForWorkloadsOfClusterQueue(ctx, cq.Name, wq)
+	}
+	if lq, isLq := ev.Object.(*kueue.LocalQueue); isLq {
+		log := ctrl.LoggerFrom(ctx).WithValues("localQueue", klog.KObj(lq))
+		ctx = ctrl.LoggerInto(ctx, log)
+		w.queueReconcileForWorkloadsOfLocalQueue(ctx, lq, wq)
```

  Done

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-14T18:06:15Z

- **apis/kueue/v1beta1/constants.go:21** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-14T18:06:15Z
```diff
@@ -18,6 +18,13 @@ package v1beta1
 
 const (
 	ResourceInUseFinalizerName = "kueue.x-k8s.io/resource-in-use"
+	DefaultPodSetName          = "main"
```

  NIT: I would split it into two consts (`DefaultPodSetName` and `DefaultReclaimablePodName`) and move to `pkg/controller/<jobs | pod>` WDYT?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-14T18:11:25Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-14T18:11:25Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  Shouldn't it be value of StopPolicy? eg. `StopPolicyInactive StopPolicy = "Inactive"`

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-14T18:11:37Z

- **apis/kueue/v1beta1/constants.go:30** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-14T18:11:37Z
```diff
@@ -18,6 +18,13 @@ package v1beta1
 
 const (
 	ResourceInUseFinalizerName = "kueue.x-k8s.io/resource-in-use"
+	DefaultPodSetName          = "main"
+)
+
+type StopPolicy string
 
-	DefaultPodSetName = "main"
+const (
+	None         StopPolicy = "None"
+	HoldAndDrain StopPolicy = "HoldAndDrain"
+	Hold         StopPolicy = "Hold"
 )
```

  NIT:
  ```suggestion
  const (
  	StopPolicyNone         StopPolicy = "None"
  	StopPolicyHoldAndDrain StopPolicy = "HoldAndDrain"
  	StopPolicyHold         StopPolicy = "Hold"
  )
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T18:14:46Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T18:14:45Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  What it will be for?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-14T18:16:49Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-14T18:16:49Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  It can help not breaking api when new stop policy appears, thus releasing it earlier
  eg. "NewPolicy" appeared, and in old version it would be equal to "Inactive", while in new version it would mean "NewPolicy"
  WDYT?

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-14T19:44:42Z

- **apis/kueue/v1beta1/constants.go:21** by [@trasc](https://github.com/trasc) — 2024-05-14T19:40:44Z
```diff
@@ -18,6 +18,13 @@ package v1beta1
 
 const (
 	ResourceInUseFinalizerName = "kueue.x-k8s.io/resource-in-use"
+	DefaultPodSetName          = "main"
```

  I think this comes from a bad diff display,  we are not touching `DefaultPodSetName` in this PR.

- **apis/kueue/v1beta1/constants.go:30** by [@trasc](https://github.com/trasc) — 2024-05-14T19:41:22Z
```diff
@@ -18,6 +18,13 @@ package v1beta1
 
 const (
 	ResourceInUseFinalizerName = "kueue.x-k8s.io/resource-in-use"
+	DefaultPodSetName          = "main"
+)
+
+type StopPolicy string
 
-	DefaultPodSetName = "main"
+const (
+	None         StopPolicy = "None"
+	HoldAndDrain StopPolicy = "HoldAndDrain"
+	Hold         StopPolicy = "Hold"
 )
```

  This could help.

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@trasc](https://github.com/trasc) — 2024-05-14T19:44:37Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  I don't understand?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-14T20:06:14Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-14T20:06:14Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  I think explicit setting of Inactive StopPolicy is better then treating empty StopPolicy as Inactive
  As stated above it could be useful when new policy appears: users of old versions could be notified that they are defaulting to Inactive stop policy, when they are trying to use new StopPolicy with version where that policy is not yet supported

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-14T20:08:24Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-14T20:08:24Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  One more possible pro: there could be validating admission web hook, that allow to stop only workloads which currently have None as SP, and start only those that are not fully drained(Inactive) or on hold(Hold)

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-14T20:09:59Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-14T20:09:59Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  ps. None is fine, I mean "anything other than existing policies" -> "Inactive"

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T20:36:34Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T20:36:34Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  > I think explicit setting of Inactive StopPolicy is better then treating empty StopPolicy as Inactive
  
  We are using `Hold` and `HoldAndDrain` as Inactive state of LQ and CQ. Just difference that on `HoldAndDrain` we are evicting Workload. Empty StopPolicy (nil) this is the same as `None` (Active).

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-14T21:06:00Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-14T21:06:00Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  Oh, then please update the comment, it confused me 💯 
  
  stopPolicy - if set to a value different from None `or any non-existing`, the LocalQueue is considered Inactive, no new reservation being made.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-14T21:23:03Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T21:23:03Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  I think everything correct. 
  
  https://github.com/kubernetes-sigs/kueue/blob/900d6b743484c92efa81a2c2b8c1fe0d36416ee3/apis/kueue/v1beta1/localqueue_types.go#L42

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-15T05:36:52Z

- **apis/kueue/v1beta1/localqueue_types.go:32** by [@trasc](https://github.com/trasc) — 2024-05-15T05:36:52Z
```diff
@@ -27,6 +27,20 @@ type LocalQueueSpec struct {
 	// clusterQueue is a reference to a clusterQueue that backs this localQueue.
 	// +kubebuilder:validation:XValidation:rule="self == oldSelf", message="field is immutable"
 	ClusterQueue ClusterQueueReference `json:"clusterQueue,omitempty"`
+
+	// stopPolicy - if set to a value different from None, the LocalQueue is considered Inactive,
+	// no new reservation being made.
```

  The entire approach was approved in an API review during the StopPolicy implementation for clusterQueues.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-15T07:15:03Z

- **apis/kueue/v1beta1/constants.go:30** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T07:15:03Z
```diff
@@ -18,6 +18,13 @@ package v1beta1
 
 const (
 	ResourceInUseFinalizerName = "kueue.x-k8s.io/resource-in-use"
+	DefaultPodSetName          = "main"
+)
+
+type StopPolicy string
 
-	DefaultPodSetName = "main"
+const (
+	None         StopPolicy = "None"
+	HoldAndDrain StopPolicy = "HoldAndDrain"
+	Hold         StopPolicy = "Hold"
 )
```

  I propose to make this optimisation in next follow ups to do not make changes for ClusterQueue.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-15T09:28:02Z

A couple more comments. otherwise LGTM overall

- **pkg/controller/core/localqueue_controller.go:108** by [@trasc](https://github.com/trasc) — 2024-05-15T08:54:19Z
```diff
@@ -99,19 +101,25 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
+	var err error
+	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
+		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, localQueueIsInactiveReason, localQueueIsInactiveMsg)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
```

  nit:
  ```suggestion
  	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
  		err := r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, localQueueIsInactiveReason, localQueueIsInactiveMsg)
  		return ctrl.Result{}, client.IgnoreNotFound(err)
  	}
  
  ```

- **pkg/controller/core/localqueue_controller.go:194** by [@trasc](https://github.com/trasc) — 2024-05-15T09:03:08Z
```diff
@@ -146,20 +157,45 @@ func (r *LocalQueueReconciler) Delete(e event.DeleteEvent) bool {
 }
 
 func (r *LocalQueueReconciler) Update(e event.UpdateEvent) bool {
-	q, match := e.ObjectNew.(*kueue.LocalQueue)
-	if !match {
+	oldLq, oldIsLq := e.ObjectOld.(*kueue.LocalQueue)
+	newLq, newIsLq := e.ObjectNew.(*kueue.LocalQueue)
+	if !oldIsLq || !newIsLq {
 		// No need to interact with the queue manager for other objects.
 		return true
 	}
-	log := r.log.WithValues("localQueue", klog.KObj(q))
+	log := r.log.WithValues("localQueue", klog.KObj(newLq))
 	log.V(2).Info("Queue update event")
-	if err := r.queues.UpdateLocalQueue(q); err != nil {
-		log.Error(err, "Failed to update queue in the queueing system")
+
+	oldStopPolicy := ptr.Deref(oldLq.Spec.StopPolicy, kueue.None)
+	newStopPolicy := ptr.Deref(newLq.Spec.StopPolicy, kueue.None)
+
+	if oldStopPolicy == kueue.None && newStopPolicy != kueue.None {
+		r.queues.DeleteLocalQueue(oldLq)
+		r.cache.DeleteLocalQueue(oldLq)
+		return true
+	}
+
+	if oldStopPolicy != kueue.None && newStopPolicy == kueue.None {
+		ctx := logr.NewContext(context.Background(), log)
+		if err := r.queues.AddLocalQueue(ctx, newLq); err != nil {
+			log.Error(err, "Failed to add localQueue to the queueing system")
+		}
+		if err := r.cache.AddLocalQueue(newLq); err != nil {
+			log.Error(err, "Failed to add localQueue to the cache")
+		}
+		return true
 	}
-	oldQ := e.ObjectOld.(*kueue.LocalQueue)
-	if err := r.cache.UpdateLocalQueue(oldQ, q); err != nil {
-		log.Error(err, "Failed to update localQueue in the cache")
+
+	if newStopPolicy == kueue.None {
+		if err := r.queues.UpdateLocalQueue(newLq); err != nil {
+			log.Error(err, "Failed to update queue in the queueing system")
+		}
+		oldQ := e.ObjectOld.(*kueue.LocalQueue)
+		if err := r.cache.UpdateLocalQueue(oldQ, newLq); err != nil {
```

  ```suggestion
  		if err := r.cache.UpdateLocalQueue(oldLq, newLq); err != nil {
  ```

- **pkg/controller/core/localqueue_controller.go:172** by [@trasc](https://github.com/trasc) — 2024-05-15T09:09:29Z
```diff
@@ -146,20 +157,45 @@ func (r *LocalQueueReconciler) Delete(e event.DeleteEvent) bool {
 }
 
 func (r *LocalQueueReconciler) Update(e event.UpdateEvent) bool {
-	q, match := e.ObjectNew.(*kueue.LocalQueue)
-	if !match {
+	oldLq, oldIsLq := e.ObjectOld.(*kueue.LocalQueue)
+	newLq, newIsLq := e.ObjectNew.(*kueue.LocalQueue)
+	if !oldIsLq || !newIsLq {
 		// No need to interact with the queue manager for other objects.
 		return true
 	}
-	log := r.log.WithValues("localQueue", klog.KObj(q))
+	log := r.log.WithValues("localQueue", klog.KObj(newLq))
 	log.V(2).Info("Queue update event")
-	if err := r.queues.UpdateLocalQueue(q); err != nil {
-		log.Error(err, "Failed to update queue in the queueing system")
+
+	oldStopPolicy := ptr.Deref(oldLq.Spec.StopPolicy, kueue.None)
+	newStopPolicy := ptr.Deref(newLq.Spec.StopPolicy, kueue.None)
+
```

  maybe something like 
  ```go
  if newStopPolicy==oldStopPolicy {
    if  newStopPolicy == None {
       // update in cache/queue
    }
    return true
  } 
  
  if  newStopPolicy == None {
       // add to cache/queue
  } else {
       // rm from cache/queue
  }
  
  ```
  
  is easier to follow

- **pkg/controller/core/workload_controller.go:405** by [@trasc](https://github.com/trasc) — 2024-05-15T09:23:23Z
```diff
@@ -331,6 +364,46 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 	return false, nil
 }
 
+func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Context, wl *kueue.Workload, lq *kueue.LocalQueue) (bool, error) {
+	queueStopPolicy := ptr.Deref(lq.Spec.StopPolicy, kueue.None)
+
+	log := ctrl.LoggerFrom(ctx)
+
+	if workload.IsAdmitted(wl) {
+		if queueStopPolicy != kueue.HoldAndDrain {
+			return false, nil
+		}
+		if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+			log.V(3).Info("Workload is already evicted due to LocalQueue stopped.", "localQueue", klog.KRef("", wl.Spec.QueueName))
+			return false, nil
+		}
+		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, wl.Spec.QueueName))
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, cqExist := r.queues.ClusterQueueForWorkload(wl)
+			if cqExist {
+				metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByLocalQueueStopped)
+			}
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+
+	if lq.Name == "" || !lq.DeletionTimestamp.IsZero() {
+		log.V(3).Info("Workload is inadmissible because the LocalQueue is terminating or missing", "localQueue", klog.KRef("", wl.Spec.QueueName))
+		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("LocalQueue %s is terminating or missing", wl.Spec.QueueName))
+		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	}
```

  ... that is problematic also, on the `isAdmitted`  branch, if the queue is not found, it used the stop policy extracted from a "unitialized"  object. Maybe we can change them both (separate cleanup pr for CQ).
  
  Regardless of this, we should not use `lq.Name == ""` as an indication of LQ not found.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-15T12:34:29Z

- **pkg/controller/core/localqueue_controller.go:172** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T12:34:29Z
```diff
@@ -146,20 +157,45 @@ func (r *LocalQueueReconciler) Delete(e event.DeleteEvent) bool {
 }
 
 func (r *LocalQueueReconciler) Update(e event.UpdateEvent) bool {
-	q, match := e.ObjectNew.(*kueue.LocalQueue)
-	if !match {
+	oldLq, oldIsLq := e.ObjectOld.(*kueue.LocalQueue)
+	newLq, newIsLq := e.ObjectNew.(*kueue.LocalQueue)
+	if !oldIsLq || !newIsLq {
 		// No need to interact with the queue manager for other objects.
 		return true
 	}
-	log := r.log.WithValues("localQueue", klog.KObj(q))
+	log := r.log.WithValues("localQueue", klog.KObj(newLq))
 	log.V(2).Info("Queue update event")
-	if err := r.queues.UpdateLocalQueue(q); err != nil {
-		log.Error(err, "Failed to update queue in the queueing system")
+
+	oldStopPolicy := ptr.Deref(oldLq.Spec.StopPolicy, kueue.None)
+	newStopPolicy := ptr.Deref(newLq.Spec.StopPolicy, kueue.None)
+
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-15T12:34:52Z

- **pkg/controller/core/localqueue_controller.go:194** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T12:34:52Z
```diff
@@ -146,20 +157,45 @@ func (r *LocalQueueReconciler) Delete(e event.DeleteEvent) bool {
 }
 
 func (r *LocalQueueReconciler) Update(e event.UpdateEvent) bool {
-	q, match := e.ObjectNew.(*kueue.LocalQueue)
-	if !match {
+	oldLq, oldIsLq := e.ObjectOld.(*kueue.LocalQueue)
+	newLq, newIsLq := e.ObjectNew.(*kueue.LocalQueue)
+	if !oldIsLq || !newIsLq {
 		// No need to interact with the queue manager for other objects.
 		return true
 	}
-	log := r.log.WithValues("localQueue", klog.KObj(q))
+	log := r.log.WithValues("localQueue", klog.KObj(newLq))
 	log.V(2).Info("Queue update event")
-	if err := r.queues.UpdateLocalQueue(q); err != nil {
-		log.Error(err, "Failed to update queue in the queueing system")
+
+	oldStopPolicy := ptr.Deref(oldLq.Spec.StopPolicy, kueue.None)
+	newStopPolicy := ptr.Deref(newLq.Spec.StopPolicy, kueue.None)
+
+	if oldStopPolicy == kueue.None && newStopPolicy != kueue.None {
+		r.queues.DeleteLocalQueue(oldLq)
+		r.cache.DeleteLocalQueue(oldLq)
+		return true
+	}
+
+	if oldStopPolicy != kueue.None && newStopPolicy == kueue.None {
+		ctx := logr.NewContext(context.Background(), log)
+		if err := r.queues.AddLocalQueue(ctx, newLq); err != nil {
+			log.Error(err, "Failed to add localQueue to the queueing system")
+		}
+		if err := r.cache.AddLocalQueue(newLq); err != nil {
+			log.Error(err, "Failed to add localQueue to the cache")
+		}
+		return true
 	}
-	oldQ := e.ObjectOld.(*kueue.LocalQueue)
-	if err := r.cache.UpdateLocalQueue(oldQ, q); err != nil {
-		log.Error(err, "Failed to update localQueue in the cache")
+
+	if newStopPolicy == kueue.None {
+		if err := r.queues.UpdateLocalQueue(newLq); err != nil {
+			log.Error(err, "Failed to update queue in the queueing system")
+		}
+		oldQ := e.ObjectOld.(*kueue.LocalQueue)
+		if err := r.cache.UpdateLocalQueue(oldQ, newLq); err != nil {
```

  Thanks. Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-15T12:35:33Z

- **pkg/controller/core/localqueue_controller.go:108** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T12:35:33Z
```diff
@@ -99,19 +101,25 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
+	var err error
+	if ptr.Deref(queueObj.Spec.StopPolicy, kueue.None) != kueue.None {
+		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, localQueueIsInactiveReason, localQueueIsInactiveMsg)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-15T12:36:10Z

- **pkg/controller/core/workload_controller.go:405** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T12:36:10Z
```diff
@@ -331,6 +364,46 @@ func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, w
 	return false, nil
 }
 
+func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Context, wl *kueue.Workload, lq *kueue.LocalQueue) (bool, error) {
+	queueStopPolicy := ptr.Deref(lq.Spec.StopPolicy, kueue.None)
+
+	log := ctrl.LoggerFrom(ctx)
+
+	if workload.IsAdmitted(wl) {
+		if queueStopPolicy != kueue.HoldAndDrain {
+			return false, nil
+		}
+		if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+			log.V(3).Info("Workload is already evicted due to LocalQueue stopped.", "localQueue", klog.KRef("", wl.Spec.QueueName))
+			return false, nil
+		}
+		log.V(3).Info("Workload is evicted because the LocalQueue is stopped", "localQueue", klog.KRef(wl.Namespace, wl.Spec.QueueName))
+		workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped")
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+		if err == nil {
+			cqName, cqExist := r.queues.ClusterQueueForWorkload(wl)
+			if cqExist {
+				metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByLocalQueueStopped)
+			}
+		}
+		return true, client.IgnoreNotFound(err)
+	}
+
+	if lq.Name == "" || !lq.DeletionTimestamp.IsZero() {
+		log.V(3).Info("Workload is inadmissible because the LocalQueue is terminating or missing", "localQueue", klog.KRef("", wl.Spec.QueueName))
+		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("LocalQueue %s is terminating or missing", wl.Spec.QueueName))
+		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true)
+	}
```

  > Regardless of this, we should not use lq.Name == "" as an indication of LQ not found.
  
  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-16T20:00:28Z

- **pkg/controller/core/localqueue_controller.go:195** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:58:58Z
```diff
@@ -146,20 +156,44 @@ func (r *LocalQueueReconciler) Delete(e event.DeleteEvent) bool {
 }
 
 func (r *LocalQueueReconciler) Update(e event.UpdateEvent) bool {
-	q, match := e.ObjectNew.(*kueue.LocalQueue)
-	if !match {
+	oldLq, oldIsLq := e.ObjectOld.(*kueue.LocalQueue)
+	newLq, newIsLq := e.ObjectNew.(*kueue.LocalQueue)
+	if !oldIsLq || !newIsLq {
 		// No need to interact with the queue manager for other objects.
 		return true
 	}
-	log := r.log.WithValues("localQueue", klog.KObj(q))
+	log := r.log.WithValues("localQueue", klog.KObj(newLq))
 	log.V(2).Info("Queue update event")
-	if err := r.queues.UpdateLocalQueue(q); err != nil {
-		log.Error(err, "Failed to update queue in the queueing system")
+
+	oldStopPolicy := ptr.Deref(oldLq.Spec.StopPolicy, kueue.None)
+	newStopPolicy := ptr.Deref(newLq.Spec.StopPolicy, kueue.None)
+
+	if newStopPolicy == oldStopPolicy {
+		if newStopPolicy == kueue.None {
+			if err := r.queues.UpdateLocalQueue(newLq); err != nil {
+				log.Error(err, "Failed to update queue in the queueing system")
+			}
+			if err := r.cache.UpdateLocalQueue(oldLq, newLq); err != nil {
+				log.Error(err, "Failed to update localQueue in the cache")
+			}
+		}
+		return true
 	}
-	oldQ := e.ObjectOld.(*kueue.LocalQueue)
-	if err := r.cache.UpdateLocalQueue(oldQ, q); err != nil {
-		log.Error(err, "Failed to update localQueue in the cache")
+
+	if newStopPolicy == kueue.None {
+		ctx := logr.NewContext(context.Background(), log)
+		if err := r.queues.AddLocalQueue(ctx, newLq); err != nil {
+			log.Error(err, "Failed to add localQueue to the queueing system")
+		}
+		if err := r.cache.AddLocalQueue(newLq); err != nil {
+			log.Error(err, "Failed to add localQueue to the cache")
+		}
+		return true
 	}
+
+	r.queues.DeleteLocalQueue(oldLq)
+	r.cache.DeleteLocalQueue(oldLq)
```

  Doesn't this immediately release the quota for all the workloads in this localqueue? Even if `stopPolicy == Hold`?

- **pkg/controller/core/localqueue_controller.go:331** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T20:00:26Z
```diff
@@ -275,21 +309,29 @@ func (r *LocalQueueReconciler) UpdateStatusIfChanged(
 	reason, msg string,
 ) error {
 	oldStatus := queue.Status.DeepCopy()
-	pendingWls, err := r.queues.PendingWorkloads(queue)
-	if err != nil {
-		r.log.Error(err, failedUpdateLqStatusMsg)
-		return err
-	}
-	stats, err := r.cache.LocalQueueUsage(queue)
-	if err != nil {
-		r.log.Error(err, failedUpdateLqStatusMsg)
-		return err
+	if ptr.Deref(queue.Spec.StopPolicy, kueue.None) == kueue.None {
+		pendingWls, err := r.queues.PendingWorkloads(queue)
+		if err != nil {
+			r.log.Error(err, failedUpdateLqStatusMsg)
+			return err
+		}
+		stats, err := r.cache.LocalQueueUsage(queue)
+		if err != nil {
+			r.log.Error(err, failedUpdateLqStatusMsg)
+			return err
+		}
+		queue.Status.PendingWorkloads = pendingWls
+		queue.Status.ReservingWorkloads = int32(stats.ReservingWorkloads)
+		queue.Status.AdmittedWorkloads = int32(stats.AdmittedWorkloads)
+		queue.Status.FlavorsReservation = stats.ReservedResources
+		queue.Status.FlavorUsage = stats.AdmittedResources
+	} else {
+		queue.Status.PendingWorkloads = 0
+		queue.Status.ReservingWorkloads = 0
+		queue.Status.AdmittedWorkloads = 0
```

  This is not accurate. If the policy is just Hold, we still need to track what is still running.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-28T16:22:48Z

- **pkg/controller/core/localqueue_controller.go:331** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-28T16:22:48Z
```diff
@@ -275,21 +309,29 @@ func (r *LocalQueueReconciler) UpdateStatusIfChanged(
 	reason, msg string,
 ) error {
 	oldStatus := queue.Status.DeepCopy()
-	pendingWls, err := r.queues.PendingWorkloads(queue)
-	if err != nil {
-		r.log.Error(err, failedUpdateLqStatusMsg)
-		return err
-	}
-	stats, err := r.cache.LocalQueueUsage(queue)
-	if err != nil {
-		r.log.Error(err, failedUpdateLqStatusMsg)
-		return err
+	if ptr.Deref(queue.Spec.StopPolicy, kueue.None) == kueue.None {
+		pendingWls, err := r.queues.PendingWorkloads(queue)
+		if err != nil {
+			r.log.Error(err, failedUpdateLqStatusMsg)
+			return err
+		}
+		stats, err := r.cache.LocalQueueUsage(queue)
+		if err != nil {
+			r.log.Error(err, failedUpdateLqStatusMsg)
+			return err
+		}
+		queue.Status.PendingWorkloads = pendingWls
+		queue.Status.ReservingWorkloads = int32(stats.ReservingWorkloads)
+		queue.Status.AdmittedWorkloads = int32(stats.AdmittedWorkloads)
+		queue.Status.FlavorsReservation = stats.ReservedResources
+		queue.Status.FlavorUsage = stats.AdmittedResources
+	} else {
+		queue.Status.PendingWorkloads = 0
+		queue.Status.ReservingWorkloads = 0
+		queue.Status.AdmittedWorkloads = 0
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-28T16:23:00Z

- **pkg/controller/core/localqueue_controller.go:195** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-28T16:23:00Z
```diff
@@ -146,20 +156,44 @@ func (r *LocalQueueReconciler) Delete(e event.DeleteEvent) bool {
 }
 
 func (r *LocalQueueReconciler) Update(e event.UpdateEvent) bool {
-	q, match := e.ObjectNew.(*kueue.LocalQueue)
-	if !match {
+	oldLq, oldIsLq := e.ObjectOld.(*kueue.LocalQueue)
+	newLq, newIsLq := e.ObjectNew.(*kueue.LocalQueue)
+	if !oldIsLq || !newIsLq {
 		// No need to interact with the queue manager for other objects.
 		return true
 	}
-	log := r.log.WithValues("localQueue", klog.KObj(q))
+	log := r.log.WithValues("localQueue", klog.KObj(newLq))
 	log.V(2).Info("Queue update event")
-	if err := r.queues.UpdateLocalQueue(q); err != nil {
-		log.Error(err, "Failed to update queue in the queueing system")
+
+	oldStopPolicy := ptr.Deref(oldLq.Spec.StopPolicy, kueue.None)
+	newStopPolicy := ptr.Deref(newLq.Spec.StopPolicy, kueue.None)
+
+	if newStopPolicy == oldStopPolicy {
+		if newStopPolicy == kueue.None {
+			if err := r.queues.UpdateLocalQueue(newLq); err != nil {
+				log.Error(err, "Failed to update queue in the queueing system")
+			}
+			if err := r.cache.UpdateLocalQueue(oldLq, newLq); err != nil {
+				log.Error(err, "Failed to update localQueue in the cache")
+			}
+		}
+		return true
 	}
-	oldQ := e.ObjectOld.(*kueue.LocalQueue)
-	if err := r.cache.UpdateLocalQueue(oldQ, q); err != nil {
-		log.Error(err, "Failed to update localQueue in the cache")
+
+	if newStopPolicy == kueue.None {
+		ctx := logr.NewContext(context.Background(), log)
+		if err := r.queues.AddLocalQueue(ctx, newLq); err != nil {
+			log.Error(err, "Failed to add localQueue to the queueing system")
+		}
+		if err := r.cache.AddLocalQueue(newLq); err != nil {
+			log.Error(err, "Failed to add localQueue to the cache")
+		}
+		return true
 	}
+
+	r.queues.DeleteLocalQueue(oldLq)
+	r.cache.DeleteLocalQueue(oldLq)
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-07T16:37:41Z

- **pkg/controller/core/localqueue_controller.go:53** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-07T16:21:20Z
```diff
@@ -44,11 +44,13 @@ import (
 )
 
 const (
-	queueIsInactiveMsg      = "Can't submit new workloads to clusterQueue"
-	failedUpdateLqStatusMsg = "Failed to retrieve localQueue status"
+	localQueueIsInactiveMsg   = "Can't submit new workloads to localQueue"
+	clusterQueueIsInactiveMsg = "Can't submit new workloads to clusterQueue"
+	failedUpdateLqStatusMsg   = "Failed to retrieve localQueue status"
 )
 
 const (
+	localQueueIsInactiveReason   = "LocalQueueIsInactive"
```

  ```suggestion
  	StoppedReason   = "Stopped"
  ```

- **pkg/controller/core/localqueue_controller.go:47** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-07T16:22:17Z
```diff
@@ -44,11 +44,13 @@ import (
 )
 
 const (
-	queueIsInactiveMsg      = "Can't submit new workloads to clusterQueue"
-	failedUpdateLqStatusMsg = "Failed to retrieve localQueue status"
+	localQueueIsInactiveMsg   = "Can't submit new workloads to localQueue"
```

  ```suggestion
  	localQueueIsInactiveMsg   = "LocalQueue is stopped"
  ```

- **pkg/controller/core/localqueue_controller.go:133** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-07T16:23:34Z
```diff
@@ -123,6 +130,9 @@ func (r *LocalQueueReconciler) Create(e event.CreateEvent) bool {
 	}
 	log := r.log.WithValues("localQueue", klog.KObj(q))
 	log.V(2).Info("LocalQueue create event")
+	if ptr.Deref(q.Spec.StopPolicy, kueue.None) != kueue.None {
```

  `Create` is also called when Kueue restarts. It is possible that this queue had running jobs before Kueue restarted. So this needs to be handled a bit more similar to `Update`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-07T18:24:23Z

- **pkg/controller/core/localqueue_controller.go:53** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-07T18:24:23Z
```diff
@@ -44,11 +44,13 @@ import (
 )
 
 const (
-	queueIsInactiveMsg      = "Can't submit new workloads to clusterQueue"
-	failedUpdateLqStatusMsg = "Failed to retrieve localQueue status"
+	localQueueIsInactiveMsg   = "Can't submit new workloads to localQueue"
+	clusterQueueIsInactiveMsg = "Can't submit new workloads to clusterQueue"
+	failedUpdateLqStatusMsg   = "Failed to retrieve localQueue status"
 )
 
 const (
+	localQueueIsInactiveReason   = "LocalQueueIsInactive"
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-07T18:24:33Z

- **pkg/controller/core/localqueue_controller.go:47** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-07T18:24:33Z
```diff
@@ -44,11 +44,13 @@ import (
 )
 
 const (
-	queueIsInactiveMsg      = "Can't submit new workloads to clusterQueue"
-	failedUpdateLqStatusMsg = "Failed to retrieve localQueue status"
+	localQueueIsInactiveMsg   = "Can't submit new workloads to localQueue"
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-06-07T19:37:09Z

- **pkg/controller/core/localqueue_controller.go:133** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-06-07T19:37:09Z
```diff
@@ -123,6 +130,9 @@ func (r *LocalQueueReconciler) Create(e event.CreateEvent) bool {
 	}
 	log := r.log.WithValues("localQueue", klog.KObj(q))
 	log.V(2).Info("LocalQueue create event")
+	if ptr.Deref(q.Spec.StopPolicy, kueue.None) != kueue.None {
```

  Done
