# PR #2185: Enhance troubleshooting for pending jobs

**Summary**: Enhance troubleshooting for pending jobs

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2185

**Last updated**: 2024-05-15T09:12:28Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `ts-jobs` → `main`
- **Created**: 2024-05-10T20:29:15Z
- **Updated**: 2024-05-15T09:12:28Z
- **Merged**: 2024-05-15T08:50:33Z
- **Closed**: 2024-05-15T08:50:33Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 1   **+112 / -41**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Trying to enhance the documentation making very few assumptions about the knowledge of a Kubernetes system.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-10T20:29:19Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2185#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-10T20:29:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 91dc08e8eeed718ccc491dbd46c63151d114c863 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/664395682b6b380008abb826 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2185--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2185--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTIxODUtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.VF9XqtPur2F13uE0YXMGu0ENQ1GKVILEZaG90Heeogk)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-15T08:49:01Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-15T08:49:08Z

LGTM label has been added.  <details>Git tree hash: 31fc5ddc00f35002ed7cc330d21852d6a3dd7076</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-15T09:11:50Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-05-15T09:12:27Z

@tenzen-y: #2185 failed to apply on top of branch "website":
```
Applying: Enhance troubleshooting for pending jobs
Using index info to reconstruct a base tree...
M	site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md
Falling back to patching base and 3-way merge...
Auto-merging site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md
Applying: Editorial review
Using index info to reconstruct a base tree...
M	site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md
Falling back to patching base and 3-way merge...
Auto-merging site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md
CONFLICT (content): Merge conflict in site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0002 Editorial review
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2185#issuecomment-2111980242):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2024-05-10T20:50:27Z

I liked these changes a lot. Thanks, Aldo!

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:14** by [@cortespao](https://github.com/cortespao) — 2024-05-10T20:37:22Z
```diff
@@ -6,17 +6,19 @@ description: >
   Troubleshooting the status of a Job
 ---
 
-This doc is about troubleshooting pending kubernetes Jobs, however, most of the ideas can be extrapolated
-to other supported CRDs.
+This doc is about troubleshooting pending [Kubernetes Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+however, most of the ideas can be extrapolated to other [supported job types](/docs/tasks/run).
+
+See the [Kueue overview](/docs/overview/#high-level-kueue-operation) to visualize the components that collaborate to run a Job.
+
+In the remaining of the document, let's assume your Job is called `my-job` and it's in the `my-namespace` namespace.
```

  ```suggestion
  In this document, assume that your Job is called `my-job` and it's in the `my-namespace` namespace.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:21** by [@cortespao](https://github.com/cortespao) — 2024-05-10T20:42:08Z
```diff
@@ -6,17 +6,19 @@ description: >
   Troubleshooting the status of a Job
 ---
 
-This doc is about troubleshooting pending kubernetes Jobs, however, most of the ideas can be extrapolated
-to other supported CRDs.
+This doc is about troubleshooting pending [Kubernetes Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+however, most of the ideas can be extrapolated to other [supported job types](/docs/tasks/run).
+
+See the [Kueue overview](/docs/overview/#high-level-kueue-operation) to visualize the components that collaborate to run a Job.
+
+In the remaining of the document, let's assume your Job is called `my-job` and it's in the `my-namespace` namespace.
 
 ## Identifying the Workload for your Job
 
-For each Job (a Kubernetes Job or a CRD), Kueue creates a [Workload](/docs/concepts/workload) object to hold the
-information about the admission of the Job. The Workload object allows Kueue to make admission decisions without
-knowing the specifics of each CRD.
+For each Job, Kueue creates a [Workload](/docs/concepts/workload) object to hold the
+details about the admission of the Job, wether it was admitted or not.
 
-There are multiple ways to find the Workload for a Job. In the following examples, let's assume your
-Job is called `my-job` in the `my-namespace` namespace.
+There are multiple ways to find the Workload for a Job.
```

  ```suggestion
  To find the Workload for a Job, you can use any of the following steps:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:23** by [@cortespao](https://github.com/cortespao) — 2024-05-10T20:42:30Z
```diff
@@ -6,17 +6,19 @@ description: >
   Troubleshooting the status of a Job
 ---
 
-This doc is about troubleshooting pending kubernetes Jobs, however, most of the ideas can be extrapolated
-to other supported CRDs.
+This doc is about troubleshooting pending [Kubernetes Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+however, most of the ideas can be extrapolated to other [supported job types](/docs/tasks/run).
+
+See the [Kueue overview](/docs/overview/#high-level-kueue-operation) to visualize the components that collaborate to run a Job.
+
+In the remaining of the document, let's assume your Job is called `my-job` and it's in the `my-namespace` namespace.
 
 ## Identifying the Workload for your Job
 
-For each Job (a Kubernetes Job or a CRD), Kueue creates a [Workload](/docs/concepts/workload) object to hold the
-information about the admission of the Job. The Workload object allows Kueue to make admission decisions without
-knowing the specifics of each CRD.
+For each Job, Kueue creates a [Workload](/docs/concepts/workload) object to hold the
+details about the admission of the Job, wether it was admitted or not.
 
-There are multiple ways to find the Workload for a Job. In the following examples, let's assume your
-Job is called `my-job` in the `my-namespace` namespace.
+There are multiple ways to find the Workload for a Job.
 
 1. You can obtain the Workload name from the Job events, running the following command:
```

  ```suggestion
  * You can obtain the Workload name from the Job events by running the following command:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:50** by [@cortespao](https://github.com/cortespao) — 2024-05-10T20:42:56Z
```diff
@@ -47,7 +49,7 @@ Job is called `my-job` in the `my-namespace` namespace.
 
 3. You can list all of the workloads in the same namespace of your job and identify the one
```

  ```suggestion
  * You can list all of the workloads in the same namespace of your job and identify the one
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:88** by [@cortespao](https://github.com/cortespao) — 2024-05-10T20:46:58Z
```diff
@@ -60,16 +62,29 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue    cluster-queue True       9m45s
    ```
 
-## Is my Job running?
+Once you have identified the name of the Workload, you can obtain all details by
+running the following comand:
+
+```bash
+kubectl describe workload -n my-namespace job-my-job-19797
+```
 
-To know whether your Job is running, look for the value of the `.spec.suspend` field, by
+## Is my Job suspended?
+
+To know whether your Job is suspended, look for the value of the `.spec.suspend` field, by
 running the following command:
 
 ```
 kubectl get job -n my-namespace my-job -o jsonpath='{.spec.suspend}'
 ```
 
-If your Job is running, the output will be `false`.
+When a job is suspended, Kubernetes will not create any [Pods](https://kubernetes.io/docs/concepts/workloads/pods/)
+for it or it will terminate and delete them if they were already running.
+
+A job is suspended when Kueue hasn't admitted it yet or when it was preempted to
+accommodate another job. To understand why your job is suspended,
+check the corresponding Workload object.
+
 
```

  ```suggestion
  When a Job is suspended, Kubernetes does not create any [Pods](https://kubernetes.io/docs/concepts/workloads/pods/)
  for the Job. If the Job has Pods already running, Kubernetes terminates and deletes these Pods.
  
  A Job is suspended when Kueue hasn't admitted it yet or when it was preempted to
  accommodate another Job. To understand why your job is suspended,
  check the corresponding Workload object.
  
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:271** by [@cortespao](https://github.com/cortespao) — 2024-05-10T20:48:11Z
```diff
@@ -250,3 +265,60 @@ status:
 
 The `Evicted` condition shows that the Workload was preempted and the `QuotaReserved` condition with `status: "True"`
 shows that Kueue already attempted to admit it again, unsuccessfully in this case.
+
+## Are the Pods of my Job running?
+
+When a Job is not suspended, Kubernetes creates Pods for it.
```

  ```suggestion
  When a Job is not suspended, Kubernetes creates Pods for this Job.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:272** by [@cortespao](https://github.com/cortespao) — 2024-05-10T20:48:28Z
```diff
@@ -250,3 +265,60 @@ status:
 
 The `Evicted` condition shows that the Workload was preempted and the `QuotaReserved` condition with `status: "True"`
 shows that Kueue already attempted to admit it again, unsuccessfully in this case.
+
+## Are the Pods of my Job running?
+
+When a Job is not suspended, Kubernetes creates Pods for it.
+To check how many of these Pods are scheduled and running, you can first check the Job status.
```

  ```suggestion
  To check how many of these Pods are scheduled and running, check the Job status.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:292** by [@cortespao](https://github.com/cortespao) — 2024-05-10T20:49:06Z
```diff
@@ -250,3 +265,60 @@ status:
 
 The `Evicted` condition shows that the Workload was preempted and the `QuotaReserved` condition with `status: "True"`
 shows that Kueue already attempted to admit it again, unsuccessfully in this case.
+
+## Are the Pods of my Job running?
+
+When a Job is not suspended, Kubernetes creates Pods for it.
+To check how many of these Pods are scheduled and running, you can first check the Job status.
+
+Run the following command to obtain the full status of a Job:
+
+```bash
+kubectl get job -n my-namespace my-job -o yaml
+```
+
+The output will be similar to the following:
+```yaml
+...
+status:
+  active: 5
+  ready: 4
+  succeeded: 1
+  ...
+```
+
+The `active` field shows how many Pods for the Job exist, and the `ready` field shows how many of them are running.
+
+To list all the pods of the Job, you can can run the following command:
```

  ```suggestion
  To list all the Pods of the Job, run the following command:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:311** by [@cortespao](https://github.com/cortespao) — 2024-05-10T20:50:01Z
```diff
@@ -250,3 +265,60 @@ status:
 
 The `Evicted` condition shows that the Workload was preempted and the `QuotaReserved` condition with `status: "True"`
 shows that Kueue already attempted to admit it again, unsuccessfully in this case.
+
+## Are the Pods of my Job running?
+
+When a Job is not suspended, Kubernetes creates Pods for it.
+To check how many of these Pods are scheduled and running, you can first check the Job status.
+
+Run the following command to obtain the full status of a Job:
+
+```bash
+kubectl get job -n my-namespace my-job -o yaml
+```
+
+The output will be similar to the following:
+```yaml
+...
+status:
+  active: 5
+  ready: 4
+  succeeded: 1
+  ...
+```
+
+The `active` field shows how many Pods for the Job exist, and the `ready` field shows how many of them are running.
+
+To list all the pods of the Job, you can can run the following command:
+
+```bash
+kubectl get pods -n my-namespace -l batch.kubernetes.io/job-name=my-job
+```
+
+The output will be similar to the following:
+
+```
+NAME             READY   STATUS    RESTARTS   AGE
+my-job-0-nxmpx   1/1     Running   0          3m20s
+my-job-1-vgms2   1/1     Running   0          3m20s
+my-job-2-74gw7   1/1     Running   0          3m20s
+my-job-3-d4559   1/1     Running   0          3m20s
+my-job-4-pg75n   0/1     Pending   0          3m20s
+```
+
+If a Pod or Pods are stuck in `Pending`, you can run the following command
+to check the latest events issued for the Pod
+
```

  ```suggestion
  If a Pod or Pods are stuck in `Pending` status, check the latest events issued for the Pod by running the following command:
  
  ```

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-13T10:22:36Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:74** by [@mwielgus](https://github.com/mwielgus) — 2024-05-13T10:22:36Z
```diff
@@ -60,16 +62,29 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue    cluster-queue True       9m45s
    ```
 
-## Is my Job running?
+Once you have identified the name of the Workload, you can obtain all details by
+running the following comand:
+
+```bash
+kubectl describe workload -n my-namespace job-my-job-19797
+```
 
-To know whether your Job is running, look for the value of the `.spec.suspend` field, by
+## Is my Job suspended?
+
+To know whether your Job is suspended, look for the value of the `.spec.suspend` field, by
```

  Maybe also include info how to tell that the job is suspended by looking at the corresponding workload object.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-13T12:54:33Z

LGTM

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-13T18:41:21Z

Thanks for the reviews!

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T18:28:46Z
```diff
@@ -6,17 +6,19 @@ description: >
   Troubleshooting the status of a Job
 ---
 
-This doc is about troubleshooting pending kubernetes Jobs, however, most of the ideas can be extrapolated
-to other supported CRDs.
+This doc is about troubleshooting pending [Kubernetes Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+however, most of the ideas can be extrapolated to other [supported job types](/docs/tasks/run).
+
+See the [Kueue overview](/docs/overview/#high-level-kueue-operation) to visualize the components that collaborate to run a Job.
+
+In the remaining of the document, let's assume your Job is called `my-job` and it's in the `my-namespace` namespace.
```

  Done

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:23** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T18:29:50Z
```diff
@@ -6,17 +6,19 @@ description: >
   Troubleshooting the status of a Job
 ---
 
-This doc is about troubleshooting pending kubernetes Jobs, however, most of the ideas can be extrapolated
-to other supported CRDs.
+This doc is about troubleshooting pending [Kubernetes Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+however, most of the ideas can be extrapolated to other [supported job types](/docs/tasks/run).
+
+See the [Kueue overview](/docs/overview/#high-level-kueue-operation) to visualize the components that collaborate to run a Job.
+
+In the remaining of the document, let's assume your Job is called `my-job` and it's in the `my-namespace` namespace.
 
 ## Identifying the Workload for your Job
 
-For each Job (a Kubernetes Job or a CRD), Kueue creates a [Workload](/docs/concepts/workload) object to hold the
-information about the admission of the Job. The Workload object allows Kueue to make admission decisions without
-knowing the specifics of each CRD.
+For each Job, Kueue creates a [Workload](/docs/concepts/workload) object to hold the
+details about the admission of the Job, wether it was admitted or not.
 
-There are multiple ways to find the Workload for a Job. In the following examples, let's assume your
-Job is called `my-job` in the `my-namespace` namespace.
+There are multiple ways to find the Workload for a Job.
 
 1. You can obtain the Workload name from the Job events, running the following command:
```

  Done

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:21** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T18:29:53Z
```diff
@@ -6,17 +6,19 @@ description: >
   Troubleshooting the status of a Job
 ---
 
-This doc is about troubleshooting pending kubernetes Jobs, however, most of the ideas can be extrapolated
-to other supported CRDs.
+This doc is about troubleshooting pending [Kubernetes Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+however, most of the ideas can be extrapolated to other [supported job types](/docs/tasks/run).
+
+See the [Kueue overview](/docs/overview/#high-level-kueue-operation) to visualize the components that collaborate to run a Job.
+
+In the remaining of the document, let's assume your Job is called `my-job` and it's in the `my-namespace` namespace.
 
 ## Identifying the Workload for your Job
 
-For each Job (a Kubernetes Job or a CRD), Kueue creates a [Workload](/docs/concepts/workload) object to hold the
-information about the admission of the Job. The Workload object allows Kueue to make admission decisions without
-knowing the specifics of each CRD.
+For each Job, Kueue creates a [Workload](/docs/concepts/workload) object to hold the
+details about the admission of the Job, wether it was admitted or not.
 
-There are multiple ways to find the Workload for a Job. In the following examples, let's assume your
-Job is called `my-job` in the `my-namespace` namespace.
+There are multiple ways to find the Workload for a Job.
```

  Done

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:50** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T18:30:34Z
```diff
@@ -47,7 +49,7 @@ Job is called `my-job` in the `my-namespace` namespace.
 
 3. You can list all of the workloads in the same namespace of your job and identify the one
```

  Done

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:74** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T18:32:19Z
```diff
@@ -60,16 +62,29 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue    cluster-queue True       9m45s
    ```
 
-## Is my Job running?
+Once you have identified the name of the Workload, you can obtain all details by
+running the following comand:
+
+```bash
+kubectl describe workload -n my-namespace job-my-job-19797
+```
 
-To know whether your Job is running, look for the value of the `.spec.suspend` field, by
+## Is my Job suspended?
+
+To know whether your Job is suspended, look for the value of the `.spec.suspend` field, by
```

  That's the next question: Is my job admitted?

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:88** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T18:35:41Z
```diff
@@ -60,16 +62,29 @@ Job is called `my-job` in the `my-namespace` namespace.
    job-my-job-19797   user-queue    cluster-queue True       9m45s
    ```
 
-## Is my Job running?
+Once you have identified the name of the Workload, you can obtain all details by
+running the following comand:
+
+```bash
+kubectl describe workload -n my-namespace job-my-job-19797
+```
 
-To know whether your Job is running, look for the value of the `.spec.suspend` field, by
+## Is my Job suspended?
+
+To know whether your Job is suspended, look for the value of the `.spec.suspend` field, by
 running the following command:
 
 ```
 kubectl get job -n my-namespace my-job -o jsonpath='{.spec.suspend}'
 ```
 
-If your Job is running, the output will be `false`.
+When a job is suspended, Kubernetes will not create any [Pods](https://kubernetes.io/docs/concepts/workloads/pods/)
+for it or it will terminate and delete them if they were already running.
+
+A job is suspended when Kueue hasn't admitted it yet or when it was preempted to
+accommodate another job. To understand why your job is suspended,
+check the corresponding Workload object.
+
 
```

  Done

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:271** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T18:36:10Z
```diff
@@ -250,3 +265,60 @@ status:
 
 The `Evicted` condition shows that the Workload was preempted and the `QuotaReserved` condition with `status: "True"`
 shows that Kueue already attempted to admit it again, unsuccessfully in this case.
+
+## Are the Pods of my Job running?
+
+When a Job is not suspended, Kubernetes creates Pods for it.
```

  Done

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:292** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T18:37:46Z
```diff
@@ -250,3 +265,60 @@ status:
 
 The `Evicted` condition shows that the Workload was preempted and the `QuotaReserved` condition with `status: "True"`
 shows that Kueue already attempted to admit it again, unsuccessfully in this case.
+
+## Are the Pods of my Job running?
+
+When a Job is not suspended, Kubernetes creates Pods for it.
+To check how many of these Pods are scheduled and running, you can first check the Job status.
+
+Run the following command to obtain the full status of a Job:
+
+```bash
+kubectl get job -n my-namespace my-job -o yaml
+```
+
+The output will be similar to the following:
+```yaml
+...
+status:
+  active: 5
+  ready: 4
+  succeeded: 1
+  ...
+```
+
+The `active` field shows how many Pods for the Job exist, and the `ready` field shows how many of them are running.
+
+To list all the pods of the Job, you can can run the following command:
```

  Done

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:272** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T18:37:49Z
```diff
@@ -250,3 +265,60 @@ status:
 
 The `Evicted` condition shows that the Workload was preempted and the `QuotaReserved` condition with `status: "True"`
 shows that Kueue already attempted to admit it again, unsuccessfully in this case.
+
+## Are the Pods of my Job running?
+
+When a Job is not suspended, Kubernetes creates Pods for it.
+To check how many of these Pods are scheduled and running, you can first check the Job status.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-14T06:53:27Z

LGTM

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:19** by [@mimowo](https://github.com/mimowo) — 2024-05-14T06:49:54Z
```diff
@@ -6,74 +6,88 @@ description: >
   Troubleshooting the status of a Job
 ---
 
-This doc is about troubleshooting pending kubernetes Jobs, however, most of the ideas can be extrapolated
-to other supported CRDs.
+This doc is about troubleshooting pending [Kubernetes Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+however, most of the ideas can be extrapolated to other [supported job types](/docs/tasks/run).
+
+See the [Kueue overview](/docs/overview/#high-level-kueue-operation) to visualize the components that collaborate to run a Job.
+
+In this document, assume that your Job is called `my-job` and it's in the `my-namespace` namespace.
 
 ## Identifying the Workload for your Job
 
-For each Job (a Kubernetes Job or a CRD), Kueue creates a [Workload](/docs/concepts/workload) object to hold the
-information about the admission of the Job. The Workload object allows Kueue to make admission decisions without
-knowing the specifics of each CRD.
+For each Job, Kueue creates a [Workload](/docs/concepts/workload) object to hold the
+details about the admission of the Job, wether it was admitted or not.
```

  ```suggestion
  details about the admission of the Job, whether it was admitted or not.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-14T16:46:39Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_jobs.md:19** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-14T16:46:39Z
```diff
@@ -6,74 +6,88 @@ description: >
   Troubleshooting the status of a Job
 ---
 
-This doc is about troubleshooting pending kubernetes Jobs, however, most of the ideas can be extrapolated
-to other supported CRDs.
+This doc is about troubleshooting pending [Kubernetes Jobs](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+however, most of the ideas can be extrapolated to other [supported job types](/docs/tasks/run).
+
+See the [Kueue overview](/docs/overview/#high-level-kueue-operation) to visualize the components that collaborate to run a Job.
+
+In this document, assume that your Job is called `my-job` and it's in the `my-namespace` namespace.
 
 ## Identifying the Workload for your Job
 
-For each Job (a Kubernetes Job or a CRD), Kueue creates a [Workload](/docs/concepts/workload) object to hold the
-information about the admission of the Job. The Workload object allows Kueue to make admission decisions without
-knowing the specifics of each CRD.
+For each Job, Kueue creates a [Workload](/docs/concepts/workload) object to hold the
+details about the admission of the Job, wether it was admitted or not.
```

  Fixed
