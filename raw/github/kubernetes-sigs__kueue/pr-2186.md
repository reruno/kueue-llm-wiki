# PR #2186: Restructure integration page and add content on external integrations

**Summary**: Restructure integration page and add content on external integrations

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2186

**Last updated**: 2024-05-13T17:55:29Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@dgrove-oss](https://github.com/dgrove-oss)
- **Branch**: `extending-kueue-docs` → `main`
- **Created**: 2024-05-10T20:48:48Z
- **Updated**: 2024-05-13T17:55:29Z
- **Merged**: 2024-05-13T17:49:41Z
- **Closed**: 2024-05-13T17:49:41Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@trasc](https://github.com/trasc)
- **Changed files**: 2   **+75 / -39**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Expand documentation on integrating a new Job type and adds material on external integrations.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2082 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-10T20:48:57Z

Hi @dgrove-oss. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-10T20:49:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a0ce70f9750fdc3a4b5dda5af3a04e1eae53f214 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6642478691b5890008c2eb93 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2186--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2186--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTIxODYtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.IY5Rx0xBLH1CnAHuarvYE6kGLd8SOPd15-S3g4P6uQE)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-13T13:03:33Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T17:46:27Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-13T17:46:32Z

LGTM label has been added.  <details>Git tree hash: 9005a02c7b0f81f565a58ef1d9d8558163dbad9c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-13T17:46:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2186#issuecomment-2108411696" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2186#" title="Author self-approved">dgrove-oss</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-13T09:54:45Z

- **site/content/en/docs/tasks/dev/integrate_a_custom_job.md:13** by [@mwielgus](https://github.com/mwielgus) — 2024-05-13T09:54:45Z
```diff
@@ -6,63 +6,99 @@ description: >
   Integrate a custom Job with Kueue.
 ---
 
-Kueue integrates with a couple of Jobs, including Kubernetes batch Job, MPIJob, RayJob or JobSet.  
+Kueue has built-in integrations for several Job types, including
+Kubernetes batch Job, MPIJob, RayJob and JobSet.
 
-There are two options for integrating a Job-like CRD with Kueue:
-- As part of the Kueue repository
+There are two options for adding an additional integration for a Job-like CRD with Kueue:
 - Writing an external controller
+- As part of the Kueue repository
```

  I guess for now, in-repository integration for public and well-adopted CRDs is preferred.  This allows us to bulk-update integrations in case of changes/new features in the core Kueue. Once the feature set in Kueue is more mature external controllers will be more preferred option. For that reason, please restore the original order of options.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-13T10:15:40Z

- **site/content/en/docs/tasks/dev/integrate_a_custom_job.md:17** by [@mwielgus](https://github.com/mwielgus) — 2024-05-13T10:15:40Z
```diff
@@ -6,63 +6,99 @@ description: >
   Integrate a custom Job with Kueue.
 ---
 
-Kueue integrates with a couple of Jobs, including Kubernetes batch Job, MPIJob, RayJob or JobSet.  
+Kueue has built-in integrations for several Job types, including
+Kubernetes batch Job, MPIJob, RayJob and JobSet.
 
-There are two options for integrating a Job-like CRD with Kueue:
-- As part of the Kueue repository
+There are two options for adding an additional integration for a Job-like CRD with Kueue:
 - Writing an external controller
+- As part of the Kueue repository
 
-This guide is for [platform developers](/docs/tasks#platform-developer) and focuses on the first approach.
-If there is a widely used Job which you would like supported in Kueue, please consider contributing.
-This page shows how to integrate a custom Job with Kueue.
-
-## Pre-requisites
-
-1. The custom Job CRD should have a suspend-like field, with semantics similar to the [`suspend` field in a Kubernetes Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job).
+This guide is for [platform developers](/docs/tasks#platform-developer) and describes how
+to build a new integration.  We recommend building your new integration as an external
```

  Same as above. I'm not sure if the external integration is the recommended option in all of the cases. For private, company-specific CRDs - definitely yes and it is the only possibility. But for public, well-adopted CRDs the decision is, imho, not that clear at this stage of Kueue maturity. 
  
  @alculquicondor @tenzen-y please chime in.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-13T12:04:47Z

- **site/content/en/docs/tasks/dev/integrate_a_custom_job.md:27** by [@mimowo](https://github.com/mimowo) — 2024-05-13T12:04:47Z
```diff
@@ -6,63 +6,99 @@ description: >
   Integrate a custom Job with Kueue.
 ---
 
-Kueue integrates with a couple of Jobs, including Kubernetes batch Job, MPIJob, RayJob or JobSet.  
+Kueue has built-in integrations for several Job types, including
+Kubernetes batch Job, MPIJob, RayJob and JobSet.
 
-There are two options for integrating a Job-like CRD with Kueue:
-- As part of the Kueue repository
+There are two options for adding an additional integration for a Job-like CRD with Kueue:
 - Writing an external controller
+- As part of the Kueue repository
 
-This guide is for [platform developers](/docs/tasks#platform-developer) and focuses on the first approach.
-If there is a widely used Job which you would like supported in Kueue, please consider contributing.
-This page shows how to integrate a custom Job with Kueue.
-
-## Pre-requisites
-
-1. The custom Job CRD should have a suspend-like field, with semantics similar to the [`suspend` field in a Kubernetes Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job).
+This guide is for [platform developers](/docs/tasks#platform-developer) and describes how
+to build a new integration.  We recommend building your new integration as an external
+controller that uses Kueue's job framework as a library. By reusing Kueue's job framework,
+your controller will be significantly easier to write and will be properly structured to
+be eventually contributed to Kueue as a built-in integration if your Job type becomes
+widely used by the community.
 
-## What you need to specify
+## Overview of Requirements
 
-1. Configuration
-2. Controller
-3. Webhook
-4. Adjust build system
+Kueue uses the [controller-runtime](https://github.com/kubernetes-sigs/controller-runtime). If you are
+unfamiliar with the controller-runtime, we recommend becoming familiar with it and with
+[Kubebuilder](https://github.com/kubernetes-sigs/kubebuilder) before starting to build a Kueue integration.
```

  ```suggestion
  Kueue uses the [controller-runtime](https://github.com/kubernetes-sigs/controller-runtime). 
  We recommend becoming familiar with it and with
  [Kubebuilder](https://github.com/kubernetes-sigs/kubebuilder) before starting to build a Kueue integration.
  ```
  nit: same message, but shorter.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-13T13:03:12Z

- **site/content/en/docs/tasks/dev/integrate_a_custom_job.md:17** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-13T13:03:12Z
```diff
@@ -6,63 +6,99 @@ description: >
   Integrate a custom Job with Kueue.
 ---
 
-Kueue integrates with a couple of Jobs, including Kubernetes batch Job, MPIJob, RayJob or JobSet.  
+Kueue has built-in integrations for several Job types, including
+Kubernetes batch Job, MPIJob, RayJob and JobSet.
 
-There are two options for integrating a Job-like CRD with Kueue:
-- As part of the Kueue repository
+There are two options for adding an additional integration for a Job-like CRD with Kueue:
 - Writing an external controller
+- As part of the Kueue repository
 
-This guide is for [platform developers](/docs/tasks#platform-developer) and focuses on the first approach.
-If there is a widely used Job which you would like supported in Kueue, please consider contributing.
-This page shows how to integrate a custom Job with Kueue.
-
-## Pre-requisites
-
-1. The custom Job CRD should have a suspend-like field, with semantics similar to the [`suspend` field in a Kubernetes Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job).
+This guide is for [platform developers](/docs/tasks#platform-developer) and describes how
+to build a new integration.  We recommend building your new integration as an external
```

  @mwielgus I agree with you. We should implement controllers for the famous CRDs in this repository so that we can maturity the jobframework interfaces more and more.
  If someone would like to contribute the controllers for not-so-famous CRDs, we can accommodate the controllers as  `experimental` ones.
  
  https://github.com/kubernetes-sigs/kueue/tree/main/cmd/experimental

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-05-13T13:48:52Z

- **site/content/en/docs/tasks/dev/integrate_a_custom_job.md:17** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-05-13T13:48:52Z
```diff
@@ -6,63 +6,99 @@ description: >
   Integrate a custom Job with Kueue.
 ---
 
-Kueue integrates with a couple of Jobs, including Kubernetes batch Job, MPIJob, RayJob or JobSet.  
+Kueue has built-in integrations for several Job types, including
+Kubernetes batch Job, MPIJob, RayJob and JobSet.
 
-There are two options for integrating a Job-like CRD with Kueue:
-- As part of the Kueue repository
+There are two options for adding an additional integration for a Job-like CRD with Kueue:
 - Writing an external controller
+- As part of the Kueue repository
 
-This guide is for [platform developers](/docs/tasks#platform-developer) and focuses on the first approach.
-If there is a widely used Job which you would like supported in Kueue, please consider contributing.
-This page shows how to integrate a custom Job with Kueue.
-
-## Pre-requisites
-
-1. The custom Job CRD should have a suspend-like field, with semantics similar to the [`suspend` field in a Kubernetes Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job).
+This guide is for [platform developers](/docs/tasks#platform-developer) and describes how
+to build a new integration.  We recommend building your new integration as an external
```

  I revised as requested.
  
  As a practical matter, building an integration as an experimental integration isn't that attractive.  If the downstream project can't enable their controller without providing a bespoke built of Kueue, it isn't that useful.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-13T13:58:37Z

- **site/content/en/docs/tasks/dev/integrate_a_custom_job.md:92** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T13:58:13Z
```diff
@@ -6,63 +6,98 @@ description: >
   Integrate a custom Job with Kueue.
 ---
 
-Kueue integrates with a couple of Jobs, including Kubernetes batch Job, MPIJob, RayJob or JobSet.  
+Kueue has built-in integrations for several Job types, including
+Kubernetes batch Job, MPIJob, RayJob and JobSet.
 
-There are two options for integrating a Job-like CRD with Kueue:
+There are two options for adding an additional integration for a Job-like CRD with Kueue:
 - As part of the Kueue repository
 - Writing an external controller
 
-This guide is for [platform developers](/docs/tasks#platform-developer) and focuses on the first approach.
-If there is a widely used Job which you would like supported in Kueue, please consider contributing.
-This page shows how to integrate a custom Job with Kueue.
+This guide is for [platform developers](/docs/tasks#platform-developer) and describes how
+to build a new integration. Integrations should be built using the APIs provided by
+Kueue's `jobframework` package. This will both simplify development and ensure that
+your controller will be properly structured to become a core built-in integration if your
+Job type is widely used by the community.
 
-## Pre-requisites
+## Overview of Requirements
 
-1. The custom Job CRD should have a suspend-like field, with semantics similar to the [`suspend` field in a Kubernetes Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job).
+Kueue uses the [controller-runtime](https://github.com/kubernetes-sigs/controller-runtime).
+We recommend becoming familiar with it and with
+[Kubebuilder](https://github.com/kubernetes-sigs/kubebuilder) before starting to build a Kueue integration.
 
-## What you need to specify
+Whether you are building an external or built-in integration, your main tasks are
+the following:
 
-1. Configuration
-2. Controller
-3. Webhook
-4. Adjust build system
+1. To work with Kueue, your custom Job CRD should have a suspend-like
+field, with semantics similar to the [`suspend` field in a Kubernetes
+Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job).
+The field needs to be in your CRD's `spec`, not its `status`, to enable its
+value to be set from a webhook. Your CRD's primary controller must respond to
+changes in the value of this field by suspending or unsuspending its owned resources.
 
-## Configuration
+2. You will need to register the GroupVersionKind of your CRD with Kueue as
+an integration.
+
+3. You will need to instantiate various pieces of Kueue's `jobframework` package for
+your CRD:
+   - You will need to implement Kueue's `GenericJob` interface for your CRD
+   - You will need to instantiate a `ReconcilerFactory` and register it with the controller runtime.
+   - You will need to register webhooks that set the initial value of the `suspend` field in instances
+     of your CRD and validate Kueue invariants on creation and update operations.
+   - You will need to instantiate a `Workload` indexer for your CRD.
+
+## Building a Built-in Integration
+
+To get started, add a new folder in `./pkg/controller/jobs/` to host the implementation of the integration.
+
+Here are completed built-in integrations you can learn from:
+   - [BatchJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/job)
+   - [JobSet](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/jobset)
+   - [MPIJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/mpijob)
+   - [RayJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/rayjob)
+
+### Registration
 
 Add your framework name to `.integrations.frameworks` in [controller_manager_config.yaml](https://github.com/kubernetes-sigs/kueue/blob/main/config/components/manager/controller_manager_config.yaml)
 
-## Controller
+Add RBAC Authorization for your CRD using [kubebuilder marker comments](https://book.kubebuilder.io/reference/markers/rbac.html).
 
-Add a new folder in `./pkg/controller/jobs/` to host the implementation of the integration.
+Write a go `func init()` that invokes the jobframework `RegisterIntegration()` function.
 
-1. Register the new framework in `func Init()`
-2. Add RBAC Authorization using [kubebuilder marker comments](https://book.kubebuilder.io/reference/markers/rbac.html)
-    - Add RBAC Authorization for priorityclasses, events, workloads, resourceflavors and any resources you want.
-3. Implement the `GenericJob` interface, and other optional [interfaces defined by the framework](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go).
+### Job Framework
 
-## Webhook
+In the `mycrd_controller.go` file in your folder, implement the `GenericJob` interface, and other optional [interfaces defined by the framework](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go).
 
-Create a webhook file in the dedicated directory for the integration.
+In the `mycrd_webhook.go` file in your folder, provide the webhook that invokes helper methods in from the jobframework to
+set the initial suspend status of created jobs and validate invariants.
 
-You can learn how to create webhook in this [page](https://book.kubebuilder.io/cronjob-tutorial/webhook-implementation.html).
+Add testing files for both the controller and the webhook.  You can check the test files in the other subfolders of `./pkg/controller/jobs/`
+to learn how to implement them.
 
-The framework provides some validation functions for common labels and annotations.
+### Adjust build system
 
-Your webhook should have ability to suspend jobs.
+Add required dependencies to compile your code. For example, using `go get github.com/kubeflow/mpi-operator@0.4.0`.
 
+Update the [Makefile](https://github.com/kubernetes-sigs/kueue/blob/main/Makefile) for testing.
+   - Add commands which copy the CRD of your custom job to the Kueue project.
+   - Add your custom job operator CRD dependencies into `test-integration`.
 
-## Adjust build system
+## Building an External Integration
 
-1. Add required dependencies to compile the controller and webhook code. For example, using `go get github.com/kubeflow/mpi-operator@0.4.0`.
-2. Update [Makefile](https://github.com/kubernetes-sigs/kueue/blob/main/Makefile) for testing.
-   - Add commands which copy the crd of your custom job to the Kueue project.
-   - Add your custom job operator crd dependencies into `test-integration`.
+Here are completed external integrations you can learn from:
+   - [AppWrapper](https://github.com/project-codeflare/appwrapper)
 
-   For testing files, you can check the sample test files in [completed integrations](#completed-integrations) to learn how to implement them.
+### Registration
 
-## Completed integrations
-Here are completed integrations you can learn from:
-   - [BatchJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/job)
-   - [JobSet](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/jobset)
-   - [MPIJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/mpijob)
-   - [RayJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/rayjob)
\ No newline at end of file
+Add your framework's GroupVersionKind to `.integrations.externalFrameworks` in [controller_manager_config.yaml](https://github.com/kubernetes-sigs/kueue/blob/main/config/components/manager/controller_manager_config.yaml)
```

  Link to https://kueue.sigs.k8s.io/docs/installation/#install-a-custom-configured-released-version instead

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2024-05-13T14:09:55Z

- **site/content/en/docs/tasks/dev/integrate_a_custom_job.md:17** by [@mwielgus](https://github.com/mwielgus) — 2024-05-13T14:09:55Z
```diff
@@ -6,63 +6,99 @@ description: >
   Integrate a custom Job with Kueue.
 ---
 
-Kueue integrates with a couple of Jobs, including Kubernetes batch Job, MPIJob, RayJob or JobSet.  
+Kueue has built-in integrations for several Job types, including
+Kubernetes batch Job, MPIJob, RayJob and JobSet.
 
-There are two options for integrating a Job-like CRD with Kueue:
-- As part of the Kueue repository
+There are two options for adding an additional integration for a Job-like CRD with Kueue:
 - Writing an external controller
+- As part of the Kueue repository
 
-This guide is for [platform developers](/docs/tasks#platform-developer) and focuses on the first approach.
-If there is a widely used Job which you would like supported in Kueue, please consider contributing.
-This page shows how to integrate a custom Job with Kueue.
-
-## Pre-requisites
-
-1. The custom Job CRD should have a suspend-like field, with semantics similar to the [`suspend` field in a Kubernetes Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job).
+This guide is for [platform developers](/docs/tasks#platform-developer) and describes how
+to build a new integration.  We recommend building your new integration as an external
```

  I agree that the situation for custom integrations is far from perfect. To resolve it we should probably extract the framework to separate repository, finalize the interfaces, as well as complete KEPs that may impact the APIs, like Elastic Jobs and finish some aspects of MultiKueue.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-05-13T14:22:49Z

- **site/content/en/docs/tasks/dev/integrate_a_custom_job.md:92** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-05-13T14:22:49Z
```diff
@@ -6,63 +6,98 @@ description: >
   Integrate a custom Job with Kueue.
 ---
 
-Kueue integrates with a couple of Jobs, including Kubernetes batch Job, MPIJob, RayJob or JobSet.  
+Kueue has built-in integrations for several Job types, including
+Kubernetes batch Job, MPIJob, RayJob and JobSet.
 
-There are two options for integrating a Job-like CRD with Kueue:
+There are two options for adding an additional integration for a Job-like CRD with Kueue:
 - As part of the Kueue repository
 - Writing an external controller
 
-This guide is for [platform developers](/docs/tasks#platform-developer) and focuses on the first approach.
-If there is a widely used Job which you would like supported in Kueue, please consider contributing.
-This page shows how to integrate a custom Job with Kueue.
+This guide is for [platform developers](/docs/tasks#platform-developer) and describes how
+to build a new integration. Integrations should be built using the APIs provided by
+Kueue's `jobframework` package. This will both simplify development and ensure that
+your controller will be properly structured to become a core built-in integration if your
+Job type is widely used by the community.
 
-## Pre-requisites
+## Overview of Requirements
 
-1. The custom Job CRD should have a suspend-like field, with semantics similar to the [`suspend` field in a Kubernetes Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job).
+Kueue uses the [controller-runtime](https://github.com/kubernetes-sigs/controller-runtime).
+We recommend becoming familiar with it and with
+[Kubebuilder](https://github.com/kubernetes-sigs/kubebuilder) before starting to build a Kueue integration.
 
-## What you need to specify
+Whether you are building an external or built-in integration, your main tasks are
+the following:
 
-1. Configuration
-2. Controller
-3. Webhook
-4. Adjust build system
+1. To work with Kueue, your custom Job CRD should have a suspend-like
+field, with semantics similar to the [`suspend` field in a Kubernetes
+Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job).
+The field needs to be in your CRD's `spec`, not its `status`, to enable its
+value to be set from a webhook. Your CRD's primary controller must respond to
+changes in the value of this field by suspending or unsuspending its owned resources.
 
-## Configuration
+2. You will need to register the GroupVersionKind of your CRD with Kueue as
+an integration.
+
+3. You will need to instantiate various pieces of Kueue's `jobframework` package for
+your CRD:
+   - You will need to implement Kueue's `GenericJob` interface for your CRD
+   - You will need to instantiate a `ReconcilerFactory` and register it with the controller runtime.
+   - You will need to register webhooks that set the initial value of the `suspend` field in instances
+     of your CRD and validate Kueue invariants on creation and update operations.
+   - You will need to instantiate a `Workload` indexer for your CRD.
+
+## Building a Built-in Integration
+
+To get started, add a new folder in `./pkg/controller/jobs/` to host the implementation of the integration.
+
+Here are completed built-in integrations you can learn from:
+   - [BatchJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/job)
+   - [JobSet](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/jobset)
+   - [MPIJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/mpijob)
+   - [RayJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/rayjob)
+
+### Registration
 
 Add your framework name to `.integrations.frameworks` in [controller_manager_config.yaml](https://github.com/kubernetes-sigs/kueue/blob/main/config/components/manager/controller_manager_config.yaml)
 
-## Controller
+Add RBAC Authorization for your CRD using [kubebuilder marker comments](https://book.kubebuilder.io/reference/markers/rbac.html).
 
-Add a new folder in `./pkg/controller/jobs/` to host the implementation of the integration.
+Write a go `func init()` that invokes the jobframework `RegisterIntegration()` function.
 
-1. Register the new framework in `func Init()`
-2. Add RBAC Authorization using [kubebuilder marker comments](https://book.kubebuilder.io/reference/markers/rbac.html)
-    - Add RBAC Authorization for priorityclasses, events, workloads, resourceflavors and any resources you want.
-3. Implement the `GenericJob` interface, and other optional [interfaces defined by the framework](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go).
+### Job Framework
 
-## Webhook
+In the `mycrd_controller.go` file in your folder, implement the `GenericJob` interface, and other optional [interfaces defined by the framework](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go).
 
-Create a webhook file in the dedicated directory for the integration.
+In the `mycrd_webhook.go` file in your folder, provide the webhook that invokes helper methods in from the jobframework to
+set the initial suspend status of created jobs and validate invariants.
 
-You can learn how to create webhook in this [page](https://book.kubebuilder.io/cronjob-tutorial/webhook-implementation.html).
+Add testing files for both the controller and the webhook.  You can check the test files in the other subfolders of `./pkg/controller/jobs/`
+to learn how to implement them.
 
-The framework provides some validation functions for common labels and annotations.
+### Adjust build system
 
-Your webhook should have ability to suspend jobs.
+Add required dependencies to compile your code. For example, using `go get github.com/kubeflow/mpi-operator@0.4.0`.
 
+Update the [Makefile](https://github.com/kubernetes-sigs/kueue/blob/main/Makefile) for testing.
+   - Add commands which copy the CRD of your custom job to the Kueue project.
+   - Add your custom job operator CRD dependencies into `test-integration`.
 
-## Adjust build system
+## Building an External Integration
 
-1. Add required dependencies to compile the controller and webhook code. For example, using `go get github.com/kubeflow/mpi-operator@0.4.0`.
-2. Update [Makefile](https://github.com/kubernetes-sigs/kueue/blob/main/Makefile) for testing.
-   - Add commands which copy the crd of your custom job to the Kueue project.
-   - Add your custom job operator crd dependencies into `test-integration`.
+Here are completed external integrations you can learn from:
+   - [AppWrapper](https://github.com/project-codeflare/appwrapper)
 
-   For testing files, you can check the sample test files in [completed integrations](#completed-integrations) to learn how to implement them.
+### Registration
 
-## Completed integrations
-Here are completed integrations you can learn from:
-   - [BatchJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/job)
-   - [JobSet](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/jobset)
-   - [MPIJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/mpijob)
-   - [RayJob](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/jobs/rayjob)
\ No newline at end of file
+Add your framework's GroupVersionKind to `.integrations.externalFrameworks` in [controller_manager_config.yaml](https://github.com/kubernetes-sigs/kueue/blob/main/config/components/manager/controller_manager_config.yaml)
```

  Link updated.  I also added externalFrameworks as a commented out stanza to the example config map.
  
  I left the sample config map otherwise unchanged, but I was wondering if we really needed to have the disclaimer about fields that were only available in version 0.3 and later.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-13T16:55:39Z

- **site/content/en/docs/installation/_index.md:111** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-13T16:55:39Z
```diff
@@ -108,7 +108,8 @@ the default Kueue Configuration
 struct ([v1beta1@main](https://pkg.go.dev/sigs.k8s.io/kueue@main/apis/config/v1beta1#Configuration)).
 The contents of the ConfigMap are similar to the following:
 
-> __The `namespace` and `internalCertManagement` fields are available in Kueue v0.3.0 and later__
+> __The `namespace`, `waitForPodsReady`, and `internalCertManagement` fields are available in Kueue v0.3.0 and later.
```

  yeah, this can go away

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-13T16:56:05Z

/approve

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2024-05-13T17:02:39Z

- **site/content/en/docs/installation/_index.md:111** by [@dgrove-oss](https://github.com/dgrove-oss) — 2024-05-13T17:02:39Z
```diff
@@ -108,7 +108,8 @@ the default Kueue Configuration
 struct ([v1beta1@main](https://pkg.go.dev/sigs.k8s.io/kueue@main/apis/config/v1beta1#Configuration)).
 The contents of the ConfigMap are similar to the following:
 
-> __The `namespace` and `internalCertManagement` fields are available in Kueue v0.3.0 and later__
+> __The `namespace`, `waitForPodsReady`, and `internalCertManagement` fields are available in Kueue v0.3.0 and later.
```

  Removed the v0.3.0 field caveats; left just 1 copy of the field added in 0.7.0 caveat
