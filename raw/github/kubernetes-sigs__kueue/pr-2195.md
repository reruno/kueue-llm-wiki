# PR #2195: [kueuectl] Added list workloads command.

**Summary**: [kueuectl] Added list workloads command.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2195

**Last updated**: 2024-05-27T20:51:09Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/kueuectl-add-list-workloads-command` → `main`
- **Created**: 2024-05-13T19:15:05Z
- **Updated**: 2024-05-27T20:51:09Z
- **Merged**: 2024-05-27T20:50:17Z
- **Closed**: 2024-05-27T20:50:17Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus), [@kerthcet](https://github.com/kerthcet), [@trasc](https://github.com/trasc)
- **Changed files**: 16   **+1345 / -67**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Added list workloads command.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2135 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
CLI: Added list workloads command.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-13T19:15:08Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-13T19:15:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b3778e7db3a27dce6fcc1aac3406b76ae6e19fb2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6654ddaa24c92400088c8305 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-13T19:21:34Z

/assign

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T20:53:40Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T21:12:55Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-14T21:22:34Z

/test pull-kueue-test-scheduling-perf-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T12:44:54Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T12:48:15Z

/assign @trasc

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T19:13:09Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T19:38:43Z

/test pull-kueue-test-integration-main

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-16T18:35:20Z

/cc @mwielgus

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-20T06:01:34Z

LGTM label has been added.  <details>Git tree hash: 900cc4029c6f85ee2bb98fcd77a0dfe616457f76</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-21T18:04:22Z

/assign @alculquicondor

### Comment by [@trasc](https://github.com/trasc) — 2024-05-23T12:40:13Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-23T12:40:18Z

LGTM label has been added.  <details>Git tree hash: 63daf69883f69ce794202cb41dfc093b3295114d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-27T20:46:24Z

LGTM label has been added.  <details>Git tree hash: b93775db84e0847aa07fa2872f7a0611819cc9d4</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-27T20:46:30Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2195#pullrequestreview-2081448017" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2195#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-15T14:29:51Z

- **cmd/kueuectl/app/list/helpers.go:40** by [@trasc](https://github.com/trasc) — 2024-05-15T13:23:06Z
```diff
@@ -37,7 +37,12 @@ func addAllNamespacesFlagVar(cmd *cobra.Command, p *bool) {
 
 func addClusterQueueFilterFlagVar(cmd *cobra.Command, p *string) {
 	cmd.Flags().StringVarP(p, "clusterqueue", "c", "",
-		"Filter by cluster queue name which associated with the local queue.")
+		"Filter by cluster queue name which associated with resource.")
```

  ```suggestion
  		"Filter by cluster queue name which is associated with the resource.")
  ```

- **cmd/kueuectl/app/list/helpers.go:45** by [@trasc](https://github.com/trasc) — 2024-05-15T13:23:29Z
```diff
@@ -37,7 +37,12 @@ func addAllNamespacesFlagVar(cmd *cobra.Command, p *bool) {
 
 func addClusterQueueFilterFlagVar(cmd *cobra.Command, p *string) {
 	cmd.Flags().StringVarP(p, "clusterqueue", "c", "",
-		"Filter by cluster queue name which associated with the local queue.")
+		"Filter by cluster queue name which associated with resource.")
+}
+
+func addLocalQueueFilterFlagVar(cmd *cobra.Command, p *string) {
+	cmd.Flags().StringVarP(p, "localqueue", "q", "",
+		"Filter by local queue name which associated with resource.")
```

  ```suggestion
  		"Filter by local queue name which is associated with the resource.")
  ```

- **cmd/kueuectl/app/list/list.go:28** by [@trasc](https://github.com/trasc) — 2024-05-15T13:25:43Z
```diff
@@ -25,7 +25,13 @@ import (
 
 const (
 	listExample = `  # List LocalQueue 
-  kueuectl list localqueue`
+  kueuectl list localqueue
```

  Do we need the examples here as well if we have them for each type supported?

- **cmd/kueuectl/app/list/list_workload_printer.go:88** by [@trasc](https://github.com/trasc) — 2024-05-15T13:54:17Z
```diff
@@ -0,0 +1,170 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadPrinter struct {
+	printOptions     printers.PrintOptions
+	resources        []*metav1.APIResourceList
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
+			{Name: "Position in Queue", Type: "string"},
+			{Name: "Age", Type: "string"},
+		},
+		Rows: p.printWorkloadList(list),
+	}
+
+	return printer.PrintObj(table, out)
+}
+
+func (p *listWorkloadPrinter) WithNamespace() *listWorkloadPrinter {
+	p.printOptions.WithNamespace = true
+	return p
+}
+
+func (p *listWorkloadPrinter) WithLocalQueues(localQueues map[string]*v1beta1.LocalQueue) *listWorkloadPrinter {
+	p.localQueues = localQueues
+	return p
+}
+
+func (p *listWorkloadPrinter) WithPendingWorkloads(pendingWorkloads map[string]*v1alpha1.PendingWorkload) *listWorkloadPrinter {
+	p.pendingWorkloads = pendingWorkloads
+	return p
+}
+
+func (p *listWorkloadPrinter) WithResources(resources []*metav1.APIResourceList) *listWorkloadPrinter {
+	p.resources = resources
```

  Make this "smarter" , put the resources in some maps usable in `printWorkload`

- **cmd/kueuectl/app/list/list_workload_printer.go:170** by [@trasc](https://github.com/trasc) — 2024-05-15T13:55:06Z
```diff
@@ -0,0 +1,170 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadPrinter struct {
+	printOptions     printers.PrintOptions
+	resources        []*metav1.APIResourceList
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
+			{Name: "Position in Queue", Type: "string"},
+			{Name: "Age", Type: "string"},
+		},
+		Rows: p.printWorkloadList(list),
+	}
+
+	return printer.PrintObj(table, out)
+}
+
+func (p *listWorkloadPrinter) WithNamespace() *listWorkloadPrinter {
+	p.printOptions.WithNamespace = true
+	return p
+}
+
+func (p *listWorkloadPrinter) WithLocalQueues(localQueues map[string]*v1beta1.LocalQueue) *listWorkloadPrinter {
+	p.localQueues = localQueues
+	return p
+}
+
+func (p *listWorkloadPrinter) WithPendingWorkloads(pendingWorkloads map[string]*v1alpha1.PendingWorkload) *listWorkloadPrinter {
+	p.pendingWorkloads = pendingWorkloads
+	return p
+}
+
+func (p *listWorkloadPrinter) WithResources(resources []*metav1.APIResourceList) *listWorkloadPrinter {
+	p.resources = resources
+	return p
+}
+
+func newWorkloadTablePrinter() *listWorkloadPrinter {
+	return &listWorkloadPrinter{}
+}
+
+func (p *listWorkloadPrinter) printWorkloadList(list *v1beta1.WorkloadList) []metav1.TableRow {
+	rows := make([]metav1.TableRow, len(list.Items))
+	for index := range list.Items {
+		rows[index] = p.printWorkload(&list.Items[index])
+	}
+	return rows
+}
+
+func (p *listWorkloadPrinter) printWorkload(wl *v1beta1.Workload) metav1.TableRow {
+	row := metav1.TableRow{
+		Object: runtime.RawExtension{Object: wl},
+	}
+
+	var clusterQueueName string
+	if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+		clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+	} else if lq := p.localQueues[localQueueKeyForWorkload(wl)]; lq != nil {
+		clusterQueueName = string(lq.Spec.ClusterQueue)
+	}
+
+	var positionInQueue string
+	if pendingWorkload, ok := p.pendingWorkloads[workload.Key(wl)]; ok {
+		positionInQueue = fmt.Sprintf("%d", pendingWorkload.PositionInLocalQueue)
+	}
+
+	crdTypes := make(map[string]struct{}, len(wl.ObjectMeta.OwnerReferences))
+	crdNames := make(map[string]struct{}, len(wl.ObjectMeta.OwnerReferences))
+	for _, ref := range wl.ObjectMeta.OwnerReferences {
+		var resourceList *metav1.APIResourceList
+		for _, rl := range p.resources {
+			if rl.GroupVersion == ref.APIVersion {
+				resourceList = rl
+			}
+		}
+		var resource *metav1.APIResource
+		if resourceList != nil {
+			for _, r := range resourceList.APIResources {
+				if r.Kind == ref.Kind {
+					resource = &r
+				}
+			}
+		}
+
+		if resource != nil {
+			crdTypeParts := make([]string, 0, 2)
+
+			if len(resource.SingularName) > 0 {
+				crdTypeParts = append(crdTypeParts, resource.SingularName)
+			}
+
+			if len(resource.Group) > 0 {
+				crdTypeParts = append(crdTypeParts, resource.Group)
+			}
+
+			crdType := strings.Join(crdTypeParts, ".")
+			if len(crdType) > crdTypeMaxLength {
+				crdType = fmt.Sprintf("%s...", crdType[:crdTypeMaxLength])
+			}
+			crdTypes[crdType] = struct{}{}
+		}
+
+		crdNames[ref.Name] = struct{}{}
```

  Make this a method of p

- **cmd/kueuectl/app/list/list_workload_test.go:539** by [@trasc](https://github.com/trasc) — 2024-05-15T14:00:17Z
```diff
@@ -0,0 +1,348 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns         string
+		objs       []runtime.Object
+		args       []string
+		wantOut    string
+		wantOutErr string
+		wantErr    error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter": {
+			args: []string{"--localqueue", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter (short flag)": {
+			args: []string{"-q", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter": {
+			args: []string{"--clusterqueue", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter (short flag)": {
+			args: []string{"-c", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with only pending filter": {
+			args: []string{"--only-pending"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print workload list with only admitted filter": {
+			args: []string{"--only-admitted"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionTrue,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS     POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            ADMITTED                       60m
+`,
+		},
+		"should print workload list with label selector filter": {
+			args: []string{"--selector", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with label selector filter (short flag)": {
+			args: []string{"-l", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with pending status": {
+			args: []string{},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print not found error": {
+			wantOutErr: fmt.Sprintf("No resources found in %s namespace.\n", cmdtesting.DefaultNamespace),
+		},
+		"should print not found error with all-namespaces filter": {
+			args:       []string{"-A"},
+			wantOutErr: "No resources found\n",
+		},
```

  Why?

- **pkg/workload/workload.go:47** by [@trasc](https://github.com/trasc) — 2024-05-15T14:06:31Z
```diff
@@ -43,6 +43,13 @@ import (
 	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
 )
 
+const (
+	PendingStatus       = "pending"
```

  I think 
  ```suggestion
  	StatusPending       = "pending"
  ```
  sounds a bit better. Same for the others.

- **cmd/kueuectl/app/list/list_workload_test.go:1** by [@trasc](https://github.com/trasc) — 2024-05-15T14:14:40Z
  Here or in integration, we need a way to test the queuPosition and kind name.
  
  If no other way around it, create a printer based test with  p.With...

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-15T15:10:06Z

- **cmd/kueuectl/app/list/list_workload_test.go:539** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T15:10:06Z
```diff
@@ -0,0 +1,348 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns         string
+		objs       []runtime.Object
+		args       []string
+		wantOut    string
+		wantOutErr string
+		wantErr    error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter": {
+			args: []string{"--localqueue", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter (short flag)": {
+			args: []string{"-q", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter": {
+			args: []string{"--clusterqueue", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter (short flag)": {
+			args: []string{"-c", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with only pending filter": {
+			args: []string{"--only-pending"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print workload list with only admitted filter": {
+			args: []string{"--only-admitted"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionTrue,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS     POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            ADMITTED                       60m
+`,
+		},
+		"should print workload list with label selector filter": {
+			args: []string{"--selector", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with label selector filter (short flag)": {
+			args: []string{"-l", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with pending status": {
+			args: []string{},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print not found error": {
+			wantOutErr: fmt.Sprintf("No resources found in %s namespace.\n", cmdtesting.DefaultNamespace),
+		},
+		"should print not found error with all-namespaces filter": {
+			args:       []string{"-A"},
+			wantOutErr: "No resources found\n",
+		},
```

  To tell user that we don't have resources, similarly as in kubectl.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-15T15:15:53Z

- **cmd/kueuectl/app/list/list_workload_test.go:539** by [@trasc](https://github.com/trasc) — 2024-05-15T15:15:53Z
```diff
@@ -0,0 +1,348 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns         string
+		objs       []runtime.Object
+		args       []string
+		wantOut    string
+		wantOutErr string
+		wantErr    error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter": {
+			args: []string{"--localqueue", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter (short flag)": {
+			args: []string{"-q", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter": {
+			args: []string{"--clusterqueue", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter (short flag)": {
+			args: []string{"-c", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with only pending filter": {
+			args: []string{"--only-pending"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print workload list with only admitted filter": {
+			args: []string{"--only-admitted"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionTrue,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS     POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            ADMITTED                       60m
+`,
+		},
+		"should print workload list with label selector filter": {
+			args: []string{"--selector", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with label selector filter (short flag)": {
+			args: []string{"-l", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with pending status": {
+			args: []string{},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print not found error": {
+			wantOutErr: fmt.Sprintf("No resources found in %s namespace.\n", cmdtesting.DefaultNamespace),
+		},
+		"should print not found error with all-namespaces filter": {
+			args:       []string{"-A"},
+			wantOutErr: "No resources found\n",
+		},
```

  Do we have a test wit `-A` when we have workloads?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-15T15:18:42Z

- **cmd/kueuectl/app/list/list_workload_test.go:539** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-15T15:18:42Z
```diff
@@ -0,0 +1,348 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns         string
+		objs       []runtime.Object
+		args       []string
+		wantOut    string
+		wantOutErr string
+		wantErr    error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter": {
+			args: []string{"--localqueue", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter (short flag)": {
+			args: []string{"-q", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter": {
+			args: []string{"--clusterqueue", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter (short flag)": {
+			args: []string{"-c", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with only pending filter": {
+			args: []string{"--only-pending"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print workload list with only admitted filter": {
+			args: []string{"--only-admitted"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionTrue,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS     POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            ADMITTED                       60m
+`,
+		},
+		"should print workload list with label selector filter": {
+			args: []string{"--selector", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with label selector filter (short flag)": {
+			args: []string{"-l", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with pending status": {
+			args: []string{},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print not found error": {
+			wantOutErr: fmt.Sprintf("No resources found in %s namespace.\n", cmdtesting.DefaultNamespace),
+		},
+		"should print not found error with all-namespaces filter": {
+			args:       []string{"-A"},
+			wantOutErr: "No resources found\n",
+		},
```

  Yes, [here](https://github.com/kubernetes-sigs/kueue/blob/7f67e8e2777567b7985d23013bf224a2f4720e01/cmd/kueuectl/app/list/list_test.go#L115)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-16T18:12:22Z

- **cmd/kueuectl/app/list/list_workload.go:303** by [@trasc](https://github.com/trasc) — 2024-05-16T17:00:41Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		localQueue, ok := localQueues[localQueueKeyForWorkload(&wl)]
+		if !ok {
+			localQueue, err = o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				localQueue = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = localQueue
+		}
+		if localQueue == nil {
+			continue
+		}
+	}
+
+	return localQueues, nil
+}
+
+func (o *WorkloadOptions) pendingWorkloads(ctx context.Context, list *v1beta1.WorkloadList, localQueues map[string]*v1beta1.LocalQueue) (map[string]*v1alpha1.PendingWorkload, error) {
+	var err error
+
+	pendingWorkloads := make(map[string]*v1alpha1.PendingWorkload)
+	pendingWorkloadsSummaries := make(map[string]*v1alpha1.PendingWorkloadsSummary)
+
+	for _, wl := range list.Items {
+		if !workloadPending(&wl) {
+			continue
+		}
+		var clusterQueueName string
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+		} else if lq := localQueues[localQueueKeyForWorkload(&wl)]; lq != nil {
+			clusterQueueName = string(lq.Spec.ClusterQueue)
+		}
```

  Let's keep this simple and always get it from the local queue.

- **cmd/kueuectl/app/list/list_workload.go:322** by [@trasc](https://github.com/trasc) — 2024-05-16T17:03:33Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		localQueue, ok := localQueues[localQueueKeyForWorkload(&wl)]
+		if !ok {
+			localQueue, err = o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				localQueue = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = localQueue
+		}
+		if localQueue == nil {
+			continue
+		}
+	}
+
+	return localQueues, nil
+}
+
+func (o *WorkloadOptions) pendingWorkloads(ctx context.Context, list *v1beta1.WorkloadList, localQueues map[string]*v1beta1.LocalQueue) (map[string]*v1alpha1.PendingWorkload, error) {
+	var err error
+
+	pendingWorkloads := make(map[string]*v1alpha1.PendingWorkload)
+	pendingWorkloadsSummaries := make(map[string]*v1alpha1.PendingWorkloadsSummary)
+
+	for _, wl := range list.Items {
+		if !workloadPending(&wl) {
+			continue
+		}
+		var clusterQueueName string
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+		} else if lq := localQueues[localQueueKeyForWorkload(&wl)]; lq != nil {
+			clusterQueueName = string(lq.Spec.ClusterQueue)
+		}
+		pendingWorkloadsSummary, ok := pendingWorkloadsSummaries[clusterQueueName]
+		if !ok {
+			pendingWorkloadsSummary, err = o.ClientSet.VisibilityV1alpha1().ClusterQueues().
+				GetPendingWorkloadsSummary(ctx, clusterQueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				pendingWorkloadsSummary = nil
+			}
+			pendingWorkloadsSummaries[clusterQueueName] = pendingWorkloadsSummary
+		}
+		if pendingWorkloadsSummary == nil {
+			continue
+		}
+		for _, pendingWorkload := range pendingWorkloadsSummary.Items {
+			if pendingWorkload.Name == wl.Name && pendingWorkload.Namespace == wl.Namespace {
+				pendingWorkloads[workload.Key(&wl)] = &pendingWorkload
+			}
+		}
+	}
+
+	return pendingWorkloads, nil
+}
+
+func (o *WorkloadOptions) apiResources(list *v1beta1.WorkloadList) (map[string]*metav1.APIResourceList, error) {
+	var err error
+
+	apiResourceLists := make(map[string]*metav1.APIResourceList)
+
+	for _, wl := range list.Items {
+		for _, ref := range wl.ObjectMeta.OwnerReferences {
+			rl, ok := apiResourceLists[ref.APIVersion]
+			if !ok {
+				rl, err = o.ClientSet.Discovery().ServerResourcesForGroupVersion(ref.APIVersion)
+				if client.IgnoreNotFound(err) != nil {
+					return nil, err
+				}
+				if err != nil {
+					rl = nil
+				}
+				apiResourceLists[ref.APIVersion] = rl
+			}
+			if rl == nil {
+				continue
+			}
+		}
+	}
+
+	return apiResourceLists, nil
+}
+
+func workloadPending(wl *v1beta1.Workload) bool {
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	return cond != nil && cond.Type == v1beta1.WorkloadQuotaReserved && cond.Status == metav1.ConditionFalse && cond.Reason == "Pending"
```

  ```suggestion
  	return cond != nil && cond.Status == metav1.ConditionFalse && cond.Reason == "Pending"
  ```
  
  We can trust `meta.FindStatusCondition` with `cond.Type == v1beta1.WorkloadQuotaReserved`

- **cmd/kueuectl/app/list/list_workload.go:283** by [@trasc](https://github.com/trasc) — 2024-05-16T17:09:18Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		localQueue, ok := localQueues[localQueueKeyForWorkload(&wl)]
+		if !ok {
+			localQueue, err = o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				localQueue = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = localQueue
+		}
+		if localQueue == nil {
+			continue
+		}
```

  ```suggestion
  
  ```

- **cmd/kueuectl/app/list/list_workload.go:270** by [@trasc](https://github.com/trasc) — 2024-05-16T17:11:47Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
```

  But if we want to use the returned map for more then the display of the queue name....

- **cmd/kueuectl/app/list/list_workload_printer.go:72** by [@trasc](https://github.com/trasc) — 2024-05-16T17:44:57Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
```

  @alculquicondor @mwielgus I think we should replace `CRD` with `Owner`, ( the owner is not necessarily a Custom Resource , and the fields have nothing to do with the owner resource definition ).

- **cmd/kueuectl/app/list/list_workload.go:313** by [@trasc](https://github.com/trasc) — 2024-05-16T18:05:51Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		localQueue, ok := localQueues[localQueueKeyForWorkload(&wl)]
+		if !ok {
+			localQueue, err = o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				localQueue = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = localQueue
+		}
+		if localQueue == nil {
+			continue
+		}
+	}
+
+	return localQueues, nil
+}
+
+func (o *WorkloadOptions) pendingWorkloads(ctx context.Context, list *v1beta1.WorkloadList, localQueues map[string]*v1beta1.LocalQueue) (map[string]*v1alpha1.PendingWorkload, error) {
+	var err error
+
+	pendingWorkloads := make(map[string]*v1alpha1.PendingWorkload)
+	pendingWorkloadsSummaries := make(map[string]*v1alpha1.PendingWorkloadsSummary)
+
+	for _, wl := range list.Items {
+		if !workloadPending(&wl) {
+			continue
+		}
+		var clusterQueueName string
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+		} else if lq := localQueues[localQueueKeyForWorkload(&wl)]; lq != nil {
+			clusterQueueName = string(lq.Spec.ClusterQueue)
+		}
+		pendingWorkloadsSummary, ok := pendingWorkloadsSummaries[clusterQueueName]
+		if !ok {
+			pendingWorkloadsSummary, err = o.ClientSet.VisibilityV1alpha1().ClusterQueues().
+				GetPendingWorkloadsSummary(ctx, clusterQueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				pendingWorkloadsSummary = nil
+			}
+			pendingWorkloadsSummaries[clusterQueueName] = pendingWorkloadsSummary
+		}
+		if pendingWorkloadsSummary == nil {
+			continue
+		}
+		for _, pendingWorkload := range pendingWorkloadsSummary.Items {
+			if pendingWorkload.Name == wl.Name && pendingWorkload.Namespace == wl.Namespace {
+				pendingWorkloads[workload.Key(&wl)] = &pendingWorkload
+			}
+		}
+	}
+
+	return pendingWorkloads, nil
+}
+
+func (o *WorkloadOptions) apiResources(list *v1beta1.WorkloadList) (map[string]*metav1.APIResourceList, error) {
+	var err error
+
+	apiResourceLists := make(map[string]*metav1.APIResourceList)
+
+	for _, wl := range list.Items {
+		for _, ref := range wl.ObjectMeta.OwnerReferences {
+			rl, ok := apiResourceLists[ref.APIVersion]
+			if !ok {
+				rl, err = o.ClientSet.Discovery().ServerResourcesForGroupVersion(ref.APIVersion)
+				if client.IgnoreNotFound(err) != nil {
+					return nil, err
+				}
+				if err != nil {
+					rl = nil
+				}
+				apiResourceLists[ref.APIVersion] = rl
+			}
+			if rl == nil {
+				continue
+			}
```

  ```suggestion
  
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-16T18:20:35Z

- **cmd/kueuectl/app/list/list_workload.go:270** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-16T18:20:35Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
```

  Yes, agree with you. But it will require more requests... and now it's not need...

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-16T18:26:21Z

- **cmd/kueuectl/app/list/list_workload.go:303** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-16T18:26:21Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		localQueue, ok := localQueues[localQueueKeyForWorkload(&wl)]
+		if !ok {
+			localQueue, err = o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				localQueue = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = localQueue
+		}
+		if localQueue == nil {
+			continue
+		}
+	}
+
+	return localQueues, nil
+}
+
+func (o *WorkloadOptions) pendingWorkloads(ctx context.Context, list *v1beta1.WorkloadList, localQueues map[string]*v1beta1.LocalQueue) (map[string]*v1alpha1.PendingWorkload, error) {
+	var err error
+
+	pendingWorkloads := make(map[string]*v1alpha1.PendingWorkload)
+	pendingWorkloadsSummaries := make(map[string]*v1alpha1.PendingWorkloadsSummary)
+
+	for _, wl := range list.Items {
+		if !workloadPending(&wl) {
+			continue
+		}
+		var clusterQueueName string
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+		} else if lq := localQueues[localQueueKeyForWorkload(&wl)]; lq != nil {
+			clusterQueueName = string(lq.Spec.ClusterQueue)
+		}
```

  It's related with https://github.com/kubernetes-sigs/kueue/pull/2195#discussion_r1603747380.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-16T18:31:51Z

- **cmd/kueuectl/app/list/list_workload.go:283** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-16T18:31:51Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		localQueue, ok := localQueues[localQueueKeyForWorkload(&wl)]
+		if !ok {
+			localQueue, err = o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				localQueue = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = localQueue
+		}
+		if localQueue == nil {
+			continue
+		}
```

  Thank you. Fixed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-16T18:32:04Z

- **cmd/kueuectl/app/list/list_workload.go:313** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-16T18:32:04Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		localQueue, ok := localQueues[localQueueKeyForWorkload(&wl)]
+		if !ok {
+			localQueue, err = o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				localQueue = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = localQueue
+		}
+		if localQueue == nil {
+			continue
+		}
+	}
+
+	return localQueues, nil
+}
+
+func (o *WorkloadOptions) pendingWorkloads(ctx context.Context, list *v1beta1.WorkloadList, localQueues map[string]*v1beta1.LocalQueue) (map[string]*v1alpha1.PendingWorkload, error) {
+	var err error
+
+	pendingWorkloads := make(map[string]*v1alpha1.PendingWorkload)
+	pendingWorkloadsSummaries := make(map[string]*v1alpha1.PendingWorkloadsSummary)
+
+	for _, wl := range list.Items {
+		if !workloadPending(&wl) {
+			continue
+		}
+		var clusterQueueName string
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+		} else if lq := localQueues[localQueueKeyForWorkload(&wl)]; lq != nil {
+			clusterQueueName = string(lq.Spec.ClusterQueue)
+		}
+		pendingWorkloadsSummary, ok := pendingWorkloadsSummaries[clusterQueueName]
+		if !ok {
+			pendingWorkloadsSummary, err = o.ClientSet.VisibilityV1alpha1().ClusterQueues().
+				GetPendingWorkloadsSummary(ctx, clusterQueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				pendingWorkloadsSummary = nil
+			}
+			pendingWorkloadsSummaries[clusterQueueName] = pendingWorkloadsSummary
+		}
+		if pendingWorkloadsSummary == nil {
+			continue
+		}
+		for _, pendingWorkload := range pendingWorkloadsSummary.Items {
+			if pendingWorkload.Name == wl.Name && pendingWorkload.Namespace == wl.Namespace {
+				pendingWorkloads[workload.Key(&wl)] = &pendingWorkload
+			}
+		}
+	}
+
+	return pendingWorkloads, nil
+}
+
+func (o *WorkloadOptions) apiResources(list *v1beta1.WorkloadList) (map[string]*metav1.APIResourceList, error) {
+	var err error
+
+	apiResourceLists := make(map[string]*metav1.APIResourceList)
+
+	for _, wl := range list.Items {
+		for _, ref := range wl.ObjectMeta.OwnerReferences {
+			rl, ok := apiResourceLists[ref.APIVersion]
+			if !ok {
+				rl, err = o.ClientSet.Discovery().ServerResourcesForGroupVersion(ref.APIVersion)
+				if client.IgnoreNotFound(err) != nil {
+					return nil, err
+				}
+				if err != nil {
+					rl = nil
+				}
+				apiResourceLists[ref.APIVersion] = rl
+			}
+			if rl == nil {
+				continue
+			}
```

  Thank you. Fixed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-16T18:32:28Z

- **cmd/kueuectl/app/list/list_workload.go:322** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-16T18:32:27Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		localQueue, ok := localQueues[localQueueKeyForWorkload(&wl)]
+		if !ok {
+			localQueue, err = o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				localQueue = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = localQueue
+		}
+		if localQueue == nil {
+			continue
+		}
+	}
+
+	return localQueues, nil
+}
+
+func (o *WorkloadOptions) pendingWorkloads(ctx context.Context, list *v1beta1.WorkloadList, localQueues map[string]*v1beta1.LocalQueue) (map[string]*v1alpha1.PendingWorkload, error) {
+	var err error
+
+	pendingWorkloads := make(map[string]*v1alpha1.PendingWorkload)
+	pendingWorkloadsSummaries := make(map[string]*v1alpha1.PendingWorkloadsSummary)
+
+	for _, wl := range list.Items {
+		if !workloadPending(&wl) {
+			continue
+		}
+		var clusterQueueName string
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+		} else if lq := localQueues[localQueueKeyForWorkload(&wl)]; lq != nil {
+			clusterQueueName = string(lq.Spec.ClusterQueue)
+		}
+		pendingWorkloadsSummary, ok := pendingWorkloadsSummaries[clusterQueueName]
+		if !ok {
+			pendingWorkloadsSummary, err = o.ClientSet.VisibilityV1alpha1().ClusterQueues().
+				GetPendingWorkloadsSummary(ctx, clusterQueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				pendingWorkloadsSummary = nil
+			}
+			pendingWorkloadsSummaries[clusterQueueName] = pendingWorkloadsSummary
+		}
+		if pendingWorkloadsSummary == nil {
+			continue
+		}
+		for _, pendingWorkload := range pendingWorkloadsSummary.Items {
+			if pendingWorkload.Name == wl.Name && pendingWorkload.Namespace == wl.Namespace {
+				pendingWorkloads[workload.Key(&wl)] = &pendingWorkload
+			}
+		}
+	}
+
+	return pendingWorkloads, nil
+}
+
+func (o *WorkloadOptions) apiResources(list *v1beta1.WorkloadList) (map[string]*metav1.APIResourceList, error) {
+	var err error
+
+	apiResourceLists := make(map[string]*metav1.APIResourceList)
+
+	for _, wl := range list.Items {
+		for _, ref := range wl.ObjectMeta.OwnerReferences {
+			rl, ok := apiResourceLists[ref.APIVersion]
+			if !ok {
+				rl, err = o.ClientSet.Discovery().ServerResourcesForGroupVersion(ref.APIVersion)
+				if client.IgnoreNotFound(err) != nil {
+					return nil, err
+				}
+				if err != nil {
+					rl = nil
+				}
+				apiResourceLists[ref.APIVersion] = rl
+			}
+			if rl == nil {
+				continue
+			}
+		}
+	}
+
+	return apiResourceLists, nil
+}
+
+func workloadPending(wl *v1beta1.Workload) bool {
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	return cond != nil && cond.Type == v1beta1.WorkloadQuotaReserved && cond.Status == metav1.ConditionFalse && cond.Reason == "Pending"
```

  Thanks.

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-17T10:49:17Z

- **cmd/kueuectl/app/list/list_workload.go:75** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-17T10:42:50Z
```diff
@@ -0,0 +1,311 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
```

  I think we're missing to indicate the fieldselector and labelselector flags.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-17T10:58:01Z

- **cmd/kueuectl/app/list/list_workload.go:75** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-17T10:58:01Z
```diff
@@ -0,0 +1,311 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
```

  Thanks. Added.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-20T06:00:31Z

- **cmd/kueuectl/app/list/list_workload.go:270** by [@trasc](https://github.com/trasc) — 2024-05-20T06:00:31Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
```

  ok for now

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-20T06:01:05Z

- **cmd/kueuectl/app/list/list_workload.go:303** by [@trasc](https://github.com/trasc) — 2024-05-20T06:01:05Z
```diff
@@ -0,0 +1,327 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	var err error
+
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		localQueue, ok := localQueues[localQueueKeyForWorkload(&wl)]
+		if !ok {
+			localQueue, err = o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				localQueue = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = localQueue
+		}
+		if localQueue == nil {
+			continue
+		}
+	}
+
+	return localQueues, nil
+}
+
+func (o *WorkloadOptions) pendingWorkloads(ctx context.Context, list *v1beta1.WorkloadList, localQueues map[string]*v1beta1.LocalQueue) (map[string]*v1alpha1.PendingWorkload, error) {
+	var err error
+
+	pendingWorkloads := make(map[string]*v1alpha1.PendingWorkload)
+	pendingWorkloadsSummaries := make(map[string]*v1alpha1.PendingWorkloadsSummary)
+
+	for _, wl := range list.Items {
+		if !workloadPending(&wl) {
+			continue
+		}
+		var clusterQueueName string
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+		} else if lq := localQueues[localQueueKeyForWorkload(&wl)]; lq != nil {
+			clusterQueueName = string(lq.Spec.ClusterQueue)
+		}
```

  ok for now

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-20T06:01:29Z

/lgtm

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-20T17:47:20Z

- **cmd/kueuectl/app/list/list_workload.go:159** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-20T17:24:06Z
```diff
@@ -0,0 +1,311 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		} else {
+			printer, err := o.PrintFlags.ToPrinter()
+			if err != nil {
+				return nil, err
+			}
+			return printer.PrintObj, nil
+		}
```

  ```suggestion
  		}
  		printer, err := o.PrintFlags.ToPrinter()
  		if err != nil {
  			return nil, err
  		}
  		return printer.PrintObj, nil
  ```
  
  This "else" could be eliminated.

- **cmd/kueuectl/app/list/list_workload_printer.go:143** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-20T17:33:49Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
+			{Name: "Position in Queue", Type: "string"},
+			{Name: "Age", Type: "string"},
+		},
+		Rows: p.printWorkloadList(list),
+	}
+
+	return printer.PrintObj(table, out)
+}
+
+func (p *listWorkloadPrinter) WithNamespace() *listWorkloadPrinter {
+	p.printOptions.WithNamespace = true
+	return p
+}
+
+func (p *listWorkloadPrinter) WithResources(r *listWorkloadResources) *listWorkloadPrinter {
+	if r == nil {
+		r = newListWorkloadResources()
+	}
+	p.resources = r
+	return p
+}
+
+func newWorkloadTablePrinter() *listWorkloadPrinter {
+	return &listWorkloadPrinter{
+		resources: newListWorkloadResources(),
+	}
+}
+
+func (p *listWorkloadPrinter) printWorkloadList(list *v1beta1.WorkloadList) []metav1.TableRow {
+	rows := make([]metav1.TableRow, len(list.Items))
+	for index := range list.Items {
+		rows[index] = p.printWorkload(&list.Items[index])
+	}
+	return rows
+}
+
+func (p *listWorkloadPrinter) printWorkload(wl *v1beta1.Workload) metav1.TableRow {
+	row := metav1.TableRow{
+		Object: runtime.RawExtension{Object: wl},
+	}
+
+	var clusterQueueName string
+	if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+		clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+	} else if lq := p.resources.localQueues[localQueueKeyForWorkload(wl)]; lq != nil {
+		clusterQueueName = string(lq.Spec.ClusterQueue)
+	}
+
+	var positionInQueue string
+	if pendingWorkload, ok := p.resources.pendingWorkloads[workload.Key(wl)]; ok {
+		positionInQueue = fmt.Sprintf("%d", pendingWorkload.PositionInLocalQueue)
+	}
+
+	row.Cells = []any{
+		wl.Name,
+		strings.Join(p.crdTypes(wl), ", "),
+		strings.Join(p.crdNames(wl), ", "),
+		wl.Spec.QueueName,
+		clusterQueueName,
+		strings.ToUpper(workload.Status(wl)),
+		positionInQueue,
+		duration.HumanDuration(time.Since(wl.CreationTimestamp.Time)),
+	}
+
+	return row
+}
+
+func (p *listWorkloadPrinter) crdTypes(wl *v1beta1.Workload) []string {
+	crdTypes := make(map[string]struct{}, len(wl.ObjectMeta.OwnerReferences))
```

  ```suggestion
  	crdTypes := sets.New[string]()
  ```
  
  I would recommend using sets library to avoid maintaining such that map.

- **cmd/kueuectl/app/list/list_workload_printer.go:166** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-20T17:34:59Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
+			{Name: "Position in Queue", Type: "string"},
+			{Name: "Age", Type: "string"},
+		},
+		Rows: p.printWorkloadList(list),
+	}
+
+	return printer.PrintObj(table, out)
+}
+
+func (p *listWorkloadPrinter) WithNamespace() *listWorkloadPrinter {
+	p.printOptions.WithNamespace = true
+	return p
+}
+
+func (p *listWorkloadPrinter) WithResources(r *listWorkloadResources) *listWorkloadPrinter {
+	if r == nil {
+		r = newListWorkloadResources()
+	}
+	p.resources = r
+	return p
+}
+
+func newWorkloadTablePrinter() *listWorkloadPrinter {
+	return &listWorkloadPrinter{
+		resources: newListWorkloadResources(),
+	}
+}
+
+func (p *listWorkloadPrinter) printWorkloadList(list *v1beta1.WorkloadList) []metav1.TableRow {
+	rows := make([]metav1.TableRow, len(list.Items))
+	for index := range list.Items {
+		rows[index] = p.printWorkload(&list.Items[index])
+	}
+	return rows
+}
+
+func (p *listWorkloadPrinter) printWorkload(wl *v1beta1.Workload) metav1.TableRow {
+	row := metav1.TableRow{
+		Object: runtime.RawExtension{Object: wl},
+	}
+
+	var clusterQueueName string
+	if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+		clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+	} else if lq := p.resources.localQueues[localQueueKeyForWorkload(wl)]; lq != nil {
+		clusterQueueName = string(lq.Spec.ClusterQueue)
+	}
+
+	var positionInQueue string
+	if pendingWorkload, ok := p.resources.pendingWorkloads[workload.Key(wl)]; ok {
+		positionInQueue = fmt.Sprintf("%d", pendingWorkload.PositionInLocalQueue)
+	}
+
+	row.Cells = []any{
+		wl.Name,
+		strings.Join(p.crdTypes(wl), ", "),
+		strings.Join(p.crdNames(wl), ", "),
+		wl.Spec.QueueName,
+		clusterQueueName,
+		strings.ToUpper(workload.Status(wl)),
+		positionInQueue,
+		duration.HumanDuration(time.Since(wl.CreationTimestamp.Time)),
+	}
+
+	return row
+}
+
+func (p *listWorkloadPrinter) crdTypes(wl *v1beta1.Workload) []string {
+	crdTypes := make(map[string]struct{}, len(wl.ObjectMeta.OwnerReferences))
+
+	for _, ref := range wl.ObjectMeta.OwnerReferences {
+		var apiResource *metav1.APIResource
+
+		if resourceList := p.resources.apiResourceLists[ref.APIVersion]; resourceList != nil {
+			for _, r := range resourceList.APIResources {
+				if r.Kind == ref.Kind && len(r.SingularName) > 0 {
+					apiResource = &r
+					break
+				}
+			}
+		}
+
+		if apiResource != nil {
+			crdTypes[p.apiResourceType(apiResource)] = struct{}{}
+		}
+	}
+
+	return maps.Keys(crdTypes)
+}
+
+func (p *listWorkloadPrinter) crdNames(wl *v1beta1.Workload) []string {
+	crdNames := make(map[string]struct{}, len(wl.ObjectMeta.OwnerReferences))
```

  ditto

- **cmd/kueuectl/app/testing/fake.go:12** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-20T17:38:47Z
```diff
@@ -9,6 +9,8 @@ import (
 	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
 )
 
+const DefaultNamespace = "default"
```

  ```suggestion
  ```
  
  Could you use the metav1.DefaultNamespace instead of this?
  
  https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#pkg-constants

- **cmd/kueuectl/app/list/list_workload_test.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-20T17:40:10Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
```

  Could we use the fake time instead of real time?

- **cmd/kueuectl/app/list/list_workload_test.go:57** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-20T17:41:07Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns               string
+		apiResourceLists []*metav1.APIResourceList
+		pendingWorkloads []v1alpha1.PendingWorkload
+		objs             []runtime.Object
+		args             []string
+		wantOut          string
+		wantOutErr       string
+		wantErr          error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
```

  ```suggestion
  					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
  ```
  
  Let's use the constant.

- **cmd/kueuectl/app/list/list_workload_test.go:64** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-20T17:42:01Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns               string
+		apiResourceLists []*metav1.APIResourceList
+		pendingWorkloads []v1alpha1.PendingWorkload
+		objs             []runtime.Object
+		args             []string
+		wantOut          string
+		wantOutErr       string
+		wantErr          error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
```

  ```suggestion
  					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
  ```

- **cmd/kueuectl/app/list/list_workload_test.go:283** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-20T17:44:15Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns               string
+		apiResourceLists []*metav1.APIResourceList
+		pendingWorkloads []v1alpha1.PendingWorkload
+		objs             []runtime.Object
+		args             []string
+		wantOut          string
+		wantOutErr       string
+		wantErr          error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter": {
+			args: []string{"--localqueue", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter (short flag)": {
+			args: []string{"-q", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter": {
+			args: []string{"--clusterqueue", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter (short flag)": {
+			args: []string{"-c", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with only pending filter": {
+			args: []string{"--only-pending"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print workload list with only admitted filter": {
+			args: []string{"--only-admitted"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionTrue,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS     POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            ADMITTED                       60m
+`,
+		},
+		"should print workload list with label selector filter": {
+			args: []string{"--selector", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with label selector filter (short flag)": {
+			args: []string{"-l", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with CRD types": {
+			args: []string{},
```

  ```suggestion
  ```
  
  What is the reason that we should say an empty slice?

- **site/content/en/docs/reference/kubectl-kueue/commands/list.md:26** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-20T17:45:07Z
```diff
@@ -18,15 +18,20 @@ kubectl kueue list [TYPE]
 ```bash
 # List cluster queues
 kubectl kueue list clusterqueue
-# List workloads
+
+# List local queues
 kubectl kueue list localqueue my-local-queue
+
+# List workloads
+kubectl kueue list workload my-local-queue
```

  ```suggestion
  kubectl kueue list workload --localqueue my-local-queue
  ```
  
  ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-20T18:37:24Z

- **cmd/kueuectl/app/list/list_workload_test.go:43** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-20T18:37:24Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
```

  Do you mean this one `func NewFakeClock(t time.Time)`?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-20T18:42:52Z

- **site/content/en/docs/reference/kubectl-kueue/commands/list.md:26** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-20T18:42:52Z
```diff
@@ -18,15 +18,20 @@ kubectl kueue list [TYPE]
 ```bash
 # List cluster queues
 kubectl kueue list clusterqueue
-# List workloads
+
+# List local queues
 kubectl kueue list localqueue my-local-queue
+
+# List workloads
+kubectl kueue list workload my-local-queue
```

  Thanks. Fixed.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-20T18:43:01Z

- **cmd/kueuectl/app/list/list_workload_test.go:283** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-20T18:43:01Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns               string
+		apiResourceLists []*metav1.APIResourceList
+		pendingWorkloads []v1alpha1.PendingWorkload
+		objs             []runtime.Object
+		args             []string
+		wantOut          string
+		wantOutErr       string
+		wantErr          error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter": {
+			args: []string{"--localqueue", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter (short flag)": {
+			args: []string{"-q", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter": {
+			args: []string{"--clusterqueue", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter (short flag)": {
+			args: []string{"-c", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with only pending filter": {
+			args: []string{"--only-pending"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print workload list with only admitted filter": {
+			args: []string{"--only-admitted"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionTrue,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS     POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            ADMITTED                       60m
+`,
+		},
+		"should print workload list with label selector filter": {
+			args: []string{"--selector", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with label selector filter (short flag)": {
+			args: []string{"-l", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", cmdtesting.DefaultNamespace).
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   CRD TYPE   CRD NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with CRD types": {
+			args: []string{},
```

  Removed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-20T18:43:32Z

- **cmd/kueuectl/app/list/list_workload_test.go:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-20T18:43:32Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns               string
+		apiResourceLists []*metav1.APIResourceList
+		pendingWorkloads []v1alpha1.PendingWorkload
+		objs             []runtime.Object
+		args             []string
+		wantOut          string
+		wantOutErr       string
+		wantErr          error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(schema.GroupVersionKind{Group: "ray.io", Version: "v1", Kind: "RayJob"}, "j2", "test-uid").
```

  Thank you. It's very useful.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-20T18:43:49Z

- **cmd/kueuectl/app/list/list_workload_test.go:57** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-20T18:43:49Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns               string
+		apiResourceLists []*metav1.APIResourceList
+		pendingWorkloads []v1alpha1.PendingWorkload
+		objs             []runtime.Object
+		args             []string
+		wantOut          string
+		wantOutErr       string
+		wantErr          error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(schema.GroupVersionKind{Group: "batch", Version: "v1", Kind: "Job"}, "j1", "test-uid").
```

  Thanks

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-20T18:44:25Z

- **cmd/kueuectl/app/testing/fake.go:12** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-20T18:44:25Z
```diff
@@ -9,6 +9,8 @@ import (
 	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
 )
 
+const DefaultNamespace = "default"
```

  Good point. Thanks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-23T12:42:31Z

- **cmd/kueuectl/app/list/list_workload_printer.go:72** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-23T12:42:31Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
```

  @alculquicondor @mwielgus WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-23T15:28:14Z

- **cmd/kueuectl/app/list/list_workload_printer.go:72** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-23T15:28:13Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
```

  Let's try to be user friendly: Job type

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-24T03:59:22Z

- **cmd/kueuectl/app/list/list_workload_printer.go:72** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-24T03:55:47Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
```

  @mbobrovskyi Could you address this change?

- **cmd/kueuectl/app/list/list_workload_printer.go:159** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-24T03:57:30Z
```diff
@@ -0,0 +1,193 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
+			{Name: "Position in Queue", Type: "string"},
+			{Name: "Age", Type: "string"},
+		},
+		Rows: p.printWorkloadList(list),
+	}
+
+	return printer.PrintObj(table, out)
+}
+
+func (p *listWorkloadPrinter) WithNamespace() *listWorkloadPrinter {
+	p.printOptions.WithNamespace = true
+	return p
+}
+
+func (p *listWorkloadPrinter) WithResources(r *listWorkloadResources) *listWorkloadPrinter {
+	if r == nil {
+		r = newListWorkloadResources()
+	}
+	p.resources = r
+	return p
+}
+
+func newWorkloadTablePrinter() *listWorkloadPrinter {
+	return &listWorkloadPrinter{
+		resources: newListWorkloadResources(),
+	}
+}
+
+func (p *listWorkloadPrinter) printWorkloadList(list *v1beta1.WorkloadList) []metav1.TableRow {
+	rows := make([]metav1.TableRow, len(list.Items))
+	for index := range list.Items {
+		rows[index] = p.printWorkload(&list.Items[index])
+	}
+	return rows
+}
+
+func (p *listWorkloadPrinter) printWorkload(wl *v1beta1.Workload) metav1.TableRow {
+	row := metav1.TableRow{
+		Object: runtime.RawExtension{Object: wl},
+	}
+
+	var clusterQueueName string
+	if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+		clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+	} else if lq := p.resources.localQueues[localQueueKeyForWorkload(wl)]; lq != nil {
+		clusterQueueName = string(lq.Spec.ClusterQueue)
+	}
+
+	var positionInQueue string
+	if pendingWorkload, ok := p.resources.pendingWorkloads[workload.Key(wl)]; ok {
+		positionInQueue = fmt.Sprintf("%d", pendingWorkload.PositionInLocalQueue)
+	}
+
+	row.Cells = []any{
+		wl.Name,
+		strings.Join(p.crdTypes(wl), ", "),
+		strings.Join(p.crdNames(wl), ", "),
+		wl.Spec.QueueName,
+		clusterQueueName,
+		strings.ToUpper(workload.Status(wl)),
+		positionInQueue,
+		duration.HumanDuration(time.Since(wl.CreationTimestamp.Time)),
+	}
+
+	return row
+}
+
+func (p *listWorkloadPrinter) crdTypes(wl *v1beta1.Workload) []string {
+	crdTypes := sets.New[string]()
+
+	for _, ref := range wl.ObjectMeta.OwnerReferences {
+		var apiResource *metav1.APIResource
+
+		if resourceList := p.resources.apiResourceLists[ref.APIVersion]; resourceList != nil {
+			for _, r := range resourceList.APIResources {
+				if r.Kind == ref.Kind && len(r.SingularName) > 0 {
+					apiResource = &r
+					break
+				}
+			}
+		}
+
+		if apiResource != nil {
+			crdTypes[p.apiResourceType(apiResource)] = struct{}{}
```

  ```suggestion
  			crdTypes.Insert(p.apiResourceType(apiResource))
  ```

- **cmd/kueuectl/app/list/list_workload_printer.go:170** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-24T03:57:55Z
```diff
@@ -0,0 +1,193 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
+			{Name: "Position in Queue", Type: "string"},
+			{Name: "Age", Type: "string"},
+		},
+		Rows: p.printWorkloadList(list),
+	}
+
+	return printer.PrintObj(table, out)
+}
+
+func (p *listWorkloadPrinter) WithNamespace() *listWorkloadPrinter {
+	p.printOptions.WithNamespace = true
+	return p
+}
+
+func (p *listWorkloadPrinter) WithResources(r *listWorkloadResources) *listWorkloadPrinter {
+	if r == nil {
+		r = newListWorkloadResources()
+	}
+	p.resources = r
+	return p
+}
+
+func newWorkloadTablePrinter() *listWorkloadPrinter {
+	return &listWorkloadPrinter{
+		resources: newListWorkloadResources(),
+	}
+}
+
+func (p *listWorkloadPrinter) printWorkloadList(list *v1beta1.WorkloadList) []metav1.TableRow {
+	rows := make([]metav1.TableRow, len(list.Items))
+	for index := range list.Items {
+		rows[index] = p.printWorkload(&list.Items[index])
+	}
+	return rows
+}
+
+func (p *listWorkloadPrinter) printWorkload(wl *v1beta1.Workload) metav1.TableRow {
+	row := metav1.TableRow{
+		Object: runtime.RawExtension{Object: wl},
+	}
+
+	var clusterQueueName string
+	if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+		clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+	} else if lq := p.resources.localQueues[localQueueKeyForWorkload(wl)]; lq != nil {
+		clusterQueueName = string(lq.Spec.ClusterQueue)
+	}
+
+	var positionInQueue string
+	if pendingWorkload, ok := p.resources.pendingWorkloads[workload.Key(wl)]; ok {
+		positionInQueue = fmt.Sprintf("%d", pendingWorkload.PositionInLocalQueue)
+	}
+
+	row.Cells = []any{
+		wl.Name,
+		strings.Join(p.crdTypes(wl), ", "),
+		strings.Join(p.crdNames(wl), ", "),
+		wl.Spec.QueueName,
+		clusterQueueName,
+		strings.ToUpper(workload.Status(wl)),
+		positionInQueue,
+		duration.HumanDuration(time.Since(wl.CreationTimestamp.Time)),
+	}
+
+	return row
+}
+
+func (p *listWorkloadPrinter) crdTypes(wl *v1beta1.Workload) []string {
+	crdTypes := sets.New[string]()
+
+	for _, ref := range wl.ObjectMeta.OwnerReferences {
+		var apiResource *metav1.APIResource
+
+		if resourceList := p.resources.apiResourceLists[ref.APIVersion]; resourceList != nil {
+			for _, r := range resourceList.APIResources {
+				if r.Kind == ref.Kind && len(r.SingularName) > 0 {
+					apiResource = &r
+					break
+				}
+			}
+		}
+
+		if apiResource != nil {
+			crdTypes[p.apiResourceType(apiResource)] = struct{}{}
+		}
+	}
+
+	return maps.Keys(crdTypes)
+}
+
+func (p *listWorkloadPrinter) crdNames(wl *v1beta1.Workload) []string {
+	crdNames := sets.New[string]()
+
+	for _, ref := range wl.ObjectMeta.OwnerReferences {
+		crdNames[ref.Name] = struct{}{}
```

  ```suggestion
  		crdNames.Insert(ref.Name)
  ```

- **cmd/kueuectl/app/list/list_workload_test.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-24T03:58:44Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
```

  I meant that couldn't we introduce the same approach as controllers like this:
  
  - https://github.com/kubernetes-sigs/kueue/blob/4b9744b29bec33bbd34e01a66f09002b478a6493/pkg/controller/core/workload_controller.go#L117
  - https://github.com/kubernetes-sigs/kueue/blob/4b9744b29bec33bbd34e01a66f09002b478a6493/pkg/controller/core/workload_controller_test.go#L807
  
  But I'm ok with follow-up.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T05:16:11Z

- **cmd/kueuectl/app/list/list_workload_printer.go:72** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T05:16:11Z
```diff
@@ -0,0 +1,192 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T05:16:56Z

- **cmd/kueuectl/app/list/list_workload_printer.go:170** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T05:16:56Z
```diff
@@ -0,0 +1,193 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
+			{Name: "Position in Queue", Type: "string"},
+			{Name: "Age", Type: "string"},
+		},
+		Rows: p.printWorkloadList(list),
+	}
+
+	return printer.PrintObj(table, out)
+}
+
+func (p *listWorkloadPrinter) WithNamespace() *listWorkloadPrinter {
+	p.printOptions.WithNamespace = true
+	return p
+}
+
+func (p *listWorkloadPrinter) WithResources(r *listWorkloadResources) *listWorkloadPrinter {
+	if r == nil {
+		r = newListWorkloadResources()
+	}
+	p.resources = r
+	return p
+}
+
+func newWorkloadTablePrinter() *listWorkloadPrinter {
+	return &listWorkloadPrinter{
+		resources: newListWorkloadResources(),
+	}
+}
+
+func (p *listWorkloadPrinter) printWorkloadList(list *v1beta1.WorkloadList) []metav1.TableRow {
+	rows := make([]metav1.TableRow, len(list.Items))
+	for index := range list.Items {
+		rows[index] = p.printWorkload(&list.Items[index])
+	}
+	return rows
+}
+
+func (p *listWorkloadPrinter) printWorkload(wl *v1beta1.Workload) metav1.TableRow {
+	row := metav1.TableRow{
+		Object: runtime.RawExtension{Object: wl},
+	}
+
+	var clusterQueueName string
+	if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+		clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+	} else if lq := p.resources.localQueues[localQueueKeyForWorkload(wl)]; lq != nil {
+		clusterQueueName = string(lq.Spec.ClusterQueue)
+	}
+
+	var positionInQueue string
+	if pendingWorkload, ok := p.resources.pendingWorkloads[workload.Key(wl)]; ok {
+		positionInQueue = fmt.Sprintf("%d", pendingWorkload.PositionInLocalQueue)
+	}
+
+	row.Cells = []any{
+		wl.Name,
+		strings.Join(p.crdTypes(wl), ", "),
+		strings.Join(p.crdNames(wl), ", "),
+		wl.Spec.QueueName,
+		clusterQueueName,
+		strings.ToUpper(workload.Status(wl)),
+		positionInQueue,
+		duration.HumanDuration(time.Since(wl.CreationTimestamp.Time)),
+	}
+
+	return row
+}
+
+func (p *listWorkloadPrinter) crdTypes(wl *v1beta1.Workload) []string {
+	crdTypes := sets.New[string]()
+
+	for _, ref := range wl.ObjectMeta.OwnerReferences {
+		var apiResource *metav1.APIResource
+
+		if resourceList := p.resources.apiResourceLists[ref.APIVersion]; resourceList != nil {
+			for _, r := range resourceList.APIResources {
+				if r.Kind == ref.Kind && len(r.SingularName) > 0 {
+					apiResource = &r
+					break
+				}
+			}
+		}
+
+		if apiResource != nil {
+			crdTypes[p.apiResourceType(apiResource)] = struct{}{}
+		}
+	}
+
+	return maps.Keys(crdTypes)
+}
+
+func (p *listWorkloadPrinter) crdNames(wl *v1beta1.Workload) []string {
+	crdNames := sets.New[string]()
+
+	for _, ref := range wl.ObjectMeta.OwnerReferences {
+		crdNames[ref.Name] = struct{}{}
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T05:17:06Z

- **cmd/kueuectl/app/list/list_workload_printer.go:159** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T05:17:06Z
```diff
@@ -0,0 +1,193 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return fmt.Errorf("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "CRD Type", Type: "string"},
+			{Name: "CRD Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
+			{Name: "Position in Queue", Type: "string"},
+			{Name: "Age", Type: "string"},
+		},
+		Rows: p.printWorkloadList(list),
+	}
+
+	return printer.PrintObj(table, out)
+}
+
+func (p *listWorkloadPrinter) WithNamespace() *listWorkloadPrinter {
+	p.printOptions.WithNamespace = true
+	return p
+}
+
+func (p *listWorkloadPrinter) WithResources(r *listWorkloadResources) *listWorkloadPrinter {
+	if r == nil {
+		r = newListWorkloadResources()
+	}
+	p.resources = r
+	return p
+}
+
+func newWorkloadTablePrinter() *listWorkloadPrinter {
+	return &listWorkloadPrinter{
+		resources: newListWorkloadResources(),
+	}
+}
+
+func (p *listWorkloadPrinter) printWorkloadList(list *v1beta1.WorkloadList) []metav1.TableRow {
+	rows := make([]metav1.TableRow, len(list.Items))
+	for index := range list.Items {
+		rows[index] = p.printWorkload(&list.Items[index])
+	}
+	return rows
+}
+
+func (p *listWorkloadPrinter) printWorkload(wl *v1beta1.Workload) metav1.TableRow {
+	row := metav1.TableRow{
+		Object: runtime.RawExtension{Object: wl},
+	}
+
+	var clusterQueueName string
+	if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+		clusterQueueName = string(wl.Status.Admission.ClusterQueue)
+	} else if lq := p.resources.localQueues[localQueueKeyForWorkload(wl)]; lq != nil {
+		clusterQueueName = string(lq.Spec.ClusterQueue)
+	}
+
+	var positionInQueue string
+	if pendingWorkload, ok := p.resources.pendingWorkloads[workload.Key(wl)]; ok {
+		positionInQueue = fmt.Sprintf("%d", pendingWorkload.PositionInLocalQueue)
+	}
+
+	row.Cells = []any{
+		wl.Name,
+		strings.Join(p.crdTypes(wl), ", "),
+		strings.Join(p.crdNames(wl), ", "),
+		wl.Spec.QueueName,
+		clusterQueueName,
+		strings.ToUpper(workload.Status(wl)),
+		positionInQueue,
+		duration.HumanDuration(time.Since(wl.CreationTimestamp.Time)),
+	}
+
+	return row
+}
+
+func (p *listWorkloadPrinter) crdTypes(wl *v1beta1.Workload) []string {
+	crdTypes := sets.New[string]()
+
+	for _, ref := range wl.ObjectMeta.OwnerReferences {
+		var apiResource *metav1.APIResource
+
+		if resourceList := p.resources.apiResourceLists[ref.APIVersion]; resourceList != nil {
+			for _, r := range resourceList.APIResources {
+				if r.Kind == ref.Kind && len(r.SingularName) > 0 {
+					apiResource = &r
+					break
+				}
+			}
+		}
+
+		if apiResource != nil {
+			crdTypes[p.apiResourceType(apiResource)] = struct{}{}
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T05:20:42Z

- **cmd/kueuectl/app/list/list_workload_test.go:43** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T05:20:41Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
```

  Let's make it in another PR.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-24T11:39:47Z

- **cmd/kueuectl/app/list/list_workload.go:75** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-24T11:39:47Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
```

  NIT: maybe make --only string slice flag to be able to specify multiple conditions eg. `--only Pending --only Admitted` or --only Pending,Admitted`
  If you think it's overhead, feel free to skip this comment

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-24T11:42:21Z

- **cmd/kueuectl/app/list/list_workload.go:212** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-24T11:42:21Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		}
+		printer, err := o.PrintFlags.ToPrinter()
+		if err != nil {
+			return nil, err
+		}
+		return printer.PrintObj, nil
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
```

  Isn't pending reason is available in API to use it as const?
  eg. `v1beta1.CheckStatePending`

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-24T11:46:50Z

- **cmd/kueuectl/app/list/list_workload.go:283** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-24T11:46:50Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		}
+		printer, err := o.PrintFlags.ToPrinter()
+		if err != nil {
+			return nil, err
+		}
+		return printer.PrintObj, nil
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		if _, ok := localQueues[localQueueKeyForWorkload(&wl)]; !ok {
+			lq, err := o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				lq = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = lq
+		}
```

  WDYT about first gathering all needed local queue names, and only then making list request?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T15:52:49Z

- **cmd/kueuectl/app/list/list_workload.go:75** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T15:52:49Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
```

  I think we haven't found final solution yet.
  https://github.com/kubernetes-sigs/kueue/pull/2093/files#r1583488370

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T15:52:59Z

- **cmd/kueuectl/app/list/list_workload.go:75** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T15:52:59Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
```

  As for me it's a good idea. 
  @alculquicondor @tenzen-y WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T16:01:42Z

- **cmd/kueuectl/app/list/list_workload.go:283** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T16:01:42Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		}
+		printer, err := o.PrintFlags.ToPrinter()
+		if err != nil {
+			return nil, err
+		}
+		return printer.PrintObj, nil
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		if _, ok := localQueues[localQueueKeyForWorkload(&wl)]; !ok {
+			lq, err := o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				lq = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = lq
+		}
```

  Why?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-24T18:08:31Z

- **cmd/kueuectl/app/list/list_workload.go:283** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-24T18:08:31Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		}
+		printer, err := o.PrintFlags.ToPrinter()
+		if err != nil {
+			return nil, err
+		}
+		return printer.PrintObj, nil
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		if _, ok := localQueues[localQueueKeyForWorkload(&wl)]; !ok {
+			lq, err := o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				lq = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = lq
+		}
```

  To have less requests

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T18:13:14Z

- **cmd/kueuectl/app/list/list_workload.go:283** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T18:13:14Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		}
+		printer, err := o.PrintFlags.ToPrinter()
+		if err != nil {
+			return nil, err
+		}
+		return printer.PrintObj, nil
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
+		return true
+	}
+	return false
+}
+
+func (o *WorkloadOptions) filterByOnlyAdmitted(wl *v1beta1.Workload) bool {
+	return !o.OnlyAdmitted || meta.IsStatusConditionTrue(wl.Status.Conditions, v1beta1.WorkloadAdmitted)
+}
+
+func (o *WorkloadOptions) localQueues(ctx context.Context, list *v1beta1.WorkloadList) (map[string]*v1beta1.LocalQueue, error) {
+	localQueues := make(map[string]*v1beta1.LocalQueue)
+	for _, wl := range list.Items {
+		// It's not necessary to get localqueue if we have clusterqueue name on Admission status.
+		if wl.Status.Admission != nil && len(wl.Status.Admission.ClusterQueue) > 0 {
+			continue
+		}
+		if _, ok := localQueues[localQueueKeyForWorkload(&wl)]; !ok {
+			lq, err := o.ClientSet.KueueV1beta1().LocalQueues(wl.Namespace).Get(ctx, wl.Spec.QueueName, metav1.GetOptions{})
+			if client.IgnoreNotFound(err) != nil {
+				return nil, err
+			}
+			if err != nil {
+				lq = nil
+			}
+			localQueues[localQueueKeyForWorkload(&wl)] = lq
+		}
```

  It will the same. We are requesting only unique LQs.
  ```go
  if err != nil {
      lq = nil
  }
  localQueues[localQueueKeyForWorkload(&wl)] = lq
  ```
  
  So on the next iteration it will found nil.
  ```go
  if _, ok := localQueues[localQueueKeyForWorkload(&wl)]; !ok {
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-27T06:05:00Z

- **cmd/kueuectl/app/list/list_workload.go:212** by [@trasc](https://github.com/trasc) — 2024-05-27T06:05:00Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		}
+		printer, err := o.PrintFlags.ToPrinter()
+		if err != nil {
+			return nil, err
+		}
+		return printer.PrintObj, nil
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
```

  It's just a coincidence, `CheckStatePending` is an admission check state.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-27T07:20:07Z

- **cmd/kueuectl/app/list/list_workload.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-27T07:20:07Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
```

  IMO, `--condition Pending|Admitted` or `--phase Pending|Admitted` might be better.
  @alculquicondor WDYT?
  @mwielgus Please let us know what you assumed at the design stage.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-27T09:02:28Z

- **cmd/kueuectl/app/list/list_workload.go:212** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-27T09:02:28Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		}
+		printer, err := o.PrintFlags.ToPrinter()
+		if err != nil {
+			return nil, err
+		}
+		return printer.PrintObj, nil
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
```

  Then `workload.StatusPending`, but it's not in API for now, maybe it should be moved there

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-27T17:29:51Z

- **cmd/kueuectl/app/list/list_workload.go:75** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T17:29:51Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
```

  I like `--status`, to match the name of the column. For completeness, it can also be `All` or `Finished`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-27T17:47:26Z

- **cmd/kueuectl/app/list/list_workload.go:212** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T17:33:45Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		}
+		printer, err := o.PrintFlags.ToPrinter()
+		if err != nil {
+			return nil, err
+		}
+		return printer.PrintObj, nil
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
```

  This filter should return the workloads that are not Admitted or Finished. They might be missing the Pending reason.

- **cmd/kueuectl/app/list/list_workload_test.go:460** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T17:43:54Z
```diff
@@ -0,0 +1,471 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	batchv1 "k8s.io/api/batch/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns               string
+		apiResourceLists []*metav1.APIResourceList
+		pendingWorkloads []v1alpha1.PendingWorkload
+		objs             []runtime.Object
+		args             []string
+		wantOut          string
+		wantOutErr       string
+		wantErr          error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter": {
+			args: []string{"--localqueue", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter (short flag)": {
+			args: []string{"-q", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter": {
+			args: []string{"--clusterqueue", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter (short flag)": {
+			args: []string{"-c", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with only pending filter": {
+			args: []string{"--only-pending"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print workload list with only admitted filter": {
+			args: []string{"--only-admitted"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionTrue,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS     POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            ADMITTED                       60m
+`,
+		},
+		"should print workload list with label selector filter": {
+			args: []string{"--selector", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with label selector filter (short flag)": {
+			args: []string{"-l", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with Job types": {
+			apiResourceLists: []*metav1.APIResourceList{
+				{
+					GroupVersion: "batch/v1",
+					APIResources: []metav1.APIResource{
+						{
+							SingularName: "job",
+							Kind:         "Job",
+							Group:        "",
+						},
+					},
+				},
+				{
+					GroupVersion: "ray.io/v1",
+					APIResources: []metav1.APIResource{
+						{
+							SingularName: "rayjob",
+							Kind:         "RayJob",
+							Group:        "ray.io",
+						},
+					},
+				}, {
+					GroupVersion: "kubeflow.org/v1",
+					APIResources: []metav1.APIResource{
+						{
+							SingularName: "pytorchjob",
+							Kind:         "PyTorchJob",
+							Group:        "kubeflow.org",
+						},
+					},
+				},
+			},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl3", metav1.NamespaceDefault).
+					OwnerReference(kftraining.SchemeGroupVersion.WithKind(kftraining.PyTorchJobKind), "j3", "test-uid").
+					Queue("lq3").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq3").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-3 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE                  JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1    job                       j1         lq1          cq1            PENDING                       60m
+wl2    rayjob.ray.io             j2         lq2          cq2            PENDING                       120m
+wl3    pytorchjob.kubeflow....   j3         lq3          cq3            PENDING                       3h
+`,
+		},
+		"should print workload list with position in queue": {
+			pendingWorkloads: []v1alpha1.PendingWorkload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "wl1",
+						Namespace: "default",
+					},
+					Priority:               10,
+					LocalQueueName:         "lq1",
+					PositionInClusterQueue: 11,
+					PositionInLocalQueue:   12,
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "wl2",
+						Namespace: "default",
+					},
+					Priority:               20,
+					LocalQueueName:         "lq2",
+					PositionInClusterQueue: 21,
+					PositionInLocalQueue:   22,
+				},
+			},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING   12                  60m
+wl2               j2         lq2          cq2            PENDING   22                  120m
+`,
+		},
+		"should print not found error": {
+			wantOutErr: fmt.Sprintf("No resources found in %s namespace.\n", metav1.NamespaceDefault),
+		},
+		"should print not found error with all-namespaces filter": {
+			args:       []string{"-A"},
+			wantOutErr: "No resources found\n",
+		},
+	}
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			streams, _, out, outErr := genericiooptions.NewTestIOStreams()
+
+			tf := cmdtesting.NewTestClientGetter()
+			if len(tc.ns) > 0 {
+				tf.WithNamespace(tc.ns)
+			}
+
+			clientset := fake.NewSimpleClientset(tc.objs...)
+
+			// `SimpleClientset` not allow to add `PendingWorkloadsSummary` objects,
+			// because of `PendingWorkload` resources not implement `runtime.Object`.
+			// Default `Reaction` handle all verbs and resources, so need to add on
+			// head of chain.
+			clientset.Fake.ReactionChain = append([]kubetesting.Reactor{
+				&kubetesting.SimpleReactor{
+					Verb:     "get",
+					Resource: "clusterqueues",
+					Reaction: func(action kubetesting.Action) (handled bool, ret runtime.Object, err error) {
+						obj := &v1alpha1.PendingWorkloadsSummary{Items: tc.pendingWorkloads}
+						return true, obj, err
+					},
+				},
+			}, clientset.Fake.ReactionChain...)
+
+			tf.ClientSet = clientset
+			tf.ClientSet.Discovery().(*fakediscovery.FakeDiscovery).Resources = tc.apiResourceLists
+
+			cmd := NewWorkloadCmd(tf, streams)
+			cmd.SetArgs(tc.args)
+
+			gotErr := cmd.Execute()
+			if diff := cmp.Diff(tc.wantErr, gotErr, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("Unexpected error (-want/+got)\n%s", diff)
+			}
+
+			gotOut := out.String()
+			fmt.Println(gotOut)
```

  ```suggestion
  ```

- **cmd/kueuectl/app/list/list_workload_test.go:460** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T17:44:07Z
```diff
@@ -0,0 +1,471 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	batchv1 "k8s.io/api/batch/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns               string
+		apiResourceLists []*metav1.APIResourceList
+		pendingWorkloads []v1alpha1.PendingWorkload
+		objs             []runtime.Object
+		args             []string
+		wantOut          string
+		wantOutErr       string
+		wantErr          error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter": {
+			args: []string{"--localqueue", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter (short flag)": {
+			args: []string{"-q", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter": {
+			args: []string{"--clusterqueue", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter (short flag)": {
+			args: []string{"-c", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with only pending filter": {
+			args: []string{"--only-pending"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print workload list with only admitted filter": {
+			args: []string{"--only-admitted"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionTrue,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS     POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            ADMITTED                       60m
+`,
+		},
+		"should print workload list with label selector filter": {
+			args: []string{"--selector", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with label selector filter (short flag)": {
+			args: []string{"-l", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with Job types": {
+			apiResourceLists: []*metav1.APIResourceList{
+				{
+					GroupVersion: "batch/v1",
+					APIResources: []metav1.APIResource{
+						{
+							SingularName: "job",
+							Kind:         "Job",
+							Group:        "",
+						},
+					},
+				},
+				{
+					GroupVersion: "ray.io/v1",
+					APIResources: []metav1.APIResource{
+						{
+							SingularName: "rayjob",
+							Kind:         "RayJob",
+							Group:        "ray.io",
+						},
+					},
+				}, {
+					GroupVersion: "kubeflow.org/v1",
+					APIResources: []metav1.APIResource{
+						{
+							SingularName: "pytorchjob",
+							Kind:         "PyTorchJob",
+							Group:        "kubeflow.org",
+						},
+					},
+				},
+			},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl3", metav1.NamespaceDefault).
+					OwnerReference(kftraining.SchemeGroupVersion.WithKind(kftraining.PyTorchJobKind), "j3", "test-uid").
+					Queue("lq3").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq3").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-3 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE                  JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1    job                       j1         lq1          cq1            PENDING                       60m
+wl2    rayjob.ray.io             j2         lq2          cq2            PENDING                       120m
+wl3    pytorchjob.kubeflow....   j3         lq3          cq3            PENDING                       3h
+`,
+		},
+		"should print workload list with position in queue": {
+			pendingWorkloads: []v1alpha1.PendingWorkload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "wl1",
+						Namespace: "default",
+					},
+					Priority:               10,
+					LocalQueueName:         "lq1",
+					PositionInClusterQueue: 11,
+					PositionInLocalQueue:   12,
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "wl2",
+						Namespace: "default",
+					},
+					Priority:               20,
+					LocalQueueName:         "lq2",
+					PositionInClusterQueue: 21,
+					PositionInLocalQueue:   22,
+				},
+			},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING   12                  60m
+wl2               j2         lq2          cq2            PENDING   22                  120m
+`,
+		},
+		"should print not found error": {
+			wantOutErr: fmt.Sprintf("No resources found in %s namespace.\n", metav1.NamespaceDefault),
+		},
+		"should print not found error with all-namespaces filter": {
+			args:       []string{"-A"},
+			wantOutErr: "No resources found\n",
+		},
+	}
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			streams, _, out, outErr := genericiooptions.NewTestIOStreams()
+
+			tf := cmdtesting.NewTestClientGetter()
+			if len(tc.ns) > 0 {
+				tf.WithNamespace(tc.ns)
+			}
+
+			clientset := fake.NewSimpleClientset(tc.objs...)
+
+			// `SimpleClientset` not allow to add `PendingWorkloadsSummary` objects,
+			// because of `PendingWorkload` resources not implement `runtime.Object`.
+			// Default `Reaction` handle all verbs and resources, so need to add on
+			// head of chain.
+			clientset.Fake.ReactionChain = append([]kubetesting.Reactor{
+				&kubetesting.SimpleReactor{
+					Verb:     "get",
+					Resource: "clusterqueues",
+					Reaction: func(action kubetesting.Action) (handled bool, ret runtime.Object, err error) {
+						obj := &v1alpha1.PendingWorkloadsSummary{Items: tc.pendingWorkloads}
+						return true, obj, err
+					},
+				},
+			}, clientset.Fake.ReactionChain...)
+
+			tf.ClientSet = clientset
+			tf.ClientSet.Discovery().(*fakediscovery.FakeDiscovery).Resources = tc.apiResourceLists
+
+			cmd := NewWorkloadCmd(tf, streams)
+			cmd.SetArgs(tc.args)
+
+			gotErr := cmd.Execute()
+			if diff := cmp.Diff(tc.wantErr, gotErr, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("Unexpected error (-want/+got)\n%s", diff)
+			}
+
+			gotOut := out.String()
+			fmt.Println(gotOut)
```

  Or you can use `t.Log`, if really necessary.

- **cmd/kueuectl/app/list/list_workload_printer.go:75** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T17:46:16Z
```diff
@@ -0,0 +1,193 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"errors"
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return errors.New("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "Job Type", Type: "string"},
+			{Name: "Job Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
```

  ```suggestion
  			{Name: "Status", Type: "string"},
  ```
  ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T19:24:29Z

- **cmd/kueuectl/app/list/list_workload_printer.go:75** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T19:24:29Z
```diff
@@ -0,0 +1,193 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"errors"
+	"fmt"
+	"io"
+	"strings"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/util/duration"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/cli-runtime/pkg/printers"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const crdTypeMaxLength = 20
+
+type listWorkloadResources struct {
+	localQueues      map[string]*v1beta1.LocalQueue
+	pendingWorkloads map[string]*v1alpha1.PendingWorkload
+	apiResourceLists map[string]*metav1.APIResourceList
+}
+
+func newListWorkloadResources() *listWorkloadResources {
+	return &listWorkloadResources{
+		localQueues:      make(map[string]*v1beta1.LocalQueue),
+		pendingWorkloads: make(map[string]*v1alpha1.PendingWorkload),
+		apiResourceLists: make(map[string]*metav1.APIResourceList),
+	}
+}
+
+type listWorkloadPrinter struct {
+	printOptions printers.PrintOptions
+	resources    *listWorkloadResources
+}
+
+var _ printers.ResourcePrinter = (*listWorkloadPrinter)(nil)
+
+func (p *listWorkloadPrinter) PrintObj(obj runtime.Object, out io.Writer) error {
+	printer := printers.NewTablePrinter(p.printOptions)
+
+	list, ok := obj.(*v1beta1.WorkloadList)
+	if !ok {
+		return errors.New("invalid object type")
+	}
+
+	table := &metav1.Table{
+		ColumnDefinitions: []metav1.TableColumnDefinition{
+			{Name: "Name", Type: "string", Format: "name"},
+			{Name: "Job Type", Type: "string"},
+			{Name: "Job Name", Type: "string"},
+			{Name: "LocalQueue", Type: "string"},
+			{Name: "ClusterQueue", Type: "string"},
+			{Name: "Status", Type: "bool"},
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T19:24:49Z

- **cmd/kueuectl/app/list/list_workload_test.go:460** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T19:24:49Z
```diff
@@ -0,0 +1,471 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	rayv1 "github.com/ray-project/kuberay/ray-operator/apis/ray/v1"
+	batchv1 "k8s.io/api/batch/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
+
+	testCases := map[string]struct {
+		ns               string
+		apiResourceLists []*metav1.APIResourceList
+		pendingWorkloads []v1alpha1.PendingWorkload
+		objs             []runtime.Object
+		args             []string
+		wantOut          string
+		wantOutErr       string
+		wantErr          error
+	}{
+		"should print workload list with namespace filter": {
+			ns: "ns1",
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns2").
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter": {
+			args: []string{"--localqueue", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with localqueue filter (short flag)": {
+			args: []string{"-q", "lq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter": {
+			args: []string{"--clusterqueue", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with clusterqueue filter (short flag)": {
+			args: []string{"-c", "cq1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with only pending filter": {
+			args: []string{"--only-pending"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadQuotaReserved,
+							Reason: "Pending",
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+wl2               j2         lq2          cq2            PENDING                       120m
+`,
+		},
+		"should print workload list with only admitted filter": {
+			args: []string{"--only-admitted"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionTrue,
+						},
+					}...).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Conditions([]metav1.Condition{
+						{
+							Type:   kueue.WorkloadAdmitted,
+							Status: metav1.ConditionFalse,
+						},
+					}...).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS     POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            ADMITTED                       60m
+`,
+		},
+		"should print workload list with label selector filter": {
+			args: []string{"--selector", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with label selector filter (short flag)": {
+			args: []string{"-l", "key=value1"},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Creation(testStartTime.Add(-1*time.Hour).Truncate(time.Second)).
+					Label("key", "value1").
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Creation(testStartTime.Add(-2*time.Hour).Truncate(time.Second)).
+					Label("key", "value2").
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING                       60m
+`,
+		},
+		"should print workload list with Job types": {
+			apiResourceLists: []*metav1.APIResourceList{
+				{
+					GroupVersion: "batch/v1",
+					APIResources: []metav1.APIResource{
+						{
+							SingularName: "job",
+							Kind:         "Job",
+							Group:        "",
+						},
+					},
+				},
+				{
+					GroupVersion: "ray.io/v1",
+					APIResources: []metav1.APIResource{
+						{
+							SingularName: "rayjob",
+							Kind:         "RayJob",
+							Group:        "ray.io",
+						},
+					},
+				}, {
+					GroupVersion: "kubeflow.org/v1",
+					APIResources: []metav1.APIResource{
+						{
+							SingularName: "pytorchjob",
+							Kind:         "PyTorchJob",
+							Group:        "kubeflow.org",
+						},
+					},
+				},
+			},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl3", metav1.NamespaceDefault).
+					OwnerReference(kftraining.SchemeGroupVersion.WithKind(kftraining.PyTorchJobKind), "j3", "test-uid").
+					Queue("lq3").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq3").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-3 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE                  JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1    job                       j1         lq1          cq1            PENDING                       60m
+wl2    rayjob.ray.io             j2         lq2          cq2            PENDING                       120m
+wl3    pytorchjob.kubeflow....   j3         lq3          cq3            PENDING                       3h
+`,
+		},
+		"should print workload list with position in queue": {
+			pendingWorkloads: []v1alpha1.PendingWorkload{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "wl1",
+						Namespace: "default",
+					},
+					Priority:               10,
+					LocalQueueName:         "lq1",
+					PositionInClusterQueue: 11,
+					PositionInLocalQueue:   12,
+				},
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "wl2",
+						Namespace: "default",
+					},
+					Priority:               20,
+					LocalQueueName:         "lq2",
+					PositionInClusterQueue: 21,
+					PositionInLocalQueue:   22,
+				},
+			},
+			objs: []runtime.Object{
+				utiltesting.MakeWorkload("wl1", metav1.NamespaceDefault).
+					OwnerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "j1", "test-uid").
+					Queue("lq1").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq1").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-1 * time.Hour).Truncate(time.Second)).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", metav1.NamespaceDefault).
+					OwnerReference(rayv1.GroupVersion.WithKind("RayJob"), "j2", "test-uid").
+					Queue("lq2").
+					Active(true).
+					Admission(utiltesting.MakeAdmission("cq2").Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadQuotaReserved,
+						Status: metav1.ConditionFalse,
+						Reason: "Pending",
+					}).
+					Creation(testStartTime.Add(-2 * time.Hour).Truncate(time.Second)).
+					Obj(),
+			},
+			wantOut: `NAME   JOB TYPE   JOB NAME   LOCALQUEUE   CLUSTERQUEUE   STATUS    POSITION IN QUEUE   AGE
+wl1               j1         lq1          cq1            PENDING   12                  60m
+wl2               j2         lq2          cq2            PENDING   22                  120m
+`,
+		},
+		"should print not found error": {
+			wantOutErr: fmt.Sprintf("No resources found in %s namespace.\n", metav1.NamespaceDefault),
+		},
+		"should print not found error with all-namespaces filter": {
+			args:       []string{"-A"},
+			wantOutErr: "No resources found\n",
+		},
+	}
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			streams, _, out, outErr := genericiooptions.NewTestIOStreams()
+
+			tf := cmdtesting.NewTestClientGetter()
+			if len(tc.ns) > 0 {
+				tf.WithNamespace(tc.ns)
+			}
+
+			clientset := fake.NewSimpleClientset(tc.objs...)
+
+			// `SimpleClientset` not allow to add `PendingWorkloadsSummary` objects,
+			// because of `PendingWorkload` resources not implement `runtime.Object`.
+			// Default `Reaction` handle all verbs and resources, so need to add on
+			// head of chain.
+			clientset.Fake.ReactionChain = append([]kubetesting.Reactor{
+				&kubetesting.SimpleReactor{
+					Verb:     "get",
+					Resource: "clusterqueues",
+					Reaction: func(action kubetesting.Action) (handled bool, ret runtime.Object, err error) {
+						obj := &v1alpha1.PendingWorkloadsSummary{Items: tc.pendingWorkloads}
+						return true, obj, err
+					},
+				},
+			}, clientset.Fake.ReactionChain...)
+
+			tf.ClientSet = clientset
+			tf.ClientSet.Discovery().(*fakediscovery.FakeDiscovery).Resources = tc.apiResourceLists
+
+			cmd := NewWorkloadCmd(tf, streams)
+			cmd.SetArgs(tc.args)
+
+			gotErr := cmd.Execute()
+			if diff := cmp.Diff(tc.wantErr, gotErr, cmpopts.EquateErrors()); diff != "" {
+				t.Errorf("Unexpected error (-want/+got)\n%s", diff)
+			}
+
+			gotOut := out.String()
+			fmt.Println(gotOut)
```

  Removed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T19:25:17Z

- **cmd/kueuectl/app/list/list_workload.go:212** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T19:25:17Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
+		// To do not add "[flags]" suffix on the end of usage line
+		DisableFlagsInUseLine: true,
+		Aliases:               []string{"wl"},
+		Short:                 "List Workload",
+		Long:                  wlLong,
+		Example:               wlExample,
+		Run: func(cmd *cobra.Command, args []string) {
+			cobra.CheckErr(o.Complete(clientGetter, cmd, args))
+			cobra.CheckErr(o.Run(cmd.Context()))
+		},
+	}
+
+	o.PrintFlags.AddFlags(cmd)
+
+	addAllNamespacesFlagVar(cmd, &o.AllNamespaces)
+	addFieldSelectorFlagVar(cmd, &o.FieldSelector)
+	addLabelSelectorFlagVar(cmd, &o.LabelSelector)
+	addClusterQueueFilterFlagVar(cmd, &o.ClusterQueueFilter)
+	addLocalQueueFilterFlagVar(cmd, &o.LocalQueueFilter)
+
+	cmd.Flags().BoolVar(&o.OnlyPending, "only-pending", false,
+		"Filter only pending workload.")
+	cmd.Flags().BoolVar(&o.OnlyAdmitted, "only-admitted", false,
+		"Filter only admitted workload.")
+
+	return cmd
+}
+
+// Complete completes all the required options
+func (o *WorkloadOptions) Complete(clientGetter util.ClientGetter, cmd *cobra.Command, args []string) error {
+	var err error
+
+	o.Namespace, _, err = clientGetter.ToRawKubeConfigLoader().Namespace()
+	if err != nil {
+		return err
+	}
+
+	o.ClientSet, err = clientGetter.KueueClientSet()
+	if err != nil {
+		return err
+	}
+
+	o.ToPrinter = func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error) {
+		if !o.PrintFlags.OutputFlagSpecified() {
+			printer := newWorkloadTablePrinter().WithResources(r)
+			if o.AllNamespaces {
+				printer.WithNamespace()
+			}
+			return printer.PrintObj, nil
+		}
+		printer, err := o.PrintFlags.ToPrinter()
+		if err != nil {
+			return nil, err
+		}
+		return printer.PrintObj, nil
+	}
+
+	return nil
+}
+
+// Run performs the list operation.
+func (o *WorkloadOptions) Run(ctx context.Context) error {
+	namespace := o.Namespace
+	if o.AllNamespaces {
+		namespace = ""
+	}
+
+	opts := metav1.ListOptions{LabelSelector: o.LabelSelector, FieldSelector: o.FieldSelector}
+	list, err := o.ClientSet.KueueV1beta1().Workloads(namespace).List(ctx, opts)
+	if err != nil {
+		return err
+	}
+
+	o.filterList(list)
+
+	if len(list.Items) == 0 {
+		if !o.AllNamespaces {
+			fmt.Fprintf(o.ErrOut, "No resources found in %s namespace.\n", o.Namespace)
+		} else {
+			fmt.Fprintln(o.ErrOut, "No resources found")
+		}
+		return nil
+	}
+
+	r := newListWorkloadResources()
+
+	r.localQueues, err = o.localQueues(ctx, list)
+	if err != nil {
+		return err
+	}
+
+	r.pendingWorkloads, err = o.pendingWorkloads(ctx, list, r.localQueues)
+	if err != nil {
+		return err
+	}
+
+	r.apiResourceLists, err = o.apiResources(list)
+	if err != nil {
+		return err
+	}
+
+	printer, err := o.ToPrinter(r)
+	if err != nil {
+		return err
+	}
+
+	return printer.PrintObj(list, o.Out)
+}
+
+func (o *WorkloadOptions) filterList(list *v1beta1.WorkloadList) {
+	if len(list.Items) == 0 {
+		return
+	}
+	filteredItems := make([]v1beta1.Workload, 0, len(o.LocalQueueFilter))
+	for _, wl := range list.Items {
+		if o.filterByLocalQueue(&wl) && o.filterByClusterQueue(&wl) && o.filterByOnlyPending(&wl) && o.filterByOnlyAdmitted(&wl) {
+			filteredItems = append(filteredItems, wl)
+		}
+	}
+	list.Items = filteredItems
+}
+
+func (o *WorkloadOptions) filterByLocalQueue(wl *v1beta1.Workload) bool {
+	return len(o.LocalQueueFilter) == 0 || wl.Spec.QueueName == o.LocalQueueFilter
+}
+
+func (o *WorkloadOptions) filterByClusterQueue(wl *v1beta1.Workload) bool {
+	return len(o.ClusterQueueFilter) == 0 || wl.Status.Admission != nil &&
+		wl.Status.Admission.ClusterQueue == v1beta1.ClusterQueueReference(o.ClusterQueueFilter)
+}
+
+func (o *WorkloadOptions) filterByOnlyPending(wl *v1beta1.Workload) bool {
+	if !o.OnlyPending {
+		return true
+	}
+	cond := meta.FindStatusCondition(wl.Status.Conditions, v1beta1.WorkloadQuotaReserved)
+	if cond != nil && cond.Reason == "Pending" && cond.Status == metav1.ConditionFalse {
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T19:25:33Z

- **cmd/kueuectl/app/list/list_workload.go:75** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T19:25:33Z
```diff
@@ -0,0 +1,310 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/spf13/cobra"
+	"k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/cli-runtime/pkg/genericclioptions"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	"k8s.io/cli-runtime/pkg/printers"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	clientset "sigs.k8s.io/kueue/client-go/clientset/versioned"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/scheme"
+	"sigs.k8s.io/kueue/cmd/kueuectl/app/util"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	wlLong    = `Lists Workloads that match the provided criteria.`
+	wlExample = `  # List Workload 
+  kueuectl list workload`
+)
+
+type WorkloadOptions struct {
+	PrintFlags *genericclioptions.PrintFlags
+
+	OnlyPending        bool
+	OnlyAdmitted       bool
+	AllNamespaces      bool
+	Namespace          string
+	FieldSelector      string
+	LabelSelector      string
+	ClusterQueueFilter string
+	LocalQueueFilter   string
+
+	ClientSet clientset.Interface
+
+	ToPrinter func(r *listWorkloadResources) (printers.ResourcePrinterFunc, error)
+
+	genericiooptions.IOStreams
+}
+
+func NewWorkloadOptions(streams genericiooptions.IOStreams) *WorkloadOptions {
+	return &WorkloadOptions{
+		PrintFlags: genericclioptions.NewPrintFlags("").WithTypeSetter(scheme.Scheme),
+		IOStreams:  streams,
+	}
+}
+
+func NewWorkloadCmd(clientGetter util.ClientGetter, streams genericiooptions.IOStreams) *cobra.Command {
+	o := NewWorkloadOptions(streams)
+
+	cmd := &cobra.Command{
+		Use: "workload [-–clusterqueue CLUSTER_QUEUE_NAME] [-–localqueue LOCAL_QUEUE_NAME] [--only-pending] [--only-admitted] [--selector key1=value1] [--field-selector key1=value1] [--all-namespaces]",
```

  Added

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T19:34:35Z

- **cmd/kueuectl/app/list/list_workload_test.go:43** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T19:34:35Z
```diff
@@ -0,0 +1,470 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package list
+
+import (
+	"fmt"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/cli-runtime/pkg/genericiooptions"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	kubetesting "k8s.io/client-go/testing"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/apis/visibility/v1alpha1"
+	"sigs.k8s.io/kueue/client-go/clientset/versioned/fake"
+	cmdtesting "sigs.k8s.io/kueue/cmd/kueuectl/app/testing"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+func TestWorkloadCmd(t *testing.T) {
+	testStartTime := time.Now()
```

  #2287

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-27T20:46:20Z

/lgtm
/approve
