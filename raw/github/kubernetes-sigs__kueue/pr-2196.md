# PR #2196: Provide support for ProvisioningRequest's CapacityRevoked condition

**Summary**: Provide support for ProvisioningRequest's CapacityRevoked condition

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2196

**Last updated**: 2024-07-31T11:42:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `support-provreq` → `main`
- **Created**: 2024-05-14T08:28:02Z
- **Updated**: 2024-07-31T11:42:33Z
- **Merged**: 2024-06-07T09:14:28Z
- **Closed**: 2024-06-07T09:14:28Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk), [@trasc](https://github.com/trasc)
- **Changed files**: 11   **+562 / -238**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Provide support for the new ProvisioningRequest's condition `CapacityRevoked`. When user uses a job that allows retries and sets `.spec.backOffLimit` > 0 and the job gets evicted because of `MaxRunDuration` an according AdmissionCheck gets rejected.

After a Workload is finished we no longer delete corresponding ProvisioningRequest. This is because Workload might be finished due to `MaxRunDuration` timeout, before ClusterAutoscaler updates ProvisioningRequest's status to `CapacityRevoked` and the Kueue's controller has no opportunity to react to that.

Additionally I did a small cleanup.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #2041 

#### Special notes for your reviewer:
I've tested it e2e manually

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
ProvisioningRequets: Support for ProvisioningRequest's condition `CapacityRevoked`. ProvisioningRequests objects persist until the corresponding Job or the Workload is deleted
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-14T08:28:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bbcf7c55bfeb2f9c8699e70e191b9f684dbc341d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6662c8dfedea9f0008ea2d70 |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-05-14T08:28:45Z

/assign @yaroslava-serdiuk 
/assign @alculquicondor

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-14T10:20:18Z

please, fix the tests

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-15T09:23:00Z

Please, rebase the PR to resolve merge conflict

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-15T14:06:05Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-15T14:06:11Z

LGTM label has been added.  <details>Git tree hash: 22bd86626a7137e9e4aebc737e06cc5dcebd29d5</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-05-16T11:03:34Z

/assign @tenzen-y

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:05:40Z

Please also add to the release note that ProvisioningRequests objects persist until the Job or the Workload is deleted, IIUC.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:09:59Z

I saw that you resolved some comments... did you forget to push?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-05-31T14:23:53Z

Could you please take a look on that @tenzen-y ? I've deleted a test you've added recently. I needed to delete it for my use case:

When ProvisioningRequest gets `CapacityRevoked` we mark the AdmissionCheck filed as Rejected in Workload's Status. Then in the provisioning controller we deactivate the Workload. There is a race condition between the provisioning controller and the workload controller. The workload controller might set the condition to `EvictedByAdmissionCheck` before the provisioning controller deactivates the Workload. We would like to have the condition set to `EvictedByDeactivation`, so even if the workload controller sets the Evicted condition first to `EvictedByAdmissionCheck` we would like to update it.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-31T16:54:15Z

> Could you please take a look on that @tenzen-y ? I've deleted a test you've added recently. I needed to delete it for my use case:
> 
> When ProvisioningRequest gets `CapacityRevoked` we mark the AdmissionCheck filed as Rejected in Workload's Status. Then in the provisioning controller we deactivate the Workload. There is a race condition between the provisioning controller and the workload controller. The workload controller might set the condition to `EvictedByAdmissionCheck` before the provisioning controller deactivates the Workload. We would like to have the condition set to `EvictedByDeactivation`, so even if the workload controller sets the Evicted condition first to `EvictedByAdmissionCheck` we would like to update it.

TBH, I don't prefer to merge features that we find the race condition, obviously. Actually, the admission controller is able to override the previous evicted reason by race condition. So, I'd like to resolve the #2291, first.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-06-05T08:09:43Z

To avoid race condition we decided to drop changes regarding deactivating workload in the provisioning controller in this PR. I'll follow up with an another PR that will deactivate Workload always when any check gets `Rejected`.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-06T11:27:46Z

/lgtm
@tenzen-y anything to add? The deactivation will happen in the follow up PR.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-06T11:27:51Z

LGTM label has been added.  <details>Git tree hash: 4d47a984051746c6a582c663b021d272b2b094fc</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-06T11:34:59Z

> /lgtm @tenzen-y anything to add? The deactivation will happen in the follow up PR.

Let me see this.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-06T19:43:45Z

Please update description and release note.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-07T08:48:24Z

LGTM label has been added.  <details>Git tree hash: 8c067e1612f2fd8f0620a2824677ee2fa7bf2e0f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-07T08:48:26Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2196#pullrequestreview-2103073543" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2196#" title="Author self-approved">PBundyra</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2196#pullrequestreview-2104005334" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-07T09:09:25Z

/hold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-07T09:09:31Z

/hold cancel

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T20:43:27Z

/release-note-edit
```release-note
ProvisioningRequets: Support for ProvisioningRequest's condition `CapacityRevoked`. ProvisioningRequests objects persist until the corresponding Job or the Workload is deleted
```

## Reviews

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-05-14T13:03:13Z

Minor comment

- **pkg/controller/admissionchecks/provisioning/provisioning.go:41** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-14T12:39:39Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isNotProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
```

  What will be returned if Provisioned condition is not present? Seems the function will return false, but should true. 
  I don't think we need this function if we have `isProvisioned`.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-14T14:07:12Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:41** by [@PBundyra](https://github.com/PBundyra) — 2024-05-14T14:07:12Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isNotProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
```

  Function `isProvisioned` will return `false` in two cases:
  - if the condition is not present
  - if the condition is present and set to `false`
  
  The intention behind `isNotProvisioned` was to return `true` if the condition is present and set to `false`. In case the condition is not present, as you said it will return `false` but that intended behavior.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-05-14T14:12:05Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:41** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-14T14:12:05Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isNotProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
```

  > What will be returned if Provisioned condition is not present? Seems the function will return false, but should true.
  
  I mean, if the Provisioned conditioned is not present, it should be treated in the same way as Provisioned condition is set to false

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-14T15:03:25Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:41** by [@PBundyra](https://github.com/PBundyra) — 2024-05-14T15:03:25Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isNotProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
```

  IIUC the difference between Provisioned condition set to `false` and not present, is that in the first case it provides a message with ETA (or will be doing so soon) and if not present the message is not there as well. Am I missing anything?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-05-15T09:21:42Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:41** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-15T09:21:42Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isNotProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
```

  Oh, I see, that makes sense. 
  Let's rename the function to `isProvisionedFalse` to avoid confusions.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-15T10:51:34Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:41** by [@PBundyra](https://github.com/PBundyra) — 2024-05-15T10:51:34Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isNotProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
```

  Good point, done!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-16T19:42:03Z

- **pkg/controller/admissionchecks/provisioning/controller.go:136** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:04:59Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
```

  unnecessary comment. Unless you can explain why we don't need to sync anything else after this.

- **pkg/controller/admissionchecks/provisioning/controller.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:07:04Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
+		log.V(5).Info("workload with no reservation or is finished")
```

  since we are not doing anything after this, I would also remove the log.

- **pkg/controller/admissionchecks/provisioning/controller.go:142** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:10:19Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
+		log.V(5).Info("workload with no reservation or is finished")
+		return reconcile.Result{}, nil
+	}
+
 	if workload.IsAdmitted(wl) {
 		// check the state of the provision requests, eventually toggle the checks to false
```

  The comment looks outdated.

- **pkg/controller/admissionchecks/provisioning/controller.go:144** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:10:29Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
+		log.V(5).Info("workload with no reservation or is finished")
+		return reconcile.Result{}, nil
+	}
+
 	if workload.IsAdmitted(wl) {
 		// check the state of the provision requests, eventually toggle the checks to false
 		// otherwise there is nothing to here
-		log.V(5).Info("workload admitted, sync checks")
-		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+		log.V(5).Info("workload admitted")
```

  Consider removing the log line

- **pkg/controller/admissionchecks/provisioning/controller.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:12:41Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
+		log.V(5).Info("workload with no reservation or is finished")
+		return reconcile.Result{}, nil
+	}
+
 	if workload.IsAdmitted(wl) {
 		// check the state of the provision requests, eventually toggle the checks to false
 		// otherwise there is nothing to here
-		log.V(5).Info("workload admitted, sync checks")
-		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+		log.V(5).Info("workload admitted")
+		return reconcile.Result{}, nil
 	}
 
 	err = c.deleteUnusedProvisioningRequests(ctx, ownedPrs, activeOrLastPRForChecks)
```

  the log line under here looks wrong.

- **pkg/controller/admissionchecks/provisioning/controller.go:232** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:25:56Z
```diff
@@ -240,8 +223,16 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					if err := c.deactivateWorkload(ctx, wl); err != nil {
+						return nil, err
+					}
+					c.record.Eventf(oldPr, corev1.EventTypeNormal, "CapacityRevoked", "Capacity for %v, has been revoked", oldPr.Name)
```

  ```suggestion
  					c.record.Eventf(oldPr, corev1.EventTypeNormal, "CapacityRevoked", "Deactivating workload because capacity for %v", oldPr.Name)
  ```
  
  Thanks for adding the event.
  
  I regret not having added a reason along side the `active` field to explain why Kueue decided to deactivate the Workload.
  
  But maybe it's just worth adding an Evicted condition just before deactivating that could record the reason?

- **pkg/controller/admissionchecks/provisioning/controller_test.go:86** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:26:39Z
```diff
@@ -83,6 +83,7 @@ func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType stri
 
 func TestReconcile(t *testing.T) {
 	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		Active(true).
```

  By default it's nil, which is interpreted as active. I think it's worth not setting this, in case we are masquerading any null pointer exception.

- **pkg/controller/admissionchecks/provisioning/controller_test.go:767** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:28:40Z
```diff
@@ -707,6 +708,117 @@ func TestReconcile(t *testing.T) {
 					Obj(),
 			},
 		},
+		"workloads status gets deactivated when is not finished and receives the provisioning request's CapacityRevoked condition": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
+			flavors:  []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
+			configs: []kueue.ProvisioningRequestConfig{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "config1",
+					},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+					},
+				},
+			},
+			requests: []autoscaling.ProvisioningRequest{
+				*requestWithConditions(baseRequest,
+					[]metav1.Condition{
+						{
+							Type:   autoscaling.Failed,
+							Status: metav1.ConditionFalse,
+						},
+						{
+							Type:   autoscaling.Provisioned,
+							Status: metav1.ConditionTrue,
+						},
+						{
+							Type:   autoscaling.Accepted,
+							Status: metav1.ConditionTrue,
+						},
+						{
+							Type:   autoscaling.CapacityRevoked,
+							Status: metav1.ConditionTrue,
+						},
+					}),
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateRejected,
+					}, kueue.AdmissionCheckState{
+						Name:  "not-provisioning",
+						State: kueue.CheckStatePending,
+					}).
+					Active(false).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Namespace: "ns", Name: "wl-check1-1"},
+					EventType: corev1.EventTypeNormal,
+					Reason:    "CapacityRevoked",
+					Message:   "Capacity for wl-check1-1, has been revoked",
+				},
+			},
+		},
+		"workloads status doesnt get deactivated when it's finished and receives the provisioning request's CapacityRevoked condition": {
```

  Another case for when the workload was still not admitted?

- **pkg/controller/admissionchecks/provisioning/controller.go:494** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:31:42Z
```diff
@@ -535,34 +491,34 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 					// to change to the "successfully provisioned" message after provisioning
 					updateCheckMessage(&checkState, apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Provisioned).Message)
 				}
-			case prAccepted:
-				if provisionedCond := apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Provisioned); provisionedCond != nil {
-					// propagate the ETA update from the provisioning request into the workload
-					updated = updateCheckMessage(&checkState, provisionedCond.Message) || updated
-					updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-				}
+			case isAccepted(pr) && isProvisionedFalse(pr):
```

  I prefer the previous implementation, so that we don't hide the detail about what it means if the condition is not present.

- **pkg/controller/admissionchecks/provisioning/provisioning.go:75** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:38:20Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
```

  ```suggestion
  func matchesWorkloadAndCheck(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
  ```

- **pkg/controller/admissionchecks/provisioning/provisioning.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:38:28Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
```

  ```suggestion
  func ProvisioningRequestName(workloadName, checkName string, attempt int32) string {
  ```

- **pkg/workload/workload.go:567** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-16T19:39:28Z
```diff
@@ -562,6 +562,11 @@ func IsFinished(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadFinished)
 }
 
+// IsActive returns true if the workload is active.
+func IsActive(w *kueue.Workload) bool {
+	return *w.Spec.Active
```

  Use `ptr.Defer`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-17T19:30:12Z

- **pkg/controller/admissionchecks/provisioning/controller.go:135** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T17:27:11Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
```

  I could not follow the reason why we should move this logic to address the CapacityRevoked condition.
  What is the reason that we should sync the check state instead of just removing the provisioning request?

- **pkg/util/testing/wrappers.go:359** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T17:31:17Z
```diff
@@ -350,6 +350,14 @@ func (w *WorkloadWrapper) ResourceVersion(v string) *WorkloadWrapper {
 	return w
 }
 
+func WorkloadWithConditions(wl *kueue.Workload, conditions []metav1.Condition) *kueue.Workload {
+	wl = wl.DeepCopy()
+	for _, condition := range conditions {
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+	}
+	return wl
+}
```

  Instead of this function, could you use this?
  
  https://github.com/kubernetes-sigs/kueue/blob/e11d77608ef3dd5a5023824a7e01ca75b11fe2e1/pkg/util/testing/wrappers.go#L246-L249

- **pkg/util/testing/core.go:73** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T17:39:25Z
```diff
@@ -67,3 +67,18 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
+
+// HasEventAppeared return if an event has been emitted
+func HasEventAppeared(ctx context.Context, k8sClient client.Client, event corev1.Event) (bool, error) {
```

  ```suggestion
  // HasEventAppeared returns if an event has been emitted
  func HasEventAppeared(ctx context.Context, k8sClient client.Client, event corev1.Event) (bool, error) {
  ```

- **pkg/controller/admissionchecks/provisioning/provisioning.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T17:45:09Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
```

  TBH, I couldn't find it valuable to have these helpers since these just call a single function.
  @PBundyra Any reason that we should have these?

- **pkg/controller/admissionchecks/provisioning/controller.go:218** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T17:53:46Z
```diff
@@ -240,8 +223,16 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
```

  ```suggestion
  				if ptr.Deref(wl, true) && !workload.IsFinished(wl) {
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:547** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T17:58:29Z
```diff
@@ -583,6 +539,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload) error {
+	updatedWl := &kueue.Workload{}
+	wlKey := types.NamespacedName{
+		Namespace: wl.Namespace,
+		Name:      wl.Name,
+	}
+	if err := c.client.Get(ctx, wlKey, updatedWl); err != nil {
+		return err
+	}
```

  What is the reason we should re-obtain the Workload?
  We have already received the workload from the reconcile queue.

- **pkg/controller/admissionchecks/provisioning/provisioning.go:82** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T19:21:12Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
```

  ```suggestion
  func getAttempt(logger logr.Logger, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
  ```
  
  The `logger` should be enough.

- **pkg/controller/admissionchecks/provisioning/provisioning.go:87** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T19:21:51Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	if len(matches) > 0 {
+		number, err := strconv.Atoi(matches[1])
+		if err != nil {
+			logger.Error(err, "Parsing the attempt number from provisioning request", "requestName", pr.Name)
+			return 1
+		} else {
+			return int32(number)
+		}
+	} else {
+		logger.Info("No attempt suffix in provisioning request", "requestName", pr.Name)
+		return 1
+	}
```

  ```suggestion
  	}
  	logger.Info("No attempt suffix in provisioning request", "requestName", pr.Name)
  	return 1	
  ```

- **pkg/controller/admissionchecks/provisioning/provisioning.go:85** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T19:22:08Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	if len(matches) > 0 {
+		number, err := strconv.Atoi(matches[1])
+		if err != nil {
+			logger.Error(err, "Parsing the attempt number from provisioning request", "requestName", pr.Name)
+			return 1
+		} else {
+			return int32(number)
+		}
```

  ```suggestion
  		}
  		return int32(number)
  ```

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:575** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-17T19:30:08Z
```diff
@@ -456,6 +438,127 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 
+		ginkgo.It("Should deactivate the workload when it's not Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job allows retries and a user sets .spec.backOffLimit > 0
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					util.SetWorkloadsAdmissionCheck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as CapacityRevoked", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.CapacityRevoked,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.CapacityRevoked,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking if workload is deactivated and an event is emitted", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateRejected))
+
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+					ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+						Reason:  "CapacityRevoked",
+						Type:    corev1.EventTypeNormal,
+						Message: fmt.Sprintf("Capacity for %v, has been revoked", provReqKey.Name),
+					})
+					g.Expect(ok).To(gomega.BeTrue())
+					g.Expect(err).NotTo(gomega.HaveOccurred())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should not deactivate the workload when it's Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job doesn't allow retries or a user sets .spec.backOffLimit = 0
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCheck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Marking the workload as Finished", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.FinishWorkloads(ctx, k8sClient, updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as Provisioned", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.Provisioned,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as CapacityRevoked", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.CapacityRevoked,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.CapacityRevoked,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking if workload is active and an event is not emitted", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
```

  What is the reason this is `Ready` even though the admission controller update states with `rejected` here:
  
  https://github.com/kubernetes-sigs/kueue/blob/cf6330a7fef15d74338c9dc31b29136b807e493a/pkg/controller/admissionchecks/provisioning/controller.go#L479-L484

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T08:01:12Z

- **pkg/controller/admissionchecks/provisioning/controller.go:547** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T08:01:12Z
```diff
@@ -583,6 +539,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload) error {
+	updatedWl := &kueue.Workload{}
+	wlKey := types.NamespacedName{
+		Namespace: wl.Namespace,
+		Name:      wl.Name,
+	}
+	if err := c.client.Get(ctx, wlKey, updatedWl); err != nil {
+		return err
+	}
```

  This is because the Workload has been Patched in `syncCheckStates` function

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T08:04:37Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:54** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T08:04:37Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
```

  I wanted to make code more concise and self-explanatory, but I discard these if you think it's unnecessary

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T08:06:47Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:82** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T08:06:47Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
```

  We don't always have a logger when calling this function

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T08:58:39Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:575** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T08:58:39Z
```diff
@@ -456,6 +438,127 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 
+		ginkgo.It("Should deactivate the workload when it's not Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job allows retries and a user sets .spec.backOffLimit > 0
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					util.SetWorkloadsAdmissionCheck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as CapacityRevoked", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.CapacityRevoked,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.CapacityRevoked,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking if workload is deactivated and an event is emitted", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateRejected))
+
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+					ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+						Reason:  "CapacityRevoked",
+						Type:    corev1.EventTypeNormal,
+						Message: fmt.Sprintf("Capacity for %v, has been revoked", provReqKey.Name),
+					})
+					g.Expect(ok).To(gomega.BeTrue())
+					g.Expect(err).NotTo(gomega.HaveOccurred())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should not deactivate the workload when it's Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job doesn't allow retries or a user sets .spec.backOffLimit = 0
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCheck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Marking the workload as Finished", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					util.FinishWorkloads(ctx, k8sClient, updatedWl)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as Provisioned", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.Provisioned,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as CapacityRevoked", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.CapacityRevoked,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.CapacityRevoked,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking if workload is active and an event is not emitted", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
```

  This is a scenario where the controller doesn't update the state because the workload is Finished

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:17:58Z

- **pkg/controller/admissionchecks/provisioning/controller.go:135** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:17:58Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
```

  We would prefer not to delete provisioning requests to make it easier for users to follow their workflow and debug

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:46:18Z

- **pkg/controller/admissionchecks/provisioning/controller.go:136** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:46:18Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:46:29Z

- **pkg/controller/admissionchecks/provisioning/controller.go:142** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:46:29Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
+		log.V(5).Info("workload with no reservation or is finished")
+		return reconcile.Result{}, nil
+	}
+
 	if workload.IsAdmitted(wl) {
 		// check the state of the provision requests, eventually toggle the checks to false
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:46:37Z

- **pkg/controller/admissionchecks/provisioning/controller.go:144** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:46:36Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
+		log.V(5).Info("workload with no reservation or is finished")
+		return reconcile.Result{}, nil
+	}
+
 	if workload.IsAdmitted(wl) {
 		// check the state of the provision requests, eventually toggle the checks to false
 		// otherwise there is nothing to here
-		log.V(5).Info("workload admitted, sync checks")
-		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+		log.V(5).Info("workload admitted")
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:46:50Z

- **pkg/controller/admissionchecks/provisioning/controller.go:137** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:46:50Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
+		log.V(5).Info("workload with no reservation or is finished")
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:47:05Z

- **pkg/controller/admissionchecks/provisioning/controller.go:137** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:47:04Z
```diff
@@ -134,14 +123,26 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
 		return reconcile.Result{}, err
 	}
+
 	ownedPrs := list.Items
 	activeOrLastPRForChecks := c.activeOrLastPRForChecks(ctx, wl, relevantChecks, ownedPrs)
 
+	err = c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
+		// 1.2 workload has no reservation or is finished
+		log.V(5).Info("workload with no reservation or is finished")
+		return reconcile.Result{}, nil
+	}
+
 	if workload.IsAdmitted(wl) {
 		// check the state of the provision requests, eventually toggle the checks to false
 		// otherwise there is nothing to here
-		log.V(5).Info("workload admitted, sync checks")
-		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks, activeOrLastPRForChecks)
+		log.V(5).Info("workload admitted")
+		return reconcile.Result{}, nil
 	}
 
 	err = c.deleteUnusedProvisioningRequests(ctx, ownedPrs, activeOrLastPRForChecks)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:47:22Z

- **pkg/controller/admissionchecks/provisioning/controller.go:232** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:47:22Z
```diff
@@ -240,8 +223,16 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					if err := c.deactivateWorkload(ctx, wl); err != nil {
+						return nil, err
+					}
+					c.record.Eventf(oldPr, corev1.EventTypeNormal, "CapacityRevoked", "Capacity for %v, has been revoked", oldPr.Name)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:47:30Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:86** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:47:30Z
```diff
@@ -83,6 +83,7 @@ func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType stri
 
 func TestReconcile(t *testing.T) {
 	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		Active(true).
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:47:52Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:767** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:47:52Z
```diff
@@ -707,6 +708,117 @@ func TestReconcile(t *testing.T) {
 					Obj(),
 			},
 		},
+		"workloads status gets deactivated when is not finished and receives the provisioning request's CapacityRevoked condition": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []kueue.AdmissionCheck{*baseCheck.DeepCopy()},
+			flavors:  []kueue.ResourceFlavor{*baseFlavor1.DeepCopy(), *baseFlavor2.DeepCopy()},
+			configs: []kueue.ProvisioningRequestConfig{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Name: "config1",
+					},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+					},
+				},
+			},
+			requests: []autoscaling.ProvisioningRequest{
+				*requestWithConditions(baseRequest,
+					[]metav1.Condition{
+						{
+							Type:   autoscaling.Failed,
+							Status: metav1.ConditionFalse,
+						},
+						{
+							Type:   autoscaling.Provisioned,
+							Status: metav1.ConditionTrue,
+						},
+						{
+							Type:   autoscaling.Accepted,
+							Status: metav1.ConditionTrue,
+						},
+						{
+							Type:   autoscaling.CapacityRevoked,
+							Status: metav1.ConditionTrue,
+						},
+					}),
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateRejected,
+					}, kueue.AdmissionCheckState{
+						Name:  "not-provisioning",
+						State: kueue.CheckStatePending,
+					}).
+					Active(false).
+					Obj(),
+			},
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Namespace: "ns", Name: "wl-check1-1"},
+					EventType: corev1.EventTypeNormal,
+					Reason:    "CapacityRevoked",
+					Message:   "Capacity for wl-check1-1, has been revoked",
+				},
+			},
+		},
+		"workloads status doesnt get deactivated when it's finished and receives the provisioning request's CapacityRevoked condition": {
```

  Great idea, thanks, done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:48:01Z

- **pkg/controller/admissionchecks/provisioning/controller.go:494** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:48:00Z
```diff
@@ -535,34 +491,34 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 					// to change to the "successfully provisioned" message after provisioning
 					updateCheckMessage(&checkState, apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Provisioned).Message)
 				}
-			case prAccepted:
-				if provisionedCond := apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Provisioned); provisionedCond != nil {
-					// propagate the ETA update from the provisioning request into the workload
-					updated = updateCheckMessage(&checkState, provisionedCond.Message) || updated
-					updated = updateCheckState(&checkState, kueue.CheckStatePending) || updated
-				}
+			case isAccepted(pr) && isProvisionedFalse(pr):
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:48:08Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:75** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:48:08Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:48:14Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:60** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:48:14Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:48:20Z

- **pkg/workload/workload.go:567** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:48:20Z
```diff
@@ -562,6 +562,11 @@ func IsFinished(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadFinished)
 }
 
+// IsActive returns true if the workload is active.
+func IsActive(w *kueue.Workload) bool {
+	return *w.Spec.Active
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:48:30Z

- **pkg/util/testing/wrappers.go:359** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:48:30Z
```diff
@@ -350,6 +350,14 @@ func (w *WorkloadWrapper) ResourceVersion(v string) *WorkloadWrapper {
 	return w
 }
 
+func WorkloadWithConditions(wl *kueue.Workload, conditions []metav1.Condition) *kueue.Workload {
+	wl = wl.DeepCopy()
+	for _, condition := range conditions {
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+	}
+	return wl
+}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:48:37Z

- **pkg/util/testing/core.go:73** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:48:37Z
```diff
@@ -67,3 +67,18 @@ func CheckLatestEvent(ctx context.Context, k8sClient client.Client,
 
 	return false, fmt.Errorf("mismatch with the latest event: got r:%v t:%v n:%v, reg %v", item.Reason, item.Type, item.Note, item.Regarding)
 }
+
+// HasEventAppeared return if an event has been emitted
+func HasEventAppeared(ctx context.Context, k8sClient client.Client, event corev1.Event) (bool, error) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:48:53Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:87** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:48:52Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	if len(matches) > 0 {
+		number, err := strconv.Atoi(matches[1])
+		if err != nil {
+			logger.Error(err, "Parsing the attempt number from provisioning request", "requestName", pr.Name)
+			return 1
+		} else {
+			return int32(number)
+		}
+	} else {
+		logger.Info("No attempt suffix in provisioning request", "requestName", pr.Name)
+		return 1
+	}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-20T09:48:58Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:85** by [@PBundyra](https://github.com/PBundyra) — 2024-05-20T09:48:58Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	if len(matches) > 0 {
+		number, err := strconv.Atoi(matches[1])
+		if err != nil {
+			logger.Error(err, "Parsing the attempt number from provisioning request", "requestName", pr.Name)
+			return 1
+		} else {
+			return int32(number)
+		}
```

  Done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-22T18:49:35Z

/assign @mimowo

- **pkg/controller/admissionchecks/provisioning/controller.go:219** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-22T18:26:33Z
```diff
@@ -240,8 +213,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByDeactivation, fmt.Sprintf("Deactivating workload because capacity for %v has been revoked", oldPr.Name))
```

  you need an API call for updating the Workload status, separate from updating the spec below.
  
  And this is a sign that we are missing an check in the integration test.

- **pkg/controller/admissionchecks/provisioning/controller.go:217** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-22T18:45:14Z
```diff
@@ -240,8 +213,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
```

  Something is off in terms of separation of concerns.
  
  This controller should be updating the check statuses.
  
  The workload controller then uses the information in the check statuses to decide whether to evict the workload.
  
  That separation allows any admission check to not have to reimplement what to do about `CheckStateRejected`.

- **pkg/controller/admissionchecks/provisioning/provisioning.go:82** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-22T18:48:55Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
```

  You can always do `logger := log.FromContext(ctx)` in the caller.
  
  The point is that, when we have a logger, we don't need to call `FromContext` again, which can be expensive in some situations.

- **pkg/controller/admissionchecks/provisioning/provisioning.go:89** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-22T18:49:14Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func ProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matchesWorkloadAndCheck(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	if len(matches) > 0 {
+		number, err := strconv.Atoi(matches[1])
+		if err != nil {
+			logger.Error(err, "Parsing the attempt number from provisioning request", "requestName", pr.Name)
+			return 1
+		}
+		return int32(number)
+	}
+	logger.Info("No attempt suffix in provisioning request", "requestName", pr.Name)
```

  Error?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-23T10:52:01Z

- **pkg/controller/admissionchecks/provisioning/controller.go:115** by [@mimowo](https://github.com/mimowo) — 2024-05-23T10:09:58Z
```diff
@@ -118,10 +113,8 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	if !workload.HasQuotaReservation(wl) || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		// 1.2 workload has no reservation or is finished
-		log.V(5).Info("workload with no reservation, delete owned requests")
-		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
```

  I suppose this is not necessary for the CapacityRevoked support. If so, can we extract to a dedicated PR?

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:264** by [@mimowo](https://github.com/mimowo) — 2024-05-23T10:17:53Z
```diff
@@ -260,30 +266,6 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
 				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
 			})
-
-			ginkgo.By("Removing the quota reservation from the workload", func() {
```

  If now the workload is preserved, and this is an expected change we advertise in the release note, we should probably check it. I think we could use `gomega.Consistently` to prove that it continues to exist.

- **pkg/controller/admissionchecks/provisioning/controller.go:223** by [@mimowo](https://github.com/mimowo) — 2024-05-23T10:24:51Z
```diff
@@ -240,8 +213,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByDeactivation, fmt.Sprintf("Deactivating workload because capacity for %v has been revoked", oldPr.Name))
+					if err := c.deactivateWorkload(ctx, wl); err != nil {
+						return nil, err
+					}
+					c.record.Eventf(wl, corev1.EventTypeNormal, "CapacityRevoked", "Deactivating workload because capacity for %v has been revoked", oldPr.Name)
```

  This seems like a warning-level event. It suggests something is misconfigured.

- **pkg/controller/admissionchecks/provisioning/controller.go:472** by [@mimowo](https://github.com/mimowo) — 2024-05-23T10:36:37Z
```diff
@@ -526,7 +467,13 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
 					}
 				}
-			case prProvisioned:
+			case isCapacityRevoked(pr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					// happens only if the job allows retries and a user sets .spec.backOffLimit > 0
```

  I would like to explain this a little better. First, this situation may happen even if retries are not allowed if this controller reconciles before the Job controller transitions the Job to Failed. Second, there are other ways to enable retries, for example using podFailurePolicy with `ignore` action.
  
  Maybe something like:
  ```
  // We mark the admission check as rejected to trigger workload eviction and deactivation. 
  // This is needed to prevent replacement pods being stuck in the pending phase indefinitely
  // as the nodes are already deleted by Cluster Autoscaler.
  ```
  I think something along the lines would be helpful in release notes also.

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:442** by [@mimowo](https://github.com/mimowo) — 2024-05-23T10:40:00Z
```diff
@@ -456,6 +438,127 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 
+		ginkgo.It("Should deactivate the workload when it's not Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job allows retries and a user sets .spec.backOffLimit > 0
```

  Let's drop this comment.

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:472** by [@mimowo](https://github.com/mimowo) — 2024-05-23T10:41:13Z
```diff
@@ -456,6 +438,127 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 
+		ginkgo.It("Should deactivate the workload when it's not Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job allows retries and a user sets .spec.backOffLimit > 0
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					util.SetWorkloadsAdmissionCheck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as CapacityRevoked", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.CapacityRevoked,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.CapacityRevoked,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking if workload is deactivated and an event is emitted", func() {
```

  Check also the Evicted condition. And if the corresponding metric of the evicted workloads is incremented.

- **pkg/controller/admissionchecks/provisioning/provisioning.go:36** by [@mimowo](https://github.com/mimowo) — 2024-05-23T10:45:14Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
```

  I'm hesitant here, it feels this move of functions could be a dedicated cleanup PR. It distracts from reviewing the main body of changes. OTOH doing a little bit of drive-by cleanup should be fine.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-23T12:36:42Z

- **pkg/controller/admissionchecks/provisioning/controller.go:115** by [@PBundyra](https://github.com/PBundyra) — 2024-05-23T12:36:42Z
```diff
@@ -118,10 +113,8 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	if !workload.HasQuotaReservation(wl) || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		// 1.2 workload has no reservation or is finished
-		log.V(5).Info("workload with no reservation, delete owned requests")
-		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
```

  It actually is. If MaxRunDuration time will be exceeded, workload may get finished before ClusterAutoscaler sets CapacityRevoked. Hence we may not be able to sync with the conditions, set appropriate status and emit an event

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-23T12:39:31Z

- **pkg/controller/admissionchecks/provisioning/controller.go:472** by [@PBundyra](https://github.com/PBundyra) — 2024-05-23T12:39:31Z
```diff
@@ -526,7 +467,13 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
 					}
 				}
-			case prProvisioned:
+			case isCapacityRevoked(pr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					// happens only if the job allows retries and a user sets .spec.backOffLimit > 0
```

  Looks great, thank you

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-23T12:43:14Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:36** by [@PBundyra](https://github.com/PBundyra) — 2024-05-23T12:43:14Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
```

  Since I've already addressed some comments regarding those function I'd rather stick with this PR for better visibility of changes if you don't mind. However, I'll keep this in mind and will dedicate a separate PR for this kind of changes in the future

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-23T12:47:44Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:82** by [@PBundyra](https://github.com/PBundyra) — 2024-05-23T12:47:44Z
```diff
@@ -0,0 +1,155 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isProvisionedFalse(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionFalse(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func GetProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matches(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
```

  I see, thank you for the explanation. Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-23T12:48:00Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:36** by [@mimowo](https://github.com/mimowo) — 2024-05-23T12:48:00Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
```

  sgtm

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-23T12:49:08Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:442** by [@PBundyra](https://github.com/PBundyra) — 2024-05-23T12:49:08Z
```diff
@@ -456,6 +438,127 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 
+		ginkgo.It("Should deactivate the workload when it's not Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job allows retries and a user sets .spec.backOffLimit > 0
```

  Fixed!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-23T12:50:40Z

- **pkg/controller/admissionchecks/provisioning/controller.go:115** by [@mimowo](https://github.com/mimowo) — 2024-05-23T12:50:40Z
```diff
@@ -118,10 +113,8 @@ func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reco
 		return reconcile.Result{}, client.IgnoreNotFound(err)
 	}
 
-	if !workload.HasQuotaReservation(wl) || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-		// 1.2 workload has no reservation or is finished
-		log.V(5).Info("workload with no reservation, delete owned requests")
-		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	if !workload.HasQuotaReservation(wl) || workload.IsFinished(wl) {
```

  Ah, got it, so we keep ProvReq to give CA time to set the condition. I think the relationship is not obvious, please explain the motivation for coupling the changes in the PR description.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T20:10:52Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:472** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T20:10:52Z
```diff
@@ -456,6 +438,127 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 
+		ginkgo.It("Should deactivate the workload when it's not Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job allows retries and a user sets .spec.backOffLimit > 0
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					util.SetWorkloadsAdmissionCheck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as CapacityRevoked", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.CapacityRevoked,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.CapacityRevoked,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking if workload is deactivated and an event is emitted", func() {
```

  Fixed, without checking the metric though as it would require a lot of changes in tests.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T20:18:33Z

- **pkg/controller/admissionchecks/provisioning/controller.go:219** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T20:18:33Z
```diff
@@ -240,8 +213,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByDeactivation, fmt.Sprintf("Deactivating workload because capacity for %v has been revoked", oldPr.Name))
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T20:34:24Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:264** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T20:34:24Z
```diff
@@ -260,30 +266,6 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
 				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
 			})
-
-			ginkgo.By("Removing the quota reservation from the workload", func() {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T20:53:48Z

- **pkg/controller/admissionchecks/provisioning/controller.go:223** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T20:53:48Z
```diff
@@ -240,8 +213,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByDeactivation, fmt.Sprintf("Deactivating workload because capacity for %v has been revoked", oldPr.Name))
+					if err := c.deactivateWorkload(ctx, wl); err != nil {
+						return nil, err
+					}
+					c.record.Eventf(wl, corev1.EventTypeNormal, "CapacityRevoked", "Deactivating workload because capacity for %v has been revoked", oldPr.Name)
```

  Could you elaborate please? I thought it won't be treated as a warning-level event since its type is `Normal` instead of `Warning`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T20:53:56Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:89** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T20:53:56Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/log"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func ProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matchesWorkloadAndCheck(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(ctx context.Context, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
+	logger := log.FromContext(ctx)
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	if len(matches) > 0 {
+		number, err := strconv.Atoi(matches[1])
+		if err != nil {
+			logger.Error(err, "Parsing the attempt number from provisioning request", "requestName", pr.Name)
+			return 1
+		}
+		return int32(number)
+	}
+	logger.Info("No attempt suffix in provisioning request", "requestName", pr.Name)
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T20:56:34Z

- **pkg/controller/admissionchecks/provisioning/controller.go:217** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T20:56:34Z
```diff
@@ -240,8 +213,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
```

  As we discussed offline it's a bit complexed to unify behavior of the workload controller for every `CheckStateRejected` as it may be caused by different reasons, and it deserves a separate PR.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-27T21:29:14Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:77** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T21:19:16Z
```diff
@@ -0,0 +1,148 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"errors"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	"github.com/go-logr/logr"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func ProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matchesWorkloadAndCheck(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(log *logr.Logger, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
```

  ```suggestion
  func getAttempt(log logr.Logger, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
  ```
  
  Logger is already an interface, so a pointer by itself.

- **pkg/controller/admissionchecks/provisioning/controller.go:223** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T21:23:24Z
```diff
@@ -240,8 +213,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByDeactivation, fmt.Sprintf("Deactivating workload because capacity for %v has been revoked", oldPr.Name))
+					if err := c.deactivateWorkload(ctx, wl); err != nil {
+						return nil, err
+					}
+					c.record.Eventf(wl, corev1.EventTypeNormal, "CapacityRevoked", "Deactivating workload because capacity for %v has been revoked", oldPr.Name)
```

  ```suggestion
  					c.record.Eventf(wl, corev1.EventTypeWarning, "CapacityRevoked", "Deactivating workload because capacity for %v has been revoked", oldPr.Name)
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:549** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T21:29:10Z
```diff
@@ -583,6 +533,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload, msg string) error {
+	updatedWl := &kueue.Workload{}
+	if err := c.client.Get(ctx, client.ObjectKeyFromObject(wl), updatedWl); err != nil {
+		return err
+	}
+	workload.SetEvictedCondition(updatedWl, kueue.WorkloadEvictedByDeactivation, msg)
+	if err := workload.ApplyAdmissionStatus(ctx, c.client, updatedWl, true); err != nil {
+		return err
+	}
+	updatedWl.Spec.Active = ptr.To(false)
+	return c.client.Update(ctx, updatedWl)
```

  I think this will cause conflicts in general. You need to get the workload again after doing an Apply.
  
  Alternative, you can set `Active` to false via Patch.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T07:52:10Z

- **pkg/controller/admissionchecks/provisioning/controller.go:223** by [@mimowo](https://github.com/mimowo) — 2024-05-28T07:52:10Z
```diff
@@ -240,8 +213,17 @@ func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Wo
 		attempt := int32(1)
 		shouldCreatePr := false
 		if exists {
-			attempt = getAttempt(ctx, oldPr, wl.Name, checkName)
-			if apimeta.IsStatusConditionTrue(oldPr.Status.Conditions, autoscaling.Failed) {
+			switch {
+			case isCapacityRevoked(oldPr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					workload.SetEvictedCondition(wl, kueue.WorkloadEvictedByDeactivation, fmt.Sprintf("Deactivating workload because capacity for %v has been revoked", oldPr.Name))
+					if err := c.deactivateWorkload(ctx, wl); err != nil {
+						return nil, err
+					}
+					c.record.Eventf(wl, corev1.EventTypeNormal, "CapacityRevoked", "Deactivating workload because capacity for %v has been revoked", oldPr.Name)
```

  Yeah, changing to `Warning` is what I wanted to suggest.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T07:55:11Z

- **pkg/controller/admissionchecks/provisioning/controller.go:541** by [@mimowo](https://github.com/mimowo) — 2024-05-28T07:55:11Z
```diff
@@ -583,6 +533,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload, msg string) error {
+	updatedWl := &kueue.Workload{}
+	if err := c.client.Get(ctx, client.ObjectKeyFromObject(wl), updatedWl); err != nil {
+		return err
+	}
+	workload.SetEvictedCondition(updatedWl, kueue.WorkloadEvictedByDeactivation, msg)
```

  Only set evicted condition if it is not set yet. Analogous as we do [here](https://github.com/kubernetes-sigs/kueue/blob/2b5184c3b1da933321b1c52795f4b2e04094c7ec/pkg/controller/core/workload_controller.go#L184-L214). Note that a workload may get evicted due to another reason in the meanwhile.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T07:57:59Z

- **pkg/controller/admissionchecks/provisioning/controller.go:548** by [@mimowo](https://github.com/mimowo) — 2024-05-28T07:57:59Z
```diff
@@ -583,6 +533,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload, msg string) error {
+	updatedWl := &kueue.Workload{}
+	if err := c.client.Get(ctx, client.ObjectKeyFromObject(wl), updatedWl); err != nil {
+		return err
+	}
+	workload.SetEvictedCondition(updatedWl, kueue.WorkloadEvictedByDeactivation, msg)
+	if err := workload.ApplyAdmissionStatus(ctx, c.client, updatedWl, true); err != nil {
+		return err
+	}
+	updatedWl.Spec.Active = ptr.To(false)
```

  I think the order should be first set `active: false`, then add the `WorkloadInactive` condition. The message should be reverse-engineered unfortunately, as we do [here](https://github.com/kubernetes-sigs/kueue/blob/2b5184c3b1da933321b1c52795f4b2e04094c7ec/pkg/controller/core/workload_controller.go#L184-L214). I think actually setting `active: false` here should be enough. The code to add the workload condition and determine the message can be moved to the `workload_controller`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T08:00:49Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:472** by [@mimowo](https://github.com/mimowo) — 2024-05-28T08:00:49Z
```diff
@@ -456,6 +438,127 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 
+		ginkgo.It("Should deactivate the workload when it's not Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job allows retries and a user sets .spec.backOffLimit > 0
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					util.SetWorkloadsAdmissionCheck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as CapacityRevoked", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.CapacityRevoked,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.CapacityRevoked,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking if workload is deactivated and an event is emitted", func() {
```

  It should be in a follow up at least.
  
  However, I think if you set `active: false`, and let the `workload_controller` handle setting `WorkloadInactive` condition the metric will bump (see [here](https://github.com/kubernetes-sigs/kueue/blob/2b5184c3b1da933321b1c52795f4b2e04094c7ec/pkg/controller/core/workload_controller.go#L211))

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-28T08:59:46Z

- **pkg/controller/admissionchecks/provisioning/provisioning.go:77** by [@PBundyra](https://github.com/PBundyra) — 2024-05-28T08:59:46Z
```diff
@@ -0,0 +1,148 @@
+/*
+Copyright 2024 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"errors"
+	"fmt"
+	"regexp"
+	"strconv"
+	"strings"
+	"time"
+
+	"github.com/go-logr/logr"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/apis/provisioningrequest/autoscaling.x-k8s.io/v1beta1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/util/admissioncheck"
+)
+
+func isProvisioned(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+}
+
+func isAccepted(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Accepted)
+}
+
+func isFailed(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+}
+
+func isBookingExpired(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.BookingExpired)
+}
+
+func isCapacityRevoked(pr *autoscaling.ProvisioningRequest) bool {
+	return apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityRevoked)
+}
+
+func ProvisioningRequestName(workloadName, checkName string, attempt int32) string {
+	fullName := fmt.Sprintf("%s-%s-%d", workloadName, checkName, int(attempt))
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestNamePrefix(workloadName, checkName string) string {
+	fullName := fmt.Sprintf("%s-%s-", workloadName, checkName)
+	return limitObjectName(fullName)
+}
+
+func getProvisioningRequestPodTemplateName(prName, podsetName string) string {
+	fullName := fmt.Sprintf("%s-%s-%s", podTemplatesPrefix, prName, podsetName)
+	return limitObjectName(fullName)
+}
+
+func matchesWorkloadAndCheck(pr *autoscaling.ProvisioningRequest, workloadName, checkName string) bool {
+	attemptRegex := getAttemptRegex(workloadName, checkName)
+	matches := attemptRegex.FindStringSubmatch(pr.Name)
+	return len(matches) > 0
+}
+
+func getAttempt(log *logr.Logger, pr *autoscaling.ProvisioningRequest, workloadName, checkName string) int32 {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-28T09:43:55Z

- **pkg/controller/admissionchecks/provisioning/controller.go:549** by [@PBundyra](https://github.com/PBundyra) — 2024-05-28T09:43:55Z
```diff
@@ -583,6 +533,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload, msg string) error {
+	updatedWl := &kueue.Workload{}
+	if err := c.client.Get(ctx, client.ObjectKeyFromObject(wl), updatedWl); err != nil {
+		return err
+	}
+	workload.SetEvictedCondition(updatedWl, kueue.WorkloadEvictedByDeactivation, msg)
+	if err := workload.ApplyAdmissionStatus(ctx, c.client, updatedWl, true); err != nil {
+		return err
+	}
+	updatedWl.Spec.Active = ptr.To(false)
+	return c.client.Update(ctx, updatedWl)
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-28T09:44:06Z

- **pkg/controller/admissionchecks/provisioning/controller.go:541** by [@PBundyra](https://github.com/PBundyra) — 2024-05-28T09:44:06Z
```diff
@@ -583,6 +533,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload, msg string) error {
+	updatedWl := &kueue.Workload{}
+	if err := c.client.Get(ctx, client.ObjectKeyFromObject(wl), updatedWl); err != nil {
+		return err
+	}
+	workload.SetEvictedCondition(updatedWl, kueue.WorkloadEvictedByDeactivation, msg)
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-28T12:19:00Z

- **pkg/controller/admissionchecks/provisioning/controller.go:548** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-28T12:19:00Z
```diff
@@ -583,6 +533,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload, msg string) error {
+	updatedWl := &kueue.Workload{}
+	if err := c.client.Get(ctx, client.ObjectKeyFromObject(wl), updatedWl); err != nil {
+		return err
+	}
+	workload.SetEvictedCondition(updatedWl, kueue.WorkloadEvictedByDeactivation, msg)
+	if err := workload.ApplyAdmissionStatus(ctx, c.client, updatedWl, true); err != nil {
+		return err
+	}
+	updatedWl.Spec.Active = ptr.To(false)
```

  The problem for the reverse engineering is that the logic needs to know that the ProvReq has the CapacityRevoked condition. And the workload controller should not know about ProvReqs, due to separation of concerns. My recommendation to @PBundyra was that we should just rely on the Rejected status for the admission checks. However, right now this is limited just to the case when ProvReqs have WorkloadInactive, and not other Rejects (like reaching the maximum number of failures).
  
  I think we should change the logic to always set the workload to inactive when the admission check is in Reject and the workload is not admitted.
  But I'm undecided whether we should leave it for a follow up.
  
  I think it might be safer to do it as a preparatory PR, instead of a follow up.
  
  That said, I'm tempted to leave these two PRs for v0.8 not to delay v0.7 any further.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T12:25:00Z

- **pkg/controller/admissionchecks/provisioning/controller.go:548** by [@mimowo](https://github.com/mimowo) — 2024-05-28T12:25:00Z
```diff
@@ -583,6 +533,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload, msg string) error {
+	updatedWl := &kueue.Workload{}
+	if err := c.client.Get(ctx, client.ObjectKeyFromObject(wl), updatedWl); err != nil {
+		return err
+	}
+	workload.SetEvictedCondition(updatedWl, kueue.WorkloadEvictedByDeactivation, msg)
+	if err := workload.ApplyAdmissionStatus(ctx, c.client, updatedWl, true); err != nil {
+		return err
+	}
+	updatedWl.Spec.Active = ptr.To(false)
```

  I agree, in this PR we can just set `active: false` and rely on workload controller to set `Evicted: true` with "WorkloadInactive" reason, and generic message.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T13:23:03Z

- **pkg/controller/admissionchecks/provisioning/controller.go:548** by [@mimowo](https://github.com/mimowo) — 2024-05-28T13:23:03Z
```diff
@@ -583,6 +533,19 @@ func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest) []ku
 	})
 }
 
+func (c *Controller) deactivateWorkload(ctx context.Context, wl *kueue.Workload, msg string) error {
+	updatedWl := &kueue.Workload{}
+	if err := c.client.Get(ctx, client.ObjectKeyFromObject(wl), updatedWl); err != nil {
+		return err
+	}
+	workload.SetEvictedCondition(updatedWl, kueue.WorkloadEvictedByDeactivation, msg)
+	if err := workload.ApplyAdmissionStatus(ctx, c.client, updatedWl, true); err != nil {
+		return err
+	}
+	updatedWl.Spec.Active = ptr.To(false)
```

  @alculquicondor I think going forward (0.8+) a viable approach could be to have a DeactivationTarget condition. It would be set before `active: false`. Then, its message could be used in a generic fashion for `Evicted` condition. A similar approach I menion here: https://github.com/kubernetes-sigs/kueue/issues/2291#issuecomment-2135185436

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-31T11:09:41Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:472** by [@PBundyra](https://github.com/PBundyra) — 2024-05-31T11:09:41Z
```diff
@@ -456,6 +438,127 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 
+		ginkgo.It("Should deactivate the workload when it's not Finished, and the ProvisioningRequest's condition is set to CapacityRevoked", func() {
+			// This happens only if the job allows retries and a user sets .spec.backOffLimit > 0
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					util.SetWorkloadsAdmissionCheck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the ProvisioningRequeset as CapacityRevoked", func() {
+				createdRequest := &autoscaling.ProvisioningRequest{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.CapacityRevoked,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.CapacityRevoked,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking if workload is deactivated and an event is emitted", func() {
```

  I've fixed this in this PR eventually

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-06T08:49:59Z

One more comment / question regarding sending workload updates per AC

- **pkg/controller/admissionchecks/provisioning/controller.go:510** by [@mimowo](https://github.com/mimowo) — 2024-06-06T08:49:26Z
```diff
@@ -554,23 +495,24 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 			}
 		}
 
+		wlPatch := workload.BaseSSAWorkload(wl)
 		existingCondition := workload.FindAdmissionCheck(wlPatch.Status.AdmissionChecks, checkState.Name)
+		var message string
 		if existingCondition != nil && existingCondition.State != checkState.State {
-			message := fmt.Sprintf("Admission check %s updated state from %s to %s", checkState.Name, existingCondition.State, checkState.State)
+			message = fmt.Sprintf("Admission check %s updated state from %s to %s", checkState.Name, existingCondition.State, checkState.State)
 			if checkState.Message != "" {
 				message += fmt.Sprintf(" with message %s", checkState.Message)
 			}
-			recorderMessages = append(recorderMessages, message)
 		}
-
 		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
-	}
-	if updated {
-		if err := c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership); err != nil {
-			return err
-		}
-		for i := range recorderMessages {
-			c.record.Event(wl, corev1.EventTypeNormal, "AdmissionCheckUpdated", api.TruncateEventMessage(recorderMessages[i]))
+
+		if updated {
+			if err := c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership); err != nil {
```

  Any reason to send the workload updates per admission checks?
  
  We used to send one build Patch for all updates to the workload which is more performant. In practice there should not be too many ACs, but still it is preferred to combine the workload updates, if possible.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-06T11:22:58Z

- **pkg/controller/admissionchecks/provisioning/controller.go:510** by [@PBundyra](https://github.com/PBundyra) — 2024-06-06T11:22:58Z
```diff
@@ -554,23 +495,24 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 			}
 		}
 
+		wlPatch := workload.BaseSSAWorkload(wl)
 		existingCondition := workload.FindAdmissionCheck(wlPatch.Status.AdmissionChecks, checkState.Name)
+		var message string
 		if existingCondition != nil && existingCondition.State != checkState.State {
-			message := fmt.Sprintf("Admission check %s updated state from %s to %s", checkState.Name, existingCondition.State, checkState.State)
+			message = fmt.Sprintf("Admission check %s updated state from %s to %s", checkState.Name, existingCondition.State, checkState.State)
 			if checkState.Message != "" {
 				message += fmt.Sprintf(" with message %s", checkState.Message)
 			}
-			recorderMessages = append(recorderMessages, message)
 		}
-
 		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
-	}
-	if updated {
-		if err := c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership); err != nil {
-			return err
-		}
-		for i := range recorderMessages {
-			c.record.Event(wl, corev1.EventTypeNormal, "AdmissionCheckUpdated", api.TruncateEventMessage(recorderMessages[i]))
+
+		if updated {
+			if err := c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership); err != nil {
```

  Fixed, thanks

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-06T19:51:36Z

/approve
/hold
for nit and description updates.

- **pkg/controller/admissionchecks/provisioning/controller.go:474** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-06T19:46:59Z
```diff
@@ -534,7 +469,15 @@ func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, ch
 						checkState.Message = apimeta.FindStatusCondition(pr.Status.Conditions, autoscaling.Failed).Message
 					}
 				}
-			case prProvisioned:
+			case isCapacityRevoked(pr):
+				if workload.IsActive(wl) && !workload.IsFinished(wl) {
+					// We mark the admission check as rejected to trigger workload eviction and deactivation.
```

  ```suggestion
  					// We mark the admission check as rejected to trigger workload eviction.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-07T07:52:38Z

Basically, lgtm

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:252** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-07T07:46:23Z
```diff
@@ -215,7 +251,7 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking that the provision request is created", func() {
 				provReqKey := types.NamespacedName{
```

  Couldn't remove each provReqKey? Because we introduced it on the top level in Line158?

- **pkg/workload/workload.go:620** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-07T07:50:32Z
```diff
@@ -616,6 +616,11 @@ func IsFinished(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadFinished)
 }
 
+// IsActive returns true if the workload is active.
+func IsActive(w *kueue.Workload) bool {
```

  Could you introduce this function to other places where using `ptr.Deref(w.Spec.Active, true)`?
  But, I'm ok with leaving it in the follow up.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-07T08:45:35Z

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:252** by [@PBundyra](https://github.com/PBundyra) — 2024-06-07T08:45:35Z
```diff
@@ -215,7 +251,7 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			ginkgo.By("Checking that the provision request is created", func() {
 				provReqKey := types.NamespacedName{
```

  Sure, fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-07T08:45:44Z

- **pkg/workload/workload.go:620** by [@PBundyra](https://github.com/PBundyra) — 2024-06-07T08:45:44Z
```diff
@@ -616,6 +616,11 @@ func IsFinished(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadFinished)
 }
 
+// IsActive returns true if the workload is active.
+func IsActive(w *kueue.Workload) bool {
```

  Done!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-07T08:48:20Z

Thank you!
/lgtm
/approve
