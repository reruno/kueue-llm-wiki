# PR #223: test: add testcases to cover ClusterQueueImpl methods

**Summary**: test: add testcases to cover ClusterQueueImpl methods

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/223

**Last updated**: 2022-04-19T22:06:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `test/add-test-cover-cluster-queue-implement` → `main`
- **Created**: 2022-04-15T16:04:43Z
- **Updated**: 2022-04-19T22:06:48Z
- **Merged**: 2022-04-19T16:15:16Z
- **Closed**: 2022-04-19T16:15:16Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 1   **+169 / -0**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-15T16:04:51Z

Hi @kerthcet. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T22:07:08Z

/ok-to-test

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-18T01:46:34Z

> I am in favor of doing something similar to
> 
> https://github.com/kubernetes-sigs/kueue/blob/a92130e5113090bad8add8a9df8b6d3f0eeb2ceb/pkg/cache/cache_test.go#L100
> 
> one tabulated test with operations and expectations, unless it is not easy to do.

That's what I thought at first, then I realized it's procedure-oriented not scenario-oriented just like heap_test.go, so I changed it this way.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-18T11:29:13Z

addressed all the comments @ahg-g

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-19T01:53:29Z

squashed.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-19T16:13:19Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-19T16:13:32Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/223#issuecomment-1102838130" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/223#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-15T20:00:36Z

I am in favor of doing something similar to https://github.com/kubernetes-sigs/kueue/blob/a92130e5113090bad8add8a9df8b6d3f0eeb2ceb/pkg/cache/cache_test.go#L100

one tabulated test with operations and expectations, unless it is not easy to do.

- **pkg/queue/cluster_queue_impl_test.go:52** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:35:36Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
```

  ```suggestion
  	// Just used to validate the update operation.
  ```

- **pkg/queue/cluster_queue_impl_test.go:57** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:36:26Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
```

  ```suggestion
  		t.Error("failed to update a workload in ClusterQueue")
  ```

- **pkg/queue/cluster_queue_impl_test.go:76** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:37:37Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
```

  ```suggestion
  		t.Error("clusterQueue should be empty")
  ```

- **pkg/queue/cluster_queue_impl_test.go:44** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:38:03Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
```

  ```suggestion
  		t.Error("clusterQueue should be empty")
  ```

- **pkg/queue/cluster_queue_impl_test.go:41** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:42:26Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
```

  ```suggestion
  	wl := testing.MakeWorkload("workload-1", defaultNamespace).Obj()
  ```
  
  and import `sigs.k8s.io/kueue/pkg/testing`

- **pkg/queue/cluster_queue_impl_test.go:73** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:44:42Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
```

  ```suggestion
  	wl1 := testing.MakeWorkload("workload-1", defaultNamespace).Obj()
  	wl1.CreationTimestamp = metav1.NewTime(now)
  	wl2 := testing.MakeWorkload("workload-2", defaultNamespace).Obj()
  	wl1.CreationTimestamp = metav1.NewTime(now.Add(time.Second))
  ```

- **pkg/queue/cluster_queue_impl_test.go:73** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:45:58Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
```

  you could also add CreationTimestamp to the WorkloadWrapper if you want so you can do:
  
  ```
  wl1 := testing.MakeWorkload("workload-2", defaultNamespace).CreationTimestamp(metav1.NewTime(now)).Obj()
  ```

- **pkg/queue/cluster_queue_impl_test.go:93** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:46:53Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
```

  ```suggestion
  		t.Error("clusterQueue should be empty")
  ```

- **pkg/queue/cluster_queue_impl_test.go:108** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:47:44Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
```

  ditto regarding how to create the workloads and replacing cluster queue with clusterQueue here and all places below please

- **pkg/queue/cluster_queue_impl_test.go:89** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:52:11Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if cq.Pending() != 2 {
+		t.Error("cluster queue should have two workload")
+	}
+
+	cq.Delete(wl1)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have only one workload")
+	}
+
+	// Change workload item, ClusterQueue.Delete should only care about the namespace and name.
+	wl2.Spec = kueue.WorkloadSpec{QueueName: "default"}
```

  not sure if this is related to the `Delete` test, I would remove it.

- **pkg/queue/cluster_queue_impl_test.go:125** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:52:48Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if cq.Pending() != 2 {
+		t.Error("cluster queue should have two workload")
+	}
+
+	cq.Delete(wl1)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have only one workload")
+	}
+
+	// Change workload item, ClusterQueue.Delete should only care about the namespace and name.
+	wl2.Spec = kueue.WorkloadSpec{QueueName: "default"}
+	cq.Delete(wl2)
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should have be empty")
```

  ```suggestion
  		t.Error("clusterQueue should be empty")
  ```

- **pkg/queue/cluster_queue_impl_test.go:102** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:53:30Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if cq.Pending() != 2 {
+		t.Error("cluster queue should have two workload")
+	}
+
+	cq.Delete(wl1)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have only one workload")
+	}
+
+	// Change workload item, ClusterQueue.Delete should only care about the namespace and name.
+	wl2.Spec = kueue.WorkloadSpec{QueueName: "default"}
+	cq.Delete(wl2)
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should have be empty")
+	}
+}
+
+func Test_Dump(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+
+	if _, ok := cq.Dump(); ok {
+		t.Error("cluster queue should be empty")
+	}
```

  not necessary

- **pkg/queue/cluster_queue_impl_test.go:173** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:55:07Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if cq.Pending() != 2 {
+		t.Error("cluster queue should have two workload")
+	}
+
+	cq.Delete(wl1)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have only one workload")
+	}
+
+	// Change workload item, ClusterQueue.Delete should only care about the namespace and name.
+	wl2.Spec = kueue.WorkloadSpec{QueueName: "default"}
+	cq.Delete(wl2)
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should have be empty")
+	}
+}
+
+func Test_Dump(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+
+	if _, ok := cq.Dump(); ok {
+		t.Error("cluster queue should be empty")
+	}
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if data, ok := cq.Dump(); !(ok && data.HasAll("workload-1", "workload-2")) {
+		t.Error("dump data is not right")
+	}
+
+	cq.Delete(wl1)
+	cq.Delete(wl2)
+
+	if _, ok := cq.Dump(); ok {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Info(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl := newWorkload("workload-1")
+
+	if info := cq.Info(keyFunc(workload.NewInfo(wl))); info != nil {
+		t.Error("workload doesn't exists")
```

  ```suggestion
  		t.Error("workload doesn't exist")
  ```

- **pkg/queue/cluster_queue_impl_test.go:178** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:55:42Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if cq.Pending() != 2 {
+		t.Error("cluster queue should have two workload")
+	}
+
+	cq.Delete(wl1)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have only one workload")
+	}
+
+	// Change workload item, ClusterQueue.Delete should only care about the namespace and name.
+	wl2.Spec = kueue.WorkloadSpec{QueueName: "default"}
+	cq.Delete(wl2)
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should have be empty")
+	}
+}
+
+func Test_Dump(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+
+	if _, ok := cq.Dump(); ok {
+		t.Error("cluster queue should be empty")
+	}
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if data, ok := cq.Dump(); !(ok && data.HasAll("workload-1", "workload-2")) {
+		t.Error("dump data is not right")
+	}
+
+	cq.Delete(wl1)
+	cq.Delete(wl2)
+
+	if _, ok := cq.Dump(); ok {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Info(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl := newWorkload("workload-1")
+
+	if info := cq.Info(keyFunc(workload.NewInfo(wl))); info != nil {
+		t.Error("workload doesn't exists")
+	}
+
+	cq.PushOrUpdate(wl)
+	if info := cq.Info(keyFunc(workload.NewInfo(wl))); info == nil {
+		t.Error("workload exists, result shouldn't be nil")
```

  ```suggestion
  		t.Error("expected workload to exist")
  ```

- **pkg/queue/cluster_queue_impl_test.go:201** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:56:39Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if cq.Pending() != 2 {
+		t.Error("cluster queue should have two workload")
+	}
+
+	cq.Delete(wl1)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have only one workload")
+	}
+
+	// Change workload item, ClusterQueue.Delete should only care about the namespace and name.
+	wl2.Spec = kueue.WorkloadSpec{QueueName: "default"}
+	cq.Delete(wl2)
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should have be empty")
+	}
+}
+
+func Test_Dump(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+
+	if _, ok := cq.Dump(); ok {
+		t.Error("cluster queue should be empty")
+	}
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if data, ok := cq.Dump(); !(ok && data.HasAll("workload-1", "workload-2")) {
+		t.Error("dump data is not right")
+	}
+
+	cq.Delete(wl1)
+	cq.Delete(wl2)
+
+	if _, ok := cq.Dump(); ok {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Info(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl := newWorkload("workload-1")
+
+	if info := cq.Info(keyFunc(workload.NewInfo(wl))); info != nil {
+		t.Error("workload doesn't exists")
+	}
+
+	cq.PushOrUpdate(wl)
+	if info := cq.Info(keyFunc(workload.NewInfo(wl))); info == nil {
+		t.Error("workload exists, result shouldn't be nil")
+	}
+}
+
+func Test_AddFromQueue(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workload to cluster queue.
+	wl := newWorkload("workload-1")
+	queue := &Queue{
+		items: map[string]*workload.Info{
+			wl.Name: workload.NewInfo(wl),
+		},
+	}
+	cq.PushOrUpdate(wl)
+
+	if added := cq.AddFromQueue(queue); added {
+		t.Error("workload shouldn't be added to the cluster queue for it's already exists")
```

  ```suggestion
  		t.Error("expected workload not to be added")
  ```

- **pkg/queue/cluster_queue_impl_test.go:106** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:59:08Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
```

  nit: comments like this don't add more information, the operation is self descriptive; here and other places in the reset of the test

- **pkg/queue/cluster_queue_impl_test.go:147** by [@ahg-g](https://github.com/ahg-g) — 2022-04-15T19:59:52Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if cq.Pending() != 2 {
+		t.Error("cluster queue should have two workload")
+	}
+
+	cq.Delete(wl1)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have only one workload")
+	}
+
+	// Change workload item, ClusterQueue.Delete should only care about the namespace and name.
+	wl2.Spec = kueue.WorkloadSpec{QueueName: "default"}
+	cq.Delete(wl2)
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should have be empty")
+	}
+}
+
+func Test_Dump(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+
+	if _, ok := cq.Dump(); ok {
+		t.Error("cluster queue should be empty")
+	}
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
```

  nit: I would remove those empty lines, it just makes the file too sparse and so longer than it should be.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-18T11:16:37Z

- **pkg/queue/cluster_queue_impl_test.go:73** by [@kerthcet](https://github.com/kerthcet) — 2022-04-18T11:16:37Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
```

  It has already been implemented as `Creation`

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-18T11:17:55Z

- **pkg/queue/cluster_queue_impl_test.go:108** by [@kerthcet](https://github.com/kerthcet) — 2022-04-18T11:17:55Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
```

  got it.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-18T11:24:38Z

- **pkg/queue/cluster_queue_impl_test.go:89** by [@kerthcet](https://github.com/kerthcet) — 2022-04-18T11:24:38Z
```diff
@@ -0,0 +1,261 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha1"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	defaultNamespace = "default"
+)
+
+func Test_PushOrUpdate(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+	wl := newWorkload("workload-1")
+
+	if cq.Pending() != 0 {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have one workload")
+	}
+
+	// Just used for validate Update operation.
+	wl.ResourceVersion = "1"
+	cq.PushOrUpdate(wl)
+	newWl := cq.Pop()
+	if cq.Pending() != 0 || newWl.Obj.ResourceVersion != "1" {
+		t.Error("failed to update workload in cluster queue")
+	}
+}
+
+func Test_Pop(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	now := time.Now()
+	newWorkload := func(name string, t metav1.Time) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.CreationTimestamp = t
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	wl1 := newWorkload("workload-1", metav1.NewTime(now))
+	wl2 := newWorkload("workload-2", metav1.NewTime(now.Add(time.Second)))
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	newWl := cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-1" {
+		t.Error("failed to Pop workload")
+	}
+
+	newWl = cq.Pop()
+	if newWl == nil || newWl.Obj.Name != "workload-2" {
+		t.Error("failed to Pop workload")
+	}
+
+	if cq.Pop() != nil {
+		t.Error("cluster queue should be empty")
+	}
+}
+
+func Test_Delete(t *testing.T) {
+	cq := newClusterQueueImpl(keyFunc, byCreationTime)
+	newWorkload := func(name string) *kueue.Workload {
+		wl := &kueue.Workload{}
+		wl.Name = name
+		wl.Namespace = defaultNamespace
+		return wl
+	}
+
+	// Add workloads to cluster queue.
+	wl1 := newWorkload("workload-1")
+	wl2 := newWorkload("workload-2")
+	cq.PushOrUpdate(wl1)
+	cq.PushOrUpdate(wl2)
+
+	if cq.Pending() != 2 {
+		t.Error("cluster queue should have two workload")
+	}
+
+	cq.Delete(wl1)
+	if cq.Pending() != 1 {
+		t.Error("cluster queue should have only one workload")
+	}
+
+	// Change workload item, ClusterQueue.Delete should only care about the namespace and name.
+	wl2.Spec = kueue.WorkloadSpec{QueueName: "default"}
```

  We used to call `delete` with `workload` obj as parameter passed through to `heap.Delete`, then I changed to delete with workload key, so I added this test.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-18T22:05:13Z

thanks, pls squash
