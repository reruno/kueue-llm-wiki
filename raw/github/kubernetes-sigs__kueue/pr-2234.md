# PR #2234: Add condition WaitingForReplacementPods for pod groups integration

**Summary**: Add condition WaitingForReplacementPods for pod groups integration

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2234

**Last updated**: 2024-05-30T14:06:05Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/add-condition-WaitingForReplacementPods-for-pod-groups-integration` → `main`
- **Created**: 2024-05-20T09:06:50Z
- **Updated**: 2024-05-30T14:06:05Z
- **Merged**: 2024-05-24T18:43:10Z
- **Closed**: 2024-05-24T18:43:10Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 6   **+1206 / -25**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add condition WaitingForReplacementPods for pod groups integration.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2207 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Pod Integration: Add condition WaitingForReplacementPods to Workloads of pod groups with incomplete number of pods
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-20T09:06:53Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-20T09:07:06Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1c2075e3c57aaf00dd21f4d22b9895e55ea65808 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6650da31e1e9e8000821f348 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2234--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2234--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTIyMzQtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.rxTfxMoX51kEOmw2n754eMC96LJ_UNegGnuGyKRDewo)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-20T09:30:07Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-20T09:55:56Z

/assign @trasc

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-21T06:37:22Z

/test all

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-24T18:41:38Z

LGTM label has been added.  <details>Git tree hash: d22055cf144807ed663f8aa495662b4f33171f8c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-24T18:41:38Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2234#pullrequestreview-2077616765" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2234#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T14:06:02Z

/release-note-edit

```release-note
Pod Integration: Add condition WaitingForReplacementPods to Workloads of pod groups with incomplete number of pods
```

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-21T05:39:11Z

Overall LGTM

- **pkg/controller/jobframework/interface.go:125** by [@trasc](https://github.com/trasc) — 2024-05-20T16:05:19Z
```diff
@@ -116,6 +117,12 @@ type ComposableJob interface {
 	ForEach(f func(obj runtime.Object))
 }
 
+// JobWithCustomWorkloadConditions interface should be implemented by generic jobs,
+// when custom workload conditions should be updated after ensure that the workload exists.
+type JobWithCustomWorkloadConditions interface {
+	CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool)
+}
```

  ```suggestion
  // JobWithCustomWorkloadConditions interface should be implemented by generic jobs,
  // when custom workload conditions should be propagated to its workload.
  type JobWithCustomWorkloadConditions interface {
          // - add short description
  	CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool)
  }
  ```

- **pkg/controller/jobframework/reconciler.go:383** by [@trasc](https://github.com/trasc) — 2024-05-21T05:26:06Z
```diff
@@ -373,6 +374,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
+	// Update workload conditions if implemented JobWithCustomWorkloadConditions interface.
+	if jobCond, ok := job.(JobWithCustomWorkloadConditions); ok {
+		if conditions, updated := jobCond.CustomWorkloadConditions(wl); updated {
+			wlPatch := workload.BaseSSAWorkload(wl)
+			wlPatch.Status.Conditions = conditions
+			return reconcile.Result{}, r.client.Status().Patch(ctx, wlPatch, client.Apply,
+				client.FieldOwner(fmt.Sprintf("%s-custom", constants.JobControllerName)))
```

  Let's make `fmt.Sprintf("%s-custom", constants.JobControllerName))` a constant, there is no point in formatting it at runtime if nothing is changing.

- **pkg/controller/jobs/pod/pod_controller.go:1264** by [@trasc](https://github.com/trasc) — 2024-05-21T05:31:24Z
```diff
@@ -1215,6 +1217,81 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type:   kueue.WorkloadWaitingForReplacementPods,
+			Status: metav1.ConditionFalse,
+		}
+	}
+
+	var updated bool
+
+	if replCondStatus {
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else {
+		if replCond.Status != metav1.ConditionFalse {
+			replCond.Status = metav1.ConditionFalse
+			updated = true
+		}
+	}
+
+	if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
```

  ```suggestion
  	if  apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
  ```

- **pkg/controller/jobs/pod/pod_controller.go:1259** by [@trasc](https://github.com/trasc) — 2024-05-21T05:34:57Z
```diff
@@ -1215,6 +1217,81 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type:   kueue.WorkloadWaitingForReplacementPods,
+			Status: metav1.ConditionFalse,
+		}
+	}
+
+	var updated bool
+
+	if replCondStatus {
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else {
+		if replCond.Status != metav1.ConditionFalse {
+			replCond.Status = metav1.ConditionFalse
```

  Maybe we should keep a generic reason and message here, and not update it in L1264-L1274

- **pkg/util/testing/wrappers.go:192** by [@trasc](https://github.com/trasc) — 2024-05-21T05:37:44Z
```diff
@@ -189,6 +189,22 @@ func (w *WorkloadWrapper) Admitted(a bool) *WorkloadWrapper {
 	return w
 }
 
+func (w *WorkloadWrapper) WaitingForPodsReady(condStatus bool) *WorkloadWrapper {
```

  The name is misleading , ` WaitiForPodsReady` is a totally different functionality.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-21T05:51:08Z

- **pkg/controller/jobs/pod/pod_controller.go:1264** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-21T05:51:08Z
```diff
@@ -1215,6 +1217,81 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type:   kueue.WorkloadWaitingForReplacementPods,
+			Status: metav1.ConditionFalse,
+		}
+	}
+
+	var updated bool
+
+	if replCondStatus {
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else {
+		if replCond.Status != metav1.ConditionFalse {
+			replCond.Status = metav1.ConditionFalse
+			updated = true
+		}
+	}
+
+	if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
```

  But we need reason and message in the next steps.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-21T06:36:30Z

- **pkg/util/testing/wrappers.go:192** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-21T06:36:30Z
```diff
@@ -189,6 +189,22 @@ func (w *WorkloadWrapper) Admitted(a bool) *WorkloadWrapper {
 	return w
 }
 
+func (w *WorkloadWrapper) WaitingForPodsReady(condStatus bool) *WorkloadWrapper {
```

  Thanks

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-21T06:36:52Z

- **pkg/controller/jobframework/reconciler.go:383** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-21T06:36:52Z
```diff
@@ -373,6 +374,16 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		return ctrl.Result{}, err
 	}
 
+	// Update workload conditions if implemented JobWithCustomWorkloadConditions interface.
+	if jobCond, ok := job.(JobWithCustomWorkloadConditions); ok {
+		if conditions, updated := jobCond.CustomWorkloadConditions(wl); updated {
+			wlPatch := workload.BaseSSAWorkload(wl)
+			wlPatch.Status.Conditions = conditions
+			return reconcile.Result{}, r.client.Status().Patch(ctx, wlPatch, client.Apply,
+				client.FieldOwner(fmt.Sprintf("%s-custom", constants.JobControllerName)))
```

  Good point, thank you.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-21T06:37:06Z

- **pkg/controller/jobframework/interface.go:125** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-21T06:37:06Z
```diff
@@ -116,6 +117,12 @@ type ComposableJob interface {
 	ForEach(f func(obj runtime.Object))
 }
 
+// JobWithCustomWorkloadConditions interface should be implemented by generic jobs,
+// when custom workload conditions should be updated after ensure that the workload exists.
+type JobWithCustomWorkloadConditions interface {
+	CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool)
+}
```

  Added.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-21T10:47:35Z

- **pkg/controller/jobs/pod/pod_controller.go:1259** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-21T10:47:35Z
```diff
@@ -1215,6 +1217,81 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type:   kueue.WorkloadWaitingForReplacementPods,
+			Status: metav1.ConditionFalse,
+		}
+	}
+
+	var updated bool
+
+	if replCondStatus {
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else {
+		if replCond.Status != metav1.ConditionFalse {
+			replCond.Status = metav1.ConditionFalse
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-21T21:34:12Z

- **apis/kueue/v1beta1/workload_types.go:333** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:05:34Z
```diff
@@ -327,6 +327,10 @@ const (
 
 	// WorkloadRequeued means that the Workload was requeued due to eviction.
 	WorkloadRequeued = "Requeued"
+
+	// WorkloadWaitingForReplacementPods is True when Kueue doesn't observe all
+	// the Pods declared for the group.
+	WorkloadWaitingForReplacementPods = "WaitingForReplacementPods"
```

  let's put this in the pod controller for now, given that it's not universal to other integrations, and the pod integration is disabled by default.

- **pkg/controller/jobframework/interface.go:124** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:08:18Z
```diff
@@ -116,6 +117,13 @@ type ComposableJob interface {
 	ForEach(f func(obj runtime.Object))
 }
 
+// JobWithCustomWorkloadConditions interface should be implemented by generic jobs,
+// when custom workload conditions should be updated after ensure that the workload exists.
+type JobWithCustomWorkloadConditions interface {
+	// CustomWorkloadConditions return custom workload conditions and status changed or not.
+	CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool)
```

  Can we instead put it somewhere inside the implementation of `FindMatchingWorkloads` for the pod integration? We are already removing excess pods and doing patches to the Workload object there.

- **pkg/controller/jobs/pod/pod_controller.go:1307** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:16:00Z
```diff
@@ -1215,6 +1217,82 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: kueue.WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = kueue.WorkloadPodsReady
+			replCond.Message = "Has not runnable or not succeeded pods"
+			updated = true
+		}
+
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else if replCond.Status != metav1.ConditionFalse {
+		replCond.Status = metav1.ConditionFalse
+		updated = true
+	}
+
+	if updated {
+		replCond.ObservedGeneration = wl.Generation
+		replCond.LastTransitionTime = metav1.Now()
+	}
+
+	return replCond, updated
+}
+
+// hasNotRunnableNorSucceededPods returns true if it has at least one not runnable
```

  I prefer we base this on counters.
  
  If somehow a Failed pod disappears, and all other pods are Succeeded, we are still waiting for a replacement Pod.

- **test/integration/controller/jobs/pod/pod_controller_test.go:699** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:23:48Z
```diff
@@ -675,6 +675,20 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 					}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
 				})
 
+				ginkgo.By("checking that WaitingForReplacementPods status is set to true", func() {
```

  I would also like a stronger test case:
  
  1. Evict the group
  2. Observe the Pod to have a deletionTimestamp
  3. Immediately readmit the workload
  4. Observe that the workload has the condition for WaitingForReplacementPod set to true with reason related to the eviction.
  5. Only now: create replacement pod
  6. Observe condition WaitingForReplacementPod transition back to false
  
  I think the existing implementation should pass this test, but the above is what worries the customers (that the Workload gets readmitted too fast and they miss the eviction condition).

- **pkg/controller/jobframework/reconciler.go:56** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:28:33Z
```diff
@@ -52,7 +53,8 @@ import (
 )
 
 const (
-	FailedToStartFinishedReason = "FailedToStart"
+	JobControllerWithCustomWorkloadConditionsName = constants.JobControllerName + "-with-custom-workload-conditions"
```

  This name is visible to users, so it's not great.
  
  So prefer to have a name specific to the pod integration.

- **pkg/controller/jobs/pod/pod_controller.go:1264** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:31:50Z
```diff
@@ -1215,6 +1217,82 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: kueue.WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = kueue.WorkloadPodsReady
+			replCond.Message = "Has not runnable or not succeeded pods"
```

  ```suggestion
  			replCond.Message = "Some Failed pods need replacement"
  ```

- **pkg/controller/jobs/pod/pod_controller.go:1289** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:33:29Z
```diff
@@ -1215,6 +1217,82 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: kueue.WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = kueue.WorkloadPodsReady
+			replCond.Message = "Has not runnable or not succeeded pods"
+			updated = true
+		}
+
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
```

  in which case do we reach this?

- **pkg/controller/jobs/pod/pod_controller.go:1293** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:34:04Z
```diff
@@ -1215,6 +1217,82 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: kueue.WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = kueue.WorkloadPodsReady
+			replCond.Message = "Has not runnable or not succeeded pods"
+			updated = true
+		}
+
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else if replCond.Status != metav1.ConditionFalse {
+		replCond.Status = metav1.ConditionFalse
```

  also change reason and message

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-22T07:46:40Z

- **pkg/controller/jobs/pod/pod_controller.go:1289** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-22T07:46:40Z
```diff
@@ -1215,6 +1217,82 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: kueue.WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = kueue.WorkloadPodsReady
+			replCond.Message = "Has not runnable or not succeeded pods"
+			updated = true
+		}
+
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
```

  After replacement finished we are setting status to false. And when replacement happening again we need to set to true and update. In this case we need to know that status updated. But also we don't need to update workload if it's no changes.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-22T14:10:33Z

- **pkg/controller/jobframework/interface.go:124** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-22T14:10:33Z
```diff
@@ -116,6 +117,13 @@ type ComposableJob interface {
 	ForEach(f func(obj runtime.Object))
 }
 
+// JobWithCustomWorkloadConditions interface should be implemented by generic jobs,
+// when custom workload conditions should be updated after ensure that the workload exists.
+type JobWithCustomWorkloadConditions interface {
+	// CustomWorkloadConditions return custom workload conditions and status changed or not.
+	CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool)
```

  I think that `FindMatchingWorkloads` already doing so much. The function name is `Find...` that is shouldn't update workload. So, if we can move this logic to another place, that's better in my opinion.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-22T15:59:55Z

- **pkg/controller/jobs/pod/pod_controller.go:1293** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-22T15:59:55Z
```diff
@@ -1215,6 +1217,82 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: kueue.WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = kueue.WorkloadPodsReady
+			replCond.Message = "Has not runnable or not succeeded pods"
+			updated = true
+		}
+
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else if replCond.Status != metav1.ConditionFalse {
+		replCond.Status = metav1.ConditionFalse
```

  Added

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-22T16:00:48Z

- **pkg/controller/jobs/pod/pod_controller.go:1264** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-22T16:00:48Z
```diff
@@ -1215,6 +1217,82 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: kueue.WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = kueue.WorkloadPodsReady
+			replCond.Message = "Has not runnable or not succeeded pods"
```

  Changed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-22T16:01:06Z

- **pkg/controller/jobframework/reconciler.go:56** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-22T16:01:06Z
```diff
@@ -52,7 +53,8 @@ import (
 )
 
 const (
-	FailedToStartFinishedReason = "FailedToStart"
+	JobControllerWithCustomWorkloadConditionsName = constants.JobControllerName + "-with-custom-workload-conditions"
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-22T16:12:32Z

- **apis/kueue/v1beta1/workload_types.go:333** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-22T16:12:32Z
```diff
@@ -327,6 +327,10 @@ const (
 
 	// WorkloadRequeued means that the Workload was requeued due to eviction.
 	WorkloadRequeued = "Requeued"
+
+	// WorkloadWaitingForReplacementPods is True when Kueue doesn't observe all
+	// the Pods declared for the group.
+	WorkloadWaitingForReplacementPods = "WaitingForReplacementPods"
```

  Thank you. Moved.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-22T16:13:09Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:699** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-22T16:13:09Z
```diff
@@ -675,6 +675,20 @@ var _ = ginkgo.Describe("Pod controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 					}, util.ConsistentDuration, util.Interval).Should(gomega.BeFalse())
 				})
 
+				ginkgo.By("checking that WaitingForReplacementPods status is set to true", func() {
```

  Good point. Thanks. Added.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-22T18:37:20Z

- **pkg/controller/jobs/pod/pod_controller.go:1307** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-22T18:37:20Z
```diff
@@ -1215,6 +1217,82 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods()
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: kueue.WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = kueue.WorkloadPodsReady
+			replCond.Message = "Has not runnable or not succeeded pods"
+			updated = true
+		}
+
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else if replCond.Status != metav1.ConditionFalse {
+		replCond.Status = metav1.ConditionFalse
+		updated = true
+	}
+
+	if updated {
+		replCond.ObservedGeneration = wl.Generation
+		replCond.LastTransitionTime = metav1.Now()
+	}
+
+	return replCond, updated
+}
+
+// hasNotRunnableNorSucceededPods returns true if it has at least one not runnable
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-24T18:04:46Z

I forgot to send my review yesterday :(

- **pkg/controller/jobs/pod/pod_controller.go:1121** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-23T18:53:29Z
```diff
@@ -1105,6 +1118,10 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 			return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", errors.Join(roleHashErrors...))
 		}
 
+		if absentCount := int(ps.Count) - len(roleActivePods) - len(roleInactivePods); absentCount > 0 {
```

  why discount the inactivePods? Aren't those the pods that need to be replaced?

- **pkg/controller/jobs/pod/pod_controller.go:1256** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-23T18:54:55Z
```diff
@@ -1215,6 +1233,88 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	if !p.isGroup {
+		return nil, false
+	}
+
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods() || p.absentPods > 0
```

  this can be simplified to just `p.absentPods` if you follow my suggestion above.

- **pkg/controller/jobs/pod/pod_controller.go:1295** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-23T18:57:24Z
```diff
@@ -1215,6 +1233,88 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	if !p.isGroup {
+		return nil, false
+	}
+
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods() || p.absentPods > 0
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = WorkloadPodsFailed
+			replCond.Message = "Some Failed pods need replacement"
+			updated = true
+		}
+
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else if replCond.Status != metav1.ConditionFalse {
+		replCond.Status = metav1.ConditionFalse
+		replCond.Reason = kueue.WorkloadPodsReady
+		replCond.Message = "Pods replacement finished"
```

  ```suggestion
  		replCond.Message = "No pods need replacement"
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T18:23:45Z

- **pkg/controller/jobs/pod/pod_controller.go:1121** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T18:23:45Z
```diff
@@ -1105,6 +1118,10 @@ func (p *Pod) FindMatchingWorkloads(ctx context.Context, c client.Client, r reco
 			return nil, nil, fmt.Errorf("failed to calculate pod role hash: %w", errors.Join(roleHashErrors...))
 		}
 
+		if absentCount := int(ps.Count) - len(roleActivePods) - len(roleInactivePods); absentCount > 0 {
```

  Good point. Thanks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T18:23:56Z

- **pkg/controller/jobs/pod/pod_controller.go:1295** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T18:23:56Z
```diff
@@ -1215,6 +1233,88 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	if !p.isGroup {
+		return nil, false
+	}
+
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods() || p.absentPods > 0
+
+	// Nothing to change.
+	if replCond == nil && !replCondStatus {
+		return nil, false
+	}
+
+	var updated bool
+
+	if replCond == nil {
+		replCond = &metav1.Condition{
+			Type: WorkloadWaitingForReplacementPods,
+		}
+		updated = true
+	}
+
+	if replCondStatus {
+		if evictCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); evictCond != nil && evictCond.Status == metav1.ConditionTrue {
+			if replCond.Reason != evictCond.Reason {
+				replCond.Reason = evictCond.Reason
+				updated = true
+			}
+			if replCond.Message != evictCond.Message {
+				replCond.Message = evictCond.Message
+				updated = true
+			}
+		} else if replCond.Status != metav1.ConditionTrue {
+			replCond.Reason = WorkloadPodsFailed
+			replCond.Message = "Some Failed pods need replacement"
+			updated = true
+		}
+
+		if replCond.Status != metav1.ConditionTrue {
+			replCond.Status = metav1.ConditionTrue
+			updated = true
+		}
+	} else if replCond.Status != metav1.ConditionFalse {
+		replCond.Status = metav1.ConditionFalse
+		replCond.Reason = kueue.WorkloadPodsReady
+		replCond.Message = "Pods replacement finished"
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T18:38:53Z

- **pkg/controller/jobs/pod/pod_controller.go:1256** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T18:38:53Z
```diff
@@ -1215,6 +1233,88 @@ func (p *Pod) ReclaimablePods() ([]kueue.ReclaimablePod, error) {
 	return result, nil
 }
 
+func (p *Pod) CustomWorkloadConditions(wl *kueue.Workload) ([]metav1.Condition, bool) {
+	var (
+		conditions           []metav1.Condition
+		needToUpdateWorkload bool
+	)
+	if condition, updated := p.waitingForReplacementPodsCondition(wl); condition != nil {
+		conditions = append(conditions, *condition)
+		if updated {
+			needToUpdateWorkload = true
+		}
+	}
+	return conditions, needToUpdateWorkload
+}
+
+func (p *Pod) waitingForReplacementPodsCondition(wl *kueue.Workload) (*metav1.Condition, bool) {
+	if !p.isGroup {
+		return nil, false
+	}
+
+	replCond := apimeta.FindStatusCondition(wl.Status.Conditions, WorkloadWaitingForReplacementPods)
+	replCondStatus := p.hasNotRunnableNorSucceededPods() || p.absentPods > 0
```

  Optimised.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-24T18:41:33Z

/lgtm
/approve
