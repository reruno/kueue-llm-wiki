# PR #2239: Plain Pod gets deleted once admitted via ProvisioningRequest (DWS)

**Summary**: Plain Pod gets deleted once admitted via ProvisioningRequest (DWS)

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2239

**Last updated**: 2024-05-30T14:30:19Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `Plain-Pod-gets-deleted` → `main`
- **Created**: 2024-05-20T16:03:17Z
- **Updated**: 2024-05-30T14:30:19Z
- **Merged**: 2024-05-22T18:22:59Z
- **Closed**: 2024-05-22T18:22:59Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@vladikkuzn](https://github.com/vladikkuzn), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 4   **+39 / -30**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Ignores pods' tolerations during equality check for admitted workloads
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes [#2213](https://github.com/kubernetes-sigs/kueue/issues/2213)

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Pod Integration: Prevent Pod from being deleted when admitted via ProvisioningRequest that has pod updates on tolerations
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-20T16:03:19Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-20T16:03:26Z

/assign

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-20T16:03:36Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 39571d03260aaeb464583ff5191d0751432bef05 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/664b74474d08780008c94fb8 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2239--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2239--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTIyMzktLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.QA1zpnmW-M64PMVmdVY8uKea6VkZr4gWUAFmHQjQx-Q)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-20T16:03:36Z

/cc @trasc @alculquicondor

### Comment by [@trasc](https://github.com/trasc) — 2024-05-20T16:10:41Z

/assign

### Comment by [@trasc](https://github.com/trasc) — 2024-05-20T16:11:18Z

/test all

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-21T05:20:35Z

LGTM label has been added.  <details>Git tree hash: 5e8cbb0f5bedafeb681fe1d3b14d0f5068185dee</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:49:23Z

/approve
/hold for additional test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-21T21:49:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2239#issuecomment-2123497087" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2239#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@trasc](https://github.com/trasc) — 2024-05-22T05:53:18Z

/test pull-kueue-test-integration-main

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-22T18:01:29Z

we can leave the additional test for a follow up.

/hold cancel

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-22T18:01:50Z

/cherry-pick release-0.6

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-05-22T18:01:52Z

@alculquicondor: once the present PR merges, I will cherry-pick it on top of release-0.6 in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2239#issuecomment-2125438031):

>/cherry-pick release-0.6


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-05-22T18:23:35Z

@alculquicondor: #2239 failed to apply on top of branch "release-0.6":
```
Applying: Plain Pod gets deleted once admitted via ProvisioningRequest (DWS)
Using index info to reconstruct a base tree...
M	pkg/controller/jobframework/reconciler.go
M	pkg/controller/jobs/job/job_controller_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobs/job/job_controller_test.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/job/job_controller_test.go
Auto-merging pkg/controller/jobframework/reconciler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
Patch failed at 0001 Plain Pod gets deleted once admitted via ProvisioningRequest (DWS)
When you have resolved this problem, run "git am --continue".
If you prefer to skip this patch, run "git am --skip" instead.
To restore the original branch and stop patching, run "git am --abort".

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2239#issuecomment-2125438031):

>/cherry-pick release-0.6


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T21:40:10Z

/release-note-edit
```release-note
Prevent Pod from being deleted when admitted via ProvisioningRequest that has pod updates on tolerations
```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T14:30:16Z

/release-note-edit
```release-note
Pod Integration: Prevent Pod from being deleted when admitted via ProvisioningRequest that has pod updates on tolerations
```

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-21T05:20:31Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-21T08:10:35Z

- **pkg/util/equality/podset.go:30** by [@mimowo](https://github.com/mimowo) — 2024-05-21T08:10:22Z
```diff
@@ -26,8 +26,8 @@ import (
 
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
-func comparePodTemplate(a, b *corev1.PodSpec) bool {
-	if !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
+func comparePodTemplate(a, b *corev1.PodSpec, ignoreTolerations bool) bool {
+	if !ignoreTolerations && !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
```

  Currently this PR only ignores the equality check on tolerations, while the [comment](https://github.com/kubernetes-sigs/kueue/issues/2213#issuecomment-2118287310) indicates we should also relax the validation for nodeSelectors. How confident we are tolerations are enough? 
  IIUC the `autoscaling.gke.io/provisioning-request` nodeSelector can be added as well based on the annotation.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-21T09:33:14Z

- **pkg/util/equality/podset.go:30** by [@trasc](https://github.com/trasc) — 2024-05-21T09:33:14Z
```diff
@@ -26,8 +26,8 @@ import (
 
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
-func comparePodTemplate(a, b *corev1.PodSpec) bool {
-	if !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
+func comparePodTemplate(a, b *corev1.PodSpec, ignoreTolerations bool) bool {
+	if !ignoreTolerations && !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
```

  The node selectors are not part of the equivalency checks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-21T09:38:48Z

- **pkg/util/equality/podset.go:30** by [@mimowo](https://github.com/mimowo) — 2024-05-21T09:38:48Z
```diff
@@ -26,8 +26,8 @@ import (
 
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
-func comparePodTemplate(a, b *corev1.PodSpec) bool {
-	if !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
+func comparePodTemplate(a, b *corev1.PodSpec, ignoreTolerations bool) bool {
+	if !ignoreTolerations && !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
```

  Got it, thanks for clarifying. Do we have this covered in a unit / integration test?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-21T09:44:08Z

- **pkg/controller/jobframework/reconciler.go:748** by [@mimowo](https://github.com/mimowo) — 2024-05-21T09:44:08Z
```diff
@@ -745,17 +745,17 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets, workload.IsAdmitted(wl)) {
```

  This ignoring of tolerations is done independently of the framework, but there is no issue with jobs, just with pods. I'm wondering if we should make the fix more specific. WDYT @alculquicondor ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-21T10:42:58Z

- **pkg/controller/jobs/job/job_controller_test.go:2402** by [@mimowo](https://github.com/mimowo) — 2024-05-21T10:42:58Z
```diff
@@ -2397,14 +2397,12 @@ func TestReconciler(t *testing.T) {
 				Key:      "tolerationkey2",
 				Operator: corev1.TolerationOpExists,
 				Effect:   corev1.TaintEffectNoSchedule,
-			}).
-				Suspend(false).
-				Obj(),
+			}).Suspend(false).Obj(),
 			wantJob: *baseJobWrapper.Clone().Toleration(corev1.Toleration{
-				Key:      "tolerationkey1",
+				Key:      "tolerationkey2",
```

  Please make sure we have an analogous test (either by adding or finding an existing) which demonstrate that node selectors are not part of the equality check.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2024-05-21T15:28:00Z

- **pkg/util/equality/podset.go:30** by [@vladikkuzn](https://github.com/vladikkuzn) — 2024-05-21T15:28:00Z
```diff
@@ -26,8 +26,8 @@ import (
 
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
-func comparePodTemplate(a, b *corev1.PodSpec) bool {
-	if !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
+func comparePodTemplate(a, b *corev1.PodSpec, ignoreTolerations bool) bool {
+	if !ignoreTolerations && !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
```

  It's not ignored, because it doesn't participate in comparison as of now

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-21T15:34:40Z

- **pkg/util/equality/podset.go:30** by [@mimowo](https://github.com/mimowo) — 2024-05-21T15:34:40Z
```diff
@@ -26,8 +26,8 @@ import (
 
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
-func comparePodTemplate(a, b *corev1.PodSpec) bool {
-	if !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
+func comparePodTemplate(a, b *corev1.PodSpec, ignoreTolerations bool) bool {
+	if !ignoreTolerations && !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
```

  I would just like to have a test to make sure this is the case, to prevent future regressions, since this is an important scenario.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-21T15:38:37Z

- **pkg/util/equality/podset.go:30** by [@mimowo](https://github.com/mimowo) — 2024-05-21T15:38:37Z
```diff
@@ -26,8 +26,8 @@ import (
 
 // TODO: Revisit this, maybe we should extend the check to everything that could potentially impact
 // the workload scheduling (priority, nodeSelectors(when suspended), tolerations and maybe more)
-func comparePodTemplate(a, b *corev1.PodSpec) bool {
-	if !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
+func comparePodTemplate(a, b *corev1.PodSpec, ignoreTolerations bool) bool {
+	if !ignoreTolerations && !equality.Semantic.DeepEqual(a.Tolerations, b.Tolerations) {
```

  Anyway we can probably defer this to a follow up manual testing

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-21T16:40:35Z

- **pkg/controller/jobframework/reconciler.go:748** by [@mimowo](https://github.com/mimowo) — 2024-05-21T16:40:34Z
```diff
@@ -745,17 +745,17 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets, workload.IsAdmitted(wl)) {
```

  On one hand the relaxation of the check isn't necessary for Jobs.
  
  On the other it seems that scoping the fix would be more involving because we would need to pass this information, the options I see:
  1. add a new param to `FindMatchingWorkloads`
  2. add a new interface like `CustomEquivalenceConfigJob` with a function like `ignoreTolerations() bool`
  
  None of these is appealing, especially since we will not need the custom options when the proper fix is implemented.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-21T21:48:59Z

- **pkg/controller/jobframework/reconciler.go:748** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:44:50Z
```diff
@@ -745,17 +745,17 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets, workload.IsAdmitted(wl)) {
```

  I'm ok with this for now. But can we use the condition status of QuotaReserved, instead of Admitted?

- **pkg/util/equality/podset_test.go:102** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-21T21:48:55Z
```diff
@@ -97,10 +97,9 @@ func TestComparePodSetSlices(t *testing.T) {
 			wantEqual: false,
 		},
 	}
-
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
-			got := ComparePodSetSlices(tc.a, tc.b)
+			got := ComparePodSetSlices(tc.a, tc.b, false)
```

  needs a unit test for true too

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-22T07:27:01Z

- **pkg/controller/jobframework/reconciler.go:748** by [@trasc](https://github.com/trasc) — 2024-05-22T07:27:01Z
```diff
@@ -745,17 +745,17 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets, workload.IsAdmitted(wl)) {
```

  I don't think it makes any difference, the toleration are only changing after the pods are unsuspended.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-22T14:12:37Z

- **pkg/controller/jobframework/reconciler.go:748** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-22T14:12:37Z
```diff
@@ -745,17 +745,17 @@ func equivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	jobPodSets := clearMinCountsIfFeatureDisabled(job.PodSets())
 
 	if runningPodSets := expectedRunningPodSets(ctx, c, wl); runningPodSets != nil {
-		if equality.ComparePodSetSlices(jobPodSets, runningPodSets) {
+		if equality.ComparePodSetSlices(jobPodSets, runningPodSets, workload.IsAdmitted(wl)) {
```

  sg, thanks for the reminder.
