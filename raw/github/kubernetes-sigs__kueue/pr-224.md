# PR #224: feat: add detailed errors to assignFlavors

**Summary**: feat: add detailed errors to assignFlavors

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/224

**Last updated**: 2022-05-04T03:05:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feature/add-datailed-errors-to-assignFlavors` → `main`
- **Created**: 2022-04-17T17:25:51Z
- **Updated**: 2022-05-04T03:05:23Z
- **Merged**: 2022-05-04T02:45:32Z
- **Closed**: 2022-05-04T02:45:32Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 2   **+131 / -29**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/189

#### Special notes for your reviewer:
Errors like: `Workload didn't fit: request cpu resource error: [flavor one doesn't match with node affinity, flavor two still lack 100 after borrowing, flavor three borrowing limit is exceeded]`

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-17T17:26:00Z

Hi @kerthcet. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-18T04:40:30Z

/ok-to-test

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T20:24:44Z

I will leave this to @alculquicondor, take my comments as suggestions...

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:26:19Z

/assign

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-28T08:40:38Z

Addressed the comments.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-29T01:39:08Z

Updated @alculquicondor

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-01T11:14:16Z

updated as advised.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-02T15:39:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/224#pullrequestreview-959227072" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/224#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-03T02:44:46Z

squashed.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-03T02:52:56Z

/retest

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-04T02:41:03Z

/lgtm

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-25T20:17:12Z

- **pkg/scheduler/scheduler.go:30** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T19:51:39Z
```diff
@@ -27,6 +27,7 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/labels"
 	"k8s.io/apimachinery/pkg/types"
+	utilerrors "k8s.io/apimachinery/pkg/util/errors"
```

  I would prefer either `apierrors` or just `errors`.

- **pkg/scheduler/scheduler.go:197** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T19:53:01Z
```diff
@@ -193,16 +194,16 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 // borrow from the cohort.
 // It returns whether the entry would fit. If it doesn't fit, the object is
 // unmodified.
-func (e *entry) assignFlavors(log logr.Logger, resourceFlavors map[string]*kueue.ResourceFlavor, cq *cache.ClusterQueue) bool {
+func (e *entry) assignFlavors(log logr.Logger, resourceFlavors map[string]*kueue.ResourceFlavor, cq *cache.ClusterQueue) (bool, error) {
```

  I don't think `error` is the right abstraction. Maybe we can have an `admissionStatus` struct with reasons per podSet and resource name?

- **pkg/scheduler/scheduler.go:293** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:03:38Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
```

  "resource unavailable in ClusterQueue"
  
  There is no need to include the resource name, as it's already part of the message in `assignFlavors`.

- **pkg/scheduler/scheduler.go:302** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:05:04Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
+		return "", 0, errs
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
-			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
+			// TODO: if "flavor not found" error emitted too frequently, we'd like to aggregate them into one error.
```

  I think this should be fine. Most likely different resources would use different flavors.

- **pkg/scheduler/scheduler.go:310** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:07:07Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
+		return "", 0, errs
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
-			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
+			// TODO: if "flavor not found" error emitted too frequently, we'd like to aggregate them into one error.
+			errs = append(errs, fmt.Errorf("flavor %s not found", flvLimit.Name))
 			continue
 		}
-		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
+			errs = append(errs, fmt.Errorf("flavor %s holds untolerated taint %s", flvLimit.Name, taint))
```

  ```suggestion
  			errs = append(errs, fmt.Errorf("flavor %s has untolerated taint %s", flvLimit.Name, taint))
  ```

- **pkg/scheduler/scheduler.go:315** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:09:02Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
+		return "", 0, errs
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
-			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
+			// TODO: if "flavor not found" error emitted too frequently, we'd like to aggregate them into one error.
+			errs = append(errs, fmt.Errorf("flavor %s not found", flvLimit.Name))
 			continue
 		}
-		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
+			errs = append(errs, fmt.Errorf("flavor %s holds untolerated taint %s", flvLimit.Name, taint))
 			continue
 		}
 		if match, err := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.Labels}}); !match || err != nil {
 			if err != nil {
-				log.Error(err, "Matching workload affinity against flavor; no flavor assigned")
-				return "", 0
+				errs = append(errs, fmt.Errorf("flavor %s match node affinity error: %s", flvLimit.Name, err))
```

  this would be an actual error. Maybe we should return it separately. Or the status can have an error field, separate from reasons.

- **pkg/scheduler/scheduler.go:318** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:10:02Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
+		return "", 0, errs
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
-			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
+			// TODO: if "flavor not found" error emitted too frequently, we'd like to aggregate them into one error.
+			errs = append(errs, fmt.Errorf("flavor %s not found", flvLimit.Name))
 			continue
 		}
-		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
+			errs = append(errs, fmt.Errorf("flavor %s holds untolerated taint %s", flvLimit.Name, taint))
 			continue
 		}
 		if match, err := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.Labels}}); !match || err != nil {
 			if err != nil {
-				log.Error(err, "Matching workload affinity against flavor; no flavor assigned")
-				return "", 0
+				errs = append(errs, fmt.Errorf("flavor %s match node affinity error: %s", flvLimit.Name, err))
+				return "", 0, errs
 			}
+			errs = append(errs, fmt.Errorf("flavor %s doesn't match with node affinity", flvLimit.Name))
```

  ```suggestion
  			errs = append(errs, fmt.Errorf("flavor %s doesn't match the node affinity", flvLimit.Name))
  ```

- **pkg/scheduler/scheduler.go:385** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:11:12Z
```diff
@@ -365,11 +379,10 @@ func flavorSelector(spec *corev1.PodSpec, allowedKeys sets.String) nodeaffinity.
 
 // fitsFlavorLimits returns whether a requested resource fits in a specific flavor's quota limits.
 // If it fits, also returns any borrowing required.
-func fitsFlavorLimits(name corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (bool, int64) {
+func fitsFlavorLimits(name corev1.ResourceName, val int64, cq *cache.ClusterQueue, flavor *cache.FlavorLimits) (bool, int64, error) {
 	used := cq.UsedResources[name][flavor.Name]
 	if flavor.Max != nil && used+val > *flavor.Max {
-		// Past borrowing limit.
-		return false, 0
+		return false, 0, fmt.Errorf("flavor %s borrowing limit is exceeded", flavor.Name)
```

  ```suggestion
  		return false, 0, fmt.Errorf("borrowing limit for flavor %s exceeded", flavor.Name)
  ```

- **pkg/scheduler/scheduler.go:401** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:12:39Z
```diff
@@ -381,12 +394,13 @@ func fitsFlavorLimits(name corev1.ResourceName, val int64, cq *cache.ClusterQueu
 	if borrow < 0 {
 		borrow = 0
 	}
-	if cohortUsed+val > cohortTotal {
-		// Doesn't fit even with borrowing.
+
+	lack := cohortUsed + val - cohortTotal
+	if lack > 0 {
 		// TODO(PostMVP): preemption could help if borrow == 0
-		return false, 0
+		return false, 0, fmt.Errorf("flavor %s still lack %d after borrowing", flavor.Name, lack)
```

  If there is no cohort, then there is no borrowing. Let's distinguish the 2 cases in the message.

- **pkg/scheduler/scheduler.go:183** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:17:05Z
```diff
@@ -178,8 +179,8 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
 			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
-		} else if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			e.inadmissibleReason = "Workload didn't fit in the remaining quota"
+		} else if ok, err := e.assignFlavors(log, snap.ResourceFlavors, cq); !ok {
+			e.inadmissibleReason = fmt.Sprintf("Workload didn't fit: %s", err.Error())
```

  What if the message is too long for the Event? Make sure to trim it:
  
  https://github.com/kubernetes/kubernetes/blob/f02682c628c530219966a00ae002d799f0d813dc/pkg/scheduler/schedule_one.go#L836-L844

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-25T20:21:27Z

- **pkg/scheduler/scheduler.go:293** by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T18:22:30Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
```

  ```suggestion
  		errs = append(errs, fmt.Errorf("no quotas defined for resource %s", name))
  ```

- **pkg/scheduler/scheduler.go:206** by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T20:03:57Z
```diff
@@ -193,16 +194,16 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 // borrow from the cohort.
 // It returns whether the entry would fit. If it doesn't fit, the object is
 // unmodified.
-func (e *entry) assignFlavors(log logr.Logger, resourceFlavors map[string]*kueue.ResourceFlavor, cq *cache.ClusterQueue) bool {
+func (e *entry) assignFlavors(log logr.Logger, resourceFlavors map[string]*kueue.ResourceFlavor, cq *cache.ClusterQueue) (bool, error) {
 	flavoredRequests := make([]workload.PodSetResources, 0, len(e.TotalRequests))
 	wUsed := make(cache.Resources)
 	wBorrows := make(cache.Resources)
 	for i, podSet := range e.TotalRequests {
 		flavors := make(map[corev1.ResourceName]string, len(podSet.Requests))
 		for resName, reqVal := range podSet.Requests {
-			rFlavor, borrow := findFlavorForResource(log, resName, reqVal, wUsed[resName], resourceFlavors, cq, &e.Obj.Spec.PodSets[i].Spec)
+			rFlavor, borrow, errs := findFlavorForResource(log, resName, reqVal, wUsed[resName], resourceFlavors, cq, &e.Obj.Spec.PodSets[i].Spec)
 			if rFlavor == "" {
-				return false
+				return false, fmt.Errorf("request %s resource error: %s", resName, aggregateErrors(errs))
```

  ```suggestion
  				return false, fmt.Errorf("insufficient %s: %s", resName, aggregateErrors(errs))
  ```

- **pkg/scheduler/scheduler.go:301** by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T20:05:47Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
+		return "", 0, errs
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
-			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
```

  keep this, this is actually a system error.

- **pkg/scheduler/scheduler.go:310** by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T20:08:59Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
+		return "", 0, errs
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
-			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
+			// TODO: if "flavor not found" error emitted too frequently, we'd like to aggregate them into one error.
+			errs = append(errs, fmt.Errorf("flavor %s not found", flvLimit.Name))
 			continue
 		}
-		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
+			errs = append(errs, fmt.Errorf("flavor %s holds untolerated taint %s", flvLimit.Name, taint))
```

  ```suggestion
  			errs = append(errs, fmt.Errorf("untolerated taint %s in flavor %s", taint, flvLimit.Name))
  ```

- **pkg/scheduler/scheduler.go:312** by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T20:09:45Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
+		return "", 0, errs
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
-			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
+			// TODO: if "flavor not found" error emitted too frequently, we'd like to aggregate them into one error.
+			errs = append(errs, fmt.Errorf("flavor %s not found", flvLimit.Name))
 			continue
 		}
-		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
+			errs = append(errs, fmt.Errorf("flavor %s holds untolerated taint %s", flvLimit.Name, taint))
 			continue
 		}
 		if match, err := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.Labels}}); !match || err != nil {
 			if err != nil {
-				log.Error(err, "Matching workload affinity against flavor; no flavor assigned")
```

  pls keep the error message, this is an indication of something wrong.

- **pkg/scheduler/scheduler.go:315** by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T20:10:36Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
+		return "", 0, errs
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
-			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
+			// TODO: if "flavor not found" error emitted too frequently, we'd like to aggregate them into one error.
+			errs = append(errs, fmt.Errorf("flavor %s not found", flvLimit.Name))
 			continue
 		}
-		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
+			errs = append(errs, fmt.Errorf("flavor %s holds untolerated taint %s", flvLimit.Name, taint))
 			continue
 		}
 		if match, err := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.Labels}}); !match || err != nil {
 			if err != nil {
-				log.Error(err, "Matching workload affinity against flavor; no flavor assigned")
-				return "", 0
+				errs = append(errs, fmt.Errorf("flavor %s match node affinity error: %s", flvLimit.Name, err))
```

  ```suggestion
  				errs = append(errs, fmt.Errorf("affinity match error for flavor %s: %s", flvLimit.Name, err))
  ```

- **pkg/scheduler/scheduler.go:401** by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T20:18:16Z
```diff
@@ -381,12 +394,13 @@ func fitsFlavorLimits(name corev1.ResourceName, val int64, cq *cache.ClusterQueu
 	if borrow < 0 {
 		borrow = 0
 	}
-	if cohortUsed+val > cohortTotal {
-		// Doesn't fit even with borrowing.
+
+	lack := cohortUsed + val - cohortTotal
+	if lack > 0 {
 		// TODO(PostMVP): preemption could help if borrow == 0
-		return false, 0
+		return false, 0, fmt.Errorf("flavor %s still lack %d after borrowing", flavor.Name, lack)
```

  ```suggestion
  		return false, 0, fmt.Errorf("insufficient quota in flavor %s; %d more needed", flavor.Name, lack)
  ```

- **pkg/scheduler/scheduler.go:183** by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T20:20:56Z
```diff
@@ -178,8 +179,8 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 			e.inadmissibleReason = fmt.Sprintf("Could not obtain workload namespace: %v", err)
 		} else if !cq.NamespaceSelector.Matches(labels.Set(ns.Labels)) {
 			e.inadmissibleReason = "Workload namespace doesn't match ClusterQueue selector"
-		} else if !e.assignFlavors(log, snap.ResourceFlavors, cq) {
-			e.inadmissibleReason = "Workload didn't fit in the remaining quota"
+		} else if ok, err := e.assignFlavors(log, snap.ResourceFlavors, cq); !ok {
+			e.inadmissibleReason = fmt.Sprintf("Workload didn't fit: %s", err.Error())
```

  ```suggestion
  			e.inadmissibleReason = fmt.Sprintf("Workload didn't fit; %s", err.Error())
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-28T08:35:52Z

- **pkg/scheduler/scheduler.go:302** by [@kerthcet](https://github.com/kerthcet) — 2022-04-27T03:53:41Z
```diff
@@ -285,36 +286,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, []error) {
+	var errs []error
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		errs = append(errs, fmt.Errorf("resource %s not found", name))
+		return "", 0, errs
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
-			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
+			// TODO: if "flavor not found" error emitted too frequently, we'd like to aggregate them into one error.
```

  `resourceFlavors` is the snapshot of cache's resourceFlavors, if flavor not found, it means we don't have flavor created in our cluster, we should record this and return immediately. We'll mark the CQ not ready when flavor not found in this PR https://github.com/kubernetes-sigs/kueue/pull/230. And this shouldn't happen in that case.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-28T13:57:38Z

- **pkg/scheduler/scheduler.go:208** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T13:49:42Z
```diff
@@ -188,21 +190,61 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
```

  Better return the full message here (the one that is currently in line 184)

- **pkg/scheduler/scheduler.go:226** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T13:52:27Z
```diff
@@ -188,21 +190,61 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
+	if s == nil {
+		return ""
+	}
+	return strings.Join(s.reasons, ", ")
+}
+
+// AppendReason appends given reasons to the admissionStatus.
+func (s *admissionStatus) AppendReason(reasons ...string) {
+	s.reasons = append(s.reasons, reasons...)
+}
+
+// IsSuccess returns true if "admissionStatus" is nil.
+func (s *admissionStatus) IsSuccess() bool {
+	return s == nil
+}
+
+// IsError returns true if "admissionStatus" has error.
+func (s *admissionStatus) IsError() bool {
+	return s != nil && s.err != nil
+}
+
+// AsStatus wraps an error in a admissionStatus.
+func AsStatus(err error) *admissionStatus {
+	return &admissionStatus{
+		reasons: []string{err.Error()},
```

  why not just the error? And then `Message` should call `err.Error()` if there is one.

- **pkg/scheduler/scheduler.go:340** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T13:54:15Z
```diff
@@ -285,36 +327,48 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, *admissionStatus) {
```

  Maybe all these fields can be part of the status.

- **pkg/scheduler/scheduler.go:443** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T13:55:41Z
```diff
@@ -381,12 +436,18 @@ func fitsFlavorLimits(name corev1.ResourceName, val int64, cq *cache.ClusterQueu
 	if borrow < 0 {
 		borrow = 0
 	}
-	if cohortUsed+val > cohortTotal {
-		// Doesn't fit even with borrowing.
+
+	lack := cohortUsed + val - cohortTotal
+	if lack > 0 {
+		if cq.Cohort == nil {
+			status.AppendReason(fmt.Sprintf("insufficient quota in flavor %s, %d more needed with no borrowing", flavor.Name, lack))
```

  ```suggestion
  			status.AppendReason(fmt.Sprintf("insufficient quota for flavor %s, %d more needed", flavor.Name, lack))
  ```

- **pkg/scheduler/scheduler.go:445** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T13:56:13Z
```diff
@@ -381,12 +436,18 @@ func fitsFlavorLimits(name corev1.ResourceName, val int64, cq *cache.ClusterQueu
 	if borrow < 0 {
 		borrow = 0
 	}
-	if cohortUsed+val > cohortTotal {
-		// Doesn't fit even with borrowing.
+
+	lack := cohortUsed + val - cohortTotal
+	if lack > 0 {
+		if cq.Cohort == nil {
+			status.AppendReason(fmt.Sprintf("insufficient quota in flavor %s, %d more needed with no borrowing", flavor.Name, lack))
+		} else {
+			status.AppendReason(fmt.Sprintf("insufficient quota in flavor %s, %d more needed after borrowing", flavor.Name, lack))
```

  ```suggestion
  			status.AppendReason(fmt.Sprintf("insufficient quota for flavor %s, %d more needed after borrowing", flavor.Name, lack))
  ```

- **pkg/scheduler/scheduler_test.go:1015** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T13:57:16Z
```diff
@@ -1012,12 +1012,7 @@ func TestEntryAssignFlavors(t *testing.T) {
 					},
 				},
 			},
-			wantFits: true,
-			wantFlavors: map[string]map[corev1.ResourceName]string{
-				"main": {
-					corev1.ResourceCPU: "two",
-				},
-			},
+			wantFits: false,
```

  why is this test changing? The only functional change should be the message

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-28T15:09:18Z

- **pkg/scheduler/scheduler_test.go:1015** by [@kerthcet](https://github.com/kerthcet) — 2022-04-28T14:17:56Z
```diff
@@ -1012,12 +1012,7 @@ func TestEntryAssignFlavors(t *testing.T) {
 					},
 				},
 			},
-			wantFits: true,
-			wantFlavors: map[string]map[corev1.ResourceName]string{
-				"main": {
-					corev1.ResourceCPU: "two",
-				},
-			},
+			wantFits: false,
```

  I returned quickly here https://github.com/kubernetes-sigs/kueue/blob/9306c2e91d288a230a93c4a09d6be074925408bc/pkg/scheduler/scheduler.go#L340-L344
  we used to perform with `continue`. Leave the reason here https://github.com/kubernetes-sigs/kueue/pull/224#discussion_r859353928

- **pkg/scheduler/scheduler.go:226** by [@kerthcet](https://github.com/kerthcet) — 2022-04-28T14:32:01Z
```diff
@@ -188,21 +190,61 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
+	if s == nil {
+		return ""
+	}
+	return strings.Join(s.reasons, ", ")
+}
+
+// AppendReason appends given reasons to the admissionStatus.
+func (s *admissionStatus) AppendReason(reasons ...string) {
+	s.reasons = append(s.reasons, reasons...)
+}
+
+// IsSuccess returns true if "admissionStatus" is nil.
+func (s *admissionStatus) IsSuccess() bool {
+	return s == nil
+}
+
+// IsError returns true if "admissionStatus" has error.
+func (s *admissionStatus) IsError() bool {
+	return s != nil && s.err != nil
+}
+
+// AsStatus wraps an error in a admissionStatus.
+func AsStatus(err error) *admissionStatus {
+	return &admissionStatus{
+		reasons: []string{err.Error()},
```

  If we have error, we'll return immediately. ` admissionStatus.Message()`  will also return the error message.

- **pkg/scheduler/scheduler.go:340** by [@kerthcet](https://github.com/kerthcet) — 2022-04-28T14:55:28Z
```diff
@@ -285,36 +327,48 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, *admissionStatus) {
```

  It seems like admissionStatus not only holds the status but also the result, prefer keep them the way they are.

- **pkg/scheduler/scheduler.go:208** by [@kerthcet](https://github.com/kerthcet) — 2022-04-28T15:03:24Z
```diff
@@ -188,21 +190,61 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
```

  done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-28T15:37:12Z

- **pkg/scheduler/scheduler.go:226** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T15:12:06Z
```diff
@@ -188,21 +190,61 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
+	if s == nil {
+		return ""
+	}
+	return strings.Join(s.reasons, ", ")
+}
+
+// AppendReason appends given reasons to the admissionStatus.
+func (s *admissionStatus) AppendReason(reasons ...string) {
+	s.reasons = append(s.reasons, reasons...)
+}
+
+// IsSuccess returns true if "admissionStatus" is nil.
+func (s *admissionStatus) IsSuccess() bool {
+	return s == nil
+}
+
+// IsError returns true if "admissionStatus" has error.
+func (s *admissionStatus) IsError() bool {
+	return s != nil && s.err != nil
+}
+
+// AsStatus wraps an error in a admissionStatus.
+func AsStatus(err error) *admissionStatus {
+	return &admissionStatus{
+		reasons: []string{err.Error()},
```

  yes, but it's kind of wasteful to store the message in 2 places (the error itself and reasons)

- **pkg/scheduler/scheduler.go:216** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T15:12:29Z
```diff
@@ -188,21 +190,62 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
+	if s == nil {
+		return ""
+	}
+	msg := strings.Join(s.reasons, ", ")
+	return fmt.Sprintf("Workload didn't fit, insufficient %s in %s, reasons: %s;", s.resourceName, s.podSet, msg)
```

  if it's an error, it's not about insufficient quota

- **pkg/scheduler/scheduler.go:344** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T15:14:26Z
```diff
@@ -285,36 +328,48 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, *admissionStatus) {
+	var status admissionStatus
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		return "", 0, AsStatus(fmt.Errorf("resource unavailable in ClusterQueue"))
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
 			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
-			continue
+			return "", 0, AsStatus(fmt.Errorf("flavor %s not found", flvLimit.Name))
```

  we should still continue instead of return

- **pkg/scheduler/scheduler.go:355** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T15:16:47Z
```diff
@@ -285,36 +328,48 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, *admissionStatus) {
+	var status admissionStatus
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		return "", 0, AsStatus(fmt.Errorf("resource unavailable in ClusterQueue"))
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
 			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
-			continue
+			return "", 0, AsStatus(fmt.Errorf("flavor %s not found", flvLimit.Name))
 		}
-		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
+			status.AppendReason(fmt.Sprintf("untolerated taint %s in flavor %s", taint, flvLimit.Name))
 			continue
 		}
 		if match, err := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.Labels}}); !match || err != nil {
 			if err != nil {
-				log.Error(err, "Matching workload affinity against flavor; no flavor assigned")
-				return "", 0
+				return "", 0, AsStatus(fmt.Errorf("affinity match error %s for flavor: %s", err, flvLimit.Name))
```

  ```suggestion
  				return "", 0, AsStatus(fmt.Errorf("matching affinity flavor %s: %w", flvLimit.Name, err))
  ```

- **pkg/scheduler/scheduler_test.go:1015** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T15:20:41Z
```diff
@@ -1012,12 +1012,7 @@ func TestEntryAssignFlavors(t *testing.T) {
 					},
 				},
 			},
-			wantFits: true,
-			wantFlavors: map[string]map[corev1.ResourceName]string{
-				"main": {
-					corev1.ResourceCPU: "two",
-				},
-			},
+			wantFits: false,
```

  ah ok, we won't have this case once #230 is finished.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-28T16:10:36Z

- **pkg/scheduler/scheduler.go:216** by [@kerthcet](https://github.com/kerthcet) — 2022-04-28T16:10:35Z
```diff
@@ -188,21 +190,62 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
+	if s == nil {
+		return ""
+	}
+	msg := strings.Join(s.reasons, ", ")
+	return fmt.Sprintf("Workload didn't fit, insufficient %s in %s, reasons: %s;", s.resourceName, s.podSet, msg)
```

  Then I'll change to report error from `admissionStatus.err`, and report reasons from `admissionStatus.reasons`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-28T17:13:17Z

- **pkg/scheduler/scheduler.go:206** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T17:09:59Z
```diff
@@ -199,9 +199,12 @@ type admissionStatus struct {
 
 // Message returns a concatenated message on reasons of the admissionStatus.
 func (s *admissionStatus) Message() string {
-	if s == nil {
+	if s.IsSuccess() {
 		return ""
 	}
+	if s.IsError() {
+		return fmt.Sprintf("Workload didn't fit, failed to find flavor for %s in %s: %s;", s.resourceName, s.podSet, s.err.Error())
```

  ```suggestion
  		return fmt.Sprintf("Could not assign a flavor for %s in podSet %s: %v", s.resourceName, s.podSet, s.err.Error())
  ```

- **pkg/scheduler/scheduler.go:238** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-28T17:11:41Z
```diff
@@ -224,16 +227,15 @@ func (s *admissionStatus) IsError() bool {
 // AsStatus wraps an error in a admissionStatus.
 func AsStatus(err error) *admissionStatus {
 	return &admissionStatus{
-		reasons: []string{err.Error()},
-		err:     err,
+		err: err,
 	}
 }
 
 // assignFlavors calculates the flavors that should be assigned to this entry
 // if admitted by this clusterQueue, including details of how much it needs to
 // borrow from the cohort.
-// It returns whether the entry would fit. If it doesn't fit, the object is
-// unmodified.
+// It returns admissionStatus indicating whether the entry fits. If it doesn't fit,
+// the object is unmodified.
```

  Let's be more clear:
  
  ```suggestion
  // the entry is unmodified.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-29T14:51:32Z

- **pkg/scheduler/scheduler.go:213** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T14:42:15Z
```diff
@@ -195,21 +197,64 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
+	if s.IsSuccess() {
+		return ""
+	}
+	if s.IsError() {
+		return fmt.Sprintf("Could not assign a flavor for %s in podSet %s: %v;", s.resourceName, s.podSet, s.err.Error())
```

  why the `;`?
  
  ```suggestion
  		return fmt.Sprintf("Could not assign a flavor for %s in podSet %s: %v", s.resourceName, s.podSet, s.err)
  ```

- **pkg/scheduler/scheduler.go:216** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T14:42:46Z
```diff
@@ -195,21 +197,64 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
+	if s.IsSuccess() {
+		return ""
+	}
+	if s.IsError() {
+		return fmt.Sprintf("Could not assign a flavor for %s in podSet %s: %v;", s.resourceName, s.podSet, s.err.Error())
+	}
+	msg := strings.Join(s.reasons, ", ")
+	return fmt.Sprintf("Workload didn't fit, insufficient %s in %s, reasons: %s;", s.resourceName, s.podSet, msg)
```

  ```suggestion
  	return fmt.Sprintf("Workload didn't fit, insufficient %s in %s: %s", s.resourceName, s.podSet, msg)
  ```

- **pkg/scheduler/scheduler.go:235** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T14:43:56Z
```diff
@@ -195,21 +197,64 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
+	if s.IsSuccess() {
+		return ""
+	}
+	if s.IsError() {
+		return fmt.Sprintf("Could not assign a flavor for %s in podSet %s: %v;", s.resourceName, s.podSet, s.err.Error())
+	}
+	msg := strings.Join(s.reasons, ", ")
+	return fmt.Sprintf("Workload didn't fit, insufficient %s in %s, reasons: %s;", s.resourceName, s.podSet, msg)
+}
+
+// AppendReason appends given reasons to the admissionStatus.
+func (s *admissionStatus) AppendReason(reasons ...string) {
+	s.reasons = append(s.reasons, reasons...)
+}
+
+// IsSuccess returns true if "admissionStatus" is nil.
+func (s *admissionStatus) IsSuccess() bool {
+	return s == nil
+}
+
+// IsError returns true if "admissionStatus" has error.
+func (s *admissionStatus) IsError() bool {
+	return s != nil && s.err != nil
+}
+
+// AsStatus wraps an error in a admissionStatus.
+func AsStatus(err error) *admissionStatus {
```

  this can be unexported

- **pkg/scheduler/scheduler.go:375** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T14:48:05Z
```diff
@@ -292,36 +337,49 @@ func findFlavorForResource(
 	wUsed map[string]int64,
 	resourceFlavors map[string]*kueue.ResourceFlavor,
 	cq *cache.ClusterQueue,
-	spec *corev1.PodSpec) (string, int64) {
+	spec *corev1.PodSpec) (string, int64, *admissionStatus) {
+	var status admissionStatus
+
+	if _, exists := cq.RequestableResources[name]; !exists {
+		return "", 0, AsStatus(fmt.Errorf("resource unavailable in ClusterQueue"))
+	}
+
 	// We will only check against the flavors' labels for the resource.
 	selector := flavorSelector(spec, cq.LabelKeys[name])
 	for _, flvLimit := range cq.RequestableResources[name] {
 		flavor, exist := resourceFlavors[flvLimit.Name]
 		if !exist {
 			log.Error(nil, "Flavor not found", "Flavor", flvLimit.Name)
+			status.AppendReason(fmt.Sprintf("flavor %s not found", flvLimit.Name))
 			continue
 		}
-		_, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
+		taint, untolerated := corev1helpers.FindMatchingUntoleratedTaint(flavor.Taints, spec.Tolerations, func(t *corev1.Taint) bool {
 			return t.Effect == corev1.TaintEffectNoSchedule || t.Effect == corev1.TaintEffectNoExecute
 		})
 		if untolerated {
+			status.AppendReason(fmt.Sprintf("untolerated taint %s in flavor %s", taint, flvLimit.Name))
 			continue
 		}
 		if match, err := selector.Match(&corev1.Node{ObjectMeta: metav1.ObjectMeta{Labels: flavor.Labels}}); !match || err != nil {
 			if err != nil {
-				log.Error(err, "Matching workload affinity against flavor; no flavor assigned")
-				return "", 0
+				return "", 0, AsStatus(fmt.Errorf("matching affinity flavor %s: %w", flvLimit.Name, err))
 			}
+			status.AppendReason(fmt.Sprintf("flavor %s doesn't match with node affinity", flvLimit.Name))
 			continue
 		}
 
 		// Check considering the flavor usage by previous pod sets.
-		ok, borrow := fitsFlavorLimits(name, val+wUsed[flavor.Name], cq, &flvLimit)
-		if ok {
-			return flavor.Name, borrow
+		borrow, s := fitsFlavorLimits(name, val+wUsed[flavor.Name], cq, &flvLimit)
+		if s.IsSuccess() {
+			return flavor.Name, borrow, nil
+		} else {
+			if s.IsError() {
+				return "", 0, s
+			}
+			status.AppendReason(s.reasons...)
 		}
```

  ```suggestion
  		if s.IsSuccess() {
  			return flavor.Name, borrow, nil
  		}
  		if s.IsError() {
  			return "", 0, s
  		}
  		status.AppendReason(s.reasons...)
  ```

- **pkg/scheduler/scheduler_test.go:1440** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-29T14:51:26Z
```diff
@@ -1404,12 +1423,12 @@ func TestEntryAssignFlavors(t *testing.T) {
 				}),
 			}
 			tc.clusterQueue.UpdateLabelKeys(resourceFlavors)
-			fits := e.assignFlavors(log, resourceFlavors, &tc.clusterQueue)
-			if fits != tc.wantFits {
-				t.Errorf("e.assignFlavors(_)=%t, want %t", fits, tc.wantFits)
+			status := e.assignFlavors(log, resourceFlavors, &tc.clusterQueue)
+			if status.IsSuccess() != tc.wantFits {
+				t.Errorf("e.assignFlavors(_)=%t, want %t", status.IsSuccess(), tc.wantFits)
 			}
 			var flavors map[string]map[corev1.ResourceName]string
-			if fits {
+			if status.IsSuccess() {
```

  Should we test the message if it doesn't fit?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-29T16:48:47Z

- **pkg/scheduler/scheduler_test.go:1440** by [@kerthcet](https://github.com/kerthcet) — 2022-04-29T16:48:47Z
```diff
@@ -1404,12 +1423,12 @@ func TestEntryAssignFlavors(t *testing.T) {
 				}),
 			}
 			tc.clusterQueue.UpdateLabelKeys(resourceFlavors)
-			fits := e.assignFlavors(log, resourceFlavors, &tc.clusterQueue)
-			if fits != tc.wantFits {
-				t.Errorf("e.assignFlavors(_)=%t, want %t", fits, tc.wantFits)
+			status := e.assignFlavors(log, resourceFlavors, &tc.clusterQueue)
+			if status.IsSuccess() != tc.wantFits {
+				t.Errorf("e.assignFlavors(_)=%t, want %t", status.IsSuccess(), tc.wantFits)
 			}
 			var flavors map[string]map[corev1.ResourceName]string
-			if fits {
+			if status.IsSuccess() {
```

  Add unfit message verifications.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-01T01:48:10Z

- **pkg/scheduler/scheduler.go:216** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T01:43:37Z
```diff
@@ -195,21 +197,64 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 	return entries
 }
 
+type admissionStatus struct {
+	podSet       string
+	resourceName string
+	reasons      []string
+	err          error
+}
+
+// Message returns a concatenated message on reasons of the admissionStatus.
+func (s *admissionStatus) Message() string {
+	if s.IsSuccess() {
+		return ""
+	}
+	if s.IsError() {
+		return fmt.Sprintf("Could not assign a flavor for %s in podSet %s: %v", s.resourceName, s.podSet, s.err)
+	}
+	msg := strings.Join(s.reasons, ", ")
+	return fmt.Sprintf("Workload didn't fit, insufficient %s in podSet %s: %s", s.resourceName, s.podSet, msg)
```

  ```suggestion
  	return fmt.Sprintf("Workload didn't fit, insufficient %s for podSet %s: %s", s.resourceName, s.podSet, msg)
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-02T15:39:41Z

/approve
Thanks!

Please squash
