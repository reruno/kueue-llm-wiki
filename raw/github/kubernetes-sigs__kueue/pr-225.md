# PR #225: Add validation for Workload via webhook

**Summary**: Add validation for Workload via webhook

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/225

**Last updated**: 2022-05-15T13:09:15Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@knight42](https://github.com/knight42)
- **Branch**: `feat/validate-webhook` → `main`
- **Created**: 2022-04-20T03:55:58Z
- **Updated**: 2022-05-15T13:09:15Z
- **Merged**: 2022-05-06T14:03:05Z
- **Closed**: 2022-05-06T14:03:05Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 4   **+178 / -17**

## Description

Signed-off-by: Jian Zeng <anonymousknight96@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add basic validating webhook for Workload.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #171

#### Special notes for your reviewer:

## Discussion

### Comment by [@knight42](https://github.com/knight42) — 2022-04-20T03:56:26Z

/cc @ahg-g @kerthcet

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-20T03:56:28Z

@knight42: GitHub didn't allow me to request PR reviews from the following users: kerthcet.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/225#issuecomment-1103445277):

>/cc @ahg-g @kerthcet 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:25:34Z

/assign

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:31:47Z

/retitle Add validation for Workload via webhook

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-05T01:26:06Z

@knight42 do you need help moving forward with this PR?

### Comment by [@knight42](https://github.com/knight42) — 2022-05-05T15:30:40Z

@ahg-g Hi! Sorry for the late response, I just return from vacation, I am discussing with @alculquicondor about the implmenetation details: https://github.com/kubernetes-sigs/kueue/pull/225#discussion_r866039252. It would be nice if I could hear your advice!

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-05T15:33:15Z

> @ahg-g Hi! Sorry for the late response, I just return from vacation, I am discussing with @alculquicondor about the implmenetation details: [#225 (comment)](https://github.com/kubernetes-sigs/kueue/pull/225#discussion_r866039252). 

Thank a lot @knight42 , I was just wondering if there is something you needed help with, I hope you had a great vacation :)

> It would be nice if I could hear your advice!

I will leave this to Aldo just so you get consistent feedback.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-05T18:11:25Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/225#pullrequestreview-963768023" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/225#" title="Author self-approved">knight42</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-20T06:41:19Z

- **apis/kueue/v1alpha1/workload_webhook.go:75** by [@kerthcet](https://github.com/kerthcet) — 2022-04-20T06:28:29Z
```diff
@@ -58,15 +59,29 @@ var _ webhook.Validator = &Workload{}
 
 // ValidateCreate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateCreate() error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateUpdate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateUpdate(old runtime.Object) error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateDelete implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateDelete() error {
 	return nil
 }
+
+func validateWorkload(obj *Workload) field.ErrorList {
```

  We should also validate the `PriorityClassName`, it should be a DNS subdomain, although we ensured this in JobController, but we will have multi implements in the future, it can prevent the problem as early as possible.

- **apis/kueue/v1alpha1/workload_webhook.go:75** by [@kerthcet](https://github.com/kerthcet) — 2022-04-20T06:31:32Z
```diff
@@ -58,15 +59,29 @@ var _ webhook.Validator = &Workload{}
 
 // ValidateCreate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateCreate() error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateUpdate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateUpdate(old runtime.Object) error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateDelete implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateDelete() error {
 	return nil
 }
+
+func validateWorkload(obj *Workload) field.ErrorList {
```

  Should we also make sure the queue name shouldn't be empty as I asked before. I think so if we don't have default ones. @ahg-g

- **test/integration/webhook/v1alpha1/workload_test.go:64** by [@kerthcet](https://github.com/kerthcet) — 2022-04-20T06:32:07Z
```diff
@@ -70,3 +70,50 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
```

  `Validating Workload webhooks`

- **test/integration/webhook/v1alpha1/workload_test.go:75** by [@kerthcet](https://github.com/kerthcet) — 2022-04-20T06:32:41Z
```diff
@@ -70,3 +70,50 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating Workload", func() {
```

  When creating a Workload

- **test/integration/webhook/v1alpha1/workload_test.go:76** by [@kerthcet](https://github.com/kerthcet) — 2022-04-20T06:40:53Z
```diff
@@ -70,3 +70,50 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating Workload", func() {
+		ginkgo.It("Should validate spec", func() {
```

  `Should validate PodSet.Count` ? I think we only verify the count here.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-20T11:43:55Z

Thanks @knight42  
some comments.

- **apis/kueue/v1alpha1/workload_webhook.go:75** by [@denkensk](https://github.com/denkensk) — 2022-04-20T11:37:25Z
```diff
@@ -58,15 +59,29 @@ var _ webhook.Validator = &Workload{}
 
 // ValidateCreate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateCreate() error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateUpdate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateUpdate(old runtime.Object) error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateDelete implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateDelete() error {
 	return nil
 }
+
+func validateWorkload(obj *Workload) field.ErrorList {
```

  > Should we also make sure the queue name shouldn't be empty as I asked before. I think so if we don't have default ones.
  
  How about if the user create the default queue after the workload created ?

- **test/integration/webhook/v1alpha1/workload_test.go:92** by [@denkensk](https://github.com/denkensk) — 2022-04-20T11:40:32Z
```diff
@@ -70,3 +70,50 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating Workload", func() {
+		ginkgo.It("Should validate spec", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, workload)).Should(gomega.HaveOccurred())
```

  I think we'd better to check the err message here.

- **apis/kueue/v1alpha1/workload_webhook.go:80** by [@denkensk](https://github.com/denkensk) — 2022-04-20T11:43:17Z
```diff
@@ -58,15 +59,29 @@ var _ webhook.Validator = &Workload{}
 
 // ValidateCreate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateCreate() error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateUpdate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateUpdate(old runtime.Object) error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateDelete implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateDelete() error {
 	return nil
 }
+
+func validateWorkload(obj *Workload) field.ErrorList {
+	var allErrs field.ErrorList
+	for i, podSet := range obj.Spec.PodSets {
+		if podSet.Count <= 0 {
+			allErrs = append(allErrs, field.Invalid(
+				field.NewPath("spec").Child("podSets").Index(i).Child("count"),
```

  Can you also check the priority in workload?

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-04-20T15:12:28Z

- **apis/kueue/v1alpha1/workload_webhook.go:80** by [@knight42](https://github.com/knight42) — 2022-04-20T15:12:28Z
```diff
@@ -58,15 +59,29 @@ var _ webhook.Validator = &Workload{}
 
 // ValidateCreate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateCreate() error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateUpdate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateUpdate(old runtime.Object) error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateDelete implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateDelete() error {
 	return nil
 }
+
+func validateWorkload(obj *Workload) field.ErrorList {
+	var allErrs field.ErrorList
+	for i, podSet := range obj.Spec.PodSets {
+		if podSet.Count <= 0 {
+			allErrs = append(allErrs, field.Invalid(
+				field.NewPath("spec").Child("podSets").Index(i).Child("count"),
```

  Yeah of course, the highest priority for user defined priority class is 1 biliion, should we take predefined system priority classes into account as well?

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-04-20T15:20:00Z

- **test/integration/webhook/v1alpha1/workload_test.go:64** by [@knight42](https://github.com/knight42) — 2022-04-20T15:20:00Z
```diff
@@ -70,3 +70,50 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
```

  🤔 I consider `validating/defaulting webhook` as terminology here, please see also https://book.kubebuilder.io/cronjob-tutorial/webhook-implementation.html

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-04-20T15:21:14Z

- **test/integration/webhook/v1alpha1/workload_test.go:76** by [@knight42](https://github.com/knight42) — 2022-04-20T15:21:14Z
```diff
@@ -70,3 +70,50 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating Workload", func() {
+		ginkgo.It("Should validate spec", func() {
```

  ~~That's because we only validate `podSet.count` in the webhook server right now, I think we could put more tests here in the future.~~
  
  Yes, I think we could put each test into respective `It` node.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-21T04:37:25Z

- **test/integration/webhook/v1alpha1/workload_test.go:100** by [@kerthcet](https://github.com/kerthcet) — 2022-04-21T03:52:20Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PriorityClassName = "invalid_class"
```

  use `PriorityClass()` instead, see https://github.com/kubernetes-sigs/kueue/blob/99683618876a004f3332fb99cae1e3a9bf6f2124/pkg/util/testing/wrappers.go#L117-L124

- **test/integration/webhook/v1alpha1/workload_test.go:111** by [@kerthcet](https://github.com/kerthcet) — 2022-04-21T04:02:41Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PriorityClassName = "invalid_class"
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("a lowercase RFC 1123 subdomain must consist of lower case alphanumeric characters"))
+		})
+	})
+
+	ginkgo.Context("When updating a Workload", func() {
+		ginkgo.It("Should validate spec.podSet.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
```

  I wonder whether we really need this as `MakeWorkload()` already contains a qualified podset.

- **test/integration/webhook/v1alpha1/workload_test.go:139** by [@kerthcet](https://github.com/kerthcet) — 2022-04-21T04:03:37Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PriorityClassName = "invalid_class"
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("a lowercase RFC 1123 subdomain must consist of lower case alphanumeric characters"))
+		})
+	})
+
+	ginkgo.Context("When updating a Workload", func() {
+		ginkgo.It("Should validate spec.podSet.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: 2,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, workload)).Should(gomega.Succeed())
+
+			ginkgo.By("Updating the Workload")
+			workload.Spec.PodSets[0].Count = -1
+			err := k8sClient.Update(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, workload)).Should(gomega.Succeed())
+
+			ginkgo.By("Updating the Workload")
+			workload.Spec.PriorityClassName = "invalid_class"
```

  ditto

- **test/integration/webhook/v1alpha1/workload_test.go:78** by [@kerthcet](https://github.com/kerthcet) — 2022-04-21T04:12:16Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
```

  nit: no need to specify the workload name different with each other in `It` node.

- **apis/kueue/v1alpha1/workload_webhook.go:95** by [@kerthcet](https://github.com/kerthcet) — 2022-04-21T04:35:57Z
```diff
@@ -58,15 +62,41 @@ var _ webhook.Validator = &Workload{}
 
 // ValidateCreate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateCreate() error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateUpdate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateUpdate(old runtime.Object) error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateDelete implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateDelete() error {
 	return nil
 }
+
+func validateWorkload(obj *Workload) field.ErrorList {
+	var allErrs field.ErrorList
+	specField := field.NewPath("spec")
+	for i, podSet := range obj.Spec.PodSets {
+		if podSet.Count <= 0 {
+			allErrs = append(allErrs, field.Invalid(
+				specField.Child("podSets").Index(i).Child("count"),
+				podSet.Count,
+				"count must be greater than 0"),
+			)
+		}
+	}
+
+	if len(obj.Spec.PriorityClassName) > 0 {
+		msgs := validation.IsDNS1123Subdomain(obj.Spec.PriorityClassName)
```

  replace with something like below as we may have multi different type errors, not different msgs for one error.
  ```
  		fldPath := specField.Child("priorityClassName")
  		for _, msg := range validation.IsDNS1123Subdomain(obj.Spec.PriorityClassName) {
  			allErrs = append(allErrs, field.Invalid(fldPath, obj.Spec.PriorityClassName, msg))
  		}
  ```

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-04-24T09:17:07Z

- **test/integration/webhook/v1alpha1/workload_test.go:100** by [@knight42](https://github.com/knight42) — 2022-04-24T09:17:07Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PriorityClassName = "invalid_class"
```

  I am not sure if I understand what you mean correctly. Did you suggest we should create a `PriorityClass` object here rather than setting the `.spec.PriorityClassName` field of the `Workload`?

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-04-24T09:19:11Z

- **test/integration/webhook/v1alpha1/workload_test.go:111** by [@knight42](https://github.com/knight42) — 2022-04-24T09:19:11Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PriorityClassName = "invalid_class"
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("a lowercase RFC 1123 subdomain must consist of lower case alphanumeric characters"))
+		})
+	})
+
+	ginkgo.Context("When updating a Workload", func() {
+		ginkgo.It("Should validate spec.podSet.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
```

  We need to update the field `spec.podSet[0].count` later in the test, I think we could set this field in advance.

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-04-24T09:20:06Z

- **test/integration/webhook/v1alpha1/workload_test.go:78** by [@knight42](https://github.com/knight42) — 2022-04-24T09:20:06Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
```

  Okay I would use a constant name later.

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-04-24T09:23:08Z

- **apis/kueue/v1alpha1/workload_webhook.go:95** by [@knight42](https://github.com/knight42) — 2022-04-24T09:23:08Z
```diff
@@ -58,15 +62,41 @@ var _ webhook.Validator = &Workload{}
 
 // ValidateCreate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateCreate() error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateUpdate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateUpdate(old runtime.Object) error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateDelete implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateDelete() error {
 	return nil
 }
+
+func validateWorkload(obj *Workload) field.ErrorList {
+	var allErrs field.ErrorList
+	specField := field.NewPath("spec")
+	for i, podSet := range obj.Spec.PodSets {
+		if podSet.Count <= 0 {
+			allErrs = append(allErrs, field.Invalid(
+				specField.Child("podSets").Index(i).Child("count"),
+				podSet.Count,
+				"count must be greater than 0"),
+			)
+		}
+	}
+
+	if len(obj.Spec.PriorityClassName) > 0 {
+		msgs := validation.IsDNS1123Subdomain(obj.Spec.PriorityClassName)
```

  Thanks! I have updated the code.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-25T06:14:58Z

- **test/integration/webhook/v1alpha1/workload_test.go:100** by [@kerthcet](https://github.com/kerthcet) — 2022-04-25T06:14:53Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PriorityClassName = "invalid_class"
```

  Ah sorry for the mistake, I paste the wrong link, here's the right one:
  https://github.com/kubernetes-sigs/kueue/blob/99683618876a004f3332fb99cae1e3a9bf6f2124/pkg/util/testing/wrappers.go#L189-L192
  So we can simplify like this:
  ```
  workload := testing.MakeWorkload("workload3", ns.Name).PriorityClass("invalid_class").Obj()
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-25T06:22:01Z

- **test/integration/webhook/v1alpha1/workload_test.go:111** by [@kerthcet](https://github.com/kerthcet) — 2022-04-25T06:21:53Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PriorityClassName = "invalid_class"
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("a lowercase RFC 1123 subdomain must consist of lower case alphanumeric characters"))
+		})
+	})
+
+	ginkgo.Context("When updating a Workload", func() {
+		ginkgo.It("Should validate spec.podSet.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
```

  I mean MakeWorkload() already created a podSet in advanced https://github.com/kubernetes-sigs/kueue/blob/99683618876a004f3332fb99cae1e3a9bf6f2124/pkg/util/testing/wrappers.go#L141-L163

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-25T20:58:52Z

- **apis/kueue/v1alpha1/workload_webhook.go:75** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:37:46Z
```diff
@@ -58,15 +59,29 @@ var _ webhook.Validator = &Workload{}
 
 // ValidateCreate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateCreate() error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateUpdate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateUpdate(old runtime.Object) error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateDelete implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateDelete() error {
 	return nil
 }
+
+func validateWorkload(obj *Workload) field.ErrorList {
```

  I think we should not fail the Workload creation if the queue name is missing. This is because a user could create a Job without a queue annotation and decide to add it later. We need the Workload object to post an Event that indicates why the workload is not schedulable.

- **apis/kueue/v1alpha1/workload_webhook.go:84** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:38:35Z
```diff
@@ -58,15 +60,40 @@ var _ webhook.Validator = &Workload{}
 
 // ValidateCreate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateCreate() error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateUpdate implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateUpdate(old runtime.Object) error {
-	return nil
+	return validateWorkload(r).ToAggregate()
 }
 
 // ValidateDelete implements webhook.Validator so a webhook will be registered for the type
 func (r *Workload) ValidateDelete() error {
 	return nil
 }
+
+func validateWorkload(obj *Workload) field.ErrorList {
+	var allErrs field.ErrorList
+	specField := field.NewPath("spec")
+	for i, podSet := range obj.Spec.PodSets {
```

  Let's validate that there is at least one element in the slice.

- **test/integration/webhook/v1alpha1/workload_test.go:64** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:49:18Z
```diff
@@ -70,3 +70,50 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
```

  I think this is fine. It reads: "Describe Workload validating webhook"

- **test/integration/webhook/v1alpha1/workload_test.go:93** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:50:59Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
```

  Verify the error is of the correct type using "k8s.io/apimachinery/pkg/util/errors"

- **test/integration/webhook/v1alpha1/workload_test.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:52:29Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
```

  Please do it now. This is simple enough and you might forget later.

- **apis/kueue/v1alpha1/workload_webhook.go:18** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:55:27Z
```diff
@@ -18,6 +18,8 @@ package v1alpha1
 
```

  Can you also add unit tests?
  
  The integration tests are good, but they require a lot of code and they are expensive to run.
  
  We can get most of the coverage using unit tests.

- **test/integration/webhook/v1alpha1/workload_test.go:76** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:56:11Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
```

  Should we just have a single object with a bunch of errors?
  
  As stated above, we can have most of the coverage in unit tests.

- **test/integration/webhook/v1alpha1/workload_test.go:133** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T20:58:49Z
```diff
@@ -70,3 +70,76 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate spec.podSets.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload2", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: -1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PriorityClassName = "invalid_class"
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("a lowercase RFC 1123 subdomain must consist of lower case alphanumeric characters"))
+		})
+	})
+
+	ginkgo.Context("When updating a Workload", func() {
+		ginkgo.It("Should validate spec.podSet.count", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload("workload3", ns.Name).Obj()
+			workload.Spec.PodSets = []v1alpha1.PodSet{
+				{
+					Count: 2,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name:  "test",
+								Image: "fake-image",
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, workload)).Should(gomega.Succeed())
+
+			ginkgo.By("Updating the Workload")
+			workload.Spec.PodSets[0].Count = -1
+			err := k8sClient.Update(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			gomega.Expect(err.Error()).Should(gomega.ContainSubstring("must be greater than 0"))
+		})
+
+		ginkgo.It("Should validate spec.priorityClassName", func() {
```

  I would remove this test.
  
  We need to ensure that the webhook validates something during create/update, and leave the rest to unit tests.

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-04-27T17:08:32Z

- **apis/kueue/v1alpha1/workload_webhook.go:18** by [@knight42](https://github.com/knight42) — 2022-04-27T17:08:32Z
```diff
@@ -18,6 +18,8 @@ package v1alpha1
 
```

  unit tests have been added.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-27T19:20:50Z

- **apis/kueue/v1alpha1/workload_webhook_test.go:13** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-27T19:20:50Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+)
+
+// makeWorkload is copied from "sigs.k8s.io/kueue/pkg/util/testing"
+// NOTE: we could not import "sigs.k8s.io/kueue/pkg/util/testing" because of cyclic import.
```

  I think you can avoid the circular dependency doing this: https://github.com/golang/go/wiki/CodeReviewComments#import-dot

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-04-27T19:28:17Z

- **apis/kueue/v1alpha1/workload_webhook_test.go:4** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-27T19:22:52Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
```

  Use cmp.Equal instead

- **apis/kueue/v1alpha1/workload_webhook_test.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-27T19:23:11Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+)
+
+// makeWorkload is copied from "sigs.k8s.io/kueue/pkg/util/testing"
+// NOTE: we could not import "sigs.k8s.io/kueue/pkg/util/testing" because of cyclic import.
+func makeWorkload(override func(*Workload)) *Workload {
+	obj := &Workload{
+		ObjectMeta: metav1.ObjectMeta{Name: "name", Namespace: "ns"},
+		Spec: WorkloadSpec{
+			PodSets: []PodSet{
+				{
+					Name:  "main",
+					Count: 1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "c",
+								Resources: corev1.ResourceRequirements{
+									Requests: make(corev1.ResourceList),
+								},
+							},
+						},
+					},
+				},
+			},
+		},
+	}
+	override(obj)
+	return obj
+}
+
+func TestValidateWorkload(t *testing.T) {
+	specField := field.NewPath("spec")
+	podSetsField := specField.Child("podSets")
+	testCases := map[string]struct {
+		workload    *Workload
+		expectedErr field.ErrorList
```

  ```suggestion
  		wantErr field.ErrorList
  ```

- **apis/kueue/v1alpha1/workload_webhook_test.go:68** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-27T19:26:10Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+)
+
+// makeWorkload is copied from "sigs.k8s.io/kueue/pkg/util/testing"
+// NOTE: we could not import "sigs.k8s.io/kueue/pkg/util/testing" because of cyclic import.
+func makeWorkload(override func(*Workload)) *Workload {
+	obj := &Workload{
+		ObjectMeta: metav1.ObjectMeta{Name: "name", Namespace: "ns"},
+		Spec: WorkloadSpec{
+			PodSets: []PodSet{
+				{
+					Name:  "main",
+					Count: 1,
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{
+								Name: "c",
+								Resources: corev1.ResourceRequirements{
+									Requests: make(corev1.ResourceList),
+								},
+							},
+						},
+					},
+				},
+			},
+		},
+	}
+	override(obj)
+	return obj
+}
+
+func TestValidateWorkload(t *testing.T) {
+	specField := field.NewPath("spec")
+	podSetsField := specField.Child("podSets")
+	testCases := map[string]struct {
+		workload    *Workload
+		expectedErr field.ErrorList
+	}{
+		"should have at least one podSet": {
+			workload: makeWorkload(func(w *Workload) {
+				w.Spec.PodSets = nil
+			}),
+			expectedErr: field.ErrorList{
+				field.Required(podSetsField, "at least one podSet is required"),
+			},
+		},
+		"count should be greater than 0": {
+			workload: makeWorkload(func(w *Workload) {
+				w.Spec.PodSets[0].Count = -1
+			}),
+			expectedErr: field.ErrorList{
+				field.Invalid(podSetsField.Index(0).Child("count"), int32(-1), "count must be greater than 0"),
+			},
+		},
+		"should have valid priorityClassName": {
+			workload: makeWorkload(func(w *Workload) {
+				w.Spec.PriorityClassName = "invalid_class"
+			}),
+			expectedErr: field.ErrorList{
+				field.Invalid(specField.Child("priorityClassName"), "invalid_class", "a lowercase RFC 1123 subdomain must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character (e.g. 'example.com', regex used for validation is '[a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*')"),
```

  this is a lot of text, subject to change
  
  If you use cmp.Equal, you can pass cmpopts.IgnoreFields so that we don't have to compare message.

- **test/integration/webhook/v1alpha1/workload_test.go:73** by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-27T19:28:06Z
```diff
@@ -70,3 +60,35 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate Workload", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload(workloadName, ns.Name).PriorityClass("invalid_class").Obj()
+			workload.Spec.PodSets[0].Count = -1
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			status := err.(*errors.StatusError).Status()
+			gomega.Expect(status.Reason).Should(gomega.BeEquivalentTo(
```

  I would not compare the reason, just the type of error.

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-04-30T12:02:31Z

- **apis/kueue/v1alpha1/workload_webhook_test.go:13** by [@knight42](https://github.com/knight42) — 2022-04-30T12:02:31Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+)
+
+// makeWorkload is copied from "sigs.k8s.io/kueue/pkg/util/testing"
+// NOTE: we could not import "sigs.k8s.io/kueue/pkg/util/testing" because of cyclic import.
```

  @alculquicondor Hi, I have switched to dot import, but it seems a workaround to me, should me move the validation logic to a separate package, such as `pkg/validation`?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-02T13:58:16Z

- **apis/kueue/v1alpha1/workload_webhook_test.go:13** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T13:58:16Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+)
+
+// makeWorkload is copied from "sigs.k8s.io/kueue/pkg/util/testing"
+// NOTE: we could not import "sigs.k8s.io/kueue/pkg/util/testing" because of cyclic import.
```

  yeah, that's actually a better idea.
  
  Let's do it in `apis/kueue/v1alpha1/validation`

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-05-02T17:19:50Z

- **apis/kueue/v1alpha1/workload_webhook_test.go:13** by [@knight42](https://github.com/knight42) — 2022-05-02T17:19:50Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+)
+
+// makeWorkload is copied from "sigs.k8s.io/kueue/pkg/util/testing"
+// NOTE: we could not import "sigs.k8s.io/kueue/pkg/util/testing" because of cyclic import.
```

  hmm I just realize this won't work, the circular dependency still persists: the package `apis/kueue/v1alpha1/validation` imports the `Worload` struct from `apis/kueue/v1alpha1`, and `apis/kueue/v1alpha1` imports `apis/kueue/v1alpha1/validation` for the validator. The root cause is that the validator function is called inside `apis/kueue/v1alpha1` package.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-02T17:38:11Z

- **apis/kueue/v1alpha1/workload_webhook_test.go:13** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T17:38:11Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+)
+
+// makeWorkload is copied from "sigs.k8s.io/kueue/pkg/util/testing"
+// NOTE: we could not import "sigs.k8s.io/kueue/pkg/util/testing" because of cyclic import.
```

  `Workload` doesn't have to implement the `Defaulter` or `Validator` interface. You could create a new type for it that lives in a webhooks package.

### Review by [@knight42](https://github.com/knight42) (commented) — 2022-05-05T15:28:50Z

- **apis/kueue/v1alpha1/workload_webhook_test.go:13** by [@knight42](https://github.com/knight42) — 2022-05-05T15:28:50Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+)
+
+// makeWorkload is copied from "sigs.k8s.io/kueue/pkg/util/testing"
+// NOTE: we could not import "sigs.k8s.io/kueue/pkg/util/testing" because of cyclic import.
```

  Regarding of the definition of `Defaulter` or `Validator` interface:
  ```go
  type Defaulter interface {
  	runtime.Object
  	Default()
  }
  type Validator interface {
  	runtime.Object
  	ValidateCreate() error
  	ValidateUpdate(old runtime.Object) error
  	ValidateDelete() error
  }
  ```
  it seems these interfaces could only be implements on the `Workload` struct, otherwise we could not access the fields.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-05T15:34:10Z

- **apis/kueue/v1alpha1/workload_webhook_test.go:13** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T15:34:10Z
```diff
@@ -0,0 +1,80 @@
+package v1alpha1
+
+import (
+	"reflect"
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+)
+
+// makeWorkload is copied from "sigs.k8s.io/kueue/pkg/util/testing"
+// NOTE: we could not import "sigs.k8s.io/kueue/pkg/util/testing" because of cyclic import.
```

  ahhh, I missed that they depend on `runtime.Object` :disappointed:
  
  I guess we have to go the dotimport route then.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-05T15:41:23Z

- **apis/kueue/v1alpha1/workload_webhook_test.go:20** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T15:35:56Z
```diff
@@ -0,0 +1,57 @@
+// Rename the package to avoid circular dependencies which is caused by "sigs.k8s.io/kueue/pkg/util/testing".
+// See also: https://github.com/golang/go/wiki/CodeReviewComments#import-dot
```

  This file is missing the copyright notice.
  
  Also, a comment just up from the package name makes it part of the package doc. Can you move it bellow?

- **pkg/util/testing/wrappers.go:212** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T15:38:18Z
```diff
@@ -204,6 +204,16 @@ func (w *WorkloadWrapper) Priority(priority *int32) *WorkloadWrapper {
 	return w
 }
 
+func (w *WorkloadWrapper) PodSets(podSets []kueue.PodSet) *WorkloadWrapper {
+	w.Spec.PodSets = podSets
+	return w
+}
+
+func (w *WorkloadWrapper) PodSetCount(count int32) *WorkloadWrapper {
```

  This is way too specific. Just use `PodSets` to pass a list with the necessary count.

- **test/integration/webhook/v1alpha1/workload_test.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-05T15:40:22Z
```diff
@@ -70,3 +63,40 @@ var _ = ginkgo.Describe("Workload defaulting webhook", func() {
 		})
 	})
 })
+
+var _ = ginkgo.Describe("Workload validating webhook", func() {
+	ginkgo.Context("When creating a Workload", func() {
+		ginkgo.It("Should validate Workload", func() {
+			ginkgo.By("Creating a new Workload")
+			workload := testing.MakeWorkload(workloadName, ns.Name).
+				PodSetCount(-1).
+				PriorityClass("invalid_class").
+				Obj()
+			err := k8sClient.Create(ctx, workload)
+			gomega.Expect(err).Should(gomega.HaveOccurred())
+			status := err.(*errors.StatusError).Status()
+			gomega.Expect(status).Should(testing.Equal(metav1.Status{
```

  It would be clearer to just use `errors.IsInvalid`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-05T18:11:10Z

Awesome

/approve

Can you please squash the commits?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-05-06T14:02:37Z

/lgtm
