# PR #2255: Add troubleshooting guide for ProvisioningRequest

**Summary**: Add troubleshooting guide for ProvisioningRequest

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2255

**Last updated**: 2024-07-31T11:42:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `troubleshooting-provreq` → `main`
- **Created**: 2024-05-22T12:56:33Z
- **Updated**: 2024-07-31T11:42:23Z
- **Merged**: 2024-06-05T09:12:17Z
- **Closed**: 2024-06-05T09:12:17Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet), [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk), [@trasc](https://github.com/trasc)
- **Changed files**: 3   **+226 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-22T12:56:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3cbbd550bee5340458b1d8a7c176cc2b30aa2e14 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6660137918baea00085b5f12 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2255--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2255--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTIyNTUtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.S7Nad190lJ9xynUaXKlbxe-Hx8fNV3mVLUomsl3mCr0)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-05-22T13:18:24Z

/retest

### Comment by [@PBundyra](https://github.com/PBundyra) — 2024-05-22T13:18:36Z

/assign @yaroslava-serdiuk 
/assign @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:12:51Z

cc @cortespao

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-06-03T12:00:58Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-03T12:01:04Z

LGTM label has been added.  <details>Git tree hash: b3c86d907b4499a3d949259dd019248fa0f7a93b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-04T15:18:26Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2255#pullrequestreview-2096722222" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2255#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-04T15:18:31Z

/hold
for nits

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-05T09:09:07Z

LGTM label has been added.  <details>Git tree hash: 2ab13e8f3b52e890c7e8b46674795c84fd29b17d</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-05T09:10:25Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-06-05T09:10:27Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of website in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2255#issuecomment-2149285775):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2024-06-05T09:12:55Z

@tenzen-y: new pull request created: #2357

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2255#issuecomment-2149285775):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-05-22T13:39:08Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:31** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-22T13:39:08Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
```

  This annotation is going to be deprecated (however Kueue still need to support it) 
  See: https://github.com/kubernetes/autoscaler/issues/6852

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-05-22T13:42:50Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:47** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-22T13:42:50Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in its Status in the `Conditions` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- Accepted - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned -  indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
```

  Wrong description for `Provisioned`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-22T14:24:29Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:31** by [@PBundyra](https://github.com/PBundyra) — 2024-05-22T14:24:29Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
```

  Are you suggesting replacing it with `autoscaling.x-k8s.io/consume-provisioning-request`?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-22T14:28:25Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:47** by [@PBundyra](https://github.com/PBundyra) — 2024-05-22T14:28:25Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in its Status in the `Conditions` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- Accepted - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned -  indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
```

  Thanks, fixed!

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-05-23T08:17:51Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:31** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-23T08:17:51Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
```

  Let's leave as it is for now, because this is current behavior, and update this section later

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-23T08:29:00Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:31** by [@PBundyra](https://github.com/PBundyra) — 2024-05-23T08:29:00Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
```

  Okay, I will keep this in mind. Thank you

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-05-23T08:35:43Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:19** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-23T08:19:45Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
```

  There were few patch releases, maybe worth mention the last patch release?

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:21** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-23T08:20:26Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
```

  What does mean `Your node group enables it`?

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:33** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-23T08:32:09Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
```

  In the beginning of the paragraph you suggest to check state of a Provisioning Request running `kubectl describe workload` and here you suggest run `kubectl get provisioningrequest`. 
  
  Probably you the `kubectl describe workload` is aimed to find if the ProvReq was created and what the name of it and see short info.
  
  I would put this paragraph in the begening of the section, then info about annotation and then commands `describe workload`, `get provreq` etc.
  Also, could  you add an example of output of the command?

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:222** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-05-23T08:35:21Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in its Status in the `Conditions` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- Accepted - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. CA will set this condition when the VM creation finishes successfully.
+- Failed - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- BookingExpired - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- CapacityRevoked - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Provisioning Request is not created by Kueue
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run a following command to check whether your Kueue's controller manager enables the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+### Ensure the ClusterQueue and the LocalQueue are active
+
+For details see our other [troubleshooting guide](/docs/tasks/troubleshooting/troubleshooting_queues/)
+
+
+### Ensure the Admission Check is active
+
+To check if the Admission Check your job uses is active run the following command:
+
+```bash
+kubectl describe admissionchecks ADMISSIONCHECK_NAME
+```
+
+The status of the Admission Check should be similar to:
+
+```bash
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-03-08T11:44:53Z
+    Message:               The admission check is active
+    Reason:                Active
+    Status:                True
+    Type:                  Active
+```
```

  What if all of steps above works but still no ProvReq? 
  Maybe worth to file a bug in the repo or ask in slack?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-24T18:34:01Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:12:39Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
```

  This is not a vendor-neutral documentation. You can point to the design doc, though https://github.com/kubernetes/autoscaler/blob/master/cluster-autoscaler/proposals/provisioning-request.md

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:13** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:21:18Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
```

  Use active voice: Kueue creates ProvisioningRequests via the Provisioning Admission Check Controller.
  
  Also: In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:18** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:25:04Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
```

  This one is tricky. Maybe we can say something like:
  
  Check your cloud provider documentation to determine the versions that support ProvisioningRequest. Then, we can say "for example, in GKE...".
  
  And once we have support for OSS engines, we can remove the note about GKE and only mention the supported OSS versions.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:21** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:28:08Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
```

  In the case of GKE, only DWS nodepools support ProvisioningRequest.
  
  Not sure how to express this a bit more generically.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:25** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:28:56Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
```

  Link to "troubleshooting jobs" to learn how to identify the workload for a job.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:25** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:29:10Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
```

  ```suggestion
  You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:31** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:30:54Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
```

  At which point does Kueue add the annotation and to what exactly?
  
  Where exactly do you find the annotation in the Workload status.
  
  A sample output could be helpful.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:33** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:31:32Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
```

  Consider adding a subtitle to this paragraph, preferably phrased as a question.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:46** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:31:56Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in its Status in the `Conditions` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- Accepted - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
```

  ```suggestion
  - `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:45** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T18:32:59Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in its Status in the `Conditions` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
```

  ```suggestion
  Provisioning Request state is described in the `.conditions[*].status` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
  ```
  
  what is "it" in this context?

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2024-05-24T19:30:35Z

Sending some suggestions.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:11** by [@cortespao](https://github.com/cortespao) — 2024-05-24T19:17:05Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
```

  ```suggestion
  This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
  
  You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:17** by [@cortespao](https://github.com/cortespao) — 2024-05-24T19:19:31Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
```

  ```suggestion
  Before you begin troubleshooting make sure your cluster meets the following requirements:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:21** by [@cortespao](https://github.com/cortespao) — 2024-05-24T19:21:19Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
```

  Maybe `The type of nodes that you use supports ProvisioningRequest` ?

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:25** by [@cortespao](https://github.com/cortespao) — 2024-05-24T19:21:56Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
```

  nit: "You can run the following.."

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:33** by [@cortespao](https://github.com/cortespao) — 2024-05-24T19:27:28Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
```

  I'd recommend that you add an example of what error message might appear when this happens. The users tend to look for the error so that they find the troubleshooting steps, not vice versa.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:47** by [@cortespao](https://github.com/cortespao) — 2024-05-24T19:28:49Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in its Status in the `Conditions` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- Accepted - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. CA will set this condition when the VM creation finishes successfully.
```

  ```suggestion
  - Provisioned - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
  ```
  ```suggestion
  - Provisioned - indicates that all of the requested resources were created and are available in the cluster. CA will set this condition when the VM creation finishes successfully.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:58** by [@cortespao](https://github.com/cortespao) — 2024-05-24T19:30:03Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in its Status in the `Conditions` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- Accepted - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. CA will set this condition when the VM creation finishes successfully.
+- Failed - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- BookingExpired - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- CapacityRevoked - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Provisioning Request is not created by Kueue
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
```

  Is there any error message that signals this scenario to the user?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T06:38:24Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:11** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T06:38:24Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T06:40:04Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:13** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T06:40:04Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T06:44:52Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:25** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T06:44:51Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T06:45:36Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:25** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T06:45:36Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T07:08:36Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:31** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T07:08:36Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T07:10:35Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:33** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T07:10:35Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
```

  Fixed

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-27T07:38:38Z

- **site/static/images/prov-req-states.svg:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-27T07:27:43Z
  Could we explain the criteria like this for when the transition of state will happen?
  
  ```mermaid
  stateDiagram-v2
      [*] --> Accepted: ProvisioningRequest was accepted by cluster-autoscaler
      Accepted --> Failed: cluster-autoscaler failed \n to scale out cluster
      Accepted --> Provisioned: The cluster-autoscaler succeeded \n in provisioning nodes.
      Provisioned --> Failed: ...
      Provisioned --> BookingExpired: ...
      BookingExpired --> CapacityRevoked: ...
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:50** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-27T07:31:44Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in the `.conditions[*].status` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
+- Failed - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- BookingExpired - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- CapacityRevoked - indicates that requested resources are not longer valid.
```

  ```suggestion
  - `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
  - `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
  - `BookingExpired` - indicates that the ProvisioningRequest had `Provisioned` condition before and capacity reservation time is expired.
  - `CapacityRevoked` - indicates that requested resources are no longer valid.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:66** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-27T07:33:10Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in the `.conditions[*].status` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
+- Failed - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- BookingExpired - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- CapacityRevoked - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Provisioning Request is not created by Kueue
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run a following command to check whether your Kueue's controller manager enables the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
```

  Could you mention the deployment instead of Pods?

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:80** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-27T07:37:49Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in the `.conditions[*].status` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
+- Failed - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- BookingExpired - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- CapacityRevoked - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Provisioning Request is not created by Kueue
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run a following command to check whether your Kueue's controller manager enables the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+### Ensure the ClusterQueue and the LocalQueue are active
+
+For details see our other [troubleshooting guide](/docs/tasks/troubleshooting/troubleshooting_queues/)
```

  ```suggestion
  You can check the state of ClusterQueues and LocalQueues if the queues are ready for the Workloads.
  Please see the [Troubleshooting Queues](/docs/tasks/troubleshooting/troubleshooting_queues/) for more details.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T08:16:17Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:50** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T08:16:17Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in the `.conditions[*].status` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
+- Failed - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- BookingExpired - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- CapacityRevoked - indicates that requested resources are not longer valid.
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-05-27T08:17:50Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:66** by [@PBundyra](https://github.com/PBundyra) — 2024-05-27T08:17:50Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in the `.conditions[*].status` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
+- Failed - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- BookingExpired - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- CapacityRevoked - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Provisioning Request is not created by Kueue
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run a following command to check whether your Kueue's controller manager enables the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-27T17:20:19Z

Did you forget to push an update?

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T17:17:57Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
```

  ```suggestion
  This document helps you troubleshoot ProvisioningRequests, which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:11** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T17:18:33Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
```

  Remove this line

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:13** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T17:19:41Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
```

  Can you also address the second part of my comment?

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:17** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T17:19:54Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
```

  ```suggestion
  Before you begin troubleshooting, make sure your cluster meets the following requirements:
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T08:34:25Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:11** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T08:34:25Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T08:34:40Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:13** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T08:34:39Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T08:34:49Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:18** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T08:34:49Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
```

  Thanks, fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T08:34:57Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:21** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T08:34:57Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T08:35:23Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:33** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T08:35:22Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
```

  Thank you, please take a look if the changes align with your idea

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T08:35:31Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:33** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T08:35:31Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
```

  Added!

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T08:36:35Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:58** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T08:36:35Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in its Status in the `Conditions` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- Accepted - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. CA will set this condition when the VM creation finishes successfully.
+- Failed - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- BookingExpired - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- CapacityRevoked - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Provisioning Request is not created by Kueue
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
```

  Unfortunately if the Provisioning Request is not created there isnt any error message. A user can only notice the lack of the object

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T08:36:41Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:222** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T08:36:40Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+and more detailed version with a command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+Provisioning Request state is described in its Status in the `Conditions` field.  it means it still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- Accepted - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- Provisioned - indicates that all of the requested resources were created and are available in the cluster. CA will set this condition when the VM creation finishes successfully.
+- Failed - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- BookingExpired - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- CapacityRevoked - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Provisioning Request is not created by Kueue
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run a following command to check whether your Kueue's controller manager enables the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+### Ensure the ClusterQueue and the LocalQueue are active
+
+For details see our other [troubleshooting guide](/docs/tasks/troubleshooting/troubleshooting_queues/)
+
+
+### Ensure the Admission Check is active
+
+To check if the Admission Check your job uses is active run the following command:
+
+```bash
+kubectl describe admissionchecks ADMISSIONCHECK_NAME
+```
+
+The status of the Admission Check should be similar to:
+
+```bash
+...
+Status:
+  Conditions:
+    Last Transition Time:  2024-03-08T11:44:53Z
+    Message:               The admission check is active
+    Reason:                Active
+    Status:                True
+    Type:                  Active
+```
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T09:04:01Z

- **site/static/images/prov-req-states.svg:1** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T09:04:01Z
  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T09:13:02Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:19** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T09:13:02Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
```

  Fixed

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-06-03T09:35:42Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:16** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-06-03T09:23:05Z
```diff
@@ -0,0 +1,164 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000` with the Cluster Autoscaler at least `28.122.0`
```

  ```suggestion
  - Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API. 
  Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:16** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-06-03T09:23:59Z
```diff
@@ -0,0 +1,164 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000` with the Cluster Autoscaler at least `28.122.0`
```

  In GKE you can't specify Cluster Autoscaler version since it's managed by GKE.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:18** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-06-03T09:25:22Z
```diff
@@ -0,0 +1,164 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000` with the Cluster Autoscaler at least `28.122.0`
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
```

  Let's put it as second point, since it's related to `cluster supports ProvisioningRequest`

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:33** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-06-03T09:29:37Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This doc is about troubleshooting ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+See the details about  Provisioning Request[here](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin further investigation make sure these fundamental requirements are met:
+- Your cluster's version is at least 1.28.3-gke.1098000 with the Cluster Autoscaler at least 28.122.0
+- Kueue's version is at least 0.5.0
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- Your node group enables it. It may vary depending on your cloud provider.
+
+## Provisioning Request's state
+
+You can run a following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `Admission Checks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue adds the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` with Provisionig Request's name as a value, which allows identifying a corresponding Provisioning Request to your Workload. You can find this annotation in Workload's Status in the `Admission Checks` field.
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
```

  All good, thank you!

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:105** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-06-03T09:31:01Z
```diff
@@ -0,0 +1,164 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000` with the Cluster Autoscaler at least `28.122.0`
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If you ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field.  it means it is still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
```

  ```suggestion
  Provisioning Request state is described in the `.conditions[*].status` field.  Empty field means the ProvisinongRequest is still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:124** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-06-03T09:35:17Z
```diff
@@ -0,0 +1,164 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000` with the Cluster Autoscaler at least `28.122.0`
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If you ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field.  it means it is still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
```

  we can add: ensure ClusterAutoscaler version supports ProvisioningRequest API.
  The OSS ClusterAutoscaler version that supports ProvReqs  (check-capacity class) is 1.30.1 + user should specify --enable-provisioning-requests=true flag.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2024-06-03T09:50:47Z

- **site/static/images/prov-req-states.svg:1** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2024-06-03T09:50:47Z
  The comment for Failed condition is correct, but a bit misleading as for me.
  
  If ClusterAutoscaler failed to scale up the cluster for ProvReq, it will put Provisioned=False condition or will put nothing and will retry later (depends on implementation). 
  Failed condition will be put if:
  - Something wrong with ProvReq, i.e pod temlate does not exist. 
  - ClusterAutoscaler failed to scale up the cluster for ProvReq for some time, the default is 7 days IIRC. 
  So Failed condition means a bit more than "failed to scale up". Maybe "impossible to scale up the cluster" would be a better description.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T10:12:18Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:18** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T10:12:18Z
```diff
@@ -0,0 +1,164 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000` with the Cluster Autoscaler at least `28.122.0`
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-03T15:02:06Z

Basically, lgtm
Some nits.

- **site/static/images/prov-req-states.svg:1** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-03T14:54:36Z
  One nit.
  Let's use the technical term consistently, "Cluster-autoscaler" vs "cluster-autoscaler".

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:17** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-03T14:56:18Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
```

  @PBundyra It seems that you didn't push changes.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:79** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-03T14:57:24Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If you ProvisioningRequest fails to provision nodes, the error output may look similar to this:
```

  ```suggestion
  If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:106** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-03T14:59:07Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If you ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field.  Empty field means ProvisinongRequest is still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
```

  ```suggestion
  Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the Cluster Autoscaler. Otherwise, it falls into one the states listed below:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:108** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-03T15:00:49Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If you ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field.  Empty field means ProvisinongRequest is still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
```

  ```suggestion
  - `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
  ```
  
  Let's use the technical term consistently. "ClusterAutoScaler" vs "Cluster Autoscaler"

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:109** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-03T15:01:06Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If you ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field.  Empty field means ProvisinongRequest is still being processed by the Cluster Autoscaler. Otherwise it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. Cluster Autoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest.	Condition Reason and Message will contain more details about what failed.
```

  ```suggestion
  - `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T19:09:07Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:17** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T19:09:07Z
```diff
@@ -0,0 +1,102 @@
+---
+title: "Troubleshooting Provisioning Request"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request
+---
+
+This document helps you troubleshoot ProvisioningRequests which is an API defined by [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+You can see the Provisioning Request [documentation](https://cloud.google.com/kubernetes-engine/docs/how-to/provisioningrequest).
+
+Provisioning Requests are created in Kueue with [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and are treated by Kueue like an [Admission Check](/docs/concepts/admission_check/). It means Provisioning Requests needs to succeed for a Workload to be admitted.
+
+## Before you begin
+
+Before you begin troubleshooting make sure your cluster meets the following requirements:
```

  Thanks for spotting that, fixed!

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-03T19:16:46Z

- **site/static/images/prov-req-states.svg:1** by [@PBundyra](https://github.com/PBundyra) — 2024-06-03T19:16:46Z
  Fixed, now there is ClusterAutoscaler everywhere

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-03T19:36:18Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:19** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:26:35Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
```

  ```suggestion
  - Kueue's version is `v0.5.3` or newer.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:20** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:26:59Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
```

  ```suggestion
  - You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration).
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:42** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:28:36Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
```

  ```suggestion
  When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name.
  
  The output of the `kubectl describe workload` command should look similar to the following:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:60** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:29:29Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
```

  ```suggestion
  One of the reasons why your job is not running might be that ProvisioningRequest is waiting to be provisioned.
  To find out if this is the case you can view Provisioning Request's state by running the following command:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:66** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:29:47Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
```

  ```suggestion
  If this is the case, the output should look similar to the following:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:79** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:30:13Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
```

  ```suggestion
  If your ProvisioningRequest fails to provision nodes, the error output may look similar to the following:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:104** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:31:00Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
```

  ```suggestion
  Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on the
  reason that prevented the provisioning.
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:106** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:31:13Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
```

  ```suggestion
  The Provisioning Request state is described in the `.conditions[*].status` field.
  An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler.
  Otherwise, it falls into one the states listed below:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:119** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:32:43Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
```

  ```suggestion
  If Kueue didn't create a Provisioning Request for your job, try checking the following requirements:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:121** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:32:58Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
```

  ```suggestion
  ### a. Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:144** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:33:19Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run the following command to check whether your Kueue's controller manager has enabled the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
```

  Add a note that in v0.7.0 or newer, the feature is enabled by default.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:139** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:34:24Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run the following command to check whether your Kueue's controller manager has enabled the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+### Ensure the ClusterQueue and the LocalQueue are active
```

  More generally, users need to check whether the Workload has the QuotaReserved condition. If it doesn't, then they can follow troubleshooting_jobs to understand why.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:146** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:35:06Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run the following command to check whether your Kueue's controller manager has enabled the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+### Ensure the ClusterQueue and the LocalQueue are active
+You can check the state of ClusterQueues and LocalQueues if the queues are ready for the Workloads.
+Please see the [Troubleshooting Queues](/docs/tasks/troubleshooting/troubleshooting_queues/) for more details.
+
+
+### Ensure the Admission Check is active
+
+To check if the Admission Check your job uses is active run the following command:
```

  ```suggestion
  To check if the Admission Check that your job uses is active run the following command:
  ```

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:206** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T19:35:25Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run the following command to check whether your Kueue's controller manager has enabled the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+### Ensure the ClusterQueue and the LocalQueue are active
+You can check the state of ClusterQueues and LocalQueues if the queues are ready for the Workloads.
+Please see the [Troubleshooting Queues](/docs/tasks/troubleshooting/troubleshooting_queues/) for more details.
+
+
+### Ensure the Admission Check is active
+
+To check if the Admission Check your job uses is active run the following command:
+
+```bash
+kubectl describe admissionchecks ADMISSIONCHECK_NAME
```

  How do I get this name?
  Explain that this is in the ClusterQueue spec.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-03T20:07:28Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:150** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T20:07:28Z
```diff
@@ -143,7 +149,7 @@ Please see the [Troubleshooting Queues](/docs/tasks/troubleshooting/troubleshoot
 
 ### Ensure the Admission Check is active
```

  ```suggestion
  ### b. Ensure the Admission Check is active
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-03T20:07:41Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:150** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T20:07:41Z
```diff
@@ -143,7 +149,7 @@ Please see the [Troubleshooting Queues](/docs/tasks/troubleshooting/troubleshoot
 
 ### Ensure the Admission Check is active
```

  or c, I think

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-04T08:23:39Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:144** by [@PBundyra](https://github.com/PBundyra) — 2024-06-04T08:23:39Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run the following command to check whether your Kueue's controller manager has enabled the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-04T08:23:49Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:150** by [@PBundyra](https://github.com/PBundyra) — 2024-06-04T08:23:49Z
```diff
@@ -143,7 +149,7 @@ Please see the [Troubleshooting Queues](/docs/tasks/troubleshooting/troubleshoot
 
 ### Ensure the Admission Check is active
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-04T08:30:27Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:206** by [@PBundyra](https://github.com/PBundyra) — 2024-06-04T08:30:27Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run the following command to check whether your Kueue's controller manager has enabled the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+### Ensure the ClusterQueue and the LocalQueue are active
+You can check the state of ClusterQueues and LocalQueues if the queues are ready for the Workloads.
+Please see the [Troubleshooting Queues](/docs/tasks/troubleshooting/troubleshooting_queues/) for more details.
+
+
+### Ensure the Admission Check is active
+
+To check if the Admission Check your job uses is active run the following command:
+
+```bash
+kubectl describe admissionchecks ADMISSIONCHECK_NAME
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2024-06-04T09:25:42Z

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:139** by [@PBundyra](https://github.com/PBundyra) — 2024-06-04T09:25:42Z
```diff
@@ -0,0 +1,165 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster's version should be at least `1.28.3-gke.1098000`.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is at least `0.5.3`
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration)
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the corresponding request for your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name. The output of the `kubectl describe workload` command should look similar to this:
+
+```bash
+[...]
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One of the reason your job is not running is that ProvisioningRequest is waiting to be provisioned. To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to this:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to this:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on an error.
+
+Provisioning Request state is described in the `.conditions[*].status` field. An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler. Otherwise, it falls into one the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If your job does not create a corresponding Provisioning Request try checking following requirements:
+
+### Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run the following command to check whether your Kueue's controller manager has enabled the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The args for kueue container should be similar to this:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+### Ensure the ClusterQueue and the LocalQueue are active
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-06-04T15:18:19Z

/approve
/ho

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:146** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-04T15:16:05Z
```diff
@@ -0,0 +1,224 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster should be running version `1.28.3-gke.1098000` or newer.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is `v0.5.3` or newer.
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration). This feature gate is enabled by default for Kueue `v0.7.0` or newer.
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the request corresponding to your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name.
+
+The output of the `kubectl describe workload` command should look similar to the following:
+
+```bash
+[...]
+Status:
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One possible reason your job is not running might be that ProvisioningRequest is waiting to be provisioned.
+To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to the following:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to the following:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on the
+reason that prevented the provisioning.
+
+The Provisioning Request state is described in the `.conditions[*].status` field.
+An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler.
+Otherwise, it falls into one of the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If Kueue did not create a Provisioning Request for your job, try checking the following requirements:
+
+### a. Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run the following command to check whether your Kueue's controller manager has enabled the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The arguments for Kueue container should be similar to the following:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+Please note for Kueue `v0.7.0` or newer the feature is enabled by default, so you may see different output.
```

  Avoid "please" https://kubernetes.io/docs/contribute/style/style-guide/#use-simple-and-direct-language
  
  Same recommendation in other places.

- **site/content/en/docs/tasks/troubleshooting/troubleshooting_provreq.md:182** by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-04T15:17:13Z
```diff
@@ -0,0 +1,224 @@
+---
+title: "Troubleshooting Provisioning Request in Kueue"
+date: 2024-05-20
+weight: 3
+description: >
+  Troubleshooting the status of a Provisioning Request in Kueue
+---
+
+This document helps you troubleshoot ProvisioningRequests, an API defined by [ClusterAutoscaler](https://github.com/kubernetes/autoscaler/blob/4872bddce2bcc5b4a5f6a3d569111c11b8a2baf4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/types.go#L41).
+
+Kueue creates ProvisioningRequests via the [Provisioning Admission Check Controller](/docs/admission-check-controllers/provisioning/), and treats them like an [Admission Check](/docs/concepts/admission_check/). In order for Kueue to admit a Workload, the ProvisioningRequest created for it needs to succeed.
+
+## Before you begin
+
+Before you begin troubleshooting, make sure your cluster meets the following requirements:
+- Your cluster has ClusterAutoscaler enabled and ClusterAutoscaler supports ProvisioningRequest API.
+Check your cloud provider's documentation to determine the minimum versions that support ProvisioningRequest. If you use GKE, your cluster should be running version `1.28.3-gke.1098000` or newer.
+- You use a type of nodes that support ProvisioningRequest. It may vary depending on your cloud provider.
+- Kueue's version is `v0.5.3` or newer.
+- You have enabled the `ProvisioningACC` in [the feature gates configuration](/docs/installation/#change-the-feature-gates-configuration). This feature gate is enabled by default for Kueue `v0.7.0` or newer.
+
+## Identifying the Provisioning Request for your job
+
+See the [Troubleshooting Jobs guide](/docs/tasks/troubleshooting/troubleshooting_jobs/#identifying-the-workload-for-your-job), to learn how to identify the Workload for your job.
+
+You can run the following command to see a brief state of a Provisioning Request (and other Admission Checks) in the `admissionChecks` field of the Workload's Status.
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+Kueue creates ProvisioningRequests using a naming pattern that helps you identify the request corresponding to your workload.
+
+```
+[NAME OF YOUR WORKLOAD]-[NAME OF THE ADMISSION CHECK]-[NUMBER OF RETRY]
+```
+e.g.
+```bash
+sample-job-2zcsb-57864-sample-admissioncheck-1
+```
+
+When nodes for your job are provisioned, Kueue will also add the annotation `cluster-autoscaler.kubernetes.io/consume-provisioning-request` to the `.admissionChecks[*].podSetUpdate[*]` field in Workload's status. The value of this annotation is the Provisioning Request's name.
+
+The output of the `kubectl describe workload` command should look similar to the following:
+
+```bash
+[...]
+Status:
+  Admission Checks:
+    Last Transition Time:  2024-05-22T10:47:46Z
+    Message:               Provisioning Request was successfully provisioned.
+    Name:                  sample-admissioncheck
+    Pod Set Updates:
+      Annotations:
+        cluster-autoscaler.kubernetes.io/consume-provisioning-request:  sample-job-2zcsb-57864-sample-admissioncheck-1
+        cluster-autoscaler.kubernetes.io/provisioning-class-name:       queued-provisioning.gke.io
+      Name:                                                             main
+    State:                                                              Ready
+```
+
+## What is the current state of my Provisioning Request?
+
+One possible reason your job is not running might be that ProvisioningRequest is waiting to be provisioned.
+To find out if this is the case you can view Provisioning Request's state by running the following command:
+
+```bash
+kubectl get provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If this is the case, the output should look similar to the following:
+
+```bash
+NAME                                                 ACCEPTED   PROVISIONED   FAILED   AGE
+sample-job-2zcsb-57864-sample-admissioncheck-1       True       False         False    20s
+```
+
+You can also view more detailed status of your ProvisioningRequest by running the following command:
+
+```bash
+kubectl describe provisioningrequest PROVISIONING_REQUEST_NAME
+```
+
+If your ProvisioningRequest fails to provision nodes, the error output may look similar to the following:
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't accepted.
+    Observed Generation:   1
+    Reason:                NotAccepted
+    Status:                False
+    Type:                  Accepted
+    Last Transition Time:  2024-05-22T13:04:54Z
+    Message:               Provisioning Request wasn't provisioned.
+    Observed Generation:   1
+    Reason:                NotProvisioned
+    Status:                False
+    Type:                  Provisioned
+    Last Transition Time:  2024-05-22T13:06:49Z
+    Message:               max cluster limit reached, nodepools out of resources: default-nodepool (cpu, memory)
+    Observed Generation:   1
+    Reason:                OutOfResources
+    Status:                True
+    Type:                  Failed
+```
+
+Note that the `Reason` and `Message` values for `Failed` condition may differ from your output, depending on the
+reason that prevented the provisioning.
+
+The Provisioning Request state is described in the `.conditions[*].status` field.
+An empty field means ProvisinongRequest is still being processed by the ClusterAutoscaler.
+Otherwise, it falls into one of the states listed below:
+- `Accepted` - indicates that the ProvisioningRequest was accepted by ClusterAutoscaler, so ClusterAutoscaler will attempt to provision the nodes for it.
+- `Provisioned` - indicates that all of the requested resources were created and are available in the cluster. ClusterAutoscaler will set this condition when the VM creation finishes successfully.
+- `Failed` - indicates that it is impossible to obtain resources to fulfill this ProvisioningRequest. Condition Reason and Message will contain more details about what failed.
+- `BookingExpired` - indicates that the ProvisioningRequest had Provisioned condition before and capacity reservation time is expired.
+- `CapacityRevoked` - indicates that requested resources are not longer valid.
+
+The states transitions are as follow:
+
+![Provisioning Request's states](/images/prov-req-states.svg)
+
+## Why a Provisioning Request is not created?
+
+If Kueue did not create a Provisioning Request for your job, try checking the following requirements:
+
+### a. Ensure the Kueue's controller manager enables the `ProvisioningACC` feature gate
+
+Run the following command to check whether your Kueue's controller manager has enabled the `ProvisioningACC` feature gate:
+
+```bash
+kubectl describe pod -n kueue-system kueue-controller-manager-
+```
+
+The arguments for Kueue container should be similar to the following:
+
+```bash
+    ...
+    Args:
+      --config=/controller_manager_config.yaml
+      --zap-log-level=2
+      --feature-gates=ProvisioningACC=true
+```
+
+Please note for Kueue `v0.7.0` or newer the feature is enabled by default, so you may see different output.
+
+### b. Ensure your Workload has reserved quota
+
+To check if your Workload has reserved quota in a ClusterQueue check your Workload's status by running the following command:
+
+```bash
+kubectl describe workload WORKLOAD_NAME
+```
+
+The output should be similar to the following:
+
+```bash
+[...]
+Status:
+  Conditions:
+    Last Transition Time:  2024-05-22T10:26:40Z
+    Message:               Quota reserved in ClusterQueue cluster-queue
+    Observed Generation:   1
+    Reason:                QuotaReserved
+    Status:                True
+    Type:                  QuotaReserved
+```
+
+If the output you get is similar to the following:
+
+```bash
+  Conditions:
+    Last Transition Time:  2024-05-22T08:48:47Z
+    Message:               couldn't assign flavors to pod set main: insufficient unused quota for memory in flavor default-flavor, 4396Mi more needed
+    Observed Generation:   1
+    Reason:                Pending
+    Status:                False
+    Type:                  QuotaReserved
+```
+
+This means you do not have sufficient free capacity in your ClusterQueue.
```

  ```suggestion
  This means you do not have sufficient free quota in your ClusterQueue.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-05T09:09:02Z

/lgtm
/hold cancel
