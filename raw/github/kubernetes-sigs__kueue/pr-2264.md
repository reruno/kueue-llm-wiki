# PR #2264: Add limit to the retry period for re-queing workloads

**Summary**: Add limit to the retry period for re-queing workloads

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2264

**Last updated**: 2024-05-28T11:34:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@IrvingMg](https://github.com/IrvingMg)
- **Branch**: `feature/limit-retry-period-waitforpodsready` → `main`
- **Created**: 2024-05-22T21:46:13Z
- **Updated**: 2024-05-28T11:34:06Z
- **Merged**: 2024-05-24T08:22:11Z
- **Closed**: 2024-05-24T08:22:11Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet), [@trasc](https://github.com/trasc)
- **Changed files**: 11   **+102 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:

Adds the `BackoffMaxSeconds` property to RequeuingStrategy so that a limit can be set to the retry period.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2216

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Adds the `BackoffMaxSeconds` property to limit the retry period length for re-queing workloads.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-22T21:46:16Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-22T21:46:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a489bddf32e2fb2acd2023036a024849941be381 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/665045a6836cae00080fc1c5 |

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-22T22:04:00Z

/test all

### Comment by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-22T22:15:19Z

/assign @trasc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-23T10:44:36Z

LGTM label has been added.  <details>Git tree hash: b4345fdf2b72f1ca8a0baf6331ee4f4525652461</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-23T18:22:29Z

/approve
for API
/hold
Leaving LGTM to @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-23T18:22:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2264#issuecomment-2127782751" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2264#" title="Author self-approved">IrvingMg</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-24T08:14:11Z

LGTM label has been added.  <details>Git tree hash: 03c1845c1a7e52d4bd5cea9e67647c800f3b8142</details>

### Comment by [@trasc](https://github.com/trasc) — 2024-05-24T08:18:13Z

/unhold

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-23T05:26:15Z

- **pkg/config/validation.go:133** by [@trasc](https://github.com/trasc) — 2024-05-23T05:26:15Z
```diff
@@ -130,6 +130,10 @@ func validateWaitForPodsReady(c *configapi.Configuration) field.ErrorList {
 			allErrs = append(allErrs, field.Invalid(requeuingStrategyPath.Child("backoffBaseSeconds"),
 				*strategy.BackoffBaseSeconds, constants.IsNegativeErrorMsg))
 		}
+		if strategy.BackoffMaxSeconds != nil && *strategy.BackoffMaxSeconds < 0 {
```

  ```suggestion
  		if ptr.Deref(strategy.BackoffMaxSeconds, 0)  < 0 {
  ```
  nit

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-23T05:34:48Z

- **pkg/controller/core/workload_controller.go:484** by [@trasc](https://github.com/trasc) — 2024-05-23T05:34:38Z
```diff
@@ -470,6 +471,18 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 	for backoff.Steps > 0 {
 		waitDuration = backoff.Step()
 	}
+
+	maxWaitDuration := time.Duration(r.waitForPodsReady.requeuingBackoffMaxSeconds) * time.Second
+	if waitDuration > maxWaitDuration {
+		wl.Spec.Active = ptr.To(false)
+		if err := r.client.Update(ctx, wl); err != nil {
+			return false, err
+		}
+		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
+			"Deactivated Workload %q by reached re-queue backoffMaxSeconds", klog.KObj(wl))
+		return true, nil
+	}
```

  I think the desired behaviour is not to have wait times more then `BackoffMaxSeconds`
  
  ```suggestion
  	maxWaitDuration := time.Duration(r.waitForPodsReady.requeuingBackoffMaxSeconds) * time.Second
  	for backoff.Steps > 0 {
  		waitDuration = backoff.Step()
  		if waitDuration > maxWaitDuration {
  			waitDuration > maxWaitDuration
  			break
  		}
  	}
  ```

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-23T10:00:53Z

- **pkg/controller/core/workload_controller.go:484** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-23T10:00:53Z
```diff
@@ -470,6 +471,18 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 	for backoff.Steps > 0 {
 		waitDuration = backoff.Step()
 	}
+
+	maxWaitDuration := time.Duration(r.waitForPodsReady.requeuingBackoffMaxSeconds) * time.Second
+	if waitDuration > maxWaitDuration {
+		wl.Spec.Active = ptr.To(false)
+		if err := r.client.Update(ctx, wl); err != nil {
+			return false, err
+		}
+		r.recorder.Eventf(wl, corev1.EventTypeNormal, kueue.WorkloadEvictedByDeactivation,
+			"Deactivated Workload %q by reached re-queue backoffMaxSeconds", klog.KObj(wl))
+		return true, nil
+	}
```

  Got it. Thanks

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-23T10:44:31Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-23T12:34:50Z

- **pkg/controller/core/workload_controller.go:477** by [@mimowo](https://github.com/mimowo) — 2024-05-23T12:29:42Z
```diff
@@ -467,9 +468,15 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		Steps:    int(requeuingCount),
 	}
 	var waitDuration time.Duration
+	maxWaitDuration := time.Duration(r.waitForPodsReady.requeuingBackoffMaxSeconds) * time.Second
 	for backoff.Steps > 0 {
-		waitDuration = backoff.Step()
+		newWaitDuration := backoff.Step()
+		if newWaitDuration > maxWaitDuration {
+			break
+		}
+		waitDuration = newWaitDuration
```

  ```suggestion
  		waitDuration := min(backoff.Step(), maxWaitDuration)
  ```
  seems much easier to read

- **pkg/controller/core/workload_controller.go:471** by [@mimowo](https://github.com/mimowo) — 2024-05-23T12:31:34Z
```diff
@@ -467,9 +468,15 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		Steps:    int(requeuingCount),
 	}
 	var waitDuration time.Duration
+	maxWaitDuration := time.Duration(r.waitForPodsReady.requeuingBackoffMaxSeconds) * time.Second
```

  We could record this on a field of the controller to avoid re-computation.

- **apis/config/v1beta1/configuration_types.go:263** by [@mimowo](https://github.com/mimowo) — 2024-05-23T12:34:39Z
```diff
@@ -259,6 +259,12 @@ type RequeuingStrategy struct {
 	// Defaults to 60.
 	// +optional
 	BackoffBaseSeconds *int32 `json:"backoffBaseSeconds,omitempty"`
+
+	// BackoffMaxSeconds defines the maximum length of retry period for re-queuing.
```

  ```suggestion
  	// BackoffMaxSeconds defines the maximum backoff time to re-queue an evicted workload.
  ```
  nit suggestion

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-23T15:39:31Z

- **pkg/controller/core/workload_controller.go:471** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-23T15:39:31Z
```diff
@@ -467,9 +468,15 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		Steps:    int(requeuingCount),
 	}
 	var waitDuration time.Duration
+	maxWaitDuration := time.Duration(r.waitForPodsReady.requeuingBackoffMaxSeconds) * time.Second
```

  I've modified this to record the value in RequeueState.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-23T16:01:03Z

- **pkg/controller/core/workload_controller.go:471** by [@trasc](https://github.com/trasc) — 2024-05-23T16:01:03Z
```diff
@@ -467,9 +468,15 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		Steps:    int(requeuingCount),
 	}
 	var waitDuration time.Duration
+	maxWaitDuration := time.Duration(r.waitForPodsReady.requeuingBackoffMaxSeconds) * time.Second
```

  Adding a dedicated API field (which is close to impossible do remove) just for this is a bit too much in my opinion, I'd go with a "magic value" for `status.RequeueState.Count` (say `-1` , `maxInt` ), indicating that the maximum value should be used.
  
  @alculquicondor @mimowo WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-23T16:27:00Z

- **pkg/controller/core/workload_controller.go:471** by [@mimowo](https://github.com/mimowo) — 2024-05-23T16:27:00Z
```diff
@@ -467,9 +468,15 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		Steps:    int(requeuingCount),
 	}
 	var waitDuration time.Duration
+	maxWaitDuration := time.Duration(r.waitForPodsReady.requeuingBackoffMaxSeconds) * time.Second
```

  Sorry for not clarifying enough, I was thinking about storing it only in memory, not API, as an internal field of the reconciler when instantiating it

### Review by [@IrvingMg](https://github.com/IrvingMg) (commented) — 2024-05-23T17:36:03Z

- **pkg/controller/core/workload_controller.go:471** by [@IrvingMg](https://github.com/IrvingMg) — 2024-05-23T17:36:03Z
```diff
@@ -467,9 +468,15 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		Steps:    int(requeuingCount),
 	}
 	var waitDuration time.Duration
+	maxWaitDuration := time.Duration(r.waitForPodsReady.requeuingBackoffMaxSeconds) * time.Second
```

  Done. I've added the value to waitForPodsReadyConfig in WorkloadReconciler

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-24T07:05:53Z

Almost lgtm, one pending comment

- **pkg/controller/core/workload_controller.go:471** by [@mimowo](https://github.com/mimowo) — 2024-05-24T07:05:05Z
```diff
@@ -467,9 +468,10 @@ func (r *WorkloadReconciler) triggerDeactivationOrBackoffRequeue(ctx context.Con
 		Steps:    int(requeuingCount),
 	}
 	var waitDuration time.Duration
-	for backoff.Steps > 0 {
-		waitDuration = backoff.Step()
+	for backoff.Steps > 0 && waitDuration < r.waitForPodsReady.requeuingBackoffMaxDuration {
```

  This new check for wait duration looks unnecessary. Please drop.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-24T08:14:06Z

/lgtm
Thanks
