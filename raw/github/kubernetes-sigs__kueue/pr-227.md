# PR #227: Change make cluster queue in wrappers to bestfifo

**Summary**: Change make cluster queue in wrappers to bestfifo

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/227

**Last updated**: 2022-05-02T22:53:55Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@denkensk](https://github.com/denkensk)
- **Branch**: `change-make-cq` → `main`
- **Created**: 2022-04-21T09:08:40Z
- **Updated**: 2022-05-02T22:53:55Z
- **Merged**: 2022-05-01T03:19:12Z
- **Closed**: 2022-05-01T03:19:12Z
- **Labels**: `size/L`, `lgtm`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 4   **+113 / -44**

## Description

Signed-off-by: Alex Wang <wangqingcan1990@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
https://github.com/kubernetes-sigs/kueue/pull/179#discussion_r844249409_

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-21T09:09:03Z

/assign @ahg-g

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-21T09:13:14Z

/test pull-kueue-test-integration-main

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-04-25T14:57:19Z

I will leave this one to @ahg-g

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-30T14:49:04Z

> Can you please also add a test dedicated for StrictFIFO? 

Added @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T02:23:58Z

please squash.

### Comment by [@denkensk](https://github.com/denkensk) — 2022-05-01T02:46:24Z

/test pull-kueue-test-integration-main

### Comment by [@denkensk](https://github.com/denkensk) — 2022-05-01T02:48:50Z

> please squash.

Done. @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T02:49:10Z

> /test pull-kueue-test-integration-main

We shouldn't dismiss any failed run, if it was a new flake, we need to fix it. We shouldn't just keep retrying the test, this is not k8s repo :)

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T03:17:50Z

> > /test pull-kueue-test-integration-main
> 
> We shouldn't dismiss any failed run, if it was a new flake, we need to fix it. We shouldn't just keep retrying the test, this is not k8s repo :)

Created https://github.com/kubernetes-sigs/kueue/issues/241; I think this flake discovered a potential bug.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T03:17:58Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-01T03:18:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/227#issuecomment-1114116922" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/227#" title="Author self-approved">denkensk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@denkensk](https://github.com/denkensk) — 2022-05-01T03:21:36Z

> Created https://github.com/kubernetes-sigs/kueue/issues/241; I think this flake discovered a potential bug.

Thanks. I tested it a lot on my own computer and did not reproduce it. But in CI test there is a chance that it will fail.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-05-02T13:49:03Z

Is the new flake only showing up after this PR?

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-02T22:53:55Z

It was on this PR, but this PR only changes the tests, so the flake is very likely unrelated to this PR.

## Reviews

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-21T09:23:09Z

- **test/integration/scheduler/scheduler_test.go:260** by [@denkensk](https://github.com/denkensk) — 2022-04-21T09:23:09Z
```diff
@@ -285,6 +257,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 	ginkgo.It("Should schedule jobs from the selected namespaces", func() {
 		clusterQ := testing.MakeClusterQueue("cluster-queue-with-selector").
+			QueueingStrategy(kueue.StrictFIFO).
```

  I think we may also need to watch the events of `ns` to trigger the movement of workloads in ClusterQueue with BestFIFO.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-25T04:17:07Z

Can you please also add a test dedicated for StrictFIFO?

- **test/integration/scheduler/scheduler_test.go:260** by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T04:01:46Z
```diff
@@ -285,6 +257,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 	ginkgo.It("Should schedule jobs from the selected namespaces", func() {
 		clusterQ := testing.MakeClusterQueue("cluster-queue-with-selector").
+			QueueingStrategy(kueue.StrictFIFO).
```

  yes, can you please open a tracking issue for that? I am guessing this test didn't pass without changing the strategy because of this, right?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-04-30T14:48:46Z

- **test/integration/scheduler/scheduler_test.go:260** by [@denkensk](https://github.com/denkensk) — 2022-04-30T14:48:46Z
```diff
@@ -285,6 +257,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 	ginkgo.It("Should schedule jobs from the selected namespaces", func() {
 		clusterQ := testing.MakeClusterQueue("cluster-queue-with-selector").
+			QueueingStrategy(kueue.StrictFIFO).
```

  Yes opened https://github.com/kubernetes-sigs/kueue/issues/234

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-30T20:31:58Z

- **test/integration/scheduler/scheduler_test.go:621** by [@ahg-g](https://github.com/ahg-g) — 2022-04-30T19:55:22Z
```diff
@@ -554,18 +580,53 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		// Make sure workloads are in the same scheduling cycle.
 		testBEClusterQ := testing.MakeClusterQueue("test-be-cq").
-			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
+			Cohort("be").
 			Resource(testing.MakeResource(corev1.ResourceCPU).
 				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
 					"15").Max("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, testBEClusterQ)).Should(gomega.Succeed())
 		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, prodBEClusterQ)).Should(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, devBEClusterQ)).Should(gomega.Succeed())
 			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, testBEClusterQ)).Should(gomega.Succeed())
 		}()
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodBEClusterQ.Name, wl1)
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devBEClusterQ.Name, wl2)
 	})
+
+	ginkgo.It("Should schedule workloads by their priority strictly in StrictFIFO", func() {
+		strictFIFOClusterQ := testing.MakeClusterQueue("strict-fifo-cq").
+			QueueingStrategy(kueue.StrictFIFO).
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"5").Max("5").Obj()).
+				Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, strictFIFOClusterQ)).Should(gomega.Succeed())
+		}()
+
+		strictFIFOQueue := testing.MakeQueue("strict-fifo-q", ns.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+		ginkgo.By("Creating workloads")
+		wl1 := testing.MakeWorkload("wl1", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "2").Priority(pointer.Int32(100)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		wl2 := testing.MakeWorkload("wl2", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "5").Priority(pointer.Int32(10)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		// wl3 can't be scheduled before wl2 even it can be scheduled successfully.
```

  ```suggestion
  		// wl3 can't be scheduled before wl2 even though there is enough quota.
  ```

- **test/integration/scheduler/scheduler_test.go:425** by [@ahg-g](https://github.com/ahg-g) — 2022-04-30T20:07:24Z
```diff
@@ -433,7 +422,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 
-		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(devBEQueue.Name).Request(corev1.ResourceCPU, "8").Obj()
+		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
```

  just to make sure I am understanding the test correctly, the change from 8 to 5 is not necessary, right?

- **test/integration/scheduler/scheduler_test.go:466** by [@ahg-g](https://github.com/ahg-g) — 2022-04-30T20:11:29Z
```diff
@@ -470,7 +459,23 @@ var _ = ginkgo.Describe("Scheduler", func() {
 	})
 
 	ginkgo.It("Should re-enqueue by the updated event of ClusterQueue", func() {
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodBEQueue.Name).Request(corev1.ResourceCPU, "8").Obj()
+		devBEClusterQ := testing.MakeClusterQueue("dev-be-cq").
+			Cohort("be").
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"5").Max("5").Obj()).
```

  ```suggestion
  				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Max("5").Obj()).
  ```

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-01T00:40:31Z

- **test/integration/scheduler/scheduler_test.go:425** by [@denkensk](https://github.com/denkensk) — 2022-05-01T00:40:31Z
```diff
@@ -433,7 +422,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 
-		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(devBEQueue.Name).Request(corev1.ResourceCPU, "8").Obj()
+		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
```

  because it reuse `prodClusterQ` again, min of it in `onDemandFlavor` is `5`. Just want it's not over the min.
  ```go
  prodClusterQ = testing.MakeClusterQueue("prod-cq").
  	Cohort("prod").
  	Resource(testing.MakeResource(corev1.ResourceCPU).
  		Flavor(testing.MakeFlavor(spotTaintedFlavor.Name, "5").Max("5").Obj()).
  		Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Obj()).
  		Obj()).
  	Obj()
  ```

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-01T00:44:18Z

- **test/integration/scheduler/scheduler_test.go:466** by [@denkensk](https://github.com/denkensk) — 2022-05-01T00:44:18Z
```diff
@@ -470,7 +459,23 @@ var _ = ginkgo.Describe("Scheduler", func() {
 	})
 
 	ginkgo.It("Should re-enqueue by the updated event of ClusterQueue", func() {
-		job1 := testing.MakeJob("on-demand-job1", ns.Name).Queue(prodBEQueue.Name).Request(corev1.ResourceCPU, "8").Obj()
+		devBEClusterQ := testing.MakeClusterQueue("dev-be-cq").
+			Cohort("be").
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"5").Max("5").Obj()).
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-01T00:44:23Z

- **test/integration/scheduler/scheduler_test.go:621** by [@denkensk](https://github.com/denkensk) — 2022-05-01T00:44:23Z
```diff
@@ -554,18 +580,53 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		// Make sure workloads are in the same scheduling cycle.
 		testBEClusterQ := testing.MakeClusterQueue("test-be-cq").
-			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
+			Cohort("be").
 			Resource(testing.MakeResource(corev1.ResourceCPU).
 				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
 					"15").Max("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, testBEClusterQ)).Should(gomega.Succeed())
 		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, prodBEClusterQ)).Should(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, devBEClusterQ)).Should(gomega.Succeed())
 			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, testBEClusterQ)).Should(gomega.Succeed())
 		}()
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodBEClusterQ.Name, wl1)
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devBEClusterQ.Name, wl2)
 	})
+
+	ginkgo.It("Should schedule workloads by their priority strictly in StrictFIFO", func() {
+		strictFIFOClusterQ := testing.MakeClusterQueue("strict-fifo-cq").
+			QueueingStrategy(kueue.StrictFIFO).
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"5").Max("5").Obj()).
+				Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, strictFIFOClusterQ)).Should(gomega.Succeed())
+		}()
+
+		strictFIFOQueue := testing.MakeQueue("strict-fifo-q", ns.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+		ginkgo.By("Creating workloads")
+		wl1 := testing.MakeWorkload("wl1", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "2").Priority(pointer.Int32(100)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		wl2 := testing.MakeWorkload("wl2", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "5").Priority(pointer.Int32(10)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		// wl3 can't be scheduled before wl2 even it can be scheduled successfully.
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-01T01:40:04Z

- **test/integration/scheduler/scheduler_test.go:629** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T01:40:04Z
```diff
@@ -554,18 +579,53 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		// Make sure workloads are in the same scheduling cycle.
 		testBEClusterQ := testing.MakeClusterQueue("test-be-cq").
-			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
+			Cohort("be").
 			Resource(testing.MakeResource(corev1.ResourceCPU).
 				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
 					"15").Max("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, testBEClusterQ)).Should(gomega.Succeed())
 		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, prodBEClusterQ)).Should(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, devBEClusterQ)).Should(gomega.Succeed())
 			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, testBEClusterQ)).Should(gomega.Succeed())
 		}()
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodBEClusterQ.Name, wl1)
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devBEClusterQ.Name, wl2)
 	})
+
+	ginkgo.It("Should schedule workloads by their priority strictly in StrictFIFO", func() {
+		strictFIFOClusterQ := testing.MakeClusterQueue("strict-fifo-cq").
+			QueueingStrategy(kueue.StrictFIFO).
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"5").Max("5").Obj()).
+				Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, strictFIFOClusterQ)).Should(gomega.Succeed())
+		}()
+
+		strictFIFOQueue := testing.MakeQueue("strict-fifo-q", ns.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+		ginkgo.By("Creating workloads")
+		wl1 := testing.MakeWorkload("wl1", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "2").Priority(pointer.Int32(100)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		wl2 := testing.MakeWorkload("wl2", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "5").Priority(pointer.Int32(10)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		// wl3 can't be scheduled before wl2 even though there is enough quota.
+		wl3 := testing.MakeWorkload("wl3", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "1").Priority(pointer.Int32(1)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOQueue)).Should(gomega.Succeed())
+
+		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, strictFIFOClusterQ.Name, wl1)
+		framework.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
+		framework.ExpectWorkloadsToBeInadmissible(ctx, k8sClient, wl3)
```

  I think wl3 will never be tried by the scheduler, so its condition in status will never be set. I guess the thing we need to check here is that the admission field is consistently not set.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-01T02:00:49Z

- **test/integration/scheduler/scheduler_test.go:629** by [@denkensk](https://github.com/denkensk) — 2022-05-01T02:00:49Z
```diff
@@ -554,18 +579,53 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		// Make sure workloads are in the same scheduling cycle.
 		testBEClusterQ := testing.MakeClusterQueue("test-be-cq").
-			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
+			Cohort("be").
 			Resource(testing.MakeResource(corev1.ResourceCPU).
 				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
 					"15").Max("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, testBEClusterQ)).Should(gomega.Succeed())
 		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, prodBEClusterQ)).Should(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, devBEClusterQ)).Should(gomega.Succeed())
 			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, testBEClusterQ)).Should(gomega.Succeed())
 		}()
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodBEClusterQ.Name, wl1)
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devBEClusterQ.Name, wl2)
 	})
+
+	ginkgo.It("Should schedule workloads by their priority strictly in StrictFIFO", func() {
+		strictFIFOClusterQ := testing.MakeClusterQueue("strict-fifo-cq").
+			QueueingStrategy(kueue.StrictFIFO).
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"5").Max("5").Obj()).
+				Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, strictFIFOClusterQ)).Should(gomega.Succeed())
+		}()
+
+		strictFIFOQueue := testing.MakeQueue("strict-fifo-q", ns.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+		ginkgo.By("Creating workloads")
+		wl1 := testing.MakeWorkload("wl1", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "2").Priority(pointer.Int32(100)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		wl2 := testing.MakeWorkload("wl2", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "5").Priority(pointer.Int32(10)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		// wl3 can't be scheduled before wl2 even though there is enough quota.
+		wl3 := testing.MakeWorkload("wl3", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "1").Priority(pointer.Int32(1)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOQueue)).Should(gomega.Succeed())
+
+		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, strictFIFOClusterQ.Name, wl1)
+		framework.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
+		framework.ExpectWorkloadsToBeInadmissible(ctx, k8sClient, wl3)
```

  Yes. I change it to consistently check.
  
  By the way, its condition in status is set as `Inadmissible Queue strict-fifo-q doesn't exist`
  https://github.com/kubernetes-sigs/kueue/blob/82201f8ca8bc245324800b3a89747d173c55767e/pkg/controller/core/workload_controller.go#L85

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-01T02:20:07Z

- **test/integration/scheduler/scheduler_test.go:629** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T02:20:07Z
```diff
@@ -554,18 +579,53 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		// Make sure workloads are in the same scheduling cycle.
 		testBEClusterQ := testing.MakeClusterQueue("test-be-cq").
-			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
+			Cohort("be").
 			Resource(testing.MakeResource(corev1.ResourceCPU).
 				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
 					"15").Max("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, testBEClusterQ)).Should(gomega.Succeed())
 		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, prodBEClusterQ)).Should(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, devBEClusterQ)).Should(gomega.Succeed())
 			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, testBEClusterQ)).Should(gomega.Succeed())
 		}()
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodBEClusterQ.Name, wl1)
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devBEClusterQ.Name, wl2)
 	})
+
+	ginkgo.It("Should schedule workloads by their priority strictly in StrictFIFO", func() {
+		strictFIFOClusterQ := testing.MakeClusterQueue("strict-fifo-cq").
+			QueueingStrategy(kueue.StrictFIFO).
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"5").Max("5").Obj()).
+				Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, strictFIFOClusterQ)).Should(gomega.Succeed())
+		}()
+
+		strictFIFOQueue := testing.MakeQueue("strict-fifo-q", ns.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+		ginkgo.By("Creating workloads")
+		wl1 := testing.MakeWorkload("wl1", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "2").Priority(pointer.Int32(100)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		wl2 := testing.MakeWorkload("wl2", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "5").Priority(pointer.Int32(10)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		// wl3 can't be scheduled before wl2 even though there is enough quota.
+		wl3 := testing.MakeWorkload("wl3", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "1").Priority(pointer.Int32(1)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOQueue)).Should(gomega.Succeed())
+
+		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, strictFIFOClusterQ.Name, wl1)
+		framework.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
+		framework.ExpectWorkloadsToBeInadmissible(ctx, k8sClient, wl3)
```

  But the queue does exist, it is just that the workload will never be at the head of the clusterQueue that that queue references and so will never be tried.
  
  did you ever see it marked as Inadmissible in the test?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-01T02:23:23Z

- **test/integration/scheduler/scheduler_test.go:626** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T02:23:23Z
```diff
@@ -554,18 +579,57 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		// Make sure workloads are in the same scheduling cycle.
 		testBEClusterQ := testing.MakeClusterQueue("test-be-cq").
-			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
+			Cohort("be").
 			Resource(testing.MakeResource(corev1.ResourceCPU).
 				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
 					"15").Max("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, testBEClusterQ)).Should(gomega.Succeed())
 		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, prodBEClusterQ)).Should(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, devBEClusterQ)).Should(gomega.Succeed())
 			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, testBEClusterQ)).Should(gomega.Succeed())
 		}()
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodBEClusterQ.Name, wl1)
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devBEClusterQ.Name, wl2)
 	})
+
+	ginkgo.It("Should schedule workloads by their priority strictly in StrictFIFO", func() {
+		strictFIFOClusterQ := testing.MakeClusterQueue("strict-fifo-cq").
+			QueueingStrategy(kueue.StrictFIFO).
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"5").Max("5").Obj()).
+				Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, strictFIFOClusterQ)).Should(gomega.Succeed())
+		}()
+
+		strictFIFOQueue := testing.MakeQueue("strict-fifo-q", ns.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+		ginkgo.By("Creating workloads")
+		wl1 := testing.MakeWorkload("wl1", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "2").Priority(pointer.Int32(100)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		wl2 := testing.MakeWorkload("wl2", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "5").Priority(pointer.Int32(10)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		// wl3 can't be scheduled before wl2 even though there is enough quota.
+		wl3 := testing.MakeWorkload("wl3", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "1").Priority(pointer.Int32(1)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOQueue)).Should(gomega.Succeed())
```

  ah, you are creating the queue after creating the workloads, that is why sometimes you are getting the inadmissible condition, but that is never going to be guaranteed because the job-controller may not get the workload create event before this queue creation call.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-01T02:26:20Z

- **test/integration/scheduler/scheduler_test.go:425** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T02:26:20Z
```diff
@@ -433,7 +422,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 
-		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(devBEQueue.Name).Request(corev1.ResourceCPU, "8").Obj()
+		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
```

  but the min was also 5 for the dev BE:
  
  ```
  devBEClusterQ = testing.MakeClusterQueue("dev-be-cq").
  			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
  			Resource(testing.MakeResource(corev1.ResourceCPU).
  				Flavor(testing.MakeFlavor(onDemandFlavor.Name, "5").Max("15").Obj()).
  				Obj()).
  			Obj()
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-01T02:27:08Z

- **test/integration/scheduler/scheduler_test.go:260** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T02:27:08Z
```diff
@@ -285,6 +257,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 	ginkgo.It("Should schedule jobs from the selected namespaces", func() {
 		clusterQ := testing.MakeClusterQueue("cluster-queue-with-selector").
+			QueueingStrategy(kueue.StrictFIFO).
```

  thanks, this is actually a bug that we may need to backport to 0.1.0

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-01T02:44:32Z

- **test/integration/scheduler/scheduler_test.go:425** by [@denkensk](https://github.com/denkensk) — 2022-05-01T02:44:32Z
```diff
@@ -433,7 +422,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 		gomega.Expect(createdJob1.Spec.Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
 
-		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(devBEQueue.Name).Request(corev1.ResourceCPU, "8").Obj()
+		job2 := testing.MakeJob("on-demand-job2", ns.Name).Queue(prodQueue.Name).Request(corev1.ResourceCPU, "5").Obj()
```

  `clusterQ` created in this case share the same `Cohort` with `prodClusterQ`, so we change to use `prodClusterQ`. But I find it's also ok to keep `8` here instead of changing to `5`, I revert it.
  ```go
  clusterQ := testing.MakeClusterQueue("cq").
  	Cohort("prod").
  	Resource(testing.MakeResource(corev1.ResourceCPU).
  		Flavor(testing.MakeFlavor(onDemandFlavor.Name,
  			"5").Obj()).
  		Obj()).
  	Obj()
  ```

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-05-01T02:49:37Z

- **test/integration/scheduler/scheduler_test.go:626** by [@denkensk](https://github.com/denkensk) — 2022-05-01T02:49:37Z
```diff
@@ -554,18 +579,57 @@ var _ = ginkgo.Describe("Scheduler", func() {
 
 		// Make sure workloads are in the same scheduling cycle.
 		testBEClusterQ := testing.MakeClusterQueue("test-be-cq").
-			Cohort("be").QueueingStrategy(kueue.BestEffortFIFO).
+			Cohort("be").
 			Resource(testing.MakeResource(corev1.ResourceCPU).
 				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
 					"15").Max("15").Obj()).
 				Obj()).
 			Obj()
 		gomega.Expect(k8sClient.Create(ctx, testBEClusterQ)).Should(gomega.Succeed())
 		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, prodBEClusterQ)).Should(gomega.Succeed())
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, devBEClusterQ)).Should(gomega.Succeed())
 			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, testBEClusterQ)).Should(gomega.Succeed())
 		}()
 
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, prodBEClusterQ.Name, wl1)
 		framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, devBEClusterQ.Name, wl2)
 	})
+
+	ginkgo.It("Should schedule workloads by their priority strictly in StrictFIFO", func() {
+		strictFIFOClusterQ := testing.MakeClusterQueue("strict-fifo-cq").
+			QueueingStrategy(kueue.StrictFIFO).
+			Resource(testing.MakeResource(corev1.ResourceCPU).
+				Flavor(testing.MakeFlavor(onDemandFlavor.Name,
+					"5").Max("5").Obj()).
+				Obj()).
+			Obj()
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOClusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, strictFIFOClusterQ)).Should(gomega.Succeed())
+		}()
+
+		strictFIFOQueue := testing.MakeQueue("strict-fifo-q", ns.Name).ClusterQueue(strictFIFOClusterQ.Name).Obj()
+		ginkgo.By("Creating workloads")
+		wl1 := testing.MakeWorkload("wl1", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "2").Priority(pointer.Int32(100)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+		wl2 := testing.MakeWorkload("wl2", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "5").Priority(pointer.Int32(10)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+		// wl3 can't be scheduled before wl2 even though there is enough quota.
+		wl3 := testing.MakeWorkload("wl3", ns.Name).Queue(strictFIFOQueue.
+			Name).Request(corev1.ResourceCPU, "1").Priority(pointer.Int32(1)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+
+		gomega.Expect(k8sClient.Create(ctx, strictFIFOQueue)).Should(gomega.Succeed())
```

  Yes. You are right. It's not guaranteed.
