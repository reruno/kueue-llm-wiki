# PR #2276: Show fair share of a CQ in status and a metric

**Summary**: Show fair share of a CQ in status and a metric

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2276

**Last updated**: 2024-05-27T17:14:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/show-fair-share-of-a-cq-in-status-and-a-metric` → `main`
- **Created**: 2024-05-24T15:14:45Z
- **Updated**: 2024-05-27T17:14:06Z
- **Merged**: 2024-05-27T17:13:29Z
- **Closed**: 2024-05-27T17:13:29Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 19   **+326 / -21**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Show fair share of a CQ in status and a metric.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #2258 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Show fair share of a CQ in status and a metric
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-24T15:14:48Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-24T15:15:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1ac6a70655f3a21aa0a065f9c533d16288f0dfc4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6654b8e2c7dbfb0008ad4967 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T15:39:34Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T15:39:49Z

/assign @trasc

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T21:49:32Z

/test all

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T16:21:15Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-27T17:08:50Z

LGTM label has been added.  <details>Git tree hash: 1d7a867953eaa5bc42527c7f147c1f542c0047ec</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-27T17:08:51Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2276#pullrequestreview-2081287297" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2276#" title="Author self-approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-24T20:07:53Z

- **apis/kueue/v1beta1/clusterqueue_types.go:306** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T20:00:13Z
```diff
@@ -300,6 +300,10 @@ type ClusterQueueStatus struct {
 	// status of the pending workloads in the cluster queue.
 	// +optional
 	PendingWorkloadsStatus *ClusterQueuePendingWorkloadsStatus `json:"pendingWorkloadsStatus"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharing *FairSharingStatus `json:"fairSharing"`
```

  ```suggestion
  	FairSharing *FairSharingStatus `json:"fairSharing,omitempty"`
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:356** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T20:00:39Z
```diff
@@ -346,6 +350,14 @@ type ResourceUsage struct {
 	Borrowed resource.Quantity `json:"borrowed,omitempty"`
 }
 
+type FairSharingStatus struct {
+	// WeightShare represent the maximum of the ratios of usage above nominal
+	// quota to the lendable resources in the cohort, among all the resources
+	// provided by the ClusterQueue.
```

  And divided by the weight

- **apis/kueue/v1beta1/clusterqueue_types.go:356** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T20:01:07Z
```diff
@@ -346,6 +350,14 @@ type ResourceUsage struct {
 	Borrowed resource.Quantity `json:"borrowed,omitempty"`
 }
 
+type FairSharingStatus struct {
+	// WeightShare represent the maximum of the ratios of usage above nominal
+	// quota to the lendable resources in the cohort, among all the resources
+	// provided by the ClusterQueue.
```

  Also, for a weight of zero, this will return MAXINT64 (put the exact number in the comment).

- **apis/kueue/v1beta1/clusterqueue_types.go:358** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T20:01:20Z
```diff
@@ -346,6 +350,14 @@ type ResourceUsage struct {
 	Borrowed resource.Quantity `json:"borrowed,omitempty"`
 }
 
+type FairSharingStatus struct {
+	// WeightShare represent the maximum of the ratios of usage above nominal
+	// quota to the lendable resources in the cohort, among all the resources
+	// provided by the ClusterQueue.
+	// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
+	WeightShare int `json:"weightedShare,omitempty"`
```

  ```suggestion
  	WeightShare int64 `json:"weightedShare,omitempty"`
  ```

- **pkg/cache/cache.go:583** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T20:02:38Z
```diff
@@ -579,11 +580,14 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 		return nil, errCqNotFound
 	}
 
+	weightedShare, _ := cq.DominantResourceShare()
```

  if the feature is disabled, we shouldn't spend time calculating this.

- **pkg/cache/clusterqueue.go:728** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-24T20:03:27Z
```diff
@@ -706,16 +715,17 @@ func (c *ClusterQueue) dominantResourceShare(wlReq FlavorResourceQuantities, m i
 
 	var drs int64 = -1
 	var dRes corev1.ResourceName
+	lendable := c.Cohort.Lendable()
 	for rName, b := range borrowing {
-		if lendable := c.Cohort.Lendable[rName]; lendable > 0 {
-			ratio := b * 1000 / lendable
+		if lr := lendable[rName]; lr > 0 {
```

  why did the implementation change?
  If there was a bug, please open a separate PR for it with accompanying unit tests.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T20:47:00Z

- **pkg/cache/clusterqueue.go:728** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T20:47:00Z
```diff
@@ -706,16 +715,17 @@ func (c *ClusterQueue) dominantResourceShare(wlReq FlavorResourceQuantities, m i
 
 	var drs int64 = -1
 	var dRes corev1.ResourceName
+	lendable := c.Cohort.Lendable()
 	for rName, b := range borrowing {
-		if lendable := c.Cohort.Lendable[rName]; lendable > 0 {
-			ratio := b * 1000 / lendable
+		if lr := lendable[rName]; lr > 0 {
```

  The `c.Cohort.Lendable` calculated by `Snapshot()`. To do not execute this method on each time when we need to execute `dominantResourceShare` function this logic moved to another helper that calculating cohort lendable.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T21:40:50Z

- **apis/kueue/v1beta1/clusterqueue_types.go:306** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T21:40:50Z
```diff
@@ -300,6 +300,10 @@ type ClusterQueueStatus struct {
 	// status of the pending workloads in the cluster queue.
 	// +optional
 	PendingWorkloadsStatus *ClusterQueuePendingWorkloadsStatus `json:"pendingWorkloadsStatus"`
+
+	// FairSharing contains the information about the current status of fair sharing.
+	// +optional
+	FairSharing *FairSharingStatus `json:"fairSharing"`
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T21:40:59Z

- **apis/kueue/v1beta1/clusterqueue_types.go:356** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T21:40:59Z
```diff
@@ -346,6 +350,14 @@ type ResourceUsage struct {
 	Borrowed resource.Quantity `json:"borrowed,omitempty"`
 }
 
+type FairSharingStatus struct {
+	// WeightShare represent the maximum of the ratios of usage above nominal
+	// quota to the lendable resources in the cohort, among all the resources
+	// provided by the ClusterQueue.
```

  Added

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T21:41:27Z

- **apis/kueue/v1beta1/clusterqueue_types.go:358** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T21:41:26Z
```diff
@@ -346,6 +350,14 @@ type ResourceUsage struct {
 	Borrowed resource.Quantity `json:"borrowed,omitempty"`
 }
 
+type FairSharingStatus struct {
+	// WeightShare represent the maximum of the ratios of usage above nominal
+	// quota to the lendable resources in the cohort, among all the resources
+	// provided by the ClusterQueue.
+	// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
+	WeightShare int `json:"weightedShare,omitempty"`
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-24T22:00:29Z

- **pkg/cache/cache.go:583** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-24T22:00:29Z
```diff
@@ -579,11 +580,14 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) (*ClusterQueueUsageStats, error
 		return nil, errCqNotFound
 	}
 
+	weightedShare, _ := cq.DominantResourceShare()
```

  Thanks.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-05-27T11:10:34Z

- **pkg/cache/cache.go:78** by [@trasc](https://github.com/trasc) — 2024-05-27T10:58:38Z
```diff
@@ -73,6 +75,14 @@ func WithExcludedResourcePrefixes(excludedPrefixes []string) Option {
 	}
 }
 
+func WithFairSharing(fs *config.FairSharing) Option {
```

  since it looks like we only use the `c.fairSharing.Enable` it might be better to for this option to be called just with a `bool`

- **pkg/cache/clusterqueue.go:99** by [@trasc](https://github.com/trasc) — 2024-05-27T11:07:18Z
```diff
@@ -96,7 +96,6 @@ type Cohort struct {
 	// RequestableResources equals to the sum of LendingLimit when feature LendingLimit enabled.
 	RequestableResources FlavorResourceQuantities
 	Usage                FlavorResourceQuantities
-	Lendable             map[corev1.ResourceName]int64
```

  An alternative to removing this could be to keep it up to date in the cache not only the snapshot. (update it when a cq is added removed or it's lending specs are changing).

- **pkg/cache/clusterqueue.go:143** by [@trasc](https://github.com/trasc) — 2024-05-27T11:09:35Z
```diff
@@ -141,6 +140,16 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+func (c *Cohort) Lendable() map[corev1.ResourceName]int64 {
```

  Is this tested?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T11:26:07Z

- **pkg/cache/clusterqueue.go:99** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T11:26:07Z
```diff
@@ -96,7 +96,6 @@ type Cohort struct {
 	// RequestableResources equals to the sum of LendingLimit when feature LendingLimit enabled.
 	RequestableResources FlavorResourceQuantities
 	Usage                FlavorResourceQuantities
-	Lendable             map[corev1.ResourceName]int64
```

  Yes, I thought about it. But I'm not sure that it will more optimised. First of all it's a lot of places where need to support this code so it's potential risk for bugs. And also it's just using only if fire sharing enabled.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T12:26:03Z

- **pkg/cache/clusterqueue.go:143** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T12:26:03Z
```diff
@@ -141,6 +140,16 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+func (c *Cohort) Lendable() map[corev1.ResourceName]int64 {
```

  Added test.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-27T14:26:51Z

- **pkg/cache/clusterqueue.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T14:26:51Z
```diff
@@ -141,6 +140,16 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+func (c *Cohort) Lendable() map[corev1.ResourceName]int64 {
+	lendable := make(map[corev1.ResourceName]int64)
+	for _, member := range c.Members.UnsortedList() {
```

  ```suggestion
  	for member := range c.Members {
  ```
  
  This will avoid unnecessary allocations

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T14:30:32Z

- **pkg/cache/clusterqueue.go:145** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T14:30:32Z
```diff
@@ -141,6 +140,16 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+func (c *Cohort) Lendable() map[corev1.ResourceName]int64 {
+	lendable := make(map[corev1.ResourceName]int64)
+	for _, member := range c.Members.UnsortedList() {
```

  Thanks

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-27T14:31:44Z

- **pkg/cache/clusterqueue.go:99** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T14:31:44Z
```diff
@@ -96,7 +96,6 @@ type Cohort struct {
 	// RequestableResources equals to the sum of LendingLimit when feature LendingLimit enabled.
 	RequestableResources FlavorResourceQuantities
 	Usage                FlavorResourceQuantities
-	Lendable             map[corev1.ResourceName]int64
```

  I agree that it might be better to maintain the map in the cache, but also doing this late in the release cycle could introduce bugs.
  
  But, we can still keep the map.
  
  There are a lot of calls to `.DominantResourceShare` during a preemption calculation, where the value of the map doesn't change.
  
  in `dominantResourceShare`, you can do:
  
  ```golang
  lendable := c.lendable // c.lendable is the map
  if lendable == nil {
    lendable = c.Lendable()
  }
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-27T14:36:34Z

- **pkg/cache/clusterqueue_test.go:1175** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T14:34:00Z
```diff
@@ -1155,3 +1170,42 @@ func TestDominantResourceShare(t *testing.T) {
 		})
 	}
 }
+
+func TestCohortLendable(t *testing.T) {
+	cases := map[string]struct {
```

  either add more test cases or don't use a test table.

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:161** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T14:35:34Z
```diff
@@ -139,6 +149,18 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectPendingWorkloadsMetric(cqA, 0, 0)
 			util.ExpectReservingActiveWorkloadsMetric(cqB, 4)
 			util.ExpectPendingWorkloadsMetric(cqB, 0, 1)
+			util.ExpectClusterQueueWeightedShareMetric(cqA, 125)
+			util.ExpectClusterQueueWeightedShareMetric(cqB, 375)
+			util.ExpectClusterQueueWeightedShareMetric(cqShared, 0)
+
+			ginkgo.By("Checking that weight share status changed")
+			cqAKey := client.ObjectKeyFromObject(cqA)
+			createdCqA := &kueue.ClusterQueue{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, cqAKey, createdCqA)).Should(gomega.Succeed())
+				g.Expect(createdCqA.Status.FairSharing).ShouldNot(gomega.BeNil())
```

  ```suggestion
  				g.Expect(createdCqA.Status.FairSharing).ShouldBeComparableTo(&kueue.FairSharingStatus{WeightedShare: 125})
  ```

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:272** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T14:35:44Z
```diff
@@ -221,15 +246,33 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			ginkgo.By("Finishing eviction of 4 running workloads in cqA: shared quota is fair-shared")
 			finishEvictionOfWorkloadsInCQ(cqA, 4)
 			util.ExpectReservingActiveWorkloadsMetric(cqB, 4)
+			util.ExpectClusterQueueWeightedShareMetric(cqA, 222)
+			util.ExpectClusterQueueWeightedShareMetric(cqB, 111)
+			util.ExpectClusterQueueWeightedShareMetric(cqC, 0)
 
 			ginkgo.By("cq-c reclaims one unit, preemption happens in cq-a")
 			cWorkload := testing.MakeWorkload("c0", ns.Name).Queue("c").Request(corev1.ResourceCPU, "1").Obj()
 			gomega.Expect(k8sClient.Create(ctx, cWorkload)).To(gomega.Succeed())
 			util.ExpectPendingWorkloadsMetric(cqC, 1, 0)
+			util.ExpectClusterQueueWeightedShareMetric(cqA, 222)
+			util.ExpectClusterQueueWeightedShareMetric(cqB, 111)
+			util.ExpectClusterQueueWeightedShareMetric(cqC, 0)
 
 			ginkgo.By("Finishing eviction of 1 running workloads in the CQ with highest usage: cqA")
 			finishEvictionOfWorkloadsInCQ(cqA, 1)
 			util.ExpectReservingActiveWorkloadsMetric(cqC, 1)
+			util.ExpectClusterQueueWeightedShareMetric(cqA, 222)
+			util.ExpectClusterQueueWeightedShareMetric(cqB, 111)
+			util.ExpectClusterQueueWeightedShareMetric(cqC, 0)
+
+			ginkgo.By("Checking that weight share status changed")
+			cqAKey := client.ObjectKeyFromObject(cqA)
+			createdCqA := &kueue.ClusterQueue{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, cqAKey, createdCqA)).Should(gomega.Succeed())
+				g.Expect(createdCqA.Status.FairSharing).ShouldNot(gomega.BeNil())
```

  same here

- **apis/kueue/v1beta1/clusterqueue_types.go:359** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T14:35:56Z
```diff
@@ -346,6 +350,15 @@ type ResourceUsage struct {
 	Borrowed resource.Quantity `json:"borrowed,omitempty"`
 }
 
+type FairSharingStatus struct {
+	// WeightShare represent the maximum of the ratios of usage above nominal
+	// quota to the lendable resources in the cohort, among all the resources
+	// provided by the ClusterQueue, and divided by the weight.
+	// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
+	// Also for a weight of zero, this will return 9223372036854775807.
+	WeightShare int64 `json:"weightedShare,omitempty"`
```

  ```suggestion
  	WeightedShare int64 `json:"weightedShare,omitempty"`
  ```

- **apis/kueue/v1beta1/clusterqueue_types.go:358** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-27T14:36:02Z
```diff
@@ -346,6 +350,15 @@ type ResourceUsage struct {
 	Borrowed resource.Quantity `json:"borrowed,omitempty"`
 }
 
+type FairSharingStatus struct {
+	// WeightShare represent the maximum of the ratios of usage above nominal
+	// quota to the lendable resources in the cohort, among all the resources
+	// provided by the ClusterQueue, and divided by the weight.
+	// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
+	// Also for a weight of zero, this will return 9223372036854775807.
```

  ```suggestion
  	// If the ClusterQueue has a weight of zero, this will return 9223372036854775807, the maximum possible share value.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T16:07:46Z

- **apis/kueue/v1beta1/clusterqueue_types.go:358** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T16:07:46Z
```diff
@@ -346,6 +350,15 @@ type ResourceUsage struct {
 	Borrowed resource.Quantity `json:"borrowed,omitempty"`
 }
 
+type FairSharingStatus struct {
+	// WeightShare represent the maximum of the ratios of usage above nominal
+	// quota to the lendable resources in the cohort, among all the resources
+	// provided by the ClusterQueue, and divided by the weight.
+	// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
+	// Also for a weight of zero, this will return 9223372036854775807.
```

  Thanks

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T16:08:03Z

- **apis/kueue/v1beta1/clusterqueue_types.go:359** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T16:08:02Z
```diff
@@ -346,6 +350,15 @@ type ResourceUsage struct {
 	Borrowed resource.Quantity `json:"borrowed,omitempty"`
 }
 
+type FairSharingStatus struct {
+	// WeightShare represent the maximum of the ratios of usage above nominal
+	// quota to the lendable resources in the cohort, among all the resources
+	// provided by the ClusterQueue, and divided by the weight.
+	// If zero, it means that the usage of the ClusterQueue is below the nominal quota.
+	// Also for a weight of zero, this will return 9223372036854775807.
+	WeightShare int64 `json:"weightedShare,omitempty"`
```

  Renamed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T16:08:27Z

- **test/integration/scheduler/fairsharing/fair_sharing_test.go:161** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T16:08:27Z
```diff
@@ -139,6 +149,18 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectPendingWorkloadsMetric(cqA, 0, 0)
 			util.ExpectReservingActiveWorkloadsMetric(cqB, 4)
 			util.ExpectPendingWorkloadsMetric(cqB, 0, 1)
+			util.ExpectClusterQueueWeightedShareMetric(cqA, 125)
+			util.ExpectClusterQueueWeightedShareMetric(cqB, 375)
+			util.ExpectClusterQueueWeightedShareMetric(cqShared, 0)
+
+			ginkgo.By("Checking that weight share status changed")
+			cqAKey := client.ObjectKeyFromObject(cqA)
+			createdCqA := &kueue.ClusterQueue{}
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, cqAKey, createdCqA)).Should(gomega.Succeed())
+				g.Expect(createdCqA.Status.FairSharing).ShouldNot(gomega.BeNil())
```

  Good point, thanks.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T16:08:51Z

- **pkg/cache/clusterqueue_test.go:1175** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T16:08:51Z
```diff
@@ -1155,3 +1170,42 @@ func TestDominantResourceShare(t *testing.T) {
 		})
 	}
 }
+
+func TestCohortLendable(t *testing.T) {
+	cases := map[string]struct {
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T16:09:41Z

- **pkg/cache/clusterqueue.go:99** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T16:09:41Z
```diff
@@ -96,7 +96,6 @@ type Cohort struct {
 	// RequestableResources equals to the sum of LendingLimit when feature LendingLimit enabled.
 	RequestableResources FlavorResourceQuantities
 	Usage                FlavorResourceQuantities
-	Lendable             map[corev1.ResourceName]int64
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2024-05-27T16:48:33Z

- **pkg/cache/cache.go:78** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2024-05-27T16:48:33Z
```diff
@@ -73,6 +75,14 @@ func WithExcludedResourcePrefixes(excludedPrefixes []string) Option {
 	}
 }
 
+func WithFairSharing(fs *config.FairSharing) Option {
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-27T17:08:45Z

/lgtm
/approve
