# PR #2288: [release-0.6] Improve deduce acuracy for the deactivated reason

**Summary**: [release-0.6] Improve deduce acuracy for the deactivated reason

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2288

**Last updated**: 2025-08-15T23:12:40Z

---

## Metadata

- **State**: closed
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `improve-deduce-accuracy-deactivated-reason` → `release-0.6`
- **Created**: 2024-05-28T04:19:47Z
- **Updated**: 2025-08-15T23:12:40Z
- **Merged**: —
- **Closed**: 2024-06-11T13:39:46Z
- **Labels**: `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `do-not-merge/hold`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@trasc](https://github.com/trasc)
- **Changed files**: 3   **+57 / -10**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:
In the current implementation, the workload can be marked deactivated due to exceeding the backoffLimitCount even though the workload is manually deactivated. We can imagine the situation in which users manually deactivate (`.spec.active`=false) the workload during the backoff duration.

I missed this edge case in https://github.com/kubernetes-sigs/kueue/pull/2220.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-28T04:20:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9fa5f1dc9510f94faa95ec7052494fd1222fa492 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66557d4c1cc7d900085164b1 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-28T06:28:46Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2288#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/release-0.6/pkg/controller/OWNERS)~~ [tenzen-y]
- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/release-0.6/test/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-28T06:52:37Z

@tenzen-y: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-unit-release-0-6 | 9fa5f1dc9510f94faa95ec7052494fd1222fa492 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/2288/pull-kueue-test-unit-release-0-6/1795345474299891712) | true | `/test pull-kueue-test-unit-release-0-6`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=2288). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Atenzen-y). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@mimowo](https://github.com/mimowo) — 2024-05-28T08:44:20Z

```
In the current implementation, the workload can be marked deactivated due to exceeding the backoffLimitCount 
even though the workload is manually deactivated. We can imagine the situation in which users manually 
deactivate (.spec.active=false) the workload during the backoff duration.
```
Wouldn't the same happen on the main branch? I this this is an intrinsic issue with the current mechanism that we reverse-engineer the reason. I prefer to have a proper fix on the main branch first than patch the corner case on 0.6.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-28T09:27:50Z

/hold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-11T13:39:42Z

let's close for now.
Because we will never release the new patch version for the 0.6.
/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-11T13:39:48Z

@tenzen-y: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/2288#issuecomment-2160796191):

>let's close for now.
>Because we will never release the new patch version for the 0.6.
>/close
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-28T06:30:07Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:1415** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-28T06:30:07Z
```diff
@@ -1412,7 +1412,7 @@ var _ = ginkgo.Describe("Pod controller interacting with Workload controller whe
 		cfg = fwk.Init()
 		waitForPodsReady := &configapi.WaitForPodsReady{
 			Enable:  true,
-			Timeout: &metav1.Duration{Duration: 10 * time.Millisecond},
+			Timeout: &metav1.Duration{Duration: 1 * time.Second},
```

  I increased the timeout since I faced a similar flakiness as https://github.com/kubernetes-sigs/kueue/pull/2161#discussion_r1594199967
  
  https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/2288/pull-kueue-test-integration-release-0-6/1795313969330655232

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T07:11:28Z

- **test/integration/controller/jobs/pod/pod_controller_test.go:1415** by [@mimowo](https://github.com/mimowo) — 2024-05-28T07:11:28Z
```diff
@@ -1412,7 +1412,7 @@ var _ = ginkgo.Describe("Pod controller interacting with Workload controller whe
 		cfg = fwk.Init()
 		waitForPodsReady := &configapi.WaitForPodsReady{
 			Enable:  true,
-			Timeout: &metav1.Duration{Duration: 10 * time.Millisecond},
+			Timeout: &metav1.Duration{Duration: 1 * time.Second},
```

  I prefer not to mix unrelated fixes here. The flow should be to fix this on the main branch first, then cherry-pick the fix.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T08:40:02Z

- **pkg/controller/core/workload_controller.go:176** by [@mimowo](https://github.com/mimowo) — 2024-05-28T08:40:02Z
```diff
@@ -169,10 +169,12 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
 			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
 			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
+			//  3. The workload already has been re-queued previously, which means the requeueAt has already passed.
+			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
 			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
 				((!workload.HasRequeueState(&wl) && ptr.Equal(r.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(workload.HasRequeueState(&wl) && ptr.Equal(wl.Status.RequeueState.Count, r.requeuingBackoffLimitCount))) {
+					(workload.HasRequeueState(&wl) && wl.Status.RequeueState.RequeueAt != nil && wl.Status.RequeueState.RequeueAt.Sub(realClock.Now().Truncate(time.Second)) <= 0 &&
```

  What is the reason to check the `requeueAt`? The field should be set to nil rather than compared. It is set to nil on the main branch.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-28T08:46:31Z

- **pkg/controller/core/workload_controller.go:176** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-28T08:46:31Z
```diff
@@ -169,10 +169,12 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
 			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
 			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
+			//  3. The workload already has been re-queued previously, which means the requeueAt has already passed.
+			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
 			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
 				((!workload.HasRequeueState(&wl) && ptr.Equal(r.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(workload.HasRequeueState(&wl) && ptr.Equal(wl.Status.RequeueState.Count, r.requeuingBackoffLimitCount))) {
+					(workload.HasRequeueState(&wl) && wl.Status.RequeueState.RequeueAt != nil && wl.Status.RequeueState.RequeueAt.Sub(realClock.Now().Truncate(time.Second)) <= 0 &&
```

  > The field should be set to nil rather than compared. 
  
  No, in the release 0.6, the `requeueAt` wouldn't reset to nil. That is functionality only for the main branch.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T08:52:39Z

- **pkg/controller/core/workload_controller.go:176** by [@mimowo](https://github.com/mimowo) — 2024-05-28T08:52:39Z
```diff
@@ -169,10 +169,12 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
 			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
 			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
+			//  3. The workload already has been re-queued previously, which means the requeueAt has already passed.
+			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
 			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
 				((!workload.HasRequeueState(&wl) && ptr.Equal(r.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(workload.HasRequeueState(&wl) && ptr.Equal(wl.Status.RequeueState.Count, r.requeuingBackoffLimitCount))) {
+					(workload.HasRequeueState(&wl) && wl.Status.RequeueState.RequeueAt != nil && wl.Status.RequeueState.RequeueAt.Sub(realClock.Now().Truncate(time.Second)) <= 0 &&
```

  I would argue that cleaning `requeueAt` is not a functionality on main, but a bugfix.
  
  I think keeping the `requeueAt` when backoffLimitCount is exceeded might be misleading to the user. I would rather suggest to investigate clearing `requeueAt` in 0.6 as we do on the `main` branch, when the backoffLimitCount is exceeded.
  
  EDIT: (unless I'm missing something, but it should be investigated more if we should / shouldn't set requeueAt on 0.6 when the backoffLimitCount is exceeded).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-28T09:27:18Z

- **pkg/controller/core/workload_controller.go:176** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-28T09:27:18Z
```diff
@@ -169,10 +169,12 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
 			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
 			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
+			//  3. The workload already has been re-queued previously, which means the requeueAt has already passed.
+			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
 			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
 				((!workload.HasRequeueState(&wl) && ptr.Equal(r.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(workload.HasRequeueState(&wl) && ptr.Equal(wl.Status.RequeueState.Count, r.requeuingBackoffLimitCount))) {
+					(workload.HasRequeueState(&wl) && wl.Status.RequeueState.RequeueAt != nil && wl.Status.RequeueState.RequeueAt.Sub(realClock.Now().Truncate(time.Second)) <= 0 &&
```

  I synced with @mimowo offline. In conclusion, we should cherry-pick https://github.com/kubernetes-sigs/kueue/pull/2143 in separate PR so that we can bring the functionality to reset the `requeueAt` in v0.6 as well.
  
  By doing so, we can avoid the current hacky mechanism.
  But, we can not just cherry-pick #2143 to v0.6 since #2143 depends on the `Requeued` condition, which is the condition type only in v0.6.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T10:02:33Z

- **pkg/controller/core/workload_controller.go:176** by [@mimowo](https://github.com/mimowo) — 2024-05-28T10:02:33Z
```diff
@@ -169,10 +169,12 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
 			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
 			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
+			//  3. The workload already has been re-queued previously, which means the requeueAt has already passed.
+			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
 			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
 				((!workload.HasRequeueState(&wl) && ptr.Equal(r.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(workload.HasRequeueState(&wl) && ptr.Equal(wl.Status.RequeueState.Count, r.requeuingBackoffLimitCount))) {
+					(workload.HasRequeueState(&wl) && wl.Status.RequeueState.RequeueAt != nil && wl.Status.RequeueState.RequeueAt.Sub(realClock.Now().Truncate(time.Second)) <= 0 &&
```

  I'm wondering if we actually need to wait for `requeueAt` being cleaned on the main branch [here](https://github.com/kubernetes-sigs/kueue/blob/1ecfdc1964b548b558506a2ee27c632525d3a982/pkg/controller/core/workload_controller.go#L194C40-L194C82). 
  
  I think there is no point to wait until `Requeued: true` when setting `active: false` on the main branch, because the fate of the workload is known as soon as we hit the PodsReady timeout.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-05-28T11:18:54Z

- **pkg/controller/core/workload_controller.go:176** by [@mimowo](https://github.com/mimowo) — 2024-05-28T11:18:53Z
```diff
@@ -169,10 +169,12 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			// The deactivation reason could be deduced as the maximum number of re-queuing retries if the workload met all criteria below:
 			//  1. The waitForPodsReady feature is enabled, which means that it has "PodsReady" condition.
 			//  2. The workload has already exceeded the PodsReadyTimeout.
-			//  3. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
+			//  3. The workload already has been re-queued previously, which means the requeueAt has already passed.
+			//  4. The number of re-queued has already reached the waitForPodsReady.requeuingBackoffLimitCount.
 			if apimeta.IsStatusConditionFalse(wl.Status.Conditions, kueue.WorkloadPodsReady) &&
 				((!workload.HasRequeueState(&wl) && ptr.Equal(r.requeuingBackoffLimitCount, ptr.To[int32](0))) ||
-					(workload.HasRequeueState(&wl) && ptr.Equal(wl.Status.RequeueState.Count, r.requeuingBackoffLimitCount))) {
+					(workload.HasRequeueState(&wl) && wl.Status.RequeueState.RequeueAt != nil && wl.Status.RequeueState.RequeueAt.Sub(realClock.Now().Truncate(time.Second)) <= 0 &&
```

  I opened the issue https://github.com/kubernetes-sigs/kueue/issues/2291 to eliminate the unnecessary delay. The proposed solution of incrementing `requeuingState.count` to actually exceed the `backoffLimitCount` will work both on 0.7 and 0.6.
