# PR #230: feat: clusterQueue add status field

**Summary**: feat: clusterQueue add status field

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/230

**Last updated**: 2022-05-07T09:27:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `feature/add-new-status-to-clusterqueue` → `main`
- **Created**: 2022-04-24T06:30:36Z
- **Updated**: 2022-05-07T09:27:02Z
- **Merged**: 2022-05-07T09:23:17Z
- **Closed**: 2022-05-07T09:23:17Z
- **Labels**: `kind/feature`, `lgtm`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 14   **+587 / -86**

## Description

Signed-off-by: kerthcet <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of https://github.com/kubernetes-sigs/kueue/issues/134

#### Special notes for your reviewer:

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-04-24T06:30:44Z

Hi @kerthcet. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@denkensk](https://github.com/denkensk) — 2022-04-24T09:26:29Z

/ok-to-test

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-04-25T20:25:33Z

/assign

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-04-29T06:14:24Z

I changed the implement as now we will insert CQs to cohort in any case and we will not remove them unless the delete action. But we will still maintain the `status` both in qManager and cache, so that when popping elements in scheduling, we will filter out not ready CQs, which is more efficiency. Also in snapshotting, we will exclude not ready CQs also. @ahg-g

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-01T09:54:37Z

Sorry for the force-pushed due to some conflicts. I changed the implement:
1. Only cache will manage the clusterQueue status
2. We'll filter inactive clusterQueues in Snapshot.
3. When resourceFlavor created, scheduler will be waked.

@ahg-g PTAL if you have time.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-07T01:42:49Z

updated @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-07T08:25:43Z

Thanks, please squash

### Comment by [@kerthcet](https://github.com/kerthcet) — 2022-05-07T09:17:57Z

squashed, thanks @ahg-g a lot.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-05-07T09:21:00Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-05-07T09:21:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/230#issuecomment-1120173382" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/230#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-28T02:20:08Z

- **pkg/cache/cache.go:88** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T02:25:00Z
```diff
@@ -80,6 +80,15 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+type ClusterQueueStatus int
+
+const (
+	pending ClusterQueueStatus = iota
+	ready
+	// TODO: Add terminating state.
```

  pls remove such todo's unless there is an issue we can reference and clearly describe the todo.

- **pkg/cache/cache.go:87** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T02:25:37Z
```diff
@@ -80,6 +80,15 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+type ClusterQueueStatus int
+
+const (
+	pending ClusterQueueStatus = iota
+	ready
```

  document when a CQ enters each status.

- **pkg/cache/cache.go:167** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T02:27:13Z
```diff
@@ -136,13 +150,21 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[string
 		usedResources[r.Name] = usedFlavors
 	}
 	c.UsedResources = usedResources
-	c.UpdateLabelKeys(resourceFlavors)
+	c.UpdateByFlavors(resourceFlavors)
 	return nil
 }
 
-// UpdateLabelKeys updates a ClusterQueue's LabelKeys based on the passed ResourceFlavors set.
+// UpdateByFlavors updates a ClusterQueue based on the passed ResourceFlavors set.
 // Exported only for testing.
-func (c *ClusterQueue) UpdateLabelKeys(flavors map[string]*kueue.ResourceFlavor) {
+func (c *ClusterQueue) UpdateByFlavors(flavors map[string]*kueue.ResourceFlavor) {
+	if flavorNotFound := c.updateLabelKeys(flavors); flavorNotFound {
+		c.Status = pending
+		return
+	}
+	c.Status = ready
+}
+
+func (c *ClusterQueue) updateLabelKeys(flavors map[string]*kueue.ResourceFlavor) (flavorNotFound bool) {
```

  please avoid named return variables, here and in other places in this PR, we are not using them in this repo and I think we should be consistent with this pattern.

- **pkg/cache/cache.go:232** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T02:37:47Z
```diff
@@ -205,16 +229,25 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
-func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) {
+func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) (cqNames sets.String) {
```

  ditto on named return variables

- **pkg/cache/cache.go:159** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T02:42:58Z
```diff
@@ -136,13 +150,21 @@ func (c *ClusterQueue) update(in *kueue.ClusterQueue, resourceFlavors map[string
 		usedResources[r.Name] = usedFlavors
 	}
 	c.UsedResources = usedResources
-	c.UpdateLabelKeys(resourceFlavors)
+	c.UpdateByFlavors(resourceFlavors)
 	return nil
 }
 
-// UpdateLabelKeys updates a ClusterQueue's LabelKeys based on the passed ResourceFlavors set.
+// UpdateByFlavors updates a ClusterQueue based on the passed ResourceFlavors set.
 // Exported only for testing.
-func (c *ClusterQueue) UpdateLabelKeys(flavors map[string]*kueue.ResourceFlavor) {
+func (c *ClusterQueue) UpdateByFlavors(flavors map[string]*kueue.ResourceFlavor) {
```

  ```suggestion
  func (c *ClusterQueue) UpdateWithFlavors(flavors map[string]*kueue.ResourceFlavor) {
  ```

- **pkg/cache/cache.go:261** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T02:44:55Z
```diff
@@ -223,6 +256,16 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) {
 	c.Unlock()
 }
 
+func (c *Cache) ClusterQueue(name string) (*ClusterQueue, error) {
+	c.RLock()
+	defer c.RLocker().Unlock()
```

  RUnlock()

- **pkg/cache/cache.go:249** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T02:46:22Z
```diff
@@ -205,16 +229,25 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
-func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) {
+func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) (cqNames sets.String) {
 	c.Lock()
 	c.resourceFlavors[rf.Name] = rf
 	for _, cq := range c.clusterQueues {
+		prevStatus := cq.Status
 		// We call update on all ClusterQueues irrespective of which CQ actually use this flavor
 		// because it is not expensive to do so, and is not worth tracking which ClusterQueues use
 		// which flavors.
-		cq.UpdateLabelKeys(c.resourceFlavors)
+		cq.UpdateByFlavors(c.resourceFlavors)
+		curStatus := cq.Status
+		if prevStatus == pending && curStatus == ready {
+			if cqNames == nil {
+				cqNames = sets.NewString(cq.Name)
+			}
+			cqNames.Insert(cq.Name)
+		}
 	}
 	c.Unlock()
```

  now that the function is longer, I suggest we use `defer c.Unlock()` instead

- **pkg/cache/cache.go:232** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T02:46:54Z
```diff
@@ -205,16 +229,25 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
-func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) {
+func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) (cqNames sets.String) {
```

  I think this should just return a bool indicating if at least one CQ changed its status to from pending to ready.

- **pkg/cache/cache.go:311** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T02:53:47Z
```diff
@@ -264,12 +307,19 @@ func (c *Cache) UpdateClusterQueue(cq *kueue.ClusterQueue) error {
 	if err := cqImpl.update(cq, c.resourceFlavors); err != nil {
 		return err
 	}
-	if cqImpl.Cohort != nil {
-		if cqImpl.Cohort.Name != cq.Spec.Cohort {
-			c.deleteClusterQueueFromCohort(cqImpl)
-			c.addClusterQueueToCohort(cqImpl, cq.Spec.Cohort)
-		}
-	} else {
+
+	if !cqImpl.Ready() {
```

  I don't think we need to exclude the CQ from the cohort, all we need to do is exclude it from the snapshot if it was not ready here: https://github.com/kubernetes-sigs/kueue/blob/af47921646d3fcc2c7db0189e7b8bd1017e717b6/pkg/cache/snapshot.go#L47
  
  This should simplify things a lot.

- **pkg/cache/cache.go:507** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T03:03:32Z
```diff
@@ -451,8 +502,36 @@ func (c *Cache) addClusterQueueToCohort(cq *ClusterQueue, cohortName string) {
 		cohort = newCohort(cohortName, 1)
 		c.cohorts[cohortName] = cohort
 	}
-	cohort.members[cq] = struct{}{}
 	cq.Cohort = cohort
+
+	if cq.Ready() {
```

  this makes `AddClusterQueue` inconsistent with `UpdateClusterQueue`; when cq is not ready, on update, the cq.Cohort will be reset to nil upon calling deleteClusterQueueFromCohort; on update cq.Cohort will be set even if it not ready.
  
  I suggest we change the function name to `addClusterQueueToCohortIfReady` and at the beginning of the function we return directly if the passed cq is not ready.

- **pkg/cache/cache.go:524** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T03:23:45Z
```diff
@@ -451,8 +502,36 @@ func (c *Cache) addClusterQueueToCohort(cq *ClusterQueue, cohortName string) {
 		cohort = newCohort(cohortName, 1)
 		c.cohorts[cohortName] = cohort
 	}
-	cohort.members[cq] = struct{}{}
 	cq.Cohort = cohort
+
+	if cq.Ready() {
+		cohort.members[cq] = struct{}{}
+	}
+}
+
+// UpdateCohortMembers should only be called when resourceFlavor created/updated.
+func (c *Cache) UpdateCohortMembers() {
+	c.Lock()
+	defer c.Unlock()
+	for _, cq := range c.clusterQueues {
+		if cq.Cohort == nil {
+			continue
+		}
+		c.addClusterQueueToCohort(cq, cq.Cohort.Name)
+	}
+}
+
+func (c *Cache) DeleteCohorts(cqNames sets.String) {
```

  why do we need this and the function above? shouldn't this happens inside `AddOrUpdateResourceFlavor`/`DeleteResourceFlavor `?

- **pkg/cache/cache.go:253** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T03:25:26Z
```diff
@@ -205,16 +229,25 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
-func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) {
+func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) (cqNames sets.String) {
 	c.Lock()
 	c.resourceFlavors[rf.Name] = rf
 	for _, cq := range c.clusterQueues {
+		prevStatus := cq.Status
 		// We call update on all ClusterQueues irrespective of which CQ actually use this flavor
 		// because it is not expensive to do so, and is not worth tracking which ClusterQueues use
 		// which flavors.
-		cq.UpdateLabelKeys(c.resourceFlavors)
+		cq.UpdateByFlavors(c.resourceFlavors)
+		curStatus := cq.Status
+		if prevStatus == pending && curStatus == ready {
+			if cqNames == nil {
+				cqNames = sets.NewString(cq.Name)
+			}
+			cqNames.Insert(cq.Name)
+		}
 	}
 	c.Unlock()
+	return
 }
 
 func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) {
```

  we should iterate over the clusterQueues in this function and do something similar to what we are doing in `AddOrUpdateResourceFlavor` to update the labels and status.
  
  Also, similar to `AddOrUpdateResourceFlavor`, this should just return a bool indicating if at least one CQ changed its status to from pending to ready.

- **pkg/controller/core/clusterqueue_controller.go:94** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T20:56:51Z
```diff
@@ -90,9 +91,15 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 }
 
 func (r *ClusterQueueReconciler) NotifyWorkloadUpdate(w *kueue.Workload) {
+	r.log.V(2).Info("received workload event")
```

  please revert, this will spam the logs

- **pkg/controller/core/clusterqueue_controller.go:99** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T20:57:02Z
```diff
@@ -90,9 +91,15 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 }
 
 func (r *ClusterQueueReconciler) NotifyWorkloadUpdate(w *kueue.Workload) {
+	r.log.V(2).Info("received workload event")
 	r.wlUpdateCh <- event.GenericEvent{Object: w}
 }
 
+func (r *ClusterQueueReconciler) NotifyResourceFlavorEvent(rf *kueue.ResourceFlavor) {
+	r.log.V(2).Info("received resourceFlavor event")
```

  ditto, pls remove

- **pkg/controller/core/clusterqueue_controller.go:119** by [@ahg-g](https://github.com/ahg-g) — 2022-04-27T21:04:03Z
```diff
@@ -108,7 +115,12 @@ func (r *ClusterQueueReconciler) Create(e event.CreateEvent) bool {
 	if err := r.cache.AddClusterQueue(ctx, cq); err != nil {
 		log.Error(err, "Failed to add clusterQueue to cache")
 	}
-	if err := r.qManager.AddClusterQueue(ctx, cq); err != nil {
+
+	cqImpl, err := r.cache.ClusterQueue(cq.Name)
```

  Do we need to lookup the whole CQ impl? I would prefer a narrower api, something like r.cache.ClusterQueueReady() that returns a boolean if the CQ is ready, false if it doesn't exist.

- **pkg/controller/core/clusterqueue_controller.go:229** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T00:52:53Z
```diff
@@ -191,14 +208,41 @@ func (h *cqWorkloadHandler) requestForWorkloadClusterQueue(w *kueue.Workload) *r
 	}
 }
 
+// resourceFlavorHandler enqueues reconcile.Requests in response to ResourceFlavor events.
+// Since the events come from a channel Source, only the Generic handler will
+// receive events.
+type resourceFlavorHandler struct {
+	qManager *queue.Manager
+	cache    *cache.Cache
+}
+
+func (h *resourceFlavorHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+}
+
+func (h *resourceFlavorHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *resourceFlavorHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *resourceFlavorHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
+	h.qManager.Broadcast()
```

  what we need to do here is iterate over all CQs that point to this resourceFlavor, create a request similar to what we do in `requestForWorkloadClusterQueue` and then push to the workqueue using `q.AddAfter(*req, constants.UpdatesBatchPeriod)`; and in reconcile we invoke the update functions.

- **pkg/queue/manager.go:131** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:31:33Z
```diff
@@ -106,35 +105,53 @@ func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) e
 
 	queued := m.queueAllInadmissibleWorkloadsInCohort(cqImpl)
 	if queued || addedWorkloads {
-		m.cond.Broadcast()
+		m.Broadcast()
 	}
 	return nil
 }
 
-func (m *Manager) UpdateClusterQueue(cq *kueue.ClusterQueue) error {
+func (m *Manager) UpdateClusterQueue(cq *kueue.ClusterQueue, cqReady bool) error {
 	m.Lock()
 	defer m.Unlock()
 	cqImpl, ok := m.clusterQueues[cq.Name]
 	if !ok {
 		return errClusterQueueDoesNotExist
 	}
-	oldCohort := cqImpl.Cohort()
 
+	oldCohort := cqImpl.Cohort()
 	// TODO(#8): recreate heap based on a change of queueing policy.
-	cqImpl.Update(cq)
+	cqImpl.Update(cq, cqReady)
 	newCohort := cqImpl.Cohort()
-	if oldCohort != newCohort {
-		m.updateCohort(oldCohort, newCohort, cq.Name)
+
+	if cqReady {
+		if oldCohort != newCohort {
+			m.updateCohort(oldCohort, newCohort, cq.Name)
+		}
+	} else {
+		m.deleteCohort(oldCohort, cq.Name)
```

  same here, why are we removing it from the cohort here? I don't think we should.

- **pkg/queue/cluster_queue_impl.go:36** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:33:22Z
```diff
@@ -33,19 +33,22 @@ type ClusterQueueImpl struct {
 
 	heap   heap.Heap
 	cohort string
+	ready  bool
```

  I would call it `active`

- **pkg/queue/cluster_queue_impl.go:137** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:34:56Z
```diff
@@ -126,3 +129,11 @@ func (c *ClusterQueueImpl) Info(key string) *workload.Info {
 	}
 	return info.(*workload.Info)
 }
+
+func (c *ClusterQueueImpl) Ready() bool {
+	return c.ready
+}
+
+func (c *ClusterQueueImpl) SetStatus(ready bool) {
```

  if we rename to active, then:
  
  ```suggestion
  func (c *ClusterQueueImpl) SetActive(active bool) {
  ```

- **pkg/queue/cluster_queue_impl.go:42** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:35:53Z
```diff
@@ -33,19 +33,22 @@ type ClusterQueueImpl struct {
 
 	heap   heap.Heap
 	cohort string
+	ready  bool
 }
 
 func newClusterQueueImpl(keyFunc func(obj interface{}) string, lessFunc func(a, b interface{}) bool) *ClusterQueueImpl {
 	return &ClusterQueueImpl{
-		heap: heap.New(keyFunc, lessFunc),
+		heap:  heap.New(keyFunc, lessFunc),
+		ready: true,
```

  why true by default? likely it doesn't need to since we call update right after; or did you need this to simplify the tests?

- **pkg/queue/cluster_queue_interface.go:31** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:37:52Z
```diff
@@ -28,7 +28,7 @@ import (
 // to be scheduled.
 type ClusterQueue interface {
 	// Update updates the properties of this ClusterQueue.
-	Update(*kueue.ClusterQueue)
+	Update(*kueue.ClusterQueue, bool)
```

  update comment to document the active flag

- **pkg/queue/manager.go:83** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:39:23Z
```diff
@@ -65,23 +65,22 @@ func NewManager(client client.Client) *Manager {
 	return m
 }
 
-func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) error {
+func (m *Manager) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue, cqReady bool) error {
 	m.Lock()
 	defer m.Unlock()
 
 	if _, ok := m.clusterQueues[cq.Name]; ok {
 		return errClusterQueueAlreadyExists
 	}
 
-	cqImpl, err := newClusterQueue(cq)
+	cqImpl, err := newClusterQueue(cq, cqReady)
 	if err != nil {
 		return err
 	}
-
 	m.clusterQueues[cq.Name] = cqImpl
 
 	cohort := cq.Spec.Cohort
-	if cohort != "" {
+	if cohort != "" && cqReady {
```

  why do we need to exclude it from the cohort here?

- **pkg/cache/cache.go:253** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:46:57Z
```diff
@@ -205,16 +229,25 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
-func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) {
+func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) (cqNames sets.String) {
 	c.Lock()
 	c.resourceFlavors[rf.Name] = rf
 	for _, cq := range c.clusterQueues {
+		prevStatus := cq.Status
 		// We call update on all ClusterQueues irrespective of which CQ actually use this flavor
 		// because it is not expensive to do so, and is not worth tracking which ClusterQueues use
 		// which flavors.
-		cq.UpdateLabelKeys(c.resourceFlavors)
+		cq.UpdateByFlavors(c.resourceFlavors)
+		curStatus := cq.Status
+		if prevStatus == pending && curStatus == ready {
+			if cqNames == nil {
+				cqNames = sets.NewString(cq.Name)
+			}
+			cqNames.Insert(cq.Name)
+		}
 	}
 	c.Unlock()
+	return
 }
 
 func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) {
```

  if we do this, then we don't need `cache.DeleteCohorts` func

- **pkg/cache/cache.go:259** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:48:34Z
```diff
@@ -223,6 +256,16 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) {
 	c.Unlock()
 }
 
+func (c *Cache) ClusterQueue(name string) (*ClusterQueue, error) {
```

  I think we should just change this function to return the active status, so function name would be `ClusterQueueActive` and just returns a bool; return false if it doesn't exist.

- **pkg/cache/cache.go:513** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:57:28Z
```diff
@@ -451,8 +502,36 @@ func (c *Cache) addClusterQueueToCohort(cq *ClusterQueue, cohortName string) {
 		cohort = newCohort(cohortName, 1)
 		c.cohorts[cohortName] = cohort
 	}
-	cohort.members[cq] = struct{}{}
 	cq.Cohort = cohort
+
+	if cq.Ready() {
+		cohort.members[cq] = struct{}{}
+	}
+}
+
+// UpdateCohortMembers should only be called when resourceFlavor created/updated.
+func (c *Cache) UpdateCohortMembers() {
```

  we will not need this function if we are not going to remove inactive CQs from the cohort.

- **pkg/cache/cache.go:550** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:58:04Z
```diff
@@ -466,6 +545,25 @@ func (c *Cache) deleteClusterQueueFromCohort(cq *ClusterQueue) {
 	cq.Cohort = nil
 }
 
+// ClusterQueueSetsByFlavor returns clusterQueue set which has specified resourceFlavor.
+// TODO: reduce the complexity.
+func (c *Cache) ClusterQueueSetsByFlavor(name string) (cqNames sets.String) {
```

  ditto, we don't need this if we are not going to need to remove the CQ from the cohort.

- **pkg/controller/core/resourceflavor_controller.go:100** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T01:59:19Z
```diff
@@ -64,9 +87,17 @@ func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	if !match {
 		return false
 	}
+	// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly,
+	// and notifyWatchers should be called after deletion.
+	defer r.notifyWatchers(flv)
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
 	r.cache.DeleteResourceFlavor(flv)
+
+	cqNames := r.cache.ClusterQueueSetsByFlavor(flv.Name)
+	r.qManager.DeleteCohorts(cqNames)
+	r.cache.DeleteCohorts(cqNames)
```

  I don't think we need 98-100; we should never remove a CQ from the cohort based on a resourceFlavor event.

- **pkg/controller/core/resourceflavor_controller.go:79** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T02:17:07Z
```diff
@@ -64,9 +87,17 @@ func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	if !match {
 		return false
 	}
+	// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly,
+	// and notifyWatchers should be called after deletion.
+	defer r.notifyWatchers(flv)
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
 	r.cache.DeleteResourceFlavor(flv)
+
```

  if `DeleteResourceFlavor` returns a bool to indicate if at least one CQ changed its status from pending to ready, if true we call `r.qManager.Broadcast()`

- **pkg/controller/core/clusterqueue_controller.go:214** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T02:19:10Z
```diff
@@ -191,14 +208,41 @@ func (h *cqWorkloadHandler) requestForWorkloadClusterQueue(w *kueue.Workload) *r
 	}
 }
 
+// resourceFlavorHandler enqueues reconcile.Requests in response to ResourceFlavor events.
+// Since the events come from a channel Source, only the Generic handler will
+// receive events.
+type resourceFlavorHandler struct {
```

  I don't think we need this handler and this whole watch and notification setup. Updating the CQ status and invoking broadcast can be done in the resource flavor controller.

- **pkg/controller/core/resourceflavor_controller.go:77** by [@ahg-g](https://github.com/ahg-g) — 2022-04-28T02:20:05Z
```diff
@@ -49,13 +59,26 @@ func (r *ResourceFlavorReconciler) Reconcile(ctx context.Context, req ctrl.Reque
 }
 
 func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
+	var toNotify bool
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+	defer func() {
+		if toNotify {
+			r.notifyWatchers(flv)
+		}
+	}()
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue got ready, we should inform clusterQueue controller to broadcast the event.
+	cqNames := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
```

  if AddOrUpdateResourceFlavor returns a bool to indicate if at least one CQ changed its status from pending to ready, if true we call r.qManager.Broadcast(); we don't need lines 78-81 below.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-28T09:08:56Z

- **pkg/cache/cache.go:311** by [@kerthcet](https://github.com/kerthcet) — 2022-04-28T09:08:55Z
```diff
@@ -264,12 +307,19 @@ func (c *Cache) UpdateClusterQueue(cq *kueue.ClusterQueue) error {
 	if err := cqImpl.update(cq, c.resourceFlavors); err != nil {
 		return err
 	}
-	if cqImpl.Cohort != nil {
-		if cqImpl.Cohort.Name != cq.Spec.Cohort {
-			c.deleteClusterQueueFromCohort(cqImpl)
-			c.addClusterQueueToCohort(cqImpl, cq.Spec.Cohort)
-		}
-	} else {
+
+	if !cqImpl.Ready() {
```

  Good point!

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-29T03:00:26Z

- **pkg/controller/core/resourceflavor_controller.go:79** by [@kerthcet](https://github.com/kerthcet) — 2022-04-28T10:32:40Z
```diff
@@ -64,9 +87,17 @@ func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	if !match {
 		return false
 	}
+	// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly,
+	// and notifyWatchers should be called after deletion.
+	defer r.notifyWatchers(flv)
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
 	r.cache.DeleteResourceFlavor(flv)
+
```

  We need to set the status of CQs in `qManager` so that when popping in scheduler, we can skip not ready CQs. So we may need the CQ names which holds the deleted flavor.

- **pkg/controller/core/resourceflavor_controller.go:77** by [@kerthcet](https://github.com/kerthcet) — 2022-04-28T10:40:36Z
```diff
@@ -49,13 +59,26 @@ func (r *ResourceFlavorReconciler) Reconcile(ctx context.Context, req ctrl.Reque
 }
 
 func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
+	var toNotify bool
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+	defer func() {
+		if toNotify {
+			r.notifyWatchers(flv)
+		}
+	}()
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue got ready, we should inform clusterQueue controller to broadcast the event.
+	cqNames := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
```

  cqNames here are used to update CQ's status in qManager, the status is used in scheduling, when CQ is not ready, we'll skip it.

- **pkg/cache/cache.go:507** by [@kerthcet](https://github.com/kerthcet) — 2022-04-29T02:59:37Z
```diff
@@ -451,8 +502,36 @@ func (c *Cache) addClusterQueueToCohort(cq *ClusterQueue, cohortName string) {
 		cohort = newCohort(cohortName, 1)
 		c.cohorts[cohortName] = cohort
 	}
-	cohort.members[cq] = struct{}{}
 	cq.Cohort = cohort
+
+	if cq.Ready() {
```

  I changed the implement, cq will always be inserted into the members, but holds an `active` status.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-29T06:08:06Z

- **pkg/controller/core/clusterqueue_controller.go:229** by [@kerthcet](https://github.com/kerthcet) — 2022-04-29T03:16:55Z
```diff
@@ -191,14 +208,41 @@ func (h *cqWorkloadHandler) requestForWorkloadClusterQueue(w *kueue.Workload) *r
 	}
 }
 
+// resourceFlavorHandler enqueues reconcile.Requests in response to ResourceFlavor events.
+// Since the events come from a channel Source, only the Generic handler will
+// receive events.
+type resourceFlavorHandler struct {
+	qManager *queue.Manager
+	cache    *cache.Cache
+}
+
+func (h *resourceFlavorHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+}
+
+func (h *resourceFlavorHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *resourceFlavorHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *resourceFlavorHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
+	h.qManager.Broadcast()
```

  removed the handler. We'll call broadcast in resourceFlavor Controller directly.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-04-29T18:21:47Z

- **pkg/cache/cache.go:90** by [@ahg-g](https://github.com/ahg-g) — 2022-04-29T13:21:36Z
```diff
@@ -80,6 +80,16 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+type ClusterQueueStatus int
+
+const (
+	// Pending means this ClusterQueue is accepted but not ready,
+	// this can be caused by its request flavors not found.
+	Pending ClusterQueueStatus = iota
+	// Ready means all of the request flavors in ClusterQueue exist in cluster.
+	Ready
```

  we changed to Active, right?

- **pkg/cache/cache.go:87** by [@ahg-g](https://github.com/ahg-g) — 2022-04-29T13:25:11Z
```diff
@@ -80,6 +80,16 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+type ClusterQueueStatus int
+
+const (
+	// Pending means this ClusterQueue is accepted but not ready,
+	// this can be caused by its request flavors not found.
```

  ```suggestion
  	// Pending means the ClusterQueue is accepted but not yet active, 
  	// this can be because of a missing ResourceFlavor referenced by the ClusterQueue.
  	// In this state, the ClusterQueue can't admit new workloads and its quota can't be borrowed 
  	// by other active ClusterQueues in the cohort.
  ```

- **pkg/cache/cache.go:89** by [@ahg-g](https://github.com/ahg-g) — 2022-04-29T13:26:52Z
```diff
@@ -80,6 +80,16 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+type ClusterQueueStatus int
+
+const (
+	// Pending means this ClusterQueue is accepted but not ready,
+	// this can be caused by its request flavors not found.
+	Pending ClusterQueueStatus = iota
+	// Ready means all of the request flavors in ClusterQueue exist in cluster.
```

  ```suggestion
  	// Active means the ClusterQueue can admit new workloads and its quota 
  	// can be borrowed by other ClusterQueues in the cohort.
  ```

- **pkg/controller/core/resourceflavor_controller.go:86** by [@ahg-g](https://github.com/ahg-g) — 2022-04-29T18:20:35Z
```diff
@@ -53,20 +56,37 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue got ready, we should inform clusterQueue controller to broadcast the event.
+	// Ignore the second value for add/update flavor will not cause clusterQueue not ready.
+	cqNames, _ := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+	r.qManager.UpdateClusterQueuesStatus(cqNames, true)
+	if len(cqNames) > 0 {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
 func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
-	r.cache.DeleteResourceFlavor(flv)
+
+	// TODO: When finalizer implemented in resourceFlavor controller, we'll no longer need notReadyNames.
+	readyNames, notReadyNames := r.cache.DeleteResourceFlavor(flv)
+	r.qManager.UpdateClusterQueuesStatus(readyNames, true)
+	r.qManager.UpdateClusterQueuesStatus(notReadyNames, false)
```

  I think there is a potential for a race condition here.
  
  The race could happen between updating the state in the cache and updating it in the manager; the fact that those two updates are not done while holding a common lock is going to cause race conditions  between the resourceFlavor controller and the clusterQueue controller.
  
  I don't think we need to track the active state in the manager at all; in fact nothing should change in the queue manager; instead in the scheduler, [after we get the heads of the queues](https://github.com/kubernetes-sigs/kueue/blob/277a453a215f5935d266011e27a97c597cb2d7e7/pkg/scheduler/scheduler.go#L83), for each workload we check if its CQ exists in the snapshot or not.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-30T05:25:40Z

- **pkg/controller/core/resourceflavor_controller.go:86** by [@kerthcet](https://github.com/kerthcet) — 2022-04-30T05:25:40Z
```diff
@@ -53,20 +56,37 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue got ready, we should inform clusterQueue controller to broadcast the event.
+	// Ignore the second value for add/update flavor will not cause clusterQueue not ready.
+	cqNames, _ := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+	r.qManager.UpdateClusterQueuesStatus(cqNames, true)
+	if len(cqNames) > 0 {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
 func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
-	r.cache.DeleteResourceFlavor(flv)
+
+	// TODO: When finalizer implemented in resourceFlavor controller, we'll no longer need notReadyNames.
+	readyNames, notReadyNames := r.cache.DeleteResourceFlavor(flv)
+	r.qManager.UpdateClusterQueuesStatus(readyNames, true)
+	r.qManager.UpdateClusterQueuesStatus(notReadyNames, false)
```

  > I don't think we need to track the active state in the manager at all; in fact nothing should change in the queue manager; instead in the scheduler, [after we get the heads of the queues](https://github.com/kubernetes-sigs/kueue/blob/277a453a215f5935d266011e27a97c597cb2d7e7/pkg/scheduler/scheduler.go#L83), for each workload we check if its CQ exists in the snapshot or not.
  
  This is my first design, but as you said, the potential data race is always exist, I think correctness here is more important than performance, and the loss is not very big.  I'll change as you advised.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-04-30T05:44:19Z

- **pkg/cache/cache.go:90** by [@kerthcet](https://github.com/kerthcet) — 2022-04-30T05:44:19Z
```diff
@@ -80,6 +80,16 @@ func newCohort(name string, size int) *Cohort {
 	}
 }
 
+type ClusterQueueStatus int
+
+const (
+	// Pending means this ClusterQueue is accepted but not ready,
+	// this can be caused by its request flavors not found.
+	Pending ClusterQueueStatus = iota
+	// Ready means all of the request flavors in ClusterQueue exist in cluster.
+	Ready
```

  yes

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-01T19:24:03Z

- **pkg/cache/cache.go:275** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T16:15:35Z
```diff
@@ -205,22 +234,45 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
-func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) {
-	c.Lock()
-	c.resourceFlavors[rf.Name] = rf
+func (c *Cache) updateClusterQueues() bool {
+	var activate bool
+
 	for _, cq := range c.clusterQueues {
+		prevStatus := cq.Status
 		// We call update on all ClusterQueues irrespective of which CQ actually use this flavor
 		// because it is not expensive to do so, and is not worth tracking which ClusterQueues use
 		// which flavors.
-		cq.UpdateLabelKeys(c.resourceFlavors)
+		cq.UpdateWithFlavors(c.resourceFlavors)
+		curStatus := cq.Status
+		if prevStatus == Pending && curStatus == Active {
+			activate = true
+		}
 	}
-	c.Unlock()
+	return activate
+}
+
+func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) bool {
+	c.Lock()
+	defer c.Unlock()
+	c.resourceFlavors[rf.Name] = rf
+	return c.updateClusterQueues()
 }
 
-func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) {
+func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) bool {
 	c.Lock()
+	defer c.Unlock()
 	delete(c.resourceFlavors, rf.Name)
-	c.Unlock()
+	return c.updateClusterQueues()
+}
+
+func (c *Cache) ClusterQueueActive(name string) bool {
+	c.RLock()
+	defer c.RUnlock()
+	cq, exists := c.clusterQueues[name]
+	if !exists {
+		return false
+	}
+	return cq.Active()
```

  safest is to do this:
  
  ```suggestion
  	return cq != nil && cq.Active()
  ```

- **pkg/cache/cache_test.go:275** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T16:21:50Z
```diff
@@ -227,6 +272,25 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 							NamespaceSelector: &metav1.LabelSelector{}, // everything
 						},
 					},
+					{
```

  can we add another separate case, name is "add resource flavor" testing that when the nonexistent-flavor becomes existent, the CQ becomes active?

- **pkg/controller/core/resourceflavor_controller.go:73** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T16:44:43Z
```diff
@@ -53,20 +56,30 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue becomes active,
+	// we should inform clusterQueue controller to broadcast the event.
+	if r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()) {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
```

  When a resourceFlavor is deleted, the clusterQueues that reference it goes into pending status, which is fine. The more problematic issue is if there are admitted workloads that reference this flavor but not yet unsuspended.  We could still address this by preventing to delete a resourceFlavor unless no clusterQueue references it (using finalizers). 
  
  In any case, please add this todo to the tracking issue and remove this comment.

- **pkg/controller/core/resourceflavor_controller.go:81** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T16:47:19Z
```diff
@@ -53,20 +56,30 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue becomes active,
+	// we should inform clusterQueue controller to broadcast the event.
+	if r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()) {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
 func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
-	r.cache.DeleteResourceFlavor(flv)
+
+	// No need to broadcast here as deleting resourceFlavor will not bring clusterQueue to active.
```

  We don't need to make this assumption here, `DeleteResourceFlavor` already returns a flag indicating whether or not a CQ became active, so I would do the same as above:
  
  ```
  if r.cache.DeleteResourceFlavor(flv) {
    r.qManager.Broadcast()
  }
  ```

- **pkg/controller/core/resourceflavor_controller.go:96** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T16:48:42Z
```diff
@@ -75,9 +88,12 @@ func (r *ResourceFlavorReconciler) Update(e event.UpdateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor update event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// No need to broadcast here as updating resourceFlavor will not bring status change in clusterQueue.
+	_ = r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
```

  ditto, the return value will tell us that, no need to make other assumptions
  
  ```
  if r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()) {
    r.qManager.Broadcast()
  }
  ```

- **pkg/scheduler/scheduler.go:185** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T16:56:51Z
```diff
@@ -180,7 +180,9 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if cq == nil {
-			e.inadmissibleReason = "ClusterQueue not found"
+			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
+		} else if snap.InactiveClusterQueueSets.Has(w.ClusterQueue) {
+			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s is inactive", cq.Name)
```

  we should flip this because an inactive ClusterQueue is not going to be in `snap.ClusterQueues` map. Any way we can add a check on the inadmissibleReason in the unit tests so we can capture this case?

- **test/integration/scheduler/scheduler_test.go:638** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T17:11:05Z
```diff
@@ -633,4 +634,115 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return wl3.Spec.Admission == nil
 		}, framework.ConsistentDuration, framework.Interval).Should(gomega.Equal(true))
 	})
+
+	ginkgo.Context("When clusterQueue contains nonexistent flavors", func() {
```

  ```suggestion
  	ginkgo.Context("When clusterQueue references nonexistent flavors", func() {
  ```

- **test/integration/scheduler/scheduler_test.go:662** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T17:11:45Z
```diff
@@ -633,4 +634,115 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return wl3.Spec.Admission == nil
 		}, framework.ConsistentDuration, framework.Interval).Should(gomega.Equal(true))
 	})
+
+	ginkgo.Context("When clusterQueue contains nonexistent flavors", func() {
+		var (
+			fooFlavor *kueue.ResourceFlavor
+			fooQ      *kueue.Queue
+			fooCq     *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			fooCq = testing.MakeClusterQueue("foo-cq").
+				QueueingStrategy(kueue.StrictFIFO).
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor("foo-flavor", "15").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooCq)).Should(gomega.Succeed())
+			fooQ = testing.MakeQueue("foo-queue", ns.Name).ClusterQueue(fooCq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooQ)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, fooCq)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, fooFlavor)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should be inactive until nonexistent flavor created", func() {
```

  ```suggestion
  		ginkgo.It("Should be inactive until the flavor is created", func() {
  ```

- **test/integration/scheduler/scheduler_test.go:647** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T17:13:25Z
```diff
@@ -633,4 +634,115 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return wl3.Spec.Admission == nil
 		}, framework.ConsistentDuration, framework.Interval).Should(gomega.Equal(true))
 	})
+
+	ginkgo.Context("When clusterQueue contains nonexistent flavors", func() {
+		var (
+			fooFlavor *kueue.ResourceFlavor
+			fooQ      *kueue.Queue
+			fooCq     *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			fooCq = testing.MakeClusterQueue("foo-cq").
+				QueueingStrategy(kueue.StrictFIFO).
```

  I am guessing the test fails in BestEffortFIFO mode because the workloads are in the inadmissible list and nothing it taking them out?
  
  We need to export [queueAllInadmissibleWorkloadsInCohort](https://github.com/kubernetes-sigs/kueue/blob/26f4ac4464d39cfa0ef355810dcb1bef36116e64/pkg/queue/manager.go#L366) and invoke it from the resourceFlavor controller before broadcast.

- **test/integration/scheduler/scheduler_test.go:679** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T17:26:14Z
```diff
@@ -633,4 +634,115 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return wl3.Spec.Admission == nil
 		}, framework.ConsistentDuration, framework.Interval).Should(gomega.Equal(true))
 	})
+
+	ginkgo.Context("When clusterQueue contains nonexistent flavors", func() {
+		var (
+			fooFlavor *kueue.ResourceFlavor
+			fooQ      *kueue.Queue
+			fooCq     *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			fooCq = testing.MakeClusterQueue("foo-cq").
+				QueueingStrategy(kueue.StrictFIFO).
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor("foo-flavor", "15").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooCq)).Should(gomega.Succeed())
+			fooQ = testing.MakeQueue("foo-queue", ns.Name).ClusterQueue(fooCq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooQ)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, fooCq)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, fooFlavor)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should be inactive until nonexistent flavor created", func() {
+			ginkgo.By("Creating two workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("0"))},
+					},
+				},
+			}))
```

  I think the check should be on the workload condition, it should have WorkloadAdmitted condition, with status false, reason is `Pending` and message "ClusterQueue %s is inactive".

- **test/integration/scheduler/scheduler_test.go:686** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T18:09:09Z
```diff
@@ -633,4 +634,115 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return wl3.Spec.Admission == nil
 		}, framework.ConsistentDuration, framework.Interval).Should(gomega.Equal(true))
 	})
+
+	ginkgo.Context("When clusterQueue contains nonexistent flavors", func() {
+		var (
+			fooFlavor *kueue.ResourceFlavor
+			fooQ      *kueue.Queue
+			fooCq     *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			fooCq = testing.MakeClusterQueue("foo-cq").
+				QueueingStrategy(kueue.StrictFIFO).
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor("foo-flavor", "15").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooCq)).Should(gomega.Succeed())
+			fooQ = testing.MakeQueue("foo-queue", ns.Name).ClusterQueue(fooCq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooQ)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, fooCq)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, fooFlavor)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should be inactive until nonexistent flavor created", func() {
+			ginkgo.By("Creating two workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("0"))},
+					},
+				},
+			}))
+
+			ginkgo.By("Creating foo flavor")
+			fooFlavor = testing.MakeResourceFlavor("foo-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooFlavor)).Should(gomega.Succeed())
+
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, fooCq.Name, wl1, wl2)
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
```

  no need for this check, lets keep the test as short as possible and concise

- **test/integration/scheduler/scheduler_test.go:712** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T18:34:16Z
```diff
@@ -633,4 +634,115 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return wl3.Spec.Admission == nil
 		}, framework.ConsistentDuration, framework.Interval).Should(gomega.Equal(true))
 	})
+
+	ginkgo.Context("When clusterQueue contains nonexistent flavors", func() {
+		var (
+			fooFlavor *kueue.ResourceFlavor
+			fooQ      *kueue.Queue
+			fooCq     *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			fooCq = testing.MakeClusterQueue("foo-cq").
+				QueueingStrategy(kueue.StrictFIFO).
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor("foo-flavor", "15").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooCq)).Should(gomega.Succeed())
+			fooQ = testing.MakeQueue("foo-queue", ns.Name).ClusterQueue(fooCq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooQ)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, fooCq)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, fooFlavor)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should be inactive until nonexistent flavor created", func() {
+			ginkgo.By("Creating two workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("0"))},
+					},
+				},
+			}))
+
+			ginkgo.By("Creating foo flavor")
+			fooFlavor = testing.MakeResourceFlavor("foo-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooFlavor)).Should(gomega.Succeed())
+
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, fooCq.Name, wl1, wl2)
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("2"))},
+					},
+				},
+				AdmittedWorkloads: 2,
+			}))
+		})
+
+		ginkgo.It("Should be inactive when corresponding flavors deleted", func() {
+			ginkgo.By("Creating foo flavors")
+			fooFlavor = testing.MakeResourceFlavor("foo-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooFlavor)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating two workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, fooCq.Name, wl1, wl2)
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
```

  ditto, please remove this check.

- **test/integration/scheduler/scheduler_test.go:734** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T18:39:03Z
```diff
@@ -633,4 +634,115 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return wl3.Spec.Admission == nil
 		}, framework.ConsistentDuration, framework.Interval).Should(gomega.Equal(true))
 	})
+
+	ginkgo.Context("When clusterQueue contains nonexistent flavors", func() {
+		var (
+			fooFlavor *kueue.ResourceFlavor
+			fooQ      *kueue.Queue
+			fooCq     *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			fooCq = testing.MakeClusterQueue("foo-cq").
+				QueueingStrategy(kueue.StrictFIFO).
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor("foo-flavor", "15").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooCq)).Should(gomega.Succeed())
+			fooQ = testing.MakeQueue("foo-queue", ns.Name).ClusterQueue(fooCq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooQ)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, fooCq)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, fooFlavor)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should be inactive until nonexistent flavor created", func() {
+			ginkgo.By("Creating two workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("0"))},
+					},
+				},
+			}))
+
+			ginkgo.By("Creating foo flavor")
+			fooFlavor = testing.MakeResourceFlavor("foo-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooFlavor)).Should(gomega.Succeed())
+
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, fooCq.Name, wl1, wl2)
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("2"))},
+					},
+				},
+				AdmittedWorkloads: 2,
+			}))
+		})
+
+		ginkgo.It("Should be inactive when corresponding flavors deleted", func() {
+			ginkgo.By("Creating foo flavors")
+			fooFlavor = testing.MakeResourceFlavor("foo-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooFlavor)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating two workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, fooCq.Name, wl1, wl2)
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("2"))},
+					},
+				},
+				AdmittedWorkloads: 2,
+			}))
+
+			ginkgo.By("Deleting foo flavors")
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, fooFlavor)).To(gomega.Succeed())
+
+			ginkgo.By("Creating another two workloads")
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl4 := testing.MakeWorkload("wl-4", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
```

  ditto, we should check the workloads condition here. 
  
  Also, we need a field in ClusterQueueStatus to reflect its condition (pending vs active) which we can check here, but we can do that as a followup.

- **pkg/controller/core/resourceflavor_controller.go:86** by [@ahg-g](https://github.com/ahg-g) — 2022-05-01T19:24:03Z
```diff
@@ -53,20 +56,37 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue got ready, we should inform clusterQueue controller to broadcast the event.
+	// Ignore the second value for add/update flavor will not cause clusterQueue not ready.
+	cqNames, _ := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+	r.qManager.UpdateClusterQueuesStatus(cqNames, true)
+	if len(cqNames) > 0 {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
 func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
-	r.cache.DeleteResourceFlavor(flv)
+
+	// TODO: When finalizer implemented in resourceFlavor controller, we'll no longer need notReadyNames.
+	readyNames, notReadyNames := r.cache.DeleteResourceFlavor(flv)
+	r.qManager.UpdateClusterQueuesStatus(readyNames, true)
+	r.qManager.UpdateClusterQueuesStatus(notReadyNames, false)
```

  I think we can keep the check in the manager when getting the heads, this will be better especially for BestEffortFIFO. The check can be done from the cache by invoking `ClusterQueueActive`.
  
  I feel that a dependency from the queue to the cache is ok (the queue is logically over the cache), but we could also pass in the function's reference to the manager rather than the whole cache to avoid that dependency. @alculquicondor do you think there is an issue with the queue to cache dependency?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-02T22:49:47Z

- **pkg/controller/core/resourceflavor_controller.go:86** by [@ahg-g](https://github.com/ahg-g) — 2022-05-02T22:49:47Z
```diff
@@ -53,20 +56,37 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue got ready, we should inform clusterQueue controller to broadcast the event.
+	// Ignore the second value for add/update flavor will not cause clusterQueue not ready.
+	cqNames, _ := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+	r.qManager.UpdateClusterQueuesStatus(cqNames, true)
+	if len(cqNames) > 0 {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
 func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
-	r.cache.DeleteResourceFlavor(flv)
+
+	// TODO: When finalizer implemented in resourceFlavor controller, we'll no longer need notReadyNames.
+	readyNames, notReadyNames := r.cache.DeleteResourceFlavor(flv)
+	r.qManager.UpdateClusterQueuesStatus(readyNames, true)
+	r.qManager.UpdateClusterQueuesStatus(notReadyNames, false)
```

  @kerthcet lets go with a function reference or interface instead of passing the whole cache.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-05T13:57:50Z

- **pkg/controller/core/resourceflavor_controller.go:81** by [@kerthcet](https://github.com/kerthcet) — 2022-05-05T13:57:50Z
```diff
@@ -53,20 +56,30 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue becomes active,
+	// we should inform clusterQueue controller to broadcast the event.
+	if r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()) {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
 func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
-	r.cache.DeleteResourceFlavor(flv)
+
+	// No need to broadcast here as deleting resourceFlavor will not bring clusterQueue to active.
```

  got it.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-05T14:02:29Z

- **pkg/scheduler/scheduler.go:185** by [@kerthcet](https://github.com/kerthcet) — 2022-05-05T14:02:29Z
```diff
@@ -180,7 +180,9 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if cq == nil {
-			e.inadmissibleReason = "ClusterQueue not found"
+			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
+		} else if snap.InactiveClusterQueueSets.Has(w.ClusterQueue) {
+			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s is inactive", cq.Name)
```

  yes, this is a trick here. 😄 Thanks for catching that. I'll add some verifications.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-06T02:04:24Z

- **pkg/controller/core/resourceflavor_controller.go:86** by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T02:04:23Z
```diff
@@ -53,20 +56,37 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue got ready, we should inform clusterQueue controller to broadcast the event.
+	// Ignore the second value for add/update flavor will not cause clusterQueue not ready.
+	cqNames, _ := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+	r.qManager.UpdateClusterQueuesStatus(cqNames, true)
+	if len(cqNames) > 0 {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
 func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
-	r.cache.DeleteResourceFlavor(flv)
+
+	// TODO: When finalizer implemented in resourceFlavor controller, we'll no longer need notReadyNames.
+	readyNames, notReadyNames := r.cache.DeleteResourceFlavor(flv)
+	r.qManager.UpdateClusterQueuesStatus(readyNames, true)
+	r.qManager.UpdateClusterQueuesStatus(notReadyNames, false)
```

  Or maybe follow this in the next PR. I'd like to implement this feature firstly.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-06T02:28:46Z

- **pkg/controller/core/resourceflavor_controller.go:73** by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T02:28:46Z
```diff
@@ -53,20 +56,30 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue becomes active,
+	// we should inform clusterQueue controller to broadcast the event.
+	if r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()) {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
```

  issue: https://github.com/kubernetes-sigs/kueue/issues/249

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-06T06:19:25Z

- **test/integration/scheduler/scheduler_test.go:734** by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T06:18:28Z
```diff
@@ -633,4 +634,115 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return wl3.Spec.Admission == nil
 		}, framework.ConsistentDuration, framework.Interval).Should(gomega.Equal(true))
 	})
+
+	ginkgo.Context("When clusterQueue contains nonexistent flavors", func() {
+		var (
+			fooFlavor *kueue.ResourceFlavor
+			fooQ      *kueue.Queue
+			fooCq     *kueue.ClusterQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			fooCq = testing.MakeClusterQueue("foo-cq").
+				QueueingStrategy(kueue.StrictFIFO).
+				Resource(testing.MakeResource(corev1.ResourceCPU).
+					Flavor(testing.MakeFlavor("foo-flavor", "15").Obj()).
+					Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooCq)).Should(gomega.Succeed())
+			fooQ = testing.MakeQueue("foo-queue", ns.Name).ClusterQueue(fooCq.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooQ)).Should(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, fooCq)).To(gomega.Succeed())
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, fooFlavor)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should be inactive until nonexistent flavor created", func() {
+			ginkgo.By("Creating two workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("0"))},
+					},
+				},
+			}))
+
+			ginkgo.By("Creating foo flavor")
+			fooFlavor = testing.MakeResourceFlavor("foo-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooFlavor)).Should(gomega.Succeed())
+
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, fooCq.Name, wl1, wl2)
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("2"))},
+					},
+				},
+				AdmittedWorkloads: 2,
+			}))
+		})
+
+		ginkgo.It("Should be inactive when corresponding flavors deleted", func() {
+			ginkgo.By("Creating foo flavors")
+			fooFlavor = testing.MakeResourceFlavor("foo-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, fooFlavor)).Should(gomega.Succeed())
+
+			ginkgo.By("Creating two workloads")
+			wl1 := testing.MakeWorkload("wl-1", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl2 := testing.MakeWorkload("wl-2", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl2)).Should(gomega.Succeed())
+
+			framework.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, fooCq.Name, wl1, wl2)
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
+				var updatedCq kueue.ClusterQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(fooCq), &updatedCq)).To(gomega.Succeed())
+				return updatedCq.Status
+			}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.ClusterQueueStatus{
+				UsedResources: kueue.UsedResources{
+					corev1.ResourceCPU: {
+						"foo-flavor": {Total: pointer.Quantity(resource.MustParse("2"))},
+					},
+				},
+				AdmittedWorkloads: 2,
+			}))
+
+			ginkgo.By("Deleting foo flavors")
+			gomega.Expect(framework.DeleteResourceFlavor(ctx, k8sClient, fooFlavor)).To(gomega.Succeed())
+
+			ginkgo.By("Creating another two workloads")
+			wl3 := testing.MakeWorkload("wl-3", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			wl4 := testing.MakeWorkload("wl-4", ns.Name).Queue(fooQ.Name).Request(corev1.ResourceCPU, "1").Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl3)).Should(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, wl4)).Should(gomega.Succeed())
+
+			gomega.Eventually(func() kueue.ClusterQueueStatus {
```

  This is also what I have planed to do. Raise a issue here: https://github.com/kubernetes-sigs/kueue/issues/250

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-06T06:30:26Z

- **pkg/controller/core/resourceflavor_controller.go:86** by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T06:30:26Z
```diff
@@ -53,20 +56,37 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue got ready, we should inform clusterQueue controller to broadcast the event.
+	// Ignore the second value for add/update flavor will not cause clusterQueue not ready.
+	cqNames, _ := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+	r.qManager.UpdateClusterQueuesStatus(cqNames, true)
+	if len(cqNames) > 0 {
+		r.qManager.Broadcast()
+	}
 	return false
 }
 
+// TODO: When ResourceFlavors are used by ClusterQueues, they shouldn't been deleted directly.
 func (r *ResourceFlavorReconciler) Delete(e event.DeleteEvent) bool {
 	flv, match := e.Object.(*kueue.ResourceFlavor)
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor delete event")
-	r.cache.DeleteResourceFlavor(flv)
+
+	// TODO: When finalizer implemented in resourceFlavor controller, we'll no longer need notReadyNames.
+	readyNames, notReadyNames := r.cache.DeleteResourceFlavor(flv)
+	r.qManager.UpdateClusterQueuesStatus(readyNames, true)
+	r.qManager.UpdateClusterQueuesStatus(notReadyNames, false)
```

  created issue here: https://github.com/kubernetes-sigs/kueue/issues/251

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2022-05-06T07:45:53Z

- **pkg/scheduler/scheduler.go:185** by [@kerthcet](https://github.com/kerthcet) — 2022-05-06T07:45:53Z
```diff
@@ -180,7 +180,9 @@ func (s *Scheduler) nominate(ctx context.Context, workloads []workload.Info, sna
 		ns := corev1.Namespace{}
 		e := entry{Info: w}
 		if cq == nil {
-			e.inadmissibleReason = "ClusterQueue not found"
+			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s not found", w.ClusterQueue)
+		} else if snap.InactiveClusterQueueSets.Has(w.ClusterQueue) {
+			e.inadmissibleReason = fmt.Sprintf("ClusterQueue %s is inactive", cq.Name)
```

  I thought about the solutions, one is to hijack the log output, but I didn't find the way to redirect the output. Another one is to cache the entries in scheduler, both seems not elegant.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-05-06T22:21:38Z

- **pkg/queue/manager.go:380** by [@ahg-g](https://github.com/ahg-g) — 2022-05-06T21:46:56Z
```diff
@@ -357,6 +357,28 @@ func (m *Manager) QueueAssociatedInadmissibleWorkloads(w *kueue.Workload) {
 	}
 }
 
+// QueueInadmissibleWorkloads moves all inadmissibleWorkloads in
+// corresponding ClusterQueues to heap. If at least one workload queued,
+// returns true, otherwise, returns false.
+func (m *Manager) QueueInadmissibleWorkloads(cqNames sets.String) bool {
+	if len(cqNames) == 0 {
+		return false
+	}
+
+	var queued bool
+	for name := range cqNames {
+		cq, exists := m.clusterQueues[name]
+		if !exists {
+			continue
+		}
+		if m.queueAllInadmissibleWorkloadsInCohort(cq) {
+			queued = true
+		}
+	}
+
```

  like the function above, lets call broadcast here instead of relying on the caller to invoke it.

- **pkg/cache/cache.go:237** by [@ahg-g](https://github.com/ahg-g) — 2022-05-06T21:52:21Z
```diff
@@ -205,22 +234,47 @@ func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
 	}
 }
 
-func (c *Cache) AddOrUpdateResourceFlavor(rf *kueue.ResourceFlavor) {
-	c.Lock()
-	c.resourceFlavors[rf.Name] = rf
+func (c *Cache) updateClusterQueues() (sets.String, bool) {
```

  we don't need the boolean flag, returning just the set is enough.

- **pkg/controller/core/resourceflavor_controller.go:65** by [@ahg-g](https://github.com/ahg-g) — 2022-05-06T21:53:00Z
```diff
@@ -53,9 +56,17 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue becomes active,
+	// we should inform clusterQueue controller to broadcast the event.
+	if cqNames, activate := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()); activate {
```

  if you remove the flag, then this becomes:
  
  ```suggestion
  	if cqNames := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()); len(cqNames) != 0 {
  ```

- **pkg/controller/core/resourceflavor_controller.go:67** by [@ahg-g](https://github.com/ahg-g) — 2022-05-06T21:53:41Z
```diff
@@ -53,9 +56,17 @@ func (r *ResourceFlavorReconciler) Create(e event.CreateEvent) bool {
 	if !match {
 		return false
 	}
+
 	log := r.log.WithValues("resourceFlavor", klog.KObj(flv))
 	log.V(2).Info("ResourceFlavor create event")
-	r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy())
+
+	// As long as one clusterQueue becomes active,
+	// we should inform clusterQueue controller to broadcast the event.
+	if cqNames, activate := r.cache.AddOrUpdateResourceFlavor(flv.DeepCopy()); activate {
+		if r.qManager.QueueInadmissibleWorkloads(cqNames) {
+			r.qManager.Broadcast()
```

  move the broadcast to inside `QueueInadmissibleWorkloads`, and `QueueInadmissibleWorkloads` doesn't need to return anything.

- **pkg/scheduler/scheduler_test.go:262** by [@ahg-g](https://github.com/ahg-g) — 2022-05-06T22:00:55Z
```diff
@@ -221,7 +258,8 @@ func TestSchedule(t *testing.T) {
 		// wantScheduled is the subset of workloads that got scheduled/admitted in this cycle.
 		wantScheduled []string
 		// wantLeft is the workload keys that are left in the queues after this cycle.
-		wantLeft map[string]sets.String
+		wantLeft           map[string]sets.String
+		inadmissibleReason string
```

  remove this

- **pkg/queue/manager.go:363** by [@ahg-g](https://github.com/ahg-g) — 2022-05-06T22:14:24Z
```diff
@@ -356,8 +353,30 @@ func (m *Manager) QueueAssociatedInadmissibleWorkloads(w *kueue.Workload) {
 	}
 
 	if m.queueAllInadmissibleWorkloadsInCohort(cq) {
-		m.cond.Broadcast()
+		m.Broadcast()
+	}
+}
+
+// QueueInadmissibleWorkloads moves all inadmissibleWorkloads in
+// corresponding ClusterQueues to heap. If at least one workload queued,
+// returns true, otherwise, returns false.
+func (m *Manager) QueueInadmissibleWorkloads(cqNames sets.String) bool {
```

  m.Lock()
  defer m.Unlock()
