# PR #2327: WaitForPodsReady: Add documentation for the backoffMaxSeconds

**Summary**: WaitForPodsReady: Add documentation for the backoffMaxSeconds

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2327

**Last updated**: 2024-05-31T17:04:16Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `add-back-off-max-seconds-docs` → `main`
- **Created**: 2024-05-30T01:48:58Z
- **Updated**: 2024-05-31T17:04:16Z
- **Merged**: 2024-05-31T17:03:50Z
- **Closed**: 2024-05-31T17:03:50Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo)
- **Changed files**: 1   **+14 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:
We don't have any documentation about https://github.com/kubernetes-sigs/kueue/pull/2264.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part-of #2216

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-30T01:49:05Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2327#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-30T01:49:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b7a4e837c3487159f482e156f2a627ef3106b7f5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6659f4a4cd8547000884d562 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-2327--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-2327--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTIzMjctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.9ojZZ-TfJ93fXo8dGtn9NxjTxM0RBtwSiCp5Vd_eIt0)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-30T01:49:42Z

/assign @alculquicondor

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-31T16:03:15Z

@alculquicondor I addressed all comments. PTAL, thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-05-31T17:00:11Z

LGTM label has been added.  <details>Git tree hash: 54615eb5d71c5f90d13dd4a32e2dcd82d029ecc2</details>

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-30T12:34:15Z

- **site/content/en/docs/tasks/manage/setup_sequential_admission.md:82** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T12:29:28Z
```diff
@@ -77,9 +78,15 @@ When the `blockAdmission` is set to `true`, admitted Workload with not ready pod
 {{% alert title="Warning" color="warning" %}}
 _Available in Kueue v0.6.0 and later_
 {{% /alert %}}
+{{% alert title="Note" color="primary" %}}
+_The `backoffBaseSeconds` and `backodMaxSeconds` are available in Kueue v0.7.0 and later_
```

  ```suggestion
  The `backoffBaseSeconds` and `backodMaxSeconds` are available in Kueue v0.7.0 and later
  ```

- **site/content/en/docs/tasks/manage/setup_sequential_admission.md:89** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T12:30:12Z
```diff
@@ -77,9 +78,15 @@ When the `blockAdmission` is set to `true`, admitted Workload with not ready pod
 {{% alert title="Warning" color="warning" %}}
 _Available in Kueue v0.6.0 and later_
 {{% /alert %}}
+{{% alert title="Note" color="primary" %}}
+_The `backoffBaseSeconds` and `backodMaxSeconds` are available in Kueue v0.7.0 and later_
+{{% /alert %}}
 
 The `requeuingStrategy` (`waitForPodsReady.requeuingStrategy`) contains optional parameters: 
-`timestamp` (`waitForPodsReady.requeuingStrategy.timestamp`) and `backoffLimitCount` (`waitForPodsReady.requeuingStrategy.backoffLimitCount`).
+- `timestamp` (`waitForPodsReady.requeuingStrategy.timestamp`)
+- `backoffLimitCount` (`waitForPodsReady.requeuingStrategy.backoffLimitCount`)
+- `backoffBaseSeconds` (`waitForPodsReady.requeuingStrategy.backoffBaseSeconds`)
+- `backoffMaxSeconds` (`waitForPodsReady.requeuingStrategy.backoffMaxSeconds`)
```

  ```suggestion
  - `timestamp`
  - `backoffLimitCount`
  - `backoffBaseSeconds`
  - `backoffMaxSeconds`
  ```

- **site/content/en/docs/tasks/manage/setup_sequential_admission.md:108** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T12:33:26Z
```diff
@@ -94,13 +101,13 @@ If you don't specify any value for `backoffLimitCount`,
 a Workload is repeatedly and endlessly re-queued to the queue based on the `timestamp`.
 Once the number of re-queues reaches the limit, Kueue [deactivates the Workload](/docs/concepts/workload/#active).
 
-{{% alert title="Note" color="primary" %}}
-_The `backoffBaseSeconds` is available in Kueue v0.7.0 and later_
-{{% /alert %}}
 The time to re-queue a workload after each consecutive timeout is increased
 exponentially, with the exponent of 2. The first delay is determined by the
 `backoffBaseSeconds` parameter (defaulting to 60). So, after the consecutive timeouts
-the evicted workload is re-queued after approximately `60, 120, 240, ...` seconds.
+the evicted workload is re-queued after approximately `60, 120, 240, ..., 3600, 3600 ..., 3600` seconds 
+if you don't specify the maximum backoff time by the `backoffMaxSeconds` (defaulting to 3600).
```

  ```markdown
  `backoffBaseSeconds` parameter (defaulting to 60). You can configure the maximum
  backoff time by setting `backoffMaxSeconds` (defaulting to 3600). Using the defaults, the
  evicted workload is re-queued after approximately `60, 120, 240, ..., 3600, ..., 3600` seconds.
  ```

- **site/content/en/docs/tasks/manage/setup_sequential_admission.md:109** by [@alculquicondor](https://github.com/alculquicondor) — 2024-05-30T12:34:10Z
```diff
@@ -94,13 +101,13 @@ If you don't specify any value for `backoffLimitCount`,
 a Workload is repeatedly and endlessly re-queued to the queue based on the `timestamp`.
 Once the number of re-queues reaches the limit, Kueue [deactivates the Workload](/docs/concepts/workload/#active).
 
-{{% alert title="Note" color="primary" %}}
-_The `backoffBaseSeconds` is available in Kueue v0.7.0 and later_
-{{% /alert %}}
 The time to re-queue a workload after each consecutive timeout is increased
 exponentially, with the exponent of 2. The first delay is determined by the
 `backoffBaseSeconds` parameter (defaulting to 60). So, after the consecutive timeouts
-the evicted workload is re-queued after approximately `60, 120, 240, ...` seconds.
+the evicted workload is re-queued after approximately `60, 120, 240, ..., 3600, 3600 ..., 3600` seconds 
+if you don't specify the maximum backoff time by the `backoffMaxSeconds` (defaulting to 3600).
+Even if the backoff time reaches the `backoffMaxSeconds`, Kueue will keep to re-queue an evicted Workload with the `backoffMaxSeconds`
```

  ```suggestion
  Even if the backoff time reaches the `backoffMaxSeconds`, Kueue will continue to re-queue an evicted Workload with the `backoffMaxSeconds`
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-31T16:02:16Z

- **site/content/en/docs/tasks/manage/setup_sequential_admission.md:108** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-31T16:02:16Z
```diff
@@ -94,13 +101,13 @@ If you don't specify any value for `backoffLimitCount`,
 a Workload is repeatedly and endlessly re-queued to the queue based on the `timestamp`.
 Once the number of re-queues reaches the limit, Kueue [deactivates the Workload](/docs/concepts/workload/#active).
 
-{{% alert title="Note" color="primary" %}}
-_The `backoffBaseSeconds` is available in Kueue v0.7.0 and later_
-{{% /alert %}}
 The time to re-queue a workload after each consecutive timeout is increased
 exponentially, with the exponent of 2. The first delay is determined by the
 `backoffBaseSeconds` parameter (defaulting to 60). So, after the consecutive timeouts
-the evicted workload is re-queued after approximately `60, 120, 240, ...` seconds.
+the evicted workload is re-queued after approximately `60, 120, 240, ..., 3600, 3600 ..., 3600` seconds 
+if you don't specify the maximum backoff time by the `backoffMaxSeconds` (defaulting to 3600).
```

  Thank you for great suggestion!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-31T16:02:43Z

- **site/content/en/docs/tasks/manage/setup_sequential_admission.md:82** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-31T16:02:43Z
```diff
@@ -77,9 +78,15 @@ When the `blockAdmission` is set to `true`, admitted Workload with not ready pod
 {{% alert title="Warning" color="warning" %}}
 _Available in Kueue v0.6.0 and later_
 {{% /alert %}}
+{{% alert title="Note" color="primary" %}}
+_The `backoffBaseSeconds` and `backodMaxSeconds` are available in Kueue v0.7.0 and later_
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-31T16:02:47Z

- **site/content/en/docs/tasks/manage/setup_sequential_admission.md:89** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-31T16:02:47Z
```diff
@@ -77,9 +78,15 @@ When the `blockAdmission` is set to `true`, admitted Workload with not ready pod
 {{% alert title="Warning" color="warning" %}}
 _Available in Kueue v0.6.0 and later_
 {{% /alert %}}
+{{% alert title="Note" color="primary" %}}
+_The `backoffBaseSeconds` and `backodMaxSeconds` are available in Kueue v0.7.0 and later_
+{{% /alert %}}
 
 The `requeuingStrategy` (`waitForPodsReady.requeuingStrategy`) contains optional parameters: 
-`timestamp` (`waitForPodsReady.requeuingStrategy.timestamp`) and `backoffLimitCount` (`waitForPodsReady.requeuingStrategy.backoffLimitCount`).
+- `timestamp` (`waitForPodsReady.requeuingStrategy.timestamp`)
+- `backoffLimitCount` (`waitForPodsReady.requeuingStrategy.backoffLimitCount`)
+- `backoffBaseSeconds` (`waitForPodsReady.requeuingStrategy.backoffBaseSeconds`)
+- `backoffMaxSeconds` (`waitForPodsReady.requeuingStrategy.backoffMaxSeconds`)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-31T16:02:49Z

- **site/content/en/docs/tasks/manage/setup_sequential_admission.md:108** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-31T16:02:49Z
```diff
@@ -94,13 +101,13 @@ If you don't specify any value for `backoffLimitCount`,
 a Workload is repeatedly and endlessly re-queued to the queue based on the `timestamp`.
 Once the number of re-queues reaches the limit, Kueue [deactivates the Workload](/docs/concepts/workload/#active).
 
-{{% alert title="Note" color="primary" %}}
-_The `backoffBaseSeconds` is available in Kueue v0.7.0 and later_
-{{% /alert %}}
 The time to re-queue a workload after each consecutive timeout is increased
 exponentially, with the exponent of 2. The first delay is determined by the
 `backoffBaseSeconds` parameter (defaulting to 60). So, after the consecutive timeouts
-the evicted workload is re-queued after approximately `60, 120, 240, ...` seconds.
+the evicted workload is re-queued after approximately `60, 120, 240, ..., 3600, 3600 ..., 3600` seconds 
+if you don't specify the maximum backoff time by the `backoffMaxSeconds` (defaulting to 3600).
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-05-31T16:02:51Z

- **site/content/en/docs/tasks/manage/setup_sequential_admission.md:109** by [@tenzen-y](https://github.com/tenzen-y) — 2024-05-31T16:02:51Z
```diff
@@ -94,13 +101,13 @@ If you don't specify any value for `backoffLimitCount`,
 a Workload is repeatedly and endlessly re-queued to the queue based on the `timestamp`.
 Once the number of re-queues reaches the limit, Kueue [deactivates the Workload](/docs/concepts/workload/#active).
 
-{{% alert title="Note" color="primary" %}}
-_The `backoffBaseSeconds` is available in Kueue v0.7.0 and later_
-{{% /alert %}}
 The time to re-queue a workload after each consecutive timeout is increased
 exponentially, with the exponent of 2. The first delay is determined by the
 `backoffBaseSeconds` parameter (defaulting to 60). So, after the consecutive timeouts
-the evicted workload is re-queued after approximately `60, 120, 240, ...` seconds.
+the evicted workload is re-queued after approximately `60, 120, 240, ..., 3600, 3600 ..., 3600` seconds 
+if you don't specify the maximum backoff time by the `backoffMaxSeconds` (defaulting to 3600).
+Even if the backoff time reaches the `backoffMaxSeconds`, Kueue will keep to re-queue an evicted Workload with the `backoffMaxSeconds`
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2024-05-31T17:00:07Z

/lgtm
