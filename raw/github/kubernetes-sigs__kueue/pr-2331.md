# PR #2331: [multikueue] Use batch/Job `spec.managedBy` field.

**Summary**: [multikueue] Use batch/Job `spec.managedBy` field.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/2331

**Last updated**: 2024-07-18T20:53:32Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `multikueue-job-managed-by` → `main`
- **Created**: 2024-05-30T14:42:42Z
- **Updated**: 2024-07-18T20:53:32Z
- **Merged**: 2024-06-05T13:09:31Z
- **Closed**: 2024-06-05T13:09:31Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 15   **+613 / -39**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Use `spec.managedBy` to detect delegatable jobs.
Enable live status updates for batch/Jobs.
No longer keep the multikueue admission check state `Pending` when the job is running in a worker cluster.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #693

#### Special notes for your reviewer:


#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
MultiKueue: Use batch/Job `spec.managedBy` field
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2024-05-30T14:43:05Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 359134f0cf2e532a9c461a73ac9e755125c0c1c7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/66605e66018a98000870a75b |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-06-03T18:41:31Z

/assign @mimowo

### Comment by [@trasc](https://github.com/trasc) — 2024-06-04T15:12:34Z

@alculquicondor @mimowo  @mwielgus 

We could  also keep the old integration approach and put the `spec.managedBy` support under kueue featuregate (say `MultiKueueBetchJobWithManage`) WDYT?

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-04T15:27:32Z

> @alculquicondor @mimowo @mwielgus
> 
> We could also keep the old integration approach and put the `spec.managedBy` support under kueue featuregate (say `MultiKueueBetchJobWithManage`) WDYT?

+1 sounds like a good idea. 

It will let users configure kueue according to their k8s configuration. Yet, we will not introduce API configuration which may turn out unnecessary over time.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-05T07:33:21Z

@trasc Thank you for responding to my questions. It seems that I missed reading codes somehow.
Now, everything is clear. So feel free to close my thread.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-05T07:33:48Z

> @alculquicondor @mimowo @mwielgus
> 
> We could also keep the old integration approach and put the `spec.managedBy` support under kueue featuregate (say `MultiKueueBetchJobWithManage`) WDYT?

SGTM

### Comment by [@trasc](https://github.com/trasc) — 2024-06-05T11:35:43Z

> I think we should also have defaulting of managedBy field for Jobs, as we do for JobSet. when the feature gate is enabled.
> 
> @trasc would you like to extend this PR or follow up?

Foillow-up I think @mbobrovskyi  has some experience around this topic.

### Comment by [@mimowo](https://github.com/mimowo) — 2024-06-05T13:05:42Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-05T13:05:47Z

LGTM label has been added.  <details>Git tree hash: c5295e560277a3d5f7d1a565c48a78ec03f90d2e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-06-05T13:05:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/2331#issuecomment-2149831451" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/2331#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-07-18T20:52:30Z

/release-note-edit
```release-note
MultiKueue: Use batch/Job `spec.managedBy` field
```

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-04T08:20:27Z

- **Makefile-test.mk:70** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-04T07:05:30Z
```diff
@@ -67,6 +67,7 @@ test: gotestsum ## Run tests.
 test-integration: gomod-download envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd kf-training-operator-crd  cluster-autoscaler-crd kueuectl ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" \
 	KUEUE_BIN=$(PROJECT_DIR)/bin \
+	ENVTEST_K8S_VERSION=$(ENVTEST_K8S_VERSION) \
```

  What is the reason that we need to override this environment variable here?

- **hack/multikueue/manager-cluster.kind-1.30.0.yaml:20** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-04T07:08:56Z
```diff
@@ -0,0 +1,20 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
+nodes:
+- role: control-plane
+  kubeadmConfigPatches:
+  - |
+    kind: ClusterConfiguration
+    apiVersion: kubeadm.k8s.io/v1beta3
+    scheduler:
+      extraArgs:
+        v: "2"
+    controllerManager:
+      extraArgs:
+        v: "2"
+        feature-gates: "JobManagedBy=true"
+    apiServer:
+      extraArgs:
+        enable-aggregator-routing: "true"
+        v: "2"
+        feature-gates: "JobManagedBy=true"
```

  ```YAML
  kind: Cluster
  apiVersion: kind.x-k8s.io/v1alpha4
  name: kind
  featureGates:
    JobManagedBy: true
  ```
  
  I guess that we don't need to fully control the kubeadm configuration. This simplified configuration should work fine.

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:116** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-04T08:12:57Z
```diff
@@ -97,10 +90,35 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 	return client.IgnoreNotFound(remoteClient.Delete(ctx, &job))
 }
 
-func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
-	return true
+func (b *batchJobAdapter) IsJobManagedByKueue(ctx context.Context, c client.Client, key types.NamespacedName) (bool, string, error) {
```

  Where do we use this function?

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:128** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-04T08:14:02Z
```diff
@@ -97,10 +90,35 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 	return client.IgnoreNotFound(remoteClient.Delete(ctx, &job))
 }
 
-func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
-	return true
+func (b *batchJobAdapter) IsJobManagedByKueue(ctx context.Context, c client.Client, key types.NamespacedName) (bool, string, error) {
+	job := batchv1.Job{}
+	err := c.Get(ctx, key, &job)
+	if err != nil {
+		return false, "", err
+	}
+	jobControllerName := ptr.Deref(job.Spec.ManagedBy, "")
+	if jobControllerName != ControllerName {
+		return false, fmt.Sprintf("Expecting spec.managedBy to be %q not %q", ControllerName, jobControllerName), nil
```

  Could you clarify the reasons that we handle this case as just warning?

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:137** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-04T08:14:56Z
```diff
@@ -97,10 +90,35 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 	return client.IgnoreNotFound(remoteClient.Delete(ctx, &job))
 }
 
-func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
-	return true
+func (b *batchJobAdapter) IsJobManagedByKueue(ctx context.Context, c client.Client, key types.NamespacedName) (bool, string, error) {
+	job := batchv1.Job{}
+	err := c.Get(ctx, key, &job)
+	if err != nil {
+		return false, "", err
+	}
+	jobControllerName := ptr.Deref(job.Spec.ManagedBy, "")
+	if jobControllerName != ControllerName {
+		return false, fmt.Sprintf("Expecting spec.managedBy to be %q not %q", ControllerName, jobControllerName), nil
+	}
+	return true, "", nil
 }
 
-func (b *batchJobAdapter) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
-	return true, "", nil
+var _ multiKueueWatcher = (*batchJobAdapter)(nil)
+
+func (*batchJobAdapter) GetEmptyList() client.ObjectList {
+	return &batchv1.JobList{}
+}
```

  Where do we use this function?

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:142** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-04T08:19:39Z
```diff
@@ -97,10 +90,35 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 	return client.IgnoreNotFound(remoteClient.Delete(ctx, &job))
 }
 
-func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
-	return true
+func (b *batchJobAdapter) IsJobManagedByKueue(ctx context.Context, c client.Client, key types.NamespacedName) (bool, string, error) {
+	job := batchv1.Job{}
+	err := c.Get(ctx, key, &job)
+	if err != nil {
+		return false, "", err
+	}
+	jobControllerName := ptr.Deref(job.Spec.ManagedBy, "")
+	if jobControllerName != ControllerName {
+		return false, fmt.Sprintf("Expecting spec.managedBy to be %q not %q", ControllerName, jobControllerName), nil
+	}
+	return true, "", nil
 }
 
-func (b *batchJobAdapter) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
-	return true, "", nil
+var _ multiKueueWatcher = (*batchJobAdapter)(nil)
+
+func (*batchJobAdapter) GetEmptyList() client.ObjectList {
+	return &batchv1.JobList{}
+}
+
+func (*batchJobAdapter) GetWorkloadKey(o runtime.Object) (types.NamespacedName, error) {
+	job, isJob := o.(*batchv1.Job)
+	if !isJob {
+		return types.NamespacedName{}, errors.New("not a job")
```

  ```suggestion
  		return types.NamespacedName{}, errors.New("this %q is not a batch/v1,Kind=Job", klog.KObj(o))
  ```
  
  This seems to be also not used anywhere.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-04T08:32:46Z

- **pkg/controller/admissionchecks/multikueue/workload_test.go:58** by [@mimowo](https://github.com/mimowo) — 2024-06-04T08:32:46Z
```diff
@@ -54,7 +54,8 @@ func TestWlReconcile(t *testing.T) {
 	}
 
 	baseWorkloadBuilder := utiltesting.MakeWorkload("wl1", TestNamespace)
-	baseJobBuilder := testingjob.MakeJob("job1", TestNamespace)
+	baseUnmanagedJobBuilder := testingjob.MakeJob("job1", TestNamespace)
+	baseJobBuilder := baseUnmanagedJobBuilder.Clone().ManagedBy(ControllerName)
```

  ```suggestion
  	baseUnmanagedJobBuilder := testingjob.MakeJob("job1", TestNamespace)
  	baseJobBuilder := baseUnmanagedJobBuilder.Clone().ManagedBy(ControllerName)
  ```
  ```suggestion
  	baseJobBuilder := testingjob.MakeJob("job1", TestNamespace)
  	baseJobManagedByKueueBuilder := baseUnmanagedJobBuilder.Clone().ManagedBy(ControllerName)
  ```
  If there is no managedBy, it is managed by the default controller, so I think it might be misleading to say "unmanaged", and saying `managedByKueue`, `customManager` or `customController` might be better.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-04T10:34:38Z

- **Makefile-test.mk:70** by [@trasc](https://github.com/trasc) — 2024-06-04T09:34:32Z
```diff
@@ -67,6 +67,7 @@ test: gotestsum ## Run tests.
 test-integration: gomod-download envtest ginkgo mpi-operator-crd ray-operator-crd jobset-operator-crd kf-training-operator-crd  cluster-autoscaler-crd kueuectl ## Run tests.
 	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" \
 	KUEUE_BIN=$(PROJECT_DIR)/bin \
+	ENVTEST_K8S_VERSION=$(ENVTEST_K8S_VERSION) \
```

  It is not overridden, it's just passed on. 
  We need a way to chose the kind config used for the manager cluster based on the version we are trying to start.

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:116** by [@trasc](https://github.com/trasc) — 2024-06-04T09:44:02Z
```diff
@@ -97,10 +90,35 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 	return client.IgnoreNotFound(remoteClient.Delete(ctx, &job))
 }
 
-func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
-	return true
+func (b *batchJobAdapter) IsJobManagedByKueue(ctx context.Context, c client.Client, key types.NamespacedName) (bool, string, error) {
```

  In the multikueue workload controller to determine if the owner job of a workload is able to unsuspended in the manager cluster without staring it's actual execution. 
  
  After admission in a worker cluster a clone of that job is executing in the worker cluster and it's status synced.

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:128** by [@trasc](https://github.com/trasc) — 2024-06-04T09:44:10Z
```diff
@@ -97,10 +90,35 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 	return client.IgnoreNotFound(remoteClient.Delete(ctx, &job))
 }
 
-func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
-	return true
+func (b *batchJobAdapter) IsJobManagedByKueue(ctx context.Context, c client.Client, key types.NamespacedName) (bool, string, error) {
+	job := batchv1.Job{}
+	err := c.Get(ctx, key, &job)
+	if err != nil {
+		return false, "", err
+	}
+	jobControllerName := ptr.Deref(job.Spec.ManagedBy, "")
+	if jobControllerName != ControllerName {
+		return false, fmt.Sprintf("Expecting spec.managedBy to be %q not %q", ControllerName, jobControllerName), nil
```

  It is not a warning, if we encounter an unexpected managedBy value, the workload will be `Rejected`  and marked as failed.

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:142** by [@trasc](https://github.com/trasc) — 2024-06-04T09:50:46Z
```diff
@@ -97,10 +90,35 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 	return client.IgnoreNotFound(remoteClient.Delete(ctx, &job))
 }
 
-func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
-	return true
+func (b *batchJobAdapter) IsJobManagedByKueue(ctx context.Context, c client.Client, key types.NamespacedName) (bool, string, error) {
+	job := batchv1.Job{}
+	err := c.Get(ctx, key, &job)
+	if err != nil {
+		return false, "", err
+	}
+	jobControllerName := ptr.Deref(job.Spec.ManagedBy, "")
+	if jobControllerName != ControllerName {
+		return false, fmt.Sprintf("Expecting spec.managedBy to be %q not %q", ControllerName, jobControllerName), nil
+	}
+	return true, "", nil
 }
 
-func (b *batchJobAdapter) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
-	return true, "", nil
+var _ multiKueueWatcher = (*batchJobAdapter)(nil)
+
+func (*batchJobAdapter) GetEmptyList() client.ObjectList {
+	return &batchv1.JobList{}
+}
+
+func (*batchJobAdapter) GetWorkloadKey(o runtime.Object) (types.NamespacedName, error) {
+	job, isJob := o.(*batchv1.Job)
+	if !isJob {
+		return types.NamespacedName{}, errors.New("not a job")
```

  https://github.com/kubernetes-sigs/kueue/blob/a14bc68990c2ff1e5ae6a93474b3f9c4dc84fd25/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L210

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:137** by [@trasc](https://github.com/trasc) — 2024-06-04T09:50:52Z
```diff
@@ -97,10 +90,35 @@ func (b *batchJobAdapter) DeleteRemoteObject(ctx context.Context, remoteClient c
 	return client.IgnoreNotFound(remoteClient.Delete(ctx, &job))
 }
 
-func (b *batchJobAdapter) KeepAdmissionCheckPending() bool {
-	return true
+func (b *batchJobAdapter) IsJobManagedByKueue(ctx context.Context, c client.Client, key types.NamespacedName) (bool, string, error) {
+	job := batchv1.Job{}
+	err := c.Get(ctx, key, &job)
+	if err != nil {
+		return false, "", err
+	}
+	jobControllerName := ptr.Deref(job.Spec.ManagedBy, "")
+	if jobControllerName != ControllerName {
+		return false, fmt.Sprintf("Expecting spec.managedBy to be %q not %q", ControllerName, jobControllerName), nil
+	}
+	return true, "", nil
 }
 
-func (b *batchJobAdapter) IsJobManagedByKueue(_ context.Context, _ client.Client, _ types.NamespacedName) (bool, string, error) {
-	return true, "", nil
+var _ multiKueueWatcher = (*batchJobAdapter)(nil)
+
+func (*batchJobAdapter) GetEmptyList() client.ObjectList {
+	return &batchv1.JobList{}
+}
```

  When setting watches in the worker cluster clients and get events when the job objects are updated in the worker clusters.
  
  https://github.com/kubernetes-sigs/kueue/blob/a14bc68990c2ff1e5ae6a93474b3f9c4dc84fd25/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L171-L184
  
  https://github.com/kubernetes-sigs/kueue/blob/a14bc68990c2ff1e5ae6a93474b3f9c4dc84fd25/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L191-L194

- **pkg/controller/admissionchecks/multikueue/workload_test.go:58** by [@trasc](https://github.com/trasc) — 2024-06-04T10:02:56Z
```diff
@@ -54,7 +54,8 @@ func TestWlReconcile(t *testing.T) {
 	}
 
 	baseWorkloadBuilder := utiltesting.MakeWorkload("wl1", TestNamespace)
-	baseJobBuilder := testingjob.MakeJob("job1", TestNamespace)
+	baseUnmanagedJobBuilder := testingjob.MakeJob("job1", TestNamespace)
+	baseJobBuilder := baseUnmanagedJobBuilder.Clone().ManagedBy(ControllerName)
```

  done

- **hack/multikueue/manager-cluster.kind-1.30.0.yaml:20** by [@trasc](https://github.com/trasc) — 2024-06-04T10:03:21Z
```diff
@@ -0,0 +1,20 @@
+kind: Cluster
+apiVersion: kind.x-k8s.io/v1alpha4
+nodes:
+- role: control-plane
+  kubeadmConfigPatches:
+  - |
+    kind: ClusterConfiguration
+    apiVersion: kubeadm.k8s.io/v1beta3
+    scheduler:
+      extraArgs:
+        v: "2"
+    controllerManager:
+      extraArgs:
+        v: "2"
+        feature-gates: "JobManagedBy=true"
+    apiServer:
+      extraArgs:
+        enable-aggregator-routing: "true"
+        v: "2"
+        feature-gates: "JobManagedBy=true"
```

  I did not know about that, thanks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-05T07:53:56Z

- **pkg/features/kube_features.go:114** by [@mimowo](https://github.com/mimowo) — 2024-06-05T07:53:56Z
```diff
@@ -97,14 +103,15 @@ func init() {
 // Entries are separated from each other with blank lines to avoid sweeping gofmt changes
 // when adding or removing one entry.
 var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
-	PartialAdmission:            {Default: true, PreRelease: featuregate.Beta},
-	QueueVisibility:             {Default: false, PreRelease: featuregate.Alpha},
-	FlavorFungibility:           {Default: true, PreRelease: featuregate.Beta},
-	ProvisioningACC:             {Default: true, PreRelease: featuregate.Beta},
-	VisibilityOnDemand:          {Default: false, PreRelease: featuregate.Alpha},
-	PrioritySortingWithinCohort: {Default: true, PreRelease: featuregate.Beta},
-	MultiKueue:                  {Default: false, PreRelease: featuregate.Alpha},
-	LendingLimit:                {Default: false, PreRelease: featuregate.Alpha},
+	PartialAdmission:               {Default: true, PreRelease: featuregate.Beta},
+	QueueVisibility:                {Default: false, PreRelease: featuregate.Alpha},
+	FlavorFungibility:              {Default: true, PreRelease: featuregate.Beta},
+	ProvisioningACC:                {Default: true, PreRelease: featuregate.Beta},
+	VisibilityOnDemand:             {Default: false, PreRelease: featuregate.Alpha},
+	PrioritySortingWithinCohort:    {Default: true, PreRelease: featuregate.Beta},
+	MultiKueue:                     {Default: false, PreRelease: featuregate.Alpha},
+	LendingLimit:                   {Default: false, PreRelease: featuregate.Alpha},
+	MultiKueueBetchJobWithManageBy: {Default: false, PreRelease: featuregate.Alpha},
```

  ```suggestion
  	MultiKueueBatchJobWithManagedBy: {Default: false, PreRelease: featuregate.Alpha},
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-05T08:33:30Z

- **pkg/features/kube_features.go:114** by [@trasc](https://github.com/trasc) — 2024-06-05T08:33:30Z
```diff
@@ -97,14 +103,15 @@ func init() {
 // Entries are separated from each other with blank lines to avoid sweeping gofmt changes
 // when adding or removing one entry.
 var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
-	PartialAdmission:            {Default: true, PreRelease: featuregate.Beta},
-	QueueVisibility:             {Default: false, PreRelease: featuregate.Alpha},
-	FlavorFungibility:           {Default: true, PreRelease: featuregate.Beta},
-	ProvisioningACC:             {Default: true, PreRelease: featuregate.Beta},
-	VisibilityOnDemand:          {Default: false, PreRelease: featuregate.Alpha},
-	PrioritySortingWithinCohort: {Default: true, PreRelease: featuregate.Beta},
-	MultiKueue:                  {Default: false, PreRelease: featuregate.Alpha},
-	LendingLimit:                {Default: false, PreRelease: featuregate.Alpha},
+	PartialAdmission:               {Default: true, PreRelease: featuregate.Beta},
+	QueueVisibility:                {Default: false, PreRelease: featuregate.Alpha},
+	FlavorFungibility:              {Default: true, PreRelease: featuregate.Beta},
+	ProvisioningACC:                {Default: true, PreRelease: featuregate.Beta},
+	VisibilityOnDemand:             {Default: false, PreRelease: featuregate.Alpha},
+	PrioritySortingWithinCohort:    {Default: true, PreRelease: featuregate.Beta},
+	MultiKueue:                     {Default: false, PreRelease: featuregate.Alpha},
+	LendingLimit:                   {Default: false, PreRelease: featuregate.Alpha},
+	MultiKueueBetchJobWithManageBy: {Default: false, PreRelease: featuregate.Alpha},
```

  thanks, done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2024-06-05T08:43:09Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-05T07:49:59Z
```diff
@@ -47,21 +53,24 @@ func (b *batchJobAdapter) SyncJob(ctx context.Context, localClient client.Client
 
 	// the remote job exists
 	if err == nil {
-		// This will no longer be necessary when batchJob will support live status update, by then
-		// we should only sync the Status of the job if it's "Finished".
-		remoteFinished := false
-		for _, c := range remoteJob.Status.Conditions {
-			if (c.Type == batchv1.JobComplete || c.Type == batchv1.JobFailed) && c.Status == corev1.ConditionTrue {
-				remoteFinished = true
-				break
-			}
-		}
-
-		if remoteFinished {
+		if features.Enabled(features.MultiKueueBetchJobWithManageBy) {
 			localJob.Status = remoteJob.Status
 			return localClient.Status().Update(ctx, &localJob)
 		} else {
```

  ```suggestion
  ```
  
  This `else` is not valuable.

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:71** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-05T07:50:16Z
```diff
@@ -47,21 +53,24 @@ func (b *batchJobAdapter) SyncJob(ctx context.Context, localClient client.Client
 
 	// the remote job exists
 	if err == nil {
-		// This will no longer be necessary when batchJob will support live status update, by then
-		// we should only sync the Status of the job if it's "Finished".
-		remoteFinished := false
-		for _, c := range remoteJob.Status.Conditions {
-			if (c.Type == batchv1.JobComplete || c.Type == batchv1.JobFailed) && c.Status == corev1.ConditionTrue {
-				remoteFinished = true
-				break
-			}
-		}
-
-		if remoteFinished {
+		if features.Enabled(features.MultiKueueBetchJobWithManageBy) {
 			localJob.Status = remoteJob.Status
 			return localClient.Status().Update(ctx, &localJob)
 		} else {
-			return nil
+			remoteFinished := false
+			for _, c := range remoteJob.Status.Conditions {
+				if (c.Type == batchv1.JobComplete || c.Type == batchv1.JobFailed) && c.Status == corev1.ConditionTrue {
+					remoteFinished = true
+					break
+				}
+			}
+
+			if remoteFinished {
+				localJob.Status = remoteJob.Status
+				return localClient.Status().Update(ctx, &localJob)
+			} else {
```

  ```suggestion
  ```

- **test/integration/multikueue/multikueue_test.go:766** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-05T08:09:34Z
```diff
@@ -651,7 +761,11 @@ var _ = ginkgo.Describe("Multikueue", func() {
 	})
 
 	ginkgo.It("Should remove the worker's workload and job when managers job is deleted", func() {
+		if managerK8sVersion.LessThan(versionutil.MustParseSemantic("1.30.0")) {
+			ginkgo.Skip("the managers kubernetes version is less then 1.30")
+		}
```

  Should we leave this case for the k8s version less than 1.30?

- **test/integration/multikueue/multikueue_test.go:538** by [@tenzen-y](https://github.com/tenzen-y) — 2024-06-05T08:43:07Z
```diff
@@ -509,6 +512,113 @@ var _ = ginkgo.Describe("Multikueue", func() {
 					LastTransitionTime: metav1.Now(),
 					Message:            "Job finished successfully",
 				})
+				createdJob.Status.StartTime = ptr.To(metav1.Now())
+				createdJob.Status.CompletionTime = ptr.To(metav1.Now())
+				g.Expect(worker1TestCluster.client.Status().Update(worker1TestCluster.ctx, &createdJob)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+
+				g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadFinished)).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  kueue.WorkloadFinishedReasonSucceeded,
+					Message: "Job finished successfully",
+				}, util.IgnoreConditionTimestampsAndObservedGeneration))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWorkload := &kueue.Workload{}
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Should run a job on worker if admitted (ManagedBy)", func() {
```

  Shouldn't we need to have this case for the K8s version less than v1.30, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-05T08:54:27Z

- **pkg/controller/admissionchecks/multikueue/workload.go:201** by [@mimowo](https://github.com/mimowo) — 2024-06-05T08:54:27Z
```diff
@@ -198,11 +198,11 @@ func (w *wlReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 	}
 
 	managed, unmanagedReason, err := adapter.IsJobManagedByKueue(ctx, w.client, types.NamespacedName{Name: owner.Name, Namespace: wl.Namespace})
-	if err != nil {
+	if err != nil && !isDeleted {
```

  This will conflict with https://github.com/kubernetes-sigs/kueue/pull/2355. We can probably revert it from here

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-05T09:47:23Z

- **pkg/controller/admissionchecks/multikueue/workload.go:201** by [@trasc](https://github.com/trasc) — 2024-06-05T09:47:23Z
```diff
@@ -198,11 +198,11 @@ func (w *wlReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 	}
 
 	managed, unmanagedReason, err := adapter.IsJobManagedByKueue(ctx, w.client, types.NamespacedName{Name: owner.Name, Namespace: wl.Namespace})
-	if err != nil {
+	if err != nil && !isDeleted {
```

  sure ,  I found 2355 while working on this.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-05T10:41:17Z

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:59** by [@trasc](https://github.com/trasc) — 2024-06-05T09:48:41Z
```diff
@@ -47,21 +53,24 @@ func (b *batchJobAdapter) SyncJob(ctx context.Context, localClient client.Client
 
 	// the remote job exists
 	if err == nil {
-		// This will no longer be necessary when batchJob will support live status update, by then
-		// we should only sync the Status of the job if it's "Finished".
-		remoteFinished := false
-		for _, c := range remoteJob.Status.Conditions {
-			if (c.Type == batchv1.JobComplete || c.Type == batchv1.JobFailed) && c.Status == corev1.ConditionTrue {
-				remoteFinished = true
-				break
-			}
-		}
-
-		if remoteFinished {
+		if features.Enabled(features.MultiKueueBetchJobWithManageBy) {
 			localJob.Status = remoteJob.Status
 			return localClient.Status().Update(ctx, &localJob)
 		} else {
```

  removed, thanks.

- **pkg/controller/admissionchecks/multikueue/batchjob_adapter.go:71** by [@trasc](https://github.com/trasc) — 2024-06-05T09:49:07Z
```diff
@@ -47,21 +53,24 @@ func (b *batchJobAdapter) SyncJob(ctx context.Context, localClient client.Client
 
 	// the remote job exists
 	if err == nil {
-		// This will no longer be necessary when batchJob will support live status update, by then
-		// we should only sync the Status of the job if it's "Finished".
-		remoteFinished := false
-		for _, c := range remoteJob.Status.Conditions {
-			if (c.Type == batchv1.JobComplete || c.Type == batchv1.JobFailed) && c.Status == corev1.ConditionTrue {
-				remoteFinished = true
-				break
-			}
-		}
-
-		if remoteFinished {
+		if features.Enabled(features.MultiKueueBetchJobWithManageBy) {
 			localJob.Status = remoteJob.Status
 			return localClient.Status().Update(ctx, &localJob)
 		} else {
-			return nil
+			remoteFinished := false
+			for _, c := range remoteJob.Status.Conditions {
+				if (c.Type == batchv1.JobComplete || c.Type == batchv1.JobFailed) && c.Status == corev1.ConditionTrue {
+					remoteFinished = true
+					break
+				}
+			}
+
+			if remoteFinished {
+				localJob.Status = remoteJob.Status
+				return localClient.Status().Update(ctx, &localJob)
+			} else {
```

  done

- **test/integration/multikueue/multikueue_test.go:538** by [@trasc](https://github.com/trasc) — 2024-06-05T10:07:12Z
```diff
@@ -509,6 +512,113 @@ var _ = ginkgo.Describe("Multikueue", func() {
 					LastTransitionTime: metav1.Now(),
 					Message:            "Job finished successfully",
 				})
+				createdJob.Status.StartTime = ptr.To(metav1.Now())
+				createdJob.Status.CompletionTime = ptr.To(metav1.Now())
+				g.Expect(worker1TestCluster.client.Status().Update(worker1TestCluster.ctx, &createdJob)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(managerTestCluster.client.Get(managerTestCluster.ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+
+				g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadFinished)).To(gomega.BeComparableTo(&metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  kueue.WorkloadFinishedReasonSucceeded,
+					Message: "Job finished successfully",
+				}, util.IgnoreConditionTimestampsAndObservedGeneration))
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				createdWorkload := &kueue.Workload{}
+				g.Expect(worker1TestCluster.client.Get(worker1TestCluster.ctx, wlLookupKey, createdWorkload)).To(utiltesting.BeNotFoundError())
+			}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.It("Should run a job on worker if admitted (ManagedBy)", func() {
```

  yes we can use the managedBy implementation only with k8s 1.30+
  
  "Should run a job on worker if admitted" it's the same thing for the non managedBy implementation

- **test/integration/multikueue/multikueue_test.go:766** by [@trasc](https://github.com/trasc) — 2024-06-05T10:12:59Z
```diff
@@ -651,7 +761,11 @@ var _ = ginkgo.Describe("Multikueue", func() {
 	})
 
 	ginkgo.It("Should remove the worker's workload and job when managers job is deleted", func() {
+		if managerK8sVersion.LessThan(versionutil.MustParseSemantic("1.30.0")) {
+			ginkgo.Skip("the managers kubernetes version is less then 1.30")
+		}
```

  we could.. , done

- **pkg/controller/admissionchecks/multikueue/workload.go:201** by [@trasc](https://github.com/trasc) — 2024-06-05T10:34:14Z
```diff
@@ -198,11 +198,11 @@ func (w *wlReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 	}
 
 	managed, unmanagedReason, err := adapter.IsJobManagedByKueue(ctx, w.client, types.NamespacedName{Name: owner.Name, Namespace: wl.Namespace})
-	if err != nil {
+	if err != nil && !isDeleted {
```

  rebased

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-05T10:53:29Z

LGTM, one naming nit from me.

I think we should also have  defaulting of managedBy field for Jobs, as we do for JobSet. when the feature gate is enabled.

@trasc  would you like to extend this PR or follow up?

- **pkg/features/kube_features.go:114** by [@mimowo](https://github.com/mimowo) — 2024-06-05T10:50:34Z
```diff
@@ -97,14 +103,15 @@ func init() {
 // Entries are separated from each other with blank lines to avoid sweeping gofmt changes
 // when adding or removing one entry.
 var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
-	PartialAdmission:            {Default: true, PreRelease: featuregate.Beta},
-	QueueVisibility:             {Default: false, PreRelease: featuregate.Alpha},
-	FlavorFungibility:           {Default: true, PreRelease: featuregate.Beta},
-	ProvisioningACC:             {Default: true, PreRelease: featuregate.Beta},
-	VisibilityOnDemand:          {Default: false, PreRelease: featuregate.Alpha},
-	PrioritySortingWithinCohort: {Default: true, PreRelease: featuregate.Beta},
-	MultiKueue:                  {Default: false, PreRelease: featuregate.Alpha},
-	LendingLimit:                {Default: false, PreRelease: featuregate.Alpha},
+	PartialAdmission:               {Default: true, PreRelease: featuregate.Beta},
+	QueueVisibility:                {Default: false, PreRelease: featuregate.Alpha},
+	FlavorFungibility:              {Default: true, PreRelease: featuregate.Beta},
+	ProvisioningACC:                {Default: true, PreRelease: featuregate.Beta},
+	VisibilityOnDemand:             {Default: false, PreRelease: featuregate.Alpha},
+	PrioritySortingWithinCohort:    {Default: true, PreRelease: featuregate.Beta},
+	MultiKueue:                     {Default: false, PreRelease: featuregate.Alpha},
+	LendingLimit:                   {Default: false, PreRelease: featuregate.Alpha},
+	MultiKueueBatchJobWithManageBy: {Default: false, PreRelease: featuregate.Alpha},
```

  ```suggestion
  	MultiKueueBatchJobWithManagedBy: {Default: false, PreRelease: featuregate.Alpha},
  ```
  to match the field name. Alternative name I'm thinking would be ok: `MultiKueueManagerWithJobManagedBy`, or `MultiKueueUseManagedByForBatchJob`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-05T11:33:54Z

- **pkg/features/kube_features.go:114** by [@trasc](https://github.com/trasc) — 2024-06-05T11:33:54Z
```diff
@@ -97,14 +103,15 @@ func init() {
 // Entries are separated from each other with blank lines to avoid sweeping gofmt changes
 // when adding or removing one entry.
 var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
-	PartialAdmission:            {Default: true, PreRelease: featuregate.Beta},
-	QueueVisibility:             {Default: false, PreRelease: featuregate.Alpha},
-	FlavorFungibility:           {Default: true, PreRelease: featuregate.Beta},
-	ProvisioningACC:             {Default: true, PreRelease: featuregate.Beta},
-	VisibilityOnDemand:          {Default: false, PreRelease: featuregate.Alpha},
-	PrioritySortingWithinCohort: {Default: true, PreRelease: featuregate.Beta},
-	MultiKueue:                  {Default: false, PreRelease: featuregate.Alpha},
-	LendingLimit:                {Default: false, PreRelease: featuregate.Alpha},
+	PartialAdmission:               {Default: true, PreRelease: featuregate.Beta},
+	QueueVisibility:                {Default: false, PreRelease: featuregate.Alpha},
+	FlavorFungibility:              {Default: true, PreRelease: featuregate.Beta},
+	ProvisioningACC:                {Default: true, PreRelease: featuregate.Beta},
+	VisibilityOnDemand:             {Default: false, PreRelease: featuregate.Alpha},
+	PrioritySortingWithinCohort:    {Default: true, PreRelease: featuregate.Beta},
+	MultiKueue:                     {Default: false, PreRelease: featuregate.Alpha},
+	LendingLimit:                   {Default: false, PreRelease: featuregate.Alpha},
+	MultiKueueBatchJobWithManageBy: {Default: false, PreRelease: featuregate.Alpha},
```

  done , `MultiKueueBatchJobWithManageBy`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2024-06-05T11:53:44Z

- **pkg/features/kube_features.go:114** by [@mimowo](https://github.com/mimowo) — 2024-06-05T11:53:44Z
```diff
@@ -97,14 +103,15 @@ func init() {
 // Entries are separated from each other with blank lines to avoid sweeping gofmt changes
 // when adding or removing one entry.
 var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
-	PartialAdmission:            {Default: true, PreRelease: featuregate.Beta},
-	QueueVisibility:             {Default: false, PreRelease: featuregate.Alpha},
-	FlavorFungibility:           {Default: true, PreRelease: featuregate.Beta},
-	ProvisioningACC:             {Default: true, PreRelease: featuregate.Beta},
-	VisibilityOnDemand:          {Default: false, PreRelease: featuregate.Alpha},
-	PrioritySortingWithinCohort: {Default: true, PreRelease: featuregate.Beta},
-	MultiKueue:                  {Default: false, PreRelease: featuregate.Alpha},
-	LendingLimit:                {Default: false, PreRelease: featuregate.Alpha},
+	PartialAdmission:               {Default: true, PreRelease: featuregate.Beta},
+	QueueVisibility:                {Default: false, PreRelease: featuregate.Alpha},
+	FlavorFungibility:              {Default: true, PreRelease: featuregate.Beta},
+	ProvisioningACC:                {Default: true, PreRelease: featuregate.Beta},
+	VisibilityOnDemand:             {Default: false, PreRelease: featuregate.Alpha},
+	PrioritySortingWithinCohort:    {Default: true, PreRelease: featuregate.Beta},
+	MultiKueue:                     {Default: false, PreRelease: featuregate.Alpha},
+	LendingLimit:                   {Default: false, PreRelease: featuregate.Alpha},
+	MultiKueueBatchJobWithManagadBy: {Default: false, PreRelease: featuregate.Alpha},
```

  ```suggestion
  	MultiKueueBatchJobWithManagedBy: {Default: false, PreRelease: featuregate.Alpha},
  ```
  almost there :)

### Review by [@trasc](https://github.com/trasc) (commented) — 2024-06-05T12:48:13Z

- **pkg/features/kube_features.go:114** by [@trasc](https://github.com/trasc) — 2024-06-05T12:48:13Z
```diff
@@ -97,14 +103,15 @@ func init() {
 // Entries are separated from each other with blank lines to avoid sweeping gofmt changes
 // when adding or removing one entry.
 var defaultFeatureGates = map[featuregate.Feature]featuregate.FeatureSpec{
-	PartialAdmission:            {Default: true, PreRelease: featuregate.Beta},
-	QueueVisibility:             {Default: false, PreRelease: featuregate.Alpha},
-	FlavorFungibility:           {Default: true, PreRelease: featuregate.Beta},
-	ProvisioningACC:             {Default: true, PreRelease: featuregate.Beta},
-	VisibilityOnDemand:          {Default: false, PreRelease: featuregate.Alpha},
-	PrioritySortingWithinCohort: {Default: true, PreRelease: featuregate.Beta},
-	MultiKueue:                  {Default: false, PreRelease: featuregate.Alpha},
-	LendingLimit:                {Default: false, PreRelease: featuregate.Alpha},
+	PartialAdmission:               {Default: true, PreRelease: featuregate.Beta},
+	QueueVisibility:                {Default: false, PreRelease: featuregate.Alpha},
+	FlavorFungibility:              {Default: true, PreRelease: featuregate.Beta},
+	ProvisioningACC:                {Default: true, PreRelease: featuregate.Beta},
+	VisibilityOnDemand:             {Default: false, PreRelease: featuregate.Alpha},
+	PrioritySortingWithinCohort:    {Default: true, PreRelease: featuregate.Beta},
+	MultiKueue:                     {Default: false, PreRelease: featuregate.Alpha},
+	LendingLimit:                   {Default: false, PreRelease: featuregate.Alpha},
+	MultiKueueBatchJobWithManagadBy: {Default: false, PreRelease: featuregate.Alpha},
```

  done
